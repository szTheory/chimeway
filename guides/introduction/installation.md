# Installation

> **Note:** This guide is a stub. Full content coming in v1.0 docs.

Add Chimeway to your Elixir project by declaring the dependency in `mix.exs`, running `mix deps.get`, and running the provided Ecto migrations to set up the durable notification schema.

## Overview

<!-- TODO: expand with full content -->
