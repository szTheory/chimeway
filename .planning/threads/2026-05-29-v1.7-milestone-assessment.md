# Thread: v1.7 milestone boundary assessment

**Opened:** 2026-05-29  
**Status:** open — handoff to `/gsd-new-milestone` for v1.7 READ  
**Trigger:** Milestone next-step assessment at post-v1.6 boundary

## Executive verdict

**Done band: ~88–92%** (80–89% strong, upper edge) — engine, adoption surface (v1.5), and adoption evidence (v1.6) are credible. Remaining delta is **FOUNDATIONAL-ADJACENT** (READ glue) plus **IMPORTANT-BUT-NARROW** demo polish.

**Next milestone (single pick): v1.7 READ** — not a re-run of Consumer Journey Proof, not SEED-003, not INBX.

## Adoption evidence clarification

v1.6 already shipped the realistic demo + seeds + journey CI the adopter-evidence prompt describes:

| Capability | Evidence |
|------------|----------|
| Realistic domain + personas | TeamPulse (Feature Dev, Support Op, Product Manager) |
| Rich seeds | `DemoHost.Seeds` — idempotent, adopter-copyable |
| One-command spin-up | `mix demo.up --serve`, `mix demo.admin` |
| Automated journeys | JOUR-01..05 (`mix verify.journeys`) |
| Click-around UI | `/admin/chimeway` trace search/detail |

**Decision:** v1.6 satisfied adoption-evidence foundation; do not re-milestone.

## Residual gaps (narrow polish — v1.7 tail)

| Gap | Notes |
|-----|-------|
| Admin journey invite-only | JOUR-04 covers Alex; Sam suppression + Morgan escalation not in admin UI journeys |
| Staged escalation demo | JOUR-03 uses `stage_escalation_webhook/1` + `PendingWebhookAdapter` — masks READ engine gap |
| README webhook contradiction | § "Not this path: webhook progression" vs TeamPulse table + JOUR-03 |
| `mix demo.up --check` moduledoc | Says "seed only"; also runs migrate + app.start |
| Installer → fresh host → demo | Not one CI story |
| TraceDemo vs TeamPulse split | Two parallel demo narratives in README |

## Engine gap (v1.7 READ scope)

| ID | Requirement |
|----|-------------|
| READ-01 | Populate `pending_signals` on `wait_until` workflow transitions |
| READ-02 | Inbox `mark_read` / `mark_seen` emits durable signals routing workflow progression |

PM JTBD (SEED-004): "If they don't open the email in 2 hours, send push" — workflows + feedback exist; read→signal glue deferred.

## Top wedges ranked

1. **v1.7 READ** — pending_signals + inbox read→signal (highest leverage)
2. Adoption polish tail — README, admin journeys for all personas, natural escalation demo
3. **v1.8 SEED-003** — ecosystem integration blueprints
4. **v1.9 INBX** — bell inbox UI
5. Greenfield `phx.new` install smoke

## Diminishing returns (do not pursue now)

- Full TeamPulse SaaS shell (signup, teams, billing UI)
- Playwright until ConnTest proves flaky (INV-004)
- Broad channel matrix before READ
- Retroactive GSD phase dirs 43–47 (audit trail only)

## Graduation candidates

- "Adoption surface (v1.5) ≠ adoption evidence (v1.6)" — permanent pattern (see RETROSPECTIVE)
- "Consumer journey proof before READ" — validated at v1.6 close
- "Staged seed choreography masks engine gaps" — address in v1.7 READ + demo polish

## Maintainer takeaway

Kick off v1.7 READ. Fold demo polish into v1.7 close-out. Then v1.8 ecosystem plugins. Closer to "done for scope" than it feels — gap is one engine glue wedge, not a missing demo app.
