---
quick_id: 260912-x9f
phase: 260912-x9f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/trigger.ex
  - test/chimeway/trigger_inbox_change_test.exs
  - examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs
autonomous: true
must_haves:
  truths:
    - "A notifier that returns a rendering error causes Chimeway.trigger/3 to return the stable notifications_insert_failed tuple instead of raising."
    - "The failed notification build rolls back its event and notifications and emits no inbox creation hint."
    - "A connected, authorized demo-host bell mounted before a public Chimeway.trigger/3 call refreshes from zero to one unread item without polling, direct inserts, or manual publication."
    - "The correction preserves the closed publisher payload, post-commit boundary, idempotency, tenant/opaque-recipient scoping, and first-transition lifecycle behavior required by D-01 through D-12."
  artifacts:
    - path: "lib/chimeway/trigger.ex"
      provides: "Tagged notification-attribute errors halt the reducer and propagate through Ecto.Multi."
      contains: "{:halt, error}"
    - path: "test/chimeway/trigger_inbox_change_test.exs"
      provides: "Rendering-failure return-shape, rollback, and no-publication regression contract."
      contains: "RenderingFailureNotifier"
    - path: "examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs"
      provides: "Mounted public Trigger-to-bell arrival and badge refresh proof."
      contains: "Chimeway.trigger"
  key_links:
    - from: "lib/chimeway/trigger.ex:notifications_attrs/6"
      to: "lib/chimeway/trigger.ex:insert_notifications/6"
      via: "Enum.reduce_while/3 halts with the already-tagged rendering error so Ecto.Multi.run/3 can roll back."
      pattern: "\\{:error, _reason\\} = error -> \\{:halt, error\\}"
    - from: "lib/chimeway/trigger.ex:normalize_trigger_result/3"
      to: "Chimeway.trigger/3 public result"
      via: "The :notifications transaction failure is wrapped exactly once as notifications_insert_failed."
      pattern: "notifications_insert_failed"
    - from: "Chimeway.trigger/3"
      to: "examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs"
      via: "Successful commit invokes the configured publisher, whose closed PubSub reload message reaches the already-connected bell."
      pattern: "Notifications, 1 unread"
---

<objective>
Restore stable Trigger error propagation for notifier rendering failures and lock the complete public Trigger-to-mounted-bell refresh path with executable evidence.

Purpose: Close the runtime exception and the last missing arrival proof without changing the publisher protocol, Phoenix dependency boundary, lifecycle semantics, or bell presentation (D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12).
Output: One reducer correction, one core rollback/no-publish regression, and one mounted demo-host refresh test.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/quick/260912-x9f-fix-trigger-notifier-rendering-error-propagation-add-the-mis/260912-x9f-RESEARCH.md
@lib/chimeway/trigger.ex
@lib/chimeway/rendering.ex
@test/chimeway/trigger_inbox_change_test.exs
@examples/chimeway_demo_host/config/config.exs
@examples/chimeway_demo_host/lib/demo_host/notifiers/invite_sent.ex
@examples/chimeway_demo_host/lib/demo_host/seeds.ex
@examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Propagate rendering failures through the transaction boundary</name>
  <files>lib/chimeway/trigger.ex, test/chimeway/trigger_inbox_change_test.exs</files>
  <behavior>
    - RED: A notifier whose rendering/2 returns `{:error, :forced_rendering_failure}` must make `Trigger.trigger/3` return `{:error, {:notifications_insert_failed, {:rendering_resolution_failed, :forced_rendering_failure}}}`; the unmodified reducer raises instead.
    - GREEN: The same call returns that tuple without an exception, and the event selected by tenant plus idempotency key is absent.
    - GREEN: No notification for the synthetic tenant/opaque recipient exists and `RecordingPublisher` receives no change message.
    - REGRESSION: Successful creation still publishes after commit, duplicate creation stays silent, and publisher failures cannot falsify a committed Trigger result (D-03, D-04, D-05).
  </behavior>
  <action>
Add `RenderingFailureNotifier` beside the existing synthetic notifier modules. Implement the normal notifier callbacks with an opaque `cw_` recipient and make `rendering/2` return the forced tagged error. Add a focused test under the existing `RecordingPublisher` setup that calls `Trigger.trigger/3` with a unique tenant/idempotency pair, asserts the exact public error tuple, queries `Event` by that pair, queries `Notification` by tenant and opaque recipient, and refutes a publisher message. Run the focused test before production code and retain its failing output as the RED evidence.

Then update only the `Enum.reduce_while/3` callback in `notifications_attrs/6`: add an `else` arm that matches the already-tagged `{:error, _reason}` value and returns `{:halt, error}`. Preserve the inner error unchanged so the existing terminal case, `Ecto.Multi.run/3`, and `normalize_trigger_result/3` wrap it exactly once. Keep `ChangePublisher.publish/3` in the successful post-transaction normalization path and introduce no Phoenix/PubSub dependency in core (D-01, D-02, D-03). This bounded correction must not add exception rescue semantics or alter duplicate/lifecycle publication behavior (D-04, D-05, D-11).
  </action>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/trigger_inbox_change_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The pre-change focused test demonstrably fails at the reducer contract; after the correction it passes with the exact error shape, zero durable rows for the failed trigger, zero publication, and all existing creation/duplicate/publisher contracts green.</done>
</task>

<task type="auto">
  <name>Task 2: Prove mounted public Trigger-to-bell refresh</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs</files>
  <action>
Add one `@moduletag :inbox` test to the existing non-async demo proof. Create unique synthetic team and idempotency strings, build the Alex session, and mount `/inbox` before triggering. Assert the initial render has `Notifications, 0 unread` and omits the unique team text. Call public `Chimeway.trigger/3` with `DemoHost.Notifiers.InviteSent`, Alex's synthetic demo email, the unique team name, the unique idempotency key, and `tenant_id: DemoHost.Seeds.tenant_id()`. Assert one notification was inserted, call `render(view)` to process the closed reload message, assert the badge is `Notifications, 1 unread`, open the existing bell control, and assert the unique invitation text is present.

Use only the configured `ChimewayInbox.PubSubPublisher` bridge and the notifier's existing opaque recipient derivation. Do not call publisher, topic, broadcast, or direct-insert helpers from the new test, and do not expose or log topic inputs (D-06, D-07, D-08, D-09, D-12). Leave markup, copy, pagination, row-read behavior, archive semantics, and authorization code unchanged (D-10, D-11). Treat this as a missing integration-contract addition: production changes beyond Task 1 are not justified if the mounted test is already green.
  </action>
  <verify>
    <automated>cd examples/chimeway_demo_host &amp;&amp; mix test test/demo_host_web/inbox_bell_proof_test.exs --only inbox --warnings-as-errors</automated>
  </verify>
  <done>A bell connected before notification creation observes the real post-commit publisher path, refreshes its authoritative first page and unread badge, and renders the new invitation without polling, cross-scope data, manual publication, or a direct database insert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host notifier callback → Trigger reducer | Host code returns a rendering declaration or tagged error into core transaction orchestration. |
| Ecto transaction → inbox publisher | Publication is allowed only after durable commit and must remain absent on rollback or duplicate creation. |
| Configured PubSub stream → mounted bell | A closed reload signal may cause a tenant-scoped query only for the currently authorized opaque recipient. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-x9f-01 | Denial of Service | `Trigger.notifications_attrs/6` | medium | mitigate | Convert a returned tagged rendering error into the required reducer halt tuple and prove the public API returns rather than raising. |
| T-x9f-02 | Tampering | Transaction-to-publisher boundary | medium | mitigate | Assert both durable rollback and no `RecordingPublisher` message for the same failed trigger; keep publication reachable only after successful normalization. |
| T-x9f-03 | Information Disclosure | PubSub topic and bell refresh path | high | mitigate | Exercise the public Trigger path using the existing opaque-recipient derivation and closed reload message; forbid raw topic inspection/manual broadcast and run the existing tenant/recipient isolation gate. |
| T-x9f-04 | Repudiation | Public Trigger error result | medium | mitigate | Assert the exact stable nested error classification so hosts can distinguish notification-build failure without exposing notifier data. |

ASVS level 1 policy is satisfied: every medium threat on a primary trust boundary and the high disclosure threat has a concrete test-backed mitigation; no critical or high threat is accepted.
</threat_model>

<verification>
Run the RED core regression before editing `lib/chimeway/trigger.ex`, then rerun it GREEN. After the mounted test passes, run the complete named gate:

1. `scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/trigger_inbox_change_test.exs --warnings-as-errors`
2. `cd examples/chimeway_demo_host && mix test test/demo_host_web/inbox_bell_proof_test.exs --only inbox --warnings-as-errors`
3. `mix verify.inbox`

The final aggregate must pass without changing gate membership or suppressing warnings.
</verification>

<source_audit>

| Source | ID | Feature / Requirement | Task | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | 260912-x9f | Fix Trigger rendering-error propagation, add rollback/no-publish coverage, and add one mounted arrival refresh proof | 1, 2 | COVERED | Core correction plus the missing end-to-end test. |
| REQ | INBX-03 | Closed publisher emits only after durable creation/first lifecycle transition without core Phoenix dependency | 1 | COVERED | Rollback and no-publication are explicit; successful existing contracts rerun. |
| REQ | INBX-04 | Authorized connected bell refreshes on relevant arrival without polling or cross-scope disclosure | 2 | COVERED | Public Trigger is invoked only after mount; badge and item update are asserted. |
| RESEARCH | root cause | `Enum.reduce_while/3` receives a bare tagged error from notifier rendering | 1 | COVERED | Exact halt-with-existing-error correction. |
| RESEARCH | core contract | Returned error, event/notification rollback, and no publication | 1 | COVERED | One focused DB integration regression. |
| RESEARCH | mounted contract | Trigger → configured publisher → HMAC topic → LiveView reload | 2 | COVERED | No direct insertion or manual publication. |
| CONTEXT | D-01, D-02 | Closed reload hint and dependency-free core publisher seam | 1, 2 | COVERED | Publisher shape and dependency boundaries remain unchanged. |
| CONTEXT | D-03, D-04, D-05 | Post-commit/first-transition ordering, idempotency, and publisher-failure independence | 1 | COVERED | Failed build is silent and existing focused contracts rerun. |
| CONTEXT | D-06, D-07 | Optional Phoenix adapter and HMAC-derived closed stream | 2 | COVERED | Mounted proof uses configured adapter without inspecting topics. |
| CONTEXT | D-08, D-09 | Connected authorized subscription and authorization-aware reload | 2 | COVERED | Routed connected mount precedes Trigger and existing authorization tests remain in the aggregate. |
| CONTEXT | D-10, D-11 | Preserve bell presentation/read behavior and archive first-transition semantics | 2 | COVERED | Test adds proof only; aggregate guards unchanged lifecycle behavior. |
| CONTEXT | D-12 | Synthetic opaque fixtures and tenant/recipient non-delivery proof | 1, 2 | COVERED | New fixtures are synthetic/opaque; existing isolation contracts rerun in `mix verify.inbox`. |

Deferred Phase 106/107 work and non-Phoenix transport/cursor/delta ideas are excluded from this quick item.
</source_audit>

<success_criteria>
- Returned notifier rendering errors produce the exact stable `notifications_insert_failed` result and never raise from the reducer.
- The failed Trigger leaves no event, notification, or inbox-change publication behind.
- A connected demo bell refreshes from zero to one unread after a real public Trigger call and renders the new item.
- Focused root, focused demo-host, and aggregate `mix verify.inbox` commands all pass with warnings treated as errors.
- No core dependency, publisher payload, PubSub topic, authorization rule, lifecycle semantic, or UI presentation is broadened.
</success_criteria>

<output>
Create `.planning/quick/260912-x9f-fix-trigger-notifier-rendering-error-propagation-add-the-mis/260912-x9f-SUMMARY.md` when done.
</output>
