---
phase: 58-accrue-dunning-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - mix.lock
  - config/test.exs
  - test/test_helper.exs
  - test/support/accrue/data_case.ex
  - test/support/accrue/fixtures.ex
  - test/chimeway/integrations/accrue_dunning_harness_test.exs
autonomous: true
requirements: [ECOS-06]
must_haves:
  truths:
    - "Chimeway compiles with and without optional `{:accrue, \"~> 1.2\", optional: true}` dep (D-11)"
    - "`mix ci.test` excludes `@moduletag :accrue` tests; `mix verify.accrue` runs only accrue-tagged tests (D-11)"
    - "When Accrue dep is present, test harness bootstraps `Accrue.TestRepo`, configures `Config.dunning_engine/0` → `Accrue.Integrations.Chimeway`, and proves engine module loads (D-12, D-13)"
    - "Harness tests skip cleanly when `Code.ensure_loaded?(Accrue)` is false — no compile-time coupling (D-02)"
  artifacts:
    - path: "mix.exs"
      provides: "optional accrue dep, ci.test exclude, verify.accrue alias"
      contains: "verify.accrue"
    - path: "test/support/accrue/data_case.ex"
      provides: "Shared SQL sandbox for Accrue.TestRepo + Chimeway.Repo"
      contains: "Accrue.DataCase"
    - path: "test/support/accrue/fixtures.ex"
      provides: "Customer/subscription/invoice inserts + dunning engine config helper"
      contains: "configure_chimeway_dunning_engine"
    - path: "test/chimeway/integrations/accrue_dunning_harness_test.exs"
      provides: "Wave 58-01 event subscription stub + config round-trip"
      contains: "@moduletag :accrue"
  key_links:
    - from: "mix.exs"
      to: "test/chimeway/integrations/accrue_dunning_harness_test.exs"
      via: "verify.accrue alias runs --only accrue"
      pattern: "verify\\.accrue"
    - from: "test/test_helper.exs"
      to: "Accrue.TestRepo"
      via: "conditional bootstrap when Code.ensure_loaded?(Accrue)"
      pattern: "Accrue\\.TestRepo"
    - from: "test/support/accrue/fixtures.ex"
      to: "Accrue.Integrations.Chimeway"
      via: "Application.put_env(:accrue, :dunning, engine: ...)"
      pattern: "Accrue\\.Integrations\\.Chimeway"
---

<objective>
Establish the Accrue selective-CI lane and integration test harness in the Chimeway repo: optional `accrue` dependency, `@moduletag :accrue` exclusion from default CI, `mix verify.accrue` alias, conditional test bootstrap, and a harness test proving Accrue dunning engine config round-trips without host glue.

Purpose: Wave 58-02/03 integration tests depend on a Mailglass-precedent harness (D-11, D-12). This wave delivers infrastructure only — no `DunningNotifier.workflow/2` or full E2E lifecycle yet.

Output: Updated `mix.exs`, `config/test.exs`, `test/test_helper.exs`, `test/support/accrue/*`, and `test/chimeway/integrations/accrue_dunning_harness_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-accrue-dunning-core/58-CONTEXT.md
@.planning/phases/58-accrue-dunning-core/58-RESEARCH.md
@.planning/phases/58-accrue-dunning-core/58-VALIDATION.md
@AGENTS.md

@mix.exs
@config/test.exs
@test/test_helper.exs
@test/support/mailglass/data_case.ex
@test/chimeway/adapters/mailglass_adapter_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optional accrue dep + selective CI aliases (D-11)</name>
  <files>mix.exs, mix.lock</files>
  <read_first>
    - `mix.exs` — `deps/0` Mailglass optional dep pattern (line ~41); `ci.test` exclude (line ~60); `verify.mailglass` alias (lines ~97–100)
    - `.planning/phases/58-accrue-dunning-core/58-CONTEXT.md` — D-11, D-02
    - `.planning/phases/58-accrue-dunning-core/58-RESEARCH.md` — §8.1 Mailglass template table
  </read_first>
  <action>
    In `mix.exs` `deps/0`, add after the Mailglass optional dep:

    ```elixir
    {:accrue, "~> 1.2", optional: true},
    ```

    For local cross-repo dev, allow path override via `ACCRUE_PATH` env (document in comment only — do not commit path dep):

    ```elixir
    # Local dev: ACCRUE_PATH=../accrue/accrue mix deps.get
    {:accrue, accrue_dep(), optional: true},
    ```

    Add private helper in `project/0` module section:

    ```elixir
    defp accrue_dep do
      case System.get_env("ACCRUE_PATH") do
        nil -> "~> 1.2"
        path -> {:path, path}
      end
    end
    ```

    Update `aliases/0`:

    1. Change `"ci.test"` from `["cmd env MIX_ENV=test mix test --exclude mailglass"]` to `["cmd env MIX_ENV=test mix test --exclude mailglass --exclude accrue"]`.
    2. Add `"verify.accrue"` alias (Phase 58 root tests only — no demo host yet per D-13):

    ```elixir
    "verify.accrue": [
      "cmd env MIX_ENV=test mix test --only accrue --warnings-as-errors"
    ],
    ```

    Run `mix deps.get` to update `mix.lock`. Do **not** add GATE-05 CI job wiring (Phase 60).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` contains `{:accrue,` with `optional: true`.
    - `mix.exs` `"ci.test"` alias includes `--exclude accrue`.
    - `mix.exs` defines `"verify.accrue"` alias running `--only accrue --warnings-as-errors`.
    - `mix compile --warnings-as-errors` exits 0 with Accrue dep absent OR present.
    - No changes to `lib/chimeway/*` core modules.
  </acceptance_criteria>
  <done>Optional accrue dep and selective CI aliases mirror Mailglass pattern (D-11).</done>
</task>

<task type="auto">
  <name>Task 2: Accrue test config + test_helper bootstrap (D-11, D-12)</name>
  <files>config/test.exs, test/test_helper.exs</files>
  <read_first>
    - `config/test.exs` — Mailglass conditional block (lines ~29–46)
    - `test/test_helper.exs` — Mailglass.TestRepo bootstrap (lines ~4–33)
    - `../accrue/accrue/config/test.exs` — Accrue TestRepo database config
    - `.planning/phases/58-accrue-dunning-core/58-RESEARCH.md` — §8.2 setup checklist items 1–4
  </read_first>
  <action>
    In `config/test.exs`, add a conditional block gated on `Code.ensure_loaded?(Accrue)` (mirror Mailglass block structure):

    ```elixir
    if Code.ensure_loaded?(Accrue) do
      config :accrue, repo: Accrue.TestRepo
      config :accrue, processor: Accrue.Processor.Fake
      config :accrue, dunning,
        engine: Accrue.Integrations.Chimeway,
        campaign: [enabled: true]

      config :accrue, Accrue.TestRepo,
        username: "postgres",
        password: "postgres",
        hostname: "localhost",
        database: "chimeway_accrue_test#{System.get_env("MIX_TEST_PARTITION")}",
        pool: Ecto.Adapters.SQL.Sandbox
    end
    ```

    In `test/test_helper.exs`, after the Mailglass block, add conditional Accrue bootstrap:

    1. `Application.ensure_all_started(:accrue)` when loaded.
    2. Resolve migrations path — prefer `Path.join([__DIR__, "support", "accrue", "migrations"])` if copied/symlinked; otherwise use Accrue package migrations via `:code.priv_dir(:accrue)` or documented path from sibling repo.
    3. `Ecto.Adapters.Postgres.storage_up/1` for `Accrue.TestRepo` config.
    4. Run migrations with `Ecto.Migrator.with_repo/2`.
    5. Start `Accrue.TestRepo` and set `Ecto.Adapters.SQL.Sandbox.mode(Accrue.TestRepo, :manual)`.
    6. Call `Accrue.Test.setup_fake_processor/0` if that function exists in loaded Accrue version.

    Ensure Chimeway Oban test config remains `testing: :manual` (existing) so signal worker tests can drain in Wave 58-03.

    Do **not** modify Accrue repo files in this task.
  </action>
  <verify>
    <automated>mix test --exclude accrue --exclude mailglass --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `config/test.exs` contains `if Code.ensure_loaded?(Accrue)` block configuring `:dunning` engine to `Accrue.Integrations.Chimeway`.
    - `test/test_helper.exs` conditionally starts `Accrue.TestRepo` when Accrue is loaded.
    - `mix test --exclude accrue --exclude mailglass --warnings-as-errors` exits 0 (default CI lane unaffected).
    - When Accrue dep is fetched, `mix test test/chimeway/integrations/accrue_dunning_harness_test.exs --only accrue` does not crash on missing TestRepo.
  </acceptance_criteria>
  <done>Accrue TestRepo and dunning engine config bootstrap when optional dep is present.</done>
</task>

<task type="auto">
  <name>Task 3: Accrue DataCase, fixtures, and harness stub test (D-12, D-13)</name>
  <files>test/support/accrue/data_case.ex, test/support/accrue/fixtures.ex, test/chimeway/integrations/accrue_dunning_harness_test.exs</files>
  <read_first>
    - `test/support/mailglass/data_case.ex` — ExUnit.CaseTemplate + Sandbox owner pattern
    - `../accrue/accrue/lib/accrue/integrations/chimeway.ex` — `start_campaign/3`, `Config.dunning_engine/0` runtime resolution
    - `../accrue/accrue/lib/accrue/test.ex` — `Accrue.Test.trigger_event/2` delegate
    - `.planning/phases/58-accrue-dunning-core/58-RESEARCH.md` — §8.3 Wave 58-01 minimum harness proof
    - `.planning/phases/58-accrue-dunning-core/58-VALIDATION.md` — tasks 58-01-02, 58-01-03
  </read_first>
  <action>
    Create `test/support/accrue/data_case.ex` (conditional compile — only when `Code.ensure_loaded?(Accrue)`):

    - `use ExUnit.CaseTemplate` with `using` block importing Ecto helpers.
    - `setup` starts shared SQL sandbox owners for **both** `Accrue.TestRepo` and `Chimeway.Repo` (`shared: true` when not async) — required for webhook + Chimeway cross-repo tests in Wave 58-02.
    - Export `allow_repo_access/1` helper to share sandbox between processes (follow accrue `dunning_campaign_keying_test` pattern if present in sibling repo).

    Create `test/support/accrue/fixtures.ex` (conditional compile):

    - `configure_chimeway_dunning_engine!/0` — sets `Application.put_env(:accrue, :dunning, engine: Accrue.Integrations.Chimeway, campaign: [enabled: true])`.
    - `insert_customer!/1` — minimal `Accrue.Billing.Customer` with email.
    - `insert_subscription!/2` — subscription linked to customer.
    - `insert_failed_invoice!/2` — invoice in failed-payment state suitable for `Accrue.Test.trigger_event(:invoice_payment_failed, invoice)`.
    - `configure_chimeway_logger_adapter!/0` — set `Application.put_env(:chimeway, :channel_adapter_configs, email: {Chimeway.Adapters.Logger, []})` for test delivery.

    Create `test/chimeway/integrations/accrue_dunning_harness_test.exs` wrapped in:

    ```elixir
    if Code.ensure_loaded?(Accrue) and Code.ensure_loaded?(Accrue.Integrations.Chimeway) do
      defmodule Chimeway.Integrations.AccrueDunningHarnessTest do
        use Accrue.DataCase, async: false
        @moduletag :accrue
        ...
      end
    end
    ```

    Minimum describe `"accrue dunning harness (ECOS-06 wave 1)"` with tests:

    1. **Engine config round-trip** — call `configure_chimeway_dunning_engine!/0`; assert `Accrue.Config.dunning_engine()` returns `Accrue.Integrations.Chimeway` (or equivalent `Config.dunning_engine/0` from Accrue).
    2. **Chimeway adapter module loaded** — assert `function_exported?(Accrue.Integrations.Chimeway, :start_campaign, 3)`.
    3. **Event subscription stub** — insert customer + subscription + failed invoice fixtures; call `Accrue.Test.trigger_event(:invoice_payment_failed, invoice)` and assert `{:ok, _}` (proves Accrue Fake processor + DefaultHandler path reachable — WorkflowRun assertion deferred to 58-02).
    4. **Direct start_campaign smoke** (optional supplement) — call `Accrue.Integrations.Chimeway.start_campaign(sub, anchor, [])` with UTC anchor; assert no crash. WorkflowRun row assertion optional in 58-01 if `workflow/2` not yet shipped in Accrue — document skip with `@tag :skip` only if Accrue sibling lacks `workflow/2`; prefer path dep for local dev.

    Do **not** implement full lifecycle or termination tests — those belong to 58-02/58-03.
  </action>
  <verify>
    <automated>mix test test/chimeway/integrations/accrue_dunning_harness_test.exs --only accrue --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/support/accrue/data_case.ex` exists with shared sandbox for Accrue + Chimeway repos.
    - `test/support/accrue/fixtures.ex` exports `configure_chimeway_dunning_engine!/0` and invoice/subscription helpers.
    - `test/chimeway/integrations/accrue_dunning_harness_test.exs` uses `@moduletag :accrue`.
    - Harness test proves `Accrue.Test.trigger_event(:invoice_payment_failed, invoice)` returns `{:ok, _}` without host callback glue.
    - `mix verify.accrue` exits 0 when Accrue dep is present.
    - `mix test --exclude accrue --warnings-as-errors` skips accrue tests cleanly.
  </acceptance_criteria>
  <done>Accrue integration harness and event subscription stub prove config + webhook path reachability (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chimeway repo → optional Accrue dep | Accrue is optional; core library must compile and test without it (D-02). |
| Test harness → Accrue TestRepo | Test fixtures insert billing rows into isolated test DB — not production data. |
| Config → dunning engine | Runtime `Application.put_env/3` in tests only; no compile-time secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | Denial of Service | optional dep breaks default CI | mitigate | `@moduletag :accrue` + `--exclude accrue` on `ci.test`; harness conditional compile via `Code.ensure_loaded?/1`. |
| T-58-02 | Tampering | test config leaks to runtime | mitigate | Accrue config block gated on `MIX_ENV=test` in `config/test.exs` only. |
| T-58-03 | Elevation of Privilege | cross-tenant test fixture bleed | mitigate | Shared sandbox with explicit tenant_id = customer_id in fixtures (Wave 58-02 asserts isolation). |
| T-58-04 | Information Disclosure | Accrue test DB credentials | accept | Local postgres test credentials mirror existing Mailglass/Chimeway test pattern. |

**No `high`-severity threats.** Infrastructure-only wave; no new runtime API surface. Block condition not triggered.
</threat_model>

<verification>
- [ ] `mix compile --warnings-as-errors` green
- [ ] `mix test --exclude accrue --exclude mailglass --warnings-as-errors` green (default CI lane)
- [ ] `mix verify.accrue` green when Accrue dep fetched
- [ ] `grep -n "verify.accrue" mix.exs` returns at least one match
- [ ] `grep -rn "@moduletag :accrue" test/` returns harness test file
- [ ] No edits to `lib/chimeway/signal.ex`, `lib/chimeway/workflows.ex`, or Accrue `chimeway.ex` in this plan
</verification>

<success_criteria>
- Optional `accrue` dep + selective CI lane operational (D-11)
- Test harness bootstraps Accrue TestRepo and dunning engine config (D-12)
- Harness proves `Accrue.Test.trigger_event/2` path reachable without host glue (D-13 stub)
- Default `mix ci.test` unaffected when Accrue dep absent
</success_criteria>

<output>
After completion, create `.planning/phases/58-accrue-dunning-core/58-01-SUMMARY.md`
</output>
