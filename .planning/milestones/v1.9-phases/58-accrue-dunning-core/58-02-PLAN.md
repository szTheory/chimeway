---
phase: 58-accrue-dunning-core
plan: 02
type: execute
wave: 2
depends_on: [58-01]
files_modified:
  - ../accrue/accrue/lib/accrue/integrations/chimeway.ex
  - ../accrue/accrue/test/accrue/integrations/chimeway_test.exs
  - test/chimeway/integrations/accrue_dunning_lifecycle_test.exs
  - test/support/accrue/fixtures.ex
autonomous: true
requirements: [ECOS-06]
must_haves:
  truths:
    - "`Accrue.Integrations.Chimeway.DunningNotifier` implements `workflow/2` with Email 1 → 48h `wait_until` → Email 2 escalation and `cancel_signals: [\"invoice.paid\"]` (D-06, SEED-003)"
    - "`DunningNotifier` implements `rendering/2` so `Chimeway.trigger/3` succeeds on the email workflow path (RESEARCH §6)"
    - "`Accrue.Test.trigger_event(:invoice_payment_failed, invoice)` creates a `WorkflowRun` with explainable trace — no direct `Chimeway.trigger/3` in primary ECOS-06 proof (D-04, D-05, D-12)"
    - "Duplicate `payment_failed` events are idempotent via `accrue.dunning:{subscription_id}:{anchor_iso}` key (D-05)"
    - "After first email delivery terminal + `Progression.progress_run/2`, run enters `:waiting` with `pending_signals == [\"invoice.paid\"]` (D-08, READ-01 spine)"
  artifacts:
    - path: "../accrue/accrue/lib/accrue/integrations/chimeway.ex"
      provides: "DunningNotifier.workflow/2, rendering/2; updated moduledoc"
      contains: "cancel_signals"
    - path: "test/chimeway/integrations/accrue_dunning_lifecycle_test.exs"
      provides: "Start-path integration tests (@moduletag :accrue)"
      contains: "invoice_payment_failed"
  key_links:
    - from: "../accrue/accrue/lib/accrue/integrations/chimeway.ex"
      to: "Chimeway.trigger/3"
      via: "start_campaign/3 → DunningNotifier"
      pattern: "Chimeway\\.trigger"
    - from: "test/chimeway/integrations/accrue_dunning_lifecycle_test.exs"
      to: "Accrue.Test.trigger_event/2"
      via: ":invoice_payment_failed webhook synthetic path"
      pattern: "trigger_event\\(:invoice_payment_failed"
    - from: "DunningNotifier.workflow/2"
      to: "Chimeway.Workflows.Progression.enter_waiting/6"
      via: "wait_until progress rule with cancel_signals"
      pattern: "invoice\\.paid"
---

<objective>
Upgrade Accrue's `DunningNotifier` from v1.40 email-only `:immediate` mode to a multi-step Chimeway workflow (48h escalation per SEED-003), and prove the start path: `invoice.payment_failed` → `start_campaign/3` → `Chimeway.trigger/3` → `WorkflowRun` with explainable trace and `pending_signals` populated after first-step wait entry.

Purpose: ECOS-06 requires billing failure to start dunning orchestration without host glue. Wave 58-01 harness enables tests; this wave delivers the workflow authoring and start-path proof (ROADMAP SC #1).

Output: Updated Accrue `chimeway.ex`, Accrue integration test extensions, and Chimeway `accrue_dunning_lifecycle_test.exs` start describe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-accrue-dunning-core/58-CONTEXT.md
@.planning/phases/58-accrue-dunning-core/58-RESEARCH.md
@.planning/phases/58-accrue-dunning-core/58-VALIDATION.md
@.planning/seeds/SEED-003-ecosystem-integrations.md

@../accrue/accrue/lib/accrue/integrations/chimeway.ex
@lib/chimeway/trigger.ex
@lib/chimeway/workflows/progression.ex
@examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex
@test/chimeway/orchestration/workflow_progression_test.exs
@test/support/accrue/fixtures.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accrue DunningNotifier workflow/2 + rendering/2 (D-06, D-07)</name>
  <files>../accrue/accrue/lib/accrue/integrations/chimeway.ex</files>
  <read_first>
    - `../accrue/accrue/lib/accrue/integrations/chimeway.ex` — current `DunningNotifier` (lines ~112–157), v1.40 moduledoc scope
    - `examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex` — `wait_until` + `cancel_signals` authoring pattern (lines ~59–88)
    - `.planning/phases/58-accrue-dunning-core/58-RESEARCH.md` — §6 workflow authoring sketch
    - `.planning/phases/58-accrue-dunning-core/58-CONTEXT.md` — D-06, D-07, D-01 (no Adapter)
  </read_first>
  <action>
    In `../accrue/accrue/lib/accrue/integrations/chimeway.ex`, update file header and `Accrue.Integrations.Chimeway` moduledoc to remove "v1.40 email-only immediate-only" scope — document Phase 58 multi-step workflow + Signal bridge (D-01).

    In nested `DunningNotifier` module, add `@impl Chimeway.Notifier` callbacks:

    **`rendering/2`** — return email assigns + render keys (required by `Chimeway.trigger/3` `resolve_rendering/1`):

    ```elixir
    @impl Chimeway.Notifier
    def rendering(params, _recipient) do
      sub_id = params[:subscription_id] || params["subscription_id"]

      {:ok,
       %{
         assigns: %{
           "subscription_id" => sub_id,
           "subject" => "Payment reminder",
           "html_body" => "<p>Please update your payment method.</p>",
           "text_body" => "Please update your payment method."
         },
         channels: %{
           email: %{
             render_key: "accrue.dunning.initial_email",
             render_version: 1
           }
         }
       }}
    end
    ```

    **`workflow/2`** — two-step email escalation per SEED-003 (48h = 172_800 seconds):

    ```elixir
    @impl Chimeway.Notifier
    def workflow(_params, _recipient) do
      {:ok,
       %{
         workflow_key: "accrue.dunning",
         workflow_version: 1,
         steps: [
           %{
             step_key: "initial_email",
             step_order: 1,
             channel: :email,
             config: %{
               "progress" => [
                 %{
                   "kind" => "wait_until",
                   "anchor" => "prior_delivery_terminal_at",
                   "delay_seconds" => 172_800,
                   "to_step" => "escalation_email",
                   "cancel_signals" => ["invoice.paid"]
                 }
               ]
             }
           },
           %{
             step_key: "escalation_email",
             step_order: 2,
             channel: :email,
             config: %{}
           }
         ]
       }}
    end
    ```

    Keep `orchestration/2` → `{:ok, :immediate}` unchanged (RESEARCH OQ-2 recommendation).

    Update `DunningNotifier` moduledoc: list `workflow/2` and `rendering/2`; remove "intentionally omitted" language.

    Do **not** add `Chimeway.Adapter` callbacks. Do **not** change `start_campaign/3` idempotency key format (`accrue.dunning:{subscription_id}:{anchor_iso}`).
  </action>
  <verify>
    <automated>cd ../accrue/accrue && mix test test/accrue/integrations/chimeway_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `DunningNotifier` exports `workflow/2` returning `workflow_key: "accrue.dunning"` with steps `initial_email` and `escalation_email`.
    - First step `wait_until` rule includes `"cancel_signals" => ["invoice.paid"]` and `"delay_seconds" => 172_800`.
    - `DunningNotifier` exports `rendering/2` with `render_key: "accrue.dunning.initial_email"`.
    - `orchestration/2` still returns `{:ok, :immediate}`.
    - Accrue integration tests compile (extend in Task 2 if existing tests assert omitted `workflow/2`).
  </acceptance_criteria>
  <done>Accrue DunningNotifier declares multi-step workflow with 48h escalation and invoice.paid cancel_signals (D-06).</done>
</task>

<task type="auto">
  <name>Task 2: Accrue-side behaviour tests for workflow/2 export (D-03)</name>
  <files>../accrue/accrue/test/accrue/integrations/chimeway_test.exs</files>
  <read_first>
    - `../accrue/accrue/test/accrue/integrations/chimeway_test.exs` — existing conditional-compile tests for Chimeway adapter
    - `../accrue/accrue/lib/accrue/integrations/chimeway.ex` — Task 1 `DunningNotifier` callbacks
    - `.planning/phases/58-accrue-dunning-core/58-RESEARCH.md` — §4 Accrue baseline vs target table
  </read_first>
  <action>
    Extend `../accrue/accrue/test/accrue/integrations/chimeway_test.exs` (inside existing `Code.ensure_loaded?(Chimeway)` guard):

    1. **workflow/2 exported** — assert `function_exported?(Accrue.Integrations.Chimeway.DunningNotifier, :workflow, 2)`.
    2. **workflow declaration normalizes** — call `Chimeway.Notifier.normalize_workflow_declaration/1` with Task 1 workflow map (via applying `workflow/2` with `%{}` and `%{}` recipient stub) and assert `{:ok, _}`.
    3. **rendering/2 exported** — assert `function_exported?(Accrue.Integrations.Chimeway.DunningNotifier, :rendering, 2)`.
    4. **cancel_signals present** — assert normalized first-step progress includes `"cancel_signals" => ["invoice.paid"]`.

    Do **not** change `cancel_campaign/3` in this task — Wave 58-03 owns termination signal fix (D-09).
  </action>
  <verify>
    <automated>cd ../accrue/accrue && mix test test/accrue/integrations/chimeway_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Accrue `chimeway_test.exs` contains tests asserting `workflow/2` and `rendering/2` export when Chimeway loaded.
    - `mix test test/accrue/integrations/chimeway_test.exs --warnings-as-errors` exits 0 in Accrue repo.
    - No changes to `cancel_campaign/3` signal emission in this task.
  </acceptance_criteria>
  <done>Accrue repo unit tests lock DunningNotifier workflow/2 + rendering/2 contract.</done>
</task>

<task type="auto">
  <name>Task 3: Start-path integration tests — payment_failed → WorkflowRun + waiting state (D-05, D-12)</name>
  <files>test/chimeway/integrations/accrue_dunning_lifecycle_test.exs, test/support/accrue/fixtures.ex</files>
  <read_first>
    - `test/support/accrue/fixtures.ex` — Wave 58-01 helpers
    - `test/chimeway/orchestration/workflow_progression_test.exs` — delivery terminal + `Progression.progress_run/2` pattern (lines ~340–355)
    - `../accrue/accrue/lib/accrue/webhook/default_handler.ex` — `maybe_start_dunning_campaign/2` (research cites L1250–1264)
    - `.planning/phases/58-accrue-dunning-core/58-CONTEXT.md` — D-04, D-05, D-12
    - `examples/chimeway_demo_host/test/demo_host_web/journey_test.exs` — JOUR-06 cancel semantics reference for progression helpers
  </read_first>
  <action>
    Create `test/chimeway/integrations/accrue_dunning_lifecycle_test.exs` (conditional compile when Accrue + Chimeway adapter loaded):

    ```elixir
    if Code.ensure_loaded?(Accrue) and Code.ensure_loaded?(Accrue.Integrations.Chimeway) do
      defmodule Chimeway.Integrations.AccrueDunningLifecycleTest do
        use Accrue.DataCase, async: false
        use Oban.Testing, repo: Chimeway.Repo
        @moduletag :accrue
        ...
      end
    end
    ```

    Add helper functions in test module or extend `test/support/accrue/fixtures.ex`:

    - `drain_initial_email_delivery!/1` — fetch first email delivery for notification, transition to `:dispatched`, `Deliveries.record_attempt(..., %{outcome: :succeeded})`.
    - `progress_to_waiting!/1` — call `Chimeway.Workflows.Progression.progress_run(workflow_run_id, [])`, assert `{:ok, {:noop, run, :wait_not_due}}`.

    Describe `"invoice.payment_failed starts dunning workflow (ECOS-06 start)"` with tests:

    1. **payment_failed creates WorkflowRun** — fixtures: customer, subscription, failed invoice; `configure_chimeway_dunning_engine!/0`, `configure_chimeway_logger_adapter!/0`. Call `Accrue.Test.trigger_event(:invoice_payment_failed, invoice)`. Query `Chimeway.Workflows.WorkflowRun` by `tenant_id == customer.id`. Assert exactly 1 run with `workflow_key == "accrue.dunning"`, `state in [:active, :waiting]`. Assert `Chimeway.Workflows.explain/2` (or transition list) returns non-empty explainable trace.

    2. **Idempotent duplicate payment_failed** — trigger same event twice (or call `start_campaign/3` twice with same anchor). Assert WorkflowRun count == 1; second trigger returns `:ok` or `{:duplicate, _}` treated as `:ok` per adapter.

    3. **wait_until sets pending_signals** — after test 1, drain initial email delivery and call `progress_to_waiting!/1`. Assert run `state == :waiting`, `status_reason == "waiting_for_step_progression"`, `pending_signals == ["invoice.paid"]`.

    Primary proof MUST use `Accrue.Test.trigger_event(:invoice_payment_failed, invoice)` — not direct `Chimeway.trigger/3` (D-12). Supplementary direct-trigger unit test acceptable in separate describe tagged `:unit` if needed.

    Extend `fixtures.ex` if invoice fixture must set dunning anchor column for Accrue campaign elector — follow `dunning_campaign_keying_test` in Accrue repo for anchor semantics.

    Do **not** implement termination tests — Wave 58-03.
  </action>
  <verify>
    <automated>mix verify.accrue --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/chimeway/integrations/accrue_dunning_lifecycle_test.exs` exists with `@moduletag :accrue`.
    - Start describe uses `Accrue.Test.trigger_event(:invoice_payment_failed, invoice)` as primary entry.
    - Asserts `WorkflowRun` created with `workflow_key: "accrue.dunning"`.
    - Duplicate trigger asserts single WorkflowRun (idempotency D-05).
    - After progression, run has `pending_signals == ["invoice.paid"]` and `state == :waiting`.
    - `mix verify.accrue` exits 0 (start describe green; terminate describe may be pending/skipped until 58-03).
  </acceptance_criteria>
  <done>Integration tests prove invoice.payment_failed → WorkflowRun start + wait_until pending_signals (ROADMAP SC #1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Accrue webhook → Chimeway trigger | `start_campaign/3` is sole failed-payment entry; idempotency key prevents duplicate runs (D-04, D-05). |
| Notifier workflow DSL → persisted config | Host-authored `DunningNotifier.workflow/2` normalized at declaration time. |
| Test → production billing | Fixtures use Accrue.TestRepo isolated database only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04 | Tampering | duplicate payment_failed webhooks | mitigate | Stable idempotency key `accrue.dunning:{subscription_id}:{anchor_iso}`; adapter treats `{:duplicate, _}` as `:ok`. |
| T-58-05 | Spoofing | cross-tenant workflow start | mitigate | `tenant_id: sub.customer_id` on trigger; tests assert run tenant matches customer UUID. |
| T-58-06 | Denial of Service | workflow step explosion | mitigate | Fixed 2-step workflow; single `wait_until` rule; max 1 cancel_signal entry. |
| T-58-07 | Information Disclosure | subscription_id in render assigns | accept | Test assigns only; operator trace redaction unchanged from core. |

**No `high`-severity threats.** Start path reuses existing trigger + progression spine. Block condition not triggered.
</threat_model>

<verification>
- [ ] `cd ../accrue/accrue && mix test test/accrue/integrations/chimeway_test.exs --warnings-as-errors` green
- [ ] `mix verify.accrue` green (start describe)
- [ ] `mix ci.test` green (accrue tests excluded)
- [ ] `grep -n "workflow/2" ../accrue/accrue/lib/accrue/integrations/chimeway.ex` returns match in DunningNotifier
- [ ] `grep -n "172_800" ../accrue/accrue/lib/accrue/integrations/chimeway.ex` returns match
- [ ] No `Chimeway.Adapter` module added
</verification>

<success_criteria>
- DunningNotifier implements multi-step workflow with 48h escalation (D-06)
- `invoice.payment_failed` starts WorkflowRun via Accrue engine — no host glue (ECOS-06, D-12)
- Idempotent duplicate triggers (D-05)
- Run enters `:waiting` with `pending_signals: ["invoice.paid"]` after first email (D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/58-accrue-dunning-core/58-02-SUMMARY.md`
</output>
