---
phase: 58-accrue-dunning-core
plan: 03
type: execute
wave: 3
depends_on: [58-02]
files_modified:
  - ../accrue/accrue/lib/accrue/integrations/chimeway.ex
  - ../accrue/accrue/test/accrue/integrations/chimeway_test.exs
  - test/chimeway/integrations/accrue_dunning_lifecycle_test.exs
autonomous: true
requirements: [ECOS-06]
must_haves:
  truths:
    - "`cancel_campaign/3` emits `Chimeway.Signal.track/4` with `event_name: \"invoice.paid\"` and `actor_id` = customer email — not `\"accrue.dunning\"` / `\"payment_recovered\"` (D-09)"
    - "`Accrue.Test.trigger_event(:invoice_paid, invoice)` terminates active dunning via Outcome Signal — no host callback glue (D-08, D-10, D-12)"
    - "Matched `:waiting` run transitions to `:active`, `pending_signals: []`, `status_reason: \"signal_received\"` with context `%{\"event_name\" => \"invoice.paid\"}` only (JOUR-06 / READ-03 parity)"
    - "No delivery row created for `escalation_email` step after signal before due_at — escalation blocked (ECOS-06 terminate semantics)"
  artifacts:
    - path: "../accrue/accrue/lib/accrue/integrations/chimeway.ex"
      provides: "cancel_campaign/3 Outcome Signal fix"
      contains: "invoice.paid"
    - path: "test/chimeway/integrations/accrue_dunning_lifecycle_test.exs"
      provides: "Terminate-path integration tests (@moduletag :accrue)"
      contains: "invoice_paid"
  key_links:
    - from: "../accrue/accrue/lib/accrue/integrations/chimeway.ex"
      to: "Chimeway.Signal.track/4"
      via: "cancel_campaign/3 with customer.email actor_id"
      pattern: "Signal\\.track"
    - from: "Chimeway.Workflows.route_signal/1"
      to: "WorkflowRun.pending_signals"
      via: "find_runs_waiting_for_signal/3 tenant + recipient_identity + event_name match"
      pattern: "invoice\\.paid"
    - from: "test/chimeway/integrations/accrue_dunning_lifecycle_test.exs"
      to: "Chimeway.Dispatch.SignalRouterWorker"
      via: "Oban.Testing perform_job after invoice_paid trigger"
      pattern: "SignalRouterWorker"
---

<objective>
Fix Accrue `cancel_campaign/3` to emit the canonical Outcome Signal (`invoice.paid` with customer email as `actor_id`), and prove termination: an in-flight dunning run in `:waiting` resumes via signal routing, blocks escalation email delivery, and records explainable `signal_received` transition — matching JOUR-06 READ/cancel semantics.

Purpose: ECOS-06 requires payment success to terminate active dunning without host glue. v1.40 `cancel_campaign/3` routes to zero runs (wrong actor + event name); this wave closes the termination loop (ROADMAP SC #2).

Output: Updated Accrue `cancel_campaign/3`, Accrue unit tests for signal shape, and Chimeway lifecycle test terminate describe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-accrue-dunning-core/58-CONTEXT.md
@.planning/phases/58-accrue-dunning-core/58-RESEARCH.md
@.planning/phases/58-accrue-dunning-core/58-VALIDATION.md

@../accrue/accrue/lib/accrue/integrations/chimeway.ex
@../accrue/accrue/lib/accrue/webhook/default_handler.ex
@lib/chimeway/signal.ex
@lib/chimeway/workflows.ex
@lib/chimeway/dispatch/signal_router_worker.ex
@test/chimeway/integrations/accrue_dunning_lifecycle_test.exs
@test/chimeway/orchestration/workflow_progression_test.exs
@examples/chimeway_demo_host/test/demo_host_web/journey_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix cancel_campaign/3 Outcome Signal emission (D-09, D-10)</name>
  <files>../accrue/accrue/lib/accrue/integrations/chimeway.ex</files>
  <read_first>
    - `../accrue/accrue/lib/accrue/integrations/chimeway.ex` — current `cancel_campaign/3` (lines ~95–110) using `"accrue.dunning"` + `"payment_recovered"`
    - `lib/chimeway/workflows.ex` — `find_runs_waiting_for_signal/3` matching on `recipient_identity == actor_id` and `event_name in pending_signals` (research L437–450)
    - `.planning/phases/58-accrue-dunning-core/58-CONTEXT.md` — D-08, D-09, D-10
    - `.planning/phases/58-accrue-dunning-core/58-RESEARCH.md` — §7.1 target implementation shape
  </read_first>
  <action>
    Replace `cancel_campaign/3` in `../accrue/accrue/lib/accrue/integrations/chimeway.ex`:

    ```elixir
    @impl Accrue.Dunning.Engine
    def cancel_campaign(%Subscription{} = sub, _iso_anchor, _opts) do
      customer = Accrue.Repo.repo().get!(Accrue.Billing.Customer, sub.customer_id)

      case Chimeway.Signal.track(
             sub.customer_id,
             customer.email,
             "invoice.paid",
             %{subscription_id: sub.id}
           ) do
        {:ok, _signal} -> :ok
        {:error, reason} -> {:error, reason}
      end
    end
    ```

    Update inline comments: remove v1.40 "routes to zero runs — safe no-op" rationale for wrong actor/event. Document D-09 contract: `actor_id` MUST match `DunningNotifier.recipients/1` `recipient_identity` (customer email); `event_name` MUST match `cancel_signals` on wait_until rule (`"invoice.paid"`).

    Update `Accrue.Integrations.Chimeway` moduledoc: document Outcome Signal termination path via `invoice.paid`.

    Do **not** change `DefaultHandler` anchor-clear logic (D-10 backstop remains in Accrue core). Do **not** add `:stopped`/`:completed` API unless terminate tests fail (deferred per 58-CONTEXT).

    Remove file-header comment claiming `Signal.track("payment_recovered")` is intentional no-op.
  </action>
  <verify>
    <automated>cd ../accrue/accrue && mix test test/accrue/integrations/chimeway_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `cancel_campaign/3` calls `Chimeway.Signal.track(sub.customer_id, customer.email, "invoice.paid", ...)`.
    - No remaining `"payment_recovered"` or `"accrue.dunning"` actor_id strings in `cancel_campaign/3`.
    - `tenant_id` remains `sub.customer_id` (consistent with `start_campaign/3`).
    - Accrue repo compiles with `--warnings-as-errors`.
  </acceptance_criteria>
  <done>cancel_campaign/3 emits canonical invoice.paid Outcome Signal with customer email actor (D-09).</done>
</task>

<task type="auto">
  <name>Task 2: Accrue unit tests for cancel_campaign signal shape (D-09)</name>
  <files>../accrue/accrue/test/accrue/integrations/chimeway_test.exs</files>
  <read_first>
    - `../accrue/accrue/test/accrue/integrations/chimeway_test.exs` — Wave 58-02 extensions
    - `../accrue/accrue/lib/accrue/integrations/chimeway.ex` — Task 1 `cancel_campaign/3`
    - `lib/chimeway/signal.ex` — Signal schema fields (`tenant_id`, `actor_id`, `event_name`, `payload`)
  </read_first>
  <action>
    Add describe `"cancel_campaign/3 Outcome Signal (ECOS-06)"` in Accrue `chimeway_test.exs` (Chimeway loaded guard):

    1. Insert customer with known email + subscription fixture.
    2. Call `Accrue.Integrations.Chimeway.cancel_campaign(sub, DateTime.utc_now() |> DateTime.to_iso8601(), [])`.
    3. Assert `Chimeway.Repo` (or test signal query helper) has exactly 1 signal with:
       - `tenant_id == sub.customer_id`
       - `actor_id == customer.email`
       - `event_name == "invoice.paid"`
       - `payload[:subscription_id] == sub.id` or equivalent map access

    Use Accrue test sandbox + Chimeway.Repo if cross-repo signal insert requires both repos started (mirror Chimeway integration harness).

    Assert `"payment_recovered"` and `"accrue.dunning"` do NOT appear in inserted signal row.
  </action>
  <verify>
    <automated>cd ../accrue/accrue && mix test test/accrue/integrations/chimeway_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Accrue test asserts signal `event_name == "invoice.paid"` and `actor_id == customer.email`.
    - `mix test test/accrue/integrations/chimeway_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Accrue-side unit test locks cancel_campaign signal shape before Chimeway E2E.</done>
</task>

<task type="auto">
  <name>Task 3: Terminate-path E2E — invoice.paid blocks escalation (D-08, D-12, JOUR-06)</name>
  <files>test/chimeway/integrations/accrue_dunning_lifecycle_test.exs</files>
  <read_first>
    - `test/chimeway/integrations/accrue_dunning_lifecycle_test.exs` — Wave 58-02 start describe + helpers
    - `test/chimeway/orchestration/workflow_progression_test.exs` — signal router test pattern (lines ~357–398)
    - `examples/chimeway_demo_host/test/demo_host_web/journey_test.exs` — JOUR-06: after signal, no email deliveries, step stays on first (lines ~135–144)
    - `../accrue/accrue/lib/accrue/test/webhooks.ex` — `:invoice_paid` → `"invoice.paid"` mapping
    - `.planning/phases/58-accrue-dunning-core/58-RESEARCH.md` — §7.3 terminate semantics table + §7.4 test assertions
  </read_first>
  <action>
    Add describe `"invoice.paid terminates dunning via Outcome Signal (ECOS-06 terminate)"` to `accrue_dunning_lifecycle_test.exs`.

    Shared setup helper `start_dunning_and_wait!/0`:

    1. Insert customer, subscription, failed invoice fixtures.
    2. `Accrue.Test.trigger_event(:invoice_payment_failed, invoice)`.
    3. Drain initial email delivery to terminal (`Deliveries.record_attempt` `:succeeded`).
    4. `Progression.progress_run/2` → assert `:waiting`, `pending_signals == ["invoice.paid"]`.
    5. Return `%{workflow_run: run, notification: notification, customer: customer, invoice: invoice}`.

    Tests:

    1. **invoice_paid via Accrue webhook path** — call `start_dunning_and_wait!/0`, then `Accrue.Test.trigger_event(:invoice_paid, invoice)` (recovery path through DefaultHandler → `cancel_campaign/3`). Drain Oban `:chimeway_signals` queue: `Oban.drain_queue(queue: :chimeway_signals)` or `perform_job(SignalRouterWorker, ...)`. Assert run `state == :active`, `pending_signals == []`, `status_reason == "signal_received"`. Assert exactly one transition with `reason == "signal_received"` and `context == %{"event_name" => "invoice.paid"}` (event_name only — READ-03).

    2. **No escalation email delivery** — after test 1, query deliveries for notification/workflow_run. Assert no delivery with step_key / workflow step matching `"escalation_email"`. Assert current workflow step cursor remains `"initial_email"` (JOUR-06 parity — terminate = block escalation, not necessarily `:stopped`).

    3. **Signal before due_at** — explicitly assert test does NOT advance time 48h; signal arrives while `status_context["due_at"]` is in the future. Confirms Outcome Signal cancels wait early.

    4. **Optional explain trace** — `Chimeway.Workflows.explain/2` on workflow_run_id includes waiting → signal_received chain (ROADMAP SC #1 explainability on terminate path).

    Primary terminate entry MUST be `Accrue.Test.trigger_event(:invoice_paid, invoice)` — not direct `Chimeway.Signal.track/4` (D-12). Direct `Signal.track` acceptable only as tagged `:unit` supplement.

    Ensure test ordering: run MUST be `:waiting` before invoice_paid trigger (pitfall: signal processed before wait entry).

    Configure `Application.put_env(:chimeway, :dispatcher, Chimeway.Dispatch.Oban)` in setup if not already from 58-01 harness.
  </action>
  <verify>
    <automated>mix verify.accrue --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Terminate describe uses `Accrue.Test.trigger_event(:invoice_paid, invoice)` as primary entry.
    - After signal routing, run `state == :active`, `pending_signals == []`, `status_reason == "signal_received"`.
    - Transition context contains only `%{"event_name" => "invoice.paid"}` — no raw payload fields (READ-03).
    - Zero deliveries for `escalation_email` step after termination.
    - `mix verify.accrue` exits 0 with both start and terminate describes green.
    - `mix ci.test` exits 0 (accrue excluded).
  </acceptance_criteria>
  <done>Full ECOS-06 lifecycle proven: payment_failed start → waiting → invoice.paid terminate without host glue (ROADMAP SC #2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Accrue recovery webhook → Signal.track | `cancel_campaign/3` resolves customer email server-side — never from webhook caller args. |
| Signal routing → WorkflowRun | `route_signal/1` matches `tenant_id` + `recipient_identity` + `pending_signals` — cross-tenant isolation unchanged (T-27-03). |
| Signal payload → operator traces | Transition context stores `event_name` only (READ-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-07 | Spoofing | forged invoice.paid signal | mitigate | Termination in production flows through Accrue `DefaultHandler` → `cancel_campaign/3`; tests use Accrue Fake processor. Cross-tenant routing regression via existing `workflows_test.exs`. |
| T-58-08 | Spoofing | wrong actor_id cancels another customer's dunning | mitigate | D-09: `actor_id = customer.email` must match `recipient_identity` on notification; `route_signal/1` enforces equality. |
| T-58-09 | Tampering | stale payment_recovered event name | mitigate | Remove `"payment_recovered"` from `cancel_campaign/3`; tests assert canonical `"invoice.paid"`. |
| T-58-10 | Repudiation | silent no-op on wrong signal shape | mitigate | Integration tests assert signal row + run transition; v1.40 silent failure explicitly fixed. |
| T-58-11 | Information Disclosure | subscription_id in signal payload | accept | Payload persisted on signal row; transition context remains event_name only per READ-03. |

**No `high`-severity threats.** Termination reuses v1.7 Signal spine; actor/event fix closes v1.40 routing gap. Block condition not triggered.
</threat_model>

<verification>
- [ ] `mix verify.accrue --warnings-as-errors` green (start + terminate describes)
- [ ] `mix ci.test` green
- [ ] `cd ../accrue/accrue && mix test test/accrue/integrations/chimeway_test.exs --warnings-as-errors` green
- [ ] `grep -n "payment_recovered" ../accrue/accrue/lib/accrue/integrations/chimeway.ex` returns zero matches in `cancel_campaign/3`
- [ ] `grep -n "invoice.paid" ../accrue/accrue/lib/accrue/integrations/chimeway.ex` returns match in `cancel_campaign/3`
- [ ] `mix test test/chimeway/workflows_test.exs --warnings-as-errors` green (T-27-03 regression)
- [ ] No `:stopped`/`:completed` API added unless terminate tests fail
</verification>

<success_criteria>
- `cancel_campaign/3` emits `invoice.paid` with customer email actor (D-09)
- `invoice.paid` terminates active dunning via Outcome Signal — no host glue (ECOS-06, D-12)
- Escalation email blocked; run resumes `:active` with `signal_received` (JOUR-06 parity)
- Full `mix verify.accrue` green; Phase 58 library-level ECOS-06 complete
</success_criteria>

<output>
After completion, create `.planning/phases/58-accrue-dunning-core/58-03-SUMMARY.md`
</output>
