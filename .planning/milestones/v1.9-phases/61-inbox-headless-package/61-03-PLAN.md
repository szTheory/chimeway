---
phase: 61-inbox-headless-package
plan: 03
type: execute
wave: 3
depends_on: [61-02]
files_modified:
  - chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs
  - chimeway_inbox/test/support/live_view_case.ex
  - mix.exs
autonomous: true
requirements: [INBX-02]
must_haves:
  truths:
    - "LiveViewTest proves mount → list items → mark_read → badge count decreases (D-08)"
    - "mark_seen path callable from handler without host glue (D-08)"
    - "Unauthorized auth does not render inbox chrome (D-05)"
    - "UI-SPEC contractual copy and data-cw-inbox-* hooks asserted in HTML (D-07)"
  artifacts:
    - path: "chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs"
      provides: "INBX-02 package proof"
      contains: "mark_read"
    - path: "mix.exs"
      provides: "verify.example includes chimeway_inbox lane"
      contains: "chimeway_inbox"
  key_links:
    - from: "chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs"
      to: "lib/chimeway/inbox.ex"
      via: "DataCase inserts + Chimeway public API side effects"
      pattern: "Chimeway\\.mark_read"
---

<objective>
Prove INBX-02 with package LiveViewTests: list → mark_read/seen → badge refresh, plus unauthorized mount behavior — using AllowAuth test support only (no demo host glue).

Purpose: Closes Phase 61 success criterion #3; demo mount remains Phase 62 (DEMO-08).

Output: `bell_dropdown_live_test.exs` and optional `mix.exs` `verify.example` extension for chimeway_inbox parity with admin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-inbox-headless-package/61-CONTEXT.md
@.planning/phases/61-inbox-headless-package/61-UI-SPEC.md
@.planning/phases/61-inbox-headless-package/61-VALIDATION.md
@chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs
@test/chimeway/inbox_integration_test.exs
@mix.exs
</context>

<threat_model>
| ID | STRIDE | Threat | Severity | Mitigation |
|----|--------|--------|----------|------------|
| T-61-01 | Spoofing | Test uses wrong recipient for mark_read | medium | Tests seed `user:42` and AllowAuth returns same identity; assert not_found for other id |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: LiveViewCase sandbox + notification fixtures (D-08)</name>
  <files>chimeway_inbox/test/support/live_view_case.ex</files>
  <read_first>
    - `chimeway_admin/test/support/live_view_case.ex` — AllowAuth swap in setup
    - `test/chimeway/inbox_query_test.exs` — notification insert helpers
    - `test/chimeway/inbox_integration_test.exs` — trigger spine if needed
  </read_first>
  <action>
    Extend `ChimewayInbox.LiveViewCase` setup:
    1. Swap `auth_module` to `AllowAuth` (existing pattern).
    2. Checkout `Chimeway.Repo` sandbox in shared mode for LiveView + test process (`Ecto.Adapters.SQL.Sandbox.checkout(Chimeway.Repo)` and `allow(Chimeway.Repo, self(), live_view_pid)` pattern when using `live/2` connected mount — use `live_isolated/3` if sandbox sharing is simpler per admin tests).

    Add helper module `ChimewayInbox.TestSupport.Fixtures` (same file or `test/support/fixtures.ex`) with `insert_inbox_notification!(recipient_identity, attrs \\ %{})` inserting `Event` + `Notification` rows (copy minimal helpers from `inbox_query_test.exs`).

    Do not add demo host dependency.
  </action>
  <acceptance_criteria>
    - `chimeway_inbox/test/support/` contains fixture helper for notifications
    - `cd chimeway_inbox && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>cd chimeway_inbox && mix compile --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 2: Bell dropdown LiveView tests (D-07, D-08)</name>
  <files>chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs</files>
  <read_first>
    - `chimeway_inbox/lib/chimeway_inbox/live/bell_dropdown_live.ex` — events and DOM
    - `.planning/phases/61-inbox-headless-package/61-UI-SPEC.md` — copy + event table
    - `chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs` — live_isolated pattern
  </read_first>
  <action>
    Create `bell_dropdown_live_test.exs` with tests:

    1. **mount lists notifications** — seed 2 unread for `user:42`, mount with AllowAuth session, open panel via `render_click` `toggle_panel`, assert `data-cw-inbox-items` contains both `data-notification-id` values and panel title `Notifications`.

    2. **mark_read updates badge** — seed 1 unread, assert badge shows `1` (or aria-label contains `1 unread`), click row `mark_read`, assert unread_count assign reflected in re-rendered badge hidden or `0 unread` aria-label.

    3. **mark_seen invoked** — if LiveView exposes seen on mount or first open, assert `seen_at` set via `Repo.get!(Notification, id)` after `render_click` toggle; if not auto-seen, add explicit test event only if implemented — prefer asserting `Chimeway.mark_seen/3` called by documenting handler; minimum: mark_read test required, mark_seen optional if not in LiveView v1.9 (skip with comment referencing D-08 discretion).

    4. **unauthorized mount** — use DenyAuth in setup override, assert redirect and HTML does not contain `data-cw-inbox-bell`.

    5. **UI-SPEC copy** — assert strings `Mark as read`, `Load more notifications`, `No notifications yet` in appropriate states (empty state test with zero notifications).

    Use `live_isolated(conn, BellDropdownLive, session: %{"current_actor" => "user:42"}, on_mount: [{LiveAuth, :inbox_bell}])` or connected `live/2` through test router — match admin precedent.
  </action>
  <acceptance_criteria>
    - `bell_dropdown_live_test.exs` has at least 4 tests
    - `cd chimeway_inbox && mix test test/chimeway_inbox/live/bell_dropdown_live_test.exs --warnings-as-errors` exits 0
    - Tests prove badge/count changes after `mark_read` (grep assert on aria-label or `data-cw-inbox-badge`)
  </acceptance_criteria>
  <verify>cd chimeway_inbox && mix test test/chimeway_inbox/live/bell_dropdown_live_test.exs --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 3: verify.example parity for chimeway_inbox (optional GATE-05 prep)</name>
  <files>mix.exs</files>
  <read_first>
    - `mix.exs` — `verify.example` alias (admin + demo host lines)
    - `.planning/phases/61-inbox-headless-package/61-CONTEXT.md` — deferred GATE-05 to Phase 62
  </read_first>
  <action>
    Append to root `mix.exs` `verify.example` alias list (after chimeway_admin line):

    ```elixir
    "cmd --shell cd chimeway_inbox && mix deps.get && mix test --warnings-as-errors",
    ```

    Do **not** add `verify.inbox` or CI job yet (Phase 62 GATE-05). Comment in alias: inbox selective gate lands Phase 62.
  </action>
  <acceptance_criteria>
    - `mix.exs` `verify.example` includes `chimeway_inbox` shell command
    - `grep -q chimeway_inbox mix.exs` succeeds
  </acceptance_criteria>
  <verify>grep -q chimeway_inbox mix.exs</verify>
</task>

</tasks>

<verification>
- [ ] `cd chimeway_inbox && mix test --warnings-as-errors` fully green
- [ ] Phase 61 ROADMAP success criteria #3 satisfied
- [ ] No demo host files modified (Phase 62)
</verification>
