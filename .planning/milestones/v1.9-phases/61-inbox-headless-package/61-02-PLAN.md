---
phase: 61-inbox-headless-package
plan: 02
type: execute
wave: 2
depends_on: [61-01]
files_modified:
  - chimeway_inbox/mix.exs
  - chimeway_inbox/mix.lock
  - chimeway_inbox/.formatter.exs
  - chimeway_inbox/config/config.exs
  - chimeway_inbox/config/test.exs
  - chimeway_inbox/lib/chimeway_inbox.ex
  - chimeway_inbox/lib/chimeway_inbox/application.ex
  - chimeway_inbox/lib/chimeway_inbox/auth.ex
  - chimeway_inbox/lib/chimeway_inbox/live_auth.ex
  - chimeway_inbox/lib/chimeway_inbox/router.ex
  - chimeway_inbox/lib/chimeway_inbox/live/bell_dropdown_live.ex
  - chimeway_inbox/test/test_helper.exs
  - chimeway_inbox/test/support/endpoint.ex
  - chimeway_inbox/test/support/router.ex
  - chimeway_inbox/test/support/allow_auth.ex
  - chimeway_inbox/test/support/deny_auth.ex
  - chimeway_inbox/test/support/live_view_case.ex
  - chimeway_inbox/test/chimeway_inbox/router_test.exs
autonomous: true
requirements: [INBX-02]
must_haves:
  truths:
    - "chimeway_inbox optional package compiles with path dep on chimeway (D-04)"
    - "ChimewayInbox.Auth behaviour exposes current_recipient/2; LiveAuth fail-closed (D-05)"
    - "ChimewayInbox.Router macro mounts BellDropdownLive with live_session + LiveAuth (D-06)"
    - "BellDropdownLive renders UI-SPEC DOM hooks and handles contractual LiveView events (D-07)"
    - "LiveView calls Chimeway.unread_count/1 and paginated list_for_recipient/2 only — never Chimeway.Inbox (D-04)"
  artifacts:
    - path: "chimeway_inbox/mix.exs"
      provides: "Package definition v0.1.0"
      contains: ":chimeway_inbox"
    - path: "chimeway_inbox/lib/chimeway_inbox/live/bell_dropdown_live.ex"
      provides: "Bell dropdown LiveView"
      contains: "data-cw-inbox-bell"
    - path: "chimeway_inbox/lib/chimeway_inbox/router.ex"
      provides: "chimeway_inbox_routes macro"
      contains: "chimeway_inbox_routes"
  key_links:
    - from: "chimeway_inbox/lib/chimeway_inbox/live/bell_dropdown_live.ex"
      to: "lib/chimeway.ex"
      via: "Chimeway.list_for_recipient/2, Chimeway.mark_read/3"
      pattern: "Chimeway\\."
    - from: "chimeway_inbox/lib/chimeway_inbox/live_auth.ex"
      to: "chimeway_inbox/lib/chimeway_inbox/auth.ex"
      via: "auth_module.current_recipient/2"
      pattern: "current_recipient"
---

<objective>
Bootstrap the optional `chimeway_inbox` Phoenix package: Auth behaviour, fail-closed LiveAuth, mountable Router macro, and unstyled `BellDropdownLive` implementing the approved UI-SPEC (copy, DOM `data-*` hooks, LiveView events).

Purpose: INBX-02 delivers mountable end-user inbox chrome for Feature Developer JTBD; Wave 61-03 adds LiveViewTest proof.

Output: New `chimeway_inbox/` directory tree cloned from `chimeway_admin/` patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-inbox-headless-package/61-CONTEXT.md
@.planning/phases/61-inbox-headless-package/61-RESEARCH.md
@.planning/phases/61-inbox-headless-package/61-UI-SPEC.md
@.planning/phases/61-inbox-headless-package/61-VALIDATION.md
@chimeway_admin/mix.exs
@chimeway_admin/lib/chimeway_admin/router.ex
@chimeway_admin/lib/chimeway_admin/live_auth.ex
@chimeway_admin/lib/chimeway_admin/auth.ex
@chimeway_admin/test/support/endpoint.ex
@chimeway_admin/test/support/live_view_case.ex
@chimeway_admin/config/test.exs
@lib/chimeway.ex
</context>

<threat_model>
| ID | STRIDE | Threat | Severity | Mitigation |
|----|--------|--------|----------|------------|
| T-61-01 | Spoofing | Handler marks read without resolved recipient | high | `current_recipient/2` on mount; `ensure_authorized/2` before events; pass identity to `Chimeway.mark_read/3` |
| T-61-03 | Elevation | Unauthorized session renders inbox UI | high | LiveAuth `{:halt, redirect}`; test config defaults `DenyAuth` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Package scaffold (D-04)</name>
  <files>chimeway_inbox/mix.exs, chimeway_inbox/.formatter.exs, chimeway_inbox/config/config.exs, chimeway_inbox/lib/chimeway_inbox.ex, chimeway_inbox/lib/chimeway_inbox/application.ex</files>
  <read_first>
    - `chimeway_admin/mix.exs` — deps, version 0.1.0, elixirc_paths test support
    - `chimeway_admin/lib/chimeway_admin.ex` and `application.ex`
    - `.planning/phases/61-inbox-headless-package/61-CONTEXT.md` — D-04
  </read_first>
  <action>
    Create `chimeway_inbox/` sibling package:
    - `app: :chimeway_inbox`, `version: "0.1.0"`, `elixir: "~> 1.17"`
    - deps: `{:chimeway, path: ".."}`, `{:phoenix, "~> 1.7"}`, `{:phoenix_html, "~> 4.0"}`, `{:phoenix_live_view, "~> 1.0"}`, `{:jason, "~> 1.4"}`, `{:ecto_sql, "~> 3.11"}`, `{:oban, "~> 2.17"}`, test-only `floki`, `lazy_html`
    - `config/config.exs`: `config :chimeway_inbox, auth_module: ChimewayInbox` placeholder comment for host
    - `ChimewayInbox.Application` — empty supervisor (mirror admin)
    - Root module `ChimewayInbox` with moduledoc describing optional package purpose

    Run `cd chimeway_inbox && mix deps.get` as part of verification.
  </action>
  <acceptance_criteria>
    - `chimeway_inbox/mix.exs` exists with `app: :chimeway_inbox`
    - `cd chimeway_inbox && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>cd chimeway_inbox && mix deps.get && mix compile --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 2: Auth + LiveAuth + test harness (D-05)</name>
  <files>chimeway_inbox/lib/chimeway_inbox/auth.ex, chimeway_inbox/lib/chimeway_inbox/live_auth.ex, chimeway_inbox/config/test.exs, chimeway_inbox/test/test_helper.exs, chimeway_inbox/test/support/endpoint.ex, chimeway_inbox/test/support/router.ex, chimeway_inbox/test/support/allow_auth.ex, chimeway_inbox/test/support/deny_auth.ex, chimeway_inbox/test/support/live_view_case.ex</files>
  <read_first>
    - `chimeway_admin/lib/chimeway_admin/auth.ex` — behaviour + auth_module/0
    - `chimeway_admin/lib/chimeway_admin/live_auth.ex` — on_mount + ensure_authorized
    - `chimeway_admin/test/support/*` — endpoint port 4002 → use **4003** for inbox to avoid collision
    - `chimeway_admin/config/test.exs` — Chimeway.Repo sandbox config
  </read_first>
  <action>
    **Auth behaviour** `ChimewayInbox.Auth`:
    - `@callback current_recipient(session :: map(), context :: map()) :: {:ok, String.t()} | {:error, :unauthorized}`
    - `auth_module/0` via `Application.fetch_env!(:chimeway_inbox, :auth_module)`

    **LiveAuth** `ChimewayInbox.LiveAuth`:
    - `on_mount(:inbox_bell, ...)` — resolve recipient via `auth_module.current_recipient/2`; assign `:recipient_identity`, `:chimeway_inbox_session`; halt redirect on error
    - `ensure_authorized/2` for event handlers (same pattern as admin)
    - `unauthorized_redirect` from `Application.get_env(:chimeway_inbox, :unauthorized_redirect, "/")`
    - Log + treat unexpected auth returns as unauthorized

    **Test support:** Clone admin endpoint/router/LiveViewCase; `AllowAuth` returns `{:ok, "user:42"}`; `DenyAuth` returns `{:error, :unauthorized}`.

    **test_helper.exs:** `Application.ensure_all_started(:chimeway)`, start test endpoint, `Ecto.Adapters.SQL.Sandbox.mode(Chimeway.Repo, :manual)`.

    **config/test.exs:** `auth_module: DenyAuth` default; endpoint config; Chimeway.Repo sandbox (copy admin repo_config block).
  </action>
  <acceptance_criteria>
    - `chimeway_inbox/lib/chimeway_inbox/auth.ex` defines `current_recipient` callback
    - `chimeway_inbox/test/support/allow_auth.ex` implements behaviour
    - `cd chimeway_inbox && mix test test/chimeway_inbox/router_test.exs --warnings-as-errors` exits 0 (router test from Task 3)
  </acceptance_criteria>
  <verify>cd chimeway_inbox && mix test test/chimeway_inbox/router_test.exs --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 3: Router macro + mount smoke test (D-06)</name>
  <files>chimeway_inbox/lib/chimeway_inbox/router.ex, chimeway_inbox/test/support/router.ex, chimeway_inbox/test/chimeway_inbox/router_test.exs</files>
  <read_first>
    - `chimeway_admin/lib/chimeway_admin/router.ex` — `chimeway_admin_routes` macro
    - `chimeway_admin/test/support/router.ex`
    - `.planning/phases/61-inbox-headless-package/61-UI-SPEC.md` — surface ownership
  </read_first>
  <action>
    Add `ChimewayInbox.Router` with `defmacro chimeway_inbox_routes(_opts \\ [])` quoting:
    - `live_session :chimeway_inbox_bell, on_mount: [{ChimewayInbox.LiveAuth, :inbox_bell}]`
    - `live "/", ChimewayInbox.Live.BellDropdownLive, :index`

    Test router imports macro. Add `router_test.exs` asserting route compiles (e.g. `Phoenix.Router.routes/1` includes bell live route) — minimal smoke.

    Document host mount example in `ChimewayInbox.Router` moduledoc (scope + `import ChimewayInbox.Router`).
  </action>
  <acceptance_criteria>
    - `grep -q 'chimeway_inbox_routes' chimeway_inbox/lib/chimeway_inbox/router.ex`
    - `cd chimeway_inbox && mix test test/chimeway_inbox/router_test.exs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>cd chimeway_inbox && mix test test/chimeway_inbox/router_test.exs --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 4: BellDropdownLive UI-SPEC implementation (D-07)</name>
  <files>chimeway_inbox/lib/chimeway_inbox/live/bell_dropdown_live.ex</files>
  <read_first>
    - `.planning/phases/61-inbox-headless-package/61-UI-SPEC.md` — DOM structure, events, copy table (full file)
    - `lib/chimeway.ex` — public inbox API from Wave 61-01
    - `chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex` — LiveView structure reference
  </read_first>
  <action>
    Implement `ChimewayInbox.Live.BellDropdownLive`:

    **Mount:** After LiveAuth, load `Chimeway.unread_count(recipient_identity)` and paginated `Chimeway.list_for_recipient(recipient_identity, limit: page_size())` where `page_size/0` reads `Application.get_env(:chimeway_inbox, :page_size, 20)`.

    **Assigns:** `panel_open`, `unread_count`, `items`, `has_more`, `load_error`, `recipient_identity`.

    **Render:** Root `.chimeway-inbox`; button `data-cw-inbox-bell` with dynamic `aria-label` per UI-SPEC (0 vs n unread); badge `data-cw-inbox-badge` hidden when count 0; panel `data-cw-inbox-panel` role dialog when open; list `data-cw-inbox-items`; rows with `data-notification-id`, `data-unread` from nil `read_at` in item map; footer CTAs exact copy strings.

    **Events:**
    - `toggle_panel` — flip `panel_open`, set `aria-expanded`
    - `mark_read` — `Chimeway.mark_read(id, recipient_identity)` then refresh count/items
    - `mark_all_read` — mark each visible unread item
    - `load_more` — append next page using cursor from last item's `inserted_at` + `id`
    - `retry_load` — clear `load_error`, re-fetch

    **Security:** Call `LiveAuth.ensure_authorized(socket, :inbox_bell)` at start of each `handle_event`.

    **No PubSub.** **No** `Chimeway.Inbox` alias.

    Optional: `assign(:item_link_fun, nil)` — href not rendered in v1.9.
  </action>
  <acceptance_criteria>
    - `bell_dropdown_live.ex` contains `data-cw-inbox-bell` and event handlers `mark_read`, `toggle_panel`
    - `grep -r 'Chimeway\.Inbox' chimeway_inbox/lib` returns no matches
    - `cd chimeway_inbox && mix compile --warnings-as-errors` exits 0
    - HTML render includes exact string `Notifications` from UI-SPEC (compile-time constant in template)
  </acceptance_criteria>
  <verify>cd chimeway_inbox && mix compile --warnings-as-errors && ! grep -r 'Chimeway\.Inbox' chimeway_inbox/lib</verify>
</task>

</tasks>

<verification>
- [ ] Package structure mirrors chimeway_admin conventions
- [ ] UI-SPEC DOM hooks and copy present in BellDropdownLive
- [ ] `cd chimeway_inbox && mix test --warnings-as-errors` passes router smoke (LiveView tests in 61-03)
</verification>
