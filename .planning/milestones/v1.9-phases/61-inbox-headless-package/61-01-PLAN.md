---
phase: 61-inbox-headless-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/inbox.ex
  - lib/chimeway/inbox/item.ex
  - lib/chimeway.ex
  - test/chimeway/inbox_query_test.exs
  - test/chimeway/inbox_pagination_test.exs
autonomous: true
requirements: [INBX-01]
must_haves:
  truths:
    - "Chimeway.unread_count/1 returns count of unread notifications for recipient, exclude_archived default true (D-01)"
    - "Paginated list_for_recipient/2 returns %{items: dto_maps, has_more: boolean} when limit/cursor opts present (D-02)"
    - "Non-paginated list_for_recipient/2 still returns [%Notification{}] for backward compatibility (D-02)"
    - "DTO maps include id, title, body_preview, inserted_at, read_at, seen_at per UI-SPEC (D-03)"
  artifacts:
    - path: "lib/chimeway/inbox/item.ex"
      provides: "Serializable inbox item maps"
      contains: "def to_map"
    - path: "lib/chimeway/inbox.ex"
      provides: "unread_count/1 and paginated list branch"
      contains: "unread_count"
    - path: "lib/chimeway.ex"
      provides: "Public unread_count delegate"
      contains: "def unread_count"
    - path: "test/chimeway/inbox_pagination_test.exs"
      provides: "Pagination + DTO + archived filter tests"
      contains: "has_more"
  key_links:
    - from: "lib/chimeway/inbox.ex"
      to: "lib/chimeway/inbox/item.ex"
      via: "Item.to_map/1 on paginated branch"
      pattern: "Chimeway\\.Inbox\\.Item"
    - from: "lib/chimeway.ex"
      to: "lib/chimeway/inbox.ex"
      via: "defdelegate unread_count"
      pattern: "unread_count"
---

<objective>
Deliver UI-ready headless inbox queries on core Chimeway: `unread_count/1`, keyset-paginated `list_for_recipient/2` with stable DTO maps, and `exclude_archived` defaults — without Phoenix dependencies in core.

Purpose: INBX-01 unblocks `chimeway_inbox` LiveView (Wave 61-02) which consumes only `Chimeway` public API.

Output: Updated `lib/chimeway/inbox.ex`, new `lib/chimeway/inbox/item.ex`, `lib/chimeway.ex` delegate, extended query tests + new pagination tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/61-inbox-headless-package/61-CONTEXT.md
@.planning/phases/61-inbox-headless-package/61-RESEARCH.md
@.planning/phases/61-inbox-headless-package/61-VALIDATION.md
@.planning/phases/61-inbox-headless-package/61-UI-SPEC.md
@AGENTS.md
@lib/chimeway/inbox.ex
@lib/chimeway.ex
@lib/chimeway/notifications/notification.ex
@test/chimeway/inbox_query_test.exs
</context>

<threat_model>
| ID | STRIDE | Threat | Severity | Mitigation |
|----|--------|--------|----------|------------|
| T-61-02 | Info disclosure | List/count leaks other recipients' rows | high | All queries filter `recipient_identity == ^id`; tests use distinct identities |
| T-61-04 | Tampering | Invalid `before_id` cursor causes query errors | low | Validate UUID with `Ecto.UUID.cast/1`; invalid cursor → treat as no cursor (first page) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: DTO mapper module (D-03)</name>
  <files>lib/chimeway/inbox/item.ex</files>
  <read_first>
    - `lib/chimeway/notifications/notification.ex` — metadata fields
    - `.planning/phases/61-inbox-headless-package/61-UI-SPEC.md` — Serializable item map table
    - `.planning/phases/61-inbox-headless-package/61-CONTEXT.md` — D-03
  </read_first>
  <action>
    Create `lib/chimeway/inbox/item.ex` with `Chimeway.Inbox.Item.to_map/1` accepting `%Notification{}`.

    Map keys (string values where noted):
    - `id` — `notification.id` as string
    - `title` — `metadata["subject"] || metadata["title"] || ""`
    - `body_preview` — first non-nil among `metadata["body_preview"]`, `metadata["preview"]`, `metadata["body"]`; truncate to 120 graphemes/codepoints (use `String.slice/2`)
    - `inserted_at`, `read_at`, `seen_at` — `DateTime.to_iso8601/1` or nil
    - Do not include `href` in v1.9 (omit key)

    Add `@spec to_map(Notification.t()) :: map()` and keep module Phoenix-free.
  </action>
  <acceptance_criteria>
    - `lib/chimeway/inbox/item.ex` defines `def to_map(%Notification{})`
    - `mix compile --warnings-as-errors` exits 0
    - `grep -q 'body_preview' lib/chimeway/inbox/item.ex` succeeds
  </acceptance_criteria>
  <verify>mix compile --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 2: unread_count + exclude_archived query helpers (D-01)</name>
  <files>lib/chimeway/inbox.ex, lib/chimeway.ex</files>
  <read_first>
    - `lib/chimeway/inbox.ex` — existing `list_for_recipient/2`, `maybe_filter_unread/2`
    - `lib/chimeway.ex` — delegate pattern for inbox functions
    - `.planning/phases/61-inbox-headless-package/61-CONTEXT.md` — D-01
  </read_first>
  <action>
    In `Chimeway.Inbox`:
    1. Add private `exclude_archived?/1` defaulting opts `exclude_archived` to `true`; when true, `where(is_nil(archived_at))`.
    2. Apply `exclude_archived?` to base recipient query used by list and count.
    3. Add `unread_count/2` (arity-2 with opts) counting rows where `read_at IS NULL` for recipient.
    4. Add `@spec unread_count(String.t(), keyword()) :: non_neg_integer()`.

    In `Chimeway`, add `def unread_count(recipient_identity, opts \\ [])` delegating to `Inbox.unread_count/2`.

    Keep `:unread_only` behaviour on struct-list path unchanged.
  </action>
  <acceptance_criteria>
    - `lib/chimeway/inbox.ex` contains `def unread_count`
    - `lib/chimeway.ex` contains `def unread_count` delegating to Inbox
    - `mix test test/chimeway/inbox_pagination_test.exs --only unread_count --warnings-as-errors` exits 0 (test added in Task 4)
  </acceptance_criteria>
  <verify>mix test test/chimeway/inbox_pagination_test.exs --only unread_count --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 3: Paginated list branch + DTO return (D-02)</name>
  <files>lib/chimeway/inbox.ex</files>
  <read_first>
    - `lib/chimeway/inbox.ex` — current `list_for_recipient/2`
    - `lib/chimeway/inbox/item.ex` — Task 1 output
    - `.planning/phases/61-inbox-headless-package/61-RESEARCH.md` — §3.2 cursor semantics
    - `.planning/phases/61-inbox-headless-package/61-UI-SPEC.md` — default limit 20
  </read_first>
  <action>
    Refactor `list_for_recipient/2`:

    **Pagination detection:** when `Keyword.has_key?(opts, :limit)` OR `:before_inserted_at` OR `:before_id` OR `Keyword.get(opts, :paginate) == true`, use paginated branch.

    **Paginated branch:**
    - `limit` default 20
    - `order_by: [desc: inserted_at, desc: id]`
    - Cursor: when both `before_inserted_at` and `before_id` present (valid UUID), filter rows older than cursor: `(inserted_at < ^ts) OR (inserted_at == ^ts AND id < ^id)` using microsecond DateTime from opt
    - Fetch `limit + 1` rows; set `has_more: length(rows) > limit`; trim to `limit`
    - Return `%{items: Enum.map(rows, &Item.to_map/1), has_more: has_more}`

    **Legacy branch:** when pagination not triggered, keep existing struct list return (`Repo.all()` on ordered query) including `:unread_only`.

    Update `@spec` to union return types or use `term()` with moduledoc explaining both shapes.
  </action>
  <acceptance_criteria>
    - Paginated call `Chimeway.list_for_recipient("user:1", limit: 2)` returns map with `:items` and `:has_more` keys
    - Non-paginated call returns list of `%Notification{}` structs (existing `inbox_query_test.exs` green)
    - `mix test test/chimeway/inbox_query_test.exs test/chimeway/inbox_pagination_test.exs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>mix test test/chimeway/inbox_query_test.exs test/chimeway/inbox_pagination_test.exs --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 4: Pagination + DTO tests (D-01, D-02, D-03)</name>
  <files>test/chimeway/inbox_pagination_test.exs, test/chimeway/inbox_query_test.exs</files>
  <read_first>
    - `test/chimeway/inbox_query_test.exs` — `insert_notification!/2`, `set_inserted_at!/2` helpers to reuse
    - `.planning/phases/61-inbox-headless-package/61-VALIDATION.md` — verification map
  </read_first>
  <action>
    Add `test/chimeway/inbox_pagination_test.exs` using `Chimeway.DataCase` (copy private helpers from inbox_query_test or extract shared helper module if duplication is large — prefer copy for minimal diff).

    Tests (tag `@tag :unread_count` on unread test):
    1. **unread_count** — 2 unread + 1 read + 1 archived unread → count 2 with default opts; count 3 with `exclude_archived: false`
    2. **DTO keys** — paginated result items include `"id"`, `"title"`, `"inserted_at"`, `"read_at"`, `"seen_at"`
    3. **has_more** — insert 3 notifications, `limit: 2` → `has_more: true`, 2 items; second page with cursor → remaining item, `has_more: false`
    4. **archived excluded** — archived notification absent from paginated list by default
    5. **title from metadata** — `metadata: %{"subject" => "Hello"}` → title `"Hello"`

    Ensure `inbox_query_test.exs` still passes without modification unless struct path regressed.
  </action>
  <acceptance_criteria>
    - `test/chimeway/inbox_pagination_test.exs` exists with at least 4 tests
    - `mix test test/chimeway/inbox_query_test.exs test/chimeway/inbox_pagination_test.exs --warnings-as-errors` exits 0
    - `mix ci.test` still passes (no new excluded tags required)
  </acceptance_criteria>
  <verify>mix test test/chimeway/inbox_query_test.exs test/chimeway/inbox_pagination_test.exs --warnings-as-errors</verify>
</task>

</tasks>

<verification>
- [ ] INBX-01 acceptance: public API exposes unread_count, paginated list with exclude_archived, stable DTO maps
- [ ] `mix test test/chimeway/inbox_* --warnings-as-errors` green
- [ ] No Phoenix imports added under `lib/chimeway/`
</verification>
