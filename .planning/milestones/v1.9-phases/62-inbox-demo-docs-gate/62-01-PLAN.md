---
phase: 62-inbox-demo-docs-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/chimeway_demo_host/mix.exs
  - examples/chimeway_demo_host/config/config.exs
  - examples/chimeway_demo_host/lib/demo_host/inbox_auth.ex
  - examples/chimeway_demo_host/lib/demo_host_web/router.ex
  - examples/chimeway_demo_host/lib/demo_host/seeds.ex
  - examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs
autonomous: true
requirements: [DEMO-08]
must_haves:
  truths:
    - "Demo host mounts chimeway_inbox at /inbox with DemoHost.InboxAuth resolving end-user recipient identity (D-03, D-04)"
    - "DemoHost.Seeds.seed_inbox/0 is adopter-copyable standalone API seeding in_app notifications (D-05)"
    - "Dedicated @moduletag :inbox proof test proves list → mark_read badge update and mark_seen via API (D-06, D-07)"
    - "Journey suite unchanged — proof isolated from @tag :journey and Logger adapter defaults (D-06)"
  artifacts:
    - path: "examples/chimeway_demo_host/lib/demo_host/inbox_auth.ex"
      provides: "ChimewayInbox.Auth behaviour for demo host"
      contains: "ChimewayInbox.Auth"
    - path: "examples/chimeway_demo_host/lib/demo_host/seeds.ex"
      provides: "seed_inbox/0 adopter-copyable seed"
      contains: "seed_inbox"
    - path: "examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs"
      provides: "DEMO-08 selective proof"
      contains: "@moduletag :inbox"
  key_links:
    - from: "examples/chimeway_demo_host/lib/demo_host_web/router.ex"
      to: "chimeway_inbox/lib/chimeway_inbox/router.ex"
      via: "chimeway_inbox_routes/0"
      pattern: "chimeway_inbox_routes"
    - from: "examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs"
      to: "examples/chimeway_demo_host/lib/demo_host/seeds.ex"
      via: "seed_inbox/0"
      pattern: "DemoHost.Seeds.seed_inbox"
---

<objective>
Deliver DEMO-08: demo host end-user inbox mount with adopter-copyable seeds and selective `:inbox` proof test — mirroring `mailglass_delivery_proof_test.exs` and `accrue_dunning_proof_test.exs` isolation patterns.

Purpose: Runnable `/inbox` bell on TeamPulse demo host proves list → mark_read (LiveView) → badge count update and mark_seen (API) for Phase 62 guide verification and `mix verify.inbox` demo lane. **D-02:** No new package API — reuse Phase 61 `chimeway_inbox` artifacts only.

Output: Demo host dep/config/auth/router mount, `seed_inbox/0`, `inbox_bell_proof_test.exs`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-inbox-demo-docs-gate/62-CONTEXT.md
@.planning/phases/62-inbox-demo-docs-gate/62-RESEARCH.md
@examples/chimeway_demo_host/lib/demo_host_web/router.ex
@examples/chimeway_demo_host/lib/demo_host/admin_auth.ex
@examples/chimeway_demo_host/lib/demo_host/seeds.ex
@examples/chimeway_demo_host/test/demo_host_web/mailglass_delivery_proof_test.exs
@examples/chimeway_demo_host/test/demo_host_web/accrue_dunning_proof_test.exs
@chimeway_inbox/lib/chimeway_inbox/router.ex
@chimeway_inbox/lib/chimeway_inbox/auth.ex
@chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs
</context>

<threat_model>
| ID | STRIDE | Threat | Severity | Mitigation |
|----|--------|--------|----------|------------|
| T-62-01 | Spoofing | `DemoHost.InboxAuth` resolves `"demo:operator"` admin actor instead of end-user recipient | high | Resolve `DemoHost.Seeds.recipient_identity/1` from dedicated session key (e.g. `"demo_user_email"`) — never reuse AdminActor identity |
| T-62-02 | Tampering | Proof lands in `journey_test.exs` and pollutes journey CI adapter config | high | Dedicated module with `@moduletag :inbox` only; moduledoc cites Mailglass/Accrue D-10 precedent |
| T-62-03 | Repudiation | `seed_inbox/0` seeds email-only notifications — bell list empty | medium | Trigger via `Chimeway.trigger/3` with `:in_app` channel (reuse `DemoHost.Notifiers.InviteSent` or inbox-specific notifier) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add chimeway_inbox dep, config, InboxAuth, and router mount (D-03, D-04)</name>
  <files>examples/chimeway_demo_host/mix.exs, examples/chimeway_demo_host/config/config.exs, examples/chimeway_demo_host/lib/demo_host/inbox_auth.ex, examples/chimeway_demo_host/lib/demo_host_web/router.ex</files>
  <read_first>
    - `examples/chimeway_demo_host/mix.exs` — `chimeway_admin` path dep pattern
    - `examples/chimeway_demo_host/config/config.exs` — `config :chimeway_admin, auth_module: DemoHost.AdminAuth`
    - `examples/chimeway_demo_host/lib/demo_host/admin_auth.ex` — permissive dev/test auth behaviour template
    - `chimeway_inbox/lib/chimeway_inbox/auth.ex` — `@callback current_recipient/2` contract
    - `chimeway_inbox/lib/chimeway_inbox/router.ex` — `chimeway_inbox_routes/0` macro and `/inbox` moduledoc
    - `examples/chimeway_demo_host/lib/demo_host_web/router.ex` — admin mount scope pattern
  </read_first>
  <action>
    In `examples/chimeway_demo_host/mix.exs` deps, add after `chimeway_admin` entry:
    `{:chimeway_inbox, path: "../../chimeway_inbox"}`

    In `examples/chimeway_demo_host/config/config.exs`, add:
    `config :chimeway_inbox, auth_module: DemoHost.InboxAuth`

    Create `examples/chimeway_demo_host/lib/demo_host/inbox_auth.ex`:
    - `@moduledoc` explaining permissive dev/test recipient resolution; production denies (mirror `DemoHost.AdminAuth` tone)
    - `@behaviour ChimewayInbox.Auth`
    - `@impl true` `current_recipient/2` reads session key `"demo_user_email"` (or equivalent) and returns `{:ok, DemoHost.Seeds.recipient_identity(email)}` when email present and env is `:dev` or `:test`
    - Return `{:error, :unauthorized}` in `:prod` or when session key absent
    - **Do not** return `"demo:operator"` — that is operator admin identity from `AdminActor` plug

    In `examples/chimeway_demo_host/lib/demo_host_web/router.ex`, add after admin scope:

    ```elixir
    scope "/inbox" do
      pipe_through :browser

      import ChimewayInbox.Router
      chimeway_inbox_routes()
    end
    ```
  </action>
  <acceptance_criteria>
    - `examples/chimeway_demo_host/mix.exs` contains `{:chimeway_inbox, path: "../../chimeway_inbox"}`
    - `examples/chimeway_demo_host/config/config.exs` contains `config :chimeway_inbox, auth_module: DemoHost.InboxAuth`
    - `examples/chimeway_demo_host/lib/demo_host/inbox_auth.ex` exists with `@behaviour ChimewayInbox.Auth`
    - `examples/chimeway_demo_host/lib/demo_host_web/router.ex` contains `scope "/inbox"` and `chimeway_inbox_routes()`
    - `cd examples/chimeway_demo_host && mix deps.get && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <automated>cd examples/chimeway_demo_host && mix deps.get && mix compile --warnings-as-errors</automated>
</task>

<task type="auto">
  <name>Task 2: Add DemoHost.Seeds.seed_inbox/0 (D-05)</name>
  <files>examples/chimeway_demo_host/lib/demo_host/seeds.ex</files>
  <read_first>
    - `examples/chimeway_demo_host/lib/demo_host/seeds.ex` — `seed_accrue_dunning/0` standalone API pattern and `alex_identity/0`
    - `examples/chimeway_demo_host/lib/demo_host/notifiers/invite_sent.ex` — `:in_app` channel + metadata `subject`
    - `lib/chimeway.ex` — public `Chimeway.trigger/3` usage in existing seeds
  </read_first>
  <action>
    Add `@doc` and `@spec seed_inbox/0` to `examples/chimeway_demo_host/lib/demo_host/seeds.ex` mirroring `seed_accrue_dunning/0`:
    - Moduledoc: DEMO-08 adopter-copyable inbox seed; standalone API not invoked from `run/0`
    - Trigger **two** unread in_app notifications for `alex_identity()` via `Chimeway.trigger/3` (reuse `DemoHost.Notifiers.InviteSent` or thin inbox notifier with `:in_app` channel)
    - Use stable idempotency keys (e.g. `"teampulse-seed-inbox-v1-a"`, `"teampulse-seed-inbox-v1-b"`) for safe re-runs
    - Return `{:ok, %{notification_ids: [...], recipient_identity: ..., trace: ...}}` with at least two notification IDs for separate mark_read and mark_seen proof targets
    - Include metadata with `subject` for bell list assertions

    Do **not** add `seed_inbox/0` to `run/0` bundle.
  </action>
  <acceptance_criteria>
    - `DemoHost.Seeds.seed_inbox/0` function exists in `seeds.ex`
    - Moduledoc states standalone API not in `run/0`
    - `iex -S mix` in demo host: `DemoHost.Seeds.seed_inbox()` returns `{:ok, %{notification_ids: ids}}` when `length(ids) >= 2`
    - Seeded notifications have `read_at` nil for alex recipient
  </acceptance_criteria>
  <automated>grep -q "def seed_inbox" examples/chimeway_demo_host/lib/demo_host/seeds.ex && grep -q "Standalone API" examples/chimeway_demo_host/lib/demo_host/seeds.ex</automated>
</task>

<task type="auto">
  <name>Task 3: Create inbox_bell_proof_test.exs with @moduletag :inbox (D-06, D-07)</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs</files>
  <read_first>
    - `examples/chimeway_demo_host/test/demo_host_web/mailglass_delivery_proof_test.exs` — `@moduletag :mailglass` moduledoc and setup pattern
    - `examples/chimeway_demo_host/test/demo_host_web/accrue_dunning_proof_test.exs` — selective tag isolation moduledoc
    - `chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs` — mark_read phx-click and badge assertions
    - `examples/chimeway_demo_host/lib/demo_host/seeds.ex` — `seed_inbox/0` and `alex_identity/0`
    - `lib/chimeway.ex` — `Chimeway.mark_seen/3` public API
  </read_first>
  <action>
    Create `examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs`:

    Module `DemoHostWeb.InboxBellProofTest` with moduledoc:
    - DEMO-08 proof: end-user inbox list → mark_read → badge count update; mark_seen via API
    - Tagged `:inbox` only — journey suite keeps default Logger adapter (D-06)

    `use DemoHostWeb.ConnCase, async: false`
    `import Phoenix.LiveViewTest`
    `@moduletag :inbox`

    **Test A — "DEMO-08 list, mark_read, and badge update":**
    1. `assert {:ok, result} = DemoHost.Seeds.seed_inbox/0`
    2. Build conn with `Phoenix.ConnTest.init_test_session/2` setting `"demo_user_email" => DemoHost.Seeds` alex email constant (match InboxAuth session key)
    3. `{:ok, view, html} = live(conn, "/inbox")`
    4. Assert badge shows unread count (e.g. `data-cw-inbox-badge`, "2 unread" or equivalent)
    5. Open panel via `phx-click="toggle_panel"` or bell button click
    6. Click `phx-click="mark_read"` on first notification row
    7. Assert badge decrements (hidden at 0 or "1 unread" depending on remaining items)
    8. Assert `Repo.get!(Notification, id).read_at` set

    **Test B — "DEMO-08 mark_seen via host API":**
    1. Seed via `seed_inbox/0`
    2. Pick unread notification ID from result (second ID if first used in Test A, or fresh seed)
    3. `assert :ok = Chimeway.mark_seen(notification_id, DemoHost.Seeds.alex_identity())`
    4. Assert `Repo.get!(Notification, id).seen_at` is set
    5. **Do not** assert badge change — unread badge keys off `read_at` nil (D-07)

    No `@tag :journey`. No Mailglass adapter setup.
  </action>
  <acceptance_criteria>
    - File `examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs` exists
    - Module contains `@moduletag :inbox`
    - Moduledoc contains "Tagged `:inbox` only"
    - Test A uses `live(conn, "/inbox")` and `phx-click="mark_read"`
    - Test B calls `Chimeway.mark_seen/3`
    - `cd examples/chimeway_demo_host && mix test --only inbox --warnings-as-errors` exits 0 with at least 2 tests
  </acceptance_criteria>
  <automated>cd examples/chimeway_demo_host && mix test --only inbox --warnings-as-errors</automated>
</task>

</tasks>

<verification>
- [ ] `cd examples/chimeway_demo_host && mix test --only inbox --warnings-as-errors` green
- [ ] `cd examples/chimeway_demo_host && mix test --only journey --warnings-as-errors` still green (journey isolation)
- [ ] `/inbox` route reachable; InboxAuth does not return `"demo:operator"`
</verification>

<success_criteria>
- DEMO-08: demo host mounts end-user inbox with selective proof test
- `seed_inbox/0` adopter-copyable and documented standalone
- Proof isolated from journey suite via `@moduletag :inbox`
</success_criteria>
