---
phase: 62-inbox-demo-docs-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .github/workflows/ci.yml
  - test/chimeway/release_gate_contract_test.exs
  - MAINTAINING.md
autonomous: true
requirements: [GATE-05 (Inbox)]
must_haves:
  truths:
    - "mix verify.inbox alias runs chimeway_inbox package tests + demo host --only inbox (D-16)"
    - "verify_inbox CI job runs mix verify.inbox with Postgres — no sibling checkout (D-17)"
    - "release_gate_contract_test.exs documents eight-gate pre-ship and nine ci-gate lanes (D-18)"
    - "MAINTAINING pre-ship checklist documents mix verify.inbox as eighth gate (D-19)"
    - "Existing journey/mailglass/accrue verify jobs unchanged — additive CI only (ROADMAP SC #3)"
  artifacts:
    - path: "mix.exs"
      provides: "verify.inbox alias"
      contains: "verify.inbox"
    - path: ".github/workflows/ci.yml"
      provides: "verify_inbox job"
      contains: "verify_inbox"
    - path: "MAINTAINING.md"
      provides: "Pre-ship octet including inbox gate"
      contains: "mix verify.inbox"
    - path: "test/chimeway/release_gate_contract_test.exs"
      provides: "GATE-05 inbox parity contract"
      contains: "verify.inbox"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "mix.exs"
      via: "mix verify.inbox command"
      pattern: "mix verify.inbox"
    - from: "MAINTAINING.md"
      to: ".github/workflows/ci.yml"
      via: "Maintainer runs same command locally"
      pattern: "verify.inbox"
    - from: "test/chimeway/release_gate_contract_test.exs"
      to: "MAINTAINING.md"
      via: "eight-gate assertion"
      pattern: "All eight must pass"
---

<objective>
Deliver GATE-05 (Inbox): formal `mix verify.inbox` alias, `verify_inbox` CI job, release gate parity contract update, and MAINTAINING.md pre-ship octet — without sibling checkout (unlike Accrue).

Purpose: Inbox integration gate runs in CI reproducibly; maintainers have documented pre-ship command parity with existing verify entrypoints. **D-01:** Wave 1 parallel with 62-01. **Note:** `mix verify.inbox` demo lane passes only after 62-01 proof test lands — acceptable Wave 1 parallel if both merge before phase sign-off.

Output: `verify.inbox` alias, CI job, release_gate_contract octet/nine-lane parity, MAINTAINING eight-gate checklist.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-inbox-demo-docs-gate/62-CONTEXT.md
@.planning/phases/62-inbox-demo-docs-gate/62-RESEARCH.md
@mix.exs
@.github/workflows/ci.yml
@test/chimeway/release_gate_contract_test.exs
@MAINTAINING.md
</context>

<threat_model>
| ID | STRIDE | Threat | Severity | Mitigation |
|----|--------|--------|----------|------------|
| T-62-07 | Tampering | CI job adds Accrue-style sibling checkout for inbox gate | high | Mirror verify_mailglass only — in-repo path deps; no `ACCRUE_PATH` or external checkout |
| T-62-08 | Denial of Service | Broken verify_inbox job blocks all PRs via ci-gate | medium | Mirror proven verify_mailglass job structure; same Postgres service block |
| T-62-09 | Repudiation | MAINTAINING checklist out of sync with CI jobs or release_gate_contract | high | Update MAINTAINING, ci.yml, and release_gate_contract_test.exs in same plan |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add verify.inbox alias to mix.exs (D-16)</name>
  <files>mix.exs</files>
  <read_first>
    - `mix.exs` — `verify.mailglass` and `verify.accrue` alias blocks
    - `mix.exs` — `verify.example` chimeway_inbox lane (line ~88 comment)
    - `.planning/phases/62-inbox-demo-docs-gate/62-CONTEXT.md` — D-16 alias composition
  </read_first>
  <action>
    In root `mix.exs` aliases, add after `verify.accrue` block:

    ```elixir
    # v1.9 GATE-05 Inbox: chimeway_inbox package + demo host DEMO-08 :inbox proof
    "verify.inbox": [
      "cmd --shell cd chimeway_inbox && mix deps.get && mix test --warnings-as-errors",
      "cmd --shell cd examples/chimeway_demo_host && mix deps.get && mix test --only inbox --warnings-as-errors"
    ]
    ```

    Remove or update the Phase 62 deferral comment on `verify.example` chimeway_inbox lane if redundant (keep verify.example unchanged otherwise).

    Do **not** add root `mix test --only inbox` lane — core inbox API stays in default CI.
    Do **not** add Accrue-style sibling checkout env vars.
  </action>
  <acceptance_criteria>
    - `mix.exs` contains `"verify.inbox": [` alias with two cmd steps
    - First step runs `cd chimeway_inbox && mix test --warnings-as-errors`
    - Second step runs `cd examples/chimeway_demo_host && mix test --only inbox --warnings-as-errors`
    - `verify.mailglass` and `verify.accrue` aliases unchanged
    - `mix verify.inbox --warnings-as-errors` exits 0 once 62-01 proof test exists (may fail until then)
  </acceptance_criteria>
  <automated>grep -q '"verify.inbox":' mix.exs && grep -A5 '"verify.inbox":' mix.exs | grep -q chimeway_inbox && grep -A8 '"verify.inbox":' mix.exs | grep -q '\-\-only inbox'</automated>
</task>

<task type="auto">
  <name>Task 2: Add verify_inbox CI job and ci-gate aggregation (D-17)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` — `verify_mailglass` job (full block)
    - `.github/workflows/ci.yml` — `ci-gate` job needs array and env loop
    - `mix.exs` — `verify.inbox` alias from Task 1
  </read_first>
  <action>
    Add new job `verify_inbox` immediately after `verify_accrue` in `.github/workflows/ci.yml`.

    Mirror `verify_mailglass` structure exactly:
    - `name: Inbox integration gate`
    - `runs-on: ubuntu-latest`
    - Same `postgres:15` service block
    - Same `MIX_ENV: test` and `DATABASE_URL: postgres://postgres:postgres@localhost/chimeway_test`
    - Same checkout/beam/cache/deps.get/ecto.create/ecto.migrate action SHA pins as verify_mailglass
    - Final step: `mix verify.inbox`
    - Cache key: `${{ runner.os }}-mix-verify-inbox-${{ hashFiles('**/mix.lock') }}`

    Update `ci-gate` job:
    - Add `verify_inbox` to `needs:` array after `verify_accrue`
    - Add env var `VERIFY_INBOX: ${{ needs.verify_inbox.result }}`
    - Add `VERIFY_INBOX` to the lane-check loop (same pattern as `VERIFY_ACCRUE`)

    Do **not** add sibling repo checkout steps.
    Do **not** modify `verify_mailglass`, `verify_accrue`, or default `ci` job.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains job id `verify_inbox`
    - Job runs `mix verify.inbox` after ecto.migrate
    - Job does **not** contain `szTheory/accrue` or `ACCRUE_PATH`
    - `ci-gate` needs array includes `verify_inbox`
    - `ci-gate` env loop includes `VERIFY_INBOX`
    - `verify_mailglass` job unchanged
  </acceptance_criteria>
  <automated>grep -q verify_inbox .github/workflows/ci.yml && grep -q mix\ verify.inbox .github/workflows/ci.yml && ! grep -A30 'verify_inbox:' .github/workflows/ci.yml | grep -q ACCRUE_PATH</automated>
</task>

<task type="auto">
  <name>Task 3: Update release_gate_contract and MAINTAINING octet (D-18, D-19)</name>
  <files>test/chimeway/release_gate_contract_test.exs, MAINTAINING.md</files>
  <read_first>
    - `test/chimeway/release_gate_contract_test.exs` — `@pre_ship_verify_commands`, `@ci_gate_lanes`, seven-gate test, GATE-06 eight-lane test
    - `MAINTAINING.md` — pre-ship command block and seven-gate language
    - `.planning/phases/62-inbox-demo-docs-gate/62-CONTEXT.md` — D-18, D-19
  </read_first>
  <action>
    In `test/chimeway/release_gate_contract_test.exs`:

    1. Add `verify_inbox` to `@ci_gate_lanes` after `verify_accrue` (nine lanes total)
    2. Add `{"verify.inbox", "verify_inbox", "mix verify.inbox"}` to `@pre_ship_verify_commands`
    3. Rename/update test `"MAINTAINING documents seven-gate pre-ship requirement"` → assert `All eight must pass` (not seven)
    4. Update GATE-06 test `"ci-gate aggregates 8 required lanes"` → `"ci-gate aggregates 9 required lanes"` with nine `@ci_gate_lanes`

    In `MAINTAINING.md` pre-ship block:
    - Change "Run all seven" → "Run all eight"
    - Add after `mix verify.accrue`:
      ```bash
      mix verify.inbox
      ```
    - Add bullet:
      `- mix verify.inbox — Inbox integration gate (GATE-05 Inbox): chimeway_inbox package tests and DEMO-08 demo host :inbox proof; in-repo path deps only — no sibling checkout`
    - Change "All seven must pass" → "All eight must pass before publishing."

    Do **not** remove or alter existing verify command descriptions except count language.
  </action>
  <acceptance_criteria>
    - `release_gate_contract_test.exs` `@pre_ship_verify_commands` includes `verify.inbox` tuple
    - `@ci_gate_lanes` includes `verify_inbox` (9 items)
    - Test asserts "All eight must pass" in MAINTAINING
    - GATE-06 test asserts 9 ci-gate lanes
    - `MAINTAINING.md` contains `mix verify.inbox` in pre-ship bash block
    - `MAINTAINING.md` contains "All eight must pass"
    - `mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <automated>mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors</automated>
</task>

</tasks>

<verification>
- [ ] `grep verify_inbox .github/workflows/ci.yml` shows single new job
- [ ] `mix verify.inbox --warnings-as-errors` green after 62-01 completes
- [ ] `mix ci.verify_gates --warnings-as-errors` green after release_gate_contract update
- [ ] No changes to `verify.accrue` alias or Accrue sibling checkout
</verification>

<success_criteria>
- GATE-05 (Inbox): CI job + MAINTAINING octet + release gate parity complete
- Existing seven verify commands still documented and unchanged in behavior
- ci-gate aggregates nine lanes including verify_inbox
</success_criteria>
