# Phase 62.1: Address v1.9 tech debt — Context

**Gathered:** 2026-05-30 (assumptions mode)
**Status:** Ready for planning

<domain>
## Phase Boundary

Close v1.9 milestone **process debt** flagged by the v1.9 milestone audit (`tech_debt` status): retroactive Nyquist validation sign-off for phases 58, 59, 60.1, 61, and 62, plus REQUIREMENTS.md traceability table sync for Accrue DOCS-08/09.

**In scope:**
- Re-run automated verify commands from each target phase's `VALIDATION.md` Per-Task map; update per-task rows to green; set frontmatter `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete`
- Correct Phase 58 VALIDATION.md content drift (Accrue 1.3+ `pending_signals == []` model; Wave 0 artifacts already shipped)
- Sync `.planning/REQUIREMENTS.md` traceability: `DOCS-08 (Accrue)` and `DOCS-09 (Accrue)` → `Complete`
- Fix Phase 60.1 planning artifact lane-count drift (8 → 9 ci-gate lanes) during 60.1 Nyquist closure

**Out of scope:**
- `lib/chimeway/` or application code changes
- New functional requirements or tests (Wave 0 already shipped in target phases)
- Full `/gsd-validate-phase` gap-filling test generation (Phase 53 retroactive pattern applies)
- Phases 54–57 Nyquist metadata lag (v1.8 optional retroactive — deferred backlog)
- Phase 60 (already `nyquist_compliant: true`)
- Optional SUMMARY.md `requirements_completed` frontmatter (audit lists as optional; defer unless explicitly added to a plan)
- Hex 1.1.0 publish E2E, `mark_seen` BellDropdownLive wiring (INT-03), inbox-read timeline UI (INT-02)

**Depends on:** Phase 62 complete (v1.9 implementation verified)

**Source:** `.planning/milestones/v1.9-MILESTONE-AUDIT.md` — requirements 10/10 satisfied; Nyquist `overall: partial` (1/6 compliant)
</domain>

<decisions>
## Implementation Decisions

### Work type (Phase 53 precedent)
- **D-01:** Retroactive Nyquist closure is **documentation + command re-run only** — mirror Phase 53 (`53-CONTEXT.md`, `53-01-PLAN.md`). No engine changes, no new ExUnit files unless a re-run exposes a genuine regression (unlikely; all target phases have `VERIFICATION.md` with `passed` status).

### Nyquist target phases
- **D-02:** Sign off phases **58, 59, 60.1, 61, 62** only. **Exclude Phase 60** (already compliant). **Exclude Phases 54–57** (v1.8 deferred optional retroactive per `STATE.md` Deferred Items).

### Per-phase closure procedure
- **D-03:** For each target phase: (1) read `*-VALIDATION.md` Per-Task Verification Map; (2) re-run listed `Automated Command` values; (3) flip Status → ✅ green; (4) check Wave 0 boxes where artifacts exist on disk; (5) complete Validation Sign-Off checkboxes; (6) set frontmatter `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete`.
- **D-04:** Primary verify commands by spine:
  - Accrue (58–60): `ACCRUE_PATH="${ACCRUE_PATH:-../accrue/accrue}" mix verify.accrue --warnings-as-errors`
  - Docs/gates (60, 62): `mix ci.verify_gates`
  - Hex pipeline (60.1): structural checks per `60.1-VALIDATION.md` + `mix ci.verify_gates`
  - Inbox (61, 62): `mix verify.inbox`

### Phase 58 content correction
- **D-05:** Phase 58 VALIDATION.md must **correct** task 58-02-03 Secure Behavior text: Accrue 1.3+ model uses `pending_signals == []` while waiting (termination via `cancel_campaign/3` → `Chimeway.Signal.track/4`), **not** `pending_signals: ["invoice.paid"]`. Mark Wave 0 checkboxes checked and `File Exists` ✅ — artifacts delivered in 58-01/02/03 execution.
- **D-06:** Do **not** invoke full `/gsd-validate-phase 58` test-generation path unless a re-run fails — prefer Phase 53-style metadata + command closure.

### REQUIREMENTS traceability
- **D-07:** Update `.planning/REQUIREMENTS.md` traceability table rows:
  - `DOCS-08 (Accrue) | Phase 60 | Pending` → `Complete`
  - `DOCS-09 (Accrue) | Phase 60 | Pending` → `Complete`
  Evidence: Phase 60 `VERIFICATION.md` passed; inbox counterparts already `Complete`; v1.9 requirement bullets already `[x]`.

### 60.1 ci-gate lane-count hygiene
- **D-08:** During 60.1 Nyquist closure, update stale **8-lane** references to **9-lane** ci-gate in `60.1-VALIDATION.md` (task 60.1-01-02) and `60.1-VERIFICATION.md` prose. Runtime truth: `.github/workflows/ci.yml` `ci-gate` needs includes `verify_inbox`; `release_gate_contract_test.exs` asserts 9 lanes.

### Claude's Discretion
- Plan wave split (single plan vs per-phase plans vs Accrue-spine + Inbox-spine + hygiene plan)
- Whether to update v1.9-MILESTONE-AUDIT.md Nyquist section inline or leave for `/gsd-audit-milestone` re-run
- Exact command subset per phase when VALIDATION.md rows reference overlapping commands (dedupe re-runs across plans)
- Optional SUMMARY frontmatter — omit unless a plan explicitly adds it
</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

- `.planning/milestones/v1.9-MILESTONE-AUDIT.md` — Tech debt inventory and Nyquist partial phase list (source of truth for 62.1 scope)
- `.planning/milestones/v1.7-phases/53-milestone-close-out-nyquist-validation-journey-test-hygiene/53-CONTEXT.md` — Retroactive Nyquist closure precedent (no code changes)
- `.planning/milestones/v1.7-phases/53-milestone-close-out-nyquist-validation-journey-test-hygiene/53-01-PLAN.md` — Execution pattern: re-run commands, update VALIDATION.md rows + frontmatter
- `.planning/REQUIREMENTS.md` — Traceability table to sync (DOCS-08/09 Accrue Pending rows)
- `.planning/phases/58-accrue-dunning-core/58-VALIDATION.md` — Draft; Accrue 1.3+ row correction needed
- `.planning/phases/59-accrue-blueprint-demo/59-VALIDATION.md` — Draft Nyquist sign-off target
- `.planning/phases/60.1-hex-release-pipeline/60.1-VALIDATION.md` — Draft; 8→9 lane correction needed
- `.planning/phases/61-inbox-headless-package/61-VALIDATION.md` — Draft Nyquist sign-off target
- `.planning/phases/62-inbox-demo-docs-gate/62-VALIDATION.md` — Draft Nyquist sign-off target
- `.planning/phases/60-accrue-docs-release-gate/60-VALIDATION.md` — Compliant reference (`nyquist_compliant: true`)
- `.github/workflows/ci.yml` — 9-lane ci-gate runtime truth
- `test/chimeway/release_gate_contract_test.exs` — Pre-ship verify + ci-gate lane parity contracts
- `AGENTS.md` — Docs/release-gate phases accept `mix ci.verify_gates` evidence; skip `/gsd-verify-work`
</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `mix verify.accrue` — Accrue spine integration proof (Phases 58–60)
- `mix verify.inbox` — Inbox package + demo proof (Phases 61–62)
- `mix ci.verify_gates` — Doc-contract + release-gate parity (Phases 60, 62, 60.1)
- Per-phase `*-VERIFICATION.md` — All v1.9 phases `passed`; commands already validated at execution time
- Phase 60 `60-VALIDATION.md` — Template for compliant frontmatter and sign-off checklist

### Established Patterns
- Phase 53 retroactive Nyquist: re-run VALIDATION.md commands → update metadata only
- v1.9 Accrue 1.3+ termination: `cancel_campaign/3` emits signal; `wait_until` does not use `cancel_signals`; `pending_signals == []` while waiting
- Milestone audit 3-source cross-reference: VERIFICATION + REQUIREMENTS + optional SUMMARY frontmatter

### Integration Points
- Nyquist closure updates `.planning/phases/*/` artifacts only — no runtime integration
- REQUIREMENTS traceability sync is a single-table edit in `.planning/REQUIREMENTS.md`
- Post-62.1: `/gsd-audit-milestone` re-run should show Nyquist `overall: compliant` (or accept remaining optional debt explicitly)
</code_context>

<specifics>
## Specific Ideas

- Follow Phase 53 plan structure: grouped plans by phase cluster (Accrue 58–59, docs/gates 60.1, inbox 61–62) or single milestone-close-out plan — planner decides wave split
- Phase 58 row correction is a **content fix**, not a behavioral change — align VALIDATION prose with `accrue_dunning_lifecycle_test.exs` assertions and v1.9 audit Accrue 1.3+ model description
</specifics>

<deferred>
## Deferred Ideas

- **Phases 54–57 Nyquist metadata lag** — v1.8 optional retroactive; separate phase if desired
- **SUMMARY.md `requirements_completed` frontmatter** — optional 3-source audit enhancement; not blocking v1.9 close
- **Hex 1.1.0 publish E2E** — human push + bootstrap Release PR; not 62.1 scope
- **`mark_seen` BellDropdownLive wiring (INT-03)** — product polish deferred from Phase 61
- **Inbox-read on delivery timeline UI (INT-02)** — optional integration polish

None — analysis stayed within phase scope for mandatory work; items above explicitly deferred from milestone audit.
</deferred>
