# Phase 62.1 Research — v1.9 Tech Debt Closure

**Researched:** 2026-05-30  
**Phase:** 62.1 — Address v1.9 tech debt (Nyquist + REQUIREMENTS traceability)

## RESEARCH COMPLETE

## Summary

Phase 62.1 closes **process debt only** — no `lib/chimeway/` changes. The v1.9 milestone audit (`status: tech_debt`) reports 10/10 requirements satisfied but Nyquist `overall: partial` because phases 58, 59, 60.1, 61, and 62 retain draft `VALIDATION.md` with `nyquist_compliant: false` despite green `VERIFICATION.md` and passing verify commands at execution time.

**Precedent:** Phase 53 (`53-01-PLAN.md`) retroactively re-ran Per-Task `Automated Command` values and flipped metadata — no new ExUnit files.

**Fresh command evidence (2026-05-30):**

```bash
mix ci.verify_gates                              # 264 tests, 0 failures
ACCRUE_PATH="${ACCRUE_PATH:-../accrue/accrue}" mix verify.accrue --warnings-as-errors
mix verify.inbox --warnings-as-errors
```

## Target Phase Inventory

| Phase | VALIDATION status | Primary closure command | Special work |
|-------|-------------------|-------------------------|--------------|
| 58 | draft, wave_0 false | `mix verify.accrue` | Fix 58-02-03 Secure Behavior: `pending_signals == []` (Accrue 1.3+); mark Wave 0 ✅ (artifacts exist) |
| 59 | draft | `mix verify.accrue` | Mark Wave 0 checkboxes checked (delivered in 59-01/02) |
| 60 | **compliant** | — | **Exclude** per D-02 |
| 60.1 | draft | `mix ci.verify_gates` | 8→9 lane prose in VALIDATION + VERIFICATION (D-08) |
| 61 | draft | Core inbox tests + `cd chimeway_inbox && mix test` | File Exists → ✅ where tests exist |
| 62 | draft | `mix verify.inbox` + `mix ci.verify_gates` | Doc/gate rows green |

## Phase 58 Content Correction (D-05)

Row **58-02-03** currently says:

> `wait_until` sets `pending_signals: ["invoice.paid"]`

**Correct (Accrue 1.3+):** `wait_until` leaves `pending_signals == []` while waiting; termination via `cancel_campaign/3` → `Chimeway.Signal.track/4` with `invoice.paid`. Evidence: `accrue_dunning_lifecycle_test.exs`, v1.9 audit Accrue spine description.

## REQUIREMENTS Traceability (D-07)

`.planning/REQUIREMENTS.md` traceability table:

| Row | Current | Target |
|-----|---------|--------|
| DOCS-08 (Accrue) \| Phase 60 | Pending | Complete |
| DOCS-09 (Accrue) \| Phase 60 | Pending | Complete |

Inbox rows (Phase 62) already `Complete`. v1.9 requirement bullets already `[x]`.

## 60.1 ci-gate Lane Count (D-08)

Runtime (post-Phase 62): `.github/workflows/ci.yml` `ci-gate` `needs` lists **9** jobs including `verify_inbox`. `release_gate_contract_test.exs` asserts nine `@ci_gate_lanes`.

Stale artifacts cite **8** lanes in `60.1-VALIDATION.md` task 60.1-01-02 and `60.1-VERIFICATION.md` prose — update to 9 without changing CI behavior.

## Recommended Plan Split

Three parallel Wave-1 plans (no `lib/` conflicts):

1. **62.1-01** — Accrue spine: phases 58 + 59  
2. **62.1-02** — Hex/docs hygiene: phase 60.1 + REQUIREMENTS.md  
3. **62.1-03** — Inbox spine: phases 61 + 62  

Optional post-close: re-run `/gsd-audit-milestone` for v1.9 — out of 62.1 execute scope unless added to a plan task.

## Out of Scope (Confirmed)

- Phases 54–57 Nyquist lag (v1.8 deferred)
- `/gsd-validate-phase` test generation (unless re-run fails)
- SUMMARY `requirements_completed` frontmatter
- Hex 1.1.0 E2E publish, INT-02/INT-03 polish

## Validation Architecture

Phase 62.1 validates **planning artifact closure**, not new product behavior.

| Property | Value |
|----------|-------|
| **Framework** | ExUnit via existing `mix verify.*` and `mix ci.verify_gates` |
| **Quick run** | `mix ci.verify_gates` |
| **Full pre-close** | Accrue + inbox verify commands (deduped once per spine in execute) |
| **Success signal** | Target phase `*-VALIDATION.md` frontmatter `nyquist_compliant: true`; REQUIREMENTS rows `Complete`; `rg 'nyquist_compliant: false' .planning/phases/5[89]-* .planning/phases/60.1-* .planning/phases/6[12]-*` empty |

**Per-plan verify:**

| Plan | Command block |
|------|---------------|
| 62.1-01 | `ACCRUE_PATH="${ACCRUE_PATH:-../accrue/accrue}" mix verify.accrue --warnings-as-errors` |
| 62.1-02 | `mix ci.verify_gates` |
| 62.1-03 | `mix verify.inbox --warnings-as-errors` then `mix ci.verify_gates` |

**Blocking criteria:** Any re-run failure blocks metadata sign-off — investigate regression before flipping rows green.

## Canonical References

- `.planning/milestones/v1.9-MILESTONE-AUDIT.md`
- `.planning/milestones/v1.7-phases/53-milestone-close-out-nyquist-validation-journey-test-hygiene/53-01-PLAN.md`
- `.planning/phases/60-accrue-docs-release-gate/60-VALIDATION.md` (compliant template)
- `AGENTS.md` — docs/release-gate phases: `mix ci.verify_gates` evidence; skip `/gsd-verify-work`
