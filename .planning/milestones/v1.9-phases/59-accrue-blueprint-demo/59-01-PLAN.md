---
phase: 59-accrue-blueprint-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - examples/chimeway_demo_host/mix.exs
  - examples/chimeway_demo_host/mix.lock
  - examples/chimeway_demo_host/test/test_helper.exs
  - examples/chimeway_demo_host/test/support/accrue_fixtures.ex
  - examples/chimeway_demo_host/lib/demo_host/seeds.ex
  - examples/chimeway_demo_host/test/demo_host_web/accrue_dunning_proof_test.exs
autonomous: true
requirements: [DEMO-07]
must_haves:
  truths:
    - "Demo host proves Accrue-driven dunning via billing events — not direct Chimeway.trigger/3 from host glue (D-05)"
    - "Failed payment triggers initial Logger email; workflow enters :waiting with pending_signals [\"invoice.paid\"]; invoice.paid terminates before escalation (D-07)"
    - "Operator trace at /admin/chimeway shows accrue.dunning workflow progression and signal_received (D-08)"
    - "@moduletag :accrue tests isolated from :journey suite (D-03)"
    - "mix verify.accrue runs root and demo host :accrue tests (D-04)"
  artifacts:
    - path: "examples/chimeway_demo_host/test/demo_host_web/accrue_dunning_proof_test.exs"
      provides: "DEMO-07 proof with admin trace"
      contains: "@moduletag :accrue"
    - path: "examples/chimeway_demo_host/lib/demo_host/seeds.ex"
      provides: "Adopter-copyable seed_accrue_dunning/0"
      contains: "seed_accrue_dunning"
    - path: "mix.exs"
      provides: "verify.accrue includes demo host"
      contains: "chimeway_demo_host"
  key_links:
    - from: "examples/chimeway_demo_host/lib/demo_host/seeds.ex"
      to: "Accrue.Test.trigger_event"
      via: "seed_accrue_dunning uses Accrue billing events"
      pattern: "trigger_event"
    - from: "examples/chimeway_demo_host/test/demo_host_web/accrue_dunning_proof_test.exs"
      to: "/admin/chimeway"
      via: "LiveView trace search"
      pattern: "trace-search-form"
---

<objective>
Deliver DEMO-07: demo host Accrue dunning end-to-end proof with operator trace inspectability, selective `:accrue` CI lane, and extended `mix verify.accrue` alias — without re-proving ECOS-06 in Chimeway root tests (D-02).

Purpose: Runnable proof that adopters can reproduce locally before the blueprint recipe references it (Wave 59-02 depends on this wave).

Output: Wired demo host Accrue dep, TestRepo bootstrap, `DemoHost.Seeds.seed_accrue_dunning/0`, `AccrueDunningProofTest`, updated root `verify.accrue` alias.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-accrue-blueprint-demo/59-CONTEXT.md
@.planning/phases/59-accrue-blueprint-demo/59-RESEARCH.md
@.planning/phases/59-accrue-blueprint-demo/59-VALIDATION.md
@.planning/phases/58-accrue-dunning-core/58-VERIFICATION.md
@AGENTS.md

@mix.exs
@examples/chimeway_demo_host/mix.exs
@examples/chimeway_demo_host/test/test_helper.exs
@examples/chimeway_demo_host/test/demo_host_web/mailglass_delivery_proof_test.exs
@test/chimeway/integrations/accrue_dunning_lifecycle_test.exs
@test/support/accrue/fixtures.ex
</context>

<threat_model>
| ID | STRIDE | Threat | Severity | Mitigation |
|----|--------|--------|----------|------------|
| T-59-01 | Denial of Service | Accrue tests slow or break default demo host `mix test` | medium | `@moduletag :accrue` only; do not tag journey tests; optional accrue dep |
| T-59-02 | Tampering | Demo uses direct Chimeway.trigger bypassing Accrue events | high | seed and tests must call `Accrue.Test.trigger_event/2` (or public Accrue API) |
| T-59-03 | Repudiation | Admin trace cannot explain dunning termination | medium | Assert `signal_received` and workflow_key in admin search results |
| T-59-04 | Elevation | Accrue TestRepo affects production config | low | Test-only bootstrap in test_helper; sandbox checkout per test |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Demo host optional Accrue dep + verify.accrue extension (D-04, D-09)</name>
  <files>examples/chimeway_demo_host/mix.exs, examples/chimeway_demo_host/mix.lock, mix.exs</files>
  <read_first>
    - `mix.exs` — `accrue_dep/0`, `verify.mailglass` alias (lines 99–108), `verify.accrue` (lines 105–108)
    - `examples/chimeway_demo_host/mix.exs` — current deps (no accrue yet)
    - `.planning/phases/59-accrue-blueprint-demo/59-CONTEXT.md` — D-04, D-09
  </read_first>
  <action>
    In `examples/chimeway_demo_host/mix.exs` `deps/0`, add Accrue optional dep using a private `accrue_dep/0` helper identical to root pattern:

    - `nil` ACCRUE_PATH → `{:accrue, "~> 1.2", optional: true, runtime: false}`
    - `ACCRUE_PATH` set → `{:accrue, path: path, optional: true, runtime: false}`

    Comment: `# Local dev: ACCRUE_PATH=../../accrue/accrue mix deps.get`

    In root `mix.exs`, extend `"verify.accrue"` alias to append demo host step (mirror `verify.mailglass`):

    ```elixir
    "cmd --shell cd examples/chimeway_demo_host && mix deps.get && mix test --only accrue --warnings-as-errors"
    ```

    Run `mix deps.get` in demo host when Accrue path available.
  </action>
  <acceptance_criteria>
    - `examples/chimeway_demo_host/mix.exs` contains `:accrue` in deps list
    - `grep chimeway_demo_host mix.exs` matches verify.accrue alias block
    - `mix verify.accrue` command list includes demo host cd step (may fail until tests exist — Task 4)
  </acceptance_criteria>
  <automated>grep -n "chimeway_demo_host" mix.exs && grep -n ":accrue" examples/chimeway_demo_host/mix.exs</automated>
</task>

<task type="auto">
  <name>Task 2: Accrue TestRepo bootstrap in demo test_helper (D-09)</name>
  <files>examples/chimeway_demo_host/test/test_helper.exs</files>
  <read_first>
    - `examples/chimeway_demo_host/test/test_helper.exs` — Mailglass TestRepo block (lines 13–41)
    - `test/test_helper.exs` — Chimeway root Accrue bootstrap
    - `test/support/accrue/` — migrations path used by root
  </read_first>
  <action>
    After the existing Mailglass block in demo `test_helper.exs`, add conditional Accrue bootstrap when `Code.ensure_loaded?(Accrue)`:

    1. `Application.ensure_all_started(:accrue)`
    2. Resolve migrations from `Path.expand("../../../test/support/accrue/migrations", __DIR__)` (Chimeway repo test support — same as root)
    3. `Ecto.Adapters.Postgres.storage_up` for `Accrue.TestRepo` config
    4. Run migrator with `Ecto.Migrator.with_repo/2`
    5. `Accrue.TestRepo.start_link/0`
    6. `Ecto.Adapters.SQL.Sandbox.mode(Accrue.TestRepo, :manual)`

    Do not start Accrue when dep not loaded — block must be fully gated.
  </action>
  <acceptance_criteria>
    - `test_helper.exs` contains `Code.ensure_loaded?(Accrue)` guard
    - `test_helper.exs` contains `Accrue.TestRepo` and `Sandbox.mode(Accrue.TestRepo`
    - With ACCRUE_PATH set, `cd examples/chimeway_demo_host && mix test --only accrue` does not raise TestRepo missing (after Task 3–4)
  </acceptance_criteria>
  <automated>grep -n "Accrue.TestRepo" examples/chimeway_demo_host/test/test_helper.exs</automated>
</task>

<task type="auto">
  <name>Task 3: Demo accrue fixtures + seed_accrue_dunning/0 (D-05, D-06, D-10)</name>
  <files>examples/chimeway_demo_host/test/support/accrue_fixtures.ex, examples/chimeway_demo_host/lib/demo_host/seeds.ex</files>
  <read_first>
    - `test/support/accrue/fixtures.ex` — `configure_chimeway_dunning_engine!/0`, `configure_chimeway_logger_adapter!/0`, `trigger_invoice_payment_failed_event!/4`, `trigger_invoice_paid_event!/4`, progression helpers
    - `examples/chimeway_demo_host/lib/demo_host/seeds.ex` — `seed_escalation_waiting/0`, `trigger/3` private pattern
    - `test/chimeway/integrations/accrue_dunning_lifecycle_test.exs` — `start_dunning_and_wait!/4` flow
  </read_first>
  <action>
    Create `examples/chimeway_demo_host/test/support/accrue_fixtures.ex` (compile only in test via existing `elixirc_paths(:test)`). Copy minimal helpers from Chimeway `test/support/accrue/fixtures.ex` needed for demo proof: engine config, Logger adapter, customer/subscription/invoice inserts, event triggers, `start_dunning_and_wait!/4`, `drain_initial_email_delivery!/1`.

    Use deterministic demo customer email `accrue.demo@teampulse.test` for admin search.

    In `DemoHost.Seeds`, add public `seed_accrue_dunning/0` that:

    1. Requires Accrue loaded (return `{:error, :accrue_not_available}` if not)
    2. Configures dunning engine + Logger adapter
    3. Inserts customer/subscription/failed invoice
    4. Calls `Accrue.Test.trigger_event` for `invoice.payment_failed`
    5. Drains initial email and progresses to `:waiting`
    6. Returns `{:ok, %{customer_id:, workflow_run_id:, recipient_identity:, trace: %{delivery_ids:}}}`

    Do **not** add to `Seeds.run/0` (standalone API per D-10 discretion).

    Document in `@moduledoc` that seed uses Accrue billing events, not `Chimeway.trigger/3`.
  </action>
  <acceptance_criteria>
    - `DemoHost.Seeds` defines `seed_accrue_dunning/0`
    - `accrue_fixtures.ex` defines `configure_chimeway_dunning_engine!/0`
    - `seeds.ex` or fixtures call `Accrue.Test.trigger_event` (grep match)
    - `Seeds.run/0` does not call `seed_accrue_dunning`
  </acceptance_criteria>
  <automated>grep -n "def seed_accrue_dunning" examples/chimeway_demo_host/lib/demo_host/seeds.ex && grep -n "trigger_event" examples/chimeway_demo_host/lib/demo_host/seeds.ex examples/chimeway_demo_host/test/support/accrue_fixtures.ex</automated>
</task>

<task type="auto">
  <name>Task 4: AccrueDunningProofTest — escalation + termination + admin trace (D-03, D-07, D-08)</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/accrue_dunning_proof_test.exs</files>
  <read_first>
    - `examples/chimeway_demo_host/test/demo_host_web/mailglass_delivery_proof_test.exs` — setup, `@moduletag :mailglass`, admin LiveView form `#trace-search-form`
    - `test/chimeway/integrations/accrue_dunning_lifecycle_test.exs` — terminate describe assertions (no escalation email, signal_received)
    - `examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs` — JOUR-08 Morgan escalation trace pattern
  </read_first>
  <action>
    Create `DemoHostWeb.AccrueDunningProofTest` with `@moduletag :accrue`, `async: false`.

    Setup (mirror mailglass proof):
    - Checkout `Accrue.TestRepo` sandbox shared mode
    - Checkout `Chimeway.Repo` sandbox (ConnCase default)
    - `configure_chimeway_dunning_engine!/0` and `configure_chimeway_logger_adapter!/0`
    - Set `Application.put_env(:chimeway, :dispatcher, Chimeway.Dispatch.Oban)` for signal tests; restore on_exit
    - `Oban.Testing` with `repo: Chimeway.Repo`

    Tests:

    1. `"DEMO-07 payment_failed starts dunning and delivers initial email"` — `DemoHost.Seeds.seed_accrue_dunning/0` returns waiting run; assert Logger email delivery exists for workflow; assert `workflow_key == "accrue.dunning"`.

    2. `"DEMO-07 invoice.paid terminates before escalation"` — from waiting state, `trigger_invoice_paid_event!/4`, drain `:chimeway_signals` queue; assert run `status_reason == "signal_received"`; assert zero deliveries for `step_key == "escalation_email"`.

    3. `"DEMO-07 admin trace shows dunning workflow"` — seed, GET `/admin/chimeway`, LiveView submit trace search with customer `recipient_identity`, assert HTML contains `accrue.dunning` or `signal_received` / waiting reason text; open delivery detail if applicable.

    Wrap module in `if Code.ensure_loaded?(Accrue) and Code.ensure_loaded?(Accrue.Integrations.Chimeway)` like root lifecycle tests.
  </action>
  <acceptance_criteria>
    - File exists with `@moduletag :accrue`
    - Contains tests named with `DEMO-07`
    - Contains `trace-search-form` and `invoice.paid` or `signal_received` assertions
    - `cd examples/chimeway_demo_host && ACCRUE_PATH=../../accrue/accrue mix test --only accrue --warnings-as-errors` exits 0
    - `cd examples/chimeway_demo_host && mix test --only journey --warnings-as-errors` still exits 0 (no regression)
  </acceptance_criteria>
  <automated>cd examples/chimeway_demo_host && ACCRUE_PATH="${ACCRUE_PATH:-../../accrue/accrue}" mix test --only accrue --warnings-as-errors</automated>
</task>

<task type="auto">
  <name>Task 5: Full verify.accrue gate (root + demo)</name>
  <files></files>
  <read_first>
    - `mix.exs` — verify.accrue alias
    - `.planning/phases/58-accrue-dunning-core/58-VERIFICATION.md` — baseline 11 root tests
  </read_first>
  <action>
    Run full verification and fix any failures:

    `ACCRUE_PATH=../accrue/accrue mix verify.accrue --warnings-as-errors`

    Ensure root lifecycle tests (11) plus new demo tests all pass. If demo host count is 0, debug Task 4 module guard.
  </action>
  <acceptance_criteria>
    - `mix verify.accrue --warnings-as-errors` exits 0
    - Test count &gt; 11 (demo tests added)
    - `mix ci.test --warnings-as-errors` exits 0 (accrue excluded from default CI)
  </acceptance_criteria>
  <automated>ACCRUE_PATH="${ACCRUE_PATH:-../accrue/accrue}" mix verify.accrue --warnings-as-errors && mix ci.test --warnings-as-errors</automated>
</task>

</tasks>

<verification>
- [ ] `mix verify.accrue --warnings-as-errors` green (root + demo host)
- [ ] `mix test examples/chimeway_demo_host --only journey` green (journey isolation)
- [ ] `DemoHost.Seeds.seed_accrue_dunning/0` documented and callable
- [ ] ROADMAP SC #2 and #3 satisfied for demo layer
</verification>
