---
phase: 59-accrue-blueprint-demo
plan: 02
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - guides/recipes/accrue-dunning-blueprint.md
  - mix.exs
  - test/chimeway/doc_contract_test.exs
autonomous: true
requirements: [ECOS-07]
must_haves:
  truths:
    - "Published recipe documents DunningNotifier authoring, Accrue event subscription, and Chimeway vs Accrue responsibility split (D-11, SEED-003)"
    - "Recipe cites real modules: Accrue.Integrations.Chimeway.DunningNotifier, DemoHost.Seeds.seed_accrue_dunning/0 (D-14)"
    - "Recipe does not duplicate golden-path guide — points to Phase 60 (D-12)"
    - "CI doc-contract fails on drift or missing required strings (D-15, D-16)"
  artifacts:
    - path: "guides/recipes/accrue-dunning-blueprint.md"
      provides: "ECOS-07 reference recipe"
      contains: "Accrue.Integrations.Chimeway"
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "ECOS-07 doc-contract describe"
      contains: "accrue dunning blueprint recipe doc contract"
  key_links:
    - from: "guides/recipes/accrue-dunning-blueprint.md"
      to: "examples/chimeway_demo_host/lib/demo_host/seeds.ex"
      via: "Runnable demo pointer seed_accrue_dunning"
      pattern: "DemoHost.Seeds.seed_accrue_dunning"
    - from: "test/chimeway/doc_contract_test.exs"
      to: "guides/recipes/accrue-dunning-blueprint.md"
      via: "File.read! contract tests"
      pattern: "accrue-dunning-blueprint"
---

<objective>
Deliver ECOS-07: published Accrue dunning reference recipe with CI doc-contract coverage, cross-links to Mailglass blueprint and Phase 60 guide placeholder — building on runnable demo proof from 59-01 (D-01).

Purpose: Adopters can copy the recipe without reading Phase 58 implementation commits; doc-contract prevents documentation drift.

Output: `guides/recipes/accrue-dunning-blueprint.md`, ECOS-07 describe in `doc_contract_test.exs`, `mix.exs` docs extras entry.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/59-accrue-blueprint-demo/59-CONTEXT.md
@.planning/phases/59-accrue-blueprint-demo/59-RESEARCH.md
@.planning/phases/59-accrue-blueprint-demo/59-01-PLAN.md
@guides/recipes/mailglass-integration-blueprint.md
@test/chimeway/doc_contract_test.exs
@mix.exs
</context>

<threat_model>
| ID | STRIDE | Threat | Severity | Mitigation |
|----|--------|--------|----------|------------|
| T-59-05 | Tampering | Recipe documents fictional Chimeway.Workflow or wrong adapter seam | high | doc-contract `@required` + forbidden regex (mirror ECOS-05) |
| T-59-06 | Information Disclosure | Recipe embeds secrets or raw PII patterns | medium | Reuse `@recipe_forbidden_strings`; no API keys in examples |
| T-59-07 | Repudiation | Recipe claims host glue path bypassing Accrue | medium | Required strings include Accrue events; forbid direct-only Chimeway.trigger adoption story |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write accrue-dunning-blueprint.md (D-11..D-14)</name>
  <files>guides/recipes/accrue-dunning-blueprint.md</files>
  <read_first>
    - `guides/recipes/mailglass-integration-blueprint.md` — full structure
    - `.planning/phases/59-accrue-blueprint-demo/59-CONTEXT.md` — D-11..D-14, specifics
    - `.planning/seeds/SEED-003-ecosystem-integrations.md` — billing-state split, 48h escalation
    - `../accrue/accrue/lib/accrue/integrations/chimeway/dunning_notifier.ex` — real workflow/2 (if ACCRUE_PATH available)
  </read_first>
  <action>
    Create `guides/recipes/accrue-dunning-blueprint.md` parallel to Mailglass blueprint:

    **Sections (required):**
    1. Title: `# Accrue Dunning Blueprint`
    2. Who this is for — Feature Developer (DunningNotifier) + Adopter (Accrue engine config)
    3. Prerequisites — Golden Path link; note `ACCRUE_PATH` / optional dep for local dev
    4. Responsibility split — Chimeway orchestrates when/why (workflows, suppression, idempotency, traces); Accrue owns billing state (subscriptions, invoices, anchors). Use words `orchestrates` and billing-state language from SEED-003.
    5. Feature Developer — document `Accrue.Integrations.Chimeway.DunningNotifier`, `workflow/2`, `cancel_signals: ["invoice.paid"]`, 48h `wait_until` escalation shape (Email 1 → wait → Email 2). Include `notification_key`, `tenant_id`, `idempotency_key` concepts.
    6. Adopter — `config :accrue, dunning: [engine: Accrue.Integrations.Chimeway]`; events `invoice.payment_failed` (start) and `invoice.paid` (terminate via Outcome Signal). Explicitly state **not** a `Chimeway.Adapter` seam.
    7. Trigger example — `Accrue.Test.trigger_event/2` or production webhook path; **do not** present host calling `Chimeway.trigger(DunningNotifier, ...)` as the adoption path.
    8. Runnable demo — `DemoHost.Seeds.seed_accrue_dunning/0`, `mix verify.accrue`, `/admin/chimeway` operator trace.
    9. Out of scope — golden-path Accrue integration guide (Phase 60 DOCS-08), `mix verify.accrue` CI job in MAINTAINING (Phase 60 GATE-05).
    10. Related guides — link `mailglass-integration-blueprint.md` (optional email), placeholder line for Phase 60 Accrue guide, Golden Path, mention-escalation recipe.

    Cross-link Mailglass blueprint for optional Mailglass email delivery (Phase 58 D-07).
  </action>
  <acceptance_criteria>
    - File `guides/recipes/accrue-dunning-blueprint.md` exists
    - Contains `Accrue.Integrations.Chimeway.DunningNotifier`
    - Contains `invoice.payment_failed` and `invoice.paid`
    - Contains `DemoHost.Seeds.seed_accrue_dunning`
    - Contains `/admin/chimeway`
    - Contains out-of-scope section referencing Phase 60 guide
    - Does not contain `Chimeway.Adapter` as the dunning integration mechanism
  </acceptance_criteria>
  <automated>test -f guides/recipes/accrue-dunning-blueprint.md && grep -E "Accrue.Integrations.Chimeway|invoice.payment_failed|seed_accrue_dunning|/admin/chimeway" guides/recipes/accrue-dunning-blueprint.md</automated>
</task>

<task type="auto">
  <name>Task 2: ECOS-07 doc-contract describe block (D-15, D-16)</name>
  <files>test/chimeway/doc_contract_test.exs</files>
  <read_first>
    - `test/chimeway/doc_contract_test.exs` — `describe "mailglass blueprint recipe doc contract (ECOS-05)"` block
    - `.planning/phases/59-accrue-blueprint-demo/59-CONTEXT.md` — D-16 required strings list
  </read_first>
  <action>
    Add after ECOS-05 mailglass blueprint describe:

    ```elixir
    @accrue_blueprint_recipe Path.expand("../../guides/recipes/accrue-dunning-blueprint.md", __DIR__)

    describe "accrue dunning blueprint recipe doc contract (ECOS-07)" do
    ```

    Reuse `@recipe_forbidden_strings` for forbidden tests.

    Add regex test forbidding `Chimeway.Workflow` (not `Workflows`) — same as ECOS-05.

    `@required` strings (minimum per D-16):
    `Accrue.Integrations.Chimeway`, `invoice.payment_failed`, `invoice.paid`, `cancel_signals`, `workflow/2`, `config :accrue`, `dunning`, `idempotency_key`, `tenant_id`, `orchestrates`, `DemoHost.Seeds.seed_accrue_dunning`, `/admin/chimeway`

    Add one test requiring billing-state split language: assert content contains both `billing` and (`state` or `Accrue`) — or exact phrase from recipe like "billing state" / "billing-state".

    Loop `for required <- @required` pattern matching ECOS-05.
  </action>
  <acceptance_criteria>
    - `doc_contract_test.exs` contains `accrue dunning blueprint recipe doc contract (ECOS-07)`
    - `mix test test/chimeway/doc_contract_test.exs --only "accrue dunning"` exits 0
    - Removing any `@required` string causes test failure (manual spot-check optional)
  </acceptance_criteria>
  <automated>mix test test/chimeway/doc_contract_test.exs --only "accrue dunning" --warnings-as-errors</automated>
</task>

<task type="auto">
  <name>Task 3: Register blueprint in mix docs extras</name>
  <files>mix.exs</files>
  <read_first>
    - `mix.exs` — `docs/0` extras list (mailglass blueprint entry if present)
  </read_first>
  <action>
    In `mix.exs` `docs/0` `extras:` list, add `"guides/recipes/accrue-dunning-blueprint.md"` adjacent to mailglass blueprint entry (keep alphabetical or recipe grouping).
  </action>
  <acceptance_criteria>
    - `grep accrue-dunning-blueprint mix.exs` returns a match in extras list
  </acceptance_criteria>
  <automated>grep -n "accrue-dunning-blueprint" mix.exs</automated>
</task>

<task type="auto">
  <name>Task 4: Phase verification — doc-contract + verify.accrue</name>
  <files></files>
  <read_first>
    - `.planning/phases/59-accrue-blueprint-demo/59-VALIDATION.md`
  </read_first>
  <action>
    Run:

    1. `mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` (full doc contract suite)
    2. `ACCRUE_PATH=../accrue/accrue mix verify.accrue --warnings-as-errors`

    Fix any recipe string gaps surfaced by ECOS-07 tests.
  </action>
  <acceptance_criteria>
    - Both commands exit 0
    - ECOS-07 and DEMO-07 requirements covered by combined 59-01 + 59-02 artifacts
  </acceptance_criteria>
  <automated>mix test test/chimeway/doc_contract_test.exs --warnings-as-errors && ACCRUE_PATH="${ACCRUE_PATH:-../accrue/accrue}" mix verify.accrue --warnings-as-errors</automated>
</task>

</tasks>

<verification>
- [ ] `guides/recipes/accrue-dunning-blueprint.md` published
- [ ] ECOS-07 doc-contract describe green
- [ ] ROADMAP SC #1 satisfied
- [ ] Recipe cross-links Mailglass blueprint and Phase 60 placeholder
</verification>
