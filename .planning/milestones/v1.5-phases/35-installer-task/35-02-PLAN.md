---
phase: 35
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/mix/tasks/chimeway.gen.migrations.ex
  - test/chimeway/install/migrations_test.exs
autonomous: true
requirements:
  - INST-01
must_haves:
  truths:
    - mix chimeway.gen.migrations exists as Mix.Tasks.Chimeway.Gen.Migrations (D-01).
    - Task calls Mix.Task.run("app.config") then delegates to Chimeway.Install.Migrations.run/1.
    - No mix chimeway.install shipped (D-02).
    - First run in tmp host creates 31 migration files with InstallerHost.Repo.Migrations namespaces.
    - Task name matches guides/introduction/installation.md without doc edits (D-14).
  artifacts:
    - path: lib/mix/tasks/chimeway.gen.migrations.ex
      provides: "Documented CLI entrypoint mix chimeway.gen.migrations"
    - path: test/chimeway/install/migrations_test.exs
      provides: "Integration-style tests invoking run/1 in tmp cwd (extends wave 1 unit tests)"
  key_links:
    - from: lib/mix/tasks/chimeway.gen.migrations.ex
      to: lib/chimeway/install/migrations.ex
      via: "Chimeway.Install.Migrations.run/1"
    - from: guides/introduction/installation.md
      to: mix chimeway.gen.migrations
      via: "Task name parity — doc unchanged in Phase 35"
---

<objective>
Ship the thin Mix task wrapper `mix chimeway.gen.migrations` (D-01) and prove end-to-end file generation from a minimal tmp host cwd — completing INST-01 before golden CI contracts land in wave 3.

Repo resolution follows wave 1 D-05 Option A: config `:chimeway, :repo` when set, else `{App}.Repo` inferred from host `mix.exs` so the task works when run before `config :chimeway, repo:` per current installation guide order (D-14 — no guide reorder).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-installer-task/35-CONTEXT.md
@.planning/phases/35-installer-task/35-RESEARCH.md
@.planning/phases/35-installer-task/35-PATTERNS.md
@lib/chimeway/install/migrations.ex
@lib/mix/tasks/preview_rendering.ex
@lib/mix/tasks/verify_published.ex
@guides/introduction/installation.md
</context>

<tasks>

<task type="auto">
  <name>Task 35-02-01: Implement Mix.Tasks.Chimeway.Gen.Migrations</name>
  <files>lib/mix/tasks/chimeway.gen.migrations.ex</files>
  <read_first>
    - lib/chimeway/install/migrations.ex
    - lib/mix/tasks/preview_rendering.ex
    - /Users/jon/projects/mailglass/lib/mix/tasks/mailglass.gen.migration.ex
    - .planning/phases/35-installer-task/35-PATTERNS.md §2.1
    - .planning/phases/35-installer-task/35-CONTEXT.md D-01, D-02, D-14
    - guides/introduction/installation.md (confirm task name mix chimeway.gen.migrations)
  </read_first>
  <action>
    Create `lib/mix/tasks/chimeway.gen.migrations.ex`:

    ```elixir
    defmodule Mix.Tasks.Chimeway.Gen.Migrations do
      use Mix.Task

      @shortdoc "Copy Chimeway migration templates into the host priv/repo/migrations"

      @moduledoc """
      Generates Chimeway schema migrations in the host repo.

      ## Usage

          mix chimeway.gen.migrations

      Copies #{31} migration templates from `priv/chimeway_migrations/` (Oban excluded).
      See `guides/recipes/oban-integration.md` for Oban setup (D-10).

      Re-running is idempotent — existing slugs print `unchanged`.
      """
    ```

    **run/1 behavior:**
    1. `Mix.Task.run("app.config")` — load host config before env read (D-05)
    2. `OptionParser.parse(argv, strict: [])` — reject positional args and unknown switches (no `--force` in Phase 35)
    3. Call `Chimeway.Install.Migrations.run([])`
    4. On `{:error, :repo_missing}` → `Mix.raise/1` with message explaining both `config :chimeway, repo: MyApp.Repo` and mix.exs `app:` inference
    5. On `:ok` → `:ok`

    **Do not** create `mix chimeway.install` (D-02). **Do not** modify `guides/introduction/installation.md` or README (D-14).
  </action>
  <acceptance_criteria>
    - File `lib/mix/tasks/chimeway.gen.migrations.ex` exists
    - Module name is `Mix.Tasks.Chimeway.Gen.Migrations`
    - `grep -c 'Mix.Task.run("app.config")' lib/mix/tasks/chimeway.gen.migrations.ex` ≥ 1
    - `grep -c 'Chimeway.Install.Migrations.run' lib/mix/tasks/chimeway.gen.migrations.ex` ≥ 1
    - `grep -c 'oban-integration' lib/mix/tasks/chimeway.gen.migrations.ex` ≥ 1
    - `mix help chimeway.gen.migrations` succeeds and shows @shortdoc
    - `grep -r 'mix chimeway.install' lib/` returns no matches (D-02)
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors && mix help chimeway.gen.migrations | grep -q 'Copy Chimeway migration templates'</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 35-02-02: Add tmp-host integration tests for run/1 file generation</name>
  <files>test/chimeway/install/migrations_test.exs</files>
  <read_first>
    - test/chimeway/install/migrations_test.exs (wave 1 tests)
    - lib/chimeway/install/migrations.ex
    - .planning/phases/35-installer-task/35-RESEARCH.md §5–§6
    - .planning/phases/35-installer-task/35-CONTEXT.md D-06, D-07, D-08, D-10
  </read_first>
  <action>
    Extend `test/chimeway/install/migrations_test.exs` with `async: false` describe `"run/1 in tmp host"`:

    **Setup per test:**
    - `tmp = System.tmp_dir!() |> Path.join("chimeway_install_" <> unique)` 
    - Write minimal `mix.exs`: `app: :installer_host`, `defp deps, do: [{:chimeway, path: ...}]` pointing to repo root
    - Write `config/config.exs`: `config :chimeway, repo: InstallerHost.Repo`
    - `File.mkdir_p!(Path.join(tmp, "priv/repo/migrations"))`
    - `File.cd!(tmp, fn -> ... end)`

    **Test: first run creates 31 files (INST-01)**
    - Capture IO with custom `io` callback or StringIO
    - Assert 31 files in `priv/repo/migrations/`
    - Assert each file contains `# chimeway_migration:` marker (D-09)
    - Assert no file content contains `Chimeway.Repo.Migrations` (D-06)
    - Assert no slug `create_oban_jobs_tables` (D-10)
    - Assert stdout lines include 31× `created priv/repo/migrations/`

    **Test: repo inference without config (D-05 fallback)**
    - Tmp host with `mix.exs` `app: :installer_host` only — no `config :chimeway, repo:`
    - `Application.delete_env(:chimeway, :repo)` before run
    - Assert generation succeeds with `InstallerHost.Repo.Migrations` namespace

    **Test: second run idempotency preview (D-08) — partial INST-02 proof**
    - Run `Chimeway.Install.Migrations.run([])` twice in same tmp dir
    - Assert file count still 31
    - Assert second stdout has 31× `unchanged` and 0× `created`

    Full golden-diff contract deferred to 35-03 (INST-02).
  </action>
  <acceptance_criteria>
    - `mix test test/chimeway/install/migrations_test.exs --warnings-as-errors` exits 0
    - Test file contains `"run/1 in tmp host"` describe block
    - Test file asserts `31` file count after first run
    - Test file asserts second run `unchanged` behavior
    - `grep -c 'InstallerHost.Repo.Migrations' test/chimeway/install/migrations_test.exs` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/install/migrations_test.exs --warnings-as-errors</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 35-02-03: Smoke-verify Mix CLI subprocess path</name>
  <files>test/chimeway/install/migrations_test.exs</files>
  <read_first>
    - lib/mix/tasks/chimeway.gen.migrations.ex
    - .planning/phases/35-installer-task/35-PATTERNS.md §2.1 (app.config requirement)
  </read_first>
  <action>
    Add one test in `migrations_test.exs` describe `"mix chimeway.gen.migrations subprocess"` (async: false):

    - Scaffold same tmp host as 35-02-02
    - Run `System.cmd("mix", ["chimeway.gen.migrations"], cd: tmp, env: [{"MIX_ENV", "dev"}], stderr_to_stdout: true)`
    - Assert exit status 0
    - Assert `ls priv/repo/migrations | wc -l` equivalent == 31

    This proves the Mix task is registered and loads app.config before generation (INST-01 CLI surface).
  </action>
  <acceptance_criteria>
    - Subprocess test exists and passes
    - `mix test test/chimeway/install/migrations_test.exs --warnings-as-errors` exits 0
    - `mix test --warnings-as-errors` (full suite) exits 0 — no regression
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/install/migrations_test.exs --warnings-as-errors && mix test --warnings-as-errors</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host developer → mix chimeway.gen.migrations | Invokes Mix task with cwd = host project root |
| Mix task → Chimeway.Install.Migrations | Delegates after app.config; no arbitrary code eval |
| Mix CLI → filesystem | Creates migration files only via core module |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-02-01 | Tampering | Unexpected CLI args | Medium | mitigate | Strict OptionParser — `Mix.raise` on positional or unknown switches |
| T-35-02-02 | Denial of Service | Running task outside project root | Low | accept | Document cwd requirement in @moduledoc; fails naturally if mix.exs missing |
| T-35-02-03 | Spoofing | Task name drift from docs | Medium | mitigate | Assert `mix help chimeway.gen.migrations` works; Phase 41 GATE-01 will add doc-contract; D-14 keeps installation.md unchanged |
| T-35-02-04 | Elevation of Privilege | mix chimeway.install confusion | Low | mitigate | Do not ship install task (D-02); grep CI ensures no accidental module |
| T-35-02-05 | Repudiation | Silent partial install | Medium | mitigate | stdout `created`/`unchanged` lines; golden contract in 35-03 |

**Block gate:** No HIGH severity threats without mitigation. All HIGH threats mitigated in wave 1 core module.
</threat_model>

<verification>
1. `mix test test/chimeway/install/migrations_test.exs --warnings-as-errors`
2. `mix test --warnings-as-errors`
3. `mix help chimeway.gen.migrations`
4. Ad-hoc: in tmp `mix new` host with path dep, run `mix chimeway.gen.migrations` twice — 31 files, second run unchanged only
</verification>

<success_criteria>
- `mix chimeway.gen.migrations` exists and matches installation guide task name (D-01, D-14)
- First run generates 31 host-namespaced migrations; Oban excluded (D-06, D-10)
- Re-run in same host prints `unchanged` only (D-07, D-08) — full golden proof in 35-03
- INST-01 satisfied; INST-02 golden CI deferred to wave 3
- No doc/README changes (D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/35-installer-task/35-02-SUMMARY.md` via orchestration.
</output>
