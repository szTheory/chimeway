---
phase: 35
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/chimeway_migrations/001_create_chimeway_events.exs
  - priv/chimeway_migrations/002_create_chimeway_notifications.exs
  - priv/chimeway_migrations/003_create_chimeway_deliveries.exs
  - priv/chimeway_migrations/004_create_chimeway_delivery_attempts.exs
  - priv/chimeway_migrations/005_create_chimeway_notification_preferences.exs
  - priv/chimeway_migrations/006_add_correlation_id_to_chimeway_events.exs
  - priv/chimeway_migrations/007_create_chimeway_category_preferences.exs
  - priv/chimeway_migrations/008_create_chimeway_policy_settings.exs
  - priv/chimeway_migrations/009_add_attempt_history_columns.exs
  - priv/chimeway_migrations/010_add_delivery_orchestration_fields_to_chimeway_deliveries.exs
  - priv/chimeway_migrations/011_add_time_zone_to_chimeway_policy_settings.exs
  - priv/chimeway_migrations/012_create_chimeway_digest_rules.exs
  - priv/chimeway_migrations/013_create_chimeway_digest_buckets.exs
  - priv/chimeway_migrations/014_create_chimeway_digest_memberships.exs
  - priv/chimeway_migrations/015_alter_chimeway_digest_buckets_for_emission.exs
  - priv/chimeway_migrations/016_alter_chimeway_digest_memberships_for_resolution.exs
  - priv/chimeway_migrations/017_alter_chimeway_deliveries_for_digest_outcome.exs
  - priv/chimeway_migrations/018_add_rendering_contract_fields.exs
  - priv/chimeway_migrations/019_add_render_channels_to_chimeway_notifications.exs
  - priv/chimeway_migrations/020_add_orchestration_snapshot_to_chimeway_notifications.exs
  - priv/chimeway_migrations/021_create_chimeway_workflow_definitions.exs
  - priv/chimeway_migrations/022_create_chimeway_workflow_steps.exs
  - priv/chimeway_migrations/023_add_workflow_definition_id_to_chimeway_notifications.exs
  - priv/chimeway_migrations/024_create_chimeway_workflow_runs.exs
  - priv/chimeway_migrations/025_create_chimeway_workflow_transitions.exs
  - priv/chimeway_migrations/026_alter_chimeway_deliveries_for_workflow_linkage.exs
  - priv/chimeway_migrations/027_create_chimeway_signals_and_spine.exs
  - priv/chimeway_migrations/028_add_adapter_module_to_chimeway_delivery_attempts.exs
  - priv/chimeway_migrations/029_add_provider_message_id_to_delivery_attempts.exs
  - priv/chimeway_migrations/030_add_tenant_and_actor_to_chimeway_deliveries.exs
  - priv/chimeway_migrations/031_create_chimeway_webhook_ingress.exs
  - lib/chimeway/install/migrations.ex
  - test/chimeway/install/migrations_test.exs
autonomous: true
requirements:
  - INST-01
must_haves:
  truths:
    - Exactly 31 canonical migration templates exist under priv/chimeway_migrations/ with order prefixes 001–031.
    - Oban wrapper migration create_oban_jobs_tables is excluded from templates (D-10).
    - Each template first line is marker comment # chimeway_migration: {slug} (D-09).
    - Chimeway.Install.Migrations lists templates, rewrites namespaces, matches idempotency by slug, and resolves host repo from config or mix.exs fallback (D-05 Option A).
    - Pure-function unit tests pass without subprocess or Postgres.
  artifacts:
    - path: priv/chimeway_migrations/
      provides: "Hex-shipped canonical migration templates (D-03, D-04)"
    - path: lib/chimeway/install/migrations.ex
      provides: "Testable installer core — template enum, slug idempotency, namespace rewrite, timestamp batch (D-06, D-07, D-08)"
    - path: test/chimeway/install/migrations_test.exs
      provides: "Unit tests for list_templates/0, extract_slug/1, rewrite_namespace/2, find_existing_by_slug/2, resolve_repo!/0"
  key_links:
    - from: priv/chimeway_migrations/*.exs
      to: priv/repo/migrations/*.exs
      via: "One-time extraction excluding 20260424093000_create_oban_jobs_tables.exs"
    - from: lib/chimeway/install/migrations.ex
      to: :code.priv_dir(:chimeway) <> "/chimeway_migrations"
      via: "list_templates/0 reads shipped templates at runtime"
---

<objective>
Extract 31 ordered migration templates from the dev repo and implement `Chimeway.Install.Migrations` — the testable core of the copy-based installer (D-03, D-04).

**D-05 resolution (planning decision):** Resolve host repo from `Application.get_env(:chimeway, :repo)` when set; when unset, infer `{App}.Repo` from host `mix.exs` `app:` atom (mailglass-style fallback) so `mix chimeway.gen.migrations` works in the order documented in `guides/introduction/installation.md` (task before `config :chimeway, repo:`). Fail with `{:error, :repo_missing}` when both paths fail — do not reorder installation.md (D-14).

**Out of scope:** Mix task CLI wrapper (35-02), golden fixtures (35-03), `mix chimeway.install` (D-02), programmatic `Chimeway.Migration.up/0` (D-03 deferral), README/doc semver fixes (D-14 → Phase 36).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-installer-task/35-CONTEXT.md
@.planning/phases/35-installer-task/35-RESEARCH.md
@.planning/phases/35-installer-task/35-PATTERNS.md
@.planning/phases/35-installer-task/35-VALIDATION.md
@priv/repo/migrations/
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 35-01-01: Extract 31 migration templates to priv/chimeway_migrations/</name>
  <files>priv/chimeway_migrations/*.exs</files>
  <read_first>
    - .planning/phases/35-installer-task/35-RESEARCH.md §4 (ordered slug inventory)
    - .planning/phases/35-installer-task/35-CONTEXT.md D-04, D-09, D-10
    - priv/repo/migrations/ (all 32 files — identify Oban exclusion)
    - mix.exs package/0 files list (priv/ ships in Hex)
  </read_first>
  <action>
    Create directory `priv/chimeway_migrations/`.

    For each of the 31 slugs in RESEARCH §4 table (rows 1–31), copy the corresponding source file body from `priv/repo/migrations/` into `priv/chimeway_migrations/{NNN}_{slug}.exs` where `{NNN}` is zero-padded order (`001` … `031`).

    **Exclude** source file `20260424093000_create_oban_jobs_tables.exs` (D-10).

    For each template:
    1. Prepend first line: `# chimeway_migration: {slug}\n` (D-09)
    2. Preserve original migration body verbatim including `defmodule Chimeway.Repo.Migrations.*` (namespace rewrite happens at generation — D-06)
    3. Do not rewrite table/index names (`:chimeway_*` stay unchanged)

    Slug order must match RESEARCH §4 exactly (FK dependency order preserved by later timestamp batching).
  </action>
  <acceptance_criteria>
    - `ls priv/chimeway_migrations/*.exs | wc -l` outputs **31**
    - `ls priv/chimeway_migrations/ | grep -c oban` outputs **0**
    - `ls priv/chimeway_migrations/ | grep -c create_oban_jobs_tables` outputs **0**
    - Every file matches pattern `^[0-9]{3}_[a-z0-9_]+\.exs$`
    - `grep -r '^# chimeway_migration:' priv/chimeway_migrations/ | wc -l` outputs **31**
    - First template is `001_create_chimeway_events.exs`; last is `031_create_chimeway_webhook_ingress.exs`
    - Each template still contains `Chimeway.Repo.Migrations` module prefix (not rewritten in template)
  </acceptance_criteria>
  <verify>
    <automated>test $(ls priv/chimeway_migrations/*.exs | wc -l) -eq 31 && ! ls priv/chimeway_migrations/ | grep -q oban && test $(grep -r '^# chimeway_migration:' priv/chimeway_migrations/ | wc -l) -eq 31</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 35-01-02: Implement Chimeway.Install.Migrations core module</name>
  <files>lib/chimeway/install/migrations.ex</files>
  <read_first>
    - .planning/phases/35-installer-task/35-PATTERNS.md §2.2
    - .planning/phases/35-installer-task/35-RESEARCH.md §3 D-05–D-09, §5–§6
    - /Users/jon/projects/mailglass/lib/mix/tasks/mailglass.gen.migration.ex (slug match, app inference)
    - lib/mix/tasks/preview_rendering.ex (error formatting style)
  </read_first>
  <action>
    Create `lib/chimeway/install/migrations.ex` with `@moduledoc` noting Oban exclusion and link to `guides/recipes/oban-integration.md` (D-10).

    **Public API:**
    - `list_templates/0` — returns `[{order, slug, template_path}]` sorted by order prefix; reads from `:code.priv_dir(:chimeway) <> "/chimeway_migrations"`
    - `run/1` — opts keyword `[repo: nil, io: Mix.shell()]`; orchestrates generation
    - `resolve_repo!/0` — returns host repo atom or raises `{:error, :repo_missing}`
    - `extract_slug/1` — from template filename `{NNN}_{slug}.exs` or marker comment
    - `rewrite_namespace/2` — `String.replace(content, "Chimeway.Repo.Migrations", host_prefix_string)`
    - `find_existing_by_slug/2` — glob `priv/repo/migrations/*_{slug}.exs`, sort, first
    - `host_migrations_prefix/1` — `MyApp.Repo` → `"MyApp.Repo.Migrations"`

    **Repo resolution (D-05 Option A):**
    1. If `Application.get_env(:chimeway, :repo)` is atom → use it
    2. Else read `mix.exs` in `File.cwd!()`, regex `app:\s*:(\w+)`, camelize → `{App}.Repo`
    3. Else return `{:error, :repo_missing}`

    **Generation loop (D-07, D-08):**
    - `File.mkdir_p!("priv/repo/migrations")` if missing
    - Batch base timestamp: `NaiveDateTime.utc_now() |> NaiveDateTime.truncate(:second)`
    - For index `i` (0-based): timestamp = base + `i` seconds, formatted `%Y%m%d%H%M%S`
    - For each template: if slug exists → `io.info("unchanged #{path}")`; else write `priv/repo/migrations/{ts}_{slug}.exs` with rewritten namespace → `io.info("created #{path}")`
    - Return `:ok` on success

    **Do not** implement `--force` overwrite (D-07 anti-pattern). Do not ship `mix chimeway.install` (D-02).
  </action>
  <acceptance_criteria>
    - Module `Chimeway.Install.Migrations` exists and compiles
    - `grep -c 'def list_templates' lib/chimeway/install/migrations.ex` ≥ 1
    - `grep -c 'def run' lib/chimeway/install/migrations.ex` ≥ 1
    - `grep -c 'Chimeway.Repo.Migrations' lib/chimeway/install/migrations.ex` ≥ 1 (rewrite target)
    - `grep -c 'repo_missing' lib/chimeway/install/migrations.ex` ≥ 1
    - `grep -c 'unchanged' lib/chimeway/install/migrations.ex` ≥ 1
    - `grep -c 'created' lib/chimeway/install/migrations.ex` ≥ 1
    - `grep -c 'oban-integration' lib/chimeway/install/migrations.ex` ≥ 1
    - `mix compile --warnings-as-errors` succeeds
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 35-01-03: Add pure-function unit tests for installer core</name>
  <files>test/chimeway/install/migrations_test.exs</files>
  <read_first>
    - lib/chimeway/install/migrations.ex
    - .planning/phases/35-installer-task/35-VALIDATION.md (task 35-01-01, 35-01-02)
    - .planning/phases/35-installer-task/35-PATTERNS.md §3 (assert error tags not strings)
  </read_first>
  <action>
    Create `test/chimeway/install/migrations_test.exs` with `@moduletag :installer`.

    **Tests (no subprocess, no DB):**
    1. `list_templates/0` returns exactly 31 entries in order; slugs match RESEARCH §4 list; no `create_oban_jobs_tables` (D-10)
    2. `extract_slug/1` on `"001_create_chimeway_events.exs"` → `"create_chimeway_events"`
    3. `rewrite_namespace/2` replaces `Chimeway.Repo.Migrations` with `InstallerHost.Repo.Migrations`; output must not contain `Chimeway.Repo.Migrations` (D-06)
    4. `host_migrations_prefix/1` for `InstallerHost.Repo` → `"InstallerHost.Repo.Migrations"`
    5. `find_existing_by_slug/2` in tmp dir — write `99999999999999_create_chimeway_events.exs`, assert slug match finds it (D-07)
    6. `resolve_repo!/0` with `Application.put_env(:chimeway, :repo, TestHost.Repo)` → `TestHost.Repo`
    7. Repo missing: unset env + cwd without valid mix.exs → assert `{:error, :repo_missing}` (use `assert_raise` or match on error tuple from helper)

    Use `ExUnit.Case, async: true` for pure tests.
  </action>
  <acceptance_criteria>
    - File `test/chimeway/install/migrations_test.exs` exists
    - `mix test test/chimeway/install/migrations_test.exs --warnings-as-errors` exits 0
    - Test file asserts `length(templates) == 31`
    - Test file refutes slug `create_oban_jobs_tables` in template list
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/install/migrations_test.exs --warnings-as-errors</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template source → priv/chimeway_migrations/ | Maintainer-controlled copy from dev migrations; no host input |
| Chimeway.Install.Migrations → host filesystem | Writes only under `priv/repo/migrations/` relative to `File.cwd!()` |
| Repo resolution | Reads host `mix.exs` and `:chimeway` application env — no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01-01 | Tampering | Path traversal in slug/filename | High | mitigate | `extract_slug/1` only accepts `[a-z0-9_]+`; target path always `Path.join(["priv","repo","migrations", "#{ts}_#{slug}.exs"])` — reject slugs with `..`, `/`, or `\` |
| T-35-01-02 | Tampering | Write outside migrations dir | High | mitigate | Hard-code output root `priv/repo/migrations`; never use user-supplied base path |
| T-35-01-03 | Elevation of Privilege | Arbitrary module injection via repo atom | Medium | mitigate | Validate resolved repo atom ends with `.Repo`; reject non-atom config values |
| T-35-01-04 | Denial of Service | Unbounded template enumeration | Low | accept | Fixed 31 templates shipped in priv/; no runtime download |
| T-35-01-05 | Information Disclosure | mix.exs read exposes app name | Low | accept | Standard Mix task behavior; only reads `app:` line for inference |
| T-35-01-06 | Spoofing | Wrong namespace rewrite | Medium | mitigate | Unit test asserts full replacement; golden contract in 35-03 catches drift |

**Block gate:** T-35-01-01 and T-35-01-02 are HIGH — must implement slug sanitization and fixed output directory before merge.
</threat_model>

<verification>
1. `mix test test/chimeway/install/migrations_test.exs --warnings-as-errors`
2. `mix compile --warnings-as-errors`
3. Manual: `mix hex.build --unpack --output /tmp/chimeway_verify && ls /tmp/chimeway_verify/priv/chimeway_migrations | wc -l` expect 31
</verification>

<success_criteria>
- 31 templates in `priv/chimeway_migrations/` with marker comments; Oban excluded (D-04, D-09, D-10)
- `Chimeway.Install.Migrations` implements slug idempotency, namespace rewrite, repo fallback (D-05–D-08)
- Unit tests green; no Mix task or golden fixture yet (wave 2–3)
- `guides/introduction/installation.md` and README **not** modified (D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/35-installer-task/35-01-SUMMARY.md` via orchestration.
</output>
