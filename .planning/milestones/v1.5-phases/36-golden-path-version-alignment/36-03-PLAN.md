---
phase: 36
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - guides/introduction/golden-path.md
  - guides/introduction/getting-started.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - Golden-path includes optional webhook feedback appendix cross-linking demo host E2E test (D-09, D-10).
    - getting-started.md trigger example includes tenant_id (OQ-2); no copy-paste runtime failure.
    - getting-started What's Next points to golden-path and tracing recipe (D-12).
    - Phase 36 grep gates pass across all edited introduction docs.
    - mix ci.docs passes after final doc edits.
  artifacts:
    - path: guides/introduction/golden-path.md
      provides: "Webhook appendix completing DOCS-01 optional feedback loop pointer"
    - path: guides/introduction/getting-started.md
      provides: "Depth guide with fixed trigger contract and golden-path cross-links"
  key_links:
    - from: guides/introduction/golden-path.md
      to: examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs
      via: "Webhook appendix outcome + runnable proof link (D-09)"
    - from: guides/introduction/getting-started.md
      to: guides/introduction/golden-path.md
      via: "What's Next canonical vertical slice pointer (D-12)"
    - from: guides/introduction/getting-started.md
      to: guides/recipes/tracing-a-notification.md
      via: "Explainability depth link"
---

<objective>
Complete Phase 36 cross-guide wiring and verification: add golden-path webhook appendix (D-09/D-10), fix getting-started trigger `tenant_id` gap and Next Steps links (D-12, OQ-2), and run final DOCS-01/DOCS-02 grep + HexDocs gates.

Depends on plans 36-01 (golden-path core) and 36-02 (README/installation alignment).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-golden-path-version-alignment/36-CONTEXT.md
@.planning/phases/36-golden-path-version-alignment/36-RESEARCH.md
@.planning/phases/36-golden-path-version-alignment/36-PATTERNS.md
@.planning/phases/36-golden-path-version-alignment/36-VALIDATION.md
@guides/introduction/golden-path.md
@guides/introduction/getting-started.md
@examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs
@README.md
@guides/introduction/installation.md
</context>

<tasks>

<task type="auto">
  <name>Task 36-03-01: Add golden-path webhook feedback appendix</name>
  <files>guides/introduction/golden-path.md</files>
  <read_first>
    - .planning/phases/36-golden-path-version-alignment/36-CONTEXT.md D-09, D-10
    - .planning/phases/36-golden-path-version-alignment/36-PATTERNS.md Pattern 11
    - examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs (outcome-level proof)
    - guides/introduction/golden-path.md §7 (append after What's next)
  </read_first>
  <action>
    Append new section to `guides/introduction/golden-path.md` after §7:

    **## Next: webhook feedback loop**

    Content (outcome-only, no inline Phoenix webhook tutorial per D-10):
    - When inbound delivery feedback should drive workflow progression, Chimeway surfaces webhook handling on the delivery timeline
    - Two outcome bullets: progress path (succeeded signal → workflow resume → `:webhook_received` on timeline) and stop path (bounced → workflow stopped)
    - Links:
      - `[Demo host example](../../examples/chimeway_demo_host/)`
      - `[Feedback pipeline E2E test](../../examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs)`
    - Closing sentence: feedback appears as `:webhook_received` entries on `Chimeway.Traces.explain_delivery/1` timelines

    Do NOT duplicate Phase 34 progression internals, demo controller setup, or Oban wiring.
  </action>
  <acceptance_criteria>
    - `grep -c 'webhook feedback loop' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'feedback_pipeline_e2e_test' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'chimeway_demo_host' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'webhook_received' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'explain_delivery' guides/introduction/golden-path.md` ≥ 2 (§6 proof + appendix mention)
    - Appendix does not contain `Oban.insert` or controller route definitions (grep count 0 for each)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'feedback_pipeline_e2e_test' guides/introduction/golden-path.md && grep -q 'webhook_received' guides/introduction/golden-path.md && ! grep -q 'Oban.insert' guides/introduction/golden-path.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 36-03-02: Fix getting-started tenant_id and update What's Next</name>
  <files>guides/introduction/getting-started.md</files>
  <read_first>
    - guides/introduction/getting-started.md §2 (missing tenant_id at lines 50–53)
    - .planning/phases/36-golden-path-version-alignment/36-RESEARCH.md OQ-2
    - .planning/phases/36-golden-path-version-alignment/36-PATTERNS.md Pattern 10
    - test/chimeway/integration/trigger_explain_test.exs (missing_tenant_id)
  </read_first>
  <action>
    In `guides/introduction/getting-started.md` §2 Trigger a Notification:

    Replace:
    ```elixir
    opts = [idempotency_key: "signup_user_12345"]

    {:ok, trigger_result} = Chimeway.trigger(MyApp.Notifiers.WelcomeUser, params, opts)
    ```
    With:
    ```elixir
    {:ok, trigger_result} =
      Chimeway.trigger(
        MyApp.Notifiers.WelcomeUser,
        params,
        idempotency_key: "signup_user_12345",
        tenant_id: "default"
      )
    ```

    Add one sentence after the snippet: "Both `:idempotency_key` and `:tenant_id` are required."

    Replace ## What's Next? section opening with:
    "If you haven't yet, start with the [Golden Path](golden-path.md) for the canonical install-to-trace vertical slice including `Chimeway.Traces.explain_delivery/1`."

    Add to bullet list (before existing bullets):
    "- [Tracing a Notification](../recipes/tracing-a-notification.md) for explainability and telemetry depth"

    Keep §3 inbox section unchanged (depth guide; golden-path owns explainability proof per D-04).
  </action>
  <acceptance_criteria>
    - `grep -c 'tenant_id:' guides/introduction/getting-started.md` ≥ 1
    - `grep -c 'idempotency_key:' guides/introduction/getting-started.md` ≥ 1
    - `grep -c 'golden-path.md' guides/introduction/getting-started.md` ≥ 1
    - `grep -c 'explain_delivery' guides/introduction/getting-started.md` ≥ 1
    - `grep -c 'tracing-a-notification' guides/introduction/getting-started.md` ≥ 1
    - `grep -c 'def recipients' guides/introduction/getting-started.md` ≥ 1 (notifier pattern preserved)
    - `rg 'resolve_recipients|identity:' guides/introduction/getting-started.md` returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'tenant_id:' guides/introduction/getting-started.md && grep -q 'golden-path.md' guides/introduction/getting-started.md && ! rg -q 'resolve_recipients|identity:' guides/introduction/getting-started.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 36-03-03: Run Phase 36 verification gates and HexDocs build</name>
  <files>guides/introduction/golden-path.md
guides/introduction/getting-started.md
README.md
guides/introduction/installation.md
mix.exs</files>
  <read_first>
    - .planning/phases/36-golden-path-version-alignment/36-VALIDATION.md (full gate table)
    - .planning/phases/36-golden-path-version-alignment/36-RESEARCH.md Validation Architecture
    - All files modified across plans 36-01, 36-02, 36-03
  </read_first>
  <action>
    Run Phase 36 manual grep gates and automated docs build. Fix any failures in edited files (do not expand scope to other guides).

    **Gate 1 — Version alignment:**
    ```bash
    rg '~> 1\.0|1\.0\.0' README.md guides/introduction/
    ```
    Expected: zero matches.

    **Gate 2 — API alignment:**
    ```bash
    rg 'resolve_recipients|identity:' README.md guides/introduction/golden-path.md guides/introduction/getting-started.md
    ```
    Expected: zero matches.

    **Gate 3 — Required trigger opts in golden-path:**
    ```bash
    test "$(grep -c 'Chimeway\.trigger' guides/introduction/golden-path.md)" -eq "$(grep -c 'idempotency_key:' guides/introduction/golden-path.md)" && \
    test "$(grep -c 'Chimeway\.trigger' guides/introduction/golden-path.md)" -eq "$(grep -c 'tenant_id:' guides/introduction/golden-path.md)"
    ```
    Expected: exit 0 (every trigger includes both opts).

    **Gate 4 — Version source-of-truth alignment:**
    ```bash
    grep '@version "0.1.0"' mix.exs && \
    rg '{:chimeway, "~> 0.1"}' README.md guides/introduction/ && \
    ! rg '{:chimeway, "~> 1\.|~> 1\.0' README.md guides/introduction/
    ```
    Expected: mix.exs @version is 0.1.0; all dep blocks use ~> 0.1; no 1.x drift.

    **Gate 5 — Cross-link enumeration:**
    - README links golden-path
    - installation Next Steps links golden-path
    - getting-started What's Next links golden-path

    **Gate 6 — HexDocs:**
    ```bash
    mix ci.docs
    ```

    **Gate 7 — Full CI (no regression):**
    ```bash
    mix ci
    ```

    Document pass/fail in plan summary; fix doc issues only — no engine changes.
  </action>
  <acceptance_criteria>
    - `rg '~> 1\.0|1\.0\.0' README.md guides/introduction/` exits 1 (no matches)
    - `rg 'resolve_recipients|identity:' README.md guides/introduction/golden-path.md guides/introduction/getting-started.md` exits 1 (no matches)
    - `grep -c '@version "0.1.0"' mix.exs` ≥ 1
    - `grep -c '{:chimeway, "~> 0.1"}' README.md guides/introduction/golden-path.md guides/introduction/installation.md` — each file ≥ 1
    - `grep -c 'golden-path' README.md guides/introduction/installation.md guides/introduction/getting-started.md` — each file ≥ 1
    - Trigger opt count gate on golden-path: `Chimeway.trigger` count equals `idempotency_key:` count equals `tenant_id:` count
    - `mix ci.docs` exits 0
    - `mix ci` exits 0
    - golden-path contains sections through webhook appendix; getting-started has tenant_id on trigger
  </acceptance_criteria>
  <verify>
    <automated>grep -q '@version "0.1.0"' mix.exs && ! rg -q '~> 1\.0|1\.0\.0' README.md guides/introduction/ && ! rg -q 'resolve_recipients|identity:' README.md guides/introduction/golden-path.md guides/introduction/getting-started.md && test "$(grep -c 'Chimeway\.trigger' guides/introduction/golden-path.md)" -eq "$(grep -c 'idempotency_key:' guides/introduction/golden-path.md)" && mix ci.docs && mix ci</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Golden-path appendix → adopter expectations | Must not imply webhook setup is part of minimal path |
| getting-started → copy-paste trigger | Missing tenant_id causes silent adoption failure |
| Cross-guide links → navigation | Broken relative links frustrate onboarding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-03-01 | Tampering | Webhook appendix scope creep | Medium | mitigate | Outcome-only copy; link to E2E test; no Oban/controller inline setup (D-10) |
| T-36-03-02 | Tampering | getting-started trigger missing tenant_id | High | mitigate | Add tenant_id: "default" to §2; prose documents requirement |
| T-36-03-03 | Spoofing | Conflicting trigger examples across guides | Medium | mitigate | Both golden-path and getting-started show idempotency_key + tenant_id |
| T-36-03-04 | Repudiation | Phase 36 claimed complete without grep gates | Medium | mitigate | Task 36-03-03 runs version + API grep and mix ci.docs |
| T-36-03-05 | Denial of Service | Broken relative links in cross-guide nav | Low | mitigate | Verify golden-path.md links resolve from guides/introduction/ paths |

**Block gate:** T-36-03-02 is HIGH — getting-started tenant_id fix must ship before phase sign-off.
</threat_model>

<verification>
1. `rg 'feedback_pipeline_e2e_test' guides/introduction/golden-path.md` → ≥ 1 match (D-09)
2. `grep 'tenant_id:' guides/introduction/getting-started.md` → ≥ 1 match
3. `rg '~> 1\.0|1\.0\.0' README.md guides/introduction/` → zero matches (DOCS-02)
4. `rg 'resolve_recipients|identity:' README.md guides/introduction/` → zero matches
5. `mix ci.docs` and `mix ci` → exit 0
6. Recommended once: fresh Phoenix host follows golden-path → IEx → explain_delivery/1 → status :succeeded (manual UAT per 36-VALIDATION.md)
</verification>

<success_criteria>
- DOCS-01 complete: golden-path covers vertical slice + optional webhook appendix pointer
- DOCS-02 complete: all consumer version strings aligned; cross-guide navigation wired
- getting-started trigger copy-paste safe with tenant_id
- All Phase 36 grep gates green; CI passes
- No engine/API code changes
</success_criteria>

<output>
After completion, create `.planning/phases/36-golden-path-version-alignment/36-03-SUMMARY.md` via orchestration.
</output>
