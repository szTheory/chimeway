---
phase: 36
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/introduction/golden-path.md
  - mix.exs
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - guides/introduction/golden-path.md exists as the primary DOCS-01 vertical-slice guide (D-01).
    - Guide walks dependency → mix chimeway.gen.migrations → mix ecto.migrate → config (repo + Chimeway.Repo shared DB) → notifier → trigger with idempotency_key and tenant_id → explain_delivery/1 proof (D-04, D-05).
    - Notifier examples use recipients/1 with recipient_identity and recipient_type — never resolve_recipients/2 or identity keys (D-03).
    - Dependency snippet uses {:chimeway, "~> 0.1"} aligned to mix.exs @version "0.1.0" (D-06).
    - golden-path.md registered in mix.exs docs/[:extras] after installation.md (D-11).
  artifacts:
    - path: guides/introduction/golden-path.md
      provides: "Canonical install-to-explainable-trace onboarding spine"
    - path: mix.exs
      provides: "HexDocs extras registration for golden-path under Introduction group"
  key_links:
    - from: guides/introduction/golden-path.md
      to: guides/introduction/installation.md
      via: "Links to installation §2–§4 instead of duplicating full config blocks (D-02)"
    - from: guides/introduction/golden-path.md
      to: lib/chimeway/traces.ex
      via: "explain_delivery/1 IEx snippets match moduledoc contract (D-04, D-05)"
    - from: mix.exs docs/[:extras]
      to: guides/introduction/golden-path.md
      via: "HexDocs publish path (D-11)"
---

<objective>
Create `guides/introduction/golden-path.md` — the DOCS-01 vertical-slice guide from dependency through first explainable trace — and register it in `mix.exs` HexDocs extras (D-01, D-11).

This plan delivers the core golden-path content (sections 1–7). Webhook appendix (D-09/D-10) and cross-guide Next Steps wiring (D-12) land in plans 36-02 and 36-03. README and installation version fixes are DOCS-02 scope in plan 36-02.

**Critical doc truths (from research):** Every `Chimeway.trigger/3` example must include `idempotency_key` and `tenant_id`. Validation uses `Chimeway.Traces.explain_delivery/1` on `result.trace.delivery_ids`, not inbox listing alone. Host must configure `config :chimeway, Chimeway.Repo, ...` pointing at the same database as host migrations — `config :chimeway, repo:` alone is installer-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-golden-path-version-alignment/36-CONTEXT.md
@.planning/phases/36-golden-path-version-alignment/36-RESEARCH.md
@.planning/phases/36-golden-path-version-alignment/36-PATTERNS.md
@.planning/phases/36-golden-path-version-alignment/36-VALIDATION.md
@guides/introduction/installation.md
@guides/introduction/getting-started.md
@lib/chimeway/notifier.ex
@lib/chimeway/trigger.ex
@lib/chimeway/traces.ex
@lib/chimeway/traces/explanation.ex
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 36-01-01: Create golden-path.md sections 1–3 (dependency, schema, config)</name>
  <files>guides/introduction/golden-path.md</files>
  <read_first>
    - .planning/phases/36-golden-path-version-alignment/36-CONTEXT.md D-01, D-02, D-06
    - .planning/phases/36-golden-path-version-alignment/36-PATTERNS.md Pattern 1, Pattern 9
    - guides/introduction/installation.md (§1–§4 structure to link into, not duplicate)
    - mix.exs line 4 (@version "0.1.0")
    - lib/mix/tasks/chimeway.gen.migrations.ex (@moduledoc usage)
    - config/dev.exs (Chimeway.Repo shared-DB pattern)
  </read_first>
  <action>
    Create `guides/introduction/golden-path.md` with title `# Golden Path` and opening paragraph stating this is the canonical path from dependency to first explainable trace.

    Add numbered H2 sections:

    **## 1. Add the dependency**
    - Code block with `{:chimeway, "~> 0.1"}` (exact string)
    - Bash: `mix deps.get`

    **## 2. Install database schema**
    - Bash: `mix chimeway.gen.migrations` then `mix ecto.migrate`
    - One sentence linking detailed install steps to [Installation §2](installation.md#2-generate-and-run-migrations) (D-02)

    **## 3. Configure Chimeway**
    - Include BOTH config blocks (research OQ-1):
      1. `config :chimeway, repo: MyApp.Repo` — note this is for the installer task
      2. `config :chimeway, Chimeway.Repo, username: ..., password: ..., hostname: ..., database: "my_app_dev", pool_size: 10` — explicit prose that runtime queries use `Chimeway.Repo` and it must point at the **same database** where host migrations created `chimeway_*` tables
    - Link to [Installation §3–§4](installation.md#3-configuration) for supervisor setup; do not paste full application.ex block verbatim (D-02)
    - One sentence that host must add `{Chimeway.Application, []}` (or equivalent) to the application supervisor per Installation §4 — grep target: `Chimeway.Application`
  </action>
  <acceptance_criteria>
    - File `guides/introduction/golden-path.md` exists
    - `grep -c '{:chimeway, "~> 0.1"}' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'mix chimeway.gen.migrations' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'mix ecto.migrate' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'config :chimeway, repo:' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'config :chimeway, Chimeway.Repo' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'Chimeway.Application' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'same database' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'installation.md' guides/introduction/golden-path.md` ≥ 1
    - `grep -c '~> 1\.0' guides/introduction/golden-path.md` equals 0
  </acceptance_criteria>
  <verify>
    <automated>test -f guides/introduction/golden-path.md && grep -q '{:chimeway, "~> 0.1"}' guides/introduction/golden-path.md && grep -q 'config :chimeway, repo:' guides/introduction/golden-path.md && grep -q 'config :chimeway, Chimeway.Repo' guides/introduction/golden-path.md && grep -q 'Chimeway.Application' guides/introduction/golden-path.md && ! grep -q '~> 1\.0' guides/introduction/golden-path.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 36-01-02: Add golden-path sections 4–5 (notifier + trigger)</name>
  <files>guides/introduction/golden-path.md</files>
  <read_first>
    - .planning/phases/36-golden-path-version-alignment/36-CONTEXT.md D-03
    - .planning/phases/36-golden-path-version-alignment/36-PATTERNS.md Pattern 6, Pattern 7
    - lib/chimeway/notifier.ex (@callback recipients/1, __using__ macro)
    - lib/chimeway/trigger.ex (fetch_tenant_id/1, normalize_trigger_result)
    - guides/introduction/getting-started.md §1 (correct notifier pattern)
    - test/chimeway/integration/trigger_explain_test.exs (missing_tenant_id test)
  </read_first>
  <action>
    Append to `guides/introduction/golden-path.md`:

    **## 4. Define a minimal :in_app notifier**
    - Full module `MyApp.Notifiers.WelcomeUser` using `use Chimeway.Notifier` with explicit `@impl` callbacks:
      - `notification_key/0` → `"welcome_user"`
      - `version/0` → `1`
      - `recipients/1` → `{:ok, [%{recipient_identity: params.user_id, recipient_type: "user"}]}`
      - `build/2` → subject + body map
    - Omit `channels/2` (defaults to `:in_app`)
    - Do NOT use `use Chimeway.Notifier, notification_key: ...` macro opts
    - Do NOT use `resolve_recipients/2`, `identity`, or `type` map keys

    **## 5. Trigger your first notification**
    - IEx-ready snippet:
      ```elixir
      params = %{user_id: "user_12345", name: "Alice"}

      {:ok, result} =
        Chimeway.trigger(
          MyApp.Notifiers.WelcomeUser,
          params,
          idempotency_key: "signup_user_12345",
          tenant_id: "default"
        )
      ```
    - Prose note: both `:idempotency_key` and `:tenant_id` are required; omitting `tenant_id` returns `{:error, :missing_tenant_id}`
    - Mention `result.trace.event_id`, `result.trace.delivery_ids`, and `result.trace.correlation_id` as trace pointers
  </action>
  <acceptance_criteria>
    - `grep -c 'def recipients' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'recipient_identity' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'recipient_type' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'tenant_id:' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'idempotency_key:' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'missing_tenant_id' guides/introduction/golden-path.md` ≥ 1
    - `rg 'resolve_recipients|identity:' guides/introduction/golden-path.md` returns 0 matches
    - Every `Chimeway.trigger` occurrence includes both opts: `grep -c 'Chimeway\.trigger' guides/introduction/golden-path.md` equals `grep -c 'idempotency_key:' guides/introduction/golden-path.md` and equals `grep -c 'tenant_id:' guides/introduction/golden-path.md` (count gate; each trigger block must list both opts)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'recipient_identity' guides/introduction/golden-path.md && ! rg -q 'resolve_recipients|identity:' guides/introduction/golden-path.md && test "$(grep -c 'Chimeway\.trigger' guides/introduction/golden-path.md)" -eq "$(grep -c 'idempotency_key:' guides/introduction/golden-path.md)" && test "$(grep -c 'Chimeway\.trigger' guides/introduction/golden-path.md)" -eq "$(grep -c 'tenant_id:' guides/introduction/golden-path.md)"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 36-01-03: Add golden-path section 6 (explain_delivery proof) and section 7 (What's next)</name>
  <files>guides/introduction/golden-path.md</files>
  <read_first>
    - .planning/phases/36-golden-path-version-alignment/36-CONTEXT.md D-04, D-05
    - .planning/phases/36-golden-path-version-alignment/36-PATTERNS.md Pattern 8
    - lib/chimeway/traces.ex moduledoc (IEx examples lines 16–28)
    - lib/chimeway/traces/explanation.ex (status, suppression_reason, timeline fields)
    - test/chimeway/trigger_pipeline_test.exs (trace.delivery_ids assertions)
  </read_first>
  <action>
    Append to `guides/introduction/golden-path.md`:

    **## 6. Prove explainability**
    - Primary proof path using `result` from §5:
      ```elixir
      [delivery_id | _] = result.trace.delivery_ids

      {:ok, explanation} = Chimeway.Traces.explain_delivery(delivery_id)

      explanation.status
      #=> :succeeded

      explanation.suppression_reason
      #=> nil

      Enum.map(explanation.timeline, & &1.event)
      ```
    - Prose: inbox listing proves a message exists; `explain_delivery/1` answers *why* it sent, failed, or was suppressed (D-04)
    - Alternate one-liner: `Chimeway.Traces.get_trace(result.trace.event_id)` with note to flat_map deliveries
    - Optional (Claude discretion): one-line `correlation_id` opt on trigger + `find_traces_by_correlation_id/1`

    **## 7. What's next?**
    - Bullet links (placeholders OK for guides not yet edited in this plan):
      - [Getting Started](getting-started.md) for inbox and channel depth
      - [Tracing a Notification](../recipes/tracing-a-notification.md) for telemetry and diagnosis depth
    - Do NOT add webhook appendix here (plan 36-03)
  </action>
  <acceptance_criteria>
    - `grep -c 'explain_delivery' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'explanation.status' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'explanation.suppression_reason' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'explanation.timeline' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'result.trace.delivery_ids' guides/introduction/golden-path.md` ≥ 1
    - `grep -c 'list_for_recipient' guides/introduction/golden-path.md` equals 0 (inbox is not the proof step)
    - `grep -c 'tracing-a-notification' guides/introduction/golden-path.md` ≥ 1
    - `grep -cE '^## [1-7]\.' guides/introduction/golden-path.md` ≥ 7 (numbered sections 1–7 present)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'explain_delivery' guides/introduction/golden-path.md && grep -q 'explanation.status' guides/introduction/golden-path.md && ! grep -q 'list_for_recipient' guides/introduction/golden-path.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 36-01-04: Register golden-path in mix.exs docs extras</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs docs/0 function (lines 94–116)
    - .planning/phases/36-golden-path-version-alignment/36-PATTERNS.md Pattern 4
    - .planning/phases/36-golden-path-version-alignment/36-VALIDATION.md (task 36-01-04)
  </read_first>
  <action>
    In `mix.exs` `docs/0` `extras` list, insert `"guides/introduction/golden-path.md"` immediately after `"guides/introduction/installation.md"` and before `"guides/flows/trigger-to-delivery.md"`.

    Do not change `groups_extras` — Introduction regex auto-groups `guides/introduction/*.md`.

    Run `mix ci.docs` to confirm HexDocs build passes with warnings-as-errors.
  </action>
  <acceptance_criteria>
    - `grep -c 'guides/introduction/golden-path.md' mix.exs` ≥ 1
    - Line-order gate: `rg -n 'guides/introduction/(installation|golden-path|trigger-to-delivery)\.md' mix.exs` shows installation before golden-path before trigger-to-delivery
    - `mix ci.docs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'guides/introduction/golden-path.md' mix.exs && python3 -c "import re,sys; t=open('mix.exs').read(); m=re.findall(r'guides/introduction/(installation|golden-path|trigger-to-delivery)\\.md', t); sys.exit(0 if m==['installation','golden-path','trigger-to-delivery'] else 1)" && mix ci.docs</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer → golden-path.md | Author-controlled documentation; no runtime host input |
| Adopter → copy-paste snippets | Reader executes doc examples in their host app |
| HexDocs → published extras | mix.exs docs list controls what ships to hexdocs.pm |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-01-01 | Tampering | Doc misinformation — wrong API (`resolve_recipients/2`) | High | mitigate | Grep gate rejects `resolve_recipients` and `identity:` keys; copy from notifier.ex + getting-started §1 |
| T-36-01-02 | Tampering | Doc misinformation — missing `tenant_id` on trigger | High | mitigate | Every trigger example includes `tenant_id:`; prose documents `:missing_tenant_id` error |
| T-36-01-03 | Tampering | Shared-DB config omission | High | mitigate | §3 documents `config :chimeway, Chimeway.Repo` pointing at host database explicitly |
| T-36-01-04 | Spoofing | Version confusion (`~> 1.0.0` in golden-path) | Medium | mitigate | Dep block locked to `{:chimeway, "~> 0.1"}`; grep rejects `~> 1.0` |
| T-36-01-05 | Repudiation | Explainability claim without proof step | Medium | mitigate | §6 requires `explain_delivery/1` with status/suppression_reason/timeline; forbids inbox-only proof |
| T-36-01-06 | Denial of Service | Unpublished guide (HexDocs gap) | Low | mitigate | Register in mix.exs extras; verify with `mix ci.docs` |

**Block gate:** T-36-01-01, T-36-01-02, T-36-01-03 are HIGH — incorrect API, missing tenant_id, or missing Chimeway.Repo config will cause fresh-host adoption failures. Must not merge until acceptance criteria pass.
</threat_model>

<verification>
1. Section checklist: golden-path covers D-01 steps (dependency → migrations → config → notifier → trigger → trace)
2. `rg 'resolve_recipients|identity:' guides/introduction/golden-path.md` → zero matches
3. `rg 'explain_delivery' guides/introduction/golden-path.md` → ≥ 1 match
4. `grep 'golden-path' mix.exs` → registered in extras
5. `mix ci.docs` → exits 0
</verification>

<success_criteria>
- `guides/introduction/golden-path.md` exists with sections 1–7 covering full vertical slice (DOCS-01 core)
- Notifier and trigger examples match engine API; `tenant_id` and `idempotency_key` on every trigger
- Explainability proof uses `Chimeway.Traces.explain_delivery/1` (D-04, D-05)
- Shared-database `Chimeway.Repo` config documented (research OQ-1)
- golden-path registered in HexDocs extras (D-11)
- README, installation.md, getting-started.md, webhook appendix unchanged (plans 36-02, 36-03)
</success_criteria>

<output>
After completion, create `.planning/phases/36-golden-path-version-alignment/36-01-SUMMARY.md` via orchestration.
</output>
