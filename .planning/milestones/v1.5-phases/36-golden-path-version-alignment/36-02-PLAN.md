---
phase: 36
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - README.md
  - guides/introduction/installation.md
autonomous: true
requirements:
  - DOCS-02
must_haves:
  truths:
    - Consumer-facing version strings align to mix.exs @version "0.1.0" as {:chimeway, "~> 0.1"} everywhere (D-06, D-07).
    - installation.md line 12 fixed from ~> 1.0.0 to ~> 0.1.
    - README Quick Start no longer uses resolve_recipients/2, identity/type keys, or macro notification_key opts (D-08).
    - README points to golden-path as primary onboarding; Documentation section lists golden-path first.
    - README install steps mention mix chimeway.gen.migrations before mix ecto.migrate.
    - installation.md Next Steps recommends golden-path before getting-started (D-12).
  artifacts:
    - path: README.md
      provides: "Thin first-touch pointer with correct dep and API surface"
    - path: guides/introduction/installation.md
      provides: "Version-aligned install reference with golden-path Next Steps"
  key_links:
    - from: README.md
      to: guides/introduction/golden-path.md
      via: "Quick Start and Documentation section primary link (D-08)"
    - from: guides/introduction/installation.md
      to: guides/introduction/golden-path.md
      via: "Next Steps recommended post-install path (D-12)"
    - from: README.md
      to: mix.exs @version
      via: "Both use ~> 0.1 constraint (DOCS-02)"
---

<objective>
Align consumer-facing version strings and first-touch onboarding surfaces for DOCS-02: fix installation guide semver drift, rewrite README as a thin pointer to golden-path with correct API hints, and route installation Next Steps to golden-path (D-06, D-08, D-12).

Depends on plan 36-01 — README and installation Next Steps link to `guides/introduction/golden-path.md` which must exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-golden-path-version-alignment/36-CONTEXT.md
@.planning/phases/36-golden-path-version-alignment/36-RESEARCH.md
@.planning/phases/36-golden-path-version-alignment/36-PATTERNS.md
@README.md
@guides/introduction/installation.md
@guides/introduction/golden-path.md
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 36-02-01: Fix installation.md version string and add Chimeway.Repo cross-link</name>
  <files>guides/introduction/installation.md</files>
  <read_first>
    - guides/introduction/installation.md (line 12 drift)
    - mix.exs line 4 (@version "0.1.0")
    - .planning/phases/36-golden-path-version-alignment/36-CONTEXT.md D-06, D-07
    - guides/introduction/golden-path.md §3 (Chimeway.Repo shared-DB note)
  </read_first>
  <action>
    In `guides/introduction/installation.md` §1 dependency block, replace:
    `{:chimeway, "~> 1.0.0"}`
    with:
    `{:chimeway, "~> 0.1"}`

    After §3 Configuration block (after the `config :chimeway, repo: MyApp.Repo` example), add one short paragraph:
    "At runtime, Chimeway queries through `Chimeway.Repo`. Configure it to use the same database where your host migrations created the `chimeway_*` tables — see [Golden Path §3](golden-path.md#3-configure-chimeway) for the full shared-database setup."

    Do NOT change §2 task name or §4 supervisor block. Do NOT edit Next Steps yet (task 36-02-03).
  </action>
  <acceptance_criteria>
    - `grep -c '{:chimeway, "~> 0.1"}' guides/introduction/installation.md` ≥ 1
    - `grep -c '~> 1.0.0' guides/introduction/installation.md` equals 0
    - `grep -c '~> 1\.0' guides/introduction/installation.md` equals 0
    - `grep -c 'Chimeway.Repo' guides/introduction/installation.md` ≥ 1
    - `grep -c 'golden-path.md' guides/introduction/installation.md` ≥ 1
    - `grep -c 'mix chimeway.gen.migrations' guides/introduction/installation.md` ≥ 1 (unchanged from Phase 35)
  </acceptance_criteria>
  <verify>
    <automated>grep -q '{:chimeway, "~> 0.1"}' guides/introduction/installation.md && ! grep -q '~> 1.0' guides/introduction/installation.md && grep -q 'Chimeway.Repo' guides/introduction/installation.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 36-02-02: Rewrite README as thin golden-path pointer</name>
  <files>README.md</files>
  <read_first>
    - README.md (broken Quick Start lines 27–53)
    - .planning/phases/36-golden-path-version-alignment/36-CONTEXT.md D-08
    - .planning/phases/36-golden-path-version-alignment/36-PATTERNS.md Pattern 3
    - lib/chimeway/notifier.ex (real callback API)
    - guides/introduction/golden-path.md (link target)
  </read_first>
  <action>
    **Installation section:** After `mix deps.get`, add `mix chimeway.gen.migrations` before `mix ecto.migrate`. Keep dep block as `{:chimeway, "~> 0.1"}` (already correct).

    **Replace Quick Start section entirely:**
    - Remove the broken `MyApp.OrderShippedNotifier` module with `resolve_recipients/2` and `identity`/`type` keys
    - Remove `use Chimeway.Notifier, notification_key: ..., version: ...` pattern
    - New prose: "Follow the [Golden Path guide](guides/introduction/golden-path.md) for install, notifier setup, and your first explainable trace."
    - Single minimal trigger example (no full notifier module):
      ```elixir
      Chimeway.trigger(MyApp.Notifiers.WelcomeUser, %{user_id: "u1", name: "Ada"},
        idempotency_key: "welcome-u1",
        tenant_id: "default"
      )
      ```

    **Documentation section:** Reorder to list golden-path first:
    - [Golden Path Guide](guides/introduction/golden-path.md)
    - [Hex Docs](https://hexdocs.pm/chimeway)
    - [Installation Guide](guides/introduction/installation.md)
    - [Getting Started Guide](guides/introduction/getting-started.md)
    - Keep existing flow/cheatsheet links if present

    Preserve value prop paragraph, badges, and License section unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c 'golden-path.md' README.md` ≥ 2 (Quick Start + Documentation)
    - `grep -c 'tenant_id:' README.md` ≥ 1
    - `grep -c 'mix chimeway.gen.migrations' README.md` ≥ 1
    - `grep -c '{:chimeway, "~> 0.1"}' README.md` ≥ 1
    - `rg 'resolve_recipients|identity:' README.md` returns 0 matches
    - `grep -c 'resolve_recipients' README.md` equals 0
    - `grep -c 'OrderShippedNotifier' README.md` equals 0
    - Line-order gate: `rg -n 'Golden Path Guide|Getting Started Guide' README.md` shows Golden Path before Getting Started
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'golden-path.md' README.md && grep -q 'tenant_id:' README.md && ! rg -q 'resolve_recipients|identity:' README.md && python3 -c "import re,sys; t=open('README.md').read(); gp=re.search(r'Golden Path Guide', t); gs=re.search(r'Getting Started Guide', t); sys.exit(0 if gp and gs and gp.start()<gs.start() else 1)"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 36-02-03: Update installation.md Next Steps to golden-path</name>
  <files>guides/introduction/installation.md</files>
  <read_first>
    - .planning/phases/36-golden-path-version-alignment/36-CONTEXT.md D-12
    - .planning/phases/36-golden-path-version-alignment/36-PATTERNS.md Pattern 10
    - guides/introduction/golden-path.md (must exist from 36-01)
  </read_first>
  <action>
    Replace `guides/introduction/installation.md` ## Next Steps section with:

    ```markdown
    ## Next Steps

    Follow the [Golden Path](golden-path.md) guide to define a notifier, trigger your first notification, and verify explainability with `Chimeway.Traces.explain_delivery/1`.

    For inbox and channel depth, continue to [Getting Started](getting-started.md).
    ```

    Remove the old single-link paragraph that only pointed to getting-started.
  </action>
  <acceptance_criteria>
    - `grep -c 'golden-path.md' guides/introduction/installation.md` ≥ 2 (§3 cross-link + Next Steps)
    - `grep -c 'explain_delivery' guides/introduction/installation.md` ≥ 1
    - `grep -c 'Getting Started' guides/introduction/installation.md` ≥ 1
    - Line-order gate: `rg -n 'Golden Path|Getting Started' guides/introduction/installation.md` within ## Next Steps shows Golden Path before Getting Started
    - `rg '~> 1\.0|1\.0\.0' README.md guides/introduction/installation.md` returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Golden Path' guides/introduction/installation.md && grep -q 'explain_delivery' guides/introduction/installation.md && ! rg -q '~> 1\.0|1\.0\.0' README.md guides/introduction/installation.md && python3 -c "import re,sys; t=open('guides/introduction/installation.md').read(); ns=t.split('## Next Steps',1)[-1]; gp=re.search(r'Golden Path', ns); gs=re.search(r'Getting Started', ns); sys.exit(0 if gp and gs and gp.start()<gs.start() else 1)"</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → adopter first impression | Highest-traffic entry point; broken examples block adoption |
| installation.md → install reference | Version drift causes dep resolution failures or false expectations |
| Hex badge → live Hex version | Informational only; not edited in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-02-01 | Spoofing | Version confusion (~> 1.0.0 in installation) | High | mitigate | Replace with ~> 0.1; grep gate across README + guides/introduction/ |
| T-36-02-02 | Tampering | API example drift in README | High | mitigate | Remove resolve_recipients/2; use golden-path link + minimal trigger with tenant_id |
| T-36-02-03 | Repudiation | README implies migrate without gen.migrations | Medium | mitigate | Add mix chimeway.gen.migrations to install bash block |
| T-36-02-04 | Denial of Service | Adopter follows wrong onboarding path | Medium | mitigate | Next Steps and README Documentation list golden-path first (D-12) |
| T-36-02-05 | Information Disclosure | N/A for docs-only phase | Low | accept | No secrets in doc examples |

**Block gate:** T-36-02-01 and T-36-02-02 are HIGH — version drift and broken README API must be fixed before merge.
</threat_model>

<verification>
1. `rg '~> 1\.0|1\.0\.0' README.md guides/introduction/` → zero matches
2. `rg 'resolve_recipients|identity:' README.md` → zero matches
3. Visual: README Quick Start links golden-path; trigger includes tenant_id
4. Visual: installation Next Steps recommends golden-path with explain_delivery mention
5. All three surfaces show `{:chimeway, "~> 0.1"}`: README, installation, golden-path
</verification>

<success_criteria>
- DOCS-02 satisfied: README, installation, golden-path agree on ~> 0.1 / 0.1.0 story (D-06, D-07)
- README is thin pointer — no full broken notifier tutorial (D-08)
- installation Next Steps routes to golden-path (D-12)
- No `~> 1.0.0` remains in consumer docs
- getting-started.md unchanged (plan 36-03)
</success_criteria>

<output>
After completion, create `.planning/phases/36-golden-path-version-alignment/36-02-SUMMARY.md` via orchestration.
</output>
