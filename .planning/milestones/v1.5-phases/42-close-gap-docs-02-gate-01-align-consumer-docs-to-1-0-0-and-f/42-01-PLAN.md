---
phase: 42
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/introduction/installation.md
  - guides/introduction/golden-path.md
  - test/chimeway/doc_contract_test.exs
autonomous: true
requirements:
  - DOCS-02
  - GATE-01
must_haves:
  truths:
    - README, installation, and golden-path show {:chimeway, "~> 1.0"} aligned with mix.exs @version 1.0.0 (D-03, D-04).
    - stale_drift_patterns/2 forbids 0.x drift at major 1 and 1.x drift at major 0 (D-05, D-06).
    - Consumer version alignment test derives expected constraint from mix.exs dynamically — no hard-coded 1.0.0 (D-07).
    - mix ci.verify_gates exits 0 after this plan lands.
  artifacts:
    - path: README.md
      provides: "Consumer dep constraint ~> 1.0"
    - path: guides/introduction/installation.md
      provides: "Consumer dep constraint ~> 1.0"
    - path: guides/introduction/golden-path.md
      provides: "§1 dependency snippet ~> 1.0"
    - path: test/chimeway/doc_contract_test.exs
      provides: "Major-aware stale_drift_patterns/2 helper"
  key_links:
    - from: test/chimeway/doc_contract_test.exs
      to: mix.exs
      via: "Dynamic @version → ~> MAJOR.MINOR derivation (D-07)"
    - from: test/chimeway/doc_contract_test.exs
      to: README.md
      via: "consumer version alignment describe (D-03)"
---

<objective>
Land DOCS-02 consumer version alignment and reconcile doc-contract drift patterns for the 1.0.0 major bump. Closes the regression where mix.exs @version 1.0.0 contradicted consumer docs still on ~> 0.1 and where static drift patterns forbade the correct ~> 1.0 constraint.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-close-gap-docs-02-gate-01-align-consumer-docs-to-1-0-0-and-f/42-CONTEXT.md
@.planning/phases/42-close-gap-docs-02-gate-01-align-consumer-docs-to-1-0-0-and-f/42-RESEARCH.md
@.planning/phases/42-close-gap-docs-02-gate-01-align-consumer-docs-to-1-0-0-and-f/42-VALIDATION.md
@.planning/milestones/v1.5-MILESTONE-AUDIT.md
@test/chimeway/doc_contract_test.exs
@mix.exs
@README.md
@guides/introduction/installation.md
@guides/introduction/golden-path.md
@.planning/phases/41-release-verification-gates/41-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 42-01-01: Align consumer docs to {:chimeway, "~> 1.0"}</name>
  <files>README.md, guides/introduction/installation.md, guides/introduction/golden-path.md</files>
  <read_first>
    - .planning/phases/42-close-gap-docs-02-gate-01-align-consumer-docs-to-1-0-0-and-f/42-CONTEXT.md D-03, D-04
    - mix.exs @version line (SSOT)
    - README.md Quick Start dep block
    - guides/introduction/installation.md §1 dependency block
    - guides/introduction/golden-path.md §1 Add the dependency block
    - test/chimeway/doc_contract_test.exs @consumer_files list
  </read_first>
  <action>
    In each of the three consumer files, ensure the dependency code block contains exactly:

    `{:chimeway, "~> 1.0"}`

    Replace any `{:chimeway, "~> 0.1"}` or patch-level `{:chimeway, "~> 1.0.0"}` occurrences in those dep blocks.

    **Locations:**
    - `README.md` — Quick Start / installation snippet (near top)
    - `guides/introduction/installation.md` — first `deps` code block
    - `guides/introduction/golden-path.md` — §1 "Add the dependency" code block only

    Do NOT change cross-package `../../` links in golden-path §6 — plan 42-02 handles those.

    If working tree already has correct values, verify with grep and make no unnecessary edits.
  </action>
  <acceptance_criteria>
    - `grep -c '{:chimeway, "~> 1.0"}' README.md` ≥ 1
    - `grep -c '{:chimeway, "~> 1.0"}' guides/introduction/installation.md` ≥ 1
    - `grep -c '{:chimeway, "~> 1.0"}' guides/introduction/golden-path.md` ≥ 1
    - `! grep -q '{:chimeway, "~> 0.1"}' README.md guides/introduction/installation.md guides/introduction/golden-path.md`
    - `! grep -q '~> 1.0.0' README.md guides/introduction/installation.md guides/introduction/golden-path.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -q '{:chimeway, "~> 1.0"}' README.md guides/introduction/installation.md guides/introduction/golden-path.md && ! grep -q '{:chimeway, "~> 0.1"}' README.md guides/introduction/installation.md guides/introduction/golden-path.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 42-01-02: Implement major-aware stale_drift_patterns/2</name>
  <files>test/chimeway/doc_contract_test.exs</files>
  <read_first>
    - .planning/phases/42-close-gap-docs-02-gate-01-align-consumer-docs-to-1-0-0-and-f/42-CONTEXT.md D-05, D-06, D-07
    - test/chimeway/doc_contract_test.exs consumer version alignment describe block
    - .planning/phases/41-release-verification-gates/41-01-SUMMARY.md (static @drift_patterns bug history)
    - mix.exs @version "1.0.0"
  </read_first>
  <action>
    In `test/chimeway/doc_contract_test.exs`, within the `describe "consumer version alignment (DOCS-02 / GATE-01)"` block:

    1. Ensure the drift test calls `stale_drift_patterns(major, minor)` with major/minor parsed from `mix.exs` `@version` — not a static `@drift_patterns` module attribute.

    2. Add private function `stale_drift_patterns/2` with these clauses:

    - `stale_drift_patterns("1", "0")` → `["{:chimeway, \"~> 0.1\"}", "0.1.0", ~s({:chimeway, "~> 0.)]`
    - `stale_drift_patterns("0", _minor)` → `["{:chimeway, \"~> 1.0\"}", "1.0.0", ~s({:chimeway, "~> 1.)]`
    - `stale_drift_patterns(major, minor)` fallback → forbid previous major series: `["{:chimeway, \"~> #{prev_major}.#{minor}\"}", ~s({:chimeway, "~> #{prev_major}.)]` where `prev_major = String.to_integer(major) - 1` (empty list if prev_major &lt; 0)

    3. Remove any legacy static `@drift_patterns` module attribute that forbids `~> 1.0` unconditionally.

    4. Keep the existing patch-level constraint regex refute: `~r/\{:chimeway,\s*"~>\s*\d+\.\d+\.\d+"/`

    Do NOT hard-code `"1.0.0"` or `"~> 1.0"` in alignment assertions — derive from `@version`.
  </action>
  <acceptance_criteria>
    - `grep -c 'stale_drift_patterns' test/chimeway/doc_contract_test.exs` ≥ 3
    - `grep -c 'consumer version alignment' test/chimeway/doc_contract_test.exs` ≥ 1
    - `! grep -q '@drift_patterns' test/chimeway/doc_contract_test.exs`
    - `mix ci.verify_gates` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix ci.verify_gates</automated>
  </verify>
</task>

</tasks>

<threat_model>
| Ref | Threat | Severity | Mitigation |
|-----|--------|----------|------------|
| T-42-01 | Version confusion — consumer installs wrong major series | Medium | Dynamic alignment + major-aware drift patterns; mix ci.verify_gates enforces |
</threat_model>

<verification_criteria>
- [ ] All three consumer files contain `{:chimeway, "~> 1.0"}`
- [ ] No `{:chimeway, "~> 0.1"}` in consumer files
- [ ] `stale_drift_patterns/2` present with major 1 and major 0 clauses
- [ ] `mix ci.verify_gates` exits 0
</verification_criteria>
