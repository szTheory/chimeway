---
phase: 40
plan: "02"
type: execute
wave: 2
depends_on:
  - "40-01"
files_modified:
  - chimeway_admin/lib/chimeway_admin/redaction.ex
  - chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex
  - chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex
  - chimeway_admin/lib/chimeway_admin/components/timeline_event.ex
  - chimeway_admin/test/chimeway_admin/redaction_test.exs
  - chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs
autonomous: true
requirements:
  - OPER-01
  - OPER-02
must_haves:
  truths:
    - Search supports recipient ID and correlation ID modes calling the correct Traces functions.
    - Detail view renders Explanation fields and full timeline without Repo access or raw metadata blobs.
    - List and detail views redact recipient identity and sensitive timeline detail keys.
  artifacts:
    - path: chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex
      provides: "OPER-01 search UI (D-08, D-09, D-10)"
    - path: chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex
      provides: "OPER-02 unified timeline (D-11, D-12)"
    - path: chimeway_admin/lib/chimeway_admin/redaction.ex
      provides: "View-layer redaction (D-13, D-14)"
  key_links:
    - from: chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex
      to: lib/chimeway/traces.ex
      via: "Chimeway.Traces.explain_delivery/1 only"
    - from: chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex
      to: lib/chimeway/traces/explanation.ex
      via: "Explanation.timeline event atoms"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-40-03 | PII leaked in list/detail templates | high | `ChimewayAdmin.Redaction` masks emails; timeline component whitelists `:detail` keys |
| T-40-04 | SQL injection via search fields | low | Traces API uses parameterized Ecto queries; LiveView passes strings only to `find_traces_*` |
</threat_model>

<objective>
Wave 2: Implement search and detail LiveViews with redaction helpers — full OPER-01 lookup and OPER-02 timeline surface consuming only `Chimeway.Traces` public API. No engine/API changes (D-18) — if a blocking gap appears, stop and file a follow-up rather than patching core from admin.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-operator-trace-mvp/40-CONTEXT.md
@.planning/phases/40-operator-trace-mvp/40-RESEARCH.md
@lib/chimeway/traces.ex
@lib/chimeway/traces/explanation.ex
@guides/recipes/password-reset-support-trace.md
@lib/chimeway/adapter.ex
</context>

<tasks>

<task type="auto">
  <name>Task 40-02-01: Redaction helpers</name>
  <files>chimeway_admin/lib/chimeway_admin/redaction.ex, chimeway_admin/test/chimeway_admin/redaction_test.exs</files>
  <read_first>
    - .planning/phases/40-operator-trace-mvp/40-CONTEXT.md (D-13, D-14)
    - lib/chimeway/adapter.ex (sensitive field list in moduledoc)
    - lib/chimeway/telemetry.ex
  </read_first>
  <action>
    Implement `ChimewayAdmin.Redaction`:

    1. `redact_recipient/1` — for `"user:123"` show `"user:***123"`; for emails `a***@domain.com` style; pass through opaque IDs unchanged if no `@` or `:` pattern.

    2. `safe_timeline_detail/1` — accept timeline `:detail` map; return only whitelisted keys: `reason`, `outcome`, `event_name`, `step_key`, `adapter`, `status` (drop any key matching ~r/password|token|secret|api_key|auth/i).

    3. Tests assert `"user:abc123"` → contains `***` and does not contain full local part for emails.
  </action>
  <acceptance_criteria>
    - `cd chimeway_admin && mix test test/chimeway_admin/redaction_test.exs` exits 0
    - `chimeway_admin/lib/chimeway_admin/redaction.ex` defines `redact_recipient/1`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 40-02-02: TraceSearchLive — recipient and correlation modes</name>
  <files>chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex, chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs</files>
  <read_first>
    - lib/chimeway/traces.ex (`find_traces_for_recipient/2`, `find_traces_by_correlation_id/1`)
    - chimeway_admin/lib/chimeway_admin/live_auth.ex
    - .planning/phases/40-operator-trace-mvp/40-CONTEXT.md (D-08, D-09, D-10)
  </read_first>
  <action>
    Replace stub `TraceSearchLive` with:

    1. Form fields: `mode` (`"recipient"` | `"correlation"`), `query`, optional `notification_key` (recipient mode only).

    2. `handle_event("search", ...)`:
       - Authorize via existing mount (`:search_traces`).
       - Recipient: `Chimeway.Traces.find_traces_for_recipient(query, notification_key: key, limit: 50)` — flatten notifications → delivery rows.
       - Correlation: `Chimeway.Traces.find_traces_by_correlation_id(query)` — flatten events → notifications → deliveries.

    3. Each result row assign: `delivery_id`, `notification_key`, `channel`, `status`, `inserted_at`, `redacted_recipient` (via `Redaction.redact_recipient/1`).

    4. `live_redirect` to `~p"/deliveries/#{delivery_id}"` on row click (use path helper matching router — relative `/deliveries/:id` under host scope).

    5. LiveView test: with authorized stub, submit recipient search calls Traces (use Mox or inject test module only if needed; prefer integration against Chimeway.Repo test DB if already available — otherwise test assign shape with bypass module).

    Minimum: compile + mount test proving search event updates `@results` assign when Traces returns empty list.
  </action>
  <acceptance_criteria>
    - `rg 'find_traces_for_recipient|find_traces_by_correlation_id' chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex` shows both functions
    - `rg 'Chimeway\.Repo' chimeway_admin/lib/chimeway_admin/live` returns no matches
    - `cd chimeway_admin && mix test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 40-02-03: TraceDetailLive — Explanation timeline</name>
  <files>chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex, chimeway_admin/lib/chimeway_admin/components/timeline_event.ex</files>
  <read_first>
    - lib/chimeway/traces/explanation.ex (fields + timeline entry shape)
    - lib/chimeway/traces.ex (`explain_delivery/1`)
    - guides/recipes/password-reset-support-trace.md (Support Operator diagnostic fields)
    - .planning/phases/40-operator-trace-mvp/40-CONTEXT.md (D-11, D-12, D-17)
  </read_first>
  <action>
    1. `mount(%{"delivery_id" => id}, ...)` — `Chimeway.Traces.explain_delivery(id)`; on `{:error, :not_found}` show friendly empty state.

    2. Assign and render top-level fields: `status`, `suppression_reason`, `planning_reason`, `correlation_id`, `notification_key`, `channel`, `last_attempt` (outcome + error_class only), `recipient_id` redacted.

    3. `ChimewayAdmin.Components.TimelineEvent` — iterate `@explanation.timeline`; render `:at`, humanized `:event` atom, `Redaction.safe_timeline_detail(entry.detail)`.

    4. Include moduledoc comment listing supported timeline events (`:attempt_recorded`, `:suppressed`, `:webhook_received`, `:workflow_progressed`, etc.) — satisfies OPER-02 single-surface requirement.

    5. No references to bell inbox, campaigns, or `aggregate_outcomes` in templates (D-17 scope fence in UI copy footer optional).
  </action>
  <acceptance_criteria>
    - `rg 'explain_delivery' chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex` matches
    - `rg 'timeline' chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex` matches
    - `rg 'metadata' chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex` returns no matches
    - `cd chimeway_admin && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] OPER-01: search by recipient and correlation ID paths exist in LiveView
- [ ] OPER-02: detail template iterates `explanation.timeline`
</verification>
