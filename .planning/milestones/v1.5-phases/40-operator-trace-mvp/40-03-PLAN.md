---
phase: 40
plan: "03"
type: execute
wave: 3
depends_on:
  - "40-02"
files_modified:
  - examples/chimeway_demo_host/mix.exs
  - examples/chimeway_demo_host/lib/demo_host/admin_auth.ex
  - examples/chimeway_demo_host/lib/demo_host_web.ex
  - examples/chimeway_demo_host/lib/demo_host_web/router.ex
  - examples/chimeway_demo_host/lib/demo_host_web/endpoint.ex
  - examples/chimeway_demo_host/config/config.exs
  - examples/chimeway_demo_host/README.md
  - guides/introduction/golden-path.md
autonomous: true
requirements:
  - OPER-01
  - OPER-02
must_haves:
  truths:
    - Demo host mounts chimeway_admin under `/admin/chimeway` with permissive dev auth stub.
    - README documents operator UI walkthrough; golden-path links visual validation step.
    - Scope exclusion for bell inbox and campaign UI is documented in admin README section.
  artifacts:
    - path: examples/chimeway_demo_host/lib/demo_host/admin_auth.ex
      provides: "Dev auth stub (D-15)"
    - path: examples/chimeway_demo_host/lib/demo_host_web/router.ex
      provides: "Mounted admin routes (D-06)"
    - path: examples/chimeway_demo_host/README.md
      provides: "Operator UI proof (D-16)"
  key_links:
    - from: guides/introduction/golden-path.md
      to: examples/chimeway_demo_host/README.md
      via: "Visual trace validation cross-link (D-16)"
    - from: examples/chimeway_demo_host/README.md
      to: .planning/phases/39-demo-host-trace-path/39-CONTEXT.md
      via: "IEx path complement, not replacement"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-40-01 | Dev auth stub shipped to production unchanged | high | README section "Production auth" states stub is dev-only; `AdminAuth` returns `{:error, :unauthorized}` when `Mix.env() == :prod` unless `ALLOW_DEMO_ADMIN` env set (document explicitly) |
| T-40-05 | Admin mounted without host session/auth | medium | Router places admin scope after host documents required `live_session` auth plugs for real deployments |
</threat_model>

<objective>
Wave 3: Wire demo host proof, document mount and operator walkthrough, cross-link golden-path — closes OPER-01 host-auth requirement and OPER-02 adopter-facing validation path.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-operator-trace-mvp/40-CONTEXT.md
@.planning/phases/40-operator-trace-mvp/40-RESEARCH.md
@examples/chimeway_demo_host/mix.exs
@examples/chimeway_demo_host/lib/demo_host_web/router.ex
@examples/chimeway_demo_host/README.md
@guides/introduction/golden-path.md
@.planning/phases/39-demo-host-trace-path/39-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 40-03-01: Demo host deps and AdminAuth stub</name>
  <files>examples/chimeway_demo_host/mix.exs, examples/chimeway_demo_host/lib/demo_host/admin_auth.ex, examples/chimeway_demo_host/config/config.exs</files>
  <read_first>
    - examples/chimeway_demo_host/mix.exs
    - chimeway_admin/lib/chimeway_admin/auth.ex
    - .planning/phases/40-operator-trace-mvp/40-CONTEXT.md (D-15)
  </read_first>
  <action>
    1. Add deps to `examples/chimeway_demo_host/mix.exs`:
       - `{:chimeway_admin, path: "../../chimeway_admin"}`
       - `{:phoenix_live_view, "~> 1.0"}`
       - `{:phoenix_html, "~> 4.0"}` (if required by LiveView)

    2. `config/config.exs` add:
       `config :chimeway_admin, auth_module: DemoHost.AdminAuth`

    3. `DemoHost.AdminAuth` implements `ChimewayAdmin.Auth`:
       - `authorize/3` returns `:ok` when `Mix.env()` in `[:dev, :test]`
       - returns `{:error, :unauthorized}` in `:prod` unless `System.get_env("ALLOW_DEMO_ADMIN") == "true"` (document escape hatch for staging demos only).
  </action>
  <acceptance_criteria>
    - `examples/chimeway_demo_host/mix.exs` contains `chimeway_admin`
    - `examples/chimeway_demo_host/lib/demo_host/admin_auth.ex` contains `@behaviour ChimewayAdmin.Auth`
    - `cd examples/chimeway_demo_host && mix deps.get && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 40-03-02: Browser pipeline and mount admin routes</name>
  <files>examples/chimeway_demo_host/lib/demo_host_web.ex, examples/chimeway_demo_host/lib/demo_host_web/router.ex, examples/chimeway_demo_host/lib/demo_host_web/endpoint.ex</files>
  <read_first>
    - chimeway_admin/lib/chimeway_admin/router.ex
    - examples/chimeway_demo_host/lib/demo_host_web/endpoint.ex
    - examples/chimeway_demo_host/config/config.exs (live_view signing_salt already present)
  </read_first>
  <action>
    1. Extend `DemoHostWeb` with `live_view` and `html` quotes (minimal — no full core_components required; inline HEEx OK).

    2. `router.ex`:
       - Add `pipeline :browser` with `plug :accepts, ["html"]`, `plug :fetch_session`, `plug :fetch_live_flash`, `plug :put_root_layout` (simple layout module or `html: {DemoHostWeb.Layouts, :root}`).
       - `scope "/admin/chimeway"` pipe_through `:browser`, `import ChimewayAdmin.Router`, `chimeway_admin_routes()`.

    3. `endpoint.ex` — add `socket "/live", Phoenix.LiveView.Socket` if missing.

    4. Keep existing `/webhooks/chimeway` API scope unchanged.
  </action>
  <acceptance_criteria>
    - `rg 'chimeway_admin_routes|/admin/chimeway' examples/chimeway_demo_host/lib/demo_host_web/router.ex` matches
    - `rg 'live_view' examples/chimeway_demo_host/lib/demo_host_web.ex` matches
    - `cd examples/chimeway_demo_host && mix test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 40-03-03: Documentation cross-links and scope statement</name>
  <files>examples/chimeway_demo_host/README.md, guides/introduction/golden-path.md</files>
  <read_first>
    - examples/chimeway_demo_host/README.md (Phase 39 IEx section)
    - guides/introduction/golden-path.md (§6–§7 structure)
    - .planning/phases/40-operator-trace-mvp/40-CONTEXT.md (D-16, D-17)
    - .planning/ROADMAP.md (Phase 40 success criteria)
  </read_first>
  <action>
    1. `examples/chimeway_demo_host/README.md` — add **Operator trace UI (browser)** section:
       - Prereq: Phase 39 IEx trace data or `mix demo.trace`
       - Start server: `mix phx.server` from demo host dir
       - Visit `http://localhost:4001/admin/chimeway`
       - Search by recipient from trace demo (`user:...` identity)
       - Open delivery detail; confirm timeline sections
       - **Production auth** warning about `DemoHost.AdminAuth`
       - Explicit **Out of scope**: bell inbox, campaigns, health dashboard (D-17)

    2. `guides/introduction/golden-path.md` — add subsection under validation area (near Phase 39 IEx cross-link):
       - Title like "Validate with operator UI (optional)"
       - Link to demo host README operator section and `chimeway_admin` package
       - State this complements IEx, does not replace webhook E2E proof

    3. Add one line to password-reset recipe Support Operator section cross-linking browser UI (optional, if section exists).
  </action>
  <acceptance_criteria>
    - `rg 'admin/chimeway' examples/chimeway_demo_host/README.md` matches
    - `rg 'operator|chimeway_admin|admin/chimeway' guides/introduction/golden-path.md` matches
    - `rg 'bell inbox|campaign' examples/chimeway_demo_host/README.md` shows explicit exclusion wording
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd examples/chimeway_demo_host && mix test` green
- [ ] Manual: browser walkthrough per 40-VALIDATION.md
</verification>
