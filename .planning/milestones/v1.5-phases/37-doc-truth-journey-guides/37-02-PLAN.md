---
phase: 37
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/recipes/oban-integration.md
autonomous: true
requirements:
  - DOCS-03
must_haves:
  truths:
    - Oban worker modules are Chimeway.Dispatch.WorkflowProgressionWorker and Chimeway.Dispatch.SignalRouterWorker (D-13).
    - Queue names match lib/chimeway/dispatch/ — WorkflowProgressionWorker on chimeway_delivery, SignalRouterWorker on chimeway_signals (D-14).
    - Scheduling model documents per-run due_at jobs; cron is optional fallback not primary (D-14).
    - chimeway_workflows queue removed or marked unused (D-14).
    - Signal semantics clarify pending_signals matching, not "stop conditions" fiction (D-14).
  artifacts:
    - path: guides/recipes/oban-integration.md
      provides: "Accurate Oban integration for workflow dispatch and progression"
  key_links:
    - from: guides/recipes/oban-integration.md
      to: lib/chimeway/dispatch/workflow_progression_worker.ex
      via: "Worker module and queue :chimeway_delivery"
    - from: guides/recipes/oban-integration.md
      to: lib/chimeway/dispatch/signal_router_worker.ex
      via: "Worker module and queue :chimeway_signals"
---

<objective>
Fix `guides/recipes/oban-integration.md` worker paths, queue configuration, and workflow scheduling semantics so it aligns with `lib/chimeway/dispatch/` (D-13, D-14). Supports DOCS-03 by ensuring cross-linked Oban recipe does not reintroduce aspirational Workflows.Workers APIs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-doc-truth-journey-guides/37-CONTEXT.md
@.planning/phases/37-doc-truth-journey-guides/37-RESEARCH.md
@.planning/phases/37-doc-truth-journey-guides/37-PATTERNS.md
@lib/chimeway/dispatch/workflow_progression_worker.ex
@lib/chimeway/dispatch/signal_router_worker.ex
@lib/chimeway/dispatch/oban_worker.ex
@guides/recipes/oban-integration.md
</context>

<tasks>

<task type="auto">
  <name>Task 37-02-01: Fix Oban queue and cron configuration section</name>
  <files>guides/recipes/oban-integration.md</files>
  <read_first>
    - .planning/phases/37-doc-truth-journey-guides/37-CONTEXT.md D-13, D-14
    - .planning/phases/37-doc-truth-journey-guides/37-RESEARCH.md §2 drift inventory
    - lib/chimeway/dispatch/workflow_progression_worker.ex (queue :chimeway_delivery)
    - lib/chimeway/dispatch/signal_router_worker.ex (queue :chimeway_signals)
    - guides/recipes/oban-integration.md lines 31-53
  </read_first>
  <action>
    In `guides/recipes/oban-integration.md` § "Setting Up the Queues and Workers":

    1. Replace cron entry `Chimeway.Workflows.Workers.ProgressionWorker` with note that **primary** wait advancement uses per-run scheduled jobs: when `dispatcher: Chimeway.Dispatch.Oban` is configured, the engine enqueues `Chimeway.Dispatch.WorkflowProgressionWorker` at each run's `due_at`. Optional cron calling `Chimeway.Workflows.Progression.progress_due_runs/1` is a fallback for missed schedules — not the primary model.

    2. Update queues list:
       - Keep `chimeway_delivery: [limit: 20]` — used by dispatch AND WorkflowProgressionWorker
       - Keep `chimeway_signals: [limit: 10]` — used by SignalRouterWorker
       - Remove `chimeway_workflows` queue OR add one sentence: "No Chimeway worker uses `:chimeway_workflows`; omit unless you have host-specific jobs."

    3. If cron example remains, use module string `Chimeway.Dispatch.WorkflowProgressionWorker` only as optional fallback documentation — lead with scheduled-at-due_at explanation.
  </action>
  <acceptance_criteria>
    - `grep -c 'Workflows\.Workers' guides/recipes/oban-integration.md` equals 0
    - `grep -c 'Chimeway\.Dispatch\.WorkflowProgressionWorker' guides/recipes/oban-integration.md` ≥ 1
    - `grep -c 'Chimeway\.Dispatch\.SignalRouterWorker' guides/recipes/oban-integration.md` ≥ 1
    - `grep -c 'chimeway_delivery' guides/recipes/oban-integration.md` ≥ 1
    - `grep -c 'chimeway_signals' guides/recipes/oban-integration.md` ≥ 1
    - `grep -c 'due_at' guides/recipes/oban-integration.md` ≥ 1
    - `grep -c 'chimeway_workflows' guides/recipes/oban-integration.md` equals 0 OR appears only in "omit/unused" context
  </acceptance_criteria>
  <verify>
    <automated>! grep -q 'Workflows\.Workers' guides/recipes/oban-integration.md && grep -q 'Chimeway\.Dispatch\.WorkflowProgressionWorker' guides/recipes/oban-integration.md && grep -q 'Chimeway\.Dispatch\.SignalRouterWorker' guides/recipes/oban-integration.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 37-02-02: Fix Workflow Engine Workers prose and trigger example</name>
  <files>guides/recipes/oban-integration.md</files>
  <read_first>
    - .planning/phases/37-doc-truth-journey-guides/37-RESEARCH.md §2 items 4-5
    - lib/chimeway/workflows.ex route_signal/1 moduledoc
    - guides/recipes/oban-integration.md lines 55-90
  </read_first>
  <action>
    Rewrite § "Workflow Engine Workers":

    1. **WorkflowProgressionWorker** — Evaluates past-due `wait_until` rules via `progress_run/2`. Scheduled per waiting run at `due_at` when using Oban dispatcher.

    2. **SignalRouterWorker** — Processes `Chimeway.Signal.track/4` events asynchronously. Calls `Workflows.route_signal/1` to match `:waiting` runs whose `pending_signals` contains the event name. Does NOT "satisfy stop conditions" — it routes signals to waiting runs; progression rules (`on_outcome`/`stop`) apply on delivery convergence separately.

    3. Fix transactional enqueue example if it uses `WelcomeNotifier.trigger(...)` — change to `Chimeway.trigger(WelcomeNotifier, params, opts)` with `idempotency_key` and `tenant_id` in opts.
  </action>
  <acceptance_criteria>
    - `grep -c 'stop conditions' guides/recipes/oban-integration.md` equals 0
    - `grep -c 'pending_signals' guides/recipes/oban-integration.md` ≥ 1
    - `grep -c 'Chimeway\.trigger' guides/recipes/oban-integration.md` ≥ 1 OR existing trigger example unchanged if already correct
    - `grep -c 'Notifier\.trigger' guides/recipes/oban-integration.md` equals 0
  </acceptance_criteria>
  <verify>
    <automated>! grep -q 'Workflows\.Workers\|stop conditions' guides/recipes/oban-integration.md && grep -q 'pending_signals' guides/recipes/oban-integration.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-02-01 | Tampering | Wrong worker module paths | High | mitigate | Grep forbids Workflows.Workers; require Dispatch.* modules |
| T-37-02-02 | Tampering | Dead chimeway_workflows queue misleads hosts | Medium | mitigate | Remove or document as unused |
| T-37-02-03 | Tampering | Cron-only scheduling hides due_at jobs | Medium | mitigate | Document per-run scheduling as primary |

**Block gate:** T-37-02-01 HIGH — wrong worker paths break production Oban config.
</threat_model>

<verification>
1. `rg 'Workflows\.Workers' guides/` → zero matches repo-wide
2. Worker module strings match lib/chimeway/dispatch/*.ex
3. `mix ci.docs` exits 0
</verification>

<success_criteria>
- oban-integration.md references correct Dispatch worker modules and queues (D-13, D-14)
- No aspirational stop_conditions or Workflows.Workers namespace remains in guides/
</success_criteria>

<output>
After completion, create `.planning/phases/37-doc-truth-journey-guides/37-02-SUMMARY.md` via orchestration.
</output>
