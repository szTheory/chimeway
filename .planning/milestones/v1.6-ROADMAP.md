# Milestone v1.6: Consumer Journey Proof

**Status:** ✅ SHIPPED 2026-05-29
**Phases:** 43–47 (5 phases)
**Total Plans:** 5 (implementation outside standard GSD phase dirs)
**Requirements:** 9/9 satisfied
**Audit:** [v1.6-MILESTONE-AUDIT.md](v1.6-MILESTONE-AUDIT.md)

## Overview

Realistic TeamPulse demo domain with deterministic adopter-copyable seeds, one-command admin spin-up, and shift-left journey proof in CI. Closes the pre-adopter confidence gap between v1.5 adoption surface and production workflow glue.

---

## Phase 43: TeamPulse Domain + Seeds

**Goal:** Minimal B2B SaaS demo domain with persona JTBD notifiers and adopter-copyable seed API.
**Depends on:** v1.5 (Phases 39–40 demo host + operator trace)
**Requirements:** DEMO-02, SEED-01

**Delivered:**

- `DemoHost.Notifiers.{InviteSent, PasswordReset, PaymentReminder}` — stable `notification_key`s mapping SEED-004 personas
- `DemoHost.Seeds` — idempotent `run/0`, scenario helpers (`seed_invite/0`, `password_reset_explanation/0`, `escalation_waiting!/0`)
- `mix demo.seed` + `priv/repo/seeds.exs` — uses `Chimeway.trigger/3`, not internal fixture inserts

---

## Phase 44: Demo Commands

**Goal:** One-command local spin-up with admin URL banner for adopters and maintainers.
**Depends on:** Phase 43
**Requirements:** CMD-01

**Delivered:**

- `lib/mix/tasks/demo.up.ex` (root) — `--check`, `--serve`, banner with admin URL
- `examples/chimeway_demo_host/lib/mix/tasks/demo.admin.ex` — demo host admin spin-up
- `examples/chimeway_demo_host/lib/mix/tasks/demo.seed.ex` — subprocess seed entrypoint

---

## Phase 45: Journey Tests (Engine Paths)

**Goal:** Shift-left proof of invite delivery, suppression explainability, and webhook workflow progression.
**Depends on:** Phases 43–44
**Requirements:** JOUR-01, JOUR-02, JOUR-03

**Delivered:**

- `test/demo_host_web/journey_test.exs` — tagged `:journey` for `mix test --only journey`
- JOUR-01: seed → invite → `:succeeded` delivery
- JOUR-02: seed → password reset → `:suppressed` / `"channel_disabled"`
- JOUR-03: seed → webhook POST → Oban drain → workflow `:active`, `:webhook_received` in timeline

---

## Phase 46: Host-Mount Admin Integration

**Goal:** Prove `chimeway_admin` through demo host router without Playwright.
**Depends on:** Phase 40 (v1.5 operator trace MVP)
**Requirements:** JOUR-04

**Delivered:**

- `test/demo_host_web/admin_trace_live_test.exs` — `GET /admin/chimeway` → search → detail shows `teampulse.invite_sent`
- Host-mount ConnTest + LiveViewTest (INV-004: defer Playwright)

---

## Phase 47: CI Gates + verify.journeys

**Goal:** Separate CI job for journey proof; extend pre-ship bar to quintet.
**Depends on:** Phases 45–46
**Requirements:** JOUR-05, GATE-02

**Delivered:**

- `mix verify.journeys` alias in root `mix.exs`
- CI job `verify_journeys` in `.github/workflows/ci.yml`
- `MAINTAINING.md` pre-ship quintet (extends v1.5 quartet)
- `test/mix/tasks/demo_up_test.exs` — `mix demo.up --check` smoke (JOUR-05)

---

## Verification Bar

Pre-ship quintet:

```bash
mix ci && mix ci.docs && mix ci.verify_gates && mix verify.example && mix verify.journeys
```

Fresh evidence at close (2026-05-29): 647 (`mix ci`) + 86 (`ci.verify_gates`) + 11 (`verify.example`) + 5 (`verify.journeys`) — all green.

---

## Milestone Summary

**Key Decisions:**

- Adoption surface (v1.5) and adoption evidence (v1.6) are distinct milestones — journey CI is not optional polish
- `DemoHost.Seeds` is adopter-copyable public API, not internal test fixtures
- Defer Playwright; host-mount ConnTest + LiveViewTest sufficient for JOUR-04
- `verify.journeys` as separate CI job, not bundled into default `mix ci`

**Issues Resolved:**

- Pre-adopter confidence gap: realistic TeamPulse domain with deterministic seeds
- One-command demo spin-up (`mix demo.up`) with admin URL banner
- Shift-left journey proof in CI before READ/workflow glue milestones

**Issues Deferred:**

- Phase-level GSD artifacts (43–47 dirs, SUMMARY.md, VERIFICATION.md) — implemented outside standard execute-phase workflow
- README webhook path contradiction (TeamPulse escalation vs TraceDemo IEx walkthrough)
- `mix demo.up --check` moduledoc drift; Phoenix.ConnTest deprecation warnings in journey tests

**Technical Debt:**

- Retroactive phase dirs optional for audit trail parity
- Nyquist validation not run (phases implemented outside GSD execute-phase)

**After v1.6:**

1. **v1.7 READ** — read/unread workflow glue (`pending_signals`, inbox→signal)
2. **v1.8 ECOS** — Mailglass / Accrue blueprint slices (SEED-003)
3. **v1.9 INBX** — bell inbox components

---

_For current project status, see .planning/ROADMAP.md_
