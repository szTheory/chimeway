---
phase: 68-admin-truth-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - chimeway_admin/lib/chimeway_admin/components/layout.ex
  - chimeway_admin/lib/chimeway_admin/live/dashboard_live.ex
  - chimeway_admin/lib/chimeway_admin/routes.ex
  - chimeway_admin/test/chimeway_admin/routes_test.exs
  - chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs
  - examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs
autonomous: true
requirements: [ADMIN-01, ADMIN-02]
user_setup: []
must_haves:
  truths:
    - "D-01: route/nav/page tests treat `/`, `/traces`, `/deliveries/:delivery_id`, `/feed`, `/definitions`, `/health`, and `/recovery` as the real shipped admin route map"
    - "D-02: navigation labels and page hierarchy match existing operator pages instead of hiding already-built pages"
    - "D-03: `/admin/chimeway` remains the Command Center"
    - "D-04: Trace Lookup is the primary Command Center action; Health, Recovery, Definitions, and Feed Debug are secondary paths"
    - "D-07: Phase 68 verification uses lightweight route, navigation, mounted-page, and doc-contract tests"
    - "D-08: Phase 68 does not add browser smoke infrastructure, design-system audits, recovery safety hardening, or accessibility audits"
    - "ADMIN-01: an operator landing on `/admin/chimeway` can choose Trace Lookup, Health, Recovery, Definitions, and Feed Debug"
    - "ADMIN-02: command center, traces, detail, feed, definitions, health, and recovery have consistent route helpers, labels, and page hierarchy"
  artifacts:
    - path: "chimeway_admin/test/chimeway_admin/routes_test.exs"
      provides: "prefix-aware route helper contract for all seven admin pages"
      contains: "/admin/chimeway/recovery"
    - path: "chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs"
      provides: "isolated LiveView/page-label and Command Center hierarchy assertions"
      contains: "Open Trace Lookup"
    - path: "examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs"
      provides: "host-mounted `/admin/chimeway` nonblank route and navigation proof using existing LiveViewTest pattern"
  key_links:
    - from: "ChimewayAdmin.Router.chimeway_admin_routes/1"
      to: "ChimewayAdmin.Routes helper tests"
      via: "route suffixes"
      pattern: "deliveries/:delivery_id"
    - from: "ChimewayAdmin.Components.Layout.admin_shell/1"
      to: "LiveView tests"
      via: "sidebar labels"
      pattern: "Command Center|Trace Lookup|Feed Debug|Definitions|Health|Recovery"
    - from: "ChimewayAdmin.Live.DashboardLive"
      to: "ADMIN-01 landing behavior"
      via: "primary CTA and secondary path assertions"
      pattern: "Open Trace Lookup"
---

<objective>
Lock the real admin information architecture with route, navigation, landing-page, and host-mounted page tests.

Purpose: ADMIN-01 and ADMIN-02 require operators to land on the mounted console and understand the available paths through the shipped multi-page admin surface.
Output: Focused admin package and demo-host tests, plus only the minimal admin copy/helper changes required to make the truth contracts pass.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-admin-truth-alignment/68-CONTEXT.md
@.planning/phases/68-admin-truth-alignment/68-RESEARCH.md
@.planning/phases/68-admin-truth-alignment/68-UI-SPEC.md
@.planning/phases/68-admin-truth-alignment/68-VALIDATION.md
@chimeway_admin/lib/chimeway_admin/router.ex
@chimeway_admin/lib/chimeway_admin/routes.ex
@chimeway_admin/lib/chimeway_admin/components/layout.ex
@chimeway_admin/lib/chimeway_admin/live/dashboard_live.ex
@chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex
@chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex
@chimeway_admin/lib/chimeway_admin/live/feed_live.ex
@chimeway_admin/lib/chimeway_admin/live/definitions_live.ex
@chimeway_admin/lib/chimeway_admin/live/health_live.ex
@chimeway_admin/lib/chimeway_admin/live/recovery_live.ex
@chimeway_admin/test/chimeway_admin/routes_test.exs
@chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs
@examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs
</context>

<interfaces>
Route helper expectations with `config :chimeway_admin, path_prefix: "/admin/chimeway"`:
- `Routes.search_path()` -> `/admin/chimeway/`
- `Routes.traces_path()` -> `/admin/chimeway/traces`
- `Routes.delivery_path("abc")` -> `/admin/chimeway/deliveries/abc`
- `Routes.feed_path()` -> `/admin/chimeway/feed`
- `Routes.definitions_path()` -> `/admin/chimeway/definitions`
- `Routes.health_path()` -> `/admin/chimeway/health`
- `Routes.recovery_path()` -> `/admin/chimeway/recovery`

Sidebar labels:
- Command Center
- Trace Lookup
- Feed Debug
- Definitions
- Health
- Recovery

Command Center hierarchy:
- Primary action: `Open Trace Lookup`
- Secondary path labels must include Health, Recovery, Definitions, and Feed Debug.
- Do not introduce icons, new UI packages, theme tokens, Playwright/browser smoke, or accessibility audit tooling in this phase.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Expand route helper coverage to the seven-page route map</name>
  <files>chimeway_admin/test/chimeway_admin/routes_test.exs, chimeway_admin/lib/chimeway_admin/routes.ex</files>
  <read_first>
    - chimeway_admin/test/chimeway_admin/routes_test.exs
    - chimeway_admin/lib/chimeway_admin/routes.ex
    - chimeway_admin/lib/chimeway_admin/router.ex
    - .planning/phases/68-admin-truth-alignment/68-CONTEXT.md
  </read_first>
  <action>
    Ensure `chimeway_admin/test/chimeway_admin/routes_test.exs` asserts every prefix-aware helper for the shipped route map under `/admin/chimeway`: search/command center, traces, delivery detail, feed, definitions, health, and recovery.

    If `ChimewayAdmin.Routes` is missing a helper for one of those existing routes, add the smallest helper function to `chimeway_admin/lib/chimeway_admin/routes.ex` using the existing `path/1` helper style. Do not create a duplicate route map outside the helper module.

    Preserve host-prefix behavior and do not rename existing public helpers unless tests show a helper already has the correct name and only lacks coverage.
  </action>
  <verify>
    <automated>cd chimeway_admin && mix test test/chimeway_admin/routes_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `chimeway_admin/test/chimeway_admin/routes_test.exs` asserts `/admin/chimeway/`.
    - `chimeway_admin/test/chimeway_admin/routes_test.exs` asserts `/admin/chimeway/traces`.
    - `chimeway_admin/test/chimeway_admin/routes_test.exs` asserts `/admin/chimeway/deliveries/abc` or an equivalent delivery id fixture path.
    - `chimeway_admin/test/chimeway_admin/routes_test.exs` asserts `/admin/chimeway/feed`, `/admin/chimeway/definitions`, `/admin/chimeway/health`, and `/admin/chimeway/recovery`.
    - `cd chimeway_admin && mix test test/chimeway_admin/routes_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Route helper contract covers all seven shipped admin pages under the configured prefix.</done>
</task>

<task type="auto">
  <name>Task 2: Assert Command Center hierarchy and sidebar labels in LiveView tests</name>
  <files>chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs, chimeway_admin/lib/chimeway_admin/components/layout.ex, chimeway_admin/lib/chimeway_admin/live/dashboard_live.ex</files>
  <read_first>
    - chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs
    - chimeway_admin/lib/chimeway_admin/components/layout.ex
    - chimeway_admin/lib/chimeway_admin/live/dashboard_live.ex
    - chimeway_admin/lib/chimeway_admin/live/feed_live.ex
    - chimeway_admin/lib/chimeway_admin/live/definitions_live.ex
    - chimeway_admin/lib/chimeway_admin/live/health_live.ex
    - chimeway_admin/lib/chimeway_admin/live/recovery_live.ex
    - .planning/phases/68-admin-truth-alignment/68-UI-SPEC.md
  </read_first>
  <action>
    Extend existing isolated LiveView tests to prove the operator hierarchy without starting browser smoke tooling.

    Add assertions that the Command Center render includes `Command Center`, primary CTA `Open Trace Lookup`, and secondary path labels `Health`, `Recovery`, `Definitions`, and `Feed Debug`. Use the existing test setup/session/on_mount patterns in `trace_search_live_test.exs`.

    Add or extend mounted page label assertions so Feed Debug, Definitions, Health, and Recovery each render their page label through the shared admin shell. Sidebar labels should include Command Center, Trace Lookup, Feed Debug, Definitions, Health, and Recovery.

    If the tests reveal admin copy drift, make the smallest changes in `layout.ex` or `dashboard_live.ex` to match locked labels from 68-UI-SPEC. Do not add new components, design tokens, icons, animations, or visual polish.
  </action>
  <verify>
    <automated>cd chimeway_admin && mix test test/chimeway_admin/live/trace_search_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs` asserts `Command Center`.
    - The test asserts primary CTA `Open Trace Lookup`.
    - The test asserts secondary path labels `Health`, `Recovery`, `Definitions`, and `Feed Debug`.
    - The test asserts sidebar labels `Command Center`, `Trace Lookup`, `Feed Debug`, `Definitions`, `Health`, and `Recovery`.
    - `cd chimeway_admin && mix test test/chimeway_admin/live/trace_search_live_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>LiveView tests prove `/admin/chimeway` presents Trace Lookup as the primary operator job and all secondary operator paths remain visible with locked labels.</done>
</task>

<task type="auto">
  <name>Task 3: Extend demo-host mounted admin proof for landing and navigation truth</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs</files>
  <read_first>
    - examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs
    - examples/chimeway_demo_host/lib/demo_host_web/router.ex
    - examples/chimeway_demo_host/config/config.exs
    - chimeway_admin/lib/chimeway_admin/router.ex
    - chimeway_admin/lib/chimeway_admin/routes.ex
    - .planning/phases/68-admin-truth-alignment/68-VALIDATION.md
  </read_first>
  <action>
    Extend the existing demo-host mounted admin LiveView test pattern to prove `/admin/chimeway` is nonblank and navigable through the host route mount.

    Add assertions against the mounted Command Center response for `Command Center`, `Open Trace Lookup`, `Trace Lookup`, `Feed Debug`, `Definitions`, `Health`, and `Recovery`. If the existing test already visits `/admin/chimeway/traces`, keep that coverage and add only the missing landing/sidebar assertions.

    Do not introduce Playwright, Wallaby, browser screenshots, or `mix verify.admin`; those are Phase 72 scope. Keep this as ConnTest/LiveViewTest coverage under the demo host.

    Note the local PostgreSQL version caveat from research if the demo-host DB-backed test fails only because local PostgreSQL is 14.17 while the project target is 15+. Do not convert environment failures into code changes.
  </action>
  <verify>
    <automated>cd examples/chimeway_demo_host && mix test test/demo_host_web/admin_trace_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs` requests or mounts `/admin/chimeway`.
    - The test asserts `Command Center` and `Open Trace Lookup` in the mounted console.
    - The test asserts `Trace Lookup`, `Feed Debug`, `Definitions`, `Health`, and `Recovery`.
    - No Playwright, browser-smoke, or new verify alias infrastructure is added by this plan.
    - `cd examples/chimeway_demo_host && mix test test/demo_host_web/admin_trace_live_test.exs --warnings-as-errors` exits 0 when the demo-host DB environment is healthy.
  </acceptance_criteria>
  <done>Demo-host test proves the host-mounted `/admin/chimeway` landing page is nonblank and exposes the expected operator paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host route mount -> admin package routes | Demo host mounts `ChimewayAdmin.Router.chimeway_admin_routes/1` under `/admin/chimeway`; route helper drift can misroute operators. |
| LiveView shell -> operator navigation | Sidebar labels and command-center links guide support operators during incident/debug workflows. |
| Admin UI labels -> implemented capabilities | Page labels can overclaim or obscure the actual shipped route map. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-04 | Repudiation / Operational safety | route helper and host route map | mitigate | Add helper tests and host-mounted LiveView assertions for every shipped admin path. |
| T-68-05 | Repudiation | Command Center hierarchy | mitigate | Assert `/admin/chimeway` renders `Open Trace Lookup` as primary and Health/Recovery/Definitions/Feed Debug as secondary paths. |
| T-68-06 | Tampering / UX confusion | shared sidebar labels | mitigate | Assert locked sidebar labels in LiveView tests so labels cannot drift silently. |
| T-68-07 | Scope creep | browser smoke/design/security hardening | accept | Plan explicitly uses ExUnit/LiveViewTest only and forbids Playwright, `mix verify.admin`, design-system work, and Phase 70 safety changes. |
| T-68-SC | Tampering / Supply chain | package installation | accept | No package installs or dependency changes in this plan. |
</threat_model>

<verification>
- `cd chimeway_admin && mix test test/chimeway_admin/routes_test.exs test/chimeway_admin/live/trace_search_live_test.exs --warnings-as-errors` exits 0.
- `cd examples/chimeway_demo_host && mix test test/demo_host_web/admin_trace_live_test.exs --warnings-as-errors` exits 0 when the demo-host DB environment is healthy.
- No files under `.github/workflows/`, `mix.exs`, or new browser-smoke tooling are modified by this plan.
</verification>

<success_criteria>
- Route helper tests cover Command Center, Trace Lookup, Trace Detail, Feed Debug, Definitions, Health, and Recovery.
- Isolated LiveView tests prove the Command Center primary action is Trace Lookup and secondary paths include Health, Recovery, Definitions, and Feed Debug.
- Sidebar/page label tests cover Command Center, Trace Lookup, Feed Debug, Definitions, Health, and Recovery.
- Demo-host mounted test proves `/admin/chimeway` renders nonblank with the same operator paths.
- ADMIN-01 and ADMIN-02 are fully covered by this plan without Phase 69, 70, 71, or 72 scope creep.
</success_criteria>

<output>
After completion, create `.planning/phases/68-admin-truth-alignment/68-02-SUMMARY.md`.
</output>
