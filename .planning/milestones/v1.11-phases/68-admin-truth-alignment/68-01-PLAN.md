---
phase: 68-admin-truth-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/chimeway_demo_host/README.md
  - test/chimeway/doc_contract_test.exs
autonomous: true
requirements: [ADMIN-03]
user_setup: []
must_haves:
  truths:
    - "D-01: demo/admin copy treats the seven-page route map as shipped scope: Command Center, Trace Lookup, Trace Detail, Feed Debug, Definitions, Health, and Recovery"
    - "D-02: docs language aligns to the existing operator page hierarchy instead of hiding or removing already-built pages"
    - "D-05: examples/chimeway_demo_host/README.md no longer says health aggregates, notification definitions registry, or related admin pages are not included"
    - "D-06: Definitions copy is DB-inferred durable key/version usage, not code-registry skew detection; Feed Debug copy is operator lifecycle inspection, not the end-user inbox surface"
    - "ADMIN-03: operator-facing demo copy accurately describes the current admin pages and forbids stale trace-only/out-of-scope claims"
  artifacts:
    - path: "examples/chimeway_demo_host/README.md"
      provides: "current embedded admin console page inventory and honest boundary language"
      contains: "Command Center"
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "doc-contract assertions for demo-host admin truth"
      contains: "examples/chimeway_demo_host/README.md"
  key_links:
    - from: "examples/chimeway_demo_host/README.md admin section"
      to: "Phase 68 route map"
      via: "required page labels"
      pattern: "Command Center.*Trace Lookup.*Trace Detail.*Feed Debug.*Definitions.*Health.*Recovery"
    - from: "test/chimeway/doc_contract_test.exs"
      to: "examples/chimeway_demo_host/README.md"
      via: "File.read!/1 doc contract"
      pattern: "trace lookup only"
---

<objective>
Update demo/admin documentation so it describes the real embedded operator console instead of the earlier trace-only MVP boundary.

Purpose: ADMIN-03 requires operator-facing copy to match shipped admin pages and stop marking current pages as out of scope.
Output: A corrected demo host README section plus a root doc-contract test that prevents stale admin claims from returning.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-admin-truth-alignment/68-CONTEXT.md
@.planning/phases/68-admin-truth-alignment/68-RESEARCH.md
@.planning/phases/68-admin-truth-alignment/68-UI-SPEC.md
@examples/chimeway_demo_host/README.md
@test/chimeway/doc_contract_test.exs
@chimeway_admin/lib/chimeway_admin/router.ex
@chimeway_admin/lib/chimeway_admin/live/feed_live.ex
@chimeway_admin/lib/chimeway_admin/live/definitions_live.ex
@lib/chimeway/admin.ex
</context>

<interfaces>
Locked page labels and routes:
- `/admin/chimeway` -> Command Center
- `/admin/chimeway/traces` -> Trace Lookup
- `/admin/chimeway/deliveries/:delivery_id` -> Trace Detail
- `/admin/chimeway/feed` -> Feed Debug
- `/admin/chimeway/definitions` -> Definitions
- `/admin/chimeway/health` -> Health
- `/admin/chimeway/recovery` -> Recovery

Required honest boundary language:
- Not generic CRUD over Chimeway tables.
- Not template editing or provider configuration.
- Not the end-user inbox product surface.
- Not cohort analytics.
- Not arbitrary bulk resend/delete recovery.

Forbidden stale claim themes:
- Admin is trace lookup only.
- Health aggregates dashboard is not included.
- Notification definitions registry is not included.
- Definitions detects code-registry skew.
- Feed Debug is the end-user inbox.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the demo-host admin copy around the shipped console</name>
  <files>examples/chimeway_demo_host/README.md</files>
  <read_first>
    - examples/chimeway_demo_host/README.md
    - .planning/phases/68-admin-truth-alignment/68-CONTEXT.md
    - .planning/phases/68-admin-truth-alignment/68-UI-SPEC.md
    - chimeway_admin/lib/chimeway_admin/router.ex
    - chimeway_admin/lib/chimeway_admin/live/feed_live.ex
    - chimeway_admin/lib/chimeway_admin/live/definitions_live.ex
    - lib/chimeway/admin.ex
  </read_first>
  <action>
    Edit the README admin/browser section so it names the current `/admin/chimeway` console as a multi-page embedded operator console. The copy must explicitly list Command Center, Trace Lookup, Trace Detail, Feed Debug, Definitions, Health, and Recovery.

    Replace any stale "Out of scope for chimeway_admin MVP" language that says health aggregates, notification definitions registry, or related admin pages are not included. The replacement boundary statement must say the admin console is not generic CRUD, not template/provider configuration UI, not the end-user inbox product surface, not cohort analytics, and not arbitrary bulk resend/delete.

    Keep Definitions wording narrow: "DB-inferred durable notification key/version usage" or equivalent. Do not claim code registry comparison, code scanning, or skew detection. Keep Feed Debug wording narrow: operator lifecycle inspection by recipient, not end-user inbox UI.

    Keep existing demo setup commands, seed commands, and route prefix `/admin/chimeway` intact unless they are already incorrect.
  </action>
  <verify>
    <automated>grep -q "Command Center" examples/chimeway_demo_host/README.md && grep -q "Trace Lookup" examples/chimeway_demo_host/README.md && grep -q "Trace Detail" examples/chimeway_demo_host/README.md && grep -q "Feed Debug" examples/chimeway_demo_host/README.md && grep -q "Definitions" examples/chimeway_demo_host/README.md && grep -q "Health" examples/chimeway_demo_host/README.md && grep -q "Recovery" examples/chimeway_demo_host/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `examples/chimeway_demo_host/README.md` contains `Command Center`, `Trace Lookup`, `Trace Detail`, `Feed Debug`, `Definitions`, `Health`, and `Recovery`.
    - `examples/chimeway_demo_host/README.md` contains `/admin/chimeway`.
    - `examples/chimeway_demo_host/README.md` no longer contains the stale phrase `trace lookup only`.
    - `examples/chimeway_demo_host/README.md` no longer describes health aggregates or notification definitions as not included.
    - Definitions wording in the README does not contain `skew detection`, `code registry`, or `code-registry`.
    - Feed Debug wording in the README does not describe the page as the `end-user inbox`.
  </acceptance_criteria>
  <done>Demo-host README describes the current seven-page admin console and replaces old MVP exclusions with current product boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Add a doc contract for demo-host admin truth</name>
  <files>test/chimeway/doc_contract_test.exs</files>
  <read_first>
    - test/chimeway/doc_contract_test.exs
    - examples/chimeway_demo_host/README.md
    - .planning/phases/68-admin-truth-alignment/68-CONTEXT.md
    - .planning/phases/68-admin-truth-alignment/68-RESEARCH.md
  </read_first>
  <action>
    Add a focused ExUnit test or describe block to `test/chimeway/doc_contract_test.exs` that reads `examples/chimeway_demo_host/README.md`.

    The test must require these exact strings: `Command Center`, `Trace Lookup`, `Trace Detail`, `Feed Debug`, `Definitions`, `Health`, `Recovery`, and `/admin/chimeway`.

    The test must forbid stale strings or claims that would reintroduce D-05/D-06 drift. At minimum, forbid `trace lookup only`, `health aggregates dashboard`, `notification definitions registry`, `skew detection`, `code-registry`, and `end-user inbox surface` when those phrases are used to describe the admin pages as shipped/out-of-scope incorrectly. Use simple `String.contains?/2` checks consistent with existing doc-contract style.

    Keep the test independent of database state and Phoenix startup; it should be a fast file-content contract under the root Mix project.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/chimeway/doc_contract_test.exs` reads `examples/chimeway_demo_host/README.md`.
    - The test asserts all seven page labels and `/admin/chimeway` are present.
    - The test refutes at least `trace lookup only`, `health aggregates dashboard`, `notification definitions registry`, `skew detection`, and `code-registry`.
    - `MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Root doc-contract coverage fails if demo/admin copy regresses to trace-only or overclaiming language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> operator behavior | Operators and adopters use README copy to decide which admin page to open during support/debug workflows. |
| Documentation -> implementation claims | README prose can overclaim capabilities that the embedded admin package does not implement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Repudiation / Operational safety | demo-host README admin copy | mitigate | Require all seven shipped page labels and forbid stale trace-only/out-of-scope claims in `test/chimeway/doc_contract_test.exs`. |
| T-68-02 | Repudiation | Definitions copy | mitigate | README and doc contract must keep Definitions scoped to DB-inferred durable key/version usage and avoid code-registry skew-detection claims. |
| T-68-03 | Information disclosure / UX confusion | Feed Debug copy | mitigate | README must describe Feed Debug as operator lifecycle inspection, not the end-user inbox product surface. |
| T-68-SC | Tampering / Supply chain | package installation | accept | No package installs, no dependency changes, no browser tooling in this plan. |
</threat_model>

<verification>
- `MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` exits 0.
- `grep -q "Command Center" examples/chimeway_demo_host/README.md` exits 0.
- `grep -qi "trace lookup only" examples/chimeway_demo_host/README.md` exits non-zero.
</verification>

<success_criteria>
- Demo-host README names Command Center, Trace Lookup, Trace Detail, Feed Debug, Definitions, Health, and Recovery.
- Demo-host README no longer marks shipped admin pages as out of scope.
- Demo-host README avoids Definitions skew-detection and Feed Debug end-user-inbox overclaims.
- Root doc-contract test locks required and forbidden admin-copy strings.
- ADMIN-03 is fully covered by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/68-admin-truth-alignment/68-01-SUMMARY.md`.
</output>
