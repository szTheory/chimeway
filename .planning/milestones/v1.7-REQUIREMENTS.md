# v1.7 Read/Unread Workflow Glue — Requirements (planned)

**Milestone:** v1.7 READ  
**Status:** planned — after v1.6 Consumer Journey Proof  
**Depends on:** v1.6 shipped

## Requirements (draft)

| ID | Requirement |
|----|-------------|
| **READ-01** | Populate `pending_signals` on `wait_until` workflow transitions |
| **READ-02** | Inbox `mark_read` / `mark_seen` emits durable signals that route workflow progression without host glue |

## Rationale

SEED-004 Product Manager JTBD: "If they don't open the email in 2 hours, send push." Engine supports workflows and inbox APIs separately; v1.7 connects them.

## Explicitly out of scope for v1.7

- Bell inbox UI (INBX — v1.9)
- SEED-003 ecosystem blueprints (v1.8)
