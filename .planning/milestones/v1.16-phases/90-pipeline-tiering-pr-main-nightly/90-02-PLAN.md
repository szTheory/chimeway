---
phase: 90-pipeline-tiering-pr-main-nightly
plan: 02
type: execute
wave: 2
depends_on: ["90-01"]
files_modified:
  - .github/workflows/ci.yml
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
requirements: [TIER-02]

must_haves:
  truths:
    - "verify_admin (the heavy Playwright admin-console smoke lane) no longer runs on push or pull_request events; it runs only when resolve_tiers.outputs.run_nightly is the string true."
    - "ci-gate continues to pass on ordinary push runs after verify_admin's relocation — a skipped verify_admin no longer fails ci-gate."
    - "release_gate_contract_test.exs's ci-gate lane-count contract reflects 13 required lanes, not 14."
  artifacts:
    - ".github/workflows/ci.yml: verify_admin gated on needs.resolve_tiers.outputs.run_nightly == 'true' with needs: [resolve_tiers]"
    - ".github/workflows/ci.yml: ci-gate's needs: list with verify_admin removed (13 entries), and its aggregate-gate.sh call/env stripped of the Admin lane"
    - "test/chimeway/release_gate_contract_test.exs: @ci_gate_lanes with 13 entries (verify_admin dropped) and the lane-count test renamed to match"
  key_links:
    - "verify_admin's if: now reads needs.resolve_tiers.outputs.run_nightly instead of the bare pull_request exclusion guard it used before this plan"
  prohibitions:
    - "ci-gate MUST NOT keep verify_admin in its needs list once verify_admin's trigger is nightly-only (Pitfall 1 — a skipped dependency fails aggregate-gate.sh identically to a real failure)"
    - "concurrency MUST NOT allow a push to cancel an in-flight nightly run (fixed in 90-01; unaffected by this plan's edits, re-asserted here since both plans touch ci.yml)"
---

## Artifacts This Phase Produces

(Full phase-wide list, reproduced in every plan of Phase 90 for self-containment — see `.planning/phases/90-pipeline-tiering-pr-main-nightly/90-RESEARCH.md` Decision Table and Job-Relocation Map for the design source.)

- Job `resolve_tiers` and its `run_nightly`/`otp_matrix` outputs, `schedule:`/`workflow_dispatch.inputs.run_nightly`, and the fixed `concurrency` group — delivered in Plan 90-01.
- New job `nightly_cold_build`, new job `test_floor_1_17` (with cache-key namespace `test-floor-*`), new job `nightly-gate` — delivered in Plan 90-03.
- Changed `@ci_gate_lanes` module attribute in `test/chimeway/release_gate_contract_test.exs`: 14 lanes -> 13 lanes (verify_admin removed) — **this plan**.

This plan (90-02) relocates `verify_admin`'s trigger to nightly-only and performs the mandatory atomic fix that keeps `ci-gate` green on ordinary pushes afterward.

<objective>
Relocate the heavy Playwright `verify_admin` lane from "every push/dispatch" to "nightly tier only" (TIER-02), and — in the exact same commit — remove `verify_admin` from `ci-gate`'s `needs:` list and update `release_gate_contract_test.exs`'s `@ci_gate_lanes` from 14 to 13 entries (RESEARCH.md Pitfall 1: a `skipped` dependency fails `scripts/ci/aggregate-gate.sh` identically to a real failure, so any one of these three edits landing without the other two red-lights every push).

Purpose: Remove the single heaviest non-matrix lane (npm install + Playwright browser download + browser automation) from the push-blocking `ci-gate`/release path without breaking `ci-gate` for even one push.
Output: `verify_admin` gated on the nightly tier; `ci-gate` back to a coherent, currently-accurate 13-lane contract; both proven live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-pipeline-tiering-pr-main-nightly/90-RESEARCH.md
@.planning/phases/90-pipeline-tiering-pr-main-nightly/90-VALIDATION.md
@.github/workflows/ci.yml
@test/chimeway/release_gate_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic fix — relocate verify_admin to nightly-only + strip it from ci-gate + update the lane-count contract</name>
  <files>.github/workflows/ci.yml, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>.github/workflows/ci.yml (the verify_admin job block, and the ci-gate job block including its `needs:` array and its "Verify required CI lanes" step); test/chimeway/release_gate_contract_test.exs (the `@ci_gate_lanes` module attribute, the `"ci-gate aggregates 14 required lanes"` test, and the `extract_ci_gate_needs/1` helper); .planning/phases/90-pipeline-tiering-pr-main-nightly/90-RESEARCH.md ("verify_admin trigger change" and "ci-gate needs-list fix" unified diff sketches under Code Examples; Common Pitfalls, Pitfall 1)</read_first>
  <reversibility rating="costly">Landing only one or two of the three edits below leaves ci-gate red on the very next push (a skipped verify_admin fails aggregate-gate.sh identically to a real failure). The fix is fully reversible by reverting this single commit, but a partial mid-flight state is not — hence one atomic commit, not three.</reversibility>
  <action>
Make exactly three edits in this one commit — do not split them across tasks or commits:

(a) In the `verify_admin` job block, replace its current single-line pull-request-exclusion trigger guard with two lines: a `needs: [resolve_tiers]` line, followed by an `if:` line that reads `needs.resolve_tiers.outputs.run_nightly == 'true'`. Every other line in `verify_admin` (name, runs-on, services, env, all of its steps including the npm/Playwright/nested-admin/demo-host cache steps) stays byte-identical — only the trigger guard changes.

(b) In the `ci-gate` job's `needs:` array, delete the Admin lane's entry so the array goes from 14 entries to 13, preserving the order of the remaining 13. In that same job's "Verify required CI lanes" step, delete the environment-variable line that maps the Admin lane's `needs.<job>.result` to its uppercase env-var name, and delete that same uppercase token from the `scripts/ci/aggregate-gate.sh` argument list on the `run:` line.

(c) In `test/chimeway/release_gate_contract_test.exs`, edit the `@ci_gate_lanes` module attribute so the Admin lane's entry is removed from the `~w(...)` list (13 entries remain, same order as the other 13 in ci-gate's `needs:`). Rename the test currently named for "14 required lanes" so its name says "13 required lanes" instead — no change to the test's assertion body is needed, since `extract_ci_gate_needs/1` and the `length(@ci_gate_lanes)` comparison already re-derive correctness from the updated constant.

Do not touch `nightly_cold_build`, `test_floor_1_17`, or `nightly-gate` in this task — those are Plan 90-03.
  </action>
  <verify>
    <automated>actionlint .github/workflows/ci.yml && MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs test/chimeway/ci_observability_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - verify_admin's job block in ci.yml contains `needs: [resolve_tiers]` and `if: needs.resolve_tiers.outputs.run_nightly == 'true'`.
    - verify_admin's job block in ci.yml no longer carries its previous bare pull-request-exclusion guard as its trigger condition.
    - ci-gate's `needs:` array in ci.yml (as read by `extract_ci_gate_needs/1`) has exactly 13 entries and does not include the Admin lane.
    - ci-gate's "Verify required CI lanes" `run:` line no longer names the Admin lane's uppercase env-var token.
    - `test/chimeway/release_gate_contract_test.exs`'s `@ci_gate_lanes` has exactly 13 entries and does not include the Admin lane's job id.
    - `mix test test/chimeway/release_gate_contract_test.exs` passes 0 failures, including the renamed 13-lane test.
    - `mix test test/chimeway/ci_observability_contract_test.exs` still passes 0 failures unchanged — verify_admin's `id: cache_main` and trailing `obs-summary.sh` step are untouched by this task, only its trigger condition moved.
    - `actionlint .github/workflows/ci.yml` exits 0.
  </acceptance_criteria>
  <done>All three edits (verify_admin's trigger, ci-gate's needs-list + aggregate call, and the contract test's @ci_gate_lanes + test name) are committed together in one commit; actionlint and both contract-test files pass green.</done>
</task>

<task type="auto">
  <name>Task 2: Live proof — verify_admin skips on a plain dispatch, runs on a run_nightly=true dispatch (TIER-02)</name>
  <files>none — live GitHub Actions verification only, no repo files modified</files>
  <precondition>This branch's Task 1 commit is pushed to origin (gh workflow run dispatches against the remote ref, not local uncommitted state).</precondition>
  <read_first>.github/workflows/ci.yml (verify_admin + resolve_tiers, post-Task-1 state); .planning/phases/90-pipeline-tiering-pr-main-nightly/90-VALIDATION.md (the "TIER-02 (relocated, live)" row); .planning/phases/90-pipeline-tiering-pr-main-nightly/90-RESEARCH.md (Decision row 7, Pitfall 5)</read_first>
  <action>
With `BRANCH=$(git branch --show-current)`, dispatch `gh workflow run ci.yml --ref "$BRANCH"` with no `-f` flag (so `run_nightly` defaults to `false`). Resolve the new run id via `RUN_ID=$(gh run list --workflow=ci.yml --branch="$BRANCH" --limit 1 --json databaseId --jq '.[0].databaseId')`, then wait for completion with `gh run watch "$RUN_ID" --exit-status`. Query `gh run view "$RUN_ID" --json jobs --jq '.jobs[] | select(.name=="Admin integration gate") | .conclusion'` and confirm it prints `skipped`.

Then dispatch a second run with `gh workflow run ci.yml --ref "$BRANCH" -f run_nightly=true`, resolve its run id the same way into a new `RUN_ID`, watch it to completion, and run the same jq query against the new run id — confirm it now prints `success`.

In both cases, also confirm the "Resolve tier flags" job's conclusion is `success` (resolve_tiers correctly computed `run_nightly` for each dispatch).
  </action>
  <verify>
    <automated>gh run watch "$RUN_ID" --exit-status</automated>
  </verify>
  <acceptance_criteria>
    - A `workflow_dispatch` run with `run_nightly` omitted shows the "Admin integration gate" job's conclusion equal to `skipped`.
    - A `workflow_dispatch` run with `-f run_nightly=true` shows the "Admin integration gate" job's conclusion equal to `success`.
    - Both dispatched runs show the "Resolve tier flags" job's conclusion equal to `success`.
  </acceptance_criteria>
  <done>Two live dispatches confirm verify_admin's relocation behaves exactly as TIER-02 requires: skipped on an ordinary trigger, success only when the nightly tier is explicitly requested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fork PR -> ci.yml execution | A `pull_request` from a fork can propose edits to `ci.yml` itself; the repo uses `pull_request` (not `pull_request_target`), so the fork's edited workflow runs with the fork's own limited, secret-less `GITHUB_TOKEN`. |
| `workflow_dispatch.inputs.run_nightly` -> job conditionals | A manually-supplied boolean input flows into `if:` expressions and shell string comparisons that decide which jobs execute. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-90-01 | Tampering | `workflow_dispatch.inputs.run_nightly` | low | accept | Boolean-typed input compared only via `==`/shell string comparison against the literal "true"; never interpolated into a `run:` shell command as free text. |
| T-90-02 | Elevation of Privilege | `ci.yml` itself (fork PR edits) | low | accept | The repo already uses `pull_request` (not `pull_request_target`) everywhere; a fork PR's edits run with the fork's own limited token and cannot reach repo secrets or the nightly-only lanes regardless of what the PR edits. |
| T-90-03 | Denial of Service | `ci-gate` needs: list / `scripts/ci/aggregate-gate.sh` (Pitfall 1) | high | mitigate | The verify_admin relocation, the ci-gate needs-list removal, and the contract-test update ship as one atomic commit (Task 1); actionlint + both contract-test files gate that commit before it can land, and Task 2's live dispatch proves the fix holds on real runs. |
</threat_model>

<verification>
- `actionlint .github/workflows/ci.yml` exits 0 after Task 1.
- `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs test/chimeway/ci_observability_contract_test.exs` passes 0 failures after Task 1.
- Two live `gh workflow run ci.yml` dispatches (Task 2) confirm verify_admin's skipped/success split exactly matches the nightly-tier flag.
</verification>

<success_criteria>
- verify_admin only ever runs when the nightly tier is active.
- ci-gate's needs-list and the contract test agree on 13 required lanes, and ci-gate stays green on ordinary pushes.
- Both the relocation and the ci-gate fix are proven live, not just statically.
</success_criteria>

<output>
Create `.planning/phases/90-pipeline-tiering-pr-main-nightly/90-02-SUMMARY.md` when done
</output>
