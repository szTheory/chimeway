---
phase: 92-reliability-triage-determinism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/env_helper.ex
  - test/chimeway/test_support/env_helper_test.exs
  - test/chimeway/policy_test.exs
  - test/chimeway/ci_observability_contract_test.exs
autonomous: true
requirements: [REL-04]
user_setup: []

must_haves:
  truths:
    - "Chimeway.TestSupport.EnvHelper.put_env_isolated/3 sets an app-env key for the current test and, on exit, restores the exact prior value when the key was present (REL-04)."
    - "put_env_isolated/3 DELETES the key on exit when it was absent before the call (fetch_env returned :error) rather than leaving it set to nil (REL-04 restore-when-absent edge)."
    - "policy_test.exs (async: true) sets :chimeway/:adapter only through EnvHelper.put_env_isolated/3 — no bare app-env mutation remains in any async DataCase module (REL-04 adoption)."
    - "A contract assertion greps every async: true DataCase test module and fails if any carries a bare app-env put call, keeping the async split safe as the suite grows (REL-04)."
    - "mix ci.test stays green with the helper in place (full default suite, --warnings-as-errors)."
    - statement: "put_env_isolated/3 bounds a mutation per-test-in-time via on_exit but not across concurrently-running async modules; adoption is limited to keys not concurrently mutated elsewhere (:chimeway/:adapter is the only confirmed async target)."
      verification: backstop
  prohibitions:
    - "Must not change lib/ runtime behavior — the helper is test-support only (milestone invariant)."
    - "Must not flip an async: false app-env mutator to async: true on the basis that the helper makes it safe (process-global race)."
  artifacts:
    - test/support/env_helper.ex
    - test/chimeway/test_support/env_helper_test.exs
  key_links:
    - "policy_test.exs → Chimeway.TestSupport.EnvHelper.put_env_isolated/3 (adoption site, lines 248 and 279)"
    - "ci_observability_contract_test.exs adoption assertion → async test-file sources (regression guard)"
---

<objective>
Deliver REL-04: a capture/restore `put_env` test helper and adopt it in the one async DataCase
module that mutates global app-env unguarded, standardizing app-env isolation so the Phase 89
async split stays safe as the suite grows.

This is the phase TRACER: it wires one path end-to-end through every test-infra layer this phase
touches — new test-support artifact → unit test proving behavior → adoption in a real production
test file → contract guard against regression → green full suite — proving the
"new artifact + contract guard + green mix ci.test" pattern before the phase fans out.

Purpose: The async conversion introduced a latent hazard — `test/chimeway/policy_test.exs`
(`use Chimeway.DataCase, async: true`) mutates `:chimeway/:adapter` with no `on_exit` restore.
A single blessed helper (the exact pattern already living inline in `test/support/accrue/data_case.ex`)
makes app-env isolation explicit, greppable, and regression-guarded.
Output: `Chimeway.TestSupport.EnvHelper` + its unit test + policy_test.exs adoption + adoption contract assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-reliability-triage-determinism/92-RESEARCH.md
</context>

## Artifacts this phase produces (full phase set — for source-grounding drift exclusion)

New symbols/files created across all of Phase 92 (this plan creates the ones marked **[92-01]**):
- **[92-01]** `Chimeway.TestSupport.EnvHelper` (`test/support/env_helper.ex`) + function `put_env_isolated/3`
- **[92-01]** `test/chimeway/test_support/env_helper_test.exs`
- **[92-01]** new adoption assertion in `test/chimeway/ci_observability_contract_test.exs` (no bare app-env put in async modules)
- **[92-01]** `policy_test.exs` adoption edits (lines 248, 279)
- [92-02] `scripts/ci/reliability-report.sh`, `test/fixtures/ci/run_list_sample.json`, `test/chimeway/ci_reliability_contract_test.exs`, `.planning/CI-RELIABILITY-REPORT.md`
- [92-03] `test_seed_zero` job in `.github/workflows/ci.yml`; new `release_gate_contract_test.exs` nightly-gate 5-lane assertions; `ci_observability_contract_test.exs` `test_seed_zero` exemption assertion; updated `.planning/CI-HARDENING-BACKLOG.md`

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: EnvHelper capture/restore helper + unit test + policy_test adoption (end-to-end slice)</name>
  <files>test/support/env_helper.ex, test/chimeway/test_support/env_helper_test.exs, test/chimeway/policy_test.exs</files>
  <read_first>
    - test/support/accrue/data_case.ex — lines 38–47 hold the canonical fetch_env → put_env → on_exit restore/delete body to extract verbatim into the helper.
    - test/chimeway/policy_test.exs — the adoption target; `use Chimeway.DataCase, async: true` (line 2); the two bare app-env mutations (`:chimeway, :adapter` → `Chimeway.Adapters.Test`) at lines 248 and 279.
    - mix.exs — line 21 `elixirc_paths(:test)` includes `test/support`, so `env_helper.ex` compiles in the test env.
  </read_first>
  <behavior>
    - Test A (key present): given `:chimeway/:some_probe_key` is set to `:orig`, register an `on_exit` assertion (BEFORE calling the helper, so LIFO runs it AFTER the helper's restore) that the key equals `:orig`; call `put_env_isolated(:chimeway, :some_probe_key, :mutated)`; assert `Application.get_env(:chimeway, :some_probe_key) == :mutated` inside the test. The helper's on_exit restores `:orig`.
    - Test B (key absent): ensure `:chimeway/:absent_probe_key` is deleted; register an `on_exit` assertion (before the helper call) that `Application.fetch_env(:chimeway, :absent_probe_key) == :error` (key fully deleted, not set to nil); call `put_env_isolated(:chimeway, :absent_probe_key, :mutated)`; assert the key equals `:mutated` inside the test. The helper's on_exit DELETES the key.
    - Helper returns `:ok`.
  </behavior>
  <action>
    Create `Chimeway.TestSupport.EnvHelper` (moduledoc false) in `test/support/env_helper.ex` exposing
    `put_env_isolated/3` that snapshots the key with `Application.fetch_env(app, key)` (`{:ok, v}` | `:error`),
    sets the new value, and registers an `on_exit/1` that restores the snapshot when present or deletes the key
    when it was absent — the exact body from `accrue/data_case.ex:38-47`, generalized to `(app, key, value)`.
    Import only `ExUnit.Callbacks, only: [on_exit: 1]`. Write the unit test `env_helper_test.exs` FIRST covering
    Test A and Test B above (RED before GREEN, using the LIFO on_exit assertion idiom so restoration is asserted
    deterministically after the helper's callback). Then adopt: at policy_test.exs lines 248 and 279 replace the
    two bare `Application.put_env/3` calls with `Chimeway.TestSupport.EnvHelper.put_env_isolated(:chimeway, :adapter, Chimeway.Adapters.Test)`
    (use the fully-qualified module name; leave the following `Chimeway.Adapters.Test.clear()` calls untouched).
    Do NOT flip any async: false module to async: true — adoption is limited to the already-async policy_test.exs.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/test_support/env_helper_test.exs test/chimeway/policy_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/chimeway/test_support/env_helper_test.exs` exits 0 with both the present-key-restore and absent-key-delete tests green.
    - `mix test test/chimeway/policy_test.exs` exits 0 (adoption did not change behavior).
    - `grep -n "put_env_isolated" test/chimeway/policy_test.exs` returns two matches (lines ~248, ~279).
    - `git diff --stat -- lib/` is empty (no lib/ change).
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only additions and a two-line adoption edit; git revert restores the prior inline mutation.</reversibility>
  <done>The helper exists, its unit test proves restore-when-present and delete-when-absent, policy_test.exs uses the helper for both adapter mutations, and the file-scoped suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: adoption contract guard — no bare app-env put in async DataCase modules + full-suite proof</name>
  <files>test/chimeway/ci_observability_contract_test.exs</files>
  <read_first>
    - test/chimeway/ci_observability_contract_test.exs — the existing contract harness (describe blocks, `System.cmd` fixture pattern, file-read assertions); model the new assertion on the existing source-reading tests.
    - test/chimeway/policy_test.exs — confirm it now routes through the helper (the assertion must pass post-adoption).
  </read_first>
  <action>
    Add a new `describe`/`test` to `ci_observability_contract_test.exs` (REL-04 adoption guard) that reads every
    `test/chimeway/*.exs` source, selects the modules declaring `async: true`, strips comment lines, and refutes
    that any of them contains a bare `Application.put_env/3` invocation — the only sanctioned app-env mutation in
    an async module is via `Chimeway.TestSupport.EnvHelper.put_env_isolated/3`. Include a clear failure message
    naming the offending file and pointing at the helper. This is a regression guard so a future async module that
    reintroduces an unguarded mutation fails CI.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/ci_observability_contract_test.exs --warnings-as-errors && MIX_ENV=test mix ci.test</automated>
  </verify>
  <acceptance_criteria>
    - The new contract test negative-greps the regex `Application\.put_env\(` (comment lines excluded) across async-flagged `test/chimeway/*.exs` files and passes (zero offenders after adoption).
    - Manually appending a bare app-env put to an async module makes the assertion fail (spot-checkable; do not commit the manual break).
    - `MIX_ENV=test mix ci.test` exits 0 (full default suite green, --warnings-as-errors).
  </acceptance_criteria>
  <done>The adoption contract asserts no async DataCase module carries a bare app-env put, and the full default suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process ↔ process-global app-env | Tests mutate a process-global config table; without restore, mutation leaks across tests/modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-92-01 | Tampering | EnvHelper.put_env_isolated/3 (test app-env) | low | mitigate | on_exit restores exact prior value or deletes when absent; unit test proves both branches; adoption contract prevents unguarded regressions |
| T-92-02 | Denial of Service | async cross-module app-env race | low | accept | Documented caveat (backstop truth); adoption limited to :chimeway/:adapter, the only confirmed async target — grep-exhaustive |

No package installs occur in this plan (zero npm/pip/hex/crates), so no supply-chain install threat applies. No secrets, network, or `lib/` surface is touched.
</threat_model>

<verification>
- `MIX_ENV=test mix test test/chimeway/test_support/env_helper_test.exs test/chimeway/policy_test.exs test/chimeway/ci_observability_contract_test.exs --warnings-as-errors` exits 0.
- `MIX_ENV=test mix ci.test` exits 0.
- `git diff --stat -- lib/` is empty.
</verification>

<success_criteria>
REL-04 satisfied: the capture/restore helper exists, restores-when-present and deletes-when-absent (unit-proven), is adopted by the one async DataCase module that mutated app-env unguarded, and a contract guard keeps the async split safe. Full suite green; zero lib/ change.
</success_criteria>

<output>
Create `.planning/phases/92-reliability-triage-determinism/92-01-SUMMARY.md` when done.
</output>
