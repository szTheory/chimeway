---
phase: 91-quality-supply-chain-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/dependabot.yml
  - mix.exs
  - mix.lock
autonomous: true
requirements: [QUAL-02, QUAL-04]
must_haves:
  truths:
    - ".github/dependabot.yml exists with `version: 2` and covers the `mix` and `github-actions` ecosystems (both `directory: \"/\"`) on a weekly grouped schedule (QUAL-02, D-05/D-06)"
    - "mix.exs deps includes `{:mix_audit, \"~> 2.1\", only: [:dev, :test], runtime: false}` and the `ci.audit` alias == `[\"hex.audit\", \"deps.audit\"]` (QUAL-04, D-10/D-11)"
    - "mix.lock resolves mix_audit (2.1.x) after `mix deps.get`"
  backstops:
    - statement: "Dependabot parses the config and lists the mix + github-actions ecosystems with no config error (only observable in GitHub Insights → Dependency graph → Dependabot)"
      verification: backstop
    - statement: "The lint job's advisory-audit step runs `hex.audit` + `deps.audit`, prints findings, and never fails the gate (only observable in a CI run)"
      verification: backstop
  artifacts:
    - ".github/dependabot.yml"
    - "mix.exs (mix_audit dep + extended ci.audit alias)"
    - "mix.lock (mix_audit resolved)"
  key_links:
    - "`mix ci.audit` → `hex.audit` + `deps.audit` ← `mix_audit` dep provides `mix deps.audit`"
    - "Dependabot `github-actions` ecosystem → updates SHA-pinned `uses:` refs in `.github/workflows`"
  prohibitions:
    - "The advisory audit step must stay advisory-only — it must not become blocking / must not lose `continue-on-error` (QUAL-04, D-12)"
---

<objective>
Add automated dependency-update PRs (QUAL-02) and an advisory-only CVE-advisory-DB scan (QUAL-04),
the two supply-chain-hygiene requirements that touch files independent of `ci.yml` and so run in
parallel with the QUAL-01 tracer.

Purpose: keep SHA-pinned actions and Hex deps current via native Dependabot PRs, and surface
disclosed CVEs in Hex deps via `mix deps.audit` — without ever silently blocking the merge gate.

Output: new `.github/dependabot.yml` (mix + github-actions); `mix_audit` dep + extended `ci.audit`
alias in `mix.exs`; refreshed `mix.lock`. The advisory CI step and `release.yml` are untouched.
</objective>

<artifacts_produced>
This plan creates / modifies the following symbols and files:
- **NEW file** `.github/dependabot.yml` — `version: 2`; two `updates:` entries: `package-ecosystem: "mix"` (`directory: "/"`) and `package-ecosystem: "github-actions"` (`directory: "/"`), both weekly, each with a `groups:` entry grouping `["minor", "patch"]`.
- **NEW dep** in `mix.exs` `deps()` — `{:mix_audit, "~> 2.1", only: [:dev, :test], runtime: false}`.
- **EXTENDED alias** in `mix.exs` — `ci.audit` alias member `deps.audit` added: `["hex.audit"]` → `["hex.audit", "deps.audit"]`.
- **MODIFIED** `mix.lock` — mix_audit (and any dev/test transitive) resolved via `mix deps.get`.
- **UNCHANGED (asserted)** — `ci.yml`'s "Dependency advisory audit (advisory-only)" step (still `mix ci.audit`, still `continue-on-error: true`); `mix.exs` `elixir: "~> 1.17"` (line 10).
</artifacts_produced>

<flagged_assumptions>
The deterministic edge-probe classifier left QUAL-02 and QUAL-04 `unresolved`/`unclassified`
because they are config/CI requirements, not product behaviors. Surfaced here (no silent drop):

- **[ASSUMPTION — QUAL-02]** Dependabot with a `groups:` entry carrying only `update-types:
  ["minor","patch"]` (no `patterns:`) groups all minor/patch updates per ecosystem into one weekly PR
  (RESEARCH Assumption A3 — cosmetic, Claude's Discretion). Config-validation is the observable:
  GitHub Insights lists both ecosystems as parsed with no error.
- **[ASSUMPTION — QUAL-04]** `~> 2.1` resolves to mix_audit 2.1.5 (current, `>= 2.1.0 and < 3.0.0`),
  which provides `mix deps.audit` and is a legitimate package (RESEARCH Package Legitimacy Audit:
  mirego/mix_audit, BSD-3, `[VERIFIED: hex.pm]`). Verification is `mix deps.get` resolving it into
  `mix.lock` + the CI advisory step running it advisory-only.
</flagged_assumptions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/91-quality-supply-chain-polish/91-CONTEXT.md
@.planning/phases/91-quality-supply-chain-polish/91-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add .github/dependabot.yml (mix + github-actions)</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - `.planning/phases/91-quality-supply-chain-polish/91-RESEARCH.md` Code Example 2 — the verified `version: 2` schema with both ecosystems, weekly schedule, and `groups`/`update-types`.
    - `.planning/phases/91-quality-supply-chain-polish/91-CONTEXT.md` D-05/D-06 — two ecosystems (`mix` `/`, `github-actions` `/`), weekly, grouped minor/patch, SHA updated in place with human-readable tag.
    - `.github/workflows/` — confirm actions are SHA-pinned with a trailing human tag comment (so the `github-actions` ecosystem bumps the SHA, not the tag).
  </read_first>
  <action>
    Create `.github/dependabot.yml` with `version: 2` and two `updates:` entries (D-05). Entry one:
    `package-ecosystem: "mix"`, `directory: "/"`, `schedule` with `interval: "weekly"`, and a `groups:`
    entry (e.g. `mix-minor-patch`) with `update-types: ["minor", "patch"]` to collapse minor/patch
    bumps into one weekly PR (D-06). Entry two: identical shape with `package-ecosystem:
    "github-actions"`, `directory: "/"` (scans `.github/workflows` + any root `action.yml`), weekly,
    grouped minor/patch. Dependabot cosmetics — `open-pull-requests-limit`, `commit-message` prefix,
    labels, finer `patterns` grouping — are Claude's Discretion; keep it minimal and readable. The
    `github-actions` ecosystem updates the pinned SHA in place while preserving the human-readable tag
    comment; no workflow wiring is needed (Dependabot is GitHub-native).
  </action>
  <acceptance_criteria>
    - Source: `.github/dependabot.yml` contains `version: 2`, a `package-ecosystem: "mix"` entry, and a `package-ecosystem: "github-actions"` entry.
    - Source: both ecosystem entries declare `directory: "/"` and a weekly `schedule`.
    - Source: each ecosystem entry has a `groups:` block whose `update-types` include `"minor"` and `"patch"`.
    - Command: the file is valid YAML — `python3 -c "import yaml; yaml.safe_load(open('.github/dependabot.yml'))"` exits 0 (or `actionlint`/`yamllint` if available).
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'version: 2' .github/dependabot.yml && grep -q 'package-ecosystem: "mix"' .github/dependabot.yml && grep -q 'package-ecosystem: "github-actions"' .github/dependabot.yml && grep -q 'interval: "weekly"' .github/dependabot.yml && python3 -c "import yaml; yaml.safe_load(open('.github/dependabot.yml'))"</automated>
    <human-check>Backstop (post-merge): GitHub → Insights → Dependency graph → Dependabot lists both `mix` and `github-actions` ecosystems as parsed/enabled with no config error.</human-check>
  </verify>
  <done>`.github/dependabot.yml` exists, is valid YAML, and configures weekly grouped update PRs for both the `mix` and `github-actions` ecosystems.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add mix_audit dep + extend the ci.audit alias</name>
  <files>mix.exs, mix.lock</files>
  <read_first>
    - `.planning/phases/91-quality-supply-chain-polish/91-RESEARCH.md` Code Example 4 + Package Legitimacy Audit — the exact dep tuple, the alias extension, and the mix_audit legitimacy verdict (Approved/`[VERIFIED: hex.pm]`).
    - `mix.exs` line 42 — the last deps entry (`{:credo, "~> 1.7", only: [:dev, :test], runtime: false}`) before the `] ++ mailglass_deps()...` tail, where the mix_audit tuple is added.
    - `mix.exs` line 83 — the current `"ci.audit": ["hex.audit"]` alias.
    - `.github/workflows/ci.yml` lines 91-99 — the "Dependency advisory audit (advisory-only)" step (`run: mix ci.audit`, `continue-on-error: true`), which is UNCHANGED and already runs the extended alias (D-12).
  </read_first>
  <action>
    In `mix.exs` `deps()`, add the dependency tuple `{:mix_audit, "~> 2.1", only: [:dev, :test],
    runtime: false}` (D-10) — mix_audit provides the `mix deps.audit` task that scans `mix.lock`
    against the Elixir security-advisory DB. If `~> 2.1` is stale at execute time, pick the current
    stable minor (Claude's Discretion) — verify current with `mix hex.info mix_audit`. Then extend the
    `ci.audit` alias (line 83) from `["hex.audit"]` to `["hex.audit", "deps.audit"]` (D-11): keep both
    — `hex.audit` checks retired packages, `deps.audit` scans disclosed CVEs; they are complementary.
    Run `mix deps.get` to resolve mix_audit into `mix.lock` and commit the lockfile. Do NOT touch the
    CI advisory step or its `continue-on-error: true` — the advisory-only posture is preserved by
    decision (D-12); promoting it to blocking is the deferred `DEF-AUDIT-BLOCK`, out of scope.
  </action>
  <acceptance_criteria>
    - Source: `mix.exs` `deps()` contains `{:mix_audit, "~> 2.1", only: [:dev, :test], runtime: false}` (or the chosen current-minor bound).
    - Source: `mix.exs` contains `"ci.audit": ["hex.audit", "deps.audit"]`.
    - Command: `mix deps.get` succeeds and `mix.lock` then contains a `mix_audit` entry (`grep -q 'mix_audit' mix.lock`).
    - Command: `mix help deps.audit` exits 0 (mix_audit provides the task).
    - Source (D-12 intact, read-only): `ci.yml`'s advisory-audit step still declares `continue-on-error: true`.
  </acceptance_criteria>
  <reversibility rating="reversible">Dev/test-only dep + alias member; removing the tuple and the `deps.audit` string reverts with no runtime impact (`runtime: false`).</reversibility>
  <verify>
    <automated>grep -q ':mix_audit' mix.exs && grep -q '"ci.audit": \["hex.audit", "deps.audit"\]' mix.exs && mix deps.get >/dev/null && grep -q 'mix_audit' mix.lock && mix help deps.audit >/dev/null</automated>
    <human-check>Backstop (CI): the `lint` job's "Dependency advisory audit (advisory-only)" step logs both `hex.audit` and `deps.audit` output and the gate stays green even if an advisory is printed.</human-check>
  </verify>
  <done>mix_audit is a dev/test dep resolved in `mix.lock`; `ci.audit` runs `hex.audit` + `deps.audit`; the CI advisory step is unchanged and still advisory-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependabot service → repo | automated dependency-update PRs are proposed changes, untrusted until human-reviewed and merged |
| Hex registry → build | `mix deps.get` pulls `mix_audit` (dev/test) into the dependency tree |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-91-SC | Tampering | hex/github-actions installs — `mix_audit` dep + Dependabot SHA/dep bumps | high | mitigate | `mix_audit` is Approved / `[VERIFIED: hex.pm]` (RESEARCH Package Legitimacy Audit), pinned `~> 2.1`, `only: [:dev, :test]`, `runtime: false` (never in the shipped lib); Dependabot updates SHA-pinned actions in place with the human tag → every bump is a reviewable PR, SHA pinning retained |
| T-91-03 | Tampering | stale / compromised pinned action SHA | high | mitigate | Dependabot `github-actions` ecosystem opens grouped weekly PRs to bump pinned SHAs; human review before merge closes the drift window (QUAL-02) |
| T-91-04 | Information Disclosure | vulnerable transitive Hex dep | medium | accept | `mix deps.audit` advisory scan surfaces disclosed CVEs in job output; advisory-only by explicit decision (D-12 / deferred `DEF-AUDIT-BLOCK`), accepted-risk documented in SECURITY.md — not silently treated as blocking |
</threat_model>

<verification>
- `.github/dependabot.yml` is valid YAML covering `mix` + `github-actions`, weekly, grouped minor/patch.
- `mix_audit` (`~> 2.1`, dev/test, `runtime: false`) is in `mix.exs` and resolved in `mix.lock`.
- `ci.audit` == `["hex.audit", "deps.audit"]`; the CI advisory step and its `continue-on-error: true` are unchanged.
- Backstop: a CI run shows the advisory step running both audits without failing the gate; GitHub Insights shows both Dependabot ecosystems parsed.
</verification>

<success_criteria>
QUAL-02 satisfied: Dependabot opens update PRs for both `mix` and `github-actions`.
QUAL-04 satisfied: `mix_audit` runs as a real advisory-DB scan, non-blocking, with findings visible
in job output — matching the deliberate `hex.audit` posture.
</success_criteria>

<output>
Create `.planning/phases/91-quality-supply-chain-polish/91-02-SUMMARY.md` when done.
</output>
