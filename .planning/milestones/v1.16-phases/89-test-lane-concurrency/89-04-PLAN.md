---
phase: 89-test-lane-concurrency
plan: 04
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - test/chimeway/rendering/render_identity_integration_test.exs
  - test/chimeway/dispatch/signal_router_worker_test.exs
  - test/chimeway/webhooks/process_feedback_worker_test.exs
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "The rendering module and both Oban-worker-named modules (`signal_router_worker_test`, `process_feedback_worker_test`) all run `async: true` and pass standalone under `--seed 0` (CONC-01/D-01)."
    - "`signal_router_worker_test.exs` and `process_feedback_worker_test.exs` — the two candidates D-02 flagged as 'highest risk' by name — are confirmed by RESEARCH.md's per-candidate audit to cross NO process boundary at all: `Oban.Testing.perform_job/2` and a direct `Worker.perform/1` call both execute synchronously in the test process, so there is nothing to grant via `Sandbox.allow/3`."
    - "This task closes out the full 20-file D-01 flip set: 1 (tracer, Plan 89-01) + 9 (Plan 89-02) + 7 (Plan 89-03) + 3 (this plan) = 20; `telemetry_correlation_test.exs` remains the sole `async: false` exception (D-01/D-02/D-03)."
  artifacts:
    - "3 test files with `use Chimeway.DataCase, async: true` (was `async: false`)"
  key_links:
    - "'Worker' in a test's name suggests async execution, but `Oban.Testing.perform_job/2` and a bare `Worker.perform/1` call are both synchronous, in-process calls in this codebase's test harness — the two files named as riskiest are, per RESEARCH.md's corrected finding, actually the lowest-risk on the process-boundary axis."
---

<objective>
Close out the D-01 candidate flip set: the rendering module and the two Oban-worker-named modules that D-02 originally flagged as highest-risk on the spawned-process axis, now confirmed by RESEARCH.md's completed audit to be the lowest-risk (no process boundary crossed at all). After this plan, 20 of the 21 D-01 candidates run `async: true`; only `telemetry_correlation_test.exs` stays `async: false`, by design (D-01/D-02).

Purpose: Finish the fan-out with the two files whose names most invite second-guessing, backed by the audit's concrete finding rather than the naming intuition.
Output: 3 test files running `async: true`, each independently verified to still pass under `--seed 0`. The full 20-file CONC-01 flip set is complete after this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-test-lane-concurrency/89-CONTEXT.md
@.planning/phases/89-test-lane-concurrency/89-RESEARCH.md
</context>

## Artifacts this plan produces

Three atomic, per-directory async flips. No config files are touched — only the `async:` tag on 3 existing test files, closing out the 20-file CONC-01 candidate set built on Plan 89-01's proven mechanism.

<tasks>

<task type="auto">
  <name>Task 1: Flip the rendering module to async: true</name>
  <files>test/chimeway/rendering/render_identity_integration_test.exs</files>
  <read_first>
    - test/chimeway/rendering/render_identity_integration_test.exs:1-2 (`use Chimeway.DataCase, async: false` at line 2; calls `Trigger.trigger/3` but never `assert_receive`s on a telemetry event — same fire-without-assert safety reasoning as Plan 89-03's trigger group)
    - .planning/phases/89-test-lane-concurrency/89-RESEARCH.md §4 and section 3 audit table (D-02)
  </read_first>
  <action>
    Change line 2 from `use Chimeway.DataCase, async: false` to `use Chimeway.DataCase, async: true` (CONC-01/D-01). This file fires the same production telemetry spans `telemetry_correlation_test.exs` listens for but never itself asserts on a telemetry message, so it carries no cross-talk risk. No app-env mutation, no prefix/dynamic-repo touch, no spawn primitives. Make no other change to this file.

    Commit as `test(89-04): flip rendering DataCase module to async` — test-tag-only work, not `feat`/`perf`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/rendering/render_identity_integration_test.exs --seed 0 && echo RENDERING_OK</automated>
  </verify>
  <acceptance_criteria>
    - The file reads `use Chimeway.DataCase, async: true` at line 2.
    - `mix test` on the file with `--seed 0` passes with zero failures.
  </acceptance_criteria>
  <done>The rendering `DataCase` module runs `async: true` and passes under a fixed seed.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the dispatch worker test to async: true</name>
  <files>test/chimeway/dispatch/signal_router_worker_test.exs</files>
  <read_first>
    - test/chimeway/dispatch/signal_router_worker_test.exs:1-3 (`use Chimeway.DataCase, async: false` at line 2, `use Oban.Testing, repo: Chimeway.Repo` at line 3; RESEARCH.md's audit confirms `Oban.Testing.perform_job/2` runs the worker's `perform/1` synchronously in the calling test process — no process is spawned, so there is nothing for `Sandbox.allow/3` to grant)
    - .planning/phases/89-test-lane-concurrency/89-RESEARCH.md, section 3 audit table row for `dispatch/signal_router_worker_test.exs` and the "two clarifying findings" paragraph (D-02)
  </read_first>
  <action>
    Change line 2 from `use Chimeway.DataCase, async: false` to `use Chimeway.DataCase, async: true` (CONC-01/D-01). Despite the "worker" name and D-02's original flag as highest-risk-by-naming, RESEARCH.md's audit confirms `Oban.Testing.perform_job/2` executes the job's `perform/1` synchronously in the test process — no process boundary is crossed, so this file needs no code change beyond the tag flip. Make no other change to this file.

    Commit as `test(89-04): flip signal_router_worker_test to async` — test-tag-only work, not `feat`/`perf`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/dispatch/signal_router_worker_test.exs --seed 0 && echo DISPATCH_OK</automated>
  </verify>
  <acceptance_criteria>
    - The file reads `use Chimeway.DataCase, async: true` at line 2.
    - `mix test` on the file with `--seed 0` passes with zero failures.
  </acceptance_criteria>
  <done>The dispatch worker `DataCase` module runs `async: true` and passes under a fixed seed.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the webhooks worker test to async: true — completes the CONC-01 flip set</name>
  <files>test/chimeway/webhooks/process_feedback_worker_test.exs</files>
  <read_first>
    - test/chimeway/webhooks/process_feedback_worker_test.exs:1-3 (`use Chimeway.DataCase, async: false` at line 2, `use Oban.Testing, repo: Chimeway.Repo` at line 3; RESEARCH.md's audit confirms the test calls `ProcessFeedbackWorker.perform/1` directly, same synchronous in-process reasoning as Task 2)
    - .planning/phases/89-test-lane-concurrency/89-RESEARCH.md, section 3 audit table row for `webhooks/process_feedback_worker_test.exs` (D-02)
  </read_first>
  <action>
    Change line 2 from `use Chimeway.DataCase, async: false` to `use Chimeway.DataCase, async: true` (CONC-01/D-01). Same reasoning as Task 2: calling `Worker.perform/1` directly is a synchronous, in-process call — no process boundary, no `Sandbox.allow/3` needed. Make no other change to this file. This is the 20th and final file in the D-01 flip set — `telemetry_correlation_test.exs` remains the sole `async: false` exception (D-03).

    Commit as `test(89-04): flip process_feedback_worker_test to async — completes the CONC-01 flip set (20/21)` — test-tag-only work, not `feat`/`perf`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/webhooks/process_feedback_worker_test.exs --seed 0 && echo WEBHOOKS_OK && grep -c "async: false" test/chimeway/telemetry_correlation_test.exs | grep -q "^1$" && echo TELEMETRY_STILL_SERIAL_OK</automated>
  </verify>
  <acceptance_criteria>
    - The file reads `use Chimeway.DataCase, async: true` at line 2.
    - `mix test` on the file with `--seed 0` passes with zero failures.
    - `test/chimeway/telemetry_correlation_test.exs` is untouched by this plan and still reads `async: false` (D-03 exception, confirmed still in place).
  </acceptance_criteria>
  <done>The webhooks worker `DataCase` module runs `async: true` and passes under a fixed seed; the full 20-file CONC-01 flip set is complete with `telemetry_correlation_test.exs` confirmed as the sole remaining `async: false` exception.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `test/support/data_case.ex` sandbox mode boundary | These 3 modules move from shared-mode to allowance-mode; the two worker-named files are the ones D-02 originally flagged as highest-risk on the spawned-process axis |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-01 | Tampering | Ecto Sandbox isolation | high | mitigate | RESEARCH.md's audit confirms both worker-named files execute synchronously in the test process — no spawned process, nothing to grant via `Sandbox.allow/3`; verified by re-running each file standalone under async. |
| T-89-04 | Repudiation | Ordering-coupling regression from closing the flip set | low | mitigate | Each file verified independently; the CONC-04 multi-run proof in Plan 89-06 covers the fully-assembled 20-file set once this plan lands (last of the three parallel fan-out plans this wave). |
</threat_model>

<verification>
- `mix test test/chimeway/rendering/render_identity_integration_test.exs --seed 0` — passes.
- `mix test test/chimeway/dispatch/signal_router_worker_test.exs --seed 0` — passes.
- `mix test test/chimeway/webhooks/process_feedback_worker_test.exs --seed 0` — passes.
- `telemetry_correlation_test.exs` confirmed still `async: false` and untouched.
</verification>

<success_criteria>
The rendering module and both worker-named modules run `async: true`, each proven to cross no process boundary at all despite their names. The full 20-file CONC-01 candidate set is now flipped, with `telemetry_correlation_test.exs` the sole documented exception — ready for Plan 89-05's warnings-as-errors work and Plan 89-06's closing seed-parity proof.
</success_criteria>

<output>
Create `.planning/phases/89-test-lane-concurrency/89-04-SUMMARY.md` when done.
</output>
