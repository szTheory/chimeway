---
phase: 89-test-lane-concurrency
plan: 02
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - test/chimeway/inbox_state_transition_test.exs
  - test/chimeway/inbox_pagination_test.exs
  - test/chimeway/inbox_query_test.exs
  - test/chimeway/inbox_integration_test.exs
  - test/chimeway/digests/digest_rule_test.exs
  - test/chimeway/digests/emission_test.exs
  - test/chimeway/digests/accumulation_test.exs
  - test/chimeway/workflows_inspection_test.exs
  - test/chimeway/workflows_test.exs
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "The 4 inbox-suite modules, 3 digest modules, and 2 top-level workflow modules all run `async: true` and pass standalone under `--seed 0` (CONC-01/D-01)."
    - "Every flipped file in this plan was D-02-vetted: none mutate `Application.put_env`/`delete_env`, none touch tenant prefix or a dynamic repo, and neither of the two files with a `Task.async_stream` (only `workflows_test.exs` in this group) reads/writes `Chimeway.Repo` from a spawned process without `Sandbox.allow/3`."
    - "No file in this plan's group is `telemetry_correlation_test.exs` or an app-env/prefix mutator — those stay untouched per D-03."
  artifacts:
    - "9 test files with `use Chimeway.DataCase, async: true` (was `async: false`)"
  key_links:
    - "`workflows_test.exs`'s `Task.async_stream` block already calls `Sandbox.allow(Chimeway.Repo, parent, self())` inside every spawned task before touching `Repo` — this is what makes it safe to flip despite spawning concurrent tasks."
---

<objective>
Fan the tracer-proven pattern (Plan 89-01) out to 9 of the remaining 19 D-01 candidates, grouped by directory for atomic, reviewable commits: the 4 inbox-suite modules, the 3 digest modules, and the 2 remaining top-level workflow modules. Every file here was individually read/grepped in RESEARCH.md's completed per-candidate audit and confirmed D-02-safe.

Purpose: Convert the largest contiguous, lowest-risk block of pure-DB candidates to `async: true`, expanding the tracer's proven mechanism without introducing any new config or process-boundary risk.
Output: 9 test files running `async: true`, each independently verified to still pass under `--seed 0`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-test-lane-concurrency/89-CONTEXT.md
@.planning/phases/89-test-lane-concurrency/89-RESEARCH.md
</context>

## Artifacts this plan produces

Three atomic, directory-grouped async flips building on Plan 89-01's proven pool_size + sandbox-allowance mechanism. No config files are touched in this plan — only the `async:` tag on 9 existing test files.

<tasks>

<task type="auto">
  <name>Task 1: Flip the inbox-suite modules to async: true</name>
  <files>test/chimeway/inbox_state_transition_test.exs, test/chimeway/inbox_pagination_test.exs, test/chimeway/inbox_query_test.exs, test/chimeway/inbox_integration_test.exs</files>
  <read_first>
    - test/chimeway/inbox_state_transition_test.exs:1-3 (`use Chimeway.DataCase, async: false` at line 2; also carries `use Oban.Testing, repo: Chimeway.Repo` at line 3, but grep found no `Task`/`GenServer`/`spawn` primitives in this file — RESEARCH.md's "remaining 17: no spawn primitives found" bucket)
    - test/chimeway/inbox_pagination_test.exs:1-2, test/chimeway/inbox_query_test.exs:1-2, test/chimeway/inbox_integration_test.exs:1-2 (each `use Chimeway.DataCase, async: false` at line 2, no app-env/prefix mutation, no spawn primitives)
    - .planning/phases/89-test-lane-concurrency/89-RESEARCH.md, section 3 per-candidate audit table (D-02)
  </read_first>
  <action>
    In each of the 4 files, change line 2 from `use Chimeway.DataCase, async: false` to `use Chimeway.DataCase, async: true` (CONC-01/D-01). All four are D-02-vetted flip-safe: none mutate global app-env, none touch tenant prefix or a dynamic repo, and none spawn a `Task`/`GenServer`/process that reaches `Chimeway.Repo` outside the owning test process — `inbox_state_transition_test.exs`'s `Oban.Testing` usage exercises jobs synchronously in-process, same reasoning as the already-flipped `signal_test.exs` in Plan 89-01. Make no other change to these files.

    Commit as `test(89-02): flip inbox-suite DataCase modules to async` — test-tag-only work, not `feat`/`perf`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/inbox_state_transition_test.exs test/chimeway/inbox_pagination_test.exs test/chimeway/inbox_query_test.exs test/chimeway/inbox_integration_test.exs --seed 0 && echo INBOX_GROUP_OK</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files read `use Chimeway.DataCase, async: true` at line 2.
    - `mix test` on the 4 files together with `--seed 0` passes with zero failures.
  </acceptance_criteria>
  <done>The 4 inbox-suite `DataCase` modules run `async: true` and pass together under a fixed seed.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the digest modules to async: true</name>
  <files>test/chimeway/digests/digest_rule_test.exs, test/chimeway/digests/emission_test.exs, test/chimeway/digests/accumulation_test.exs</files>
  <read_first>
    - test/chimeway/digests/digest_rule_test.exs:1-2, test/chimeway/digests/emission_test.exs:1-2, test/chimeway/digests/accumulation_test.exs:1-2 (each `use Chimeway.DataCase, async: false` at line 2, no app-env/prefix mutation, no spawn primitives per RESEARCH.md's audit)
    - .planning/phases/89-test-lane-concurrency/89-RESEARCH.md, section 3 per-candidate audit table (D-02)
  </read_first>
  <action>
    In each of the 3 files, change line 2 from `use Chimeway.DataCase, async: false` to `use Chimeway.DataCase, async: true` (CONC-01/D-01). All three are in RESEARCH.md's "no spawn primitives found — safe" bucket: no app-env mutation, no prefix/dynamic-repo touch, no cross-process `Repo` access. Make no other change to these files.

    Commit as `test(89-02): flip digest DataCase modules to async` — test-tag-only work, not `feat`/`perf`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/digests/digest_rule_test.exs test/chimeway/digests/emission_test.exs test/chimeway/digests/accumulation_test.exs --seed 0 && echo DIGESTS_GROUP_OK</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files read `use Chimeway.DataCase, async: true` at line 2.
    - `mix test` on the 3 files together with `--seed 0` passes with zero failures.
  </acceptance_criteria>
  <done>The 3 digest `DataCase` modules run `async: true` and pass together under a fixed seed.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the remaining top-level workflow modules to async: true</name>
  <files>test/chimeway/workflows_inspection_test.exs, test/chimeway/workflows_test.exs</files>
  <read_first>
    - test/chimeway/workflows_inspection_test.exs:1-2 (`use Chimeway.DataCase, async: false` at line 2, no app-env/prefix mutation, no spawn primitives)
    - test/chimeway/workflows_test.exs:1-2 and its `Task.async_stream` block (2 concurrent tasks) — RESEARCH.md confirms it already calls `Ecto.Adapters.SQL.Sandbox.allow(Repo, parent, self())` inside every spawned task before any `Repo` call, so the flip is safe without code changes; per RESEARCH.md's Pitfall 2, `allow/3` grants the *existing* checked-out connection rather than checking out a new one, so 2 concurrent tasks still consume exactly 1 pool connection
    - .planning/phases/89-test-lane-concurrency/89-RESEARCH.md, section 3 per-candidate audit table and "Pitfall 2" (D-02)
  </read_first>
  <action>
    In both files, change line 2 from `use Chimeway.DataCase, async: false` to `use Chimeway.DataCase, async: true` (CONC-01/D-01). `workflows_inspection_test.exs` has no spawn primitives at all. `workflows_test.exs` spawns `Task.async_stream` but already grants sandbox access to each spawned task before touching `Repo` — it was written defensively in anticipation of concurrent execution and needs no code change to flip safely. Make no other change to these files.

    Commit as `test(89-02): flip remaining top-level workflow DataCase modules to async` — test-tag-only work, not `feat`/`perf`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/workflows_inspection_test.exs test/chimeway/workflows_test.exs --seed 0 && echo WORKFLOWS_GROUP_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both files read `use Chimeway.DataCase, async: true` at line 2.
    - `mix test` on the 2 files together with `--seed 0` passes with zero failures, including `workflows_test.exs`'s concurrent-task assertions.
  </acceptance_criteria>
  <done>Both remaining top-level workflow `DataCase` modules run `async: true` and pass together under a fixed seed, including the `Task.async_stream`-based concurrent test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `test/support/data_case.ex` sandbox mode boundary | Each of these 9 modules moves from shared-mode to allowance-mode sandbox connections; `workflows_test.exs`'s spawned tasks are the one case in this group that must already hold an explicit grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-01 | Tampering | Ecto Sandbox isolation | high | mitigate | All 9 files in this plan were individually read/grepped in RESEARCH.md's D-02 audit and confirmed to hold no un-granted cross-process `Repo` access; `workflows_test.exs`'s spawned tasks already call the sandbox-allowance function before touching `Repo`. |
| T-89-04 | Repudiation | Ordering-coupling regression from this group's flip | medium | mitigate | Each task's per-file group is verified independently under `--seed 0`; the full-suite, multi-run CONC-04 proof in Plan 89-06 covers cross-module interaction once all fan-out plans land. |
</threat_model>

<verification>
- `mix test <inbox files> --seed 0` — 4 files pass together.
- `mix test <digest files> --seed 0` — 3 files pass together.
- `mix test <workflow files> --seed 0` — 2 files pass together, including the async-stream test.
</verification>

<success_criteria>
9 of the remaining 19 D-01 candidates run `async: true`, each individually vetted flip-safe and independently verified to pass under a fixed seed, with zero code changes needed beyond the tag flip.
</success_criteria>

<output>
Create `.planning/phases/89-test-lane-concurrency/89-02-SUMMARY.md` when done.
</output>
