---
phase: 89-test-lane-concurrency
plan: 06
type: execute
wave: 4
depends_on: ["89-05"]
files_modified: []
autonomous: true
requirements: [CONC-04]
must_haves:
  truths:
    - "3 consecutive `mix ci.test` runs on CI's default (random, timestamp-derived) seed produce an identical pass/fail set — the exact same ExUnit summary line — with zero DBConnection ownership/queue-timeout signatures in any of the three logs (CONC-04/D-06)."
    - "One additional `mix ci.test --seed 0` run produces the identical pass/fail set as the 3 random-seed runs, proving no intra-file definition-order dependency was introduced alongside the async conversion."
    - "`release_gate_contract_test.exs` and `ci_observability_contract_test.exs` stay green after the full phase's changes land."
    - "No `lib/` file was modified across any commit in this phase (Plans 89-01 through 89-06) — a git-diff check across the phase's commit range confirms this test/config-only constraint held end to end."
  artifacts:
    - "4 captured run logs (3 random-seed + 1 --seed 0) retained as this plan's verification evidence"
  key_links:
    - "`--seed 0` only fixes intra-file test-definition order — it does NOT serialize `async: true` cross-module scheduling (RESEARCH.md, 'Ordering-Coupling Proof'). The 3 repeated random-seed runs are what actually exercise the cross-module interleaving hazard; `--seed 0` is a necessary-but-not-sufficient supplementary check, never treated as the sole proof."
    - "A diverging run here means: identify the diverging module, then revert IT to `async: false` (D-06) rather than chase a shared-state fix under time pressure — this plan's action explicitly does not attempt a same-session fix-forward on a genuine flake."
---

<objective>
Close the phase with the CONC-04 ordering-coupling proof: run the fully-assembled 20-file async conversion (Plans 89-01–89-04) plus the warnings-as-errors lane (Plan 89-05) three consecutive times on CI's default random seed, then once more with `--seed 0`, and confirm all four runs produce the identical pass/fail set with no pool or ownership failure signatures. Close by re-confirming both release-gate/CI-observability contract suites and that zero `lib/` files were touched anywhere in the phase, then map each of the phase's four ROADMAP/CONTEXT success criteria to its concrete evidence.

Purpose: Prove — not assert — that the async conversion introduced no test-ordering coupling and no pool exhaustion, per D-06's explicit standard: "a green-on-lucky-seed flip is a failure, not a pass." A single local proof run here is the automated gate that lands the phase; the same `mix ci.test` outcome will additionally be visible on push-triggered CI runs per 89-VALIDATION.md's manual cross-check, but is not itself blocking given the deterministic local proof.
Output: 4 captured, compared run logs showing identical pass/fail across 3 random seeds + 1 fixed seed; both contract suites green; a documented zero-`lib/`-changes confirmation; and a closing map of all 4 phase success criteria to their evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-test-lane-concurrency/89-CONTEXT.md
@.planning/phases/89-test-lane-concurrency/89-RESEARCH.md
@.planning/phases/89-test-lane-concurrency/89-VALIDATION.md
</context>

## Artifacts this plan produces

No source files are modified by this plan — it is the phase's closing verification wave, producing captured run-log evidence and a goal-backward closure check against all four ROADMAP/CONTEXT success criteria.

<tasks>

<task type="auto">
  <name>Task 1: 3 consecutive default-random-seed mix ci.test runs, identical pass/fail</name>
  <files>none (verification-only; no file changes)</files>
  <read_first>
    - .planning/phases/89-test-lane-concurrency/89-CONTEXT.md, success criterion 1 and D-06
    - .planning/phases/89-test-lane-concurrency/89-RESEARCH.md, "Ordering-Coupling Proof (CONC-04)" and "Pitfall 3"
  </read_first>
  <action>
    Run `mix ci.test` three consecutive times, each on its own default (random, timestamp-derived) seed — do not pass `--seed` on any of these three. Capture each run's full output and its final ExUnit summary line. Confirm all three summary lines are byte-identical (same test count, same failure count — expected to be the full count with zero failures) and that none of the three run logs contains a missing-allowance or pool/ownership-timeout failure signature. If any of the three diverges from the others, do not attempt a same-session fix: identify the diverging module from the log and flag it for reversion to `async: false` (D-06) rather than chasing a shared-state fix under time pressure — a flip that only passes on a lucky seed is a failure of this phase's goal, not an acceptable outcome to patch around.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && for i in 1 2 3; do mix ci.test > /tmp/89-06-run-$i.log 2>&1; done && for i in 1 2 3; do grep -oE '[0-9]+ (test|tests), [0-9]+ (failure|failures)' /tmp/89-06-run-$i.log; done | sort -u > /tmp/89-06-unique-summaries.txt && [ "$(wc -l < /tmp/89-06-unique-summaries.txt)" -eq 1 ] && ! grep -qE "OwnershipError|owned the connection for longer than|DBConnection\.ConnectionError" /tmp/89-06-run-1.log /tmp/89-06-run-2.log /tmp/89-06-run-3.log && echo THREE_RANDOM_RUNS_IDENTICAL_AND_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - All 3 runs produce the identical ExUnit summary line (same test/failure counts).
    - None of the 3 captured logs contains a missing-allowance or pool/ownership-timeout failure signature.
  </acceptance_criteria>
  <done>3 consecutive default-random-seed `mix ci.test` runs pass identically with zero pool or ownership errors — no seed-dependent flake detected across the fully-assembled 20-file async conversion.</done>
</task>

<task type="auto">
  <name>Task 2: --seed 0 run, cross-check against the 3 random runs, contract guard, lib/ cleanliness, and success-criteria closure</name>
  <files>none (verification-only; no file changes)</files>
  <read_first>
    - .planning/phases/89-test-lane-concurrency/89-CONTEXT.md, all 4 success criteria
    - .planning/phases/89-test-lane-concurrency/89-RESEARCH.md, "Ordering-Coupling Proof (CONC-04)" (why `--seed 0` alone is necessary but not sufficient)
  </read_first>
  <action>
    Run `mix ci.test --seed 0` once and confirm its ExUnit summary line matches the identical summary captured from Task 1's 3 random-seed runs, with no missing-allowance or pool/ownership-timeout failure signature in its log. Then re-run `release_gate_contract_test.exs` and `ci_observability_contract_test.exs` as the phase's final contract guard. Then confirm, via a git-diff across every commit in this phase (from the first Plan 89-01 commit through the current HEAD), that no path under `lib/` was ever touched — this phase is test/config-only by constraint. Finally, write the phase's closing goal-backward mapping into the SUMMARY: (1) CONC-01 — 20/21 D-01 candidates run `async: true`, `telemetry_correlation_test.exs` the sole documented exception, evidenced by Plans 89-01–89-04's per-file/-group `--seed 0` passes plus this plan's 4 full-suite runs; (2) CONC-02 — `config/test.exs` sets an explicit, formula-derived `pool_size`, evidenced by zero pool/ownership failures across all 4 runs in this plan; (3) CONC-03 — `ci.test` runs `--warnings-as-errors` at `verify.*` parity, evidenced by Plan 89-05's clean run plus its documented inject/revert negative proof; (4) CONC-04 — identical pass/fail across 3 random-seed runs and 1 `--seed 0` run, evidenced by this task's cross-check. Note in the SUMMARY that the equivalent push-triggered CI runs provide the natural follow-on confirmation once this phase is merged, per 89-VALIDATION.md's Manual-Only Verifications table — not a blocking step given the deterministic local proof already captured.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix ci.test --seed 0 > /tmp/89-06-seed0-run.log 2>&1; grep -oE '[0-9]+ (test|tests), [0-9]+ (failure|failures)' /tmp/89-06-seed0-run.log > /tmp/89-06-seed0-summary.txt && diff -q /tmp/89-06-unique-summaries.txt /tmp/89-06-seed0-summary.txt && ! grep -qE "OwnershipError|owned the connection for longer than|DBConnection\.ConnectionError" /tmp/89-06-seed0-run.log && mix test test/chimeway/release_gate_contract_test.exs test/chimeway/ci_observability_contract_test.exs && FIRST=$(git log --oneline --grep="(89-01)" --reverse | head -1 | cut -d' ' -f1) && { [ -z "$FIRST" ] || ! git diff --stat "${FIRST}"^..HEAD -- lib/ | grep -q .; } && echo CONC04_CLOSING_PROOF_OK</automated>
  </verify>
  <acceptance_criteria>
    - `mix ci.test --seed 0`'s summary line is byte-identical to the 3 random-seed runs' summary line, with no failure signature present.
    - `release_gate_contract_test.exs` and `ci_observability_contract_test.exs` are both green.
    - No commit in this phase's range touched any path under `lib/`.
    - The SUMMARY documents all 4 phase success criteria mapped to their concrete evidence.
  </acceptance_criteria>
  <done>The ordered `--seed 0` run matches the 3 random-seed runs exactly, both contract suites are green, zero `lib/` changes are confirmed across the phase, and all 4 ROADMAP/CONTEXT success criteria are closed out with documented evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExUnit scheduler nondeterminism → CI pass/fail signal | The only way to detect an ordering-coupling regression is repeated observation across different random seeds plus one fixed seed — no single run is sufficient |
| Phase-wide `lib/`-untouched constraint → this phase's actual commit history | The constraint is only provably held by inspecting the real git history, not by trusting the plan's stated scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-04 | Repudiation | Ordering-coupling regression (async scheduling nondeterminism) | medium | mitigate | D-06: 3 consecutive default-random-seed runs + 1 `--seed 0` run must produce an identical pass/fail set; any divergence is treated as a real failure requiring reversion of the offending module, never patched around under time pressure. |
| T-89-02 | Denial of Service | `Chimeway.Repo` connection pool under sustained repeated-run load | low (already proven in Plan 89-01) | mitigate | All 4 runs in this plan are checked for the pool/ownership failure-signature class; a clean result across 4 full-suite runs (6 total including Plan 89-01's 2) is the final CONC-02 confirmation. |
| T-89-05 | Tampering | ci-gate/pr-gate lane structure and constraint compliance (`lib/`-untouched) | low | mitigate | Both contract suites re-run one final time; a git-diff across the full phase commit range confirms zero `lib/` paths were touched. |
</threat_model>

<verification>
- 3 consecutive `mix ci.test` runs (default random seed) — identical pass/fail, zero pool/ownership failure signatures.
- 1 `mix ci.test --seed 0` run — identical pass/fail to the 3 random runs, zero failure signatures.
- `mix test test/chimeway/release_gate_contract_test.exs test/chimeway/ci_observability_contract_test.exs` — green.
- Git-diff across the phase's full commit range — zero `lib/` paths touched.
</verification>

<success_criteria>
All 4 ROADMAP/CONTEXT Phase 89 success criteria are proven with captured evidence: (1) the vetted pure-DB modules run `async: true` with no data races across repeated runs; (2) `config/test.exs`'s explicit pool_size produces zero connection-pool-exhaustion failures; (3) `ci.test` fails on any compiler warning at `verify.*` parity; (4) the suite passes identically across randomized-seed runs and one ordered `--seed 0` run. The phase is ready to close.
</success_criteria>

<output>
Create `.planning/phases/89-test-lane-concurrency/89-06-SUMMARY.md` when done.
</output>
