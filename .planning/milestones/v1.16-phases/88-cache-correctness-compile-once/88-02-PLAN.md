---
phase: 88-cache-correctness-compile-once
plan: 02
type: execute
wave: 2
depends_on: ["88-01"]
files_modified:
  - .github/workflows/ci.yml
  - test/chimeway/ci_observability_contract_test.exs
autonomous: true
requirements: [CACHE-01, CACHE-02, CACHE-04]
must_haves:
  truths:
    - "Each of `verify_gates`, `verify_example`, `verify_runtime_prefix`, `verify_journeys`, `verify_mailglass`, `verify_inbox`, `verify_admin` declares `needs: [build]` and restores the identical shared `deps-…` and `build-test-…` keys via `actions/cache/restore` (restore-only) with `fail-on-cache-miss: true`, so all default-graph `:test` lanes collapse onto the one warm producer cache (D-05/D-10)."
    - "The demo-host and nested caches (`cache_demo`, `cache_nested_inbox`, `cache_nested_admin`, `playwright-cache`) keep their own uniquely-namespaced keys and their specific-lockfile globs, byte-for-byte unchanged (D-08)."
    - "`verify_docs` stays on `MIX_ENV: dev`, self-caches a distinct `build-dev-…` key, and does NOT declare `needs: [build]` (D-07 — `ex_doc` is `only: :dev`, so it cannot join `build-test`)."
    - "The `test` matrix self-caches an OTP-parameterized key under a role distinct from `build-test-` (so the OTP-27 leg never collides with or clobbers the producer's `build-test` cache) and does NOT declare `needs: [build]` (D-12)."
    - "Partner lanes `verify_accrue`/`verify_threadline`/`verify_sigra` keep graph-scoped self-cached keys (e.g. `build-test-accrue-…`), never consume the shared cache, and retain their `*_PATH` path-dep injection (D-08)."
    - "Every default-graph root cache key hashes root `mix.lock` (not the recursive lockfile glob), and no lane carries a broad `${{ runner.os }}-mix-` restore-key that could fall back onto a wrong-`MIX_ENV` tree (D-04)."
    - "`release_gate_contract_test.exs` stays green (ci-gate 14 / pr-gate 4 lane counts and names unchanged); `ci_observability_contract_test.exs` is extended to assert the new key schema and the `needs: [build]` wiring on the converted consumers."
  artifacts:
    - ".github/workflows/ci.yml (7 default-graph lanes converted to restore-only consumers; docs/test/partner exception lanes reschemed to distinct self-cached roles; all root globs narrowed to root `mix.lock`)"
    - "test/chimeway/ci_observability_contract_test.exs (extended: shared-key-schema + `needs: [build]` assertions across converted lanes)"
  key_links:
    - "The 7 converted consumers reuse the producer's warm caches only if their `build-test`/`deps` key strings match the producer's exactly — that shared string is the single collision point the whole de-fragmentation depends on."
    - "Distinct role prefixes (`build-dev`, `test-matrix`, `build-test-<partner>`) are what stop the exception lanes from colliding with or clobbering the shared `build-test` cache under GitHub's write-once cache semantics."
---

<objective>
Fan the tracer-proven producer/consumer pattern out across the rest of the default graph: convert the seven remaining plain-hex `:test` lanes to restore-only consumers of the single shared `build-test` + `deps` cache, and reschema the five exception lanes (docs, the OTP matrix, and the three partner lanes) onto distinct self-cached roles so they never collide with the shared key. This is the horizontal expansion that turns the tracer into the de-fragmented, compile-once pipeline CACHE-02 demands, then locks it with contract-test assertions.

Purpose: Collapse the ~11-way cache fragmentation onto one warm entry (fixing the 10 GB LRU churn) while keeping the genuinely-different graphs (partners' path-deps, docs' `:dev`, the OTP-26 leg) correctly isolated on their own keys.
Output: `ci.yml` with all default-graph consumers on the shared restore-only cache and every exception lane reschemed, plus `ci_observability_contract_test.exs` extended to assert the new schema and wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/88-cache-correctness-compile-once/88-CONTEXT.md
@.github/workflows/ci.yml
@test/chimeway/ci_observability_contract_test.exs
@test/chimeway/release_gate_contract_test.exs
</context>

## Artifacts this phase produces

Building on Plan 01's `build` producer and the two converted lanes (`lint`, `install_golden_contract`), this plan touches only `ci.yml` cache/`needs:` structure plus the observability contract test:
- Seven default-graph consumers reschemed to the tracer pattern: `verify_gates`, `verify_example`, `verify_runtime_prefix`, `verify_journeys`, `verify_mailglass`, `verify_inbox`, `verify_admin`. Each gets `id: beam` on `setup-beam`, `needs: [build]`, and its single root `actions/cache` replaced by two `actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830` steps (`cache_deps` + `cache_main`) on the exact shared keys with `fail-on-cache-miss: true`. The extra caches on the demo/nested lanes stay exactly as they are.
- Five exception lanes reschemed but kept self-caching (full `actions/cache`, no `needs: [build]`): `verify_docs` → `build-dev` role on `MIX_ENV: dev`; the `test` matrix → a `test-matrix` role parameterized on `${{ matrix.elixir }}`/`${{ matrix.otp }}`; `verify_accrue`/`verify_threadline`/`verify_sigra` → `build-test-<partner>` graph-scoped roles. All adopt `id: beam`, root-`mix.lock` globs, and drop any broad `${{ runner.os }}-mix-` restore-key.
- `ci_observability_contract_test.exs` extended with a describe block asserting the shared key schema and `needs: [build]` on the converted consumers, and asserting the exception lanes stay off `needs: [build]`.

<tasks>

<task type="auto">
  <name>Task 1: Convert the seven plain-hex default-graph lanes to shared restore-only consumers</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml lines 26-69 (the ALREADY-converted `lint` lane from Plan 01 — the canonical consumer template: `id: beam`, `MIX_ENV: test`, `needs: [build]`, two `actions/cache/restore` steps `cache_deps`/`cache_main` with `fail-on-cache-miss: true`, no `restore-keys:`)
    - .github/workflows/ci.yml `build` producer job (added by Plan 01) — copy its `deps-…` and `build-test-…` key strings verbatim so consumers collide with it
    - .github/workflows/ci.yml lines 71-126 (`verify_gates`, root cache 95-103), 238-313 (`verify_example`, root cache 263-271 + demo cache 272-280 that stays), 314-368 (`verify_runtime_prefix`, root cache 339-346), 369-445 (`verify_journeys`, root cache + demo cache that stays), 446-512 (`verify_mailglass`, root cache + demo cache that stays), 593-671 (`verify_inbox`, root cache 618-625 + nested-inbox 627-634 + demo 636-643 that stay), 836-929 (`verify_admin`, root cache 866-874 + playwright 875-881 + nested-admin 882-890 + demo 891-899 that stay)
    - .planning/phases/88-cache-correctness-compile-once/88-CONTEXT.md (decisions D-04, D-05, D-08, D-10)
  </read_first>
  <action>
    For each of `verify_gates`, `verify_example`, `verify_runtime_prefix`, `verify_journeys`, `verify_mailglass`, `verify_inbox`, `verify_admin`, apply the exact `lint` consumer template proven in Plan 01: (1) add `id: beam` to the lane's `setup-beam` step; (2) add `needs: [build]` to the lane; (3) replace ONLY the ROOT cache step — the one currently carrying `id: cache_main` with `path: deps` + `_build` and the `-mix-verify-<lane>-` key infix — with two `actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830` steps: `id: cache_deps` (`path: deps`, `key: deps-${{ runner.os }}-${{ steps.beam.outputs.elixir-version }}-${{ steps.beam.outputs.otp-version }}-${{ hashFiles('mix.lock') }}`, `fail-on-cache-miss: true`, no `restore-keys:`) and `id: cache_main` (`path: _build`, `key: build-test-${{ runner.os }}-${{ steps.beam.outputs.elixir-version }}-${{ steps.beam.outputs.otp-version }}-${{ hashFiles('mix.lock') }}`, `fail-on-cache-miss: true`, no `restore-keys:`). Every key uses the root `mix.lock` glob. Keep `id: cache_main` on the `build-test` restore so each lane's trailing obs-summary step keeps resolving `CACHE_MAIN_*` and the observability contract's `id: cache_main` assertion stays satisfied.

    Leave every OTHER cache step in these lanes exactly as-is: `verify_example`/`verify_journeys`/`verify_mailglass` keep their `cache_demo` step (keyed on `examples/chimeway_demo_host/mix.lock`); `verify_inbox` keeps `cache_nested_inbox` (keyed on `chimeway_inbox/mix.lock`) and its `cache_demo`; `verify_admin` keeps `playwright-cache`, `cache_nested_admin` (keyed on `chimeway_admin/mix.lock`), and its `cache_demo`. Do NOT touch those keys or their globs — they are uniquely namespaced and intentionally scoped to their own nested lockfiles (D-08). Leave the `verify_example` "Warm :dev build for JOUR-05" step (lines 292-298) in place unchanged (its `:dev` root warm-compile is owned by CI-HARDENING-BACKLOG #2 / Phase 92, not this phase). Do not alter any lane's `if: github.event_name != 'pull_request'` guard, its postgres service, or its `mix verify.*` / `mix ci.*` run steps.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/release_gate_contract_test.exs test/chimeway/ci_observability_contract_test.exs && for l in verify_gates verify_example verify_runtime_prefix verify_journeys verify_mailglass verify_inbox verify_admin; do awk -v L="  $l:" '$0==L{f=1;next} /^  [a-z_]+:/{f=0} f&&/needs: \[build\]/{n=1} f&&/fail-on-cache-miss: true/{c=1} END{exit !(n&&c)}' .github/workflows/ci.yml || { echo "FAIL $l"; exit 1; }; done && echo FANOUT_CONSUMERS_OK</automated>
  </verify>
  <acceptance_criteria>
    - All seven lanes declare `needs: [build]` and carry two `actions/cache/restore` steps (`cache_deps` + `cache_main`) each with `fail-on-cache-miss: true` and no `restore-keys:` (proven per-lane by the awk loop emitting `FANOUT_CONSUMERS_OK`).
    - Every converted lane's `build-test`/`deps` keys are byte-identical to the producer's (same resolved-toolchain interpolation, root `mix.lock`).
    - The demo/nested/playwright caches on `verify_example`, `verify_journeys`, `verify_mailglass`, `verify_inbox`, `verify_admin` are unchanged and still hash their specific nested lockfiles.
    - `mix test test/chimeway/release_gate_contract_test.exs` green: ci-gate 14 / pr-gate 4 counts and names unchanged; `verify_gates` still runs `mix ci.verify_gates`.
    - `mix test test/chimeway/ci_observability_contract_test.exs` green: each converted lane keeps `id: cache_main` and its trailing `if: always()` obs-summary step.
  </acceptance_criteria>
  <done>All seven plain-hex default-graph lanes consume the single shared `deps` + `build-test` cache restore-only via `needs: [build]` with `fail-on-cache-miss: true`, their auxiliary demo/nested caches untouched, both contract suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Reschema the five exception lanes onto distinct self-cached roles (docs, OTP matrix, partners)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml lines 127-162 (`verify_docs` — no `env: MIX_ENV` block, so it defaults to `:dev`; root cache 136-144 keyed `-mix-verify-docs-`)
    - .github/workflows/ci.yml lines 164-221 (`test` matrix — cache 193-201, self-caching full `actions/cache`, key already OTP-parameterized off `${{ matrix.elixir }}`/`${{ matrix.otp }}`)
    - .github/workflows/ci.yml lines 513-591 (`verify_accrue` — `ACCRUE_PATH` path-dep at 534, second checkout 537-541, root cache 546-554 keyed `-mix-verify-accrue-`), plus `verify_threadline` (672-744, `THREADLINE_PATH`) and `verify_sigra` (745-835, `SIGRA_PATH`)
    - .planning/phases/88-cache-correctness-compile-once/88-CONTEXT.md (decisions D-04, D-07, D-08, D-12)
  </read_first>
  <action>
    Reschema each exception lane's ROOT cache key to the new schema but keep it a SELF-CACHING full `actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830` step (no `needs: [build]`, no `actions/cache/restore`), on a role prefix DISTINCT from the shared `build-test-` so it can never collide with or clobber the producer cache under GitHub's write-once semantics. Add `id: beam` to each lane's `setup-beam` step and narrow every root glob to root `mix.lock`. Keep `id: cache_main` on each reschemed root cache step (preserves the obs-summary + observability-contract wiring). Give each root cache a single narrow, role-scoped `restore-keys:` prefix — the full key minus its `${{ hashFiles('mix.lock') }}` segment — NOT the broad `${{ runner.os }}-mix-` prefix.

    - `verify_docs` (D-07): keep it on `MIX_ENV: dev` (do NOT add an `env: MIX_ENV: test` block; `ex_doc` is `only: :dev`, so `mix ci.docs` must run in `:dev`). Reschema its root cache key to `build-dev-${{ runner.os }}-${{ steps.beam.outputs.elixir-version }}-${{ steps.beam.outputs.otp-version }}-${{ hashFiles('mix.lock') }}` with `path: deps` + `_build`. Do NOT give it `needs: [build]`. Do NOT modify `mix.exs` or `ex_doc`'s `only:` (user-confirmed divergence).
    - `test` matrix (D-12): reschema its key to `test-matrix-${{ runner.os }}-${{ matrix.elixir }}-${{ matrix.otp }}-${{ hashFiles('mix.lock') }}` (stay OTP-parameterized off the matrix values so the OTP-26 and OTP-27 legs keep separate caches) with `path: deps` + `_build`, self-caching. The `test-matrix-` role prefix keeps the OTP-27 leg from colliding with the producer's `build-test-…` key even when its resolved versions equal `1.19`/`27`. Do NOT give it `needs: [build]` — the OTP-26 leg cannot consume an OTP-27 `build-test` cache.
    - `verify_accrue`/`verify_threadline`/`verify_sigra` (D-08): reschema each root cache key to a graph-scoped `build-test-<partner>-${{ runner.os }}-${{ steps.beam.outputs.elixir-version }}-${{ steps.beam.outputs.otp-version }}-${{ hashFiles('mix.lock') }}` (`<partner>` ∈ accrue/threadline/sigra) with `path: deps` + `_build`, self-caching. Leave their `*_PATH` env, their extra partner/demo checkouts, and their `cache_demo` steps untouched. Do NOT give them `needs: [build]` — their injected path-deps genuinely change the graph.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/release_gate_contract_test.exs test/chimeway/ci_observability_contract_test.exs && grep -q "build-dev-\${{ runner.os }}" .github/workflows/ci.yml && grep -q "test-matrix-\${{ runner.os }}" .github/workflows/ci.yml && grep -q 'build-test-accrue-' .github/workflows/ci.yml && grep -q 'build-test-threadline-' .github/workflows/ci.yml && grep -q 'build-test-sigra-' .github/workflows/ci.yml && echo EXCEPTIONS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `verify_docs` stays `:dev` and self-caches `build-dev-…`; it has no `needs: [build]` and `mix.exs` is untouched.
    - The `test` matrix self-caches `test-matrix-…` (OTP-parameterized) with no `needs: [build]`, and its role prefix differs from `build-test-` so the OTP-27 leg cannot clobber the producer cache.
    - `verify_accrue`/`verify_threadline`/`verify_sigra` self-cache graph-scoped `build-test-<partner>-…` keys, keep their `*_PATH` injection, and have no `needs: [build]`.
    - No exception lane carries a broad `${{ runner.os }}-mix-` restore-key; every reschemed root key hashes root `mix.lock`.
    - `mix test test/chimeway/release_gate_contract_test.exs test/chimeway/ci_observability_contract_test.exs` both green.
  </acceptance_criteria>
  <done>Docs (`build-dev`, `:dev`), the OTP matrix (`test-matrix`, OTP-parameterized), and the three partner lanes (`build-test-<partner>`, path-dep graphs) are reschemed onto distinct self-cached roles with root-`mix.lock` globs and no broad restore-keys — provably isolated from the shared `build-test` cache and off `needs: [build]`.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the observability contract to lock the new key schema and consumer wiring</name>
  <files>test/chimeway/ci_observability_contract_test.exs</files>
  <read_first>
    - test/chimeway/ci_observability_contract_test.exs lines 1-22 (module header, `@ci_yml`, `@build_lanes`, `setup` reading ci.yml) and lines 221-256 (the existing per-lane `id: cache_main` + obs-summary fan-out describe block and the D-14-relaxed compile-warnings block to model the new block after)
    - test/chimeway/release_gate_contract_test.exs lines 885-916 (`extract_ci_job_block/2` and the gate `needs:` extractors — the marker-extraction idioms to reuse)
    - .github/workflows/ci.yml (the converted lanes from Tasks 1-2, to assert against)
  </read_first>
  <action>
    Add a new describe block `"CACHE-01/02/04 shared cache schema (Phase 88)"` to `ci_observability_contract_test.exs`, reusing the existing `extract_ci_job_block/2` helper and `setup` fixture. Assert:
    (a) A `build` producer job block exists and contains both `deps-${{ runner.os }}-${{ steps.beam.outputs.elixir-version }}-${{ steps.beam.outputs.otp-version }}-${{ hashFiles('mix.lock') }}` and the `build-test-…` sibling key (schema + resolved-toolchain + root `mix.lock`).
    (b) For each shared-consumer lane in a new `@shared_consumer_lanes ~w(lint install_golden_contract verify_gates verify_example verify_runtime_prefix verify_journeys verify_mailglass verify_inbox verify_admin)`, the lane block contains `needs: [build]`, `actions/cache/restore`, `fail-on-cache-miss: true`, and the exact shared `build-test-…` key string.
    (c) For each exception lane in `@self_cache_lanes ~w(verify_docs test verify_accrue verify_threadline verify_sigra)`, `refute` the block contains `needs: [build]`; assert `verify_docs` contains `build-dev-`, `test` contains `test-matrix-`, and the three partner blocks contain `build-test-accrue-`/`build-test-threadline-`/`build-test-sigra-` respectively.
    (d) A whole-file `refute` that the recursive `**/mix.lock` lockfile glob no longer appears in any `hashFiles(...)` call for a default-graph root key (scope the refute so it does not trip on the intentionally-nested `chimeway_inbox/mix.lock`, `chimeway_admin/mix.lock`, or `examples/chimeway_demo_host/mix.lock` demo/nested globs — match on the literal recursive-glob `hashFiles` form only).
    Keep every existing assertion in the file green; do not touch the OBS-01/02/03/04 blocks or the D-14 compile-warnings block.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/ci_observability_contract_test.exs test/chimeway/release_gate_contract_test.exs && echo CONTRACT_EXTENDED_OK</automated>
  </verify>
  <acceptance_criteria>
    - The new describe block asserts the `build` producer key schema, the 9-lane shared-consumer wiring (`needs: [build]` + restore-only + `fail-on-cache-miss: true` + exact shared key), and the 5-lane exception isolation (no `needs: [build]`; distinct roles).
    - The whole-file refute confirms no default-graph root key uses the recursive lockfile glob, without flagging the intentionally-nested demo/nested globs.
    - `mix test test/chimeway/ci_observability_contract_test.exs` is green with all prior OBS + D-14 assertions intact.
    - `mix test test/chimeway/release_gate_contract_test.exs` is green (ci-gate 14 / pr-gate 4 unchanged).
  </acceptance_criteria>
  <done>The observability contract now machine-enforces the shared-cache schema and consumer/exception wiring introduced across Plans 01-02, so any future drift (a lane silently dropping `needs: [build]`, a role colliding with `build-test-`, or a broad glob creeping back) fails the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared `build-test` cache → 9 default-graph consumers | One compiled `_build/test` tree is reused across many lanes; a role collision or wrong key would let one lane clobber or misread another's artifacts |
| Exception lanes (partners/docs/OTP-26) → shared cache namespace | Path-dep, `:dev`, and OTP-26 graphs differ from the producer's; sharing the `build-test-` key would reuse the wrong artifacts |
| ci.yml lane set → ruleset 18486746 required-check on `main` | The 14/4 gate lane counts/names are contract; a rename or recount can strand the required check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-04 | Tampering | fan-out edits altering the gate lane set | high | mitigate | Only cache steps and `needs:` are edited; lane names and `mix verify.*` bodies are untouched; `release_gate_contract_test.exs` (ci-gate=14 / pr-gate=4) runs in every task's `<verify>`. |
| T-88-05 | Tampering | exception-lane key colliding with shared `build-test` | high | mitigate | Distinct role prefixes (`build-dev`, `test-matrix`, `build-test-<partner>`) guarantee no exception lane writes to the shared key; Task 3's contract test asserts each role and refutes `needs: [build]` on the exceptions. |
| T-88-06 | Spoofing / DoS (vacuous pass) | consumer cache restore | high | mitigate | All 7 converted consumers use `fail-on-cache-miss: true`, so a missing producer cache fails loudly instead of cold-compiling green; `aggregate-gate.sh` treats the resulting `skipped` lanes as failure. |
| T-88-07 | Tampering | broad restore-key fallback onto wrong-`MIX_ENV` tree | medium | mitigate | The broad `${{ runner.os }}-mix-` restore-key is removed from every lane; consumers use exact keys with no `restore-keys:`, and every root key hashes root `mix.lock` so unrelated nested-lock bumps no longer churn it. |
</threat_model>

<verification>
- `mix test test/chimeway/release_gate_contract_test.exs` green — ci-gate 14 / pr-gate 4 lane counts and names unchanged after the fan-out.
- `mix test test/chimeway/ci_observability_contract_test.exs` green — existing per-lane `id: cache_main` + obs-summary assertions plus the new shared-schema / `needs: [build]` / exception-isolation assertions all pass.
- Per-lane awk check emits `FANOUT_CONSUMERS_OK` (all 7 consumers carry `needs: [build]` + `fail-on-cache-miss: true`).
- Grep confirms the reschemed exception roles `build-dev-`, `test-matrix-`, `build-test-accrue-/threadline-/sigra-` are present and distinct from the shared `build-test-` role.
</verification>

<success_criteria>
The whole default graph now compiles once: the seven remaining plain-hex `:test` lanes consume the single shared `deps` + `build-test` cache restore-only via `needs: [build]` with `fail-on-cache-miss: true`, collapsing the ~11-way fragmentation onto one warm entry; the five genuinely-different lanes (docs `:dev`, the OTP-26/27 matrix, and the three path-dep partners) are reschemed onto distinct self-cached roles that cannot collide with the shared key; all root keys hash root `mix.lock` with no broad restore-key fallback; and the observability + release-gate contracts stay green and now machine-enforce the new structure. Plan 03 can prove the warm-run win by run-link delta.
</success_criteria>

<output>
Create `.planning/phases/88-cache-correctness-compile-once/88-02-SUMMARY.md` when done.
</output>
