---
phase: 105-tenant-safe-inbox-change-stream
plan: "01"
type: tracer
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/inbox/change.ex
  - lib/chimeway/inbox/change_publisher.ex
  - lib/chimeway/inbox/change_publisher/noop.ex
  - lib/chimeway/inbox.ex
  - lib/chimeway/trigger.ex
  - lib/chimeway/telemetry.ex
  - test/chimeway/inbox_change_publisher_test.exs
  - test/chimeway/inbox_state_transition_test.exs
  - test/chimeway/trigger_inbox_change_test.exs
autonomous: true
requirements: [INBX-03]
must_haves:
  truths:
    - "D-01/D-02: root Chimeway exposes a replaceable publisher receiving only a closed reload hint and defaults to a Phoenix-free no-op."
    - "D-03/D-04/D-11: one creation hint is attempted per committed notification and one lifecycle hint per first exact seen/read/archive transition; duplicates emit none."
    - "D-05: publisher errors, invalid returns, exits, throws, and exceptions cannot roll back or falsify durable trigger/lifecycle success, and diagnostics contain only a stable classification."
    - "A root consumer compiles and exercises the default publisher with no Phoenix or chimeway_inbox dependency."
  artifacts:
    - path: lib/chimeway/inbox/change.ex
      provides: closed versioned created/seen/read/archived reload-hint type
    - path: lib/chimeway/inbox/change_publisher.ex
      provides: behaviour, configured dispatch, containment, and safe telemetry
    - path: test/chimeway/inbox_change_publisher_test.exs
      provides: callback and adversarial failure contract
  key_links:
    - from: lib/chimeway/trigger.ex
      to: lib/chimeway/inbox/change_publisher.ex
      via: successful Repo.transaction normalization only
      pattern: ChangePublisher.publish
    - from: lib/chimeway/inbox.ex
      to: lib/chimeway/inbox/change_publisher.ex
      via: first-transition update count equals one
      pattern: publish.*seen|read|archived
---

<objective>
Create the Phoenix-free durable-to-change-hint seam in core Chimeway.
</objective>

<context>
@.planning/phases/105-tenant-safe-inbox-change-stream/105-CONTEXT.md
@.planning/phases/105-tenant-safe-inbox-change-stream/105-RESEARCH.md
@lib/chimeway/inbox.ex
@lib/chimeway/trigger.ex
@lib/chimeway/telemetry.ex
@test/chimeway/inbox_state_transition_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and contain the replaceable closed publisher</name>
  <files>lib/chimeway/inbox/change.ex, lib/chimeway/inbox/change_publisher.ex, lib/chimeway/inbox/change_publisher/noop.ex, lib/chimeway/telemetry.ex, test/chimeway/inbox_change_publisher_test.exs</files>
  <action>Write RED tests for a version-1 change carrying only tenant_id, opaque recipient_ref, and one of created/seen/read/archived. Implement a `@behaviour` publisher with no-op default selected from `:chimeway, :inbox_change_publisher`. Guard every callback outcome, rescue/catch exits and throws, return `:ok` to callers regardless, and emit only `[:chimeway, :inbox, :change_publish]` success/failure telemetry with allowlisted outcome classification. Reject unsafe tenant/recipient/event construction before callback. Assert root mix dependencies and all root `lib/` files remain free of Phoenix and `ChimewayInbox` references except textual optionality documentation in the behaviour if needed.</action>
  <verify><automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/inbox_change_publisher_test.exs --warnings-as-errors</automated></verify>
  <done>The configured callback receives only valid closed hints, every callback failure is contained, and the default root path has no Phoenix dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish only from committed creation and first lifecycle transitions</name>
  <files>lib/chimeway/trigger.ex, lib/chimeway/inbox.ex, test/chimeway/trigger_inbox_change_test.exs, test/chimeway/inbox_state_transition_test.exs</files>
  <action>Write RED integration tests with a recording publisher. Carry each inserted notification's tenant/opaque recipient change descriptor through the transaction result, but invoke the publisher only after `Repo.transaction/1` returns success. Do not publish on duplicate or failed trigger. Refactor archive to the same conditional first-transition update used by seen/read; after a successful first update, retain existing Signal behavior where applicable, then attempt the hint. Assert exact event order/count, cross-tenant denial, duplicate idempotency, and query-visible committed state inside the publisher. Add error/exception publisher cases proving the original successful public return and durable timestamps remain unchanged.</action>
  <verify><automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/inbox_change_publisher_test.exs test/chimeway/trigger_inbox_change_test.exs test/chimeway/inbox_state_transition_test.exs --warnings-as-errors</automated></verify>
  <done>Committed creations and first lifecycle transitions publish exactly once, while duplicate, denied, failed-transaction, and failing-publisher paths retain durable truth.</done>
</task>

</tasks>

<verification>
- Focused core tests pass with warnings as errors.
- `mix compile --warnings-as-errors` passes without Phoenix installed as a root dependency.
- Publisher-failure telemetry contains no tenant or recipient values.
</verification>

<output>Create `105-01-SUMMARY.md` when complete.</output>
