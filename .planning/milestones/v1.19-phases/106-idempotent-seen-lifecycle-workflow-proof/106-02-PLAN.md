---
phase: 106-idempotent-seen-lifecycle-workflow-proof
plan: "02"
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs
autonomous: true
requirements: [INT-04]
must_haves:
  truths:
    - "The mounted bell's first seen transition progresses an exact eligible waiting workflow once."
    - "Replay, wrong tenant, wrong recipient, and authorization drift do not progress a waiting workflow."
---

<objective>
Prove the real arrival-to-visible-seen-to-workflow path in the reference host.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Build the mounted seen workflow journey</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs</files>
  <action>Create a deterministic waiting workflow run for the seeded recipient with pending `chimeway.notification.seen`, mount the real inbox route, open the bell, drain the real signal queue, and assert one signal plus one `signal_received` transition.</action>
  <verify><automated>cd examples/chimeway_demo_host &amp;&amp; mix test test/demo_host_web/inbox_bell_proof_test.exs --include inbox --warnings-as-errors</automated></verify>
  <done>The mounted bell progresses one exact eligible waiting workflow through the real signal router.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Prove replay and scope denials</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs</files>
  <action>Add exact replay, wrong-tenant, wrong-recipient, and authorization-change controls. Assert every ineligible run remains waiting and no duplicate transition appears.</action>
  <verify><automated>mix verify.inbox</automated></verify>
  <done>Replays and scope mismatches cannot progress or duplicate workflow state.</done>
</task>
</tasks>

<output>Create `106-02-SUMMARY.md` when complete.</output>
