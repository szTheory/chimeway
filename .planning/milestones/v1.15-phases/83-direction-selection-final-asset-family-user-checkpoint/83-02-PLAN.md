---
phase: 83-direction-selection-final-asset-family-user-checkpoint
plan: 02
type: execute
wave: 2
depends_on: [83-01]
files_modified:
  - brandbook/assets/logo/chimeway-logotype.svg
  - brandbook/assets/logo/chimeway-logotype-mono.svg
  - brandbook/assets/logo/chimeway-logotype-inverse.svg
  - brandbook/assets/logo/chimeway-logotype-stacked.svg
  - brandbook/assets/logo/chimeway-mark.svg
  - brandbook/assets/logo/chimeway-mark-mono.svg
autonomous: true
requirements: [LOGO-03, LOGO-04]
tags: [brand, logo, svg, wordmark, fonttools, ofl]

must_haves:
  truths:
    - "brandbook/assets/logo/ contains the six lockup-family SVGs: primary logotype, mono, inverse, stacked, icon mark, icon mono."
    - "The wordmark geometry is re-cut from an OFL/libre humanist face (NOT Optima) via fontTools."
    - "The keystone-`i` is two overlaid wedges: --cw-ink body + --cw-teal facet at ~65% width."
    - "The inverse mark ships on a transparent background with no baked night-color rect."
    - "Every logo SVG is well-formed, uses only allowed token hex (or currentColor), and carries a viewBox."
  artifacts:
    - brandbook/assets/logo/chimeway-logotype.svg
    - brandbook/assets/logo/chimeway-logotype-mono.svg
    - brandbook/assets/logo/chimeway-logotype-inverse.svg
    - brandbook/assets/logo/chimeway-logotype-stacked.svg
    - brandbook/assets/logo/chimeway-mark.svg
    - brandbook/assets/logo/chimeway-mark-mono.svg
  key_links:
    - "chimeway-logotype.svg is a filename contract consumed by Phase 85 README wiring — must be stable."
    - "mono variants use currentColor so one file recolors to any context (RESEARCH Pattern 2)."
    - "The OFL face + license chosen here is recorded in notes/decision-log.md in Plan 03 — pass the face name + license identifier forward in this plan's SUMMARY."
---

<objective>
Promote the ratified Keystone finalist into the six-mark logo lockup family under `brandbook/assets/logo/`, as optimized SVGs. This is the heart of LOGO-03.

LOCKED DECISION (this session): the `chimeway` wordmark is RE-CUT in an openly-licensed (OFL/libre) humanist face — NOT Optima. chimeway is a public OSS repo; committing macOS-bundled Optima glyph outlines may exceed its license (RESEARCH Pitfall 5 / Q1). Regenerate the wordmark `<path>` geometry from a vetted OFL humanist face with Optima-like flared/humanist character via fontTools (the Phase 82 method). The keystone-`i` construction, token-hex color law, viewBox/role/aria-label structure, and mono/inverse expression all carry over from the Phase 82 finalist; only the glyph geometry changes.

Purpose: These are the marks Phases 84 (HTML book) and 85 (README) consume; their filenames are a contract. Every file must pass the Wave-0 `--assets` gate.

Output: six optimized SVGs in `brandbook/assets/logo/`.
</objective>

<artifacts>
## Artifacts this plan produces
- `brandbook/assets/logo/chimeway-logotype.svg` — PRIMARY horizontal lockup = wordmark with the two-tone keystone-`i` (primary and wordmark coincide; the integrated typemark collapses the icon-left lockup — RESEARCH Q2/A3).
- `brandbook/assets/logo/chimeway-logotype-mono.svg` — single-color via currentColor (drops the teal).
- `brandbook/assets/logo/chimeway-logotype-inverse.svg` — paper body + teal facet, TRANSPARENT background.
- `brandbook/assets/logo/chimeway-logotype-stacked.svg` — icon keystone above the wordmark, square-ish viewBox, no cage.
- `brandbook/assets/logo/chimeway-mark.svg` — icon-only two-tone keystone (viewBox 0 0 24 24).
- `brandbook/assets/logo/chimeway-mark-mono.svg` — icon in currentColor.
</artifacts>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-direction-selection-final-asset-family-user-checkpoint/83-RESEARCH.md
@.planning/phases/83-direction-selection-final-asset-family-user-checkpoint/83-PATTERNS.md
@notes/logo-options.md
@brandbook/tokens/tokens.css
@svgo.config.mjs
@scripts/logo-guards.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select + vet an OFL humanist face, re-cut the wordmark, author the primary logotype</name>
  <files>brandbook/assets/logo/chimeway-logotype.svg</files>
  <read_first>
    - notes/logo-options.md line 41 — the Phase 82 primary logotype (two-tone Optima-outlined wordmark, viewBox "30 10 172 46"); lines 24-54 describe the keystone-`i` construction and color law.
    - 83-PATTERNS.md section "chimeway-logotype.svg" — the two mandatory transforms (OFL re-cut + SVGO precision-2) and the keystone-`i` two-path structure.
    - 83-RESEARCH.md Pitfall 5 / Open Question 1 (Optima licensing) and Standard Stack (fontTools 4.62.1, SVGO safe config).
    - brandbook/tokens/tokens.css lines 12-21 — the --cw-* primitive hexes (ink #102027, paper #fffdf8, teal #0e7c86, night = proof backdrop only).
  </read_first>
  <action>
    Select and vet an OFL/libre humanist face with Optima-like flared/humanist character (elegant tapered strokes that rhyme with the keystone wedge). Confirm the face's license explicitly permits redistributing outlined glyph paths in a public repo (OFL and most libre licenses do). Record the chosen face name and its license identifier for this plan's SUMMARY (Plan 03 writes it into notes/decision-log.md).

    Using fontTools, convert the letters of `chimeway` from the chosen face to font-independent SVG `<path>` outlines (the Phase 82 method). Assemble chimeway-logotype.svg: the wordmark body in --cw-ink (#102027), with the `i` replaced by the keystone-`i` — two overlaid wedge paths re-fitted to the new face's `i` slot: an ink body path plus a --cw-teal (#0e7c86) facet path at roughly 65% width. Keep viewBox, role="img", aria-label="chimeway". Literal token hex only (never CSS custom-property references — they do not resolve in a standalone SVG). Transparent background — no background rect. Then optimize with the Wave-0 config: npx -y svgo@4.0.2 --config svgo.config.mjs -p 2 --multipass (precision-2 is the single biggest byte win; the raw Phase 82 SVG carried 15-digit floats).

    An optional render-and-compare-to-Optima check is welcome, but the OFL face is the shipped source of truth.
  </action>
  <verify>
    <automated>xmllint --noout brandbook/assets/logo/chimeway-logotype.svg && grep -q 'viewBox' brandbook/assets/logo/chimeway-logotype.svg && grep -q 'aria-label' brandbook/assets/logo/chimeway-logotype.svg && ! grep -qi 'var(' brandbook/assets/logo/chimeway-logotype.svg && for h in $(grep -oiE '#[0-9a-f]{6}' brandbook/assets/logo/chimeway-logotype.svg | tr 'A-F' 'a-f' | sed 's/#//' | sort -u); do case " 102027 07131a fffdf8 0e7c86 d6a84f 9adbcf " in *" $h "*) : ;; *) echo "STRAY HEX $h"; exit 1;; esac; done</automated>
  </verify>
  <acceptance_criteria>
    - chimeway-logotype.svg exists, is well-formed, carries viewBox + role + aria-label.
    - Geometry is re-cut from a vetted OFL/libre humanist face (Optima retired); the face name + license are captured for the SUMMARY.
    - The keystone-`i` is two overlaid wedges (ink body + teal facet ~65% width); background is transparent (no rect).
    - Only allowed token hex appears; no CSS custom-property references; the file is SVGO-optimized (precision-2, no editor cruft / long floats).
  </acceptance_criteria>
  <done>chimeway-logotype.svg exists as an OFL-recut, SVGO-optimized two-tone keystone-i wordmark on a transparent background, passing xmllint + token-hex + viewBox checks.</done>
</task>

<task type="auto">
  <name>Task 2: Derive the remaining five family SVGs (mono, inverse, stacked, mark, mark-mono)</name>
  <files>brandbook/assets/logo/chimeway-logotype-mono.svg, brandbook/assets/logo/chimeway-logotype-inverse.svg, brandbook/assets/logo/chimeway-logotype-stacked.svg, brandbook/assets/logo/chimeway-mark.svg, brandbook/assets/logo/chimeway-mark-mono.svg</files>
  <read_first>
    - brandbook/assets/logo/chimeway-logotype.svg (from Task 1) — the recut geometry these derive from.
    - notes/logo-options.md line 45 (inverse — copy geometry, DELETE the baked backdrop rect) and line 49 (the 24x24 two-tone keystone mark).
    - 83-PATTERNS.md sections for chimeway-logotype-mono/-inverse/-stacked, chimeway-mark, chimeway-mark-mono, plus RESEARCH Pattern 2 (mono via currentColor; inverse transparent) and Pitfall 4 (no baked backdrop).
  </read_first>
  <action>
    Author all five from the recut geometry, then optimize each with npx -y svgo@4.0.2 --config svgo.config.mjs -p 2 --multipass:
      - chimeway-logotype-mono.svg: collapse the wordmark body and the keystone facet to a single fill of currentColor (drops the teal); consumer supplies the color with one CSS rule. No background.
      - chimeway-logotype-inverse.svg: same recut geometry recolored ink to --cw-paper (#fffdf8), keeping the --cw-teal facet. Ship on a TRANSPARENT background — do NOT bake the Phase 82 proof-strip backdrop rect (the night primitive that the L45 analog opens with). The --assets gate fails if that night rect reappears in this file.
      - chimeway-logotype-stacked.svg: compose the 24x24 icon keystone ABOVE the recut wordmark in one square-ish viewBox; two-tone (#102027 + #0e7c86); transparent; no enclosing rect/cage (LOGO-05). Keep role="img" + aria-label="chimeway".
      - chimeway-mark.svg: the standalone two-tone keystone from notes/logo-options.md line 49 (viewBox 0 0 24 24), ink body + teal facet at the right ~65%.
      - chimeway-mark-mono.svg: one path, fill currentColor (drops the teal).
    Literal token hex only (or currentColor); no CSS custom-property references; keep viewBox/role/aria-label on each.
  </action>
  <verify>
    <automated>for f in brandbook/assets/logo/chimeway-logotype-mono.svg brandbook/assets/logo/chimeway-logotype-inverse.svg brandbook/assets/logo/chimeway-logotype-stacked.svg brandbook/assets/logo/chimeway-mark.svg brandbook/assets/logo/chimeway-mark-mono.svg; do xmllint --noout "$f" || exit 1; grep -q 'viewBox' "$f" || { echo "no viewBox $f"; exit 1; }; grep -qi 'var(' "$f" && { echo "custom-prop $f"; exit 1; }; done; grep -qE '<rect[^>]*07131a' brandbook/assets/logo/chimeway-logotype-inverse.svg && { echo "baked backdrop in inverse"; exit 1; }; grep -q 'currentColor' brandbook/assets/logo/chimeway-logotype-mono.svg && grep -q 'currentColor' brandbook/assets/logo/chimeway-mark-mono.svg</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist, are well-formed, carry viewBox, use only allowed token hex or currentColor, and reference no CSS custom properties.
    - The inverse file has a transparent background (no night-hex rect).
    - The two mono files use currentColor (no baked teal).
    - After this task, `bash scripts/logo-guards.sh --assets` passes every logo-family check (presence/hygiene/token/viewBox/inverse-backdrop for the six logo SVGs); favicon.svg + rasters + og remain absent (green in Plan 03).
  </acceptance_criteria>
  <done>The five derived SVGs exist and pass xmllint + token/currentColor + viewBox checks; inverse is transparent (no night-hex rect); mono variants use currentColor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored SVG → committed repo file | Hand-authored + SVGO-optimized marks enter the repo and are later rendered by a browser (GitHub/HexDocs/brandbook). |
| font source → outlined paths | Glyph outlines are redistributed; the license boundary is the redistribution-permission question resolved by the OFL re-cut. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-01 | Tampering / Info-disclosure | the six committed logo SVGs | medium | mitigate | `scripts/logo-guards.sh --assets` hygiene greps enforce presentation-only content (no active or remote elements) on every mark. |
| T-83-02 | Tampering | SVGO optimization step | low | mitigate | Optimize only with the pinned `svgo@4.0.2` + Wave-0 config; xmllint + token-hex gate over output. |
| T-83-LIC | (Legal / redistribution) | wordmark glyph outlines | medium | mitigate | Re-cut the wordmark in a vetted OFL/libre humanist face; retire Optima; record face + license in the decision log (Plan 03). |
</threat_model>

<verification>
- `bash scripts/logo-guards.sh --assets` passes all logo-family checks (six logo SVGs present, well-formed, token-hex only, viewBox present, inverse has no baked backdrop).
- Per-file xmllint + token-hex + viewBox + currentColor checks green.
</verification>

<success_criteria>
The six-mark lockup family exists as optimized SVGs, re-cut from a licensed OFL humanist face, passing the Wave-0 `--assets` gate for all logo-family checks. Perceptual legibility (16px/mono/inverse) is confirmed at the Plan 03 human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/83-direction-selection-final-asset-family-user-checkpoint/83-02-SUMMARY.md` when done. Record the chosen OFL face name + license identifier so Plan 03 can enter it in notes/decision-log.md.
</output>
