---
phase: 81-design-tokens-reconciliation-documentation
plan: 03
type: execute
wave: 2
depends_on: [81-01]
files_modified:
  - brandbook/tokens/tokens.json
autonomous: true
requirements: [TOKEN-02, TOKEN-03]
nyquist_compliant: true

must_haves:
  truths:
    - "brandbook/tokens/tokens.json parses as valid JSON — TOKEN-02"
    - "Every DTCG alias ref {a.b.c} resolves to a real token path — TOKEN-02/D-10"
    - "Every dimension and duration $value is a {value,unit} object, never a bare string — TOKEN-02/DTCG 2025.10"
    - "Semantic tokens use alias refs into color.primitive.*; light/dark are two sibling groups color.semantic.light.* / color.semantic.dark.* with no system node — D-11"
    - "--cw-info maps to {color.primitive.blue} (alias, not a hex literal) — D-06"
    - "All 15 color.primitive.* $value hexes are byte-equal (lowercase) to the shipped --cw-* primitives in chimeway_admin/priv/static/chimeway_admin.css — TOKEN-02/D-03"
    - "Both admin CSS files (chimeway_admin/priv/static/chimeway_admin.css + chimeway_admin/assets/css/chimeway_admin.css wrapper) show zero changes — TOKEN-04"
  artifacts:
    - brandbook/tokens/tokens.json
  key_links:
    - "tokens.json is the hand-synced DTCG mirror of tokens.css (81-01); every CSS token maps to a JSON token, kept diff-legible by alias refs (no build tool, D-09)"
---

<objective>
Author `brandbook/tokens/tokens.json` — the hand-authored DTCG (Design Tokens Format Module 2025.10) mirror of `brandbook/tokens/tokens.css` (81-01). Nested groups, `$type`/`$value`/`$description`, alias references into `color.primitive.*`, and two sibling `color.semantic.light.*` / `color.semantic.dark.*` groups. No build tool (D-09).

Purpose: Satisfy TOKEN-02 — a tool-portable, spec-valid DTCG mirror kept in sync with the CSS by hand via alias refs.
Output: `brandbook/tokens/tokens.json`.
</objective>

## Artifacts this phase produces

- `brandbook/tokens/tokens.json` — hand-authored DTCG (D-09/D-10/D-11): nested groups (`color.primitive.*`, `color.semantic.light.*`, `color.semantic.dark.*`, `space.*`, `type.*`, `radius.*`, `border.*`, `shadow.*`, `focus.*`, `z.*`, `motion.*`); alias refs `{color.primitive.teal}`; dimension/duration as `{value,unit}` objects; `--cw-info` → `{color.primitive.blue}`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-design-tokens-reconciliation-documentation/81-CONTEXT.md
@.planning/phases/81-design-tokens-reconciliation-documentation/81-RESEARCH.md
@.planning/phases/81-design-tokens-reconciliation-documentation/81-PATTERNS.md

# The CSS mirror source authored in 81-01 (read for the exact value set to reproduce)
@brandbook/tokens/tokens.css
# Authoritative shipped values (fallback for any value not yet in tokens.css)
@chimeway_admin/priv/static/chimeway_admin.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the primitive + non-color scalar DTCG groups</name>
  <files>brandbook/tokens/tokens.json</files>
  <read_first>
    - brandbook/tokens/tokens.css (81-01 output — the value set to mirror; primitives + non-color scalars)
    - 81-RESEARCH.md § "DTCG Hand-Authoring Reference (verified against 2025.10)" (the $type/$value shape table, incl. dimension/duration object form, fontFamily array, inheritance)
    - 81-PATTERNS.md § "brandbook/tokens/tokens.json" (2-space JSON house style; dimension/duration object gotcha; $type inheritance per group)
  </read_first>
  <action>
    Create `brandbook/tokens/tokens.json` — a single JSON object, 2-space indent, double-quoted keys, no trailing commas (house style from package.json/.mcp.json). Set `$type` once on each parent group (inheritance, RESEARCH § DTCG Inheritance). Author these groups mirroring the tokens.css scalar tier:

    - `color.primitive` (`$type: color`): the 15 primitives as legacy hex-string `$value` copied verbatim lowercase from tokens.css (ink..code). Add `color.primitive.info` with `$value: "{color.primitive.blue}"` (alias, D-06 — never a hex literal).
    - `space` (`$type: dimension`): xs..3xl as `{ "value": N, "unit": "px" }` objects (4/8/16/24/32/48/64).
    - `type.family` (`$type: fontFamily`): `sans` and `mono` as ARRAYS of the font stack strings from tokens.css (RESEARCH: fontFamily authored as array).
    - `type.size` (`$type: dimension`): label/body/heading/display as `{value,unit:"px"}` (14/16/20/28).
    - `type.lineHeight` (`$type: number`): label/body/heading/display as JSON numbers (1.35/1.5/1.2/1.15).
    - `type.weight` (`$type: fontWeight`): regular 400, semibold 600 (numbers).
    - `radius` (`$type: dimension`): sm `{value:5,unit:"px"}` (verbatim, DIV-1 DEFERRED — NOT 4px), md `{value:8,unit:"px"}`, pill `{value:999,unit:"px"}`.
    - `border` (`$type: dimension`): `width` = `{ "value": 1, "unit": "px" }` (net-new, DIV-7 DOCUMENTED). Border color aliases live in the semantic tier (Task 2).
    - `shadow.panel` (`$type: shadow`): the light `--cw-shadow-panel` decomposed to a `{ color, offsetX, offsetY, blur, spread }` object (0/16px/42px, color rgb(16 32 39 / 0.08)); dark elevation is a CSS theming concern, not a JSON sibling (D-11 splits only color.semantic). Note this in `$description`.
    - `focus` : `ring` `$type: color` `$value: "{color.primitive.blue}"` (mirrors --cw-focus-ring: var(--cw-blue)); `offset` `$type: dimension` `{value:3,unit:"px"}`.
    - `z` (`$type: number`): sidebar 10, focus 50 (DIV-6 DOCUMENTED, verbatim).
    - `motion` (`$type: duration`): fast `{value:120,unit:"ms"}`, pressed `{value:80,unit:"ms"}`, each with a `$description` noting the CSS shorthand keeps the `ease-out` easing (DIV-5 DOCUMENTED; emit duration, note easing in $description rather than hard-coding a cubicBezier — RESEARCH Open Q2).
  </action>
  <acceptance_criteria>
    - Well-formed JSON: `node -e "JSON.parse(require('fs').readFileSync('brandbook/tokens/tokens.json','utf8'))"` (exit 0)
    - No bare-string dimension/duration values (DTCG 2025.10 shape) — every dimension/duration `$value` is a `{value,unit}` object:
      ```
      node -e '
      const t=JSON.parse(require("fs").readFileSync("brandbook/tokens/tokens.json","utf8"));
      let bad=[];
      (function walk(n,path,type){ if(n&&typeof n==="object"){ const ty=n.$type||type;
        if("$value" in n){ const v=n.$value;
          if((ty==="dimension"||ty==="duration")&&!(v&&typeof v==="object"&&"value" in v&&"unit" in v)) bad.push(ty+" bare @ "+path); }
        for(const k in n) if(!k.startsWith("$")) walk(n[k],path+"."+k,ty); } })(t,"",undefined);
      if(bad.length){console.error(bad.join("\n"));process.exit(1)}'
      ```
      (exit 0, no output)
    - `--cw-info` alias, no hex: `node -e "const t=require('./brandbook/tokens/tokens.json'); if(t.color.primitive.info.\$value!=='{color.primitive.blue}')process.exit(1)"`
    - radius-sm verbatim 5px: the `radius.sm.$value` equals `{value:5,unit:"px"}` (DIV-1)
    - 15-primitive hex-equality vs the shipped CSS SSOT — for each `color.primitive.<name>.$value`, lowercase and byte-compare to `--cw-<name>` in `chimeway_admin/priv/static/chimeway_admin.css`; all 15 must be equal (mirrors 81-01's CSS-side hex loop, closing the JSON-side asymmetry):
      ```
      node -e '
      const fs=require("fs");
      const t=JSON.parse(fs.readFileSync("brandbook/tokens/tokens.json","utf8"));
      const c=fs.readFileSync("chimeway_admin/priv/static/chimeway_admin.css","utf8");
      const N=["ink","night","paper","porcelain","line","muted","teal","blue","brass","mint","violet","success","warning","danger","code"];
      let bad=[];
      for(const n of N){ const j=(((t.color||{}).primitive||{})[n]||{}).$value; const jv=(typeof j==="string"?j:"").toLowerCase();
        const m=c.match(new RegExp("--cw-"+n+": *(#[0-9a-fA-F]+)")); const hv=m?m[1].toLowerCase():null;
        if(!jv||jv!==hv) bad.push(n+": "+jv+" != "+hv); }
      if(bad.length){console.error(bad.join("\n"));process.exit(1)}'
      ```
      (exit 0, no output)
    - Zero-drift on both admin CSS files: `git diff --exit-code chimeway_admin/priv/static/chimeway_admin.css chimeway_admin/assets/css/chimeway_admin.css` (exit 0)
  </acceptance_criteria>
  <verify>
    <automated>node -e 'const fs=require("fs");const t=JSON.parse(fs.readFileSync("brandbook/tokens/tokens.json","utf8"));const c=fs.readFileSync("chimeway_admin/priv/static/chimeway_admin.css","utf8");const N=["ink","night","paper","porcelain","line","muted","teal","blue","brass","mint","violet","success","warning","danger","code"];let bad=[];for(const n of N){const j=(((t.color||{}).primitive||{})[n]||{}).$value;const jv=(typeof j==="string"?j:"").toLowerCase();const m=c.match(new RegExp("--cw-"+n+": *(#[0-9a-fA-F]+)"));const hv=m?m[1].toLowerCase():null;if(!jv||jv!==hv)bad.push(n);}if(bad.length){console.error(bad.join("\n"));process.exit(1)}' && git diff --exit-code chimeway_admin/priv/static/chimeway_admin.css chimeway_admin/assets/css/chimeway_admin.css</automated>
  </verify>
  <done>tokens.json defines the primitive color group (incl. --cw-info alias) and all non-color scalar groups (space/type/radius/border-width/shadow/focus/z/motion) in spec-valid DTCG 2025.10 shape with {value,unit} dimension/duration objects and per-group $type inheritance; all 15 primitive $value hexes are byte-equal to the shipped CSS; JSON parses; both admin CSS files unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Author the semantic light/dark sibling groups with alias refs</name>
  <files>brandbook/tokens/tokens.json</files>
  <read_first>
    - brandbook/tokens/tokens.css (81-01 output — the light + dark semantic values to mirror, incl. status triads, generalized aliases, control/surface layer)
    - 81-RESEARCH.md § "DTCG Hand-Authoring Reference" (alias syntax `{color.primitive.teal}`; D-11 light/dark sibling-group shape)
    - 81-RESEARCH.md § "Exact Shipped-Token Inventory" tables 3 & 5 (light and dark semantic values with hex)
  </read_first>
  <action>
    Add two sibling semantic groups `color.semantic.light` and `color.semantic.dark` (`$type: color`), each a plain token group aliasing the single primitive tier where a primitive exists, else holding the unique hex (D-11). "system" gets NO JSON node — it is a CSS-only strategy.

    For each theme group, mirror the tokens.css semantic set:
    - The 7 generalized names: `surface-bg`, `surface-panel`, `fg`, `fg-muted`, `border`, `accent`, `focus`. Where the tokens.css value is `var(--cw-<primitive>)`, author the JSON as an alias ref `{color.primitive.<primitive>}` (e.g. light `surface-bg` → `{color.primitive.paper}`, `fg` → `{color.primitive.ink}`, `accent` → `{color.primitive.teal}`; dark `surface-bg` → `{color.primitive.night}`, `accent` → `{color.primitive.mint}`, `focus` → `{color.primitive.brass}`). Where the value is a unique hex with no primitive (e.g. light `surface-panel` #ffffff, dark `surface-panel` #10232c, `fg-muted` #b8c5c9, `border` #29414a), hold the hex string directly — do NOT invent a primitive and do NOT duplicate an existing primitive's hex (D-10, RESEARCH Pitfall 4).
    - The five status triads (`status.success/warning/danger/info/neutral`, each `text/surface/border`) with the light values in `color.semantic.light` and the dark values in `color.semantic.dark`, copied from tokens.css. The info triad stays teal-hued verbatim (DIV-3).
    - The control/surface + button/row/link layer (`surface-hover/active`, `control-*`, `border-strong`, `focus-halo`, `button-primary/danger-bg/fg`, `table-row-hover`, `row-bg/hover`, `link-fg`) with per-theme values from tokens.css.

    Every value that equals a primitive hex MUST be an alias ref, never the raw hex (D-10). Keep 2-space indent; no trailing commas.
  </action>
  <acceptance_criteria>
    - Well-formed JSON: `node -e "JSON.parse(require('fs').readFileSync('brandbook/tokens/tokens.json','utf8'))"` (exit 0)
    - Every alias ref resolves to a real token path AND no bare-string dimension/duration (combined DTCG walk):
      ```
      node -e '
      const t=JSON.parse(require("fs").readFileSync("brandbook/tokens/tokens.json","utf8"));
      const get=(o,p)=>p.split(".").reduce((a,k)=>a&&a[k],o);
      let bad=[];
      (function walk(n,path,type){ if(n&&typeof n==="object"){ const ty=n.$type||type;
        if("$value" in n){ const v=n.$value;
          if(typeof v==="string"&&/^\{[^}]+\}$/.test(v)){ const ref=v.slice(1,-1); const tgt=get(t,ref);
            if(!tgt||!(tgt&&typeof tgt==="object"&&"$value" in tgt)) bad.push("unresolved {"+ref+"} @ "+path); }
          if((ty==="dimension"||ty==="duration")&&!(v&&typeof v==="object"&&"value" in v&&"unit" in v)) bad.push(ty+" bare @ "+path); }
        for(const k in n) if(!k.startsWith("$")) walk(n[k],path+"."+k,ty); } })(t,"",undefined);
      if(bad.length){console.error(bad.join("\n"));process.exit(1)}'
      ```
      (exit 0, no output)
    - Two sibling groups present, no system node: `node -e "const t=require('./brandbook/tokens/tokens.json'); const s=t.color.semantic; if(!s.light||!s.dark||s.system)process.exit(1)"`
    - Semantic tokens alias the primitive tier (spot-check): light `accent` resolves to `{color.primitive.teal}` and dark `accent` to `{color.primitive.mint}` — `node -e "const t=require('./brandbook/tokens/tokens.json'); const l=t.color.semantic.light, d=t.color.semantic.dark; if(JSON.stringify(l).indexOf('{color.primitive.teal}')<0||JSON.stringify(d).indexOf('{color.primitive.mint}')<0)process.exit(1)"`
    - Zero-drift on both admin CSS files: `git diff --exit-code chimeway_admin/priv/static/chimeway_admin.css chimeway_admin/assets/css/chimeway_admin.css` (exit 0)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const t=require('./brandbook/tokens/tokens.json'); const s=t.color.semantic; if(!s.light||!s.dark||s.system)process.exit(1)" && git diff --exit-code chimeway_admin/priv/static/chimeway_admin.css chimeway_admin/assets/css/chimeway_admin.css</automated>
  </verify>
  <done>tokens.json defines color.semantic.light and color.semantic.dark sibling groups (no system node) covering the 7 generalized names, five status triads, and the control/surface/button/row/link layer, using alias refs into color.primitive.* wherever a primitive exists and unique hex otherwise; all alias refs resolve; JSON parses; both admin CSS files unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan authors an inert static `.json` asset with no runtime, no input handling, no auth, no crypto, no network, and no CI changes. The JSON is authored, not received (81-RESEARCH.md § Security Domain — V5 Input Validation N/A).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-03 | N/A | Static `tokens.json` (authored DTCG data, no runtime surface) | low | accept | No STRIDE category applies to an inert JSON token file; no ASVS L1 control is relevant. Assessment per 81-RESEARCH.md § Security Domain. |
| T-81-SC | Tampering | package installs (npm/pip/cargo) | low | accept | No packages installed or permitted this phase; the `node` used for validation is the ambient runtime, not an install. Supply-chain threat surface is empty. |

No `high`+ threats — nothing to block on.
</threat_model>

<verification>
Run the 81-VALIDATION.md rows for TOKEN-02 and TOKEN-03 (json side):
- `node -e JSON.parse(...)` parses (exit 0)
- alias-resolution walk: zero unresolved `{a.b.c}` refs
- DTCG shape lint: zero bare-string dimension/duration values
- `--cw-info` = `{color.primitive.blue}` (no hex literal)
- 15-primitive hex-equality: every `color.primitive.<name>.$value` byte-equal (lowercase) to `--cw-<name>` in the shipped CSS
- Hard gate: `git diff --exit-code chimeway_admin/priv/static/chimeway_admin.css chimeway_admin/assets/css/chimeway_admin.css` (exit 0)
</verification>

<success_criteria>
- brandbook/tokens/tokens.json mirrors the token set in hand-authored DTCG (`$value`/`$type`/`$description`), no build tool, covering color/type/spacing/radius/border/shadow/motion/focus-ring/z-index, two-tier only (TOKEN-02, TOKEN-03)
- Light/dark = two sibling semantic groups; system has no JSON node (D-11)
- All alias refs resolve; all dimension/duration are {value,unit} objects
- All 15 color.primitive.* hexes byte-equal to the shipped CSS primitives (JSON-side hex-equality guard)
- Both admin CSS files (priv/static + assets/css wrapper) show zero changes (TOKEN-04 support)
</success_criteria>

<output>
Create `.planning/phases/81-design-tokens-reconciliation-documentation/81-03-SUMMARY.md` when done
</output>
