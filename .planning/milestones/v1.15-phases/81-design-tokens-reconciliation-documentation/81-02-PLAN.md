---
phase: 81-design-tokens-reconciliation-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - notes/decision-log.md
autonomous: true
requirements: [TOKEN-04]
nyquist_compliant: true

must_haves:
  truths:
    - "notes/decision-log.md records DIV-1 through DIV-7, each dispositioned DOCUMENTED or DEFERRED — TOKEN-04/D-12"
    - "Each divergence entry carries both-side line refs (shipped chimeway_admin.css + brand-book intent) and closes with the git-diff zero-drift invariant naming both admin CSS files"
    - "Both admin CSS files (chimeway_admin/priv/static/chimeway_admin.css + chimeway_admin/assets/css/chimeway_admin.css wrapper) show zero changes (git diff --exit-code exit 0) — TOKEN-04"
  artifacts:
    - notes/decision-log.md
  key_links:
    - "decision-log.md is the provenance record ADMIN-RETHEME-01 (future milestone) consumes to resolve the DEFERRED sub-primitive conflicts live"
---

<objective>
Author `notes/decision-log.md` — the divergence ledger recording every named sub-primitive divergence between the shipped `chimeway_admin.css` token layer and the brand-book intent (DIV-1..DIV-7), each dispositioned DOCUMENTED or DEFERRED, each closing with the `git diff --exit-code` zero-drift invariant (naming both admin CSS files — `chimeway_admin/priv/static/chimeway_admin.css` + the `chimeway_admin/assets/css/chimeway_admin.css` wrapper) that proves the shipped CSS was never patched.

Purpose: Satisfy TOKEN-04 — divergences are RECORDED, not resolved this phase. The live re-theme is the separate ADMIN-RETHEME-01 milestone.
Output: `notes/decision-log.md` (greenfield — creates the `notes/` tree via the first Write).
</objective>

## Artifacts this phase produces

- `notes/decision-log.md` — DIV-1..DIV-7 divergence ledger (D-12), house decision-doc format (header block + `## Sources` + ID/disposition table + per-DIV entries + `## Validation Commands` / `## Scope Guard`), each entry closing with the zero-drift invariant.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-design-tokens-reconciliation-documentation/81-CONTEXT.md
@.planning/phases/81-design-tokens-reconciliation-documentation/81-RESEARCH.md
@.planning/phases/81-design-tokens-reconciliation-documentation/81-PATTERNS.md

# Divergence source refs (read for exact both-side line numbers)
@chimeway_admin/priv/static/chimeway_admin.css
# House decision-doc analog to mirror for format
@.planning/phases/77-truth-baseline-and-package-model-decision/77-PACKAGE-MODEL-DECISION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the DIV-1..DIV-7 divergence ledger</name>
  <files>notes/decision-log.md</files>
  <read_first>
    - 81-RESEARCH.md § "Divergence Ledger Inputs" (the DIV-1..DIV-7 table with both-side line refs and dispositions — the authoritative source content)
    - 81-PATTERNS.md § "notes/decision-log.md" (house format: header block, `## Sources`, ID/disposition table, minimal per-DIV entry format, `## Validation Commands` + `## Scope Guard`)
    - .planning/phases/77-truth-baseline-and-package-model-decision/77-PACKAGE-MODEL-DECISION.md (primary house decision-record analog)
  </read_first>
  <action>
    Create `notes/decision-log.md` (this first Write establishes the greenfield `notes/` directory). Follow the house decision-doc format from the PATTERNS analog:

    1. Header block: `# Chimeway Design-Token Divergence Log`, then `**Date:** 2026-07-09`, `**Decision status:** reconciliation phase — documented/deferred, not patched`, `**Scope:** sub-primitive divergences from shipped chimeway_admin/priv/static/chimeway_admin.css`.
    2. `## Sources` — bullet list, one line per cited file with its role: `chimeway_admin/priv/static/chimeway_admin.css` (shipped SSOT), `prompts/chimeway-brand-book.md` (brand intent), `.planning/phases/81-…/81-CONTEXT.md` (locked decisions D-01..D-12), `.planning/phases/81-…/81-RESEARCH.md` (divergence ledger inputs), `.planning/REQUIREMENTS.md` (TOKEN-01..05).
    3. `## Divergence Summary` table with columns `| # | Divergence | Shipped side | Brand-book / intent side | Disposition |`, seeded directly from the RESEARCH DIV-1..DIV-7 table (carry the both-side line refs).
    4. One `### DIV-N: <title> — DOCUMENTED | DEFERRED` subsection per divergence, each with `**Shipped side:**` (value + `chimeway_admin.css:<line>`), `**Brand-book / intent side:**` (value + `chimeway-brand-book.md:<line>`), `**Disposition:**` line, and a closing `**Zero-drift invariant:** \`git diff --exit-code chimeway_admin/priv/static/chimeway_admin.css chimeway_admin/assets/css/chimeway_admin.css\` → exit 0` (both admin CSS files: the shipped SSOT and its 3-line `@import` wrapper). The seven entries and dispositions (from RESEARCH):
       - DIV-1 radius-sm 5px (`chimeway_admin.css:40`) vs 4px brand intent → DEFERRED (keep shipped 5px; patch = ADMIN-RETHEME-01).
       - DIV-2 missing --cw-info primitive vs brand book info→blue (`chimeway-brand-book.md:612`) → DOCUMENTED (added as `--cw-info: var(--cw-blue)` alias, D-06; no new hex).
       - DIV-3 teal-hued --cw-status-info-* triad (`chimeway_admin.css:60-62`) vs blue brand intent (`:612`) → DEFERRED (copied teal verbatim; blue-info reconciliation = ADMIN-RETHEME-01).
       - DIV-4 status-pill mapping conflicts (brand book Sending→violet `:674`, Cancelled→muted/neutral outline `:678`, Expired→warning outline `:679`) → DOCUMENTED/DEFERRED (no sending/cancelled/expired triads exist in shipped; do NOT invent them; log intended mapping for STATE/component phases).
       - DIV-5 net-new motion representation: CSS keeps `120ms ease-out`/`80ms ease-out` shorthand verbatim (`:48-49`), JSON decomposes to a `duration` token + easing noted in `$description` → DOCUMENTED.
       - DIV-6 net-new z-index DTCG tokens (--cw-z-sidebar 10 / --cw-z-focus 50, `:46-47`) first expressed in DTCG → DOCUMENTED (verbatim, `$type: number`).
       - DIV-7 net-new border-width dimension (no border-width in shipped) vs TOKEN-03 "border" family → DOCUMENTED (add `--cw-border-width: 1px` least-surprise default; distinct from the `--cw-border` color alias).
    5. `## Validation Commands` and `## Scope Guard` sections mirroring the analog, restating the zero-drift invariant as the hard gate over both admin CSS files: `git diff --exit-code chimeway_admin/priv/static/chimeway_admin.css chimeway_admin/assets/css/chimeway_admin.css`.
  </action>
  <acceptance_criteria>
    - All seven DIV ids present, each with the zero-drift invariant:
      ```
      ok=1; for n in 1 2 3 4 5 6 7; do grep -q "DIV-$n" notes/decision-log.md || { echo "missing DIV-$n"; ok=0; }; done; [ $ok -eq 1 ]
      ```
    - Both dispositions used: `grep -q 'DOCUMENTED' notes/decision-log.md && grep -q 'DEFERRED' notes/decision-log.md`
    - Zero-drift invariant (naming both admin CSS files) appears at least seven times (once per entry) — `[ "$(grep -c 'git diff --exit-code chimeway_admin/priv/static/chimeway_admin.css chimeway_admin/assets/css/chimeway_admin.css' notes/decision-log.md)" -ge 7 ]`
    - `## Sources` section present: `grep -q '## Sources' notes/decision-log.md`
    - The DEFERRED entries reference the follow-on milestone: `grep -q 'ADMIN-RETHEME-01' notes/decision-log.md`
    - Zero-drift proof on both admin CSS files: `git diff --exit-code chimeway_admin/priv/static/chimeway_admin.css chimeway_admin/assets/css/chimeway_admin.css` (exit 0)
  </acceptance_criteria>
  <verify>
    <automated>for n in 1 2 3 4 5 6 7; do grep -q "DIV-$n" notes/decision-log.md || exit 1; done; grep -q 'DOCUMENTED' notes/decision-log.md && grep -q 'DEFERRED' notes/decision-log.md && [ "$(grep -c 'git diff --exit-code chimeway_admin/priv/static/chimeway_admin.css chimeway_admin/assets/css/chimeway_admin.css' notes/decision-log.md)" -ge 7 ] && git diff --exit-code chimeway_admin/priv/static/chimeway_admin.css chimeway_admin/assets/css/chimeway_admin.css</automated>
  </verify>
  <done>notes/decision-log.md records DIV-1..DIV-7 in house decision-doc format, each dispositioned DOCUMENTED/DEFERRED with both-side line refs and a closing git-diff zero-drift invariant naming both admin CSS files; DEFERRED items point to ADMIN-RETHEME-01; both admin CSS files unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan authors an inert static Markdown document with no runtime, no input handling, no auth, no crypto, no network, and no CI changes (81-RESEARCH.md § Security Domain).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-81-02 | N/A | Static `notes/decision-log.md` (documentation, no runtime surface) | low | accept | No STRIDE category applies to an inert Markdown file; no ASVS L1 control is relevant. Assessment per 81-RESEARCH.md § Security Domain. |
| T-81-SC | Tampering | package installs (npm/pip/cargo) | low | accept | No packages installed or permitted this phase; supply-chain threat surface is empty. No legitimacy checkpoint warranted. |

No `high`+ threats — nothing to block on.
</threat_model>

<verification>
Run the 81-VALIDATION.md TOKEN-04 doc-presence row:
- `notes/decision-log.md` has a DOCUMENTED/DEFERRED entry for DIV-1..DIV-7, each with the `git diff` invariant naming both admin CSS files
- Hard gate: `git diff --exit-code chimeway_admin/priv/static/chimeway_admin.css chimeway_admin/assets/css/chimeway_admin.css` (exit 0)
</verification>

<success_criteria>
- notes/decision-log.md records every sub-primitive divergence (radius-sm 5px vs 4px; info/cancelled/sending/expired status triads; missing --cw-info; net-new motion + z-index tokens) as DOCUMENTED/DEFERRED (TOKEN-04)
- Both admin CSS files (priv/static + assets/css wrapper) show zero changes (TOKEN-04)
</success_criteria>

<output>
Create `.planning/phases/81-design-tokens-reconciliation-documentation/81-02-SUMMARY.md` when done
</output>
