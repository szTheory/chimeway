---
phase: 86-accessibility-audit-notes-red-team-close
plan: 03
type: execute
wave: 2
depends_on: ["86-01"]
files_modified:
  - notes/accessibility-checks.md
autonomous: false
requirements: [A11Y-03, A11Y-04]

must_haves:
  truths:
    - "An operator runs Chrome DevTools 'Emulate vision deficiencies' (protanopia, deuteranopia, tritanopia, achromatopsia) on the rendered brandbook/index.html via file:// and confirms every status remains distinguishable by label+icon, not color alone (A11Y-04)."
    - "An operator keyboard-tabs the rendered page and confirms no focused control is ENTIRELY hidden behind the sticky .cwb-nav (SC 2.4.11 focus-not-obscured, A11Y-03)."
    - "The operator sign-off (date + result) is recorded into the A11Y-03 / A11Y-04 checklist in notes/accessibility-checks.md; no CVD tooling and no screenshot/binary artifacts are added."
  planner_assumptions:
    - "[A11Y-03 unclassified] Focus-not-obscured is verified by keyboard tab on the rendered page (RESEARCH A3, low risk — scroll-margin/anchor offsets typically keep targets visible); if a control is entirely hidden it is recorded as a documented finding, no CSS change."
    - "[A11Y-04 unclassified] CVD safety is operator-attested via DevTools emulation, corroborating the machine-grep never-color-alone argument from Plan 01; no automated CVD check exists by design (milestone forbids CVD tooling/binaries)."
  prohibitions:
    - "MUST NOT add any CVD-simulation tooling or commit any simulated-screenshot / binary artifact."
    - "MUST NOT edit any value in brandbook/tokens/tokens.css or any brandbook/brandbook.css rule to 'fix' a manual-check finding (out of scope — record as a documented finding)."
  artifacts:
    - notes/accessibility-checks.md
  key_links:
    - "brandbook/index.html (file://) -> DevTools CVD emulation + keyboard-tab pass"
    - "operator sign-off -> A11Y-03/A11Y-04 checklist in notes/accessibility-checks.md"
---

<objective>
Close the two genuinely-manual accessibility checks against the RENDERED brand book: the
Chrome DevTools CVD emulation (A11Y-04) and the keyboard-tab focus-not-obscured pass
(SC 2.4.11, A11Y-03), recording the operator sign-off into the checklist stubs Plan 01 wrote.

Purpose: A11Y-04 rests primarily on the machine-grep never-color-alone architecture; this
plan supplies the corroborating human-run browser verification D-05/D-06 prescribe — with no
tooling and no binaries.
Output: operator-signed A11Y-03 / A11Y-04 checklist in `notes/accessibility-checks.md`.

Covers the operator-attested parts of A11Y-03 and A11Y-04.
</objective>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
- Updated `notes/accessibility-checks.md` — operator sign-off appended to the A11Y-03 (focus-not-obscured) and A11Y-04 (CVD emulation) checklists. No new files, no binaries.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-accessibility-audit-notes-red-team-close/86-CONTEXT.md
@.planning/phases/86-accessibility-audit-notes-red-team-close/86-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator CVD emulation + focus-not-obscured pass on the rendered brandbook</name>
  <files>notes/accessibility-checks.md</files>
  <action>
    Operator-run manual verification against the rendered brandbook/index.html (file://): run
    the Chrome DevTools "Emulate vision deficiencies" pass (A11Y-04) and the keyboard-tab
    focus-not-obscured pass (SC 2.4.11, A11Y-03) per how-to-verify, then record the dated
    sign-off (or a documented finding) into the A11Y-03 / A11Y-04 checklist stubs in
    notes/accessibility-checks.md. No CVD tooling, no screenshots/binaries, no CSS/token edits.
  </action>
  <what-built>
    Plan 01 recorded the machine-evidenceable accessibility findings in
    notes/accessibility-checks.md: the offline contrast table, the WCAG-cited exemptions, the
    A11Y-03 rendered-CSS evidence, and the A11Y-04 never-color-alone architectural argument
    (status = surface + text + label + icon). Two checks remain that require a real browser and
    a human: the CVD emulation and the keyboard focus-not-obscured pass.
  </what-built>
  <how-to-verify>
    1. Open brandbook/index.html in Chrome via file:// (double-click or `open brandbook/index.html`).
    2. DevTools -> More tools -> Rendering -> "Emulate vision deficiencies". Cycle through
       Protanopia, Deuteranopia, Tritanopia, and Achromatopsia (blurred/greyscale). For each,
       confirm every status (success/warning/danger/info/neutral) is still tellable apart by its
       LABEL + ICON, not by hue — i.e. meaning is never color-alone.
    3. Click into the page, then press Tab repeatedly through all interactive controls (nav
       links, jump-nav anchors, buttons, theme toggle). Confirm no focused control is ENTIRELY
       hidden behind the sticky top .cwb-nav bar (SC 2.4.11 — partial overlap is allowed).
    4. In notes/accessibility-checks.md, tick the A11Y-04 CVD checklist and the A11Y-03
       focus-not-obscured checklist with the date and result. If any status is indistinguishable
       under emulation, or any control is entirely obscured, record it as a documented finding
       (do NOT edit tokens.css or brandbook.css — that is out of scope).
  </how-to-verify>
  <resume-signal>Type "approved" once the CVD emulation + focus-not-obscured checklists are signed in notes/accessibility-checks.md, or describe the finding to record.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Manual browser verification + a markdown checklist sign-off; no code, no network, no input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-86-06 | Tampering | notes/accessibility-checks.md sign-off | low | accept | Human-attested checklist; no artifact beyond the markdown line; scope-audited in Plan 04. |
| T-86-SC | Tampering | package installs | low | accept | No installs; no supply-chain surface. |
</threat_model>

<verification>
- notes/accessibility-checks.md carries a signed A11Y-04 CVD checklist and A11Y-03 focus-not-obscured checklist (or a recorded documented finding).
- No CVD tooling, no screenshot/binary committed; tokens.css and brandbook.css unchanged.
</verification>

<success_criteria>
The operator-attested browser checks for A11Y-03 (focus-not-obscured) and A11Y-04 (CVD
emulation) are recorded in the audit, with no tooling and no binaries added.
</success_criteria>

<output>
Create `.planning/phases/86-accessibility-audit-notes-red-team-close/86-03-SUMMARY.md` when done.
</output>
