---
phase: 84-html-brandbook-voice-component-states
plan: 03
type: execute
wave: 3
depends_on: ["84-02"]
files_modified:
  - brandbook/index.html
autonomous: true
requirements: [STATE-01, STATE-02, BOOK-03]
tags: [brandbook, html, component-states, contrast-matrix, do-dont]

must_haves:
  truths:
    - "All nine component states (hover/focus/active/disabled/loading/error/empty/skeleton/selected) render statically without interaction, each shown twice — a live control and a frozen .is-* copy labelled with its state name (STATE-01)"
    - "Three do/don't visual pairs (logo/color/spacing misuse) render as .cwb-do/.cwb-dont side-by-sides, with every 'don't' produced by CSS-only misuse around a correct asset — no broken SVG (STATE-02, D-06)"
    - "A live WCAG contrast matrix computes fg×bg ratios via getComputedStyle on a painted probe with the inline relative-luminance formula, recomputing on theme flip and prefers-color-scheme change (BOOK-03, D-03)"
  artifacts:
    - brandbook/index.html
  key_links:
    - "the .is-* classes and .cwb-skeleton/.cwb-dont wrappers referenced here are defined in plan-01 brandbook.css"
    - "the contrast matrix extends the SAME single inline classic <script> created in plan 02 (no second script tag) — D-03"
    - "the matrix's painted-probe read resolves var() aliases + the active data-theme, proving it is live"
---

<objective>
Extend `brandbook/index.html` with the interactive-proof sections: the full nine-state component showcase (each state shown live AND frozen via `.is-*`), the three do/don't visual pairs (CSS-only misuse), and the live WCAG contrast matrix wired into the existing inline script.

Purpose: These sections prove the system works rather than merely describing it (STATE-01, STATE-02) and deliver the BOOK-03 live contrast matrix.
Output: `#states`, `#dodont`, and `#contrast` sections plus the contrast-matrix logic appended to the single inline script.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-html-brandbook-voice-component-states/84-CONTEXT.md
@.planning/phases/84-html-brandbook-voice-component-states/84-UI-SPEC.md
@.planning/phases/84-html-brandbook-voice-component-states/84-PATTERNS.md
@.planning/phases/84-html-brandbook-voice-component-states/84-RESEARCH.md
@brandbook/brandbook.css
@brandbook/tokens/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nine-state component showcase (live control + frozen .is-* copy each)</name>
  <files>brandbook/index.html</files>
  <read_first>
    - .planning/phases/84-html-brandbook-voice-component-states/84-UI-SPEC.md (§Component States table — the nine states + driving tokens; the "demoed twice" contract; empty-state copy)
    - brandbook/brandbook.css (the `.cwb-btn` + `.is-*` classes and `.cwb-skeleton` authored in plan 01 — reference, do not redefine)
    - .planning/phases/84-html-brandbook-voice-component-states/84-PATTERNS.md (§`.is-*` static state-forcing pattern)
  </read_first>
  <action>
    Author the `#states` section (`.cwb-section`). For each of the nine states — hover, focus, active, disabled, loading, error, empty, skeleton, selected — render it TWICE (UI-SPEC contract): (a) a live interactive `.cwb-btn` control that exhibits the real pseudo-class on interaction, and (b) a frozen copy carrying the matching `.is-*` forcing class (`.is-hover`, `.is-focus`, `.is-active`, `.is-disabled`, `.is-loading`, `.is-error`, `.is-empty`, `.is-selected`) or `.cwb-skeleton` for skeleton, each labelled visibly with its state name so the state proves out on a static page (STATE-01). Use the plan-01 CSS classes — do not add inline styles or redefine tokens. For the error state, pair the danger triad with a text label + icon (never color-alone). For the empty state, include the copy `No deliveries yet` with body `Trigger an event to see it traced here.` (lifted from UI-SPEC). Ensure interactive targets are ≥24×24px (SC 2.5.8).
  </action>
  <verify>
    <automated>grep -q 'id="states"' brandbook/index.html && for s in hover focus active disabled loading error empty skeleton selected; do grep -q "is-$s\|cwb-skeleton" brandbook/index.html || { echo "MISSING $s"; exit 1; }; done && grep -q 'No deliveries yet' brandbook/index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All nine states appear, each as a live control AND a labelled frozen `.is-*`/`.cwb-skeleton` copy
    - Error state pairs color with label + icon; empty state carries the specified copy
    - No inline styles or token redefinitions — uses plan-01 classes
  </acceptance_criteria>
  <done>The nine-state showcase renders every state statically (frozen .is-*) alongside its live control, labelled and token-driven.</done>
</task>

<task type="auto">
  <name>Task 2: Do/don't visual pairs (logo / color / spacing), CSS-only misuse</name>
  <files>brandbook/index.html</files>
  <read_first>
    - .planning/phases/84-html-brandbook-voice-component-states/84-UI-SPEC.md (§Do/Don't Visual Pairs — the three pairings and their sources)
    - .planning/phases/84-html-brandbook-voice-component-states/84-CONTEXT.md (D-06 — CSS-only .cwb-dont wrapper; never commit a broken SVG)
    - brandbook/brandbook.css (the `.cwb-do`/`.cwb-dont` wrappers from plan 01)
  </read_first>
  <action>
    Author the `#dodont` section with three `.cwb-do`/`.cwb-dont` side-by-side pairs (STATE-02): (1) Logo — DO: mark + wordmark as one unit on open field, inverse mark reading with no backdrop; DON'T: rectangular background cage behind the mark and icon-left/text-right split. (2) Color — DO: status by color + label + icon, body text as `--cw-ink` on `--cw-paper`; DON'T: brass text on paper for body copy and color-alone status. (3) Spacing — DO: consistent `--cw-space-*` rhythm with documented clear-space; DON'T: cramped/ad-hoc gaps and sub-minimum logo clear-space. Every "don't" MUST be produced by the plan-01 `.cwb-dont` CSS treatment applied around the CORRECT shipped asset/token (D-06) — do NOT author, import, or commit a deliberately-broken SVG, and do not add a hard-coded hex. Label each side "Do" and "Don't" explicitly.
  </action>
  <verify>
    <automated>grep -q 'id="dodont"' brandbook/index.html && grep -q 'cwb-dont' brandbook/index.html && grep -q 'cwb-do' brandbook/index.html && grep -qi '>Do<\|Do:' brandbook/index.html && grep -qi "Don" brandbook/index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Three do/don't pairs (logo, color, spacing) render as `.cwb-do`/`.cwb-dont` side-by-sides
    - Every "don't" is CSS-only misuse around a correct asset/token; no broken SVG committed
    - Sides are explicitly labelled Do / Don't
  </acceptance_criteria>
  <done>Three do/don't visual pairs render via CSS-only misuse wrappers with no broken assets.</done>
</task>

<task type="auto">
  <name>Task 3: Live WCAG contrast matrix (inline, extends the existing script)</name>
  <files>brandbook/index.html</files>
  <read_first>
    - .planning/phases/84-html-brandbook-voice-component-states/84-RESEARCH.md (§Code Examples — the WCAG relative-luminance + ratio + painted-probe tokenRGB functions; Pitfall 5 sanity pairs)
    - .planning/phases/84-html-brandbook-voice-component-states/84-PATTERNS.md (§Live WCAG contrast matrix excerpt)
    - .planning/phases/84-html-brandbook-voice-component-states/84-UI-SPEC.md (§Component States "Live contrast matrix" — AA thresholds 4.5 normal / 3 large-UI; recompute on theme + prefers-color-scheme change)
  </read_first>
  <action>
    Author the `#contrast` section (`.cwb-matrix` grid of `.cwb-cell`/`.cwb-badge`) and EXTEND the single inline classic `<script>` created in plan 02 (do NOT add a second `<script>` tag, D-03). Implement the exact WCAG relative-luminance formula inline (sRGB linearization `_lin`, weighted `luminance`, `ratio` with the +0.05 terms) — no color library. Resolve each `--cw-*` token to `[r,g,b]` via `getComputedStyle` on a hidden painted probe appended inside `.cw-brandbook`, so `var()` aliases and the active `data-theme` resolve correctly (proving the matrix is LIVE, BOOK-03). Render a badge per fg×bg cell showing the ratio and AA pass/fail (≥4.5:1 normal text, ≥3:1 large text / non-text UI). Recompute the matrix on theme-toggle click and on `matchMedia('(prefers-color-scheme: dark)')` change. Include a sanity assertion in a comment: ink-on-paper reads very high; brass-on-paper FAILs body text (Pitfall 5). The word `luminance` must appear (guard section-presence check).
  </action>
  <verify>
    <automated>grep -q 'id="contrast"' brandbook/index.html && grep -q 'luminance' brandbook/index.html && grep -q 'getComputedStyle' brandbook/index.html && grep -q 'prefers-color-scheme' brandbook/index.html && test "$(grep -c '<script' brandbook/index.html)" -le 1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `#contrast` matrix renders fg×bg cells with AA pass/fail badges computed live
    - Luminance/ratio uses the exact WCAG formula via a painted-probe getComputedStyle read (no library)
    - Matrix recomputes on theme flip and prefers-color-scheme change; still a single inline `<script>` tag
  </acceptance_criteria>
  <done>A live contrast matrix computes WCAG ratios from resolved token values and recomputes on theme change, wired into the one inline script.</done>
</task>

</tasks>

<artifacts_produced>
Extends `brandbook/index.html`:
- Section anchors: `states`, `dodont`, `contrast`
- Nine frozen state demos referencing `.is-*` / `.cwb-skeleton`; three `.cwb-do`/`.cwb-dont` pairs
- Contrast-matrix DOM (`.cwb-matrix`/`.cwb-cell`/`.cwb-badge`) + inline WCAG luminance/ratio/tokenRGB logic appended to the existing single `<script>`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk→browser (file://) | inline script reads only same-document token values via getComputedStyle — never user or network data |
| book snippet→host app | do/don't misuse styling must stay inside `.cwb-dont`/`.cw-brandbook` scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-84-01 | Tampering | inline contrast script | high | mitigate | Extends the existing single classic inline script; no external/module/fetch code — guard family 1 (plan 01) enforces |
| T-84-05 | Information disclosure | contrast matrix data source | low | accept | Script reads only same-document `getComputedStyle` token values; no user/network input is processed, so there is nothing to exfiltrate |
| T-84-03 | Tampering | .cwb-dont scope leakage | medium | mitigate | Misuse styling confined to `.cwb-dont` under `.cw-brandbook`; guard family 2 audit (plan 01) flags unscoped selectors |
| T-84-SC | Tampering | package installs | high | mitigate | Zero installs; no dependency surface introduced |
</threat_model>

<verification>
- `bash scripts/brandbook-guards.sh` — section-presence family now finds the nine `is-*` tokens, `data-cwb-theme`, and `luminance`; file://-safety + scope families still pass. Voice anchors still RED until plan 04 (expected).
- Manual: open file:// in Chromium — every state visible frozen, do/don't pairs read correctly, contrast badges flip pass/fail when the theme toggles.
</verification>

<success_criteria>
The nine states render statically, do/don't pairs render via CSS-only misuse, and a live contrast matrix computes WCAG ratios that recompute on theme change — all file://-safe with a single inline script.
</success_criteria>

<output>
Create `.planning/phases/84-html-brandbook-voice-component-states/84-03-SUMMARY.md` when done.
</output>
