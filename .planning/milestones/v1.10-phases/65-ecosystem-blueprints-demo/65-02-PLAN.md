---
phase: 65-ecosystem-blueprints-demo
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/chimeway_demo_host/test/test_helper.exs
  - examples/chimeway_demo_host/lib/demo_host/seeds.ex
autonomous: true
requirements:
  - DEMO-09
  - DEMO-10

must_haves:
  truths:
    - "demo host test_helper.exs bootstraps Threadline.Test.Repo and Sigra.TestRepo analogous to Accrue block"
    - "DemoHost.Seeds.seed_threadline_notification/0 returns {:ok, map()} with recipient_identity and trace.delivery_ids and trace.correlation_id"
    - "DemoHost.Seeds.seed_sigra_auth/0 returns {:ok, map()} with recipient_identity and trace.delivery_ids; never exposes raw_token"
    - "@compile {:no_warn_undefined, [...]} in seeds.ex covers Sigra.Integrations.Chimeway"
  artifacts:
    - path: "examples/chimeway_demo_host/test/test_helper.exs"
      provides: "Threadline + Sigra TestRepo bootstrap for demo host test suite"
      contains: "Code.ensure_loaded?(Threadline)"
    - path: "examples/chimeway_demo_host/lib/demo_host/seeds.ex"
      provides: "seed_threadline_notification/0 + seed_sigra_auth/0 seed helpers"
      contains: "seed_threadline_notification"
  key_links:
    - from: "examples/chimeway_demo_host/test/demo_host_web/threadline_telemetry_proof_test.exs"
      to: "examples/chimeway_demo_host/lib/demo_host/seeds.ex"
      via: "DemoHost.Seeds.seed_threadline_notification/0"
      pattern: "seed_threadline_notification"
    - from: "examples/chimeway_demo_host/test/demo_host_web/sigra_auth_proof_test.exs"
      to: "examples/chimeway_demo_host/lib/demo_host/seeds.ex"
      via: "DemoHost.Seeds.seed_sigra_auth/0"
      pattern: "seed_sigra_auth"
---

<objective>
Add Threadline and Sigra TestRepo bootstrap to the demo host test helper, and add the two DEMO-09/DEMO-10 seed helpers to DemoHost.Seeds.

Purpose: The demo proof test files (Plan 03, Wave 2) require (a) Threadline.Test.Repo and Sigra.TestRepo to be started and available in the sandbox and (b) DemoHost.Seeds.seed_threadline_notification/0 and seed_sigra_auth/0 as their trigger entry points. Neither exists yet in the demo host.

Output: (1) examples/chimeway_demo_host/test/test_helper.exs with Threadline + Sigra bootstrap blocks; (2) examples/chimeway_demo_host/lib/demo_host/seeds.ex with two new seed helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/65-ecosystem-blueprints-demo/65-CONTEXT.md
@.planning/phases/65-ecosystem-blueprints-demo/65-RESEARCH.md
@.planning/phases/65-ecosystem-blueprints-demo/65-PATTERNS.md
@examples/chimeway_demo_host/test/test_helper.exs
@examples/chimeway_demo_host/lib/demo_host/seeds.ex
@test/chimeway/integrations/threadline_telemetry_lifecycle_test.exs
@test/chimeway/integrations/sigra_auth_lifecycle_test.exs
@test/support/threadline/fixtures.ex
@test/support/sigra/fixtures.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Threadline + Sigra TestRepo bootstrap to demo host test_helper.exs (DEMO-09, DEMO-10)</name>
  <files>examples/chimeway_demo_host/test/test_helper.exs</files>
  <read_first>
    - examples/chimeway_demo_host/test/test_helper.exs — read in full; understand the Mailglass block (lines 13–42) and Accrue block (lines 44–107) as the pattern templates; note the exact steps: ensure_all_started, storage_up, pool swap for migration, Migrator.run, pool restore, start_link, Sandbox.mode(:manual)
    - test/chimeway/integrations/threadline_telemetry_lifecycle_test.exs — see how Threadline.Test.Repo is used in tests; find the DataCase or repo start pattern to confirm migration path
    - test/chimeway/integrations/sigra_auth_lifecycle_test.exs — see how Sigra.TestRepo is used; confirm migration path analogous to Accrue
    - .planning/phases/65-ecosystem-blueprints-demo/65-RESEARCH.md — Pitfall 1 (this gap is confirmed); Shared Patterns section "Wave 0 Prerequisite: Demo Host test_helper.exs Bootstrap"
    - .planning/phases/65-ecosystem-blueprints-demo/65-PATTERNS.md — Shared Patterns section condensed Accrue block steps to replicate
  </read_first>

  <action>
  Append two new conditional blocks to examples/chimeway_demo_host/test/test_helper.exs, after the closing "end" of the Accrue block (line ~107).

  Block 1 — Threadline bootstrap (mirrors Mailglass/Accrue pattern):
  if Code.ensure_loaded?(Threadline) do
    1. migrations_path: locate via test/support/threadline/migrations/ wildcard or fallback to :threadline priv_dir repo/migrations (same wildcard pattern as Accrue block)
    2. test_repo_config = Application.get_env(:threadline, Threadline.Test.Repo)
    3. Ecto.Adapters.Postgres.storage_up(test_repo_config) — ok / already_up / raise
    4. Swap pool to DBConnection.ConnectionPool
    5. Ecto.Migrator.with_repo(Threadline.Test.Repo, fn repo -> Ecto.Migrator.run(repo, migrations_path, :up, all: true, log: false) end)
    6. Restore original config (put_env without pool swap)
    7. Threadline.Test.Repo.start_link()
    8. Ecto.Adapters.SQL.Sandbox.mode(Threadline.Test.Repo, :manual)
  end

  Block 2 — Sigra bootstrap (same structure, Sigra.TestRepo):
  if Code.ensure_loaded?(Sigra) do
    Same 8 steps with Sigra.TestRepo and :sigra app key.
    Additionally: if Code.ensure_loaded?(Sigra.Integrations.Chimeway), configure integration:
      Application.put_env(:sigra, :chimeway, enabled: false)
      (proof test setup will set enabled: true per test — start disabled as default)
  end

  Key constraint: Do NOT set enabled: true globally — each proof test enables it in setup and cleans up in on_exit (mirrors Accrue dunning engine config pattern at lines 101–106 of current file where dunning engine is configured per test).
  </action>

  <verify>
    <automated>cd /Users/jon/projects/chimeway/examples/chimeway_demo_host && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>

  <done>mix compile --warnings-as-errors exits 0 in the demo host directory. The test_helper.exs file contains Code.ensure_loaded?(Threadline) and Code.ensure_loaded?(Sigra) blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Add seed_threadline_notification/0 and seed_sigra_auth/0 to DemoHost.Seeds (D-05, DEMO-09, DEMO-10)</name>
  <files>examples/chimeway_demo_host/lib/demo_host/seeds.ex</files>
  <read_first>
    - examples/chimeway_demo_host/lib/demo_host/seeds.ex — read in full; find @compile {:no_warn_undefined, [DemoHost.AccrueSeeds]} at line ~2; find seed_accrue_dunning/0 (lines ~139–146) as delegate pattern; find trigger/3 private helper (lines ~216–227) and normalize_trigger_result/1 (line ~229) to understand the result map shape returned for Chimeway.trigger calls; find @alex_email and @tenant_id module attributes for reuse
    - test/chimeway/integrations/threadline_telemetry_lifecycle_test.exs — verify the DemoHost.Notifiers.InviteSent notifier is the right choice for seed_threadline_notification (or find the notifier the lifecycle test uses); confirm correlation_id propagates through normalize_trigger_result/1
    - test/chimeway/integrations/sigra_auth_lifecycle_test.exs — find dispatch_magic_link_after_request/3 or dispatch_confirmation_after_generate/3 call signature and return shape {:ok, {raw_token, url, result}} (line ~41 area); note the result struct fields to extract delivery_ids and event_id
    - .planning/phases/65-ecosystem-blueprints-demo/65-PATTERNS.md — Pattern 4: seed_threadline_notification/0 full pattern; seed_sigra_auth/0 delegate vs inline discussion; return map shape requirement; @compile guard extension; seed_sigra_auth note about {raw_token, url, result} unwrapping without exposing raw_token
    - .planning/phases/65-ecosystem-blueprints-demo/65-RESEARCH.md — Pitfall 2 (@compile guard), Pitfall 3 (Code.ensure_loaded? is sufficient for exclusion), Pattern 4 seeds private trigger/3 helper; seed_sigra_auth return map shape note (MUST NOT expose raw_token or url)
  </read_first>

  <action>
  Modify examples/chimeway_demo_host/lib/demo_host/seeds.ex:

  1. Extend the @compile {:no_warn_undefined, [...]} directive at the top of the module to add Sigra.Integrations.Chimeway to the list. Current form is @compile {:no_warn_undefined, [DemoHost.AccrueSeeds]}; new form: @compile {:no_warn_undefined, [DemoHost.AccrueSeeds, Sigra.Integrations.Chimeway]}.

  2. Add seed_threadline_notification/0 after seed_accrue_dunning/0 (or in the ecosystem seed section):

     @doc """
     DEMO-09: Threadline audit correlation for notification lifecycle with reporter attached.
     Standalone API; not invoked from run/0. Caller must attach ThreadlineReporter in setup.
     """
     @spec seed_threadline_notification() :: {:ok, map()} | {:error, term()}
     def seed_threadline_notification do
       if Code.ensure_loaded?(Chimeway.Telemetry.ThreadlineReporter) do
         trigger(
           DemoHost.Notifiers.InviteSent,
           %{email: @alex_email, team_name: "Threadline Demo"},
           idempotency_key: "teampulse-seed-threadline-v1-#{System.unique_integer([:positive])}",
           correlation_id: "teampulse-seed-threadline-corr-#{System.unique_integer([:positive])}",
           tenant_id: @tenant_id
         )
       else
         {:error, :threadline_not_available}
       end
     end

     NOTE: The trigger/3 private helper normalizes to %{trace: %{delivery_ids: [...], correlation_id: ...}, recipient_identity: ...}. Verify that normalize_trigger_result/1 propagates correlation_id from the Chimeway trigger opts into the result (check its implementation). If it does not, inline the correlation_id from opts into the returned map after the trigger/3 call.

  3. Add seed_sigra_auth/0 after seed_threadline_notification/0:

     @doc """
     DEMO-10: Sigra auth → Chimeway durable delivery with operator trace inspectability.
     Standalone API; not invoked from run/0. Caller must configure Sigra integration in setup.
     """
     @spec seed_sigra_auth() :: {:ok, map()} | {:error, term()}
     def seed_sigra_auth do
       if Code.ensure_loaded?(Sigra.Integrations.Chimeway) do
         case Sigra.Integrations.Chimeway.dispatch_magic_link_after_request(
                @alex_email,
                %{idempotency_key: "teampulse-seed-sigra-magic-link-#{System.unique_integer([:positive])}", tenant_id: @tenant_id, correlation_id: "teampulse-seed-sigra-corr-#{System.unique_integer([:positive])}"}
              ) do
           {:ok, {_raw_token, _url, result}} ->
             # Normalize to proof-test shape: never expose raw_token or url
             delivery_ids = get_in(result, [:trace, :delivery_ids]) || []
             {:ok, %{recipient_identity: @alex_email, trace: %{delivery_ids: delivery_ids, correlation_id: result[:trace][:correlation_id] || result[:correlation_id]}}}
           {:error, _} = err ->
             err
         end
       else
         {:error, :sigra_not_available}
       end
     end

     NOTE: The exact Sigra dispatch function signature and result shape must be read from sigra_auth_lifecycle_test.exs and test/support/sigra/fixtures.ex before writing this function. Adapt the dispatch call and result unwrapping to match the actual Phase 64 API. The critical invariant is: _raw_token and _url are discarded (prefixed with _); only delivery_ids and correlation_id are forwarded in the returned map.
  </action>

  <verify>
    <automated>cd /Users/jon/projects/chimeway/examples/chimeway_demo_host && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>

  <done>mix compile --warnings-as-errors exits 0. seeds.ex contains seed_threadline_notification, seed_sigra_auth, and @compile {:no_warn_undefined, [..., Sigra.Integrations.Chimeway]}. Neither function exposes raw_token or url in its return value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DemoHost.Seeds public API | seed_* functions are adopter-copyable; must not leak internal token/URL values |
| Sigra.Integrations.Chimeway dispatch return | Returns {:ok, {raw_token, url, result}} — raw_token must be discarded at the seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-03 | Information Disclosure | seed_sigra_auth/0 return map | mitigate | Unwrap {raw_token, url, result} and bind raw_token + url to _ variables; return map contains only delivery_ids and correlation_id — never token or URL values |
| T-65-04 | Tampering | Sigra integration enabled globally in test_helper | mitigate | Set enabled: false in test_helper global config; each proof test enables in setup and cleans up in on_exit — prevents cross-test contamination |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in Plan 02 |
</threat_model>

<verification>
Demo host compiles clean with both new modules:
```
cd /Users/jon/projects/chimeway/examples/chimeway_demo_host && mix compile --warnings-as-errors
```

Seed helper presence:
```
grep -c "seed_threadline_notification\|seed_sigra_auth" examples/chimeway_demo_host/lib/demo_host/seeds.ex
```
Expected: 2+ matches.

test_helper bootstrap presence:
```
grep -c "Code.ensure_loaded?(Threadline)\|Code.ensure_loaded?(Sigra)" examples/chimeway_demo_host/test/test_helper.exs
```
Expected: 2 matches.

No raw_token exposure in seeds:
```
grep -v "raw_token}" examples/chimeway_demo_host/lib/demo_host/seeds.ex | grep -c "raw_token"
```
Expected: 0 (only _raw_token pattern allowed as discarded binding).
</verification>

<success_criteria>
- examples/chimeway_demo_host/test/test_helper.exs has Threadline and Sigra bootstrap blocks; mix compile exits 0
- examples/chimeway_demo_host/lib/demo_host/seeds.ex has seed_threadline_notification/0 and seed_sigra_auth/0; both return {:ok, map()} with recipient_identity and trace.delivery_ids; seed_sigra_auth never exposes raw_token or url
- @compile {:no_warn_undefined, [...]} covers Sigra.Integrations.Chimeway
- All existing demo host tests still pass (no regression): cd examples/chimeway_demo_host && mix test --exclude accrue --exclude threadline --exclude sigra --exclude inbox
</success_criteria>

<output>
Create `.planning/phases/65-ecosystem-blueprints-demo/65-02-SUMMARY.md` when done.
</output>
