---
phase: 65-ecosystem-blueprints-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/recipes/sigra-auth-blueprint.md
  - test/chimeway/doc_contract_test.exs
  - mix.exs
autonomous: true
requirements:
  - ECOS-10

must_haves:
  truths:
    - "guides/recipes/sigra-auth-blueprint.md exists with all D-08 required strings present"
    - "ECOS-10 describe block in doc_contract_test.exs passes: all 10 required strings asserted, forbidden strings refuted"
    - "mix.exs HexDocs extras list contains sigra-auth-blueprint.md entry"
  artifacts:
    - path: "guides/recipes/sigra-auth-blueprint.md"
      provides: "Sigra auth notification reference blueprint (ECOS-10)"
      contains: "Sigra.Integrations.Chimeway"
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "ECOS-10 doc-contract CI coverage"
      contains: "sigra auth blueprint recipe doc contract (ECOS-10)"
    - path: "mix.exs"
      provides: "HexDocs extras registration for Sigra blueprint"
      contains: "sigra-auth-blueprint.md"
  key_links:
    - from: "test/chimeway/doc_contract_test.exs"
      to: "guides/recipes/sigra-auth-blueprint.md"
      via: "@sigra_blueprint_recipe path attribute + File.read!"
      pattern: "sigra_blueprint_recipe.*sigra-auth-blueprint"
---

<objective>
Deliver the Sigra auth reference blueprint document and its CI doc-contract coverage.

Purpose: ECOS-10 requires a published blueprint recipe that documents the Sigra → Chimeway trigger bridge with the correct responsibility split, notifier key examples, and adopter wiring — locked behind a doc-contract that fails CI if the content regresses.

Output: (1) guides/recipes/sigra-auth-blueprint.md — full blueprint following accrue-dunning-blueprint.md structure; (2) ECOS-10 describe block appended to doc_contract_test.exs; (3) mix.exs extras entry registering the blueprint for HexDocs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/65-ecosystem-blueprints-demo/65-CONTEXT.md
@.planning/phases/65-ecosystem-blueprints-demo/65-RESEARCH.md
@.planning/phases/65-ecosystem-blueprints-demo/65-PATTERNS.md
@guides/recipes/accrue-dunning-blueprint.md
@test/chimeway/doc_contract_test.exs
@mix.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write guides/recipes/sigra-auth-blueprint.md (ECOS-10, D-01, D-02, D-08, D-09)</name>
  <files>guides/recipes/sigra-auth-blueprint.md</files>
  <read_first>
    - guides/recipes/accrue-dunning-blueprint.md — exact section order to replicate; responsibility split language template; trigger example shape; runnable demo pattern; related guides cross-link format
    - .planning/phases/65-ecosystem-blueprints-demo/65-CONTEXT.md — locked decisions D-01 (section list), D-02 (responsibility split language), D-08 (required strings), D-09 (mix.exs extras)
    - .planning/phases/65-ecosystem-blueprints-demo/65-RESEARCH.md — Pattern 1 section order, security constraint (no raw_token in code examples), forbidden phrases, ECOS-10 required string list
    - .planning/phases/65-ecosystem-blueprints-demo/65-PATTERNS.md — exact content templates for each section including responsibility split prose, not-a-Chimeway.Adapter callout, runnable demo section, related guides
  </read_first>

  <behavior>
    - Blueprint contains all 10 D-08 required strings: Sigra.Integrations.Chimeway, sigra.auth.magic_link, sigra.auth.confirmation_code, Chimeway.trigger, idempotency_key, tenant_id, orchestrates, DemoHost.Seeds.seed_sigra, /admin/chimeway, sigra-auth-integration.md
    - Blueprint does NOT contain: raw_token, raw token (forbidden per Phase 64 D-07 redaction)
    - Blueprint does NOT contain: stop_conditions, Workflows.Workers, Chimeway.Trigger.trigger (recipe_forbidden_strings)
    - Blueprint section order matches accrue-dunning-blueprint.md exactly (per D-01)
    - Responsibility split says "Chimeway orchestrates the when and why" and "Sigra owns auth state" (per D-02)
    - "not a Chimeway.Adapter seam" callout present (per D-02)
    - Code examples show identifier-only params: user_id, email, correlation_id — no raw token or code verbatim values
    - Related guides section includes sigra-auth-integration.md cross-link (required string for doc-contract)
    - Runnable demo section references DemoHost.Seeds.seed_sigra_auth/0 and /admin/chimeway
  </behavior>

  <action>
  Create guides/recipes/sigra-auth-blueprint.md with these sections in order (per D-01, mirroring accrue-dunning-blueprint.md structure):

  Section 1 "## Who this is for": Two JTBDs — Feature Developer (SigraAuthNotifier authoring, stable notification_key for sigra.auth.magic_link and sigra.auth.confirmation_code) and Adopter (wiring Sigra auth events into Chimeway trigger bridge via Sigra.Integrations.Chimeway config).

  Section 2 "## Prerequisites": Links to Golden Path guide and Sigra sibling checkout env var (SIGRA_PATH for optional path dep).

  Section 3 "## Responsibility split (SEED-003)" (per D-02): "Chimeway orchestrates the when and why: durable notification lifecycle, suppression and preference gates, idempotency, and operator traces you can search at /admin/chimeway." Then "Sigra owns auth state: token generation, hashed persistence, rate limits, and magic link / confirmation code TTL. Sigra emits auth events; Chimeway does not mutate Sigra records." Then the not-a-Chimeway.Adapter callout: "This integration is not a Chimeway.Adapter seam — it is a Sigra auth event → Chimeway.trigger/3 bridge only."

  Section 4 "## Feature Developer: SigraAuthNotifier authoring": Stable key constants sigra.auth.magic_link and sigra.auth.confirmation_code — show notification_key returning "sigra.auth.magic_link" in a code example. build/2 implementation that returns rendered email params (user_id, email — never token values). Do NOT include raw_token or confirmation code values in any param map.

  Section 5 "## Adopter: Sigra.Integrations.Chimeway registration": Config wiring for Sigra integration — config :sigra, chimeway: [enabled: true] and the Chimeway trigger call that Sigra.Integrations.Chimeway makes internally with idempotency_key and tenant_id.

  Section 6 "## Trigger example (local / test)": Show calling Sigra.Integrations.Chimeway.dispatch_magic_link_after_request/3 or dispatch_confirmation_after_generate/3 with identifier-only params (user_id, email, correlation_id — no raw_token).

  Section 7 "## Runnable demo": Reference DemoHost.Seeds.seed_sigra_auth/0 as standalone API. Verification: SIGRA_PATH=../sigra mix verify.sigra --warnings-as-errors. Operator trace: Search /admin/chimeway by user email.

  Section 8 "## Out of scope": Defer golden-path Sigra auth integration guide, guide doc-contract, and formal mix verify.sigra CI job to sigra-auth-integration.md (Phase 66 DOCS-10). Include the cross-link text with sigra-auth-integration.md in the path.

  Section 9 "## Related guides": Include link to ../introduction/sigra-auth-integration.md, Golden Path, and accrue-dunning-blueprint.md. The sigra-auth-integration.md link satisfies the D-08 required string.
  </action>

  <verify>
    <automated>grep -c "Sigra.Integrations.Chimeway" guides/recipes/sigra-auth-blueprint.md && grep -c "sigra.auth.magic_link" guides/recipes/sigra-auth-blueprint.md && grep -c "sigra.auth.confirmation_code" guides/recipes/sigra-auth-blueprint.md && grep -c "Chimeway.trigger" guides/recipes/sigra-auth-blueprint.md && grep -c "idempotency_key" guides/recipes/sigra-auth-blueprint.md && grep -c "tenant_id" guides/recipes/sigra-auth-blueprint.md && grep -c "orchestrates" guides/recipes/sigra-auth-blueprint.md && grep -c "DemoHost.Seeds.seed_sigra" guides/recipes/sigra-auth-blueprint.md && grep -c "/admin/chimeway" guides/recipes/sigra-auth-blueprint.md && grep -c "sigra-auth-integration.md" guides/recipes/sigra-auth-blueprint.md</automated>
  </verify>

  <done>All 10 grep commands return >= 1. grep -c "raw_token" guides/recipes/sigra-auth-blueprint.md returns 0. File has all 9 sections matching accrue-dunning-blueprint.md order.</done>
</task>

<task type="auto">
  <name>Task 2: Append ECOS-10 doc-contract describe block to doc_contract_test.exs (D-07, D-08)</name>
  <files>test/chimeway/doc_contract_test.exs</files>
  <read_first>
    - test/chimeway/doc_contract_test.exs — read in full; find the @accrue_blueprint_recipe attribute (line ~297) and ECOS-07 describe block (lines 297–364) to use as the append position and structural template; verify @recipe_forbidden_strings is defined at line ~104
    - guides/recipes/sigra-auth-blueprint.md — verify exact string content matches the @required list before adding doc-contract assertions
    - .planning/phases/65-ecosystem-blueprints-demo/65-CONTEXT.md — D-07 (append position after ECOS-07), D-08 (required strings list)
    - .planning/phases/65-ecosystem-blueprints-demo/65-PATTERNS.md — Pattern 2 full describe block template, @sigra_blueprint_recipe path attribute
  </read_first>

  <action>
  Append to test/chimeway/doc_contract_test.exs immediately after the closing "end" of the ECOS-07 describe block (which ends around line 364, before the @mailglass_integration_guide attribute at line ~366):

  1. Add module attribute before the describe block:
     @sigra_blueprint_recipe Path.expand("../../guides/recipes/sigra-auth-blueprint.md", __DIR__)

  2. Add describe block "sigra auth blueprint recipe doc contract (ECOS-10)" with:
     - setup: File.read!(@sigra_blueprint_recipe) → %{content: content}
     - "for forbidden <- @recipe_forbidden_strings" loop: refute String.contains? — reusing the existing @recipe_forbidden_strings (stop_conditions, Workflows.Workers, Chimeway.Trigger.trigger)
     - inline forbidden test for raw_token: refute String.contains?(content, "raw_token") with message "sigra auth blueprint recipe must not expose raw token values"
     - inline forbidden test for "raw token" (prose form): same pattern
     - "forbids Chimeway.Workflow module (not Workflows)" test: refute Regex.match?(~r/Chimeway\.Workflow(?![s])/, content)
     - @required list: ~w(Sigra.Integrations.Chimeway sigra.auth.magic_link sigra.auth.confirmation_code Chimeway.trigger idempotency_key tenant_id orchestrates DemoHost.Seeds.seed_sigra /admin/chimeway sigra-auth-integration.md)
     - "for required <- @required" loop: assert String.contains?
     - "requires auth-state split language" test: assert String.contains?(content, "auth state") or String.contains?(content, "auth_state")
     - "requires reciprocal link to sigra auth integration guide" test: assert String.contains?(content, "sigra-auth-integration.md")

  The @sigra_blueprint_recipe attribute must be placed at module scope (not inside a describe block) — same position as @accrue_blueprint_recipe at line ~297.
  </action>

  <verify>
    <automated>mix test test/chimeway/doc_contract_test.exs --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>

  <done>mix test test/chimeway/doc_contract_test.exs --warnings-as-errors exits 0 with all tests passing including the new ECOS-10 describe block tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add sigra-auth-blueprint.md to mix.exs HexDocs extras (D-09)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs — read the docs/0 function; find the extras list; locate "guides/recipes/accrue-dunning-blueprint.md" and "guides/recipes/mailglass-integration-blueprint.md" entries (lines ~192–199); verify current ordering
    - .planning/phases/65-ecosystem-blueprints-demo/65-PATTERNS.md — Pattern 5: insert "guides/recipes/sigra-auth-blueprint.md" after mailglass-integration-blueprint.md entry
    - .planning/phases/65-ecosystem-blueprints-demo/65-RESEARCH.md — note that existing hexdocs extras contract test (lines ~851–901) checks only integration guides not recipe blueprints; the addition is for publishability + forward Phase 66 contract coverage
  </read_first>

  <action>
  In mix.exs docs/0 extras list, insert "guides/recipes/sigra-auth-blueprint.md" immediately after the "guides/recipes/mailglass-integration-blueprint.md" line. No other changes to mix.exs.
  </action>

  <verify>
    <automated>grep -c "sigra-auth-blueprint.md" mix.exs && mix test test/chimeway/doc_contract_test.exs --warnings-as-errors 2>&1 | tail -3</automated>
  </verify>

  <done>grep returns 1 for sigra-auth-blueprint.md in mix.exs. doc_contract_test.exs still passes (no regression from extras list change).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blueprint doc content | Static Markdown file; no runtime execution path |
| doc_contract assertions | Read-only file assertions in ExUnit — no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Information Disclosure | sigra-auth-blueprint.md code examples | mitigate | Blueprint code examples must show only identifier-only params (user_id, email, correlation_id); never raw_token or confirmation code verbatim values — enforced by ECOS-10 doc-contract refute assertions for "raw_token" and "raw token" |
| T-65-02 | Tampering | doc_contract_test.exs @required list drift | mitigate | Required strings in @required are derived from actual blueprint content (co-authored in same task); any mismatch causes immediate test failure |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No new package manager installs in Phase 65 — no slopcheck needed |
</threat_model>

<verification>
Full phase doc-contract gate:
```
mix test test/chimeway/doc_contract_test.exs --warnings-as-errors
```
Expected: All ECOS-10 tests pass (10 required string assertions + 3+ forbidden + 2 narrative tests).

HexDocs extras present:
```
grep "sigra-auth-blueprint.md" mix.exs
```
Expected: 1 match.

Blueprint required strings (spot check):
```
grep -l "Sigra.Integrations.Chimeway\|sigra.auth.magic_link\|sigra-auth-integration.md" guides/recipes/sigra-auth-blueprint.md
```
Expected: file listed.
</verification>

<success_criteria>
- guides/recipes/sigra-auth-blueprint.md exists, follows accrue-dunning-blueprint.md section structure (per D-01), contains all 10 D-08 required strings, contains no raw_token or recipe_forbidden_strings
- test/chimeway/doc_contract_test.exs has new describe "sigra auth blueprint recipe doc contract (ECOS-10)" block; mix test exits 0
- mix.exs extras list includes "guides/recipes/sigra-auth-blueprint.md"
- ECOS-10 requirement satisfied: published blueprint + CI doc-contract coverage
</success_criteria>

<output>
Create `.planning/phases/65-ecosystem-blueprints-demo/65-01-SUMMARY.md` when done.
</output>
