---
phase: 65-ecosystem-blueprints-demo
plan: 03
type: execute
wave: 2
depends_on:
  - 65-02
files_modified:
  - examples/chimeway_demo_host/test/demo_host_web/threadline_telemetry_proof_test.exs
  - examples/chimeway_demo_host/test/demo_host_web/sigra_auth_proof_test.exs
autonomous: true
requirements:
  - DEMO-09
  - DEMO-10

must_haves:
  truths:
    - "threadline_telemetry_proof_test.exs: DEMO-09 audit row test passes — Threadline audit_actions row with matching correlation_id is created after seed_threadline_notification/0"
    - "threadline_telemetry_proof_test.exs: DEMO-09 admin trace test passes — /admin/chimeway LiveView returns delivery detail for notification"
    - "sigra_auth_proof_test.exs: DEMO-10 delivery test passes — durable Chimeway.Delivery row with :succeeded or :dispatched status created"
    - "sigra_auth_proof_test.exs: DEMO-10 admin trace test passes — /admin/chimeway LiveView returns delivery detail for Sigra auth notification"
    - "Both files guarded by Code.ensure_loaded?/1 — compile to nothing when deps absent"
    - "mix test --only threadline exits 0 in demo host directory"
    - "mix test --only sigra exits 0 in demo host directory"
  artifacts:
    - path: "examples/chimeway_demo_host/test/demo_host_web/threadline_telemetry_proof_test.exs"
      provides: "DEMO-09 proof: Threadline audit correlation + operator trace"
      contains: "@moduletag :threadline"
    - path: "examples/chimeway_demo_host/test/demo_host_web/sigra_auth_proof_test.exs"
      provides: "DEMO-10 proof: Sigra auth delivery + operator trace"
      contains: "@moduletag :sigra"
  key_links:
    - from: "threadline_telemetry_proof_test.exs"
      to: "Threadline.Test.Repo"
      via: "Ecto.Adapters.SQL.Sandbox.checkout in setup"
      pattern: "Sandbox.checkout(ThreadlineRepo)"
    - from: "sigra_auth_proof_test.exs"
      to: "Sigra.TestRepo"
      via: "Ecto.Adapters.SQL.Sandbox.checkout in setup"
      pattern: "Sandbox.checkout(Sigra.TestRepo)"
    - from: "both proof tests"
      to: "/admin/chimeway LiveView"
      via: "get(conn, /admin/chimeway) + live(conn) + form(#trace-search-form)"
      pattern: "trace-search-form"
---

<objective>
Create the two demo host proof test files that satisfy DEMO-09 (Threadline) and DEMO-10 (Sigra) requirements.

Purpose: These tests prove the full end-to-end paths: Chimeway trigger → Threadline audit row (DEMO-09), Sigra auth dispatch → Chimeway delivery (DEMO-10), and both with operator trace inspectability at /admin/chimeway. They are the acceptance evidence for DEMO-09 and DEMO-10 requirements.

Output: (1) threadline_telemetry_proof_test.exs — two tests: audit row correlation + admin trace; (2) sigra_auth_proof_test.exs — two tests: delivery status + admin trace. Both guarded by Code.ensure_loaded? and tagged with @moduletag for selective CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/65-ecosystem-blueprints-demo/65-CONTEXT.md
@.planning/phases/65-ecosystem-blueprints-demo/65-RESEARCH.md
@.planning/phases/65-ecosystem-blueprints-demo/65-PATTERNS.md
@examples/chimeway_demo_host/test/demo_host_web/accrue_dunning_proof_test.exs
@examples/chimeway_demo_host/test/test_helper.exs
@examples/chimeway_demo_host/lib/demo_host/seeds.ex
@examples/chimeway_demo_host/test/support/conn_case.ex
@.planning/phases/65-ecosystem-blueprints-demo/65-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create threadline_telemetry_proof_test.exs (DEMO-09, D-03, D-04, D-05, D-06)</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/threadline_telemetry_proof_test.exs</files>
  <read_first>
    - examples/chimeway_demo_host/test/demo_host_web/accrue_dunning_proof_test.exs — full file; the structural template for module guard, use clauses, imports, setup, test bodies, and admin LiveView assertion (lines 95–124 are the exact /admin/chimeway assertion to adapt)
    - .planning/phases/65-ecosystem-blueprints-demo/65-PATTERNS.md — Pattern 3: Threadline-specific proof considerations; module guard form for Threadline; setup with ThreadlineRepo sandbox + attach_threadline_reporter! + configure_chimeway_logger_adapter! + on_exit detach; primary proof test: ThreadlineRepo.all from AuditAction where correlation_id matches; admin trace test: identical to accrue proof lines 95–124
    - .planning/phases/65-ecosystem-blueprints-demo/65-RESEARCH.md — Pitfall 5 (attach reporter in setup, NOT in seed helper); Pitfall 1 (test_helper.exs bootstrap — Plan 02 adds this); Pattern 3 full threadline proof code example; don't-hand-roll list for ThreadlineDataCase / attach_threadline_reporter!
    - test/support/threadline/fixtures.ex — verify attach_threadline_reporter!/0, detach_threadline_reporter!/0 function signatures; verify import or direct call form available to demo host (or check if must be inlined)
    - examples/chimeway_demo_host/test/support/conn_case.ex — check what helpers are imported; verify configure_chimeway_logger_adapter! is available
    - examples/chimeway_demo_host/lib/demo_host/seeds.ex — verify seed_threadline_notification/0 exists (written in Plan 02) and its return shape: {:ok, %{recipient_identity:, trace: %{delivery_ids:, correlation_id:}}}
    - .planning/phases/65-ecosystem-blueprints-demo/65-02-SUMMARY.md — confirm seed_threadline_notification return shape and test_helper bootstrap presence
  </read_first>

  <action>
  Create examples/chimeway_demo_host/test/demo_host_web/threadline_telemetry_proof_test.exs:

  Module guard: if Code.ensure_loaded?(Threadline) and Code.ensure_loaded?(Chimeway.Telemetry.ThreadlineReporter) do ... end

  Module header:
  - use DemoHostWeb.ConnCase, async: false
  - use Oban.Testing, repo: Chimeway.Repo
  - import Phoenix.LiveViewTest
  - import Ecto.Query
  - @moduletag :threadline
  - alias Threadline.Semantics.AuditAction (verify this is the correct module name from test/support/threadline/fixtures.ex or lifecycle test)
  - alias Threadline.Test.Repo, as: ThreadlineRepo

  For attach_threadline_reporter!/0 and detach_threadline_reporter!/0: read test/support/threadline/fixtures.ex to determine if these can be imported into the demo host test. If the module is accessible (Chimeway is a dep of demo host and test/support is in elixirc_paths), use import Chimeway.TestSupport.ThreadlineFixtures. If NOT accessible (most likely given demo host is a separate Mix project), inline the three-line body of attach_threadline_reporter! directly in setup (it is ~3 lines: :telemetry.attach with the reporter handler function). Read the fixture source to get the exact handler reference.

  configure_chimeway_logger_adapter!/0: check if available via ConnCase import or DemoHost.AccrueFixtures import pattern. If not, inline Application.put_env(:chimeway, :adapter, Chimeway.Adapters.Logger).

  Setup block:
  1. :ok = Ecto.Adapters.SQL.Sandbox.checkout(ThreadlineRepo)
  2. Ecto.Adapters.SQL.Sandbox.mode(ThreadlineRepo, {:shared, self()})
  3. ThreadlineRepo.delete_all(AuditAction)  — clear audit rows so assertions are clean
  4. attach_threadline_reporter!() (or inlined equivalent)
  5. configure_chimeway_logger_adapter!() (or inlined equivalent)
  6. on_exit(fn -> detach_threadline_reporter!() end)
  7. :ok

  Test 1 — "DEMO-09 threadline audit row created for notification lifecycle event":
  assert {:ok, result} = DemoHost.Seeds.seed_threadline_notification()
  rows = ThreadlineRepo.all(from(a in AuditAction, where: a.correlation_id == ^result.trace.correlation_id))
  assert length(rows) >= 1

  Test 2 — "DEMO-09 admin trace shows threadline notification" (conn: conn):
  Copy the admin LiveView search assertion verbatim from accrue_dunning_proof_test.exs lines 95–124, substituting seed_threadline_notification() for seed_accrue_dunning() and removing the workflow detail assertion (last two lines) since Threadline proof focuses on delivery trace, not workflow state.

  Key detail: Do NOT call Oban.drain_queue in these tests — seed_threadline_notification/0 uses the trigger/3 helper which calls Chimeway.trigger/3 with Sync dispatcher behavior, and ThreadlineReporter fires on the telemetry dispatch :stop event synchronously. Verify this assumption against lifecycle test behavior before writing final drain call.
  </action>

  <verify>
    <automated>cd /Users/jon/projects/chimeway/examples/chimeway_demo_host && mix test --only threadline --warnings-as-errors 2>&1 | tail -10</automated>
  </verify>

  <done>mix test --only threadline --warnings-as-errors exits 0 in the demo host. Both DEMO-09 tests pass: audit row created with correct correlation_id, admin trace returns delivery detail HTML.</done>
</task>

<task type="auto">
  <name>Task 2: Create sigra_auth_proof_test.exs (DEMO-10, D-03, D-04, D-05, D-06)</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/sigra_auth_proof_test.exs</files>
  <read_first>
    - examples/chimeway_demo_host/test/demo_host_web/accrue_dunning_proof_test.exs — full file; structural template (same as Threadline proof but with Sigra-specific setup)
    - .planning/phases/65-ecosystem-blueprints-demo/65-PATTERNS.md — Pattern 3: Sigra-specific proof considerations; module guard; setup with Sigra.TestRepo sandbox + Application.put_env for sigra enabled: true and repo + dispatcher config + configure_chimeway_logger_adapter! + on_exit cleanup; primary proof test: Chimeway.Repo.get!(Chimeway.Delivery, delivery_id) assert status in [:succeeded, :dispatched]; admin trace test: same LiveView assertion pattern
    - .planning/phases/65-ecosystem-blueprints-demo/65-RESEARCH.md — Pitfall 6 (configure_sigra_chimeway_integration! from root fixtures may not be importable — inline the setup directly); Pattern 3 sigra proof setup code; don't-hand-roll for Sigra DataCase
    - test/support/sigra/fixtures.ex — read to understand configure_sigra_chimeway_integration! body (Application.put_env calls); will be inlined in setup if not directly importable
    - examples/chimeway_demo_host/lib/demo_host/seeds.ex — verify seed_sigra_auth/0 exists (written in Plan 02) and its return shape: {:ok, %{recipient_identity:, trace: %{delivery_ids:}}}
    - .planning/phases/65-ecosystem-blueprints-demo/65-02-SUMMARY.md — confirm seed_sigra_auth return shape and test_helper bootstrap presence
    - examples/chimeway_demo_host/test/support/conn_case.ex — verify what imports are available (configure_chimeway_logger_adapter!)
  </read_first>

  <action>
  Create examples/chimeway_demo_host/test/demo_host_web/sigra_auth_proof_test.exs:

  Module guard: if Code.ensure_loaded?(Sigra) and Code.ensure_loaded?(Sigra.Integrations.Chimeway) do ... end

  Module header:
  - use DemoHostWeb.ConnCase, async: false
  - use Oban.Testing, repo: Chimeway.Repo
  - import Phoenix.LiveViewTest
  - @moduletag :sigra
  - alias Chimeway.{Delivery, Repo}

  Setup block — inline Sigra integration config (do NOT attempt to import Chimeway.TestSupport.SigraFixtures as it is likely unavailable in demo host scope per RESEARCH.md Pitfall 6):
  1. :ok = Ecto.Adapters.SQL.Sandbox.checkout(Sigra.TestRepo)
  2. Ecto.Adapters.SQL.Sandbox.mode(Sigra.TestRepo, {:shared, self()})
  3. previous_dispatcher = Application.get_env(:chimeway, :dispatcher)
  4. Application.put_env(:sigra, :chimeway, enabled: true)
  5. Application.put_env(:sigra, :repo, Sigra.TestRepo)
  6. Application.put_env(:chimeway, :dispatcher, Chimeway.Dispatch.Sync)
  7. configure_chimeway_logger_adapter!() (if available from ConnCase, else inline Application.put_env(:chimeway, :adapter, Chimeway.Adapters.Logger))
  8. on_exit(fn ->
       Application.put_env(:chimeway, :dispatcher, previous_dispatcher)
       Application.delete_env(:sigra, :chimeway)
       Application.delete_env(:sigra, :repo)
     end)
  9. :ok

  Test 1 — "DEMO-10 sigra auth creates durable delivery":
  assert {:ok, result} = DemoHost.Seeds.seed_sigra_auth()
  delivery_id = hd(result.trace.delivery_ids)
  delivery = Repo.get!(Delivery, delivery_id)
  assert delivery.status in [:succeeded, :dispatched]

  Test 2 — "DEMO-10 admin trace shows sigra auth notification" (conn: conn):
  Copy the admin LiveView assertion from accrue_dunning_proof_test.exs lines 95–124 verbatim, substituting seed_sigra_auth() for seed_accrue_dunning(). Adapt the final detail assertion to assert detail_html =~ "Trace detail" (no workflow-specific assertion).

  Key detail: Sigra proof uses Chimeway.Dispatch.Sync — no Oban.drain_queue needed before delivery assertion. The sync dispatcher writes the delivery row synchronously before returning. Verify this against sigra_auth_lifecycle_test.exs setup if uncertain.
  </action>

  <verify>
    <automated>cd /Users/jon/projects/chimeway/examples/chimeway_demo_host && mix test --only sigra --warnings-as-errors 2>&1 | tail -10</automated>
  </verify>

  <done>mix test --only sigra --warnings-as-errors exits 0 in the demo host. Both DEMO-10 tests pass: delivery row with :succeeded or :dispatched status, admin trace returns delivery detail HTML with "Trace detail".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sigra.TestRepo sandbox | Shared-mode sandbox; test process is owner; on_exit cleanup prevents cross-test contamination |
| Application env mutation in test setup | put_env + on_exit delete_env pattern; async: false prevents concurrent modification conflicts |
| /admin/chimeway LiveView | Protected by existing ChimewayAdmin auth (demo host configured with demo operator credentials in ConnCase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-05 | Tampering | Application.put_env in test setup | mitigate | on_exit cleanup restores previous values; async: false prevents concurrent mutation across tests |
| T-65-06 | Information Disclosure | Sigra raw_token in test assertions | mitigate | seed_sigra_auth/0 (Plan 02) discards raw_token before returning; proof test only accesses delivery_id and recipient_identity — never token values |
| T-65-07 | Tampering | ThreadlineRepo sandbox mode shared | mitigate | Sandbox.mode(:shared, self()) + ThreadlineRepo.delete_all(AuditAction) in setup ensures each test starts with clean audit table; audit row count assertion is bounded by the single test's trigger |
| T-65-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in Plan 03 |
</threat_model>

<verification>
Threadline proof gate:
```
cd /Users/jon/projects/chimeway/examples/chimeway_demo_host && mix test --only threadline --warnings-as-errors
```
Expected: 2 tests pass (DEMO-09 audit row + DEMO-09 admin trace).

Sigra proof gate:
```
cd /Users/jon/projects/chimeway/examples/chimeway_demo_host && mix test --only sigra --warnings-as-errors
```
Expected: 2 tests pass (DEMO-10 delivery + DEMO-10 admin trace).

Default suite regression check (no new failures in untagged tests):
```
cd /Users/jon/projects/chimeway/examples/chimeway_demo_host && mix test --exclude threadline --exclude sigra --exclude accrue --exclude inbox --warnings-as-errors
```
Expected: 0 failures.

Root doc-contract still passes (not regressed by Plan 01 additions):
```
cd /Users/jon/projects/chimeway && mix test test/chimeway/doc_contract_test.exs --warnings-as-errors
```
Expected: all tests pass including ECOS-10 block.
</verification>

<success_criteria>
- threadline_telemetry_proof_test.exs: Code.ensure_loaded? guard, @moduletag :threadline, 2 tests passing (audit row correlation_id match, /admin/chimeway delivery detail visible)
- sigra_auth_proof_test.exs: Code.ensure_loaded? guard, @moduletag :sigra, 2 tests passing (delivery :succeeded or :dispatched, /admin/chimeway delivery detail visible)
- mix test --only threadline exits 0 in demo host
- mix test --only sigra exits 0 in demo host
- DEMO-09 and DEMO-10 requirements fully satisfied
- No regression in existing demo host tests
</success_criteria>

<output>
Create `.planning/phases/65-ecosystem-blueprints-demo/65-03-SUMMARY.md` when done.
</output>
