---
phase: 67-close-ecos-09-repin-sigra-ci-sha-harden-verify-lanes-against
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - test/test_helper.exs
  - test/chimeway/integrations/sigra_auth_harness_test.exs
  - test/chimeway/integrations/accrue_dunning_harness_test.exs
  - test/chimeway/integrations/threadline_telemetry_harness_test.exs
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
requirements: [ECOS-09, GATE-07]
must_haves:
  truths:
    - "D-01: verify_sigra CI pins a szTheory/sigra SHA that contains lib/sigra/integrations/chimeway.ex (62ceb46a, remote-verified to contain blob 3e2e2437)"
    - "D-02a: when Sigra (or Accrue) is loaded but its integration source file is absent on disk, test_helper.exs raises loud instead of silently skipping"
    - "D-02b sigra (IN): sigra harness hard-asserts Sigra.Integrations.Chimeway loads and exports dispatch_magic_link_after_request/3 (verified export; trigger/3 does NOT exist on this module)"
    - "D-02b accrue (IN): accrue harness goes RED (not skip) when Accrue.Integrations.Chimeway is absent; asserts start_campaign/3"
    - "D-02b threadline (OUT): threadline harness hard-asserts Chimeway.Telemetry.ThreadlineReporter loads and exports attach/0, plus Threadline.record_action/2 — NOT a partner integration module"
    - "D-02c: release_gate_contract_test floor-asserts each lane declares >= its integration test count (sigra root >=5, sigra demo >=2, accrue >=11, threadline >=7) using >= not =="
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "verify_sigra sibling checkout pinned to integration-bearing SHA"
      contains: "62ceb46a38c4e617f6c06d874ecb12e1ab19d97c"
    - path: "test/test_helper.exs"
      provides: "raise-loud-on-missing-integration-file for sigra + accrue conditional-compile blocks"
    - path: "test/chimeway/integrations/sigra_auth_harness_test.exs"
      provides: "module-load + verified-export hard-assert outside lifecycle guard"
    - path: "test/chimeway/integrations/accrue_dunning_harness_test.exs"
      provides: "integration-module check moved out of outer guard"
    - path: "test/chimeway/integrations/threadline_telemetry_harness_test.exs"
      provides: "OUT-direction reporter hard-assert"
    - path: "test/chimeway/release_gate_contract_test.exs"
      provides: "per-lane test-count floor describe block"
  key_links:
    - from: ".github/workflows/ci.yml verify_sigra"
      to: "szTheory/sigra@62ceb46a lib/sigra/integrations/chimeway.ex"
      via: "actions/checkout ref"
      pattern: "ref: 62ceb46a"
    - from: "test/test_helper.exs sigra block"
      to: "Sigra.Integrations.Chimeway compile/load"
      via: "else-raise on missing file"
      pattern: "raise.*Sigra.Integrations.Chimeway"
---

<objective>
Close the root-cause defect and make the vacuous-pass gate footgun structurally impossible.

This plan lands D-01 (the one-line CI repin that is the entire root-cause fix) and D-02a/b/c (the symmetric guard hardening across the sigra, accrue, and threadline lanes). After this plan, the sigra verify lane stops passing vacuously: the sibling checkout is non-empty, the lifecycle test compiles real tests, and any future regression to an empty/absent integration turns RED rather than silently green.

Purpose: ECOS-09 (Sigra auth notification flow proven against the real partner module, not a local gitignored copy) and GATE-07 (`mix verify.sigra` runs >0 integration tests and fails loud).
Output: Edited ci.yml, test_helper.exs, three harness tests, and release_gate_contract_test.exs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-close-ecos-09-repin-sigra-ci-sha-harden-verify-lanes-against/67-CONTEXT.md
@.planning/phases/67-close-ecos-09-repin-sigra-ci-sha-harden-verify-lanes-against/67-RESEARCH.md

<interfaces>
<!-- Verified during planning against deps/sigra and the live remote. Use these directly. -->

VERIFIED public exports of Sigra.Integrations.Chimeway (grepped from deps/sigra/lib/sigra/integrations/chimeway.ex):
  enabled?/0, repo/0,
  dispatch_magic_link/5,
  dispatch_magic_link_after_request/3,   <-- assert THIS in the sigra harness (exercised by the lifecycle test)
  dispatch_confirmation_code/6,
  dispatch_confirmation_after_generate/3
  (NOTE: there is NO trigger/3 on this module. Chimeway.trigger/3 lives on the Chimeway module, not here. Do NOT assert trigger/3.)
  Nested notifier modules: Sigra.Integrations.Chimeway.MagicLinkNotifier, .ConfirmationCodeNotifier, .PendingDelivery

VERIFIED remote (git fetch szTheory/sigra 62ceb46a... && git ls-tree): SHA 62ceb46a38c4e617f6c06d874ecb12e1ab19d97c contains lib/sigra/integrations/chimeway.ex (blob 3e2e2437). The stale pin b186f03c == tag v0.3.0 (pre-integration).

Accrue harness today (DOUBLE-guarded, silently skips when integration absent):
  Line 1: if Code.ensure_loaded?(Accrue) and Code.ensure_loaded?(Accrue.Integrations.Chimeway) do
  Verified export: Accrue.Integrations.Chimeway.start_campaign/3

Threadline harness today (single-guarded on Threadline):
  Line 1: if Code.ensure_loaded?(Threadline) do
  OUT direction — chimeway owns lib/chimeway/telemetry/threadline_reporter.ex. Verified export: Chimeway.Telemetry.ThreadlineReporter.attach/0, plus partner fn Threadline.record_action/2.

VERIFIED test counts (grep, comments filtered): sigra_auth_harness_test 3 + sigra_auth_lifecycle_test 2 = 5 (root lane); accrue_dunning_harness_test 4 + accrue_dunning_lifecycle_test 7 = 11; threadline_telemetry_harness_test 3 + threadline_telemetry_lifecycle_test 4 = 7; demo-host sigra_auth_proof_test 2.

ci.yml coordinates: verify_sigra job @383; sigra sibling checkout @404-408 (stale ref at line 407); accrue precedent ref @275 (236fa2f1). test_helper.exs: accrue block silent-skip if @43 (no else); sigra block silent-skip if @160 (no else).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repin the Sigra CI SHA and add raise-loud-on-missing-file (D-01 + D-02a)</name>
  <files>.github/workflows/ci.yml, test/test_helper.exs</files>
  <read_first>
    - .github/workflows/ci.yml (the verify_sigra block @404-408, accrue precedent @272-276)
    - test/test_helper.exs (sigra block @140-167, accrue block @35-50)
    - 67-RESEARCH.md Coordinate 1, Coordinate 2, and the "raise-loud-on-missing-file (D-02a)" pattern example
  </read_first>
  <action>
    In .github/workflows/ci.yml line 407, change the verify_sigra sibling checkout from `ref: b186f03ccc5bbc9416f495df3e5dd0bec2f814a4` to `ref: 62ceb46a38c4e617f6c06d874ecb12e1ab19d97c` (per D-01; this SHA was remote-verified during planning to contain lib/sigra/integrations/chimeway.ex). Change nothing else in the block (repository, path, SIGRA_PATH stay).

    In test/test_helper.exs, harden BOTH IN-direction conditional-compile blocks (D-02a):
    - Sigra block: the `if is_binary(source) and File.exists?(source) do ... end` at lines 160-166 currently has no else, so an absent file silently skips. Add an `else` that raises with a message naming Sigra.Integrations.Chimeway, the resolved source path, and pointing at the verify_sigra pin (e.g. "Sigra is loaded but its integration source is missing — verify_sigra is pinned to a SHA without lib/sigra/integrations/chimeway.ex"). This branch is reached only inside the outer `Code.ensure_loaded?(Sigra) and Code.ensure_loaded?(Chimeway) and not Code.ensure_loaded?(Sigra.Integrations.Chimeway)` guard, so it fires only in the exact vacuous-pass scenario (partner present, integration file absent) — not when the optional dep is simply unloaded.
    - Accrue block: mirror the same change on the `if File.exists?(source) do ... end` at lines 43-49 — add an `else` that raises naming Accrue.Integrations.Chimeway.

    Do NOT touch the @sensitive_keys redaction boundary (out of scope). Do NOT vendor any sigra source.
  </action>
  <verify>
    <automated>grep -q "ref: 62ceb46a38c4e617f6c06d874ecb12e1ab19d97c" .github/workflows/ci.yml && ! grep -q "b186f03ccc5bbc9416f495df3e5dd0bec2f814a4" .github/workflows/ci.yml && grep -c "raise" test/test_helper.exs</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `ref: 62ceb46a38c4e617f6c06d874ecb12e1ab19d97c` exactly once and no longer contains `b186f03ccc5bbc9416f495df3e5dd0bec2f814a4`.
    - `test/test_helper.exs` sigra block has an `else` branch that raises a message containing the substring `Sigra.Integrations.Chimeway`.
    - `test/test_helper.exs` accrue block has an `else` branch that raises a message containing the substring `Accrue.Integrations.Chimeway`.
    - `SIGRA_PATH=../sigra/sigra mix test --only sigra --warnings-as-errors` boots test_helper without raising (the integration file is present locally), exits 0.
  </acceptance_criteria>
  <done>CI sigra pin updated to the integration-bearing SHA; both IN-direction test_helper blocks raise loud when the partner is loaded but its integration file is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the three harness guards so an absent integration goes RED (D-02b)</name>
  <files>test/chimeway/integrations/sigra_auth_harness_test.exs, test/chimeway/integrations/accrue_dunning_harness_test.exs, test/chimeway/integrations/threadline_telemetry_harness_test.exs</files>
  <read_first>
    - test/chimeway/integrations/sigra_auth_harness_test.exs (single-guarded on Sigra; only asserts Sigra.Auth today)
    - test/chimeway/integrations/accrue_dunning_harness_test.exs (DOUBLE-guarded line 1; asserts start_campaign/3 @27)
    - test/chimeway/integrations/threadline_telemetry_harness_test.exs (single-guarded on Threadline; OUT direction)
    - 67-RESEARCH.md Coordinate 3 + Pitfalls 1-2 (per-direction symbols differ — do NOT mirror trigger/3)
    - deps/sigra/lib/sigra/integrations/chimeway.ex (confirm the export before asserting; planning already verified dispatch_magic_link_after_request/3 exists and trigger/3 does NOT)
  </read_first>
  <action>
    Make each lane fail loud when its integration glue is missing. The principle mirrors across all three; the exact symbol differs per integration direction (do NOT literally mirror trigger/3 — Pitfall 1/2).

    Sigra (IN) — test/chimeway/integrations/sigra_auth_harness_test.exs: add a test inside the existing describe (or a new test) that hard-asserts `Code.ensure_loaded?(Sigra.Integrations.Chimeway)` AND `function_exported?(Sigra.Integrations.Chimeway, :dispatch_magic_link_after_request, 3)`. Use dispatch_magic_link_after_request/3 — it is the VERIFIED public export exercised by sigra_auth_lifecycle_test (trigger/3 does not exist on this module). The outer guard at line 1 stays `if Code.ensure_loaded?(Sigra) do`; the new assertion is what turns the lane RED when Sigra is present but the integration module is absent — closing the vacuous footgun where the harness only checked Sigra.Auth.

    Accrue (IN) — test/chimeway/integrations/accrue_dunning_harness_test.exs: move the `Code.ensure_loaded?(Accrue.Integrations.Chimeway)` check OUT of the outer `and` guard on line 1 (change it to `if Code.ensure_loaded?(Accrue) do`) so the suite runs whenever Accrue is present, then add/keep a hard-assert `function_exported?(Accrue.Integrations.Chimeway, :start_campaign, 3)` that goes RED (not skip) when the integration module is absent. Preserve the existing alias `ChimewayDunningEngine = Accrue.Integrations.Chimeway` and the existing dunning tests; guard the body tests that need the integration module so only the load/export assertion is the loud canary while the heavier dunning tests still tolerate a clean-but-incomplete checkout if needed. (start_campaign/3 is its verified seam fn — Accrue is a dunning engine, not a notifier.)

    Threadline (OUT) — test/chimeway/integrations/threadline_telemetry_harness_test.exs: there is NO Threadline.Integrations.Chimeway. Add a test that hard-asserts `Code.ensure_loaded?(Chimeway.Telemetry.ThreadlineReporter)` AND `function_exported?(Chimeway.Telemetry.ThreadlineReporter, :attach, 0)` (chimeway-owned reporter), keeping the existing `function_exported?(Threadline, :record_action, 2)` assertion (the partner fn the reporter calls). Do NOT add a `function_exported?(Threadline.Integrations.Chimeway, ...)` check — it would fail to compile.
  </action>
  <verify>
    <automated>SIGRA_PATH=../sigra/sigra mix test test/chimeway/integrations/sigra_auth_harness_test.exs --only sigra --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - sigra_auth_harness_test.exs contains `function_exported?(Sigra.Integrations.Chimeway, :dispatch_magic_link_after_request, 3)` and does NOT contain `:trigger, 3`.
    - accrue_dunning_harness_test.exs line 1 guard is `if Code.ensure_loaded?(Accrue) do` (the `and Code.ensure_loaded?(Accrue.Integrations.Chimeway)` is removed from the outer guard), and the file asserts `function_exported?(Accrue.Integrations.Chimeway, :start_campaign, 3)`.
    - threadline_telemetry_harness_test.exs asserts `Code.ensure_loaded?(Chimeway.Telemetry.ThreadlineReporter)` and `function_exported?(Chimeway.Telemetry.ThreadlineReporter, :attach, 0)`, and contains no reference to `Threadline.Integrations.Chimeway`.
    - `SIGRA_PATH=../sigra/sigra mix test test/chimeway/integrations/sigra_auth_harness_test.exs --only sigra --warnings-as-errors` exits 0 (integration module present locally).
  </acceptance_criteria>
  <done>All three harnesses hard-assert their integration glue loads with a verified export; absence of the glue turns the lane RED (sigra/accrue) rather than green/skipped, with the OUT-direction threadline asserting the chimeway-owned reporter.</done>
</task>

<task type="auto">
  <name>Task 3: Add per-lane test-count floor assertions (D-02c)</name>
  <files>test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    - test/chimeway/release_gate_contract_test.exs (existing describe blocks @24 and @121; @pre_ship_verify_commands @14-22; this is a fast async static-contract test)
    - test/chimeway/integrations/ (the six tagged lane files whose `test "` declarations are counted)
    - 67-RESEARCH.md Coordinate 4 (verified counts + the two-lane sigra caveat) and Pitfall 3
  </read_first>
  <action>
    Add a new `describe "verify-lane test-count floor (WARNING-2)"` block to test/chimeway/release_gate_contract_test.exs. Since this file is a fast static-contract test (it cannot run the lanes), implement the floor as a static count of `test "` declarations per lane source file (regex over file contents), asserting `>=` the verified floor — NOT `==` (D-02c / Pitfall: use >= so legitimate additions do not break the gate; A4).

    Floor each lane against its verified current counts:
    - sigra ROOT lane: sigra_auth_harness_test.exs (3) + sigra_auth_lifecycle_test.exs (2) => assert >= 5 combined.
    - sigra DEMO-HOST proof lane (separate mix project): examples/chimeway_demo_host/test/demo_host_web/sigra_auth_proof_test.exs (2) => assert >= 2. Floor this SEPARATELY from the root lane (Pitfall 3 — verify.sigra is two mix invocations; do not roll 7 into one floor).
    - accrue: accrue_dunning_harness_test.exs (4) + accrue_dunning_lifecycle_test.exs (7) => assert >= 11.
    - threadline: threadline_telemetry_harness_test.exs (3) + threadline_telemetry_lifecycle_test.exs (4) => assert >= 7.

    Count by matching lines that begin (after whitespace) with `test "` and excluding comment lines, so the count is not self-invalidated by prose. Read each file via File.read! and count matches of `~r/^\s*test "/m`.

    This is defense-in-depth complementing the harness module-load hard-assert from Task 2 (which catches "compiled out to 0 because the integration was absent"); the static floor catches "lane file silently lost its tests".
  </action>
  <verify>
    <automated>mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - release_gate_contract_test.exs contains a describe block whose name includes `floor`.
    - The block asserts sigra root >= 5, sigra demo-host proof >= 2 (as a separate assertion), accrue >= 11, threadline >= 7, all using `>=` (no `==` count equality on a test-count floor).
    - `mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors` exits 0 against current HEAD counts.
    - The floor block references the demo-host proof file path examples/chimeway_demo_host/test/demo_host_web/sigra_auth_proof_test.exs distinctly from the root sigra lane files.
  </acceptance_criteria>
  <done>release_gate_contract_test floor-asserts each verify lane declares at least its expected integration-test count (>=), with the sigra root and demo-host lanes floored separately, so a regression to 0 tests fails the gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chimeway CI -> szTheory/sigra (sibling checkout) | chimeway pins a partner-repo SHA; the integration code crosses this boundary into the verify_sigra job |
| optional dep load -> test harness | the conditional-compile blocks load partner integration source at test boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Tampering / Supply-chain | ci.yml verify_sigra pinned SHA | mitigate | Pin the exact SHA 62ceb46a (immutable commit, not a mutable branch/tag). Planning remote-verified via `git fetch szTheory/sigra 62ceb46a && git ls-tree` that this SHA contains lib/sigra/integrations/chimeway.ex (blob 3e2e2437). Task 1 acceptance asserts the pin; Task 2 harness + Task 3 floor make a wrong/empty SHA go RED instead of vacuous-green. |
| T-67-02 | Spoofing / False-green (vacuous gate) | verify_sigra lane passing while proving nothing | mitigate | D-02a raise-loud on missing file + D-02b harness hard-assert (module load + verified export) + D-02c >= test-count floor. Three independent canaries; absence of the integration now turns RED at boot, in the harness, and in the floor. |
| T-67-03 | Tampering | accrue/threadline lanes hiding the same footgun class | mitigate | Symmetric hardening: accrue integration check moved out of the silent-skip outer guard; threadline asserts the chimeway-owned OUT-direction reporter. |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan; sigra is an existing optional Hex/path dep already in mix.exs (no new package). The pinned partner SHA is the only external code reference and is mitigated by T-67-01. |
| T-67-04 | Information disclosure | @sensitive_keys redaction boundary | accept | Verified-good and explicitly OUT of scope; this plan does not modify trigger.ex. Existing mitigation retained, not new work. |
</threat_model>

<verification>
- `SIGRA_PATH=../sigra/sigra mix test --only sigra --warnings-as-errors` exits 0 (root lane, ~5 tests).
- `mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors` exits 0.
- `grep "ref: 62ceb46a" .github/workflows/ci.yml` matches; `grep b186f03c .github/workflows/ci.yml` matches nothing.
</verification>

<success_criteria>
- ci.yml verify_sigra pins 62ceb46a (integration-bearing, remote-verified).
- test_helper.exs sigra + accrue blocks raise loud on missing integration file.
- All three harnesses hard-assert their integration glue with a verified export; absence goes RED.
- release_gate_contract_test floors sigra root >=5, sigra demo >=2, accrue >=11, threadline >=7 (>= not ==).
</success_criteria>

<output>
Create `.planning/phases/67-close-ecos-09-repin-sigra-ci-sha-harden-verify-lanes-against/67-01-SUMMARY.md` when done
</output>
