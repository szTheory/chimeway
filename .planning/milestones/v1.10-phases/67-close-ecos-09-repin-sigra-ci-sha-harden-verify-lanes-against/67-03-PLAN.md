---
phase: 67-close-ecos-09-repin-sigra-ci-sha-harden-verify-lanes-against
plan: 03
type: execute
wave: 2
depends_on: ["67-01", "67-02"]
files_modified:
  - .planning/phases/64-sigra-auth-flows-core/64-VERIFICATION.md
  - .planning/phases/64-sigra-auth-flows-core/64-02-SUMMARY.md
  - .planning/phases/64-sigra-auth-flows-core/64-VALIDATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - mix.exs
autonomous: false
requirements: [ECOS-09, DEMO-10]
must_haves:
  truths:
    - "D-05: mix verify.sigra runs green under clean-CI conditions (root --only sigra lane + demo-host proof lane), proving ECOS-09 E2E against the repinned partner module — not the local gitignored deps/sigra"
    - "D-05: 64-VERIFICATION.md exists and documents the ECOS-09 E2E proof (redacted-trace assertions, both dispatch flows, demo-host proof)"
    - "D-05: 64-02-SUMMARY.md is committed to git (was untracked)"
    - "D-05: ROADMAP flips 64-02 to [x] and Phase 64 to Complete; REQUIREMENTS/Traceability flips ECOS-09 to [x]/Satisfied"
    - "D-06: 64-VALIDATION.md frontmatter flipped status draft->closed and nyquist_compliant false->true, consistent with the new VERIFICATION"
    - "D-06: mix.exs sigra_dep has a one-line comment explaining override: true (ecto 3.12-vs-3.11 diamond; inert for adopters since optional deps are not pulled transitively)"
  artifacts:
    - path: ".planning/phases/64-sigra-auth-flows-core/64-VERIFICATION.md"
      provides: "ECOS-09 E2E verification record"
      min_lines: 30
    - path: ".planning/phases/64-sigra-auth-flows-core/64-02-SUMMARY.md"
      provides: "Phase 64 plan-02 summary (now git-tracked)"
    - path: ".planning/phases/64-sigra-auth-flows-core/64-VALIDATION.md"
      provides: "Nyquist contract closed out"
      contains: "nyquist_compliant: true"
    - path: "mix.exs"
      provides: "override: true rationale comment on sigra_dep"
  key_links:
    - from: "mix verify.sigra"
      to: "szTheory/sigra@62ceb46a Sigra.Integrations.Chimeway"
      via: "clean-CI sibling checkout (no local deps/sigra)"
      pattern: "verify.sigra"
    - from: ".planning/REQUIREMENTS.md ECOS-09"
      to: "64-VERIFICATION.md"
      via: "Satisfied flip backed by the E2E proof"
      pattern: "ECOS-09"
---

<objective>
Verify ECOS-09 end-to-end under clean-CI conditions and reconcile all tracking + Nyquist debt.

This plan lands D-05 (run `mix verify.sigra` cleanly against the repinned partner SHA, produce 64-VERIFICATION.md, commit the untracked 64-02-SUMMARY.md, flip ROADMAP + REQUIREMENTS to Complete/Satisfied) and D-06 (close out the Phase 64 Nyquist VALIDATION contract and add the accurate `override: true` rationale comment to mix.exs). It runs LAST because the clean-CI verify depends on Plans 01 (repin + guards) and 02 (guide/doc-contract) being green.

Purpose: ECOS-09 (proven E2E in clean CI, tracking reconciled) and Nyquist-debt closeout.
Output: New 64-VERIFICATION.md; committed 64-02-SUMMARY.md; updated 64-VALIDATION.md, ROADMAP.md, REQUIREMENTS.md, mix.exs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-close-ecos-09-repin-sigra-ci-sha-harden-verify-lanes-against/67-CONTEXT.md
@.planning/phases/67-close-ecos-09-repin-sigra-ci-sha-harden-verify-lanes-against/67-RESEARCH.md
@.planning/phases/64-sigra-auth-flows-core/64-VALIDATION.md
@.planning/phases/64-sigra-auth-flows-core/64-02-SUMMARY.md

<interfaces>
<!-- Verified during planning. Use directly. -->

mix verify.sigra (mix.exs:134-137) runs THREE steps:
  1. deps.compile sigra --force
  2. root lane: cmd env MIX_ENV=test mix test --only sigra --warnings-as-errors  (harness 3 + lifecycle 2 = 5 tests; lifecycle is the ECOS-09 redacted-trace proof: dispatch_magic_link_after_request/3 + dispatch_confirmation_after_generate/3, asserting Traces.get_trace, length(deliveries)>=1, refute_sensitive_in_trace!, refute_sensitive_in_telemetry!)
  3. demo-host lane: cd examples/chimeway_demo_host && CHIMEWAY_SKIP_SIGRA_DEP=1 SIGRA_PATH=../../../sigra/sigra CHIMEWAY_PATH=../.. mix deps.get && deps.compile sigra --force && mix test --only sigra --warnings-as-errors  (sigra_auth_proof_test 2 tests, DEMO-10)

sigra_dep (mix.exs:174-180): {:sigra, "~> 0.3", optional: true, runtime: false, override: true} (hex) / path variant. override: true is UNIQUE to sigra (accrue_dep @142-148 and threadline_dep @158-164 lack it).
A2 RESOLVED during planning: sigra's mix.exs requires {:ecto, "~> 3.12"} / {:ecto_sql, "~> 3.12"}; chimeway pins {:ecto_sql, "~> 3.11"}. override: true makes chimeway's 3.11 constraint win the diamond. Inert for adopters because :sigra is optional+runtime:false and optional deps are not pulled transitively into adopter apps.

Phase 64 tracking state (verified via git ls-files): 64-02-SUMMARY.md is present on disk (2353 bytes) but UNTRACKED — D-05 must git add it. No 64-VERIFICATION.md exists yet — D-05 produces it. 64-VALIDATION.md frontmatter is status: draft / nyquist_compliant: false / wave_0_complete: false — D-06 flips it.

ROADMAP coordinates: Phase 64 line "- [ ] **Phase 64: Sigra Auth Flows Core**" @29; 64-02-PLAN.md plan-list line @86; Traceability ECOS-09 row @157 ("| ECOS-09 | 64 | ... |"); status table "| 67 Close ECOS-09 | 0/? | Not planned |" @150. REQUIREMENTS: ECOS-09 "- [ ] **ECOS-09**" @15; Traceability "| ECOS-09 | Phase 64 | Pending |" @59.

CLEAN-CI caveat (A3): the local gitignored deps/sigra masked the gap. The authoritative clean-CI proof is the verify_sigra GitHub Actions job running with the sibling checkout (no local deps/sigra). A local `mix verify.sigra` run that uses SIGRA_PATH=../sigra/sigra is a strong pre-check but the executor MUST treat the pushed-branch verify_sigra job as the binding evidence (human checkpoint below). The pinned SHA 62ceb46a was remote-verified during planning to contain lib/sigra/integrations/chimeway.ex.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run clean verify.sigra, produce 64-VERIFICATION.md, commit summary, close Nyquist, add override comment (D-05 + D-06)</name>
  <files>.planning/phases/64-sigra-auth-flows-core/64-VERIFICATION.md, .planning/phases/64-sigra-auth-flows-core/64-02-SUMMARY.md, .planning/phases/64-sigra-auth-flows-core/64-VALIDATION.md, mix.exs</files>
  <read_first>
    - .planning/phases/64-sigra-auth-flows-core/64-02-SUMMARY.md (the work being verified)
    - .planning/phases/64-sigra-auth-flows-core/64-VALIDATION.md (the Nyquist contract to close)
    - mix.exs lines 134-137 (verify.sigra) and 174-180 (sigra_dep override: true)
    - 67-RESEARCH.md Coordinate 8 (what verify.sigra runs) + Coordinate 9 (override rationale, A2)
    - test/chimeway/integrations/sigra_auth_lifecycle_test.exs (the redacted-trace assertions to cite in VERIFICATION)
  </read_first>
  <action>
    Run `SIGRA_PATH=../sigra/sigra mix verify.sigra` locally as a pre-check (root --only sigra lane + demo-host proof lane). Capture test counts and pass/fail. This exercises the ECOS-09 lifecycle proof (magic-link + confirmation dispatch creating durable deliveries with redacted traces) and the DEMO-10 demo-host proof. If it fails, fix-forward only within the scope of Plans 01/02 patterns (do NOT vendor, do NOT touch @sensitive_keys); a true clean-CI failure requiring partner-side changes is a stop condition to surface.

    Produce .planning/phases/64-sigra-auth-flows-core/64-VERIFICATION.md documenting the ECOS-09 E2E proof: the verify.sigra command, both lanes and their test counts (root >=5, demo >=2), the specific redacted-trace assertions (refute_sensitive_in_trace!, refute_sensitive_in_telemetry!) from sigra_auth_lifecycle_test, the demo-host operator-trace proof, and the binding clean-CI evidence (the pinned SHA 62ceb46a + the verify_sigra job). State that the proof is now against the repinned partner module, not the local gitignored deps/sigra.

    git add the untracked .planning/phases/64-sigra-auth-flows-core/64-02-SUMMARY.md so it is tracked.

    Close the Phase 64 Nyquist debt (D-06): in 64-VALIDATION.md flip frontmatter `status: draft` -> `status: closed` and `nyquist_compliant: false` -> `nyquist_compliant: true`, set `wave_0_complete: true`, and update the per-task rows to reflect the green state, consistent with the new VERIFICATION. (Per CONTEXT, Phase 64 is the in-scope Nyquist minimum; Phases 63/65/66 VALIDATION closeout is explicitly deferred — do NOT touch them.)

    Add the override: true rationale comment to mix.exs sigra_dep (lines 174-180). The comment must be accurate (A2 resolved during planning): sigra requires ecto/ecto_sql ~> 3.12 while chimeway pins ecto_sql ~> 3.11; `override: true` resolves that diamond in chimeway's favor for the test/dev sibling checkout, and is inert for adopters because :sigra is optional + runtime:false (optional deps are not pulled transitively). One line, factual — do not guess.
  </action>
  <verify>
    <automated>SIGRA_PATH=../sigra/sigra mix verify.sigra; test $? -eq 0 && test -f .planning/phases/64-sigra-auth-flows-core/64-VERIFICATION.md && git ls-files --error-unmatch .planning/phases/64-sigra-auth-flows-core/64-02-SUMMARY.md && grep -q "nyquist_compliant: true" .planning/phases/64-sigra-auth-flows-core/64-VALIDATION.md && grep -A6 "defp sigra_dep" mix.exs | grep -qiE "override|diamond|optional|transitive"</automated>
  </verify>
  <acceptance_criteria>
    - `SIGRA_PATH=../sigra/sigra mix verify.sigra` exits 0 (both lanes), or any failure is surfaced as a stop condition with the failing output.
    - `.planning/phases/64-sigra-auth-flows-core/64-VERIFICATION.md` exists, is >= 30 lines, names the verify.sigra lanes + counts and the refute_sensitive_in_trace!/refute_sensitive_in_telemetry! assertions, and cites the pinned SHA 62ceb46a.
    - `git ls-files --error-unmatch .planning/phases/64-sigra-auth-flows-core/64-02-SUMMARY.md` succeeds (file is tracked).
    - `64-VALIDATION.md` frontmatter contains `nyquist_compliant: true` and `status: closed` (not draft); 63/65/66 VALIDATION files are untouched.
    - `mix.exs` sigra_dep has a comment within 6 lines of `defp sigra_dep` mentioning the override/diamond/optional-transitive rationale.
  </acceptance_criteria>
  <done>verify.sigra is green against the repinned partner module; 64-VERIFICATION.md proves ECOS-09 E2E; 64-02-SUMMARY.md is tracked; the Phase 64 Nyquist contract is closed; mix.exs documents the override.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile ROADMAP + REQUIREMENTS (D-05)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 64 line @29, 64-02 plan-list @86, Traceability ECOS-09 @157, status table @150)
    - .planning/REQUIREMENTS.md (ECOS-09 @15, Traceability @59)
    - the 64-VERIFICATION.md produced in Task 1 (the evidence backing the flip)
  </read_first>
  <action>
    Reconcile tracking now that ECOS-09 is proven (D-05). Only flip after Task 1's verification is green — these are the evidence-backed status updates.

    In .planning/ROADMAP.md:
    - Phase 64 header line @29: `- [ ]` -> `- [x]` and mark Phase 64 Complete (matching the repo's completion convention, e.g. an "(completed 2026-06-...)" suffix like Phase 66 uses).
    - 64-02-PLAN.md plan-list line @86: `- [ ]` -> `- [x]`.
    - Traceability table ECOS-09 row: reflect Satisfied/Complete (consistent with REQUIREMENTS).
    - Optionally update the Phase 67 status-table row from "Not planned" to reflect the plan count (3 plans) — keep consistent with however execute-phase tracks it.
    - Reconcile Phase 67 Success Criterion 4 (ROADMAP:182) to match LOCKED D-06 (Phase 64 is the in-scope Nyquist minimum; 63/65/66 are explicitly deferred). Rewrite the trailing clause "the four draft `*-VALIDATION.md` Nyquist contracts closed out" to: "Phase 64 `64-VALIDATION.md` closed out (`nyquist_compliant: true`); Phases 63/65/66 VALIDATION drafts tracked as deferred Nyquist debt (owner: next milestone planning; trigger: their respective phase re-verification)". This makes the post-execution gsd-verifier read of criterion 4 honest rather than a false failure. Do NOT change criteria 1-3.

    In .planning/REQUIREMENTS.md:
    - ECOS-09 @15: `- [ ]` -> `- [x]`.
    - Traceability row @59: `| ECOS-09 | Phase 64 | Pending |` -> `| ECOS-09 | Phase 64 | Satisfied |` (use the repo's standard term — match how ECOS-08/ECOS-10 rows read, e.g. "Complete"/"Satisfied").

    Do not alter unrelated requirement rows. Keep DEMO-10/DOCS-10/GATE-07 consistent — they were partial and are restored by Plans 01/02; if their rows note "partial", update to satisfied with the same evidence.
  </action>
  <verify>
    <automated>grep -q "ECOS-09" .planning/REQUIREMENTS.md && ! grep -E "ECOS-09.*Pending" .planning/REQUIREMENTS.md && grep -E "\- \[x\] \*\*Phase 64" .planning/ROADMAP.md && ! grep -q "the four draft `*-VALIDATION.md` Nyquist contracts closed out" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md ECOS-09 line is `- [x]` and the Traceability ECOS-09 row no longer says `Pending` (says Satisfied/Complete).
    - .planning/ROADMAP.md Phase 64 header line is `- [x]` and marked Complete; the 64-02-PLAN.md plan-list line is `- [x]`.
    - .planning/ROADMAP.md Phase 67 Success Criterion 4 no longer claims "the four draft `*-VALIDATION.md` Nyquist contracts closed out" — it reflects D-06 (Phase 64 closed; 63/65/66 deferred as tracked Nyquist debt). Criteria 1-3 unchanged.
    - No unrelated requirement or phase rows are modified.
  </acceptance_criteria>
  <done>ROADMAP marks Phase 64 + 64-02 complete; REQUIREMENTS marks ECOS-09 satisfied — backed by 64-VERIFICATION.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Clean-CI verify_sigra binding proof (human checkpoint)</name>
  <files>(none — CI verification only)</files>
  <what-built>Repinned verify_sigra to szTheory/sigra@62ceb46a (remote-verified to contain the integration file), hardened the three verify-lane guards, fixed the sigra guide + doc-contract, ran verify.sigra green locally, produced 64-VERIFICATION.md, and reconciled ROADMAP/REQUIREMENTS. The local run uses the gitignored deps/sigra; the BINDING ECOS-09 proof is the verify_sigra job running in clean CI with the sibling checkout (A3).</what-built>
  <how-to-verify>
    1. Push the branch and open/let CI run.
    2. Confirm the `verify_sigra` job is green AND runs > 0 sigra integration tests (the root --only sigra lane should report >= 5 tests, the demo-host proof lane >= 2) — not a vacuous 0-test pass.
    3. Confirm `verify_accrue` and `verify_threadline` remain green after the harness-guard changes (D-02b).
    4. Sanity-check the diff: `ci.yml` shows `ref: 62ceb46a...` (no `b186f03c`), and the `ci-gate` aggregator still lists `verify_sigra` in `needs`.
    5. Optionally re-confirm the pin contains the file: `git fetch szTheory/sigra 62ceb46a... && git ls-tree FETCH_HEAD -- lib/sigra/integrations/chimeway.ex` returns a blob.
  </how-to-verify>
  <action>This is a human-verify checkpoint, not an autonomous task. The executor pushes the branch and waits for the operator to confirm the clean-CI verify_sigra job (binding ECOS-09 evidence per A3). See <how-to-verify> for the exact steps. Do not flip any further status without operator approval.</action>
  <verify>
    <human-check>verify_sigra job green in clean CI with >0 sigra integration tests (root >=5, demo-host proof >=2); verify_accrue + verify_threadline still green</human-check>
  </verify>
  <done>Operator confirms verify_sigra is green with >0 tests in clean CI; the vacuous-pass footgun is closed against the real partner module.</done>
  <resume-signal>Type "approved" once verify_sigra is green with >0 tests in clean CI, or describe the CI failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev (gitignored deps/sigra) -> clean CI (sibling checkout) | the local copy masked the gap; the binding proof must come from clean CI |
| planning-doc status -> milestone truth | flipping ECOS-09 to Satisfied asserts a verified claim; must be evidence-backed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-08 | Spoofing / false-green | local verify.sigra passing via gitignored deps/sigra while clean CI would differ (A3) | mitigate | The human-verify checkpoint requires the pushed-branch verify_sigra job to be green with >0 tests as the BINDING evidence; the local run is only a pre-check. ROADMAP/REQUIREMENTS flips are gated behind verified green (Task 2 ordered after Task 1). |
| T-67-09 | Repudiation / unverified status claim | flipping ECOS-09 to Satisfied without proof | mitigate | 64-VERIFICATION.md captures the concrete proof (lanes, counts, redaction assertions, pinned SHA) before the status flip; 64-02-SUMMARY.md committed for auditability. |
| T-67-10 | Tampering | inaccurate override: true comment misleading future maintainers | mitigate | A2 resolved during planning (ecto 3.12 vs 3.11 diamond, verified against sigra's mix.exs); the comment states the factual reason, not a guess. |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; `mix deps.get` in the demo-host lane fetches the already-pinned optional sigra path dep only. The partner SHA is mitigated by T-67-01 (Plan 01) and the checkpoint above. |
| T-67-04 | Information disclosure | @sensitive_keys redaction boundary | accept | Verified-good, out of scope; the lifecycle redaction assertions (refute_sensitive_*) are the EXISTING mitigation this plan proves, not new work. |
</threat_model>

<verification>
- `SIGRA_PATH=../sigra/sigra mix verify.sigra` exits 0 locally (pre-check).
- Clean-CI `verify_sigra` job green with >0 tests (binding — human checkpoint).
- ECOS-09 is `- [x]`/Satisfied in REQUIREMENTS; Phase 64 `- [x]`/Complete in ROADMAP.
- 64-VERIFICATION.md exists; 64-02-SUMMARY.md tracked; 64-VALIDATION.md nyquist_compliant: true.
</verification>

<success_criteria>
- ECOS-09 proven E2E in clean CI (verify_sigra green, >0 tests) — restoring DEMO-10/DOCS-10/GATE-07 to satisfied.
- 64-VERIFICATION.md produced; 64-02-SUMMARY.md committed; tracking reconciled.
- Phase 64 Nyquist contract closed; mix.exs override: true comment accurate.
</success_criteria>

<output>
Create `.planning/phases/67-close-ecos-09-repin-sigra-ci-sha-harden-verify-lanes-against/67-03-SUMMARY.md` when done
</output>
