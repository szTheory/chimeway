---
phase: 66-docs-release-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .github/workflows/ci.yml
  - MAINTAINING.md
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
requirements: [GATE-07]

must_haves:
  truths:
    - "mix verify.threadline runs root threadline tests and demo host threadline tests"
    - "mix verify.sigra runs root sigra tests and demo host sigra tests"
    - "verify_threadline and verify_sigra jobs exist in ci.yml with sibling checkout + postgres service"
    - "ci-gate needs list grows from 9 to 11 entries (adds verify_threadline and verify_sigra)"
    - "MAINTAINING.md pre-ship checklist shows 10 commands and says All ten must pass"
    - "release_gate_contract_test.exs reflects 11 ci-gate lanes and 7 pre-ship verify commands"
  artifacts:
    - path: "mix.exs"
      provides: "verify.threadline and verify.sigra aliases in aliases/0"
      contains: "verify.threadline"
    - path: ".github/workflows/ci.yml"
      provides: "verify_threadline and verify_sigra jobs + expanded ci-gate needs"
      contains: "verify_threadline"
    - path: "MAINTAINING.md"
      provides: "10-command pre-ship checklist"
      contains: "All ten must pass"
    - path: "test/chimeway/release_gate_contract_test.exs"
      provides: "Updated contract: 11 ci-gate lanes, 7 pre-ship commands, sibling checkout tests"
      contains: "verify_threadline"
  key_links:
    - from: "mix.exs verify.threadline alias"
      to: "examples/chimeway_demo_host"
      via: "cmd --shell cd examples/chimeway_demo_host && env CHIMEWAY_SKIP_THREADLINE_DEP=1 THREADLINE_PATH=../../../threadline/threadline"
      pattern: "THREADLINE_PATH"
    - from: "ci.yml verify_threadline job"
      to: "szTheory/threadline"
      via: "actions/checkout with path: threadline/threadline"
      pattern: "szTheory/threadline"
    - from: "release_gate_contract_test.exs @ci_gate_lanes"
      to: "ci.yml ci-gate needs"
      via: "extract_ci_gate_needs/1 helper"
      pattern: "verify_threadline"
---

<objective>
Wire the two new verify gates into mix.exs aliases, CI workflow, MAINTAINING.md pre-ship checklist, and release gate contract test. This plan touches tooling and infrastructure only — no guide content.

Purpose: GATE-07 — mix verify.threadline and mix verify.sigra must be named entrypoints in CI and the pre-ship checklist, and the release gate contract must reflect the new counts.
Output: Updated mix.exs, ci.yml, MAINTAINING.md, and release_gate_contract_test.exs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/66-docs-release-gates/66-CONTEXT.md
@.planning/phases/66-docs-release-gates/66-PATTERNS.md
</context>

<interfaces>
<!-- Extracted from codebase — executor reads these files first for current state. -->

From mix.exs lines 113–125 (verify aliases block, current tail):
  "verify.accrue": [
    "deps.compile accrue --force",
    "cmd env MIX_ENV=test mix test --only accrue --warnings-as-errors",
    "cmd --shell cd examples/chimeway_demo_host && env CHIMEWAY_SKIP_ACCRUE_DEP=1 ACCRUE_PATH=../../../accrue/accrue CHIMEWAY_PATH=../.. mix deps.get && env CHIMEWAY_SKIP_ACCRUE_DEP=1 ACCRUE_PATH=../../../accrue/accrue CHIMEWAY_PATH=../.. mix deps.compile accrue --force && env CHIMEWAY_SKIP_ACCRUE_DEP=1 ACCRUE_PATH=../../../accrue/accrue CHIMEWAY_PATH=../.. mix test --only accrue --warnings-as-errors"
  ],
  "verify.inbox": [
    "cmd --shell cd chimeway_inbox && mix deps.get && mix test --warnings-as-errors",
    "cmd --shell cd examples/chimeway_demo_host && mix deps.get && mix test --only inbox --warnings-as-errors"
  ]
  -- new aliases go after verify.inbox --

From mix.exs lines 187–188 (docs/0 extras list tail of Introduction group):
  "guides/introduction/inbox-integration.md",
  -- new entries go here (before next section) --

From .github/workflows/ci.yml lines 295–335 (verify_accrue → verify_inbox job boundary):
  verify_accrue ends at line 295 with: - run: mix verify.accrue
  verify_inbox begins at line 297

From .github/workflows/ci.yml lines 383–414 (ci-gate job):
  needs: [lint, test, verify_gates, verify_docs, verify_example, verify_journeys, verify_mailglass, verify_accrue, verify_inbox]
  env vars: LINT, TEST, VERIFY_GATES, VERIFY_DOCS, VERIFY_EXAMPLE, VERIFY_JOURNEYS, VERIFY_MAILGLASS, VERIFY_ACCRUE, VERIFY_INBOX
  for loop: "for lane in LINT TEST VERIFY_GATES VERIFY_DOCS VERIFY_EXAMPLE VERIFY_JOURNEYS VERIFY_MAILGLASS VERIFY_ACCRUE VERIFY_INBOX; do"

From test/chimeway/release_gate_contract_test.exs:
  @ci_gate_lanes ~w(lint test verify_gates verify_docs verify_example verify_journeys verify_mailglass verify_accrue verify_inbox)  # line 12 — needs verify_threadline verify_sigra appended
  @pre_ship_verify_commands [ 5 tuples ]  # lines 14-20 — needs 2 more tuples
  test "MAINTAINING documents eight-gate pre-ship requirement"  # line 44 — rename to "ten-gate", change regex to ~r/All ten must pass/i
  test "ci-gate aggregates 9 required lanes"  # line 112 — rename to "11 required lanes"

From MAINTAINING.md lines 46-72 (pre-ship section):
  "Run all eight before opening..."  → "Run all ten..."
  bash block: 8 commands → 10 commands (add mix verify.threadline and mix verify.sigra after mix verify.inbox)
  bullet list: add two new bullets after verify.inbox bullet
  "All eight must pass before publishing." → "All ten must pass before publishing."
  "These eight local commands" → "These ten local commands"
  Sibling checkout subsection (lines 74-76): add threadline and sigra alongside accrue
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add verify.threadline and verify.sigra aliases to mix.exs; add guides to docs extras</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (full file — read lines 100-210 covering aliases/0 and docs/0)
    - .planning/phases/66-docs-release-gates/66-PATTERNS.md (Pattern 3: Mix Verify Alias Shape; Pattern 7: HexDocs Extras Update)
  </read_first>
  <action>
    Edit mix.exs in two places:

    Place 1 — aliases/0: After the closing `]` of "verify.inbox" (currently last alias), insert two new aliases before the outer closing `]` of aliases/0:

    "verify.threadline": [
      "deps.compile threadline --force",
      "cmd env MIX_ENV=test mix test --only threadline --warnings-as-errors",
      "cmd --shell cd examples/chimeway_demo_host && env CHIMEWAY_SKIP_THREADLINE_DEP=1 THREADLINE_PATH=../../../threadline/threadline CHIMEWAY_PATH=../.. mix deps.get && env CHIMEWAY_SKIP_THREADLINE_DEP=1 THREADLINE_PATH=../../../threadline/threadline CHIMEWAY_PATH=../.. mix deps.compile threadline --force && env CHIMEWAY_SKIP_THREADLINE_DEP=1 THREADLINE_PATH=../../../threadline/threadline CHIMEWAY_PATH=../.. mix test --only threadline --warnings-as-errors"
    ],
    "verify.sigra": [
      "deps.compile sigra --force",
      "cmd env MIX_ENV=test mix test --only sigra --warnings-as-errors",
      "cmd --shell cd examples/chimeway_demo_host && env CHIMEWAY_SKIP_SIGRA_DEP=1 SIGRA_PATH=../../../sigra/sigra CHIMEWAY_PATH=../.. mix deps.get && env CHIMEWAY_SKIP_SIGRA_DEP=1 SIGRA_PATH=../../../sigra/sigra CHIMEWAY_PATH=../.. mix deps.compile sigra --force && env CHIMEWAY_SKIP_SIGRA_DEP=1 SIGRA_PATH=../../../sigra/sigra CHIMEWAY_PATH=../.. mix test --only sigra --warnings-as-errors"
    ]

    Note: path convention is `threadline/threadline` and `sigra/sigra` (nested) — mirrors `accrue/accrue` per PATTERNS.md critical note #2.

    Place 2 — docs/0 extras list: After the line `"guides/introduction/inbox-integration.md",` (currently last Introduction group guide), insert:
    "guides/introduction/threadline-integration.md",
    "guides/introduction/sigra-auth-integration.md",

    These must appear before `"guides/flows/trigger-to-delivery.md"` (the next section). The Introduction group is matched by regex `~r/guides\/introduction\//` so ordering within the group matters for HexDocs sidebar.
  </action>
  <verify>
    <automated>grep -c '"verify.threadline"' mix.exs | grep -q "^1$" && grep -c '"verify.sigra"' mix.exs | grep -q "^1$" && grep -c 'threadline-integration.md' mix.exs | grep -q "^1$" && grep -c 'sigra-auth-integration.md' mix.exs | grep -q "^1$" && mix compile --dry-run 2>/dev/null || elixir -e 'Code.compile_file("mix.exs")' 2>&1 | grep -v "^$" | tail -5; echo "mix.exs syntax check done"</automated>
  </verify>
  <acceptance_criteria>
    - mix.exs contains exactly 1 occurrence of `"verify.threadline":` with a 3-element list
    - mix.exs contains exactly 1 occurrence of `"verify.sigra":` with a 3-element list
    - The threadline alias demo host step includes CHIMEWAY_SKIP_THREADLINE_DEP=1, THREADLINE_PATH=../../../threadline/threadline, CHIMEWAY_PATH=../..
    - The sigra alias demo host step includes CHIMEWAY_SKIP_SIGRA_DEP=1, SIGRA_PATH=../../../sigra/sigra, CHIMEWAY_PATH=../..
    - mix.exs contains "guides/introduction/threadline-integration.md" in docs extras
    - mix.exs contains "guides/introduction/sigra-auth-integration.md" in docs extras
    - Both guide paths appear after "guides/introduction/inbox-integration.md" in the file
  </acceptance_criteria>
  <done>mix.exs aliases/0 has two new verify aliases and docs/0 extras list has two new guide paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add verify_threadline and verify_sigra CI jobs; expand ci-gate to 11 lanes</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (read lines 251-415 covering verify_accrue job through ci-gate job)
    - .planning/phases/66-docs-release-gates/66-PATTERNS.md (Pattern 4: CI Job Shape, Pattern 5: ci-gate Expansion)
  </read_first>
  <action>
    Edit .github/workflows/ci.yml in three places:

    Place 1 — New CI jobs: After the verify_inbox job (which ends with `- run: mix verify.inbox`) and before `install_golden_contract:`, insert two new jobs following the exact verify_accrue structure:

    verify_threadline job:
    - Job name: "Threadline telemetry gate"
    - Same postgres service block (image: postgres:15, POSTGRES_PASSWORD, health checks, port 5432)
    - env: MIX_ENV: test, DATABASE_URL: postgres://postgres:postgres@localhost/chimeway_test, THREADLINE_PATH: ${{ github.workspace }}/threadline/threadline
    - Steps:
      1. uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 (Chimeway checkout)
      2. uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 with repository: szTheory/threadline, ref: <IMPLEMENTER: run `git ls-remote https://github.com/szTheory/threadline HEAD | cut -f1` to get the HEAD SHA and insert it here>, path: threadline/threadline
      3. uses: erlef/setup-beam@8251c48667b97e88a0a24ec512f5b72a039fcea7 with elixir-version: "1.17", otp-version: "27"
      4. uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 with path: deps/_build, key: ${{ runner.os }}-mix-verify-threadline-${{ hashFiles('**/mix.lock') }}, restore-keys: ${{ runner.os }}-mix-verify-threadline-
      5. run: mix local.rebar --force && mix local.hex --force && mix deps.get
      6. run: mix ecto.create --quiet
      7. run: mix ecto.migrate --quiet
      8. run: mix verify.threadline

    verify_sigra job (same structure):
    - Job name: "Sigra auth integration gate"
    - Same postgres service block
    - env: MIX_ENV: test, DATABASE_URL: postgres://postgres:postgres@localhost/chimeway_test, SIGRA_PATH: ${{ github.workspace }}/sigra/sigra
    - Steps: same pattern but repository: szTheory/sigra, ref: <IMPLEMENTER: run `git ls-remote https://github.com/szTheory/sigra HEAD | cut -f1`>, path: sigra/sigra, cache key: verify-sigra, final run: mix verify.sigra

    Place 2 — ci-gate needs line (currently: `needs: [lint, test, verify_gates, verify_docs, verify_example, verify_journeys, verify_mailglass, verify_accrue, verify_inbox]`): Add verify_threadline and verify_sigra to become:
    needs: [lint, test, verify_gates, verify_docs, verify_example, verify_journeys, verify_mailglass, verify_accrue, verify_inbox, verify_threadline, verify_sigra]

    Place 3 — ci-gate env + for loop: In the ci-gate job's env block, add after VERIFY_INBOX:
      VERIFY_THREADLINE: ${{ needs.verify_threadline.result }}
      VERIFY_SIGRA: ${{ needs.verify_sigra.result }}

    Update the for loop from:
    `for lane in LINT TEST VERIFY_GATES VERIFY_DOCS VERIFY_EXAMPLE VERIFY_JOURNEYS VERIFY_MAILGLASS VERIFY_ACCRUE VERIFY_INBOX; do`
    to:
    `for lane in LINT TEST VERIFY_GATES VERIFY_DOCS VERIFY_EXAMPLE VERIFY_JOURNEYS VERIFY_MAILGLASS VERIFY_ACCRUE VERIFY_INBOX VERIFY_THREADLINE VERIFY_SIGRA; do`
  </action>
  <verify>
    <automated>grep -c "verify_threadline:" .github/workflows/ci.yml | grep -q "^1$" && grep -c "verify_sigra:" .github/workflows/ci.yml | grep -q "^1$" && grep -c "szTheory/threadline" .github/workflows/ci.yml | grep -q "^1$" && grep -c "szTheory/sigra" .github/workflows/ci.yml | grep -q "^1$" && grep -c "VERIFY_THREADLINE" .github/workflows/ci.yml | grep -q "^3$" && grep -c "VERIFY_SIGRA" .github/workflows/ci.yml | grep -q "^3$" && echo "ci.yml gate checks OK"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml contains a verify_threadline job with name "Threadline telemetry gate", postgres service, THREADLINE_PATH env, szTheory/threadline sibling checkout at path threadline/threadline, and `mix verify.threadline` final step
    - .github/workflows/ci.yml contains a verify_sigra job with name "Sigra auth integration gate", postgres service, SIGRA_PATH env, szTheory/sigra sibling checkout at path sigra/sigra, and `mix verify.sigra` final step
    - ci-gate needs list contains verify_threadline and verify_sigra (11 entries total)
    - ci-gate env block contains VERIFY_THREADLINE and VERIFY_SIGRA entries
    - ci-gate for loop includes VERIFY_THREADLINE and VERIFY_SIGRA
    - Both new CI jobs have a pinned commit SHA (not a placeholder string) for the sibling repo ref
  </acceptance_criteria>
  <done>ci.yml has two new verify jobs and ci-gate aggregates 11 lanes.</done>
</task>

<task type="auto">
  <name>Task 3: Update MAINTAINING.md pre-ship checklist and release_gate_contract_test.exs to reflect 10 commands / 11 lanes</name>
  <files>MAINTAINING.md, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    - MAINTAINING.md (read lines 40-90 covering the pre-ship section and sibling checkout subsection)
    - test/chimeway/release_gate_contract_test.exs (full file — 205 lines)
    - .planning/phases/66-docs-release-gates/66-PATTERNS.md (Pattern 6: Release Gate Contract Test Update; MAINTAINING.md section)
    - .planning/phases/66-docs-release-gates/66-RESEARCH.md (MAINTAINING.md target state code example; release_gate_contract_test.exs target state)
  </read_first>
  <action>
    Edit MAINTAINING.md in the pre-ship section (lines ~46-76):
    1. "Run all eight" → "Run all ten"
    2. Add `mix verify.threadline` and `mix verify.sigra` to the bash block after `mix verify.inbox`
    3. "All eight must pass before publishing." → "All ten must pass before publishing."
    4. "These eight local commands" → "These ten local commands"
    5. Add two new bullet descriptions after the `mix verify.inbox` bullet:
       - `mix verify.threadline` — Threadline telemetry integration gate (GATE-07): Threadline reporter lifecycle proof and demo host audit correlation; requires sibling Threadline checkout — set `THREADLINE_PATH=../threadline/threadline` locally or let CI job check out szTheory/threadline
       - `mix verify.sigra` — Sigra auth integration gate (GATE-07): Sigra auth notification lifecycle proof and demo host auth flow; requires sibling Sigra checkout — set `SIGRA_PATH=../sigra/sigra` locally or let CI job check out szTheory/sigra
    6. Expand the "Accrue sibling checkout" subsection to also document Threadline and Sigra sibling checkout conventions (THREADLINE_PATH=../threadline/threadline and SIGRA_PATH=../sigra/sigra, citing the relevant CI pinned SHA placeholders).

    Edit test/chimeway/release_gate_contract_test.exs in four places:

    Place 1 — @ci_gate_lanes (line 12): Append `verify_threadline verify_sigra` so the list has 11 entries:
    @ci_gate_lanes ~w(lint test verify_gates verify_docs verify_example verify_journeys verify_mailglass verify_accrue verify_inbox verify_threadline verify_sigra)

    Place 2 — @pre_ship_verify_commands (lines 14-20): Add two tuples after the verify.inbox tuple:
      {"verify.threadline", "verify_threadline", "mix verify.threadline"},
      {"verify.sigra", "verify_sigra", "mix verify.sigra"}

    Place 3 — "MAINTAINING documents eight-gate pre-ship requirement" test (line 44): Rename to "MAINTAINING documents ten-gate pre-ship requirement", change regex from `~r/All eight must pass/i` to `~r/All ten must pass/i`, update failure message to "MAINTAINING.md must state all ten verify gates must pass before publishing".

    Place 4 — "ci-gate aggregates 9 required lanes" test (line 112): Rename to "ci-gate aggregates 11 required lanes".

    Place 5 — Add two new sibling-checkout tests after the "verify_accrue job checks out szTheory/accrue" test (line 73-84), per PATTERNS.md Pattern 6:
      test "verify_threadline job checks out szTheory/threadline with THREADLINE_PATH"
        — extract_ci_job_block(ci_yml, "verify_threadline"); assert contains "szTheory/threadline" and "THREADLINE_PATH"
      test "verify_sigra job checks out szTheory/sigra with SIGRA_PATH"
        — extract_ci_job_block(ci_yml, "verify_sigra"); assert contains "szTheory/sigra" and "SIGRA_PATH"

    Also add a test "MAINTAINING documents THREADLINE_PATH sibling checkout" and "MAINTAINING documents SIGRA_PATH sibling checkout" mirroring the existing ACCRUE_PATH test at line 49.
  </action>
  <verify>
    <automated>grep -c "All ten must pass" MAINTAINING.md | grep -q "^1$" && grep -c "mix verify.threadline" MAINTAINING.md | grep -q "^1$" && grep -c "mix verify.sigra" MAINTAINING.md | grep -q "^1$" && grep -c "verify_threadline" test/chimeway/release_gate_contract_test.exs | grep -q "^[3-9]$" && grep -c "verify_sigra" test/chimeway/release_gate_contract_test.exs | grep -q "^[3-9]$" && grep -c "All ten must pass" test/chimeway/release_gate_contract_test.exs | grep -q "^1$" && MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs 2>&1 | grep -E "^[0-9]+ tests" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - MAINTAINING.md bash block contains mix verify.threadline and mix verify.sigra (after mix verify.inbox)
    - MAINTAINING.md contains "Run all ten" and "All ten must pass before publishing."
    - MAINTAINING.md has bullet descriptions for both verify.threadline and verify.sigra
    - release_gate_contract_test.exs @ci_gate_lanes has 11 entries including verify_threadline and verify_sigra
    - release_gate_contract_test.exs @pre_ship_verify_commands has 7 tuples including the two new verify aliases
    - release_gate_contract_test.exs test formerly "eight-gate" now says "ten-gate" with `~r/All ten must pass/i`
    - release_gate_contract_test.exs test formerly "9 required lanes" now says "11 required lanes"
    - Two new sibling-checkout tests in release_gate_contract_test.exs for verify_threadline and verify_sigra jobs
    - `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs` passes (all tests green)
  </acceptance_criteria>
  <done>MAINTAINING.md shows 10 commands and release_gate_contract_test.exs fully reflects the new 11-lane / 10-command state with all tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ci.yml sibling checkout | GitHub Actions checks out szTheory/threadline and szTheory/sigra — public repos assumed; pinned SHA prevents unexpected changes |
| mix verify.* alias | Alias runs external commands via cmd --shell; env vars scoped to each step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-03 | Tampering | ci.yml szTheory/threadline sibling checkout | mitigate | Pin to specific commit SHA (same pattern as verify_accrue pins 236fa2f1649e771f3b515603495436badeed3c7b); implementer must look up and pin actual SHA from `git ls-remote https://github.com/szTheory/threadline HEAD` |
| T-66-04 | Tampering | ci.yml szTheory/sigra sibling checkout | mitigate | Same pinned SHA requirement for szTheory/sigra |
| T-66-05 | Elevation of Privilege | CHIMEWAY_SKIP_THREADLINE_DEP=1 in verify alias | accept | Env var only skips optional dep resolution in demo host; does not grant elevated privileges; follows established CHIMEWAY_SKIP_ACCRUE_DEP pattern |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — config and test file edits only |
</threat_model>

<verification>
After all three tasks complete in sequence:

1. `mix compile` exits 0 (mix.exs aliases parseable)
2. `grep -c '"verify.threadline"' mix.exs` = 1
3. `grep -c '"verify.sigra"' mix.exs` = 1
4. `grep -c "verify_threadline:" .github/workflows/ci.yml` = 1
5. `grep -c "verify_sigra:" .github/workflows/ci.yml` = 1
6. `grep "All ten must pass" MAINTAINING.md` exits 0
7. `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs` — all tests pass
</verification>

<success_criteria>
All three tooling files updated, MAINTAINING.md shows 10 commands, release gate contract test reflects new 11-lane / 10-command state and all tests pass. CI job structure follows verify_accrue pattern exactly with pinned SHAs.
</success_criteria>

<output>
Create .planning/phases/66-docs-release-gates/66-02-SUMMARY.md when done
</output>
