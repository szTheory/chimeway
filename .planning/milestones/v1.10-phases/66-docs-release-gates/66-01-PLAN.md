---
phase: 66-docs-release-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/introduction/threadline-integration.md
  - guides/introduction/sigra-auth-integration.md
autonomous: true
requirements: [DOCS-10]

must_haves:
  truths:
    - "A host can follow the Threadline guide from dep → attach reporter → understand outcomes → verify"
    - "A host can follow the Sigra guide from dep → seam config → notifier keys → trigger pattern → verify"
    - "Both guides use responsibility-split language (Chimeway orchestrates; ecosystem package owns its domain)"
    - "Sigra guide section 4 contains the inline anti-pattern note forbidding :raw_token / :magic_link_url / :confirmation_code in Chimeway.trigger/3 params"
  artifacts:
    - path: "guides/introduction/threadline-integration.md"
      provides: "4-section golden-path Threadline integration guide"
      contains: "Chimeway.Telemetry.ThreadlineReporter"
    - path: "guides/introduction/sigra-auth-integration.md"
      provides: "5-section golden-path Sigra auth integration guide"
      contains: "Sigra.Integrations.Chimeway"
  key_links:
    - from: "guides/introduction/threadline-integration.md"
      to: "Chimeway.Telemetry.ThreadlineReporter.attach/0"
      via: "Application.start/2 code example"
      pattern: "attach/0"
    - from: "guides/introduction/sigra-auth-integration.md"
      to: "Chimeway.trigger/3"
      via: "auth event trigger section"
      pattern: "Chimeway.trigger"
---

<objective>
Write the two new SEED-003 golden-path integration guides: Threadline telemetry bridge and Sigra auth notification flows. Both follow the accrue-dunning-integration.md template (responsibility-split header, numbered sections, Related guides footer) with guide-specific section content from Phase 63/64 context decisions.

Purpose: DOCS-10 — adopters need a canonical dependency→config→trigger→proof path for each integration.
Output: guides/introduction/threadline-integration.md (4 sections) and guides/introduction/sigra-auth-integration.md (5 sections).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/66-docs-release-gates/66-CONTEXT.md
@.planning/phases/66-docs-release-gates/66-PATTERNS.md

@guides/introduction/accrue-dunning-integration.md
</context>

<interfaces>
<!-- Key doc-contract required strings the guides MUST contain — failure to include any will break Plan 03's describe blocks. -->

Threadline @required strings (D-04):
  Chimeway.Telemetry.ThreadlineReporter
  attach/0
  config :chimeway
  correlation_id
  notification_suppressed
  DemoHost.Seeds.seed_threadline_notification
  /admin/chimeway
  mix verify.threadline

Sigra @required strings (D-05):
  Sigra.Integrations.Chimeway
  sigra.auth.magic_link
  sigra.auth.confirmation_code
  Chimeway.trigger
  idempotency_key
  tenant_id
  DemoHost.Seeds.seed_sigra
  /admin/chimeway
  mix verify.sigra
  SIGRA_PATH
  orchestrates

@recipe_forbidden_strings (from doc_contract_test.exs line 104-108) — MUST NOT appear in either guide:
  stop_conditions
  Workflows.Workers
  Chimeway.Trigger.trigger

Chimeway.Workflow (without trailing 's') — forbidden by regex test.

Sigra guide @sigra_forbidden — MUST NOT appear as atom syntax in code examples (prose "raw token" OK):
  :raw_token
  :magic_link_url

Responsibility split language for Sigra (D-02 / PATTERNS.md):
  "Chimeway orchestrates the when and why: durable notification lifecycle, suppression and preference gates, idempotency, and operator traces you can search at /admin/chimeway."
  "Sigra owns auth state: token generation, hashed persistence, rate limits, and magic link / confirmation code TTL. Sigra emits auth events; Chimeway does not mutate Sigra records."

Outcome table atoms (Threadline section 3, from RESEARCH.md):
  :notification_suppressed, :notification_deferred, :notification_dispatched, :notification_failed

Verify alias demo paths (for guide section 4/5 verification commands):
  THREADLINE_PATH=../threadline/threadline mix verify.threadline
  SIGRA_PATH=../sigra/sigra mix verify.sigra
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write Threadline integration guide (D-01)</name>
  <files>guides/introduction/threadline-integration.md</files>
  <read_first>
    - guides/introduction/accrue-dunning-integration.md (section structure template: intro paragraph, responsibility-split header, numbered sections, Related guides footer)
    - .planning/phases/66-docs-release-gates/66-CONTEXT.md (D-01 locked section structure)
    - .planning/phases/66-docs-release-gates/66-PATTERNS.md (Threadline-specific content block, outcome table, correlation_id callout)
    - .planning/phases/63-threadline-telemetry-bridge/63-CONTEXT.md (reporter attach pattern, outcome atoms, correlation_id usage)
  </read_first>
  <action>
    Create guides/introduction/threadline-integration.md with the 4-section structure locked in D-01. Base the overall file skeleton on accrue-dunning-integration.md (intro + blueprint link, responsibility-split section, numbered sections, Related guides footer).

    Intro paragraph: one sentence describing Threadline as attach-only telemetry — no Chimeway migrations, no notifier authoring, no host-triggered events. Link to the Threadline telemetry blueprint recipe if one exists (check guides/recipes/ for a threadline blueprint; if absent, omit the blueprint link).

    Responsibility split section heading: "## Responsibility split (SEED-003)". Bold lines:
    - "Chimeway orchestrates the when and why:" — durable notification lifecycle, explainability, and operator traces at /admin/chimeway.
    - "Threadline owns audit ledger:" — immutable append-only event records. Chimeway does not write to Threadline directly; the reporter translates telemetry events.

    Section 1 "## 1. Dependencies": `{:threadline, "~> 0.7", optional: true}` — use `threadline_dep/0` env-override pattern (THREADLINE_PATH → path dep, else Hex dep). Local dev bash: `THREADLINE_PATH=../threadline mix deps.get`. Production adopters use the Hex dep.

    Section 2 "## 2. Attach reporter": Show `Application.start/2` one-liner calling `Chimeway.Telemetry.ThreadlineReporter.attach/0`. Show the config block with only `:repo` and `:actor` (no other keys). Per D-01: no THREADLINE_PATH local dev docs in this section (keep tight).

    Section 3 "## 3. What gets recorded": 4-row outcome table with columns "Lifecycle outcome | Threadline action atom | correlation_id". Rows: suppressed→:notification_suppressed, deferred→:notification_deferred, dispatched→:notification_dispatched, failed→:notification_failed. All rows note correlation_id "forwarded from Chimeway trigger opts". After the table, add the correlation_id callout: Pass `correlation_id:` in `Chimeway.trigger/3` opts to thread the audit row through to `Threadline.Query.timeline/2` strict filter. Without it, the Threadline timeline cannot isolate this notification's lifecycle in a multi-event trace.

    Section 4 "## 4. Verification": Show `DemoHost.Seeds.seed_threadline_notification/0` as runnable demo entry point. Show `mix verify.threadline` as the gate command (no `--warnings-as-errors` flag needed in prose; the alias handles it). Mention /admin/chimeway search for operator trace inspectability.

    Related guides footer: list golden-path.md, the threadline telemetry blueprint (if it exists), mailglass-integration-blueprint.md, installation.md. Mirror accrue guide footer style.

    CRITICAL: The guide MUST contain every string in the Threadline @required list and MUST NOT contain any string in @recipe_forbidden_strings or match Chimeway.Workflow(?![s]).
  </action>
  <verify>
    <automated>grep -l "Chimeway.Telemetry.ThreadlineReporter" guides/introduction/threadline-integration.md && grep -l "attach/0" guides/introduction/threadline-integration.md && grep -l "notification_suppressed" guides/introduction/threadline-integration.md && grep -l "correlation_id" guides/introduction/threadline-integration.md && grep -l "mix verify.threadline" guides/introduction/threadline-integration.md && grep -l "DemoHost.Seeds.seed_threadline_notification" guides/introduction/threadline-integration.md && grep -l "/admin/chimeway" guides/introduction/threadline-integration.md && grep -c "stop_conditions\|Workflows\.Workers\|Chimeway\.Trigger\.trigger" guides/introduction/threadline-integration.md | grep -q "^0$" && echo "threadline guide OK"</automated>
  </verify>
  <acceptance_criteria>
    - guides/introduction/threadline-integration.md exists
    - File contains: Chimeway.Telemetry.ThreadlineReporter, attach/0, config :chimeway, correlation_id, notification_suppressed, DemoHost.Seeds.seed_threadline_notification, /admin/chimeway, mix verify.threadline
    - File does NOT contain: stop_conditions, Workflows.Workers, Chimeway.Trigger.trigger
    - File does NOT match regex /Chimeway\.Workflow(?![s])/
    - 4 numbered sections present: "## 1. Dependencies", "## 2. Attach reporter", "## 3. What gets recorded", "## 4. Verification"
    - Outcome table has 4 rows covering suppressed/deferred/dispatched/failed
  </acceptance_criteria>
  <done>Threadline integration guide passes all @required presence checks and @recipe_forbidden_strings absence checks.</done>
</task>

<task type="auto">
  <name>Task 2: Write Sigra auth integration guide (D-02, D-03)</name>
  <files>guides/introduction/sigra-auth-integration.md</files>
  <read_first>
    - guides/introduction/accrue-dunning-integration.md (section structure template)
    - .planning/phases/66-docs-release-gates/66-CONTEXT.md (D-02 5-section structure, D-03 inline anti-pattern placement)
    - .planning/phases/66-docs-release-gates/66-PATTERNS.md (Sigra-specific sections, responsibility split language, anti-pattern note text)
    - .planning/phases/64-sigra-auth-flows-core/64-CONTEXT.md (Sigra.Integrations.Chimeway seam, magic link + confirmation code flows, redaction decisions D-07/D-08)
    - guides/recipes/sigra-auth-blueprint.md (existing Phase 65 blueprint — cross-link target; verify filename)
  </read_first>
  <action>
    Create guides/introduction/sigra-auth-integration.md with the 5-section structure locked in D-02. Base skeleton on accrue-dunning-integration.md.

    Intro paragraph: one sentence. Link to guides/recipes/sigra-auth-blueprint.md (the Phase 65 recipe). Distinguish guide (end-to-end path) from blueprint (copy-paste recipe).

    Responsibility split section "## Responsibility split (SEED-003)". Bold lines exactly per D-02/PATTERNS.md:
    - "Chimeway orchestrates the when and why:" — durable notification lifecycle, suppression and preference gates, idempotency, and operator traces you can search at /admin/chimeway.
    - "Sigra owns auth state:" — token generation, hashed persistence, rate limits, and magic link / confirmation code TTL. Sigra emits auth events; Chimeway does not mutate Sigra records.

    Section 1 "## 1. Dependencies": `{:sigra, "~> 0.3", optional: true}` — use `sigra_dep/0` env-override pattern (SIGRA_PATH → path dep, else Hex dep). Local dev bash: `SIGRA_PATH=../sigra mix deps.get`. Include SIGRA_PATH in the bash example so the doc-contract @required "SIGRA_PATH" passes.

    Section 2 "## 2. Integration seam": Explain how `Sigra.Integrations.Chimeway` attaches to the host via runtime config and conditional compile. Document the responsibility split: Chimeway orchestrates when/why; Sigra owns auth state, token generation, rate limits. Show the runtime config block (config key and module).

    Section 3 "## 3. Notifier reference": Document `sigra.auth.magic_link` and `sigra.auth.confirmation_code` as the two stable notification keys. Explain `build/2` resolves the magic link URL and confirmation code at dispatch time from the Sigra repo — sensitive values are never in the Chimeway event payload.

    Section 4 "## 4. Auth event triggers": Show how Sigra auth events (request_magic_link success, confirmation code generated) call `Chimeway.trigger/3` with `idempotency_key` + `tenant_id` (= user id). Include the inline anti-pattern note (D-03) — it MUST lead with "Do not pass" (not a positive framing). Exact note text per PATTERNS.md:
    "> **Do not pass** `:raw_token`, `:magic_link_url`, or `:confirmation_code` to `Chimeway.trigger/3`."
    "> Pass identifier-only params (`user_id`, `email`, opaque ref). Sensitive data resolves inside"
    "> notifier `build/2` at dispatch time and is never written to `chimeway_events.payload`."
    CRITICAL: Do NOT use `:raw_token` or `:magic_link_url` in atom syntax anywhere else in the guide (prose uses "raw token" and "magic link URL" without colon prefix). The blockquote above is the only exception — but even there, the forbidden test checks for `:raw_token` as substring. Use prose form "raw_token" (no colon) or backtick-only without colon in prose. IMPORTANT: After rereading D-05 and RESEARCH.md pitfall 4: the guide section 4 anti-pattern note should state the names WITHOUT the colon-atom prefix in body prose, e.g. "Do not pass `raw_token`, `magic_link_url`, or `confirmation_code`" — this avoids triggering @sigra_forbidden `~w(:raw_token :magic_link_url)` which fires on the literal substrings `:raw_token` and `:magic_link_url`. The doc-contract uses String.contains? which would match backtick-colon atom forms in Markdown.

    Section 5 "## 5. Verification": Show `DemoHost.Seeds.seed_sigra_auth/0` as the runnable demo entry point. Show `SIGRA_PATH=../sigra mix verify.sigra` gate command. Mention /admin/chimeway for operator trace inspectability.

    Related guides footer: golden-path.md, sigra-auth-blueprint.md, mailglass-integration-blueprint.md, installation.md.

    CRITICAL: The guide MUST contain all strings in the Sigra @required list. MUST NOT contain @recipe_forbidden_strings. MUST NOT contain the atom-syntax strings `:raw_token` or `:magic_link_url` (the @sigra_forbidden strings) — use prose form without colon prefix in all body text.
  </action>
  <verify>
    <automated>grep -l "Sigra.Integrations.Chimeway" guides/introduction/sigra-auth-integration.md && grep -l "sigra.auth.magic_link" guides/introduction/sigra-auth-integration.md && grep -l "sigra.auth.confirmation_code" guides/introduction/sigra-auth-integration.md && grep -l "Chimeway.trigger" guides/introduction/sigra-auth-integration.md && grep -l "idempotency_key" guides/introduction/sigra-auth-integration.md && grep -l "SIGRA_PATH" guides/introduction/sigra-auth-integration.md && grep -l "mix verify.sigra" guides/introduction/sigra-auth-integration.md && grep -l "orchestrates" guides/introduction/sigra-auth-integration.md && grep -c ":raw_token\|:magic_link_url" guides/introduction/sigra-auth-integration.md | grep -q "^0$" && echo "sigra guide OK"</automated>
  </verify>
  <acceptance_criteria>
    - guides/introduction/sigra-auth-integration.md exists
    - File contains: Sigra.Integrations.Chimeway, sigra.auth.magic_link, sigra.auth.confirmation_code, Chimeway.trigger, idempotency_key, tenant_id, DemoHost.Seeds.seed_sigra, /admin/chimeway, mix verify.sigra, SIGRA_PATH, orchestrates
    - File does NOT contain the atom-syntax strings `:raw_token` or `:magic_link_url` (grep -c returns 0)
    - File does NOT contain: stop_conditions, Workflows.Workers, Chimeway.Trigger.trigger
    - File does NOT match regex /Chimeway\.Workflow(?![s])/
    - 5 numbered sections present: "## 1. Dependencies", "## 2. Integration seam", "## 3. Notifier reference", "## 4. Auth event triggers", "## 5. Verification"
    - Section 4 contains the inline anti-pattern blockquote leading with "Do not pass"
  </acceptance_criteria>
  <done>Sigra auth integration guide passes all @required presence checks, @recipe_forbidden_strings absence checks, and @sigra_forbidden atom-form absence checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide prose → doc-contract test | Test reads guide file content; malformed strings cause test failures not security issues |
| Guide examples → adopter code | Adopters copy code examples; incorrect redaction guidance could lead to token leakage in adopter apps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Information Disclosure | sigra-auth-integration.md section 4 | mitigate | Anti-pattern note explicitly forbids `:raw_token`/`:magic_link_url`/`:confirmation_code` in `Chimeway.trigger/3` params; prose instructs identifier-only params. Doc-contract test enforces absence of atom-form forbidden strings. |
| T-66-02 | Tampering | doc-contract test @sigra_forbidden | mitigate | `~w(:raw_token :magic_link_url)` atom form uses colon prefix to avoid false positives on prose; test enforces guide does not teach dangerous params |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — documentation files only |
</threat_model>

<verification>
After both tasks complete:

1. `grep -l "Chimeway.Telemetry.ThreadlineReporter" guides/introduction/threadline-integration.md` — exits 0
2. `grep -l "Sigra.Integrations.Chimeway" guides/introduction/sigra-auth-integration.md` — exits 0
3. `grep -c ":raw_token\|:magic_link_url" guides/introduction/sigra-auth-integration.md | grep "^0$"` — passes
4. `grep -c "stop_conditions\|Workflows\.Workers\|Chimeway\.Trigger\.trigger" guides/introduction/threadline-integration.md guides/introduction/sigra-auth-integration.md | grep "^0$"` — passes
</verification>

<success_criteria>
Both guides exist with correct section structure, all @required strings present, all forbidden strings absent. The files are ready for Plan 03's doc-contract describe blocks to read and validate.
</success_criteria>

<output>
Create .planning/phases/66-docs-release-gates/66-01-SUMMARY.md when done
</output>
