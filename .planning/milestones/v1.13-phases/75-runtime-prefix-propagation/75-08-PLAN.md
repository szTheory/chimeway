---
phase: 75-runtime-prefix-propagation
plan: 08
type: execute
wave: 5
depends_on: ["75-07"]
files_modified:
  - test/chimeway/runtime_prefix_integration_test.exs
autonomous: true
gap_closure: true
requirements: [RUN-01, RUN-02, RUN-03, RUN-04]

must_haves:
  truths:
    - "Focused runtime-prefix proof calls Admin.command_center/1, Admin.recent_problem_deliveries/1, Admin.definitions/1, Admin.feed/1, Admin.recovery_candidates/1, and Admin.outcome_totals/1 against rows stored in the configured Chimeway schema."
    - "Focused runtime-prefix proof executes Deliveries.begin_recovery/2, Deliveries.recover_delivery/2 or Chimeway.recover_delivery/2, and Deliveries.recover_event/2 or Chimeway.recover_event/2 against prefixed rows with no public fallback."
    - "Focused runtime-prefix proof executes SignalRouterWorker.perform/1 and WorkflowProgressionWorker.perform/1 or Progression.progress_run/2 so durable ID reloads and next-step writes happen under the configured Chimeway prefix."
    - "mix verify.runtime_prefix covers the new admin, recovery, signal routing, and workflow progression assertions because the alias already targets test/chimeway/runtime_prefix_integration_test.exs."
    - "mix verify.runtime_prefix, mix ci.test, and mix verify.install_golden pass after the gap-closure assertions are added."
  artifacts:
    - path: "test/chimeway/runtime_prefix_integration_test.exs"
      provides: "Expanded prefixed runtime integration assertions for admin read models, recovery execution, signal routing execution, and workflow progression execution"
      contains: ["runtime_prefix_operator", "runtime_prefix_workflow_signal", "Admin.command_center", "Deliveries.begin_recovery", "SignalRouterWorker.perform", "WorkflowProgressionWorker.perform"]
    - path: "mix.exs"
      provides: "Existing focused runtime-prefix verification alias that includes the expanded integration test file"
      contains: "verify.runtime_prefix"
  key_links:
    - from: "test/chimeway/runtime_prefix_integration_test.exs"
      to: "lib/chimeway/admin.ex"
      via: "prefixed test calls each Admin read-model function with ordinary opts and asserts DTO contents are read from configured storage"
      pattern: "Admin\\.(command_center|recent_problem_deliveries|definitions|feed|recovery_candidates|outcome_totals)"
    - from: "test/chimeway/runtime_prefix_integration_test.exs"
      to: "lib/chimeway/deliveries.ex"
      via: "prefixed test calls recovery execution APIs and asserts recovery metadata plus delivery/event rows stay in configured storage"
      pattern: "(begin_recovery|recover_delivery|recover_event)"
    - from: "test/chimeway/runtime_prefix_integration_test.exs"
      to: "lib/chimeway/dispatch/signal_router_worker.ex"
      via: "prefixed test calls SignalRouterWorker.perform/1 with durable signal_id args"
      pattern: "SignalRouterWorker\\.perform"
    - from: "test/chimeway/runtime_prefix_integration_test.exs"
      to: "lib/chimeway/dispatch/workflow_progression_worker.ex"
      via: "prefixed test calls WorkflowProgressionWorker.perform/1 or the shared Progression.progress_run/2 seam with durable workflow_run_id"
      pattern: "(WorkflowProgressionWorker\\.perform|Progression\\.progress_run)"
---

<objective>
Close the Phase 75 verification gaps by expanding the existing focused runtime-prefix integration suite to execute admin read models, recovery execution, signal routing worker execution, and workflow progression execution under prefix mode.

Purpose: the implementation already has a green focused gate, but verification found that the gate does not execute several required runtime paths. This plan makes the gate prove the missing behavior without reopening completed plans.
Output: an expanded `test/chimeway/runtime_prefix_integration_test.exs` and green focused plus regression verification evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/75-runtime-prefix-propagation/75-CONTEXT.md
@.planning/phases/75-runtime-prefix-propagation/75-RESEARCH.md
@.planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
@.planning/phases/75-runtime-prefix-propagation/75-VERIFICATION.md
@.planning/phases/75-runtime-prefix-propagation/75-03-SUMMARY.md
@.planning/phases/75-runtime-prefix-propagation/75-04-SUMMARY.md
@.planning/phases/75-runtime-prefix-propagation/75-07-SUMMARY.md
@test/chimeway/runtime_prefix_integration_test.exs
@test/support/prefixed_runtime_case.ex
@test/chimeway/admin_test.exs
@test/chimeway/orchestration/recovery_test.exs
@test/chimeway/orchestration/workflow_progression_test.exs
@lib/chimeway/admin.ex
@lib/chimeway/deliveries.ex
@lib/chimeway/dispatch/signal_router_worker.ex
@lib/chimeway/dispatch/workflow_progression_worker.ex
@lib/chimeway/workflows/progression.ex
@mix.exs
</context>

## Source Coverage Audit

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | - | Thread the storage-prefix contract through Chimeway runtime behavior so real notification flows read and write in the configured schema | 75-08 | COVERED | Gap closure expands the focused runtime-prefix gate to execute the missing admin, recovery, signal-routing, and progression paths.
REQ | RUN-01 | Trigger fanout persists events, notifications, deliveries, and attempts into configured prefix | 75-08 | COVERED | Final `mix verify.runtime_prefix` reruns existing trigger assertions after the expanded tests.
REQ | RUN-02 | Idempotency, duplicate detection, lifecycle reads, traces, and explainability use configured prefix | 75-08 | COVERED | Final focused gate reruns trigger, trace, and inbox assertions; admin DTO assertions also exercise lifecycle read models.
REQ | RUN-03 | Workflow progression, signal routing, digests, policy/preferences, webhooks, dispatch workers, and string-source insert_all propagate prefix options | 75-08 | COVERED | Task 2 adds actual signal router and workflow progression execution under prefix mode; final gate reruns covered digest/webhook/policy/preference paths.
REQ | RUN-04 | Admin, inbox, trace, and recovery read/write surfaces use configured prefix and remain tenant/redaction-safe | 75-08 | COVERED | Task 1 adds admin read-model and recovery execution assertions under prefix mode.
RESEARCH | repo-defaults | Chimeway.Repo.default_options/1 remains the primary runtime prefix propagation seam | 75-08 | COVERED | Tests must use ordinary public APIs and worker durable IDs, not public prefix arguments.
RESEARCH | prefixed-suite | Separate non-async prefixed runtime suite is the evidence surface | 75-08 | COVERED | All gap work stays in `test/chimeway/runtime_prefix_integration_test.exs`.
RESEARCH | oban-boundary | Oban job table prefix remains separate from Chimeway storage prefix | 75-08 | COVERED | Task 2 keeps worker args durable-ID based and does not assert Chimeway storage through Oban job rows.
RESEARCH | no-packages | No package installs are needed | 75-08 | COVERED | Plan contains no package-manager install tasks.
CONTEXT | D-01, D-02, D-03, D-04, D-05 | Repo defaults, Storage.repo_opts/1 contract, no ordinary API prefix, local filtering, explicit probes | 75-08 | COVERED | Tests call ordinary APIs/opts and preserve existing explicit-probe behavior through regression gates.
CONTEXT | D-06, D-07, D-08 | Reject schema prefixes, wrapper repos, and manual per-operation prefix as the primary strategy | 75-08 | COVERED | Plan is test-focused and does not introduce new runtime prefix surfaces.
CONTEXT | D-09, D-10, D-11, D-12, D-13 | Avoid ambient routing; audit transactions, workers, insert_all; keep job args durable; keep Oban separate | 75-08 | COVERED | Task 2 executes worker/progression paths and verifies durable ID reloads without prefix-bearing args.
CONTEXT | D-14, D-15, D-16, D-17, D-18, D-19 | Public legacy baseline, prefixed runtime proof, required coverage, focused alias | 75-08 | COVERED | Final verification runs focused and regression gates after expanding the focused runtime test.
CONTEXT | D-20, D-21, D-22, D-23, D-24 | Backend-only DX, configure-once APIs, deterministic evidence, operator continuity, durable explainability | 75-08 | COVERED | Admin/recovery assertions verify operator DTOs and recovery facts without adding storage terms to public APIs.
CONTEXT | D-25, D-26, D-27, D-28, D-29 | Static visible schema ownership, deterministic storage behavior, stable identity, typed config, no dynamic prefixes | 75-08 | COVERED | Existing prefixed harness and final gates remain the evidence surface; dynamic per-tenant database prefixes are not planned.

## Artifacts This Phase Produces

- Expanded `:runtime_prefix_operator` coverage in `test/chimeway/runtime_prefix_integration_test.exs`.
- Expanded `:runtime_prefix_workflow_signal` coverage in `test/chimeway/runtime_prefix_integration_test.exs`.
- Optional test-local dispatcher/helper modules inside `test/chimeway/runtime_prefix_integration_test.exs` if deterministic recovery or workflow setup needs them.
- Direct prefixed test calls to `Admin.command_center/1`, `Admin.recent_problem_deliveries/1`, `Admin.definitions/1`, `Admin.feed/1`, `Admin.recovery_candidates/1`, and `Admin.outcome_totals/1`.
- Direct prefixed test calls to `Deliveries.begin_recovery/2`, `Deliveries.recover_delivery/2` or `Chimeway.recover_delivery/2`, and `Deliveries.recover_event/2` or `Chimeway.recover_event/2`.
- Direct prefixed test calls to `SignalRouterWorker.perform/1` and `WorkflowProgressionWorker.perform/1` or `Progression.progress_run/2`.
- Final command evidence for `mix verify.runtime_prefix`, `mix ci.test`, and `mix verify.install_golden`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand prefixed admin and recovery execution coverage</name>
  <files>test/chimeway/runtime_prefix_integration_test.exs</files>
  <read_first>
    .planning/phases/75-runtime-prefix-propagation/75-VERIFICATION.md
    .planning/phases/75-runtime-prefix-propagation/75-03-SUMMARY.md
    test/chimeway/runtime_prefix_integration_test.exs
    test/support/prefixed_runtime_case.ex
    test/chimeway/admin_test.exs
    test/chimeway/orchestration/recovery_test.exs
    lib/chimeway/admin.ex
    lib/chimeway/deliveries.ex
  </read_first>
  <behavior>
    - Prefixed admin assertions call every read model named by the verifier: `Admin.command_center/1`, `Admin.recent_problem_deliveries/1`, `Admin.definitions/1`, `Admin.feed/1`, `Admin.recovery_candidates/1`, and `Admin.outcome_totals/1`.
    - Admin assertions use prefixed event, notification, delivery, and recovery-candidate rows and assert DTOs contain expected notification keys, recipient IDs, statuses, tenant scope, and redacted field shapes.
    - Recovery assertions execute `Deliveries.begin_recovery/2`, `Deliveries.recover_delivery/2` or `Chimeway.recover_delivery/2`, and `Deliveries.recover_event/2` or `Chimeway.recover_event/2` under `Application` prefix `"chimeway"`.
    - Recovery assertions prove metadata is stamped only on configured-schema rows and public Chimeway tables remain empty for the exercised path.
  </behavior>
  <action>Expand the existing `@tag :runtime_prefix_operator` test or split it into multiple tests that keep the `:runtime_prefix_operator` tag. Reuse the existing `ChimewayTest.Notifiers.RuntimePrefix`, `create_notification/1`, `create_pending_delivery/1`, `assert_prefixed_only/2`, `prefixed_count/1`, and `public_count/1` helpers before adding any new helper. Create deterministic prefixed rows for an admin problem delivery, a feed notification that has been seen/read, a recovery delivery candidate, and a recovery event candidate. Call all six `Chimeway.Admin` read-model functions listed in the behavior block and assert the returned DTOs identify only the prefixed fixture rows; also assert DTO maps do not include payload, render data, provider response, metadata, token, secret, session, or params keys. For recovery execution, call `Deliveries.begin_recovery/2` on a prefixed ready delivery with an old `updated_at`, then execute delivery recovery and event recovery through the public Chimeway delegate or `Deliveries` context using ordinary arguments. If deterministic recovery dispatch requires a test-local dispatcher, add a small module in this test file modeled on `Chimeway.Orchestration.RecoveryDispatcherStub` that receives durable IDs, reloads rows through `Repo`, updates only test rows, and records messages to the test process. Do not pass prefix options to ordinary admin, inbox, trace, or recovery APIs per D-03 and D-21. Do not add logs, telemetry, or assertions that print raw payloads, rendered bodies, provider bodies, session tokens, or auth params.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_operator --warnings-as-errors</automated>
    <automated>MIX_ENV=test mix test test/chimeway/admin_test.exs test/chimeway/orchestration/recovery_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test/chimeway/runtime_prefix_integration_test.exs` contains calls to `Admin.command_center`, `Admin.recent_problem_deliveries`, `Admin.definitions`, `Admin.feed`, `Admin.recovery_candidates`, and `Admin.outcome_totals`.
    - Source assertion: the same file contains calls to `begin_recovery`, `recover_delivery`, and `recover_event`.
    - Source assertion: the new admin/recovery assertions do not pass `prefix:` into ordinary Chimeway public APIs.
    - Behavior assertion: `MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_operator --warnings-as-errors` exits 0.
    - Behavior assertion: after the operator tag runs, `public_count("chimeway_events")`, `public_count("chimeway_notifications")`, `public_count("chimeway_deliveries")`, and any recovery-touched Chimeway table asserted by the test remain 0.
    - CLI assertion: `MIX_ENV=test mix test test/chimeway/admin_test.exs test/chimeway/orchestration/recovery_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The focused operator prefix tag now executes admin read models and recovery execution APIs against configured-schema rows, preserves redaction/tenant behavior, and proves there is no public fallback for those paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute signal routing and workflow progression under prefix mode</name>
  <files>test/chimeway/runtime_prefix_integration_test.exs</files>
  <read_first>
    .planning/phases/75-runtime-prefix-propagation/75-VERIFICATION.md
    .planning/phases/75-runtime-prefix-propagation/75-04-SUMMARY.md
    test/chimeway/runtime_prefix_integration_test.exs
    test/support/prefixed_runtime_case.ex
    test/chimeway/orchestration/workflow_progression_test.exs
    lib/chimeway/dispatch/signal_router_worker.ex
    lib/chimeway/dispatch/workflow_progression_worker.ex
    lib/chimeway/workflows/progression.ex
    mix.exs
  </read_first>
  <behavior>
    - The prefixed `:runtime_prefix_workflow_signal` proof drives a workflow run into a durable waiting state with `pending_signals == ["chimeway.notification.read"]`.
    - The test persists a matching signal, calls `SignalRouterWorker.perform/1` with `%Oban.Job{args: %{"signal_id" => signal.id}}`, and asserts the run is reloaded and resumed from configured storage.
    - The test executes `WorkflowProgressionWorker.perform/1` with `%Oban.Job{args: %{"workflow_run_id" => workflow_run.id}}` or executes `Progression.progress_run/2` against the same durable workflow run and asserts next-step delivery/transition writes are prefixed.
    - The test asserts worker args remain durable-ID only and never carry prefix values, copied payloads, tenant-scoped database state, or rendered data.
  </behavior>
  <action>Expand the existing `@tag :runtime_prefix_workflow_signal` test so it executes the runtime paths rather than stopping at signal persistence and job enqueue. Use the workflow progression analogs from `test/chimeway/orchestration/workflow_progression_test.exs`: drive the first-step delivery through `Deliveries.transition_status/2` and `Deliveries.record_attempt/2` or an equivalent deterministic setup so `Progression.progress_run/2` enters `:waiting` with the expected cancel signal. Persist a matching signal through `Signal.track/4`, then call `SignalRouterWorker.perform/1` directly with a durable `signal_id` job. Assert the workflow run becomes `:active`, `pending_signals` is empty, `status_reason` is `"signal_received"`, and exactly one `signal_received` transition exists in prefixed storage. Then prove the progression seam by either setting the persisted wait `due_at` to a past timestamp and calling `WorkflowProgressionWorker.perform/1` with durable `workflow_run_id`, or by calling `Progression.progress_run/2` with a deterministic `now:` that advances the wait. Assert a next-step email delivery and the expected workflow transition rows are written under the configured Chimeway prefix, and assert public counts stay 0 for events, notifications, deliveries, signals, workflow runs, and workflow transitions. If deterministic setup requires a test-local plan-only dispatcher, add it inside this test file and keep it limited to planning rows through existing Chimeway contexts. Keep worker args limited to `signal_id` and `workflow_run_id` per D-12; do not use transaction opts, process state, Postgres search_path, Oban config, or prefix-bearing job args as the storage-routing mechanism per D-09 and D-13.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_workflow_signal --warnings-as-errors</automated>
    <automated>mix verify.runtime_prefix</automated>
    <automated>mix ci.test</automated>
    <automated>mix verify.install_golden</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test/chimeway/runtime_prefix_integration_test.exs` contains a direct `SignalRouterWorker.perform` call in the `:runtime_prefix_workflow_signal` coverage.
    - Source assertion: the same coverage contains either `WorkflowProgressionWorker.perform` or `Progression.progress_run`.
    - Source assertion: signal and workflow progression job args are asserted to contain only `signal_id` or `workflow_run_id`.
    - Behavior assertion: `MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_workflow_signal --warnings-as-errors` exits 0.
    - Behavior assertion: the workflow/signal proof asserts prefixed-only row counts for `chimeway_signals`, `chimeway_workflow_runs`, `chimeway_workflow_transitions`, and the next-step `chimeway_deliveries` rows it creates.
    - CLI assertion: `mix verify.runtime_prefix` exits 0.
    - CLI assertion: `mix ci.test` exits 0.
    - CLI assertion: `mix verify.install_golden` exits 0.
  </acceptance_criteria>
  <done>The focused runtime-prefix gate executes signal routing and workflow progression paths under prefix mode, and the focused plus regression gates pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prefixed runtime tests -> PostgreSQL schemas | Tests inspect configured and public schemas and must prove routing without relying on ambient search path state. |
| operator/admin reads -> DTOs | Admin read models cross from persisted lifecycle rows into operator-facing DTO maps. |
| recovery API -> durable metadata | Operator recovery evidence is persisted on delivery rows and must remain allowlisted. |
| worker args -> Chimeway storage | Durable IDs cross worker boundaries and workers rehydrate rows from configured storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-22 | Information Disclosure | Admin prefixed assertions | mitigate | Assert DTOs expose allowlisted fields only and do not include payload, render, provider, metadata, session, params, token, or secret fields. |
| T-75-23 | Elevation of Privilege | Admin/recovery tenant predicates | mitigate | Use tenant-scoped fixtures and keep existing admin/recovery regression suites green. |
| T-75-24 | Repudiation | Recovery execution | mitigate | Assert recovery source, reason, actor evidence, and recovered_at are durably stored on configured-schema rows. |
| T-75-25 | Tampering | Worker durable IDs | mitigate | Assert worker args contain only durable IDs and execution reloads canonical rows from configured storage. |
| T-75-26 | Information Disclosure | Test diagnostics and output | mitigate | Do not add logs, telemetry, or failing assertions that print raw payload, rendered data, provider body, session token, or auth params. |
| T-75-SC | Tampering | Package installs | accept | No package-manager installs are planned for this gap closure. |
</threat_model>

<verification>
Run the narrow tags while developing, then run the full gap-closure gate set:

<automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_operator --warnings-as-errors</automated>
<automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_workflow_signal --warnings-as-errors</automated>
<automated>mix verify.runtime_prefix</automated>
<automated>mix ci.test</automated>
<automated>mix verify.install_golden</automated>
</verification>

<success_criteria>
The Phase 75 verification gaps are closed when `test/chimeway/runtime_prefix_integration_test.exs` executes the missing admin read models, recovery execution APIs, signal router worker, and workflow progression path under prefix mode, all asserted through the existing `mix verify.runtime_prefix` alias, with `mix ci.test` and `mix verify.install_golden` still green.
</success_criteria>

<output>
Create `.planning/phases/75-runtime-prefix-propagation/75-08-SUMMARY.md` when done.
</output>
