---
phase: 75-runtime-prefix-propagation
plan: 02
type: execute
wave: 1
depends_on: ["75-01"]
files_modified:
  - lib/chimeway/repo.ex
  - lib/chimeway/trigger.ex
autonomous: true
requirements: [RUN-01, RUN-02, RUN-03]

must_haves:
  truths:
    - "Chimeway.Repo.default_options/1 delegates normal operations to Chimeway.Storage.repo_opts/1 per D-01 and D-02."
    - "Transactions do not carry Chimeway storage prefix through transaction options per D-09."
    - "Trigger fanout persists events, string-source notifications, deliveries, attempts, and duplicate idempotency reads in configured storage per RUN-01, RUN-02, D-10, and D-11."
    - "Chimeway.trigger/3 remains an ordinary API with no prefix option requirement per D-03 and D-21."
  artifacts:
    - path: "lib/chimeway/repo.ex"
      provides: "Repo-wide runtime prefix default hook"
      exports: ["default_options/1"]
    - path: "lib/chimeway/trigger.ex"
      provides: "Trigger fanout and duplicate idempotency path audited against repo defaults"
      exports: ["trigger/3"]
  key_links:
    - from: "lib/chimeway/repo.ex"
      to: "lib/chimeway/storage.ex"
      via: "default_options/1 delegates to Chimeway.Storage.repo_opts/1"
      pattern: "Chimeway\\.Storage\\.repo_opts"
    - from: "lib/chimeway/trigger.ex"
      to: "lib/chimeway/repo.ex"
      via: "Ecto.Multi transaction callback repo and Repo default operation options"
      pattern: "Repo\\.transaction|insert_all"
---

<objective>
Implement the central repo-default prefix propagation hook and prove trigger fanout, duplicate detection, and string-source notification inserts use it.

Purpose: satisfy the narrowest core runtime contract before touching higher-level surfaces.
Output: `Chimeway.Repo.default_options/1` plus green repo and trigger runtime prefix tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/75-runtime-prefix-propagation/75-CONTEXT.md
@.planning/phases/75-runtime-prefix-propagation/75-RESEARCH.md
@.planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
@.planning/phases/75-runtime-prefix-propagation/75-VALIDATION.md
@lib/chimeway/repo.ex
@lib/chimeway/storage.ex
@lib/chimeway/trigger.ex
@test/chimeway/repo_prefix_test.exs
@test/chimeway/runtime_prefix_integration_test.exs
</context>

## Artifacts This Phase Produces

- `Chimeway.Repo.default_options/1`
- Green behavior for `Chimeway.Repo.default_options(:transaction)`
- Trigger fanout prefix proof for `Chimeway.Trigger.trigger/3`
- String-source `repo.insert_all("chimeway_notifications", rows)` placement proof

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement repo-wide runtime prefix defaults</name>
  <files>lib/chimeway/repo.ex</files>
  <read_first>
    lib/chimeway/repo.ex
    lib/chimeway/storage.ex
    test/chimeway/repo_prefix_test.exs
    deps/ecto/lib/ecto/repo.ex
    .planning/phases/75-runtime-prefix-propagation/75-CONTEXT.md
  </read_first>
  <behavior>
    - `Chimeway.Repo.default_options(:transaction)` returns `[]`.
    - `Chimeway.Repo.default_options(_operation)` delegates to `Chimeway.Storage.repo_opts/1` with no copied mapping logic.
    - Explicit operation opts merge after defaults so diagnostic `prefix:` probes still win.
  </behavior>
  <action>Add `@impl true` and `default_options/1` to `Chimeway.Repo`. For `:transaction`, return `[]` per D-09. For every other operation, delegate to `Chimeway.Storage.repo_opts()` per D-01 and D-02. Do not introduce `@schema_prefix`, a storage wrapper repo/facade, a process dictionary/search_path carrier, or per-call public API prefix plumbing per D-06, D-07, D-08, D-21, and D-29. Keep `Chimeway.Storage.repo_opts/1` as the only mapping contract and preserve `Keyword.put_new/3` probe semantics per D-05.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/repo_prefix_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `lib/chimeway/repo.ex` defines `def default_options(:transaction), do: []`.
    - Source assertion: `lib/chimeway/repo.ex` calls `Chimeway.Storage.repo_opts()` in the non-transaction branch.
    - Behavior assertion: `MIX_ENV=test mix test test/chimeway/repo_prefix_test.exs --warnings-as-errors` exits 0.
    - Source assertion: no `@schema_prefix` or new `Chimeway.Storage.Repo` module appears under `lib/chimeway`.
  </acceptance_criteria>
  <done>Repo defaults route ordinary Chimeway-owned table operations through the configured static prefix while preserving explicit probes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove trigger fanout and duplicate idempotency use configured storage</name>
  <files>lib/chimeway/trigger.ex</files>
  <read_first>
    lib/chimeway/trigger.ex
    lib/chimeway/repo.ex
    test/chimeway/runtime_prefix_integration_test.exs
    test/chimeway/trigger_pipeline_test.exs
    lib/chimeway/deliveries.ex
    .planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
  </read_first>
  <behavior>
    - Trigger writes `chimeway_events`, string-source `chimeway_notifications`, planned `chimeway_deliveries`, and `chimeway_delivery_attempts` into the configured schema.
    - Duplicate idempotency lookup rehydrates the existing event from configured storage and does not dispatch again.
    - Trigger result preserves stable `notification_key` and version identity.
  </behavior>
  <action>Run the `:runtime_prefix_trigger` proof created by Plan 75-01. If `Chimeway.Repo.default_options/1` covers the `Ecto.Multi` callback repo and `repo.insert_all("chimeway_notifications", rows)` path, keep `lib/chimeway/trigger.ex` unchanged except for strictly necessary compile or formatting adjustments. If the trigger proof shows a specific operation is not covered by Ecto defaults in this pinned stack, add an explicit `Chimeway.Storage.repo_opts/1` call only at that failing operation per the discretion note in D-01 and D-08, and explain the exception in the summary. Do not add prefix values to `Chimeway.trigger/3` opts, dispatcher opts, or Oban worker args per D-03, D-11, and D-12.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_trigger --warnings-as-errors</automated>
    <automated>MIX_ENV=test mix test test/chimeway/trigger_pipeline_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: the `:runtime_prefix_trigger` test tag exits 0 and proves prefixed counts for event, notification, delivery, and attempt rows.
    - Behavior assertion: duplicate idempotency returns `{:duplicate, event}` and event count remains one in configured storage.
    - Source assertion: `lib/chimeway/trigger.ex` does not add `:prefix` to public trigger opts or worker/job args.
    - Source assertion: string-source notification insertion remains grounded in `chimeway_notifications` and is covered by the runtime test.
  </acceptance_criteria>
  <done>Trigger fanout and duplicate idempotency are green under configured prefix mode and existing public trigger tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application config -> Repo options | Validated prefix config is applied to repo operations. |
| Trigger API -> persistence | Untrusted notification params become durable rows without caller-supplied DB prefix. |
| Transaction callback repo -> insert_all | Ecto.Multi callback repo writes string-source rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04 | Information Disclosure | Chimeway.Repo.default_options/1 | mitigate | Centralize prefix routing through Storage.repo_opts/1 and test public fallback absence. |
| T-75-05 | Tampering | Chimeway.trigger/3 opts | mitigate | Keep prefix out of public trigger opts and preserve static install-level config only. |
| T-75-06 | Repudiation | Duplicate idempotency lookup | mitigate | Ensure duplicate path rehydrates the configured-schema event for explainable idempotency. |
| T-75-SC | Tampering | Package installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run `MIX_ENV=test mix test test/chimeway/repo_prefix_test.exs --warnings-as-errors` and `MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_trigger --warnings-as-errors`.
</verification>

<success_criteria>
Repo defaults are implemented, trigger placement and duplicate idempotency pass in prefixed mode, and public trigger behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/75-runtime-prefix-propagation/75-02-SUMMARY.md` when done.
</output>
