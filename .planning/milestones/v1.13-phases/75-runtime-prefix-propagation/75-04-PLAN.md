---
phase: 75-runtime-prefix-propagation
plan: 04
type: execute
wave: 2
depends_on: ["75-02"]
files_modified:
  - lib/chimeway/dispatch/oban.ex
  - lib/chimeway/dispatch/oban_worker.ex
  - lib/chimeway/dispatch/deferred_resume_worker.ex
  - lib/chimeway/workflows.ex
  - lib/chimeway/workflows/progression.ex
  - lib/chimeway/signal.ex
autonomous: true
requirements: [RUN-03]

must_haves:
  truths:
    - "Workflow progression and signal routing use the configured Chimeway prefix per RUN-03."
    - "Chimeway.Dispatch.ObanWorker.perform/1 and Chimeway.Dispatch.DeferredResumeWorker.perform/1 reload deliveries by durable delivery_id from configured Chimeway storage per RUN-03, D-12, and D-16."
    - "Oban job-table queries remain separate from Chimeway-owned table prefix per D-13."
    - "Oban job args remain durable-ID based and do not carry prefix values, copied payloads, tenant-scoped DB state, or rendered data per D-12."
    - "Transactional flows, Multi.run callback repo calls, preloads, update_all, delete_all, and insert_all are audited per D-10."
  artifacts:
    - path: "lib/chimeway/dispatch/oban.ex"
      provides: "Direct Oban.Job query exception for independent Oban job-table prefix"
    - path: "lib/chimeway/dispatch/oban_worker.ex"
      provides: "Delivery dispatch worker reload-by-delivery_id audited under repo defaults"
    - path: "lib/chimeway/dispatch/deferred_resume_worker.ex"
      provides: "Deferred resume worker reload-by-delivery_id audited under repo defaults"
    - path: "lib/chimeway/workflows.ex"
      provides: "Workflow definitions/runs audited under repo defaults"
    - path: "lib/chimeway/workflows/progression.ex"
      provides: "Workflow progression transaction audited under repo defaults"
    - path: "lib/chimeway/signal.ex"
      provides: "Signal tracking and routing enqueue audited under repo defaults"
  key_links:
    - from: "lib/chimeway/dispatch/oban.ex"
      to: "Oban config"
      via: "direct Oban.Job Repo.all/delete_all use Oban job-table opts instead of Chimeway.Storage.repo_opts/1"
      pattern: "Oban\\.Job|Repo\\.(all|delete_all)"
    - from: "lib/chimeway/dispatch/oban_worker.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "ObanWorker.perform/1 reloads delivery_id through Deliveries.get_delivery!/1 under Repo defaults"
      pattern: "Deliveries\\.get_delivery!"
    - from: "lib/chimeway/dispatch/deferred_resume_worker.ex"
      to: "lib/chimeway/repo.ex"
      via: "DeferredResumeWorker.perform/1 resumes delivery_id inside Repo.transaction under configured storage"
      pattern: "Repo\\.transaction"
---

<objective>
Propagate runtime prefix behavior through workflow/signal orchestration paths while preserving the separate Oban job-table prefix boundary.

Purpose: RUN-03 depends on worker reloads by durable IDs and on the Oban job table remaining separate from Chimeway-owned storage.
Output: green workflow/signal prefixed tests and safe direct Oban.Job handling.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/75-runtime-prefix-propagation/75-CONTEXT.md
@.planning/phases/75-runtime-prefix-propagation/75-RESEARCH.md
@.planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
@lib/chimeway/dispatch/oban.ex
@lib/chimeway/dispatch/oban_worker.ex
@lib/chimeway/dispatch/deferred_resume_worker.ex
@lib/chimeway/workflows.ex
@lib/chimeway/workflows/progression.ex
@lib/chimeway/signal.ex
@test/chimeway/runtime_prefix_integration_test.exs
@test/chimeway/dispatch/oban_test.exs
@test/chimeway/dispatch/oban_worker_test.exs
@test/chimeway/orchestration/deferred_resume_test.exs
@test/chimeway/dispatch/signal_router_worker_test.exs
@test/chimeway/dispatch/workflow_progression_worker_test.exs
</context>

## Artifacts This Phase Produces

- A narrow private Oban job-table option helper in `Chimeway.Dispatch.Oban` if direct `Oban.Job` queries need explicit opts
- Prefixed proof for `Chimeway.Dispatch.ObanWorker.perform/1` delivery reloads by durable `delivery_id`
- Prefixed proof for `Chimeway.Dispatch.DeferredResumeWorker.perform/1` deferred resume reloads by durable `delivery_id`
- Prefixed proof for `Chimeway.Workflows`, `Chimeway.Workflows.Progression`, and `Chimeway.Signal`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keep direct Oban.Job queries on Oban's job-table prefix</name>
  <files>lib/chimeway/dispatch/oban.ex</files>
  <read_first>
    lib/chimeway/dispatch/oban.ex
    deps/oban/lib/oban/repo.ex
    deps/oban/lib/oban/config.ex
    test/chimeway/dispatch/oban_test.exs
    test/chimeway/runtime_prefix_integration_test.exs
    .planning/phases/75-runtime-prefix-propagation/75-RESEARCH.md
  </read_first>
  <behavior>
    - Direct `Repo.all(from job in Oban.Job, ...)` and `Repo.delete_all(from job in Oban.Job, ...)` do not inherit Chimeway's storage prefix.
    - Digest duplicate collapse still finds and removes duplicate Oban jobs.
    - Chimeway storage prefix and Oban job-table prefix are treated as separate configuration domains.
  </behavior>
  <action>Add a narrow private helper in `Chimeway.Dispatch.Oban` for direct `Oban.Job` repo operations if tests show they inherit the Chimeway storage prefix. Source the helper from Oban configuration/default behavior, not from `Chimeway.Storage.repo_opts/1`, per D-13. Apply the helper only to `matching_digest_flush_jobs/2` and duplicate `Repo.delete_all` over `Oban.Job`; do not use it for Chimeway-owned tables. Keep all job args as durable IDs and do not add prefix, payload, tenant-state, or rendered data to args per D-12.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/dispatch/oban_test.exs --warnings-as-errors</automated>
    <automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_oban_boundary --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: any direct `Oban.Job` `Repo.all` or `Repo.delete_all` in `lib/chimeway/dispatch/oban.ex` passes explicit Oban-job repo opts or uses an Oban-supported API.
    - Source assertion: the Oban-job helper does not call `Chimeway.Storage.repo_opts/1`.
    - Behavior assertion: digest duplicate collapse tests continue to pass.
    - Behavior assertion: `:runtime_prefix_oban_boundary` proves Chimeway-owned rows are prefixed while Oban job rows remain queryable through Oban's own table configuration.
  </acceptance_criteria>
  <done>Direct Oban job-table queries no longer risk being routed to `chimeway.oban_jobs` by Chimeway table defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove workflow, signal, and dispatch worker reload paths</name>
  <files>lib/chimeway/dispatch/oban_worker.ex, lib/chimeway/dispatch/deferred_resume_worker.ex, lib/chimeway/workflows.ex, lib/chimeway/workflows/progression.ex, lib/chimeway/signal.ex</files>
  <read_first>
    lib/chimeway/dispatch/oban_worker.ex
    lib/chimeway/dispatch/deferred_resume_worker.ex
    lib/chimeway/workflows.ex
    lib/chimeway/workflows/progression.ex
    lib/chimeway/signal.ex
    test/chimeway/runtime_prefix_integration_test.exs
    test/chimeway/dispatch/oban_worker_test.exs
    test/chimeway/orchestration/deferred_resume_test.exs
    test/chimeway/orchestration/workflow_progression_test.exs
    test/chimeway/signal_test.exs
  </read_first>
  <behavior>
    - Workflow definitions, runs, transitions, lock helpers, and due-run progression operate in configured storage.
    - Signal tracking and signal router worker reload signals by durable ID in configured storage.
    - `Chimeway.Dispatch.ObanWorker.perform/1` reloads the delivery by durable `delivery_id` in configured storage before dispatching.
    - `Chimeway.Dispatch.DeferredResumeWorker.perform/1` reloads/resumes the deferred delivery by durable `delivery_id` in configured storage inside its transaction.
  </behavior>
  <action>Run and satisfy both the `:runtime_prefix_workflow_signal` and `:runtime_prefix_dispatch_worker` proofs. The dispatch-worker proof must exercise `Chimeway.Dispatch.ObanWorker.perform/1` reloading a dispatchable delivery by `delivery_id` and `Chimeway.Dispatch.DeferredResumeWorker.perform/1` reloading/resuming a deferred delivery by `delivery_id` from configured storage. Prefer the repo-wide default behavior from Plan 75-02 for ordinary Chimeway-owned tables. If a specific transaction body, `Multi.run/3` callback, preload, `update_all`, `delete_all`, `ObanWorker` reload, or `DeferredResumeWorker` reload does not receive repo defaults in the pinned stack, add `Chimeway.Storage.repo_opts/1` at that operation only and cite the operation in the summary per D-10. Preserve durable lifecycle facts and stable notification identity per D-24 and D-27. Do not rely on transaction options, process state, Postgres search_path, or Oban config to carry Chimeway table prefix per D-09 and D-13.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_workflow_signal --warnings-as-errors</automated>
    <automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_dispatch_worker --warnings-as-errors</automated>
    <automated>MIX_ENV=test mix test test/chimeway/orchestration/workflow_progression_test.exs test/chimeway/signal_test.exs test/chimeway/dispatch/signal_router_worker_test.exs test/chimeway/dispatch/workflow_progression_worker_test.exs test/chimeway/dispatch/oban_worker_test.exs test/chimeway/orchestration/deferred_resume_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `:runtime_prefix_workflow_signal` exits 0 for workflow and signal coverage created by Plan 75-01.
    - Behavior assertion: `:runtime_prefix_dispatch_worker` exits 0 and proves `ObanWorker.perform/1` and `DeferredResumeWorker.perform/1` reload deliveries by durable `delivery_id` from configured storage.
    - Behavior assertion: `test/chimeway/dispatch/oban_worker_test.exs` and `test/chimeway/orchestration/deferred_resume_test.exs` remain green in public legacy mode.
    - Source assertion: worker args in this plan remain `delivery_id`, `workflow_run_id`, `signal_id`, `bucket_id`, or `ingress_id` durable identifiers; no prefix value is persisted in args.
    - Source assertion: no code relies on transaction opts or `search_path` to route Chimeway-owned table access.
    - Behavior assertion: existing workflow and signal public-mode tests remain green.
  </acceptance_criteria>
  <done>Workflow, signal, delivery dispatch, and deferred resume runtime paths use configured storage for Chimeway-owned rows while keeping Oban job storage separate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chimeway runtime -> Oban job table | Chimeway inserts jobs through Oban while direct Oban.Job reads are not Chimeway-owned storage. |
| worker args -> Chimeway storage | Durable IDs cross process/job boundaries and workers rehydrate rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-10 | Tampering | Direct Oban.Job queries | mitigate | Use Oban job-table opts for direct job queries and test digest duplicate collapse. |
| T-75-11 | Information Disclosure | Worker reloads | mitigate | Keep args durable-ID only and rehydrate from configured Chimeway storage. |
| T-75-SC | Tampering | Package installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run the Oban-boundary, workflow/signal, and dispatch-worker runtime prefix tags plus existing dispatch, workflow, deferred resume, and signal tests listed in task verification.
</verification>

<success_criteria>
Workflow, signal, dispatch worker, and deferred resume portions of RUN-03 are green in configured prefix mode, Oban job-table access remains separate, and worker args stay durable-ID based.
</success_criteria>

<output>
Create `.planning/phases/75-runtime-prefix-propagation/75-04-SUMMARY.md` when done.
</output>
