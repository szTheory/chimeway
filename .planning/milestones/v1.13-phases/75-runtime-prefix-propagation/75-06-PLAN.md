---
phase: 75-runtime-prefix-propagation
plan: 06
type: execute
wave: 2
depends_on: ["75-02"]
files_modified:
  - lib/chimeway/preferences.ex
  - lib/chimeway/policy.ex
  - lib/chimeway/policy/settings.ex
autonomous: true
requirements: [RUN-03]

must_haves:
  truths:
    - "Notification preferences, category preferences, and policy settings read/write configured Chimeway storage per RUN-03."
    - "Policy evaluation reloads notification and event context from configured storage and preserves suppression reasons."
    - "Policy/preference APIs remain ordinary domain APIs and do not expose public prefix options per D-03 and D-21."
    - "Policy telemetry and logs do not leak sensitive payload fields per project security guidance."
  artifacts:
    - path: "lib/chimeway/preferences.ex"
      provides: "Notification and category preference read/write prefix proof"
    - path: "lib/chimeway/policy.ex"
      provides: "Policy evaluation reload prefix proof"
    - path: "lib/chimeway/policy/settings.ex"
      provides: "Policy settings read/write prefix proof"
  key_links:
    - from: "lib/chimeway/policy.ex"
      to: "lib/chimeway/preferences.ex"
      via: "Policy.evaluate/2 calls Preferences.channel_enabled?/3 and category_enabled?/2"
      pattern: "Preferences\\."
    - from: "lib/chimeway/policy/settings.ex"
      to: "lib/chimeway/repo.ex"
      via: "Repo.insert/get_by/one operation defaults"
      pattern: "Repo\\.(insert|get_by|one)"
---

<objective>
Prove policy and preference runtime paths use the configured storage prefix without changing their public domain API.

Purpose: RUN-03 explicitly includes policy/preferences, and these paths affect suppression explainability.
Output: green policy/preference prefixed proof and unchanged public-mode policy/preference tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/75-runtime-prefix-propagation/75-CONTEXT.md
@.planning/phases/75-runtime-prefix-propagation/75-RESEARCH.md
@.planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
@lib/chimeway/preferences.ex
@lib/chimeway/policy.ex
@lib/chimeway/policy/settings.ex
@test/chimeway/runtime_prefix_integration_test.exs
@test/chimeway/preferences_test.exs
@test/chimeway/policy_test.exs
@test/chimeway/policy_settings_test.exs
@test/chimeway/policy/delayed_fallback_test.exs
</context>

## Artifacts This Phase Produces

- Prefixed proof for `Chimeway.Preferences.upsert_preference/1`
- Prefixed proof for `Chimeway.Preferences.upsert_category_preference/1`
- Prefixed proof for `Chimeway.Policy.Settings.upsert_settings/1`
- Prefixed proof for `Chimeway.Policy.evaluate/2`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove preference and policy settings writes use configured storage</name>
  <files>lib/chimeway/preferences.ex, lib/chimeway/policy/settings.ex</files>
  <read_first>
    lib/chimeway/preferences.ex
    lib/chimeway/policy/settings.ex
    test/chimeway/runtime_prefix_integration_test.exs
    test/chimeway/preferences_test.exs
    test/chimeway/policy_settings_test.exs
    .planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
  </read_first>
  <behavior>
    - Preference upsert/get paths use configured storage for `chimeway_notification_preferences`.
    - Category preference upsert/get paths use configured storage for `chimeway_category_preferences`.
    - Policy settings upsert/get/evaluate paths use configured storage for `chimeway_policy_settings`.
  </behavior>
  <action>Run and satisfy the `:runtime_prefix_preferences` tag from `test/chimeway/runtime_prefix_integration_test.exs`. Prefer `Chimeway.Repo.default_options/1` for ordinary `Repo.insert`, `Repo.get_by`, and `Repo.one` calls. If a specific preference or settings operation bypasses repo defaults in this stack, add `Chimeway.Storage.repo_opts/1` only at that operation and cite it in the summary per D-10. Do not add prefix arguments to `Chimeway.Preferences` or `Chimeway.Policy.Settings` public functions per D-03 and D-21.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_preferences --warnings-as-errors</automated>
    <automated>MIX_ENV=test mix test test/chimeway/preferences_test.exs test/chimeway/policy_settings_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: prefixed runtime policy tag proves notification preferences, category preferences, and policy settings rows in configured storage.
    - Source assertion: public preference/settings function signatures remain unchanged.
    - Behavior assertion: existing preference and policy settings tests remain green.
  </acceptance_criteria>
  <done>Preference and settings read/write paths use configured storage and keep existing public contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove policy evaluation reloads configured storage and preserves explainability</name>
  <files>lib/chimeway/policy.ex</files>
  <read_first>
    lib/chimeway/policy.ex
    lib/chimeway/preferences.ex
    lib/chimeway/policy/settings.ex
    test/chimeway/runtime_prefix_integration_test.exs
    test/chimeway/policy_test.exs
    test/chimeway/policy/delayed_fallback_test.exs
  </read_first>
  <behavior>
    - `Chimeway.Policy.evaluate/2` reloads notification/event context from configured storage.
    - Channel/category/settings suppressions preserve stable suppression reasons.
    - Policy telemetry metadata remains safe and does not include payload or rendered data.
  </behavior>
  <action>Run and satisfy policy evaluation coverage in the `:runtime_prefix_policy_eval` proof. Keep `Chimeway.Policy.evaluate/2` focused on delivery, preference, read-state, and settings rules. If a policy context reload bypasses repo defaults, add explicit `Chimeway.Storage.repo_opts/1` only at that reload operation and document the reason. Preserve suppression reason atoms/strings and telemetry-safe metadata per the project explainability and sensitive-payload rules.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_policy_eval --warnings-as-errors</automated>
    <automated>MIX_ENV=test mix test test/chimeway/policy_test.exs test/chimeway/policy/delayed_fallback_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: policy evaluation sees prefixed preference/settings rows and returns expected proceed/defer/suppress outcomes.
    - Source assertion: policy telemetry metadata remains limited to delivery/channel/key/category/correlation/suppression/planning facts.
    - Behavior assertion: existing policy tests remain green.
  </acceptance_criteria>
  <done>Policy evaluation uses configured storage while preserving suppression explainability and telemetry safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipient preference API -> storage | Host-provided recipient IDs and notification keys become preference rows. |
| policy evaluation -> operator facts | Suppression reasons become durable explainability facts. |
| policy telemetry -> logs | Policy metadata crosses into telemetry/logging surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-19 | Information Disclosure | Policy/preference reads | mitigate | Prove configured-schema reads and preserve recipient/key predicates. |
| T-75-20 | Repudiation | Suppression decisions | mitigate | Preserve stable suppression reasons and existing policy tests. |
| T-75-21 | Information Disclosure | Policy telemetry/logs | mitigate | Keep metadata payload-free and rerun policy tests. |
| T-75-SC | Tampering | Package installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run the preference/settings and policy-evaluation runtime prefix tags plus existing preference, policy settings, policy, and delayed fallback tests listed in task verification.
</verification>

<success_criteria>
Policy and preference portions of RUN-03 are green in configured prefix mode, public-mode tests remain green, and suppression explainability is unchanged.
</success_criteria>

<output>
Create `.planning/phases/75-runtime-prefix-propagation/75-06-SUMMARY.md` when done.
</output>
