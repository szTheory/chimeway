---
phase: 73-storage-prefix-contract
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/introduction/installation.md
  - guides/introduction/golden-path.md
  - test/chimeway/doc_contract_test.exs
  - test/chimeway/migration_contract_test.exs
autonomous: true
requirements:
  - PFX-01
  - PFX-04
  - UPG-01
must_haves:
  truths:
    - "PFX-01: adopter docs show prefix: \"chimeway\" for new schema-isolated installs and prefix: false for explicit public-schema legacy mode per D-01/D-03/D-04"
    - "PFX-04: current public-schema migration proof is framed as legacy compatibility, not the new-install default per D-04/D-18"
    - "UPG-01: docs state prefix: false keeps using existing public/unprefixed Chimeway tables and does not move data automatically per D-18/D-19"
  artifacts:
    - path: "README.md"
      provides: "compact runtime prefix install copy"
    - path: "guides/introduction/installation.md"
      provides: "installation storage-prefix choice copy"
    - path: "guides/introduction/golden-path.md"
      provides: "golden-path storage-prefix choice copy"
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "doc contract assertions for prefix snippets and legacy-public microcopy"
    - path: "test/chimeway/migration_contract_test.exs"
      provides: "legacy public-schema compatibility contract naming"
  key_links:
    - from: "test/chimeway/doc_contract_test.exs"
      to: "README.md"
      via: "required strings for accepted prefix snippets"
      pattern: "prefix:\\s+\\\"chimeway\\\""
    - from: "test/chimeway/doc_contract_test.exs"
      to: "guides/introduction/installation.md"
      via: "required strings for public legacy no-move copy"
      pattern: "does not move data"
    - from: "test/chimeway/migration_contract_test.exs"
      to: "current public migrations"
      via: "public schema checks named as legacy compatibility"
      pattern: "legacy"
---

<objective>
Lock the public-facing storage-prefix contract and the current public-schema compatibility language.

Purpose: PFX-01/PFX-04/UPG-01 require public docs and contract tests to distinguish new schema-isolated installs from existing public-schema legacy installs. This plan implements D-01, D-03, D-04, D-05, D-16, D-18, D-19, and D-22 without changing migration templates, generator CLI behavior, runtime prefix propagation, Oban config, or demo-host proof.

Output: docs and tests that prove `prefix: false` is an explicit public-schema legacy compatibility path that keeps existing unprefixed tables and does not move data.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-storage-prefix-contract/73-CONTEXT.md
@.planning/phases/73-storage-prefix-contract/73-RESEARCH.md
@.planning/phases/73-storage-prefix-contract/73-VALIDATION.md
@.planning/phases/73-storage-prefix-contract/73-PATTERNS.md
@README.md
@guides/introduction/installation.md
@guides/introduction/golden-path.md
@test/chimeway/doc_contract_test.exs
@test/chimeway/migration_contract_test.exs
@lib/chimeway/install/migrations.ex
@lib/mix/tasks/chimeway.gen.migrations.ex
</context>

<must_haves>
- README, installation guide, and golden path show both accepted runtime config snippets.
- Public-schema compatibility is described as explicit legacy mode for existing installs.
- Docs state `prefix: false` keeps using existing public/unprefixed Chimeway tables and does not move data.
- Current migration contract tests remain public-schema checks, but the naming/copy frames them as legacy compatibility.
- No docs claim migration generator prefix flags, generated prefixed migrations, Oban prefix proof, demo-host schema proof, or automatic data movement.
</must_haves>

## Artifacts This Phase Produces

- Module: `Chimeway.ConfigError` from Plan 73-01
- Module: `Chimeway.Storage` from Plan 73-01
- Function: `Chimeway.Storage.validate_prefix!/0` from Plan 73-01
- Function: `Chimeway.Storage.repo_opts/1` from Plan 73-01
- Config key: `config :chimeway, prefix: "chimeway"` documented for new schema-isolated installs
- Config key: `config :chimeway, prefix: false` documented for existing public-schema legacy installs
- Docs section: README runtime storage prefix copy
- Docs section: installation guide storage-prefix configuration copy
- Docs section: golden path storage-prefix configuration copy
- Test file: `test/chimeway/doc_contract_test.exs`
- Test file: `test/chimeway/migration_contract_test.exs`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock runtime prefix docs and public legacy microcopy</name>
  <read_first>
  - .planning/phases/73-storage-prefix-contract/73-CONTEXT.md
  - .planning/phases/73-storage-prefix-contract/73-PATTERNS.md
  - README.md
  - guides/introduction/installation.md
  - guides/introduction/golden-path.md
  - test/chimeway/doc_contract_test.exs
  </read_first>
  <files>README.md, guides/introduction/installation.md, guides/introduction/golden-path.md, test/chimeway/doc_contract_test.exs</files>
  <behavior>
  - Docs show `config :chimeway, prefix: "chimeway"` for new schema-isolated installs per D-01/D-03.
  - Docs show `config :chimeway, prefix: false` only for existing public-schema legacy installs per D-04/D-18.
  - Docs state public legacy mode keeps using existing unprefixed tables and does not move data per UPG-01/D-18.
  - Doc-contract tests assert stable short phrases instead of brittle full paragraphs.
  </behavior>
  <action>Add compact storage-prefix copy near the install/configuration sections in `README.md`, `guides/introduction/installation.md`, and `guides/introduction/golden-path.md`. Frame the choice as "new isolated Chimeway schema" versus "existing public-schema legacy install" per D-19. Include both accepted runtime snippets from D-01/D-04 and the required public-mode microcopy from D-18: use `prefix: false` only for an existing install whose Chimeway tables already live in public; it keeps using those unprefixed tables and does not move data. Extend `test/chimeway/doc_contract_test.exs` using the existing required-string loop pattern to require stable phrases such as `prefix: "chimeway"`, `prefix: false`, `existing install`, `public`, `unprefixed tables`, and `does not move data`. Do not document generator `--prefix` flags per D-05, Oban prefix proof per D-22, demo-host schema proof, or automatic public-to-chimeway data movement.</action>
  <acceptance_criteria>
  - README contains both accepted runtime prefix snippets.
  - Installation guide contains both accepted runtime prefix snippets and public legacy no-move copy.
  - Golden path contains both accepted runtime prefix snippets and public legacy no-move copy.
  - Doc contract tests require the accepted snippets and legacy-public no-move phrases.
  - Docs do not present `prefix: false` as the default for new installs.
  - Docs do not mention generator prefix flags or automatic data movement.
  </acceptance_criteria>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --warnings-as-errors</automated>
    <automated>bash -lc 'if rg -n -- "--prefix|automatic data move|automatically move|Oban prefix|oban prefix" README.md guides/introduction/installation.md guides/introduction/golden-path.md; then exit 1; fi'</automated>
  </verify>
  <done>Public docs and doc contracts make the runtime prefix choice explicit while preserving Phase 73 boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reframe public migration contract as legacy compatibility</name>
  <read_first>
  - .planning/phases/73-storage-prefix-contract/73-CONTEXT.md
  - .planning/phases/73-storage-prefix-contract/73-RESEARCH.md
  - .planning/phases/73-storage-prefix-contract/73-PATTERNS.md
  - test/chimeway/migration_contract_test.exs
  - lib/chimeway/install/migrations.ex
  - lib/mix/tasks/chimeway.gen.migrations.ex
  </read_first>
  <files>test/chimeway/migration_contract_test.exs</files>
  <behavior>
  - Current `public.chimeway_*` migration assertions remain intact.
  - Test names, describe blocks, or assertion messages identify those public checks as explicit legacy/public-schema compatibility per D-04/D-18.
  - No migration templates, raw SQL, or generator CLI parsing are changed in Phase 73 per D-05/D-16.
  </behavior>
  <action>Update `test/chimeway/migration_contract_test.exs` names and any local assertion messages so the existing `public` schema checks are described as legacy public-schema compatibility, not the new-install default. Preserve the current `to_regclass("public." <> name)` and `information_schema.columns.table_schema = 'public'` behavior. Do not modify `priv/chimeway_migrations`, `lib/chimeway/install/migrations.ex`, or `lib/mix/tasks/chimeway.gen.migrations.ex`; Phase 74 owns generated prefixes and raw SQL qualification per D-05/D-16.</action>
  <acceptance_criteria>
  - Migration contract tests still assert current public-schema table/index/column presence.
  - Test names or messages include "legacy" or "public-schema compatibility".
  - No files under `priv/chimeway_migrations/` are modified.
  - `lib/chimeway/install/migrations.ex` and `lib/mix/tasks/chimeway.gen.migrations.ex` are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix ecto.create</automated>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix ecto.migrate</automated>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/migration_contract_test.exs --warnings-as-errors</automated>
    <automated>git diff --exit-code -- priv/chimeway_migrations lib/chimeway/install/migrations.ex lib/mix/tasks/chimeway.gen.migrations.ex</automated>
  </verify>
  <done>The DB-backed migration contract proves current public-schema compatibility as an explicit legacy mode without changing migration generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> adopter config | Public documentation is copied into host applications and influences storage routing. |
| current migration contract -> compatibility interpretation | Existing public-schema checks must not be mistaken for the new-install default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-09 | Information Disclosure / Tampering | README/install/golden-path docs | mitigate | Require docs to show `prefix: "chimeway"` for new installs and `prefix: false` only for existing public-schema legacy installs. |
| T-73-10 | Tampering | doc-contract tests | mitigate | Lock public legacy copy stating unprefixed tables are reused and data is not moved. |
| T-73-11 | Repudiation | `test/chimeway/migration_contract_test.exs` | mitigate | Rename/current public assertions as legacy compatibility proof so later phases can distinguish intentional compatibility from accidental default behavior. |
| T-73-12 | Denial of Service | docs | mitigate | Do not instruct adopters to rely on boot-time schema existence checks or automatic data movement. |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | Phase 73 Plan 03 installs no packages and modifies no package manifests. |
</threat_model>

<verification>
<automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --warnings-as-errors</automated>
<automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix ecto.create</automated>
<automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix ecto.migrate</automated>
<automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/migration_contract_test.exs --warnings-as-errors</automated>
<automated>mix format --check-formatted</automated>
</verification>

<success_criteria>
- PFX-01: docs and doc contracts show both accepted runtime config snippets.
- PFX-04: public-schema compatibility is explicit legacy behavior in docs/tests.
- UPG-01: docs state `prefix: false` does not move data and keeps using existing public/unprefixed Chimeway tables.
- No migration template prefixing, generator CLI flags, runtime prefix propagation, Oban proof, demo-host schema proof, or public-to-chimeway move guide is added.
</success_criteria>

<output>
Create `.planning/phases/73-storage-prefix-contract/73-03-SUMMARY.md` when done.
</output>
