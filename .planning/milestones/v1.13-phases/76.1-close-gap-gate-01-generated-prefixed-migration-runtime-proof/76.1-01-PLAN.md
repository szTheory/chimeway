---
phase: 76.1-close-gap-gate-01-generated-prefixed-migration-runtime-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/chimeway/generated_prefixed_runtime_proof_test.exs
  - test/support/generated_prefixed_runtime_case.ex
  - mix.exs
autonomous: true
requirements:
  - GATE-01-GAP
must_haves:
  truths:
    - "GATE-01-GAP is closed by a runtime test that executes generated prefixed fixture migrations before calling Chimeway public APIs."
    - "The generated migration runtime bridge runs under mix verify.runtime_prefix, preserving Phase 76 D-13 runtime-prefix CI coverage."
    - "Installer golden proof and release-gate parity remain green, preserving Phase 76 D-14 path-gated installer policy."
    - "The proof avoids clone-only schema setup, migration-runner prefix flags, unchecked SQL identifiers, and secret-bearing logs."
  artifacts:
    - path: "test/chimeway/generated_prefixed_runtime_proof_test.exs"
      provides: "RED/GREEN bridge proof for generated prefixed migrations plus Chimeway runtime API behavior"
      contains: "defmodule Chimeway.GeneratedPrefixedRuntimeProofTest"
    - path: "test/support/generated_prefixed_runtime_case.ex"
      provides: "isolated generated-prefixed migration runtime test support"
      contains: "defmodule Chimeway.GeneratedPrefixedRuntimeCase"
    - path: "mix.exs"
      provides: "verify.runtime_prefix alias includes generated_prefixed_runtime_proof_test.exs"
      contains: "test/chimeway/generated_prefixed_runtime_proof_test.exs"
  key_links:
    - from: "mix.exs"
      to: "test/chimeway/generated_prefixed_runtime_proof_test.exs"
      via: "verify.runtime_prefix command includes the generated bridge proof file"
      pattern: "verify\\.runtime_prefix.*generated_prefixed_runtime_proof_test\\.exs"
    - from: "test/chimeway/generated_prefixed_runtime_proof_test.exs"
      to: "test/support/generated_prefixed_runtime_case.ex"
      via: "use Chimeway.GeneratedPrefixedRuntimeCase"
      pattern: "use Chimeway\\.GeneratedPrefixedRuntimeCase"
    - from: "test/support/generated_prefixed_runtime_case.ex"
      to: "test/fixtures/installer_golden_prefixed/STDOUT.txt"
      via: "fixture order materialization before Ecto.Migrator.run/4"
      pattern: "installer_golden_prefixed"
    - from: "test/support/generated_prefixed_runtime_case.ex"
      to: "lib/chimeway/repo.ex"
      via: "Ecto.Migrator.run/4 and runtime API proof use Chimeway.Repo-backed storage"
      pattern: "Ecto\\.Migrator\\.run\\(Chimeway\\.Repo|Ecto\\.Migrator\\.run\\(Repo"
---

<objective>
Close GATE-01-GAP by proving generated prefixed migrations and runtime-prefix behavior are joined in one executable gate.

Purpose: Phase 76 proved runtime-prefix behavior against a cloned schema shape and separately proved generated migration execution. This plan adds the missing bridge: generated prefixed fixture migrations create the schema used by a Chimeway public API trigger-to-trace flow.

Output: A focused generated-prefixed runtime proof test, an isolated test-support case that runs committed fixture migrations through `Chimeway.Repo`, and `mix verify.runtime_prefix` wiring that puts the proof under the required CI lane.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/76.1-close-gap-gate-01-generated-prefixed-migration-runtime-proof/76.1-RESEARCH.md
@.planning/phases/76.1-close-gap-gate-01-generated-prefixed-migration-runtime-proof/76.1-VALIDATION.md
@.planning/phases/76.1-close-gap-gate-01-generated-prefixed-migration-runtime-proof/76.1-PATTERNS.md
@.planning/phases/76-prefix-docs-demo-and-gates/76-CONTEXT.md
@.planning/phases/76-prefix-docs-demo-and-gates/76-VERIFICATION.md
@mix.exs
@.github/workflows/ci.yml
@config/test.exs
@lib/chimeway/repo.ex
@lib/chimeway/storage.ex
@test/support/data_case.ex
@test/support/prefixed_runtime_case.ex
@test/chimeway/runtime_prefix_integration_test.exs
@test/chimeway/migration_contract_test.exs
@test/chimeway/release_gate_contract_test.exs
</context>

## Artifacts This Phase Produces

- File: `test/chimeway/generated_prefixed_runtime_proof_test.exs`
- Module: `Chimeway.GeneratedPrefixedRuntimeProofTest`
- Module: `ChimewayTest.Notifiers.GeneratedPrefixedRuntime`
- Test: `"generated prefixed migrations support trigger-to-trace runtime behavior through Chimeway.Repo"`
- File: `test/support/generated_prefixed_runtime_case.ex`
- Module: `Chimeway.GeneratedPrefixedRuntimeCase`
- Public helper: `prepare_generated_runtime_storage/1`
- Public helper: `checkout_generated_runtime_repo/1`
- Public helper: `reset_generated_runtime_storage/1`
- Public helper: `generated_migration_count/0`
- Public helper: `assert_generated_prefixed_runtime_tables!/0`
- Public helper: `prefixed_count/1`
- Public helper: `public_count/1`
- Public helper: `table_count/2`
- Public helper: `assert_prefixed_only/1`
- Public helper: `assert_prefixed_only/2`
- Private helper: `generated_repo_config/1`
- Private helper: `base_database_config/0`
- Private helper: `database_url_config/1`
- Private helper: `parse_userinfo/1`
- Private helper: `write_numbered_fixture_migrations!/2`
- Private helper: `migration_order/1`
- Private helper: `purge_fixture_modules!/1`
- Private helper: `purge_modules!/1`
- Private helper: `migration_modules/1`
- Private helper: `run_fixture_migrations/2`
- Private helper: `schema_exists?/1`
- Private helper: `regclass?/2`
- Private helper: `chimeway_tables/1`
- Private helper: `truncate_chimeway_rows!/1`
- Private helper: `qualified_name/2`
- Private helper: `normalize_identifier!/1`
- Private helper: `restore_env/2`
- Private helper: `restore_dynamic_repo/1`
- Mix alias update: `verify.runtime_prefix` includes `test/chimeway/generated_prefixed_runtime_proof_test.exs`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED generated-prefixed runtime bridge proof</name>
  <files>test/chimeway/generated_prefixed_runtime_proof_test.exs</files>
  <read_first>
    @test/chimeway/generated_prefixed_runtime_proof_test.exs (create this file; confirm it is absent before adding)
    @test/chimeway/runtime_prefix_integration_test.exs
    @test/chimeway/migration_contract_test.exs
    @test/support/prefixed_runtime_case.ex
    @.planning/phases/76.1-close-gap-gate-01-generated-prefixed-migration-runtime-proof/76.1-RESEARCH.md
    @.planning/phases/76.1-close-gap-gate-01-generated-prefixed-migration-runtime-proof/76.1-PATTERNS.md
  </read_first>
  <behavior>
    - The focused proof names the missing GATE-01-GAP behavior directly: generated prefixed migrations must create the schema used by a Chimeway runtime flow.
    - The test must define `ChimewayTest.Notifiers.GeneratedPrefixedRuntime` locally so the focused file is self-contained.
    - The test must call `Chimeway.trigger/3`, then `Chimeway.Traces.get_trace/1`, then assert Chimeway rows exist only under `chimeway.*`.
  </behavior>
  <action>Create `Chimeway.GeneratedPrefixedRuntimeProofTest` with a local notifier module `ChimewayTest.Notifiers.GeneratedPrefixedRuntime` using stable `notification_key/0` value `test.generated_prefixed_runtime` and `version/0` value `1`. The test module must `use Chimeway.GeneratedPrefixedRuntimeCase`, set `Chimeway.Adapters.Test` and `Chimeway.Dispatch.Sync` in setup, clear the test adapter before and after each test, call `assert_generated_prefixed_runtime_tables!/0`, assert `generated_migration_count/0 == 31`, execute `Chimeway.trigger/3` with a unique recipient and idempotency key, assert `Chimeway.Traces.get_trace/1` can reload the event, and assert `assert_prefixed_only/2` for `chimeway_events`, `chimeway_notifications`, `chimeway_deliveries`, and `chimeway_delivery_attempts`. This task is RED only: do not add `Chimeway.GeneratedPrefixedRuntimeCase` yet, so the focused command fails for the missing bridge support rather than silently passing against the existing cloned-schema case.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/generated_prefixed_runtime_proof_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    Source assertions: `test/chimeway/generated_prefixed_runtime_proof_test.exs` contains `defmodule Chimeway.GeneratedPrefixedRuntimeProofTest`, `defmodule ChimewayTest.Notifiers.GeneratedPrefixedRuntime`, `use Chimeway.GeneratedPrefixedRuntimeCase`, `Chimeway.trigger`, `Chimeway.Traces.get_trace`, `generated_migration_count() == 31`, and `assert_prefixed_only`.
    RED expected: The focused test command exits nonzero before Task 2 and the failure identifies missing `Chimeway.GeneratedPrefixedRuntimeCase` or missing generated-prefixed runtime bridge support.
    Behavior assertion: The focused test command does not pass by using `Chimeway.PrefixedRuntimeCase`.
  </acceptance_criteria>
  <done>The failing proof exists and captures GATE-01-GAP before any generated-prefixed runtime support is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN generated migration runtime support</name>
  <files>test/support/generated_prefixed_runtime_case.ex, test/chimeway/generated_prefixed_runtime_proof_test.exs</files>
  <read_first>
    @test/support/generated_prefixed_runtime_case.ex (create this file)
    @test/chimeway/generated_prefixed_runtime_proof_test.exs
    @test/chimeway/migration_contract_test.exs
    @test/support/prefixed_runtime_case.ex
    @test/support/data_case.ex
    @config/test.exs
    @lib/chimeway/repo.ex
    @lib/chimeway/storage.ex
  </read_first>
  <behavior>
    - Generated fixture migrations run from `test/fixtures/installer_golden_prefixed` in `STDOUT.txt` order through `Ecto.Migrator.run/4`.
    - The same `Chimeway.Repo` module is used for migration execution and the runtime API proof through an isolated repo process or dynamic repo checkout.
    - Raw SQL helper functions accept only allowlisted PostgreSQL identifiers before building schema-qualified names.
    - Temporary database resources, dynamic repo state, application env, copied migration files, and loaded fixture modules are cleaned up after the test.
  </behavior>
  <action>Create `Chimeway.GeneratedPrefixedRuntimeCase` as an `ExUnit.CaseTemplate` that does not use `Chimeway.DataCase`, because `DataCase` starts the SQL Sandbox owner that the generated migration bridge must avoid. Implement `prepare_generated_runtime_storage/1` to create a unique temporary PostgreSQL database named with the `chimeway_generated_prefixed_runtime_` prefix, materialize numbered migration files from `test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations` using `STDOUT.txt` order and `20_260_101_000_000 + index` versions, start `Chimeway.Repo` with the temporary database config and pool size at least 2, put that repo process as the current dynamic repo while running `Ecto.Migrator.run/4`, assert exactly 31 generated migrations ran, and register cleanup that restores dynamic repo and application env, stops the repo process, drops only the temporary database via `Ecto.Adapters.Postgres.storage_down/1`, removes the temporary migration directory, and purges copied migration modules. Implement `checkout_generated_runtime_repo/1` so each test process selects the generated repo process and restores the prior dynamic repo on exit. Implement `reset_generated_runtime_storage/1` and row placement helpers using schema-qualified SQL with the same identifier allowlist shape as `Chimeway.PrefixedRuntimeCase`. Keep the bridge synchronous with `Chimeway.Dispatch.Sync`; do not add public prefix arguments, migration-runner prefix flags, connection path state changes, SQL Sandbox migration execution, public-schema table cloning, or broad CI topology changes.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/generated_prefixed_runtime_proof_test.exs --warnings-as-errors</automated>
    <automated>mix format --check-formatted test/support/generated_prefixed_runtime_case.ex test/chimeway/generated_prefixed_runtime_proof_test.exs</automated>
  </verify>
  <acceptance_criteria>
    Behavior assertions: the focused test exits 0; the proof reports 31 generated migrations; `Chimeway.trigger/3` returns `{:ok, result}`; `Chimeway.Traces.get_trace(result.event.id)` returns `{:ok, _}`; row counts for the asserted Chimeway tables are nonzero under `chimeway` and zero under `public`.
    Source assertions: `test/support/generated_prefixed_runtime_case.ex` contains `Ecto.Migrator.run`, `test/fixtures/installer_golden_prefixed`, `put_dynamic_repo`, `storage_up`, `storage_down`, and `@identifier_regex`; `rg -n 'CREATE TABLE .*LIKE|clone_public_chimeway_schema|unboxed_run|search_path|prefix: "public"' test/support/generated_prefixed_runtime_case.ex` finds no matches.
  </acceptance_criteria>
  <done>The RED proof is green using generated fixture migrations and `Chimeway.Repo` runtime behavior in the same isolated database.</done>
</task>

<task type="auto">
  <name>Task 3: Wire bridge proof into runtime-prefix gate and preserve release evidence</name>
  <files>mix.exs</files>
  <read_first>
    @mix.exs
    @.github/workflows/ci.yml
    @test/chimeway/release_gate_contract_test.exs
    @test/chimeway/migration_contract_test.exs
    @.planning/phases/76-prefix-docs-demo-and-gates/76-CONTEXT.md
    @.planning/phases/76-prefix-docs-demo-and-gates/76-VERIFICATION.md
  </read_first>
  <action>Update the existing `verify.runtime_prefix` alias in `mix.exs` so its `mix test` command includes `test/chimeway/generated_prefixed_runtime_proof_test.exs` alongside `test/chimeway/repo_prefix_test.exs` and `test/chimeway/runtime_prefix_integration_test.exs`. Preserve the existing `verify_runtime_prefix` GitHub Actions job from Phase 76 D-13 by relying on the alias it already runs, and preserve Phase 76 D-14 by keeping `verify.install_golden`, `ci.install_golden`, `install_golden_contract`, and `ci-gate` path-gate semantics unchanged. Run the focused runtime-prefix gate first, then prove installer golden and release-gate contracts still pass.</action>
  <verify>
    <automated>mix verify.runtime_prefix</automated>
    <automated>mix verify.install_golden</automated>
    <automated>mix ci.verify_gates</automated>
  </verify>
  <acceptance_criteria>
    Source assertions: `rg -n 'generated_prefixed_runtime_proof_test\\.exs' mix.exs` finds the new proof path inside the `verify.runtime_prefix` alias; `git diff -- .github/workflows/ci.yml test/chimeway/release_gate_contract_test.exs` is empty unless a focused failing contract required a corrective update that is documented in the summary.
    CLI assertions: `mix verify.runtime_prefix`, `mix verify.install_golden`, and `mix ci.verify_gates` exit 0. `mix verify.runtime_prefix` includes the new generated-prefixed runtime proof under the same local command used by the CI `verify_runtime_prefix` job.
  </acceptance_criteria>
  <done>`mix verify.runtime_prefix` covers GATE-01-GAP, while installer golden and release-gate evidence remain intact.</done>
</task>

</tasks>

## Dependency Graph

| Task | Needs | Creates | Wave |
|------|-------|---------|------|
| Task 1 | Existing runtime and migration analog tests | RED proof file | 1 |
| Task 2 | Task 1 proof contract | Generated-prefixed runtime case support and green focused proof | 1 |
| Task 3 | Task 2 green focused proof | Runtime-prefix alias coverage and final gate evidence | 1 |

All tasks live in one autonomous plan because they form one RED/GREEN/GATE sequence and share the new proof file. No parallel plan split is useful for this phase.

## Source Audit

| Source | ID | Requirement / Constraint | Plan | Status | Notes |
|--------|----|--------------------------|------|--------|-------|
| GOAL | Phase 76.1 | Close the generated prefixed migration runtime proof gap for GATE-01 | 76.1-01 | COVERED | Objective and Tasks 1-3 create and wire the bridge proof. |
| REQ | GATE-01 | Named verify/CI gates cover prefixed install/runtime behavior and public-schema legacy compatibility | 76.1-01 | COVERED | Task 3 keeps runtime proof in `verify.runtime_prefix`, and runs installer/release gates. |
| REQ | GATE-01-GAP | Runtime behavior executes against tables created by generated prefixed migrations, not only cloned public-schema shape | 76.1-01 | COVERED | Tasks 1-2 add the generated-migration-backed runtime proof. |
| RESEARCH | Ecto.Migrator bridge | Materialize committed prefixed fixtures and run `Ecto.Migrator.run/4` | 76.1-01 | COVERED | Task 2 owns materialization, migration execution, and count assertion. |
| RESEARCH | Chimeway.Repo runtime proof | Use Chimeway public APIs through `Chimeway.Repo`, not `GeneratedRepo` only | 76.1-01 | COVERED | Task 2 starts/selects Chimeway.Repo; Task 1 calls `Chimeway.trigger/3`. |
| RESEARCH | Gate topology | Add bridge proof to `verify.runtime_prefix`; keep installer golden path-gated | 76.1-01 | COVERED | Task 3 cites Phase 76 D-13/D-14 and avoids CI topology churn. |
| RESEARCH | SQL identifier handling | Keep identifier allowlist before raw SQL helper interpolation | 76.1-01 | COVERED | Task 2 requires `@identifier_regex`, `normalize_identifier!/1`, and source assertions. |
| RESEARCH | Destructive cleanup | Clean temporary database resources without destructive schema cleanup in support code | 76.1-01 | COVERED | Task 2 uses unique temp DB plus `storage_down/1` cleanup. |
| CONTEXT | 76 D-13 | `mix verify.runtime_prefix` is a required CI lane | 76.1-01 | COVERED | Task 3 wires the new file into the existing alias. |
| CONTEXT | 76 D-14 | Installer golden remains path-gated and outside ci-gate | 76.1-01 | COVERED | Task 3 preserves the path-gated installer proof and runs `mix verify.install_golden`. |
| CONTEXT | none for 76.1 | No Phase 76.1 CONTEXT.md exists | 76.1-01 | COVERED | Plan derives decisions from research, validation, requirements, and Phase 76 dependency artifacts. |

Excluded from this phase: dynamic per-tenant database prefixes, automatic production data moves, new public prefix API options, docs rewrites, Oban prefix topology changes, and CI/CD optimization beyond alias inclusion.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture migrations -> temporary migration directory | Test support copies committed installer fixture files into executable migration files. |
| test helper SQL identifiers -> PostgreSQL | Helper functions build schema-qualified SQL for row counts and truncation. |
| test harness -> temporary PostgreSQL database | The support creates, migrates, uses, and drops an isolated database. |
| local Mix alias -> CI release evidence | `verify.runtime_prefix` is the command consumed by the required CI `verify_runtime_prefix` lane. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76.1-01 | Repudiation | `mix verify.runtime_prefix` evidence | mitigate | Add the generated-prefixed runtime proof to the alias so GATE-01 evidence cannot remain clone-only. |
| T-76.1-02 | Tampering | `Chimeway.GeneratedPrefixedRuntimeCase` SQL helpers | mitigate | Normalize schema/table identifiers with an allowlist before building qualified SQL names. |
| T-76.1-03 | Tampering | temporary database cleanup | mitigate | Use unique generated test database names and `Ecto.Adapters.Postgres.storage_down/1`; do not add support-code schema cascade cleanup. |
| T-76.1-04 | Information Disclosure | test and CI logs | mitigate | Reuse existing database env conventions, avoid logging `DATABASE_URL`, payloads, provider bodies, tokens, or raw notification data; keep migrator output quiet. |
| T-76.1-05 | Elevation of Privilege | runtime prefix API surface | mitigate | Keep prefix behavior in application config and `Chimeway.Repo.default_options/1`; do not add request/public API prefix arguments. |
| T-76.1-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned or required; package legitimacy gate is not applicable. |
</threat_model>

<verification>
Overall phase checks:
- `MIX_ENV=test mix test test/chimeway/generated_prefixed_runtime_proof_test.exs --warnings-as-errors`
- `mix verify.runtime_prefix`
- `mix verify.install_golden`
- `mix ci.verify_gates`

Expected local evidence:
- Focused bridge proof exits 0 after Task 2.
- Runtime prefix gate exits 0 after Task 3 and includes the generated bridge proof.
- Installer golden proof exits 0, preserving generated prefixed/public migration contracts.
- Release-gate contract exits 0, preserving Phase 76 gate topology.
</verification>

<success_criteria>
GATE-01-GAP is closed when `mix verify.runtime_prefix` proves a Chimeway trigger-to-trace runtime flow against tables created by generated prefixed fixture migrations through `Chimeway.Repo`, and `mix verify.install_golden` plus `mix ci.verify_gates` remain green.
</success_criteria>

<output>
Create `.planning/phases/76.1-close-gap-gate-01-generated-prefixed-migration-runtime-proof/76.1-01-SUMMARY.md` when done.
</output>
