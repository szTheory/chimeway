---
phase: 74-prefixed-migration-generator
plan: 10
type: execute
wave: 4
depends_on:
  - 74-09
files_modified:
  - test/chimeway/install/prefix_contract_test.exs
  - test/chimeway/migration_contract_test.exs
  - mix.exs
  - .github/workflows/ci.yml
  - MAINTAINING.md
autonomous: true
requirements:
  - MIG-01
  - MIG-02
  - MIG-03
  - MIG-04
must_haves:
  truths:
    - "Static contracts fail on missed bare Chimeway table references in prefixed Ecto operations or raw SQL."
    - "DB migration contracts prove normal migration execution creates and rolls back Chimeway-owned objects in the chimeway schema without a migration-runner prefix flag."
    - "Generated public-mode migrations run through the same DB migration proof and verify unprefixed/public objects plus rollback behavior."
    - "Local verify and CI installer gates run the same dual-mode fixture, static, and DB proof set."
  artifacts:
    - path: "test/chimeway/install/prefix_contract_test.exs"
      provides: "static generated-output prefix contract"
    - path: "test/chimeway/migration_contract_test.exs"
      provides: "public legacy and prefixed generated DB migration proof"
    - path: "mix.exs"
      provides: "local verify.install_golden and ci.install_golden parity aliases"
    - path: ".github/workflows/ci.yml"
      provides: "CI install_golden_contract parity gate"
  key_links:
    - from: "test/chimeway/install/prefix_contract_test.exs"
      to: "test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/"
      via: "static scans generated prefixed fixture output"
      pattern: "installer_golden_prefixed"
    - from: "test/chimeway/migration_contract_test.exs"
      to: "test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/"
      via: "runs generated prefixed migrations through normal Ecto.Migrator execution"
      pattern: "Ecto\\.Migrator"
    - from: "test/chimeway/migration_contract_test.exs"
      to: "test/fixtures/installer_golden_public/tree/priv/repo/migrations/"
      via: "runs generated public migrations through normal Ecto.Migrator execution"
      pattern: "installer_golden_public"
    - from: "mix.exs"
      to: ".github/workflows/ci.yml"
      via: "`ci.install_golden` invokes or mirrors `verify.install_golden`, and CI invokes the same proof"
      pattern: "verify\\.install_golden"
---

<objective>
Create the static, database, local verify, and CI parity contracts for Phase 74. This plan implements D-17, D-18, D-19, D-20, D-24, and D-25 by proving generated migrations work through normal database migration execution in both generated modes.

Purpose: prove the generated migrations are safe and executable, not just textually stable.
Output: static prefix contract, prefixed DB proof, public DB proof, verify alias, CI parity, and maintainer guidance.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/74-prefixed-migration-generator/74-CONTEXT.md
@.planning/phases/74-prefixed-migration-generator/74-RESEARCH.md
@.planning/phases/74-prefixed-migration-generator/74-PATTERNS.md
@.planning/phases/74-prefixed-migration-generator/74-VALIDATION.md
@.planning/phases/74-prefixed-migration-generator/74-09-SUMMARY.md
@test/chimeway/migration_contract_test.exs
@test/chimeway/release_gate_contract_test.exs
@test/chimeway/doc_contract_test.exs
@test/chimeway/install/golden_diff_test.exs
@test/chimeway/install/idempotency_test.exs
@mix.exs
@.github/workflows/ci.yml
@MAINTAINING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add static, DB, verify alias, and CI parity contracts</name>
  <files>test/chimeway/install/prefix_contract_test.exs, test/chimeway/migration_contract_test.exs, mix.exs, .github/workflows/ci.yml, MAINTAINING.md</files>
  <read_first>
    @test/chimeway/migration_contract_test.exs
    @test/chimeway/release_gate_contract_test.exs
    @test/chimeway/doc_contract_test.exs
    @test/chimeway/install/golden_diff_test.exs
    @test/chimeway/install/idempotency_test.exs
    @mix.exs
    @.github/workflows/ci.yml
    @MAINTAINING.md
    @.planning/phases/74-prefixed-migration-generator/74-CONTEXT.md
    @.planning/phases/74-prefixed-migration-generator/74-RESEARCH.md
    @.planning/phases/74-prefixed-migration-generator/74-PATTERNS.md
    @.planning/phases/74-prefixed-migration-generator/74-VALIDATION.md
  </read_first>
  <behavior>
    - Static contract scans prefixed generated output for missed bare Chimeway-owned Ecto operations and raw SQL relations.
    - DB contract runs generated default migrations through normal `Ecto.Migrator` execution and verifies objects in the `chimeway` schema.
    - DB contract runs generated `--prefix public` migrations through normal `Ecto.Migrator` execution and verifies unprefixed/public objects.
    - Rollback proof removes Chimeway-owned objects for both generated modes while preserving the D-12 no-CASCADE contract.
    - Local `verify.install_golden`, `ci.install_golden`, and CI `install_golden_contract` run the same proof set.
  </behavior>
  <action>Create `test/chimeway/install/prefix_contract_test.exs` per D-17 using the real generated prefixed fixture tree, not the canonical templates alone. Follow the regex/static-contract style in `test/chimeway/doc_contract_test.exs`; fail on missed bare Chimeway-owned Ecto operations and raw SQL relations, with allowlists only for helper definitions or safe literal construction. Extend `test/chimeway/migration_contract_test.exs` per D-18 and D-19 to keep current public legacy assertions and add DB proof for both generated fixture roots: default prefixed generation must run through normal `Ecto.Migrator` execution without a migration-runner prefix flag and verify `chimeway` schema existence, representative tables, indexes, references, columns, migration versions, and rollback behavior; generated public mode must also run through `Ecto.Migrator`, verify unprefixed/public objects, and verify rollback behavior. Keep D-12 resolved as no generated schema-drop SQL; explicitly forbid destructive schema cleanup and verify rollback removes Chimeway-owned objects without requiring `DROP SCHEMA`. Update `mix.exs` to add `verify.install_golden` for golden, idempotency, prefix contract, and migration contract tests; update `ci.install_golden` to invoke or mirror `verify.install_golden`. Update `.github/workflows/ci.yml` so the path-gated `install_golden_contract` job invokes the same proof and includes PostgreSQL 15 service wiring for database-backed proof. Update `MAINTAINING.md` with the new fixture roots and required verify gate. Keep Oban prefix behavior separate per D-24.</action>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/install/prefix_contract_test.exs test/chimeway/migration_contract_test.exs --warnings-as-errors</automated>
    <automated>mix verify.install_golden</automated>
    <automated>mix ci.install_golden</automated>
    <automated>mix format --check-formatted test/chimeway/install/prefix_contract_test.exs test/chimeway/migration_contract_test.exs mix.exs</automated>
  </verify>
  <done>Phase 74 has static generated-output proof, DB migration proof for prefixed and public generated modes, and CI/local gate parity through `verify.install_golden` and `ci.install_golden`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated migrations -> PostgreSQL | DB contract executes generated files against PostgreSQL. |
| generated fixtures -> static contract | Static contract treats committed generated output as product surface. |
| local alias -> CI job | Local proof command is mirrored by GitHub Actions path-gated job. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-74-25 | Tampering | generated prefixed output | mitigate | Static contract fails on missed bare Chimeway-owned Ecto and raw SQL relation references. |
| T-74-26 | Tampering | generated public output | mitigate | Public DB proof verifies unprefixed/public objects and rollback behavior. |
| T-74-27 | Denial of Service | rollback contract | mitigate | DB rollback proof forbids generated schema-drop SQL and verifies Chimeway-owned object rollback. |
| T-74-28 | Repudiation | CI/local gate parity | mitigate | `verify.install_golden`, `ci.install_golden`, and `install_golden_contract` run the same named proof set. |
| T-74-29 | Information Disclosure | fixtures and stdout | mitigate | Fixture output is structural migration code and command status only; no payload or operator surface is introduced. |
| T-74-SC | Tampering | package manager surface | accept | This plan installs no new packages. |
</threat_model>

<verification>
Run task-level commands, then run the phase gate:

- `CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/install/migrations_test.exs test/chimeway/install/golden_diff_test.exs test/chimeway/install/idempotency_test.exs test/chimeway/install/prefix_contract_test.exs test/chimeway/migration_contract_test.exs --warnings-as-errors`
- `mix verify.install_golden`
- `mix ci.install_golden`

CI PostgreSQL 15 proof is authoritative if local PostgreSQL is below the project baseline.
</verification>

<success_criteria>
Default generation, explicit chimeway generation, and explicit public generation are deterministic; prefixed generated migrations qualify Chimeway-owned operations and run through normal migration execution; public generated migrations also run through normal migration execution and verify unprefixed/public objects; idempotency and CI/local gate parity are green.
</success_criteria>

<output>
Create `.planning/phases/74-prefixed-migration-generator/74-10-SUMMARY.md` when done.
</output>
