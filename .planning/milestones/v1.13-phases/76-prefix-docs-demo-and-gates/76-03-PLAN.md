---
phase: 76-prefix-docs-demo-and-gates
plan: 03
type: execute
wave: 2
depends_on:
  - 76-01
  - 76-02
files_modified:
  - .github/workflows/ci.yml
  - MAINTAINING.md
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
requirements:
  - GATE-01
  - UPG-02
  - UPG-03
  - DOCS-01
  - DOCS-02
  - DEMO-01
requirements_addressed:
  - GATE-01
  - UPG-02
  - UPG-03
  - DOCS-01
  - DOCS-02
  - DEMO-01
must_haves:
  truths:
    - "mix verify.runtime_prefix is represented as a first-class CI lane required by ci-gate. Covers D-13 and GATE-01."
    - "mix verify.install_golden and mix ci.install_golden remain path-gated installer proof, with release contracts ensuring alias and CI job existence. Covers D-14 and GATE-01."
    - "MAINTAINING, GitHub Actions, and release-gate contracts agree on storage-prefix gate names and final phase verification. Covers D-17."
    - "Final local gates prove docs contracts, docs build, runtime prefix behavior, installer golden proof, and demo/example behavior after Plans 76-01 and 76-02."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "verify_runtime_prefix job and ci-gate requirement"
    - path: "test/chimeway/release_gate_contract_test.exs"
      provides: "Release-gate parity assertions for runtime-prefix and installer-golden storage gates"
    - path: "MAINTAINING.md"
      provides: "Pre-ship storage-prefix gate documentation and path-gated installer proof guidance"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "mix.exs"
      via: "verify_runtime_prefix job runs mix verify.runtime_prefix"
      pattern: "verify_runtime_prefix.*mix verify.runtime_prefix"
    - from: "test/chimeway/release_gate_contract_test.exs"
      to: ".github/workflows/ci.yml, MAINTAINING.md, mix.exs"
      via: "contract tests parse local aliases, CI jobs, ci-gate needs, and pre-ship docs"
      pattern: "verify.runtime_prefix|install_golden_contract|ci-gate"
---

<objective>
Close Phase 76 by making storage-prefix verification visible and enforceable across local aliases, CI jobs, ci-gate aggregation, release contracts, and maintainer docs.

Purpose: Prevent CI/local parity drift after storage isolation ships, and prove docs, demo, runtime prefix, public legacy compatibility, and installer proof are all named gates.
Output: A `verify_runtime_prefix` CI lane, updated release-gate contracts, updated MAINTAINING storage gate guidance, and final aggregate verification.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/76-prefix-docs-demo-and-gates/76-CONTEXT.md
@.planning/phases/76-prefix-docs-demo-and-gates/76-RESEARCH.md
@.planning/phases/76-prefix-docs-demo-and-gates/76-PATTERNS.md
@.planning/phases/76-prefix-docs-demo-and-gates/76-VALIDATION.md
@.planning/phases/75-runtime-prefix-propagation/75-VERIFICATION.md
@mix.exs
@.github/workflows/ci.yml
@MAINTAINING.md
@test/chimeway/release_gate_contract_test.exs
@test/chimeway/doc_contract_test.exs
</context>

## Artifacts this phase produces

- New CI job id: `verify_runtime_prefix`
- Modified CI aggregate: `.github/workflows/ci.yml` `ci-gate.needs`, env block, and lane loop include `verify_runtime_prefix`
- Modified release contract attributes: `@ci_gate_lanes`, `@pre_ship_verify_commands`
- Modified release contract tests for runtime-prefix lane count and installer-golden path-gated CI proof
- Modified maintainer docs: `MAINTAINING.md` pre-ship command count/list and storage prefix gate descriptions
- Verification commands: `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors`, `mix ci.verify_gates`, `mix ci.docs`, `mix verify.runtime_prefix`, `mix verify.install_golden`, `mix verify.example`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime-prefix CI lane required by ci-gate</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml
    - mix.exs
    - test/chimeway/release_gate_contract_test.exs
    - .planning/phases/75-runtime-prefix-propagation/75-VERIFICATION.md
    - .planning/phases/76-prefix-docs-demo-and-gates/76-CONTEXT.md
    - .planning/phases/76-prefix-docs-demo-and-gates/76-PATTERNS.md
  </read_first>
  <behavior>
    - CI defines verify_runtime_prefix with PostgreSQL 15 service and runs mix verify.runtime_prefix.
    - ci-gate needs includes verify_runtime_prefix.
    - ci-gate env block and required lane loop include VERIFY_RUNTIME_PREFIX.
    - install_golden_contract remains path-gated and outside ci-gate needs.
  </behavior>
  <action>
    Add a verify_runtime_prefix job to .github/workflows/ci.yml per D-13 and D-17. Model it on existing Postgres-backed verify jobs: checkout, setup-beam, cache, mix local.rebar, mix local.hex, mix deps.get, mix ecto.create --quiet, mix ecto.migrate --quiet, and mix verify.runtime_prefix. Add verify_runtime_prefix to ci-gate needs, add VERIFY_RUNTIME_PREFIX to the env block, and include it in the required lane loop. Preserve install_golden_contract as the path-gated installer proof outside ci-gate per D-14.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    .github/workflows/ci.yml contains verify_runtime_prefix running mix verify.runtime_prefix, and ci-gate fails unless that lane succeeds while install_golden_contract remains path-gated outside ci-gate.
  </acceptance_criteria>
  <done>Runtime-prefix proof is a required CI lane and ci-gate aggregation reflects it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend release-gate parity contracts for storage prefix gates</name>
  <files>test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    - test/chimeway/release_gate_contract_test.exs
    - .github/workflows/ci.yml
    - MAINTAINING.md
    - mix.exs
    - .planning/phases/76-prefix-docs-demo-and-gates/76-VALIDATION.md
  </read_first>
  <behavior>
    - @ci_gate_lanes includes verify_runtime_prefix and the lane-count test name/expectation is updated.
    - @pre_ship_verify_commands includes verify.runtime_prefix, verify_runtime_prefix, and mix verify.runtime_prefix.
    - Release contracts assert install_golden_contract job exists, runs mix verify.install_golden, and stays outside ci-gate needs.
    - Release contracts assert mix.exs defines verify.install_golden and ci.install_golden.
  </behavior>
  <action>
    Update test/chimeway/release_gate_contract_test.exs per D-13, D-14, and D-17. Add verify.runtime_prefix to @pre_ship_verify_commands and verify_runtime_prefix to @ci_gate_lanes. Update the lane-count test name from 12 to the new required count. Add explicit tests that install_golden_contract exists, runs mix verify.install_golden, and is not a ci-gate need. Add assertions that mix.exs defines both "verify.install_golden" and "ci.install_golden" aliases, because installer proof remains required but path-gated. Keep the existing verify_sigra special-case logic unchanged.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors</automated>
    <automated>mix ci.verify_gates</automated>
  </verify>
  <acceptance_criteria>
    Release gate contracts fail on missing runtime-prefix CI parity, missing storage gate MAINTAINING copy, missing install-golden aliases, or accidental inclusion of install_golden_contract in ci-gate.
  </acceptance_criteria>
  <done>Release-gate contract tests enforce runtime-prefix and installer-golden storage gate parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update maintainer gate docs and run final phase verification</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - MAINTAINING.md
    - mix.exs
    - .github/workflows/ci.yml
    - test/chimeway/release_gate_contract_test.exs
    - test/chimeway/doc_contract_test.exs
    - .planning/phases/76-prefix-docs-demo-and-gates/76-VALIDATION.md
  </read_first>
  <behavior>
    - MAINTAINING pre-ship commands include mix verify.runtime_prefix and update the count consistently.
    - MAINTAINING explains that verify.runtime_prefix proves configured-schema runtime behavior and public legacy compatibility.
    - MAINTAINING keeps verify.install_golden documented as path-gated installer proof with mix ci.install_golden parity.
    - Final phase commands pass after Plans 76-01 and 76-02 are complete.
  </behavior>
  <action>
    Update MAINTAINING.md per D-13, D-14, and D-17. Add mix verify.runtime_prefix to the pre-ship local command block and update all nearby prose counts consistently. Add a bullet explaining that mix verify.runtime_prefix covers configured-schema runtime behavior and public-schema legacy compatibility through the focused runtime-prefix suite. Keep mix verify.install_golden in the Installer template changes section as path-gated proof and keep wording that mix ci.install_golden delegates to the same proof. After docs and demo plans have landed, run the final phase command set listed in verification.
  </action>
  <verify>
    <automated>mix ci.verify_gates</automated>
    <automated>mix ci.docs</automated>
    <automated>mix verify.runtime_prefix</automated>
    <automated>mix verify.install_golden</automated>
    <automated>mix verify.example</automated>
  </verify>
  <acceptance_criteria>
    MAINTAINING, mix aliases, CI jobs, ci-gate aggregation, and release-gate contracts agree on storage prefix gates; all final verification commands pass.
  </acceptance_criteria>
  <done>GATE-01 is closed with local/CI/release documentation parity and final docs/runtime/installer/demo gates green.</done>
</task>

</tasks>

<source_coverage_audit>
| Source | ID | Requirement / decision | Plan | Status | Notes |
|--------|----|------------------------|------|--------|-------|
| GOAL | Phase 76 | Make storage isolation adoptable and supportable through docs, demo proof, upgrade guidance, Oban caveats, and named verification gates | 76-01, 76-02, 76-03 | COVERED | Docs, demo, and gate parity are planned. |
| REQ | UPG-02 | Optional manual public-to-chimeway move guide | 76-01 | COVERED | Storage guide and doc contracts. |
| REQ | UPG-03 | Rollback and failure-mode guidance | 76-01 | COVERED | Storage guide and doc contracts. |
| REQ | DOCS-01 | README/install/golden/troubleshooting prefix docs | 76-01 | COVERED | First-run docs plus storage guide. |
| REQ | DOCS-02 | Oban prefix separation docs | 76-01 | COVERED | Oban recipe and storage guide. |
| REQ | DEMO-01 | Demo host runs default chimeway schema and proves trigger-to-trace | 76-02 | COVERED | Demo config and public seed trace proof. |
| REQ | GATE-01 | Named verify/CI gates cover prefixed install/runtime and public legacy compatibility | 76-03 | COVERED | Runtime-prefix CI lane, install-golden path gate, release contracts. |
| RESEARCH | Storage guide plus doc contracts | Dedicated HexDocs guide and required/forbidden claims | 76-01 | COVERED | Uses recommended storage-prefix-upgrade.md path. |
| RESEARCH | Oban prefix caveats | Chimeway storage prefix separate from Oban job prefix | 76-01 | COVERED | Uses jobs examples. |
| RESEARCH | Demo proof path | Public seed/trigger API with schema placement assertions | 76-02 | COVERED | Reuses DemoHost.Seeds and PrefixedRuntimeCase patterns. |
| RESEARCH | Gate parity | verify_runtime_prefix in ci-gate and release contracts | 76-03 | COVERED | ci.yml, MAINTAINING, release contracts. |
| CONTEXT | D-01-D-04 | Layered docs, HexDocs guide, first-run boundaries, doc contracts | 76-01 | COVERED | Task actions cite D-01 through D-04. |
| CONTEXT | D-05-D-08 | Public compatibility and manual move guidance | 76-01 | COVERED | Task actions cite D-05 through D-08. |
| CONTEXT | D-09-D-12 | Oban and Ecto prefix separation and footgun guards | 76-01 | COVERED | Task actions cite D-09 through D-12. |
| CONTEXT | D-13-D-17 | Demo and gate parity decisions | 76-02, 76-03 | COVERED | Demo in Plan 76-02; CI/release parity in Plan 76-03. |
| CONTEXT | D-18-D-19 | Voice, local-first, no hidden magic | 76-01, 76-02 | COVERED | Docs and demo copy constraints. |
| CONTEXT | Deferred Ideas | Automated data move, dynamic prefixes, broad README cleanup, new browser smoke | none | EXCLUDED | Explicitly deferred by CONTEXT.md and not planned. |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local aliases -> CI jobs | Maintainer commands and GitHub Actions must execute equivalent gates. |
| CI jobs -> ci-gate | Required lanes must be aggregated so failures cannot be ignored. |
| Release docs -> release operator | MAINTAINING must describe actual gates, not stale release folklore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-09 | Repudiation | .github/workflows/ci.yml | mitigate | Add verify_runtime_prefix and require it through ci-gate needs/env/loop so runtime-prefix proof cannot be claimed without CI execution. |
| T-76-10 | Repudiation | test/chimeway/release_gate_contract_test.exs | mitigate | Contract-test Mix aliases, CI jobs, ci-gate lanes, MAINTAINING copy, and install-golden path-gate boundaries. |
| T-76-11 | Tampering | install_golden_contract path gate | mitigate | Assert install_golden_contract exists and runs mix verify.install_golden while staying outside ci-gate needs. |
| T-76-12 | Information Disclosure | gate docs | mitigate | Keep storage gate copy focused on command purpose and schema placement, not raw payload/provider examples. |
| T-76-SC | Tampering | package installs | accept | No npm, pip, or cargo package installs are planned; CI edits use existing GitHub Actions and Mix aliases. |
</threat_model>

<verification>
Overall plan verification:

1. `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors`
2. `mix ci.verify_gates`
3. `mix ci.docs`
4. `mix verify.runtime_prefix`
5. `mix verify.install_golden`
6. `mix verify.example`
</verification>

<success_criteria>
- GATE-01: `verify_runtime_prefix` exists in CI and is required by `ci-gate`.
- GATE-01: Runtime prefix and public legacy compatibility are named in local pre-ship docs.
- GATE-01: Installer golden proof remains path-gated but contract-tested for local/CI parity.
- Phase 76 final verification commands pass after docs and demo plans are complete.
</success_criteria>

<output>
Create `.planning/phases/76-prefix-docs-demo-and-gates/76-03-SUMMARY.md` when done.
</output>
