---
phase: 50-natural-escalation-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex
  - examples/chimeway_demo_host/lib/demo_host/seeds.ex
  - examples/chimeway_demo_host/lib/demo_host/adapters/pending_webhook_adapter.ex
  - examples/chimeway_demo_host/test/demo_host_web/journey_test.exs
autonomous: true
requirements: [DEMO-03]
must_haves:
  truths:
    - "`DemoHost.Notifiers.PaymentReminder` declares `wait_until` + `cancel_signals: [\"chimeway.notification.read\"]` on the in_app step and an `email_escalation` email step (D-01)"
    - "`DemoHost.Seeds.seed_escalation_waiting/0` is trigger-only — no `Application.put_env` adapter swap and no `stage_escalation_webhook/1` (D-02)"
    - "`DemoHost.Adapters.PendingWebhookAdapter` is deleted — no references remain in the repo (D-05)"
    - "JOUR-03 proves seed → natural `:waiting` with `pending_signals` → `Chimeway.mark_read/3` → signal → run `:active` with `signal_received` transition (D-03)"
    - "PM JTBD ('if they don't open in 2 hours, send email') is demonstrable via seeds using READ-driven progression without host `Workflows.update_run/3` glue"
  artifacts:
    - path: "examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex"
      provides: "READ-driven payment reminder workflow"
      contains: "cancel_signals"
    - path: "examples/chimeway_demo_host/lib/demo_host/seeds.ex"
      provides: "Trigger-only escalation seed"
      contains: "seed_escalation_waiting"
    - path: "examples/chimeway_demo_host/test/demo_host_web/journey_test.exs"
      provides: "JOUR-03 READ-driven journey proof"
      contains: "chimeway.notification.read"
  key_links:
    - from: "examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex"
      to: "lib/chimeway/workflows/progression.ex"
      via: "in_app terminal → enter_waiting/6 auto-populates pending_signals"
      pattern: "cancel_signals"
    - from: "examples/chimeway_demo_host/test/demo_host_web/journey_test.exs"
      to: "Chimeway.mark_read/3"
      via: "public inbox API — no host Signal.track glue"
      pattern: "Chimeway\\.mark_read"
---

<objective>
Refactor the TeamPulse payment-escalation demo from staged webhook choreography to READ-driven workflow progression: reshape `PaymentReminder`, simplify seeds, delete `PendingWebhookAdapter`, and rewrite JOUR-03 to prove `mark_read` → signal → resume.

Purpose: Phases 48–49 shipped `cancel_signals` → `pending_signals` and inbox signal emission; the demo still masks that glue with `stage_escalation_webhook/1`. This plan closes DEMO-03 without touching `lib/chimeway/*`.

Output: Updated demo host notifier, seeds, journey test; deleted `pending_webhook_adapter.ex`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-natural-escalation-demo/50-CONTEXT.md
@.planning/phases/50-natural-escalation-demo/50-RESEARCH.md
@.planning/phases/50-natural-escalation-demo/50-VALIDATION.md

@examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex
@examples/chimeway_demo_host/lib/demo_host/seeds.ex
@examples/chimeway_demo_host/lib/demo_host/adapters/pending_webhook_adapter.ex
@examples/chimeway_demo_host/test/demo_host_web/journey_test.exs
@test/chimeway/orchestration/workflow_progression_test.exs
@guides/flows/multi-step-journeys.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PaymentReminder workflow — wait_until + cancel_signals (D-01)</name>
  <files>examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex</files>
  <read_first>
    - `examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex` — current `workflow/2` (lines 53–78) and `@moduledoc` (lines 3–7)
    - `guides/flows/multi-step-journeys.md` — canonical `wait_until` + `cancel_signals` example (lines 43–70)
    - `.planning/phases/50-natural-escalation-demo/50-CONTEXT.md` — D-01 locked decision
    - `lib/chimeway/workflows/progression.ex` — `enter_waiting/6` reads `cancel_signals` from rule (read-only reference)
  </read_first>
  <action>
    In `examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex`:

    1. **Replace `workflow/2` steps** with exactly two steps:

       - Step 1: `step_key: "initial_notice"`, `step_order: 1`, `channel: :in_app`, `config: %{"progress" => [%{...}]}` where the sole progress rule is:
         - `"kind" => "wait_until"`
         - `"anchor" => "prior_delivery_terminal_at"`
         - `"delay_seconds" => 7200`
         - `"to_step" => "email_escalation"`
         - `"cancel_signals" => ["chimeway.notification.read"]`

       - Step 2: `step_key: "email_escalation"`, `step_order: 2`, `channel: :email`, `config: %{}`

    2. **Remove** the old `paid_confirmation` in_app step and the `stop` bounce rule on step 1 — not in D-01 scope.

    3. **Update `@moduledoc`** to document READ-driven escalation:
       - PM JTBD: "if they don't open in 2 hours, send email"
       - Pair with `DemoHost.Seeds.escalation_waiting!/0` and JOUR-03 (`mark_read` path)
       - Point webhook/delivery-feedback proof to `feedback_pipeline_e2e_test.exs` — not this notifier's primary story

    4. **Do not modify** `channels/2`, `build/2`, `rendering/2`, or any file under `lib/chimeway/`.
  </action>
  <verify>
    <automated>cd examples/chimeway_demo_host && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `payment_reminder.ex` `workflow/2` contains `"kind" => "wait_until"` and `"cancel_signals" => ["chimeway.notification.read"]`.
    - `payment_reminder.ex` `workflow/2` contains `step_key: "email_escalation"` with `channel: :email`.
    - `payment_reminder.ex` does **not** contain `paid_confirmation` or `chimeway.delivery.succeeded`.
    - `@moduledoc` references JOUR-03 or `mark_read` and does **not** claim webhook-driven progression as the primary path.
    - `cd examples/chimeway_demo_host && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>PaymentReminder workflow mirrors mention-escalation pattern with 7200s wait and inbox-read cancel signal.</done>
</task>

<task type="auto">
  <name>Task 2: Seeds trigger-only — remove webhook choreography (D-02)</name>
  <files>examples/chimeway_demo_host/lib/demo_host/seeds.ex</files>
  <read_first>
    - `examples/chimeway_demo_host/lib/demo_host/seeds.ex` — `seed_escalation_waiting/0` (lines 111–141), `stage_escalation_webhook/1` (lines 155–191), `@moduledoc` scenario list (line 12)
    - `examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex` — Task 1 workflow shape
    - `.planning/phases/50-natural-escalation-demo/50-CONTEXT.md` — D-02
    - `.planning/phases/50-natural-escalation-demo/50-RESEARCH.md` — seeds target shape
  </read_first>
  <action>
    In `examples/chimeway_demo_host/lib/demo_host/seeds.ex`:

    1. **Replace `seed_escalation_waiting/0`** body with trigger-only:

       ```elixir
       def seed_escalation_waiting do
         trigger(
           DemoHost.Notifiers.PaymentReminder,
           %{email: @morgan_email, invoice_id: "INV-1001"},
           idempotency_key: @payment_idempotency,
           correlation_id: "teampulse-seed-payment-corr",
           tenant_id: @tenant_id
         )
       end
       ```

    2. **Delete** private function `stage_escalation_webhook/1` entirely.

    3. **Remove** `Application.put_env(:chimeway, :channel_adapters, ...)` adapter swap block.

    4. **Remove unused aliases** only if no longer referenced: `Deliveries`, `Workflows`, `WorkflowRun` (keep `Delivery`, `Notification` if still used by `normalize_duplicate/1`).

    5. **Update `@moduledoc`** scenario bullet for `:escalation_waiting` — describe READ-driven `:waiting` after in_app delivery, not "workflow waiting for webhook signal".

    6. **Update `@doc` on `seed_escalation_waiting/0`** — JOUR-03 READ path; no `Deliveries`/`Workflows` staging language.
  </action>
  <verify>
    <automated>rg "stage_escalation_webhook|PendingWebhookAdapter" examples/chimeway_demo_host/lib/demo_host/seeds.ex; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `rg "stage_escalation_webhook" examples/chimeway_demo_host/` returns no matches.
    - `rg "PendingWebhookAdapter" examples/chimeway_demo_host/lib/demo_host/seeds.ex` returns no matches.
    - `rg "Application.put_env.*channel_adapters" examples/chimeway_demo_host/lib/demo_host/seeds.ex` returns no matches.
    - `seed_escalation_waiting/0` calls `trigger(DemoHost.Notifiers.PaymentReminder, ...)` only.
    - `@moduledoc` does **not** contain "workflow waiting for webhook signal".
  </acceptance_criteria>
  <done>Escalation seed is adopter-copyable trigger-only; no manual `Workflows.update_run/3` for pending_signals.</done>
</task>

<task type="auto">
  <name>Task 3: Delete PendingWebhookAdapter (D-05)</name>
  <files>examples/chimeway_demo_host/lib/demo_host/adapters/pending_webhook_adapter.ex</files>
  <read_first>
    - `examples/chimeway_demo_host/lib/demo_host/adapters/pending_webhook_adapter.ex` — sole consumer was seeds
    - `examples/chimeway_demo_host/lib/demo_host/seeds.ex` — Task 2 must be complete (no references)
    - `.planning/phases/50-natural-escalation-demo/50-CONTEXT.md` — D-05 (no deprecation shim)
  </read_first>
  <action>
    1. **Delete** file `examples/chimeway_demo_host/lib/demo_host/adapters/pending_webhook_adapter.ex`.

    2. **Grep** repo for `PendingWebhookAdapter` — remove any remaining references (expect zero after Task 2).

    3. **Do not** add deprecation module, `@deprecated` shim, or redirect.
  </action>
  <verify>
    <automated>rg "PendingWebhookAdapter" /Users/jon/projects/chimeway; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - File `examples/chimeway_demo_host/lib/demo_host/adapters/pending_webhook_adapter.ex` does not exist.
    - `rg "PendingWebhookAdapter" /Users/jon/projects/chimeway` returns no matches outside `.planning/`.
    - `cd examples/chimeway_demo_host && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>PendingWebhookAdapter removed; no seed-only fixture remains for removed choreography.</done>
</task>

<task type="auto">
  <name>Task 4: Rewrite JOUR-03 — READ-driven escalation proof (D-03)</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/journey_test.exs</files>
  <read_first>
    - `examples/chimeway_demo_host/test/demo_host_web/journey_test.exs` — current JOUR-03 (lines 47–90), `drain_oban!/1` helper
    - `test/chimeway/orchestration/workflow_progression_test.exs` — `"mark_read resumes waiting run (READ-02/03)"` describe (lines 401–445)
    - `examples/chimeway_demo_host/lib/demo_host/seeds.ex` — `morgan_identity/0`, `escalation_waiting!/0` return shape after Task 2
    - `lib/chimeway/inbox.ex` — `mark_read/3` public API (read-only)
    - `.planning/phases/50-natural-escalation-demo/50-CONTEXT.md` — D-03 scope fence (no JOUR-06 time-elapse assertions)
  </read_first>
  <action>
    In `examples/chimeway_demo_host/test/demo_host_web/journey_test.exs`:

    1. **Add aliases** (if not present):
       - `alias Chimeway.Delivery`
       - `alias Chimeway.Notifications.Notification`

    2. **Replace JOUR-03 test** — rename to `"JOUR-03 seeded escalation progresses via mark_read"`:

       a. `assert {:ok, %{trace: %{delivery_ids: ids}}} = DemoHost.Seeds.escalation_waiting!()`

       b. Resolve in_app delivery from `ids`:
          ```elixir
          in_app_delivery =
            ids
            |> Enum.map(&Repo.get!(Delivery, &1))
            |> Enum.find(&(&1.channel == "in_app"))
          refute is_nil(in_app_delivery)
          ```

       c. Load `notification = Repo.get!(Notification, in_app_delivery.notification_id)` and `run = Repo.get!(WorkflowRun, in_app_delivery.workflow_run_id)`.

       d. **Pre-read assertions** (proves natural `enter_waiting/6`):
          - `run.state == :waiting`
          - `run.pending_signals == ["chimeway.notification.read"]`
          - `run.status_reason == "waiting_for_step_progression"`

       e. `assert :ok = Chimeway.mark_read(notification.id, DemoHost.Seeds.morgan_identity())`

       f. `drain_oban!(:chimeway_signals)` — do **not** drain `:chimeway_delivery` for progression.

       g. Assert signal: `Repo.all(Signal)` contains row with `event_name == "chimeway.notification.read"`.

       h. Reload run: `state == :active`, `pending_signals == []`.

       i. Assert `signal_received` transition:
          - `reason == "signal_received"`
          - `context == %{"event_name" => "chimeway.notification.read"}` only
          - `refute Map.has_key?(transition.context, "notification_id")`
          - `refute Map.has_key?(transition.context, "payload")`

    3. **Remove** from JOUR-03: webhook POST, `Ingress` assertions, `:chimeway_delivery` drain, `chimeway.delivery.succeeded` signal assertion, `:webhook_received` timeline assertion, `delivery_id` on `signal_received` transition.

    4. **Scope fence:** Do **not** assert zero email deliveries, `:stopped` run, or `due_at` manipulation — JOUR-06 is Phase 51.

    5. **Do not** call `Chimeway.Signal.track/4` or host glue after `mark_read`.
  </action>
  <verify>
    <automated>cd examples/chimeway_demo_host && mix test --only jour_03 --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Test name contains `mark_read` (not `webhook`).
    - Test calls `Chimeway.mark_read(notification.id, DemoHost.Seeds.morgan_identity())`.
    - Test asserts `pending_signals == ["chimeway.notification.read"]` before `mark_read`.
    - Test asserts signal `event_name == "chimeway.notification.read"`.
    - Test asserts `signal_received` transition `context == %{"event_name" => "chimeway.notification.read"}`.
    - Test does **not** contain `conn(:post, "/webhooks/chimeway/echo"` or `chimeway.delivery.succeeded`.
    - `cd examples/chimeway_demo_host && mix test --only jour_03 --warnings-as-errors` exits 0.
    - `mix verify.journeys` exits 0.
  </acceptance_criteria>
  <done>JOUR-03 proves demo-host READ-driven escalation without webhook choreography.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo host → Chimeway engine | Seeds use public `Chimeway.trigger/3` only; no manual `Workflows.update_run/3` for `pending_signals`. |
| Journey test → Inbox API | JOUR-03 calls `Chimeway.mark_read/3` — no host `Signal.track` glue (READ-02). |
| Demo fixtures → production | `PendingWebhookAdapter` deleted — no misleading copy-paste adapter for production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Tampering | manual pending_signals in seeds | mitigate | Delete `stage_escalation_webhook/1`; natural `enter_waiting/6` only. |
| T-50-02 | Spoofing | journey test bypasses public API | mitigate | JOUR-03 uses `Chimeway.mark_read/3` only — no direct `Signal.track`. |
| T-50-03 | Repudiation | demo masks engine behavior | mitigate | JOUR-03 asserts `pending_signals` and `signal_received` transition in CI. |
| T-50-04 | Information Disclosure | trace context leaks payload | accept | Assert `event_name` only in transition context per READ-03 (matches engine contract). |

**No `high`-severity threats.** Demo-and-test scope only; no auth surface or engine changes.
</threat_model>

<verification>
- [ ] `cd examples/chimeway_demo_host && mix test --only jour_03 --warnings-as-errors` green
- [ ] `mix verify.journeys` green
- [ ] `rg "stage_escalation_webhook|PendingWebhookAdapter" examples/chimeway_demo_host/` — no matches
- [ ] `rg "cancel_signals" examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex` — match
- [ ] No edits under `lib/chimeway/` in this plan
- [ ] `mix test examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs --warnings-as-errors` green (webhook regression unchanged)
</verification>

<success_criteria>
- ROADMAP Phase 50 criterion 1: seeds no longer require `stage_escalation_webhook/1` or `PendingWebhookAdapter`
- ROADMAP Phase 50 criterion 2: PM JTBD demonstrable via READ-driven seeds + JOUR-03
- DEMO-03: payment escalation uses `wait_until` + inbox-read cancel — no staged webhook choreography
- All journey tests pass (`mix verify.journeys`)
</success_criteria>

<output>
After completion, create `.planning/phases/50-natural-escalation-demo/50-01-SUMMARY.md`
</output>
