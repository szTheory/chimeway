---
phase: 52-doc-truth-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/chimeway_demo_host/README.md
  - lib/mix/tasks/demo.up.ex
  - guides/recipes/mention-escalation.md
autonomous: true
requirements: [DOCS-04, DOCS-05]
must_haves:
  truths:
    - "Demo host README Morgan persona row describes READ-driven `:waiting` escalation — not webhook awaiting (D-02)"
    - "Webhook progression section reframed as separate path; TeamPulse Morgan story is READ-driven (D-03)"
    - "TraceDemo IEx walkthrough is supplementary; TeamPulse personas via `mix demo.up` remain primary adoption path (D-04)"
    - "`mix demo.up --check` documented as migrate + app.start + seed, skipping only `ecto.create` — not seed-only (D-05)"
    - "No runtime, CI, or alias behavior changes — documentation-only phase (D-01)"
  artifacts:
    - path: "examples/chimeway_demo_host/README.md"
      provides: "Unified TeamPulse + TraceDemo adoption narrative with READ truth"
      contains: "READ-driven"
    - path: "lib/mix/tasks/demo.up.ex"
      provides: "Accurate `@moduledoc` for `--check` smoke behavior"
      contains: "migrate + app.start + seed"
  key_links:
    - from: "examples/chimeway_demo_host/README.md"
      to: "examples/chimeway_demo_host/lib/demo_host/seeds.ex"
      via: "Morgan persona row matches `:escalation_waiting` READ-driven seed shape"
      pattern: "READ-driven.*:waiting"
    - from: "examples/chimeway_demo_host/README.md"
      to: "guides/recipes/mention-escalation.md"
      via: "Cross-link for PM JTBD read-cancel + time-fallback pattern"
      pattern: "mention-escalation"
    - from: "lib/mix/tasks/demo.up.ex"
      to: "examples/chimeway_demo_host/test/mix/tasks/demo_up_test.exs"
      via: "JOUR-05 smoke — docs must match unchanged `run/1` behavior"
      pattern: "ecto.migrate"
---

<objective>
Close DOCS-04 and DOCS-05 by aligning demo host README and `mix demo.up` moduledoc with shipped READ-driven TeamPulse escalation and actual `--check` behavior.

Purpose: Phases 50–51 shipped READ-driven Morgan escalation and 9-test journey CI; demo adoption docs still say "awaiting webhook" and "seed only" for `--check`. Adopters reading README as first touchpoint get contradictory guidance.

Output: Updated `examples/chimeway_demo_host/README.md`, `lib/mix/tasks/demo.up.ex` `@moduledoc`, and optional `guides/recipes/mention-escalation.md` line 90 scope-fence cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/52-doc-truth-gates/52-CONTEXT.md
@.planning/phases/52-doc-truth-gates/52-RESEARCH.md
@.planning/phases/52-doc-truth-gates/52-PATTERNS.md
@.planning/phases/52-doc-truth-gates/52-VALIDATION.md
@.planning/phases/51-journey-admin-proof/51-VERIFICATION.md

@examples/chimeway_demo_host/README.md
@examples/chimeway_demo_host/lib/demo_host/seeds.ex
@lib/mix/tasks/demo.up.ex
@guides/recipes/mention-escalation.md
@examples/chimeway_demo_host/test/mix/tasks/demo_up_test.exs
</context>

<threat_model>
| ID | Category | Threat | Severity | Mitigation |
|----|----------|--------|----------|------------|
| T-52-01 | Integrity | README reintroduces webhook-as-primary for Morgan escalation | high | Replace line 39 "awaiting webhook" with READ-driven `:waiting` language; grep gate forbids "awaiting webhook" |
| T-52-02 | Integrity | `--check` docs imply seed-only, causing CI/debug confusion | medium | Explicit "migrate + app.start + seed (skips ecto.create only)" in moduledoc and command table |
| T-52-03 | Integrity | Webhook section deleted — loses Golden Path cross-link for delivery-feedback adopters | medium | Reframe per D-03; retain Golden Path appendix + E2E test bullets |
| T-52-04 | Integrity | Phase 42 prod-auth docs regressed (lines 135–141) | high | Do not edit README lines 135–141; surgical edits only above that block |
| T-52-05 | Scope | Runtime or alias changes creep in | medium | D-01 fence — no edits to `demo.up.ex` `run/1`, `mix.exs` alias body, or CI workflows |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: DOCS-04 — Morgan persona row READ truth (D-02)</name>
  <files>examples/chimeway_demo_host/README.md</files>
  <read_first>
    - `examples/chimeway_demo_host/README.md` — TeamPulse scenarios table (lines 33–41)
    - `examples/chimeway_demo_host/lib/demo_host/seeds.ex` — moduledoc scenarios (lines 8–14) and `seed_escalation_waiting/0` doc (lines 109–115)
    - `guides/recipes/mention-escalation.md` — canonical READ read-cancel + time-fallback prose
    - `.planning/phases/52-doc-truth-gates/52-PATTERNS.md` — Pattern 1 (Persona Table READ Truth)
  </read_first>
  <action>
    In `examples/chimeway_demo_host/README.md`:

    1. Replace line 39 Product Manager scenario cell:

       **Before:**
       `| Product Manager | Payment escalation awaiting webhook |`

       **After:**
       `| Product Manager | Payment escalation — READ-driven \`:waiting\` (inbox read cancels; time fallback to email) |`

    2. Add one sentence below the persona table (after line 41, before "Copy `DemoHost.Seeds`...") linking the PM scenario to the recipe:

       `See the [Mention escalation recipe](../../guides/recipes/mention-escalation.md) for the read-cancel + time-fallback pattern (JOUR-03/06).`

    3. Do **not** edit lines 135–141 (Production auth block).
  </action>
  <verify>
    <automated>rg -n "awaiting webhook" examples/chimeway_demo_host/README.md; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg "awaiting webhook" examples/chimeway_demo_host/README.md` returns no matches.
    - `rg "READ-driven|:waiting" examples/chimeway_demo_host/README.md` matches the Product Manager persona row.
    - README contains link to `guides/recipes/mention-escalation.md` near the persona table.
    - Lines 135–141 (Production auth) are unchanged.
  </acceptance_criteria>
  <done>Morgan persona row reflects READ-driven escalation aligned to `DemoHost.Seeds` and mention-escalation recipe.</done>
</task>

<task type="auto">
  <name>Task 2: DOCS-04 — Webhook path reframe + TraceDemo supplementary framing (D-03, D-04)</name>
  <files>examples/chimeway_demo_host/README.md</files>
  <read_first>
    - `examples/chimeway_demo_host/README.md` — webhook section (lines 62–67), Trace walkthrough (lines 69–133), Operator UI (lines 124–133)
    - `.planning/phases/52-doc-truth-gates/52-CONTEXT.md` — D-03, D-04 locked decisions
    - `.planning/phases/52-doc-truth-gates/52-PATTERNS.md` — Pattern 2 (Webhook vs READ Path Split), Pattern 3 (TraceDemo Supplementary)
    - `examples/chimeway_demo_host/README.md` — Quick start (lines 5–14) for TeamPulse primary entry point
  </read_first>
  <action>
    In `examples/chimeway_demo_host/README.md`:

    1. **Retitle and rewrite** section at lines 62–67:

       - Change heading from `## Not this path: webhook progression` to `## Webhook progression (separate path)`.
       - Replace opening sentence denying workflow progression with prose stating:
         - **TeamPulse Morgan** (persona table above) uses **READ-driven** workflow `:waiting` — see `mention-escalation.md` and JOUR-03/06.
         - **Webhook-driven progression** is a distinct adoption path — Golden Path webhook appendix and feedback pipeline E2E test.
       - Keep both existing bullets (Golden Path link + E2E test "do not copy fixture helpers") unchanged.

    2. **Rename and introduce** Trace walkthrough section (line 69):

       - Change heading to `## Supplementary: TraceDemo IEx walkthrough`.
       - Add intro paragraph before `### Trigger`:
         - **Primary adoption path:** TeamPulse personas via `mix demo.up` / `DemoHost.Seeds` → admin UI with `user:alex@teampulse.test` (Alex, Sam, Morgan scenarios).
         - **This section:** minimal single-delivery explainability via `TraceDemo` + `mix demo.trace` — no TeamPulse domain setup required.

    3. **Update Operator UI section** (line 133) search hint:

       **Before:** `search by recipient (e.g. \`user:demo_user_1\` from \`mix demo.trace\`)`

       **After:** `search by recipient — \`user:alex@teampulse.test\` (TeamPulse seeds) or \`user:demo_user_1\` (TraceDemo)`

    4. Do **not** edit lines 135–141 (Production auth block).
  </action>
  <verify>
    <automated>rg -n "Not this path: webhook progression|explainability on a simple delivery, not workflow progression" examples/chimeway_demo_host/README.md; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - README heading is `## Webhook progression (separate path)` (not "Not this path").
    - README contains "Supplementary: TraceDemo IEx walkthrough" heading.
    - README Operator UI section mentions both `user:alex@teampulse.test` and `user:demo_user_1`.
    - `rg "Not this path: webhook progression" examples/chimeway_demo_host/README.md` returns no matches.
    - Golden Path webhook appendix link still present.
    - Lines 135–141 unchanged.
  </acceptance_criteria>
  <done>README unifies TeamPulse (primary) and TraceDemo (supplementary) narratives; webhook progression positioned as separate path from Morgan READ escalation.</done>
</task>

<task type="auto">
  <name>Task 3: DOCS-05 — `demo.up` moduledoc + README command table `--check` truth (D-05, D-06)</name>
  <files>examples/chimeway_demo_host/README.md, lib/mix/tasks/demo.up.ex</files>
  <read_first>
    - `lib/mix/tasks/demo.up.ex` — `@moduledoc` (lines 1–11) and `run/1` (lines 17–33)
    - `examples/chimeway_demo_host/README.md` — Demo commands table (lines 22–31)
    - `examples/chimeway_demo_host/test/mix/tasks/demo_up_test.exs` — JOUR-05 smoke test (lines 7–24)
    - `.planning/phases/52-doc-truth-gates/52-PATTERNS.md` — Pattern 4 (`--check` Moduledoc Truth)
  </read_first>
  <action>
    1. In `lib/mix/tasks/demo.up.ex` line 8, replace moduledoc usage line:

       **Before:**
       `mix demo.up --check      # CI smoke — seed only, exit 0`

       **After:**
       `mix demo.up --check      # CI smoke — migrate + app.start + seed (skip ecto.create), exit 0`

       Keep lines 7 and 9 unchanged.

    2. In `examples/chimeway_demo_host/README.md` line 28, replace command table Purpose cell:

       **Before:**
       `| \`mix demo.up --check\` | repo root | CI smoke — seed only, exit 0 |`

       **After:**
       `| \`mix demo.up --check\` | repo root | CI smoke — migrate + app.start + seed (skips \`ecto.create\` only), exit 0 |`

       Keep lines 26–27 (`mix demo.up`, `--serve`) unchanged — already accurate.

    3. Do **not** modify `run/1` or any runtime logic in `demo.up.ex`.
  </action>
  <verify>
    <automated>rg -n "seed only" examples/chimeway_demo_host/README.md lib/mix/tasks/demo.up.ex; test $? -ne 0 && mix verify.journeys</automated>
  </verify>
  <acceptance_criteria>
    - `rg "seed only" examples/chimeway_demo_host/README.md lib/mix/tasks/demo.up.ex` returns no matches.
    - `demo.up.ex` `@moduledoc` line 8 contains "migrate + app.start + seed".
    - README command table line 28 contains "migrate + app.start + seed".
    - `lib/mix/tasks/demo.up.ex` `run/1` body is unchanged (only `@moduledoc` edited).
    - `mix verify.journeys` from repo root exits 0 (9 tests, 0 failures).
    - `cd examples/chimeway_demo_host && mix test --only jour_05` exits 0.
  </acceptance_criteria>
  <done>DOCS-05 complete — `--check` documentation matches actual migrate + seed behavior verified by JOUR-05.</done>
</task>

<task type="auto">
  <name>Task 4 (optional): mention-escalation.md JOUR-06 scope-fence cleanup</name>
  <files>guides/recipes/mention-escalation.md</files>
  <read_first>
    - `guides/recipes/mention-escalation.md` — Time fallback section (lines 86–95)
    - `.planning/phases/51-journey-admin-proof/51-VERIFICATION.md` — JOUR-06 dual-test evidence
    - `.planning/phases/52-doc-truth-gates/52-PATTERNS.md` — Pattern 8 (Optional Recipe Scope-Fence)
  </read_first>
  <action>
    In `guides/recipes/mention-escalation.md` line 90, replace obsolete Phase-50 scope fence:

    **Before:**
    `Automated proof that email fires only when unread is JOUR-06 (Phase 51) — this recipe documents the pattern; read-cancel does not halt the scheduled worker in Phase 50.`

    **After:**
    `Automated proof: JOUR-06 under \`mix verify.journeys\` (read-cancel + time-fallback; two \`@tag :jour_06\` tests).`

    Do **not** change line 95 (JOUR-03 mark_read path reference) — still accurate.
  </action>
  <verify>
    <automated>rg -n "read-cancel does not halt the scheduled worker in Phase 50" guides/recipes/mention-escalation.md; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `rg "read-cancel does not halt the scheduled worker in Phase 50" guides/recipes/mention-escalation.md` returns no matches.
    - Line 90 references JOUR-06 with read-cancel + time-fallback dual-test wording.
    - Line 95 JOUR-03 reference unchanged.
  </acceptance_criteria>
  <done>Recipe JOUR-06 prose reflects shipped CI proof (9-test suite, dual `@tag :jour_06`).</done>
</task>

</tasks>

<verification>
- [ ] `rg "awaiting webhook|seed only" examples/chimeway_demo_host/README.md lib/mix/tasks/demo.up.ex` — no matches
- [ ] `rg "READ-driven|:waiting" examples/chimeway_demo_host/README.md` — matches Morgan row
- [ ] README manual read-through: Morgan READ-driven; TraceDemo supplementary; webhook separate path
- [ ] `mix verify.journeys` green from repo root (9 tests, 0 failures)
- [ ] `cd examples/chimeway_demo_host && mix test --only jour_05` green
- [ ] No edits to README lines 135–141; no `run/1` changes in `demo.up.ex`
</verification>

## PLAN COMPLETE
