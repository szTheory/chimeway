---
phase: 49-inbox-read-signal
plan: 03
type: execute
wave: 3
depends_on: ["49-02"]
files_modified:
  - guides/flows/multi-step-journeys.md
  - test/chimeway/doc_contract_test.exs
autonomous: true
requirements: [READ-02]
must_haves:
  truths:
    - "Journey guide documents inbox emission via `Chimeway.mark_read/3` and `Chimeway.mark_seen/3` with canonical event names (D-09)"
    - "READ-02 deferral section removed — guide no longer states inbox does not emit signals"
    - "`cancel_signals` authoring is end-to-end truthful with inbox emission shipped"
    - "Doc contract forbids stale deferral phrases and requires inbox API strings"
  artifacts:
    - path: "guides/flows/multi-step-journeys.md"
      provides: "§7 inbox lifecycle → Signal.track authoring reference"
      contains: "Chimeway.mark_read"
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "READ-02-shipped doc contract (flip from Phase 48 deferral)"
      contains: "Chimeway.mark_seen"
  key_links:
    - from: "guides/flows/multi-step-journeys.md"
      to: "Chimeway.Inbox signal emission"
      via: "§7 inbox lifecycle subsection mirroring §6 delivery-feedback pattern"
      pattern: "chimeway\\.notification\\.read"
    - from: "test/chimeway/doc_contract_test.exs"
      to: "guides/flows/multi-step-journeys.md"
      via: "@required includes Chimeway.mark_read; @forbidden_strings blocks deferral"
      pattern: "does \\*\\*not\\*\\* emit"
---

<objective>
Update `guides/flows/multi-step-journeys.md` to document shipped inbox signal emission, remove the READ-02 deferral, and flip `doc_contract_test.exs` from Phase 48's deferral contract to READ-02-shipped assertions.

Purpose: ROADMAP Phase 49 success requires doc-truth parity with engine behavior (D-09). Phase 48 intentionally kept READ-02 deferral text — Phase 49 must flip that contract.

Output: Updated journey guide and doc contract tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-inbox-read-signal/49-CONTEXT.md
@.planning/phases/49-inbox-read-signal/49-RESEARCH.md
@.planning/phases/49-inbox-read-signal/49-PATTERNS.md
@.planning/phases/49-inbox-read-signal/49-VALIDATION.md
@.planning/phases/49-inbox-read-signal/49-02-SUMMARY.md

@guides/flows/multi-step-journeys.md
@test/chimeway/doc_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Journey guide inbox emission doc-truth (D-09)</name>
  <files>guides/flows/multi-step-journeys.md</files>
  <read_first>
    - `guides/flows/multi-step-journeys.md` — §6 Delivery-Feedback Signal Routing (~lines 153–171), §7 Generic Signal Routing (~lines 174–205), Deferred / Future section (~lines 197–205)
    - `.planning/phases/49-inbox-read-signal/49-CONTEXT.md` — D-03, D-04, D-05, D-09
    - `.planning/phases/49-inbox-read-signal/49-RESEARCH.md` — Doc Contract Test Implications, content to remove/add
    - `.planning/phases/48-wait-until-pending-signals/48-03-PLAN.md` — Phase 48 deferral text that Phase 49 removes
  </read_first>
  <action>
    Edit `guides/flows/multi-step-journeys.md`:

    1. **Remove READ-02 deferral** — delete the entire block starting with `Phase 48 documents these names but does **not** emit them` through the end of the `## Deferred / Future (READ Milestone)` section (current lines ~197–205). Remove the `READ-02` bullet and intro sentence implying inbox emission is future work.

    2. **Add §7 subsection: Inbox Lifecycle Signal Routing** — mirror §6 numbered pipeline structure:
       - `Chimeway.mark_read/3` / `Chimeway.mark_seen/3` update lifecycle timestamps on first transition
       - On first transition, engine calls `Chimeway.Signal.track/4` internally with canonical events: `chimeway.notification.read` and `chimeway.notification.seen`
       - `Chimeway.Dispatch.SignalRouterWorker` routes to `Chimeway.Workflows.route_signal/1`
       - For `:waiting` runs with matching `pending_signals`, run resumes to `:active` with `signal_received` transition

    3. **Signal signature example** — add Elixir code block:

       ```elixir
       # Host calls public API — engine emits signal internally
       Chimeway.mark_read(notification_id, "user:123")

       # Equivalent durable signal (system-emitted, not host-called):
       Chimeway.Signal.track(
         tenant_id,
         "user:123",
         "chimeway.notification.read",
         %{"notification_id" => notification_id}
       )
       ```

    4. **D-04 clarity** — state `mark_read` and `mark_seen` emit **distinct** signals; neither auto-emits the other. Re-marking returns `:ok` without duplicate signals (D-06).

    5. **READ-03 trace note** — operator traces show `event_name` only in `signal_received` context; `notification_id` in signal payload is not copied to transition context.

    6. **cancel_signals cross-link** — note that `wait_until` rules declaring `cancel_signals: ["chimeway.notification.read"]` are now end-to-end truthful with inbox emission (Phase 48 population + Phase 49 emission).

    7. **Scope fence** — do **not** document read-cancel before `due_at` as shipped (JOUR-06 / Phase 51). Do **not** rewrite `guides/recipes/mention-escalation` (DEMO-04 / Phase 50). Do **not** remove `cancel_signals` or `pending_signals` documentation from Phase 48.

    8. Verify guide contains: `Chimeway.mark_read`, `Chimeway.mark_seen`, `chimeway.notification.read`, `chimeway.notification.seen`, `cancel_signals`, `pending_signals`.
  </action>
  <verify>
    <automated>mix ci.verify_gates</automated>
  </verify>
  <acceptance_criteria>
    - `guides/flows/multi-step-journeys.md` contains `Chimeway.mark_read` and `Chimeway.mark_seen`.
    - `guides/flows/multi-step-journeys.md` contains `chimeway.notification.read` and `chimeway.notification.seen`.
    - `guides/flows/multi-step-journeys.md` does **not** contain `does **not** emit`.
    - `guides/flows/multi-step-journeys.md` does **not** contain `READ-02 (Phase 49)`.
    - `guides/flows/multi-step-journeys.md` does **not** contain `## Deferred / Future (READ Milestone)`.
    - `guides/flows/multi-step-journeys.md` does **not** contain `Engine gap today`.
    - `grep -n "stage_escalation_webhook" guides/flows/multi-step-journeys.md` returns nothing.
  </acceptance_criteria>
  <done>Journey guide documents shipped inbox emission and removes READ-02 deferral.</done>
</task>

<task type="auto">
  <name>Task 2: Flip doc contract from deferral to READ-02-shipped (D-09)</name>
  <files>test/chimeway/doc_contract_test.exs</files>
  <read_first>
    - `test/chimeway/doc_contract_test.exs` — `"journey guide doc contract (DOCS-03)"` describe: `@required` (~lines 68–77), `@forbidden_strings` (~line 37), `"includes Deferred or READ milestone callout"` test (~lines 86–89)
    - `guides/flows/multi-step-journeys.md` — Task 1 edits
    - `.planning/phases/49-inbox-read-signal/49-RESEARCH.md` — Doc Contract Test Implications table (flip deferral contract)
    - `.planning/phases/48-wait-until-pending-signals/48-03-PLAN.md` — Phase 48 contract that intentionally required deferral regex
  </read_first>
  <action>
    In `test/chimeway/doc_contract_test.exs`, within `"journey guide doc contract (DOCS-03)"`:

    1. **Extend `@required`** — add: `Chimeway.mark_read`, `Chimeway.mark_seen`, `chimeway.notification.read`, `chimeway.notification.seen`.

    2. **Extend `@forbidden_strings`** — add: `does **not** emit`, `READ-02 (Phase 49)`, `Deferred / Future (READ Milestone)`. Keep existing `Engine gap today`.

    3. **Remove deferral test** — delete test `"includes Deferred or READ milestone callout"` that asserts `~r/Deferred|READ-0/` — this test becomes a regression hazard after READ-02 ships (Pitfall 3).

    4. **Add shipped assertion (optional but recommended)** — new test `"documents inbox lifecycle signal emission (READ-02 shipped)"` asserting `String.contains?(content, "Chimeway.mark_read")` and `String.contains?(content, "chimeway.notification.read")`.

    5. Run `mix ci.verify_gates` and `mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` to confirm all doc contracts pass.
  </action>
  <verify>
    <automated>mix ci.verify_gates</automated>
  </verify>
  <acceptance_criteria>
    - `@required` includes `Chimeway.mark_read`, `Chimeway.mark_seen`, `chimeway.notification.read`, `chimeway.notification.seen`.
    - `@forbidden_strings` includes `does **not** emit` and `Engine gap today`.
    - Test `"includes Deferred or READ milestone callout"` is removed.
    - `mix ci.verify_gates` exits 0.
    - `mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Doc contract flipped from Phase 48 READ-02 deferral to READ-02-shipped assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public documentation | Journey guide is adopter-facing; must reflect shipped engine behavior and not instruct host `Signal.track` glue after `mark_read`. |
| Doc contract | CI gate prevents reintroduction of stale deferral language. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-10 | Information Disclosure | doc over-promising Phase 51 semantics | mitigate | Do not document read-cancel before `due_at` as shipped; scope fence in guide (JOUR-06 deferred). |
| T-49-11 | Tampering | doc drift reintroducing host glue | mitigate | `@forbidden_strings` blocks `does **not** emit` and deferral headings; `@required` includes inbox APIs. |
| T-49-12 | Repudiation | aspirational docs without contract | mitigate | `mix ci.verify_gates` locks guide strings to shipped behavior. |

**No `high`-severity threats.** Documentation-only plan; no runtime auth or data-plane changes.
</threat_model>

<verification>
- [ ] `mix ci.verify_gates` green
- [ ] `mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` green
- [ ] `mix ci.test` green (full phase gate before `/gsd-verify-work`)
- [ ] Journey guide: READ-02 deferral removed, inbox emission documented
- [ ] Deferral regex test removed from doc contract
</verification>

<success_criteria>
- D-09: journey guide documents inbox emission for `mark_read`/`mark_seen`
- READ-02 deferral removed from guide and forbidden by contract
- `cancel_signals` authoring is end-to-end truthful with emission
- All CI gates green
</success_criteria>

<output>
After completion, create `.planning/phases/49-inbox-read-signal/49-03-SUMMARY.md`
</output>
