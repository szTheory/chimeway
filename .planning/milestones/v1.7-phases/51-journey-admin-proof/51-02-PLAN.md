---
phase: 51-journey-admin-proof
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs
autonomous: true
requirements: [JOUR-07, JOUR-08]
must_haves:
  truths:
    - "Admin search by `sam_identity()` surfaces Sam password-reset delivery with `suppressed` status and `channel_disabled` reason (D-03, JOUR-07)"
    - "Admin detail for Sam shows `teampulse.password_reset` notification key (D-03)"
    - "Admin search by `morgan_identity()` surfaces Morgan payment-escalation delivery (D-04, JOUR-08)"
    - "Admin detail for Morgan shows `teampulse.payment_reminder`, `teampulse-seed-payment-corr`, and workflow-waiting timeline content (D-04)"
    - "All admin journey tests tagged `:journey` + `:jour_07` / `:jour_08` for `mix verify.journeys` (D-05)"
    - "GATE-03 documentation and MAINTAINING.md quintet update deferred to Phase 52 — Phase 51 adds tests only (D-06)"
  artifacts:
    - path: "examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs"
      provides: "JOUR-07 Sam suppression + JOUR-08 Morgan escalation admin traces"
      contains: ":jour_07"
  key_links:
    - from: "examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs"
      to: "chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex"
      via: "LiveView detail at /admin/chimeway/deliveries/:id"
      pattern: "Trace detail"
    - from: "examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs"
      to: "DemoHost.Seeds"
      via: "sam_identity/0, morgan_identity/0, seed helpers"
      pattern: "DemoHost\\.Seeds\\."
---

<objective>
Add JOUR-07 and JOUR-08 admin LiveView journey tests covering the remaining SEED-004 personas: Sam (Support Operator suppression trace) and Morgan (Product Manager escalation trace).

Purpose: JOUR-04 proves admin search for Alex (invite). Phase 51 completes persona coverage for adoption-evidence v1.7 without engine or admin UI changes.

Output: Two new tests in `admin_trace_live_test.exs` mirroring JOUR-04 pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-journey-admin-proof/51-CONTEXT.md
@.planning/phases/51-journey-admin-proof/51-RESEARCH.md
@.planning/phases/51-journey-admin-proof/51-VALIDATION.md
@.planning/phases/51-journey-admin-proof/51-PATTERNS.md

@examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs
@examples/chimeway_demo_host/lib/demo_host/seeds.ex
@examples/chimeway_demo_host/lib/demo_host/notifiers/password_reset.ex
@examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex
@chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex
@guides/recipes/password-reset-support-trace.md
</context>

<threat_model>
| ID | Category | Threat | Severity | Mitigation |
|----|----------|--------|----------|------------|
| T-51-04 | Information Disclosure | Test asserts raw recipient email in HTML | low | Use `sam_identity()` / `morgan_identity()` identity strings only; admin redacts in production |
| T-51-05 | Integrity | JOUR-08 opens email delivery detail before escalation step exists | medium | Navigate to in_app delivery from seed `delivery_ids` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: JOUR-07 — Sam password-reset suppression admin trace (D-03)</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs</files>
  <read_first>
    - `examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs` — JOUR-04 test (lines 7-36)
    - `.planning/phases/51-journey-admin-proof/51-CONTEXT.md` — D-03 locked decision
    - `examples/chimeway_demo_host/lib/demo_host/seeds.ex` — `seed_password_reset/0`, `sam_identity/0`
    - `examples/chimeway_demo_host/test/demo_host_web/journey_test.exs` — JOUR-02 suppression values (`:suppressed`, `channel_disabled`)
    - `chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex` — status and suppression_reason rendering
  </read_first>
  <action>
    In `examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs`:

    1. Update `@moduledoc` to reference JOUR-04, JOUR-07, JOUR-08.

    2. Add test tagged `@tag :journey` and `@tag :jour_07` named `"JOUR-07 admin shows Sam password-reset suppression"`:

       - `assert {:ok, %{trace: %{delivery_ids: delivery_ids}}} = DemoHost.Seeds.seed_password_reset()`.
       - `conn = get(conn, "/admin/chimeway")` — assert 200 and `"Trace search"`.
       - `live(conn)` → submit `#trace-search-form` with `mode: "recipient"`, `query: DemoHost.Seeds.sam_identity()`, `notification_key: ""`.
       - Assert search HTML contains `sam_identity()` string.
       - Pick `delivery_id` from `delivery_ids` that appears in search results (same pattern as JOUR-04 `Enum.find/2`).
       - `live(conn, "/admin/chimeway/deliveries/#{delivery_id}")` — assert detail page.
       - Assert `render(detail_view)` contains:
         - `"suppressed"` (status)
         - `"channel_disabled"` (suppression reason)
         - `"teampulse.password_reset"` (notification key)
  </action>
  <verify>
    <automated>cd examples/chimeway_demo_host && mix test --only jour_07 --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `admin_trace_live_test.exs` contains `@tag :jour_07` test.
    - Test seeds via `DemoHost.Seeds.seed_password_reset/0`.
    - Test searches with `DemoHost.Seeds.sam_identity()`.
    - Detail HTML contains `suppressed`, `channel_disabled`, and `teampulse.password_reset`.
    - `cd examples/chimeway_demo_host && mix test --only jour_07 --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>JOUR-07 proves Support Operator can explain Sam's suppressed password reset in admin UI.</done>
</task>

<task type="auto">
  <name>Task 2: JOUR-08 — Morgan payment-escalation admin trace (D-04)</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs</files>
  <read_first>
    - `examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs` — JOUR-04 and Task 1 JOUR-07 test
    - `.planning/phases/51-journey-admin-proof/51-CONTEXT.md` — D-04 locked decision
    - `examples/chimeway_demo_host/lib/demo_host/seeds.ex` — `escalation_waiting!/0`, `morgan_identity/0`, correlation id `teampulse-seed-payment-corr`
    - `chimeway_admin/lib/chimeway_admin/components/timeline_event.ex` — `:workflow_waiting` humanized as "Workflow waiting"
    - `examples/chimeway_demo_host/test/demo_host_web/journey_test.exs` — JOUR-03 in_app delivery selection pattern
  </read_first>
  <action>
    In `examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs`:

    1. Add test tagged `@tag :journey` and `@tag :jour_08` named `"JOUR-08 admin shows Morgan payment-escalation trace"`:

       - `assert {:ok, %{trace: %{delivery_ids: delivery_ids}}} = DemoHost.Seeds.escalation_waiting!()`.
       - Select **in_app** delivery id: map ids through `Repo.get!(Delivery, &1)` and find `channel == "in_app"` (add `alias Chimeway.{Delivery, Repo}` if not present).
       - Admin flow: `get(conn, "/admin/chimeway")` → `live` → search form with `query: DemoHost.Seeds.morgan_identity()`.
       - Assert search results include morgan identity.
       - `live(conn, "/admin/chimeway/deliveries/#{in_app_delivery_id}")`.
       - Assert `render(detail_view)` contains:
         - `"teampulse.payment_reminder"`
         - `"teampulse-seed-payment-corr"`
         - Workflow timeline indicator: `"workflow waiting"` OR `"Workflow waiting"` (case-insensitive match acceptable via separate asserts).

    2. Do **not** require `mark_read` or `signal_received` in this test — `:waiting` timeline is sufficient per CONTEXT discretion.

    3. Do **not** modify `chimeway_admin` or engine code.
  </action>
  <verify>
    <automated>cd examples/chimeway_demo_host && mix test --only jour_08 --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `admin_trace_live_test.exs` contains `@tag :jour_08` test.
    - Test uses `DemoHost.Seeds.escalation_waiting!/0` and `morgan_identity()`.
    - Detail navigates to in_app delivery (not email).
    - Detail HTML contains `teampulse.payment_reminder` and `teampulse-seed-payment-corr`.
    - Detail HTML contains workflow waiting timeline text.
    - `cd examples/chimeway_demo_host && mix test --only jour_08 --warnings-as-errors` exits 0.
    - `mix verify.journeys` from repo root exits 0 (8 tests).
  </acceptance_criteria>
  <done>JOUR-08 proves Product Manager can trace Morgan's payment escalation workflow in admin UI.</done>
</task>

</tasks>

<verification>
- [ ] `mix test --only jour_07` green
- [ ] `mix test --only jour_08` green
- [ ] `mix verify.journeys` green from repo root (8 tests)
- [ ] No `chimeway_admin` or `lib/chimeway/` files modified
</verification>

## PLAN COMPLETE
