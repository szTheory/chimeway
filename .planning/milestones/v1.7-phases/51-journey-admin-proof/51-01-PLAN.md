---
phase: 51-journey-admin-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/chimeway_demo_host/test/demo_host_web/journey_test.exs
autonomous: true
requirements: [JOUR-06]
must_haves:
  truths:
    - "After `mark_read` before persisted `due_at`, no email delivery exists for the workflow run and the run resumes `:active` on `initial_notice` (D-01)"
    - "Without `mark_read`, `Progression.progress_run/2` past `due_at` creates exactly one `email_escalation` delivery (D-02)"
    - "Together read-cancel + time-fallback prove email fires only when unread (ROADMAP JOUR-06 criterion)"
    - "No changes under `lib/chimeway/` — journey-level proof only (D-05)"
  artifacts:
    - path: "examples/chimeway_demo_host/test/demo_host_web/journey_test.exs"
      provides: "JOUR-06 read-cancel and time-fallback journey proofs"
      contains: ":jour_06"
  key_links:
    - from: "examples/chimeway_demo_host/test/demo_host_web/journey_test.exs"
      to: "Chimeway.mark_read/3"
      via: "read-cancel path — drain :chimeway_signals after mark_read"
      pattern: "Chimeway\\.mark_read"
    - from: "examples/chimeway_demo_host/test/demo_host_web/journey_test.exs"
      to: "Chimeway.Workflows.Progression"
      via: "time-fallback path — progress_run with injected now past due_at"
      pattern: "Progression\\.progress_run"
---

<objective>
Add JOUR-06 journey tests proving READ cancel semantics end-to-end: read-cancel prevents email escalation before `due_at`, and complementary time-fallback advances to `email_escalation` when unread.

Purpose: Phase 50 JOUR-03 deliberately omitted zero-email and past-due assertions (scope fence). JOUR-06 closes the adoption-evidence gap for "email fires only when unread" without touching engine code.

Output: Two `@tag :jour_06` tests (or one read-cancel + one time-fallback) in `journey_test.exs`; `@moduledoc` updated to reference JOUR-01..06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/51-journey-admin-proof/51-CONTEXT.md
@.planning/phases/51-journey-admin-proof/51-RESEARCH.md
@.planning/phases/51-journey-admin-proof/51-VALIDATION.md
@.planning/phases/51-journey-admin-proof/51-PATTERNS.md

@examples/chimeway_demo_host/test/demo_host_web/journey_test.exs
@test/chimeway/orchestration/workflow_progression_test.exs
@examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex
@guides/recipes/mention-escalation.md
</context>

<threat_model>
| ID | Category | Threat | Severity | Mitigation |
|----|----------|--------|----------|------------|
| T-51-01 | Integrity | Test asserts `:stopped` or worker cancellation — not engine contract | medium | Assert `:active` on `initial_notice` + zero email deliveries only |
| T-51-02 | Integrity | Time-fallback reuses run after `mark_read` | medium | Fresh `escalation_waiting!/0` seed per test |
| T-51-03 | Information Disclosure | Test logs notification payload | low | Follow JOUR-03 — no payload in transition assertions |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: JOUR-06 read-cancel — mark_read prevents email before due_at (D-01)</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/journey_test.exs</files>
  <read_first>
    - `examples/chimeway_demo_host/test/demo_host_web/journey_test.exs` — JOUR-03 test (lines 44-85) and `drain_oban!/1` helper
    - `.planning/phases/51-journey-admin-proof/51-CONTEXT.md` — D-01 locked decision
    - `.planning/phases/51-journey-admin-proof/51-PATTERNS.md` — read-cancel extension pattern
    - `lib/chimeway/workflows.ex` — `get_current_step!/1` (read-only)
    - `test/chimeway/orchestration/workflow_progression_test.exs` — mark_read resume describe (lines 401-445)
  </read_first>
  <action>
    In `examples/chimeway_demo_host/test/demo_host_web/journey_test.exs`:

    1. Add `alias Chimeway.Workflows` (for `get_current_step!/1`).

    2. Add a new test tagged `@tag :journey` and `@tag :jour_06` named `"JOUR-06 mark_read cancels escalation before due_at"`:

       - `DemoHost.Seeds.escalation_waiting!/0` — same setup as JOUR-03 (find in_app delivery, notification, run).
       - Assert run is `:waiting` with `pending_signals == ["chimeway.notification.read"]`.
       - `Chimeway.mark_read(notification.id, DemoHost.Seeds.morgan_identity())` then `drain_oban!(:chimeway_signals)`.
       - Assert run is `:active`, `pending_signals == []`, `signal_received` transition exists.
       - **JOUR-06 extensions (not in JOUR-03):**
         - Query email deliveries: `from(d in Delivery, where: d.workflow_run_id == ^run.id and d.channel == "email") |> Repo.all()` — assert `== []`.
         - `current_step = Workflows.get_current_step!(updated_run)` — assert `current_step.step_key == "initial_notice"`.
       - Do **not** call `Progression.progress_run/2` in this test.

    3. Update `@moduledoc` first line to reference JOUR-01..06.

    4. Do **not** modify JOUR-03 test body or any file under `lib/chimeway/`.
  </action>
  <verify>
    <automated>cd examples/chimeway_demo_host && mix test test/demo_host_web/journey_test.exs:46 --only jour_06 --warnings-as-errors 2>/dev/null || cd examples/chimeway_demo_host && mix test --only jour_06 --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `journey_test.exs` contains `@tag :jour_06` test with name including `mark_read cancels escalation`.
    - Test asserts zero email deliveries for the workflow run after `mark_read`.
    - Test asserts `get_current_step!(run).step_key == "initial_notice"`.
    - JOUR-03 test body is unchanged.
    - `cd examples/chimeway_demo_host && mix test --only jour_06 --warnings-as-errors` exits 0 (read-cancel test passes; time-fallback may fail until Task 2).
  </acceptance_criteria>
  <done>JOUR-06 read-cancel path proves no email escalation after inbox read before due_at.</done>
</task>

<task type="auto">
  <name>Task 2: JOUR-06 time-fallback — unread past due_at advances to email (D-02)</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/journey_test.exs</files>
  <read_first>
    - `examples/chimeway_demo_host/test/demo_host_web/journey_test.exs` — Task 1 JOUR-06 read-cancel test
    - `test/chimeway/orchestration/workflow_progression_test.exs` — CR-01 describe (lines 448-501), `parse_iso8601!/1` or `DateTime.from_iso8601/1` pattern
    - `.planning/phases/51-journey-admin-proof/51-CONTEXT.md` — D-02 locked decision
    - `lib/chimeway/workflows/progression.ex` — `progress_run/2` accepts `now:` option (read-only)
  </read_first>
  <action>
    In `examples/chimeway_demo_host/test/demo_host_web/journey_test.exs`:

    1. Add `alias Chimeway.Workflows.Progression`.

    2. Add a **separate** test tagged `@tag :journey` and `@tag :jour_06` named `"JOUR-06 unread time-fallback advances to email_escalation"`:

       - Fresh `DemoHost.Seeds.escalation_waiting!/0` — **do not** call `mark_read`.
       - Load `:waiting` run from in_app delivery's `workflow_run_id`.
       - Parse `due_at` from `run.status_context["due_at"]` using `DateTime.from_iso8601/1`.
       - `past_due_now = DateTime.add(due_at, 1, :second)`.
       - `assert {:ok, {:advanced, advanced_run, [next_delivery]}} = Progression.progress_run(run.id, now: past_due_now)`.
       - Assert `advanced_run.state == :active`.
       - Assert `next_delivery.channel == "email"`.
       - Assert exactly one email delivery for the notification: count `Delivery` rows where `notification_id == notification.id and channel == "email"` equals 1.

    3. Add private helper `parse_due_at!/1` if needed (wrap `DateTime.from_iso8601/1` with flunk on error).

    4. Do **not** use `WorkflowProgressionWorker` drain or real-time sleep.
  </action>
  <verify>
    <automated>cd examples/chimeway_demo_host && mix test --only jour_06 --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Two distinct `@tag :jour_06` tests exist in `journey_test.exs`.
    - Time-fallback test uses `Progression.progress_run(run.id, now: past_due_now)`.
    - Time-fallback test does not call `mark_read`.
    - Exactly one email delivery asserted after past-due advance.
    - `cd examples/chimeway_demo_host && mix test --only jour_06 --warnings-as-errors` exits 0.
    - `mix verify.journeys` from repo root exits 0 (8 tests).
  </acceptance_criteria>
  <done>JOUR-06 complete — read-cancel and time-fallback together prove email fires only when unread.</done>
</task>

</tasks>

<verification>
- [ ] `mix test --only jour_06` green in demo host
- [ ] `mix verify.journeys` green from repo root (8 tests)
- [ ] No files under `lib/chimeway/` modified
- [ ] JOUR-03 test unchanged
</verification>

## PLAN COMPLETE
