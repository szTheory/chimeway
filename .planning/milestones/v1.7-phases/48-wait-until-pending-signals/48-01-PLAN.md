---
phase: 48-wait-until-pending-signals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/notifier.ex
  - test/chimeway/notifier_contract_test.exs
autonomous: true
requirements: [READ-01]
must_haves:
  truths:
    - "`wait_until` progress rules accept an optional `cancel_signals` string list at notifier normalization time (D-03, D-04)"
    - "Invalid `cancel_signals` shapes fail fast with `{:invalid_cancel_signals, _}` before persistence (V5 input validation)"
    - "Time-only `wait_until` rules without `cancel_signals` normalize identically to today — no key emitted in output map (D-06)"
  artifacts:
    - path: "lib/chimeway/notifier.ex"
      provides: "normalize_cancel_signals/1 private helper + extended normalize_wait_until_rule/1 allowlist"
      contains: "cancel_signals"
    - path: "test/chimeway/notifier_contract_test.exs"
      provides: "Contract tests for accept/reject cancel_signals shapes"
      contains: "cancel_signals"
  key_links:
    - from: "lib/chimeway/notifier.ex"
      to: "normalize_wait_until_rule/1"
      via: "extra_keys allowlist includes cancel_signals"
      pattern: "extra_keys\\(rule, ~w\\(kind anchor delay_seconds to_step cancel_signals\\)"
    - from: "normalize_wait_until_rule/1"
      to: "normalize_cancel_signals/1"
      via: "with-clause validation before output map"
      pattern: "normalize_cancel_signals"
---

<objective>
Extend the `wait_until` progress-rule DSL with optional `cancel_signals` validation in `Chimeway.Notifier.normalize_wait_until_rule/1`, backed by a private `normalize_cancel_signals/1` helper. Add notifier contract tests proving valid lists normalize and invalid shapes reject with tagged errors.

Purpose: Progress rules are validated at declaration time before persistence on `WorkflowStep.config`. READ-01 requires durable `pending_signals` derived from rule config — validation must happen here, not at runtime in progression (Pitfall 1 from RESEARCH).

Output: Updated `lib/chimeway/notifier.ex` and extended `test/chimeway/notifier_contract_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-wait-until-pending-signals/48-CONTEXT.md
@.planning/phases/48-wait-until-pending-signals/48-RESEARCH.md
@.planning/phases/48-wait-until-pending-signals/48-VALIDATION.md

@lib/chimeway/notifier.ex
@test/chimeway/notifier_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add normalize_cancel_signals/1 and extend normalize_wait_until_rule/1 (D-03, D-04, D-06)</name>
  <files>lib/chimeway/notifier.ex</files>
  <read_first>
    - `lib/chimeway/notifier.ex` — `normalize_wait_until_rule/1` (lines ~655–679), `extra_keys/2`, `normalize_progress_to_step/1`, `normalize_channel/1` trim/reject-blank pattern
    - `.planning/phases/48-wait-until-pending-signals/48-CONTEXT.md` — locked decisions D-03, D-04, D-06
    - `.planning/phases/48-wait-until-pending-signals/48-RESEARCH.md` — Discretion Recommendations table for validation rules
  </read_first>
  <action>
    In `lib/chimeway/notifier.ex`, add a private `@max_cancel_signals 10` module attribute adjacent to other progress-rule constants (`@progress_wait_anchors`, `@progress_outcomes`).

    Add private helper `normalize_cancel_signals/1` with these clauses (exact behavior per RESEARCH discretion recommendations):

    1. `normalize_cancel_signals(:unset)` → `{:ok, []}` — caller passes `:unset` when the `cancel_signals` key is absent from the input rule map.
    2. `normalize_cancel_signals(signals) when is_list(signals)` — for each element: require binary string (reject non-strings with `{:error, {:invalid_cancel_signals, {:invalid_type, entry}}}`), `String.trim/1`, reject blank after trim with `{:error, {:invalid_cancel_signals, {:blank_entry, original}}}`, require at least 1 character after trim. After mapping, apply `Enum.uniq/1` preserving first-seen order. If length exceeds `@max_cancel_signals`, return `{:error, {:invalid_cancel_signals, {:too_many, count}}}`. On success return `{:ok, normalized_list}`.
    3. Any other input → `{:error, {:invalid_cancel_signals, {:not_a_list, other}}}`.

    Extend `normalize_wait_until_rule/1`:

    1. Change `extra_keys(rule, ~w(kind anchor delay_seconds to_step))` to include `cancel_signals` in the allowlist: `~w(kind anchor delay_seconds to_step cancel_signals)`.
    2. Read raw value: if `Map.has_key?(rule, "cancel_signals")` or `Map.has_key?(rule, :cancel_signals)`, use that value; else pass `:unset` to `normalize_cancel_signals/1`.
    3. In the existing `with` chain, add `{:ok, normalized_cancel_signals} <- normalize_cancel_signals(cancel_signals_raw)` after the existing anchor/delay/to_step validations.
    4. Build output map `%{"kind" => "wait_until", "anchor" => ..., "delay_seconds" => ..., "to_step" => ...}` and include `"cancel_signals" => normalized_cancel_signals` **only when** `normalized_cancel_signals != []`. When key absent or normalizes to `[]`, omit the key from the output map (stable serialized fixtures per D-06 and RESEARCH A2).

    Do **not** change `normalize_on_outcome_rule/1`, `normalize_stop_rule/1`, or any other progress-rule kind. Do **not** auto-default inbox event names — authors must declare `cancel_signals` explicitly (D-06).
  </action>
  <verify>
    <automated>mix test test/chimeway/notifier_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/chimeway/notifier.ex` contains `defp normalize_cancel_signals(` with at least 3 function clauses.
    - `normalize_wait_until_rule/1` allowlist includes `cancel_signals` in the `extra_keys/2` call.
    - `Notifier.normalize_workflow_declaration/1` called with a valid `wait_until` rule including `"cancel_signals" => ["chimeway.notification.read"]` returns `{:ok, _}` and the normalized rule contains that string in `"cancel_signals"`.
    - `Notifier.normalize_workflow_declaration/1` called with `"cancel_signals" => ["  "]` returns `{:error, {:workflow_resolution_failed, {:invalid_workflow_progress_rule, {:invalid_cancel_signals, {:blank_entry, _}}}}}`.
    - `Notifier.normalize_workflow_declaration/1` called with 11 distinct non-blank strings in `cancel_signals` returns `{:error, _}` tagged `{:invalid_cancel_signals, {:too_many, _}}`.
    - Existing `wait_until` rule without `cancel_signals` key still normalizes to a map with keys exactly `kind`, `anchor`, `delay_seconds`, `to_step` (no `cancel_signals` key).
  </acceptance_criteria>
  <done>Notifier DSL accepts optional bounded `cancel_signals` on `wait_until` rules with strict validation; time-only rules unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Notifier contract tests for cancel_signals accept/reject matrix (READ-01)</name>
  <files>test/chimeway/notifier_contract_test.exs</files>
  <read_first>
    - `test/chimeway/notifier_contract_test.exs` — existing `"normalizes step progress rules with string-keyed wait_until and on_outcome shapes"` test (~line 409) and `"rejects invalid step progress rule shapes with tagged errors"` test (~line 453) for assertion style and `Notifier.normalize_workflow_declaration/1` fixture shape
    - `lib/chimeway/notifier.ex` — `normalize_cancel_signals/1` error tags from Task 1
    - `.planning/phases/48-wait-until-pending-signals/48-VALIDATION.md` — task 48-01-01 verification command
  </read_first>
  <action>
    Add a new describe block `"wait_until cancel_signals normalization (READ-01)"` in `test/chimeway/notifier_contract_test.exs` with at least these tests (reuse the minimal two-step workflow fixture pattern from existing progress-rule tests — `in_app` step with progress + `email` step with empty config):

    1. **Accept valid list** — rule includes `"cancel_signals" => ["chimeway.notification.read", "chimeway.notification.seen"]`; assert normalized progress contains that exact list under `"cancel_signals"`.
    2. **Trim and dedupe** — rule includes `"cancel_signals" => [" chimeway.delivery.succeeded ", "chimeway.delivery.succeeded"]`; assert normalized list is `["chimeway.delivery.succeeded"]` (one entry, trimmed).
    3. **Explicit empty list** — rule includes `"cancel_signals" => []`; assert normalized rule omits `"cancel_signals"` key OR contains `"cancel_signals" => []` — match whichever behavior Task 1 implemented; document via assertion.
    4. **Reject blank entry** — `"cancel_signals" => [""]` or `["   "]` → `{:invalid_cancel_signals, {:blank_entry, _}}`.
    5. **Reject non-list** — `"cancel_signals" => "not_a_list"` → `{:invalid_cancel_signals, {:not_a_list, _}}`.
    6. **Reject over limit** — 11 distinct valid strings → `{:invalid_cancel_signals, {:too_many, _}}`.
    7. **Reject mixed rule shape** — `wait_until` rule with both `cancel_signals` and `outcome` keys → `{:mixed_rule_shape, _}` (existing guardrail per D-04).

    Extend the existing `"normalizes step progress rules with string-keyed wait_until and on_outcome shapes"` test only if needed to confirm absence of regression — do not break its exact pattern match on progress list unless the fixture intentionally adds `cancel_signals`.
  </action>
  <verify>
    <automated>mix test test/chimeway/notifier_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/chimeway/notifier_contract_test.exs --warnings-as-errors` exits 0.
    - Test file contains describe `"wait_until cancel_signals normalization (READ-01)"`.
    - At least 6 distinct test cases covering accept, trim/dedupe, blank reject, non-list reject, too_many reject, and mixed_rule_shape reject.
    - No changes to `lib/chimeway/workflows/progression.ex` in this plan (Wave 2 owns runtime population).
  </acceptance_criteria>
  <done>Notifier contract tests lock `cancel_signals` DSL validation for Wave 2 progression consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Notifier DSL → persisted step config | Host-authored progress rules pass through normalization before `WorkflowStep.config` JSON is stored. Untrusted input is the notifier callback return value at workflow declaration time. |
| Normalization → progression engine | Validated rules are replay-safe JSON; progression reads persisted config only — no runtime re-validation in Phase 48 Wave 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | `cancel_signals` DSL input | mitigate | `normalize_cancel_signals/1`: require list type, trim non-empty strings, dedupe, max 10 entries (`@max_cancel_signals`). Reject via `{:invalid_cancel_signals, _}`. |
| T-48-02 | Denial of Service | oversized signal allowlist | mitigate | Max count 10 at normalization — bounds PG array cardinality on downstream `pending_signals` column. |
| T-48-03 | Elevation of Privilege | cross-tenant signal routing | accept | No change to `route_signal/1` in this plan (D-02, D-07). Tenant/actor isolation unchanged. |
| T-48-04 | Information Disclosure | raw payloads in traces | accept | `cancel_signals` stores event-name strings only; no payload fields added. |

**No `high`-severity threats.** Validation is additive at declaration time; existing routing isolation (T-27-03) is untouched. Block condition not triggered.
</threat_model>

<verification>
- [ ] `mix test test/chimeway/notifier_contract_test.exs --warnings-as-errors` green
- [ ] `mix compile --warnings-as-errors` green
- [ ] `grep -n "normalize_cancel_signals" lib/chimeway/notifier.ex` returns at least one match
- [ ] No edits to `lib/chimeway/workflows/progression.ex` in this plan
</verification>

<success_criteria>
- `cancel_signals` is a validated optional key on `wait_until` progress rules (D-03, D-04)
- Invalid shapes reject with tagged `{:invalid_cancel_signals, _}` errors
- Time-only waits normalize without `cancel_signals` key (D-06)
- All contract tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/48-wait-until-pending-signals/48-01-SUMMARY.md`
</output>
