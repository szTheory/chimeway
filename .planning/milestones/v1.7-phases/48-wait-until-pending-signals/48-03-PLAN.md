---
phase: 48-wait-until-pending-signals
plan: 03
type: execute
wave: 3
depends_on: ["48-02"]
files_modified:
  - guides/flows/multi-step-journeys.md
  - test/chimeway/doc_contract_test.exs
autonomous: true
requirements: [READ-01]
must_haves:
  truths:
    - "Journey guide documents optional `cancel_signals` on `wait_until` rules with canonical inbox event names (D-05, D-09)"
    - "READ-01 engine-gap callout removed from journey guide — no manual host `pending_signals` assignment documented as primary path"
    - "READ-02 deferral remains documented so doc contract `Deferred|READ-0` regex still passes"
    - "`doc_contract_test.exs` requires `cancel_signals` and forbids regression gap language"
  artifacts:
    - path: "guides/flows/multi-step-journeys.md"
      provides: "Authoring reference for cancel_signals + canonical chimeway.notification.* event names"
      contains: "cancel_signals"
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "Doc-truth contract locking cancel_signals presence and gap-callout removal"
      contains: "cancel_signals"
  key_links:
    - from: "guides/flows/multi-step-journeys.md"
      to: "wait_until progress rule DSL"
      via: "§2 progress rules table optional cancel_signals column/row"
      pattern: "cancel_signals"
    - from: "test/chimeway/doc_contract_test.exs"
      to: "guides/flows/multi-step-journeys.md"
      via: "@required includes cancel_signals"
      pattern: "cancel_signals"
---

<objective>
Update `guides/flows/multi-step-journeys.md` to document the shipped `cancel_signals` DSL, remove the READ-01 engine-gap callout, and lock doc-truth with `doc_contract_test.exs` updates.

Purpose: ROADMAP Phase 48 success criterion #3 — journey guide no longer documents READ-01 as an engine gap. D-09 scopes this to the journey guide only (mention-escalation recipe rewrite deferred to Phase 50 / DEMO-04).

Output: Updated journey guide and doc contract tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-wait-until-pending-signals/48-CONTEXT.md
@.planning/phases/48-wait-until-pending-signals/48-RESEARCH.md
@.planning/phases/48-wait-until-pending-signals/48-VALIDATION.md
@.planning/phases/48-wait-until-pending-signals/48-02-SUMMARY.md

@guides/flows/multi-step-journeys.md
@test/chimeway/doc_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Journey guide doc-truth for cancel_signals and READ-01 shipped (D-05, D-09)</name>
  <files>guides/flows/multi-step-journeys.md</files>
  <read_first>
    - `guides/flows/multi-step-journeys.md` — full file; especially §2 Progress Rules table (~lines 72–80), §7 Generic Signal Routing (~lines 174–193), Deferred / Future section
    - `.planning/phases/48-wait-until-pending-signals/48-CONTEXT.md` — D-05 (canonical event names), D-09 (doc scope), D-06 (explicit opt-in, no auto-default)
    - `.planning/phases/48-wait-until-pending-signals/48-RESEARCH.md` — Doc Contract Test Implications + recommended authoring shape
  </read_first>
  <action>
    Edit `guides/flows/multi-step-journeys.md`:

    1. **§1 example** — extend the sample `wait_until` rule in the Elixir code block to include optional `"cancel_signals"` with `["chimeway.notification.read", "chimeway.notification.seen"]`. Add one sentence: authors must declare `cancel_signals` explicitly for signal-driven early exit; time-only waits omit the key (D-06).

    2. **§2 Progress Rules table** — update the `wait_until` row to document optional `cancel_signals` (array of non-empty event-name strings). Note that when present, the engine copies the list into `run.pending_signals` at `:waiting` entry (READ-01 shipped). Keep required keys as `anchor`, `delay_seconds`, `to_step`.

    3. **§7 Generic Signal Routing** — remove the entire paragraph starting with `**Engine gap today:**` (current lines ~184–185). Replace with a short shipped note: `enter_waiting/6` auto-populates `pending_signals` from the rule's `cancel_signals` when declared; hosts no longer need manual `Workflows.update_run/3` for `wait_until`-driven waits.

    4. **Deferred / Future (READ Milestone)** section — remove the **READ-01** bullet entirely. **Keep READ-02** bullet (`Wire inbox read/seen events...`) so aspirational inbox emission remains deferred to Phase 49. Update intro sentence if needed — e.g. "Inbox read/seen signal emission (READ-02) is not wired in v1.7 Phase 48" rather than implying READ-01 is still future.

    5. **Canonical event names** — add a short subsection or bullet list under §7 or §2 naming `chimeway.notification.read` and `chimeway.notification.seen` as the canonical `cancel_signals` values for inbox-driven early exit (D-05). Explicitly state Phase 48 documents but does **not** emit these events — `Chimeway.mark_read/3` / `mark_seen/3` wiring is READ-02 (Phase 49).

    6. **Scope fence** — do **not** rewrite `guides/recipes/mention-escalation` content (Phase 50 / DEMO-04). Do **not** document read-cancel halt before `due_at` as shipped (READ-03 / JOUR-06 remain future).

    7. Verify guide still contains strings required by existing doc contract: `wait_until`, `pending_signals`, `Chimeway.Signal.track`, `Chimeway.Dispatch.SignalRouterWorker`, and a `Deferred` or `READ-0` reference for READ-02 deferral.
  </action>
  <verify>
    <automated>mix ci.verify_gates</automated>
  </verify>
  <acceptance_criteria>
    - `guides/flows/multi-step-journeys.md` contains substring `cancel_signals` at least twice (table + example or prose).
    - `guides/flows/multi-step-journeys.md` contains `chimeway.notification.read` and `chimeway.notification.seen`.
    - `guides/flows/multi-step-journeys.md` does **not** contain `Engine gap today`.
    - `guides/flows/multi-step-journeys.md` does **not** contain bullet `READ-01`.
    - `guides/flows/multi-step-journeys.md` still contains `READ-02` in Deferred/Future section.
    - `grep -n "stage_escalation_webhook" guides/flows/multi-step-journeys.md` returns nothing (out of scope).
  </acceptance_criteria>
  <done>Journey guide reflects shipped READ-01 behavior and cancel_signals authoring reference.</done>
</task>

<task type="auto">
  <name>Task 2: Doc contract test updates for cancel_signals and gap regression guard</name>
  <files>test/chimeway/doc_contract_test.exs</files>
  <read_first>
    - `test/chimeway/doc_contract_test.exs` — `"journey guide doc contract (DOCS-03)"` describe (~lines 31–88): `@required`, `@forbidden_strings`, `"includes Deferred or READ milestone callout"` test
    - `guides/flows/multi-step-journeys.md` — Task 1 edits for string presence
    - `.planning/phases/48-wait-until-pending-signals/48-RESEARCH.md` — Doc Contract Test Implications table
  </read_first>
  <action>
    In `test/chimeway/doc_contract_test.exs`, within the `"journey guide doc contract (DOCS-03)"` describe:

    1. Add `"cancel_signals"` to the `@required` list (~line 67) alongside existing `pending_signals` entry.

    2. Add `"Engine gap today"` to `@forbidden_strings` (~line 37) to prevent READ-01 gap regression.

    3. Do **not** remove or weaken the test `"includes Deferred or READ milestone callout"` — it must still pass via READ-02 deferral text in the guide.

    4. Optionally add `"READ-01"` to `@forbidden_strings` if the guide no longer should mention READ-01 as pending work — only if it does not break other legitimate references; prefer forbidding `"**READ-01:**"` pattern via a dedicated test if needed. Minimum requirement: forbid `"Engine gap today"`.

    Run `mix ci.verify_gates` to confirm all doc contracts pass (journey guide + any other gated docs).
  </action>
  <verify>
    <automated>mix ci.verify_gates</automated>
  </verify>
  <acceptance_criteria>
    - `@required` list in doc contract includes `cancel_signals`.
    - `@forbidden_strings` includes `Engine gap today`.
    - `mix ci.verify_gates` exits 0.
    - `mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Doc contract tests lock READ-01 doc-truth and prevent gap-callout regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public documentation | Journey guide is adopter-facing; must not instruct manual host glue for behavior the engine now provides, nor promise Phase 49/51 semantics as shipped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-09 | Information Disclosure | operator/adopter confusion | mitigate | Document event-name allowlist only; state inbox emission is Phase 49 (READ-02). Do not document raw payload routing. |
| T-48-10 | Tampering | doc drift reintroducing host glue | mitigate | `@forbidden_strings` includes `Engine gap today`; `@required` includes `cancel_signals`. |

**No `high`-severity threats.** Documentation-only plan; no runtime auth or data-plane changes.
</threat_model>

<verification>
- [ ] `mix ci.verify_gates` green
- [ ] `mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` green
- [ ] `mix ci.test` green (full phase gate before `/gsd-verify-work`)
- [ ] Journey guide: READ-01 gap removed, READ-02 deferral retained
</verification>

<success_criteria>
- ROADMAP success criterion #3: journey guide no longer documents READ-01 as an engine gap (D-09)
- `cancel_signals` documented with canonical `chimeway.notification.read` / `.seen` names (D-05)
- Doc contract tests prevent regression
- All CI gates green
</success_criteria>

<output>
After completion, create `.planning/phases/48-wait-until-pending-signals/48-03-SUMMARY.md`
</output>
