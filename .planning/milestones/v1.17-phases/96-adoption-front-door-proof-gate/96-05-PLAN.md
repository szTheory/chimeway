---
phase: 96-adoption-front-door-proof-gate
plan: "05"
type: execute
wave: 5
depends_on: ["96-04"]
files_modified:
  - scripts/prove-accrue-consumer.exs
  - test/support/artifact_consumer_fixture.ex
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
gap_closure: true
requirements: [GATE-01, DOCS-01]

must_haves:
  truths:
    - "GATE-01: Every ArtifactArchive validator failure in the standalone Accrue command exits with provenance status and emits exactly one fixed redacted diagnostic, with no stacktrace or proof record."
    - "DOCS-01: The Mailglass evidence contract always compiles the current package-owned fixture source, so release-gate results cannot depend on a stale test-support beam."
    - "DOCS-01: The Accrue source contract distinguishes proof-record construction from the longer test-failure environment variable name instead of rejecting a safe diagnostic control."
    - "GATE-01/DOCS-01: `mix ci.verify_gates` finishes green after the focused malformed/digest and release-contract regressions pass."
  artifacts:
    - path: "scripts/prove-accrue-consumer.exs"
      provides: "Exhaustive redacted handling for ArtifactArchive {:error, reason} results"
      contains: "archive validation failed"
    - path: "test/support/artifact_consumer_fixture.ex"
      provides: "Compiler dependency tracking for the package-owned fixture source"
      contains: "external_resource"
    - path: "test/chimeway/release_gate_contract_test.exs"
      provides: "CLI redaction regressions and corrected Mailglass/Accrue release contracts"
      contains: "accrue_packaged_cli"
  key_links:
    - from: "scripts/prove-accrue-consumer.exs"
      to: "priv/adoption_proof/artifact_archive.ex"
      via: "all {:error, _reason} validator results map to one fixed provenance diagnostic and exit code 65"
      pattern: "{:error, _reason}"
    - from: "test/support/artifact_consumer_fixture.ex"
      to: "priv/adoption_proof/artifact_consumer_fixture.ex"
      via: "the external source is an explicit Mix compiler resource rather than an untracked compile-time require"
      pattern: "@external_resource"
    - from: "test/chimeway/release_gate_contract_test.exs"
      to: "mix ci.verify_gates"
      via: "focused CLI and stale-contract repairs precede the complete doc/release parity gate"
      pattern: "release_gate_contract_test"
---

<objective>
Close the standalone Accrue redaction gap, repair the two diagnosed release-contract mechanisms, and restore green phase release-gate evidence.

Purpose: Ensure ordinary invalid packaged-proof input cannot leak an exception and ensure the phase's canonical documentation/release gate measures current source truth rather than stale compilation or substring collisions.
Output: Exhaustive Accrue validator-result handling, CLI-level malformed/digest regressions, compiler-tracked fixture loading, precise source contracts, and a green `mix ci.verify_gates` run.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/96-adoption-front-door-proof-gate/96-CONTEXT.md
@.planning/phases/96-adoption-front-door-proof-gate/96-VERIFICATION.md
@.planning/phases/96-adoption-front-door-proof-gate/96-04-SUMMARY.md
@scripts/prove-accrue-consumer.exs
@test/support/artifact_consumer_fixture.ex
@test/chimeway/release_gate_contract_test.exs
@priv/adoption_proof/artifact_consumer_fixture.ex

<interfaces>
- `Chimeway.AccrueProofCLI.run/1` returns exit 64 for usage, 65 for provenance/archive validation, 66 for proof execution, and 0 for one successful package-owned proof. Preserve these status meanings.
- `ArtifactArchive.with_validated_archive/3` returns `{:error, reason}` for digest, metadata, archive-format, limit, and containment failures. The standalone CLI must not expose `reason`.
- The one allowed validator diagnostic is exactly `Accrue package proof: archive validation failed\n` on stderr; no other line, stacktrace, path, digest, payload, or `CHIMEWAY_ACCRUE_PROOF ` record may accompany it.
- `test/support/artifact_consumer_fixture.ex` currently compiles the package-owned `priv/adoption_proof/artifact_consumer_fixture.ex`; changes to that external source must invalidate the cached test-support compilation.
</interfaces>
</context>

## Artifacts this phase produces

- Exhaustive validator-error translation in `scripts/prove-accrue-consumer.exs` with one fixed provenance diagnostic and exit 65.
- CLI regressions for malformed archive bytes and a well-formed but incorrect SHA-256, asserting exact combined output and absence of stacktrace/proof records.
- Compiler dependency tracking for the package-owned fixture plus precise Mailglass timeline and Accrue proof-emission source contracts.
- Fresh green evidence from `mix ci.verify_gates`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make standalone Accrue validator failures exactly redacted</name>
  <reversibility rating="reversible">The change only completes an existing error branch and preserves the established status-code and successful-proof contracts.</reversibility>
  <files>scripts/prove-accrue-consumer.exs, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    - `scripts/prove-accrue-consumer.exs` — preserve argument validation, proof callback, test failure controls, status codes, and successful `proof.output`; add exhaustive handling for the validator's actual error tuple.
    - `priv/adoption_proof/artifact_archive.ex` — confirm every ordinary archive failure returns `{:error, String.t()}` and may contain a more specific internal reason that the CLI must not print.
    - `test/chimeway/release_gate_contract_test.exs` — extend the existing `packaged Accrue archive proof CLI` subprocess tests and helpers so assertions cover true process output/status.
    - `.planning/phases/96-adoption-front-door-proof-gate/96-CONTEXT.md` — D-07 requires fixed redacted framing; D-08 forbids paths, payloads, full exceptions, and duplicate/unsafe proof records; D-09 preserves Accrue provenance truth.
    - `.planning/phases/96-adoption-front-door-proof-gate/96-VERIFICATION.md` — reproduce the reported `WithClauseError` from a wrong digest and close both requested malformed/digest cases.
  </read_first>
  <behavior>
    - "A valid archive path with a well-formed but incorrect 64-character digest exits 65 and emits exactly `Accrue package proof: archive validation failed` plus one newline."
    - "Malformed archive bytes paired with their actual SHA-256 also exit 65 with exactly the same single diagnostic."
    - "Neither case emits `WithClauseError`, any stacktrace marker, a filesystem path, either digest, internal validator reason, or a line beginning with the Accrue proof-record prefix."
    - "Usage errors remain exit 64 with their existing fixed usage diagnostics, proof execution errors remain exit 66, and a valid archive still emits exactly one successful proof record and exits 0."
  </behavior>
  <action>Add failing CLI-level subprocess assertions for two validator paths: a real package archive with a syntactically valid wrong digest, and malformed archive bytes with their matching digest. Assert exact combined output, exit 65, one line, no exception/stacktrace text, no path/digest/internal reason, and no line anchored to the Accrue proof-record prefix. Then add an `{:error, _reason}` branch to the `with` result handling that discards the internal reason and calls `diagnostic("archive validation failed", @provenance)`. Do not interpolate the validator reason or rescue a `WithClauseError`; make the result algebra exhaustive. Preserve the separate `{:usage, message}` and proof callback behavior and D-07/D-08/D-09 status, redaction, and evidence contracts.</action>
  <acceptance_criteria>
    - Wrong-digest and malformed-archive validator failures have byte-for-byte identical one-line output and exit 65.
    - No validator failure can fall through the `with`, raise `WithClauseError`, print a stacktrace, or emit a proof record.
    - Internal validator reasons are discarded at the CLI boundary and cannot reveal paths, sizes, metadata, or supplied digest values.
    - Existing usage, proof-failure, and successful-proof statuses/output remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --only accrue_packaged_cli --warnings-as-errors</automated>
  </verify>
  <done>The standalone Accrue command turns every archive-validator failure into exactly one fixed redacted provenance diagnostic with no exception or proof-record leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair stale and imprecise release contracts, then prove the full gate green</name>
  <reversibility rating="reversible">These changes correct test compilation/input matching and can be revised without affecting production APIs or stored data.</reversibility>
  <files>test/support/artifact_consumer_fixture.ex, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    - `test/support/artifact_consumer_fixture.ex` — the current compile-time `Code.require_file/2` does not make changes in the package-owned priv source an explicit incremental-compiler dependency.
    - `priv/adoption_proof/artifact_consumer_fixture.ex` — use its current `@mailglass_timeline` and evidence validators as the production/package source of truth; do not weaken them to accommodate a stale beam.
    - `test/chimeway/release_gate_contract_test.exs:1228` — the observed failure reports an invalid Mailglass timeline even though the source helper and current source validator agree, indicating stale compiled external source.
    - `test/chimeway/release_gate_contract_test.exs:1622` — the broad source substring assertion collides with `CHIMEWAY_ACCRUE_PROOF_TEST_FAILURE`; it must detect proof-record construction, not forbid a longer safe environment-variable name.
    - `.planning/phases/96-adoption-front-door-proof-gate/96-VERIFICATION.md` — the full required gate is red with no accepted override; completion requires a fresh successful run.
  </read_first>
  <behavior>
    - "Changing the package-owned fixture's canonical Mailglass timeline invalidates/recompiles its test-support consumer, so the release contract always parses with current source."
    - "The canonical Mailglass evidence line containing the exact five-event timeline parses successfully; missing, reordered, duplicated, or appended timeline values remain rejected."
    - "The standalone Accrue runner may contain the safe test-failure environment-variable name but may not construct or print a line beginning with the Accrue proof-record prefix."
    - "The focused tests pass in an incremental build and after removal of the relevant cached test beam, proving the result does not rely on stale compilation."
    - "The repository's canonical `mix ci.verify_gates` entrypoint exits zero with no override or excluded release-contract case."
  </behavior>
  <action>First reproduce both line-specific failures and record their exact causes in the task summary. Make `test/support/artifact_consumer_fixture.ex` declare the package-owned priv fixture as an explicit external compiler resource while retaining that file as the sole implementation/source of truth; ensure a source edit triggers recompilation instead of loading a cached module assembled before the current timeline. Keep the Mailglass validator's exact safe timeline and negative cases intact. Replace the Accrue runner source's broad prefix substring prohibition with a precise assertion against proof-record construction/emission, such as an anchored output-writing pattern or parsed source construct, so the longer `..._TEST_FAILURE` control is allowed while direct record fabrication still fails. Add a mutation assertion that injecting a real proof-record write is detected. Run the two line-focused tests, the complete release-gate test file, formatting, and finally the exact canonical `mix ci.verify_gates`; fix only genuine contract/source-parity defects revealed by those runs, never by deleting assertions, widening safe evidence values, excluding tests, or changing D-01–D-15's docs/CI scope.</action>
  <acceptance_criteria>
    - The Mailglass line-1228 contract passes against current package-owned source and still rejects every noncanonical timeline mutation.
    - Incremental compilation tracks `priv/adoption_proof/artifact_consumer_fixture.ex`; the test cannot silently execute an older fixture beam after that file changes.
    - The line-1622 Accrue contract permits the safe failure-control variable but fails if the standalone runner constructs/emits its own proof-record line.
    - The complete release-gate contract module passes without exclusions or weakened evidence/redaction assertions.
    - `mix ci.verify_gates` exits zero and its successful output is recorded in `96-05-SUMMARY.md`.
  </acceptance_criteria>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs:1228 test/chimeway/release_gate_contract_test.exs:1622 --warnings-as-errors &amp;&amp; scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors &amp;&amp; mix format --check-formatted scripts/prove-accrue-consumer.exs test/support/artifact_consumer_fixture.ex test/chimeway/release_gate_contract_test.exs &amp;&amp; mix ci.verify_gates</automated>
  </verify>
  <done>Both diagnosed contract failures are corrected at their real mechanisms, all CLI redaction tests pass, and the canonical release-gate command is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Archive validator → standalone CLI | Specific internal failure reasons cross into an adopter-facing process and must collapse to one safe diagnostic. |
| Package-owned fixture source → compiled test support | Incremental compilation can otherwise preserve stale evidence rules and make release-gate outcomes depend on local build history. |
| Source contract → proof-emission claim | A broad substring can confuse a safe test-control name with record fabrication, producing false release failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-96-21 | Information Disclosure | Accrue CLI validator failure | high | mitigate | Exhaustively map `{:error, _}` to one fixed line/status and assert exact process output with no stacktrace, path, digest, reason, or record. |
| T-96-22 | Repudiation | release-gate fixture compilation | high | mitigate | Track the priv fixture as an external compiler resource and test incremental/fresh behavior against one source of truth. |
| T-96-23 | Tampering | Mailglass evidence contract | high | mitigate | Preserve the exact canonical timeline and all mutation rejections; repair loading, not validation strictness. |
| T-96-24 | Denial of Service | false Accrue source gate | medium | mitigate | Match actual record-writing constructs and prove a mutation is caught without colliding with the safe environment variable. |
| T-96-25 | Repudiation | phase release evidence | high | mitigate | Require the canonical `mix ci.verify_gates` command to exit zero and record that run in the plan summary. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Trust CI-backed proof commands and truthful documentation | 96-01–96-05 | COVERED | 96-05 closes unsafe standalone failure output and restores the canonical release gate. |
| REQ | GATE-01 | Aggregate clean-room proofs without partner suites | 96-04, 96-05 | COVERED | Plan 04 hardens archives; Plan 05 makes standalone validator failures safe. |
| REQ | DOCS-01 | Contract-check front door, fixture commands, and CI entrypoint | 96-05 | COVERED | Current-source compilation, precise source assertions, and full gate execution restore parity evidence. |
| RESEARCH | — | Preserve strict evidence parsers and package-owned fixture boundary | 96-05 | COVERED | Contracts keep the priv fixture as sole source and do not widen evidence. |
| CONTEXT | D-07–D-09 | Fixed safe records, redacted failures, truthful Accrue semantics | 96-05 | COVERED | Task 1 handles validator tuples exactly and Task 2 protects proof construction. |
| CONTEXT | D-13–D-15 | Existing ExUnit contracts, negative drift, structural/behavioral distinction | 96-05 | COVERED | Task 2 fixes and mutation-tests the existing release contracts, then runs the canonical gate. |
| CONTEXT | D-01–D-06, D-10–D-12 | Selector, command, local runner, and one-lane topology | 96-01–96-03 | COVERED | 96-05 preserves these decisions and changes no docs, task facade, runner topology, or workflow. |
| VERIFICATION | — | Standalone malformed/digest failures are only one redacted diagnostic | 96-05 | COVERED | Task 1 adds exact CLI subprocess regressions and exhaustive tuple handling. |
| VERIFICATION | — | Release contracts near 1228 and 1622 are fixed | 96-05 | COVERED | Task 2 addresses stale external compilation and substring collision directly. |
| VERIFICATION | — | `mix ci.verify_gates` is green | 96-05 | COVERED | Task 2's final automated gate is the exact required command. |

Deferred ADPT-03/INBX-03, live-provider behavior, browser UI, new adapters, partner-suite expansion, alternate CI topology, and hosted-lane attestation remain excluded. The separate live GitHub Actions item remains behavior-unverified until an actual run URL is supplied.

<verification>
- Run the focused packaged Accrue CLI tag and assert exact malformed/wrong-digest process output and status.
- Run line 1228 and line 1622 directly to demonstrate both reported release-contract failures are closed.
- Run the entire release-gate contract module with warnings as errors.
- Run formatting on every plan-owned source/test file.
- Finish with the canonical `mix ci.verify_gates`; no substitute command or accepted override satisfies this plan.
</verification>

<success_criteria>
- Every standalone Accrue archive-validator error emits exactly one fixed redacted provenance line and exits 65.
- Malformed archive and wrong-digest subprocess tests contain no stacktrace, exception, proof record, path, digest, or internal reason.
- Mailglass contracts use the current package-owned fixture implementation in incremental and fresh builds and retain exact evidence strictness.
- Accrue source contracts detect actual proof-record fabrication without colliding with safe test controls.
- The complete canonical release-gate command is green.
</success_criteria>

<completion_criteria>
- All Task 1 and Task 2 automated commands exit zero in sequence.
- `96-05-SUMMARY.md` records the reproduced root causes, corrected mechanisms, exact CLI diagnostic/status evidence, and green `mix ci.verify_gates` result.
- No docs/CI topology, public proof command, evidence schema, provider claim, or hosted-CI verification status is altered.
</completion_criteria>

<output>
Create `.planning/phases/96-adoption-front-door-proof-gate/96-05-SUMMARY.md` when done.
</output>
