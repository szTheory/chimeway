# Phase 96.1: Close gap: ARCHIVE-ATOM-01 — atom-safe archive metadata parsing - Context

**Gathered:** 2026-08-11 (assumptions mode, expanded research)
**Status:** Ready for planning

<domain>
## Phase Boundary

Close the v1.17 release-blocking atom-table denial-of-service in the Phase 96 archive proof boundary. Replace arbitrary Erlang-term parsing of caller-controlled `metadata.config` with bounded, atom-free validation while preserving valid Hex-built archive behavior, the existing proof commands, sanitized diagnostics, and executable release-gate evidence. This phase does not redesign the broader tar subsystem, add archive authenticity, change public proof records, or expand the adoption product surface.

</domain>

<decisions>
## Implementation Decisions

### Atom-safe metadata parser
- **D-01:** Replace `:file.consult/1` with a small private, in-memory, byte-oriented parser for the canonical Hex metadata subset. Archive-controlled bytes must never reach `:file.consult/1`, `:erl_scan`, `:erl_parse`, `Code.string_to_quoted`, `String.to_atom/1`, `List.to_atom/1`, or any evaluator or atom-creating conversion.
- **D-02:** Parse repeated dot-terminated `{binary_key, value}.` forms and materialize only the binary-keyed `<<"name">>`, `<<"version">>`, and `<<"files">>` fields. Recognize only the fixed literals required by the Hex grammar, such as `true` and `false`, without deriving atoms from input.
- **D-03:** Allow unknown binary-keyed fields only when their values are structurally valid under the safe bounded grammar. Reject arbitrary bare or quoted atoms, variables, expressions, improper lists, duplicate selected keys, malformed delimiters or escapes, trailing content, and unsupported constructs. Atom-looking content inside binaries remains ordinary valid data.
- **D-04:** Bound total parser work: reject metadata above 1 MiB before scanning, cap nesting depth at 32, cap `files` entries at the existing 4,096-member budget, cap `name` and `version` at 255 bytes, and keep scanning linear in input size. Parse the already-bounded binary without a temporary metadata file.

### Valid archive compatibility and trust semantics
- **D-05:** Preserve successful validation of real `mix hex.build` archives and the existing production-shaped fixtures. Keep exact package name `chimeway`, bounded valid version handling, required runner/fixture membership, regular-file presence, supported artifact-root layouts, and version agreement with unpacked `mix.exs`.
- **D-06:** Reject duplicate `name`, `version`, or `files` fields rather than silently choosing a winner. Invoke the callback exactly once and only after digest, metadata, extraction, root, required-file, and version validation all succeed.
- **D-07:** Continue treating a caller-supplied digest as integrity only, never publisher authenticity or trust, because the caller supplies both archive and digest. Preserve the existing immutable-byte validation order and do not introduce trusted-package language.
- **D-08:** Keep the compatibility promise narrow: support archives produced for Chimeway's proof path, guarded by a real-build contract. Do not create a general Erlang-term or universal Hex metadata library and do not add `hex_core` solely for this private proof seam.

### Executable security evidence
- **D-09:** Add a direct synchronous release-gate regression using a correctly digested hostile archive. Warm the parser path, generate randomized atom-token strings without interning them, record `:erlang.system_info(:atom_count)`, assert validation rejection and callback non-invocation, assert exact atom-count equality, and confirm sampled tokens still fail `String.to_existing_atom/1`.
- **D-10:** Cover bare atoms, quoted atoms, variables, atom-looking binary contents, duplicate selected fields, oversized metadata, excessive nesting and file counts, malformed/trailing syntax, and the real Hex-built success path. Use hundreds rather than thousands of hostile tokens so the initial red test demonstrates the vulnerability without needlessly risking the test VM.
- **D-11:** Route objective acceptance through `mix ci.verify_gates` and the affected ecosystem `verify.*` jobs. This security closure receives executable evidence, not conversational UAT.

### Developer experience and scope discipline
- **D-12:** Keep `with_validated_archive/3` and both existing callers behaviorally stable. Failures remain concise and sanitized; never echo archive metadata, hostile tokens, parser excerpts, temporary paths, raw exceptions, or terminal-control bytes. Internal closed reason categories may remain private implementation details.
- **D-13:** Keep the Phase 96 cold-subprocess timeout warning outside this phase because the atom-safety regression is in-process. Permit only the smallest timeout repair if the existing warning blocks the required green release gate; do not undertake general subprocess or CI restructuring.
- **D-14:** No Phoenix, Plug, Ecto, database, telemetry, browser UI, visual design, documentation-information-architecture, or public API change belongs in this phase. The adopter-facing result should be deliberately boring: the same command and proof output work, while hostile archives fail before callback execution without mutating the VM.

### the agent's Discretion
- Exact private parser function names and internal reason representation.
- Whether the safe grammar is implemented as recursive descent or an equivalent byte-state machine, provided it is linear, bounded, atom-free, non-evaluating, and covered by the locked compatibility and adversarial contracts.
- Exact fixed sanitized wording for newly distinguished malformed/unsupported metadata failures, provided current public error compatibility and redaction remain intact.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Release blocker and phase contract
- `.planning/v1.17-MILESTONE-AUDIT.md` — ARCHIVE-ATOM-01 release blocker and required atom-count regression.
- `.planning/phases/96-adoption-front-door-proof-gate/96-REVIEW.md` — CR-01 vulnerable parse boundary and separate WR-01 timeout warning.
- `.planning/phases/96-adoption-front-door-proof-gate/96-VERIFICATION.md` — shipped Phase 96 behavior that the gap closure must preserve.
- `.planning/phases/96-adoption-front-door-proof-gate/96-CONTEXT.md` — locked adoption proof, redaction, CI, and host-ownership decisions.
- `.planning/REQUIREMENTS.md` — GATE-01 and DOCS-01 release/proof requirements inherited through Phase 96.
- `.planning/METHODOLOGY.md` — research-first, cohesive recommendation, least-surprise DX, and executable-evidence lenses.

### Implementation and executable evidence
- `priv/adoption_proof/artifact_archive.ex` — vulnerable `parse_metadata!/1`, archive bounds, validation order, extraction defenses, and callback boundary.
- `test/chimeway/release_gate_contract_test.exs` — production-shaped archives, malicious archive builders, callback assertions, CLI redaction, and release-gate contracts.
- `scripts/prove-adoption-paths.exs` — aggregate adoption caller whose behavior must remain stable.
- `scripts/prove-accrue-consumer.exs` — standalone public archive caller whose behavior must remain stable.
- `mix.exs` — package version and real `mix hex.build` source of truth.

### Ecosystem reference designs
- `https://github.com/hexpm/specifications/blob/main/package_metadata.md` — canonical Hex metadata document and value grammar.
- `https://hex-core.hexdocs.pm/hex_core.html` — current `hex_core` bounds and selective metadata handling.
- `https://github.com/hexpm/hex_core/blob/main/src/safe_erl_term.xrl` — upstream atom-safe scanner precedent; reference only, not a Chimeway dependency.
- `https://www.erlang.org/doc/system/commoncaveats.html` — OTP atom lifetime and exhaustion risk.
- `https://maven.apache.org/resolver-archives/resolver-LATEST-1.x/about-checksums.html` — integrity is not trust/authenticity.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `Chimeway.AdoptionProof.ArtifactArchive.with_validated_archive/3`: retain as the shared callback-gated validation seam.
- Existing archive limits and tar scanner in `artifact_archive.ex`: reuse the 4,096-member policy and validate-before-materialize posture rather than reopening extraction design.
- Archive builders and synchronous tests in `release_gate_contract_test.exs`: extend them for correctly digested hostile metadata and exact VM atom-count evidence.

### Established Patterns
- External identities and evidence fields remain binaries; input-derived atoms are forbidden.
- Untrusted packages fail closed before callbacks or materialization and expose only fixed sanitized diagnostics.
- Real artifact behavior and low-cost structural contracts complement rather than replace each other.
- Machine-testable release behavior is demonstrated through `mix ci.verify_gates` and ecosystem `verify.*` entrypoints.

### Integration Points
- `parse_metadata!/1` feeds `validate_metadata!/1`, then extraction and `validate_root!/3`; the replacement parser must preserve that sequencing or return the same three required semantic fields directly.
- Both proof scripts require the same private module file, so no caller-specific parser or fallback should be introduced.
- Phase 96 verification and the v1.17 milestone audit are the downstream close gates after implementation evidence is green.

</code_context>

<specifics>
## Specific Ideas

- Model the parser after `hex_core`'s successful principles—bounded input, format-specific scanning, selective field retention, and no creation of unknown atoms—without coupling Chimeway to Hex's private scanner or replacing the broader archive implementation.
- Prefer calm CLI failures following the current brand direction: what happened, why it matters, and how to rerun, without exposing parser internals.
- A representative valid document is a sequence of binary-key tuples for `name`, `version`, and `files`; a representative hostile document adds randomized bare/quoted atom forms under a matching digest and must fail with no atom-count change.

</specifics>

<deferred>
## Deferred Ideas

- Registry signatures, publisher identity, or other package authenticity mechanisms.
- A Chimeway-owned JSON/manifest format or a general-purpose Hex metadata package.
- Replacing the full custom archive implementation with public `hex_core` APIs.
- Broad tar-parser redesign, fuzzing infrastructure, outer-member hardening beyond what ARCHIVE-ATOM-01 requires, and CI/subprocess performance restructuring.
- Public error structs, new CLI flags, proof-record changes, documentation redesign, or any browser/operator UI.

</deferred>

---

*Phase: 96.1-close-gap-archive-atom-01-atom-safe-archive-metadata-parsing*
*Context gathered: 2026-08-11*
