---
phase: 103-physical-iphone-adoption-truth
plan: "02"
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - lib/chimeway/mobile_proof/physical_bundle.ex
  - lib/mix/tasks/verify.physical_proof_contract.ex
  - test/chimeway/mobile_physical_proof_test.exs
  - test/chimeway/mobile_proof_extension_test.exs
  - test/fixtures/alpha_twin_physical_proof/physical-valid.json
  - test/fixtures/alpha_twin_physical_proof/physical-negative-corpus.json
autonomous: true
requirements: [TWIN-03]
must_haves:
  truths:
    - "Per D-01, the immutable hermetic extension remains byte-compatible while a distinct physical-v1 bundle validates through the already-published CrossWake authority."
    - "Per D-02, D-03, D-05, and D-09, Chimeway, CrossWake, visible-alert, and completion claims remain independently typed, owner-qualified, and digest-linked."
    - "Per D-04, D-06, and D-07, every CrossWake consumer reads only the authority-file SHA and reruns fresh-clone exact-HEAD/clean/module/API/marker/focused/source-bound checks before accepting it."
    - "Per D-08 and D-10 through D-12, retained records are recursively privacy-safe, only externally supplied observed can promote, and publication is append-only/no-replace."
  artifacts:
    - path: "lib/chimeway/mobile_proof/physical_bundle.ex"
      provides: "Closed physical-v1 bundle validation, digest binding, privacy scan, and exclusive publication"
      exports: ["validate/2", "publish/3", "bundle_digest/1"]
    - path: "lib/mix/tasks/verify.physical_proof_contract.ex"
      provides: "Credential-free immutable-package and exact selected-CrossWake physical contract verifier"
    - path: "test/fixtures/alpha_twin_physical_proof/physical-valid.json"
      provides: "Positive physical-v1 bundle fixture distinct from hermetic v1"
    - path: "test/fixtures/alpha_twin_physical_proof/physical-negative-corpus.json"
      provides: "Ordered fail-closed physical-v1 adversarial corpus"
  key_links:
    - from: "lib/mix/tasks/verify.physical_proof_contract.ex"
      to: "priv/mobile_proof/crosswake-selected-sha"
      via: "read the sole SHA, verify canonical ref advertisement, fresh clone/fetch, detach exact HEAD, clean status, module/API/fixture/test markers, focused test, and source-bound validation"
      pattern: "ChimewayNotificationPhysicalProof.*validate_source_bound"
    - from: "lib/chimeway/mobile_proof/physical_bundle.ex"
      to: "CrossWake canonical evidence and completion marker"
      via: "lowercase SHA-256 digests only; canonical authority bytes stay inside CrossWake validation scope"
      pattern: "crosswake_(evidence|completion_marker)_sha256"
    - from: "visible-alert-attestation.json"
      to: "chimeway-envelope.json"
      via: "machine_envelope_sha256 and the same opaque run_ref"
      pattern: "machine_envelope_sha256"
  prohibitions:
    - statement: "Chimeway must not modify the selected CrossWake source, fixture, or focused test or consume a local/floating revision."
      status: unverified
      flagged: true
    - statement: "Package provenance, CI success, provider acceptance, or a CrossWake digest must not masquerade as device display or protected activation."
      status: unverified
      flagged: true
    - statement: "Retained records and errors must not contain authority bytes, raw tokens, payloads, credentials, identity, device identifiers, endpoints, media, logs, or filesystem paths."
      status: unverified
      flagged: true
    - statement: "A retry must not replace, upgrade, or erase an earlier failed, unavailable, or successful record."
      status: unverified
      flagged: true
---

<objective>
Consume the final published CrossWake authority to prove one closed credential-free physical bundle and its complete adversarial corpus.

Purpose: Establish the Chimeway-owned physical-v1 schema, privacy, attestation, immutable-artifact, exact-revision, and no-replace boundaries without reopening or changing the selected CrossWake commit.
Output: A physical bundle validator/publisher, immutable-package verifier, positive fixture, adversarial corpus, and hermetic regression proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/103-physical-iphone-adoption-truth/103-CONTEXT.md
@.planning/phases/103-physical-iphone-adoption-truth/103-RESEARCH.md
@.planning/phases/103-physical-iphone-adoption-truth/103-PATTERNS.md
@.planning/phases/103-physical-iphone-adoption-truth/103-VALIDATION.md
@.planning/phases/103-physical-iphone-adoption-truth/103-01-SUMMARY.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-04-SUMMARY.md
@priv/mobile_proof/crosswake-selected-sha
@lib/chimeway/mobile_proof/extension.ex
@lib/mix/tasks/verify.physical_proof_contract.ex
@test/chimeway/mobile_proof_extension_test.exs

<interfaces>
Selected authority: priv/mobile_proof/crosswake-selected-sha contains the only permitted CrossWake revision and Plan 103-01 proved that exact published commit from a fresh checkout.
CrossWake API at that revision: Crosswake.ProofLane.ChimewayNotificationPhysicalProof.schema_version/0, assertions/0, validate_report/1, and validate_source_bound/2.
Existing hermetic API: Chimeway.MobileProof.Extension.validate/2 returns {:ok, map()} or {:error, %{rule_id: String.t(), path: [String.t()]}}.
New physical API: Chimeway.MobileProof.PhysicalBundle.validate/2, publish/3, and bundle_digest/1 use the same stable non-echoing failure projection.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trace one selected-SHA physical bundle through validation and no-replace publication</name>
  <precondition>`priv/mobile_proof/crosswake-selected-sha` exists with one lowercase 40-character SHA, and the named canonical ref advertises it.</precondition>
  <reversibility rating="one-way">D-01 fixes a retained physical schema/proof class that later validators must continue to recognize; the user already locked this decision, so no new checkpoint is required.</reversibility>
  <files>lib/chimeway/mobile_proof/physical_bundle.ex, lib/mix/tasks/verify.physical_proof_contract.ex, test/chimeway/mobile_physical_proof_test.exs</files>
  <read_first>
    - priv/mobile_proof/crosswake-selected-sha — sole selected-revision authority from Plan 103-01
    - .planning/phases/103-physical-iphone-adoption-truth/103-01-SUMMARY.md — publication and fresh-checkout evidence, not an alternate SHA source
    - .planning/phases/103-physical-iphone-adoption-truth/103-CONTEXT.md — D-01 through D-12 and D-19 through D-21
    - lib/chimeway/mobile_proof/extension.ex — exact-key, safe-atom, recursive-scan, and stable-error analog
    - lib/mix/tasks/verify.physical_proof_contract.ex — immutable archive verifier entrypoint
    - test/chimeway/mobile_proof_extension_test.exs — non-echoing hermetic contract boundary
  </read_first>
  <behavior>
    - "A valid bundle built from one immutable Chimeway archive and the exact authority-file CrossWake revision validates and publishes exactly once."
    - "The same destination rejects a second publication and preserves every original byte."
    - "Chimeway retains CrossWake evidence/completion digests and a closed validation outcome, never canonical authority bytes."
    - "All retained Chimeway facts are fixed to delivery_succeeded, apns_provider_accepted, and trace_explainable."
    - "An observed attestation supplied as input validates; no Chimeway producer manufactures or upgrades that state."
  </behavior>
  <action>Implement D-01 through D-12 as the first Chimeway consumer of the final selected commit; do not edit any file under `../crosswake`. Create `Chimeway.MobileProof.PhysicalBundle` as proof class physical/schema version 1 beside the unchanged hermetic Extension. Use four independently exact records: `chimeway_envelope`, `crosswake_record`, `visible_alert_attestation`, and `completion_marker`. Bind the immutable Chimeway artifact digest, canonical CrossWake remote, authority-file SHA, CrossWake contract version, evidence and completion-marker digests, fresh opaque run_ref, captured-at UTC, fixed Chimeway facts, component digests, and overall bundle digest. The attestation exact fields are schema_version, run_ref, machine_envelope_sha256, observed_at, attester_ref, and state in observed/not_observed/unavailable. Apply lowercase full SHA/digest checks, exact enum/order/owner checks, revision agreement, attestation binding, recursive D-08 key/value scanning, stable PP-* rule IDs with bounded paths only, and exclusive atomic directory creation. Extend `Mix.Tasks.Verify.PhysicalProofContract` so every CrossWake use reads the SHA only from the authority file, proves the named canonical ref advertises it, clones/fetches fresh, detaches exact HEAD, requires clean status and the module/API/fixture/test markers, runs the exact focused test, invokes source-bound validation, and deletes temporary authority bytes. Then build the immutable Chimeway archive, validate and publish the happy path to a temporary destination, prove collision refusal, and clean all ephemeral data. The command is credential-free Threshold A and emits only `release_ready_physical_pending`.</action>
  <verify>
    <automated>mix test test/chimeway/mobile_physical_proof_test.exs --max-failures 1 --warnings-as-errors &amp;&amp; mix verify.physical_proof_contract</automated>
  </verify>
  <acceptance_criteria>
    - Source: no CrossWake source, fixture, or focused-test path appears in this plan's modified files or git diff.
    - Behavior: every CrossWake check starts from the sole authority-file SHA and a fresh detached exact-HEAD clean checkout, then runs module/API/marker checks and the focused test.
    - Behavior: a valid bundle publishes once; a repeated destination returns a stable collision rule without byte changes.
    - Security: retained records contain no CrossWake authority bytes or D-08-sensitive material.
    - Test command: `mix verify.physical_proof_contract` exits 0 and prints `release_ready_physical_pending` without Apple credentials.
  </acceptance_criteria>
  <done>One production-quality credential-free bundle path consumes the exact published CrossWake authority and proves immutable package, closed ownership, privacy, attestation, and no-replace behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the complete physical-v1 corpus and immutable hermetic boundary</name>
  <reversibility rating="costly">D-07 fixes rule IDs and retained adversarial fixtures consumed by verification and support tooling.</reversibility>
  <files>test/chimeway/mobile_physical_proof_test.exs, test/chimeway/mobile_proof_extension_test.exs, test/fixtures/alpha_twin_physical_proof/physical-valid.json, test/fixtures/alpha_twin_physical_proof/physical-negative-corpus.json</files>
  <read_first>
    - test/chimeway/mobile_physical_proof_test.exs — tracer contract from Task 1
    - test/chimeway/mobile_proof_extension_test.exs — immutable hermetic regression boundary
    - test/fixtures/alpha_twin_physical_proof/valid.json — committed hermetic fixture that must not change
    - test/fixtures/alpha_twin_physical_proof/negative-corpus.json — committed hermetic corpus analog that must not change
    - priv/mobile_proof/crosswake-selected-sha — sole selected revision for every corpus/source-bound case
    - .planning/phases/103-physical-iphone-adoption-truth/103-VALIDATION.md — required adversarial coverage
  </read_first>
  <behavior>
    - "Missing, unknown, duplicate, reordered, wrong-owner, wrong-revision, wrong-digest, marker-mismatch, and recursive-sensitive cases fail with the expected stable rule/path."
    - "Observed, not_observed, and unavailable are the only structural states; promotion succeeds only for externally supplied observed."
    - "A mismatched run_ref, envelope digest, selected revision, evidence digest, marker digest, or completion digest cannot promote."
    - "The hermetic extension still accepts its committed valid fixture and rejects its original corpus unchanged."
  </behavior>
  <action>Create new `physical-valid.json` and `physical-negative-corpus.json` files without repurposing the committed hermetic fixtures per D-01. Use a fixed ordered case-ID list covering root and record exact-key failures; unsupported versions/classes/states; missing, duplicate, and reordered facts; owner forgery; missing/malformed/stale/mutable/mismatched authority-file revision; canonical-ref, fetch, detached-HEAD, clean-status, module/API/marker, focused-test, and source-bound failures; artifact/evidence/marker/component/bundle digest mismatches; run_ref/envelope/attester binding mismatches; every D-08 sensitive category at nested key and value positions; automation attempting to create or infer observed; not_observed/unavailable promotion refusal; and same-destination replay/no-replace. Assert exact PP-* rule IDs and bounded paths, and prove rejected values never enter return values, exceptions, or Mix output. Extend the hermetic test only with an immutability regression over its existing fixture/corpus semantics. Run the selected commit's focused test only through `mix verify.physical_proof_contract`; do not edit or supplement that CrossWake test after the pin.</action>
  <verify>
    <automated>mix test test/chimeway/mobile_proof_extension_test.exs test/chimeway/mobile_physical_proof_test.exs --max-failures 1 --warnings-as-errors &amp;&amp; mix verify.physical_proof_contract &amp;&amp; git diff --exit-code -- lib/chimeway/mobile_proof/extension.ex test/fixtures/alpha_twin_physical_proof/valid.json test/fixtures/alpha_twin_physical_proof/negative-corpus.json</automated>
  </verify>
  <acceptance_criteria>
    - Source: physical fixtures are new physical-v1 files and the hermetic module/fixture/corpus bytes remain unchanged.
    - Behavior: every corpus case returns its declared stable rule/path without echoing rejected values.
    - Behavior: only externally supplied observed is promotable and no automated observed-producing API exists.
    - Test command: focused Chimeway suites and the exact selected-commit CrossWake verification inside `mix verify.physical_proof_contract` all exit 0.
  </acceptance_criteria>
  <done>The complete physical-v1 corpus proves schema, source, ownership, revision, privacy, subjective-boundary, digest, and append-only guarantees while hermetic v1 remains immutable.</done>
</task>

</tasks>

## Artifacts this phase produces

- Symbol `Chimeway.MobileProof.PhysicalBundle` with `validate/2`, `publish/3`, and `bundle_digest/1`.
- Updated credential-free command implementation `Mix.Tasks.Verify.PhysicalProofContract`.
- Physical-v1 positive and ordered negative fixtures, separate from immutable hermetic-v1 inputs.
- Fixed Chimeway facts `delivery_succeeded`, `apns_provider_accepted`, and `trace_explainable` plus digest-only CrossWake references.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authority file/canonical remote -> Chimeway verifier | Every use requires fresh exact-revision source and focused-test proof. |
| CrossWake canonical bytes -> retained bundle | Only safe digests, contract metadata, opaque references, and closed outcome cross the boundary. |
| Host/device inputs -> records | Raw credential, token, identity, device, payload, endpoint, log, media, and path data are excluded recursively. |
| Observer -> visible-alert attestation | One explicit subjective state is separate from all machine facts. |
| Publisher -> evidence filesystem | Fresh bundles may be created but no destination may be replaced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-103-04 | Spoofing/Tampering | CrossWake consumer | high | mitigate | Read only the authority file, prove canonical advertisement, fresh detach exact HEAD/clean state, source/API/fixture/test markers, focused test, and source-bound validation on every verifier path. |
| T-103-05 | Information Disclosure | physical records and errors | high | mitigate | Exact allowlists, recursive D-08 scans, digest-only authority references, and rule/path-only failures exclude sensitive and canonical bytes. |
| T-103-06 | Tampering/Repudiation | bundle publication | high | mitigate | Component and overall digests, completion/run binding, exclusive atomic creation, and collision tests preserve append-only truth. |
| T-103-07 | Spoofing | visible-alert state | high | mitigate | Separate digest-bound attestation accepts only three states; no producer defaults or infers observed and other states cannot promote. |

## ASVS L1 Applicability and Automated Control Evidence

ASVS L1 V4 applies to fixed authority ownership, V5 to exact validation and attestation, V6 to SHA/component/completion binding, and V8 to recursive privacy. The focused suites plus `mix verify.physical_proof_contract` exercise every high-severity mitigation; none remains unresolved.
</threat_model>

<verification>
- `mix test test/chimeway/mobile_proof_extension_test.exs test/chimeway/mobile_physical_proof_test.exs --max-failures 1 --warnings-as-errors`
- `mix verify.physical_proof_contract`
- `mix verify.alpha_twin`
- Hermetic module and original fixture/corpus diffs remain empty.
</verification>

<success_criteria>
- One credential-free physical-v1 bundle validates and publishes once through the exact selected CrossWake authority.
- The adversarial corpus closes source, schema, ownership, revision, privacy, attestation, digest, and no-replace failures without echo.
- Hermetic Extension v1 and its committed fixtures remain unchanged.
- Output remains `release_ready_physical_pending`; no physical support is promoted.
</success_criteria>

<output>
Create `.planning/phases/103-physical-iphone-adoption-truth/103-02-SUMMARY.md` when done.
</output>
