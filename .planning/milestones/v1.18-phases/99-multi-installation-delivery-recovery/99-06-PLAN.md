---
phase: 99-multi-installation-delivery-recovery
plan: "06"
type: execute
wave: 5
depends_on: ["99-05"]
files_modified:
  - lib/chimeway/target_adapter.ex
  - lib/chimeway/delivery_targets.ex
  - lib/chimeway/dispatch/executor.ex
  - test/chimeway/dispatch/target_worker_test.exs
autonomous: true
gap_closure: true
requirements: [PUSH-02, PUSH-03, RECOV-02]
must_haves:
  truths:
    - "D-05/PUSH-02 adjacency: exact-equal target revisions still converge while distinct adjacent revisions retain independent adapter outcomes."
    - "D-05/PUSH-02 empty: an ineligible target still short-circuits before callback entry and creates no attempt; every callback return after attempt_started closes that attempt."
    - "D-05/PUSH-02 ordering: retry and ambiguity outcomes finalize the exact numbered attempt_started row without rewriting earlier target history."
    - "D-03/PUSH-02 idempotency: a known pre-handoff retryable result returns the claimed target to pending with a closed failed attempt, so a retry uses a new attempt number."
    - "D-06/PUSH-02 concurrency: only the holder of the tenant-qualified durable claim can enter the adapter and finalize its matching attempt."
    - "D-06/PUSH-03 idempotency: a duplicate execution cannot reopen or overwrite a closed attempt and cannot obtain provider authority while another claim is active."
    - "D-07/PUSH-03 concurrency: any error or unexpected return whose callback may have crossed provider handoff closes as ambiguous_handoff and blocks automatic resend."
  assumptions:
    - statement: "RECOV-02 unclassified edge probe: callback entry is the conservative possible-handoff boundary; only the adapter's explicit pre_handoff classification proves a request was not handed off."
      status: unresolved
      flagged: true
  artifacts:
    - path: "lib/chimeway/target_adapter.ex"
      provides: "Typed provider-independent distinction between known pre-handoff failure and possible-handoff ambiguity"
    - path: "lib/chimeway/delivery_targets.ex"
      provides: "Atomic target/attempt failure finalization for retryable and ambiguous outcomes"
    - path: "lib/chimeway/dispatch/executor.ex"
      provides: "Exhaustive adapter-result normalization after durable attempt start"
    - path: "test/chimeway/dispatch/target_worker_test.exs"
      provides: "Focused regression evidence for retryable, ambiguous, unexpected, and duplicate execution paths"
  key_links:
    - from: "lib/chimeway/dispatch/executor.ex"
      to: "lib/chimeway/target_adapter.ex"
      via: "run_target/2 classifies every callback return against the documented pre_handoff/possible_handoff contract"
      pattern: "pre_handoff|possible_handoff"
    - from: "lib/chimeway/dispatch/executor.ex"
      to: "lib/chimeway/delivery_targets.ex"
      via: "the exact claimed target and started attempt are finalized through one tenant-qualified transaction"
      pattern: "record_target_result|record_target_failure"
    - from: "lib/chimeway/delivery_targets.ex"
      to: "lib/chimeway/delivery_target_attempt.ex"
      via: "retryable failures close as failed while possible handoff closes as ambiguous_handoff"
      pattern: "failed|ambiguous_handoff"
  prohibitions:
    - statement: "A callback error or unexpected return must not leave a target claimed with an open attempt_started row."
      status: unverified
      flagged: true
    - statement: "A possible provider handoff must not be relabeled known-unsent, automatically retried, or presented as exactly-once delivery."
      status: unverified
      flagged: true
    - statement: "Adapter failure evidence must not retain raw exception text, provider bodies, endpoint material, credentials, or host-owned identity."
      status: unverified
      flagged: true
    - statement: "A retryable target outcome must not erase or relabel any earlier attempt in the target history."
      status: unverified
      flagged: true
---

<objective>
Close the target executor's durable-finalization gap for every adapter outcome.

Purpose: Satisfy D-05 through D-07 by separating provably pre-handoff retry from possible-handoff ambiguity after the durable claim/start boundary.
Output: A typed adapter outcome contract, atomic failure finalization, and focused regression evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
@.planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
@.planning/phases/99-multi-installation-delivery-recovery/99-04-SUMMARY.md
@.planning/phases/99-multi-installation-delivery-recovery/99-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tracer — finalize retryable and ambiguous adapter outcomes end to end</name>
  <read_first>
    - lib/chimeway/target_adapter.ex
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/dispatch/executor.ex
    - lib/chimeway/delivery_target.ex
    - lib/chimeway/delivery_target_attempt.ex
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/dispatch/oban_worker.ex
    - test/chimeway/dispatch/target_worker_test.exs
    - test/chimeway/delivery_target_test.exs
    - .planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
    - .planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
  </read_first>
  <files>lib/chimeway/target_adapter.ex, lib/chimeway/delivery_targets.ex, lib/chimeway/dispatch/executor.ex, test/chimeway/dispatch/target_worker_test.exs</files>
  <behavior>
    - Test 1: after attempt_started, `{:error, :pre_handoff, reason}` closes the attempt as `:failed`, clears the lease, returns the target to `:pending`, persists only `provider_code: "adapter_pre_handoff_failure"`, and returns `{:error, :pre_handoff_retryable}`.
    - Test 2: after attempt_started, `{:error, :possible_handoff, reason}`, legacy `{:error, reason}`, an unexpected return, and a rescued/caught callback failure each close the target and attempt as `:ambiguous_handoff` with only `provider_code: "possible_provider_handoff"`; none becomes actionable without policy-authorized redrive.
    - Test 3: a subsequent run after the explicit pre-handoff result creates attempt number 2, while a subsequent run after ambiguity is a noop and produces no second adapter call.
    - Test 4: concurrent duplicate execution still yields one adapter entry and one finalized attempt for the claim generation.
  </behavior>
  <action>First add failing tests with configurable target adapters that return each documented and legacy shape and that expose callback counts without sensitive values. Extend `Chimeway.TargetAdapter.deliver/2` documentation/types to accept `{:ok, safe_facts}`, `{:error, :pre_handoff, reason}`, and `{:error, :possible_handoff, reason}` while retaining conservative handling for legacy `{:error, reason}`. In `Executor.run_target/2`, keep `DeliveryTargets.begin_target_attempt/2` before callback entry, then normalize every returned, raised, thrown, or exited callback result: only the explicit `:pre_handoff` form is retryable; explicit possible-handoff, legacy errors, and all unexpected/failure shapes are ambiguous because provider processing cannot be disproved. Add `DeliveryTargets.record_target_failure/4` (or an equivalently explicit arity) that locks and tenant-qualifies the exact target and attempt, verifies the attempt is still `:attempt_started`, and atomically closes both rows. For `:pre_handoff_retryable`, set target `:pending`, clear claim/lease fields, set attempt `:failed` and `finished_at`, and persist exactly `%{"provider_code" =&gt; "adapter_pre_handoff_failure"}`. For `:possible_handoff`, set both rows to `:ambiguous_handoff`, clear the lease, persist exactly `%{"provider_code" =&gt; "possible_provider_handoff"}`, and recompute the parent. Return stable executor atoms `:pre_handoff_retryable` and `:ambiguous_handoff`; never persist arbitrary adapter reasons or exception text. Preserve D-08 policy-authorized linked redrive and D-09 provider-acceptance semantics unchanged.</action>
  <reversibility rating="reversible">The typed classifications extend the replaceable adapter seam and use existing target/attempt states; no schema or durable identity changes.</reversibility>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/target_adapter.ex lib/chimeway/delivery_targets.ex lib/chimeway/dispatch/executor.ex test/chimeway/dispatch/target_worker_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs test/chimeway/delivery_target_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source inspection finds an exhaustive post-`attempt_started` adapter-result branch and one atomic target/attempt finalizer for retryable versus ambiguous classifications.
    - The focused tests assert exact target status, attempt outcome, `finished_at`, lease/claim state, safe provider code, executor return atom, attempt number, and adapter-call count for both paths.
    - Legacy errors, unexpected returns, and callback failures produce ambiguity evidence and zero automatic resend; explicit pre-handoff failures alone remain retryable.
    - Focused commands exit zero with no raw adapter reason or sensitive sentinel in durable `safe_facts`.
  </acceptance_criteria>
  <done>Every adapter outcome after durable attempt start leaves closed, honest target/attempt truth and preserves duplicate-risk semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Target adapter callback → executor | Replaceable host/provider code returns untrusted shapes and may fail after provider processing. |
| Executor → durable target history | Classification controls retry authorization and must bind to the exact tenant target/attempt. |
| Adapter failure → operator evidence | Arbitrary reasons may contain provider bodies, credentials, endpoints, or host identity. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-99-06-01 | Tampering | Adapter classification | high | mitigate | Recognize only the explicit pre-handoff tuple as retryable and conservatively close every other callback failure as ambiguous. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-06-02 | Repudiation | Target/attempt finalization | high | mitigate | Atomically lock and close the exact tenant-qualified started attempt with stable outcome and finished timestamp. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-06-03 | Elevation of Privilege | Retry authorization | high | mitigate | Only explicit pre-handoff evidence returns a target to pending; ambiguity remains terminal until D-08 authorization. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-06-04 | Information Disclosure | Failure evidence | high | mitigate | Persist closed provider-code atoms only and assert raw callback reasons are absent. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-06-SC | Tampering | Package supply chain | low | accept | No package-manager operation or dependency change occurs. | `git diff --exit-code -- mix.exs mix.lock` |

ASVS L1 disposition: every high-severity ambiguity, retry-authorization, durable-truth, and evidence-leak threat is mitigated by the focused executable contract.
</threat_model>

<assumption_delta_decision>
Primary noun: the durable delivery target claim/attempt remains the provider-request authorization identity.
Decision: no-change — “additional” describes the forbidden duplicate-request side effect, not a singular-to-plural identity transition; Phase 99 already models multiple provider requests as independently numbered attempts beneath opaque target revisions.
</assumption_delta_decision>

## API Coverage Decision

The pre-plan deterministic scan returned `detected: false`; the final scan fired only on this plan's explicit no-external-service declaration. Source inspection confirms the gap work changes Chimeway's internal replaceable `TargetAdapter` behaviour and durable state machine, not a vendor surface. `COVERAGE.md` records the required reasoned no-external-API declaration; no capability matrix is applicable.

## Artifacts this phase produces

- Extended callback forms: `{:error, :pre_handoff, reason}` and `{:error, :possible_handoff, reason}` on `Chimeway.TargetAdapter.deliver/2`.
- New target failure finalizer: `DeliveryTargets.record_target_failure/4` or the implementation's explicitly named equivalent.
- New stable executor error atoms: `:pre_handoff_retryable` and `:ambiguous_handoff`.
- New closed evidence values: `adapter_pre_handoff_failure` and the existing `possible_provider_handoff`.
- New regression adapter fixtures/tests in `test/chimeway/dispatch/target_worker_test.exs`.
- New schema, migration, APNs adapter, token, endpoint, credential, payload, request-header, or device-receipt artifacts: none.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Independent target truth and recovery beneath one logical decision | 99-06, 99-07 | COVERED | Executor closes every outcome; recovery consumes accurate results. |
| REQ | PUSH-01 | Opaque tenant-scoped resolver | 99-01, 99-03, 99-07 validation | COVERED | Existing behavior retained; no resolver changes. |
| REQ | PUSH-02 | Independent target claim/attempt/retry/trace history | 99-06 | COVERED | Fixes the failed adapter finalization path. |
| REQ | PUSH-03 | No duplicate target or unexplained request | 99-06, 99-07 | COVERED | Retry authority and recovery continuations are explicit. |
| REQ | PUSH-04 | Honest no-target and mixed aggregate | 99-03, 99-07 validation | COVERED | Existing behavior retained and revalidated. |
| REQ | RECOV-01 | Bounded tenant recovery with evidence | 99-07 | COVERED | Independent bounded streams and worker summary. |
| REQ | RECOV-02 | Pre-I/O evidence and ambiguous possible handoff | 99-06, 99-07 | COVERED | Conservative callback boundary and stale recovery remain explicit. |
| CONTEXT | D-01, D-02, D-03, D-04 | Canonical parent, opaque unique children, honest aggregate | 99-01, 99-03, 99-07 | COVERED | No identity or aggregate reduction. |
| CONTEXT | D-05, D-06, D-07, D-08, D-09 | Target state machine and handoff truth | 99-06 | COVERED | Exact retryable/ambiguous finalization closes the verified gap. |
| CONTEXT | D-10, D-11, D-12 | Bounded tenant recovery and safe evidence | 99-07 | COVERED | Dedicated recovery repair plan. |
| RESEARCH | — | Durable DB authority, append-only history, closed evidence | 99-06, 99-07 | COVERED | Uses existing Ecto/PostgreSQL/SafeEvidence patterns without dependencies. |
| CONTEXT | Deferred Ideas | APNs mapping/request construction, CrossWake, twin, physical proof | — | EXCLUDED | Remain in Phases 100–103. |

The gap plan set preserves all 15 fallback edge items: seven classified PUSH-02/PUSH-03 items above, six PUSH-01/PUSH-04 truths in 99-07, and two unclassified RECOV items as one flagged assumption in each plan. Prohibition recall surfaced eight bespoke identity, privacy, transparency, tenant, resource-bound, and ambiguity constraints; four descriptor-less items are authored here and four in 99-07. Generic OWASP/compliance candidates remain security-review breadcrumbs rather than duplicate prohibitions.

<verification>
Run the focused target-worker and delivery-target suites. Plan 99-07 then runs the combined repaired-path suite and refreshes the validation map.
</verification>

<success_criteria>
Known-unsent retryable failures and possible-handoff ambiguity leave distinct closed durable evidence, with no open started attempt and no unexplained resend.
</success_criteria>

<output>
Create `.planning/phases/99-multi-installation-delivery-recovery/99-06-SUMMARY.md` when done.
</output>
