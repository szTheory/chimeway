---
phase: 99-multi-installation-delivery-recovery
plan: "11"
type: execute
wave: 8
depends_on: ["99-10"]
files_modified:
  - lib/chimeway/dispatch/oban.ex
  - lib/chimeway/delivery_targets.ex
  - test/chimeway/dispatch/oban_test.exs
  - test/chimeway/orchestration/target_recovery_test.exs
autonomous: true
gap_closure: true
requirements: [PUSH-01, PUSH-02, PUSH-03, PUSH-04, RECOV-01, RECOV-02]
must_haves:
  truths:
    - "D-04/PUSH-04 empty: public Chimeway.Dispatch.Oban.dispatch_delivery/2 atomically recomputes a tenant-qualified pending/ready push parent when its actionable target snapshot is empty, returning success only after the parent is suppressed with reason no_eligible_targets."
    - "D-05/PUSH-02 concurrency: stale-attempt closeout acquires locks in the same tenant-qualified parent -> target -> attempt order as claim and finalization before changing child or aggregate truth."
    - "D-11/PUSH-03 concurrency: a deterministic closeout-versus-finalizer race produces exactly one terminal target/attempt outcome, never deadlocks, never overwrites the winner, and never appends unexplained evidence."
    - "D-10/RECOV-01: retryable database transaction failures during one stale closeout become a closed non-disclosing result and do not abort the bounded recovery pass over later tenant-owned work."
    - "D-07/RECOV-02: when stale closeout wins, the exact started attempt and target remain ambiguous_handoff with possible_provider_handoff evidence and a late finalizer is a no-op."
  artifacts:
    - path: "lib/chimeway/dispatch/oban.ex"
      provides: "Atomic empty push-snapshot parent recomputation through the public Oban delivery dispatcher"
    - path: "lib/chimeway/delivery_targets.ex"
      provides: "Tenant-qualified parent-first stale closeout and closed retryable transaction-error result"
    - path: "test/chimeway/dispatch/oban_test.exs"
      provides: "Public Oban no-target suppression and wrong-tenant regression evidence"
    - path: "test/chimeway/orchestration/target_recovery_test.exs"
      provides: "Deterministic closeout/finalizer ordering and recovery-pass continuation evidence"
  key_links:
    - from: "lib/chimeway/dispatch/oban.ex"
      to: "lib/chimeway/delivery_targets.ex"
      via: "empty actionable_targets snapshot invokes tenant-qualified recompute_delivery before dispatch_delivery reports success"
      pattern: "actionable_targets|recompute_delivery|no_eligible_targets"
    - from: "lib/chimeway/delivery_targets.ex"
      to: "lib/chimeway/delivery_target_attempt.ex"
      via: "one parent-first transaction locks the exact tenant-owned target and started attempt before ambiguous closeout and parent aggregate persistence"
      pattern: "FOR UPDATE|attempt_started|ambiguous_handoff"
    - from: "lib/chimeway/target_recovery.ex"
      to: "lib/chimeway/delivery_targets.ex"
      via: "close_stale_attempts consumes a closed retryable transaction-error result and continues reducing the bounded stale-ID page"
      pattern: "close_stale_started_attempt"
  flagged_assumptions:
    - "PUSH-01 adjacency remains unresolved by the deterministic probe and is unchanged by this closure."
    - "PUSH-01 empty remains unresolved by the deterministic probe and is unchanged by this closure."
    - "PUSH-01 ordering remains unresolved by the deterministic probe and is unchanged by this closure."
    - "PUSH-02 adjacency remains unresolved by the deterministic probe and is unchanged by this closure."
    - "PUSH-02 empty remains unresolved by the deterministic probe and is unchanged by this closure."
    - "PUSH-02 ordering remains unresolved by the deterministic probe and is unchanged by this closure."
    - "PUSH-02 idempotency remains unresolved by the deterministic probe and is unchanged by this closure."
    - "PUSH-03 idempotency remains unresolved by the deterministic probe and is unchanged by this closure."
    - "PUSH-04 adjacency remains unresolved by the deterministic probe and is unchanged by this closure."
    - "PUSH-04 ordering remains unresolved by the deterministic probe and is unchanged by this closure."
    - "RECOV-01 was unclassified by the deterministic probe; this plan verifies only transaction-error continuation for the reported gap."
    - "RECOV-02 was unclassified by the deterministic probe; this plan verifies only closeout-versus-finalizer ambiguity preservation for the reported gap."
  prohibitions:
    - statement: "Empty asynchronous fan-out must not leave an explainability-free pending parent or invent provider work."
      status: unverified
      flagged: true
    - statement: "Recovery concurrency must not weaken tenant ownership, erase possible-handoff ambiguity, or disclose whether foreign lifecycle rows exist."
      status: unverified
      flagged: true
    - statement: "A transaction retry signal must not be persisted or projected with raw database, tenant, target, attempt, or provider details."
      status: unverified
      flagged: true
---

<objective>
Close the final asynchronous empty-target and stale-recovery concurrency blockers through supported public paths.

Purpose: Ensure every push parent reaches explainable truth and every stale closeout follows the same tenant-safe lock hierarchy as ordinary execution without aborting bounded recovery on a retryable database conflict.
Output: Atomic empty-snapshot suppression, parent-first stale closeout, closed retryable transaction handling, and executable public-path/concurrency regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
@.planning/phases/99-multi-installation-delivery-recovery/99-RESEARCH.md
@.planning/phases/99-multi-installation-delivery-recovery/99-PATTERNS.md
@.planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
@.planning/phases/99-multi-installation-delivery-recovery/99-10-SUMMARY.md

<interfaces>
Existing contracts to refine:
- `Chimeway.Dispatch.Oban.dispatch_delivery/2` delegates to private `enqueue_delivery/1` and normalizes `{:ok, jobs}` to `{:ok, delivery}`.
- Push `enqueue_delivery/1` snapshots `DeliveryTargets.actionable_targets/1`; an empty snapshot currently reduces to `{:ok, []}`.
- `DeliveryTargets.recompute_delivery/2` locks and tenant-qualifies the parent, derives target aggregate metadata, and assigns suppression reason `"no_eligible_targets"` when target count is zero.
- `DeliveryTargets.close_stale_started_attempt/2` returns `{:ok, %{target: target, attempt: attempt}}` or `{:noop, :not_found}` but currently locks target/attempt before nested parent recomputation.
- `DeliveryTargets.record_target_result/4` and `begin_target_attempt/2` establish parent -> target -> attempt as the normal execution lock order.
- `TargetRecovery.recover_tenant/2` reduces a bounded stale target-ID page before discovering actionable targets; one closeout failure must not abort later IDs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Suppress an empty push snapshot through public Oban dispatch</name>
  <read_first>
    - lib/chimeway/dispatch/oban.ex
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/delivery.ex
    - test/chimeway/dispatch/oban_test.exs
    - test/chimeway/dispatch/oban_transactional_test.exs
    - test/chimeway/delivery_target_test.exs
    - .planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
  </read_first>
  <files>lib/chimeway/dispatch/oban.ex, test/chimeway/dispatch/oban_test.exs</files>
  <behavior>
    - Test 1: `Oban.dispatch_delivery(delivery, [])` on a same-tenant pending/ready push delivery with zero actionable targets returns `{:ok, suppressed_delivery}` whose status is suppressed, suppression reason is `no_eligible_targets`, and target aggregate count is zero.
    - Test 2: the same public call enqueues no delivery-ID or target-ID Oban job and a duplicate call preserves the same terminal parent evidence.
    - Test 3: a pending/ready push delivery with actionable targets still enqueues exactly one target-qualified job per snapshot target and remains pending for worker execution.
    - Test 4: stale or tenant-incoherent input cannot mutate a foreign parent and returns the existing non-authorizing result class without lifecycle disclosure.
  </behavior>
  <action>Write the public-dispatch regressions first. In the push clause of private `enqueue_delivery/1`, snapshot `DeliveryTargets.actionable_targets/1` once. When that ordered snapshot is empty, call `DeliveryTargets.recompute_delivery(delivery, delivery.tenant_id)` and map only `{:ok, %Delivery{status: :suppressed, suppression_reason: "no_eligible_targets"}}` to the existing enqueue success shape consumed by `normalize_dispatch_delivery_result/2`; propagate closed errors instead of reporting false success. Preserve `{:ok, jobs}` for non-empty snapshots, exact target-ID plus tenant-ID job args, and duplicate-safe Oban behavior. Ensure `dispatch_delivery/2` returns the recomputed authoritative parent rather than its stale caller struct on the empty branch, either by extending the internal result shape and its normalizer or by another equally explicit closed shape. D-01 keeps one canonical parent; D-04 requires the exact `no_eligible_targets` explanation; D-10 requires the tenant argument on recomputation; D-12 forbids raw target or database error details. Do not enqueue a parent-level push job, synthesize a child target, or alter non-push/deferred dispatch behavior.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/dispatch/oban.ex test/chimeway/dispatch/oban_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/oban_transactional_test.exs test/chimeway/delivery_target_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source inspection shows the empty result of one actionable-target snapshot flows through `DeliveryTargets.recompute_delivery/2` before `dispatch_delivery/2` can return success.
    - The public Oban dispatch assertion reloads or receives the authoritative parent and proves `status: :suppressed`, `suppression_reason: "no_eligible_targets"`, and target aggregate count zero.
    - Oban test assertions prove zero jobs for the empty snapshot and one tenant-qualified target job per actionable target for the non-empty control.
    - Duplicate empty dispatch preserves one canonical parent and the same stable suppression evidence.
    - Foreign/stale input cannot mutate a tenant-incoherent delivery and emits no lifecycle identifier or raw database detail.
  </acceptance_criteria>
  <reversibility rating="reversible">The change is confined to an internal dispatch branch and regression tests; reverting restores the prior result mapping without a schema or public-data migration.</reversibility>
  <done>The supported Oban delivery entry point cannot report successful empty fan-out while leaving its tenant-qualified push parent pending.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce parent-first stale closeout and continue after retryable transaction conflicts</name>
  <read_first>
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/target_recovery.ex
    - lib/chimeway/delivery.ex
    - lib/chimeway/delivery_target.ex
    - lib/chimeway/delivery_target_attempt.ex
    - test/chimeway/orchestration/target_recovery_test.exs
    - test/chimeway/dispatch/target_worker_test.exs
    - test/chimeway/tenant_scope_contract_test.exs
    - .planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
  </read_first>
  <files>lib/chimeway/delivery_targets.ex, test/chimeway/orchestration/target_recovery_test.exs</files>
  <behavior>
    - Test 1: stale closeout locks the tenant-qualified parent, then its exact claimed target, then the latest exact `attempt_started` attempt and commits target/attempt `ambiguous_handoff` plus parent recomputation in that one transaction.
    - Test 2: deterministic concurrent stale closeout and `record_target_result/4` complete without deadlock; finalizer-first yields provider_accepted plus closeout noop, while closeout-first yields ambiguous_handoff plus finalizer noop.
    - Test 3: wrong-tenant, absent, non-claimed, non-stale, or already-finalized rows all return `{:noop, :not_found}` and mutate no parent, target, or attempt.
    - Test 4: an injected/forced retryable transaction conflict for the first stale ID returns `{:error, :retryable_transaction}` without raw exception data, and `TargetRecovery.recover_tenant/2` continues to close the next tenant-owned stale ID in the same bounded pass.
  </behavior>
  <action>Write deterministic race and recovery-continuation regressions first, using Sandbox-authorized tasks plus explicit lock/barrier messages so both winner orders are exercised without timing sleeps. Refactor `close_stale_started_attempt/2` into one transaction whose queries acquire the tenant-qualified `Delivery` parent first, the exact `DeliveryTarget` second, and the exact latest `DeliveryTargetAttempt` third, all with `FOR UPDATE`; derive the parent through target ID plus matching tenant predicates before any child mutation. Require parent/target/attempt identity coherence, target `:claimed`, expired lease, and attempt `:attempt_started`. Update target and attempt to `:ambiguous_handoff` with the closed provider code `possible_provider_handoff`, then recompute the already-locked parent aggregate inside the same transaction without starting a nested transaction or re-locking in the inverse order; extract a private locked aggregate helper shared with public `recompute_delivery/2` if needed. Normalize only PostgreSQL transaction conditions safe to retry (deadlock detected, serialization failure, or lock-not-available/timeout as represented by the current Ecto/Postgrex stack) to `{:error, :retryable_transaction}`; preserve `{:noop, :not_found}` for stale/ineligible/foreign state and return other closed errors without embedding exception messages, SQL, row IDs, or tenant values. Prove `TargetRecovery.recover_tenant/2` consumes the retryable result as a skipped/continuable closeout and processes the remaining bounded IDs; if the existing wildcard reducer already provides that contract, leave `target_recovery.ex` unchanged and pin it with the regression. D-05/D-06 preserve exact target/attempt truth, D-07 preserves ambiguity, D-10 repeats tenant scope, D-11 requires atomic convergence, and D-12 closes error evidence.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/delivery_targets.ex test/chimeway/orchestration/target_recovery_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs test/chimeway/dispatch/target_worker_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source inspection shows one parent -> target -> attempt lock sequence with explicit tenant predicates and no target/attempt mutation before the parent lock.
    - Stale closeout performs aggregate persistence in the same transaction without calling a nested public recomputation that reacquires the parent after child locks.
    - Barrier-controlled tests prove both closeout-first and finalizer-first outcomes, bounded completion, one terminal attempt row, one terminal target state, and no losing-writer overwrite.
    - Retryable PostgreSQL transaction failures produce exactly `{:error, :retryable_transaction}`; returned or persisted evidence contains no exception text, SQL, tenant value, target ID, attempt ID, or provider material.
    - A bounded recovery pass continues after the first retryable closeout conflict and durably closes a later same-tenant stale attempt; foreign-tenant rows remain unchanged and undisclosed.
    - Existing target-worker ambiguity, policy-authorized redrive, and tenant-scope suites remain green.
  </acceptance_criteria>
  <reversibility rating="reversible">The lock hierarchy and error normalization change existing internal transactions without schema, persisted-vocabulary, or external API changes.</reversibility>
  <done>Stale recovery follows the canonical tenant-qualified lock order, converges deterministically with finalization, and a retryable transaction conflict cannot abort the remaining recovery page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public Oban dispatch -> durable parent/target state | A caller-provided delivery struct selects tenant-owned work and must not authorize cross-tenant mutation or unexplained success. |
| Recovery target ID -> parent/target/attempt transaction | A stale durable ID crosses into mutation authority and must be re-authorized under one tenant predicate and lock hierarchy. |
| PostgreSQL transaction failure -> recovery summary | Internal concurrency diagnostics cross into bounded operator evidence and must remain closed and non-disclosing. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-99-11-01 | Elevation of Privilege | Empty-snapshot parent recomputation | high | mitigate | Recompute only the exact parent under its persisted tenant identity and prove foreign/stale input cannot mutate another tenant. | `env MIX_ENV=test mix test test/chimeway/dispatch/oban_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` |
| T-99-11-02 | Tampering | Stale closeout locks | high | mitigate | Lock tenant-qualified parent -> exact target -> exact started attempt before any mutation and commit aggregate truth in the same transaction. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-11-03 | Repudiation | Empty asynchronous fan-out | high | mitigate | Persist suppressed/no_eligible_targets before reporting public dispatch success and prove zero jobs were created. | `env MIX_ENV=test mix test test/chimeway/dispatch/oban_test.exs --warnings-as-errors` |
| T-99-11-04 | Denial of Service | Closeout/finalizer race | high | mitigate | Use one global lock hierarchy and normalize retryable transaction conflicts so one target cannot abort later bounded recovery items. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs --warnings-as-errors` |
| T-99-11-05 | Information Disclosure | Transaction error result | high | mitigate | Return a fixed retryable atom and assert exception, SQL, tenant, target, attempt, and provider sentinels are absent. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` |
| T-99-11-SC | Tampering | Package supply chain | low | accept | No package-manager operation or dependency change occurs. | `git diff --exit-code -- mix.exs mix.lock` |
</threat_model>

<assumption_delta_decision>
Primary generalized noun: tenant-qualified lifecycle transaction.
Decision: promote — parent-first ordering becomes the shared aggregate-changing transaction invariant for stale closeout as well as normal claim/finalization; this adds no new domain entity or public contract.
</assumption_delta_decision>

## API Coverage Decision

Preserve the existing `COVERAGE.md` declaration. This closure changes only internal Ecto/Oban lifecycle behavior and adds no APNs endpoint, SDK method, authentication surface, package, or vendor API.

## Artifacts this phase produces

- New symbols: None.
- New paths: None.
- Modified existing private push enqueue/normalization flow in `Chimeway.Dispatch.Oban`.
- Modified existing `Chimeway.DeliveryTargets.close_stale_started_attempt/2` and aggregate recomputation internals.
- Expanded public Oban dispatch and target recovery regression tests.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Gap plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | All eligible opaque installations retain independent tenant-safe target truth and recovery | 99-11 | COVERED | Closes the two remaining supported-path blockers; executed plans 99-01 through 99-10 retain all prior scope. |
| REQ | PUSH-01 | Opaque tenant-scoped resolver revisions | 99-11 | COVERED | Closure preserves the executed resolver contract and proves empty asynchronous consumption does not invent target material. |
| REQ | PUSH-02 | Independent target lifecycle and history | 99-11 | COVERED | Parent-first closeout preserves exact target/attempt truth under concurrency. |
| REQ | PUSH-03 | Duplicate-safe planning, execution, and recovery | 99-11 | COVERED | Losing finalizer/closeout noops and retryable conflict continuation prevent extra requests or evidence. |
| REQ | PUSH-04 | Stable no-target suppression and honest aggregate | 99-11 | COVERED | Public Oban empty snapshot persists suppressed/no_eligible_targets before success. |
| REQ | RECOV-01 | Bounded tenant-scoped recovery | 99-11 | COVERED | One retryable closeout conflict does not abort later IDs in the bounded pass. |
| REQ | RECOV-02 | Pre-I/O evidence and explicit ambiguous handoff | 99-11 | COVERED | Deterministic races preserve either conclusive finalization or ambiguous closeout without overwrite. |
| RESEARCH | — | Database locks/transactions are durable concurrency authority; Oban uniqueness is defense-in-depth | 99-11 | COVERED | Uses the existing Ecto/PostgreSQL stack and exact durable rows; no install task exists. |
| CONTEXT | D-01–D-04 | Canonical parent, opaque children, convergent identity, stable empty aggregate | 99-11 | COVERED | Empty asynchronous fan-out recomputes the existing parent and creates no child. |
| CONTEXT | D-05–D-09 | Independent target state, pre-I/O history, ambiguity, explicit redrive, handoff-only language | 99-11 | COVERED | Closeout/finalizer races retain exact attempt truth and never claim device delivery. |
| CONTEXT | D-10–D-12 | Tenant-bounded recovery, atomic convergence, safe evidence | 99-11 | COVERED | Every lock is tenant-qualified and transaction failures collapse to a non-disclosing retryable atom. |
| CONTEXT | Deferred Ideas | APNs mapping, CrossWake registration/open, twin, physical proof | — | EXCLUDED | These remain owned by Phases 100–103. |

All verification gaps map to a task, must-have truth, acceptance criterion, and executable command. Twelve probe items unrelated or unclassified for these two blockers remain explicit under `must_haves.flagged_assumptions`; none is silently dismissed. Three values/safety prohibitions are descriptor-less and flagged-unverified as required by the spec-less fallback.

<verification>
Run both task commands, then run `mix compile --warnings-as-errors`. Accept only durable state, job-count, lock-order, bounded-completion, tenant-isolation, and closed-error evidence; no conversational UAT or human checkpoint applies.
</verification>

<success_criteria>
The public Oban empty-target path durably suppresses its canonical parent with `no_eligible_targets`, and stale recovery uses a tenant-qualified parent -> target -> attempt transaction that converges with finalization and continues after retryable database conflicts.
</success_criteria>

<output>
Create `.planning/phases/99-multi-installation-delivery-recovery/99-11-SUMMARY.md` when done.
</output>
