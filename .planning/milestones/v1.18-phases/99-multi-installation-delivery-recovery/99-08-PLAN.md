---
phase: 99-multi-installation-delivery-recovery
plan: "08"
type: execute
wave: 7
depends_on: ["99-07"]
files_modified:
  - lib/chimeway/traces.ex
  - lib/chimeway/traces/explanation.ex
  - test/chimeway/traces_target_test.exs
autonomous: true
gap_closure: true
requirements: [PUSH-02]
must_haves:
  truths:
    - "D-05/PUSH-02 adjacency: recipient, correlation, and delivery-explanation projections place every durable target and its ordered attempts beneath the same logical delivery as the full event trace."
    - "D-02/D-12: every common operator loader repeats the resolved tenant predicate for deliveries, targets, and target attempts and exposes only the existing closed target projection."
    - "PUSH-02 ordering assumption: targets sort by opaque binding revision plus durable ID, and attempts sort by attempt number plus durable ID in all four trace shapes."
    - "PUSH-02 concurrency assumption: each projection returns one internally consistent committed target history and never substitutes an unloaded association with a misleading empty history."
  artifacts:
    - path: "lib/chimeway/traces.ex"
      provides: "Shared tenant-qualified target/attempt preload contract for every operator trace loader"
    - path: "lib/chimeway/traces/explanation.ex"
      provides: "Delivery explanation fields for closed target aggregate and independent target histories"
    - path: "test/chimeway/traces_target_test.exs"
      provides: "Multi-target recipient, correlation, explanation, ordering, tenant, and disclosure regressions"
  key_links:
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "all four loaders supply tenant-qualified loaded targets and attempts to the existing trace_delivery projection"
      pattern: "targets.*attempts|trace_delivery"
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/traces/explanation.ex"
      via: "explain_delivery returns the same closed target histories and aggregate available in delivery traces"
      pattern: "target_aggregate|targets"
  prohibitions:
    - "Operator projections must not expose raw endpoint, token, credential, provider-body, or uncontrolled safe_facts values."
    - "A missing preload must not be rendered as authoritative evidence that a delivery has no targets."
---

<objective>
Close the hollow operator-projection gap by loading and projecting independent target history everywhere a delivery is explained.

Purpose: Make the durable per-installation truth from D-05 visible through every common operator path without weakening tenant or Phase 98 evidence boundaries.
Output: One reusable tenant-qualified preload shape, target-aware delivery explanations, and executable multi-target projection regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
@.planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
@.planning/phases/99-multi-installation-delivery-recovery/99-03-SUMMARY.md

<interfaces>
Existing contracts to preserve:
- `Chimeway.Traces.get_trace/2`, `find_traces_for_recipient/2`, `find_traces_by_correlation_id/2`, and `explain_delivery/2` all resolve tenant scope before querying.
- `Chimeway.SafeEvidence.trace_delivery/1` already projects `target_aggregate`, stable-sorted `targets`, and ordered target `attempts`; it intentionally maps an unloaded association to an empty list for legacy callers.
- `%Chimeway.Traces.Explanation{}` is the public structured result of `explain_delivery/2` and currently lacks target fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project multi-target history through every common operator trace</name>
  <read_first>
    - lib/chimeway/traces.ex
    - lib/chimeway/traces/explanation.ex
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/delivery.ex
    - lib/chimeway/delivery_target.ex
    - lib/chimeway/delivery_target_attempt.ex
    - test/chimeway/traces_target_test.exs
    - test/chimeway/traces_test.exs
    - .planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
  </read_first>
  <files>lib/chimeway/traces.ex, lib/chimeway/traces/explanation.ex, test/chimeway/traces_target_test.exs</files>
  <behavior>
    - Test 1: recipient lookup returns two same-tenant targets in deterministic order and each target retains its own ordered attempt history.
    - Test 2: correlation lookup returns that identical closed target history and excludes a foreign-tenant target attempt.
    - Test 3: delivery explanation contains the closed target aggregate and both independent target histories rather than an empty list.
    - Test 4: all three projections omit raw token/endpoint/provider-body sentinels and wrong-tenant calls remain empty or not found.
  </behavior>
  <action>Write the three missing-path regressions first using one event, one push delivery, two opaque target revisions, independently numbered attempts, and a deliberately foreign-tenant attempt. D-02: preserve opaque tenant-scoped binding revisions and exclude raw resolver material. D-05: expose the child target as the independent history beneath its logical parent. D-12: route all operator output through the Phase 98 closed evidence vocabulary. In `Traces`, factor the target and target-attempt queries already proven by `get_trace/2` into a private preload builder that always receives the resolved tenant; use deterministic `{binding_revision_ref, id}` and `{attempt_number, id}` order. Apply that same nested preload to `find_traces_for_recipient/2`, `find_traces_by_correlation_id/2`, and `explain_delivery/2`, repeating tenant qualification at notification, delivery, target, and attempt boundaries. Extend `Explanation` with `target_aggregate` and `targets`, and populate them from the existing `SafeEvidence.trace_delivery/1` result so there is one closed projection vocabulary rather than a parallel mapper. Preserve every existing field and return shape, all non-disclosing wrong-tenant behavior, and the Phase 98 redaction boundary. Do not broaden accepted safe facts or add APNs/provider-specific fields.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/traces.ex lib/chimeway/traces/explanation.ex test/chimeway/traces_target_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/traces_target_test.exs test/chimeway/traces_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Focused tests call recipient, correlation, and explanation APIs and assert two ordered targets with independent ordered attempts in each result.
    - Every target/attempt preload is explicitly filtered by the tenant resolved by the public function.
    - `Explanation` exposes closed `target_aggregate` and `targets` fields while all pre-existing explanation fields and tests remain compatible.
    - Wrong-tenant lookups disclose neither lifecycle IDs nor whether target history exists.
    - Exact sentinel assertions prove no raw token, endpoint, credential, provider body, or uncontrolled attempt fact reaches any projection.
  </acceptance_criteria>
  <done>Every common operator trace and explanation path exposes the same tenant-safe independent target history as the full event trace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public trace arguments → Ecto loaders | Recipient, correlation, delivery, and tenant inputs select durable lifecycle evidence. |
| Target rows → operator DTOs | Opaque child history crosses from storage through the Phase 98 closed projection boundary. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-99-08-01 | Elevation of Privilege | Nested trace preloads | high | mitigate | Repeat the resolved tenant predicate at delivery, target, and target-attempt queries and preserve non-disclosing results. | `env MIX_ENV=test mix test test/chimeway/traces_target_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` |
| T-99-08-02 | Tampering | Target-history ordering | medium | mitigate | Use stable secondary durable-ID ordering and assert independent attempt ownership. | `env MIX_ENV=test mix test test/chimeway/traces_target_test.exs --warnings-as-errors` |
| T-99-08-03 | Information Disclosure | Explanation and trace DTOs | high | mitigate | Reuse SafeEvidence.trace_delivery/1 and reject raw sentinel leakage in all projections. | `env MIX_ENV=test mix test test/chimeway/traces_target_test.exs --warnings-as-errors` |
| T-99-08-04 | Repudiation | Unloaded association fallback | high | mitigate | Ensure every operator loader supplies real target history before closed projection, preventing false empty evidence. | `env MIX_ENV=test mix test test/chimeway/traces_target_test.exs --warnings-as-errors` |
| T-99-08-SC | Tampering | Package supply chain | low | accept | No package-manager operation or dependency change occurs. | `git diff --exit-code -- mix.exs mix.lock` |
</threat_model>

<assumption_delta_decision>
Primary noun: the durable tenant-scoped DeliveryTarget remains the independent installation truth beneath one logical Delivery.
Decision: promote — the gap proves operator-facing plural target history is part of the durable target contract, so all established delivery projections must carry the same children; this does not create a competing delivery identity or new external API integration.
</assumption_delta_decision>

## API Coverage Decision

Phase 99 `COVERAGE.md` declares no external API integration. This plan extends an already-established Chimeway operator projection with existing target DTO data and adds no vendor endpoint, SDK, capability, or authentication surface.

## Artifacts this phase produces

- New `Chimeway.Traces.Explanation` fields: `target_aggregate` and `targets`.
- New private shared target-preload helper(s) in `Chimeway.Traces` for tenant-qualified, deterministic target history.
- Expanded `test/chimeway/traces_target_test.exs` cases for recipient, correlation, and explanation projections.
- New files, schemas, migrations, flags, external API fields, or provider-specific symbols: none.

<verification>
Run the focused trace, legacy trace, and tenant-scope suites with warnings treated as errors; accept only exact multi-target histories and zero sensitive sentinel leakage.
</verification>

<success_criteria>
Recipient, correlation, full-event, and delivery-explanation views all expose the same ordered independent target truth under the correct tenant.
</success_criteria>

<output>
Create `.planning/phases/99-multi-installation-delivery-recovery/99-08-SUMMARY.md` when done.
</output>
