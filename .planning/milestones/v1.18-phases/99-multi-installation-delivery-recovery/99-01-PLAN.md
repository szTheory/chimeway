---
phase: 99-multi-installation-delivery-recovery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260819000001_create_chimeway_delivery_targets.exs
  - priv/chimeway_migrations/035_create_chimeway_delivery_targets.exs
  - lib/chimeway/target_resolver.ex
  - lib/chimeway/delivery_target.ex
  - lib/chimeway/delivery_target_attempt.ex
  - lib/chimeway/target_adapter.ex
  - lib/chimeway/delivery_targets.ex
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/dispatch/executor.ex
  - lib/chimeway/dispatch/sync.ex
  - lib/chimeway/safe_evidence.ex
  - lib/chimeway/traces.ex
  - test/chimeway/delivery_target_test.exs
autonomous: true
requirements: [PUSH-01, PUSH-02, PUSH-04, RECOV-02]
must_haves:
  truths:
    - "D-01/D-03: one push notification/channel retains one canonical Delivery, and one exact duplicate binding revision converges on one durable DeliveryTarget child."
    - "D-02/PUSH-01 adjacency: exact duplicate opaque revisions merge through the target unique key, while two distinct opaque revisions remain distinct children."
    - "D-02/PUSH-01 empty: {:ok, []} is valid and suppresses the parent with no_eligible_targets; a nil result or nil element is rejected as invalid_target_resolution."
    - "D-02/PUSH-01 ordering: normalized resolver results and projected targets are stable-sorted by binding_revision_ref, including equal input entries."
    - "D-05/D-06: the successful target path durably claims a target and inserts its numbered attempt_started row before the replaceable target adapter is invoked."
    - "D-04/D-09: a conclusive provider acceptance makes the one-target parent succeed as provider-handoff evidence only, without a device receipt, display, open, seen, read, or engagement claim."
    - "D-07/RECOV-02: a started attempt is sufficient durable evidence to classify a later possible-handoff crash as ambiguous rather than known-unsent."
  artifacts:
    - path: "priv/repo/migrations/20260819000001_create_chimeway_delivery_targets.exs"
      provides: "Repository migration for target identity and ordered target attempts"
    - path: "priv/chimeway_migrations/035_create_chimeway_delivery_targets.exs"
      provides: "Prefix-aware copied migration source"
    - path: "lib/chimeway/target_resolver.ex"
      provides: "Public opaque tenant-explicit resolver and BindingRevision contract"
    - path: "lib/chimeway/delivery_targets.ex"
      provides: "Idempotent target persistence, pre-I/O claim/start, result recording, and parent aggregation"
    - path: "lib/chimeway/target_adapter.ex"
      provides: "Provider-independent target handoff behaviour and TargetEnvelope"
    - path: "test/chimeway/delivery_target_test.exs"
      provides: "Executable one-target trigger-to-safe-trace tracer"
  key_links:
    - from: "lib/chimeway/delivery_planning.ex"
      to: "lib/chimeway/delivery_targets.ex"
      via: "push planning resolves, normalizes, stable-sorts, and inserts child targets beneath the canonical Delivery"
      pattern: "TargetResolver|DeliveryTargets"
    - from: "lib/chimeway/dispatch/sync.ex"
      to: "lib/chimeway/dispatch/executor.ex"
      via: "push dispatch enumerates durable targets and executes each only through the target attempt seam"
      pattern: "run_target"
    - from: "lib/chimeway/dispatch/executor.ex"
      to: "lib/chimeway/delivery_target_attempt.ex"
      via: "begin_target_attempt completes before TargetAdapter.deliver/2"
      pattern: "begin_target_attempt"
  prohibitions:
    - statement: "Installation fan-out must not create a competing top-level Delivery identity or replace the canonical logical delivery."
      status: unverified
      flagged: true
    - statement: "Resolver or target evidence must not persist raw tokens, endpoints, credentials, provider bodies, or uncontrolled host payloads."
      status: unverified
      flagged: true
    - statement: "Provider acceptance must not be represented as device receipt, display, app handling, protected open, inbox seen/read, or engagement."
      status: unverified
      flagged: true
---

<objective>
Prove the Phase 99 architecture end to end with one real opaque installation target beneath one canonical push delivery.

Purpose: Establish the durable identity, privacy, pre-I/O evidence, replaceable adapter, aggregate, and trace seams before expanding races, multi-target outcomes, and recovery.
Output: Additive target tables, public resolver/adapter contracts, a production target lifecycle service, one successful synchronous path, and a runnable tracer test.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
@.planning/phases/99-multi-installation-delivery-recovery/99-RESEARCH.md
@.planning/phases/99-multi-installation-delivery-recovery/99-PATTERNS.md
@.planning/phases/99-multi-installation-delivery-recovery/99-VALIDATION.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-15-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Leading production tracer — deliver one canonical push decision through one opaque target to a safe trace</name>
  <read_first>
    - priv/repo/migrations/20260813000000_privacy_safe_delivery_evidence.exs
    - priv/chimeway_migrations/004_create_chimeway_delivery_attempts.exs
    - priv/chimeway_migrations/034_privacy_safe_delivery_evidence.exs
    - lib/chimeway/delivery.ex
    - lib/chimeway/delivery_attempt.ex
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/deliveries.ex
    - lib/chimeway/dispatch/executor.ex
    - lib/chimeway/dispatch/sync.ex
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/traces.ex
    - test/chimeway/orchestration/delivery_planning_test.exs
    - test/chimeway/integration/delivery_lifecycle_test.exs
    - test/chimeway/traces_test.exs
  </read_first>
  <files>priv/repo/migrations/20260819000001_create_chimeway_delivery_targets.exs, lib/chimeway/target_resolver.ex, lib/chimeway/delivery_target.ex, lib/chimeway/delivery_target_attempt.ex, lib/chimeway/target_adapter.ex, lib/chimeway/delivery_targets.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/dispatch/executor.ex, lib/chimeway/traces.ex, test/chimeway/delivery_target_test.exs</files>
  <behavior>
    - Test 1: a configured resolver returns one `%Chimeway.TargetResolver.BindingRevision{tenant_id: tenant, binding_revision_ref: ref}`, push planning retains the existing `{notification_id, "push"}` Delivery identity, and exactly one target row is stored.
    - Test 2: the fake `Chimeway.TargetAdapter` observes an existing target attempt whose outcome is `attempt_started` before its callback returns `{:ok, safe_facts}`.
    - Test 3: the target becomes `provider_accepted`, the parent becomes `succeeded` with provider-handoff aggregate facts, and the tenant-scoped trace contains the parent, target, and ordered attempt without raw sentinels.
    - Test 4: a repeated planning call returns the same Delivery and DeliveryTarget IDs and creates no additional target or started attempt.
  </behavior>
  <action>Start RED with the four end-to-end assertions above, then create the repository migration before adding dependent schemas. The migration creates `chimeway_delivery_targets` and `chimeway_delivery_target_attempts` with UUID primary keys, explicit `tenant_id`, `delivery_id`, opaque bounded `binding_revision_ref`, target status/claim/lease/timestamps, numbered attempt state, `started_at`, `finished_at`, `source`, nullable `prior_attempt_id`, `duplicate_risk`, and closed `safe_facts`; add unique indexes on `{delivery_id, binding_revision_ref}` and `{delivery_target_id, attempt_number}`, tenant-qualified discovery indexes, and no token/endpoint/credential/payload/provider-body columns. Define `Chimeway.TargetResolver.resolve_targets/2` plus `BindingRevision.new/2` as the public data-first contract: accept exactly the supplied tenant and bounded opaque revision reference, reject mismatched/unscoped/nil/uncontrolled values, and stable-sort exact refs. Per the resolved research contract and D-02/D-09, define `Chimeway.TargetAdapter.deliver/2` and `%TargetEnvelope{delivery: Delivery, target: DeliveryTarget}` as a provider-independent replaceable seam that accepts only the durable target ID, opaque binding revision reference, and closed safe context; it never accepts raw token, endpoint, credential, provider body, or uncontrolled host payload material, and APNs-specific request construction remains Phase 100. In `DeliveryPlanning`, branch only channel `"push"` through the configured/option-supplied resolver, retain the canonical parent per D-01, insert targets with `on_conflict: :nothing` and authoritative tenant-qualified reload per D-02/D-03, and suppress an empty selection with `no_eligible_targets` per D-04. In `DeliveryTargets`, implement tenant-qualified `begin_target_attempt/2` as one transaction that conditionally claims an eligible row, locks it, assigns the next contiguous attempt number, and inserts `attempt_started` before returning adapter authority; implement conclusive result recording and parent recomputation for the one accepted-target tracer. Add `Executor.run_target/1` as the target execution seam and extend Traces with tenant-qualified ordered target/attempt children and stable provider-handoff vocabulary per D-05/D-06/D-09. Exercise the thin path directly through planning and `Executor.run_target/1`, proving the architecture before the follow-on task connects all dispatch entry points and storage-copy parity. Record the assumption-delta decision as no-change: Delivery remains the canonical logical noun; DeliveryTarget is its child keyed by binding revision, not an accepted parallel identity.</action>
  <reversibility rating="costly">The additive schema and public resolver/adapter contracts are inherited by later mobile phases, but remain migratable without deleting existing lifecycle data.</reversibility>
  <verify>
    <automated>mix format --check-formatted priv/repo/migrations/20260819000001_create_chimeway_delivery_targets.exs lib/chimeway/target_resolver.ex lib/chimeway/delivery_target.ex lib/chimeway/delivery_target_attempt.ex lib/chimeway/target_adapter.ex lib/chimeway/delivery_targets.ex lib/chimeway/delivery_planning.ex lib/chimeway/dispatch/executor.ex lib/chimeway/traces.ex test/chimeway/delivery_target_test.exs &amp;&amp; env MIX_ENV=test mix ecto.migrate &amp;&amp; env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `env MIX_ENV=test mix ecto.migrate` applies the new repository migration successfully.
    - `env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs --warnings-as-errors` exits zero and proves a real resolver → canonical Delivery → target → pre-I/O attempt → fake target adapter → aggregate → trace path.
    - The fake adapter asserts its target attempt already exists with `attempt_started` before returning.
    - Exact row-count and ID assertions prove repeated planning retains one Delivery and one target for the same selected revision.
    - Complete persisted rows, trace terms, telemetry captured by the test, and inspected job-free synchronous results exclude exact raw token, endpoint, credential, payload, and provider-body sentinels.
    - Existing non-push delivery lifecycle tests remain green: `env MIX_ENV=test mix test test/chimeway/integration/delivery_lifecycle_test.exs --warnings-as-errors`.
  </acceptance_criteria>
  <done>One production-quality target path works end to end beneath the canonical logical delivery with durable pre-I/O evidence and privacy-safe provider-handoff truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete copied-migration, safe-evidence, and dispatch-entry parity</name>
  <read_first>
    - priv/repo/migrations/20260819000001_create_chimeway_delivery_targets.exs
    - priv/chimeway_migrations/034_privacy_safe_delivery_evidence.exs
    - lib/chimeway/dispatch/executor.ex
    - lib/chimeway/dispatch/sync.ex
    - lib/chimeway/safe_evidence.ex
    - test/chimeway/delivery_target_test.exs
    - test/chimeway/integration/delivery_lifecycle_test.exs
  </read_first>
  <files>priv/chimeway_migrations/035_create_chimeway_delivery_targets.exs, lib/chimeway/dispatch/sync.ex, lib/chimeway/safe_evidence.ex, test/chimeway/delivery_target_test.exs</files>
  <behavior>
    - Test 1: migration 035 carries the repository target/attempt contract through the existing static-prefix helpers without schema drift.
    - Test 2: ordinary synchronous push dispatch enumerates the persisted target and reaches the proven `Executor.run_target/1` seam, while non-push `run_delivery/1` behavior remains unchanged.
    - Test 3: target and attempt result facts pass only through closed SafeEvidence constructors and exclude raw resolver/provider material.
  </behavior>
  <action>After the tracer is green, copy its schema contract into migration 035 using the established prefix helpers, preserving every table, column, reference, index, and rollback operation. Extend SafeEvidence with the closed target/attempt constructors required by the tracer and make Sync enumerate durable push targets through `Executor.run_target/1`; preserve the existing non-push execution path. Strengthen the tracer assertions to cover the ordinary synchronous entry point and copied-migration parity without changing D-01/D-03 parent identity, D-05/D-06 pre-I/O ordering, or D-09 provider-handoff terminology. Keep APNs construction and host-owned raw material outside Phase 99.</action>
  <reversibility rating="costly">The copied migration is adopter-facing, while dispatch and SafeEvidence wiring remain locally replaceable behind the public target contracts.</reversibility>
  <verify>
    <automated>mix format --check-formatted priv/chimeway_migrations/035_create_chimeway_delivery_targets.exs lib/chimeway/dispatch/sync.ex lib/chimeway/safe_evidence.ex test/chimeway/delivery_target_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs test/chimeway/integration/delivery_lifecycle_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The copied migration contains the same target and attempt columns, references, uniqueness, tenant indexes, and rollback contract as the repository migration through static-prefix helpers.
    - The focused tracer reaches `Executor.run_target/1` through ordinary synchronous push dispatch and retains start-before-adapter evidence.
    - Existing non-push delivery lifecycle tests exit zero.
    - SafeEvidence assertions exclude raw token, endpoint, credential, payload, provider-body, and uncontrolled host fields.
  </acceptance_criteria>
  <done>The proven target slice is connected to supported synchronous dispatch and copied storage with closed evidence and no regression to non-push delivery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host resolver → Chimeway target planning | Untrusted host values cross into durable tenant-scoped target identity. |
| Durable target → replaceable adapter | A provider request may occur only after an atomic claim and durable start record. |
| Target rows → public trace | Child evidence must remain tenant-qualified, ordered, and closed-vocabulary. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-99-01 | Elevation of Privilege | Resolver/target tenant boundary | high | mitigate | Validate resolver tenant equality and repeat tenant predicates on inserts, reloads, and traces. | `env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs --warnings-as-errors` |
| T-99-02 | Tampering | Claim/start/provider sequence | high | mitigate | Make the conditional target claim and numbered attempt_started insert one transaction completed before adapter invocation. | `env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs --warnings-as-errors` |
| T-99-03 | Information Disclosure | Resolver, persistence, traces | high | mitigate | Accept only a bounded opaque revision struct and project target evidence through SafeEvidence constructors. | `env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs --warnings-as-errors` |
| T-99-04 | Repudiation | Provider acceptance aggregate | high | mitigate | Persist target attempt and expose only provider_accepted handoff terminology with exact parent aggregate facts. | `env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs --warnings-as-errors` |
| T-99-01-SC | Tampering | Package supply chain | low | accept | No package-manager install or dependency change occurs. | `git diff --exit-code -- mix.exs mix.lock` |

ASVS L1 disposition: all high-severity tenant, duplicate-handoff, leakage, and false-receipt threats are mitigated by executable tracer assertions; generic injection concerns remain canon security work and are not duplicated as fallback prohibitions.
</threat_model>

## Artifacts this phase produces

- New tables: `chimeway_delivery_targets` and `chimeway_delivery_target_attempts` with tenant-qualified identity, claims, attempt order, safe facts, and linkage.
- New migration paths: `priv/repo/migrations/20260819000001_create_chimeway_delivery_targets.exs` and `priv/chimeway_migrations/035_create_chimeway_delivery_targets.exs`.
- New modules/structs: `Chimeway.TargetResolver`, `Chimeway.TargetResolver.BindingRevision`, `Chimeway.DeliveryTarget`, `Chimeway.DeliveryTargetAttempt`, `Chimeway.TargetAdapter`, `Chimeway.TargetAdapter.TargetEnvelope`, and `Chimeway.DeliveryTargets`.
- New/changed functions: `TargetResolver.resolve_targets/2`, `BindingRevision.new/2`, `DeliveryTargets.plan_targets/3`, `begin_target_attempt/2`, `record_target_result/3`, `recompute_delivery/2`, `Executor.run_target/1`, and push-target branches in `DeliveryPlanning` and `Dispatch.Sync`.
- New reason/outcome tokens: `no_eligible_targets`, `attempt_started`, and `provider_accepted`; no APNs-specific token is introduced.
- New executable evidence: `test/chimeway/delivery_target_test.exs` one-target end-to-end tracer.

<verification>
Run the repository migration, focused tracer, existing non-push lifecycle regression, and formatting gate. Accept only zero exits and exact durable-row/trace assertions; no conversational UAT applies.
</verification>

<success_criteria>
One logical push Delivery resolves one opaque tenant-owned revision, persists one child target, records claim/start before adapter I/O, aggregates provider acceptance honestly, and exposes safe ordered trace evidence without changing non-push dispatch.
</success_criteria>

<output>
Create `.planning/phases/99-multi-installation-delivery-recovery/99-01-SUMMARY.md` when done.
</output>
