---
phase: 99-multi-installation-delivery-recovery
plan: "05"
type: execute
wave: 4
depends_on: ["99-02", "99-03", "99-04"]
files_modified:
  - lib/chimeway/target_recovery.ex
  - lib/chimeway/dispatch/recovery_worker.ex
  - lib/chimeway/deliveries.ex
  - lib/chimeway/safe_evidence.ex
  - test/chimeway/orchestration/target_recovery_test.exs
  - test/chimeway/tenant_scope_contract_test.exs
  - test/chimeway/runtime_prefix_integration_test.exs
  - .planning/phases/99-multi-installation-delivery-recovery/99-VALIDATION.md
autonomous: true
requirements: [PUSH-03, RECOV-01, RECOV-02]
must_haves:
  truths:
    - "D-10/RECOV-01: one explicit tenant-scoped worker discovers and claims stranded event planning plus actionable target work in deterministic bounded batches."
    - "D-10/D-11: every discovery, join, claim, reload, mutation, dispatch decision, and result DTO retains the resolved tenant predicate and returns a non-disclosing noop/empty result outside scope."
    - "D-11: duplicate recovery workers converge with planners and target workers on existing DeliveryTarget identity and claim state; terminal, claimed, expired, invalidated, ambiguous, and otherwise ineligible work short-circuits before provider I/O."
    - "D-12: recovery records closed reasons for claimed, skipped, resumed, no-target, and ambiguous outcomes without host identity, endpoint, credential, payload, or provider-body material."
    - "D-07/RECOV-02: recovery closes stale attempt_started work as ambiguous_handoff and never treats lease expiry alone as permission to resend."
  assumptions:
    - statement: "RECOV-01 unclassified edge probe: no extra category was inferable beyond tenant scope, bounded deterministic discovery, atomic claims, convergence, and safe evidence; verifier must flag any uncovered recovery edge."
      status: unresolved
      flagged: true
  artifacts:
    - path: "lib/chimeway/target_recovery.ex"
      provides: "Bounded tenant-qualified event/target discovery and convergence service"
    - path: "lib/chimeway/dispatch/recovery_worker.ex"
      provides: "Explicit durable-ID recovery worker entry point"
    - path: "test/chimeway/orchestration/target_recovery_test.exs"
      provides: "Stranded trigger, target, race, bound, tenant, ambiguity, and evidence proof"
    - path: ".planning/phases/99-multi-installation-delivery-recovery/99-VALIDATION.md"
      provides: "Completed Nyquist map bound to executed Phase 99 evidence"
  key_links:
    - from: "lib/chimeway/dispatch/recovery_worker.ex"
      to: "lib/chimeway/target_recovery.ex"
      via: "worker validates tenant/batch args then delegates bounded discovery and claims"
      pattern: "tenant_id|batch_size"
    - from: "lib/chimeway/target_recovery.ex"
      to: "lib/chimeway/delivery_targets.ex"
      via: "recovery reuses target identity, claim/start, stale-close, and aggregate contracts"
      pattern: "begin_target_attempt|close_stale_started_attempt"
    - from: "lib/chimeway/target_recovery.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "stranded event planning enters the existing tenant-scoped recovery spine"
      pattern: "recover"
  prohibitions:
    - statement: "Recovery must not infer tenant ownership from a lifecycle ID, binding revision, recipient, actor, or static storage prefix."
      status: unverified
      flagged: true
    - statement: "Recovery must not become arbitrary bulk replay or an unbounded scanner that can resend ambiguous work."
      status: unverified
      flagged: true
    - statement: "Recovery evidence must not expose host-owned installation identity, raw endpoint/token material, credentials, provider bodies, or hidden cross-tenant row existence."
      status: unverified
      flagged: true
---

<objective>
Recover stranded event planning and target work through one bounded tenant-safe, evidence-rich spine.

Purpose: Complete Phase 99 by making trigger-commit and target-execution interruptions recoverable without duplicate targets, blind provider retries, cross-tenant access, or unsafe diagnostics.
Output: Bounded recovery service/worker, concurrency and fault evidence, runtime-prefix proof, and signed executable validation coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
@.planning/phases/99-multi-installation-delivery-recovery/99-RESEARCH.md
@.planning/phases/99-multi-installation-delivery-recovery/99-PATTERNS.md
@.planning/phases/99-multi-installation-delivery-recovery/99-VALIDATION.md
@.planning/phases/99-multi-installation-delivery-recovery/99-03-SUMMARY.md
@.planning/phases/99-multi-installation-delivery-recovery/99-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recover bounded tenant-owned event and target work without blind resend</name>
  <read_first>
    - lib/chimeway/deliveries.ex
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/dispatch/deferred_resume_worker.ex
    - lib/chimeway/dispatch/oban_worker.ex
    - lib/chimeway/safe_evidence.ex
    - test/chimeway/orchestration/recovery_test.exs
    - test/chimeway/tenant_scope_contract_test.exs
    - test/chimeway/runtime_prefix_integration_test.exs
    - .planning/phases/99-multi-installation-delivery-recovery/99-VALIDATION.md
  </read_first>
  <files>lib/chimeway/target_recovery.ex, lib/chimeway/dispatch/recovery_worker.ex, lib/chimeway/deliveries.ex, lib/chimeway/safe_evidence.ex, test/chimeway/orchestration/target_recovery_test.exs, test/chimeway/tenant_scope_contract_test.exs, test/chimeway/runtime_prefix_integration_test.exs, .planning/phases/99-multi-installation-delivery-recovery/99-VALIDATION.md</files>
  <behavior>
    - Test 1: a trigger-committed event with no planned notification/delivery is found only in its tenant, atomically claimed once, replanned through DeliveryPlanning, and records safe recovery evidence.
    - Test 2: actionable targets are discovered in deterministic ID order with default batch 50 and hard maximum 100; a second page starts after the returned cursor and never repeats a row.
    - Test 3: two concurrent recovery workers converge on each event/target; each existing revision retains one target and each actual adapter invocation retains one started attempt.
    - Test 4: stale started work becomes ambiguous without adapter invocation; terminal, claimed, expired, invalidated, ambiguous, wrong-tenant, and absent work returns bounded non-disclosing skip evidence.
    - Test 5: ordinary recovery APIs and worker args work in public and prefixed static storage through Repo defaults, without a domain prefix argument.
  </behavior>
  <action>Create `Chimeway.TargetRecovery` with `recover_tenant/2`, `discover_stranded_events/2`, and `discover_target_work/2`. Require explicit `tenant_id`; validate `batch_size` to default 50 and range 1..100; use deterministic keyset ordering by durable ID/cursor and select only qualifying timestamps/states. Reuse the existing `Deliveries` event recovery transaction for trigger-commit gaps, extending it only where necessary to retain a tenant predicate at every notification/delivery/target join and to route push replanning through the canonical resolver/target seam. For targets, atomically claim only actionable unleased rows through `DeliveryTargets`; close stale started rows through the D-07 ambiguous path and exclude them from automatic dispatch. Create `Chimeway.Dispatch.RecoveryWorker` with args exactly `tenant_id`, optional bounded `batch_size`, and optional opaque cursor; it calls the service, enqueues/executes only returned durable IDs, and records a closed result summary. Make duplicate worker races and a concurrent normal planner/worker converge on target uniqueness and claim gates. Extend SafeEvidence with named recovery reason tokens `claimed`, `skipped_terminal`, `skipped_claimed`, `skipped_expired`, `skipped_invalidated`, `resumed_planning`, `resumed_target`, and `left_ambiguous`; DTOs expose counts, durable IDs only where already authorized, cursor, and stable reasons. Add wrong-tenant/absent equivalence and complete sentinel scans. Extend runtime-prefix proof using ordinary args only. Update 99-VALIDATION.md task/plan/wave rows and flip `wave_0_complete`/`nyquist_compliant` only after every listed focused command and the final commands below exit zero.</action>
  <reversibility rating="reversible">The worker/service are replaceable orchestration around stable target and event contracts; batch defaults and scheduling can change locally.</reversibility>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/target_recovery.ex lib/chimeway/dispatch/recovery_worker.ex lib/chimeway/deliveries.ex lib/chimeway/safe_evidence.ex test/chimeway/orchestration/target_recovery_test.exs test/chimeway/tenant_scope_contract_test.exs test/chimeway/runtime_prefix_integration_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors &amp;&amp; mix verify.runtime_prefix &amp;&amp; mix verify.install_golden &amp;&amp; mix ci.test</automated>
  </verify>
  <acceptance_criteria>
    - Focused recovery tests prove stranded-event replanning, exact batch bounds/cursors, target recovery, concurrent convergence, and stale-start ambiguous closeout.
    - Adapter call counters and target/attempt row counts prove duplicate recovery/planning/execution creates no unexplained request.
    - Every wrong-tenant/absent path returns the same public shape and emits no target/event existence, ID, binding, or status signal.
    - Complete recovery DTO, trace, telemetry, log capture, and job args exclude exact token, endpoint, identity, credential, payload, and provider-body sentinels.
    - `mix verify.runtime_prefix`, `mix verify.install_golden`, and `mix ci.test` exit zero.
    - `99-VALIDATION.md` maps all six requirements to the executed test files/commands and sets compliance flags only after the final green run.
  </acceptance_criteria>
  <done>Bounded tenant recovery safely resumes stranded planning/targets, preserves ambiguity, and closes the Phase 99 executable validation matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Recovery worker args → tenant lifecycle queries | Durable IDs/cursors must never widen tenant authority or reveal row existence. |
| Recovery discovery → claim/dispatch | Bounded scanning is not provider-call authority; durable claims and stale-start rules remain decisive. |
| Recovery results → telemetry/operator evidence | Results must explain claim/skip/resume/ambiguity without host-owned target material. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-99-05-01 | Elevation of Privilege | Recovery tenant scope | high | mitigate | Require explicit tenant and repeat it across discovery, joins, claims, reloads, mutations, and DTOs; assert absent/wrong equivalence. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` |
| T-99-05-02 | Denial of Service | Recovery discovery | high | mitigate | Enforce keyset order, default 50, maximum 100, and bounded worker args with pagination tests. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs --warnings-as-errors` |
| T-99-05-03 | Tampering | Recovery/provider convergence | high | mitigate | Reuse database target identity/claim gates and close stale starts ambiguous before any dispatch decision. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs --warnings-as-errors` |
| T-99-05-04 | Information Disclosure | Recovery evidence | high | mitigate | Construct closed reason/count/cursor DTOs and scan all observable surfaces against exact sensitive sentinels. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs --warnings-as-errors` |
| T-99-05-05 | Repudiation | Stale possible handoff | high | mitigate | Record left_ambiguous/ambiguous_handoff and assert zero automatic adapter calls. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs --warnings-as-errors` |
| T-99-05-SC | Tampering | Package supply chain | low | accept | No package-manager install or dependency change occurs. | `git diff --exit-code -- mix.exs mix.lock` |

ASVS L1 disposition: all high-severity recovery scope, resource, duplicate-handoff, leakage, and false-truth threats have executable mitigations; generic OWASP/compliance candidates remain canon breadcrumbs rather than duplicate prohibitions.
</threat_model>

## Artifacts this phase produces

- New modules: `Chimeway.TargetRecovery` and `Chimeway.Dispatch.RecoveryWorker`.
- New functions: `recover_tenant/2`, `discover_stranded_events/2`, and `discover_target_work/2`; extended tenant-safe event recovery integration in `Chimeway.Deliveries`.
- New worker args: `tenant_id`, optional bounded `batch_size`, and optional opaque `cursor`; no raw target material or storage prefix.
- New recovery reason tokens: `claimed`, `skipped_terminal`, `skipped_claimed`, `skipped_expired`, `skipped_invalidated`, `resumed_planning`, `resumed_target`, and `left_ambiguous`.
- New executable evidence: `test/chimeway/orchestration/target_recovery_test.exs`; expanded tenant-scope and runtime-prefix contracts.
- Changed planning evidence: `99-VALIDATION.md` records plan/wave ownership and final executable results.
- New APNs, CrossWake, twin, physical-device, token, endpoint, credential, request-header, payload, collapse, registration, open, or device-receipt artifacts: none.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One logical decision reaches all eligible opaque installations with independent tenant-safe truth and recovery | 99-01 through 99-05 | COVERED | Tracer proves one path; expansions cover parity, complete fan-out, dispatch races, ambiguity, and recovery. |
| REQ | PUSH-01 | Public resolver returns every active eligible tenant-scoped opaque binding revision | 99-01, 99-03 | COVERED | Closed resolver, empty/null/malformed/duplicate/order cases, no raw material. |
| REQ | PUSH-02 | Durable independent target claim/attempt/retry/expiry/invalidation/trace history | 99-01, 99-03, 99-04 | COVERED | Child schema, aggregate/trace, target dispatch and terminal state matrix. |
| REQ | PUSH-03 | Duplicate planning/jobs/recovery cannot create duplicate target or unexplained request | 99-02 through 99-05 | COVERED | Database uniqueness, authoritative reload, atomic claims, race/fault tests. |
| REQ | PUSH-04 | Stable no-target suppression and honest mixed aggregate | 99-01, 99-03 | COVERED | `no_eligible_targets`, provider-accepted iff success, partial failures retained. |
| REQ | RECOV-01 | Bounded tenant-scoped stranded event/target recovery with evidence | 99-05 | COVERED | Explicit worker/service, keyset limits, closed reasons, concurrency proof. |
| REQ | RECOV-02 | Pre-I/O evidence and ambiguous possible-handoff crash truth | 99-01, 99-04, 99-05 | COVERED | Start-before-I/O, fault injection, ambiguous closeout, linked risk-aware redrive. |
| CONTEXT | D-01, D-02, D-03, D-04 | Canonical parent, opaque revisions, unique children, honest aggregate | 99-01, 99-03 | COVERED | Decision IDs appear in concrete actions/truths. |
| CONTEXT | D-05, D-06, D-07, D-08, D-09 | Target state machine, pre-I/O history, ambiguity/redrive, provider-handoff semantics | 99-01, 99-03, 99-04 | COVERED | Durable state and machine-executable race/fault/trace assertions. |
| CONTEXT | D-10, D-11, D-12 | Bounded tenant recovery, atomic convergence, safe evidence | 99-04, 99-05 | COVERED | Tenant predicates, bounded worker, claim reuse, closed reasons. |
| RESEARCH | — | Prefix-aware migration 035 and public/prefixed/static runtime parity | 99-01, 99-02, 99-05 | COVERED | Repository/canonical/golden/database/runtime gates. |
| RESEARCH | — | Database identity/claims rather than Oban uniqueness | 99-03, 99-04, 99-05 | COVERED | Real concurrent claims and worker races. |
| RESEARCH | — | Closed SafeEvidence target/recovery projections | 99-01, 99-03, 99-05 | COVERED | Exact maps and sentinel scans across traces/telemetry/results. |
| RESEARCH | — | No package installs; reuse Ecto/PostgreSQL/Oban | all plans | COVERED | Each threat model records no package operation. |
| CONTEXT | Deferred Ideas | APNs behavior, CrossWake registration/opens, twin, physical proof | — | EXCLUDED | Explicitly owned by Phases 100–103 and absent from implementation tasks. |

All GOAL, REQ, RESEARCH, and CONTEXT items are covered. The deterministic edge report's 13 classified predicates appear as explicit machine-testable truths across 99-01/03/04; the two unclassified RECOV rows remain visible as structured `status: unresolved` / `flagged: true` assumptions in 99-04/05, preserving 15 surfaced = 13 explicit + 2 flagged. Prohibition recall retained only product-specific identity, privacy, transparency, tenant, resource, and ambiguous-handoff constraints as descriptor-less structured `status: unverified` / `flagged: true` items; routine correctness stayed in edge/tests, and generic OWASP/compliance candidates are referenced in threat-model breadcrumbs rather than minted twice.

<verification>
Run every focused Phase 99 file, both install/static-prefix gates, and `mix ci.test`. Then run `node -e 'const fs=require("node:fs"),path=require("node:path"),{parseMustHavesBlock}=require("/Users/jon/.codex/gsd-core/bin/lib/frontmatter.cjs"); const dir=".planning/phases/99-multi-installation-delivery-recovery"; const specs={artifacts:["path","provides"],key_links:["from","to","via"],assumptions:["statement","status","flagged"],prohibitions:["statement","status","flagged"]}; for(const file of fs.readdirSync(dir).filter(x=>/^99-0[1-5]-PLAN\.md$/.test(x)).sort()){const content=fs.readFileSync(path.join(dir,file),"utf8"); for(const [block,fields] of Object.entries(specs)){const items=parseMustHavesBlock(content,block); const declared=new RegExp("^  "+block+":\\s*$","m").test(content.slice(0,content.indexOf("\n---",4))); if(declared){if(items.length===0) throw new Error(file+" "+block+" parsed empty");} for(const [i,item] of items.entries()){if(!item || typeof item!=="object") throw new Error(file+" "+block+"["+i+"] is not an object"); for(const field of fields) if(!(field in item) || String(item[field]).trim()==="") throw new Error(file+" "+block+"["+i+"] missing "+field);}}} console.log("parseMustHavesBlock contract passed for five Phase 99 plans");'`. This intentionally uses the same raw-block consumer as `verify.cjs`; generic `frontmatter.get`/`extractFrontmatter` is not an object-list round-trip oracle and must not be substituted. All tracer and acceptance evidence is objective and automatic; no conversational UAT, human-check, or human verification gate applies.
</verification>

<success_criteria>
A tenant-scoped bounded worker recovers stranded planning and target work, races converge on durable identity/claims, stale possible handoffs remain ambiguous, all six requirements have executable evidence, and both static storage modes remain green.
</success_criteria>

<output>
Create `.planning/phases/99-multi-installation-delivery-recovery/99-05-SUMMARY.md` when done.
</output>
