---
phase: 98-privacy-safe-delivery-evidence
plan: 02
type: execute
wave: 2
depends_on: [98-01]
files_modified:
  - lib/chimeway/safe_evidence.ex
  - lib/chimeway/trigger.ex
  - lib/chimeway/deliveries.ex
  - lib/chimeway/inbox.ex
  - test/chimeway/trigger_sanitization_test.exs
  - test/chimeway/orchestration/delivery_planning_test.exs
  - test/chimeway/inbox_query_test.exs
  - test/chimeway/inbox_state_transition_test.exs
autonomous: true
requirements: [PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-02/D-04: event, notification, delivery-planning, and inbox identity boundaries transform or omit sensitive input before every Chimeway-owned write or predicate."
    - "D-04/D-05: persisted recipient and correlation identity are host-supplied, tenant/domain-bound opaque references; raw host identity remains transient and no reference is derived from it."
    - "D-04/D-06: Event payload, Notification metadata/render assigns, Delivery metadata/planning context/render data, and channel rendering persistence contain only their explicit safe fact allowlists, never arbitrary sanitized blobs."
    - "D-08: render key/version, lifecycle IDs, status/reason/classification, timestamps, and opaque identity remain available after generic content is removed."
    - "D-02/D-09: tenant-scoped Inbox reads and mutations accept the same validated opaque recipient reference persisted by Trigger and retain Phase 97 wrong-tenant/absence behavior."
  artifacts:
    - path: "lib/chimeway/trigger.ex"
      provides: "Safe event/notification persistence and opaque recipient/correlation identity"
    - path: "lib/chimeway/deliveries.ex"
      provides: "Closed delivery planning/update evidence writes"
    - path: "lib/chimeway/inbox.ex"
      provides: "Tenant-scoped raw-input-to-opaque-query compatibility seam"
  key_links:
    - from: "lib/chimeway/trigger.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "validated host-supplied recipient/correlation references and literal event/notification fact builders run before Ecto.Multi/insert_all"
      pattern: "SafeEvidence.opaque_ref"
    - from: "lib/chimeway/inbox.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "recipient predicates validate the identical tenant/domain-bound opaque reference"
      pattern: "SafeEvidence.opaque_ref"
  prohibitions:
    - "FLAGGED-UNVERIFIED — Raw recipient identity must not become a durable grouping key merely to preserve legacy Inbox implementation convenience."
    - "FLAGGED-UNVERIFIED — Removing generic content must not remove stable lifecycle status, reason, render identity, timestamps, or opaque correlation facts needed for explanation."
---

<objective>
Expand the proven evidence boundary across Trigger, delivery planning, and Inbox compatibility so new lifecycle writes retain only opaque identity and closed operational facts.

Purpose: Prevent sensitive host/content data from entering core storage while keeping tenant-scoped recipient workflows and operator explanation usable.
Output: Safe event/notification/delivery writes, deterministic opaque identity, and Inbox query/mutation compatibility.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-RESEARCH.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-PATTERNS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-01-SUMMARY.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-02-SUMMARY.md

<interfaces>
- `Chimeway.Trigger.trigger(notifier, params, opts \\ [])` passes raw host values only through notifier/rendering functions and owns the Event/Notification write transaction.
- `Chimeway.SafeEvidence.opaque_ref(domain, value)` from Plan 98-01 validates a bounded host/provider-supplied `cw_...` reference and never accepts raw source data.
- `Chimeway.Inbox.list_for_recipient/2`, `unread_count/2`, `mark_read/3`, `mark_seen/3`, and `archive/3` already resolve explicit tenant scope and must preserve their public results.
- `Chimeway.Deliveries.plan_delivery/3`, decision application, and render result updates write `metadata`, `planning_context`, and `render_data` through Ecto changesets.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close Trigger and delivery-planning write boundaries</name>
  <reversibility rating="costly">D-04 changes durable identity/content semantics across established schemas and contexts; reverting would require coordinated callers and historical-data handling, though the physical schema remains compatible.</reversibility>
  <read_first>
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/trigger.ex
    - lib/chimeway/events/event.ex
    - lib/chimeway/notifications/notification.ex
    - lib/chimeway/deliveries.ex
    - lib/chimeway/delivery.ex
    - test/chimeway/trigger_sanitization_test.exs
    - test/chimeway/orchestration/delivery_planning_test.exs
    - test/chimeway/privacy_boundary_test.exs
  </read_first>
  <files>lib/chimeway/safe_evidence.ex, lib/chimeway/trigger.ex, lib/chimeway/deliveries.ex, test/chimeway/trigger_sanitization_test.exs, test/chimeway/orchestration/delivery_planning_test.exs</files>
  <behavior>
    - Test 1: nested hostile trigger params and rendering assigns/channels reach notifier functions but no exact token, credential, recipient, deep-link, content, or provider-body sentinel exists in reloaded Event/Notification/Delivery rows.
    - Test 2: a notifier supplies distinct tenant/domain-bound `cw_` recipient references; Trigger persists them exactly while raw recipient identity used for rendering never appears in storage.
    - Test 3: correlation input is stored/projected only as an opaque reference, while notification key/version and render key/version remain exact.
    - Test 4: delivery planning retains only status/reason, time-zone/rule identity when valid, render identity, timestamps, and literal safe metadata; unknown keys and invalid values do not survive.
    - Test 5: zero, one, duplicate, and equal recipient inputs preserve deterministic first-recipient ordering after comparison by opaque reference.
  </behavior>
  <action>Start with failing write-boundary cases, then apply D-02 through D-06/D-08. Extend `SafeEvidence` with exact literal builders `event_payload/1`, `notification_metadata/1`, `render_channels/1`, `planning_context/1`, `delivery_metadata/1`, and `render_data/1`; each returns only documented operational facts with bounded values. Event payload and generic render/content maps have no durable catch-all: facts outside notification definition identity, render key/version, fixed orchestration/reason/timing fields, and opaque references are omitted. In `Trigger`, retain raw `params` and recipient maps only for synchronous notifier callbacks; require each normalized recipient to supply `recipient_ref` and accept an optional `correlation_ref` in opts, validate them through `SafeEvidence.opaque_ref/2`, persist those references in the existing physical identity/correlation columns, and deduplicate/order recipients by the opaque reference while carrying raw identity separately for synchronous rendering only. Missing/malformed refs fail before persistence with stable errors; never hash raw identity/correlation into a Chimeway-owned reference. Replace private shallow sanitizers with `Privacy.redact/1` followed by the relevant closed builder. In `Deliveries`, normalize every new/updated `metadata`, `planning_context`, and `render_data` value through its named builder before changeset/update construction; preserve policy evaluation, row locks, status transitions, error tuple shapes, and Phase 97 tenant predicates. Do not introduce a raw-retention debug flag. The tests must inspect every JSON/map column on the created Event, Notification, and Delivery, not only the field directly under test.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/safe_evidence.ex lib/chimeway/trigger.ex lib/chimeway/deliveries.ex test/chimeway/trigger_sanitization_test.exs test/chimeway/orchestration/delivery_planning_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/trigger_sanitization_test.exs test/chimeway/orchestration/delivery_planning_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source: Trigger and every in-scope Deliveries map write call a named `SafeEvidence` builder before Ecto construction.
    - Behavior: host-supplied tenant/domain-bound references are validated and persisted exactly; missing/malformed references fail before persistence and no raw source is transformed into a reference.
    - Storage: full-row serialization of the created Event, Notifications, and Deliveries contains zero sentinel values while render/status/reason facts remain.
    - Focused Trigger and planning suites exit zero with warnings as errors.
  </acceptance_criteria>
  <done>New event, notification, and delivery-planning rows retain explainable facts without raw identity, correlation, content, or arbitrary diagnostic maps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve tenant-scoped Inbox behavior over opaque recipient predicates</name>
  <reversibility rating="costly">The transformation becomes part of Inbox query compatibility; changing its domain/version requires dual-read or migration coordination.</reversibility>
  <read_first>
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/inbox.ex
    - lib/chimeway/tenant_scope.ex
    - lib/chimeway/notifications/notification.ex
    - test/chimeway/inbox_query_test.exs
    - test/chimeway/inbox_state_transition_test.exs
    - test/chimeway/tenant_scope_contract_test.exs
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-02-SUMMARY.md
  </read_first>
  <files>lib/chimeway/safe_evidence.ex, lib/chimeway/inbox.ex, test/chimeway/inbox_query_test.exs, test/chimeway/inbox_state_transition_test.exs</files>
  <behavior>
    - Test 1: list/count with a host-supplied opaque recipient reference resolves the tenant, validates the predicate, and returns only that tenant's rows.
    - Test 2: read/seen/archive transitions derive the same predicate for the mutation and reload, preserve first-transition idempotency, and emit no raw recipient in signal/diagnostic data.
    - Test 3: wrong tenant, absent scope, blank recipient, and unknown recipient retain established empty/error results without disclosing existence.
    - Test 4: two tenants use distinct opaque recipient references for the same host identity and cannot query or mutate each other's rows.
  </behavior>
  <action>Route all public Inbox recipient inputs through one private `recipient_ref/2` helper that first uses `TenantScope.resolve/1`, then calls the Plan 98-01 `SafeEvidence.opaque_ref(:recipient, value)` contract. Apply the validated reference consistently in list, count, atomic state transitions, post-update reloads, and first-transition signal construction. Never accept or derive a reference from raw recipient identity, and never add caller input to Logger, Telemetry, signal payload/context, or result DTOs. Preserve public result shapes, pagination/order, `exclude_archived`, `at`, first-transition idempotency, and Phase 97 absence-safe tenant outcomes. Update fixtures to create notifications through the safe Trigger path or supply explicit fixture opaque refs for essential direct inserts.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/safe_evidence.ex lib/chimeway/inbox.ex test/chimeway/inbox_query_test.exs test/chimeway/inbox_state_transition_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/inbox_query_test.exs test/chimeway/inbox_state_transition_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Every Inbox query/mutation predicate uses a tenant-bound opaque reference and no predicate compares raw host input directly to `recipient_identity`.
    - Same-input cross-tenant tests prove different stored references and no cross-tenant effect.
    - Read/seen/archive timestamps and first-transition behavior are unchanged.
    - Focused Inbox and tenant-scope suites exit zero.
  </acceptance_criteria>
  <done>Hosts retain existing tenant-scoped Inbox operations while Chimeway storage and diagnostics use only opaque recipient references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host notifier input → Trigger transaction | Raw identity/content is needed transiently but must not cross into durable lifecycle rows. |
| Host recipient input → Inbox predicate | Raw input becomes a tenant-authorized lookup/mutation key. |
| Policy/render result → Delivery changeset | Generic maps may contain links, content, identity, or adopter-specific data. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-98-02-01 | Information Disclosure | Trigger/Deliveries writes | high | mitigate | Compute opaque refs and use closed fact builders before every Ecto write; assert full-row sentinel absence. | `env MIX_ENV=test mix test test/chimeway/trigger_sanitization_test.exs test/chimeway/orchestration/delivery_planning_test.exs --warnings-as-errors` |
| T-98-02-02 | Spoofing | Opaque recipient correlation | medium | mitigate | Domain- and tenant-separate the deterministic reference and validate nonblank inputs. | `env MIX_ENV=test mix test test/chimeway/trigger_sanitization_test.exs --warnings-as-errors` |
| T-98-02-03 | Information Disclosure | Inbox state operations | high | mitigate | Resolve tenant, validate the host-supplied opaque predicate on update/reload, and omit caller values from signals/results. | `env MIX_ENV=test mix test test/chimeway/inbox_query_test.exs test/chimeway/inbox_state_transition_test.exs --warnings-as-errors` |
| T-98-02-04 | Tampering | Safe delivery facts | medium | mitigate | Literal builders validate enum/scalar/date bounds and omit uncontrolled keys before changesets. | `env MIX_ENV=test mix test test/chimeway/orchestration/delivery_planning_test.exs --warnings-as-errors` |
| T-98-02-SC | Tampering | Package supply chain | low | accept | No install occurs and no dependency changes. | `mix ci.audit` |

ASVS L1 mapping: V4 preserves explicit tenant scope in Inbox and lifecycle joins; V5 validates raw identity and every durable fact before use. Disclosure threats block on PostgreSQL-backed focused tests.
</threat_model>

## Artifacts this phase produces

- Changed functions: `Chimeway.Trigger.trigger/3` internals, `normalize_recipients/2`, Event/Notification attr construction, Trigger sanitization helpers; `Chimeway.Deliveries` planning/render/map normalizers; all recipient-scoped `Chimeway.Inbox` reads and mutations.
- New `Chimeway.SafeEvidence` symbols: `event_payload/1`, `notification_metadata/1`, `render_channels/1`, `planning_context/1`, `delivery_metadata/1`, `render_data/1`.
- Durable field semantics: `Event.correlation_id`, `Notification.recipient_identity`, and `Delivery.actor_id` carry `cw_` opaque references; existing map columns carry only their named closed facts.
- No new durable field, migration path, package, external API, environment variable, or CLI flag is introduced by this plan.

<verification>
Run focused Trigger/planning tests after Task 1 and Inbox/tenant tests after Task 2. At plan completion rerun `test/chimeway/privacy_boundary_test.exs` to ensure the original tracer still passes through the expanded writers.
</verification>

<success_criteria>
- All new core lifecycle writes exclude raw identity/content/diagnostic values.
- Host-supplied opaque identity remains tenant/domain-bound without Chimeway deriving it from raw identity.
- Inbox list/count/mutations remain tenant-safe and behaviorally compatible.
- `env MIX_ENV=test mix test test/chimeway/privacy_boundary_test.exs test/chimeway/trigger_sanitization_test.exs test/chimeway/orchestration/delivery_planning_test.exs test/chimeway/inbox_query_test.exs test/chimeway/inbox_state_transition_test.exs --warnings-as-errors` exits zero.
</success_criteria>

<output>
Create `.planning/phases/98-privacy-safe-delivery-evidence/98-02-SUMMARY.md` when done.
</output>
