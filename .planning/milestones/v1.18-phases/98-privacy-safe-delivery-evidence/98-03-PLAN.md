---
phase: 98-privacy-safe-delivery-evidence
plan: 03
type: execute
wave: 3
depends_on: [98-02]
files_modified:
  - lib/chimeway/safe_evidence.ex
  - lib/chimeway/dispatch/executor.ex
  - lib/chimeway/telemetry.ex
  - lib/chimeway/trigger.ex
  - test/chimeway/dispatch/executor_test.exs
  - test/chimeway/telemetry_integration_test.exs
autonomous: true
requirements: [PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-07/D-08/PRIV-04: adapter results, telemetry spans, and default logs expose one closed vocabulary of opaque IDs, classifications, reasons, statuses, timestamps, render identity, and validated provider facts."
    - "D-02/D-07: telemetry start, stop, and exception metadata are sanitized after all metadata merges, so extra stop metadata and thrown provider/adopter terms cannot bypass the boundary."
    - "D-06: unknown adapter returns become `unknown_classification` without persisting, logging, tracing, or returning the arbitrary term."
    - "D-04/PRIV-04: an exact sentinel matrix proves no raw token, credential, identity, trusted link, rendered content, or provider body appears in attempt persistence, telemetry, or captured logs."
  artifacts:
    - path: "lib/chimeway/telemetry.ex"
      provides: "Value-validated safe metadata for all span phases and bounded default logging"
  key_links:
    - from: "lib/chimeway/dispatch/executor.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "adapter return is classified/projected before record_attempt and error return construction"
      pattern: "SafeEvidence.attempt_attrs"
    - from: "lib/chimeway/telemetry.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "initial and stop/exception metadata are projected after merge"
      pattern: "SafeEvidence.telemetry_meta"
  prohibitions:
    - "FLAGGED-UNVERIFIED — Failure and exception paths must not use arbitrary-term inspection as an operator diagnostic."
    - "FLAGGED-UNVERIFIED — A known metadata key must not make an unvalidated sensitive value safe."
    - "FLAGGED-UNVERIFIED — APNs/provider acceptance language must not claim device display, open, seen, read, or engagement."
---

<objective>
Expand the proven vocabulary across adapter, telemetry, and log surfaces, including adversarial failure paths.

Purpose: Make delivery behavior explainable from safe facts without trusting adapter terms, raw schemas, optional Admin masking, or proof consumers to repair unsafe output.
Output: Safe adapter classification, telemetry/logs, and executable failure-path leak evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-RESEARCH.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-PATTERNS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-02-SUMMARY.md

<interfaces>
- `Chimeway.Dispatch.Executor.run_delivery/1` returns the existing success/error shapes and calls `Deliveries.record_attempt/2` once after adapter execution.
- `Chimeway.Telemetry.span(event_suffix, meta, func)` expects `func` to return `{result, extra_stop_meta}` and is the sole lifecycle telemetry façade.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bound adapter results before attempt persistence</name>
  <read_first>
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/dispatch/executor.ex
    - test/chimeway/dispatch/executor_test.exs
    - test/chimeway/privacy_boundary_test.exs
  </read_first>
  <files>lib/chimeway/safe_evidence.ex, lib/chimeway/dispatch/executor.ex, test/chimeway/dispatch/executor_test.exs</files>
  <behavior>
    - Test 1: success, temporary, permanent, bounced, and unknown adapter returns record stable outcomes/classifications and never forward the raw detail/body to persistence or the caller-visible error.
    - Test 2: malformed success/error detail and an unexpected return never appear in the persisted attempt or returned result.
  </behavior>
  <action>Implement D-04 through D-08 at the adapter boundary. Change `Executor.classify/1` to return only `{outcome, error_class, safe_attempt_facts}`: success/error variants pass their map through `SafeEvidence.attempt_attrs/1`, while unexpected terms yield `:rejected`, `"unknown_classification"`, and no source term. Keep adapter resolution and `run_delivery/1` result shape stable, but do not return or log raw provider detail. Assert the resulting real DeliveryAttempt rather than testing the classifier in isolation.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/safe_evidence.ex lib/chimeway/dispatch/executor.ex test/chimeway/dispatch/executor_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/dispatch/executor_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - No executor path forwards an adapter/provider reason into a persisted field or returned error.
    - Adapter outcome/classification behavior remains stable while all arbitrary details disappear.
    - The focused Executor suite exits zero.
  </acceptance_criteria>
  <done>Adapter and failure behavior remains explainable through classifications while persistence, telemetry, and logs expose no uncontrolled terms or values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sanitize telemetry merges and fixed failure logs</name>
  <read_first>
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/telemetry.ex
    - lib/chimeway/trigger.ex
    - test/chimeway/telemetry_integration_test.exs
  </read_first>
  <files>lib/chimeway/telemetry.ex, lib/chimeway/trigger.ex, test/chimeway/telemetry_integration_test.exs</files>
  <behavior>
    - Test 1: telemetry start and stop metadata contain only typed safe facts even when initial and extra-stop maps include mixed-case nested hostile values.
    - Test 2: span exceptions and Trigger dispatch failures produce fixed log messages plus bounded classification/opaque IDs; captured logs and telemetry contain no sentinels or arbitrary term inspection.
    - Test 3: allowed metadata keys carrying invalid, oversized, URL-shaped, credential-shaped, or recipient-shaped values are omitted rather than forwarded.
  </behavior>
  <action>Make `Telemetry.safe_meta/1` delegate to the established `SafeEvidence.telemetry_meta/1`, which recognizes atom/binary keys without atom creation and validates each value. In `span/3`, sanitize initial metadata and sanitize the merged initial-plus-extra map immediately before returning it to `:telemetry.span/3`; ensure exception events expose no uncontrolled term. Change the default handler and Trigger dispatch-failure warning to literal messages with explicitly selected safe fields and no arbitrary-term interpolation. Test attached handlers for start/stop/exception plus captured Logger output using the common sentinel matrix.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/telemetry.ex lib/chimeway/trigger.ex test/chimeway/telemetry_integration_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/telemetry_integration_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Telemetry tests prove start, stop, and exception outputs after merge, including invalid values beneath allowed keys.
    - No touched log path interpolates or inspects an arbitrary failure term.
    - The focused telemetry suite exits zero.
  </acceptance_criteria>
  <done>Telemetry and Logger failure surfaces contain only bounded classifications and opaque lifecycle facts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Adapter return/exception → executor and diagnostics | Arbitrary provider-controlled terms enter success and failure control flow. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-98-03-01 | Information Disclosure | Executor/Telemetry/Logger failure paths | high | mitigate | Classify before emission, validate metadata after merges, and use literal logs with sentinel capture tests. | `env MIX_ENV=test mix test test/chimeway/dispatch/executor_test.exs test/chimeway/telemetry_integration_test.exs --warnings-as-errors` |
| T-98-03-SC | Tampering | Package supply chain | low | accept | No package install or dependency change occurs. | `mix ci.audit` |

ASVS L1 mapping: V4 keeps Phase 97 tenant-scoped trace/Admin reads; V5 validates adapter terms, telemetry values, DTO facts, and proof fields before output. High-severity disclosure/tampering threats block on focused executable tests.
</threat_model>

## Artifacts this phase produces

- Changed symbols: `Chimeway.Dispatch.Executor.classify/1` semantics; `Chimeway.Telemetry.safe_meta/1`, `span/3`, and default handler behavior; Trigger dispatch failure logging.
- Completed `Chimeway.SafeEvidence.telemetry_meta/1` value-validation contract.
- No new file path, durable field, migration, package, external API, environment variable, or CLI flag is introduced by this plan.

<verification>
Run the focused Executor test after Task 1 and telemetry/log test after Task 2; no conversational UAT is permitted.
</verification>

<success_criteria>
- Adapter-controlled terms cannot reach persistence, caller-visible errors, telemetry, or logs.
- Adapter persistence, telemetry, and logs contain only the shared closed facts.
- Both focused suites exit zero.
</success_criteria>

<output>
Create `.planning/phases/98-privacy-safe-delivery-evidence/98-03-SUMMARY.md` when done.
</output>
