---
phase: 98-privacy-safe-delivery-evidence
plan: 10
type: execute
wave: 8
depends_on: [98-08, 98-09]
files_modified:
  - lib/chimeway/render_context_resolver.ex
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/dispatch/oban_worker.ex
  - test/chimeway/dispatch/oban_worker_test.exs
  - test/chimeway/dispatch/executor_mailglass_adapter_test.exs
  - test/chimeway/integration/delivery_lifecycle_test.exs
autonomous: true
gap_closure: true
requirements: [PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-02/D-04/D-09/PRIV-04: an Oban job contains only delivery_id and a reloaded Delivery contains only opaque recipient reference plus render identity; raw recipient/render data is resolved from the host only immediately before adapter execution."
    - "D-05/D-08: configured host context lets a queued email reach the Mailglass adapter successfully without moving token/recipient/content custody into Chimeway."
    - "D-04/D-07: resolver absence, mismatch, malformed context, and adapter failure retain stable classifications/outcomes but emit no raw context through attempts, worker returns, telemetry, logs, traces, or proof."
    - "PRIV-03/PRIV-04 unclassified boundary: every new resolver-to-worker-to-adapter value is manually enumerated in tests against the raw recipient/render/provider ban; no general map or diagnostic fallback is introduced."
    - "D-08: sync and async paths preserve the same safe public Trigger result and operator explanation even though their transient context lifetimes differ."
  artifacts:
    - path: "lib/chimeway/render_context_resolver.ex"
      provides: "Host-owned execution-time context resolution from notification key/version and opaque recipient reference"
    - path: "lib/chimeway/delivery_planning.ex"
      provides: "Non-persistent delivery hydration for queued adapter execution"
    - path: "lib/chimeway/dispatch/oban_worker.ex"
      provides: "Resolve-and-dispatch worker path with no raw job args or durable payload"
    - path: "test/chimeway/dispatch/executor_mailglass_adapter_test.exs"
      provides: "Real Oban-to-Mailglass recipient handoff proof"
  key_links:
    - from: "lib/chimeway/dispatch/oban_worker.ex"
      to: "lib/chimeway/render_context_resolver.ex"
      via: "resolve host context from persisted notification key/version and opaque recipient reference immediately before Executor.run_delivery/1"
      pattern: "RenderContextResolver"
    - from: "lib/chimeway/delivery_planning.ex"
      to: "lib/chimeway/dispatch/executor.ex"
      via: "return a hydrated in-memory Delivery with virtual recipient address/render payload; never Repo.update it"
      pattern: "recipient_address"
  prohibitions:
    - statement: "Async compatibility must never put a raw recipient, rendered payload, endpoint, token, credential, or host context into Delivery rows or Oban args."
      status: unverified
      verification: flagged
    - statement: "A resolver failure must never be diagnosed by retaining, inspecting, logging, tracing, or returning its raw context or source value."
      status: unverified
      verification: flagged
    - statement: "Provider handoff success must not be represented as device display, open, seen, read, or engagement."
      status: unverified
      verification: flagged
---

<objective>
Restore queued email delivery through the existing host-owned render-context resolver while keeping job arguments, lifecycle rows, public results, and diagnostics free of raw recipient and rendered data.

Purpose: Close the confirmed Oban recipient-handoff regression without violating Chimeway's local-first custody boundary.
Output: Execution-time private hydration, Oban worker integration, real Mailglass handoff evidence, and the final combined Phase 98 regression gate.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-RESEARCH.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-PATTERNS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-REVIEW.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-08-SUMMARY.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-09-SUMMARY.md

<interfaces>
- `RenderContextResolver.resolve/3` already validates a host resolver selected by stable notification key/version and requires the resolved recipient's `recipient_ref` to equal the persisted opaque reference.
- `ObanWorker.perform/1` reloads by `delivery_id`, evaluates policy, then calls `Executor.run_delivery/1`; raw execution context must be attached before the executor without changing the job args.
- `Delivery.recipient_address` is virtual. `render_data` is physical, so execution hydration may set it only on an in-memory struct and must never pass that struct to Repo update/changeset persistence.
- `Chimeway.Adapters.Mailglass.resolve_recipient/1` already prefers the virtual recipient address and can therefore consume the transient handoff without a new adapter contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hydrate queued deliveries from host context immediately before adapter execution</name>
  <reversibility rating="costly">The resolver-to-worker handoff becomes the async execution contract for host-owned context; changing it requires coordinated host resolver and worker changes, but no raw-data migration or third-party lock-in.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/98-privacy-safe-delivery-evidence/98-REVIEW.md
    - lib/chimeway/render_context_resolver.ex
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/dispatch/oban_worker.ex
    - lib/chimeway/dispatch/executor.ex
    - lib/chimeway/adapters/mailglass.ex
    - lib/chimeway/delivery.ex
    - lib/chimeway/notifications/notification.ex
    - lib/chimeway/events/event.ex
    - test/chimeway/dispatch/oban_worker_test.exs
  </read_first>
  <files>lib/chimeway/render_context_resolver.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/dispatch/oban_worker.ex, test/chimeway/dispatch/oban_worker_test.exs</files>
  <behavior>
    - Test 1: an Oban job with only delivery_id reloads an email delivery, resolves host context by notification key/version plus opaque recipient ref, and passes the exact address/render body to a capture adapter in memory.
    - Test 2: the job args, reloaded Event/Notification/Delivery, attempt, telemetry/log capture, Trigger result, and trace contain none of the address/render sentinels; safe IDs/status/outcome/render identity remain.
    - Test 3: resolver absent, recipient-ref mismatch, malformed recipient, and rendering failure never expose raw context and converge through an existing stable missing/invalid-context or adapter classification path.
    - Test 4: a resolver spy/counter records zero calls for terminal/idempotent, retry-scheduled, policy-suppressed, policy-deferred, and non-email/custom-channel worker paths; each path retains its established return, attempt, and scheduling behavior without recipient hydration.
  </behavior>
  <action>Begin RED in `ObanWorkerTest` with a capture resolver/adapter and exact sentinels. Reuse the existing `RenderContextResolver.resolve/3` ownership contract; do not create a second registry, store raw context, or add data to Oban args. Add an internal (`@doc false`) DeliveryPlanning execution-hydration seam that loads the delivery's Notification/Event safe identity, resolves current host context by notification key/version and the persisted opaque recipient reference, renders against the persisted render key/version, extracts the validated `user:<email>` address, and returns a new in-memory Delivery containing the virtual `recipient_address` plus transient render payload. The seam must never call Repo.update or a changeset with those values.

In `ObanWorker`, preserve this exact order: reload and apply terminal/idempotency/orchestration short-circuits; evaluate policy and return or schedule its suppressed/deferred/retry outcomes; bypass hydration for every non-email/custom-channel delivery; only for an allowed email delivery invoke the execution-hydration seam, then pass that hydrated in-memory Delivery directly to `Executor.run_delivery/1`. No host resolver or rendering call may occur before those gates. Preserve all retry/exhaustion/attempt semantics. Add a resolver spy/counter to the focused worker tests and assert an exact zero call count separately for terminal/idempotent, retry-scheduled, policy-suppressed, policy-deferred, and non-email/custom-channel cases, plus exactly one call for the allowed-email case immediately before executor handoff. If host context is unavailable or invalid, keep the failure closed and categorical: do not inspect/log/telemeter/return the raw resolver term; allow the existing adapter/worker classification contract to produce explainable failure without a raw fallback. Manually audit every value crossing resolver → hydration → worker → executor against D-04/D-07 and encode the audit as full serialized sentinel assertions plus positive safe-fact assertions.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/render_context_resolver.ex lib/chimeway/delivery_planning.ex lib/chimeway/dispatch/oban_worker.ex test/chimeway/dispatch/oban_worker_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Job args remain exactly delivery_id-only and no Chimeway row gains a raw address or rendered payload.
    - A configured host resolver supplies the exact private context to the adapter at execution time.
    - Invalid/missing context is categorical and sentinel-free across attempts, returns, telemetry, logs, and traces.
    - Resolver-call assertions prove terminal/idempotent, retry-scheduled, policy-suppressed, policy-deferred, and non-email/custom-channel paths each perform zero hydrations while retaining their established behavior.
    - The focused Oban worker suite exits zero.
  </acceptance_criteria>
  <done>Queued delivery execution can reconstruct host-owned private context at the last responsible moment without expanding Chimeway custody.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove real Oban-to-Mailglass handoff and close the combined privacy regression matrix</name>
  <read_first>
    - lib/chimeway/adapters/mailglass.ex
    - lib/chimeway/dispatch/oban_worker.ex
    - lib/chimeway/render_context_resolver.ex
    - test/chimeway/dispatch/executor_mailglass_adapter_test.exs
    - test/chimeway/integration/delivery_lifecycle_test.exs
    - test/chimeway/privacy_test.exs
    - test/chimeway/privacy_boundary_test.exs
    - test/chimeway/trigger_sanitization_test.exs
  </read_first>
  <files>test/chimeway/dispatch/executor_mailglass_adapter_test.exs, test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <behavior>
    - Test 1: with Oban dispatch and the existing host resolver registry, a triggered email is accepted by Mailglass Fake and records one succeeded attempt.
    - Test 2: before worker execution, Oban args and the delivery row contain no email/body; after execution, all Chimeway rows, Trigger result, trace, attempt, telemetry/logs, and proof remain sentinel-free.
    - Test 3: the Mailglass message contains the expected transient recipient/rendered content, proving delivery did not pass by deleting required data.
    - Test 4: duplicate-key exploit cases from Plan 09 and direct trusted-render cases from Plan 08 remain closed in the combined focused gate.
  </behavior>
  <action>Convert/extend the Mailglass executor integration into a real queued path using `perform_job(ObanWorker, ...)`, an opaque stored recipient reference, and a configured RenderContextResolver fixture that returns the raw email only at execution. Assert the Mailglass Fake received exactly one message with the expected address/rendering, while direct serialization of the Oban job args and every Chimeway-owned row/output contains none of those values. Keep provider handoff semantics honest: success proves Mailglass/provider acceptance only, not display/open/read/engagement. Extend Scenario J as needed to compare sync and async safe public/trace shapes. Then run the combined focused Phase 98 suites and full CI; do not weaken or skip an atom-count, PostgreSQL, Mailglass, or Oban assertion because earlier runs encountered shared connection exhaustion.</action>
  <verify>
    <automated>mix format --check-formatted test/chimeway/dispatch/executor_mailglass_adapter_test.exs test/chimeway/integration/delivery_lifecycle_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/dispatch/executor_mailglass_adapter_test.exs test/chimeway/dispatch/oban_worker_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/privacy_test.exs test/chimeway/privacy_boundary_test.exs test/chimeway/trigger_sanitization_test.exs test/chimeway/orchestration/delivery_planning_test.exs test/chimeway/deliveries_test.exs --warnings-as-errors &amp;&amp; mix ci</automated>
  </verify>
  <acceptance_criteria>
    - Real Oban execution reaches Mailglass Fake with the correct transient recipient/rendered content and records a succeeded attempt.
    - Job args, storage, public return, trace, attempt, telemetry/logs, and proof contain no raw recipient/render/provider sentinel.
    - Positive safe evidence prevents a delete-everything/vacuous test pass.
    - Plans 08 and 09 regressions remain green in the combined command.
    - Focused suites and `mix ci` exit zero without human/UAT evidence.
  </acceptance_criteria>
  <done>Async email delivery works end to end through host-owned transient resolution, and the complete repaired privacy boundary is proven by executable evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Oban job/durable rows → host resolver | Safe identifiers request current host-owned private execution context. |
| Host resolver → worker/adapter | Raw recipient and rendered content enter a short-lived process and must not cross back into owned storage or diagnostics. |
| Worker → Mailglass | Transient email data crosses to the configured provider adapter while evidence remains bounded. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-98-10-01 | Information Disclosure | Oban args and Delivery rows | high | mitigate | Keep delivery_id-only jobs and identity-only rows; assert exact sentinels absent before/after execution. | `env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors` |
| T-98-10-02 | Spoofing | RenderContextResolver recipient handoff | high | mitigate | Require notifier key/version and resolved recipient_ref to match persisted safe identity before using context. | `env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors` |
| T-98-10-03 | Information Disclosure | Resolver/worker failure diagnostics | high | mitigate | Collapse invalid context to stable classifications and scan attempts, returns, telemetry, logs, traces, and proof. | `env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs test/chimeway/privacy_boundary_test.exs --warnings-as-errors` |
| T-98-10-04 | Repudiation | Mailglass async outcome | medium | mitigate | Prove one provider handoff and one succeeded attempt while forbidding device-lifecycle claims. | `env MIX_ENV=test mix test test/chimeway/dispatch/executor_mailglass_adapter_test.exs --warnings-as-errors` |
| T-98-10-SC | Tampering | Package supply chain | low | accept | No dependency or package-manager operation occurs; Mailglass and Oban are existing locked optional integrations. | `mix ci.audit` |

ASVS L1: all high-severity identity/context disclosure and spoofing threats are mitigated and block on real worker/database evidence.
</threat_model>

## Artifacts this phase produces

- Changed internal symbol: a DeliveryPlanning execution-hydration seam that returns private in-memory recipient/render context without persistence.
- Changed worker path: `ObanWorker.perform/1` resolves host context only for an allowed email after terminal/orchestration and policy suppressed/deferred/retry short-circuits, immediately before `Executor.run_delivery/1`, while retaining delivery_id-only args.
- Changed executable contracts: resolver mismatch/absence privacy, capture-adapter hydration, real Oban-to-Mailglass success, sync/async return/trace parity, and the combined Phase 98 gap suite.
- No new durable field, migration, external API integration, package, environment variable, CLI flag, UI surface, proof schema, or human checkpoint is introduced. The existing host-owned `RenderContextResolver` behaviour and existing locked Oban/Mailglass seams are reused.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Explain delivery behavior without retaining/emitting sensitive endpoint, credential, identity, or content data | 98-08..10 | COVERED | Private sync/async context, safe public/storage/trace evidence, and fail-closed facts close every current regression. |
| REQ | PRIV-03 | Recursive, representation-independent privacy before storage and diagnostics | 98-09, 98-10 | COVERED | Duplicate/equal/keyword/order/empty edges plus the resolver boundary matrix are executable. |
| REQ | PRIV-04 | Only opaque refs, stable classifications, and allowlisted facts survive | 98-08..10 | COVERED | Render/recipient values are transient; provider codes and async evidence remain closed. |
| RESEARCH | — | One core privacy boundary and literal closed constructors | 98-09 | COVERED | Unified duplicate-aware logical lookup and code grammar strengthen SafeEvidence. |
| RESEARCH | — | Protect Trigger/Deliveries write and public projection boundaries | 98-08 | COVERED | Caller trust bypass removed; public/private shapes split; direct writes tested. |
| RESEARCH | — | Preserve host custody while retaining operational delivery behavior | 98-10 | COVERED | Existing host resolver reconstructs context at worker execution; no raw job/storage path. |
| RESEARCH | — | Test storage, trace, diagnostics, proof, and optional ecosystem seams with exact sentinels and positive facts | 98-08..10 | COVERED | Focused PostgreSQL/Oban/Mailglass regressions plus `mix ci`. |
| CONTEXT | D-01, D-02, D-03 | Atom-safe shared boundary, applied before writes/projections, with deterministic full-pair omission | 98-08, 98-09, 98-10 | COVERED | Duplicate-aware lookup plus write/public/worker boundaries. |
| CONTEXT | D-04, D-05, D-06 | Prohibit raw data, retain explicit facts, and never keep generic/redacted fallback blobs | 98-08..10 | COVERED | Non-persistent rendering, closed provider codes, and resolver failure omission. |
| CONTEXT | D-07, D-08, D-09 | Shared safe vocabulary, useful explanation, core-safe independently of Admin | 98-08, 98-10 | COVERED | Public results/traces keep positive facts and async uses the same core boundary. |
| CONTEXT | Deferred Ideas | None | — | EXCLUDED | Context records no deferred ideas; later APNs/fan-out/open scope is unchanged. |

Prohibition recall: the plan set retains three descriptor-less product prohibitions—no caller-forgeable trust/public private-context leak, no duplicate-based value selection/provider-code laundering, and no raw async context in jobs/storage/diagnostics. They remain flagged-unverified until the executable tasks run; none was auto-dismissed. Generic OWASP/input-validation concerns are handled in the ASVS L1 threat models rather than minted as product prohibitions.

External-API detector result: `detected=false`. These plans reuse existing in-repository Oban, Mailglass, and host resolver integration seams and add no new external API/SDK/service, so no `COVERAGE.md` matrix or no-integration override artifact is required.

<verification>
Run worker hydration first, then the real Mailglass path and combined focused/full gates. All acceptance is machine-readable; do not request conversational UAT.
</verification>

<success_criteria>
- Queued email delivery reaches Mailglass through host-resolved private context.
- Raw context never enters jobs, lifecycle rows, public results, diagnostics, traces, or proof.
- Duplicate/provider-code and render-trust regressions remain closed.
- Focused suites and `mix ci` exit zero.
</success_criteria>

<output>
Create `.planning/phases/98-privacy-safe-delivery-evidence/98-10-SUMMARY.md` when done.
</output>
