---
phase: 98-privacy-safe-delivery-evidence
plan: 11
type: execute
wave: 9
depends_on: [98-10]
files_modified:
  - lib/chimeway/safe_evidence.ex
  - lib/chimeway/dispatch/oban_worker.ex
  - test/chimeway/dispatch/oban_worker_test.exs
autonomous: true
gap_closure: true
requirements: [PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-02/D-05/D-07/D-08/PRIV-03/PRIV-04: every queued email hydration failure records one bounded attempt before the worker asks Oban to retry, using outcome failed, error_class render_context_unavailable, and empty allowlisted provider facts."
    - "D-04/D-06/PRIV-04: resolver errors, malformed host context, raw recipient values, and rendered content never enter the attempt, delivery metadata, worker return, telemetry, logs, or trace."
    - "D-05/D-08/PRIV-04: retryable hydration failures leave the delivery failed and explainable; the final retry records its attempt and converges to cancelled with retries_exhausted."
    - "D-01/D-03/PRIV-03 edge contract: missing, nil, and malformed hydration results have the same categorical fail-closed outcome, and no representation or execution ordering can select raw diagnostic content."
  assumptions:
    - "The existing error_class field plus retries_exhausted lifecycle is the durable bounded vocabulary for unavailable execution context; adding render_context_unavailable to its closed value set requires no schema change."
  artifacts:
    - path: "lib/chimeway/dispatch/oban_worker.ex"
      provides: "Queued hydration-failure attempt recording, retry return, and final-attempt convergence"
    - path: "lib/chimeway/safe_evidence.ex"
      provides: "Closed render_context_unavailable attempt and telemetry classification"
    - path: "test/chimeway/dispatch/oban_worker_test.exs"
      provides: "PostgreSQL-backed missing-context retry and exhaustion regression"
  key_links:
    - from: "lib/chimeway/dispatch/oban_worker.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "hydration failure transitions through dispatched and records fixed temporary attempt evidence before map_outcome_to_oban_return/4"
      pattern: "Deliveries\\.(transition_status|record_attempt|exhaust_delivery)"
    - from: "test/chimeway/dispatch/oban_worker_test.exs"
      to: "lib/chimeway/traces.ex"
      via: "the exhaustion regression reloads the lifecycle and asserts the tenant-scoped explanation contains attempt and terminal facts"
      pattern: "Traces\\.explain_delivery"
  prohibitions:
    - statement: "A hydration failure must not reach an Oban retry or discard decision without first writing bounded durable attempt evidence."
      status: flagged-unverified
      verification: "mix format --check-formatted lib/chimeway/safe_evidence.ex lib/chimeway/dispatch/oban_worker.ex test/chimeway/dispatch/oban_worker_test.exs && env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors"
    - statement: "Resolver errors or host context must not be copied, inspected, logged, telemetered, traced, returned, or stored as a diagnostic fallback."
      status: flagged-unverified
      verification: "mix format --check-formatted lib/chimeway/safe_evidence.ex lib/chimeway/dispatch/oban_worker.ex test/chimeway/dispatch/oban_worker_test.exs && env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors"
    - statement: "Exhaustion must not erase the attempt history or replace stable lifecycle explanation with raw context."
      status: flagged-unverified
      verification: "mix format --check-formatted lib/chimeway/safe_evidence.ex lib/chimeway/dispatch/oban_worker.ex test/chimeway/dispatch/oban_worker_test.exs && env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors"
---

<objective>
Close the queued hydration-failure gap with a production end-to-end tracer from missing host context through durable attempt recording, Oban retry semantics, final exhaustion, and tenant-scoped operator explanation.

Purpose: Ensure an operator can explain why queued delivery never reached its adapter without Chimeway retaining or emitting the unavailable private context.
Output: A bounded worker failure path and executable retry/exhaustion evidence for PRIV-03 and PRIV-04.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-RESEARCH.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-PATTERNS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-10-SUMMARY.md

<interfaces>
- `DeliveryPlanning.hydrate_execution_delivery/1` returns `{:ok, in_memory_delivery}` or the categorical `{:error, :render_context_unavailable}` after collapsing resolver and rendering failures; it does not persist private context.
- `Executor.run_delivery/1` normally transitions a delivery to `:dispatched`, records exactly one attempt, and returns the recorded attempt plus converged delivery.
- `Deliveries.record_attempt/2` accepts the closed `SafeEvidence.attempt_attrs/1` vocabulary and atomically writes attempt numbering plus failed/terminal lifecycle state; `SafeEvidence` owns the accepted error-class set used by persistence, traces, and telemetry.
- `ObanWorker.map_outcome_to_oban_return/4` already maps a temporary attempt to `{:error, safe_reason}` before the budget is exhausted and calls `Deliveries.exhaust_delivery/1` on the final attempt.
- `Traces.explain_delivery/2` is the tenant-scoped operator read seam and must expose only the recorded stable facts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trace missing render context through durable retries and exhaustion</name>
  <reversibility rating="reversible">The change is confined to the internal worker failure branch and reuses existing attempt and exhaustion contracts without changing schema or public API.</reversibility>
  <read_first>
    - .planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
    - .planning/phases/98-privacy-safe-delivery-evidence/98-10-PLAN.md
    - .planning/phases/98-privacy-safe-delivery-evidence/98-10-SUMMARY.md
    - lib/chimeway/dispatch/oban_worker.ex
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/dispatch/executor.ex
    - lib/chimeway/deliveries.ex
    - lib/chimeway/delivery_attempt.ex
    - lib/chimeway/traces.ex
    - test/chimeway/dispatch/oban_worker_test.exs
  </read_first>
  <files>lib/chimeway/safe_evidence.ex, lib/chimeway/dispatch/oban_worker.ex, test/chimeway/dispatch/oban_worker_test.exs</files>
  <behavior>
    - Test 1: an eligible queued email with no configured render-context resolver records attempt 1 as outcome failed, error_class render_context_unavailable, nil provider reference, and empty provider facts before returning the categorical retry error.
    - Test 2: resolver absence, explicit resolver error, recipient-reference mismatch, and malformed resolved context serialize to no raw resolver/context/recipient/render sentinel while retaining delivery ID, attempt number, failed status, and temporary classification.
    - Test 3: performing the missing-context job across its complete attempt budget records one contiguous safe attempt per invocation; the final invocation returns :ok and leaves the delivery cancelled with suppression_reason retries_exhausted.
    - Test 4: the tenant-scoped trace after exhaustion contains the stable failed-attempt timeline and cancelled/retries_exhausted lifecycle outcome, with no raw context or provider values.
  </behavior>
  <action>Start RED in `ObanWorkerTest` with a real email delivery carrying render identity, a resolver configuration that is absent or returns hostile private context in its error path, and exact sentinel assertions. Add `"render_context_unavailable"` to `SafeEvidence`'s closed error-class vocabulary so attempt persistence, trace projection, and telemetry retain the same exact internal classification without accepting arbitrary strings. In `ObanWorker.do_dispatch/3`, replace the hydration `with` fall-through with an explicit categorical branch. On `{:error, :render_context_unavailable}`, use the same durable spine as adapter failures: transition the fresh row to `:dispatched`, call `Deliveries.record_attempt/2` exactly once with literal closed attrs (`outcome: :failed`, `error_class: "render_context_unavailable"`, empty `provider_response`, nil provider message reference), then feed the returned attempt/delivery to `map_outcome_to_oban_return/4`. Extend only its retryable-class guard so both adapter `"temporary"` and hydration `"render_context_unavailable"` use the existing retry budget and final `Deliveries.exhaust_delivery/1`; do not add a second exhaustion implementation. Return a fixed categorical retry atom/tuple derived from the recorded class, never the resolver term. Propagate database/transition errors using the established safe tuple shapes. Do not attach the resolver error term, resolved context, recipient, rendering, or a generic sanitized map to attempt attrs, delivery metadata, worker returns, telemetry, logs, or trace output, per D-02 and D-04 through D-08. Keep terminal, policy, digest, and non-email short-circuits unchanged. Assert positive lifecycle facts before negative sentinel checks so the regression cannot pass by writing nothing.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/safe_evidence.ex lib/chimeway/dispatch/oban_worker.ex test/chimeway/dispatch/oban_worker_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Executing attempt 1 of a missing-context job exits with the safe retry result and a query finds exactly one attempt with `:failed`, `"render_context_unavailable"`, nil provider reference, and `%{}` provider facts.
    - Executing the full configured attempt budget exits zero on the final invocation, queries contiguous attempt numbers, and reloads `status: :cancelled` plus `suppression_reason: "retries_exhausted"`.
    - `Traces.explain_delivery/2` returns the expected stable attempt and terminal facts for the correct tenant, while serialized rows, result, captured diagnostics, and explanation contain none of the seeded raw sentinels.
    - `env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors` exits zero.
  </acceptance_criteria>
  <done>Every queued render-context hydration failure produces bounded durable evidence before retry, and exhaustion remains safely explainable without raw context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host resolver → worker | An untrusted failure term or malformed private context reaches the queued execution boundary. |
| Worker → lifecycle storage | The worker converts failure into one closed attempt and a bounded delivery transition. |
| Lifecycle storage → operator trace | Tenant-scoped reads expose stable classification and exhaustion facts without raw context. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-98-11-01 | Repudiation | `ObanWorker.do_dispatch/3` hydration error branch | high | mitigate | Persist one numbered temporary attempt before returning to Oban and prove final exhaustion in PostgreSQL. | `env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors` |
| T-98-11-02 | Information Disclosure | resolver failure handling | high | mitigate | Collapse all resolver/context failures to literal attempt attrs and scan rows, returns, captured diagnostics, and trace for exact sentinels. | `env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors` |
| T-98-11-03 | Tampering | retry attempt ordering | medium | mitigate | Reuse locked attempt numbering and assert one contiguous row per worker invocation before exhaustion. | `env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors` |
| T-98-11-04 | Denial of Service | retry budget | medium | mitigate | Route the fixed temporary attempt through the existing max-attempt guard and durable retries_exhausted convergence. | `env MIX_ENV=test mix test test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors` |
| T-98-11-SC | Tampering | Package supply chain | low | accept | No package-manager operation or dependency change occurs. | `mix ci.audit` |

ASVS L1 disposition: both high-severity repudiation and disclosure threats are mitigated and block completion on real worker/database/trace evidence.
</threat_model>

## Artifacts this phase produces

- Changed closed vocabulary and worker branch: `Chimeway.SafeEvidence` accepts the exact internal `render_context_unavailable` classification, and `Chimeway.Dispatch.ObanWorker` records it before using existing retry/exhaustion mapping.
- Changed executable contract: `ObanWorkerTest` proves first-attempt evidence, full-budget exhaustion, tenant-scoped trace explanation, and sentinel absence for unavailable/malformed host context.
- No new schema, migration, public API, package, environment variable, UI surface, provider capability, or human checkpoint is introduced.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Explain delivery behavior without retaining/emitting endpoint, credential, identity, or content data | 98-11 Task 1 | COVERED | Missing queued context gains durable safe explanation through exhaustion. |
| REQ | PRIV-03 | Apply deterministic fail-closed privacy handling before storage and diagnostics | 98-11 Task 1 | COVERED | Missing/nil/malformed results collapse to one fixed representation-independent outcome. |
| REQ | PRIV-04 | Retain only opaque references, stable classifications, and allowlisted facts | 98-11 Task 1 | COVERED | The attempt contains only the closed render-context classification and empty provider facts. |
| RESEARCH | — | Durable explainability uses explicit lifecycle facts instead of diagnostic blobs | 98-11 Task 1 | COVERED | Existing record_attempt and exhaustion contracts provide the evidence. |
| RESEARCH | — | Failure paths do not inspect arbitrary host/provider terms | 98-11 Task 1 | COVERED | Exact sentinel scans cover rows, returns, diagnostics, and trace. |
| CONTEXT | D-01, D-02, D-03 | One atom-safe boundary with deterministic omission at writes/projections | 98-11 Task 1 | COVERED | This branch creates no new map ingress and converts every hydration error to literal attrs. |
| CONTEXT | D-04, D-05, D-06 | Prohibit raw data, retain explicit facts, and forbid generic/redacted fallbacks | 98-11 Task 1 | COVERED | Host context is omitted; attempt plus exhaustion are the safe facts. |
| CONTEXT | D-07, D-08, D-09 | Share safe evidence across core persistence and operator projection | 98-11 Task 1 | COVERED | Core attempt and trace seams are exercised without optional Admin repair. |
| CONTEXT | Deferred Ideas | None | — | EXCLUDED | No deferred Phase 98 idea exists. |

Prohibition recall: the missing-evidence, raw-context, and exhaustion-history must-NOT constraints remain descriptor-less flagged-unverified entries until the executor produces the stated database and trace evidence.

<verification>
Run the focused worker suite with warnings as errors. Re-verification must inspect a real first-attempt retry and a complete exhausted lifecycle, including positive stable facts and negative sentinel absence.
</verification>

<success_criteria>
- A queued hydration failure writes one bounded attempt before every retry decision.
- The final allowed attempt is recorded before the delivery converges to cancelled/retries_exhausted.
- A tenant-scoped trace explains the attempt history and terminal reason without raw resolver, recipient, render, or provider values.
- The focused automated command exits zero; no UAT or human verification is requested.
</success_criteria>

<output>
Create `.planning/phases/98-privacy-safe-delivery-evidence/98-11-SUMMARY.md` when done.
</output>
