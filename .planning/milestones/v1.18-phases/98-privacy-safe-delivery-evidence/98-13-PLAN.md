---
phase: 98-privacy-safe-delivery-evidence
plan: 13
type: execute
wave: 11
depends_on: [98-12]
files_modified:
  - test/chimeway/integration/delivery_lifecycle_test.exs
autonomous: true
gap_closure: true
requirements: [PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-02/D-04/D-05/PRIV-04: every Lifecycle* notifier supplies an explicit documented cw_... opaque recipient reference, so the strict recipient boundary admits the fixture without retaining a raw user:<id> identity."
    - "D-07/D-08/D-09/PRIV-04: all fifteen lifecycle scenarios reach and preserve their intended event, notification, delivery, attempt, planning, recovery, render, trace, and workflow assertions using the same opaque recipient reference."
    - "D-01/D-03/PRIV-03: recipient-dependent queries and policy-setting fixtures use the exact opaque value supplied by their notifier instead of relying on a stale alternate representation."
    - "The exact Phase 98 focused 11-file regression matrix exits zero with --warnings-as-errors."
  assumptions:
    - statement: "Spec-less edge probe row PRIV-03 adjacency remains unresolved by a fixture-only repair and is not claimed as newly covered here."
      status: flagged-unverified
    - statement: "Spec-less edge probe row PRIV-03 empty remains unresolved by a fixture-only repair and is not claimed as newly covered here."
      status: flagged-unverified
    - statement: "Spec-less edge probe row PRIV-03 ordering remains unresolved by a fixture-only repair and is not claimed as newly covered here."
      status: flagged-unverified
    - statement: "Spec-less edge probe row PRIV-04 unclassified remains unresolved by a fixture-only repair and is not claimed as newly covered here."
      status: flagged-unverified
  artifacts:
    - path: "test/chimeway/integration/delivery_lifecycle_test.exs"
      provides: "Opaque-recipient lifecycle fixtures and the restored Phase 98 lifecycle regression evidence"
  key_links:
    - from: "test/chimeway/integration/delivery_lifecycle_test.exs"
      to: "lib/chimeway/safe_evidence.ex"
      via: "every Lifecycle* notifier recipient crosses the production recipient_reference/1 contract through Trigger"
      pattern: "cw_lifecycle"
    - from: "Lifecycle* notifier recipient fixtures"
      to: "lifecycle queries and policy settings"
      via: "the same cw_... value is used wherever each scenario correlates its Notification or Settings row"
      pattern: "recipient_(ref|identity|id)"
  prohibitions:
    - statement: "Lifecycle notifier fixtures must not supply raw numeric user identities or depend on an implicit conversion into an opaque reference."
      status: flagged-unverified
      verification: "env MIX_ENV=test mix test test/chimeway/integration/delivery_lifecycle_test.exs --warnings-as-errors"
    - statement: "The fixture repair must not delete, relax, skip, or rewrite lifecycle assertions merely to avoid the strict recipient boundary."
      status: flagged-unverified
      verification: "env MIX_ENV=test mix test test/chimeway/integration/delivery_lifecycle_test.exs --warnings-as-errors"
---

<objective>
Restore the Phase 98 lifecycle regression evidence by moving every Lifecycle* notifier fixture onto the documented opaque recipient-reference contract.

Purpose: Close the sole verification regression without weakening the strict production privacy boundary or the lifecycle behaviors the suite proves.
Output: One corrected lifecycle integration fixture file and a zero-exit Phase 98 focused regression matrix for PRIV-03 and PRIV-04.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-12-PLAN.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-12-SUMMARY.md

<interfaces>
- `Chimeway.SafeEvidence.recipient_reference/1` accepts a bounded documented `cw_...` opaque reference or the canonical lowercase-UUID compatibility form and otherwise returns `{:error, :unsafe_evidence}`.
- `Trigger.normalize_recipients/1` accepts an explicit notifier `:recipient_ref`, validates it through `recipient_reference/1`, and persists the accepted value as `Notification.recipient_identity`.
- A documented lifecycle fixture reference may use the deterministic shape `cw_lifecycle_user_<integer>`; it satisfies the existing lowercase opaque-reference grammar and remains non-sensitive test correlation data.
- The binding failed evidence is the exact Phase 98 focused matrix recorded by verification: eleven privacy, worker, Trigger, Inbox, Workflow, lifecycle, Mailglass, migration, and proof test files with warnings treated as errors.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore every lifecycle scenario with explicit opaque recipient fixtures</name>
  <read_first>
    - .planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
    - .planning/phases/98-privacy-safe-delivery-evidence/98-12-SUMMARY.md
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/trigger.ex
    - test/chimeway/integration/delivery_lifecycle_test.exs
  </read_first>
  <files>test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <action>Update all ten module-level `ChimewayTest.Notifiers.Lifecycle*` recipient callbacks—LifecycleA, LifecycleB, LifecycleC, LifecycleFanout, LifecycleDelayedFallback, LifecycleNoDelayedFallback, LifecycleCustomChannel, LifecycleDigestHeld, LifecycleRenderedEmail, and LifecycleWorkflow—to supply an explicit `recipient_ref` using one deterministic documented `cw_lifecycle_user_<user_id>` value per scenario. Apply the matching value to every recipient-dependent Notification query and to each Settings fixture whose policy lookup must match the triggered recipient. Also replace any direct lifecycle support fixture in this file that still needs a recipient with the same explicit opaque-reference vocabulary when it participates in the focused lifecycle path. Per D-02/D-04/D-05, do not change `SafeEvidence`, Trigger, or any production acceptance grammar to accommodate the tests. Per D-07/D-08/D-09, preserve every existing assertion about idempotency, fanout, attempts, delayed fallback, custom channels, suppression, deferred and digest-held states, resume/cancel behavior, safe render handling, recovery, traces, and workflow linkage; change only fixture identity representation and the exact correlation values that must follow it. Run formatting, then the exact eleven-file Phase 98 focused matrix and require a zero exit with warnings as errors.</action>
  <verify>
    <automated>mix format --check-formatted test/chimeway/integration/delivery_lifecycle_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/privacy_test.exs test/chimeway/privacy_boundary_test.exs test/chimeway/dispatch/oban_worker_test.exs test/chimeway/trigger_sanitization_test.exs test/chimeway/inbox_query_test.exs test/chimeway/inbox_state_transition_test.exs test/chimeway/workflows_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/dispatch/executor_mailglass_adapter_test.exs test/chimeway/migration_contract_test.exs test/chimeway/release_gate_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - All ten Lifecycle* notifier modules supply explicit documented `cw_...` recipient references rather than raw user:<id> values.
    - Recipient-dependent queries, Settings rows, and direct lifecycle support fixtures use the exact corresponding opaque reference wherever scenario behavior depends on identity correlation.
    - The fifteen previously blocked lifecycle scenarios execute their original lifecycle assertions instead of returning `{:error, :unsafe_evidence}`.
    - No production file or strict recipient-reference rule is modified, and no lifecycle assertion is removed, skipped, or weakened.
    - Formatting and the exact eleven-file Phase 98 focused matrix exit zero with `--warnings-as-errors`.
  </acceptance_criteria>
  <done>Every Lifecycle* fixture reaches the production lifecycle through an explicit opaque recipient reference, and the complete Phase 98 focused regression matrix is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Lifecycle notifier fixture → Trigger | Test-owned recipient input crosses the same strict production validation boundary used by adopters. |
| Trigger persistence → lifecycle assertions | Persisted opaque reference must match the reference used by queries, policy settings, traces, and workflow correlation. |
| Fixture repair → verification evidence | Test-only edits must restore real behavior without weakening production privacy or deleting assertions. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-98-13-01 | Information Disclosure | Lifecycle* recipient fixtures | high | mitigate | Supply only documented `cw_...` opaque references and keep production validation unchanged. | `env MIX_ENV=test mix test test/chimeway/integration/delivery_lifecycle_test.exs --warnings-as-errors` |
| T-98-13-02 | Tampering | Recipient-dependent lifecycle queries/settings | high | mitigate | Update every correlation site to the exact notifier-supplied opaque value so assertions exercise the intended rows rather than bypassing identity checks. | `env MIX_ENV=test mix test test/chimeway/integration/delivery_lifecycle_test.exs --warnings-as-errors` |
| T-98-13-03 | Repudiation | Phase 98 regression evidence | high | mitigate | Preserve all lifecycle assertions and require the binding eleven-file matrix to exit zero with warnings as errors. | `env MIX_ENV=test mix test test/chimeway/privacy_test.exs test/chimeway/privacy_boundary_test.exs test/chimeway/dispatch/oban_worker_test.exs test/chimeway/trigger_sanitization_test.exs test/chimeway/inbox_query_test.exs test/chimeway/inbox_state_transition_test.exs test/chimeway/workflows_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/dispatch/executor_mailglass_adapter_test.exs test/chimeway/migration_contract_test.exs test/chimeway/release_gate_contract_test.exs --warnings-as-errors` |
| T-98-13-SC | Tampering | Package supply chain | low | accept | No package-manager operation or dependency change occurs. | `git diff -- test/chimeway/integration/delivery_lifecycle_test.exs` |

ASVS L1 disposition: all high-severity disclosure, tampering, and repudiation threats block completion on executable lifecycle and full focused-matrix evidence.
</threat_model>

## Artifacts this phase produces

- New symbols/files: None.
- Modified executable evidence: `test/chimeway/integration/delivery_lifecycle_test.exs` uses explicit opaque recipient fixtures across all Lifecycle* notifiers and their correlation assertions.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Explain delivery behavior without retaining or emitting sensitive identity data | 98-13 Task 1 | COVERED | Lifecycle proof uses opaque references while preserving operator explanation assertions. |
| REQ | PRIV-03 | Case-normalized recursive safety before persistence and diagnostic surfaces | 98-01 through 98-12; regression matrix in 98-13 | COVERED | This gap plan restores the binding matrix; the four spec-less edge rows remain flagged assumptions, not newly claimed coverage. |
| REQ | PRIV-04 | Raw recipient/adopter data never enters Chimeway storage or diagnostics | 98-13 Task 1 | COVERED | Stale raw lifecycle recipients are replaced with documented opaque fixture references. |
| RESEARCH | — | Host supplies a pre-opaque bounded reference; Chimeway validates and persists it exactly | 98-13 Task 1 | COVERED | Notifier fixtures now model the resolved production contract directly. |
| RESEARCH | — | Closed evidence remains operationally explainable through lifecycle facts | 98-13 Task 1 | COVERED | Existing lifecycle, trace, render, recovery, and workflow assertions are retained. |
| CONTEXT | D-01, D-02, D-03 | One deterministic boundary applied before writes and projections | 98-01 through 98-12; regression matrix in 98-13 | COVERED | The strict implementation stays unchanged; exact fixture/query values restore its phase proof. |
| CONTEXT | D-04, D-05, D-06 | Prohibit raw identity and retain only explicit opaque facts without fallback blobs | 98-13 Task 1 | COVERED | Fixture inputs use explicit `cw_...` references and introduce no fallback representation. |
| CONTEXT | D-07, D-08, D-09 | Core-safe explainability retains lifecycle facts without downstream repair | 98-13 Task 1 | COVERED | All original lifecycle and trace assertions continue through production boundaries. |
| CONTEXT | Deferred Ideas | None | — | EXCLUDED | No deferred Phase 98 idea exists. |

The spec-less PRIV-03 adjacency, empty, and ordering rows and PRIV-04 unclassified row remain explicit flagged-unverified assumptions; this fixture-only gap closure does not silently resolve or expand them.

<verification>
Run formatting for the sole modified file, then execute the binding eleven-file Phase 98 focused matrix with `--warnings-as-errors`. Accept only exit zero; do not substitute conversational UAT, an isolated partial suite, or a result obtained by skipping lifecycle cases.
</verification>

<success_criteria>
- Every module-level Lifecycle* notifier supplies an explicit documented opaque recipient reference.
- All recipient-dependent fixture correlations use the same opaque values.
- The original lifecycle behavior assertions remain intact and execute.
- The exact Phase 98 focused eleven-file matrix exits zero with warnings as errors.
- PRIV-03 and PRIV-04 have machine-readable gap-closure evidence with no human verification checkpoint.
</success_criteria>

<output>
Create `.planning/phases/98-privacy-safe-delivery-evidence/98-13-SUMMARY.md` when done.
</output>
