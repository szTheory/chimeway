---
phase: 98-privacy-safe-delivery-evidence
plan: 08
type: execute
wave: 7
depends_on: [98-07]
files_modified:
  - lib/chimeway/trigger.ex
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/deliveries.ex
  - test/chimeway/integration/delivery_lifecycle_test.exs
  - test/chimeway/trigger_sanitization_test.exs
  - test/chimeway/orchestration/delivery_planning_test.exs
  - test/chimeway/deliveries_test.exs
autonomous: true
gap_closure: true
requirements: [PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-02/D-04/D-06/PRIV-04: rendered subject/body/recipient values may exist only in an in-process dispatch context; a reloaded Delivery and both trace APIs contain render identity but no rendered payload."
    - "D-04/D-07/D-09/PRIV-04: Trigger.trigger/3 returns an explicit safe allowlist and omits raw recipients, precomputed rendering, recipient handoffs, and every rendered-content sentinel on success and dispatch-error paths."
    - "D-05/D-08: synchronous dispatch still receives the rendered payload exactly once and operators retain delivery IDs, dispatch outcome/mode, status, classification, timestamps, and render key/version."
    - "D-02/D-04: no public option or direct planning/update call can mark rendered data trusted or make it durable."
    - "PRIV-03 empty edge: nil, empty, or absent transient rendering/handoff context yields the same safe public and durable shapes without a raw fallback."
  artifacts:
    - path: "lib/chimeway/trigger.ex"
      provides: "Private dispatch-context/public-result split"
    - path: "lib/chimeway/delivery_planning.ex"
      provides: "Transient rendered payload attached only to the in-memory delivery sent to the adapter"
    - path: "lib/chimeway/deliveries.ex"
      provides: "Render identity-only persistence with no trusted-render bypass"
    - path: "test/chimeway/integration/delivery_lifecycle_test.exs"
      provides: "Trigger-to-sync-adapter storage, return-shape, and trace privacy tracer"
  key_links:
    - from: "lib/chimeway/trigger.ex"
      to: "lib/chimeway/delivery_planning.ex"
      via: "private dispatch context passed after the Trigger transaction, never copied into the public result"
      pattern: "dispatch_after_trigger"
    - from: "lib/chimeway/delivery_planning.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "persist render identity, then attach payload only to the returned in-memory struct"
      pattern: "apply_render_identity"
  prohibitions:
    - statement: "Caller-controlled options must never authorize raw render or recipient persistence or become an internal trust capability."
      status: unverified
      verification: flagged
    - statement: "A public Trigger result must never become a transport for raw recipients, rendered content, or private dispatch handoffs."
      status: unverified
      verification: flagged
    - statement: "Removing durable render bodies must not cause a second synchronous render callback or erase safe render identity and lifecycle explanation."
      status: unverified
      verification: flagged
---

<objective>
Close the caller-forgeable render persistence bypass through one production Trigger-to-sync-adapter tracer, then lock every direct render-write seam to identity-only storage.

Purpose: Preserve real delivery behavior while making raw rendering and recipient context private, short-lived, and absent from Chimeway-owned storage, traces, and public return values.
Output: A private Trigger dispatch context, safe public Trigger result, non-persistent render handoff, and negative regression coverage for public and direct write paths.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-REVIEW.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-07-SUMMARY.md

<interfaces>
- `Trigger.trigger/3` currently normalizes transaction output into one map used both as the public return and as private dispatch input; this plan separates those responsibilities while preserving duplicate/error behavior.
- `DeliveryPlanning.plan_one_channel/5` currently derives trust from caller-visible `:precomputed_rendering` and forwards `trusted_render_data` into `Deliveries.plan_delivery/3`.
- `Deliveries.apply_render_identity/2` already persists render key/version without content; it is the durable seam to retain.
- `Delivery.render_data` remains a physical compatibility column but must contain only the closed `SafeEvidence.render_data/1` projection, which excludes subject/body/recipient payloads.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tracer — dispatch one rendered email without persisting or returning its payload</name>
  <reversibility rating="costly">The Trigger return shape and internal dispatch handoff are shared contracts; undo requires coordinated core and adopter-call-site changes, but no schema or stored-data migration.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
    - .planning/phases/98-privacy-safe-delivery-evidence/98-REVIEW.md
    - lib/chimeway/trigger.ex
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/deliveries.ex
    - lib/chimeway/delivery.ex
    - lib/chimeway/traces.ex
    - test/chimeway/integration/delivery_lifecycle_test.exs
    - test/chimeway/trigger_sanitization_test.exs
  </read_first>
  <files>lib/chimeway/trigger.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/deliveries.ex, test/chimeway/integration/delivery_lifecycle_test.exs, test/chimeway/trigger_sanitization_test.exs</files>
  <behavior>
    - Test 1: a real rendered-email Trigger call invokes rendering once, the synchronous adapter receives subject/html/text/recipient in memory, and the reloaded Delivery has render key/version with `render_data == %{}`.
    - Test 2: the returned success map contains the established safe event/notification/dispatch/trace facts but has no raw `recipients`, `precomputed_rendering`, `recipient_handoffs`, email address, subject, or body.
    - Test 3: `Traces.get_trace/2` and `Traces.explain_delivery/2` serialize without the rendering/recipient sentinels while retaining render identity, status, attempt outcome, and delivery ID.
    - Test 4: a dispatch error returns the same safe public allowlist and bounded error outcome without private context or sentinels.
    - Test 5: nil/empty rendering or handoff inputs keep the public result safe and do not crash planning.
  </behavior>
  <action>Begin RED by replacing Scenario J's persisted-body assertion with the complete production tracer and by adding the public-result assertions. Per D-02/D-04/D-07/D-09, change Trigger's internal transaction-normalization flow so the raw normalized recipients, precomputed rendering, and recipient handoffs live in a private dispatch-context value consumed only by `dispatch_after_trigger/4`; build the caller-visible result from literal safe fields and never include `recipients` or either handoff map. Preserve `{:duplicate, event}` and existing error tuples. Strip caller-supplied internal handoff keys before building dispatch options so public opts cannot replace the private values; do not use `Keyword.put_new/3` for these internals.

Remove the `trusted_render_data?` derivation and `trusted_render_data` option from DeliveryPlanning/Deliveries. Persist render key/version through the existing identity seam and always project any `render_data` through the closed durable builder. After the row is persisted, attach the full rendered map only to the returned in-memory Delivery passed to the immediate dispatcher; never call `Repo.update` with that map. Keep one-render-call behavior, policy/orchestration order, delivery IDs, dispatch outcome/mode, telemetry, and error shapes unchanged. Assert exact sentinel presence in the adapter-captured in-memory value before proving absence from the reloaded row, public result, raw trace preload, and Explanation so the negative evidence cannot pass vacuously.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/trigger.ex lib/chimeway/delivery_planning.ex lib/chimeway/deliveries.ex test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/trigger_sanitization_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/trigger_sanitization_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The synchronous adapter receives the exact rendered payload and recipient once, but a fresh Repo reload has empty closed render data.
    - Trigger success and dispatch-error returns are literal safe projections with no private keys, raw recipient collection, or sentinel value.
    - Both trace APIs retain safe render/lifecycle facts and contain no rendered or recipient sentinel.
    - No caller-controlled option determines whether render data is trusted or persisted.
    - The focused lifecycle and Trigger suites exit zero with warnings as errors.
  </acceptance_criteria>
  <done>One real rendered email is delivered synchronously from private transient context while public results, storage, and traces remain privacy-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject every direct render-persistence bypass</name>
  <read_first>
    - lib/chimeway/deliveries.ex
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/safe_evidence.ex
    - test/chimeway/orchestration/delivery_planning_test.exs
    - test/chimeway/deliveries_test.exs
    - test/chimeway/integration/delivery_lifecycle_test.exs
  </read_first>
  <files>test/chimeway/orchestration/delivery_planning_test.exs, test/chimeway/deliveries_test.exs, test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <behavior>
    - Test 1: direct `Deliveries.plan_delivery/3` with hostile render content and a legacy/caller-supplied trust option reloads with identity-only evidence and no body/recipient.
    - Test 2: direct `Deliveries.apply_render_result/2` with the same hostile content updates render identity only; the database column remains closed.
    - Test 3: direct `DeliveryPlanning.plan_notification/2` with forged precomputed rendering may return an in-memory handoff but cannot change the reloaded row.
    - Test 4: absent, nil, empty, singleton, and mixed atom/string render maps all remain non-persistent without losing valid render key/version.
  </behavior>
  <action>Replace lifecycle/planning assertions that encode durable subject/body content and add direct regression cases at both public Deliveries seams and the planning seam. Exercise the former `trusted_render_data` spelling explicitly to prove it has no authority, then reload through Repo before every storage assertion. Cover nil/empty/singleton render inputs and both atom/string field representations. Retain positive render identity assertions and prove any transient returned struct is not mistaken for durable state. Do not add a compatibility flag, redacted body archive, or generic diagnostic fallback per D-04/D-05/D-06.</action>
  <verify>
    <automated>mix format --check-formatted test/chimeway/orchestration/delivery_planning_test.exs test/chimeway/deliveries_test.exs test/chimeway/integration/delivery_lifecycle_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/orchestration/delivery_planning_test.exs test/chimeway/deliveries_test.exs test/chimeway/integration/delivery_lifecycle_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - No direct planning or render-update call can persist subject, body, recipient, endpoint, or arbitrary render fields.
    - The former trust option is demonstrably inert/removed rather than renamed into another caller capability.
    - Empty/null/singleton and atom/string forms retain the same identity-only durable result.
    - Focused planning, delivery, and lifecycle suites exit zero.
  </acceptance_criteria>
  <done>Every public and internal render-write seam enforces identity-only durable evidence regardless of caller options or input representation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host Trigger input/options → Trigger transaction and dispatcher | Caller-controlled values may attempt to impersonate internal rendering context. |
| Private render/recipient context → delivery persistence | Data required for adapter execution must not enter the durable row. |
| Trigger internals → public return | Private handoff data could leak into application logs or caller diagnostics. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-98-08-01 | Tampering | `DeliveryPlanning.plan_one_channel/5` | high | mitigate | Remove caller-derived trust state and prove direct/public calls cannot persist render payloads. | `env MIX_ENV=test mix test test/chimeway/orchestration/delivery_planning_test.exs test/chimeway/deliveries_test.exs --warnings-as-errors` |
| T-98-08-02 | Information Disclosure | `Trigger.trigger/3` return | high | mitigate | Split private dispatch context from a literal public allowlist and scan both success/error returns for exact sentinels. | `env MIX_ENV=test mix test test/chimeway/trigger_sanitization_test.exs --warnings-as-errors` |
| T-98-08-03 | Information Disclosure | Delivery row and trace preload | high | mitigate | Persist render identity only, reload from PostgreSQL, and scan both trace APIs. | `env MIX_ENV=test mix test test/chimeway/integration/delivery_lifecycle_test.exs --warnings-as-errors` |
| T-98-08-04 | Repudiation | Safe delivery explanation | medium | mitigate | Positive assertions retain render identity, dispatch mode/outcome, delivery ID, status, and attempt facts. | `env MIX_ENV=test mix test test/chimeway/integration/delivery_lifecycle_test.exs --warnings-as-errors` |
| T-98-08-SC | Tampering | Package supply chain | low | accept | No dependency or package-manager operation occurs. | `mix ci.audit` |

ASVS L1: all high-severity disclosure/tampering threats are mitigated and block on PostgreSQL-backed production-path tests.
</threat_model>

## Artifacts this phase produces

- Changed internal flow: Trigger transaction output is separated into a private dispatch context and a safe public result.
- Changed symbols: `DeliveryPlanning.plan_one_channel/5` render handoff, `Deliveries.plan_delivery/3` render normalization, and `Deliveries.apply_render_result/2` identity-only persistence.
- Changed executable contracts: Scenario J, Trigger return-shape privacy, direct trusted-option rejection, raw trace/storage negative assertions, and empty/null/singleton rendering edges.
- No new public API, durable field, migration, external API, package, environment variable, CLI flag, UI surface, or human checkpoint is introduced.

<verification>
Run the tracer first, then direct bypass regressions. Both are objectively machine-testable and require no UAT.
</verification>

<success_criteria>
- Rendered payload and recipient context are transient and usable for immediate dispatch, never durable.
- Trigger returns only safe explainability facts.
- Direct callers cannot forge trust at any render-write seam.
- Focused suites exit zero with warnings as errors.
</success_criteria>

<output>
Create `.planning/phases/98-privacy-safe-delivery-evidence/98-08-SUMMARY.md` when done.
</output>
