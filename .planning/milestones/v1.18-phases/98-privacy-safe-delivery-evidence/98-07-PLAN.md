---
phase: 98-privacy-safe-delivery-evidence
plan: 07
type: execute
wave: 6
depends_on: [98-06]
files_modified:
  - lib/chimeway/safe_evidence.ex
  - lib/chimeway/deliveries.ex
  - test/chimeway/trigger_sanitization_test.exs
  - test/chimeway/privacy_boundary_test.exs
  - test/chimeway/traces_test.exs
  - test/chimeway/orchestration/digest_explainability_test.exs
autonomous: true
gap_closure: true
requirements: [PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-04/D-05/D-06/PRIV-04: an allowlisted fact key cannot launder arbitrary recipient, credential, token, endpoint, link, content, or provider text into a Trigger row, digest row, delivery explanation, or trace timeline."
    - "D-07/D-08/PRIV-04: closed facts and digest evidence validate each field with its own code, enum, identifier, timestamp, integer, boolean, opaque-reference, or nested-entry grammar; unknown values are omitted or rejected without a generic binary fallback."
    - "D-02/D-09/PRIV-03: a real Trigger write and a real digest lifecycle prove privacy at core persistence and trace boundaries independently of optional Admin masking."
    - "PRIV-03 edge — equality/touch: exact allowed enum/code values survive, while case changes, prefixes, suffixes, delimiters, and near-miss values do not gain admission by touching an allowed value."
    - "PRIV-03 edge — empty/null/single: nil and empty inputs project no facts, and one valid field produces exactly that one field without admitting siblings."
    - "PRIV-03 edge — stable ordering: equivalent facts supplied through atom/string keys or equal-value input orderings produce the same closed projection without first/last-wins leakage."
    - "PRIV-04 unclassified/manual-review edge: unclassified diagnostic text is not retained for later review; the machine-safe result is omission/rejection or an existing stable closed classification, and no manual-review payload channel is introduced."
    - "D-08/PRIV-04: a delivery completed through `Deliveries.mark_digested/4` is explained by `Traces.explain_delivery/2` with `status: :digested` and safe categorical digest facts."
  artifacts:
    - path: "lib/chimeway/safe_evidence.ex"
      provides: "Field-specific closed fact/digest value validators and complete delivery status projection"
      contains: ":digested"
    - path: "lib/chimeway/deliveries.ex"
      provides: "Fail-closed digest-resolution reason normalization before durable update"
    - path: "test/chimeway/trigger_sanitization_test.exs"
      provides: "Adversarial approved-key value persistence regression"
    - path: "test/chimeway/traces_test.exs"
      provides: "Real digested delivery explanation and hostile digest trace regression"
  key_links:
    - from: "lib/chimeway/trigger.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "event_changeset/5 projects caller params through event_payload/1 before Event persistence"
      pattern: "SafeEvidence.event_payload"
    - from: "lib/chimeway/deliveries.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "digest resolution normalizes its categorical reason before Repo.update_all"
      pattern: "SafeEvidence"
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "explain_delivery/2 sends real status and digest context through the closed trace projection"
      pattern: "SafeEvidence.trace"
  prohibitions:
    - "FLAGGED-UNVERIFIED — PRIV-03/PRIV-04: an approved key must not become a covert channel for arbitrary identity, credential, endpoint, content, or provider-controlled text."
    - "FLAGGED-UNVERIFIED — D-06/PRIV-04: missing or invalid categorical evidence must not fall back to a redacted source blob, a generic diagnostic map, or an invented safe-looking explanation."
    - "FLAGGED-UNVERIFIED — D-08/PRIV-04: closing the privacy gap must not erase valid lifecycle status or categorical explanation needed to distinguish a digested delivery."
---

<objective>
Close the two Phase 98 verification blockers through one production path: reject sensitive values even when their keys are allowlisted, keep digest evidence closed at write and trace boundaries, and preserve the real `:digested` terminal status.

Purpose: Make the shared evidence vocabulary value-safe rather than key-safe while retaining the categorical facts operators need to explain digest behavior.
Output: Hardened `SafeEvidence` field grammars, safe digest-resolution persistence, adversarial Trigger/digest regressions, and a real digested-delivery explanation test.
</objective>

<objective_acceptance_criteria>
- A Trigger call carrying an email/token sentinel under approved fact keys persists none of those values while retaining separately supplied valid categorical evidence.
- A digest resolution carrying hostile free text persists and emits no hostile value, and its trace is built only from validated digest fields and validated nested resolution entries.
- `Traces.explain_delivery/2` returns `status: :digested` for a real delivery completed through `Deliveries.mark_digested/4`.
- Focused Phase 98 regression commands and `mix ci` exit zero without conversational UAT or a human checkpoint.
</objective_acceptance_criteria>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-RESEARCH.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-PATTERNS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-01-SUMMARY.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-02-SUMMARY.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-04-SUMMARY.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-06-SUMMARY.md

<interfaces>
- `SafeEvidence.event_payload/1`, `notification_metadata/1`, `planning_context/1`, `delivery_metadata/1`, and `render_data/1` currently delegate to `closed_facts/2`; their output remains a literal-key map, but acceptance becomes field-specific rather than one generic scalar predicate.
- `Trigger.event_changeset/5` already persists `SafeEvidence.event_payload/1` output. The gap closes at the shared constructor and is proven through the real Trigger transaction; do not add a second Trigger-local scrubber.
- `Deliveries.mark_digested/4`, `mark_digest_skipped/4`, and `mark_digest_immediate/4` converge on `resolve_digest_outcome/6`, the single write seam for `digest_flush_reason`.
- `Traces.explain_delivery/2` builds source and emitted digest maps, then calls `SafeEvidence.trace/1`; `safe_digest/1` must reconstruct the permitted digest shape instead of returning a recursively redacted input map.
- Existing digest outcomes are `digested`, `skipped_by_policy`, and `emitted_immediately`; existing membership-resolution categories are `included`, `skipped_by_policy`, and `emitted_immediately`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close approved-key values from Trigger persistence through digested trace explanation</name>
  <reversibility rating="costly">The shared evidence vocabulary is consumed by Trigger, Deliveries, traces, telemetry, Admin, and proof paths; undoing its field contracts would require a coordinated cross-surface change, but no schema migration or published wire-format break is introduced.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
    - .planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/trigger.ex
    - lib/chimeway/deliveries.ex
    - lib/chimeway/traces.ex
    - lib/chimeway/digests/emission.ex
    - test/chimeway/trigger_sanitization_test.exs
    - test/chimeway/privacy_boundary_test.exs
    - test/chimeway/traces_test.exs
    - test/chimeway/orchestration/digest_explainability_test.exs
  </read_first>
  <files>lib/chimeway/safe_evidence.ex, lib/chimeway/deliveries.ex, test/chimeway/trigger_sanitization_test.exs, test/chimeway/privacy_boundary_test.exs, test/chimeway/traces_test.exs, test/chimeway/orchestration/digest_explainability_test.exs</files>
  <behavior>
    - Test 1: Triggering with an email, reset-token text, bearer-shaped credential, URL, or rendered text under `reason`, `category`, and other approved keys stores none of those sentinels in the Event or Notification, while a valid category/code in the same request remains available.
    - Test 2: exact valid categorical values are accepted; nil, empty, near-miss/prefix/suffix/case variants, unknown strings, and duplicate atom/string spellings fail closed deterministically; one valid fact yields exactly one fact.
    - Test 3: a hostile digest-resolution reason cannot enter `digest_flush_reason`, the source digest map, emitted-digest resolution entries, or digest timeline detail; valid outcome, rule identity, opaque/lifecycle IDs, timestamps, and counts remain present.
    - Test 4: a real pending → digest-held → digested transition returns an explanation whose status is `:digested`, whose digest outcome is categorical, and whose serialized explanation contains no hostile approved-key value.
    - Test 5: an unclassified value has no retained manual-review payload; it is omitted/rejected or represented only by an existing stable classification code.
  </behavior>
  <action>Begin RED by adding the approved-key adversarial persistence case and the real digested `Traces.explain_delivery/2` case, proving both current verifier failures before production edits. Replace `closed_facts/2`'s shared `safe_scalar?/1` decision with a fixed field-definition dispatch: reason-like fields use compile-time categorical sets from existing production producers; outcome/status/behavior fields use exact enums; lifecycle IDs use UUID/opaque-reference validation as applicable; `render_version`, rule versions, ordinals, and counts use bounded non-negative/positive integers according to their schema; timestamps use UTC `DateTime`/ISO-8601 validation; custom channel, notification, render, rule, digest, category, and source identifiers use their own bounded code grammar and reject whitespace, `@`, URI/credential delimiters, control characters, and sensitive-data lexemes. Preserve literal output keys and never convert caller strings to atoms. Treat duplicate atom/string spellings deterministically by rejecting or omitting the ambiguous field rather than relying on enumeration order (D-01/D-03, PRIV-03). Remove the generic binary acceptance path entirely (D-04/D-05/D-06, PRIV-04).

Build the digest projection from explicit literal fields. Validate source-digest `outcome`, `digest_delivery_id`, `resolution_reason`, `rule_identity`, window timestamps, and boolean markers individually. Validate emitted-digest `kind`, `rule_identity`, and every `included`/`excluded`/`deferred`/`emitted_immediately` entry through a literal nested schema containing only lifecycle IDs, notification key, and categorical reason; discard an invalid entry rather than retaining its map. Apply the same categorical reason validator in `Deliveries.resolve_digest_outcome/6` before `Repo.update_all`, storing `nil` for unclassified/unsafe text while preserving the lifecycle transition and existing public return shape; existing valid reason codes include `included_in_digest`, `skipped_by_policy`, `emitted_immediately`, `recipient_muted`, `window_closed`, `digest_window_closed`, and `digest_window_expired`. Do not retain unsafe text for a manual-review path. Add `:digested` to the exact status set. Route planning, suppression, recovery, and timeline reason projections through the same closed reason families where they share these facts; do not reintroduce a generic label validator for reason fields. Keep core projections safe independently of `chimeway_admin` (D-07/D-08/D-09).</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/safe_evidence.ex lib/chimeway/deliveries.ex test/chimeway/trigger_sanitization_test.exs test/chimeway/privacy_boundary_test.exs test/chimeway/traces_test.exs test/chimeway/orchestration/digest_explainability_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/trigger_sanitization_test.exs test/chimeway/privacy_boundary_test.exs test/chimeway/traces_test.exs test/chimeway/orchestration/digest_explainability_test.exs --warnings-as-errors &amp;&amp; mix ci</automated>
  </verify>
  <acceptance_criteria>
    - The exact verifier sentinels `alex@example.test` and `reset-token=abc`, plus credential/URL/content variants, are proven present in inputs and absent from reloaded Trigger rows, digest rows, explanation digest, and timeline serialization.
    - Every retained string field is accepted by a named field family; there is no general length-only binary predicate reachable from `closed_facts/2`, `safe_digest/1`, or reason timeline projection.
    - Exact/near-miss, nil/empty/single, duplicate spelling, and stable-order tests encode all supplied PRIV-03 spec-less edge assumptions.
    - The unclassified/manual-review assumption is resolved fail-closed with no stored review payload or new review surface.
    - A production-created digested delivery explains `status: :digested` and retains safe outcome/rule/timestamp/identifier facts.
    - Focused suites and `mix ci` exit zero with no human/UAT evidence.
  </acceptance_criteria>
  <done>Approved keys no longer bypass value safety at durable or trace boundaries, and operators receive a complete privacy-safe explanation for a real digested delivery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host Trigger params → Event/Notification persistence | Caller-controlled values may use approved keys to attempt storage of identity, credentials, endpoints, or content. |
| Digest policy/resolution text → Delivery/DigestMembership persistence | Host-configured or legacy reason text may be provider/adopter controlled even when the field name is approved. |
| Durable digest rows → `Traces.explain_delivery/2` | Historical or current rows must be reconstructed into a closed operator vocabulary without forwarding generic maps. |
| Lifecycle row → status projection | Privacy filtering must not erase legitimate terminal state needed for explainability. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-98-07-01 | Information Disclosure | `SafeEvidence.closed_facts/2` / Trigger persistence | high | mitigate | Dispatch every approved key to a field-specific grammar, reject ambiguous duplicate spellings, seed real adversarial values, and inspect reloaded rows. | `env MIX_ENV=test mix test test/chimeway/trigger_sanitization_test.exs test/chimeway/privacy_boundary_test.exs --warnings-as-errors` |
| T-98-07-02 | Information Disclosure | Digest resolution persistence and `safe_digest/1` | high | mitigate | Normalize reason before write and reconstruct source/emitted digest evidence and nested entries from literal validated fields. | `env MIX_ENV=test mix test test/chimeway/orchestration/digest_explainability_test.exs test/chimeway/traces_test.exs --warnings-as-errors` |
| T-98-07-03 | Tampering | Atom/string duplicate approved keys | high | mitigate | Reject/omit ambiguous duplicates so input enumeration order cannot select an attacker-controlled value. | `env MIX_ENV=test mix test test/chimeway/trigger_sanitization_test.exs --warnings-as-errors` |
| T-98-07-04 | Repudiation | Digested status projection | medium | mitigate | Include the real terminal enum and assert a production lifecycle transition through `Traces.explain_delivery/2`. | `env MIX_ENV=test mix test test/chimeway/traces_test.exs --warnings-as-errors` |
| T-98-07-05 | Repudiation | Negative privacy regression evidence | medium | mitigate | Assert hostile values exist in test inputs and safe facts exist in outputs before sentinel absence assertions. | `env MIX_ENV=test mix test test/chimeway/privacy_boundary_test.exs test/chimeway/traces_test.exs --warnings-as-errors` |
| T-98-07-SC | Tampering | Package supply chain | low | accept | No dependency or package-manager operation is introduced. | `mix ci.audit` |

ASVS L1 mapping: V5 input validation governs the closed field grammars; V4 remains satisfied by the unchanged tenant-scoped Trigger/trace paths. Both high-severity information-disclosure threats and the duplicate-key tampering threat block completion on executable persistence/trace evidence.
</threat_model>

## Artifacts this phase produces

- New planning artifact: `.planning/phases/98-privacy-safe-delivery-evidence/98-07-PLAN.md`.
- Changed production symbols: `Chimeway.SafeEvidence.event_payload/1`, `notification_metadata/1`, `planning_context/1`, `delivery_metadata/1`, `render_data/1`, `trace/1`, and their private closed-fact/digest/status/reason validators; `Chimeway.Deliveries.resolve_digest_outcome/6` write normalization.
- Changed executable contracts: approved-key adversarial Trigger persistence, closed digest write/projection, exact/near-miss and empty/single/duplicate-order behavior, unclassified fail-closed behavior, and real digested `Traces.explain_delivery/2` status.
- No new public API, durable field, migration, external API, package, environment variable, CLI flag, UI surface, or manual-review payload channel is introduced.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Explain delivery behavior without retaining/emitting sensitive endpoint, credential, identity, or content data | 98-07 Task 1 | COVERED | One Trigger/digest-to-trace slice closes retention, emission, and status completeness blockers. |
| REQ | PRIV-03 | Recursive safe boundary before persistence and diagnostics | 98-07 Task 1 | COVERED | Existing recursion remains; field equality/touch, empty/single, duplicate spelling, and stable-order edges now govern approved values. |
| REQ | PRIV-04 | Retain only opaque refs, stable classifications, and allowlisted provider facts | 98-07 Task 1 | COVERED | General binary retention is removed; hostile approved-key values are rejected at Trigger/digest writes and trace reads. |
| RESEARCH | — | Closed constructors validate scalar types/enums and avoid allowlisted-key/unvalidated-value behavior | 98-07 Task 1 | COVERED | Implements the research's explicit anti-pattern correction in the shared vocabulary. |
| RESEARCH | — | Trace/operator evidence is reconstructed from literal safe facts rather than raw schemas/maps | 98-07 Task 1 | COVERED | `safe_digest/1` becomes a nested closed projection. |
| RESEARCH | — | Objectively machine-testable privacy evidence uses focused ExUnit plus project CI | 98-07 Task 1 | COVERED | Focused Trigger/privacy/trace/digest suites and `mix ci`; no UAT. |
| CONTEXT | D-01, D-02, D-03 | One atom-safe boundary, applied before writes/projections, drops forbidden pairs recursively | 98-07 Task 1 | COVERED | Retains the shared boundary and adds deterministic duplicate/value handling. |
| CONTEXT | D-04, D-05, D-06 | Prohibit raw data, retain explicit facts, and never use generic/redacted diagnostic fallback | 98-07 Task 1 | COVERED | Field validators and closed digest builders replace arbitrary binary/map fallbacks. |
| CONTEXT | D-07, D-08, D-09 | Shared safe vocabulary preserves operator facts and is core-safe before Admin | 98-07 Task 1 | COVERED | Trigger, Deliveries, and Traces share the same validators; `:digested` remains explainable. |
| CONTEXT | Deferred Ideas | None | — | EXCLUDED | No deferred scope exists. |

Prohibition recall: three product-specific privacy/explainability must-NOT constraints were retained descriptor-less as flagged-unverified per the SPEC-less fallback contract; no prohibition was auto-dismissed and no check descriptor was fabricated. Canon security/compliance concerns remain covered by the ASVS/STRIDE threat model rather than duplicated as bespoke prohibitions.

<verification>
Run the focused Trigger/privacy/trace/digest suites with warnings as errors, then run `mix ci`. Re-verification must inspect both positive categorical evidence and negative sentinel absence; a key-only scan or direct `SafeEvidence.trace/1` unit call is insufficient for the two failed truths.
</verification>

<success_criteria>
- No arbitrary binary can survive solely because its field name is allowlisted and its length is bounded.
- Trigger and digest persistence reject or omit hostile approved-key values before Chimeway owns them.
- Digest explanations and timeline details are literal, nested closed projections with no generic map pass-through.
- A real digested delivery retains `status: :digested` in `Traces.explain_delivery/2`.
- All supplied spec-less edge assumptions are explicit and executable, and unclassified evidence does not create a manual-review payload channel.
- Focused regression suites and `mix ci` exit zero without human verification or conversational UAT.
</success_criteria>

<output>
Create `.planning/phases/98-privacy-safe-delivery-evidence/98-07-SUMMARY.md` when done.
</output>
