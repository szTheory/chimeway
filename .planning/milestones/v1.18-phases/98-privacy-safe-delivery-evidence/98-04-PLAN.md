---
phase: 98-privacy-safe-delivery-evidence
plan: 04
type: execute
wave: 4
depends_on: [98-03]
files_modified:
  - lib/chimeway/safe_evidence.ex
  - lib/chimeway/traces.ex
  - lib/chimeway/admin.ex
  - chimeway_admin/lib/chimeway_admin/redaction.ex
  - test/chimeway/traces_test.exs
  - test/chimeway/admin_test.exs
  - chimeway_admin/test/chimeway_admin/live/privacy_leak_live_test.exs
autonomous: true
requirements: [PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-07/D-08: tenant-scoped trace and core Admin DTOs expose lifecycle IDs, opaque references, status/reason/classification, timestamps, render identity, and validated facts only."
    - "D-09: core projections are safe before optional chimeway_admin rendering; view redaction remains recursive defense in depth."
    - "D-04/D-06: raw recipient/correlation identity, planning blobs, adapter modules, and provider-controlled detail are absent from every operator projection."
    - "D-08: timeline order, aggregate counts, established absence results, and tenant-coherent joins remain intact."
  artifacts:
    - path: "lib/chimeway/traces.ex"
      provides: "Safe tenant-scoped explanation and timeline projection"
    - path: "lib/chimeway/admin.ex"
      provides: "Core-safe recent-problem, feed, definition, aggregate, and recovery DTOs"
  key_links:
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "Explanation and timeline builders use named closed projections"
      pattern: "SafeEvidence.(trace|timeline_detail)"
    - from: "lib/chimeway/admin.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "DTO rows are converted through `admin_fact/2` after tenant-coherent queries"
      pattern: "SafeEvidence.admin_fact"
  prohibitions:
    - "FLAGGED-UNVERIFIED — Operator explanation must not require a raw schema or optional view-layer mask."
    - "FLAGGED-UNVERIFIED — Privacy filtering must not weaken Phase 97 tenant predicates or absence-safe outcomes."
---

<objective>
Make core trace and Admin projections independently privacy-safe while preserving tenant-scoped operational explanation.

Purpose: Ensure optional UI rendering is never the primary barrier between sensitive lifecycle fields and an operator.
Output: Shared safe trace/Admin DTO builders, recursive Admin defense in depth, and focused tenant-safe projection evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-RESEARCH.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-03-SUMMARY.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-12-SUMMARY.md

<interfaces>
- `Chimeway.Traces.explain_delivery/2` returns a tenant-scoped `Explanation`; timeline ordering and absent results are established contracts.
- `Chimeway.Admin.recent_problem_deliveries/1`, `definitions/1`, `feed/1`, `recovery_candidates/1`, and aggregates use Phase 97 tenant-coherent joins.
- `Chimeway.SafeEvidence.trace/1`, `timeline_detail/1`, and `admin_fact/2` are literal constructors; no caller may pass through a generic map.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project tenant-scoped traces through safe evidence</name>
  <read_first>
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/traces.ex
    - lib/chimeway/traces/explanation.ex
    - test/chimeway/traces_test.exs
    - test/chimeway/privacy_boundary_test.exs
  </read_first>
  <files>lib/chimeway/safe_evidence.ex, lib/chimeway/traces.ex, test/chimeway/traces_test.exs</files>
  <behavior>
    - Test 1: Explanation/timeline retain IDs, notification/render identity, status/reason/classification, timestamps, and opaque refs.
    - Test 2: raw recipient/correlation, full planning context, adapter module, and provider detail are absent even when legacy rows contain hostile nested values.
    - Test 3: matching/wrong tenant results and timeline event order remain unchanged.
  </behavior>
  <action>Complete `SafeEvidence.trace/1` and `timeline_detail/1` as literal value-validated constructors. In `Traces`, remove raw identity passthrough, replace full planning-context inclusion with named reason/rule/time facts, omit adapter module and uncontrolled detail, and retain all Phase 97 tenant joins. Preserve Explanation result type, lifecycle/timeline ordering, last-attempt outcome/classification/ordinal/timestamp, and wrong-tenant `:not_found` behavior. Test full serialized structs/timelines against the shared sentinels plus positive safe facts.</action>
  <verify><automated>mix format --check-formatted lib/chimeway/safe_evidence.ex lib/chimeway/traces.ex test/chimeway/traces_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/traces_test.exs --warnings-as-errors</automated></verify>
  <acceptance_criteria>
    - Every trace/timeline map is built by a named SafeEvidence constructor.
    - Positive explanation facts and ordering remain; full serialized output contains no sentinel.
    - Focused trace tests exit zero.
  </acceptance_criteria>
  <done>Core tenant-scoped delivery explanation is useful and privacy-safe without a downstream view filter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project core Admin DTOs before recursive view defense</name>
  <read_first>
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/admin.ex
    - chimeway_admin/lib/chimeway_admin/redaction.ex
    - test/chimeway/admin_test.exs
    - chimeway_admin/test/chimeway_admin/live/privacy_leak_live_test.exs
  </read_first>
  <files>lib/chimeway/admin.ex, chimeway_admin/lib/chimeway_admin/redaction.ex, test/chimeway/admin_test.exs, chimeway_admin/test/chimeway_admin/live/privacy_leak_live_test.exs</files>
  <behavior>
    - Test 1: recent-problem/feed/recovery DTOs retain safe facts/counts and contain no raw identity/correlation/provider/detail values.
    - Test 2: definitions/aggregates preserve exact tenant-coherent counts and stable ordering.
    - Test 3: optional Admin redaction recursively drops an unexpected nested mixed-case hostile field from an already-safe DTO without changing allowed detail, and the rendered LiveView HTML contains none of its sentinel values.
  </behavior>
  <action>Use `SafeEvidence.admin_fact/2` for recent-problem, feed, and recovery rows; narrow Ecto selects to fields those constructors need and retain Phase 97 tenant-coherent join predicates. Keep definition/aggregate counts and established empty behavior. Update `ChimewayAdmin.Redaction` to apply `Privacy.redact/1` recursively before its display allowlist, treating it as defense in depth only. Per D-02/D-09, extend the existing Admin-package `PrivacyLeakLiveTest` fixture with nested map/list/keyword-shaped hostile values under mixed-case forbidden keys, render the affected dashboard and trace surfaces from core-safe DTOs, and assert every exact nested sentinel is absent from the returned HTML while positive allowed facts remain. Test core DTOs directly before any UI call and run the focused Admin-package LiveView test before the complete optional Admin verification.</action>
  <verify><automated>mix format --check-formatted lib/chimeway/admin.ex chimeway_admin/lib/chimeway_admin/redaction.ex test/chimeway/admin_test.exs chimeway_admin/test/chimeway_admin/live/privacy_leak_live_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/admin_test.exs --warnings-as-errors &amp;&amp; (cd chimeway_admin &amp;&amp; env MIX_ENV=test mix test test/chimeway_admin/live/privacy_leak_live_test.exs --warnings-as-errors) &amp;&amp; mix verify.admin</automated></verify>
  <acceptance_criteria>
    - Core Admin maps are safe before chimeway_admin receives them.
    - Tenant coherence, counts, ordering, and absence results remain covered.
    - Root core-DTO tests, the Admin-package `privacy_leak_live_test.exs` nested mixed-case rendered-output assertions, and `mix verify.admin` exit zero.
  </acceptance_criteria>
  <done>Core Admin DTOs share the safe vocabulary and optional UI redaction remains a tested secondary barrier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Lifecycle rows → trace/Admin DTOs | Durable state becomes operator-visible output. |
| Core DTO → optional Admin package | Safe core output crosses a separately versioned display boundary. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-98-04-01 | Information Disclosure | Trace projection | high | mitigate | Literal SafeEvidence constructors plus tenant-scoped full-output sentinel/positive assertions. | `env MIX_ENV=test mix test test/chimeway/traces_test.exs --warnings-as-errors` |
| T-98-04-02 | Information Disclosure | Core Admin DTOs | high | mitigate | Narrow selects, tenant-coherent joins, and `admin_fact/2` before optional rendering. | `env MIX_ENV=test mix test test/chimeway/admin_test.exs --warnings-as-errors` |
| T-98-04-03 | Tampering | Optional Admin defense | medium | mitigate | Recursive core privacy pass followed by fixed display allowlist. | `mix verify.admin` |
| T-98-04-SC | Tampering | Package supply chain | low | accept | No package install or dependency change occurs. | `mix ci.audit` |
</threat_model>

## Artifacts this phase produces

- Completed symbols: `Chimeway.SafeEvidence.trace/1`, `timeline_detail/1`, and `admin_fact/2`.
- Changed functions: trace Explanation/timeline constructors, Admin recent-problem/feed/recovery DTO builders, and `ChimewayAdmin.Redaction` recursive defense.
- Changed DTO fields: opaque recipient/correlation refs replace raw identity; generic planning/provider/detail maps and adapter module are absent.
- No new path, migration, package, external API, environment variable, or CLI flag.

<verification>
Run focused trace tests, focused Admin tests, then `mix verify.admin`. All evidence is automated.
</verification>

<success_criteria>
- Trace and Admin core projections are tenant-safe, sentinel-free, and positively explainable.
- Optional Admin receives already-safe DTOs and retains recursive defense in depth.
- Focused suites and `mix verify.admin` exit zero.
</success_criteria>

<output>
Create `.planning/phases/98-privacy-safe-delivery-evidence/98-04-SUMMARY.md` when done.
</output>
