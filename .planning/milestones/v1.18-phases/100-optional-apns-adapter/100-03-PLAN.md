---
phase: 100-optional-apns-adapter
plan: 03
type: execute
wave: 3
depends_on: [100-02]
files_modified:
  - lib/chimeway/apns/request_intent.ex
  - lib/chimeway/apns/payload.ex
  - lib/chimeway/apns/binding_lookup.ex
  - lib/chimeway/apns/transport.ex
  - lib/chimeway/adapters/apns.ex
  - test/support/apns_fake_transport.ex
  - test/chimeway/apns/request_test.exs
  - test/chimeway/adapters/apns_test.exs
autonomous: true
requirements: [APNS-01, APNS-02, APNS-04, APNS-05]
must_haves:
  truths:
    - "D-01/D-02: all Pigeon references are dynamically resolved inside the optional transport; core intent, payload, lookup, and adapter modules compile and boot with Pigeon absent."
    - "D-03/D-04: BindingLookup resolves only exact tenant/environment/topic/binding revision material and projects every failure to stable non-disclosing atoms."
    - "D-05/D-06: request construction always reloads the original persisted apns_id/expiry/open/collapse facts and resolves only transient token/dispatcher material per attempt."
    - "D-07: Payload emits only aps.alert title/body plus chimeway_open_ref and validates the final encoded byte length at the 4096-byte boundary."
    - "D-08/D-09: collapse is omitted for distinct work and stable apns_id is exposed only as correlation, never provider deduplication or safe-redrive authority."
    - "D-13/D-14: Pigeon timeout/exit/lost callback is possible handoff; only the pinned raw-end-stream adapter can preserve the exact HTTP status/reason/410-timestamp triple, and normalized results cannot authorize invalidation."
  assumptions:
    - statement: "APNS-05 unclassified probe item: replaceability is host-declared per occurrence; the plan assumes no implicit inference from notification key or title/body."
      flagged: true
  prohibitions:
    - statement: "Opting into APNs must not make non-push hosts install, configure, or supervise Pigeon."
      status: unverified
      flagged: true
    - statement: "Stable apns-id must not be described or used as an exactly-once delivery guarantee."
      status: unverified
      flagged: true
  artifacts:
    - path: "lib/chimeway/apns/payload.ex"
      provides: "Closed APS alert and opaque open-reference encoder with final byte bound"
      exports: ["Chimeway.APNS.Payload.build/2"]
    - path: "lib/chimeway/apns/binding_lookup.ex"
      provides: "Tenant-explicit transient lookup/invalidation behaviour and normalized result structs"
      exports: ["Chimeway.APNS.BindingLookup.resolve/2", "Chimeway.APNS.BindingLookup.invalidate/2"]
    - path: "lib/chimeway/apns/transport.ex"
      provides: "Pinned Pigeon-neutral transport result contract plus dynamic optional Pigeon implementation"
      exports: ["Chimeway.APNS.Transport.push/3"]
    - path: "test/support/apns_fake_transport.ex"
      provides: "No-network request capture and closed result fixture"
  key_links:
    - from: "lib/chimeway/adapters/apns.ex"
      to: "lib/chimeway/apns/binding_lookup.ex"
      via: "exact scope lookup after expiry check"
      pattern: "BindingLookup.resolve"
    - from: "lib/chimeway/adapters/apns.ex"
      to: "lib/chimeway/apns/payload.ex"
      via: "validated render content plus opaque open reference"
      pattern: "Payload.build"
    - from: "lib/chimeway/apns/transport.ex"
      to: "Pigeon 2.0.1"
      via: "Module.concat/1, struct/2, and apply/3 only when Code.ensure_loaded? succeeds"
      pattern: "Code.ensure_loaded"
---

<objective>
Complete the bounded APNs request boundary and optional Pigeon transport around the accepted tracer.

Purpose: Make token custody, payload shape, timeout ambiguity, and optional dependency behavior explicit replaceable contracts rather than private assumptions inside the tracer adapter.
Output: Closed payload builder, tenant-explicit host binding behaviour, pinned transport seam, dynamic Pigeon implementation, fake transport, and focused request/adapter contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-optional-apns-adapter/100-CONTEXT.md
@.planning/phases/100-optional-apns-adapter/100-RESEARCH.md
@.planning/phases/100-optional-apns-adapter/COVERAGE.md
@.planning/phases/100-optional-apns-adapter/100-01-SUMMARY.md
@lib/chimeway/adapters/apns.ex
@lib/chimeway/apns/request_intent.ex
@lib/chimeway/rendering/channels/push.ex
@lib/chimeway/target_resolver.ex
@lib/chimeway/safe_evidence.ex
@test/chimeway/adapters/mailglass_adapter_test.exs
@test/chimeway/dispatch/target_worker_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close request intent, payload, expiry, and collapse boundaries</name>
  <files>lib/chimeway/apns/request_intent.ex, lib/chimeway/apns/payload.ex, test/chimeway/apns/request_test.exs</files>
  <read_first>AGENTS.md, .planning/phases/100-optional-apns-adapter/100-CONTEXT.md, .planning/phases/100-optional-apns-adapter/100-RESEARCH.md, .planning/phases/100-optional-apns-adapter/COVERAGE.md, .planning/phases/100-optional-apns-adapter/100-01-SUMMARY.md, lib/chimeway/apns/request_intent.ex, lib/chimeway/adapters/apns.ex, lib/chimeway/rendering/channels/push.ex, lib/chimeway/privacy.ex, lib/chimeway/safe_evidence.ex, test/chimeway/rendering/channels/push_test.exs</read_first>
  <behavior>
    - "RequestIntent rejects missing/empty/invalid environment, topic, UUID, expiry, open ref, and replaceability attributes without creating atoms from input."
    - "Payload accepts validated title/body and emits exactly aps.alert plus chimeway_open_ref; arbitrary render_data.data and extra APS/top-level keys do not cross the API."
    - "Encoded byte sizes 4095 and 4096 pass; 4097 fails with `:payload_too_large` before any transport fixture message."
    - "Collapse is nil by default; replaceable intent derives the same value for identical exact inputs, a different value when any scoping input differs, and never exceeds 64 bytes."
    - "Expired equality is explicit: `expires_at <= now` is expired; one microsecond later is eligible."
  </behavior>
  <action>Refactor the tracer's validation into stable `RequestIntent` and `Payload` public data contracts without changing stored shape. Fix the closed APS surface to alert title/body plus `chimeway_open_ref`; reject generic custom data even though the generic rendering validator retains it for other push adapters. Encode once with Jason and compare `byte_size/1` to 4096. Define expiry boundary as absolute UTC `expires_at <= now`. Derive collapse only when `replaceable: true` from a versioned, length-delimited digest input containing occurrence_ref, binding_revision_ref, environment, and topic; Base.url_encode64 without padding and enforce at most 64 bytes. Preserve Phase 98 recursive sensitive-key/value rejection on every durable string/map before producing storage or payload.</action>
  <acceptance_criteria>
    - "`Payload.build/2` has no general custom-map argument and returns encoded JSON/request attributes only for the fixed key set."
    - "Request tests enumerate nil, empty, malformed, exact boundary, over-boundary, Unicode byte-size, and atom/string alias ambiguity cases."
    - "Changing occurrence, binding revision, environment, or topic changes collapse identity; distinct intent has no collapse header."
    - "Privacy sentinels in nested render data, intent, or open reference are rejected and absent from inspected errors."
  </acceptance_criteria>
  <verify><automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/apns/request_test.exs --warnings-as-errors</automated></verify>
  <done>Request construction has explicit empty/equality/adjacency behavior, a closed payload, final byte bounds, absolute expiry, and installation-safe opt-in collapse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Formalize exact host lookup and the pinned optional Pigeon transport</name>
  <reversibility rating="costly">The host callback/result algebra is a public integration seam, but provider implementation remains swappable behind it.</reversibility>
  <files>lib/chimeway/apns/binding_lookup.ex, lib/chimeway/apns/transport.ex, lib/chimeway/adapters/apns.ex, test/support/apns_fake_transport.ex, test/chimeway/adapters/apns_test.exs</files>
  <read_first>AGENTS.md, .planning/phases/100-optional-apns-adapter/100-CONTEXT.md, .planning/phases/100-optional-apns-adapter/100-RESEARCH.md, .planning/phases/100-optional-apns-adapter/COVERAGE.md, .planning/phases/100-optional-apns-adapter/100-01-SUMMARY.md, lib/chimeway/adapters/apns.ex, lib/chimeway/target_adapter.ex, lib/chimeway/target_resolver.ex, lib/chimeway/apns/request_intent.ex, lib/chimeway/apns/payload.ex, lib/chimeway/adapters/mailglass.ex, test/chimeway/adapters/mailglass_adapter_test.exs, test/chimeway/dispatch/target_worker_test.exs</read_first>
  <behavior>
    - "BindingLookup.resolve receives an exact four-field key and accepts transient material only when tenant/environment/topic/binding revision echo exactly; nil, stale, mismatched, or malformed responses become stable pre-handoff codes."
    - "Transport dynamically constructs a Pigeon.APNS.Notification with device_token, topic, id, expiration epoch, optional collapse_id, priority 10, push_type alert, and the closed payload, then pushes one notification through the opaque dispatcher."
    - "Pigeon absent returns a stable pre-handoff `pigeon_unavailable`; no static Pigeon module reference is required to compile/boot core."
    - "Pigeon synchronous timeout, exit, connection close, or missing callback after send becomes ambiguous; unknown conclusive normalized reasons are permanent."
    - "The host-supervised dispatcher selects `Chimeway.APNS.Transport.PigeonAdapter`, which intercepts Pigeon 2.0.1's raw end-stream, correlates stream id to the queued notification, and emits only bounded status/reason/timestamp/retry-delay facts; normalized Pigeon `:expired_token`/`:unregistered` without the complete triple is permanent and cannot authorize invalidation."
  </behavior>
  <action>Define `Chimeway.APNS.BindingLookup` callbacks `resolve_binding/1` and `invalidate_binding/1`, exact request/transient structs, and wrapper functions that select the configured host module while returning stable non-disclosing errors. Define `Chimeway.APNS.Transport` callback/result structs for accepted, conclusive rejection, and ambiguous post-emission outcomes. Implement the dynamic Pigeon 2.0.1 path with `Module.concat`, `Code.ensure_loaded?`, `struct/2`, and `apply/3`; send exactly one notification, fixed alert priority/push type, bounded timeout, and no async completion authority outside the synchronous caller. In the same Pigeon-neutral file, implement `Chimeway.APNS.Transport.PigeonAdapter` for the host-supervised dispatcher: dynamically delegate Pigeon APNS init/push and non-response messages, intercept a raw end-stream before Pigeon's normalizer, correlate its stream id to the pinned APNS state's queued notification, bound and decode the body, retain only integer status, recognized reason, non-negative 410 timestamp, and bounded retry delay, discard the raw body, and invoke the original response callback with the closed result. Require opted-in hosts to select this adapter when exact invalidation is enabled; a stock normalized response, missing queue entry, malformed body, or incomplete triple fails closed and never infers HTTP 410. Update APNS adapter to compose RequestIntent → BindingLookup → Payload → Transport and ensure rescue/catch after transport entry returns possible handoff with no raw exception. Provide a test fake that captures redacted request headers/payload shape but never stores or returns the token.</action>
  <acceptance_criteria>
    - "Binding lookup and invalidation callbacks accept only exact data structs; their public error/result vocabulary contains no arbitrary `term()` evidence."
    - "Source compiles with Pigeon absent; dynamic Pigeon field mapping is contract-tested against Pigeon 2.0.1 in the opt-in gate planned by 100-05."
    - "Synthetic Pigeon 2.0.1 raw end-stream fixtures prove stream-id correlation and exact extraction of `(410, ExpiredToken|Unregistered, non-negative timestamp)`; missing status, reason, timestamp, queue entry, or malformed/oversized JSON cannot yield invalidation authority."
    - "Fake transport proves the exact topic/environment-selected dispatcher, UUID id, expiry epoch, collapse omission/presence, priority, push type, and payload shape."
    - "Timeout/exit/lost response returns ambiguous and no test path automatically invokes transport twice."
    - "Raw token/credential/body/exception sentinels are absent from adapter return values, captured logs, telemetry, persisted rows, and traces."
  </acceptance_criteria>
  <verify><automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/adapters/apns_test.exs test/chimeway/apns/request_test.exs --warnings-as-errors</automated></verify>
  <done>The accepted tracer uses explicit host-custody and transport contracts, the default Pigeon path is truly optional, and ambiguous/conclusive response boundaries are honest.</done>
</task>

</tasks>

## Artifacts this phase produces

- File/module `lib/chimeway/apns/payload.ex` / `Chimeway.APNS.Payload`; function `build/2`; constants for 4096 encoded bytes and fixed `chimeway_open_ref` key.
- File/module/behaviour `lib/chimeway/apns/binding_lookup.ex` / `Chimeway.APNS.BindingLookup`; callbacks `resolve_binding/1`, `invalidate_binding/1`; request, transient binding, invalidation-key, and invalidation-result structs; wrapper functions `resolve/2`, `invalidate/2`; config key `:apns_binding_lookup`.
- File/module/behaviour `lib/chimeway/apns/transport.ex` / `Chimeway.APNS.Transport`; callback `push/3`; closed request/provider-response structs; function `push/3`; dynamic Pigeon transport plus `Chimeway.APNS.Transport.PigeonAdapter` raw-end-stream wrapper and config key `:apns_transport`.
- Expanded `Chimeway.Adapters.APNS.deliver/2` composition and stable pre-handoff/ambiguous result codes.
- File/module `test/support/apns_fake_transport.ex` / `Chimeway.Test.APNSFakeTransport`; request capture and status/reason/timestamp fixtures.
- ExUnit files/modules `test/chimeway/apns/request_test.exs` / `Chimeway.APNS.RequestTest` and `test/chimeway/adapters/apns_test.exs` / `Chimeway.Adapters.APNSTest`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Chimeway → host lookup | Exact opaque binding key requests transient token/dispatcher material. |
| Chimeway → Pigeon dispatcher | Closed request crosses provider-emission boundary. |
| Pigeon/provider → Chimeway | Normalized or reason-aware response becomes bounded result data. |

## STRIDE Threat Register

**Security gate:** OWASP ASVS Level 1 applies (`block_on: high`). Any unresolved high-severity threat blocks plan completion. T-100-12 and T-100-13 are bound to Task 1's request-suite command and its opaque-open-ref plus encoded-size acceptance criteria. T-100-11, T-100-14, T-100-15, and T-100-SC are bound to Task 2's adapter-suite command and its sentinel, ambiguous-no-resend, exact raw status/reason/timestamp, and pinned-2.0.1 acceptance criteria; every named check must be green before this plan can complete.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-100-11 | Information Disclosure | BindingLookup/Transport | high | mitigate | D-02/D-03 transient structs, exact scope, closed return algebra, sentinel log/telemetry/persistence tests. |
| T-100-12 | Tampering | opaque open reference | high | mitigate | D-05/D-07 transport treats it as opaque bounded data and never consumes/re-authorizes it; Phase 101 remains sole consumption authority. |
| T-100-13 | Denial of Service | payload/timeout | high | mitigate | Final 4096-byte gate and bounded synchronous timeout occur before/around exactly one notification push. |
| T-100-14 | Repudiation | timeout/exit/lost response | high | mitigate | D-09/D-13 maps every post-emission uncertainty to ambiguous handoff with no automatic resend. |
| T-100-15 | Tampering | normalized invalidation reason | high | mitigate | D-14 requires preserved HTTP status plus recognized reason; status-less normalized results cannot invalidate. |
| T-100-SC | Tampering | host Pigeon dependency | high | mitigate | Package legitimacy audit pins approved Pigeon 2.0.1; root package adds no dependency and the opt-in fixture resolves the exact version. |
</threat_model>

<verification>
Run both focused suites after their tasks. Then run `MIX_ENV=prod mix compile --warnings-as-errors` in the root with no Pigeon dependency. All request and adapter behaviors are machine-testable; no UAT applies.
</verification>

<success_criteria>
- Request intent/payload behavior covers empty, equality, ordering/deduplication, byte, and collapse boundaries.
- Host lookup is exact, non-disclosing, and invoked only after expiry.
- Default Pigeon calls are dynamic and conservative; the pinned raw-end-stream adapter retains only the closed safe facts.
- Timeout and process failures after emission are unambiguously classified as ambiguous handoff.
</success_criteria>

<output>
Create `.planning/phases/100-optional-apns-adapter/100-03-SUMMARY.md` when done.
</output>
