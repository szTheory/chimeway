---
phase: 100-optional-apns-adapter
plan: 05
type: execute
wave: 5
depends_on: [100-04]
files_modified:
  - scripts/verify-apns.sh
  - test/fixtures/apns_consumer/mix.exs
  - test/fixtures/apns_consumer/config/config.exs
  - test/fixtures/apns_consumer/lib/apns_consumer.ex
  - test/fixtures/apns_consumer/test/apns_consumer_test.exs
  - test/chimeway/apns/api_coverage_test.exs
  - test/chimeway/release_gate_contract_test.exs
  - mix.exs
  - .github/workflows/ci.yml
  - .planning/phases/100-optional-apns-adapter/COVERAGE.md
autonomous: true
requirements: [APNS-01, APNS-02, APNS-03, APNS-04, APNS-05, APNS-06]
must_haves:
  truths:
    - "D-01: a clean packaged consumer with APNs disabled has no Pigeon dependency/config and fetches, compiles with warnings-as-errors, boots, and runs a core Chimeway path."
    - "D-01/D-02: the same clean consumer explicitly enables Pigeon 2.0.1, starts a host-owned no-network dispatcher with the pinned raw-end-stream adapter, and completes the APNs accepted tracer without Apple credentials/network."
    - "D-03 through D-16: `mix verify.apns` runs request, provider matrix, exact invalidation, expiry, collapse, ambiguity, evidence, migration, and optionality contracts from one stable entrypoint."
    - "API coverage gate parses every COVERAGE.md row, accepts only INTEGRATE/OPT-OUT, and rejects an opt-out without a one-line reason."
    - "Local `mix verify.apns`, the `verify_apns` CI job, release-gate contract, and `ci-gate` aggregation remain in parity."
  assumptions:
    - statement: "APNS-01 unclassified probe remains flagged until the packaged no-Pigeon and opted-in Pigeon fixture both execute green; source presence alone is insufficient evidence."
      flagged: true
  prohibitions:
    - statement: "The APNs verification artifact must not claim live Apple acceptance, device display, or protected-open proof from the no-network dispatcher."
      status: unverified
      flagged: true
  artifacts:
    - path: "scripts/verify-apns.sh"
      provides: "Hermetic package-artifact two-mode APNs optionality proof"
    - path: "test/fixtures/apns_consumer"
      provides: "Pigeon-disabled and explicitly Pigeon-enabled clean host fixture"
    - path: "test/chimeway/apns/api_coverage_test.exs"
      provides: "Executable full external capability disposition gate"
    - path: "mix.exs"
      provides: "`mix verify.apns` local gate"
    - path: ".github/workflows/ci.yml"
      provides: "`verify_apns` CI job and ci-gate aggregation"
  key_links:
    - from: "mix verify.apns"
      to: "scripts/verify-apns.sh"
      via: "package artifact clean-consumer proof"
      pattern: "verify-apns"
    - from: ".github/workflows/ci.yml"
      to: "mix verify.apns"
      via: "dedicated verify_apns job"
      pattern: "verify_apns"
    - from: "test/chimeway/release_gate_contract_test.exs"
      to: "ci-gate"
      via: "asserts local alias/job/needs/env/aggregate parity"
      pattern: "VERIFY_APNS"
---

<objective>
Turn the complete APNs adapter into executable adopter and CI evidence, including proof that non-push hosts never acquire Pigeon.

Purpose: APNS-01 and the phase's external API contract are packaging properties, not unit-test claims; they require a clean artifact consumer and local/hosted gate parity.
Output: Two-mode clean-consumer fixture, `mix verify.apns`, coverage parser, dedicated CI job, release-gate assertions, and final phase regression evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-optional-apns-adapter/100-CONTEXT.md
@.planning/phases/100-optional-apns-adapter/100-RESEARCH.md
@.planning/phases/100-optional-apns-adapter/COVERAGE.md
@.planning/phases/100-optional-apns-adapter/100-04-SUMMARY.md
@mix.exs
@.github/workflows/ci.yml
@scripts/prove-adoption-paths.exs
@test/chimeway/release_gate_contract_test.exs
@lib/chimeway/apns/transport.ex
@test/support/apns_fake_transport.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove packaged Pigeon absence and explicit host opt-in</name>
  <files>scripts/verify-apns.sh, test/fixtures/apns_consumer/mix.exs, test/fixtures/apns_consumer/config/config.exs, test/fixtures/apns_consumer/lib/apns_consumer.ex, test/fixtures/apns_consumer/test/apns_consumer_test.exs</files>
  <read_first>AGENTS.md, .planning/phases/100-optional-apns-adapter/100-CONTEXT.md, .planning/phases/100-optional-apns-adapter/100-RESEARCH.md, .planning/phases/100-optional-apns-adapter/COVERAGE.md, .planning/phases/100-optional-apns-adapter/100-04-SUMMARY.md, mix.exs, scripts/prove-adoption-paths.exs, test/chimeway/release_gate_contract_test.exs, lib/chimeway/adapters/apns.ex, lib/chimeway/apns/transport.ex, test/support/apns_fake_transport.ex</read_first>
  <behavior>
    - "Disabled mode consumes only a freshly built/unpacked Chimeway package artifact; `mix deps.tree`, lockfile, and loaded applications contain no pigeon, and the host compiles/boots without APNs config."
    - "Enabled mode changes only the fixture's explicit switch, resolves exactly pigeon 2.0.1, starts a host-owned dispatcher with `Chimeway.APNS.Transport.PigeonAdapter` over a no-network HTTP/2 fixture, and exercises dynamic notification mapping plus raw status/reason/410-timestamp preservation."
    - "Enabled proof emits only a fixed safe evidence line identifying provider_accepted sandbox handoff and explicitly excluding live Apple/device/open claims."
    - "Both modes run in fresh temporary directories and fail on missing artifact, dependency drift, compile warning, boot failure, extra evidence fields, or leaked sentinel."
  </behavior>
  <action>Create a portable `scripts/verify-apns.sh` that builds one root Hex archive under `MIX_ENV=prod`, validates/unpacks it with the existing bounded archive conventions, copies the committed consumer fixture into separate `mktemp -d` disabled/enabled roots, and cleans them on exit. The fixture `mix.exs` points only at the unpacked Chimeway path; it adds `{:pigeon, "== 2.0.1"}` only when `CHIMEWAY_APNS_ENABLED=1`. Disabled mode runs deps.get/tree, asserts Pigeon absent from tree and lock, compiles warnings-as-errors, starts the app with no APNs env, and runs a core API smoke. Enabled mode supplies an in-memory no-network HTTP/2 client, supervises a host-owned Pigeon dispatcher selecting `Chimeway.APNS.Transport.PigeonAdapter`, supplies exact lookup material, and runs both an accepted response and synthetic raw 410 streams through the adapter. Assert exact stream-id correlation and closed extraction for ExpiredToken/Unregistered with timestamp, then mutation-test missing status/reason/timestamp, unknown stream id, malformed JSON, and oversized body so none can invalidate. Validate one allowlisted JSON evidence line. Use no Apple credentials/network and state in the evidence contract that provider acceptance is not live/device/open proof.</action>
  <acceptance_criteria>
    - "Disabled dependency tree and lockfile have zero Pigeon entries and the application boots with no APNs configuration."
    - "Enabled dependency tree resolves Pigeon exactly 2.0.1 from the fixture's explicit conditional dependency, never from root Chimeway."
    - "Enabled test asserts device token exists only inside the request construction/dispatcher call and is absent from package proof output and all Chimeway rows/traces."
    - "Enabled Pigeon 2.0.1 test proves the raw-end-stream adapter preserves the exact 410 status/reason/timestamp triple and rejects every incomplete or uncorrelated variant before invalidation."
    - "The script is non-interactive, uses validated temporary directories, traps cleanup, and returns nonzero for either mode failure."
  </acceptance_criteria>
  <verify><automated>bash scripts/verify-apns.sh</automated></verify>
  <done>One package artifact proves both zero-cost non-push consumption and explicit Pigeon-backed APNs opt-in under a host-owned no-network dispatcher.</done>
</task>

<execution_sequence>
Task 1 is an independent clean-consumer review boundary: implement and commit only the package script and fixture, then require `bash scripts/verify-apns.sh` green before Task 2 begins. Task 2 consumes that proven script, changes only coverage/gate files, and receives its own commit after its focused contract command and `mix verify.apns` pass. Review the two commits independently; CI/release wiring must not mask or replace Task 1's artifact-consumer evidence.
</execution_sequence>

<task type="auto" tdd="true">
  <name>Task 2: Wire full API coverage and APNs verification into local and hosted gates</name>
  <files>test/chimeway/apns/api_coverage_test.exs, test/chimeway/release_gate_contract_test.exs, mix.exs, .github/workflows/ci.yml, .planning/phases/100-optional-apns-adapter/COVERAGE.md</files>
  <read_first>AGENTS.md, .planning/phases/100-optional-apns-adapter/COVERAGE.md, .planning/phases/100-optional-apns-adapter/100-VALIDATION.md, .planning/phases/100-optional-apns-adapter/100-04-SUMMARY.md, mix.exs, .github/workflows/ci.yml, scripts/ci/aggregate-gate.sh, scripts/verify-apns.sh, test/chimeway/release_gate_contract_test.exs, test/chimeway/apns/request_test.exs, test/chimeway/apns/result_test.exs, test/chimeway/safe_evidence_test.exs, test/chimeway/migration_contract_test.exs</read_first>
  <behavior>
    - "Coverage parser finds every markdown capability row, rejects blank/unknown disposition and blank OPT-OUT reason, and cross-checks required request/result vocabulary against focused tests."
    - "`mix verify.apns` runs coverage, request, adapter/result, target worker, SafeEvidence/trace, migration contracts, and the clean-consumer script."
    - "CI `verify_apns` prepares Elixir/Postgres, runs migrations and `mix verify.apns`, is path-gated only when that cannot skip required ci-gate evidence, and renders observability consistently with other verify lanes."
    - "ci-gate needs/env/aggregate lists include VERIFY_APNS exactly once; release-gate contract fails on any local/CI drift."
  </behavior>
  <action>Keep COVERAGE.md aligned with the implemented Pigeon 2.0.1/Apple surface; add `api_coverage_test.exs` to parse the table and map all INTEGRATE rows to test tokens/files. Add `verify.apns` to Mix aliases as the sole local phase gate and include both focused database tests and `bash scripts/verify-apns.sh`. Add a dedicated `verify_apns` job to CI using the repository's pinned setup-beam/cache/Postgres/observability pattern. Add the job to `ci-gate.needs`, expose `VERIFY_APNS`, and include it in `aggregate-gate.sh` invocation. Extend release-gate contracts to assert exact alias commands, job step, dependency/result variable, aggregate membership, Pigeon absence from root deps, and fixture-only exact version. Do not add a root Pigeon dependency or Apple secrets.</action>
  <acceptance_criteria>
    - "COVERAGE.md has no unclassified row: every external capability is INTEGRATE or reasoned OPT-OUT, and the parser mutation tests fail for missing disposition/reason."
    - "`mix verify.apns` succeeds from a clean root and contains all six APNS requirement suites plus migration and consumer proof."
    - "Release-gate contract asserts verify_apns job, `mix verify.apns`, ci-gate needs, VERIFY_APNS env, aggregate argument, and no root Pigeon dependency."
    - "CI YAML parses and aggregate-gate contract has no duplicate/missing lane token."
  </acceptance_criteria>
  <verify><automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/apns/api_coverage_test.exs test/chimeway/release_gate_contract_test.exs --warnings-as-errors &amp;&amp; mix verify.apns</automated></verify>
  <done>The complete APNs contract is one reproducible local command and one required hosted lane, with full external API disposition and clean-consumer optionality evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

- Executable `scripts/verify-apns.sh` clean-artifact verifier.
- Fixture project files under `test/fixtures/apns_consumer/`: conditional `mix.exs`, no-secret config, host-owned no-network dispatcher/lookup module, and ExUnit disabled/enabled consumer proof.
- File/module `test/chimeway/apns/api_coverage_test.exs` / `Chimeway.APNS.APICoverageTest` and mutation fixtures generated in test temp space.
- Mix alias `verify.apns`.
- GitHub Actions job `verify_apns`, result variable `VERIFY_APNS`, and `ci-gate` dependency/aggregate entry.
- Final external capability contract `.planning/phases/100-optional-apns-adapter/COVERAGE.md`.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe bounded APNs dispatch, honest target outcomes, zero dependency for other hosts | 01-05 | COVERED | Tracer → expansion → lifecycle → packaging gate. |
| REQ | APNS-01 | Explicit optional Pigeon adapter | 03, 05 | COVERED | Dynamic boundary plus packaged disabled/enabled consumer. |
| REQ | APNS-02 | Exact host material, headers, stable id, bounded payload/open ref | 01, 03, 05 | COVERED | Request and clean-host contracts. |
| REQ | APNS-03 | Reason classes and exact invalidation | 04, 05 | COVERED | Pinned matrix and CAS tests. |
| REQ | APNS-04 | Absolute expiry before send/retry | 01, 03, 04, 05 | COVERED | Boundary, lifecycle, and aggregate gate. |
| REQ | APNS-05 | Opt-in safe collapse | 01, 03, 05 | COVERED | Scoped digest and omission tests. |
| REQ | APNS-06 | Distinct operator states | 04, 05 | COVERED | SafeEvidence/trace and phase gate. |
| RESEARCH | — | Persist intent; resolve secrets only at final boundary | 01-04 | COVERED | Existing Phase 99 spine retained. |
| RESEARCH | — | Static-free optional runtime boundary | 03, 05 | COVERED | Dynamic module calls and clean consumer. |
| RESEARCH | — | Closed result algebra and one transactional mutation | 04 | COVERED | Executor does no provider parsing. |
| RESEARCH | — | Pigeon 2.0.1 result/status/timestamp limitation | 03, 04, 05 | COVERED | Pinned raw-end-stream adapter plus exact-triple tests. |
| RESEARCH | — | Public/prefixed copied migration parity | 02 | COVERED | Golden and DB rollback proof. |
| CONTEXT | D-01, D-02 | Optional adapter and host dispatcher/credential custody | 01, 03, 05 | COVERED | No root dep or credential config. |
| CONTEXT | D-03, D-04 | Exact transient lookup and safe pre-handoff failure | 01, 03, 04 | COVERED | Exact structs and leak tests. |
| CONTEXT | D-05, D-06 | Durable stable intent, expiry/retry reuse | 01, 02, 03, 04 | COVERED | Stored variant and repeated gates. |
| CONTEXT | D-07, D-08, D-09 | Closed payload, scoped collapse, correlation-only id | 01, 03, 04 | COVERED | Exact boundary and ambiguity tests. |
| CONTEXT | D-10, D-11, D-12, D-13 | Honest accepted/retryable/permanent/ambiguous lifecycle | 04 | COVERED | Typed result union and Oban mapping. |
| CONTEXT | D-14, D-15, D-16 | Pinned reason seam and exact 410 invalidation | 03, 04, 05 | COVERED | Full matrix/CAS/coverage gate. |

Deferred Phase 101 registration/protected-open authorization, Phase 102 adopter twin, Phase 103 physical-device proof, and future FCM work are intentional exclusions, not source gaps.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Published Chimeway artifact → clean host | Dependency/configuration optionality must survive packaging. |
| Fixture host → Pigeon sandbox | Host explicitly owns dispatcher and transient lookup. |
| Local aliases → hosted CI | Verification parity must not allow a skipped APNs contract. |

## STRIDE Threat Register

**Security gate:** OWASP ASVS Level 1 applies (`block_on: high`). Any unresolved high-severity threat blocks plan completion. T-100-22, T-100-23, and T-100-SC are bound to Task 1's `bash scripts/verify-apns.sh` command and its exact dependency-tree, allowlisted-output/sentinel, and pinned-2.0.1 acceptance criteria. T-100-25 is bound to Task 2's coverage/release-contract plus `mix verify.apns` command and its parser-mutation and local/CI parity acceptance criteria; every named check must be green before this plan can complete.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-100-22 | Tampering | packaged dependency graph | high | mitigate | Two fresh consumer roots assert Pigeon absent by default and exactly 2.0.1 only under explicit host switch. |
| T-100-23 | Information Disclosure | proof/CI output | high | mitigate | Fixed allowlisted evidence, sandbox-only fixtures, secret sentinels, and no Apple credential environment. |
| T-100-24 | Repudiation | sandbox acceptance claim | medium | mitigate | Evidence explicitly labels provider_accepted sandbox handoff and excludes live/device/open proof. |
| T-100-25 | Tampering | coverage/CI parity | high | mitigate | Coverage parser mutation tests and release-gate AST/text contracts bind alias, job, needs, env, and aggregate token. |
| T-100-26 | Denial of Service | clean-consumer verifier | medium | mitigate | Bounded archive validation, explicit temp roots, cleanup trap, exact dependency version, and non-interactive commands. |
| T-100-SC | Tampering | Pigeon supply chain | high | mitigate | Research legitimacy audit approves Pigeon; fixture pins 2.0.1, root stays dependency-free, and CI lock/tree assertions detect substitution. |
</threat_model>

<verification>
Run `mix verify.apns`, then `mix ci` and `mix ci.verify_gates`. The dedicated hosted job is contract-checked locally; live-CI backstop, if requested at milestone close, must be asserted programmatically against the implementation SHA. No conversational UAT applies.
</verification>

<success_criteria>
- Pigeon-disabled package consumer fetches, compiles, boots, and uses Chimeway with no Pigeon/config.
- Explicit Pigeon 2.0.1 consumer runs the adapter through a host-owned no-network dispatcher.
- Every external capability has an executable disposition and every APNS requirement is present in `mix verify.apns`.
- Local aliases, CI job, ci-gate aggregation, and release-gate contracts are in parity.
</success_criteria>

<output>
Create `.planning/phases/100-optional-apns-adapter/100-05-SUMMARY.md` when done.
</output>
