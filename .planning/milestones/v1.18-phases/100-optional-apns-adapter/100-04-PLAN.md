---
phase: 100-optional-apns-adapter
plan: 04
type: execute
wave: 4
depends_on: [100-03]
files_modified:
  - lib/chimeway/target_adapter.ex
  - lib/chimeway/dispatch/executor.ex
  - lib/chimeway/delivery_targets.ex
  - lib/chimeway/dispatch/oban_worker.ex
  - lib/chimeway/safe_evidence.ex
  - test/chimeway/apns/result_test.exs
  - test/chimeway/dispatch/target_worker_test.exs
  - test/chimeway/safe_evidence_test.exs
  - test/chimeway/traces_test.exs
autonomous: true
requirements: [APNS-03, APNS-04, APNS-06]
must_haves:
  truths:
    - "D-10: accepted, retryable, permanent, and exact-invalidated conclusive adapter results close the exact claimed target/attempt through one locked mutation each."
    - "D-11: provider_accepted is provider handoff only and remains distinct from protected open, inbox seen, and inbox read facts."
    - "D-12: only confirmed pre-provider/retryable results return retry authority; ordinary request/topic/environment/credential/auth/payload errors are terminal."
    - "D-13: timeout, exit, connection loss, and missing post-emission response become terminal ambiguous_handoff and never automatic retry."
    - "D-14/D-15: only preserved HTTP 410 plus exact ExpiredToken or Unregistered reason and a non-negative APNs 410 timestamp can enter invalidation; unknown or incomplete conclusive results are permanent."
    - "D-16: host compare-and-update and Chimeway target completion use the original tenant/environment/topic/binding revision and cannot mutate a rotated/different scope."
    - "APNS-04 idempotency/concurrency: expired initial/retry execution yields one expired attempt and no lookup/transport even under duplicate workers."
    - "APNS-06 idempotency/concurrency: repeated trace reads are stable and a completion race exposes exactly one terminal result for the started attempt."
  assumptions:
    - statement: "APNS-03 unclassified probe item: the pinned provider matrix is treated as exhaustive for 2.0.1; future conclusive reasons take the permanent fallback until explicitly classified."
      flagged: true
    - statement: "APNS-06 idempotency/concurrency probes assume protected-open, seen, and read producers remain Phase 101/existing inbox authorities; Phase 100 only preserves distinct trace vocabulary."
      flagged: true
  prohibitions:
    - statement: "APNs acceptance must not be represented as device receipt, display, protected open, inbox seen/read, or engagement."
      status: unverified
      flagged: true
    - statement: "Provider rejection must not invalidate a rotated replacement, another installation, environment, topic, or tenant."
      status: unverified
      flagged: true
  artifacts:
    - path: "lib/chimeway/target_adapter.ex"
      provides: "Typed accepted/retryable/permanent/invalidated/expired/pre-handoff/ambiguous result algebra"
    - path: "lib/chimeway/delivery_targets.ex"
      provides: "Exact locked transactional completion for every APNs outcome"
      exports: ["record_target_outcome/5"]
    - path: "lib/chimeway/safe_evidence.ex"
      provides: "Bounded APNs status/reason/timestamp/retry evidence and distinct trace projections"
    - path: "test/chimeway/apns/result_test.exs"
      provides: "Pinned Pigeon/APNs reason matrix, CAS invalidation, retry, and ambiguity tests"
  key_links:
    - from: "lib/chimeway/dispatch/executor.ex"
      to: "lib/chimeway/delivery_targets.ex"
      via: "typed adapter result maps to one exact locked target/attempt mutation"
      pattern: "record_target_outcome"
    - from: "lib/chimeway/dispatch/oban_worker.ex"
      to: "typed target outcome"
      via: "returns retry only for retryable/pre-handoff, completes terminal/ambiguous/invalidated/expired"
      pattern: "run_target"
    - from: "lib/chimeway/delivery_targets.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "closed provider facts validated before transaction persistence and again during trace projection"
      pattern: "target_attempt_facts"
---

<objective>
Expand the accepted tracer into the complete honest APNs result lifecycle, exact invalidation, retry/exhaustion behavior, and operator evidence.

Purpose: Provider calls are only trustworthy when every conclusive and uncertain outcome produces a target-specific durable explanation without cross-binding mutation or engagement overclaiming.
Output: Typed TargetAdapter result algebra, atomic target mutations, Oban retry mapping, safe APNs evidence vocabulary, pinned reason matrix, and trace distinction tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-optional-apns-adapter/100-CONTEXT.md
@.planning/phases/100-optional-apns-adapter/100-RESEARCH.md
@.planning/phases/100-optional-apns-adapter/COVERAGE.md
@.planning/phases/100-optional-apns-adapter/100-03-SUMMARY.md
@lib/chimeway/target_adapter.ex
@lib/chimeway/dispatch/executor.ex
@lib/chimeway/delivery_targets.ex
@lib/chimeway/dispatch/oban_worker.ex
@lib/chimeway/safe_evidence.ex
@lib/chimeway/apns/binding_lookup.ex
@lib/chimeway/apns/transport.ex
@test/chimeway/dispatch/target_worker_test.exs
@test/chimeway/safe_evidence_test.exs
@test/chimeway/traces_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize every typed provider outcome on the exact target attempt</name>
  <reversibility rating="costly">The richer public TargetAdapter result union affects every target adapter/executor consumer, but legacy accepted and pre-handoff shapes remain supported and the provider-specific classification stays replaceable.</reversibility>
  <files>lib/chimeway/target_adapter.ex, lib/chimeway/dispatch/executor.ex, lib/chimeway/delivery_targets.ex, test/chimeway/apns/result_test.exs, test/chimeway/dispatch/target_worker_test.exs</files>
  <read_first>AGENTS.md, .planning/phases/100-optional-apns-adapter/100-CONTEXT.md, .planning/phases/100-optional-apns-adapter/100-RESEARCH.md, .planning/phases/100-optional-apns-adapter/COVERAGE.md, .planning/phases/100-optional-apns-adapter/100-03-SUMMARY.md, lib/chimeway/target_adapter.ex, lib/chimeway/dispatch/executor.ex, lib/chimeway/delivery_targets.ex, lib/chimeway/delivery_target.ex, lib/chimeway/delivery_target_attempt.ex, lib/chimeway/apns/binding_lookup.ex, lib/chimeway/apns/transport.ex, test/chimeway/dispatch/target_worker_test.exs</read_first>
  <behavior>
    - "Accepted 200 closes provider_accepted; retryable known reasons close failed/pending with corrective-action/retry delay; permanent closes failed terminal; exact CAS success closes invalidated; expiry closes expired."
    - "HTTP 410 ExpiredToken/Unregistered with a non-negative APNs timestamp invokes `BindingLookup.invalidate/2` once with the original four-field key; a missing/invalid timestamp or stale/missing CAS result becomes permanent and does not mark the target invalidated."
    - "BadDeviceToken, DeviceTokenNotForTopic, other known request/topic/auth/payload errors, status-less expired/unregistered, and unknown conclusive reasons are permanent."
    - "Timeout, exit, connection loss, missing result, raised/thrown callbacks, and legacy possible-handoff shapes close ambiguous and cannot be automatically claimed again."
    - "Concurrent duplicate execution and concurrent completion permit exactly one target/attempt mutation; rotated replacement and cross-tenant/environment/topic fixtures are unchanged."
  </behavior>
  <action>Extend `TargetAdapter.deliver_result` with closed variants for accepted, retryable, permanent, invalidated, expired, pre-handoff, and possible-handoff while preserving Phase 99 compatibility for `{:ok, map()}` and sanitizing legacy reasons. Make APNS adapter classify the full COVERAGE reason rows: known transient/provider-token refresh results carry a stable `provider_code`, bounded `retry_after_ms`, and corrective-action code; ordinary unchanged-request errors are permanent; only a `PigeonAdapter` result containing integer status 410, exact ExpiredToken or Unregistered, and a non-negative millisecond timestamp calls exact host CAS before returning invalidated; missing/malformed timestamp, stock normalized results, and unknown conclusive results are permanent; post-emission uncertainty is ambiguous. Replace Executor's coarse branch with exhaustive typed dispatch. Consolidate result completion in `DeliveryTargets.record_target_outcome/5`, validating SafeEvidence before a single transaction that locks exact parent → claimed target → attempt_started and writes target/attempt status/facts once. Preserve existing retry exhaustion and aggregation semantics; never parse provider strings in Executor.</action>
  <acceptance_criteria>
    - "TargetAdapter typespec names every result variant and contains no provider-specific raw exception/body type."
    - "Result matrix explicitly covers every Pigeon 2.0.1 reason listed in COVERAGE.md, plus unknown conclusive and post-emission failures."
    - "Only `(status == 410 and reason in [ExpiredToken, Unregistered] and timestamp is a non-negative integer and host CAS == invalidated_exactly)` yields target/attempt invalidated; deleting any member of that tuple makes the matrix test fail closed as permanent."
    - "Every completion query repeats target id, delivery id, tenant id, claimed status, attempt id, target id, tenant id, and attempt_started predicates under row locks."
    - "Cross-scope and rotated-binding tests leave host fixtures and all non-matching Chimeway target rows unchanged."
  </acceptance_criteria>
  <verify><automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/apns/result_test.exs test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors</automated></verify>
  <done>Every accepted/rejected/expired/invalidated/ambiguous result has one exact, safe, durable target path and only documented retry authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map retries and project distinct safe operator outcomes</name>
  <files>lib/chimeway/dispatch/oban_worker.ex, lib/chimeway/safe_evidence.ex, test/chimeway/safe_evidence_test.exs, test/chimeway/traces_test.exs</files>
  <read_first>AGENTS.md, .planning/phases/100-optional-apns-adapter/100-CONTEXT.md, .planning/phases/100-optional-apns-adapter/100-03-SUMMARY.md, lib/chimeway/dispatch/oban_worker.ex, lib/chimeway/dispatch/executor.ex, lib/chimeway/delivery_targets.ex, lib/chimeway/safe_evidence.ex, lib/chimeway/traces.ex, lib/chimeway/inbox.ex, test/chimeway/safe_evidence_test.exs, test/chimeway/traces_test.exs, test/chimeway/dispatch/target_worker_test.exs</read_first>
  <behavior>
    - "Oban retries only typed pre-handoff/retryable target results while attempts remain; final retry writes retry_exhausted and returns :ok."
    - "Permanent, invalidated, expired, provider_accepted, and ambiguous results return :ok with terminal durable evidence and no resend."
    - "SafeEvidence accepts bounded provider_status, provider_reason, provider_timestamp, provider_code, retry_after_ms, corrective_action, and accepted_at only; unexpected/nested/token/body/credential fields are rejected or omitted."
    - "Trace exposes local attempt_started/dispatch intent, provider_accepted/rejected, retry exhaustion, invalidation, and existing protected-open/seen/read facts as separate ordered entries without deriving one from another."
    - "Repeated trace reads are byte-equivalent and concurrent completion projects one terminal attempt outcome."
  </behavior>
  <action>Update target-job mapping so only `:pre_handoff_retryable` and typed `:provider_retryable` produce an Oban error; carry the existing final-attempt exhaustion transaction and check absolute expiry again before every retry path. Extend SafeEvidence with fixed grammars/bounds for APNs HTTP status, normalized reason, response timestamp, retry delay, corrective action, and acceptance time; redact before validation and keep trace read-time revalidation. Update trace/explanation projections to name local dispatch intent, APNs acceptance/rejection, retry exhaustion, target invalidation, protected open, inbox seen, and inbox read independently; never synthesize engagement from provider acceptance. Add ordering/idempotency/concurrency tests over reloaded target histories and unsafe legacy facts.</action>
  <acceptance_criteria>
    - "Oban target clauses return retry only for the two authorized retry classes and write `retries_exhausted` on the final authorized attempt."
    - "Safe evidence rejects provider status outside 100..599, unrecognized/oversized reason/action codes, invalid timestamps, excessive retry delay, duplicate atom/string aliases, and arbitrary nested fields."
    - "Trace tests assert provider_accepted does not create or alter protected-open, seen, or read facts and each later fact remains independently attributable."
    - "Raw token, credentials, full payload, provider body, dispatcher reference, and exception sentinels are absent from SafeEvidence output, trace, logs, and telemetry capture."
  </acceptance_criteria>
  <verify><automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/safe_evidence_test.exs test/chimeway/traces_test.exs test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors</automated></verify>
  <done>Retry scheduling and operator evidence match the durable target outcome exactly, with exhaustion, invalidation, ambiguity, open, seen, and read kept distinct.</done>
</task>

</tasks>

## Artifacts this phase produces

- Expanded `Chimeway.TargetAdapter.deliver_result` variants for accepted, retryable, permanent, invalidated, expired, pre-handoff, and possible-handoff outcomes.
- Function `Chimeway.DeliveryTargets.record_target_outcome/5` and closed outcome attributes for provider_accepted, failed/pending retryable, failed terminal, invalidated, expired, and ambiguous_handoff.
- Oban target return mappings `:provider_retryable`, `:pre_handoff_retryable`, terminal completion, and final `retry_exhausted` behavior.
- SafeEvidence keys/validators `provider_status`, `provider_reason`, `provider_timestamp`, `provider_code`, `retry_after_ms`, `corrective_action`, and `accepted_at`.
- ExUnit file/module `test/chimeway/apns/result_test.exs` / `Chimeway.APNS.ResultTest`; complete Pigeon 2.0.1 reason fixtures, exact status/reason/timestamp triple and missing-member fixtures, CAS rotation/cross-scope fixtures, and completion-race fixtures.
- Expanded target-worker, SafeEvidence, and trace fixtures for retry exhaustion, expiry, invalidation, ambiguity, provider acceptance, protected open, seen, and read.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider result → lifecycle mutation | Untrusted status/reason data selects retry, terminal failure, or invalidation. |
| Chimeway → host invalidation | Exact original binding key requests compare-and-update authority. |
| Durable attempts → operator trace | Stored facts cross a read-time redaction/allowlist boundary. |

## STRIDE Threat Register

**Security gate:** OWASP ASVS Level 1 applies (`block_on: high`). Any unresolved high-severity threat blocks plan completion. T-100-16, T-100-17, and T-100-18 are bound to Task 1's result/worker automated command and its complete-triple, exact-CAS, cross-scope, and retry/ambiguity acceptance criteria. T-100-19 is bound to Task 2's evidence/trace automated command and its fixed-grammar plus recursive sentinel acceptance criteria; every named check must be green before this plan can complete.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-100-16 | Tampering | result classifier | high | mitigate | Exhaustive pinned matrix; unknown/incomplete results fail permanent; only the exact 410 status/reason/timestamp triple reaches invalidation. |
| T-100-17 | Elevation of Privilege | host invalidation CAS | high | mitigate | D-16 exact tenant/environment/topic/binding-revision callback plus exact target transaction and rotated/cross-scope tests. |
| T-100-18 | Tampering | retry/redrive | high | mitigate | D-09/D-12/D-13 typed retry allowlist, expiry recheck, terminal ambiguity, and final exhaustion transaction. |
| T-100-19 | Information Disclosure | provider response/evidence | high | mitigate | SafeEvidence fixed fields/bounds, redact-before-validate, read-time revalidation, and sentinel tests across traces/logs/telemetry. |
| T-100-20 | Repudiation | engagement narrative | medium | mitigate | D-11 distinct target outcomes and independent protected-open/seen/read trace entries prevent acceptance overclaiming. |
| T-100-21 | Denial of Service | concurrent completion | medium | mitigate | Exact row locks and started-attempt predicate yield one completion winner; loser is a no-op with no second mutation. |
</threat_model>

<verification>
Run both focused commands and then the complete target lifecycle suite: `scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/delivery_target_test.exs test/chimeway/orchestration/target_recovery_test.exs test/chimeway/dispatch/target_worker_test.exs test/chimeway/apns --warnings-as-errors`.
</verification>

<success_criteria>
- Every pinned result has a deterministic durable path and corrective action.
- Exact invalidation cannot cross tenant, environment, topic, installation, or binding revision.
- Only conclusively retryable/pre-handoff work retries; ambiguous handoff is terminal until explicit authorization.
- Operator evidence distinguishes dispatch, APNs result, exhaustion, invalidation, open, seen, and read without leaking provider material.
</success_criteria>

<output>
Create `.planning/phases/100-optional-apns-adapter/100-04-SUMMARY.md` when done.
</output>
