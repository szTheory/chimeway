---
phase: 97-tenant-identity-compatible-upgrade
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/trigger.ex
  - test/chimeway/tenant_identity_test.exs
autonomous: true
gap_closure: true
requirements: [TENANT-01, TENANT-02]
must_haves:
  truths:
    - "D-01/D-03: Trigger trims surrounding whitespace exactly once at its explicit write boundary and uses that canonical tenant value for the Event, every Notification and WorkflowRun, duplicate recovery, and dispatch options."
    - "D-02/D-04/D-07: a lifecycle created with padded nonblank tenant input is retrievable with the canonical tenant ID, while another tenant retains the established indistinguishable not-found result."
    - "TENANT-01 adjacency/idempotency edge: padded and canonical spellings of the same tenant plus the same idempotency key converge on one event; case-distinct tenant IDs remain distinct and are not merged."
    - "TENANT-01 empty edge: missing, nil, nonbinary, and trim-empty trigger tenant inputs retain the established validation failures and persist no lifecycle rows."
    - "TENANT-01 concurrency edge: concurrent padded/canonical submissions for one tenant and key converge through the composite uniqueness contract without creating an unqueryable row."
    - "FLAGGED ASSUMPTION (TENANT-01 ordering probe, unresolved): tenant_id is scalar and has no element ordering contract; recipient normalization/order remains unchanged and is backstopped by the existing trigger suite."
    - "FLAGGED ASSUMPTION (TENANT-02 unclassified probe, unresolved): existing reader normalization and wrong-tenant non-disclosure remain authoritative; this gap changes only the trigger write/dispatch boundary and reruns their focused contract."
  artifacts:
    - path: "lib/chimeway/trigger.ex"
      provides: "Single canonical tenant value propagated through the trigger transaction and post-commit dispatch"
    - path: "test/chimeway/tenant_identity_test.exs"
      provides: "Padded/canonical persistence, trace-read, duplicate, dispatch-option, validation, and concurrency regression proof"
  key_links:
    - from: "lib/chimeway/trigger.ex"
      to: "lib/chimeway/events/event.ex"
      via: "the normalized tenant returned at the trigger boundary is passed to event_changeset/5"
      pattern: "event_changeset.*tenant_id"
    - from: "lib/chimeway/trigger.ex"
      to: "lib/chimeway/notifications/notification.ex"
      via: "the same normalized tenant is copied into notification rows and workflow creation"
      pattern: "tenant_id: tenant_id"
    - from: "lib/chimeway/trigger.ex"
      to: "dispatcher.dispatch/2"
      via: "post-commit opts replace the raw tenant_id with the normalized value before dispatch"
      pattern: "Keyword.put.*:tenant_id"
  prohibitions:
    - "FLAGGED-UNVERIFIED — Trigger canonicalization MUST NOT infer tenant ownership, change case, or route storage; it may only trim the explicitly supplied nonblank tenant identity."
    - "FLAGGED-UNVERIFIED — Canonicalization MUST NOT weaken host-owned authorization or expose tenant identifiers or sensitive payload fields through new telemetry/operator output."
---

<objective>
Close the canonical tenant identity blocker from `97-VERIFICATION.md` by making the accepted trigger tenant representation identical to the representation used by scoped readers.

Purpose: Ensure a host can trigger, persist, dispatch, deduplicate, and retrieve one lifecycle tree with the same explicit tenant identity.
Output: A single normalized trigger boundary and executable padded-input regression coverage.

Spec-less fallback: Phase 97 has no SPEC. This plan carries the supplied TENANT-01 adjacency, empty, idempotency, and concurrency probes into executable truths; the scalar ordering probe and TENANT-02 unclassified probe remain explicit flagged assumptions rather than being dismissed.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-01-SUMMARY.md

<interfaces>
- `Chimeway.Trigger.trigger/3` continues to require an explicit `opts[:tenant_id]`; single-tenant compatibility does not authorize trigger writes.
- `Chimeway.TenantScope.resolve/1` defines the reader-side canonical representation by trimming surrounding whitespace.
- `Chimeway.Traces.get_trace/2` remains the public end-to-end read proof and wrong-tenant non-disclosure boundary.
- `dispatcher.dispatch(notifications, opts)` must receive the canonical tenant in `opts[:tenant_id]` without changing static Repo or Oban prefix behavior.
</interfaces>

<scope_note>The `ChimewayInbox.LiveAuth.ensure_authorized/2` valid-but-changed-context warning is not a Phase 97 blocker: verification demonstrated no data exposure and the two authoritative failed truths are confined to Trigger canonicalization and migration downgrade behavior. Leave that warning for a separately authorized hardening change.</scope_note>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one canonical tenant from padded trigger input through trace and dispatch</name>
  <read_first>
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
    - lib/chimeway/trigger.ex
    - lib/chimeway/tenant_scope.ex
    - test/chimeway/tenant_identity_test.exs
    - test/chimeway/trigger_pipeline_test.exs
  </read_first>
  <files>lib/chimeway/trigger.ex, test/chimeway/tenant_identity_test.exs</files>
  <behavior>
    - Test 1: triggering with `tenant_id: "  tenant-a  "` persists `"tenant-a"` on the Event and every Notification/WorkflowRun and `Traces.get_trace/2` retrieves it using `tenant_id: "tenant-a"`.
    - Test 2: a spy dispatcher receives `opts[:tenant_id] == "tenant-a"`, never the padded input, while no new tenant or payload field is added to telemetry.
    - Test 3: padded and canonical submissions with the same idempotency key return one success plus one duplicate for the same event; `tenant-a` and `Tenant-A` remain independent identities.
    - Test 4: concurrent padded/canonical submissions converge on one event, and missing/nil/nonbinary/trim-empty values preserve existing errors without inserts.
  </behavior>
  <action>Start RED in `test/chimeway/tenant_identity_test.exs`, then close the verifier's root cause per D-01/D-02/D-03/D-04/D-07. Collapse Trigger's current fetch-then-validation split into one explicit boundary operation that fetches `opts[:tenant_id]`, requires a binary, trims it once, rejects an empty normalized result with the established Trigger error contract, and returns only the canonical value. Replace `opts[:tenant_id]` with that canonical value before `do_trigger/7` continues so the same value reaches Event changeset attrs, Notification insert rows, WorkflowRun creation, composite duplicate lookup, and post-commit dispatcher options. Do not use compatibility configuration for a write, do not lowercase or otherwise transform the host identity, and do not add tenant/payload data to telemetry. Add a local spy dispatcher or equivalent deterministic test seam to inspect dispatch opts, plus canonical trace lookup and padded/canonical duplicate/concurrency coverage. Keep recipient normalization, stable notification key/version, static storage-prefix routing, duplicate inertness, and all existing error shapes unchanged.</action>
  <acceptance_criteria>
    - Padded nonblank Trigger input produces only canonical tenant values across Event, Notifications, WorkflowRuns, duplicate recovery, and dispatch opts.
    - The lifecycle is readable through `Traces.get_trace/2` with the canonical tenant and is absent to a different tenant.
    - Padded/canonical retries and concurrent submissions converge; case-distinct IDs remain distinct.
    - Missing, invalid, and trim-empty input persist no rows and retain the existing Trigger error tuples.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/trigger.ex test/chimeway/tenant_identity_test.exs &amp;&amp; mix test test/chimeway/tenant_identity_test.exs test/chimeway/tenant_scope_contract_test.exs test/chimeway/trigger_pipeline_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The same canonical explicit tenant identity controls trigger persistence, duplicate recovery, dispatch, and trace retrieval for padded input without changing tenant ownership or storage routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host Trigger options → canonical write boundary | Host-controlled tenant input becomes durable lifecycle ownership and dispatch authority. |
| Trigger transaction → scoped readers | Persisted identity must exactly match the canonical predicate readers apply. |
| Trigger result → dispatcher | Post-commit options must not reintroduce the raw tenant or sensitive payload data. |

## STRIDE Threat Register (ASVS L1; block-on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Residual Risk | Executable Verification |
|---|---|---|---|---|---|---|---|
| T-97-09-01 | Tampering | Trigger tenant normalization | high | mitigate | Normalize the explicit tenant once before persistence and overwrite the downstream opts value with the canonical result. | Host authorization of the claimed tenant remains host-owned. | `mix test test/chimeway/tenant_identity_test.exs --warnings-as-errors` |
| T-97-09-02 | Information Disclosure | Trigger-to-trace lookup | high | mitigate | Prove canonical owner read succeeds and wrong-tenant lookup retains `:not_found`. | Database timing side channels are outside ASVS L1 scope. | `mix test test/chimeway/tenant_identity_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` |
| T-97-09-03 | Spoofing | Case-distinct tenant IDs | medium | mitigate | Trim only surrounding whitespace; do not case-fold or infer equivalence between host-owned tenant IDs. | Hosts must select stable tenant identifiers. | `mix test test/chimeway/tenant_identity_test.exs --warnings-as-errors` |
| T-97-09-SC | Tampering | Package supply chain | low | accept | No dependency installation occurs; use the existing lockfile. | Existing dependency provenance remains governed by project audit gates. | `mix ci.audit` |

ASVS L1 mapping: V4 access control is proven by canonical owner/wrong-owner trace outcomes; V5 input validation is proven by padded, empty, malformed, idempotent, and concurrent Trigger cases. No new authentication/session authority is introduced.
</threat_model>

## Artifacts this phase produces

- Changed internal behavior: `Chimeway.Trigger` returns one canonical explicit tenant from its write boundary and passes canonicalized opts to dispatch.
- Expanded proof: `test/chimeway/tenant_identity_test.exs` covers padded persistence/readback, dispatch opts, duplicate equality, case distinction, invalid inputs, and concurrency.
- New public symbols/files: none.

<verification>
Run the focused identity, scope, and trigger suites. The proof must exercise real PostgreSQL rows and the public trace reader; source-only inspection is insufficient.
</verification>

<success_criteria>
- Padded nonblank Trigger tenant input persists and dispatches only its canonical trimmed identity.
- Canonical tenant lookup reads the lifecycle; wrong-tenant lookup remains non-disclosing.
- Existing trigger ordering, idempotency, validation, telemetry safety, host ownership, and static storage-prefix contracts remain green.
</success_criteria>

<output>
Create `.planning/phases/97-tenant-identity-compatible-upgrade/97-09-SUMMARY.md` when done.
</output>
