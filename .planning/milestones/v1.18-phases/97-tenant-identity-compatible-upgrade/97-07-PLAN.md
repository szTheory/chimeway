---
phase: 97-tenant-identity-compatible-upgrade
plan: 07
type: execute
wave: 3
depends_on: [97-02]
files_modified:
  - lib/chimeway/deliveries.ex
  - test/chimeway/orchestration/recovery_test.exs
  - test/chimeway/tenant_scope_contract_test.exs
autonomous: true
requirements: [TENANT-02]
must_haves:
  truths:
    - "D-04: recovery discovery, atomic claims, reloads, and replanning retain one resolved tenant throughout the chain."
    - "D-05/D-06: former recovery shapes use only the concrete compatibility tenant and otherwise fail closed."
    - "D-07: wrong-tenant recovery identifiers mutate/enqueue nothing and share the established absent/noop result."
    - "D-10: recovery tenant identity remains a row predicate and never changes storage or Oban prefix."
  artifacts:
    - path: "lib/chimeway/deliveries.ex"
      provides: "Tenant-scoped recovery discovery, claim, reload, and replan"
    - path: "test/chimeway/orchestration/recovery_test.exs"
      provides: "Cross-tenant recovery non-disclosure and mutation proof"
  key_links:
    - from: "lib/chimeway/deliveries.ex"
      to: "lib/chimeway/tenant_scope.ex"
      via: "scope resolves before recovery query, claim, and reload"
      pattern: "TenantScope.resolve"
---

<objective>
Extend the core tenant boundary through recovery discovery, claims, reloads, and replanning.

Purpose: Keep the recovery chain cohesive while removing it from the file-heavy Inbox/Admin expansion plan.
Output: Tenant-scoped recovery implementation and executable cross-tenant recovery contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-PATTERNS.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-02-SUMMARY.md

<interfaces>
- `Chimeway.TenantScope.resolve/1` returns one explicit or configured concrete tenant.
- Public recovery delegates from Plan 97-02 forward tenant-bearing options without changing established result shapes.
- Recovery zero-row behavior is represented by existing noop/not-found outcomes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scope recovery discovery, atomic claims, reloads, and replanning</name>
  <read_first>
    - lib/chimeway/deliveries.ex
    - lib/chimeway/tenant_scope.ex
    - lib/chimeway.ex
    - test/chimeway/orchestration/recovery_test.exs
    - test/chimeway/tenant_scope_contract_test.exs
    - .planning/milestones/v1.11-phases/70-recovery-auth-and-tenancy-hardening/70-CONTEXT.md
  </read_first>
  <files>lib/chimeway/deliveries.ex, test/chimeway/orchestration/recovery_test.exs, test/chimeway/tenant_scope_contract_test.exs</files>
  <behavior>
    - Test 1: recoverable-event and recoverable-delivery lists contain only explicit-tenant rows.
    - Test 2: begin/recover delivery atomic claims include tenant; wrong-tenant and absent IDs share the established zero-row/noop outcome.
    - Test 3: recover_event constrains Event, Notification, Delivery, reload, and replan steps by the same tenant.
    - Test 4: compatibility fallback follows the same concrete-config rules as Inbox/Admin.
  </behavior>
  <action>Apply D-04/D-05/D-06/D-07/D-10 across `list_recoverable_events/1`, `list_recoverable_deliveries/1`, `begin_recovery/2`, `recover_delivery/2`, and `recover_event/2`. Replace `scoped_tenant_id/1` and optional tenant helpers with `TenantScope.resolve/1`; include tenant in atomic `update_all` claims and replace unscoped delivery, Event, Notification, and downstream replan reads with tenant-aware query helpers. Preserve safe recovery evidence keys, status/age eligibility, dispatcher behavior, and existing noop/error outcomes. Where internal code already has a tenant-owned struct, use its durable field rather than compatibility fallback; never turn tenant into a storage prefix.</action>
  <acceptance_criteria>
    - Source: every recovery chain lookup after the first scoped claim retains the same tenant predicate.
    - Behavior: a cross-tenant recovery attempt mutates/enqueues nothing and has the same return as an absent row.
    - Behavior: matching-tenant recovery still uses the canonical dispatcher/replan path and safe evidence contract.
    - Test command: recovery and tenant-scope contract tests exit zero.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/deliveries.ex test/chimeway/orchestration/recovery_test.exs test/chimeway/tenant_scope_contract_test.exs &amp;&amp; mix test test/chimeway/orchestration/recovery_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Recovery reads, claims, reloads, mutations, and replanning remain inside one tenant and disclose nothing through wrong-tenant identifiers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| UUID inputs → recovery claims | Untrusted identifiers could target another tenant's delivery or event. |
| Scoped claim → reload/replan | Tenant scope can be lost after the first atomic predicate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Residual Risk | Executable Verification |
|---|---|---|---|---|---|---|---|
| T-97-06 | Elevation of Privilege | Recovery reload/replan | high | mitigate | Carry the resolved tenant into every direct read, atomic update, and planning call. | New recovery call sites require the same contract pattern. | `mix test test/chimeway/orchestration/recovery_test.exs --warnings-as-errors` |
| T-97-17 | Tampering | Recovery claims | high | mitigate | Include tenant in atomic claim predicates and emit no job/evidence on zero-row updates. | Database superusers can bypass application predicates. | `mix test test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` |
| T-97-SC | Tampering | Package installs | low | accept | No package install occurs; use the existing lockfile only. | Existing dependency provenance remains governed by project audit gates. | `mix ci.audit` |

## ASVS Level 1 Execution Contract

Research identifies V2-V5 as the applicable ASVS categories for Phase 97. The owning plan/task named below implements each control; every executor preserves these cross-plan obligations, and phase completion runs the exact evidence commands after all owning plans land.

| ASVS L1 control | Responsible plan/task | Concrete mitigation | Exact automated evidence |
|---|---|---|---|
| V2 Authentication | 97-03 Task 1 and Task 2 | Host-owned Inbox/Admin authentication must authorize a concrete tenant before package state gains lifecycle authority; Chimeway does not authenticate or infer membership. | `mix verify.inbox &amp;&amp; mix verify.admin` |
| V3 Session Management | 97-03 Task 1/Task 2, 97-05 Task 1, and 97-08 Task 1 | Re-run host authorization at LiveView event time and rebuild tenant-bearing Context options for every mutation or refresh; never fall back to a stale or tenantless session value. | `mix cmd --cd chimeway_inbox mix test --warnings-as-errors &amp;&amp; mix cmd --cd chimeway_admin mix test --warnings-as-errors` |
| V4 Access Control | 97-01 Task 1/Task 2, 97-02 Task 1/Task 2, 97-07 Task 1, and 97-08 Task 1 | Resolve one tenant before access, retain its predicate across every root/nested read, atomic update, reload, and replan, and map wrong scope to the established absent outcome. | `mix test test/chimeway/tenant_scope_contract_test.exs test/chimeway/orchestration/recovery_test.exs --warnings-as-errors &amp;&amp; mix verify.inbox &amp;&amp; mix verify.admin` |
| V5 Input Validation | 97-01 Task 1/Task 2 and 97-04 Task 1/Task 2 | Accept only trimmed nonblank binary tenant IDs, validate reconciliation UUIDs, enforce strict CLI flag combinations, and reject invalid scope before any query or mutation. | `mix test test/chimeway/tenant_identity_test.exs test/chimeway/tenant_scope_contract_test.exs test/chimeway/reconciliation_test.exs test/chimeway/mix/tasks/reconcile_tenants_test.exs --warnings-as-errors` |

</threat_model>

## Artifacts this phase produces

- Tenant-scoped recovery discovery, claim, reload, and replan chains in `Chimeway.Deliveries`.
- Expanded recovery and shared tenant-scope contract tests.

<verification>
Run focused recovery and shared tenant-scope tests, then include them in the wave-level `mix ci.test` gate.
</verification>

<success_criteria>
- Recovery cannot cross the resolved tenant at any step.
- Wrong-tenant IDs preserve absence/noop outcomes and create no side effects.
- Compatibility and static storage contracts match the rest of the core API.
</success_criteria>

<output>
Create `.planning/phases/97-tenant-identity-compatible-upgrade/97-07-SUMMARY.md` when done.
</output>
