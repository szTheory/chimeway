---
phase: 97-tenant-identity-compatible-upgrade
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - chimeway_inbox/lib/chimeway_inbox/live_auth.ex
  - chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs
autonomous: true
gap_closure: true
requirements: [TENANT-02]
must_haves:
  truths:
    - "D-04/D-07/TENANT-02: LiveAuth.ensure_authorized/2 accepts only the exact recipient/tenant pair captured at mount; any later successful callback result with a changed recipient, changed tenant, or both redirects fail closed."
    - "D-04/TENANT-02: every BellDropdownLive event path using ensure_authorized/2 receives an error tuple rather than a case-clause crash when host identity changes after mount."
    - "D-07/TENANT-02: post-mount identity drift performs no inbox read or mutation under either the old or newly returned tenant/recipient context."
  assumptions:
    - "FLAGGED ASSUMPTION — TENANT-02 manual review was unclassified; the VERIFICATION.md warning identifies successful-but-changed recipient/tenant callbacks as the remaining Inbox fail-closed edge."
  artifacts:
    - path: "chimeway_inbox/lib/chimeway_inbox/live_auth.ex"
      provides: "Total fail-closed handling for successful post-mount identity mismatch"
    - path: "chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs"
      provides: "Changed-recipient and changed-tenant redirect/no-mutation proof"
  key_links:
    - from: "ChimewayInbox.LiveAuth.ensure_authorized/2"
      to: "BellDropdownLive.handle_event/3"
      via: "all resolved contexts return either exact-match {:ok, socket} or redirected {:error, socket}"
      pattern: "{:error, redirect"
  prohibitions:
    - "FLAGGED-UNVERIFIED — A successful host callback with a different recipient or tenant must not crash the LiveView or retain the mounted identity's authority."
    - "FLAGGED-UNVERIFIED — Post-mount identity drift must not mutate notification state in either tenant."
    - "FLAGGED-UNVERIFIED — ChimewayInbox must not decide membership or translate a changed host identity into a new mounted session implicitly."
---

<objective>
Make Inbox event-time authorization total and fail closed when host callbacks successfully return a different recipient or tenant after mount.

Purpose: Eliminate a case-clause crash edge and ensure long-lived inbox sockets never inherit authority across host identity changes.
Output: A successful-mismatch redirect clause and mounted changed-recipient/tenant no-mutation tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-03-SUMMARY.md

<interfaces>
- `ChimewayInbox.LiveAuth.ensure_authorized(socket, :inbox_bell)` returns `{:ok, socket}` only when re-resolved recipient and tenant exactly match mounted assigns, otherwise `{:error, redirected_socket}`.
- `ChimewayInbox.Auth.current_recipient/2` and `current_tenant/2` are host-owned callbacks and may return a different valid identity after mount.
- `ChimewayInbox.Live.BellDropdownLive` calls `ensure_authorized/2` before toggle, mark-read, mark-all-read, pagination, and retry events.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redirect one changed tenant before an inbox mutation</name>
  <read_first>
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
    - chimeway_inbox/lib/chimeway_inbox/live_auth.ex
    - chimeway_inbox/lib/chimeway_inbox/live/bell_dropdown_live.ex
    - chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs
    - chimeway_inbox/test/support/allow_auth.ex
    - chimeway_inbox/test/support/live_view_case.ex
  </read_first>
  <files>chimeway_inbox/lib/chimeway_inbox/live_auth.ex, chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs</files>
  <behavior>
    - Test 1: mount for `user:42`/tenant-a, change the host callback to return tenant-b, then attempt mark_read; the LiveView redirects to the configured unauthorized path.
    - Test 2: the target Notification remains unread and no tenant-b state is loaded or rendered.
    - Test 3: an unchanged successful callback pair still permits the same mark_read action.
  </behavior>
  <action>Add a test-local mutable auth module whose callbacks read controlled test state, mount with the original pair, then change only the tenant before invoking the production row action. In `LiveAuth.ensure_authorized/2`, retain the exact-match guarded clause and add an explicit clause for any other `{:ok, recipient_identity, tenant_id}` result that returns `{:error, redirect(socket, to: unauthorized_redirect())}`. Keep callback errors on the same redirect path. Do not update mounted assigns to the new pair, do not retry under the new tenant, and do not change host-owned authorization semantics. Assert persisted notification state after redirect so the test proves non-mutation rather than merely avoiding a crash.</action>
  <verify>
    <automated>mix format --check-formatted chimeway_inbox/lib/chimeway_inbox/live_auth.ex chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs &amp;&amp; mix cmd --cd chimeway_inbox mix test test/chimeway_inbox/live/bell_dropdown_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - A changed valid tenant produces the configured redirect rather than a process crash or successful mutation.
    - The old tenant's target Notification remains unread and no new-tenant rows appear.
    - The unchanged-pair control still succeeds in the same focused test file.
  </acceptance_criteria>
  <done>A mounted inbox socket cannot mutate through a host tenant switch, and the successful mismatch is represented by the documented fail-closed tuple.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover recipient and combined identity drift across Bell events</name>
  <read_first>
    - chimeway_inbox/lib/chimeway_inbox/live_auth.ex
    - chimeway_inbox/lib/chimeway_inbox/live/bell_dropdown_live.ex
    - chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs
  </read_first>
  <files>chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs</files>
  <behavior>
    - Test 1: a changed valid recipient with the original tenant redirects on the next event and does not mark either recipient's row.
    - Test 2: changing both recipient and tenant redirects with the same fail-closed shape.
    - Test 3: a non-mutating toggle event also redirects under identity drift, proving the shared LiveAuth seam covers every handler that calls it.
    - Test 4: existing callback-error, cross-tenant-row, pagination, and mark-read tests remain green.
  </behavior>
  <action>Expand the mounted tests to exercise changed recipient alone and changed recipient plus tenant using the same controlled auth seam. Assert the configured redirect shape, LiveView survival semantics expected by Phoenix.LiveViewTest, and unchanged database state for both the originally visible row and any row belonging to the newly returned identity. Include a toggle-panel mismatch case to prove the shared authorization guard is not mutation-specific. Reset all application environment/test state in `on_exit` because the suite is `async: false`; do not add ambient process authorization or modify BellDropdownLive handlers that already use `ensure_authorized/2`.</action>
  <verify>
    <automated>mix format --check-formatted chimeway_inbox/lib/chimeway_inbox/live_auth.ex chimeway_inbox/test/chimeway_inbox/live/bell_dropdown_live_test.exs &amp;&amp; mix cmd --cd chimeway_inbox mix test test/chimeway_inbox/live/bell_dropdown_live_test.exs --warnings-as-errors &amp;&amp; mix verify.inbox</automated>
  </verify>
  <acceptance_criteria>
    - Changed recipient, changed tenant, and combined mismatch cases all redirect without case-clause exits.
    - Database assertions show no read-state mutation under old or new identity.
    - `mix verify.inbox` exits zero, preserving package and demo-host gate parity.
  </acceptance_criteria>
  <done>All successful post-mount identity mismatch shapes fail closed through the common LiveAuth contract and the full Inbox verification lane remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host auth callbacks → mounted LiveView | Current recipient/tenant authority is re-evaluated for a long-lived socket. |
| Mounted assigns → core Inbox API | The exact mounted pair scopes subsequent reads and mutations. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-97-14-01 | Spoofing / Elevation of Privilege | `LiveAuth.ensure_authorized/2` | high | mitigate | Accept only exact equality with mounted recipient and tenant; redirect every successful mismatch. | `mix cmd --cd chimeway_inbox mix test test/chimeway_inbox/live/bell_dropdown_live_test.exs --warnings-as-errors` |
| T-97-14-02 | Tampering | Bell event mutations | high | mitigate | Return the redirected error tuple before handlers call any core Inbox mutation; assert persisted rows unchanged. | `mix cmd --cd chimeway_inbox mix test test/chimeway_inbox/live/bell_dropdown_live_test.exs --warnings-as-errors` |
| T-97-14-03 | Denial of Service | Successful mismatch case handling | medium | mitigate | Make the callback result case total so identity drift follows a controlled redirect instead of crashing the LiveView. | `mix verify.inbox` |
| T-97-14-04 | Information Disclosure | Changed identity rendering | high | mitigate | Do not rebind assigns or reload under the new pair; redirect before further event processing. | `mix verify.inbox` |
| T-97-14-SC | Tampering | Package supply chain | low | accept | No package install or dependency change occurs. | `mix ci.audit` |

ASVS L1 mapping: V2/V3 preserve host-owned identity and re-evaluate it at event time; V4 requires exact recipient/tenant equality before access. High-severity spoofing, mutation, and disclosure threats block on mounted package tests and the named Inbox gate.
</threat_model>

## Artifacts this phase produces

- New production files: None.
- New production symbols: None.
- Changed production function: `ChimewayInbox.LiveAuth.ensure_authorized/2` gains total successful-mismatch handling.
- Expanded test-only symbol: a mutable host-auth module local to `bell_dropdown_live_test.exs`.
- New external API or dependency: None.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Explicit tenant-safe lifecycle access | 97-14 | COVERED | Long-lived Inbox access cannot cross a host identity change. |
| REQ | TENANT-02 | Inbox reads/mutations only in explicit tenant scope | 97-14 | COVERED | Exact mounted recipient/tenant pair is required at event time. |
| RESEARCH | — | Host-owned auth with event-time context revalidation | 97-14 | COVERED | The optional package redirects rather than deciding a new identity. |
| CONTEXT | D-04 | Scope every Inbox read/mutation | 97-14 | COVERED | Mismatches stop before handlers call core APIs. |
| CONTEXT | D-07 | Wrong-tenant state is non-disclosing | 97-14 | COVERED | No reassignment or new-identity reload occurs. |

## Plan-set Source Audit

| Source | ID | Required gap-closure scope | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Safely query and upgrade lifecycle state within explicit tenant boundaries | 97-12, 97-13, 97-14 | COVERED |
| REQ | TENANT-01 | Immutable tenant identity/composite idempotency | 97-01, 97-09 | COVERED — already verified; five spec-less probes remain flagged assumptions in 97-12 |
| REQ | TENANT-02 | Joined Admin isolation, Feed revocation, Inbox identity drift | 97-12, 97-14 | COVERED |
| REQ | TENANT-03 | Delivery-complete reconciliation transaction and evidence | 97-13 | COVERED |
| RESEARCH | — | Tenant on every root/nested predicate; host-owned LiveView auth; explicit locked reconciliation; static prefix | 97-12, 97-13, 97-14 | COVERED |
| CONTEXT | D-01..D-03 | Durable identity and idempotency | 97-01, 97-09 | COVERED — unchanged |
| CONTEXT | D-04..D-07 | Explicit scope, compatibility, non-disclosure | 97-12, 97-14 plus executed plans | COVERED |
| CONTEXT | D-08..D-09 | Non-guessing additive reconciliation | 97-13 plus executed migration plans | COVERED |
| CONTEXT | D-10..D-11 | Static routing and deterministic migration modes | 97-13 plus executed plans | COVERED |

No required source item is unplanned. Deferred ideas remain excluded. The unclassified TENANT-02 and TENANT-03 manual-review probes are surfaced as flagged assumptions in their owning plans and resolved only to the concrete failures named by current verification evidence.

<verification>
Run the mounted Inbox test file followed by `mix verify.inbox`. Assertions must cover redirect outcome and persisted no-mutation state for tenant, recipient, and combined drift; no conversational UAT is used.
</verification>

<success_criteria>
- Every successful-but-changed host identity result redirects fail closed rather than crashing or rebinding.
- No changed-identity event reads or mutates inbox state.
- Focused tests and `mix verify.inbox` exit zero with CI/local entrypoint parity preserved.
</success_criteria>

<output>
Create `.planning/phases/97-tenant-identity-compatible-upgrade/97-14-SUMMARY.md` when done.
</output>
