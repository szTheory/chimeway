---
phase: 97-tenant-identity-compatible-upgrade
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - test/chimeway/runtime_prefix_integration_test.exs
autonomous: true
gap_closure: true
requirements: [TENANT-02, TENANT-03]
must_haves:
  truths:
    - "D-04/TENANT-02: the maintained runtime-prefix recovery proof supplies tenant_id: \"acme\" to begin_recovery/2, recover_delivery/2, and recover_event/2, so every successful mutation is explicitly tenant-scoped."
    - "D-07/TENANT-02: the test correction does not relax production fail-closed behavior; missing, wrong-tenant, and absent recovery requests retain their indistinguishable noop/absent contract under the focused tenant-scope suite."
    - "D-10/D-11/TENANT-03: all three explicitly scoped recovery paths still operate inside the configured static runtime prefix, and mix verify.runtime_prefix exits successfully."
    - "FLAGGED ASSUMPTION (TENANT-01 adjacency probe, unresolved): boundary behavior for neighboring tenant inputs remains owned by the executed tenant-identity plans; this verification-only recovery correction adds no new resolution."
    - "FLAGGED ASSUMPTION (TENANT-01 empty probe, unresolved): empty trigger-tenant behavior remains owned by the executed tenant-identity plans; this verification-only recovery correction adds no new resolution."
    - "FLAGGED ASSUMPTION (TENANT-01 ordering probe, unresolved): tenant_id is scalar and has no element-ordering contract; this verification-only recovery correction adds no new resolution."
    - "FLAGGED ASSUMPTION (TENANT-01 idempotency probe, unresolved): tenant-scoped trigger idempotency remains owned by the executed tenant-identity plans; this verification-only recovery correction adds no new resolution."
    - "FLAGGED ASSUMPTION (TENANT-01 concurrency probe, unresolved): concurrent trigger convergence remains owned by the executed tenant-identity plans; this verification-only recovery correction adds no new resolution."
    - "FLAGGED ASSUMPTION (TENANT-02 unclassified probe, unresolved): recovery scope and wrong-tenant non-disclosure remain authoritative; this plan only aligns the maintained runtime-prefix proof with that contract."
    - "FLAGGED ASSUMPTION (TENANT-03 unclassified probe, unresolved): static-prefix compatibility remains authoritative; this plan changes no migration or storage-routing behavior."
  artifacts:
    - path: "test/chimeway/runtime_prefix_integration_test.exs"
      provides: "Explicit-tenant recovery proof under the configured static runtime prefix"
  key_links:
    - from: "test/chimeway/runtime_prefix_integration_test.exs"
      to: "Chimeway.Deliveries.begin_recovery/2, recover_delivery/2, recover_event/2"
      via: "each recovery options keyword list carries tenant_id: \"acme\""
      pattern: "tenant_id: \"acme\""
    - from: "mix.exs"
      to: "test/chimeway/runtime_prefix_integration_test.exs"
      via: "the verify.runtime_prefix alias executes the maintained runtime-prefix integration suite"
      pattern: "verify.runtime_prefix"
  prohibitions:
    - "FLAGGED-UNVERIFIED — Restoring the verification gate MUST NOT weaken or bypass explicit tenant scope in production recovery APIs."
    - "FLAGGED-UNVERIFIED — The runtime-prefix proof MUST NOT reveal whether a recovery row exists outside the supplied tenant."
    - "FLAGGED-UNVERIFIED — Tenant identity MUST NOT become a dynamic Ecto, Oban, or per-request storage prefix while repairing this proof."
---

<objective>
Close the sole remaining Phase 97 verification gap by aligning the maintained runtime-prefix recovery proof with the explicit tenant contract already implemented by Plan 97-07.

Purpose: Restore executable evidence that tenant-safe recovery succeeds inside the configured static storage prefix without weakening fail-closed non-disclosure.
Output: Three corrected recovery call sites in the existing runtime-prefix integration test and a green `mix verify.runtime_prefix` gate.

Spec-less fallback: Phase 97 has no plain SPEC. All seven supplied unresolved edge rows remain visible as flagged assumptions in `must_haves.truths`; none is silently dismissed, converted to a backstop, or claimed as resolved by this test-only gap plan. Descriptor-less prohibition recall retains the three phase-specific safety constraints above without fabricating check descriptors.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-07-SUMMARY.md

<interfaces>
- `Chimeway.Deliveries.begin_recovery/2`, `recover_delivery/2`, and `recover_event/2` require tenant-bearing options and fail closed when scope is absent or wrong.
- `mix verify.runtime_prefix` runs `test/chimeway/runtime_prefix_integration_test.exs` together with repository-prefix and generated-prefixed proof suites.
- The integration fixture already assigns tenant `"acme"` to its recovery rows and configures one static runtime prefix; this plan supplies that same row identity as an API predicate, not as storage routing.
</interfaces>

<scope_note>This is a verification-only gap closure. Do not change `lib/chimeway/deliveries.ex`, tenant resolution, public signatures, migrations, Mix aliases, prefix configuration, or recovery result assertions.</scope_note>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove explicit-tenant recovery through the maintained runtime-prefix gate</name>
  <read_first>
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-07-SUMMARY.md
    - test/chimeway/runtime_prefix_integration_test.exs
    - test/chimeway/tenant_scope_contract_test.exs
    - lib/chimeway/deliveries.ex
    - mix.exs
  </read_first>
  <files>test/chimeway/runtime_prefix_integration_test.exs</files>
  <behavior>
    - Test 1: `Deliveries.begin_recovery/2` receives `tenant_id: "acme"` with the existing recovery evidence options and returns the matching claimed delivery.
    - Test 2: `Deliveries.recover_delivery/2` receives `tenant_id: "acme"` with the existing confirmation/evidence options and returns the matching succeeded delivery.
    - Test 3: `Deliveries.recover_event/2` receives `tenant_id: "acme"` with the existing recovery evidence options and returns the matching event and planned channels.
    - Test 4: the focused tenant-scope contract still proves unscoped, wrong-tenant, and absent recovery requests fail closed without mutation, and the complete runtime-prefix alias exits zero.
  </behavior>
  <action>In `test/chimeway/runtime_prefix_integration_test.exs`, update only the three recovery option lists identified by `97-VERIFICATION.md`: add `tenant_id: "acme"` to the calls for `Deliveries.begin_recovery/2`, `Deliveries.recover_delivery/2`, and `Deliveries.recover_event/2` per D-04. Preserve their existing `now`, age threshold, source, reason, actor/confirmation evidence, success assertions, fixture tenant values, and configured runtime-prefix assertions. Do not edit production recovery code or enable compatibility fallback to make the test pass. Do not pass tenant identity as `:prefix`, alter the Mix alias, or weaken the separate missing/wrong/absent-scope tests; D-07 requires those outcomes to remain non-disclosing and D-10/D-11 require storage routing to remain static. Run the focused tenant-scope contract before the named runtime-prefix gate so a green result proves both fail-closed rejection and matching-tenant success.</action>
  <acceptance_criteria>
    - The begin-recovery call includes `tenant_id: "acme"` and retains all existing evidence/assertion behavior.
    - The delivery-recovery call includes `tenant_id: "acme"` and retains all existing confirmation/evidence/assertion behavior.
    - The event-recovery call includes `tenant_id: "acme"` and retains all existing event/channel assertions.
    - `mix test test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` exits zero, preserving fail-closed recovery and wrong-tenant non-disclosure.
    - `mix verify.runtime_prefix` exits zero and proves matching-tenant recovery stays inside the configured static runtime prefix.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted test/chimeway/runtime_prefix_integration_test.exs &amp;&amp; mix test test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors &amp;&amp; mix verify.runtime_prefix</automated>
  </verify>
  <done>The maintained runtime-prefix gate passes with all three recovery operations explicitly scoped to `"acme"`, while the focused scope contract confirms unscoped and wrong-tenant recovery still fail closed without disclosure or mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runtime-prefix test fixture → production recovery APIs | Test-owned row identifiers and tenant scope cross into real recovery query/mutation predicates. |
| Named verification alias → release evidence | A green gate is trusted to prove both static-prefix operation and the explicit tenant contract. |

## STRIDE Threat Register (ASVS L1; block-on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Residual Risk | Executable Verification |
|---|---|---|---|---|---|---|---|
| T-97-11-01 | Tampering / Elevation of Privilege | Runtime-prefix recovery proof | high | mitigate | Supply the fixture's explicit `"acme"` tenant to every real recovery API call; do not alter production resolution or compatibility behavior. | The proof exercises one authorized tenant; broader tenant pairs remain covered by the focused recovery/scope suites. | `mix verify.runtime_prefix` |
| T-97-11-02 | Information Disclosure | Wrong-tenant recovery outcomes | high | mitigate | Retain the independent contract that unscoped, wrong-tenant, and absent identifiers share fail-closed outcomes and create no mutation. | Database timing side channels are outside ASVS L1 scope. | `mix test test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` |
| T-97-11-03 | Elevation of Privilege | Static storage routing | high | mitigate | Treat `tenant_id` only as a row predicate and leave the configured runtime prefix plus Mix alias unchanged. | Host database-prefix misconfiguration remains outside this test-only correction. | `mix verify.runtime_prefix` |
| T-97-11-SC | Tampering | Package supply chain | low | accept | No dependency installation occurs and no package-manager surface changes. | Existing dependency provenance remains governed by project audit gates. | `mix ci.audit` |

ASVS L1 mapping: V4 access control is exercised by explicit matching-tenant success plus wrong/missing-tenant non-disclosure; V5 input handling is preserved by the existing tenant-scope contract. No authentication, session, schema, external API, or production runtime surface changes.
</threat_model>

## Artifacts this phase produces

- New symbols: none.
- New files: none beyond this execution plan and its eventual summary.
- Modified existing proof: `test/chimeway/runtime_prefix_integration_test.exs` adds explicit `tenant_id: "acme"` to three recovery invocations.
- External API integration: none; the task invokes only in-repository Elixir APIs and local PostgreSQL-backed verification aliases, with no network protocol, third-party endpoint, credential, retry, or provider contract.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safely identify, query, and upgrade lifecycle state within an explicit tenant boundary | 97-01..97-11 | COVERED | 97-11 restores the sole red named verification gate without reopening delivered behavior. |
| REQ | TENANT-01 | Immutable tenant identity and tenant-scoped idempotency | 97-01, 97-09 | COVERED | No TENANT-01 implementation is changed; five unresolved probe rows remain flagged above. |
| REQ | TENANT-02 | Explicit tenant scope for recovery and non-disclosing fail-closed behavior | 97-01..03, 97-05, 97-07..09, 97-11 | COVERED | 97-11 aligns all three maintained recovery proof calls and reruns fail-closed evidence. |
| REQ | TENANT-03 | Static-prefix-compatible additive upgrade and reconciliation | 97-01, 97-04, 97-06, 97-10, 97-11 | COVERED | 97-11 restores the named static runtime-prefix gate; no migration behavior changes. |
| RESEARCH | — | Recovery scope must survive discovery, claim, reload, and replan while storage routing stays static | 97-07, 97-11 | COVERED | Production chain is delivered; 97-11 supplies its required test input and proves the named gate. |
| RESEARCH | — | Objective acceptance uses named executable verification entrypoints | 97-03, 97-05..08, 97-10..11 | COVERED | `mix verify.runtime_prefix` is the plan's terminal automated gate. |
| CONTEXT | D-01..D-03 | Durable tenant identity and composite trigger idempotency | 97-01, 97-09 | COVERED | Unchanged by the remaining recovery-test gap. |
| CONTEXT | D-04 | Explicit scope on every lifecycle read/mutation | 97-01..03, 97-05, 97-07..09, 97-11 | COVERED | Three stale recovery proof calls become explicit. |
| CONTEXT | D-05..D-06 | Formerly unscoped signatures fail closed unless concrete compatibility tenant is configured | 97-01..03, 97-07, 97-11 | COVERED | Focused scope suite remains a required gate; no compatibility fallback is added. |
| CONTEXT | D-07 | Cross-tenant and absent outcomes do not disclose existence | 97-01..03, 97-05, 97-07..09, 97-11 | COVERED | Focused contract proves fail-closed parity before runtime-prefix success. |
| CONTEXT | D-08..D-09 | Non-guessing additive migration and explicit reconciliation | 97-01, 97-04, 97-06, 97-10 | COVERED | No migration/reconciliation change belongs to this gap. |
| CONTEXT | D-10..D-11 | Static storage routing and deterministic public/prefixed compatibility | 97-01, 97-06, 97-09..11 | COVERED | The repaired gate continues to exercise configured static-prefix storage only. |

No source item is missing. The seven unresolved specless rows remain explicitly flagged in `must_haves.truths`; this verification-only slice neither fabricates resolutions nor silently drops them. Prohibition recall keeps only phase-specific non-disclosure, explicit-scope, and static-routing constraints as descriptor-less flagged items; generic injection and dependency provenance remain canon-owned by the STRIDE register and `$gsd-secure-phase`.

<verification>
Run formatting for the sole modified test, then the focused tenant-scope contract, then `mix verify.runtime_prefix`. All evidence is objectively machine-testable; no conversational or manual UAT is permitted.
</verification>

<success_criteria>
- All three runtime-prefix recovery invocations carry the explicit `"acme"` tenant.
- Matching-tenant recovery succeeds under the configured static prefix.
- Missing, wrong-tenant, and absent recovery remain fail closed and non-disclosing.
- `mix verify.runtime_prefix` exits successfully without production, schema, migration, alias, dependency, or external-API changes.
</success_criteria>

<output>
Create `.planning/phases/97-tenant-identity-compatible-upgrade/97-11-SUMMARY.md` when done.
</output>
