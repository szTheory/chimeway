---
phase: 97-tenant-identity-compatible-upgrade
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/admin.ex
  - test/chimeway/admin_test.exs
  - chimeway_admin/lib/chimeway_admin/live/feed_live.ex
  - chimeway_admin/test/chimeway_admin/live/feed_live_test.exs
autonomous: true
gap_closure: true
requirements: [TENANT-02]
must_haves:
  truths:
    - "D-04/D-07/TENANT-02: every Admin DTO assembled from Event, Notification, and Delivery rows admits only a lifecycle tree whose participating rows all carry the supplied tenant, and malformed cross-tenant links disclose no parent or child facts."
    - "D-04/TENANT-02: recent problems, definitions, feed rows, delivery recovery candidates, and no-delivery event candidates have adversarial tests for mismatched Event, Notification, and Delivery ownership."
    - "D-04/TENANT-02: FeedLive re-runs host :view_feed authorization inside handle_event/3 before executing a search, and post-mount revocation redirects without querying or rendering feed rows."
  assumptions:
    - "FLAGGED ASSUMPTION — TENANT-01 adjacency/neighbor-input behavior is already verified by executed Plans 97-01 and 97-09; this gap plan does not reinterpret it."
    - "FLAGGED ASSUMPTION — TENANT-01 empty-input behavior is already verified by executed Plans 97-01 and 97-09; this gap plan does not reinterpret it."
    - "FLAGGED ASSUMPTION — TENANT-01 ordering remains scalar tenant identity plus deterministic recipient ordering from the executed trigger contract; no new collection-order contract is inferred."
    - "FLAGGED ASSUMPTION — TENANT-01 idempotency behavior is already verified by the composite tenant/key contract; this gap plan changes no write identity."
    - "FLAGGED ASSUMPTION — TENANT-01 concurrency behavior is already verified by the trigger convergence tests; this gap plan changes only reads and event-time authorization."
    - "FLAGGED ASSUMPTION — TENANT-02 manual review was unclassified; the current VERIFICATION.md makes joined-row isolation and Feed post-mount revocation the executable scope of this closure."
  artifacts:
    - path: "lib/chimeway/admin.ex"
      provides: "Tenant-coherent joined Admin read models"
    - path: "test/chimeway/admin_test.exs"
      provides: "Adversarial split-tenant lifecycle coverage for every affected Admin DTO"
    - path: "chimeway_admin/lib/chimeway_admin/live/feed_live.ex"
      provides: "Event-time :view_feed authorization before search"
    - path: "chimeway_admin/test/chimeway_admin/live/feed_live_test.exs"
      provides: "Mounted FeedLive post-revocation redirect proof"
  key_links:
    - from: "lib/chimeway/admin.ex"
      to: "chimeway_events, chimeway_notifications, chimeway_deliveries"
      via: "each joined lifecycle level is constrained to the one resolved tenant; left joins keep their tenant condition in ON when absence is meaningful"
      pattern: "tenant_id == ^tenant_id"
    - from: "chimeway_admin/lib/chimeway_admin/live/feed_live.ex"
      to: "ChimewayAdmin.LiveAuth.ensure_authorized/3"
      via: "handle_event(\"search\", ...) re-checks :view_feed before Context.read_opts/2 and Chimeway.admin_feed/1"
      pattern: "ensure_authorized.*:view_feed"
  prohibitions:
    - "FLAGGED-UNVERIFIED — A joined Admin DTO must not disclose lifecycle facts from a row owned by a tenant other than the supplied tenant."
    - "FLAGGED-UNVERIFIED — A connected FeedLive must not retain search authority after the host revokes :view_feed access."
    - "FLAGGED-UNVERIFIED — Tenant identity must not be inferred from a lifecycle identifier, recipient, or static storage prefix."
---

<objective>
Close the tenant-isolation regressions in joined Admin read models and Feed Debug's event-time authorization boundary.

Purpose: Make malformed or partially reconciled lifecycle trees non-disclosing and ensure a mounted operator cannot search after host authorization changes.
Output: Tenant-coherent Admin queries, adversarial DTO tests, an authorized Feed search handler, and post-mount revocation coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-05-SUMMARY.md

<interfaces>
- `Chimeway.Admin.recent_problem_deliveries/1`, `definitions/1`, `feed/1`, and `recovery_candidates/1` first call `Chimeway.TenantScope.resolve/1` and preserve their existing DTO shapes.
- The joined schemas expose `Event.tenant_id`, `Notification.tenant_id`, and `Delivery.tenant_id` on the durable event → notification → delivery spine.
- `ChimewayAdmin.LiveAuth.ensure_authorized(socket, action, extra_context \\ %{})` returns `{:ok, socket}` or `{:error, redirected_socket}`.
- `ChimewayAdmin.Context.read_opts/2` produces the tenant-bearing options passed to `Chimeway.admin_feed/1`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject one split-tenant recent-problem lifecycle end to end</name>
  <read_first>
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
    - lib/chimeway/admin.ex
    - test/chimeway/admin_test.exs
    - lib/chimeway/delivery.ex
    - lib/chimeway/notifications/notification.ex
    - lib/chimeway/events/event.ex
  </read_first>
  <files>lib/chimeway/admin.ex, test/chimeway/admin_test.exs</files>
  <behavior>
    - Test 1: a tenant-a Delivery joined to a tenant-b Notification/Event yields no recent-problem DTO for tenant-a or tenant-b.
    - Test 2: a tenant-a Event/Notification joined to a tenant-b Delivery yields no recent-problem DTO for either tenant.
    - Test 3: a fully tenant-a lifecycle still yields the unchanged allowlisted recent-problem DTO.
  </behavior>
  <action>Start with failing real-Repo adversarial tests that deliberately change tenant columns after creating linked rows. Then update `recent_problem_deliveries/1` per D-04 and D-07 so the query requires the resolved tenant on `d.tenant_id`, `n.tenant_id`, and `e.tenant_id` in the database predicate. Keep the association joins by durable IDs and require all three tenant values to equal the supplied tenant, which also establishes row-to-row equality. Preserve ordering, limit, DTO keys, redaction behavior, static `repo_opts/1`, and the established empty-list outcome for wrong-tenant or malformed trees. Do not repair inconsistent rows in a read path and do not derive authority from whichever joined row happens to match.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/admin.ex test/chimeway/admin_test.exs &amp;&amp; mix test test/chimeway/admin_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/chimeway/admin_test.exs --warnings-as-errors` exits zero with mismatches at Delivery and parent levels.
    - The valid tenant-a recent-problem DTO retains the exact existing allowlist and ordering.
    - Neither tenant query returns recipient, event, correlation, status, or channel facts from a split-tenant tree.
  </acceptance_criteria>
  <done>A production Admin call cannot traverse a mismatched delivery-notification-event join to disclose a recent-problem row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce lifecycle-wide tenant coherence for definitions, feed, and recovery DTOs</name>
  <read_first>
    - lib/chimeway/admin.ex
    - test/chimeway/admin_test.exs
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
  </read_first>
  <files>lib/chimeway/admin.ex, test/chimeway/admin_test.exs</files>
  <behavior>
    - Test 1: definitions count only tenant-coherent notifications and deliveries and expose no key, channel, or count contribution from mismatched child rows.
    - Test 2: feed returns no row when its Event or Notification tenant mismatches, and excludes mismatched Delivery channels, statuses, and counts.
    - Test 3: delivery recovery candidates require matching Event, Notification, and Delivery tenants.
    - Test 4: no-delivery event candidates require matching Event and Notification tenants and do not misclassify a tree containing a foreign-tenant Delivery as having no delivery.
    - Test 5: command_center/1 inherits the corrected definitions, recent problems, and recovery candidates without changing outcome_totals/1's delivery-only contract.
  </behavior>
  <action>Extend the failing adversarial fixture matrix across `definitions/1`, `feed/1`, both `recovery_candidates/1` query branches, and the composed `command_center/1`. Per D-04, put tenant constraints for optional Notification/Delivery associations in each left join's `on:` condition when absence is a valid aggregate input; put unconditional tenant constraints on required Event/Notification/Delivery rows. For the no-delivery recovery branch, distinguish a genuinely absent Delivery from an attached Delivery with conflicting ownership so the latter suppresses the candidate instead of masquerading as an unplanned event. Keep the existing DTO fields, count semantics for coherent rows, sort/limit behavior, redaction, and wrong-tenant empty outcomes. Add a named adversarial test for every affected DTO surface so later query refactors cannot drop one lifecycle-level predicate.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/admin.ex test/chimeway/admin_test.exs &amp;&amp; mix test test/chimeway/admin_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The focused Admin suite has explicit mismatched-row assertions for recent problems, definitions, feed, delivery recovery candidates, event recovery candidates, and command-center composition.
    - Aggregate counts/channels/statuses include only rows owned by the supplied tenant.
    - `mix test test/chimeway/admin_test.exs --warnings-as-errors` exits zero.
  </acceptance_criteria>
  <done>Every affected Admin DTO is derived only from a tenant-coherent lifecycle tree, including left-join aggregate and no-delivery cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-authorize Feed searches after mount</name>
  <read_first>
    - chimeway_admin/lib/chimeway_admin/live/feed_live.ex
    - chimeway_admin/lib/chimeway_admin/live_auth.ex
    - chimeway_admin/lib/chimeway_admin/context.ex
    - chimeway_admin/test/support/live_view_case.ex
    - chimeway_admin/test/chimeway_admin/live/privacy_leak_live_test.exs
    - chimeway_admin/test/chimeway_admin/live_auth_test.exs
  </read_first>
  <files>chimeway_admin/lib/chimeway_admin/live/feed_live.ex, chimeway_admin/test/chimeway_admin/live/feed_live_test.exs</files>
  <behavior>
    - Test 1: an authorized mounted `/feed` search calls host authorization with `:view_feed` and a safe recipient-id context before querying.
    - Test 2: changing host authorization to deny after mount makes the next search redirect to the configured unauthorized location and renders no queried row.
    - Test 3: an authorized blank search retains the existing empty result without bypassing the authorization re-check.
  </behavior>
  <action>Create the focused mounted FeedLive test file and a test auth module whose allow/deny state can change after mount without changing production session data. In `FeedLive.handle_event/3`, normalize the recipient as today, then call `ChimewayAdmin.LiveAuth.ensure_authorized(socket, :view_feed, %{recipient_id: recipient_id})` before `Context.read_opts/2` or `Chimeway.admin_feed/1`. On `{:ok, socket}`, preserve existing blank/nonblank search behavior and clear the rendered query. On `{:error, redirected_socket}`, return `{:noreply, redirected_socket}` without executing the read. Keep the extra authorization context allowlisted to the normalized recipient identifier; do not pass raw params or session data.</action>
  <verify>
    <automated>mix format --check-formatted chimeway_admin/lib/chimeway_admin/live/feed_live.ex chimeway_admin/test/chimeway_admin/live/feed_live_test.exs &amp;&amp; mix cmd --cd chimeway_admin mix test test/chimeway_admin/live/feed_live_test.exs --warnings-as-errors &amp;&amp; mix verify.admin</automated>
  </verify>
  <acceptance_criteria>
    - The mounted test proves a successful search and an after-mount denial using the production `/feed` route.
    - The denial produces the configured redirect before any feed row is rendered.
    - `mix verify.admin` exits zero, preserving the package, demo-host, and browser proof lane.
  </acceptance_criteria>
  <done>Feed Debug checks current host authorization for every search and fails closed when access is revoked after mount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host tenant context → Admin Ecto joins | A host-authorized tenant becomes the row-level disclosure boundary for joined lifecycle DTOs. |
| Mounted LiveView → host authorization callback | A long-lived socket may outlive the permission that authorized its mount. |
| Admin DTO → operator UI | Joined and aggregated lifecycle facts cross into an operator-visible surface. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-97-12-01 | Information Disclosure | `Chimeway.Admin` joined DTOs | high | mitigate | Require the supplied tenant at every participating Event, Notification, and Delivery level and adversarially test each DTO with inconsistent linked rows. | `mix test test/chimeway/admin_test.exs --warnings-as-errors` |
| T-97-12-02 | Elevation of Privilege | `FeedLive.handle_event/3` | high | mitigate | Re-run host `:view_feed` authorization before every search and redirect on denial without querying. | `mix cmd --cd chimeway_admin mix test test/chimeway_admin/live/feed_live_test.exs --warnings-as-errors` |
| T-97-12-03 | Tampering | Aggregate/no-delivery classification | high | mitigate | Treat foreign-tenant joined rows as incoherent, not as absent, and prove counts/candidates cannot be manipulated through mismatched children. | `mix test test/chimeway/admin_test.exs --warnings-as-errors` |
| T-97-12-04 | Repudiation | Host authorization context | low | accept | ChimewayAdmin passes the normalized action and safe resource fact to the host seam; audit persistence remains host-owned. | `mix cmd --cd chimeway_admin mix test test/chimeway_admin/live/feed_live_test.exs --warnings-as-errors` |
| T-97-12-SC | Tampering | Package supply chain | low | accept | No package install or dependency change occurs. | `mix ci.audit` |

ASVS L1 mapping: V3 session management is enforced by event-time reauthorization; V4 access control is enforced by lifecycle-wide predicates and indistinguishable empty outcomes; V5 input handling retains current tenant and recipient normalization. All high-severity threats have blocking automated mitigations.
</threat_model>

## Artifacts this phase produces

- New file: `chimeway_admin/test/chimeway_admin/live/feed_live_test.exs`.
- New production symbols: None.
- Changed production functions: `Chimeway.Admin.recent_problem_deliveries/1`, `definitions/1`, `feed/1`, `recovery_candidates/1`; `ChimewayAdmin.Live.FeedLive.handle_event/3`.
- New external API or dependency: None.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Explicit-tenant safe lifecycle queries | 97-12 | COVERED | Joined Admin reads and the operator event boundary are closed. |
| REQ | TENANT-02 | Explicit tenant scope across Admin state | 97-12 | COVERED | Every affected joined DTO plus Feed revocation has executable coverage. |
| RESEARCH | — | Resolve one tenant before every root/nested query and retain host-owned authorization | 97-12 | COVERED | Core predicates and optional-package reauthorization remain separate responsibilities. |
| CONTEXT | D-04, D-07 | Scope every nested read and preserve non-disclosing outcomes | 97-12 | COVERED | All lifecycle levels are predicated and mismatches return empty DTO collections. |
| CONTEXT | D-05, D-06 | Compatibility requires a concrete tenant | 97-01..97-03 | COVERED | Existing behavior is preserved and not reopened by this gap. |
| CONTEXT | D-10 | Tenant is not a storage prefix | 97-12 | COVERED | Query predicates use durable columns; `repo_opts/1` is unchanged. |

<verification>
Run focused core Admin tests first, the mounted FeedLive revocation test second, then the existing `mix verify.admin` entrypoint. All acceptance evidence is machine-readable; no conversational UAT is used.
</verification>

<success_criteria>
- Every affected Admin DTO rejects or excludes inconsistent lifecycle ownership at every joined level.
- Feed searches re-check `:view_feed` after mount and redirect on revocation.
- Focused tests and `mix verify.admin` exit zero without changing DTO/redaction contracts or CI/local gate parity.
</success_criteria>

<output>
Create `.planning/phases/97-tenant-identity-compatible-upgrade/97-12-SUMMARY.md` when done.
</output>
