---
phase: 97-tenant-identity-compatible-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260812000000_add_tenant_identity_to_events_and_notifications.exs
  - priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs
  - lib/chimeway/tenant_scope.ex
  - lib/chimeway/events/event.ex
  - lib/chimeway/notifications/notification.ex
  - lib/chimeway/trigger.ex
  - lib/chimeway/traces.ex
  - test/chimeway/tenant_identity_test.exs
  - test/chimeway/tenant_scope_contract_test.exs
autonomous: false
requirements: [TENANT-01, TENANT-02, TENANT-03]
must_haves:
  truths:
    - "D-01/D-03: every new event and notification stores the explicit, nonblank trigger tenant while stable notification_key and notification_version identity remains unchanged."
    - "D-02: equal idempotency keys in different tenants create independent events; repeated or concurrent equal {tenant_id, idempotency_key} submissions converge on the same tenant-owned event."
    - "D-01/D-02: missing or blank tenant input is rejected before persistence, a zero-recipient trigger preserves the established event result, and equal recipient identities retain the established deterministic deduplication/order behavior."
    - "D-04/D-07: an explicit tenant can retrieve its trigger result through get_trace/2, while a wrong tenant and a missing event produce the same {:error, :not_found} outcome."
    - "D-05/D-06: Chimeway.TenantScope fails closed unless opts carry a concrete tenant or application config declares one concrete single-tenant compatibility tenant."
    - "D-08/D-10/D-11: the additive migration leaves legacy tenant ownership NULL, replaces global event idempotency uniqueness with tenant-scoped uniqueness, and uses only the existing static public-or-chimeway prefix rendering."
  artifacts:
    - path: "lib/chimeway/tenant_scope.ex"
      provides: "One explicit/fail-closed tenant resolution contract"
      exports: ["resolve/1"]
    - path: "lib/chimeway/events/event.ex"
      provides: "Durable event tenant field and composite idempotency changeset constraint"
      contains: "field(:tenant_id, :string)"
    - path: "lib/chimeway/notifications/notification.ex"
      provides: "Durable notification tenant field"
      contains: "field(:tenant_id, :string)"
    - path: "lib/chimeway/trigger.ex"
      provides: "Transaction propagation and tenant-scoped duplicate recovery"
    - path: "priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs"
      provides: "Canonical copied additive migration"
    - path: "test/chimeway/tenant_identity_test.exs"
      provides: "Tenant persistence, immutability, concurrency, and idempotency proof"
  key_links:
    - from: "lib/chimeway/trigger.ex"
      to: "lib/chimeway/events/event.ex"
      via: "Event.changeset receives tenant_id and names the composite unique index"
      pattern: "tenant_id.*idempotency_key"
    - from: "lib/chimeway/trigger.ex"
      to: "lib/chimeway/notifications/notification.ex"
      via: "insert_all rows copy the transaction's validated tenant_id"
      pattern: "tenant_id: tenant_id"
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/tenant_scope.ex"
      via: "get_trace/2 resolves scope before composing its event query"
      pattern: "TenantScope.resolve"
  prohibitions:
    - "FLAGGED-UNVERIFIED — Legacy rows MUST NOT silently acquire a fabricated or inferred tenant identity during the additive schema change."
    - "FLAGGED-UNVERIFIED — Compatibility behavior MUST NOT obscure which concrete tenant owns a formerly unscoped call."
---

<objective>
Prove the phase architecture end to end: a trigger persists tenant-owned event and notification rows, composite idempotency isolates tenants, and the resulting lifecycle is retrievable only through the matching tenant scope.

Purpose: Establish the durable identity and access-control spine before broadening tenant enforcement to every lifecycle surface.
Output: Additive development/copied migrations, `Chimeway.TenantScope`, tenant-aware schemas/trigger/trace path, and executable tenant identity contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-RESEARCH.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-PATTERNS.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-VALIDATION.md

<interfaces>
Existing entrypoints to preserve and extend:
- `Chimeway.Trigger.trigger(notifier, params, opts \\ [])`
- `Chimeway.Events.Event.changeset(event, attrs)`
- `Chimeway.Notifications.Notification.changeset(notification, attrs)`
- `Chimeway.Traces.get_trace(event_id, opts \\ [])`
- `Chimeway.Storage.repo_opts(opts)` remains the only static Repo-prefix seam.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Decision gate: Publish the locked tenant identity persistence contract</name>
  <decision>Confirm that execution may publish the D-01/D-02/D-08 tenant columns and composite idempotency contract.</decision>
  <context>D-01/D-02/D-08 are locked, but implementing them creates adopter-visible persisted identity and uniqueness semantics whose later reversal requires a data migration. This gate confirms release of the already-selected contract; changing the choice requires stopping for an explicit roadmap/context revision.</context>
  <options>
    <option id="proceed-locked-contract">
      <name>Proceed with locked contract</name>
      <pros>Implements the approved tenant identity and idempotency model exactly as specified.</pros>
      <cons>Changing the persisted contract after adoption requires a coordinated migration.</cons>
    </option>
    <option id="halt-for-roadmap-revision">
      <name>Halt for roadmap revision</name>
      <pros>Prevents publishing a durable contract before the locked decisions are formally changed.</pros>
      <cons>Blocks Phase 97 execution and requires a new discuss/plan cycle.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed-locked-contract or halt-for-roadmap-revision</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: Prove an explicit tenant from trigger through persisted trace</name>
  <reversibility rating="one-way">D-01/D-02/D-08 define persisted columns and a public uniqueness contract; changing them after release requires an adopter data migration.</reversibility>
  <read_first>
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
    - lib/chimeway/trigger.ex
    - lib/chimeway/events/event.ex
    - lib/chimeway/notifications/notification.ex
    - lib/chimeway/traces.ex
    - priv/repo/migrations/20260424023200_create_chimeway_events.exs
    - priv/repo/migrations/20260424023201_create_chimeway_notifications.exs
    - priv/chimeway_migrations/030_add_tenant_and_actor_to_chimeway_deliveries.exs
    - test/chimeway/trigger_pipeline_test.exs
    - test/chimeway/traces_test.exs
  </read_first>
  <files>priv/repo/migrations/20260812000000_add_tenant_identity_to_events_and_notifications.exs, priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs, lib/chimeway/tenant_scope.ex, lib/chimeway/events/event.ex, lib/chimeway/notifications/notification.ex, lib/chimeway/trigger.ex, lib/chimeway/traces.ex, test/chimeway/tenant_identity_test.exs</files>
  <behavior>
    - Test 1: trigger with tenant `tenant-a` writes that exact value to its Event and every Notification, and a tenant-a `get_trace/2` returns the lifecycle.
    - Test 2: tenant-b `get_trace/2` for tenant-a's event and a nonexistent event both return `{:error, :not_found}`.
    - Test 3: the same idempotency key creates one event per distinct tenant and returns `{:duplicate, event}` only inside the matching tenant.
    - Test 4: an Event or Notification changeset cannot update tenant ownership after insertion.
  </behavior>
  <action>Start RED with `test/chimeway/tenant_identity_test.exs`, then implement the production tracer per D-01/D-02/D-03/D-04/D-07/D-08/D-10/D-11. Add nullable `tenant_id :string` columns to both lifecycle tables in the development migration and canonical `032` template; drop the existing global event idempotency unique index and create the named unique index `chimeway_events_tenant_id_idempotency_key_index` on `[:tenant_id, :idempotency_key]`. Keep legacy rows NULL, keep `__CHIMEWAY_PREFIX__` rendering and static helper functions, and do not derive a prefix from tenant input. Add schema fields and require tenant only on new-write changesets; ensure update paths do not cast `tenant_id`. Change `event_changeset/5`, notification row construction, `normalize_trigger_result/4`, conflict recognition, and duplicate `Repo.get_by/2` so the exact validated tenant travels through the `Ecto.Multi` and composite lookup while `notification_key` plus version remain intact. Create `Chimeway.TenantScope.resolve/1`, accepting a trimmed explicit `:tenant_id`; absent explicit scope may resolve only from `config :chimeway, :single_tenant_compatibility, tenant_id: "..."`, with stable `{:error, :tenant_scope_required}` and `{:error, :invalid_compatibility_tenant}` failures. Update only the `get_trace/2` path in this tracer to resolve once and query Event by both id and tenant before tenant-constrained nested loading. Generic access-control and injection concerns are canon-owned by the plan threat model and `$gsd-secure-phase`; the bespoke non-guessing/compatibility prohibitions remain flagged in must-haves.</action>
  <acceptance_criteria>
    - Source: both schemas expose `tenant_id`; Trigger event attrs and every notification `insert_all` row use the validated trigger tenant.
    - Database: the old global unique event-idempotency index is replaced by `chimeway_events_tenant_id_idempotency_key_index` in both migration copies without a data backfill.
    - Behavior: a trigger-to-tenant-scoped-trace happy path passes, wrong-tenant lookup is indistinguishable from absence, and stable notification definition identity is unchanged.
    - Test command: `mix test test/chimeway/tenant_identity_test.exs --warnings-as-errors` exits zero.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted priv/repo/migrations/20260812000000_add_tenant_identity_to_events_and_notifications.exs priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs lib/chimeway/tenant_scope.ex lib/chimeway/events/event.ex lib/chimeway/notifications/notification.ex lib/chimeway/trigger.ex lib/chimeway/traces.ex test/chimeway/tenant_identity_test.exs &amp;&amp; mix test test/chimeway/tenant_identity_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The production tenant-a trigger-to-trace path is green, tenant-b receives the established absent result, and composite idempotency works across both tenants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close tenant identity edge, concurrency, and full trace-query contracts</name>
  <reversibility rating="costly">The compatibility config/error contract and trace option semantics are public library surfaces that would require coordinated adopter updates to rename.</reversibility>
  <read_first>
    - lib/chimeway/tenant_scope.ex
    - lib/chimeway/trigger.ex
    - lib/chimeway/traces.ex
    - test/chimeway/tenant_identity_test.exs
    - test/chimeway/traces_test.exs
    - test/chimeway/trigger_pipeline_test.exs
  </read_first>
  <files>lib/chimeway/tenant_scope.ex, lib/chimeway/trigger.ex, lib/chimeway/traces.ex, test/chimeway/tenant_identity_test.exs, test/chimeway/tenant_scope_contract_test.exs</files>
  <behavior>
    - Test 1: missing, nil, nonbinary, and trimmed-empty explicit/configured tenants fail before any trace query or trigger insert.
    - Test 2: concurrent same-tenant/same-key triggers converge on one Event; the same key in a second tenant remains independent.
    - Test 3: zero or one recipient preserves existing trigger semantics, and duplicate recipient identities retain deterministic first-value ordering.
    - Test 4: recipient, correlation, delivery explanation, and aggregate trace operations require tenant scope and return only tenant-owned rows.
  </behavior>
  <action>Expand the tracer per D-02/D-04/D-05/D-06/D-07. Thread tenant scope through `find_traces_for_recipient/2`, `find_traces_by_correlation_id/2`, `explain_delivery/2`, `aggregate_outcomes/1`, and `aggregate_outcomes_for_notification/2`; use unconditional tenant predicates at roots and nested joins/preloads, not optional filter helpers. Preserve each established return shape: lists/aggregates remain empty for no owned rows and detail lookups retain `{:error, :not_found}`. Ensure `TenantScope.resolve/1` gives an explicit option precedence over compatibility config and accepts only a concrete trimmed binary config value. Add SQL-sandbox concurrency coverage for the composite unique constraint and edge cases for empty/single/duplicate recipient inputs so adjacency, ordering, idempotency, and interruption/parallel execution from the specless probe have executable outcomes. Do not add tenant to Repo prefix options or infer it from delivery/workflow/recipient state.</action>
  <acceptance_criteria>
    - Every public function in `Chimeway.Traces` resolves one tenant before querying and applies it to all rows capable of determining the result.
    - Missing/invalid compatibility config produces a stable error without issuing an unscoped lifecycle query.
    - Concurrent duplicate proof has one same-tenant Event and no cross-tenant collision.
    - Test command: focused identity and tenant-scope contract tests exit zero.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/tenant_scope.ex lib/chimeway/trigger.ex lib/chimeway/traces.ex test/chimeway/tenant_identity_test.exs test/chimeway/tenant_scope_contract_test.exs &amp;&amp; mix test test/chimeway/tenant_identity_test.exs test/chimeway/tenant_scope_contract_test.exs test/chimeway/traces_test.exs --warnings-as-errors</automated>
  </verify>
  <done>All trigger identity edges and all trace read paths enforce one explicit or concretely configured tenant with established non-disclosing outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host options/config → core scope resolver | Host-controlled tenant input crosses into lifecycle query authority. |
| Trigger transaction → PostgreSQL | Validated tenant identity becomes durable ownership and uniqueness state. |
| Lifecycle identifier → trace queries | Untrusted UUID/correlation/recipient identifiers could enumerate another tenant's state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Residual Risk | Executable Verification |
|---|---|---|---|---|---|---|---|
| T-97-01 | Tampering | Trigger duplicate recovery | high | mitigate | Database composite unique index and recovery lookup use the same `{tenant_id, idempotency_key}` predicate. | A host can still deliberately supply the wrong tenant; host authorization remains outside Chimeway. | `mix test test/chimeway/tenant_identity_test.exs --warnings-as-errors` |
| T-97-02 | Information Disclosure | Trace root/nested queries | high | mitigate | Resolve tenant before querying and constrain root plus nested lifecycle relations; map wrong scope to established absent outcomes. | Timing differences in PostgreSQL are not claimed as a side-channel defense. | `mix test test/chimeway/tenant_scope_contract_test.exs test/chimeway/traces_test.exs --warnings-as-errors` |
| T-97-03 | Spoofing | Compatibility config | medium | mitigate | Require one concrete nonblank configured tenant and fail closed on absent/malformed config. | Application configuration is trusted host state. | `mix test test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` |
| T-97-SC | Tampering | Package supply chain | low | accept | No package install occurs; use the existing lockfile only. | Existing dependency provenance remains governed by project CI/audit gates. | `mix ci.audit` |

Security canon breadcrumb: generic authorization, injection, and database hardening remain covered by this STRIDE register and `$gsd-secure-phase`; no duplicate prohibition item is minted.

## ASVS Level 1 Execution Contract

Research identifies V2-V5 as the applicable ASVS categories for Phase 97. The owning plan/task named below implements each control; every executor preserves these cross-plan obligations, and phase completion runs the exact evidence commands after all owning plans land.

| ASVS L1 control | Responsible plan/task | Concrete mitigation | Exact automated evidence |
|---|---|---|---|
| V2 Authentication | 97-03 Task 1 and Task 2 | Host-owned Inbox/Admin authentication must authorize a concrete tenant before package state gains lifecycle authority; Chimeway does not authenticate or infer membership. | `mix verify.inbox &amp;&amp; mix verify.admin` |
| V3 Session Management | 97-03 Task 1/Task 2, 97-05 Task 1, and 97-08 Task 1 | Re-run host authorization at LiveView event time and rebuild tenant-bearing Context options for every mutation or refresh; never fall back to a stale or tenantless session value. | `mix cmd --cd chimeway_inbox mix test --warnings-as-errors &amp;&amp; mix cmd --cd chimeway_admin mix test --warnings-as-errors` |
| V4 Access Control | 97-01 Task 1/Task 2, 97-02 Task 1/Task 2, 97-07 Task 1, and 97-08 Task 1 | Resolve one tenant before access, retain its predicate across every root/nested read, atomic update, reload, and replan, and map wrong scope to the established absent outcome. | `mix test test/chimeway/tenant_scope_contract_test.exs test/chimeway/orchestration/recovery_test.exs --warnings-as-errors &amp;&amp; mix verify.inbox &amp;&amp; mix verify.admin` |
| V5 Input Validation | 97-01 Task 1/Task 2 and 97-04 Task 1/Task 2 | Accept only trimmed nonblank binary tenant IDs, validate reconciliation UUIDs, enforce strict CLI flag combinations, and reject invalid scope before any query or mutation. | `mix test test/chimeway/tenant_identity_test.exs test/chimeway/tenant_scope_contract_test.exs test/chimeway/reconciliation_test.exs test/chimeway/mix/tasks/reconcile_tenants_test.exs --warnings-as-errors` |

</threat_model>

## Artifacts this phase produces

- New modules/symbols: `Chimeway.TenantScope`, `Chimeway.TenantScope.resolve/1`.
- New durable fields/index: `Chimeway.Events.Event.tenant_id`, `Chimeway.Notifications.Notification.tenant_id`, `chimeway_events_tenant_id_idempotency_key_index`.
- Changed internal symbols: `Chimeway.Trigger.event_changeset/5`, tenant-aware trigger normalization/conflict recovery, tenant-aware `Chimeway.Traces` query functions.
- Config key: `config :chimeway, :single_tenant_compatibility, tenant_id: "<host-tenant-id>"`.
- New migration paths: `priv/repo/migrations/20260812000000_add_tenant_identity_to_events_and_notifications.exs`, `priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs`.
- New test paths/tasks: `test/chimeway/tenant_identity_test.exs`, `test/chimeway/tenant_scope_contract_test.exs`; Task 1 trigger-to-trace tracer and Task 2 edge/concurrency/full-trace expansion.

<verification>
Run the two focused test files, existing trigger/trace regression files, and format checks. The tracer must be green before Wave 2 plans execute.
</verification>

<success_criteria>
- The same idempotency key is independent across tenants and convergent within one tenant.
- Event and notification rows retain immutable direct tenant identity.
- Trace queries require tenant authority and reveal no cross-tenant existence.
- Additive schema work leaves legacy ownership ambiguous and preserves static prefix rendering.
</success_criteria>

<output>
Create `.planning/phases/97-tenant-identity-compatible-upgrade/97-01-SUMMARY.md` when done.
</output>
