---
phase: 97-tenant-identity-compatible-upgrade
plan: 06
type: execute
wave: 3
depends_on: [97-01, 97-04]
files_modified:
  - test/chimeway/install/migrations_test.exs
  - test/chimeway/install/golden_diff_test.exs
  - test/chimeway/migration_contract_test.exs
  - test/chimeway/generated_prefixed_runtime_proof_test.exs
  - test/chimeway/runtime_prefix_integration_test.exs
  - test/fixtures/installer_golden_public/STDOUT.txt
  - test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs
  - test/fixtures/installer_golden_prefixed/STDOUT.txt
  - test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs
autonomous: true
requirements: [TENANT-03]
must_haves:
  truths:
    - "D-08: generated migration 032 leaves legacy Event and Notification tenant ownership NULL and replaces global idempotency uniqueness without inference or a sentinel."
    - "D-09: the Plan 97-04 JSON report and explicit event-tree assignment work against both supported static storage modes."
    - "D-10/D-11: copied migration generation/application remains deterministic for `prefix: false` and static `prefix: \"chimeway\"`; tenant input never changes Ecto or Oban prefix."
    - "Migration down/up and repeated generation preserve the established copied-template and installer idempotency contracts."
  artifacts:
    - path: "test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs"
      provides: "Deterministic public-schema migration 032 copy"
    - path: "test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs"
      provides: "Deterministic static chimeway-schema migration 032 copy"
    - path: "test/chimeway/migration_contract_test.exs"
      provides: "Additive nullable and composite-index contract"
    - path: "test/chimeway/generated_prefixed_runtime_proof_test.exs"
      provides: "Generated prefixed PostgreSQL proof"
  key_links:
    - from: "priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs"
      to: "test/fixtures/installer_golden_*"
      via: "existing deterministic renderer replaces only the static prefix sentinel"
      pattern: "__CHIMEWAY_PREFIX__"
    - from: "lib/chimeway/reconciliation.ex"
      to: "test/chimeway/runtime_prefix_integration_test.exs"
      via: "report and explicit assignment execute under existing Storage repo options"
      pattern: "Reconciliation.(report|assign_event_tree)"
---

<objective>
Prove the additive tenant migration and explicit reconciliation contract in both supported static PostgreSQL storage modes.

Purpose: Keep fixture generation and database application evidence cohesive while separating them from reconciliation implementation.
Output: Refreshed deterministic migration-032 goldens, installer contracts, and public/prefixed PostgreSQL proofs.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-PATTERNS.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-01-SUMMARY.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-04-SUMMARY.md

<interfaces>
- Plan 97-01 creates canonical `032_add_tenant_identity_to_events_and_notifications.exs` with the existing static prefix sentinel.
- Plan 97-04 creates `Chimeway.Reconciliation.report/1`, `assign_event_tree/3`, and the strict JSON Mix task.
- `Chimeway.Install.Migrations.run/1` discovers canonical templates in order and renders one static host prefix.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refresh deterministic public and prefixed migration fixtures</name>
  <read_first>
    - priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs
    - lib/chimeway/install/migrations.ex
    - test/chimeway/install/migrations_test.exs
    - test/chimeway/install/golden_diff_test.exs
    - test/fixtures/installer_golden_public/STDOUT.txt
    - test/fixtures/installer_golden_prefixed/STDOUT.txt
  </read_first>
  <files>test/chimeway/install/migrations_test.exs, test/chimeway/install/golden_diff_test.exs, test/fixtures/installer_golden_public/STDOUT.txt, test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs, test/fixtures/installer_golden_prefixed/STDOUT.txt, test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs</files>
  <behavior>
    - Test 1: the generator emits template 032 once in stable order in public and prefixed modes.
    - Test 2: the generated files differ only by expected static prefix rendering and contain no tenant-derived prefix behavior.
    - Test 3: a second generator run reports migration 032 unchanged and produces no fixture drift.
  </behavior>
  <action>Apply D-10/D-11 by refreshing both committed golden trees through the repository's accepted golden workflow after canonical template 032 exists. Extend installer ordering/idempotency assertions and golden-diff coverage for the new copied migration and deterministic stdout. Preserve one canonical template tree and the existing `__CHIMEWAY_PREFIX__` replacement for `false` versus `"chimeway"`; do not add another generator or manually normalize away meaningful output differences.</action>
  <acceptance_criteria>
    - Public and prefixed migration-032 fixtures exactly match canonical rendering.
    - Repeated generation is idempotent and stdout ordering is stable.
    - Test command: `mix verify.install_golden` exits zero.
  </acceptance_criteria>
  <verify>
    <automated>mix verify.install_golden</automated>
  </verify>
  <done>Both static installer modes deterministically generate exactly one reviewable migration-032 copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove nullable upgrade, composite identity, and reconciliation in PostgreSQL</name>
  <read_first>
    - priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs
    - lib/chimeway/reconciliation.ex
    - test/chimeway/migration_contract_test.exs
    - test/chimeway/generated_prefixed_runtime_proof_test.exs
    - test/chimeway/runtime_prefix_integration_test.exs
  </read_first>
  <files>test/chimeway/migration_contract_test.exs, test/chimeway/generated_prefixed_runtime_proof_test.exs, test/chimeway/runtime_prefix_integration_test.exs</files>
  <behavior>
    - Test 1: applying 032 leaves seeded legacy rows NULL, removes global idempotency uniqueness, and enforces `{tenant_id, idempotency_key}` uniqueness.
    - Test 2: public and `chimeway` generated migrations apply on PostgreSQL and report/assignment operate inside their configured static schema.
    - Test 3: explicit tenant assignment never changes relation prefix and wrong/conflicting ownership remains rejected.
    - Test 4: down/up removes and recreates only the new columns/indexes while retaining deterministic application behavior.
  </behavior>
  <action>Extend migration contract, generated prefixed runtime proof, and public/prefixed runtime integration tests for D-08/D-09/D-10/D-11. Seed legacy rows before 032 and assert tenant columns stay NULL; inspect PostgreSQL indexes to prove the old global unique index is absent and the named composite unique index enforces same-tenant collision while permitting cross-tenant reuse. Execute `Reconciliation.report/1` and explicit `assign_event_tree/3` against each static mode and prove tenant is only a column value/predicate. Exercise down/up without adding runtime DDL or any dynamic prefix path, then run the existing named gates and CI parity gate.</action>
  <acceptance_criteria>
    - PostgreSQL evidence covers nullable legacy rows, composite uniqueness, JSON report, explicit assignment, and static prefix invariants in both modes.
    - Down/up affects only the Phase 97 tenant columns and indexes.
    - `mix verify.runtime_prefix` and `mix ci.verify_gates` exit zero.
  </acceptance_criteria>
  <verify>
    <automated>mix verify.runtime_prefix &amp;&amp; mix ci.verify_gates</automated>
  </verify>
  <done>The upgrade and reconciliation contracts are executable and deterministic in both supported static storage modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical migration template → generated adopter migration | Prefix rendering must remain deterministic and static. |
| Generated migration/reconciliation → PostgreSQL schemas | DDL and explicit ownership updates must affect only the configured installation schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Residual Risk | Executable Verification |
|---|---|---|---|---|---|---|---|
| T-97-14 | Elevation of Privilege | Storage routing | high | mitigate | Use existing static storage opts, forbid tenant-derived prefix flags, and prove public and `chimeway` schemas. | A host can misconfigure its installation prefix outside this phase. | `mix verify.runtime_prefix` |
| T-97-16 | Tampering | Copied migration output | high | mitigate | Golden-diff and idempotency tests bind generated files to the canonical migration-032 template. | Adopters can edit copied migrations after generation as an explicit host action. | `mix verify.install_golden` |
| T-97-SC | Tampering | Package installs | low | accept | No package install occurs; existing Ecto/Jason dependencies remain locked. | Existing dependency provenance remains governed by project audit gates. | `mix ci.audit` |

## ASVS Level 1 Execution Contract

Research identifies V2-V5 as the applicable ASVS categories for Phase 97. The owning plan/task named below implements each control; every executor preserves these cross-plan obligations, and phase completion runs the exact evidence commands after all owning plans land.

| ASVS L1 control | Responsible plan/task | Concrete mitigation | Exact automated evidence |
|---|---|---|---|
| V2 Authentication | 97-03 Task 1 and Task 2 | Host-owned Inbox/Admin authentication must authorize a concrete tenant before package state gains lifecycle authority; Chimeway does not authenticate or infer membership. | `mix verify.inbox &amp;&amp; mix verify.admin` |
| V3 Session Management | 97-03 Task 1/Task 2, 97-05 Task 1, and 97-08 Task 1 | Re-run host authorization at LiveView event time and rebuild tenant-bearing Context options for every mutation or refresh; never fall back to a stale or tenantless session value. | `mix cmd --cd chimeway_inbox mix test --warnings-as-errors &amp;&amp; mix cmd --cd chimeway_admin mix test --warnings-as-errors` |
| V4 Access Control | 97-01 Task 1/Task 2, 97-02 Task 1/Task 2, 97-07 Task 1, and 97-08 Task 1 | Resolve one tenant before access, retain its predicate across every root/nested read, atomic update, reload, and replan, and map wrong scope to the established absent outcome. | `mix test test/chimeway/tenant_scope_contract_test.exs test/chimeway/orchestration/recovery_test.exs --warnings-as-errors &amp;&amp; mix verify.inbox &amp;&amp; mix verify.admin` |
| V5 Input Validation | 97-01 Task 1/Task 2 and 97-04 Task 1/Task 2 | Accept only trimmed nonblank binary tenant IDs, validate reconciliation UUIDs, enforce strict CLI flag combinations, and reject invalid scope before any query or mutation. | `mix test test/chimeway/tenant_identity_test.exs test/chimeway/tenant_scope_contract_test.exs test/chimeway/reconciliation_test.exs test/chimeway/mix/tasks/reconcile_tenants_test.exs --warnings-as-errors` |

</threat_model>

## Artifacts this phase produces

- Public and prefixed generated migration-032 fixture files and deterministic stdout.
- Expanded installer, migration-contract, generated-prefix, and runtime-prefix test suites.
- Task names: deterministic fixture generation and dual-mode PostgreSQL upgrade proof.

<verification>
Run `mix verify.install_golden`, `mix verify.runtime_prefix`, and `mix ci.verify_gates`; objective evidence is fully machine-executable.
</verification>

<success_criteria>
- Legacy ownership remains NULL until explicit reconciliation.
- Composite tenant/idempotency behavior is enforced by PostgreSQL.
- Both copied migration modes generate and apply deterministically without dynamic storage routing.
</success_criteria>

<output>
Create `.planning/phases/97-tenant-identity-compatible-upgrade/97-06-SUMMARY.md` when done.
</output>
