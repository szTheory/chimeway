---
phase: 102-alpha-digital-twin-hermetic-gate
plan: "02"
type: execute
wave: 2
depends_on:
  - 102-01
files_modified:
  - lib/chimeway/clock.ex
  - lib/chimeway/delivery_targets.ex
  - lib/chimeway/adapters/apns.ex
  - test/fixtures/alpha_twin/lib/alpha_twin/clock.ex
  - test/fixtures/alpha_twin/lib/alpha_twin/registry.ex
  - test/fixtures/alpha_twin/lib/alpha_twin/scripted_apns_transport.ex
  - test/fixtures/alpha_twin/test/alpha_twin_test.exs
autonomous: true
requirements:
  - TWIN-01
  - TWIN-02
must_haves:
  truths:
    - "Production target/APNs time remains system-default while the Alpha twin explicitly advances fixed time without sleeps across claim, attempt, expiry, retry, lease, registry, intent, and open-resolution boundaries per D-06."
    - "The host-owned registry retains raw tokens, binding lifecycle authority, eligibility, and consume-once intent authority while exposing only opaque references, fingerprints, classifications, and safe facts per D-04."
    - "The scripted transport observes the real bounded APNs request through the shipped transport behaviour and returns ordered accepted, retryable, permanent, exact-invalidation, and ambiguous outcomes without network or Apple credentials per D-07."
    - "The immutable-package tracer from Plan 01 remains green after the production clock seam and dedicated fixture components are wired."
  artifacts:
    - path: "lib/chimeway/clock.ex"
      provides: "Narrow system-default and injectable clock resolution"
    - path: "test/fixtures/alpha_twin/lib/alpha_twin/registry.ex"
      provides: "Host-owned binding, eligibility, rotation/revocation, and consume-once intent authority"
    - path: "test/fixtures/alpha_twin/lib/alpha_twin/scripted_apns_transport.ex"
      provides: "Ordered credential-free transport behind the shipped behaviour"
  key_links:
    - from: "Chimeway.DeliveryTargets and Chimeway.Adapters.APNS"
      to: "Chimeway.Clock"
      via: "optional resolved now with unchanged system-clock defaults"
    - from: "AlphaTwin.Registry"
      to: "Chimeway and CrossWake public contracts"
      via: "opaque references only; raw token and authorization state never cross"
    - from: "Chimeway.Adapters.APNS"
      to: "AlphaTwin.ScriptedAPNSTransport"
      via: "real Chimeway.APNS.Transport request/result behaviour"
---

<objective>
Complete the deterministic clock, host-authority registry, and ordered scripted-transport execution unit required by D-04, D-06, and D-07.

Purpose: Turn the immutable accepted tracer into a sleep-free, multi-outcome production-path skeleton before the full D-08 scenario ledger expands it.
Output: The narrow production clock seam plus dedicated Alpha fixture clock, registry, scripted transport, and executable seam contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-CONTEXT.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-RESEARCH.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-PATTERNS.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-VALIDATION.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-01-SUMMARY.md

<interfaces>
- `Chimeway.Clock.now/1` resolves an optional provider and otherwise delegates to truncated system UTC time.
- `Chimeway.APNS.Transport.push/3` accepts the real bounded request and returns a typed result or closed error.
- `AlphaTwin.Runner` from Plan 01 already proves the packaged accepted path and is the consumer wired to these dedicated seams.
- CrossWake `Resolver.resolve/3` accepts fixture-owned intent evidence and explicit current-policy state; it does not own token or binding authority.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close deterministic clock, host-authority, and scripted-transport contracts</name>
  <files>lib/chimeway/clock.ex, lib/chimeway/delivery_targets.ex, lib/chimeway/adapters/apns.ex, test/fixtures/alpha_twin/lib/alpha_twin/clock.ex, test/fixtures/alpha_twin/lib/alpha_twin/registry.ex, test/fixtures/alpha_twin/lib/alpha_twin/scripted_apns_transport.ex, test/fixtures/alpha_twin/test/alpha_twin_test.exs</files>
  <read_first>lib/chimeway/delivery_targets.ex, lib/chimeway/adapters/apns.ex, lib/chimeway/target_recovery.ex, lib/chimeway/apns/transport.ex, test/support/apns_fake_transport.ex, test/fixtures/apns_consumer/lib/apns_consumer.ex, test/fixtures/alpha_twin/lib/alpha_twin/runner.ex, ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex, .planning/phases/102-alpha-digital-twin-hermetic-gate/102-CONTEXT.md, .planning/phases/102-alpha-digital-twin-hermetic-gate/102-RESEARCH.md, .planning/phases/102-alpha-digital-twin-hermetic-gate/102-VALIDATION.md</read_first>
  <behavior>
    - With no clock option, target/APNs timestamps use system UTC and remain compatible with existing callers per D-06.
    - With `AlphaTwin.Clock`, fixed time controls claim, attempt, expiry, retry, lease, registry, intent, and open-resolution boundaries; explicit advance changes them without sleep.
    - `AlphaTwin.Registry` owns two installation tokens, eligibility, exact revision CAS, rotation/revocation, and consume-once intents; returned observations contain only opaque refs/fingerprints/classifications per D-04.
    - `AlphaTwin.ScriptedAPNSTransport` validates and redacts the real request before observation, consumes one ordered expected outcome, and maps accepted, retryable, permanent, invalidating, and ambiguous results through the shipped adapter per D-07.
  </behavior>
  <action>Write failing fixture contract tests, then implement `Chimeway.Clock` as a narrow provider with the production implementation delegating to truncated `DateTime.utc_now/0`. Thread an optional resolved `now` through the exercised `DeliveryTargets` attempt/closeout and APNS expiry/accepted-at code while preserving every existing arity/default per D-06. Implement `AlphaTwin.Clock` as an explicitly advanced process and assert no test path uses sleeps. Implement `AlphaTwin.Registry` with two opaque installation revisions, exact `{tenant_id, environment, topic, binding_revision_ref}` comparisons, rotation/revocation races, eligibility, and atomic one-time intent consumption per D-04. Implement the transport behaviour directly: validate payload/header/topic/environment bounds, redact token and content before sending observations, consume exactly one scripted ledger outcome, and return the real `%Chimeway.APNS.Transport.Result{}` or `{:error, :ambiguous}` shapes so production classification remains authoritative per D-07. Wire Plan 01's fixture runner to these modules without changing its immutable package or CrossWake provenance contract. Start and stop all processes under a synchronous SQL Sandbox owner and await spawned work before teardown.</action>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/fixtures/alpha_twin/test/alpha_twin_test.exs --only alpha_twin_seams --seed 0 --warnings-as-errors &amp;&amp; mix verify.alpha_twin</automated>
  </verify>
  <acceptance_criteria>Focused tests prove system-default and injected time, explicit clock advance, absence of sleeps, two-installation host custody, exact old-revision invalidation that leaves a rotated replacement active, atomic replay denial, bounded redacted observations, all five scripted provider outcomes through `Chimeway.Adapters.APNS`, no network/Pigeon startup, and clean process/SQL Sandbox teardown.</acceptance_criteria>
  <done>The deterministic seams are complete and the immutable accepted tracer remains green with production defaults unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture clock -> Chimeway runtime | Test-controlled time crosses into exercised production call sites while production defaults must remain unchanged. |
| Host registry -> Chimeway/CrossWake | Raw tokens and authorization authority remain host-private; only opaque safe values cross. |
| Chimeway adapter -> scripted transport | A real bounded request crosses, but observation must be redacted before capture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-102-02A | Information Disclosure | registry and transport observation | high | mitigate | Keep raw values in `AlphaTwin.Registry`, redact before observation, scan returned facts, and mutation-test nested sentinels. |
| T-102-03A | Elevation of Privilege | binding and one-time intent authority | high | mitigate | Exact tenant/environment/topic/revision comparisons, atomic consume-once operation, and stale/session/replay denial tests. |
| T-102-04A | Tampering | clock and ordered transport script | high | mitigate | Closed provider/result contracts, explicit advancement, exact one-request/one-outcome consumption, and mutation tests for skipped/reordered outcomes. |

## ASVS L1 Control-to-Verification Trace

| High Threat | ASVS L1 controls | Executable control verification |
|-------------|------------------|---------------------------------|
| T-102-02A | V5.2.1 sanitization; V5.3.1 safe output encoding; V4.1.1 least privilege | `scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/fixtures/alpha_twin/test/alpha_twin_test.exs --only alpha_twin_seams --seed 0 --warnings-as-errors` injects raw/nested token, identity, URL, payload, credential, and provider-body sentinels and asserts redacted observations and non-echoing failures. |
| T-102-03A | V2.2.1 anti-replay controls; V3.7.1 session revalidation/termination; V4.1.2 deny by default and V4.2.1 record-level authorization | The same suite races rotation/revocation and consume-once operations, mutates tenant/session/revision, and proves one activation with all stale/replayed paths denied. |
| T-102-04A | V5.1.1/V5.1.2 allowlisted structured-input validation; V6.2.1 fail-closed trusted primitive use | The same suite mutates clock providers, request bounds, outcome type/order/count, and explicit time advancement; every malformed or reordered input fails and `mix verify.alpha_twin` remains byte-stable without sleeps. |
</threat_model>

<verification>
Run the focused `alpha_twin_seams` contract and `mix verify.alpha_twin`; both are objective automated evidence and require no human UAT.
</verification>

<success_criteria>
- Production time defaults are unchanged and every twin time boundary is explicit and sleep-free.
- Host authority never leaves the Alpha fixture; only opaque safe values cross project boundaries.
- Every scripted provider class travels through the shipped APNs behaviour with bounded redacted observation.
- The immutable accepted tracer remains green after the dedicated seams are wired.
</success_criteria>

<output>
Create `.planning/phases/102-alpha-digital-twin-hermetic-gate/102-02-SUMMARY.md` when done.
</output>
