---
phase: 101-crosswake-registration-protected-open
plan: "11"
type: execute
wave: 6
depends_on: ["101-06", "101-09"]
files_modified:
  - ../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NotificationRegistrationCoordinator.swift
  - ../crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NotificationRegistrationTests.swift
autonomous: true
gap_closure: true
requirements: [OPEN-01]
must_haves:
  truths:
    - "D-01/D-03/OPEN-01: a denied native permission recheck retains the exact binding command until the authenticated host acknowledges `.revoked` or `.staleNoop`."
    - "D-03/OPEN-01: a `.rejected` permission-loss response is retryable on the next recheck; a later `.revoked` response becomes delivered and all later denied rechecks are idempotent no-ops."
    - "D-04/OPEN-01: retries retain only the exact opaque binding_ref and closed scope; APNs token bytes, host error payloads, identity payloads, and provider bodies never enter diagnostics or test transcripts."
  assumptions:
    - statement: "FLAGGED ASSUMPTION — OPEN-01 remained unclassified and unresolved in the spec-less edge probe; 101-VERIFICATION.md supplies the concrete rejected-then-revoked retry acceptance repaired here."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-02 remained unclassified and unresolved in the spec-less edge probe; this native acknowledgement repair does not reclassify or claim new manifest-policy coverage."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-03 remained unclassified and unresolved in the spec-less edge probe; this native acknowledgement repair does not reclassify or claim new offline-intent coverage."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-04 remained unclassified and unresolved in the spec-less edge probe; this native acknowledgement repair does not reclassify or claim new no-fallback coverage."
      status: unresolved
      flagged: true
  artifacts:
    - path: "../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NotificationRegistrationCoordinator.swift"
      provides: "Host-acknowledged permission-loss delivery marker and retry-preserving state transition"
    - path: "../crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NotificationRegistrationTests.swift"
      provides: "Rejected-then-revoked retry and terminal idempotency XCTest evidence"
  key_links:
    - from: "NotificationRegistrationCoordinator.recheckPermissionState()"
      to: "NotificationRegistrationDelegate.revokeNotificationBindingForPermissionLoss"
      via: "retained exact NotificationPermissionLossCommand retried until a terminal closed host acknowledgement"
      pattern: "recheckPermissionState|revokeNotificationBindingForPermissionLoss|permissionLossDelivered"
  prohibitions:
    - statement: "Dispatching a permission-loss command must not itself count as durable host acknowledgement."
      status: unverified
      flagged: true
    - statement: "Retry state and diagnostics must not retain or expose raw APNs token material or arbitrary host response data."
      status: unverified
      flagged: true
---

<objective>
Make native permission-loss delivery acknowledge-driven and retry-safe (D-01, D-03, D-04).

Purpose: A transient host rejection must not permanently strand an exact active binding after native permission has been lost.
Output: Correct terminal acknowledgement semantics and rejected-then-revoked XCTest coverage.
</objective>

<assumption_delta_decision>
Primary noun: the exact retained binding revision awaiting host acknowledgement. Decision: no-change — “no fallback route” describes protected-open denial behavior and does not introduce multiple registration identities or acknowledgement models.
</assumption_delta_decision>

<api_coverage_decision>
The deterministic API-coverage detector returned `detected=false`. This plan repairs an existing in-process Swift host delegate state machine and adds no external API, SDK, service, endpoint, credential, or capability surface.
</api_coverage_decision>

<schema_push_decision>
No schema-push contribution applies; this plan changes only Swift state-machine source and XCTest evidence.
</schema_push_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
@.planning/phases/101-crosswake-registration-protected-open/101-06-SUMMARY.md

<interfaces>
`NotificationPermissionLossOutcome` is the closed enum `.revoked | .staleNoop | .rejected | .permissionDeniedNoop`. `NotificationRegistrationDelegate.revokeNotificationBindingForPermissionLoss(_:)` synchronously returns that acknowledgement for the exact retained `NotificationPermissionLossCommand`. `recheckPermissionState()` currently sets `permissionLossDelivered` before invoking the delegate; only `.revoked` and `.staleNoop` are terminal acknowledgements.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retry permission loss until the host acknowledges the exact revoke</name>
  <reversibility rating="reversible">The delivery marker is private state behind the established delegate and closed outcome contracts; the correction is local to the coordinator and tests.</reversibility>
  <read_first>
    - ../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NotificationRegistrationCoordinator.swift
    - ../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeDelegates.swift
    - ../crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NotificationRegistrationTests.swift
    - ../crosswake/test/fixtures/chimeway_notification_permission_loss_v1.json
    - .planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
    - .planning/phases/101-crosswake-registration-protected-open/101-06-SUMMARY.md
  </read_first>
  <files>../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NotificationRegistrationCoordinator.swift, ../crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NotificationRegistrationTests.swift</files>
  <behavior>
    - After a bound registration becomes denied, the first delegate result `.rejected` returns `.rejected`, records one exact command, leaves delivery pending, and retains no token material.
    - The next denied recheck sends the byte-equal exact command again; `.revoked` returns terminal success and marks delivery complete.
    - Every later denied recheck returns `.permissionDeniedNoop` without a third delegate call.
    - Existing `.staleNoop` acknowledgement remains terminal after one call, and missing retained binding remains a permission-denied no-op.
  </behavior>
  <action>Start RED by extending the recording delegate to supply a deterministic sequence of closed outcomes and add a rejected-then-revoked test driven by the existing opaque scope/command shape. In `recheckPermissionState()`, keep the native state at `.permissionDenied` once denial is observed, but invoke the delegate before changing the delivery marker. Set `permissionLossDelivered` only when the returned outcome is `.revoked` or `.staleNoop`; for `.rejected` or a missing delegate, return rejection while retaining the exact command and leaving the marker false so the next recheck retries. Preserve the existing delivered guard and `.permissionDeniedNoop` result after terminal acknowledgement. Do not add arbitrary retry counters, background scheduling, host error payload storage, token logging, or a second authority seam; foreground/settings rechecks remain the bounded trigger selected in D-03.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/packages/crosswake-shell-core-ios &amp;&amp; swift test --filter NotificationRegistrationTests</automated>
  </verify>
  <acceptance_criteria>
    - The rejected-then-revoked test observes exactly two byte-equal permission-loss commands followed by a no-call idempotent result.
    - The existing stale-no-op test still observes one command only.
    - Coordinator diagnostics and retained test-visible state do not contain the forbidden APNs token sentinel or arbitrary delegate payload.
    - The focused Swift suite exits zero from the package workdir.
  </acceptance_criteria>
  <done>Permission loss remains pending across rejection and becomes delivered only after the host confirms revoked or stale-no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| native permission state -> authenticated host delegate | Local denial triggers a durable exact-revision authority command, but dispatch is not acknowledgement. |
| host acknowledgement -> coordinator retry state | A closed outcome decides whether the exact revoke remains pending or is terminally delivered. |

## STRIDE Threat Register (ASVS L1; high blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-28 | Tampering/Denial of Service | Permission-loss delivery marker | high | mitigate | Mark delivered only for `.revoked`/`.staleNoop`; rejected-then-revoked retry test proves recovery. |
| T-101-29 | Elevation | Stale active binding | high | mitigate | Retain and retry the exact binding command until terminal host acknowledgement; never broaden scope. |
| T-101-30 | Information Disclosure | Coordinator state/diagnostics | high | mitigate | Retain opaque command only and assert raw-token sentinel absence across retries. |
</threat_model>

## Artifacts this phase produces

- Updated private state semantics: `permissionLossDelivered` becomes true only after `.revoked` or `.staleNoop`.
- Updated test support: `RecordingRegistrationDelegate` can return a deterministic sequence of closed permission-loss outcomes.
- New XCTest behavior: rejected first acknowledgement, revoked retry acknowledgement, then terminal idempotent no-op.
- No new production file, public enum case, delegate method, persisted field, migration, package, dependency, external API, credential, CLI flag, telemetry payload, or schema-push artifact.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Host binds APNs registrations and activates routes only under current authority | 101-01, 101-04..101-11 | COVERED | Gap plans repair the three failed registration paths; protected-open truths remain verified. |
| REQ | OPEN-01 | Permission, observation, authenticated binding, rotation/revocation/invalidation | 101-04, 101-06, 101-09, 101-10, 101-11 | COVERED | 101-10 closes upgrade and uniqueness; 101-11 closes acknowledgement retry. |
| REQ | OPEN-02 | Manifest-consistent default-deny action policy | 101-02, 101-03, 101-09 | COVERED | Previously verified; unresolved probe remains explicitly flagged and is not reclassified. |
| REQ | OPEN-03 | Opaque offline queue and current one-time reauthorization | 101-01, 101-03, 101-05, 101-07, 101-09 | COVERED | Previously verified; unresolved probe remains explicitly flagged and is not reclassified. |
| REQ | OPEN-04 | Stale/denied opens activate no fallback and emit safe evidence | 101-01, 101-05, 101-08, 101-09 | COVERED | Previously verified; unresolved probe remains explicitly flagged and is not reclassified. |
| RESEARCH | — | Host-owned exact binding/intent authority with executable race evidence | 101-04, 101-05, 101-10 | COVERED | Forward migration and posture race align schema, changeset, and lookup identity. |
| RESEARCH | — | Explicit native permission/observation/binding states and exact revocation | 101-06, 101-11 | COVERED | Terminal acknowledgement, not attempted dispatch, completes permission loss. |
| RESEARCH | — | Normalized policy, opaque queue, closed denial/telemetry, no-fallback matrix | 101-02, 101-03, 101-07..101-09 | COVERED | Verified executed plans are unchanged. |
| CONTEXT | D-01 | Permission, observation, and host binding remain distinct | 101-06, 101-11 | COVERED | Retry does not grant binding or route authority. |
| CONTEXT | D-02 | Authenticated host registry is sole scoped binding authority | 101-04, 101-10 | COVERED | Actual authority identity and forward schema are enforced. |
| CONTEXT | D-03 | Only exact current active revision is eligible under all invalidations | 101-04, 101-06, 101-10, 101-11 | COVERED | All three verification failures close here. |
| CONTEXT | D-04 | Raw tokens remain transient; durable/public facts stay closed | 101-04, 101-06, 101-08, 101-10, 101-11 | COVERED | Both gap plans include sentinel/privacy assertions. |
| CONTEXT | D-05..D-08 | Normalized host-bound manifest and RouteGate authorization | 101-01..101-03, 101-09 | COVERED | Existing verified plans; no gap-plan changes. |
| CONTEXT | D-09..D-13 | Opaque queue, atomic consume, current recheck, safe outcomes, narrow iOS seams | 101-01, 101-05..101-09, 101-11 | COVERED | Existing verified plans plus local coordinator repair only. |

Excluded without gap: Phase 102 digital twin/named CI gates, Phase 103 physical-iPhone proof/docs, Android/FCM, generic offline/background sync, arbitrary notification-action framework, dashboards, analytics, and engagement tracking. The schema-push contribution is inapplicable to these Ecto/Swift repairs. The API detector returned false. All four spec-less rows remain visible as flagged unresolved assumptions; only OPEN-01's concrete verification failures are resolved by 101-10 and 101-11.

<verification>
Run `cd /Users/jon/projects/crosswake/packages/crosswake-shell-core-ios &amp;&amp; swift test --filter NotificationRegistrationTests`; it must exit zero. Run Plan 101-10's focused Elixir commands independently from `/Users/jon/projects/crosswake/examples/phoenix_host`; the same-wave plans have no file overlap.
</verification>

<success_criteria>
A host rejection cannot permanently suppress permission-loss revocation: the exact command retries, a later terminal acknowledgement completes delivery, and subsequent rechecks are idempotent and privacy-safe.
</success_criteria>

<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-11-SUMMARY.md` when done.</output>
