---
phase: 101-crosswake-registration-protected-open
plan: "20"
type: execute
wave: 12
depends_on: ["101-15", "101-19"]
files_modified:
  - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
  - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825200000_protect_chimeway_notification_open_intent_history.exs
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs
autonomous: true
gap_closure: true
requirements: [OPEN-01, OPEN-04]
must_haves:
  truths:
    - "D-03/OPEN-01: logout authority is qualified by the authenticated session version during both initial selection and conditional mutation, so a delayed version-1 logout cannot revoke an active version-2 binding sharing the same tenant, subject, and session reference."
    - "D-12/OPEN-04: notification-open intent lifecycle events remain auditable after the forward upgrade because deleting a parent that has event history is rejected without removing either the parent or its append-only events."
    - "OPEN-01/OPEN-04: focused runtime and released-boundary regressions fail on the verified defects and pass after the repairs without exposing token, identity, route, action, payload, URL, or uncontrolled metadata values."
  assumptions:
    - statement: "FLAGGED ASSUMPTION — OPEN-01 remains unclassified by the spec-less probe; the authoritative 101-VERIFICATION.md resolves its actionable meaning to exact session_version qualification in logout selection/CAS plus a stale-v1-versus-active-v2 regression."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-04 remains unclassified by the spec-less probe; the authoritative 101-VERIFICATION.md resolves its actionable meaning to forward-only retention of append-only notification-open event history plus migration/runtime deletion regressions."
      status: unresolved
      flagged: true
    - statement: "OPEN-02 was unclassified by the spec-less probe, but 101-02, 101-03, 101-09 and the passing policy/manifest verification evidence already account for its closed default-deny action contract; no new work is inferred here."
      status: accounted
      flagged: false
    - statement: "OPEN-03 was unclassified by the spec-less probe, but 101-01, 101-03, 101-05, 101-07, 101-09, 101-13, 101-14, 101-16 and 101-18 plus the passing queue/consume verification evidence already account for it; no new work is inferred here."
      status: accounted
      flagged: false
  artifacts:
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex"
      provides: "Version-qualified logout selector and predicate-CAS mutation"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825200000_protect_chimeway_notification_open_intent_history.exs"
      provides: "Forward-only restrictive parent-deletion guard for append-only notification-open events"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs"
      provides: "Stale version-1 logout versus active version-2 binding regression"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs"
      provides: "Released-boundary upgrade proof for restrictive lifecycle-history retention"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs"
      provides: "Runtime deletion regression proving parent and event survive the rejected mutation"
  key_links:
    - from: "Registry.revoke_for_logout/2 authenticated context"
      to: "TokenBinding selection and update_all predicate"
      via: "the same exact ctx.session_version bound into both database predicates per D-03"
      pattern: "session_version.*ctx.session_version|binding_ref.*state.*session_version"
    - from: "notification-open intent parent deletion"
      to: "chimeway_notification_open_intent_events append-only history"
      via: "the new forward migration's named restrictive deletion guard"
      pattern: "BEFORE DELETE|open_intent_id|RAISE"
  prohibitions:
    - statement: "A stale logout must not mutate a binding belonging to a newer authenticated session version."
      status: unverified
      flagged: true
    - statement: "Deleting an intent must not erase its issued, consumed, denied, or reconciliation lifecycle evidence."
      status: unverified
      flagged: true
---

<objective>
Close the two regressions reported after Plan 101-19: exact-version logout authority and durable notification-open event retention (D-03, D-12; OPEN-01, OPEN-04).

Purpose: Current host authority must control logout mutations, and every protected-open outcome must remain explainable after the released schema is upgraded.
Output: One version-qualified registry repair, one forward Ecto migration that restricts destructive parent deletion, and executable runtime/released-boundary regressions.
</objective>

<gap_closure_context>
Phase 101's production tracer shipped in 101-01. This gap plan adds two objectively machine-testable expansion repairs against the authoritative verifier findings; it does not create another architectural path or reopen already-verified OPEN-02/OPEN-03 scope.
</gap_closure_context>

<assumption_delta_decision>
Primary noun: one exact active binding revision and its append-only intent lifecycle. Decision: no-change — the detector's “fallback” signal describes denial behavior, not a singular-to-plural identity transition.
</assumption_delta_decision>

<api_coverage_decision>
Preserve `COVERAGE.md` unchanged. The closure modifies only the existing local CrossWake Phoenix example-host Ecto registry, migration, and ExUnit evidence; it adds no external API, SDK, endpoint, service, credential, or capability surface.
</api_coverage_decision>

<schema_push_decision>
The active schema change is an Elixir/Ecto forward migration for the example host's SQLite repository. Exercise it through the released-boundary Ecto migrator regression; the plan-pre ORM detector lists no applicable Prisma, Drizzle, Supabase, Payload, or TypeORM schema-push command.
</schema_push_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-RESEARCH.md
@.planning/phases/101-crosswake-registration-protected-open/101-PATTERNS.md
@.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
@.planning/phases/101-crosswake-registration-protected-open/COVERAGE.md
@.planning/phases/101-crosswake-registration-protected-open/101-15-SUMMARY.md
@.planning/phases/101-crosswake-registration-protected-open/101-19-SUMMARY.md

<interfaces>
`Registry.revoke_for_logout/2` already validates a `:subject_session` context containing a non-empty `session_ref` and non-negative `session_version`. `do_revoke_for_logout_scoped/3` currently selects active rows by subject, org, session_ref, subject_scope, and state, then updates selected binding_refs by binding_ref and active state. Both predicates omit the validated version.

The released migration `20260603000000_create_chimeway_notification_open_intents.exs` created `chimeway_notification_open_intent_events.open_intent_id` with cascading parent deletion. The current example host uses `Ecto.Adapters.SQLite3`; ordered forward migrations and named SQLite trigger guards are already established in Plans 101-15 and 101-19. `NotificationOpenIntentEvent` belongs to its parent through `open_intent_id`, and runtime lifecycle paths append events rather than deleting them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind logout selection and mutation to the exact authenticated session version</name>
  <reversibility rating="reversible">The change is confined to private Ecto predicates and a focused regression; reverting it is local, though doing so would reopen the verified authority flaw.</reversibility>
  <read_first>
    - /Users/jon/projects/crosswake/AGENTS.md
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-15-SUMMARY.md
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
  </read_first>
  <files>/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs</files>
  <behavior>
    - A subject-session version-1 binding and a version-2 binding may share tenant, subject, session_ref, installation, provider, platform, environment, and app identity while remaining distinct exact authorities.
    - Calling `Registry.revoke_for_logout/2` with the stale version-1 context revokes only the version-1 binding and appends logout evidence only for that selected revision.
    - The active version-2 binding remains active and receives no logout-revoked audit event.
    - The initial selector and the conditional `update_all` predicate both bind the already-validated `ctx.session_version`; a row that ceases to match that version cannot be mutated after selection.
  </behavior>
  <action>Start RED in `RegistryTest` with a named regression that creates two active subject-session bindings under the same subject_ref, org_ref, session_ref, installation_ref, provider/platform/environment, and app identity but with session versions 1 and 2. Use the public bind seam where its existing exact-scope behavior can create the state; if setup needs explicit rows, use valid `TokenBinding` fixtures rather than weakening production uniqueness. Submit the delayed logout with the version-1 context and assert the returned/reloaded revoked set contains only the version-1 binding, the version-2 binding is still active, and logout audit evidence exists only for the old exact revision. Keep fixture values opaque and never print raw token or identity material.

Then update `do_revoke_for_logout_scoped/3` per D-03/OPEN-01. Add exact equality with `ctx.session_version` to the initial active-binding query. Add the same exact version equality to the conditional `update_all` query alongside the selected binding_refs and active-state guard, so the mutation cannot widen between read and write. Preserve the existing subject-session restriction, subject/org/session_ref filters, transaction, closed `:no_active_bindings` result, lifecycle reason, sanitized audit insertion, telemetry allowlist, and public return shape. Do not change `revoke_for_session_revocation/2`, which intentionally has its separately documented version-threshold semantics.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/registry_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Selector assertion: `do_revoke_for_logout_scoped/3` requires equality with the authenticated `ctx.session_version` before rows enter the transaction's selected set.
    - Mutation assertion: the conditional `update_all` predicate independently requires the same exact session version in addition to selected binding_ref and active state.
    - Stale-authority assertion: a delayed version-1 logout revokes and audits only version 1 while the same-session-ref version-2 binding remains active and unaudited by that logout.
    - Boundary assertion: subject scope, tenant, subject, session_ref, closed reason/result, telemetry, and privacy behavior remain unchanged.
    - The focused deterministic ExUnit command exits zero.
  </acceptance_criteria>
  <done>Logout selection and mutation use the same exact authenticated session version, and executable evidence proves stale version-1 authority cannot revoke active version 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protect append-only notification-open event history with a forward restrictive migration</name>
  <reversibility rating="costly">The forward migration changes released deletion semantics, but its named guard can be removed by a later migration without rewriting or losing existing lifecycle rows; no one-way checkpoint is warranted.</reversibility>
  <read_first>
    - /Users/jon/projects/crosswake/AGENTS.md
    - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825180000_enforce_chimeway_binding_scope_consistency.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825190000_backfill_chimeway_notification_open_intent_scope.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent_event.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-19-SUMMARY.md
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
  </read_first>
  <files>/Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825200000_protect_chimeway_notification_open_intent_history.exs, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs</files>
  <behavior>
    - A host database upgraded from the released `20260603000000` boundary through all current migrations installs a deterministic named guard that rejects deletion of any intent having lifecycle events.
    - A rejected parent deletion leaves the intent row and every issued/consumed/denied/reconciliation event row unchanged.
    - Runtime Ecto deletion of an event-bearing intent fails closed and retains queryable parent/history evidence.
    - Intent rows with no event history are outside the verified append-only-history threat; the migration does not fabricate audit events or sensitive details merely to make them undeletable.
    - Rolling the migration down removes only the named restrictive guard and never rewrites the historical creation migration or deletes/restores lifecycle data.
  </behavior>
  <action>Start RED at both required seams. In `RegistrationAuthorityMigrationUpgradeTest`, seed a dedicated notification-open intent plus a sanitized lifecycle event while the isolated database is at the released boundary, run all forward migrations, assert the new named protection object exists, attempt a direct parent delete, assert the database rejects it, and then prove exact parent/event cardinality is unchanged. Capture the dedicated intent row, its event row including sanitized evidence fields, and their exact cardinalities; run `Ecto.Migrator` down by exactly one step from `20260825200000`; assert the fixed-name guard is absent from `sqlite_master`, the immediately preceding migration remains recorded, and the captured intent/event rows, cardinalities, and evidence values are byte-for-byte unchanged. Do not attempt a parent deletion after rollback because the historical cascade is intentionally active again. Keep the existing matched-authority, reconciliation-event, re-entry, and no-sensitive-output assertions green. In `RegistryNotificationOpenTest`, create or issue an intent through the existing runtime fixture, ensure at least one append-only event exists, attempt the ordinary Ecto parent deletion path, assert it fails, and reload both the intent and its event history to prove neither was erased.

Create only the next ordered migration `20260825200000_protect_chimeway_notification_open_intent_history.exs`; never rewrite `20260603000000_create_chimeway_notification_open_intents.exs` as the effective upgrade. Implement an `up/0`/`down/0` migration with one fixed-name SQLite `BEFORE DELETE` guard on `chimeway_notification_open_intents` that aborts when an event exists for `OLD.id`. This supplies restrictive/NO-ACTION semantics over the historical cascading foreign key without copying, dropping, or rebuilding lifecycle tables. The guard error must be static and non-disclosing. `down/0` drops only that named guard and makes no data mutation. Use Ecto `execute/2` or paired `execute/1` calls consistent with neighboring migrations, retain the existing event schema/index and append-only insert behavior, and do not add delete APIs, event tombstones, copied sensitive fields, logs, telemetry, packages, or changes to runtime authorization/activation.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs test/crosswake_example/chimeway/registry_notification_open_test.exs --seed 0 &amp;&amp; git -C /Users/jon/projects/crosswake diff --exit-code HEAD -- examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs</automated>
  </verify>
  <acceptance_criteria>
    - Forward-only assertion: the new `20260825200000` migration installs the restrictive guard during ordinary Ecto migration, and `git -C /Users/jon/projects/crosswake diff --exit-code HEAD -- examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs` exits zero to prove the released migration has no staged or unstaged rewrite.
    - Released-boundary assertion: a database created at the released boundary upgrades forward, exposes the named guard, rejects direct deletion of an event-bearing intent, and retains exact parent/event counts.
    - Runtime assertion: ordinary Ecto parent deletion is rejected and subsequent queries still return both the intent and all preexisting lifecycle events.
    - Rollback assertion: an isolated `Ecto.Migrator` one-step down from `20260825200000` crosses no earlier migration, removes the fixed-name guard from `sqlite_master`, leaves the preceding migration recorded, and preserves exact intent/event cardinalities plus every captured sanitized evidence value.
    - Explainability/privacy assertion: no event is fabricated, erased, or populated with token, identity, route, action, payload, URL, provider, or caller-controlled metadata during the migration or rejected deletion.
    - The focused deterministic migration/runtime ExUnit command exits zero.
  </acceptance_criteria>
  <done>The released host schema upgrades forward to restrictive parent deletion, and runtime plus migration regressions prove notification-open lifecycle history cannot be cascade-erased.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated logout context -> registry selector/CAS | A validated but stale session version must not authorize mutation of a newer binding sharing other authority fields. |
| intent parent mutation -> append-only event relation | A destructive parent operation crosses into auditable lifecycle history and must fail before cascading evidence loss. |
| released schema -> forward migration | Existing adopter databases must gain the guard through ordered migration without historical rewrite or inferred authority/evidence. |

## STRIDE Threat Register (ASVS L1; high blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-54 | Spoofing / Elevation of Privilege | `Registry.do_revoke_for_logout_scoped/3` selector | high | mitigate | Bind the validated exact session version in the initial selector and prove stale-v1/current-v2 isolation. |
| T-101-55 | Tampering / Denial of Service | logout `update_all` conditional mutation | high | mitigate | Re-qualify exact session version in the write predicate so selected authority cannot widen before mutation. |
| T-101-56 | Repudiation / Tampering | notification-open parent/event foreign-key lifecycle | high | mitigate | Install a forward named restrictive deletion guard and prove rejected deletion retains both parent and complete history. |
| T-101-57 | Information Disclosure | guard errors, migration fixtures, and lifecycle evidence | medium | mitigate | Use a static bounded database error, opaque fixture refs, unchanged sanitized event rows, and a no-sensitive-output regression. |
| T-101-58 | Denial of Service | deletion of event-bearing intents | low | accept | Retention is the intended append-only lifecycle contract; the guard is bounded to destructive parent deletes with existing history. |
</threat_model>

## Artifacts this phase produces

- Updated private Ecto predicates in `/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex` that require exact `ctx.session_version` during logout selection and conditional mutation.
- New forward migration/module `/Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825200000_protect_chimeway_notification_open_intent_history.exs` / `CrosswakeExample.Repo.Migrations.ProtectChimewayNotificationOpenIntentHistory`.
- One fixed-name restrictive SQLite parent-deletion guard for `chimeway_notification_open_intents` when child lifecycle events exist.
- New executable regressions in `registry_test.exs`, `registration_authority_migration_upgrade_test.exs`, and `registry_notification_open_test.exs` for stale logout isolation and append-only history retention.
- No new public API, dependency, package, external integration, credential, environment variable, telemetry field, route/action, notification payload, UI surface, manual checkpoint, or API coverage matrix.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Host binds APNs registrations and activates routes only under current host authority | 101-01..101-20 | COVERED | 101-20 closes the two remaining current-authority/explainability regressions without reopening verified activation paths. |
| REQ | OPEN-01 | Permission, observation, authenticated binding, idempotency, rotation, logout/revocation, provider invalidation | 101-04, 101-06, 101-09..101-12, 101-14..101-15, 101-17, 101-20 | COVERED | 101-20 adds exact session-version logout selector/CAS evidence. |
| REQ | OPEN-02 | Manifest-consistent closed action allowlist and default denial | 101-02, 101-03, 101-09 | COVERED | Spec-less probe row is accounted for by executed plans and passing 73-test verifier evidence; no new task is warranted. |
| REQ | OPEN-03 | Opaque offline tap, one-time consume, and current reauthorization | 101-01, 101-03, 101-05, 101-07, 101-09, 101-13..101-14, 101-16, 101-18 | COVERED | Spec-less probe row is accounted for by executed plans and passing host/companion/native verifier evidence. |
| REQ | OPEN-04 | Stale/denied opens activate no fallback and retain sanitized explainable evidence | 101-01, 101-05, 101-08..101-09, 101-19..101-20 | COVERED | 101-20 protects the already-produced event history from parent-delete cascade. |
| RESEARCH | — | Host-owned exact binding authority and predicate-CAS lifecycle mutations | 101-04, 101-10, 101-12, 101-14..101-15, 101-20 | COVERED | Exact version now reaches both logout predicates. |
| RESEARCH | — | Predicate-CAS one-time intent consumption, normalized manifest, RouteGate halt, and bounded opaque queue | 101-01..101-03, 101-05, 101-07..101-09, 101-13..101-14, 101-18 | COVERED | Already verified; no task duplicates it. |
| RESEARCH | — | Closed sanitized denial and durable lifecycle evidence | 101-08..101-09, 101-14, 101-16..101-17, 101-19..101-20 | COVERED | The migration retains existing sanitized rows and emits no new evidence values. |
| RESEARCH | — | No external package; use Ecto migration/ExUnit evidence | 101-20 | COVERED | No install task or package legitimacy checkpoint is needed. |
| CONTEXT | D-01 | Permission, APNs observation, and authenticated host binding remain sequential | 101-06, 101-11 | COVERED | Existing verified state machine remains unchanged. |
| CONTEXT | D-02 | Authenticated host registry is sole scoped binding authority | 101-04, 101-10, 101-12, 101-14..101-15, 101-20 | COVERED | Repair stays within the host registry. |
| CONTEXT | D-03 | Exact current revision only; lifecycle invalidation cannot affect a replacement | 101-04, 101-10..101-12, 101-14..101-15, 101-20 | COVERED | Task 1 directly closes the stale-version logout defect. |
| CONTEXT | D-04 | Raw token/sensitive facts never enter durable or observable evidence | 101-04, 101-08, 101-10, 101-12, 101-14, 101-16..101-17, 101-19..101-20 | COVERED | New tests use opaque refs and the migration creates no evidence payload. |
| CONTEXT | D-05..D-08 | Normalized host-bound action policy and current RouteGate authorization without fallback | 101-01..101-03, 101-08..101-09 | COVERED | Already verified; unchanged. |
| CONTEXT | D-09..D-11 | Bounded opaque queue, atomic consume, current recheck, no local/offline grant | 101-01, 101-03, 101-05, 101-07, 101-09, 101-13..101-14, 101-18 | COVERED | Already verified; unchanged. |
| CONTEXT | D-12 | Stable sanitized outcomes explain protected-open denial without sensitive data | 101-08..101-09, 101-14, 101-16..101-17, 101-19..101-20 | COVERED | Task 2 preserves append-only evidence against destructive parent deletion. |
| CONTEXT | D-13 | Narrow iOS seams; host retains persistence, auth, sessions, binding CAS, and intents | 101-04..101-20 | COVERED | Both repairs remain inside the existing host Ecto boundary. |

Excluded without gap: CONTEXT deferred ideas; Phase 102 digital twin, fake transport, named cross-repository verification commands, and CI; Phase 103 physical-iPhone evidence and adoption guidance; Android/FCM; generic offline/background sync; arbitrary notification actions; dashboards, analytics, and engagement tracking. `COVERAGE.md` already records the reasoned no-external-API declaration. No source item is missing from the 101-01..101-20 set.

## Spec-less Probe / Prohibition Recall Audit

| Row | Treatment | Plan location |
|---|---|---|
| OPEN-01 | Kept as a flagged unresolved assumption and converted only through the authoritative verifier's exact stale-logout gap. | `must_haves.assumptions`, Task 1, first prohibition |
| OPEN-02 | Explicitly accounted by existing plan/verification evidence; no inferred closure work. | `must_haves.assumptions`, source audit |
| OPEN-03 | Explicitly accounted by existing plan/verification evidence; no inferred closure work. | `must_haves.assumptions`, source audit |
| OPEN-04 | Kept as a flagged unresolved assumption and converted only through the authoritative verifier's append-only retention gap. | `must_haves.assumptions`, Task 2, second prohibition |

The two kept descriptor-less prohibitions equal the two authoritative gaps. No fabricated check or silent scope expansion is introduced.

<verification>
Run `cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/registry_test.exs test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs test/crosswake_example/chimeway/registry_notification_open_test.exs --seed 0`, then `git -C /Users/jon/projects/crosswake diff --check`. Confirm the historical `20260603000000_create_chimeway_notification_open_intents.exs` is unchanged, the new forward migration is the sole schema artifact, and Chimeway's existing `COVERAGE.md` declaration remains unchanged.
</verification>

<success_criteria>
A stale version-1 logout cannot revoke an active version-2 binding sharing the same session reference, and neither direct nor runtime parent deletion can erase notification-open lifecycle event history after the forward upgrade. All focused deterministic regressions pass with sanitized evidence and no human checkpoint.
</success_criteria>

<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-20-SUMMARY.md` when done.</output>
