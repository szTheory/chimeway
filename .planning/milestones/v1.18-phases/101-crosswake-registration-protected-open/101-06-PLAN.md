---
phase: 101-crosswake-registration-protected-open
plan: "06"
type: execute
wave: 3
depends_on: ["101-01", "101-04"]
files_modified:
  - ../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeDelegates.swift
  - ../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift
  - ../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NotificationRegistrationCoordinator.swift
  - ../crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NotificationRegistrationTests.swift
  - ../crosswake/test/fixtures/chimeway_notification_permission_loss_v1.json
  - ../crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/chimeway_notification_permission_loss_v1.json
  - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_registration_adapter.ex
  - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/notification_registration_adapter_test.exs
autonomous: true
requirements: [OPEN-01]
must_haves:
  truths:
    - "D-01/D-13/OPEN-01: iOS exposes explicit permission, APNs observation, and authenticated host binding transitions through narrow host delegates."
    - "D-03/OPEN-01: a post-binding native permission recheck that observes loss invokes authenticated host revocation for the exact retained binding revision; stale loss cannot revoke a replacement or another scope."
    - "D-04/OPEN-01: shell-core does not persist, log, or publish raw APNs token material."
    - "D-02/D-03/OPEN-01: the native permission-loss callback reaches a production-owned authenticated host adapter and the real Registry.revoke_for_permission_loss/2 exact-revision CAS; a stale callback after rotation changes neither the replacement nor unrelated scopes."
  assumptions:
    - statement: "OPEN-01 unclassified probe: a coordinator plus weak host delegate is the narrowest reusable iOS seam for registration state without moving authority into CrossWake."
      status: unresolved
      flagged: true
  artifacts:
    - path: "../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NotificationRegistrationCoordinator.swift"
      provides: "Explicit native permission/observation/binding state coordinator with post-binding permission-loss recheck"
    - path: "../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_registration_adapter.ex"
      provides: "Production host adapter from authenticated permission-loss callback command to Registry.revoke_for_permission_loss/2"
    - path: "../crosswake/test/fixtures/chimeway_notification_permission_loss_v1.json"
      provides: "Canonical versioned bound-rotate-permission-denied callback transcript shared with the Swift XCTest resource copy"
    - path: "../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/notification_registration_adapter_test.exs"
      provides: "Adapter-to-real-registry CAS integration and transcript drift/privacy proof"
  key_links:
    - from: "NotificationRegistrationCoordinator"
      to: "NotificationRegistrationDelegate"
      via: "transient APNs Data handed only to authenticated host callback"
      pattern: "NotificationRegistration"
    - from: "NotificationRegistrationCoordinator.recheckPermissionState()"
      to: "NotificationRegistrationDelegate.revokeNotificationBindingForPermissionLoss"
      via: "permission transition from bound to denied sends only the exact retained binding_ref and safe scope to the authenticated host"
      pattern: "recheckPermissionState|revokeNotificationBindingForPermissionLoss"
    - from: "NotificationRegistrationDelegate.revokeNotificationBindingForPermissionLoss"
      to: "CrosswakeExample.Chimeway.NotificationRegistrationAdapter.revoke_for_permission_loss/2"
      via: "both XCTest and ExUnit consume byte-identical `chimeway_notification_permission_loss_v1` transcript copies describing the callback command and closed result"
      pattern: "chimeway_notification_permission_loss_v1|revokeNotificationBindingForPermissionLoss"
    - from: "CrosswakeExample.Chimeway.NotificationRegistrationAdapter.revoke_for_permission_loss/2"
      to: "CrosswakeExample.Chimeway.Registry.revoke_for_permission_loss/2"
      via: "authenticated host context is matched against tenant/org, subject, installation, APNs provider/environment/topic app identity, session/version, channel, and exact current binding_ref before invoking the real registry CAS"
      pattern: "NotificationRegistrationAdapter|Registry.revoke_for_permission_loss"
  prohibitions:
    - statement: "The shell must not treat token observation as an authenticated binding or route grant."
      status: unverified
      flagged: true
---
<objective>Add the bounded iOS registration seam and wire its permission-loss callback through the production authenticated host adapter to the real registry CAS without moving host authority (D-01 through D-04, D-13). Purpose: make native state explicit and prove exact-revision revocation through the actual callback contract while preserving transient token custody. Output: coordinator, delegate/config seam, shared transcript, owned host adapter, and linked XCTest/ExUnit evidence.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-PATTERNS.md
@.planning/phases/101-crosswake-registration-protected-open/101-04-SUMMARY.md
<interfaces>Follow `NotificationTokenDelegate` and weak config properties. The new delegate accepts transient token data plus safe environment/topic facts and returns opaque binding outcome; authentication and persistence remain host-owned. After a bound state, `recheckPermissionState()` observes the current native permission and `revokeNotificationBindingForPermissionLoss` carries the exact retained binding_ref to the authenticated host implementation of `Registry.revoke_for_permission_loss/2`.</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Model permission, APNs observation, and host binding as separate native states</name>
  <read_first>
    - ../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeDelegates.swift
    - ../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift
    - ../crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/BridgeConformanceTests.swift
    - ../crosswake/packages/crosswake-shell-core-ios/Package.swift
    - ../crosswake/test/fixtures/bridge_contract_vectors.json
    - ../crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift
  </read_first>
  <files>../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeDelegates.swift, ../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift, ../crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NotificationRegistrationCoordinator.swift, ../crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NotificationRegistrationTests.swift, ../crosswake/test/fixtures/chimeway_notification_permission_loss_v1.json, ../crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/chimeway_notification_permission_loss_v1.json</files>
  <behavior>
    - Permission request success moves to permission-granted without implying token or binding.
    - APNs callback exposes transient observation to the configured host delegate and does not mark bound until an opaque host result arrives.
    - A post-binding permission recheck that transitions to denied invokes the host delegate once with the exact retained binding_ref; repeated denied checks are idempotent and never broaden the target.
    - Missing delegate, denial, callback failure, and host rejection remain explicit non-bound states with no token diagnostics.
  </behavior>
  <action>Create XCTest cases first with an injected permission-status provider and a recording delegate only for native state-machine isolation. Add a narrow `NotificationRegistrationDelegate` and weak `CrosswakeShellConfig` property/capability. Implement a coordinator whose enum/state transitions distinguish permission, observing, binding, bound, and denied/failed outcomes. Pass raw token `Data` only synchronously to the host delegate; retain only opaque binding_ref and closed state/reason in shell state. Per D-03, add `recheckPermissionState()` for foreground/settings-change use: when a previously bound registration becomes denied, invoke `revokeNotificationBindingForPermissionLoss` exactly once with the retained binding_ref plus the closed tenant/org, subject, installation, APNs provider/environment/topic app-identity, and session/version scope required by the authenticated host command; await its closed outcome and enter a non-bound permission-denied state. Encode the bound -> rotated -> permission-denied callback and expected sanitized result in canonical versioned `test/fixtures/chimeway_notification_permission_loss_v1.json`, copy it into the Swift test Resources using the established bridge-contract-vector pattern, and make XCTest decode that resource to drive the actual coordinator callback. The transcript itself must contain only opaque refs and bounded safe classifications, must omit token fields entirely, and must be checked against a test-local forbidden APNs-token sentinel that never enters the committed transcript. Do not claim the recording delegate proves registry behavior; Task 2 owns the authenticated adapter-to-CAS proof.</action>
  <verify><automated>cd ../crosswake/packages/crosswake-shell-core-ios &amp;&amp; swift test --filter NotificationRegistrationTests</automated></verify>
  <acceptance_criteria>
    - Native states are sequential and token observation never equals authenticated binding.
    - Post-binding permission loss reaches the authenticated host exact-revision invalidation seam once; stale loss leaves a rotated replacement and all unrelated scopes active.
    - Raw token is absent from retained state and diagnostic descriptions.
    - Capability is advertised only when the host delegate is configured.
  </acceptance_criteria>
  <done>iOS registration is explicit, host-owned at the authority step, and privacy-safe.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Wire the authenticated host permission-loss adapter to the real registry CAS</name>
  <read_first>
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
    - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
    - ../crosswake/test/crosswake/contract/contract_drift_test.exs
    - ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/contracts.ex
    - ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/telemetry.ex
  </read_first>
  <files>../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_registration_adapter.ex, ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/notification_registration_adapter_test.exs</files>
  <behavior>
    - The production adapter accepts only an authenticated current host principal plus the closed permission-loss callback command and invokes `Registry.revoke_for_permission_loss/2`, not a recording fake.
    - After bound revision A rotates to revision B, replaying the transcript's stale callback for A changes zero rows for B and zero rows in every unrelated tenant/org, subject, installation, provider/environment/topic app identity, or session/version scope.
    - The native resource copy is byte-identical to the canonical v1 transcript, and both XCTest and ExUnit exercise its same callback fields and expected outcome.
    - Adapter result, registry audit/telemetry capture, inspected rows, and serialized transcript evidence contain no raw APNs token or test-local forbidden sentinel.
  </behavior>
  <action>Write the adapter integration test before implementation. Add production-owned `CrosswakeExample.Chimeway.NotificationRegistrationAdapter.revoke_for_permission_loss/2`: validate the authenticated host principal and current session against the closed callback command, reject missing or mismatched tenant/org, subject, installation, provider/environment/topic app identity, session/version, channel, or binding_ref fields, then construct the exact context/options and call the existing public `Registry.revoke_for_permission_loss/2`. Do not expose a bulk, installation-only, token-only, or unauthenticated revocation path. In the integration test, load the canonical v1 transcript, assert its Swift resource copy is byte-identical, create its bound revision A plus unrelated active rows, rotate the target to revision B through `Registry.bind_or_rotate/3`, deliver the stale permission-loss command through the production adapter, and assert the adapter reports the closed stale/no-op result while B and every unrelated scope stay active. Seed a test-local forbidden token value only through the transient pre-rotation token-observation input, then capture the real registry command/audit/telemetry boundary and recursively scan returned evidence and serialized safe facts for that value; neither the adapter command schema nor either committed transcript copy may contain a token field.</action>
  <verify><automated>cd ../crosswake/packages/crosswake-shell-core-ios &amp;&amp; swift test --filter NotificationRegistrationTests &amp;&amp; cd ../../examples/phoenix_host &amp;&amp; mix test test/crosswake_example/chimeway/notification_registration_adapter_test.exs --seed 0</automated></verify>
  <acceptance_criteria>
    - The versioned callback transcript drives both native delegate delivery and the production host adapter contract, with byte-for-byte drift detection.
    - The authenticated adapter invokes the real registry exact-revision command; stale revision A changes no rotated revision B or unrelated scoped binding.
    - The callback command and all resulting evidence omit raw APNs token material and pass the forbidden-sentinel scan.
  </acceptance_criteria>
  <done>Native permission loss is proven across the companion contract boundary through the owned authenticated host adapter and real exact-revision registry CAS.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Apple callback -> host delegate | Raw APNs token enters transient host custody. |
| native permission-loss callback -> authenticated Phoenix host adapter -> registry | Stale client-observed scope requests a durable exact-revision authority change. |
## STRIDE Threat Register (ASVS L1; high blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-14 | Spoofing/Tampering | Registration and permission-loss state | high | mitigate | Separate observed/bound states and exact retained-revision permission-loss callback under XCTest plus the host stale-revision race test. |
| T-101-15 | Information Disclosure | Native token | high | mitigate | Synchronous delegate handoff and retained-state leak assertions. |
| T-101-15A | Spoofing/Tampering | Permission-loss host adapter | high | mitigate | Authenticated scope matching plus shared-transcript integration against the real exact-revision registry CAS. |
</threat_model>
## Artifacts this phase produces
- Swift symbols: `NotificationRegistrationDelegate`, `NotificationRegistrationCoordinator`, explicit registration state/outcome types.
- Host symbol: `CrosswakeExample.Chimeway.NotificationRegistrationAdapter.revoke_for_permission_loss/2` authenticated adapter to `Registry.revoke_for_permission_loss/2`.
- Contract artifacts: canonical v1 permission-loss transcript plus byte-identical Swift resource copy consumed by both runtime tests.
- Config/capability surface: optional weak registration delegate. No CLI.
<verification>Focused Swift registration tests and the transcript-driven production-adapter-to-registry ExUnit integration test pass.</verification>
<success_criteria>Native observation is never authority, only host-confirmed opaque binding reaches bound state, and stale native permission loss cannot revoke a rotated replacement or unrelated scope through the actual host callback path.</success_criteria>
<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-06-SUMMARY.md` when done.</output>
