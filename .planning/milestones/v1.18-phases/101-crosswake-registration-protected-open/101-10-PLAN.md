---
phase: 101-crosswake-registration-protected-open
plan: "10"
type: execute
wave: 6
depends_on: ["101-04", "101-05", "101-09"]
files_modified:
  - ../crosswake/examples/phoenix_host/priv/repo/migrations/20260602100000_create_chimeway_token_bindings.exs
  - ../crosswake/examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs
  - ../crosswake/examples/phoenix_host/priv/repo/migrations/20260824210000_upgrade_chimeway_registration_authority.exs
  - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
  - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
  - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs
  - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
autonomous: true
gap_closure: true
requirements: [OPEN-01]
must_haves:
  truths:
    - "D-02/D-03/OPEN-01: an already-migrated Phoenix host reaches the current binding and notification-intent authority schema through a new forward migration; the two released historical migration definitions remain unchanged."
    - "D-02/D-03/OPEN-01: active token and authenticated subject authority identities exclude mutable app_identity_posture, include app_identity_ref, and permit at most one active row per actual authority scope."
    - "D-02/D-03/OPEN-01: concurrent observations of one actual authority scope with different postures converge to one active binding and both callers receive a conflict-safe result."
    - "D-04/OPEN-01: migration reconciliation, registry errors, audit facts, and test output expose no raw APNs token, credential, subject payload, or provider body."
  assumptions:
    - statement: "FLAGGED ASSUMPTION — OPEN-01 remained unclassified and unresolved in the spec-less edge probe; 101-VERIFICATION.md is the authoritative concrete acceptance source for historical upgrade safety and posture-independent concurrent uniqueness."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-02 remained unclassified and unresolved in the spec-less edge probe; this OPEN-01 gap closure does not reclassify or claim new policy coverage for it."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-03 remained unclassified and unresolved in the spec-less edge probe; this OPEN-01 gap closure does not reclassify or claim new one-time-open coverage for it."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-04 remained unclassified and unresolved in the spec-less edge probe; this OPEN-01 gap closure does not reclassify or claim new no-fallback coverage for it."
      status: unresolved
      flagged: true
  artifacts:
    - path: "../crosswake/examples/phoenix_host/priv/repo/migrations/20260824210000_upgrade_chimeway_registration_authority.exs"
      provides: "Forward authority-column backfill, conservative legacy-row reconciliation, and posture-independent replacement indexes"
    - path: "../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs"
      provides: "Released-schema to current-schema integration proof on a throwaway SQLite host database"
    - path: "../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex"
      provides: "Conflict-safe bind retry/reload semantics aligned with the database's actual active identity"
    - path: "../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs"
      provides: "Concurrent differing-posture single-active-binding proof"
  key_links:
    - from: "released migrations 20260602100000 and 20260603000000"
      to: "20260824210000_upgrade_chimeway_registration_authority.exs"
      via: "Ecto.Migrator pause, historical-row seed, forward resume, and schema/data assertions"
      pattern: "Ecto.Migrator.run|20260824210000"
    - from: "Registry.bind_or_rotate/3"
      to: "posture-independent active unique indexes"
      via: "named constraint recognition followed by bounded whole-operation retry and authoritative scope reload"
      pattern: "active_token_identity|active_subject_session_scope|active_subject_installation_scope"
  prohibitions:
    - statement: "Released migration semantics must not be extended in place; only exact restoration plus the new additive migration is allowed."
      status: unverified
      flagged: true
    - statement: "Mutable posture must not participate in active binding identity or permit a second active authority row."
      status: unverified
      flagged: true
    - statement: "Legacy rows lacking trustworthy app identity must not be guessed into current delivery or route authority."
      status: unverified
      flagged: true
---

<objective>
Repair the Phoenix host's durable OPEN-01 upgrade and concurrent binding identity path without rewriting released schema history (D-02 through D-04).

Purpose: Existing adopters must receive the same authority fields and constraints as fresh hosts, and mutable diagnostic posture must never split one current binding identity.
Output: Restored historical migrations, one forward additive authority migration, an upgrade-path integration test, and conflict-safe posture-independent binding convergence.
</objective>

<assumption_delta_decision>
Primary noun: one active host-owned binding revision per authenticated installation/provider/environment/app-identity/session scope. Decision: no-change — the phrase “no fallback route” is a denial invariant and introduces no singular-to-plural identity transition; app_identity_posture remains mutable evidence, not another binding identity.
</assumption_delta_decision>

<api_coverage_decision>
The deterministic API-coverage detector returned `detected=false`. This plan repairs an already-integrated local APNs lifecycle through Ecto migrations and an in-process registry; it adds no external API, SDK, service, endpoint, credential, or capability surface, so no coverage matrix or override artifact is required.
</api_coverage_decision>

<schema_push_decision>
The schema-push contribution does not apply: this plan uses ordered Ecto migrations and an executable upgrade path, not Payload, Prisma, Drizzle, Supabase, or TypeORM schema push.
</schema_push_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
@.planning/phases/101-crosswake-registration-protected-open/101-04-SUMMARY.md
@.planning/phases/101-crosswake-registration-protected-open/101-05-SUMMARY.md

<interfaces>
The released token-binding migration is the blob at `7680f2f5^:examples/phoenix_host/priv/repo/migrations/20260602100000_create_chimeway_token_bindings.exs`; the released intent migration is the blob at `26d225cb^:examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs`. `Registry.bind_or_rotate/3` scopes active lookup by token fingerprint, provider, platform, environment, app_identity_ref, installation, subject scope, subject/org, and session/version; app_identity_posture is updated evidence only. The example host uses `Ecto.Adapters.SQLite3`; `test/crosswake/proof/phase60_chimeway_registry_test.exs` is the established throwaway-database `Ecto.Migrator` analog.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove and implement the released-schema forward upgrade</name>
  <reversibility rating="costly">Changing an already-released durable schema requires a coordinated forward migration, but the locked authority model is already decided and the new migration has an explicit rollback path.</reversibility>
  <read_first>
    - ../crosswake/examples/phoenix_host/priv/repo/migrations/20260602100000_create_chimeway_token_bindings.exs
    - ../crosswake/examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - ../crosswake/test/crosswake/proof/phase60_chimeway_registry_test.exs
    - .planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
  </read_first>
  <files>../crosswake/examples/phoenix_host/priv/repo/migrations/20260602100000_create_chimeway_token_bindings.exs, ../crosswake/examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs, ../crosswake/examples/phoenix_host/priv/repo/migrations/20260824210000_upgrade_chimeway_registration_authority.exs, ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs</files>
  <behavior>
    - A database migrated only through the two released migration versions can contain released-shape binding and intent rows, then migrate forward without resetting or replaying either historical migration.
    - The forward migration adds tenant_ref, subject_ref, session_ref, and session_version to notification intents, backfills them from the exact binding_ref relationship, and makes unmatched legacy intent authority fail closed rather than inventing ownership.
    - A legacy binding without a trustworthy app_identity_ref is moved out of active eligibility with a stable closed lifecycle reason before receiving an explicitly non-authoritative opaque legacy marker; no tenant, topic, subject, or app identity is guessed.
    - Replacement partial unique indexes omit app_identity_posture and enforce token identity plus subject-session and subject-installation authority using app_identity_ref.
    - Duplicate active rows that differ only by posture are reconciled deterministically before replacement indexes are created, preserving one latest current row and terminally superseding the rest.
  </behavior>
  <action>Start RED with `registration_authority_migration_upgrade_test.exs`. Use a throwaway SQLite database and the Phase 60 migrator harness: migrate the checked-in path only through `20260603000000`, insert released-shape bindings and intents (including one valid exact relationship, one missing app identity, and a differing-posture active collision), then resume all migrations. First restore the two historical files exactly from the named parent-commit blobs; do not retain any Phase 101 authority extension in those released definitions. Add `20260824210000_upgrade_chimeway_registration_authority.exs` with explicit `up/0` and `down/0`. In `up/0`, add the four nullable intent authority columns, backfill matched intents from `TokenBinding` (`org_ref` is the established tenant_ref), leave unmatched intent authority non-consumable and set its lifecycle terminal rather than fabricating scope, reconcile missing binding app identity into a closed inactive legacy state with a bounded opaque non-authoritative marker, collapse pre-existing active collisions deterministically by latest `last_seen_at` then id, drop the three released active indexes, and recreate the same named indexes over the actual D-02 authority identities with app_identity_ref and without posture. In `down/0`, restore the released index definitions and remove only the four added intent columns; document that application data transitions are not resurrected. Assert exact column presence/backfill, terminal treatment of ambiguous rows, one active collision winner, exact replacement index SQL, unchanged historical module shapes, and a fresh full migration run. Keep SQL fixed to known table/column names, never interpolate row data, and emit no sensitive row values.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The upgrade test starts from the released migration boundary, seeds historical rows, resumes forward, and exits zero without database reset.
    - `git -C /Users/jon/projects/crosswake diff 7680f2f5^ -- examples/phoenix_host/priv/repo/migrations/20260602100000_create_chimeway_token_bindings.exs` and the corresponding `26d225cb^` intent comparison show only exact restoration relative to their released blobs.
    - The migrated intent row has authority values derived from its exact binding; unmatched or identity-ambiguous rows are not active authority.
    - All three replacement index definitions contain app_identity_ref and none contains app_identity_posture.
  </acceptance_criteria>
  <done>An already-migrated host upgrades additively to the current fail-closed authority schema, while fresh installs reach the identical final schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Converge concurrent differing-posture binds on one active authority row</name>
  <reversibility rating="costly">The active uniqueness definition is durable authority behavior shared by migrations, changesets, and registry transactions; reversal would require another index migration.</reversibility>
  <read_first>
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
    - ../crosswake/examples/phoenix_host/priv/repo/migrations/20260824210000_upgrade_chimeway_registration_authority.exs
    - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
    - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs
    - .planning/phases/101-crosswake-registration-protected-open/101-04-SUMMARY.md
  </read_first>
  <files>../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex, ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex, ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs</files>
  <behavior>
    - Two barrier-synchronized `bind_or_rotate/3` calls with identical actual authority and fingerprint but different matched/unknown posture both return success and converge on one binding_ref.
    - Exactly one row remains active for the actual token and subject authority scope; posture is mutable closed evidence on that row and never part of lookup or uniqueness.
    - A named active-identity constraint race triggers only a bounded retry/reload of the whole authoritative operation; unrelated changeset failures and binding_ref collisions remain errors.
    - Existing rotation, stale permission-loss, unrelated-scope, and raw-token tests remain green.
  </behavior>
  <action>Add the differing-posture concurrency case to `registry_test.exs` first, using the established ETS barrier, unique opaque refs, `async: false`, and deterministic seed. Update all three `TokenBinding.changeset/2` named unique-constraint field lists to mirror the forward migration exactly and remove posture from those identities. Preserve the existing D-02 lookup scope, where posture is absent and app_identity_ref is present. Extend the bind transaction wrapper to recognize only conflicts from the three named active indexes and retry the complete operation a bounded number of times after the winner commits; the retry must re-run the authoritative same-token/displaced lookup so the loser refreshes or rotates the winner instead of surfacing a stale constraint changeset. Retain the existing SQLite busy retry and exact-CAS invalidation semantics, keep unrelated constraint errors visible, and assert returned/audit/telemetry facts contain only the existing closed posture value and no raw token sentinel.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs test/crosswake_example/chimeway/registry_test.exs test/crosswake_example/chimeway/registry_notification_open_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - The new concurrent test returns two successful results whose authoritative binding_ref is identical and queries exactly one active row.
    - Database indexes, changeset constraint field lists, and registry lookup all name the same posture-independent authority identity.
    - Existing idempotency, rotation, exact-revision invalidation, open-intent authority, and token privacy cases exit zero.
  </acceptance_criteria>
  <done>Mutable posture cannot create duplicate active authority, and conflict races converge through the same authenticated registry contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| released host database -> forward migration | Historical rows lacking newer authority facts cross into the current schema and must not gain guessed authority. |
| concurrent authenticated observations -> registry transaction | Two client observations race to mutate one durable active authority scope. |

## STRIDE Threat Register (ASVS L1; high blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-24 | Tampering/Elevation | Historical backfill | high | mitigate | Exact binding-derived intent backfill; ambiguous legacy binding/intent rows become terminal and non-authoritative under upgrade tests. |
| T-101-25 | Elevation/DoS | Active binding constraint race | high | mitigate | Posture-independent database constraints plus bounded named-conflict retry and concurrent two-caller proof. |
| T-101-26 | Information Disclosure | Migration and registry evidence | high | mitigate | Fixed SQL, opaque legacy markers, closed posture facts, and recursive raw-token sentinel assertions. |
| T-101-27 | Repudiation | Duplicate reconciliation | medium | mitigate | Deterministic winner ordering and stable terminal lifecycle state for displaced rows asserted from migrated data. |
</threat_model>

## Artifacts this phase produces

- Restored files: released definitions of `20260602100000_create_chimeway_token_bindings.exs` and `20260603000000_create_chimeway_notification_open_intents.exs`.
- New migration file/module: `20260824210000_upgrade_chimeway_registration_authority.exs` / `CrosswakeExample.Repo.Migrations.UpgradeChimewayRegistrationAuthority`.
- Added durable intent fields: `tenant_ref`, `subject_ref`, `session_ref`, `session_version` through the forward migration.
- Replaced indexes: `chimeway_token_bindings_active_token_identity_index`, `chimeway_token_bindings_active_subject_session_scope_index`, `chimeway_token_bindings_active_subject_installation_scope_index`, each posture-independent and app-identity scoped.
- New test file/module: `registration_authority_migration_upgrade_test.exs` / `CrosswakeExample.Chimeway.RegistrationAuthorityMigrationUpgradeTest`.
- New registry behavior/helper: bounded recognition and retry/reload of named active-identity constraint races.
- New test behavior: barrier-synchronized differing-posture concurrent bind convergence in `RegistryTest`.
- No new package, dependency, external API, credential, environment variable, CLI flag, schema-push operation, or sensitive telemetry field.

<verification>
Run the two task commands from `/Users/jon/projects/crosswake/examples/phoenix_host`; both must exit zero. Then run `git -C /Users/jon/projects/crosswake diff --check`.
</verification>

<success_criteria>
Existing and fresh Phoenix hosts reach one current authority schema, released migration history is restored, and concurrent posture variance can never create a second active binding.
</success_criteria>

<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-10-SUMMARY.md` when done.</output>
