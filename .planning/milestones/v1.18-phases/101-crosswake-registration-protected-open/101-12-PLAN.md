---
phase: 101-crosswake-registration-protected-open
plan: "12"
type: execute
wave: 7
depends_on: ["101-10"]
files_modified:
  - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
  - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260824210000_upgrade_chimeway_registration_authority.exs
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs
  - /Users/jon/projects/crosswake/test/crosswake/proof/phase60_chimeway_registry_test.exs
  - /Users/jon/projects/crosswake/examples/phoenix_host/README.md
autonomous: true
gap_closure: true
requirements: [OPEN-01]
must_haves:
  truths:
    - "D-02/D-03/OPEN-01: invalidating provider feedback can mutate only one exact active binding_ref whose authenticated app, tenant/org, subject, installation, session/version, provider, platform, and environment scope all match."
    - "D-03/OPEN-01: a feedback event with a shared or colliding token selector cannot revoke a rotated revision or a binding in another authority scope."
    - "D-02/D-03/OPEN-01: the forward authority migration deterministically reconciles both the authority uniqueness partition and the token-identity uniqueness partition before creating either replacement unique index."
  assumptions:
    - statement: "FLAGGED ASSUMPTION — OPEN-01 remains unclassified and unresolved in the spec-less edge probe; 101-VERIFICATION.md supplies the exact provider-invalidation and forward-upgrade acceptance repaired here."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-02 remains unclassified and unresolved in the spec-less edge probe; this OPEN-01 closure does not reclassify or claim new manifest-policy behavior."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-03 remains unclassified and unresolved in the spec-less edge probe; this OPEN-01 closure does not reclassify or claim new offline-open behavior."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-04 remains unclassified and unresolved in the spec-less edge probe; this OPEN-01 closure does not reclassify or claim new no-fallback behavior."
      status: unresolved
      flagged: true
  artifacts:
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex"
      provides: "Authenticated exact-binding provider invalidation query and conditional update"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260824210000_upgrade_chimeway_registration_authority.exs"
      provides: "Deterministic reconciliation for both replacement uniqueness domains"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs"
      provides: "Old-valid different-authority/same-token forward-upgrade collision proof"
  key_links:
    - from: "Registry.apply_provider_feedback/2"
      to: "TokenBinding exact active revision"
      via: "validated authenticated_context plus binding_ref/app_identity_ref predicates repeated in selection and update"
      pattern: "provider_feedback_scope|binding_ref|session_version|app_identity_ref"
    - from: "reconcile_active_collisions/0"
      to: "replace_active_indexes/0"
      via: "authority-domain reconciliation followed by token-identity-domain reconciliation before unique-index creation"
      pattern: "ROW_NUMBER|token_fingerprint|active_token_identity_index"
  prohibitions:
    - requirement_id: OPEN-01
      statement: "Provider token evidence must not become revocation authority or select bindings outside the authenticated exact binding scope."
      status: unverified
      flagged: true
    - requirement_id: OPEN-01
      statement: "Migration reconciliation must not guess missing authority or expose raw token, identity payload, or provider-body values in evidence."
      status: unverified
      flagged: true
---

<objective>
Close the two remaining host-registry OPEN-01 defects: exact scoped provider invalidation and complete forward uniqueness reconciliation (D-02, D-03, D-04).

Purpose: Provider feedback and old host data must never revoke or block a different current authority scope.
Output: Fully scoped invalidation predicates, two-domain migration reconciliation, updated host proof/docs, and executable collision regressions.
</objective>

<assumption_delta_decision>
Primary noun: exact host-owned binding revision within authenticated authority scope. Decision: no-change — the roadmap phrase “no fallback route” is a protected-open denial invariant, not a second registration identity or fallback binding representation.
</assumption_delta_decision>

<api_coverage_decision>
The deterministic API-coverage detector returned `detected=false`. This plan tightens an existing in-process host registry and Ecto migration; it adds no external API, SDK, service, endpoint, credential, or capability surface, so no COVERAGE.md artifact is required.
</api_coverage_decision>

<schema_push_decision>
The schema-push contribution does not apply. The changed migration is Elixir/Ecto, which is outside the hook's supported Payload CMS, Prisma, Drizzle, Supabase, and TypeORM patterns; the throwaway-database migration test is the executable schema evidence.
</schema_push_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-RESEARCH.md
@.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
@.planning/phases/101-crosswake-registration-protected-open/101-REVIEW.md
@.planning/phases/101-crosswake-registration-protected-open/101-10-SUMMARY.md

<interfaces>
Keep `Registry.apply_provider_feedback/2` as the host command. Non-invalidating feedback remains audit-only. Invalidating feedback must require `authenticated_context:` plus exact `binding_ref` and `app_identity_ref` options; token_ref/token_fingerprint are corroborating evidence only. The migration must reconcile both uniqueness domains before `replace_active_indexes/0` creates their indexes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind provider invalidation to one authenticated exact revision</name>
  <reversibility rating="costly">The command semantics coordinate the example registry, historical proof, and adopter documentation, but remain behind the host-owned registry boundary.</reversibility>
  <read_first>
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
    - /Users/jon/projects/crosswake/test/crosswake/proof/phase60_chimeway_registry_test.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/README.md
    - /Users/jon/projects/crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/contracts.ex
    - /Users/jon/projects/crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/redaction.ex
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-10-SUMMARY.md
  </read_first>
  <files>/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs, /Users/jon/projects/crosswake/test/crosswake/proof/phase60_chimeway_registry_test.exs, /Users/jon/projects/crosswake/examples/phoenix_host/README.md</files>
  <behavior>
    - Invalidating feedback without `authenticated_context`, exact `binding_ref`, or `app_identity_ref` fails closed before a binding query or lifecycle write.
    - A valid invalidation selects and conditionally updates one row only when binding_ref, app identity, org/tenant, subject, subject scope, installation, session ref/version, provider, platform, environment, and active state all match.
    - A token fingerprint or token_ref shared with another authority scope is only corroborating evidence; it cannot select or update that other row.
    - Non-invalidating delivery/provider feedback remains advisory audit evidence and does not acquire binding authority.
  </behavior>
  <action>Start RED in `RegistryTest` with two active bindings that intentionally share the provider token selector but differ in authenticated org/tenant, subject, installation, session/version, app identity, and binding_ref. Keep the public `apply_provider_feedback/2` arity, but add `provider_feedback_scope/2` for invalidating events: validate `authenticated_context` through `validate_context/1`, require non-empty current installation/session plus `session_version`, and require exact `binding_ref` and `app_identity_ref` from options. Change `feedback_target_query/1` to accept that validated scope and make its primary identity the exact binding_ref plus every D-02/D-03 authority field and provider/platform/environment; when token_ref or token_fingerprint exists, add it only as an extra equality predicate. Repeat the same full predicate in the `repo.update_all/3` conditional write rather than updating a previously selected list by reference and active state alone. Preserve the existing closed `:no_active_bindings` mismatch outcome so scope failures do not reveal which field differed. Leave audit-only feedback non-mutating, and keep D-04 telemetry/audit projections free of authenticated identity detail and raw token material. Update the Phase 60 registry proof's invalidating calls and zero-match/no-authority regressions to supply or reject the new scoped options, and update the example-host README worker to resolve authenticated host scope and exact binding_ref before applying invalidating feedback; do not imply provider fields authenticate the command.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/registry_test.exs --seed 0 &amp;&amp; cd /Users/jon/projects/crosswake &amp;&amp; MIX_ENV=test mix test test/crosswake/proof/phase60_chimeway_registry_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: invalidating `apply_provider_feedback/2` reaches `provider_feedback_scope/2`, and both selection and `update_all` predicates contain binding_ref, org_ref, subject_ref, subject_scope, installation_ref, session_ref, session_version, app_identity_ref, provider, platform, environment, and active state.
    - Behavior assertion: one same-token/different-authority fixture remains active while only the exact authenticated binding becomes revoked or invalid.
    - Negative assertion: missing, stale, or mismatched authenticated scope returns a closed error and changes zero rows.
    - Command assertion: both focused ExUnit commands exit zero with deterministic seed 0.
  </acceptance_criteria>
  <done>Invalidating provider feedback can affect only the authenticated exact current binding revision, never another token-sharing or stale scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile both forward-migration uniqueness domains</name>
  <reversibility rating="costly">The deterministic reconciliation mutates conflicting legacy rows during forward upgrade, but extends the already locked supersession policy without changing released migration history.</reversibility>
  <read_first>
    - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260602100000_create_chimeway_token_bindings.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260824210000_upgrade_chimeway_registration_authority.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-10-SUMMARY.md
  </read_first>
  <files>/Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260824210000_upgrade_chimeway_registration_authority.exs, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs</files>
  <behavior>
    - Old-valid rows colliding in the new authority partition retain exactly the latest row by `last_seen_at DESC, id DESC` and supersede every loser.
    - Old-valid rows from different authority partitions with the same token_fingerprint/provider/platform/environment/app_identity_ref also retain exactly the latest row by the same deterministic ordering and supersede every loser.
    - `Ecto.Migrator.run(..., :up, all: true)` completes and creates all three replacement unique indexes after both reconciliations.
  </behavior>
  <action>Extend the RED forward-upgrade script with a second collision pair that is valid under the released posture-keyed indexes: use the same token_fingerprint, provider, platform, environment, and app_identity_ref, but different subjects/orgs/installations/sessions and different old app_identity_posture values so neither old uniqueness domain rejects the seed. Give the intended winner a later `last_seen_at` and assert after upgrade that it alone remains active while the loser is `superseded` with `token_rotated`. Refactor `reconcile_active_collisions/0` into two explicit fixed-SQL passes or add `reconcile_active_token_identity_collisions/0`: first reconcile the subject/session-or-installation authority partition already present, then rank remaining active rows by `token_fingerprint, provider, platform, environment, app_identity_ref`, using `ORDER BY last_seen_at DESC, id DESC`, and supersede positions greater than one before `replace_active_indexes/0`. Preserve the restored 20260602/20260603 migrations byte-for-byte, the legacy-non-authoritative invalidation, and the three replacement index definitions from Plan 101-10.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Fixture assertion: the test seeds a different-authority/same-token old-data pair that differs in old posture and is accepted before migration 20260824210000.
    - Ordering assertion: both reconciliation passes use `last_seen_at DESC, id DESC`; each constrained domain has exactly one active winner afterward.
    - Schema assertion: all three active replacement indexes exist, contain app_identity_ref, and exclude app_identity_posture after `Ecto.Migrator.run(..., all: true)`.
    - Command assertion: the focused forward-upgrade ExUnit command exits zero.
  </acceptance_criteria>
  <done>The forward migration handles every newly constrained active-row collision before replacement unique-index creation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| provider evidence -> authenticated host registry | Advisory token evidence requests a durable binding lifecycle mutation. |
| released host rows -> replacement authority indexes | Old-valid data crosses into stricter uniqueness domains during upgrade. |

## STRIDE Threat Register (ASVS L1; high blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-31 | Spoofing/Elevation | `Registry.apply_provider_feedback/2` | high | mitigate | Require authenticated full authority scope plus exact binding_ref in selection and conditional update; prove cross-scope same-token isolation. |
| T-101-32 | Tampering | Provider invalidation CAS | high | mitigate | Repeat every exact scope predicate in the update and return a closed zero-row result on mismatch. |
| T-101-33 | Denial of Service | Forward unique-index creation | high | mitigate | Reconcile both new uniqueness domains before index creation and execute a released-boundary upgrade test. |
| T-101-34 | Information Disclosure | Audit, telemetry, migration proof | medium | mitigate | Preserve bounded opaque facts and raw-token sentinel assertions; emit no authority payload in provider telemetry. |
</threat_model>

## Artifacts this phase produces

- Updated public command semantics: `CrosswakeExample.Chimeway.Registry.apply_provider_feedback/2` requires authenticated exact-binding options for invalidating events while advisory events remain audit-only.
- New private registry helpers/arity: `provider_feedback_scope/2` and scoped `feedback_target_query/2` (exact final helper names may follow existing private naming, but no second public invalidation seam is created).
- Updated migration helper behavior: `reconcile_active_collisions/0` covers both authority and token-identity domains, optionally through a private `reconcile_active_token_identity_collisions/0` helper.
- New regression fixtures inside existing test files: same-token/different-authority provider feedback isolation and old-valid/new-token-identity migration collision.
- Updated file paths: `/Users/jon/projects/crosswake/examples/phoenix_host/README.md` and `/Users/jon/projects/crosswake/test/crosswake/proof/phase60_chimeway_registry_test.exs` reflect the exact scoped invalidation contract.
- No new production file, database column, index name, package, dependency, credential, external API, telemetry event, or CLI surface.

<verification>Run the focused registry, Phase 60 registry proof, and forward-upgrade ExUnit commands; all must exit zero, and no command relies on conversational UAT.</verification>
<success_criteria>Provider invalidation is exact-scope safe and the released-schema forward migration succeeds for collisions in both replacement uniqueness domains.</success_criteria>
<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-12-SUMMARY.md` when done.</output>
