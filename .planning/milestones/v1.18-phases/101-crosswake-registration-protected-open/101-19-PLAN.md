---
phase: 101-crosswake-registration-protected-open
plan: "19"
type: execute
wave: 11
depends_on: ["101-18"]
files_modified:
  - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825190000_backfill_chimeway_notification_open_intent_scope.exs
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs
autonomous: true
gap_closure: true
requirements: [OPEN-04]
must_haves:
  truths:
    - "D-12/OPEN-04: every legacy issued intent terminally revoked by forward reconciliation receives exactly one append-only `reconciliation_revoked` event in the same migration transaction."
    - "D-04/D-12/OPEN-04: reconciliation-denial events contain only the intent foreign key, opaque event ID, stable event type, timestamp, and an empty details map."
    - "D-10/D-12/OPEN-04: the event insertion and terminal update consume one captured set of intent IDs selected by the exact current-authority NOT EXISTS predicate, so matched authoritative intents receive neither revocation nor reconciliation-denial evidence."
    - "OPEN-04: repeated migration-runner entry leaves one reconciliation-denial event per forced-revoked intent and never duplicates lifecycle evidence."
  assumptions:
    - statement: "FLAGGED ASSUMPTION — OPEN-01 is unclassified and unresolved in the spec-less edge probe; this closure plan does not alter registration binding authority."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-02 is unclassified and unresolved in the spec-less edge probe; this closure plan does not alter manifest action policy."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-03 is unclassified and unresolved in the spec-less edge probe; Plan 101-18 already proves matched legacy consumption and this plan preserves that path."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-04 is unclassified and unresolved in the spec-less edge probe; 101-VERIFICATION.md supplies the exact machine-testable reconciliation-evidence acceptance repaired here."
      status: unresolved
      flagged: true
  artifacts:
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825190000_backfill_chimeway_notification_open_intent_scope.exs"
      provides: "Atomic terminal-intent selection, append-only sanitized reconciliation evidence, and terminal state update"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs"
      provides: "Released-boundary upgrade regression for exact event cardinality, matched-row exclusion, and migration re-entry"
  key_links:
    - from: "terminal current-authority NOT EXISTS selection"
      to: "chimeway_notification_open_intent_events and chimeway_notification_open_intents"
      via: "one captured immutable intent-ID set reused by both the event insert and revoked-state update"
      pattern: "NOT EXISTS|open_intent_id|reconciliation_revoked|state"
    - from: "reconciliation event"
      to: "NotificationOpenIntentEvent lifecycle history"
      via: "existing id/open_intent_id/event_type/occurred_at/details table contract"
      pattern: "event_type|occurred_at|details"
  prohibitions:
    - statement: "Migration reconciliation evidence must not retain tenant, subject, session, binding, route, action, token, payload, URL, provider-controlled, or caller-controlled values in event details."
      status: unresolved
---

<objective>
Close OPEN-04 by making every migration-driven legacy intent revocation explainable with exactly one sanitized append-only lifecycle event, per D-04 and D-12.

Purpose: A terminal state change must have durable evidence that explains why the intent can no longer activate a notification route, without widening authority or retaining sensitive facts.
Output: A surgical amendment to the existing forward reconciliation migration and its isolated released-boundary upgrade regression.
</objective>

<assumption_delta_decision>
Primary noun: the exact legacy notification-open intent selected for terminal reconciliation. Decision: no-change — the detector fired only on “no fallback route”; this repair adds evidence to the same intent lifecycle and introduces no second authority identity or alternate activation path.
</assumption_delta_decision>

<api_coverage_decision>
The deterministic detector returns `detected=false`. This plan changes local Ecto migration data and ExUnit proof only; retain the existing reasoned no-external-API declaration in `COVERAGE.md` unchanged.
</api_coverage_decision>

<schema_push_decision>
No schema-push task applies. The plan modifies data reconciliation inside an Ecto migration, while the active contribution detects only Payload, Prisma, Drizzle, Supabase, and TypeORM schema paths; the isolated `Ecto.Migrator` regression applies the migration to a throwaway database.
</schema_push_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
@.planning/phases/101-crosswake-registration-protected-open/101-18-SUMMARY.md

<interfaces>
`chimeway_notification_open_intent_events` already stores binary UUID `id`, binary UUID foreign key `open_intent_id`, string `event_type`, UTC `occurred_at`, and map `details`. Runtime issuance and consumption use event types `issued` and `consumed` with `%{}` details. Plan 101-18's migration currently scope-backfills authoritative issued rows, then changes every remaining issued row failing its exact active-binding `NOT EXISTS` predicate to `revoked`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append exact sanitized evidence for migration-driven intent revocations</name>
  <reversibility rating="one-way">This changes durable historical lifecycle evidence inside a forward migration; the verifier has already selected the required reconciliation event, so no decision checkpoint re-asks that settled repair.</reversibility>
  <read_first>
    - /Users/jon/projects/crosswake/AGENTS.md
    - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825190000_backfill_chimeway_notification_open_intent_scope.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent_event.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-18-SUMMARY.md
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
  </read_first>
  <files>/Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825190000_backfill_chimeway_notification_open_intent_scope.exs, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs</files>
  <behavior>
    - Each issued legacy intent failing the migration's exact active-binding authority predicate changes to revoked and receives one `reconciliation_revoked` event in the same migration transaction.
    - The event uses a fresh opaque binary UUID, the selected intent ID, the stable event type, one migration timestamp, and an empty details map; it stores no authority, route, action, token, payload, URL, or provider-derived value.
    - The matched authoritative legacy intent remains issued after migration, receives no `reconciliation_revoked` event, and still consumes once then reports replay through the public registry seam.
    - Running the migrator again after the upgrade does not add another reconciliation event or change event cardinality.
  </behavior>
  <action>Start RED by extending the isolated subprocess regression to assert exactly one `reconciliation_revoked` event for each existing forced-revocation control (`open-unmatched` and `open-inactive`), zero such events for `open-valid`, empty decoded details for every reconciliation event, and unchanged counts after a second `Ecto.Migrator.run/4` entry over the already-upgraded database. Preserve the existing public valid-then-replay proof. Then amend only `20260825190000_backfill_chimeway_notification_open_intent_scope.exs`: after scope backfill, evaluate the existing terminal `issued AND NOT EXISTS` authority predicate once and capture that exact immutable set of intent IDs. Reuse the captured IDs for both operations in the migration transaction—insert one `chimeway_notification_open_intent_events` row per ID with event type `reconciliation_revoked`, a generated opaque UUID, a shared current UTC timestamp, and `%{}` details; update exactly those same IDs to `revoked` with the same timestamp. Keep event insertion and state update atomic so either both commit or neither does, and make an empty captured set a clean no-op. Do not independently restate or re-run the terminal predicate for the second operation, alter the exact-binding authority conditions, add columns/indexes/dependencies, edit earlier released migrations, modify runtime consume/activation code, or place reconciled row values in details or process output. Implement per D-04, D-10, and D-12.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs test/crosswake_example/chimeway/registry_notification_open_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Selection assertion: one evaluated terminal `issued AND NOT EXISTS` authority predicate produces the captured intent-ID set consumed by both event insertion and state update.
    - Cardinality assertion: `open-unmatched` and `open-inactive` are revoked and each has exactly one `reconciliation_revoked` event; the matched `open-valid` intent has zero.
    - Sanitization assertion: every reconciliation event has only the established lifecycle columns and its decoded details equal an empty map.
    - Atomicity assertion: event rows and revoked state are committed together inside the migration transaction with the same reconciliation timestamp.
    - Idempotency assertion: a repeated migrator entry leaves reconciliation event counts unchanged and does not rewrite matched or already-terminal intents.
    - Regression assertion: the focused upgrade and runtime notification-open suites exit zero, including the existing matched valid-then-replay public consume path.
  </acceptance_criteria>
  <done>Every migration-forced intent revocation is durably and safely explainable exactly once, while matched authoritative intents retain their existing one-time consume behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| legacy intent/binding rows -> reconciliation selection | Partial historical authority is untrusted until it fails or satisfies the exact active-binding predicate. |
| reconciliation selection -> lifecycle event log | Selected durable identities cross into append-only operator evidence and must not carry sensitive row data. |

## STRIDE Threat Register (ASVS L1; high blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-50 | Tampering | terminal selection and event/update fan-out | high | mitigate | Evaluate the terminal predicate once, capture exact intent IDs, and reuse that immutable set for both writes in one migration transaction. |
| T-101-51 | Repudiation | migration-driven revoked intent | high | mitigate | Append exactly one stable `reconciliation_revoked` event for each forced transition and prove per-intent cardinality. |
| T-101-52 | Information Disclosure | reconciliation event details and test output | medium | mitigate | Persist `%{}` details only, use opaque IDs/static type, and keep fixtures out of process output per D-04/D-12. |
| T-101-53 | Denial of Service | empty or repeated migration entry | low | accept | Empty candidate sets and already-applied migrator entry are bounded no-ops; focused regression proves unchanged counts. |
</threat_model>

## Artifacts this phase produces

- Modified migration behavior in `/Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825190000_backfill_chimeway_notification_open_intent_scope.exs` that emits the new stable durable event type `reconciliation_revoked` for exactly the terminally selected legacy intent IDs.
- New regression assertions inside `/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs` for per-intent event cardinality, empty details, matched-row exclusion, and repeated migrator entry.
- No new file, public Elixir module/function, Ecto field/table/index, external API, package, dependency, credential, service, route, or CLI surface is created.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Host binds APNs registrations and activates routes only under current host authority | 101-01..101-19 | COVERED | 101-19 supplies the remaining durable explanation for a migration denial; authority and activation remain unchanged. |
| REQ | OPEN-01 | Explicit permission, authenticated binding, rotation/revocation/invalidation | 101-04, 101-06, 101-09..101-12, 101-14..101-15, 101-17 | COVERED | Already verified; 101-19 does not touch registration authority. |
| REQ | OPEN-02 | Manifest-consistent default-deny action policy | 101-02, 101-03, 101-09 | COVERED | Already verified; 101-19 does not touch policy. |
| REQ | OPEN-03 | Opaque queued tap and exact current one-time reauthorization | 101-01, 101-03, 101-05, 101-07, 101-09, 101-13..101-14, 101-16, 101-18 | COVERED | Already verified; 101-19 preserves Plan 101-18's matched consume/replay regression. |
| REQ | OPEN-04 | Stale/denied opens activate no fallback and produce sanitized explainable denial evidence | 101-01, 101-05, 101-08, 101-09, 101-19 | COVERED | 101-19 closes the sole current verifier gap: migration-forced revocation evidence. |
| RESEARCH | — | Race-safe conditional writes against exact host-owned binding/intent authority | 101-04, 101-05, 101-10, 101-12, 101-14..101-15, 101-18..101-19 | COVERED | 101-19 reuses one exact reconciliation selection for both atomic writes. |
| RESEARCH | — | Closed recursive privacy boundary and stable sanitized denial vocabulary | 101-08, 101-14, 101-16..101-17, 101-19 | COVERED | The new stable type carries empty details only. |
| RESEARCH | — | Normalized manifest, RouteGate halt, bounded opaque queue, and no-fallback outcomes | 101-01..101-03, 101-07..101-09, 101-13 | COVERED | Existing verified work remains unchanged. |
| CONTEXT | D-01..D-03 | Explicit registration states and exact current host binding authority | 101-04, 101-06, 101-10..101-12, 101-14..101-15 | COVERED | Existing executed lifecycle work remains authoritative. |
| CONTEXT | D-04 | Durable state/evidence retains only opaque refs and closed safe facts | 101-08, 101-14, 101-16..101-17, 101-19 | COVERED | 101-19 writes empty reconciliation details. |
| CONTEXT | D-05..D-09 | Closed manifest policy, host-bound route/action, RouteGate halt, bounded opaque queue | 101-01..101-03, 101-07..101-09, 101-13 | COVERED | Previously verified; no closure-plan widening. |
| CONTEXT | D-10 | Atomic one-time consume rechecks current exact authority | 101-01, 101-05, 101-13..101-14, 101-18..101-19 | COVERED | The matched path remains unchanged; terminal writes use one captured selection transactionally. |
| CONTEXT | D-11 | Current-policy authorization activates nothing on denial | 101-01, 101-03, 101-07..101-09, 101-13 | COVERED | Existing verified activation behavior remains unchanged. |
| CONTEXT | D-12 | Stable sanitized outcomes explain denied protected opens | 101-08..101-09, 101-14, 101-16..101-17, 101-19 | COVERED | 101-19 directly closes missing migration-denial evidence. |
| CONTEXT | D-13 | Narrow iOS seams; host retains persistence/auth/session/intents | 101-04..101-08, 101-10..101-19 | COVERED | Repair stays inside the existing host migration boundary. |

Excluded without gap: CONTEXT deferred ideas; Phase 102 digital twin, fake APNs transport, named verification commands, and CI; Phase 103 physical-iPhone proof and adoption guidance; Android/FCM; generic offline/background sync; arbitrary notification actions; dashboards, analytics, and engagement tracking. No source item is missing from the 101-01..101-19 plan set.

## Prohibition Recall Audit

| Requirement | Precision result | Plan location |
|---|---|---|
| OPEN-01 | No new bespoke prohibition for this surgical evidence repair; existing registration privacy prohibition remains in 101-17. | Existing 101-17 `must_haves.prohibitions` |
| OPEN-02 | No new bespoke prohibition; policy must-NOTs are locked by D-05/D-06 and covered by executed plans. | Existing 101-02/101-03 contracts |
| OPEN-03 | No new bespoke prohibition; invented-scope prohibition remains in 101-18. | Existing 101-18 `must_haves.prohibitions` |
| OPEN-04 | Keep one privacy/transparency prohibition: reconciliation evidence cannot retain sensitive or caller/provider-controlled values. | 101-19 `must_haves.prohibitions` |

The one newly recalled kept item equals the one descriptor-less prohibition authored above. Routine atomicity/idempotency candidates are covered by positive acceptance criteria; generic injection/compliance candidates route to the ASVS threat model. Nothing is silently dropped.

<verification>
Run the focused migration-upgrade and runtime notification-open suites from the CrossWake Phoenix host. The upgrade proof must establish one event per forced revocation, zero for the matched row, empty details, and unchanged counts after re-entry while retaining valid-then-replay consumption.
</verification>

<success_criteria>
The sole current `101-VERIFICATION.md` gap is closed: every migration-revoked legacy intent has exactly one sanitized append-only explanation, matched intents receive none, and repeated migration entry cannot duplicate evidence.
</success_criteria>

<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-19-SUMMARY.md` when done.</output>
