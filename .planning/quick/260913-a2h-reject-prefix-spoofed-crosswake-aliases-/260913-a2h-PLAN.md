---
quick_id: 260913-a2h
phase: 260913-a2h-crosswake-exact-aliases
plan: 01
type: tdd
wave: 1
autonomous: true
depends_on: []
files_modified:
  - lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex
  - test/chimeway/crosswake_provider_feedback_docs_contract_test.exs
estimate:
  tokens: 7000
  raw_tokens: 7000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The public CrossWake verifier rejects rooted proof calls made through Evil.Code, Evil.Redaction, or Evil.Registry before focused execution begins."
    - "The alternative worker proof accepts only the complete MyApp.Workers.ChimewayProviderFeedbackWorker alias and rejects a prefix-spoofed worker alias."
    - "README recipe boundaries and rooted focused-test boundaries use complete trusted alias paths rather than suffix matches."
    - "All existing canonical README, ExUnit-root, reachable-helper, cycle, selector, checkout, authority, cleanup, and focused-execution fixtures remain green."
  artifacts:
    - path: "lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex"
      provides: "Exact full-alias classification for recipe and rooted executable proof calls"
      contains: "record_remote_marker"
    - path: "test/chimeway/crosswake_provider_feedback_docs_contract_test.exs"
      provides: "Adversarial qualifier-substitution coverage through public verify/1"
      contains: "exact_alias_identity"
  key_links:
    - from: "test/chimeway/crosswake_provider_feedback_docs_contract_test.exs"
      to: "lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex"
      via: "Tagged destructive fixtures invoke the public verify/1 boundary and require :error without focused_test execution"
      pattern: "CrosswakeProviderFeedbackDocs\\.verify"
    - from: "scan_reachable_expression/3"
      to: "record_remote_marker/3"
      via: "The complete aliases list is preserved through marker classification"
      pattern: "record_remote_marker\\(markers, aliases, function\\)"
    - from: "remote_call?/3"
      to: "validate_files/1"
      via: "README recipe validation compares the complete parsed alias path for both public boundaries"
      pattern: "remote_call\\?"
---

<objective>
Close the remaining CrossWake proof spoofing gap by binding every required remote marker to its complete trusted alias path.

Purpose: The release proof must reject executable-looking calls made through attacker-chosen namespaces even when their final alias segment matches a trusted module.
Output: Red-first adversarial public-verifier coverage and exact-alias matching in both detached README and rooted focused-test scans.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260912-x9g-harden-release-automation-and-release-proof-semantics-ordina/260912-x9g-PLAN.md
@.planning/quick/260912-x9g-harden-release-automation-and-release-proof-semantics-ordina/260912-x9g-SUMMARY.md
@.planning/quick/260912-x9g-harden-release-automation-and-release-proof-semantics-ordina/260912-x9g-VERIFICATION.md
@lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex
@test/chimeway/crosswake_provider_feedback_docs_contract_test.exs
</context>

<source_coverage>

| Source | Item | Coverage |
|---|---|---|
| GOAL | Reject prefix-spoofed CrossWake aliases through exact module identity | Tasks 1-2 |
| REQ | No formal requirement IDs apply to this bounded quick repair | Excluded: quick task outside milestone requirement mapping |
| RESEARCH | x9g verifier gap requires full alias matching and qualifier-substitution fixtures | Tasks 1-2 |
| CONTEXT | Red first, exact full alias equality, positive fixtures stay green, public verify/1 rejects forged cases, two-file boundary | Tasks 1-2 |

</source_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red public-verifier fixtures for prefix-spoofed aliases</name>
  <files>test/chimeway/crosswake_provider_feedback_docs_contract_test.exs</files>
  <behavior>
    - A rooted focused test using `Evil.Code.compile_string/1` in place of `Code.compile_string/1` returns `:error` and never invokes `focused_test`.
    - A rooted focused test using `Evil.Redaction.feedback_from_provider_attrs/1` in place of `Redaction.feedback_from_provider_attrs/1` returns `:error` and never invokes `focused_test`.
    - A rooted focused test using `Evil.Registry.apply_provider_feedback/2` in place of `Registry.apply_provider_feedback/2` returns `:error` and never invokes `focused_test`.
    - A worker-path fixture using `Evil.MyApp.Workers.ChimewayProviderFeedbackWorker` in place of the exact pinned worker alias returns `:error` and never invokes `focused_test`.
    - README recipe substitutions to `Evil.Redaction` and `Evil.Registry` independently return `:error` and never invoke `focused_test`.
  </behavior>
  <action>
Add separate generated ExUnit cases for each qualifier substitution so one failing assertion cannot mask the remaining boundaries. Give each case both the shared `:exact_alias_identity` tag and a unique `exact_alias_case` value: `focused_code`, `focused_redaction`, `focused_registry`, `focused_worker`, `readme_redaction`, or `readme_registry`. Drive every case through `CrosswakeProviderFeedbackDocs.verify/1` and mutate only one canonical alias at a time. After invoking the verifier, consume the mailbox signal and compare the single observed tuple `{verdict, focused_test_executed?}` with the required `{:error, false}`. Only when the observed tuple is exactly the known vulnerable result `{:ok, true}`, print the case-specific sentinel `RED_ALIAS_SPOOF_ACCEPTED:<case>` immediately before the assertion. This makes RED evidence distinguish the intended suffix-spoof acceptance from setup, compilation, selection, or other command failures. Cover all three focused-test markers, the alternative worker alias, and both README recipe boundaries. Preserve the existing valid fixture and positive worker fixture unchanged so Task 2 must retain their accepted behavior. Run each unique case independently before implementation; require its command to exit nonzero, emit its matching case sentinel, and report exactly one failing test. Do not test private helpers or duplicate the production matching logic in the test oracle.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; for boundary in focused_code focused_redaction focused_registry focused_worker readme_redaction readme_registry; do set +e; output=$(CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/crosswake_provider_feedback_docs_contract_test.exs --only "exact_alias_case:${boundary}" --warnings-as-errors 2&gt;&amp;1); status=$?; set -e; printf "%s\n" "$output"; [ "$status" -ne 0 ]; printf "%s\n" "$output" | grep -Fq "RED_ALIAS_SPOOF_ACCEPTED:${boundary}"; printf "%s\n" "$output" | grep -Fq "1 failure"; done'</automated>
  </verify>
  <done>Each uniquely tagged adversarial case independently fails against the suffix-only implementation with its matching vulnerability sentinel, the observed pre-fix tuple is exactly `{:ok, true}`, and unrelated command failures cannot satisfy the RED gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve complete aliases through both CrossWake scanners</name>
  <files>lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex, test/chimeway/crosswake_provider_feedback_docs_contract_test.exs</files>
  <behavior>
    - Marker classification recognizes only the exact alias lists `[:Code]`, `[:Redaction]`, and `[:Registry]` with their required function names.
    - The worker alternative recognizes only `[:MyApp, :Workers, :ChimewayProviderFeedbackWorker]` with literal `:perform` through `apply/3`.
    - README recipe validation compares the complete parsed alias list against `[:Redaction]` and `[:Registry]`.
    - Prefixing any trusted alias with another namespace does not contribute a proof marker, while every canonical positive fixture still passes.
  </behavior>
  <action>
Replace each suffix-only `List.last(aliases)` decision with complete alias-list matching. Pass the untouched `aliases` list from `scan_reachable_expression/3` into `record_remote_marker/3`; define marker clauses only for the exact canonical lists `[:Code]`, `[:Redaction]`, and `[:Registry]` paired with their existing function names, with the existing catch-all leaving markers unchanged. Require exact equality with `[:MyApp, :Workers, :ChimewayProviderFeedbackWorker]` for the worker `apply/3` alternative. Change `remote_call?/3` and its README validation call sites to compare the full parsed aliases against the exact expected one-segment lists rather than accepting their final segment. Keep root discovery, reachability, pruning, cycle handling, command execution, authority selection, cleanup, public entrypoints, and error behavior unchanged. Do not broaden accepted spelling, resolve arbitrary aliases, execute detached AST, or add files.
  </action>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/crosswake_provider_feedback_docs_contract_test.exs --warnings-as-errors &amp;&amp; mix format --check-formatted</automated>
  </verify>
  <done>All adversarial qualifier substitutions fail closed before focused execution, the exact canonical and worker-positive fixtures pass, the complete pre-existing CrossWake contract stays green, and formatting reports no drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Detached CrossWake source to local release proof | Untrusted parsed aliases from a separately checked-out repository determine whether release evidence is accepted and executed. |

## STRIDE Threat Register — ASVS Level 1

The configured blocking threshold is high. The high-severity spoofing/tampering threat below must be mitigated before this quick repair can pass verification.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-a2h-01 | Spoofing / Tampering | README and rooted focused-test remote markers | high | mitigate | Compare complete canonical alias lists, then mutation-test every trusted boundary through public `verify/1` with prefix-spoofed replacements and require failure before focused execution. |
| T-a2h-02 | Denial of Service | Existing AST reachability traversal | low | accept | This repair does not change traversal shape; the existing visited-definition set, pruned executable contexts, and cyclic-graph contracts continue to bound the proof walk. |
| T-a2h-03 | Information Disclosure | Verifier failure output | low | accept | The public verifier continues returning only success/error status and does not emit detached source or payload content; no output path changes in this repair. |

</threat_model>

<verification>
Run the full CrossWake contract with warnings treated as errors, confirm all tagged exact-alias mutations reject through public `verify/1` without invoking `focused_test`, confirm the existing canonical and worker-positive cases still pass, and run the formatter check. No human UAT applies because every acceptance property is objectively executable.
</verification>

<success_criteria>
- `Evil.Code`, `Evil.Redaction`, `Evil.Registry`, and a prefix-spoofed worker alias cannot satisfy any required proof marker.
- Complete alias equality is enforced in both rooted focused-test scanning and README recipe scanning.
- The full existing CrossWake contract and formatter check pass without changing files outside the authorized source/test pair.
</success_criteria>

<output>
Create `.planning/quick/260913-a2h-reject-prefix-spoofed-crosswake-aliases-/260913-a2h-SUMMARY.md` when done.
</output>
