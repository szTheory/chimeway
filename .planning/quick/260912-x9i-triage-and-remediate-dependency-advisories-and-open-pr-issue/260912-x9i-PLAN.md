---
quick_id: 260912-x9i
phase: 260912-x9i-dependency-and-inbox-triage
plan: 01
type: execute
wave: 2
autonomous: true
depends_on:
  - 260912-x9h
files_modified:
  - mix.lock
  - chimeway_admin/mix.lock
  - chimeway_inbox/mix.lock
  - examples/chimeway_demo_host/mix.exs
  - examples/chimeway_demo_host/mix.lock
  - .github/workflows/ci.yml
  - test/chimeway/release_gate_contract_test.exs
  - SECURITY.md
must_haves:
  truths:
    - "The root lock incorporates the current one-file Dependabot #28 resolution, clearing Decimal and Mint findings without broadening root manifest constraints or trusting checks from its old base."
    - "The independently runnable admin and inbox packages resolve advisory-safe Phoenix, LiveView, Plug, Postgrex, and related current-line versions inside their existing manifests, with clean Hex audits and warning-strict tests."
    - "The demo host no longer pins Decimal 2/Ecto SQL 3.13 for obsolete Accrue 1.5.0 compatibility; its manifest, lock, and mutation-locked immutable Accrue CI checkout agree on the 1.5.1 dependency contract without changing unrelated compatibility pins."
    - "The only accepted dependency residual is graph-specific and documented: Hackney 1.25.0 remains constrained by Accrue/Braintree and Threadline until those upstreams permit Hackney 4.0.1+, while any Cowlib 2.20 feed mismatch is recorded separately with authoritative range evidence."
    - "No Hackney 4 override, package major campaign, JavaScript dependency churn, Elixir/OTP floor change, hand-edited lock entry, or hidden checked-in advisory ignore is introduced."
    - "GitHub triage is conditional on two successfully captured final dependency graphs: root/demo graph-query errors are distinct fatal failures, no tracker is created when both successful graphs omit Hackney, and otherwise exactly one open semantically equivalent tracker records every required residual field."
    - "Contributor PRs #22/#28 are not closed until a replacement URL resolves publicly to the exact final implementation SHA, that SHA has a completed successful pr-gate, and each closure comment binds the replacement URL to that SHA."
  artifacts:
    - path: "mix.lock"
      provides: "PR #28-equivalent root solver resolution with Decimal 3 and Mint 1.10+"
    - path: "chimeway_admin/mix.lock"
      provides: "Advisory-safe admin dependency graph inside existing constraints"
    - path: "chimeway_inbox/mix.lock"
      provides: "Advisory-safe inbox dependency graph inside existing constraints"
    - path: "examples/chimeway_demo_host/mix.exs"
      provides: "Accrue 1.5.1-compatible Decimal 3 and Ecto SQL 3.14 override contract"
      contains: "{:decimal, \"~> 3.0\", override: true}"
    - path: "examples/chimeway_demo_host/mix.lock"
      provides: "Solver-generated mounted demo dependency graph at patched current-line floors"
    - path: ".github/workflows/ci.yml"
      provides: "Immutable Accrue 1.5.1 checkout for the integration lane"
      contains: "d30fc25dbf6ba551792c66ff451b4b93c0af4bf1"
    - path: "test/chimeway/release_gate_contract_test.exs"
      provides: "Region-scoped mutation contract for the intended Accrue integration checkout SHA"
    - path: "SECURITY.md"
      provides: "Honest current dependency advisory disposition and removal conditions"
  key_links:
    - from: "mix.lock"
      to: "SECURITY.md"
      via: "Resolved Decimal/Mint rows are removed while the exact remaining Hackney version, IDs, and dependency paths stay documented"
      pattern: "CVE-2026-47069|CVE-2026-47071|CVE-2026-47075|CVE-2026-47076"
    - from: "examples/chimeway_demo_host/mix.exs"
      to: ".github/workflows/ci.yml"
      via: "Demo constraints match the immutable Accrue 1.5.1 proof checkout while the release contract mutation-locks only that intended checkout step"
      pattern: "accrue|d30fc25dbf6ba551792c66ff451b4b93c0af4bf1"
    - from: "test/chimeway/release_gate_contract_test.exs"
      to: ".github/workflows/ci.yml"
      via: "Extract the verify_accrue job and szTheory/accrue checkout step; replacing the exact 1.5.1 SHA fails without snapshotting unrelated integration refs"
      pattern: "verify_accrue|szTheory/accrue|d30fc25dbf6ba551792c66ff451b4b93c0af4bf1"
    - from: "examples/chimeway_demo_host/mix.exs"
      to: "examples/chimeway_demo_host/mix.lock"
      via: "Mix/Hex solver regenerates the lock after the stale compatibility overrides are corrected"
      pattern: "decimal|ecto_sql"
    - from: "final pushed implementation SHA"
      to: "GitHub PR #22/#28 disposition"
      via: "Replacement URL resolves publicly to the same SHA, Checks API reports completed/successful pr-gate for that SHA, and closure comments record both values"
      pattern: "pr-gate"
---

<objective>
Bring every runnable dependency graph and the GitHub contribution inbox to the lowest-risk release-ready state, while naming rather than concealing the one upstream-constrained residual.

Purpose: Clear all safely remediable advisories, retain compatibility floors and optional integrations, bind acceptance to final implementation evidence, and leave intelligible contributor provenance.

Output: Four solver-generated Mix locks, corrected demo/CI compatibility pins, current residual-risk documentation, one bounded upstream tracking issue when warranted, and evidence-gated PR dispositions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260912-x9i-triage-and-remediate-dependency-advisories-and-open-pr-issue/260912-x9i-RESEARCH.md
@mix.exs
@mix.lock
@chimeway_admin/mix.exs
@chimeway_admin/mix.lock
@chimeway_inbox/mix.exs
@chimeway_inbox/mix.lock
@examples/chimeway_demo_host/mix.exs
@examples/chimeway_demo_host/mix.lock
@.github/workflows/ci.yml
@test/chimeway/release_gate_contract_test.exs
@SECURITY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Refresh root, admin, and inbox locks through their supported dependency graphs</name>
  <files>mix.lock, chimeway_admin/mix.lock, chimeway_inbox/mix.lock</files>
  <action>
Re-query PR #28 before using it: require that it is still open, non-draft, changes only `mix.lock`, and has the researched head `d03c7b88e51738083b3ab12575b10730f94d1592` over old base `f516b607167023edefeb767182adddbd9709aec5`. If those facts still hold, apply its exact one-file lock delta to this branch and let `mix deps.get` validate the graph; do not merge its stale-base branch or treat its historical green `pr-gate` as evidence for this implementation. If the live head or file scope changed, do not authorize the changed PR contents: instead reproduce the lowest-risk root resolution with targeted `mix deps.update` for the already-declared packages and require the resulting lock to reach at least Decimal 3.0 and Mint 1.10 without changing `mix.exs`. Lock changes must come from the Mix/Hex solver, never handwritten entries.

In `chimeway_admin` run the targeted current-line update for `phoenix phoenix_live_view plug postgrex tzdata hackney oban floki lazy_html`; in `chimeway_inbox` run the same update plus `phoenix_pubsub`. Accept only solver-required transitives inside the existing manifests. Require Phoenix 1.8.9+, LiveView 1.1.33+ (the solver may select 1.2.x), Plug 1.19.5+, Postgrex 0.22.4+, and Hackney 4.0.1+ in both subordinate graphs. Do not change package manifests, add advisory ignores, raise `elixir: "~> 1.17"`, or expand into unrelated major upgrades. Run `mix deps.get`, `mix hex.audit`, and warning-strict tests independently in both directories so one green graph cannot mask another stale lock.
  </action>
  <verify>
    <automated>mix deps.get &amp;&amp; HEX_IGNORE_ADVISORIES=CVE-2026-47069,CVE-2026-47071,CVE-2026-47075,CVE-2026-47076 mix hex.audit &amp;&amp; mix deps.audit --ignore-advisory-ids=GHSA-gp9c-pm5m-5cxr,GHSA-j9wq-vxxc-94wf,GHSA-mp55-p8c9-rfw2,GHSA-pj7v-xfvx-wmjq &amp;&amp; mix ci &amp;&amp; (cd chimeway_admin &amp;&amp; mix deps.get &amp;&amp; mix hex.audit &amp;&amp; mix test --warnings-as-errors) &amp;&amp; (cd chimeway_inbox &amp;&amp; mix deps.get &amp;&amp; mix hex.audit &amp;&amp; mix test --warnings-as-errors)</automated>
  </verify>
  <done>Root Decimal/Mint findings are gone with only the four explicitly surfaced Hackney advisories tolerated for this command invocation; admin and inbox Hex audits are genuinely clean; all three graphs resolve and test; root manifests and supported Elixir/OTP floors are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correct the demo compatibility contract and document only verified residual risk</name>
  <files>examples/chimeway_demo_host/mix.exs, examples/chimeway_demo_host/mix.lock, .github/workflows/ci.yml, test/chimeway/release_gate_contract_test.exs, SECURITY.md</files>
  <behavior>
    - The `verify_accrue` job's checkout step whose repository is exactly `szTheory/accrue` requires ref `d30fc25dbf6ba551792c66ff451b4b93c0af4bf1`.
    - Replacing or removing that exact ref makes the focused release contract fail.
    - The contract scopes its assertion/mutation to the `verify_accrue` Accrue checkout step and does not freeze, rewrite, or authorize changes to unrelated integration checkout refs or compatibility pins.
    - Demo Decimal/Ecto constraints and the pinned Accrue proof agree on the published 1.5.1 dependency contract.
  </behavior>
  <action>
Start with a focused release-gate contract. Extract the `verify_accrue` job, then the checkout step whose `repository` is exactly `szTheory/accrue`; require its ref to be exactly immutable official-tag commit `d30fc25dbf6ba551792c66ff451b4b93c0af4bf1`. Destructively replace and remove only that ref and prove both mutations fail. Do not assert a global SHA occurrence count or snapshot other checkout refs: existing unrelated compatibility pins remain governed by their own contracts and must not be changed by this item.

Update the demo's Decimal override from `~> 2.0` to `~> 3.0` and Ecto SQL override from `~> 3.13.0` to `~> 3.14.0`; rewrite only the adjacent compatibility comments so they accurately state Accrue 1.5.1's Decimal 3 / Ecto 3.13+ contract. In `.github/workflows/ci.yml`, replace only the Accrue integration checkout ref `cafc526f752b917a0abf8cbdbf3030cb367ae346` with `d30fc25dbf6ba551792c66ff451b4b93c0af4bf1`. Preserve every unrelated checkout ref/compatibility pin plus the existing checkout action pin, permissions, path, environment, Elixir/OTP matrix, and gate topology.

Regenerate the demo lock with `mix deps.update --all`, `mix deps.clean --unused`, and `mix deps.get`. Require the safe floors from research (including Cowboy 2.18+, Cowlib 2.19+, Phoenix 1.8.9+, LiveView 1.1.33+, Plug 1.19.5+, Postgrex 0.22.4+, and Swoosh 1.26.3+) while allowing the solver to remove now-unreachable Mint/HPAX/Req/Finch entries. Do not pin arbitrary Git SHAs, force Hackney 4 against published partner constraints, change Mailglass/Sigra majors, or edit lock tuples manually.

Update only the dependency-advisory sections of `SECURITY.md`. Remove Decimal from accepted risk, record Hackney 1.25.0's exact four advisory IDs and fixed floor 4.0.1, name the verified paths `accrue → braintree → hackney`, `threadline → hackney`, and `tzdata → hackney`, explain that Braintree 0.16 and Threadline 0.9 constraints block a compatible resolution, and state the removal trigger (upstream releases permitting Hackney 4 plus green integration gates). Separately, if the final demo audit still maps Cowlib 2.20.0 to CVE-2026-43971/43969/43966, record the exact raw findings, official GHSA affected ranges, and verified fix ancestry as a scanner-feed exception; if the feed no longer reports them, omit that exception. Never call the overall audit clean, add a checked-in ignore/waiver, or edit reporting/contact/repository-link prose owned by sibling item `260912-x9h`.

Run the raw audits first and retain their exact output in the task summary. Then use one-shot CLI/environment ignore lists containing only the four documented Hackney IDs and, only for the demo feed check, the three specifically cross-checked Cowlib IDs. This makes any newly disclosed or wrongly upgraded package fail the gate instead of growing an implicit allowlist.
  </action>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --exclude adoption_paths_e2e --exclude accrue_packaged_cli --warnings-as-errors &amp;&amp; (cd examples/chimeway_demo_host &amp;&amp; mix deps.get &amp;&amp; HEX_IGNORE_ADVISORIES=CVE-2026-47069,CVE-2026-47071,CVE-2026-47075,CVE-2026-47076,CVE-2026-43971,CVE-2026-43969,CVE-2026-43966 mix hex.audit &amp;&amp; mix test --warnings-as-errors) &amp;&amp; actionlint .github/workflows/ci.yml &amp;&amp; mix verify.accrue &amp;&amp; mix verify.example &amp;&amp; mix verify.inbox &amp;&amp; mix ci.verify_gates &amp;&amp; npm ci &amp;&amp; npm audit &amp;&amp; npm outdated</automated>
  </verify>
  <done>The demo and Accrue CI lane agree on Accrue 1.5.1, only the intended Accrue checkout SHA is mutation-locked and changed, unrelated compatibility pins are preserved, all safely patchable demo findings are cleared, new advisory findings remain blocking, the current Hackney residual and any immediately re-reproduced Cowlib feed exception are documented with exact evidence, mounted/integration gates pass, and JavaScript remains unchanged and clean.</done>
</task>

<task type="auto">
  <name>Task 3: Triage the live GitHub inbox without erasing contributor provenance</name>
  <files>External GitHub state only; no repository files</files>
  <action>
Re-query GitHub immediately before every action; the 2026-09-13 research state is context, not mutation authority. Determine whether Hackney remains with two separate fail-fast graph acquisitions: capture root `mix deps.tree` output first and abort with a root-specific diagnostic/exit code if it fails; then capture demo `mix deps.tree` output from `examples/chimeway_demo_host` and abort with a different demo-specific diagnostic/exit code if it fails. Only after both commands succeed may grep inspect their captured strings for an actual `hackney` node and choose present/absent tracker policy. Never pipe a fallible graph command directly into the presence grep or let command failure share the absence branch.

Use the stable issue-body marker `<!-- chimeway-hackney-residual:x9i -->` to make creation idempotent. If neither successfully captured final graph contains Hackney, create no tracker and require zero open issues carrying that marker. If either graph contains Hackney, query all open issues (excluding pull requests) and identify semantically equivalent trackers by body content, not title alone. Reuse exactly one equivalent open tracker when present; create exactly one when absent; fail without creating another when multiple equivalents exist. The one open tracker must contain all four IDs `CVE-2026-47069`, `CVE-2026-47071`, `CVE-2026-47075`, and `CVE-2026-47076`; fixed floor `4.0.1`; affected paths naming `accrue`, `braintree`, `threadline`, `tzdata`, and `hackney`; the exposure note that Chimeway adds no attacker-controlled URL or SOCKS5 configuration entrypoint; and the closure condition that Accrue/Braintree and Threadline must permit Hackney 4.0.1+ and integration gates must pass. Do not file separate advisory spam or a Cowlib issue for the cross-checked feed mismatch.

For PR #28, verify its live head/base/files and record that its exact lock delta was incorporated and re-tested on the stabilization result. For draft PR #22, verify it is still the conflicting predecessor rather than assuming the researched state. After Tasks 1-2 are committed, define the exact final implementation SHA as `git rev-parse HEAD`; do not advance local HEAD between that resolution and disposition. Before closing either PR, require a public replacement PR/commit URL on `szTheory/chimeway`. Resolve the SHA from that public object: for a PR URL read its current `headRefOid`; for a commit URL parse the full 40-character SHA and resolve it with `gh api repos/szTheory/chimeway/commits/$sha`. Require that public SHA to equal the locally resolved final implementation SHA, require the commit API to return that same SHA (public reachability), and query `repos/szTheory/chimeway/commits/$sha/check-runs` with pagination until a check named exactly `pr-gate` has `status == "completed"` and `conclusion == "success"`. These are blocking preconditions to closure, not post-hoc evidence.

Only after all of those checks pass, add one comment carrying marker `<!-- chimeway-stabilization-disposition:x9i -->` and exact fields `Replacement: <public URL>`, `Implementation-SHA: <40-character SHA>`, and the successful `pr-gate` details URL, then close #28 as incorporated and #22 as superseded. The same URL must resolve back to the same SHA recorded in each closure comment. If public resolution, SHA equality, or exact-SHA check evidence is missing, leave the PR open and record the pending disposition and blocker in the task summary for the ship step. An old-main run, #28's old-base check, a branch name, or an unpublished local SHA is never sufficient. Do not rebase #22 or discard its history.
  </action>
  <verify>
    <automated>bash -euo pipefail -c 'repo=szTheory/chimeway; vcs=git; expected_sha=$($vcs rev-parse HEAD); vcs_status=$?; [ "$vcs_status" -eq 0 ] || exit "$vcs_status"; if ! root_tree=$(mix deps.tree); then echo "root dependency graph query failed" &gt;&amp;2; exit 70; fi; if ! demo_tree=$(cd examples/chimeway_demo_host &amp;&amp; mix deps.tree); then echo "demo dependency graph query failed" &gt;&amp;2; exit 71; fi; marker="&lt;!-- chimeway-hackney-residual:x9i --&gt;"; issues=$(gh api --paginate "repos/$repo/issues?state=open&amp;per_page=100" | jq -s --arg marker "$marker" "add | [.[] | select(has(\"pull_request\") | not) | select((.body // \"\") | contains(\$marker))]"); if grep -Eq "(^|[[:space:]])hackney([[:space:]]|$)" &lt;&lt;&lt;"$root_tree" || grep -Eq "(^|[[:space:]])hackney([[:space:]]|$)" &lt;&lt;&lt;"$demo_tree"; then jq -e "length == 1 and (.[0].body | contains(\"CVE-2026-47069\") and contains(\"CVE-2026-47071\") and contains(\"CVE-2026-47075\") and contains(\"CVE-2026-47076\") and contains(\"4.0.1\") and contains(\"accrue\") and contains(\"braintree\") and contains(\"threadline\") and contains(\"tzdata\") and contains(\"hackney\") and contains(\"attacker-controlled URL\") and contains(\"SOCKS5\") and contains(\"integration gates\"))" &lt;&lt;&lt;"$issues"; else jq -e "length == 0" &lt;&lt;&lt;"$issues"; fi; for pr in 22 28; do payload=$(gh pr view "$pr" --repo "$repo" --json state,comments); state=$(jq -r .state &lt;&lt;&lt;"$payload"); if [ "$state" = CLOSED ]; then body=$(jq -er "[.comments[].body | select(contains(\"chimeway-stabilization-disposition:x9i\"))] | last" &lt;&lt;&lt;"$payload"); url=$(sed -nE "s#^Replacement: (https://github.com/szTheory/chimeway/(pull/[0-9]+|commit/[0-9a-f]{40}))$#\\1#p" &lt;&lt;&lt;"$body"); sha=$(sed -nE "s/^Implementation-SHA: ([0-9a-f]{40})$/\\1/p" &lt;&lt;&lt;"$body"); test -n "$url" &amp;&amp; test -n "$sha" &amp;&amp; test "$sha" = "$expected_sha"; case "$url" in */pull/*) resolved=$(gh pr view "$url" --json headRefOid --jq .headRefOid);; */commit/*) resolved=${url##*/};; esac; test "$resolved" = "$sha"; test "$(gh api "repos/$repo/commits/$sha" --jq .sha)" = "$sha"; gh api --paginate -H "Accept: application/vnd.github+json" "repos/$repo/commits/$sha/check-runs?per_page=100" | jq -s -e "[.[].check_runs[] | select(.name == \"pr-gate\" and .status == \"completed\" and .conclusion == \"success\")] | length &gt; 0"; grep -Fq "Implementation-SHA: $sha" &lt;&lt;&lt;"$body"; grep -Fq "Replacement: $url" &lt;&lt;&lt;"$body"; else test "$state" = OPEN; fi; done'</automated>
  </verify>
  <done>No tracker is created when Hackney is absent; when present, exactly one open semantically complete tracker describes the residual. PR #22/#28 remain open unless each closure comment's replacement URL publicly resolves to its recorded exact final implementation SHA and that SHA has a completed successful `pr-gate`; no stale check or hidden local state authorizes closure or release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex/npm registries to generated locks | External package metadata, checksums, and advisory feeds determine code entering each runnable graph. |
| Optional partner constraints to Chimeway integration proofs | Accrue/Braintree and Threadline constrain transport dependencies while mounted tests establish compatible behavior. |
| Advisory feeds to release decision | Scanner output may contain both newly actionable findings and a narrow Cowlib range-mapping disagreement. |
| Local stabilization branch to GitHub PR/issue state | Local changes and checks are not public evidence until a replacement ref exists and exact-SHA checks succeed. |

## STRIDE Threat Register — ASVS Level 1

Every project-specific high-severity threat is blocking until its mitigation and automated evidence pass. The Hackney residual is rated medium for this project because Chimeway adds no attacker-controlled URL or SOCKS5 configuration entrypoint; its upstream advisory severity and fixed floor remain documented verbatim.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-x9i-01 | Tampering | Solver and lockfile provenance | high | mitigate | Use only existing official Hex packages, solver-generated locks/checksums, and the verified one-file #28 delta; reject hand edits, new packages, and changed live PR scope. |
| T-x9i-02 | Denial of Service / Tampering | Decimal, Mint, Phoenix, LiveView, Plug, Postgrex, Cowboy, Cowlib, Req, and Swoosh findings | high | mitigate | Resolve every compatible graph to researched safe floors and run per-project audits plus mounted release gates; any new unlisted finding blocks completion. |
| T-x9i-03 | Elevation of Privilege / Tampering | Elixir/OTP compatibility and optional partner behavior | high | mitigate | Preserve `elixir: "~> 1.17"`, mutation-lock only the intended Accrue checkout to immutable 1.5.1 SHA `d30fc25dbf6ba551792c66ff451b4b93c0af4bf1`, preserve unrelated pins, and run existing CI floor/integration gates; no untested override or framework-major campaign. |
| T-x9i-04 | Spoofing / Information Disclosure / Denial of Service | Upstream-constrained Hackney 1.25.0 | medium | accept | Capture root and demo graphs in separate fail-fast commands with distinct fatal diagnostics; only successful outputs may select absent/present policy. Create no tracker if both omit Hackney; otherwise require one open marker-bound tracker with all advisory IDs, graph paths, fixed floor, exposure boundary, and closure condition. |
| T-x9i-05 | Repudiation | Cowlib 2.20 advisory-feed disagreement | low | accept | Accept only the three exact records if still emitted, preserve raw output, cite authoritative affected ranges and fix ancestry, and fail on any different/new finding. |
| T-x9i-06 | Repudiation / Tampering | Premature PR closure or stale check reuse | high | mitigate | Resolve local final SHA from committed Tasks 1-2, require the public replacement object and commit API to resolve to that exact SHA, require its completed/successful `pr-gate`, and bind URL/SHA/check URL in marked closure comments; otherwise leave PRs open. |
</threat_model>

<verification>
Run the task-scoped commands in order. After all lockfiles settle, run the release-facing aggregate from the repository root:

1. `mix ci`
2. `mix verify.example`
3. `mix verify.accrue`
4. `mix verify.inbox`
5. `mix ci.verify_gates`
6. `npm ci &amp;&amp; npm audit &amp;&amp; npm outdated`

Immediately before release, rerun the raw Cowlib audit and document an exception only if the final command reproduces the researched mapping. Before any PR disposition or release decision, push/publish the integrated stabilization ref, set the exact implementation SHA from the committed Tasks 1-2 `HEAD`, prove the public replacement resolves to that SHA, prove the commit API can reach it, and require a check named `pr-gate` with `status == completed` and `conclusion == success` on that same SHA. Preserve the raw root and demo audit output in the summary so the accepted Hackney residual and any reproduced Cowlib feed exception remain visible rather than being represented as a globally clean audit.
</verification>

<success_criteria>
- Root, admin, inbox, and demo locks are solver-generated and every compatible advisory is cleared at the researched safe floor or newer compatible version.
- Elixir 1.17+/OTP 26+ support, existing dependency declarations outside the demo's obsolete compatibility pins, and optional integration behavior remain intact.
- The release contract rejects mutation of the exact Accrue 1.5.1 checkout SHA while leaving unrelated compatibility pins outside this item's edit authority.
- No checked-in advisory ignore or unsafe Hackney override converts a known constraint into misleading green output.
- `SECURITY.md` distinguishes resolved findings, the exact Hackney upstream residual, and only a Cowlib feed exception reproduced by the immediate pre-release audit.
- Per-project audits/tests, mounted integration aliases, release gates, actionlint, and npm audit/currency checks pass under the explicit residual policy.
- Root/demo dependency-tree acquisition failures block triage distinctly; after both succeed, GitHub has no newly created Hackney tracker when the package is absent and exactly one semantically complete open tracker when present. Contributor PR dispositions bind a public replacement to the exact committed implementation SHA and its successful required check or remain safely pending.
</success_criteria>

<output>
Create `.planning/quick/260912-x9i-triage-and-remediate-dependency-advisories-and-open-pr-issue/260912-x9i-SUMMARY.md` when done, including final package versions, raw residual audit output, GitHub tracker URL, replacement URL/SHA, and PR dispositions.
</output>
