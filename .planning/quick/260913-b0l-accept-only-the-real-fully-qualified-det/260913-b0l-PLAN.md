---
quick_id: 260913-b0l
phase: 260913-b0l-detached-readme-exact-identities
plan: 01
type: tdd
wave: 1
autonomous: true
depends_on: []
files_modified:
  - lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex
  - test/chimeway/crosswake_provider_feedback_docs_contract_test.exs
estimate:
  tokens: 4500
  raw_tokens: 4500
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The synthetic positive README fixture mirrors selected SHA 36841e065ad4a71b58b80bd599d111c8ee178390 by using Crosswake.Companions.Chimeway.Redaction and CrosswakeExample.Chimeway.Registry."
    - "README validation accepts only those two complete fully qualified aliases, while all existing Evil.* substitutions still return :error before focused execution."
    - "Focused-proof validation remains context-specific: exact one-segment Code, Redaction, and Registry aliases plus the exact MyApp.Workers.ChimewayProviderFeedbackWorker alternative are accepted, and their spoof cases remain rejected."
    - "The full local CrossWake contract passes and mix ci.crosswake_provider_feedback_docs proves the selected detached authority with the crosswake_provider_feedback_docs_verified marker."
  artifacts:
    - path: "lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex"
      provides: "Context-specific exact alias expectations for detached README and focused proof sources"
      contains: "[:Crosswake, :Companions, :Chimeway, :Redaction]"
    - path: "test/chimeway/crosswake_provider_feedback_docs_contract_test.exs"
      provides: "Selected-source-shaped positive README fixture plus retained adversarial public-verifier coverage"
      contains: "Crosswake.Companions.Chimeway.Redaction"
  key_links:
    - from: "write_valid_fixture!/1"
      to: "source_contract/1"
      via: "The synthetic README uses the same complete canonical alias paths observed at the pinned selected SHA"
      pattern: "Crosswake\\.Companions\\.Chimeway\\.Redaction|CrosswakeExample\\.Chimeway\\.Registry"
    - from: "source_contract/1"
      to: "remote_call?/3"
      via: "README-only exact alias lists are passed to the existing full-list equality matcher"
      pattern: "remote_call\\?\\(recipe, \\[:Crosswake"
    - from: "test/chimeway/crosswake_provider_feedback_docs_contract_test.exs"
      to: "lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex"
      via: "Positive and Evil.* fixtures invoke public verify/1 and bind the verdict to focused-test execution"
      pattern: "CrosswakeProviderFeedbackDocs\\.verify"
    - from: "mix ci.crosswake_provider_feedback_docs"
      to: "selected SHA 36841e065ad4a71b58b80bd599d111c8ee178390"
      via: "The authority selector, advertised ref, detached checkout, source contract, and real focused test complete with the stable success marker"
      pattern: "crosswake_provider_feedback_docs_verified"
---

<objective>
Restore the real detached CrossWake proof without reopening the prefix-spoof vulnerability.

Purpose: Exact alias matching must be specific to the source context: the selected README deliberately uses fully qualified public modules, while the selected focused proof legitimately uses imported one-segment aliases.
Output: A selected-source-shaped synthetic contract, context-specific exact README identities, preserved focused identities, and a passing real detached constituent.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260912-x9g-harden-release-automation-and-release-proof-semantics-ordina/260912-x9g-PLAN.md
@.planning/quick/260912-x9g-harden-release-automation-and-release-proof-semantics-ordina/260912-x9g-SUMMARY.md
@.planning/quick/260912-x9g-harden-release-automation-and-release-proof-semantics-ordina/260912-x9g-VERIFICATION.md
@.planning/quick/260913-a2h-reject-prefix-spoofed-crosswake-aliases-/260913-a2h-PLAN.md
@.planning/quick/260913-a2h-reject-prefix-spoofed-crosswake-aliases-/260913-a2h-SUMMARY.md
@.planning/quick/260913-a2h-reject-prefix-spoofed-crosswake-aliases-/260913-a2h-VERIFICATION.md
@lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex
@test/chimeway/crosswake_provider_feedback_docs_contract_test.exs
</context>

<source_coverage>

| Source | Item | Coverage |
|---|---|---|
| GOAL | Accept only the real fully qualified detached README aliases while retaining focused identities and anti-spoof rejection | Tasks 1-2 |
| REQ | No formal requirement IDs apply to this bounded quick repair | Excluded: quick task outside milestone requirement mapping |
| RESEARCH | x9g re-verification identifies a production false negative after a2h and requires the real detached constituent | Tasks 1-2 |
| CONTEXT | README exact full paths, focused exact short paths, exact worker, all Evil.* negatives, two-file scope, no human UAT | Tasks 1-2 |
| SELECTED AUTHORITY | SHA 36841e065ad4a71b58b80bd599d111c8ee178390 README lines use the two specified fully qualified calls; focused test imports them and uses one-segment calls | Tasks 1-2 |

No package install, external API design, database/schema change, or assumption-driven behavior is in scope; the package-legitimacy, API-coverage, schema-push, and assumption-delta contributions are therefore inapplicable.

</source_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the synthetic positive README reproduce the selected authority</name>
  <files>test/chimeway/crosswake_provider_feedback_docs_contract_test.exs</files>
  <behavior>
    - The shared positive README recipe calls `Crosswake.Companions.Chimeway.Redaction.feedback_from_provider_attrs/1` and `CrosswakeExample.Chimeway.Registry.apply_provider_feedback/2`, matching the selected SHA.
    - The selected-source-shaped positive case currently observes `{:error, false}` from public `verify/1`, proving the RED failure is the README identity false negative and not focused execution or setup failure.
    - README spoof cases still replace one canonical boundary at a time with `Evil.Redaction` or `Evil.Registry` and retain their exact `{:error, false}` assertions.
    - Focused fixtures remain unchanged with exact one-segment `Code`, `Redaction`, and `Registry` calls and the existing exact worker alternative.
  </behavior>
  <action>
First update only the contract test. Change the README section written by `write_valid_fixture!/1` to the exact fully qualified conversion and registry calls observed at selected SHA `36841e065ad4a71b58b80bd599d111c8ee178390`; do not qualify the focused-test fixture. Update the README qualifier-substitution tests and the nonexistent-conversion mutation so their search strings match the new canonical fixture. Keep the hostile replacements exactly `Evil.Redaction` and `Evil.Registry`, assert the canonical source occurrence exists before each replacement, and assert the forged occurrence exists afterward so rejection cannot pass because a mutation was a no-op.

Tag the existing primary positive case `:selected_readme_identity`. Have it observe both the public-verifier result and whether `:focused_test_executed` arrived, print `RED_SELECTED_README_CANONICAL_REJECTED` only for the known pre-fix tuple `{:error, false}`, then assert the required positive tuple `{:ok, true}`. Run that one tagged test against the unchanged production verifier. The RED gate must require a nonzero exit, the exact sentinel, and exactly one failure, excluding unrelated compilation, checkout, or fixture failures. Preserve all six `:exact_alias_identity` tests, their unique tags, and their rejection helper.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; set +e; output=$(CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/crosswake_provider_feedback_docs_contract_test.exs --only selected_readme_identity --warnings-as-errors 2&gt;&amp;1); status=$?; set -e; printf "%s\n" "$output"; [ "$status" -ne 0 ]; printf "%s\n" "$output" | grep -Fq "RED_SELECTED_README_CANONICAL_REJECTED"; printf "%s\n" "$output" | grep -Fq "1 failure"'</automated>
  </verify>
  <done>The selected-source-shaped positive test fails for the exact current false-negative tuple, every README adversarial mutation is proven non-vacuous, and no production code or focused-proof identity has changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind README validation to its real full paths and replay detached proof</name>
  <files>lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex, test/chimeway/crosswake_provider_feedback_docs_contract_test.exs</files>
  <behavior>
    - `source_contract/1` requires exactly `[:Crosswake, :Companions, :Chimeway, :Redaction]` for conversion and `[:CrosswakeExample, :Chimeway, :Registry]` for persistence in the README recipe.
    - `remote_call?/3` continues comparing the complete parsed alias list for equality; no suffix, prefix, or arbitrary alias resolution is introduced.
    - `record_remote_marker/3` continues accepting only `[:Code]`, `[:Redaction]`, and `[:Registry]` in the focused proof.
    - The worker alternative continues accepting only `[:MyApp, :Workers, :ChimewayProviderFeedbackWorker]`.
    - All six Evil.* cases reject before focused execution, the full local contract passes, and the real detached constituent emits its success marker.
  </behavior>
  <action>
Change only the two expected alias lists supplied by `source_contract/1` for the extracted README recipe: use `[:Crosswake, :Companions, :Chimeway, :Redaction]` with `:feedback_from_provider_attrs` and `[:CrosswakeExample, :Chimeway, :Registry]` with `:apply_provider_feedback`. Keep `remote_call?/3` as complete-list equality. Do not change the focused-test marker clauses, exact worker list, rooted reachability algorithm, selector authorities, detached checkout, cleanup, public Mix task interface, or error codes.

Run the selected positive case, all six exact-alias spoof cases, and the complete local contract after GREEN. Then run `mix ci.crosswake_provider_feedback_docs` against the repository-owned authority selector and require exit zero plus the exact `crosswake_provider_feedback_docs_verified` output marker. Treat a local-only green suite as insufficient: the task is complete only when selected SHA `36841e065ad4a71b58b80bd599d111c8ee178390` passes source validation and real focused execution. Do not edit the selected authority, CrossWake repository, release topology, Mix aliases, or any file outside the authorized pair.
  </action>
  <verify>
    <automated>bash -c 'set -euo pipefail; CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/crosswake_provider_feedback_docs_contract_test.exs --warnings-as-errors; CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/crosswake_provider_feedback_docs_contract_test.exs --only exact_alias_identity --warnings-as-errors; output=$(mix ci.crosswake_provider_feedback_docs 2&gt;&amp;1); printf "%s\n" "$output"; printf "%s\n" "$output" | grep -Fq "crosswake_provider_feedback_docs_verified"; mix format --check-formatted'</automated>
  </verify>
  <done>The full local contract and all six spoof cases pass, the README and focused contexts retain their distinct exact identities, and the real detached constituent exits zero with `crosswake_provider_feedback_docs_verified`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Selected detached CrossWake checkout to Chimeway release proof | Parsed README and focused-test aliases from the pinned external repository determine whether release evidence is accepted and executed. |

## STRIDE Threat Register — ASVS Level 1

The configured blocking threshold is high. Both high-severity threats must be mitigated before this quick task is complete.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-b0l-01 | Spoofing / Tampering | README and focused remote-marker identities | high | mitigate | Keep separate exact alias allowlists for each source context, retain non-vacuous Evil.* public-verifier mutations, and forbid suffix/prefix matching or arbitrary alias resolution. |
| T-b0l-02 | Denial of Service | Legitimate selected README proof | high | mitigate | Mirror the selected source in the positive fixture and require the real authority-selected detached constituent to emit its stable success marker after GREEN. |
| T-b0l-03 | Information Disclosure | Detached verifier output | low | accept | Existing output remains a fixed success marker or exit status and does not expose detached source, provider payloads, or credentials; this task does not change output paths. |

</threat_model>

<verification>
Use executable evidence only: the selected-source-shaped positive fixture, six non-vacuous spoof fixtures, the full local CrossWake contract, repository formatting, and the real `mix ci.crosswake_provider_feedback_docs` detached constituent. No human UAT applies.
</verification>

<success_criteria>
- The synthetic README uses the exact fully qualified module paths present at selected SHA `36841e065ad4a71b58b80bd599d111c8ee178390`.
- README and focused-test scanners retain separate exact identities; no Evil.* or prefix/suffix lookalike is accepted.
- The complete local contract is green and the real detached constituent prints `crosswake_provider_feedback_docs_verified` with exit zero.
- Only the production verifier and its contract test are modified.
</success_criteria>

<output>
Create `.planning/quick/260913-b0l-accept-only-the-real-fully-qualified-det/260913-b0l-SUMMARY.md` when done.
</output>
