---
quick_id: 260912-x9h
phase: 260912-x9h
plan: 01
type: execute
wave: 1
depends_on: ["260912-x9g"]
files_modified:
  - README.md
  - SECURITY.md
  - AGENTS.md
  - MAINTAINING.md
  - guides/introduction/inbox-integration.md
  - guides/flows/multi-step-journeys.md
  - guides/recipes/feedback-escalation-workflow.md
  - guides/recipes/password-reset-support-trace.md
  - test/chimeway/doc_contract_test.exs
  - lib/mix/tasks/demo.up.ex
  - examples/chimeway_demo_host/test/mix/tasks/demo_up_test.exs
files_deleted:
  - GSD-CONTEXT.md
  - guides/flows/trigger-to-delivery.md
  - guides/flows/async-dispatch.md
  - guides/flows/policy-and-preferences.md
autonomous: true
must_haves:
  truths:
    - "README shows the same runnable WelcomeUser callbacks already exercised by Chimeway.ReadmeSnippetTest, including its explicit rendering/2 path."
    - "The inbox guide matches the current dependency and map-shaped auth callback contracts while retaining host-owned tenant/recipient authorization and opaque reload guidance."
    - "All current public Markdown uses the canonical repository owner; three empty shipped guides are absent and no public document links to them."
    - "The repository-only demo.up task derives and validates its source-checkout path before database or subprocess work and retains the real journey smoke."
    - "MAINTAINING describes the hardened Release Please/token/clean-tree behavior from 260912-x9g, while AGENTS points to durable STATE/ROADMAP truth and the obsolete bootstrap file is absent."
  artifacts:
    - path: "README.md"
      provides: "Complete runnable notifier snippet aligned with Chimeway.ReadmeSnippetTest.WelcomeUser."
    - path: "guides/introduction/inbox-integration.md"
      provides: "Current version, callback type, privacy, and gate wording."
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "Executable snippet, guide, link, deletion, maintainer, and onboarding assertions."
    - path: "lib/mix/tasks/demo.up.ex"
      provides: "Fail-fast repository checkout boundary for nested demo commands."
    - path: "MAINTAINING.md"
      provides: "Release/operator truth aligned with the hardened automation."
    - path: "AGENTS.md"
      provides: "Durable planning source-of-truth pointers without copied current-phase state."
  key_links:
    - from: "README.md"
      to: "test/chimeway/integration/readme_snippet_test.exs"
      via: "The docs contract binds MyApp.Notifiers.WelcomeUser to Chimeway.ReadmeSnippetTest.WelcomeUser callback names and stable rendering literals; the integration test executes the path."
      pattern: "WelcomeUser|rendering"
    - from: "guides/introduction/inbox-integration.md"
      to: "chimeway_inbox/lib/chimeway_inbox/auth.ex"
      via: "Guide prose matches the map-shaped session/context callback contract and current root major/minor version."
      pattern: "context map"
    - from: "lib/mix/tasks/demo.up.ex"
      to: "examples/chimeway_demo_host"
      via: "The active Mix project root yields one validated child path reused by both nested commands."
      pattern: "Mix.Project.project_file"
    - from: "MAINTAINING.md"
      to: ".github/workflows/release.yml and scripts/ci/verify-clean.sh"
      via: "Post-260912-x9g contract assertions bind ordinary-merge behavior, fallback CI dispatch, and complete dirty-state detection."
      pattern: "Release Please|verify.clean"
---

<objective>
Finish the highest-value release-facing documentation and repository-tooling truth within a strict 15-path cap.

Purpose: Make adopter snippets, security/source links, inbox guidance, release operations, demo tooling, and agent onboarding accurate without expanding into a broad source-comment or formatter refactor.
Output: Correct public Markdown, three removed placeholder guides, a guarded Demo.Up task, hardened maintainer prose, durable AGENTS pointers, and executable drift contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260912-x9h-clean-public-documentation-and-repository-metadata-correct-i/260912-x9h-RESEARCH.md
@.planning/quick/260912-x9g-harden-release-automation-and-release-proof-semantics-ordina/260912-x9g-SUMMARY.md
@README.md
@test/chimeway/integration/readme_snippet_test.exs
@chimeway_inbox/lib/chimeway_inbox/auth.ex
@lib/mix/tasks/demo.up.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Lock runnable public Markdown and remove dead shipped guides</name>
  <files>README.md, SECURITY.md, guides/introduction/inbox-integration.md, guides/flows/multi-step-journeys.md, guides/recipes/feedback-escalation-workflow.md, guides/recipes/password-reset-support-trace.md, test/chimeway/doc_contract_test.exs, guides/flows/trigger-to-delivery.md (delete), guides/flows/async-dispatch.md (delete), guides/flows/policy-and-preferences.md (delete)</files>
  <behavior>
    - RED: README's notifier block must contain notification_key/0, version/0, recipients/1, build/2, and rendering/2 with the stable values used by `Chimeway.ReadmeSnippetTest.WelcomeUser`.
    - RED: Inbox guidance must derive its dependency major/minor from root version truth, call the callback argument a context map, and retain tenant/recipient authorization plus opaque reload constraints.
    - RED: Public Markdown must contain no legacy-owner repository URL, neither empty-guide marker, and no reference to any of the three removed guide paths.
    - GREEN: The unchanged `Chimeway.ReadmeSnippetTest.WelcomeUser` executes the documented Trigger-to-explanation path.
  </behavior>
  <action>
Add the failing docs contracts first. Extract the README notifier block and the existing `Chimeway.ReadmeSnippetTest.WelcomeUser` source; bind the public block to its required callbacks, explicit `rendering/2`, stable notification/render keys and versions, opaque synthetic recipient fields, and safe in-app assigns. Keep the executable fixture unchanged. Derive the inbox dependency major/minor from root `mix.exs`, require `context map`, and preserve the independent host authorization, opaque recipient, topic-secret custody, connected-mount, and reauthorization anchors.

Add one table-driven canonical-link/no-placeholder contract over current public Markdown, excluding `.planning/**` and `CHANGELOG.md`. Assert the three placeholder guide paths do not exist and no public Markdown references their basenames. Then copy the working fixture's notifier behavior into README, correct inbox version/callback/gate prose, replace the four legacy-owner links in SECURITY and the three named guides, delete the three nine-line placeholder files, and replace both password-reset stub references with existing complete guide links. Do not rewrite historical planning/release artifacts or broaden the README into a second full golden path.
  </action>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/doc_contract_test.exs test/chimeway/integration/readme_snippet_test.exs --exclude adoption_paths_e2e --exclude accrue_packaged_cli --warnings-as-errors</automated>
  </verify>
  <done>README and its unchanged executable fixture are contract-aligned and green; inbox security/version wording is current; canonical links have no legacy-owner stragglers; all three placeholders and every public reference to them are gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Demo.Up fail fast outside the repository checkout</name>
  <files>lib/mix/tasks/demo.up.ex, examples/chimeway_demo_host/test/mix/tasks/demo_up_test.exs</files>
  <behavior>
    - RED: Resolving a root without `examples/chimeway_demo_host` raises an actionable `Mix.Error`.
    - GREEN: Resolving the real root returns the exact demo-host directory, independent of caller cwd.
    - GREEN: `run/1` validates once before ecto/app/subprocess work and reuses the validated path for seed and serve commands.
    - REGRESSION: Existing tagged `mix demo.up --check` retains DATABASE_URL/environment inheritance, dev execution, sequencing, 300-second timeout, and readiness output.
  </behavior>
  <action>
Update the module/short documentation to identify Demo.Up as a source-checkout helper. Derive the active Mix project root from `Mix.Project.project_file/0`, build the exact demo-host child, and validate it at the beginning of `run/1` before `ecto.create`, migration, app start, or `System.cmd/3`. Reuse that validated path for both nested commands. Expose only a `@doc false` helper with an optional explicit root for tests; a missing child raises `Mix.Error` naming the required checkout path and directing package consumers to the public guides.

Add focused real-root and synthetic-missing-root tests beside the existing journey smoke. Preserve the smoke's tags, environment, assertions, and timeout. Format these two files explicitly; do not add repository-wide formatter configuration in this item.
  </action>
  <verify>
    <automated>(cd examples/chimeway_demo_host &amp;&amp; mix test test/mix/tasks/demo_up_test.exs --exclude journey --warnings-as-errors) &amp;&amp; mix format --check-formatted lib/mix/tasks/demo.up.ex examples/chimeway_demo_host/test/mix/tasks/demo_up_test.exs</automated>
  </verify>
  <done>The helper proves both checkout branches, failure precedes side effects, touched Elixir is formatted, and the retained journey test remains available for aggregate verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Align maintainer and agent entry-point truth</name>
  <files>MAINTAINING.md, AGENTS.md, GSD-CONTEXT.md (delete), test/chimeway/doc_contract_test.exs</files>
  <behavior>
    - RED: Maintainer contracts reject ordinary-merge language that can suppress Release Please, incomplete GITHUB_TOKEN fallback CI instructions, or a clean-tree description limited to unstaged changes.
    - RED: Agent metadata must reference both `.planning/STATE.md` and `.planning/ROADMAP.md`, must not copy a current phase/count claim, and must not retain root `GSD-CONTEXT.md`.
    - GREEN: MAINTAINING describes the exact hardened behavior landed by dependency 260912-x9g and retains the public release/verification command names.
  </behavior>
  <action>
After 260912-x9g lands, add focused docs contracts that bind MAINTAINING to its implemented release behavior: uncertain/non-Release-Please merges continue into the idempotent action; a pending release PR under GITHUB_TOKEN fallback receives exact-branch CI dispatch; PAT-backed fresh updates avoid duplicate dispatch; and `mix verify.clean` covers unstaged, staged, and non-ignored untracked state while allowing ignored files. Assert against stable behavior wording and public command names, not raw secrets.

Replace AGENTS' stale copied roadmap sentence with durable instructions to read `.planning/STATE.md` and `.planning/ROADMAP.md`; add matching positive assertions plus a negative assertion for copied current-phase/count prose. Delete `GSD-CONTEXT.md` and assert its path is absent. Do not edit ROADMAP/STATE in this item; their checkbox/count reconciliation is explicitly assigned to the separate mechanical follow-up.
  </action>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/doc_contract_test.exs test/chimeway/release_gate_contract_test.exs test/chimeway/verify_clean_test.exs --exclude adoption_paths_e2e --exclude accrue_packaged_cli --warnings-as-errors</automated>
  </verify>
  <done>Maintainer prose matches the hardened release and clean-tree contracts, AGENTS delegates changing state to authoritative files, obsolete bootstrap context is absent, and focused contracts pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs → adopter host code | Copy-pasted notifier/auth examples affect tenant, identity, and notification behavior downstream. |
| Repository links → external GitHub owner | Security reports and source links cross into an externally controlled namespace. |
| Active Mix project → demo subprocess | A dependency-exposed task derives a filesystem target and launches nested commands. |
| Maintainer prose → release automation | Operators rely on prose to decide whether CI/release/clean-tree evidence is sufficient. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-x9h-01 | Spoofing / Elevation of Privilege | Inbox guide | high | mitigate | Contract independent host tenant/opaque-recipient authorization and reauthorization wording; browser/PubSub input remains non-authoritative. |
| T-x9h-02 | Information Disclosure | README/inbox examples | high | mitigate | Keep synthetic opaque identity, closed reload, topic-secret custody, and no-payload guidance under executable contracts. |
| T-x9h-03 | Spoofing | Public repository/security links | high | mitigate | Replace all current public legacy-owner URLs and enforce the canonical owner across current public Markdown. |
| T-x9h-04 | Tampering | Demo.Up checkout path | medium | mitigate | Resolve from the active Mix project, validate the exact child before any side effect, and test both path outcomes. |
| T-x9h-05 | Tampering / Repudiation | Release maintainer guidance | high | mitigate | Bind MAINTAINING to the post-260912-x9g workflow and verify-clean behavior without exposing raw secret values. |

ASVS level 1 is satisfied: every high threat and primary-boundary medium threat is mitigated by executable evidence; no critical/high threat is accepted.
</threat_model>

<verification>
Run task-focused evidence, then:

1. `mix ci.docs`
2. `mix verify.journeys`
3. `mix ci.verify_gates`
4. `git diff --check`

Do not weaken gate membership or alter the two active CrossWake machine protocol files. The broad source-comment, roadmap checkbox/count, stale bell-comment, and nested formatter-topology cleanup is a separately bounded mechanical follow-up, not part of this plan's completion claim.
</verification>

<source_audit>

| Source | ID | Feature / Requirement | Task | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | 260912-x9h | Highest-value public docs, metadata, and tooling cleanup under 15 declared paths | 1, 2, 3 | COVERED | Exactly 11 modified plus 4 deleted paths. |
| REQ | — | No new roadmap requirement is assigned to this quick item | — | COVERED | Existing product gates rerun. |
| RESEARCH | README/inbox truth | Runnable notifier parity, current dependency/callback/gate wording | 1 | COVERED | Existing `WelcomeUser` stays executable; README is brought to it. |
| RESEARCH | repository/stub truth | Four legacy links, three empty shipped guides, and dead references | 1 | COVERED | Global public-Markdown contracts plus declared deletions. |
| RESEARCH | Demo.Up boundary | Active-project root, fail-fast guard, focused test, retained smoke | 2 | COVERED | No package or DB-bootstrap redesign. |
| RESEARCH | maintainer/agent truth | Release guidance after x9g, durable AGENTS pointers, obsolete bootstrap deletion | 3 | COVERED | x9g is an explicit sibling dependency. |
| CONTEXT | — | No item-specific CONTEXT.md or locked decisions exist | — | COVERED | Parent revision sets the 15-path boundary. |

Explicit follow-up boundary: ROADMAP Phase 106/107 checkboxes/counts, the stale bell comment, broad package/demo source chronology, and root/nested formatter topology/drift. These are neither declared nor scanned by this item.
</source_audit>

<success_criteria>
- Exactly 15 unique modified/deleted paths are authorized by frontmatter.
- README and the unchanged executable WelcomeUser fixture agree and pass the real Trigger-to-explanation test.
- Inbox guidance, repository links, and shipped guide topology are current and contract-locked.
- Demo.Up fails before side effects outside a source checkout and retains its real journey smoke.
- MAINTAINING matches x9g behavior; AGENTS points to durable truth; GSD-CONTEXT is absent.
- Focused tests, docs build, journey gate, release-facing gate, and diff checks pass.
</success_criteria>

<output>
Create `.planning/quick/260912-x9h-clean-public-documentation-and-repository-metadata-correct-i/260912-x9h-SUMMARY.md` when done.
</output>
