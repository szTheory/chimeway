---
phase: 82-logo-exploration-shortlist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notes/logo-options.md
  - scripts/logo-guards.sh
autonomous: false
requirements: [LOGO-01, LOGO-02, LOGO-05, LOGO-06, NOTES-02]

must_haves:
  truths:
    - "notes/logo-options.md presents exactly 5 fully-worked directions, each with Concept/Pros/Cons/Recommendation, a `Verdict: Ship|Defer|Reject` line, and a `Confidence: High|Medium|Low` line (LOGO-01, NOTES-02)"
    - "At least 2 of the 5 directions are integrated typemarks — a motif worked INTO the lowercase `chimeway` letterforms as <path>, not an icon beside a plain font (LOGO-02, D-04)"
    - "Each shortlisted direction embeds a self-contained proof strip carrying all five labels: 16px, Mono, Inverse, Clear-space, Min-size (D-12)"
    - "Rejected candidates (2-4) are retained inline as <svg>, each annotated with the single taste gate it failed (NOTES-02, D-01)"
    - "Every 6-digit hex in every committed SVG block is a member of the --cw-* token hex set {102027,07131a,fffdf8,0e7c86,d6a84f,9adbcf} (D-11)"
    - "Each shortlisted mark reads recognizably at 16px, in one-color mono, and inverse when viewed at real scale in the ephemeral file:// gallery (D-12, manual)"
    - "scripts/logo-guards.sh exists, is executable, and passes against the finished doc"
  artifacts:
    - "notes/logo-options.md — the single committed deliverable (inline SVG shortlist + rejected set)"
    - "scripts/logo-guards.sh — Wave 0 shell guard (grep + optional xmllint)"
    - "ephemeral file://-safe HTML gallery in the executor session scratchpad — NOT committed (D-13)"
  key_links:
    - "Committed SVG hex values <-> brandbook/tokens/tokens.css (cross-phase color contract; literal hex, never var())"
    - "notes/logo-options.md <-> Phase 83 finalist selection (consumes this shortlist)"
    - "proof-strip <symbol>+<use> kept in ONE <svg> root <-> gallery render fidelity (cross-<svg> <use> fails under file://)"
  prohibitions:
    - statement: "No rectangular/enclosing background cage behind any mark (D-06/LOGO-05)"
      status: enforced
      verification: "guard flags any full-bleed rect outside a scoped inverse proof cell; human visual review confirms"
    - statement: "Zero literal bell/clapper/musical-note/staff/audio imagery; metaphor is path/route/signal/trace only (LOGO-06/D-05)"
      status: enforced
      verification: "human visual review of every direction (shortlisted + rejected) in the gallery"
    - statement: "Primary lockup carries no subtitle/slogan (D-08)"
      status: enforced
      verification: "human visual review"
    - statement: "No gradients, drop shadows, stretch, or skew inside any mark (D-11)"
      status: enforced
      verification: "guard scans for gradient/filter constructs; visual review confirms"
    - statement: "No var(--cw-*) references inside committed SVG — literal token hex only (RESEARCH Pitfall 2)"
      status: enforced
      verification: "guard asserts zero var( occurrences inside committed SVG blocks"
    - statement: "No active/malicious SVG content — presentation elements only (T-82-01)"
      status: enforced
      verification: "guard SVG-hygiene scan returns zero matches"
    - statement: "No files changed outside notes/logo-options.md and scripts/logo-guards.sh (repo discipline, NOTES-03 precursor)"
      status: enforced
      verification: "git diff --stat shows only those two committed paths"
---

<objective>
Author `notes/logo-options.md` — a vetted shortlist of exactly 5 fully-worked, distinct
Chimeway logo directions (≥2 of them integrated typemarks), each carrying rationale, a
Ship/Defer/Reject verdict + confidence, and an inline proof strip proving 16px / mono /
inverse legibility — plus a set of retained-inline rejected candidates that each teach a
different taste gate. Ship the Wave 0 automated guard (`scripts/logo-guards.sh`) first so
every commit is sampled, and produce an ephemeral (uncommitted) `file://` HTML gallery so a
human can eyeball the marks at real scale.

Purpose: Phase 83 (the user checkpoint) needs a genuine, gate-passing choice set to pick from.
This phase does NOT pick a winner and does NOT emit a `brandbook/logo/*.svg` file family (that
is Phase 83). It is doc/asset-only: touch no runtime code.

Output: `notes/logo-options.md` (committed), `scripts/logo-guards.sh` (committed), and an
ephemeral scratchpad gallery (not committed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-logo-exploration-shortlist/82-CONTEXT.md
@.planning/phases/82-logo-exploration-shortlist/82-RESEARCH.md
@.planning/phases/82-logo-exploration-shortlist/82-UI-SPEC.md
@.planning/phases/82-logo-exploration-shortlist/82-VALIDATION.md
@brandbook/tokens/tokens.css

# Read RESEARCH "Code Examples" (Pattern 1 self-contained proof strip, Pattern 2 integrated
# typemark, ephemeral gallery wrapper) before authoring any SVG — the construction technique,
# the `currentColor`/`<symbol>`+`<use>` recolor trick, and the three verified rendering
# landmines (GitHub strips inline SVG; var() does not resolve in committed SVG; cross-<svg>
# <use> fails under file://) all live there.
</context>

<constraints>
LOCKED — do not re-derive (from 82-CONTEXT.md / 82-UI-SPEC.md):

- **Token palette (D-11):** committed SVG uses LITERAL token hex, never `var(--cw-*)` (it does
  not resolve in a standalone/committed SVG — RESEARCH Pitfall 2). Legal hex set:
  `--cw-ink #102027`, `--cw-night #07131a`, `--cw-paper #fffdf8`, `--cw-teal #0e7c86`,
  `--cw-brass #d6a84f`, `--cw-mint #9adbcf`. Annotate each distinct color the first time with a
  `<!-- --cw-ink -->`-style comment and put a token->hex legend in the doc intro. `none` and
  `currentColor` are allowed (they are not hex). Mark = ink primary + a SINGLE teal accent;
  mono = one-color ink; inverse = paper on night. No gradients / drop shadows / filters /
  stretch / skew inside any mark.
- **Metaphor set (D-05/LOGO-06):** path / route / signal-arc / waypoint / trace-timeline /
  `cw`-monogram-as-path only. Abstract gentle rings/arcs are allowed as "signal". ZERO literal
  bell, clapper, musical note, staff line, or audio imagery. The brand-book §13 "chime/bell
  endpoint" is SUPERSEDED — treat the literal bell as rejected.
- **Taste gates (LOGO-05):** no rectangular/enclosing background cage (D-06); mark + wordmark
  read as ONE unified unit, no icon-left/text-right gap (D-07); primary lockup carries NO
  subtitle/slogan (D-08); brand-derived, not clipart (D-09).
- **Wordmark casing:** lowercase `chimeway` in EVERY graphic; title-case "Chimeway" only in
  prose.
- **Wordmark rendering (D-10):** non-typemark directions render `chimeway` as
  `<text font-family="Inter, ui-sans-serif, system-ui, …">`; integrated-typemark directions
  hand-draw the flourished glyph(s) as monoline `<path>` on the same baseline/x-height as the
  Inter text so the unit reads as one word. Full wordmark-to-outline conversion is DEFERRED to
  Phase 83 — do not outline every glyph now.
- **Copy vocabulary (UI-SPEC, exact):** per direction — `Concept:` (≤2 visual ideas + metaphor),
  `Pros:`, `Cons:`, `Recommendation:`, then a `Verdict: Ship|Defer|Reject` line and a
  `Confidence: High|Medium|Low` line. Proof-strip labels: `16px` `Mono` `Inverse` `Clear-space`
  `Min-size`.
- **Repo discipline:** the ONLY committed files this phase are `notes/logo-options.md` and
  `scripts/logo-guards.sh`. No `brandbook/logo/*.svg` file family (Phase 83). No base64 rasters,
  no `<image>`, no data-URIs, no binaries. `git diff --stat` at phase end must show only those
  two paths.
- **Rendering reality (RESEARCH Pitfall 1, VERIFIED):** GitHub silently strips inline `<svg>`
  from rendered Markdown. The committed `.md` is the source-of-record, NOT a GitHub render
  target. The human eyeball happens in the ephemeral `file://` gallery / a raw-HTML-permitting
  previewer — never on github.com. Do NOT phrase any check as "renders on GitHub."
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — author scripts/logo-guards.sh (the automated gate)</name>
  <files>scripts/logo-guards.sh</files>
  <read_first>
    - .planning/phases/82-logo-exploration-shortlist/82-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements — the exact guard set)
    - .planning/phases/82-logo-exploration-shortlist/82-RESEARCH.md (§Validation Architecture, §Security Domain — hygiene patterns)
  </read_first>
  <action>
    Create `scripts/logo-guards.sh` FIRST (Nyquist Wave 0) so every subsequent doc commit is
    sampled. Make it a portable POSIX/bash script, `chmod +x`, `set -euo pipefail`, that takes
    an optional doc path argument defaulting to `notes/logo-options.md`. If the doc does not yet
    exist it must exit non-zero with a clear message (so an empty run is RED, proving the guard
    works). It emits a PASS/FAIL line per check and exits non-zero if ANY check fails.

    Implement these checks against the doc:
    1. Verdict count: lines matching a case-sensitive anchored pattern for `Verdict: ` followed
       by exactly one of `Ship`/`Defer`/`Reject` at end-of-line — assert count is >= 5.
    2. Confidence count: lines matching `Confidence: ` followed by one of `High`/`Medium`/`Low`
       — assert count is >= 5.
    3. Proof-label presence: for EACH of the five label tokens `16px`, `Mono`, `Inverse`,
       `Clear-space`, `Min-size`, assert the doc contains at least 5 occurrences (one per
       shortlisted direction).
    4. Rejected section present: assert a `Rejected` heading exists AND at least 2 reason lines
       (lines matching a `Reason:`/`Failed:` label) exist beneath it.
    5. Token-hex subset (D-11): extract every 6-digit `#hex` token from inside SVG blocks only
       (between `<svg` and `</svg>`), lowercase them, and assert every distinct value is a
       member of the allowed set `102027 07131a fffdf8 0e7c86 d6a84f 9adbcf`. Any hex outside
       the set fails and is printed.
    6. No `var(` inside SVG blocks (RESEARCH Pitfall 2): assert zero occurrences of a `var(`
       token within `<svg`…`</svg>` regions.
    7. SVG-hygiene scan (T-82-01): within SVG regions, assert ZERO matches for each active-content
       marker — a `script` element open tag, a `foreignObject` element, an `image` element, any
       attribute whose name begins with `on` immediately followed by `=` (event handler), a
       `javascript:` scheme, a `data:` URI, and an off-document `http`/`https` reference inside
       an `href`/`xlink:href` attribute. Presentation elements only.
    8. Optional well-formedness: if `xmllint` is on PATH, extract each `<svg>…</svg>` block to a
       temp file and run `xmllint --noout` on it; fail on malformed SVG. If `xmllint` is absent,
       print a SKIP line and continue (do not fail).
    9. Scope-boundary helper: provide a `--scope` flag that runs `git diff --stat` and asserts
       only `notes/logo-options.md` and `scripts/logo-guards.sh` appear (used at phase end;
       tolerate the ephemeral gallery living OUTSIDE the repo).

    Do NOT hard-code color literals anywhere except the allowed-set array in check 5. Keep the
    script dependency-free (grep/sed/awk/git only, xmllint optional). This script — not any
    inline command in this plan — is the canonical acceptance gate the doc tasks call.
  </action>
  <verify>
    <automated>bash scripts/logo-guards.sh 2>&1; test -x scripts/logo-guards.sh; bash scripts/logo-guards.sh 2>&1 | grep -qiE 'missing|not found|fail'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/logo-guards.sh` exists and is executable.
    - Run against the not-yet-existing doc, it exits non-zero with a clear "doc missing" message
      (RED proves the guard is wired).
    - It implements all nine checks above; xmllint check degrades to SKIP when xmllint is absent.
    - The script contains no color hex other than the allowed-set array.
  </acceptance_criteria>
  <done>Guard script is committed, executable, fails informatively on an absent/incomplete doc, and is the single automated gate the doc tasks invoke.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 5-direction shortlist (with ≥2 integrated typemarks + proof strips)</name>
  <files>notes/logo-options.md</files>
  <read_first>
    - .planning/phases/82-logo-exploration-shortlist/82-RESEARCH.md (§Code Examples: Pattern 1 self-contained proof strip, Pattern 2 integrated typemark; §Recommended Doc Structure)
    - .planning/phases/82-logo-exploration-shortlist/82-UI-SPEC.md (Copywriting Contract, Legibility Gates)
    - brandbook/tokens/tokens.css (the only legal colors/typefaces — copy literal hex)
    - prompts/chimeway-brand-book.md §12-13 (metaphor seeds; bell endpoint SUPERSEDED)
    - prompts/brand-book-pressure-test.md (hard taste non-negotiables)
  </read_first>
  <action>
    Create `notes/logo-options.md`. Start with an intro that states the visual thesis ("calm
    infrastructure" — quiet confidence, routed paths, warm technical precision, no SaaS-gloss),
    a token->hex legend (ink #102027, night #07131a, paper #fffdf8, teal #0e7c86, brass #d6a84f,
    mint #9adbcf), the proof-label gate legend, and a one-line note that inline marks render in
    the `file://` gallery / a raw-HTML previewer, NOT on github.com (per RESEARCH Pitfall 1).
    Also add a one-line scope note: this phase captures clear-space + min-size intent only; the
    do/don't usage grid clause of LOGO-06 is consciously DEFERRED to Phase 84 (BOOK-*, per CONTEXT
    Deferred Ideas) — so downstream verification does not over-credit LOGO-06 as fully satisfied here.

    Then author a `## Shortlist` section with EXACTLY 5 distinct directions (D-03). Suggested
    seeds (refine per creative discretion within D-05/D-11 — geometry is yours):
      1. Waypoint Route (logomark + Inter wordmark) — a `c`-like curve opening into a route line
         terminating at a single teal waypoint node.
      2. Signal Arc (logomark + Inter wordmark) — two route nodes joined by a line with one
         gentle signal arc rising above; teal on the arc apex only.
      3. Routed-w typemark (INTEGRATED, D-04) — the `w` of `chimeway` drawn as a zig-zag route
         with waypoint vertices, teal terminal; `chime`/`ay` set as Inter `<text>` on the same
         baseline so the word reads as one unit.
      4. Trace-timeline typemark (INTEGRATED, D-04) — the baseline of `chimeway` becomes a trace
         timeline; one glyph node (e.g. the `i` tittle or the `y` descender) becomes a routed
         signal node/path.
      5. cw-monogram-as-path — a `cw` monogram whose strokes form one continuous route, paired
         tightly with the lowercase wordmark.
    At LEAST directions 3 and 4 must be genuinely integrated typemarks (motif worked INTO the
    letterform as `<path>`, not an icon beside plain text) to satisfy D-04/LOGO-02; the other
    three may be logomark+`<text>` lockups per D-10.

    For each direction author, in this order:
      - a heading `### Direction N — <name>`;
      - the inline `<svg>` PRIMARY LOCKUP (mark + lowercase `chimeway`, one unified unit, no cage,
        no subtitle);
      - `Concept:` (≤2 visual ideas + the metaphor), `Pros:`, `Cons:`, `Recommendation:`;
      - a `Verdict: Ship|Defer|Reject` line and a `Confidence: High|Medium|Low` line, each on its
        own line matching the guard's anchored patterns exactly;
      - a proof strip authored per RESEARCH Pattern 1: ONE `<svg>` root, the mark defined once as
        a `<symbol>` with `stroke="currentColor"`/`fill="currentColor"`, rendered three times via
        `<use>` at the 16px, mono (ink), and inverse (paper-on-night, the ONLY legal scoped
        background rect) variants, followed by a caption line carrying all five labels
        `16px · Mono · Inverse · Clear-space · Min-size` with the clear-space (= height of the
        lowercase `c`) and min-size (logomark 16px min / 24px preferred; horizontal lockup 120px
        min) intent stated.

    Use literal token hex + a first-use `<!-- --cw-* -->` comment on each color; never `var()`.
    Keep every mark monoline with wide apertures (survives 16px + mono — RESEARCH Pitfall 4),
    one stroke-width, rounded terminals, a single teal accent idea. Hand-author concise `<path>`
    data — no `<image>`, no data-URIs, no over-noded machine exports. Presentation elements only
    (no scripting, no event handlers, no foreign objects, no remote references).

    Run `bash scripts/logo-guards.sh` and fix until green before finishing. If any direction
    cannot survive its own 16px/mono proof, redesign it here (do not ship a smudge) — a genuine
    failure gets moved to the Rejected section in Task 3, never shipped as shortlist.
  </action>
  <verify>
    <automated>bash scripts/logo-guards.sh notes/logo-options.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 5 directions, each with Concept/Pros/Cons/Recommendation + a `Verdict:` line + a
      `Confidence:` line (guard checks 1-2 green: ≥5 each).
    - ≥2 directions are integrated typemarks with the motif drawn INTO a `chimeway` glyph as
      `<path>` (guard label presence + will be visually confirmed in Task 4).
    - Every shortlisted direction has a self-contained proof strip carrying all five labels
      (guard check 3 green).
    - Every SVG hex ⊆ the token set; zero `var(` in SVG; hygiene scan clean (guard checks 5-7).
    - xmllint (if present) reports every SVG block well-formed (guard check 8).
  </acceptance_criteria>
  <done>notes/logo-options.md holds 5 gate-passing shortlisted directions (≥2 integrated typemarks), each with rationale, verdict+confidence, and a self-contained proof strip; guard script is green.</done>
</task>

<task type="auto">
  <name>Task 3: Rejected candidates (each fails a different gate) + ephemeral file:// gallery</name>
  <files>notes/logo-options.md</files>
  <read_first>
    - .planning/phases/82-logo-exploration-shortlist/82-RESEARCH.md (§Code Examples: ephemeral file:// gallery wrapper; §Open Questions Q2 — retain 2-4 instructive rejects)
    - .planning/phases/82-logo-exploration-shortlist/82-CONTEXT.md (D-13 gallery; NOTES-02 rejected-inline discipline)
  </read_first>
  <action>
    Append a `## Rejected candidates` section to `notes/logo-options.md` retaining 2-4 rejected
    marks INLINE as minimal `<svg>` (NOTES-02 repo-size discipline), each under a
    `### <name>` heading with a `Reason:` line naming the SINGLE taste gate it fails, so the doc
    teaches the gates. Cover a DIFFERENT gate each (RESEARCH Open-Q2):
      - a literal bell/clapper shape — fails LOGO-06 (literal bell, superseded §13 endpoint);
      - a `cw` monogram inside a rounded-rectangle cage — fails D-06/LOGO-05 (background cage);
      - an ultra-thin hairline concentric-ring mark — fails the 16px legibility gate
        (RESEARCH Pitfall 4, dies at favicon size);
      - a route icon set to the LEFT of a plain-font wordmark with a visible gap — fails D-07
        (icon+text not a unified unit).
    Keep each rejected SVG minimal, token-hex, presentation-only, same hygiene rules.

    Then generate the ephemeral `file://`-safe HTML gallery (D-13) in the executor's session
    scratchpad directory — a single self-contained `.html` file placed OUTSIDE the repo working
    tree so it can never be accidentally committed. It wraps every direction's primary lockup +
    proof strip and every rejected mark at real scale, on a `--cw-paper` background with
    `--cw-ink` text. Inside the gallery `var(--cw-*)` linked to `brandbook/tokens/tokens.css` is
    permitted (or inline the token values with an absolute stylesheet path if the relative link
    does not resolve under file://). This file is NOT committed and NOT listed in
    files_modified. Print its absolute `file://…` path so the reviewer can open it in Task 4.

    Run `bash scripts/logo-guards.sh notes/logo-options.md` (all checks green) and
    `bash scripts/logo-guards.sh --scope` to confirm `git diff --stat` shows only
    `notes/logo-options.md` and `scripts/logo-guards.sh`.
  </action>
  <verify>
    <automated>bash scripts/logo-guards.sh notes/logo-options.md && bash scripts/logo-guards.sh --scope</automated>
  </verify>
  <acceptance_criteria>
    - `## Rejected candidates` present with 2-4 inline SVG rejects, each with a `Reason:` line
      naming a DIFFERENT failed gate (guard check 4 green).
    - Full guard suite green against the complete doc.
    - `git diff --stat` shows ONLY notes/logo-options.md and scripts/logo-guards.sh (scope gate).
    - An absolute `file://` path to the ephemeral gallery is printed; the gallery file lives
      outside the repo tree and is not staged.
  </acceptance_criteria>
  <done>Doc has an instructive rejected set (each failing a distinct gate), the ephemeral gallery renders every mark at real scale, guards are green, and the committed diff is scope-clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human eyeball — legibility + taste gates in the file:// gallery</name>
  <action>Present the ephemeral gallery for human visual review of the perceptual/semantic gates that grep cannot cover (16px/mono/inverse legibility, integrated-typemark judgment, no-cage/no-subtitle/unified-unit, zero literal bell/note/staff). Block until the reviewer approves or names directions to redesign / move to Rejected. This does NOT pick a winner — the taste sign-off is Phase 83.</action>
  <what-built>
    `notes/logo-options.md` — 5 shortlisted logo directions (≥2 integrated typemarks) with
    rationale, Ship/Defer/Reject verdict + confidence, and per-direction 16px/mono/inverse proof
    strips, plus a rejected set — and an ephemeral `file://` HTML gallery rendering every mark at
    real scale. All grep guards are green.
  </what-built>
  <how-to-verify>
    1. Open the ephemeral gallery: `open "<absolute file:// path printed in Task 3>"` (or paste
       the path into a browser). Inline marks will NOT render on github.com — that is expected
       (RESEARCH Pitfall 1); the gallery is the eyeball surface.
    2. For each of the 5 shortlisted directions confirm the mark is recognizable at 16px, holds
       as one-color mono, and reads inverse (paper-on-night). A mark that becomes a smudge at
       16px should be called out to move to Rejected.
    3. Confirm ≥2 directions genuinely work the motif INTO a `chimeway` glyph (routed path /
       ligature), not an icon beside plain text.
    4. Confirm every direction: no rectangular/enclosing background cage; mark+wordmark read as
       one unit (no icon-left/text-right gap); primary lockup has no subtitle; metaphor is
       path/route/signal/trace only — ZERO literal bell / clapper / musical note / staff.
    5. Confirm the rejected candidates each teach a different gate and read as intentional.
    Note: this phase does NOT pick a winner — the taste sign-off is Phase 83. This checkpoint
    only confirms the shortlist passes the legibility + taste gates.
  </how-to-verify>
  <resume-signal>Type "approved", or list any direction to redesign / move to Rejected.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed SVG -> downstream embedders | The hand-authored marks in `notes/logo-options.md` seed assets that later ship in README/HexDocs (Phase 83/85). GitHub sanitizes inline SVG, but downstream embedders may not — so hygiene is enforced at authoring time. |

No auth / session / access-control / input-validation / crypto surface exists in this phase:
the deliverable is a static hand-authored Markdown + SVG doc with no runtime code, no user
input, and no stored data. Standard web ASVS categories (V2–V6) are **N/A** — asserted, not
invented. Only V14 (config/hygiene) applies, lightly. No package installs occur (RESEARCH
Package Legitimacy Audit: zero external packages), so no supply-chain threat (T-82-SC) applies.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-82-01 | Tampering | hand-authored `<svg>` in `notes/logo-options.md` | low | mitigate | `scripts/logo-guards.sh` SVG-hygiene scan (Task 1, check 7) asserts ZERO active content — no script element, no `on*` event-handler attribute, no `foreignObject`, no `image`/data-URI, no `javascript:` scheme, no off-document `http(s)` `href`/`xlink:href`. Marks use presentation elements only (path/text/g/rect/circle/symbol/use). Enforced per-commit. |
</threat_model>

<verification>
- `bash scripts/logo-guards.sh notes/logo-options.md` — all checks green (verdict ≥5,
  confidence ≥5, five proof labels ≥5 each, rejected section + reasons, token-hex subset,
  no `var(` in SVG, hygiene clean, xmllint well-formed when available).
- `bash scripts/logo-guards.sh --scope` — `git diff --stat` shows ONLY
  `notes/logo-options.md` and `scripts/logo-guards.sh`.
- Human visual review of the ephemeral `file://` gallery (checkpoint): every shortlisted mark
  survives 16px / mono / inverse; ≥2 integrated typemarks; no cage / no subtitle / unified unit;
  zero literal bell/note/staff; rejected set teaches distinct gates.
- Manual/semantic gates (LOGO-06 imagery meaning, LOGO-02 "integrated vs placed-beside",
  LOGO-05 enclosure judgment) are human-verified per 82-VALIDATION.md Manual-Only table — they
  are not textually greppable and are intentionally held out of automation.
</verification>

<success_criteria>
- `notes/logo-options.md` presents exactly 5 fully-worked, distinct directions, each with
  rationale, pros/cons, a Ship/Defer/Reject recommendation + confidence (LOGO-01, NOTES-02).
- ≥2 directions are genuinely integrated typemarks (LOGO-02).
- No background cage; unified mark+wordmark; no primary subtitle; path/route/signal/trace
  metaphor only, zero literal bell/note/staff (LOGO-05, LOGO-06).
- Every shortlisted mark is proven legible at 16px, in mono, and inverse, with clear-space +
  min-size intent captured (D-12).
- Rejected candidates retained inline with reasons (NOTES-02).
- Guard script green; committed diff limited to the two allowed files; ephemeral gallery
  produced and reviewed; no runtime code touched.
</success_criteria>

<artifacts_this_phase_produces>
- `notes/logo-options.md` — the single committed deliverable (inline-SVG shortlist + rejected
  set with rationale, verdicts, confidence, proof strips).
- `scripts/logo-guards.sh` — committed Wave 0 shell guard (grep guards + optional `xmllint`;
  provides the `--scope` git-diff boundary check and the SVG-hygiene scan for T-82-01).
- Ephemeral `file://`-safe HTML gallery in the executor session scratchpad — NOT committed,
  lives outside the repo tree (D-13).
- New SVG symbols authored inline: one `<symbol>` per shortlisted direction's proof strip
  (defined once, rendered 16px/mono/inverse via `<use>`).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/82-logo-exploration-shortlist/82-01-SUMMARY.md` when done.
</output>
