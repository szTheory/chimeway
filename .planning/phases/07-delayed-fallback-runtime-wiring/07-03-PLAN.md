---
phase: 07-delayed-fallback-runtime-wiring
plan: "07-03"
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - test/chimeway/integration/delivery_lifecycle_test.exs
  - test/chimeway/dispatch/sync_test.exs
  - test/chimeway/dispatch/oban_test.exs
  - test/chimeway/dispatch/oban_worker_test.exs
  - test/chimeway/policy/delayed_fallback_test.exs
  - test/support/chimeway/dispatch_helpers.ex
autonomous: true
requirements:
  - POLC-03
user_setup: []
must_haves:
  truths:
    - "Trigger-driven tests prove planner persists delayed-fallback flags and provenance metadata without fixture-only injection."
    - "Sync and Oban parity tests assert equivalent suppressed signature (`status`, `suppression_reason`, `policy_checkpoint`, `attempt_count`) for already-read delayed fallback."
    - "Suppressed delayed-fallback deliveries produce zero adapter calls and zero attempt rows."
    - "Validation tests reject invalid delayed-fallback subset declarations and reject `in_app` delayed-fallback declarations."
    - "Backward-compatibility tests confirm notifiers without delayed-fallback callback still dispatch with `delay_fallback: false`."
  artifacts:
    - path: test/chimeway/integration/delivery_lifecycle_test.exs
      provides: "Trigger-to-planner delayed-fallback persistence evidence and end-to-end lifecycle assertions"
    - path: test/chimeway/dispatch/sync_test.exs
      provides: "Sync perform-time suppression and adapter-bypass assertions for delayed fallback"
    - path: test/chimeway/dispatch/oban_test.exs
      provides: "Oban enqueue/perform delayed-fallback parity and no-enqueue-on-suppressed assertions"
    - path: test/chimeway/policy/delayed_fallback_test.exs
      provides: "Policy and planner guardrail tests for delayed-fallback semantics"
    - path: test/support/chimeway/dispatch_helpers.ex
      provides: "Shared parity assertion helper for delivery suppression signature"
  key_links:
    - from: test/chimeway/integration/delivery_lifecycle_test.exs
      to: lib/chimeway/delivery_planning.ex
      via: "Integration tests assert planner-persisted delayed-fallback rows from real trigger flow."
      pattern: "delay_fallback|delayed_fallback_source"
    - from: test/chimeway/dispatch/sync_test.exs
      to: test/chimeway/dispatch/oban_test.exs
      via: "Paired parity scenarios compare suppression signature across execution modes."
      pattern: "already_read|policy_checkpoint|attempt_count"
    - from: test/chimeway/policy/delayed_fallback_test.exs
      to: lib/chimeway/notifier.ex
      via: "Notifier callback compatibility and invalid subset guardrails are validated."
      pattern: "delayed_fallback_channels|invalid_delayed_fallback_channels"
---

<objective>
Close Phase 07 with executable, trigger-driven proof that delayed-fallback suppression works in production flow and remains parity-safe across sync and Oban execution modes.

Purpose: Satisfy POLC-03 using runtime evidence instead of fixture-only setup.
Output: Integration and parity test matrix that validates planning wiring, perform-time suppression, and guardrail invariants.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-delayed-fallback-runtime-wiring/07-CONTEXT.md
@.planning/phases/07-delayed-fallback-runtime-wiring/07-RESEARCH.md
@.planning/phases/07-delayed-fallback-runtime-wiring/07-VALIDATION.md
@test/chimeway/integration/delivery_lifecycle_test.exs
@test/chimeway/dispatch/sync_test.exs
@test/chimeway/dispatch/oban_test.exs
@test/chimeway/dispatch/oban_worker_test.exs
@test/chimeway/policy/delayed_fallback_test.exs
@test/support/chimeway/dispatch_helpers.ex
</context>

<tasks>

<task type="auto">
  <name>Task 07-03-01: Add trigger-driven delayed-fallback planning persistence tests</name>
  <files>test/chimeway/integration/delivery_lifecycle_test.exs, test/chimeway/policy/delayed_fallback_test.exs</files>
  <read_first>test/chimeway/integration/delivery_lifecycle_test.exs, test/chimeway/policy/delayed_fallback_test.exs, lib/chimeway/trigger.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/deliveries.ex</read_first>
  <action>Add end-to-end scenarios that prove delayed-fallback wiring is planner-driven:

1. Define notifier fixture for test scope with:
   - `channels/2` returning `[:in_app, :email]`
   - `delayed_fallback_channels/2` returning `[:email]`
2. Trigger through `Chimeway.trigger/3` and assert planned deliveries include:
   - `"email"` row with `delay_fallback: true`
   - `"in_app"` row with `delay_fallback: false`
3. Assert planner metadata persists:
   - `metadata["delayed_fallback_source"] == "notifier"` for delayed-fallback channel
4. Add compatibility scenario where notifier omits `delayed_fallback_channels/2` and assert all planned rows keep `delay_fallback: false`.
5. Add explicit assertion that these tests do not manually set `delay_fallback` on fixture-created deliveries.</action>
  <verify>mix test test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/policy/delayed_fallback_test.exs && rg "delayed_fallback_channels|delay_fallback" test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/policy/delayed_fallback_test.exs && rg "delayed_fallback_source" test/chimeway/integration/delivery_lifecycle_test.exs</verify>
  <acceptance_criteria>
    - trigger-driven tests assert delayed-fallback persistence directly from planner output
    - delayed-fallback source metadata is asserted as durable evidence
    - compatibility test covers notifier without delayed-fallback callback
    - tests avoid fixture-only delayed-fallback injection for acceptance scenarios
  </acceptance_criteria>
  <done>Runtime planning delayed-fallback behavior is validated through real trigger flow.</done>
</task>

<task type="auto">
  <name>Task 07-03-02: Add sync and Oban suppression parity matrix for already-read fallback</name>
  <files>test/chimeway/dispatch/sync_test.exs, test/chimeway/dispatch/oban_test.exs, test/chimeway/dispatch/oban_worker_test.exs, test/support/chimeway/dispatch_helpers.ex</files>
  <read_first>test/chimeway/dispatch/sync_test.exs, test/chimeway/dispatch/oban_test.exs, test/chimeway/dispatch/oban_worker_test.exs, test/support/chimeway/dispatch_helpers.ex, lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban_worker.ex</read_first>
  <action>Implement parity assertions for perform-time already-read suppression:

1. Create shared assertion helper in `dispatch_helpers.ex` for suppression signature:
```elixir
%{
  status: :suppressed,
  suppression_reason: "already_read",
  policy_checkpoint: "perform",
  attempt_count: 0
}
```
2. In sync tests, add scenario:
   - planned delayed-fallback delivery
   - linked notification marked read before dispatch
   - assert adapter receives no call and signature matches helper
3. In Oban tests, add equivalent scenario:
   - delayed-fallback delivery enqueued/performed after read state
   - assert worker returns suppressed outcome and signature matches helper
4. In worker tests, assert no attempt rows are inserted for already-read suppressed deliveries.
5. Tag scenario names/comments with `POLC-03` for auditable grep evidence.</action>
  <verify>mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/oban_worker_test.exs && rg "already_read" test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/oban_worker_test.exs && rg "policy_checkpoint|attempt_count" test/support/chimeway/dispatch_helpers.ex && rg "POLC-03" test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs</verify>
  <acceptance_criteria>
    - sync and Oban test suites contain paired already-read delayed-fallback scenarios
    - suppression signature parity is asserted with shared helper
    - adapter calls and attempts are both zero in suppressed parity scenarios
    - test labels/comments include requirement traceability for POLC-03
  </acceptance_criteria>
  <done>Sync and Oban suppression behavior is parity-tested with deterministic signature assertions.</done>
</task>

<task type="auto">
  <name>Task 07-03-03: Add guardrail tests for invalid delayed-fallback declarations and run full verification</name>
  <files>test/chimeway/policy/delayed_fallback_test.exs, test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <read_first>test/chimeway/policy/delayed_fallback_test.exs, lib/chimeway/delivery_planning.ex, lib/chimeway/notifier.ex, .planning/phases/07-delayed-fallback-runtime-wiring/07-VALIDATION.md</read_first>
  <action>Lock planner guardrails and execute full phase verification loop:

1. Add test case for delayed-fallback subset violation:
   - notifier returns `channels: [:in_app, :email]`
   - delayed-fallback callback returns `[:sms]`
   - assert planner/dispatch returns `{:error, {:planning_failed, {:invalid_delayed_fallback_channels, _}}}`
2. Add test case for `in_app` delayed-fallback declaration:
   - delayed-fallback callback returns `[:in_app]`
   - assert planner rejects with invalid-channel tagged error.
3. Keep one positive control case confirming valid delayed-fallback declaration still passes.
4. Run verification commands in exact order:
   - `mix test test/chimeway/policy/delayed_fallback_test.exs`
   - `mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/oban_worker_test.exs`
   - `mix test test/chimeway/integration/delivery_lifecycle_test.exs`
   - `mix ci.test`
5. If failures occur, stabilize deterministic setup/cleanup in touched test modules before re-running full suite.</action>
  <verify>mix test test/chimeway/policy/delayed_fallback_test.exs && mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/oban_worker_test.exs && mix test test/chimeway/integration/delivery_lifecycle_test.exs && mix ci.test && rg "invalid_delayed_fallback_channels|planning_failed" test/chimeway/policy/delayed_fallback_test.exs && rg "POLC-03" test/chimeway/**/*.exs</verify>
  <acceptance_criteria>
    - invalid delayed-fallback subset declaration is covered by explicit failing test
    - `in_app` delayed-fallback declaration is covered by explicit failing test
    - positive control test confirms valid delayed-fallback declarations still work
    - targeted and full test commands pass in the specified order
    - requirement traceability for POLC-03 is grep-discoverable across updated tests
  </acceptance_criteria>
  <done>Guardrail regressions are blocked and full POLC-03 evidence suite is executable.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-07-03-FIXTURE-ONLY-COVERAGE
    severity: high
    threat: "Tests pass using fixture-injected `delay_fallback` while trigger planner path regresses silently."
    mitigation: "Acceptance tests must use `Chimeway.trigger/3` scenarios that assert planner-persisted delayed-fallback rows and metadata."
    mitigation_check: "rg 'Chimeway\\.trigger|delayed_fallback_source' test/chimeway/integration/delivery_lifecycle_test.exs"
  - id: TM-07-03-PARITY-REGRESSION
    severity: high
    threat: "Sync and Oban diverge on already-read suppression outcomes, causing inconsistent runtime behavior."
    mitigation: "Paired parity tests assert identical suppression signatures and zero attempts across sync and Oban paths."
    mitigation_check: "rg 'already_read|policy_checkpoint|attempt_count' test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/oban_worker_test.exs"
  - id: TM-07-03-INVALID-CHANNEL-DRIFT
    severity: medium
    threat: "Invalid delayed-fallback channel declarations are not tested and later ship silently."
    mitigation: "Guardrail tests assert tagged planner errors for non-subset declarations and `in_app` misuse."
    mitigation_check: "rg 'invalid_delayed_fallback_channels' test/chimeway/policy/delayed_fallback_test.exs"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix test test/chimeway/policy/delayed_fallback_test.exs`
- [ ] `mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/oban_worker_test.exs`
- [ ] `mix test test/chimeway/integration/delivery_lifecycle_test.exs`
- [ ] `mix ci.test`
- [ ] `rg "Chimeway\\.trigger" test/chimeway/integration/delivery_lifecycle_test.exs`
- [ ] `rg "delayed_fallback_source" test/chimeway/integration/delivery_lifecycle_test.exs`
- [ ] `rg "already_read|policy_checkpoint|attempt_count" test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/oban_worker_test.exs`
- [ ] `rg "invalid_delayed_fallback_channels|planning_failed" test/chimeway/policy/delayed_fallback_test.exs`
- [ ] `rg "POLC-03" test/chimeway/**/*.exs`
</verification>

<success_criteria>
- Trigger-driven tests prove delayed-fallback planning and provenance persistence from real runtime flow.
- Sync and Oban parity tests validate identical already-read suppression behavior and zero-attempt guarantees.
- Invalid delayed-fallback declarations are blocked by explicit guardrail tests.
- Full targeted + CI test suite passes and provides audit-ready POLC-03 evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/07-delayed-fallback-runtime-wiring/07-03-SUMMARY.md`.
</output>
