---
phase: 07-delayed-fallback-runtime-wiring
plan: "07-02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - lib/chimeway/dispatch/sync.ex
  - lib/chimeway/dispatch/oban.ex
  - lib/chimeway/dispatch/oban_worker.ex
  - lib/chimeway/traces.ex
autonomous: true
requirements:
  - POLC-03
user_setup: []
must_haves:
  truths:
    - "Sync and Oban worker paths both evaluate perform-time policy with `check_read_state: delivery.delay_fallback` before adapter execution."
    - "Perform-time suppression writes `policy_checkpoint: perform` metadata through durable suppression APIs."
    - "Oban enqueue flow never enqueues non-pending rows after planner output."
    - "Planner errors and runtime suppression outcomes remain explicit and parity-safe across sync and Oban dispatch."
    - "Trace explanations surface delayed-fallback provenance and policy checkpoint for suppressed deliveries."
  artifacts:
    - path: lib/chimeway/dispatch/sync.ex
      provides: "Sync runtime path with explicit perform-time delayed-fallback suppression behavior"
    - path: lib/chimeway/dispatch/oban_worker.ex
      provides: "Oban perform path with matching delayed-fallback suppression semantics"
    - path: lib/chimeway/dispatch/oban.ex
      provides: "Oban enqueue parity preserving pending-only dispatch from planned rows"
    - path: lib/chimeway/traces.ex
      provides: "Suppression explainability fields for checkpoint and delayed-fallback source"
  key_links:
    - from: lib/chimeway/dispatch/sync.ex
      to: lib/chimeway/policy.ex
      via: "Sync path evaluates perform-time delayed-fallback suppression before execution."
      pattern: "Policy\\.evaluate\\(delivery, check_read_state: delivery\\.delay_fallback\\)"
    - from: lib/chimeway/dispatch/oban_worker.ex
      to: lib/chimeway/policy.ex
      via: "Oban worker mirrors sync perform-time suppression call shape."
      pattern: "Policy\\.evaluate\\(delivery, check_read_state: delivery\\.delay_fallback\\)"
    - from: lib/chimeway/traces.ex
      to: lib/chimeway/delivery.ex
      via: "Trace timeline reads suppression checkpoint and delayed-fallback source metadata."
      pattern: "policy_checkpoint|delayed_fallback_source"
---

<objective>
Harden runtime delayed-fallback semantics so sync and Oban perform paths enforce identical suppression behavior and emit explainable suppression metadata for operators.

Purpose: Prevent POLC-03 drift between dispatcher strategies after planner wiring in 07-01.
Output: Runtime suppression parity in sync/Oban plus trace-level explainability metadata for delayed-fallback outcomes.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-delayed-fallback-runtime-wiring/07-CONTEXT.md
@.planning/phases/07-delayed-fallback-runtime-wiring/07-RESEARCH.md
@.planning/phases/07-delayed-fallback-runtime-wiring/07-PATTERNS.md
@lib/chimeway/dispatch/sync.ex
@lib/chimeway/dispatch/oban.ex
@lib/chimeway/dispatch/oban_worker.ex
@lib/chimeway/traces.ex
@lib/chimeway/deliveries.ex
@lib/chimeway/policy.ex
</context>

<tasks>

<task type="auto">
  <name>Task 07-02-01: Normalize perform-time delayed-fallback suppression in sync and worker flows</name>
  <files>lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban_worker.ex</files>
  <read_first>lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban_worker.ex, lib/chimeway/policy.ex, lib/chimeway/deliveries.ex, .planning/phases/07-delayed-fallback-runtime-wiring/07-CONTEXT.md</read_first>
  <action>Make perform-time suppression behavior explicit and equivalent in both modules:

1. Ensure both paths evaluate policy using:
```elixir
Policy.evaluate(delivery, check_read_state: delivery.delay_fallback)
```
2. On `{:suppress, :already_read}` (or any suppress result), call:
```elixir
Deliveries.suppress_delivery(delivery, reason, checkpoint: :perform)
```
3. Preserve terminal-state guard behavior (`:succeeded`, `:suppressed`, `:cancelled`) and do not re-dispatch terminal rows.
4. Preserve adapter bypass on suppression: suppressed path must return without adapter execution or attempt recording.
5. Keep public return shapes unchanged for sync and worker APIs while aligning internal suppression handling logic.</action>
  <verify>rg "Policy\\.evaluate\\(delivery, check_read_state: delivery\\.delay_fallback\\)" lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban_worker.ex && rg "checkpoint: :perform" lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban_worker.ex && rg "suppressed|cancelled|succeeded" lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban_worker.ex && mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_worker_test.exs</verify>
  <acceptance_criteria>
    - sync and worker modules use identical perform-time policy evaluation call shape
    - perform-time suppressions persist with `policy_checkpoint = perform`
    - suppressed deliveries skip adapter execution and record zero attempts
    - terminal-state protections remain in both dispatch paths
    - existing sync and worker test files pass after change
  </acceptance_criteria>
  <done>Sync and Oban worker perform-time delayed-fallback suppression semantics are parity-safe.</done>
</task>

<task type="auto">
  <name>Task 07-02-02: Preserve planner-to-runtime parity in Oban enqueue and sync planner error flow</name>
  <files>lib/chimeway/dispatch/oban.ex, lib/chimeway/dispatch/sync.ex</files>
  <read_first>lib/chimeway/dispatch/oban.ex, lib/chimeway/dispatch/sync.ex, lib/chimeway/delivery_planning.ex, .planning/phases/07-delayed-fallback-runtime-wiring/07-RESEARCH.md</read_first>
  <action>Harden dispatcher behavior around planned delayed-fallback rows:

1. Keep sync and Oban planner calls tagged with explicit planner error mapping:
```elixir
{:error, reason} -> {:error, {:planning_failed, reason}}
```
2. In Oban enqueue path, enforce pending-only filter directly on planner output before job insertion.
3. Ensure suppressed rows with delayed-fallback metadata are retained as durable rows but never enqueued.
4. Ensure sync dispatch returns suppressed planned rows as dispatch outcomes without adapter execution.
5. Do not reintroduce channel-level planning logic in dispatchers; planner remains single source for delayed-fallback flags.</action>
  <verify>rg "planning_failed" lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban.ex && rg "status == :pending|Enum\\.filter\\(.*:pending" lib/chimeway/dispatch/oban.ex && ! rg "plan_delivery\\(" lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban.ex && mix test test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/sync_test.exs</verify>
  <acceptance_criteria>
    - planner failures are tagged consistently in sync and Oban modules
    - Oban path enqueues only pending deliveries
    - suppressed planned rows remain durable and skip enqueue/adapter work
    - dispatchers do not contain direct delivery planning persistence logic
    - targeted sync/Oban dispatch tests pass
  </acceptance_criteria>
  <done>Dispatcher orchestration preserves delayed-fallback planner intent without strategy drift.</done>
</task>

<task type="auto">
  <name>Task 07-02-03: Surface delayed-fallback suppression provenance in traces</name>
  <files>lib/chimeway/traces.ex</files>
  <read_first>lib/chimeway/traces.ex, lib/chimeway/delivery.ex, .planning/phases/07-delayed-fallback-runtime-wiring/07-PATTERNS.md, .planning/phases/07-delayed-fallback-runtime-wiring/07-CONTEXT.md</read_first>
  <action>Expand suppression timeline details so operators can explain delayed-fallback outcomes:

1. In suppression timeline/event detail builder, include:
```elixir
%{
  reason: delivery.suppression_reason,
  policy_checkpoint: Map.get(metadata, "policy_checkpoint", "unknown"),
  delayed_fallback_source: Map.get(metadata, "delayed_fallback_source", "unknown")
}
```
2. Keep existing timeline ordering and event names unchanged.
3. Keep field naming stable (`policy_checkpoint`, `delayed_fallback_source`) so rg-based verification is deterministic.
4. Do not expose raw payload/body fields in trace details.</action>
  <verify>rg "policy_checkpoint" lib/chimeway/traces.ex && rg "delayed_fallback_source" lib/chimeway/traces.ex && rg "event: :suppressed|suppressed" lib/chimeway/traces.ex && mix test test/chimeway/integration/delivery_lifecycle_test.exs</verify>
  <acceptance_criteria>
    - suppressed trace entries include both checkpoint and delayed-fallback source metadata
    - trace timeline shape remains backwards-compatible
    - trace details remain redacted from payload secrets
    - integration lifecycle tests remain green
  </acceptance_criteria>
  <done>Operator traces expose delayed-fallback suppression provenance for runtime explainability.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-07-02-PERFORM-BYPASS
    severity: high
    threat: "One runtime path skips perform-time read-state check, allowing already-read outbound sends."
    mitigation: "Both sync and Oban worker must call `Policy.evaluate(delivery, check_read_state: delivery.delay_fallback)` before adapter execution."
    mitigation_check: "rg 'Policy\\.evaluate\\(delivery, check_read_state: delivery\\.delay_fallback\\)' lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban_worker.ex"
  - id: TM-07-02-ENQUEUE-LEAK
    severity: high
    threat: "Oban dispatcher enqueues suppressed delayed-fallback rows, creating unintended sends after retries."
    mitigation: "Filter planner output to `status == :pending` before enqueue and retain suppressed rows as non-enqueued evidence."
    mitigation_check: "rg 'status == :pending|Enum\\.filter\\(.*:pending' lib/chimeway/dispatch/oban.ex"
  - id: TM-07-02-EXPLAINABILITY-LOSS
    severity: medium
    threat: "Suppression details omit delayed-fallback provenance, reducing operator auditability."
    mitigation: "Include `policy_checkpoint` and `delayed_fallback_source` in suppression trace timeline detail."
    mitigation_check: "rg 'policy_checkpoint|delayed_fallback_source' lib/chimeway/traces.ex"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix compile --warnings-as-errors`
- [ ] `mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/oban_worker_test.exs`
- [ ] `mix test test/chimeway/integration/delivery_lifecycle_test.exs`
- [ ] `rg "Policy\\.evaluate\\(delivery, check_read_state: delivery\\.delay_fallback\\)" lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban_worker.ex`
- [ ] `rg "checkpoint: :perform" lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban_worker.ex`
- [ ] `rg "status == :pending|Enum\\.filter\\(.*:pending" lib/chimeway/dispatch/oban.ex`
- [ ] `rg "planning_failed" lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban.ex`
- [ ] `rg "policy_checkpoint" lib/chimeway/traces.ex`
- [ ] `rg "delayed_fallback_source" lib/chimeway/traces.ex`
</verification>

<success_criteria>
- Sync and Oban worker paths enforce the same perform-time delayed-fallback suppression semantics.
- Oban enqueue and sync dispatch honor planner output without re-planning or leaking suppressed rows into execution.
- Trace output explains delayed-fallback suppression with checkpoint and source metadata.
- Targeted dispatch and integration suites pass with runtime parity intact.
</success_criteria>

<output>
After completion, create `.planning/phases/07-delayed-fallback-runtime-wiring/07-02-SUMMARY.md`.
</output>
