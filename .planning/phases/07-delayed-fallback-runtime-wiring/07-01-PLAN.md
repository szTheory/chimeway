---
phase: 07-delayed-fallback-runtime-wiring
plan: "07-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/notifier.ex
  - lib/chimeway/deliveries.ex
  - lib/chimeway/delivery_planning.ex
autonomous: true
requirements:
  - POLC-03
user_setup: []
must_haves:
  truths:
    - "`Chimeway.Notifier` exposes optional `delayed_fallback_channels/2` without breaking modules that only implement existing callbacks."
    - "`Chimeway.Deliveries.plan_delivery/3` persists `delay_fallback` and provenance metadata while preserving `plan_delivery/2` compatibility."
    - "`Chimeway.DeliveryPlanning` resolves delayed-fallback channels deterministically and fails on invalid subsets."
    - "Planner never marks `in_app` rows with `delay_fallback: true`."
    - "Delayed fallback defaults to disabled unless explicitly declared by notifier callback or policy override."
  artifacts:
    - path: lib/chimeway/notifier.ex
      provides: "Notifier delayed-fallback callback contract and backward-compatible optional callback declaration"
      exports:
        - "validate_module!/1"
    - path: lib/chimeway/deliveries.ex
      provides: "Additive delivery planning API with delayed-fallback persistence options"
      exports:
        - "plan_delivery/2"
        - "plan_delivery/3"
    - path: lib/chimeway/delivery_planning.ex
      provides: "Runtime delayed-fallback resolver and planner validations for channel subsets"
      exports:
        - "plan_notifications/2"
        - "plan_notification/2"
  key_links:
    - from: lib/chimeway/delivery_planning.ex
      to: lib/chimeway/notifier.ex
      via: "Planner consumes optional notifier delayed-fallback callback when present."
      pattern: "delayed_fallback_channels\\("
    - from: lib/chimeway/delivery_planning.ex
      to: lib/chimeway/deliveries.ex
      via: "Planner writes `delay_fallback` and source metadata through additive plan API."
      pattern: "Deliveries\\.plan_delivery\\(.*delay_fallback:"
    - from: lib/chimeway/delivery_planning.ex
      to: lib/chimeway/policy.ex
      via: "Planner keeps planning checkpoint policy evaluation after delayed-fallback resolution."
      pattern: "Policy\\.evaluate\\(delivery, \\[\\]\\)"
---

<objective>
Wire delayed-fallback intent into normal runtime planning so POLC-03 behavior starts from trigger-driven planner output instead of fixture-only delivery setup.

Purpose: Make delayed-fallback suppression a real runtime contract by extending notifier declarations, planner resolution, and delivery persistence.
Output: Optional notifier callback, additive delivery plan API, and validated planner wiring that persists delayed-fallback decisions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-delayed-fallback-runtime-wiring/07-CONTEXT.md
@.planning/phases/07-delayed-fallback-runtime-wiring/07-RESEARCH.md
@.planning/phases/07-delayed-fallback-runtime-wiring/07-PATTERNS.md
@lib/chimeway/notifier.ex
@lib/chimeway/deliveries.ex
@lib/chimeway/delivery_planning.ex
@lib/chimeway/policy.ex
</context>

<tasks>

<task type="auto">
  <name>Task 07-01-01: Add delayed-fallback notifier callback contract</name>
  <files>lib/chimeway/notifier.ex</files>
  <read_first>lib/chimeway/notifier.ex, lib/chimeway/trigger.ex, .planning/phases/07-delayed-fallback-runtime-wiring/07-CONTEXT.md, .planning/phases/07-delayed-fallback-runtime-wiring/07-RESEARCH.md</read_first>
  <action>Update notifier behavior definition with an additive delayed-fallback callback:

1. Add callback signature:
```elixir
@callback delayed_fallback_channels(map(), map()) ::
            {:ok, [atom() | String.t()]} | {:error, term()}
```
2. Mark callback optional:
```elixir
@optional_callbacks delayed_fallback_channels: 2
```
3. Keep `validate_module!/1` compatibility exactly as-is: do not require notifier modules to implement delayed-fallback callback.
4. Add moduledoc section that delayed-fallback channels must represent outbound channel subset intent and must never include `:in_app`.
5. Do not alter existing `channels/2` callback shape in this task.</action>
  <verify>rg "@callback delayed_fallback_channels\\(map\\(\\), map\\(\\)\\)" lib/chimeway/notifier.ex && rg "@optional_callbacks delayed_fallback_channels: 2" lib/chimeway/notifier.ex && rg "validate_module!" lib/chimeway/notifier.ex && mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - `delayed_fallback_channels/2` callback exists in notifier behavior
    - callback is optional and does not break existing notifiers
    - notifier docs describe subset-only delayed-fallback intent
    - project compiles with warnings treated as errors
  </acceptance_criteria>
  <done>Notifier contract can express delayed-fallback intent without breaking existing modules.</done>
</task>

<task type="auto">
  <name>Task 07-01-02: Add additive delivery planning API for delayed-fallback persistence</name>
  <files>lib/chimeway/deliveries.ex</files>
  <read_first>lib/chimeway/deliveries.ex, lib/chimeway/delivery.ex, .planning/phases/07-delayed-fallback-runtime-wiring/07-RESEARCH.md, .planning/phases/07-delayed-fallback-runtime-wiring/07-PATTERNS.md</read_first>
  <action>Introduce additive `plan_delivery/3` API and preserve existing call sites:

1. Keep existing `plan_delivery(notification_id, channel)` public API and delegate to `plan_delivery/3` with defaults.
2. Implement `plan_delivery(notification_id, channel, opts)` with supported options:
   - `delay_fallback: boolean()` default `false`
   - `delayed_fallback_source: :default | :notifier | :policy` default `:default`
3. Persist `delay_fallback` directly on `%Delivery{}` insert attrs.
4. Merge provenance metadata non-destructively:
```elixir
metadata["delayed_fallback_source"] = "default" | "notifier" | "policy"
```
5. Preserve idempotent conflict handling (`on_conflict: :nothing` and canonical re-read behavior) so duplicate planner passes remain deterministic.
6. Do not modify suppression APIs in this task.</action>
  <verify>rg "def plan_delivery\\(notification_id, channel, opts \\\\ \\[\\]\\)" lib/chimeway/deliveries.ex && rg "delay_fallback" lib/chimeway/deliveries.ex && rg "delayed_fallback_source" lib/chimeway/deliveries.ex && rg "on_conflict: :nothing" lib/chimeway/deliveries.ex && mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - `plan_delivery/3` exists with delayed-fallback options
    - `plan_delivery/2` remains callable and backward-compatible
    - `delay_fallback` and provenance metadata persist at insert time
    - metadata merge preserves pre-existing keys
    - idempotent insert conflict behavior remains unchanged
  </acceptance_criteria>
  <done>Delivery planning API persists delayed-fallback intent additively and safely.</done>
</task>

<task type="auto">
  <name>Task 07-01-03: Wire delayed-fallback resolution and validation in shared planner</name>
  <files>lib/chimeway/delivery_planning.ex</files>
  <read_first>lib/chimeway/delivery_planning.ex, lib/chimeway/notifier.ex, lib/chimeway/deliveries.ex, lib/chimeway/policy.ex, .planning/phases/07-delayed-fallback-runtime-wiring/07-CONTEXT.md</read_first>
  <action>Implement delayed-fallback resolution inside planner before policy checkpoint:

1. Resolve normal channel list using existing channel resolution flow.
2. Resolve delayed-fallback channels with strict precedence:
   - notifier callback result (if callback exists and returns `{:ok, channels}`)
   - policy/config override in planner opts (if present)
   - default empty set
3. Validate delayed-fallback channels:
   - each delayed-fallback channel must exist in resolved channels
   - if invalid, return `{:error, {:invalid_delayed_fallback_channels, invalid_channels}}`
4. Enforce invariant: remove/forbid `in_app` from delayed-fallback set; if explicitly requested, return `{:error, {:invalid_delayed_fallback_channels, ["in_app"]}}`.
5. When planning each channel row, call:
```elixir
Deliveries.plan_delivery(notification.id, channel,
  delay_fallback: channel in delayed_fallback_channels,
  delayed_fallback_source: delayed_fallback_source_for(channel)
)
```
6. Keep planning checkpoint policy call unchanged (`Policy.evaluate(delivery, [])`) and keep suppression persistence behavior intact.
7. If notifier delayed-fallback callback returns `{:error, reason}`, propagate `{:error, {:delayed_fallback_resolution_failed, reason}}`.</action>
  <verify>rg "invalid_delayed_fallback_channels" lib/chimeway/delivery_planning.ex && rg "delayed_fallback_resolution_failed" lib/chimeway/delivery_planning.ex && rg "Deliveries\\.plan_delivery\\(.*delay_fallback:" lib/chimeway/delivery_planning.ex && rg "Policy\\.evaluate\\(delivery, \\[\\]\\)" lib/chimeway/delivery_planning.ex && ! rg "delay_fallback: true.*in_app" lib/chimeway/delivery_planning.ex && mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - planner persists delayed-fallback flags only for validated channels
    - invalid subset declarations fail with typed planner error
    - explicit `in_app` delayed-fallback declarations are rejected
    - planner keeps planning-time policy evaluation and suppression durability
    - callback resolution errors are surfaced explicitly
  </acceptance_criteria>
  <done>Shared planner now owns delayed-fallback runtime wiring with deterministic validation.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-07-01-INVALID-SUBSET-BYPASS
    severity: high
    threat: "Planner accepts delayed-fallback channels outside resolved channel set, enabling silent policy drift."
    mitigation: "Planner rejects non-subset declarations with tagged `invalid_delayed_fallback_channels` error and aborts planning."
    mitigation_check: "rg 'invalid_delayed_fallback_channels' lib/chimeway/delivery_planning.ex"
  - id: TM-07-01-INAPP-SUPPRESSION-CORRUPTION
    severity: high
    threat: "`in_app` is marked delayed fallback, allowing suppression to target the read-state source channel."
    mitigation: "Planner forbids `in_app` in delayed-fallback declarations and returns typed error."
    mitigation_check: "rg 'in_app' lib/chimeway/delivery_planning.ex | rg 'invalid_delayed_fallback_channels'"
  - id: TM-07-01-COMPAT-BREAK
    severity: medium
    threat: "New callback requirement breaks existing notifier modules at runtime."
    mitigation: "Declare delayed-fallback callback as optional and keep `validate_module!/1` required callbacks unchanged."
    mitigation_check: "rg '@optional_callbacks delayed_fallback_channels: 2' lib/chimeway/notifier.ex"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix compile --warnings-as-errors`
- [ ] `mix test test/chimeway/trigger_pipeline_test.exs`
- [ ] `mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs`
- [ ] `rg "@callback delayed_fallback_channels\\(map\\(\\), map\\(\\)\\)" lib/chimeway/notifier.ex`
- [ ] `rg "@optional_callbacks delayed_fallback_channels: 2" lib/chimeway/notifier.ex`
- [ ] `rg "def plan_delivery\\(notification_id, channel, opts \\\\ \\[\\]\\)" lib/chimeway/deliveries.ex`
- [ ] `rg "delayed_fallback_source" lib/chimeway/deliveries.ex lib/chimeway/delivery_planning.ex`
- [ ] `rg "invalid_delayed_fallback_channels" lib/chimeway/delivery_planning.ex`
- [ ] `rg "delayed_fallback_resolution_failed" lib/chimeway/delivery_planning.ex`
- [ ] `rg "Policy\\.evaluate\\(delivery, \\[\\]\\)" lib/chimeway/delivery_planning.ex`
</verification>

<success_criteria>
- Runtime planner persists delayed-fallback intent from trigger-time resolution, not fixture shortcuts.
- Planner enforces strict subset validation and rejects `in_app` delayed fallback.
- Delivery persistence stores `delay_fallback` plus decision source metadata durably.
- Existing notifier modules remain compatible without implementing new callback.
</success_criteria>

<output>
After completion, create `.planning/phases/07-delayed-fallback-runtime-wiring/07-01-SUMMARY.md`.
</output>
