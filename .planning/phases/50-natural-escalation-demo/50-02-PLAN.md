---
phase: 50-natural-escalation-demo
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - guides/recipes/mention-escalation.md
  - guides/flows/multi-step-journeys.md
  - guides/recipes/feedback-escalation-workflow.md
  - test/chimeway/doc_contract_test.exs
  - mix.exs
autonomous: true
requirements: [DEMO-04]
must_haves:
  truths:
    - "`guides/recipes/mention-escalation.md` documents read-cancel plus time-based `wait_until` fallback as the primary PM JTBD path (D-04)"
    - "`guides/flows/multi-step-journeys.md` intro positions `wait_until` and `cancel_signals` / inbox read as complementary — not mutually exclusive"
    - "Doc contract locks mention-escalation recipe truth in CI (D-06)"
    - "Recipe appears in `mix.exs` docs extras for Hex publication"
    - "Mention-escalation recipe is distinct from `feedback-escalation-workflow.md` (webhook/delivery-feedback path)"
  artifacts:
    - path: "guides/recipes/mention-escalation.md"
      provides: "PM persona walkthrough for READ-driven escalation"
      contains: "Chimeway.mark_read"
    - path: "guides/flows/multi-step-journeys.md"
      provides: "Aligned Missed Engagement Escalation intro"
      contains: "cancel_signals"
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "RECP-03 mention-escalation doc contract"
      contains: "mention escalation recipe"
    - path: "mix.exs"
      provides: "Hex docs extras entry"
      contains: "mention-escalation.md"
  key_links:
    - from: "guides/recipes/mention-escalation.md"
      to: "DemoHost.Notifiers.PaymentReminder"
      via: "runnable TeamPulse reference + JOUR-03 proof link"
      pattern: "PaymentReminder"
    - from: "guides/flows/multi-step-journeys.md"
      to: "guides/recipes/mention-escalation.md"
      via: "Next Steps cross-link"
      pattern: "mention-escalation"
    - from: "test/chimeway/doc_contract_test.exs"
      to: "guides/recipes/mention-escalation.md"
      via: "@required / @forbidden_strings CI gate"
      pattern: "mention_escalation_recipe"
---

<objective>
Publish the mention-escalation reference recipe, fix the stale journey-guide intro, extend doc-contract tests, and register the recipe in Hex docs extras.

Purpose: DEMO-04 requires adopters to find a PM-facing walkthrough for read-cancel plus `wait_until` time fallback — distinct from the webhook feedback recipe. Wave 2 depends on 50-01 so JOUR-03 and `PaymentReminder` are truthful runnable references.

Output: New recipe, updated journey guide, doc contract describe block, `mix.exs` extras entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-natural-escalation-demo/50-CONTEXT.md
@.planning/phases/50-natural-escalation-demo/50-RESEARCH.md
@.planning/phases/50-natural-escalation-demo/50-VALIDATION.md
@.planning/phases/50-natural-escalation-demo/50-01-SUMMARY.md

@guides/recipes/password-reset-support-trace.md
@guides/recipes/feedback-escalation-workflow.md
@guides/flows/multi-step-journeys.md
@test/chimeway/doc_contract_test.exs
@mix.exs
@examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex
@examples/chimeway_demo_host/test/demo_host_web/journey_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author mention-escalation recipe (D-04)</name>
  <files>guides/recipes/mention-escalation.md</files>
  <read_first>
    - `guides/recipes/password-reset-support-trace.md` — persona + prerequisites structure
    - `guides/recipes/feedback-escalation-workflow.md` — recipe shape; note webhook path to contrast
    - `guides/flows/multi-step-journeys.md` — canonical `wait_until` + `cancel_signals` workflow example
    - `examples/chimeway_demo_host/lib/demo_host/notifiers/payment_reminder.ex` — runnable TeamPulse reference (post 50-01)
    - `examples/chimeway_demo_host/test/demo_host_web/journey_test.exs` — JOUR-03 proof (post 50-01)
    - `.planning/phases/50-natural-escalation-demo/50-CONTEXT.md` — D-04, scope fence for JOUR-06
  </read_first>
  <action>
    **Create** `guides/recipes/mention-escalation.md` with these sections:

    1. **Who this is for**
       - **Product Manager:** JTBD — "if they don't open in 2 hours, send email" (or push/email escalation framing)
       - **Feature Developer:** Author `workflow/2` with `wait_until` + `cancel_signals`

    2. **Not this path** — explicit callout: delivery-feedback / webhook escalation is [Feedback escalation workflow](feedback-escalation-workflow.md), not inbox-read cancellation.

    3. **Prerequisites** — link [Multi-step journeys](../flows/multi-step-journeys.md), [Golden Path](../introduction/golden-path.md).

    4. **Feature Developer: notifier with wait_until + cancel_signals** — workflow snippet with:
       - `"kind" => "wait_until"`
       - `"anchor" => "prior_delivery_terminal_at"`
       - `"delay_seconds" => 7200`
       - `"to_step" => "email_escalation"` (or equivalent)
       - `"cancel_signals" => ["chimeway.notification.read"]`
       - Reference `DemoHost.Notifiers.PaymentReminder` as runnable demo host example.

    5. **Trigger** — `Chimeway.trigger/3` with `idempotency_key` and `tenant_id`.

    6. **READ-driven early exit** — host calls `Chimeway.mark_read/3`; engine emits `chimeway.notification.read`; `SignalRouterWorker` → `route_signal/1`; `signal_received` transition with `event_name` only in context. **Do not** instruct host `Signal.track` after `mark_read`.

    7. **Time fallback** — when unread, `WorkflowProgressionWorker` advances at `due_at` to the email step. Link [Oban integration](oban-integration.md). **Scope fence:** cite JOUR-06 (Phase 51) for automated proof that email fires only when unread — do not claim read-cancel halts scheduled worker in Phase 50.

    8. **Runnable proof** — link `DemoHost.Seeds.escalation_waiting!/0` and JOUR-03 in `journey_test.exs`.

    9. **Related guides** — `multi-step-journeys.md`, `feedback-escalation-workflow.md` (webhook path), `oban-integration.md`.

    Required strings for downstream doc contract: `Chimeway.trigger`, `Chimeway.mark_read`, `cancel_signals`, `wait_until`, `delay_seconds`, `chimeway.notification.read`, `prior_delivery_terminal_at`, `SignalRouterWorker`, `email_escalation` or `to_step`.
  </action>
  <verify>
    <automated>test -f guides/recipes/mention-escalation.md && rg "Chimeway.mark_read" guides/recipes/mention-escalation.md</automated>
  </verify>
  <acceptance_criteria>
    - File `guides/recipes/mention-escalation.md` exists.
    - Recipe contains `Chimeway.trigger`, `Chimeway.mark_read`, `cancel_signals`, `wait_until`, `delay_seconds`, `chimeway.notification.read`.
    - Recipe contains explicit distinction from feedback/webhook escalation (references `feedback-escalation-workflow.md`).
    - Recipe references `DemoHost.Notifiers.PaymentReminder` or TeamPulse demo.
    - Recipe does **not** contain `stage_escalation_webhook`, `PendingWebhookAdapter`, or `Engine gap today`.
    - Recipe does **not** instruct host `Signal.track` after `mark_read`.
  </acceptance_criteria>
  <done>PM-facing mention-escalation recipe published with READ + wait_until fallback pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Fix journey guide intro and cross-links (D-04)</name>
  <files>guides/flows/multi-step-journeys.md, guides/recipes/feedback-escalation-workflow.md</files>
  <read_first>
    - `guides/flows/multi-step-journeys.md` — line 7 stale intro, §7 inbox emission (lines 218–225), Next Steps (lines 227–231)
    - `guides/recipes/mention-escalation.md` — Task 1 output
    - `guides/recipes/feedback-escalation-workflow.md` — line 31 cross-link to mention example
    - `.planning/phases/50-natural-escalation-demo/50-CONTEXT.md` — D-04 intro fix
  </read_first>
  <action>
  In `guides/flows/multi-step-journeys.md`:

    1. **Replace line 7** (Missed Engagement Escalation intro) — position `wait_until` as the time gate **and** `cancel_signals` / inbox read (`Chimeway.mark_read/3` → `chimeway.notification.read`) as complementary early-exit. Remove phrasing that inbox-read cancellation is **not** the mechanism or that they are mutually exclusive. Must **not** contain "not inbox-read cancellation".

    2. **Add to Next Steps** (after existing bullets):
       - `[Mention escalation recipe](../recipes/mention-escalation.md) — PM persona walkthrough for read-cancel plus wait_until time fallback`

    3. **Preserve** Phase 48–49 content: `cancel_signals`, `pending_signals`, `Chimeway.mark_read`, §7 inbox lifecycle — do not remove shipped READ documentation.

  In `guides/recipes/feedback-escalation-workflow.md` (optional but recommended):

    4. **Add Related guides link** to `mention-escalation.md` with note: inbox-read / PM JTBD path — not delivery-feedback webhooks.
  </action>
  <verify>
    <automated>rg "not inbox-read cancellation" guides/flows/multi-step-journeys.md; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `guides/flows/multi-step-journeys.md` line 7 area describes both `wait_until` time escalation and inbox-read `cancel_signals` as complementary.
    - `guides/flows/multi-step-journeys.md` does **not** contain `not inbox-read cancellation`.
    - `guides/flows/multi-step-journeys.md` Next Steps contains link to `../recipes/mention-escalation.md`.
    - `guides/flows/multi-step-journeys.md` still contains `Chimeway.mark_read` and `cancel_signals` (no regression of Phase 49 docs).
  </acceptance_criteria>
  <done>Journey guide intro aligned with shipped READ + wait_until pattern; recipe cross-linked.</done>
</task>

<task type="auto">
  <name>Task 3: Mention-escalation doc contract (D-06 / RECP-03)</name>
  <files>test/chimeway/doc_contract_test.exs</files>
  <read_first>
    - `test/chimeway/doc_contract_test.exs` — `"feedback escalation recipe doc contract (RECP-02)"` describe (lines 143–178) as mirror pattern
    - `guides/recipes/mention-escalation.md` — Task 1 output
    - `guides/flows/multi-step-journeys.md` — Task 2 output
    - `.planning/phases/50-natural-escalation-demo/50-RESEARCH.md` — suggested @required / @forbidden strings
  </read_first>
  <action>
    In `test/chimeway/doc_contract_test.exs`:

    1. Add module attribute: `@mention_escalation_recipe "guides/recipes/mention-escalation.md"`

    2. Add describe block `"mention escalation recipe doc contract (RECP-03 / DEMO-04)"` mirroring RECP-02 structure:

       **Setup:** `File.read!(@mention_escalation_recipe)`

       **`@forbidden_strings`** (extend shared `@recipe_forbidden_strings` loop or duplicate):
       - `stop_conditions`, `Workflows.Workers`, `Chimeway.Trigger.trigger` (shared)
       - `stage_escalation_webhook`, `PendingWebhookAdapter`, `waiting_for_signal`
       - `chimeway.delivery.succeeded` (forbid as primary path in this recipe)

       **`@forbidden_phrases`:**
       - `not inbox-read cancellation`
       - `Engine gap today`

       **Chimeway.Workflow regex test** (mirror other recipes).

       **`@required`:**
       - `Chimeway.trigger`
       - `Chimeway.mark_read`
       - `cancel_signals`
       - `wait_until`
       - `delay_seconds`
       - `chimeway.notification.read`
       - `prior_delivery_terminal_at`
       - `SignalRouterWorker`
       - `to_step` (or `email_escalation`)

    3. **Extend journey guide contract** (`"journey guide doc contract (DOCS-03)"`) if needed:
       - Add `@forbidden_phrases` entry: `not inbox-read cancellation` (prevents line 7 regression)

    4. Run `mix ci.verify_gates` until green.
  </action>
  <verify>
    <automated>mix ci.verify_gates</automated>
  </verify>
  <acceptance_criteria>
    - `test/chimeway/doc_contract_test.exs` contains describe `"mention escalation recipe doc contract (RECP-03 / DEMO-04)"`.
    - Doc contract `@required` includes `Chimeway.mark_read` and `cancel_signals` for mention-escalation recipe.
    - Doc contract `@forbidden_strings` includes `stage_escalation_webhook` and `PendingWebhookAdapter`.
    - Journey guide `@forbidden_phrases` includes `not inbox-read cancellation` (if extended).
    - `mix ci.verify_gates` exits 0.
    - `mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>CI locks mention-escalation recipe truth and prevents stale intro regression.</done>
</task>

<task type="auto">
  <name>Task 4: Register recipe in mix.exs docs extras</name>
  <files>mix.exs</files>
  <read_first>
    - `mix.exs` — `docs/0 extras` list (lines 110–123)
    - `guides/recipes/mention-escalation.md` — Task 1 output
    - `.planning/phases/50-natural-escalation-demo/50-RESEARCH.md` — mix.exs extras note
  </read_first>
  <action>
    In `mix.exs`, within `defp docs` → `extras` list, add after `"guides/recipes/feedback-escalation-workflow.md"`:

    ```elixir
    "guides/recipes/mention-escalation.md",
    ```

    Do not reorder other entries or change `@version`.
  </action>
  <verify>
    <automated>rg "mention-escalation.md" mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` `extras` list contains `"guides/recipes/mention-escalation.md"`.
    - Entry appears in Recipes group (`groups_extras` pattern `~r/guides\/recipes\//` covers it).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Mention-escalation recipe included in Hex documentation extras.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public documentation | Recipes are adopter-facing; must not instruct deprecated seed choreography or host `Signal.track` glue after `mark_read`. |
| Doc contract CI | `mix ci.verify_gates` prevents reintroduction of stale deferral and webhook-primary language in READ recipe. |
| Recipe separation | PM readers must not conflate inbox-read escalation with delivery-feedback webhooks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-10 | Tampering | doc drift reintroduces webhook-primary READ story | mitigate | `@forbidden_strings` blocks `stage_escalation_webhook`, `PendingWebhookAdapter`; journey guide forbids `not inbox-read cancellation`. |
| T-50-11 | Information Disclosure | recipe over-promises JOUR-06 halt semantics | mitigate | Scope fence in recipe — time fallback documented; read-cancel worker cancellation deferred to Phase 51. |
| T-50-12 | Repudiation | aspirational docs without CI lock | mitigate | RECP-03 doc contract + `mix ci.verify_gates`. |
| T-50-13 | Spoofing | adopters copy webhook recipe for inbox JTBD | mitigate | "Not this path" section + cross-links between feedback and mention recipes. |

**No `high`-severity threats.** Documentation-only plan; no runtime auth or data-plane changes.
</threat_model>

<verification>
- [ ] `mix ci.verify_gates` green
- [ ] `mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` green
- [ ] `test -f guides/recipes/mention-escalation.md`
- [ ] `rg "mention-escalation" guides/flows/multi-step-journeys.md` — match in Next Steps
- [ ] `rg "not inbox-read cancellation" guides/flows/multi-step-journeys.md` — no match
- [ ] `rg "mention-escalation.md" mix.exs` — match
- [ ] No edits under `lib/chimeway/` in this plan
</verification>

<success_criteria>
- ROADMAP Phase 50 criterion 3: mention-escalation recipe documents read-cancel plus time-based `wait_until` fallback
- DEMO-04: reference recipe is primary PM JTBD path, distinct from webhook recipe
- Journey guide intro no longer contradicts §7 inbox-read documentation
- Doc contract and Hex extras lock recipe truth in CI
</success_criteria>

<output>
After completion, create `.planning/phases/50-natural-escalation-demo/50-02-SUMMARY.md`
</output>
