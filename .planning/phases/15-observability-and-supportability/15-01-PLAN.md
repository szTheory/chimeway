---
phase: 15-observability-and-supportability
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/traces.ex
  - test/chimeway/traces_test.exs
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03

must_haves:
  truths:
    - "Operators can trace an event through notification, delivery, and attempt records using one durable identifier."
    - "Structured telemetry and logs avoid leaking sensitive payload fields."
    - "Host-app correlation and tenancy context is available in operator surfaces and traces."
  artifacts:
    - path: "lib/chimeway/traces.ex"
      provides: "Trace query methods with multi-tenancy options"
      contains: "opts \\\\"
    - path: "test/chimeway/traces_test.exs"
      provides: "Tests verifying opts propagation for Ecto prefixes"
  key_links:
    - from: "lib/chimeway/traces.ex"
      to: "Ecto.Repo"
      via: "opts passing to Repo.all/get/one/preload"
      pattern: "Repo\\.(all|get|one|preload).*opts"
---

<objective>
Enable multi-tenancy support for tracing queries to fully satisfy the host-app context requirement (OBS-03), and formally verify the end-to-end telemetry safety mechanisms.

Purpose: While the tracing logic and telemetry emission are functionally complete, host apps using Ecto multi-tenancy need to pass a `prefix` (or other standard Repo options) into operator query interfaces.
Output: Updated `Chimeway.Traces` APIs accepting optional `opts`, and validated observability tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/chimeway/traces.ex
@test/chimeway/traces_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose Repo opts in Chimeway.Traces</name>
  <files>lib/chimeway/traces.ex, test/chimeway/traces_test.exs</files>
  <action>
    Update `Chimeway.Traces` to pass optional Ecto `opts` through to all `Repo` querying and preloading calls to allow host-app features like multi-tenant schemas:
    - `get_trace(event_id, opts \\ [])` -> pass `opts` to `Repo.get` and `Repo.preload`
    - `find_traces_by_correlation_id(correlation_id, opts \\ [])` -> pass `opts` to `Repo.all` and `Repo.preload`
    - `explain_delivery(delivery_id, opts \\ [])` -> pass `opts` to `Repo.one` and `Repo.preload` (if not done inline)
    - `find_traces_for_recipient(recipient_id, opts \\ [])` -> The function signature already accepts `opts`, extracting `:limit` and `:notification_key`. Pass the remaining `opts` directly as the second argument to `Repo.all(query, opts)`.
    
    Add a test case in `test/chimeway/traces_test.exs` to verify that `opts` are propagated (e.g., passing `[prefix: "nonexistent_schema"]` and asserting an error is raised by the DB adapter, proving the option is correctly sent down to the DB).

    **Pattern Compliance:** Modifies `lib/chimeway/traces.ex` according to the PATTERNS.md analog ("Trace explanations rely on efficient preloads... to assemble nested trace relationships"). Ensure `opts` are seamlessly integrated with these existing complex preloads.
  </action>
  <verify>
    <automated>mix test test/chimeway/traces_test.exs</automated>
  </verify>
  <done>Traces queries gracefully accept and correctly pass through standard Ecto `opts` to Repo calls.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end Telemetry pipeline check</name>
  <files>test/chimeway/telemetry_integration_test.exs, test/chimeway/telemetry_correlation_test.exs</files>
  <action>
    Run the telemetry integration tests to formally execute verification that OBS-02 (PII redaction) and OBS-03 (correlation ID tracking) requirements hold. The existing `Chimeway.Telemetry.safe_meta/1` pattern handles this, but running these tests in this task checks the current state to finalize the phase validation.
    No code modifications are expected unless tests fail.
  </action>
  <verify>
    <automated>mix test test/chimeway/telemetry_integration_test.exs test/chimeway/telemetry_correlation_test.exs</automated>
  </verify>
  <done>Telemetry tests execute securely and verify redaction behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> Chimeway.Traces API | Host application or operator UI invokes trace queries. |
| Chimeway.Telemetry -> Output Sink | The system broadcasts span events safely to configured loggers without PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | `Chimeway.Telemetry` | mitigate | Ensure `safe_meta/1` strips all nested raw payload data. |
| T-15-02 | Denial of Service | `Chimeway.Traces` | mitigate | Ensure result sizing constraints like `limit: 50` remain in place during `opts` wiring. |
| T-15-03 | Information Disclosure | `Chimeway.Traces` | mitigate | Allow `[prefix: tenant_id]` to enforce safe multi-tenant data boundaries through Ecto options. |
</threat_model>

<verification>
- Tracing APIs successfully consume Ecto query `opts` without crashing.
- No payload parameter sets are leaked in `Chimeway.Telemetry.span/3` broadcasts.
- Comprehensive pass on `mix test` covering telemetry and traces modules.
</verification>

<success_criteria>
1. Operators can trace events and inspect delivery status fully.
2. Structured logs natively scrub extraneous payload attributes.
3. Tenancy boundaries and request correlation persist securely to the trace view layer.
</success_criteria>

<output>
After completion, create `.planning/phases/15-observability-and-supportability/15-01-SUMMARY.md`
</output>
