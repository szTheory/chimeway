---
phase: 71-redaction-and-explainability-contracts
plan: 2
type: execute
wave: 2
depends_on:
  - 71-01
autonomous: true
requirements:
  - EXPL-01
  - EXPL-02
files_modified:
  - chimeway_admin/lib/chimeway_admin/components/status.ex
  - chimeway_admin/lib/chimeway_admin/live/dashboard_live.ex
  - chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex
  - chimeway_admin/lib/chimeway_admin/live/definitions_live.ex
  - chimeway_admin/test/chimeway_admin/components/status_test.exs
  - chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs
  - chimeway_admin/test/chimeway_admin/live/definitions_live_test.exs
must_haves:
  truths:
    - "Admin lifecycle copy distinguishes sent, provider accepted, delivered, suppressed, retryable failure, and terminal failure without changing durable core status atoms."
    - "D-09: Operator-facing lifecycle labels are centralized in `ChimewayAdmin.Components.Status` or a nearby presenter instead of changing durable core status atoms."
    - "Trace Detail uses durable feedback facts before showing Delivered and otherwise labels successful attempts as Provider accepted."
    - "Definitions rendered copy says DB-inferred persisted history and does not claim code registry, source skew, notifier module discovery, module inventory, loaded modules, or source-code scanning."
  artifacts:
    - path: "chimeway_admin/lib/chimeway_admin/components/status.ex"
      provides: "Centralized display-only lifecycle labels and status badge copy."
    - path: "chimeway_admin/test/chimeway_admin/components/status_test.exs"
      provides: "Presenter contract for lifecycle/status labels."
    - path: "chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex"
      provides: "Trace Detail rendering that uses lifecycle presenter labels with explanation context."
    - path: "chimeway_admin/lib/chimeway_admin/live/definitions_live.ex"
      provides: "DB-inferred Definitions page copy."
    - path: "chimeway_admin/test/chimeway_admin/live/definitions_live_test.exs"
      provides: "Rendered Definitions copy and forbidden-claim contract."
  key_links:
    - from: "TraceDetailLive"
      to: "ChimewayAdmin.Components.Status"
      via: "display-only lifecycle presenter"
      pattern: "lifecycle"
    - from: "Status lifecycle presenter"
      to: "Chimeway.Delivery status enum"
      via: "read existing atoms; do not mutate schema enum"
      pattern: "pending|dispatched|succeeded|failed|suppressed|cancelled|digested"
    - from: "DefinitionsLive"
      to: "Chimeway.Admin.definitions/1"
      via: "DB-inferred persisted key/version facts"
      pattern: "persisted Chimeway events and deliveries"
---

<objective>
Improve operator explanation copy while keeping durable Chimeway semantics unchanged.

Purpose: EXPL-01 and EXPL-02 are presenter and rendered-copy contracts. This plan centralizes lifecycle labels in the admin UI layer and aligns Definitions copy with the actual DB-inferred history model.

Output: Status presenter tests and implementation, Trace Detail/dashboard label integration, and Definitions rendered-copy tests that require persisted-history wording and forbid unimplemented code-registry/source-skew claims.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
Read these before implementing any task:

- `.planning/PROJECT.md`
- `.planning/ROADMAP.md`
- `.planning/REQUIREMENTS.md`
- `.planning/STATE.md`
- `.planning/METHODOLOGY.md`
- `.planning/phases/71-redaction-and-explainability-contracts/71-CONTEXT.md`
- `.planning/phases/71-redaction-and-explainability-contracts/71-RESEARCH.md`
- `.planning/phases/71-redaction-and-explainability-contracts/71-UI-SPEC.md`
- `.planning/phases/71-redaction-and-explainability-contracts/71-VALIDATION.md`
- `.planning/phases/71-redaction-and-explainability-contracts/71-01-SUMMARY.md`
- `lib/chimeway/delivery.ex`
- `lib/chimeway/traces.ex`
- `chimeway_admin/lib/chimeway_admin/components/status.ex`
- `chimeway_admin/lib/chimeway_admin/live/dashboard_live.ex`
- `chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex`
- `chimeway_admin/lib/chimeway_admin/live/definitions_live.ex`
- `test/chimeway/traces_test.exs`
- `chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs`

Implementation constraints:

- Depends on 71-01. Preserve the rendered privacy contracts from Plan 71-01 while changing labels/copy.
- Centralize lifecycle labels in `ChimewayAdmin.Components.Status` or a nearby presenter per D-09.
- Do not change `Chimeway.Delivery` durable status atoms, Ecto enum values, migrations, or core lifecycle semantics per D-09 and D-11.
- Distinguish Sent, Provider accepted, Delivered, Suppressed, Retryable failure, and Terminal failure using existing status, attempt outcome, error class, webhook/workflow/timeline facts, and suppression reason data per D-10.
- Use Delivered only when durable feedback facts prove delivery; a successful send attempt alone is Provider accepted per D-11.
- Keep Definitions as DB-inferred durable notification key/version history per D-12.
- Forbid rendered Definitions claims about code registry, source skew, notifier module discovery, module inventory, loaded modules, or source-code scanning per D-13.
- Do not add docs, `mix verify.admin`, CI parity, browser smoke, source-code registry detection, or package installs.

<interfaces>
Existing durable delivery statuses from `lib/chimeway/delivery.ex`:
- `:pending`
- `:dispatched`
- `:succeeded`
- `:failed`
- `:suppressed`
- `:cancelled`
- `:digested`

Existing Status component API:
- `status_badge(assigns)` accepts `:status` and optional `:label`.
- `metric(assigns)` accepts value, label, and tone.

Existing trace explanation facts:
- `%Chimeway.Traces.Explanation{status, last_attempt, suppression_reason, planning_reason, timeline}`
- Timeline details may include workflow/webhook facts projected by `Chimeway.Traces.explain_delivery/2`.
</interfaces>
</context>

<dependency_graph>
This plan depends on 71-01 because label and copy changes must preserve the privacy leak contracts created there, and because `TraceDetailLive` may already contain display-safe rendering adjustments from Plan 71-01.

Task 71-02-01 and Task 71-02-02 can be implemented in either order inside this plan, but both must pass before phase-level verification. They are grouped together because both are display/presenter contracts in `chimeway_admin`.
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Core durable status atoms to operator copy | UI translates durable facts into labels without mutating persistence semantics. |
| Provider/internal send facts to Delivered copy | Provider acceptance must not be overclaimed as final recipient delivery. |
| Persisted DB definition history to Definitions UI | UI must describe only facts inferred from Chimeway rows, not source-code inventory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-71-03 | Repudiation / Integrity | Lifecycle label presenter | mitigate | Presenter tests require distinct Sent, Provider accepted, Delivered, Suppressed, Retryable failure, and Terminal failure labels from existing facts per D-10. |
| T-71-04 | Repudiation | Delivered overclaim | mitigate | Delivered label requires durable feedback/timeline evidence; `:succeeded` attempt alone maps to Provider accepted per D-11. |
| T-71-05 | Repudiation | Definitions copy | mitigate | Rendered Definitions tests require DB-inferred persisted-history language and forbid code-registry/source-skew/module-discovery claims per D-12/D-13. |
| T-71-02-SC | Tampering | Package manager installs | accept | No dependency install is planned; RESEARCH records no new packages. |
</threat_model>

<tasks>

<task type="auto" id="71-02-01" tdd="true">
  <name>Task 71-02-01: Centralize Lifecycle Status Labels</name>
  <files>chimeway_admin/lib/chimeway_admin/components/status.ex, chimeway_admin/lib/chimeway_admin/live/dashboard_live.ex, chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex, chimeway_admin/test/chimeway_admin/components/status_test.exs, chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs</files>
  <read_first>
    .planning/phases/71-redaction-and-explainability-contracts/71-CONTEXT.md
    .planning/phases/71-redaction-and-explainability-contracts/71-RESEARCH.md
    .planning/phases/71-redaction-and-explainability-contracts/71-UI-SPEC.md
    .planning/phases/71-redaction-and-explainability-contracts/71-VALIDATION.md
    lib/chimeway/delivery.ex
    lib/chimeway/traces.ex
    chimeway_admin/lib/chimeway_admin/components/status.ex
    chimeway_admin/lib/chimeway_admin/live/dashboard_live.ex
    chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex
    test/chimeway/traces_test.exs
    chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs
  </read_first>
  <behavior>
    - `:dispatched` or an internal-send-only fact renders `Sent`.
    - `:succeeded` with a successful last attempt but without durable delivery feedback renders `Provider accepted`.
    - Durable delivery feedback/timeline evidence renders `Delivered`.
    - `:suppressed` plus suppression reason renders `Suppressed`.
    - Retry-eligible failure facts render `Retryable failure`.
    - Cancelled/exhausted/permanent/bounced terminal facts render `Terminal failure`.
    - `lib/chimeway/delivery.ex` status enum values remain unchanged.
  </behavior>
  <action>
    Write `chimeway_admin/test/chimeway_admin/components/status_test.exs` first. Test a display-only presenter in or near `ChimewayAdmin.Components.Status` for the six lifecycle labels required by EXPL-01 and D-10: `Sent`, `Provider accepted`, `Delivered`, `Suppressed`, `Retryable failure`, and `Terminal failure`.

    Implement the presenter in `chimeway_admin/lib/chimeway_admin/components/status.ex` unless an adjacent private presenter module is clearly simpler. Prefer a public function such as `lifecycle_label/1` returning a map with at least `:label`, `:tone`, and `:status`/`:normalized` so `status_badge/1` and LiveViews can reuse the same contract. Keep `status_badge/1` backward compatible with its existing `:status` and optional `:label` attrs.

    Use only existing facts per D-10: delivery status atoms/strings, explanation maps/structs, last attempt outcome/error class, suppression reason, and timeline/workflow facts already projected by `Chimeway.Traces.explain_delivery/2`. Reserve `Delivered` for durable feedback evidence in the explanation timeline, such as workflow/webhook facts that explicitly prove delivery. A successful attempt or `:succeeded` status without such evidence must be `Provider accepted` per D-11.

    Update `TraceDetailLive` to show the lifecycle presenter label for the current state and, where useful, last-attempt explanation text. Update `DashboardLive` so the `succeeded` metric is labeled `Provider accepted` rather than implying recipient delivery. Existing row badges that only receive raw status should use the presenter's conservative default: `:succeeded` means `Provider accepted`, not `Delivered`.

    Add rendered assertions in `chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs` for the dashboard metric label and at least one trace/status render path that proves `Provider accepted` appears while `Delivered` is absent without durable delivery feedback. Do not add browser screenshots or Phase 72 smoke tests.
  </action>
  <verify>
    <automated>cd chimeway_admin && mix test test/chimeway_admin/components/status_test.exs test/chimeway_admin/live/trace_search_live_test.exs --warnings-as-errors</automated>
    <automated>rg "values: \\[:pending, :dispatched, :succeeded, :failed, :suppressed, :cancelled, :digested\\]" lib/chimeway/delivery.ex</automated>
  </verify>
  <acceptance_criteria>
    - Lifecycle presenter tests cover Sent, Provider accepted, Delivered, Suppressed, Retryable failure, and Terminal failure.
    - Provider acceptance is not labeled Delivered without durable feedback facts.
    - Trace Detail uses the presenter for current state copy.
    - Dashboard does not label the raw `succeeded` metric as Delivered.
    - `lib/chimeway/delivery.ex` status enum values are unchanged.
    - Covers EXPL-01 and D-09, D-10, and D-11.
  </acceptance_criteria>
  <done>Lifecycle/status copy is centralized in the admin presenter layer and distinguishes required states without core atom churn.</done>
</task>

<task type="auto" id="71-02-02" tdd="true">
  <name>Task 71-02-02: Lock Definitions DB-Inferred Copy</name>
  <files>chimeway_admin/lib/chimeway_admin/live/definitions_live.ex, chimeway_admin/test/chimeway_admin/live/definitions_live_test.exs</files>
  <read_first>
    .planning/phases/71-redaction-and-explainability-contracts/71-CONTEXT.md
    .planning/phases/71-redaction-and-explainability-contracts/71-RESEARCH.md
    .planning/phases/71-redaction-and-explainability-contracts/71-UI-SPEC.md
    .planning/phases/71-redaction-and-explainability-contracts/71-VALIDATION.md
    lib/chimeway/admin.ex
    chimeway_admin/lib/chimeway_admin/live/definitions_live.ex
    chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs
  </read_first>
  <behavior>
    - Rendered Definitions copy includes `Durable notification keys and versions inferred from persisted Chimeway events and deliveries.`
    - Rendered Definitions copy includes `Definitions seen in this app`.
    - Empty state copy says persisted notification keys and versions appear after Chimeway records events or deliveries.
    - Rendered Definitions HTML does not contain code registry, source skew, source-code skew, notifier module discovery, module inventory, loaded modules, or source code scan claims.
  </behavior>
  <action>
    Create `chimeway_admin/test/chimeway_admin/live/definitions_live_test.exs` using `ChimewayAdmin.LiveViewCase` and existing isolated LiveView patterns. Tests must mount `DefinitionsLive`, assert the positive DB-inferred copy from the UI-SPEC, assert persisted definition facts render from `Chimeway.Admin.definitions/1`, and assert forbidden overclaim phrases are absent from the rendered HTML per D-13.

    Update `DefinitionsLive` rendered copy per D-12. Replace `registry` wording with the exact page description `Durable notification keys and versions inferred from persisted Chimeway events and deliveries.` Add or preserve the section label `Definitions seen in this app`. If the definitions list is empty, render the UI-SPEC empty state: title `No definitions seen` and body `Persisted notification keys and versions will appear after Chimeway records events or deliveries.`

    Keep `Chimeway.Admin.definitions/1` as the DB-inferred source of truth. Do not add source-code scanning, code registry comparison, notifier module discovery, module inventory, loaded-module inspection, or skew detection logic. This phase only makes the rendered claim honest.
  </action>
  <verify>
    <automated>cd chimeway_admin && mix test test/chimeway_admin/live/definitions_live_test.exs test/chimeway_admin/live/trace_search_live_test.exs --warnings-as-errors</automated>
    <automated>rg "Durable notification keys and versions inferred from persisted Chimeway events and deliveries|Definitions seen in this app" chimeway_admin/lib/chimeway_admin/live/definitions_live.ex chimeway_admin/test/chimeway_admin/live/definitions_live_test.exs</automated>
    <automated>rg -i "code registry|source skew|source-code skew|notifier module discovery|module inventory|loaded modules|source code scan" chimeway_admin/lib/chimeway_admin/live/definitions_live.ex; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Definitions rendered copy states DB-inferred persisted Chimeway event/delivery history.
    - Rendered copy does not use registry/skew/module-discovery/source-code-scan claims.
    - Empty Definitions UI has the approved persisted-history empty state.
    - No core registry, notifier module discovery, source scanning, docs, verify alias, CI parity, or browser smoke work is added.
    - Covers EXPL-02 and D-12 and D-13.
  </acceptance_criteria>
  <done>Definitions copy is honest about persisted DB-inferred history and rendered tests forbid unimplemented registry/skew claims.</done>
</task>

</tasks>

<verification>
Run each task command after its task, then run this Phase 71 Plan 02 gate from `71-VALIDATION.md`:

`cd chimeway_admin && mix test test/chimeway_admin/components/status_test.exs test/chimeway_admin/live/definitions_live_test.exs test/chimeway_admin/live/trace_search_live_test.exs --warnings-as-errors`

After both Phase 71 plans complete, run the wave gate from `71-VALIDATION.md`:

`mix test test/chimeway/admin_test.exs test/chimeway/traces_test.exs --warnings-as-errors && cd chimeway_admin && mix test --warnings-as-errors`

Do not require the Phase 72 `mix verify.admin` alias, CI parity, docs gate, or browser smoke.
</verification>

<success_criteria>
- EXPL-01: Status/presenter tests prove Sent, Provider accepted, Delivered, Suppressed, Retryable failure, and Terminal failure labels from existing durable facts.
- EXPL-01: Trace/detail/status rendered copy does not call provider acceptance Delivered unless durable feedback proves delivery.
- EXPL-01: Durable core status atoms and schema enum values remain unchanged.
- EXPL-02: Definitions rendered copy says DB-inferred persisted Chimeway event/delivery history.
- EXPL-02: Definitions rendered copy forbids code registry, source skew, notifier module discovery, module inventory, loaded modules, and source-code scanning claims.
- Phase 72 docs, verify alias, CI parity, and browser-smoke work remain untouched.
</success_criteria>

<output>
Create `.planning/phases/71-redaction-and-explainability-contracts/71-02-SUMMARY.md` when done.
</output>
