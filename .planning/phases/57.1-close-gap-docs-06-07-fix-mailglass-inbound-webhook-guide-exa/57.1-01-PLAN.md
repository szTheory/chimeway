---
phase: 57.1-close-gap-docs-06-07-fix-mailglass-inbound-webhook-guide-exa
plan: 01
type: execute
wave: 1
depends_on: [57-02]
files_modified:
  - guides/introduction/mailglass-integration.md
requirements: [DOCS-06]
must_haves:
  truths:
    - "Section 6 webhook example matches Chimeway.Webhooks.process/4 API (adapter module, raw_body binary, header list, config keyword)"
    - "Webhook error responses use generic status bodies (401 for :unauthorized, 500 otherwise) — no inspect(reason) leakage"
    - "Optional raw-body iolist flattening note aligns with DemoHostWeb.WebhooksController (IN-02)"
  artifacts:
    - path: "guides/introduction/mailglass-integration.md"
      provides: "Correct inbound webhook adoption example (WR-01, WR-02 closure)"
      contains: "Chimeway.Webhooks.process(Chimeway.Adapters.Mailglass"
    - path: "examples/chimeway_demo_host/lib/demo_host_web/controllers/webhooks_controller.ex"
      provides: "Reference implementation (read-only)"
      contains: "Chimeway.Webhooks.process(adapter_module, raw_body, headers, config)"
  key_links:
    - from: "guides/introduction/mailglass-integration.md"
      to: "examples/chimeway_demo_host/lib/demo_host_web/controllers/webhooks_controller.ex"
      via: "Section 6 cross-link to reference controller"
      pattern: "WebhooksController"
    - from: "guides/introduction/mailglass-integration.md"
      to: "lib/chimeway/webhooks.ex"
      via: "process/4 signature"
      pattern: "conn.req_headers"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-57.1-01 | Adopter copies wrong Webhooks.process/4 arity/order from guide | high | Section 6 MUST use adapter module atom, raw_body binary, conn.req_headers list, config keyword (WR-01) |
| T-57.1-02 | Webhook error branch leaks internal reason tuples to provider | medium | Replace inspect(reason) with fixed 401/500 bodies per demo host (WR-02) |
| T-57.1-03 | Chunked body_reader passes iolist to HMAC verify | low | Document Enum.reverse + IO.iodata_to_binary pattern or one-line Pitfall 4 note (IN-02) |
</threat_model>

<objective>
Fix DOCS-06 Section 6 inbound webhook example in `guides/introduction/mailglass-integration.md` to match the Phase 33 host-mount contract and `DemoHostWeb.WebhooksController` reference implementation.

Purpose: Close v1.8 audit gap WR-01/WR-02 so adopters can wire inbound Mailglass feedback without runtime FunctionClauseError or error-body leakage.

Output: Corrected Section 6 code block and prose; cross-link to reference controller.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-docs-release-gates/57-REVIEW.md
@.planning/milestones/v1.8-MILESTONE-AUDIT.md
@guides/introduction/mailglass-integration.md
@lib/chimeway/webhooks.ex
@examples/chimeway_demo_host/lib/demo_host_web/controllers/webhooks_controller.ex
@guides/recipes/feedback-escalation-workflow.md
</context>

<tasks>

<task id="57.1-01-01" type="execute">
<title>Replace Section 6 webhook example with correct Webhooks.process/4 call</title>

<read_first>
- guides/introduction/mailglass-integration.md (Section 6, lines ~167-195)
- lib/chimeway/webhooks.ex:28 (process/4 spec and implementation)
- examples/chimeway_demo_host/lib/demo_host_web/controllers/webhooks_controller.ex
- .planning/phases/57-docs-release-gates/57-REVIEW.md (WR-01)
</read_first>

<action>
In `guides/introduction/mailglass-integration.md` Section 6, replace the incorrect code block (lines ~173-190) with a demo-host-aligned example:

1. **Remove wrong patterns entirely:**
   - First arg `"mailglass"` string literal
   - Second arg `conn.params`
   - Third arg `raw_body` with fourth `headers: headers` keyword option
   - `headers = Map.new(conn.req_headers)`

2. **Use correct API shape** per `Chimeway.Webhooks.process(adapter_module, raw_body, headers, config)`:
   - `adapter_module = Chimeway.Adapters.Mailglass` (or equivalent module binding)
   - `raw_body` as binary — include the Pitfall 4 / T-33-RAWBODY flattening snippet from demo host:
     ```elixir
     raw_body =
       conn.assigns
       |> Map.get(:raw_body, [])
       |> Enum.reverse()
       |> IO.iodata_to_binary()
     ```
   - `headers = conn.req_headers` (list, NOT Map.new)
   - `config = Application.get_env(:my_app, :chimeway_webhook_config, [])` (host-specific app/env name is fine; document that config is request-time keyword list)

3. **Call:** `Chimeway.Webhooks.process(adapter_module, raw_body, headers, config)`

4. **Add one prose sentence** after the code block noting that hosts using a custom `:body_reader` must cache raw bytes in `conn.assigns[:raw_body]` before parsers consume the body (point to demo host `CacheBodyReader` / endpoint config if a short cross-link fits; otherwise reference `DemoHostWeb.WebhooksController` moduledoc).

5. **Cross-link** `examples/chimeway_demo_host/lib/demo_host_web/controllers/webhooks_controller.ex` as the runnable reference controller (relative path from guide or "demo host WebhooksController" with repo path in prose).
</action>

<verify>
grep -q 'Chimeway.Webhooks.process' guides/introduction/mailglass-integration.md && \
grep -q 'Chimeway.Adapters.Mailglass' guides/introduction/mailglass-integration.md && \
grep -q 'conn.req_headers' guides/introduction/mailglass-integration.md && \
! grep -q 'process("mailglass"' guides/introduction/mailglass-integration.md && \
! grep -q 'conn.params' guides/introduction/mailglass-integration.md && \
! grep -q 'headers: headers' guides/introduction/mailglass-integration.md
</verify>

<acceptance_criteria>
- Section 6 example calls `Chimeway.Webhooks.process` with adapter module, raw_body binary, header list, config keyword (WR-01)
- Example does NOT contain `"mailglass"` string as first process arg, `conn.params`, or `headers:` keyword in process call
- Example uses `conn.req_headers` (not Map.new)
- Raw-body iolist flattening pattern present OR explicit one-line Pitfall 4 note (IN-02)
- Reference controller cross-linked
</acceptance_criteria>
</task>

<task id="57.1-01-02" type="execute">
<title>Fix webhook error response mapping (no inspect leakage)</title>

<read_first>
- guides/introduction/mailglass-integration.md (Section 6 error branch)
- examples/chimeway_demo_host/lib/demo_host_web/controllers/webhooks_controller.ex:36-47
- .planning/phases/57-docs-release-gates/57-REVIEW.md (WR-02)
</read_first>

<action>
In the same Section 6 code block, replace the single `{:error, reason}` branch that uses `send_resp(conn, 422, inspect(reason))` with demo-host status mapping:

```elixir
{:ok, _ingress} ->
  send_resp(conn, 200, "OK")

{:error, :unauthorized} ->
  send_resp(conn, 401, "Unauthorized")

{:error, _other} ->
  send_resp(conn, 500, "Internal Server Error")
```

Add one prose sentence: log `reason` server-side only; never return internal error tuples to the webhook provider (Phase 33 D-03). Optionally note hosts MAY use 400/422 for observability but MUST use non-2xx for library errors so providers retry.
</action>

<verify>
grep -q ':unauthorized' guides/introduction/mailglass-integration.md && \
grep -q '401' guides/introduction/mailglass-integration.md && \
! grep -q 'inspect(reason)' guides/introduction/mailglass-integration.md
</verify>

<acceptance_criteria>
- Error branch maps `:unauthorized` → 401 with constant body
- Non-unauthorized errors → 500 (or documented non-2xx) with constant body — no `inspect(reason)` (WR-02)
- Prose warns against leaking internal errors to provider
</acceptance_criteria>
</task>

</tasks>

<verification>
- Read Section 6 end-to-end; compare call shape to lib/chimeway/webhooks.ex:28 and DemoHostWeb.WebhooksController
- `mix test test/chimeway/doc_contract_test.exs` — existing DOCS-07 tests should still pass (shape lock added in plan 02)
</verification>

<success_criteria>
- DOCS-06 partial gap closed: Section 6 is copy-paste-safe for inbound webhook wiring
- WR-01 and WR-02 findings addressed in guide
</success_criteria>
