# Phase 21.1: rendering-durability-and-preview-hardening - Context

**Gathered:** 2026-04-28 (assumptions mode + focused subagent research)
**Status:** Ready for planning

<domain>
## Phase Boundary

Fix the three urgent rendering hardening gaps discovered in the Phase 21 review without reopening
the broader TMPL-01/TMPL-02/TMPL-03 architecture. This phase is limited to:

1. removing planner-time re-entry into `Notifier.rendering/2` so render identity stays durable in
   practice;
2. hardening `preview_rendering/3` so invalid/unloaded notifiers return tagged errors instead of
   crashing;
3. replacing `mix preview.rendering` code-eval inputs with safe, data-only developer input paths.

Hosted template workflows, broader preview UIs, new rendering channels, and general rendering
redesign remain out of scope.

</domain>

<decisions>
## Implementation Decisions

### Phase scope and recommendation posture
- **D-01:** Phase 21.1 should stay tightly scoped to the three concrete review findings from
  `21-REVIEW.md`; planning should not reopen the larger rendering-contract architecture from Phase
  21.
- **D-02:** Planning for this phase should assume the recommendation style is already decided:
  research first, converge on one coherent architecture, and escalate only if a choice changes the
  public product model or conflicts with a locked project principle.

### Durable rendering declaration
- **D-03:** Chimeway should persist the normalized rendering declaration at trigger time on
  `Notification`, not only sanitized `render_assigns`. The durable declaration should include the
  per-channel render identity needed later by planning, for example a `render_channels` map keyed by
  channel with `render_key` and `render_version`.
- **D-04:** `DeliveryPlanning` must stop calling `Notifier.rendering/2` or
  `Notifier.resolve_rendering/3` during planning. Planning should read the persisted declaration and
  call `Rendering.render_delivery/4` only.
- **D-05:** Chimeway should keep the current lifecycle split: `Notification` stores the reusable
  durable declaration, `Delivery` stores the per-channel rendered execution artifact
  (`render_key`, `render_version`, `render_data`), and adapters remain dumb transport seams.
- **D-06:** Chimeway should reject the “persist minimal extra recipient fields and keep
  recomputing `rendering/2` later” approach. It is notifier-specific, easy to regress, and violates
  least-surprise durability.
- **D-07:** Chimeway should reject the “persist fully rendered output for every channel at trigger
  time” approach as the primary design. It widens the sensitive-data surface, bloats durable rows,
  and makes `Notification` too execution-aware.

### Preview API contract
- **D-08:** `Chimeway.preview_rendering/3` should remain the canonical non-bang public preview API
  and return tagged `{:ok, preview} | {:error, reason}` tuples for expected boundary failures.
- **D-09:** `preview_rendering/3` should validate notifier modules up front before resolving
  rendering and should return stable tagged reasons such as `:notifier_not_loaded` rather than
  bubbling `UndefinedFunctionError` for unloaded modules.
- **D-10:** Phase 21.1 should not add blanket exception rescue around the preview path. Real
  rendering bugs should still surface during tests and development; only the expected boundary
  failure of invalid/unloaded notifiers should be normalized.
- **D-11:** A future bang variant such as `preview_rendering!/3` is compatible with Elixir
  conventions, but it is optional and not required for Phase 21.1.

### Preview CLI safety and DX
- **D-12:** `mix preview.rendering` should stop using `Code.eval_string/1` and `Code.eval_file/1`
  for `params` and `recipient` input.
- **D-13:** The canonical rich input format for the Mix task should be JSON, with both inline JSON
  and file-based JSON variants. This matches normal Mix-task ergonomics, stays data-only, and keeps
  the task a thin wrapper over `Chimeway.preview_rendering/3`.
- **D-14:** The Mix task should add shallow repeated key-value flags as a convenience layer for
  small examples, while keeping file-based JSON as the preferred path for anything non-trivial or
  sensitive.
- **D-15:** If developers truly need executable Elixir for advanced preview experimentation, the
  documented escape hatch should be `mix run -e` or `iex -S mix`, not hidden code evaluation inside
  `mix preview.rendering`.

### Testing and developer ergonomics
- **D-16:** Phase 21.1 should add regression coverage proving planner-time rendering no longer
  depends on recipient fields that were available only at trigger time.
- **D-17:** Phase 21.1 should add a contract test proving planning/dispatch do not re-enter
  `Notifier.rendering/2` after trigger-time declaration persistence.
- **D-18:** Phase 21.1 should extend preview tests to cover invalid/unloaded notifier handling plus
  JSON/file parsing behavior in the Mix task, while preserving preview parity with the production
  rendering pipeline.
- **D-19:** Preview and render docs should optimize for least surprise and fast inspection:
  canonical library API first, then safe CLI examples, then optional host-app browser-style preview
  integrations if the host wants them.

### Explainability guardrails
- **D-20:** This phase should preserve Chimeway’s explainability posture while hardening rendering:
  durable render identity and machine-readable failure reasons should be queryable, but rendered
  bodies and sensitive payload details should remain excluded from operator-facing traces unless
  explicitly designed otherwise.
- **D-21:** If planning touches any rendering fallback behavior, hidden fallbacks should not become
  silent durable truth. Any fallback that affects the rendered artifact should either be disallowed
  or recorded explicitly enough that operators and developers can explain what happened later.

### the agent's Discretion
- Exact durable field/module naming for the persisted declaration, for example `render_channels`
  versus `render_declaration`, provided the stored shape remains explicit and channel-scoped.
- Exact JSON flag names and whether shallow convenience flags merge over file/inline JSON, provided
  the task stays data-only and easy to explain.
- Whether to keep `Notifier.validate_module!/1` as-is for Phase 21.1 or to stage a later cleanup
  around its non-bang semantics, provided the preview API contract stays stable.

</decisions>

<specifics>
## Specific Ideas

- Follow the Swoosh model for developer preview ergonomics: one real render path, then dev/test
  inspection surfaces layered on top.
- Keep Laravel’s “preview the actual message in the browser” idea, but do not copy its class-name
  durable identity defaults or queued-object recomputation posture.
- Learn from Noticed’s explicit warning that renaming notifier classes affects persisted rows; that
  is exactly the footgun Chimeway should keep avoiding with string-based durable identities.
- Treat GitHub-style “reason” surfaces as the bar for operator clarity: rendering and preview
  failures should be explicit, stable, and easy to inspect without reverse-engineering code.

</specifics>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Milestone and phase scope
- `.planning/ROADMAP.md` — Phase 21.1 insertion and dependency on Phase 21.
- `.planning/PROJECT.md` — stable identity, local-first ownership, explainability, and DX posture.
- `.planning/REQUIREMENTS.md` — `TMPL-01`, `TMPL-02`, and `TMPL-03` remain the governing rendering
  requirements; 21.1 is a hardening follow-up, not a new feature family.
- `.planning/STATE.md` — records the inserted urgent follow-up and carried-forward rendering
  decisions.

### Review and prior phase context
- `.planning/phases/21-template-versioning-rendering-contracts/21-REVIEW.md` — the three concrete
  review findings this phase exists to fix.
- `.planning/phases/21-template-versioning-rendering-contracts/21-CONTEXT.md` — locked Phase 21
  architecture around durable render identity, pure preview, and dumb adapter seams.
- `.planning/phases/20-digest-emission-explainability/20-CONTEXT.md` — payload-safe
  explainability posture that rendering hardening must preserve.
- `.planning/phases/18-scheduled-resume-deferred-dispatch/18-CONTEXT.md` — durable row mutation and
  canonical lifecycle continuity posture reused here.
- `.planning/METHODOLOGY.md` — project-level instruction to research first, converge on one
  coherent recommendation set, optimize for least surprise, and escalate only high-impact choices.

### Existing rendering and preview seams
- `lib/chimeway/trigger.ex` — current trigger-time rendering resolution and persistence hook.
- `lib/chimeway/notifications/notification.ex` — current durable notification schema, including
  `render_assigns`.
- `lib/chimeway/delivery_planning.ex` — current planner-time re-entry into rendering that 21.1
  must remove.
- `lib/chimeway/rendering.ex` — normalized rendering declaration and channel render contract.
- `lib/chimeway/rendering/preview.ex` — pure preview path to harden.
- `lib/chimeway/notifier.ex` — notifier validation and rendering contract entrypoint.
- `lib/chimeway/delivery.ex` — durable per-channel rendered artifact fields.
- `lib/chimeway/deliveries.ex` — canonical delivery persistence helpers used by planning.
- `lib/mix/tasks/preview_rendering.ex` — unsafe code-eval preview task input path to replace.

### Existing tests that lock behavior
- `test/chimeway/rendering/preview_pipeline_test.exs` — preview parity and Mix-task coverage to
  extend.
- `test/chimeway/rendering/render_identity_integration_test.exs` — current durable rendering
  identity and render-assign persistence behavior.
- `test/chimeway/orchestration/delivery_planning_test.exs` — planning behavior and canonical-row
  reuse tests to extend for no-reentry guarantees.
- `test/chimeway/integration/delivery_lifecycle_test.exs` — end-to-end proof that dispatch uses
  pre-rendered content and traces stay payload-safe.

### Ecosystem references that shaped the recommendation
- `https://hexdocs.pm/elixir/naming-conventions.html` — bang/non-bang API conventions and least
  surprise public function design.
- `https://hexdocs.pm/elixir/Code.html` — confirms `Code.eval_*` is actual code execution, not a
  data parser.
- `https://hexdocs.pm/elixir/OptionParser.html` — idiomatic Mix-task flag parsing model.
- `https://hexdocs.pm/swoosh/Swoosh.Email.html` — explicit rendered email artifact fields.
- `https://hexdocs.pm/swoosh/Swoosh.Adapters.Local.html` — real-email preview ergonomics without
  provider delivery.
- `https://laravel.com/docs/12.x/notifications` — browser preview ergonomics and class-name
  identity caveats in a popular notification framework.
- `https://symfony.com/doc/current/mailer.html` — async template-context serialization caveats
  reinforcing durable declaration persistence.
- `https://github.com/excid3/noticed` — direct documentation of class-name persistence footguns in
  a popular notification library.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `Chimeway.Trigger.notifications_attrs/4`: already resolves rendering once at trigger time and is
  the natural place to persist the full normalized declaration.
- `Chimeway.Rendering.render_delivery/4`: already provides the explicit, validated per-channel
  render step the planner should reuse after reading persisted declaration data.
- `Chimeway.Rendering.Preview.preview/3`: already proves preview can stay pure and reuse the same
  renderer path as production.
- `Chimeway.Deliveries.apply_render_result/2`: already persists the canonical per-channel rendered
  artifact on `Delivery`.
- Existing preview/render integration tests already encode the parity story this phase should keep.

### Established Patterns
- Durable business truth belongs in Chimeway rows, not in queue state, runtime callbacks, or
  framework class names.
- Public APIs usually prefer tagged tuples for expected boundary failures, with optional bang
  variants when fail-fast raising semantics are desirable.
- Optional host-app preview integrations are good; unsafe convenience in the core library is not.
- Explainability and DX prefer explicit, queryable facts over hidden recomputation or silent
  fallback behavior.

### Integration Points
- This phase connects trigger-time declaration persistence, delivery planning, preview API error
  handling, and Mix-task input parsing into one coherent hardening pass.
- Render-declaration persistence must line up with existing `Notification` and `Delivery` schema
  boundaries rather than moving rendering truth into `planning_context`.
- Safe preview CLI parsing should remain a wrapper concern only; the canonical semantics stay in
  `Chimeway.preview_rendering/3`.

</code_context>

<deferred>
## Deferred Ideas

- Browser-based or LiveDashboard preview UI in the core library.
- Broad rendering contract redesign beyond the three concrete review findings.
- New rendering channels, workflow/template editors, or hosted-style template lifecycle tooling.
- A restricted Elixir-literal input mode for the Mix task; JSON/file input is the default for this
  phase unless planning finds a compelling narrow safe parser worth adding.

</deferred>

---

*Phase: 21.1-rendering-durability-and-preview-hardening*
*Context gathered: 2026-04-28*
