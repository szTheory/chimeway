---
phase: 21.1-rendering-durability-and-preview-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/rendering/preview.ex
  - lib/mix/tasks/preview_rendering.ex
  - test/chimeway/rendering/preview_pipeline_test.exs
autonomous: true
requirements: [TMPL-03, TMPL-02]
must_haves:
  truths:
    - "Preview callers get tagged tuple failures for invalid or unloaded notifiers instead of a crash."
    - "The Mix preview task accepts data-only JSON inputs and does not execute developer-provided Elixir."
    - "Preview output still uses the same production render path and render identity as delivery planning."
  artifacts:
    - path: "lib/chimeway/rendering/preview.ex"
      provides: "Canonical preview API boundary normalization"
    - path: "lib/mix/tasks/preview_rendering.ex"
      provides: "Safe JSON-only CLI wrapper over preview_rendering/3"
    - path: "test/chimeway/rendering/preview_pipeline_test.exs"
      provides: "Tagged-failure and JSON parsing regression coverage"
  key_links:
    - from: "lib/chimeway/rendering/preview.ex"
      to: "lib/chimeway/notifier.ex"
      via: "preview validates notifier modules before resolving rendering"
      pattern: "validate_module!"
    - from: "lib/mix/tasks/preview_rendering.ex"
      to: "lib/chimeway/rendering/preview.ex"
      via: "Mix task parses JSON then delegates to Chimeway.preview_rendering/3"
      pattern: "preview_rendering"
---

<objective>
Harden the preview API boundary and replace executable Mix-task inputs with safe JSON-based parsing.

Purpose: implement D-08 through D-15, D-18, and D-19 so preview remains production-parity, non-persistent, and least-surprise for developers.
Output: tagged preview boundary errors, JSON/file input parsing with shallow convenience flags, removal of `Code.eval_*` from the Mix task, and preview-path regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/21.1-rendering-durability-and-preview-hardening/21.1-CONTEXT.md
@.planning/phases/21.1-rendering-durability-and-preview-hardening/21.1-RESEARCH.md
@.planning/phases/21.1-rendering-durability-and-preview-hardening/21.1-PATTERNS.md
@.planning/phases/21.1-rendering-durability-and-preview-hardening/21.1-VALIDATION.md
@.planning/phases/21-template-versioning-rendering-contracts/21-REVIEW.md
@lib/chimeway/rendering/preview.ex
@lib/mix/tasks/preview_rendering.ex
@test/chimeway/rendering/preview_pipeline_test.exs

<interfaces>
From lib/chimeway/rendering/preview.ex:
```elixir
@spec preview(module(), map(), keyword()) :: {:ok, t()} | {:error, term()}
def preview(notifier, params, opts) when is_atom(notifier) and is_map(params) and is_list(opts) do
  with {:ok, recipient} <- fetch_required_option(opts, :recipient),
       {:ok, channel} <- fetch_required_channel(opts),
       {:ok, rendering} <- Notifier.resolve_rendering(notifier, params, recipient) do
```

From lib/mix/tasks/preview_rendering.ex:
```elixir
@switches [notifier: :string, params: :string, recipient: :string, channel: :string, help: :boolean]
@required_switches [:notifier, :params, :recipient, :channel]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize preview boundary failures for invalid and unloaded notifiers</name>
  <files>lib/chimeway/rendering/preview.ex, test/chimeway/rendering/preview_pipeline_test.exs</files>
  <read_first>.planning/phases/21.1-rendering-durability-and-preview-hardening/21.1-CONTEXT.md, .planning/phases/21-template-versioning-rendering-contracts/21-REVIEW.md, lib/chimeway/rendering/preview.ex, lib/chimeway/notifier.ex, test/chimeway/rendering/preview_pipeline_test.exs</read_first>
  <behavior>
    - Test 1: `Chimeway.preview_rendering/3` returns `{:error, :notifier_not_loaded}` for an unloaded notifier module instead of raising `UndefinedFunctionError`.
    - Test 2: `preview_rendering/3` still returns tagged errors for invalid channels or invalid payloads from the production renderer without blanket exception rescue.
    - Test 3: real rendering bugs continue to surface during tests because only the invalid/unloaded notifier boundary is normalized.
  </behavior>
  <action>Implement D-08, D-09, D-10, and D-18 in `Chimeway.Rendering.Preview`. Validate the notifier module before any rendering resolution by calling `Notifier.validate_module!(notifier)` at the start of the happy-path clause. Normalize the expected boundary failure for unloaded modules to the stable tagged reason `{:error, :notifier_not_loaded}` and keep the existing `{:error, {:invalid_preview_notifier, notifier}}` return for non-atom callers. Do not add `rescue` around the render pipeline; only the invalid/unloaded notifier boundary should be converted into a tuple. Extend `test/chimeway/rendering/preview_pipeline_test.exs` with exact regression cases named `test "returns notifier_not_loaded for unloaded notifier modules"` and `test "preview still surfaces renderer errors without blanket rescue"`.</action>
  <acceptance_criteria>
    - `rg -n 'validate_module!' lib/chimeway/rendering/preview.ex` returns a notifier validation call on the preview path.
    - `rg -n 'notifier_not_loaded' lib/chimeway/rendering/preview.ex test/chimeway/rendering/preview_pipeline_test.exs` returns matches in both the implementation and tests.
    - `rg -n 'rescue' lib/chimeway/rendering/preview.ex` returns no matches.
    - `rg -n 'returns notifier_not_loaded for unloaded notifier modules|preview still surfaces renderer errors without blanket rescue' test/chimeway/rendering/preview_pipeline_test.exs` returns both test names.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/rendering/preview_pipeline_test.exs --trace</automated>
  </verify>
  <done>Preview callers receive stable tagged failures for invalid or unloaded notifiers, while genuine rendering defects remain visible during development and tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace executable preview CLI inputs with JSON and shallow key-value flags</name>
  <files>lib/mix/tasks/preview_rendering.ex, test/chimeway/rendering/preview_pipeline_test.exs</files>
  <read_first>.planning/phases/21.1-rendering-durability-and-preview-hardening/21.1-CONTEXT.md, .planning/phases/21.1-rendering-durability-and-preview-hardening/21.1-RESEARCH.md, lib/mix/tasks/preview_rendering.ex, test/chimeway/rendering/preview_pipeline_test.exs</read_first>
  <behavior>
    - Test 1: the Mix task accepts `--params-json` and `--recipient-json` inline JSON, plus `--params-file` and `--recipient-file` JSON files, and delegates the resulting maps to `Chimeway.preview_rendering/3`.
    - Test 2: repeated `--param key=value` and `--recipient-field key=value` flags shallow-merge over file or inline JSON maps for small examples.
    - Test 3: the task contains no `Code.eval_string/1` or `Code.eval_file/1` calls and its help text points advanced executable experimentation to `mix run -e` or `iex -S mix`.
  </behavior>
  <action>Implement D-12, D-13, D-14, D-15, D-18, and D-19 in `Mix.Tasks.Preview.Rendering` with explicit JSON-first parsing. Replace the old `--params` and `--recipient` executable-input switches with `--params-json`, `--params-file`, `--recipient-json`, and `--recipient-file`, and add repeated `--param` plus `--recipient-field` switches that accept `key=value` pairs for shallow convenience overlays. Parse all JSON with `Jason.decode/1` using string keys only, reject non-map JSON values with a tagged error, and load file inputs as JSON files rather than `.exs` code. Apply merge order exactly as: file JSON base, inline JSON overlay, repeated key-value flags last. Update the module docs, `usage/0`, and error messages so the canonical path is `Chimeway.preview_rendering/3` first, then safe JSON CLI examples, and explicitly direct advanced executable experiments to `mix run -e` or `iex -S mix`. Extend `test/chimeway/rendering/preview_pipeline_test.exs` with exact cases named `test "mix preview.rendering parses inline JSON inputs"`, `test "mix preview.rendering parses JSON files"`, and `test "mix preview.rendering rejects executable input paths"`.</action>
  <acceptance_criteria>
    - `rg -n 'params-json|params-file|recipient-json|recipient-file|recipient-field|--param' lib/mix/tasks/preview_rendering.ex` returns all new switch names.
    - `rg -n 'Jason\\.decode' lib/mix/tasks/preview_rendering.ex` returns JSON decoding calls.
    - `rg -n 'Code\\.eval_string|Code\\.eval_file' lib/mix/tasks/preview_rendering.ex` returns no matches.
    - `rg -n 'mix run -e|iex -S mix' lib/mix/tasks/preview_rendering.ex` returns advanced escape-hatch guidance in docs or usage text.
    - `rg -n 'mix preview.rendering parses inline JSON inputs|mix preview.rendering parses JSON files|mix preview.rendering rejects executable input paths' test/chimeway/rendering/preview_pipeline_test.exs` returns all three test names.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/rendering/preview_pipeline_test.exs --trace</automated>
  </verify>
  <done>The Mix preview task is a data-only wrapper over the canonical preview API, supports safe JSON and shallow convenience flags, and no longer executes developer-provided Elixir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI input -> Mix task parser | Local command-line and file inputs cross into preview parsing |
| Mix task parser -> preview API | Parsed data is handed to the production preview surface |
| preview API -> developer-visible output | Render identity and payload data become inspectable output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.1-04 | E | `lib/mix/tasks/preview_rendering.ex` | mitigate | Remove `Code.eval_string/1` and `Code.eval_file/1`; accept only JSON plus shallow `key=value` overlays. |
| T-21.1-05 | D | `lib/chimeway/rendering/preview.ex` | mitigate | Validate notifier modules before calling rendering callbacks and normalize unloaded modules to `{:error, :notifier_not_loaded}`. |
| T-21.1-06 | I | `lib/mix/tasks/preview_rendering.ex` | mitigate | Keep help text and output focused on stable render identity and validated payload data, not raw executable input or provider details. |
</threat_model>

<verification>
- `mix test test/chimeway/rendering/preview_pipeline_test.exs --trace`
- `mix preview.rendering --help`
</verification>

<success_criteria>
- `preview_rendering/3` returns tagged boundary errors for unloaded notifiers instead of crashing.
- `mix preview.rendering` parses JSON or JSON files only, with shallow convenience flags for small examples.
- The preview CLI remains a thin wrapper over the canonical preview API and no longer evaluates Elixir input.
</success_criteria>

<output>
After completion, create `.planning/phases/21.1-rendering-durability-and-preview-hardening/21.1-02-SUMMARY.md`
</output>
