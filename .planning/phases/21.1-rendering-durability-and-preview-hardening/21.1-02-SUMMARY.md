---
phase: 21.1-rendering-durability-and-preview-hardening
plan: 02
subsystem: tooling
tags: [preview, validation, json, mix_task]

requires:
  - phase: 21.1-01-PLAN
    provides: Durable rendering declaration persistence
provides:
  - Validated notifier boundary for preview_rendering/3 returning tagged tuples
  - Safe JSON and file-based data parsing in mix preview.rendering, removing Code.eval_*
affects: [developers, operators]

tech-stack:
  added: []
  patterns: [Tagged tuple boundary failures, explicit JSON/OptionParser data extraction]

key-files:
  created: []
  modified:
    - lib/chimeway/rendering/preview.ex
    - lib/mix/tasks/preview_rendering.ex
    - test/chimeway/rendering/preview_pipeline_test.exs

key-decisions:
  - "Phase 21 local preview uses the same production rendering declaration and channel validation path as dispatch planning."
  - "The Mix task remains a convenience shell that parses local inputs, delegates to Chimeway.preview_rendering/3, and prints stable render identity plus validated payload data."

patterns-established:
  - "Public API expected errors: validate upfront and return tagged tuples like `{:error, :notifier_not_loaded}`"
  - "Safe CLI input parsing: use Jason.decode over Code.eval_string for accepting structured payload maps"

requirements-completed: [TMPL-03, TMPL-02]

duration: 15min
completed: 2026-04-28
---

# Phase 21.1 Plan 02: Rendering durability and preview hardening Summary

**Preview API hardened to return tagged boundary errors and Mix task safely parses JSON instead of executing Elixir code**

## Performance

- **Duration:** 15 min
- **Started:** 2026-04-28T20:50:00Z
- **Completed:** 2026-04-28T21:05:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- `Chimeway.preview_rendering/3` validates notifiers and returns stable tagged errors (e.g. `{:error, :notifier_not_loaded}`) instead of crashing on undefined modules.
- `mix preview.rendering` replaces executable code parsing (`Code.eval_string`/`Code.eval_file`) with explicit JSON and key-value shallow merging capabilities using standard data parsers (`Jason.decode` and `OptionParser`).
- Existing robust preview tests extended to cover boundary conditions and verify the updated Mix task flags for safely parsed outputs.

## Task Commits

Each task was committed atomically:

1. **Task 1: Normalize preview boundary failures for invalid and unloaded notifiers** - `e07ec28` (feat)
2. **Task 2: Replace executable preview CLI inputs with JSON and shallow key-value flags** - `295d495` (feat)

## Files Created/Modified
- `lib/chimeway/rendering/preview.ex` - Added `Notifier.validate_module!(notifier)` to normalize boundary failures.
- `lib/mix/tasks/preview_rendering.ex` - Rewritten to parse input data safely with `--params-json`, `--params-file`, `--recipient-json`, etc.
- `test/chimeway/rendering/preview_pipeline_test.exs` - Expanded test suites for unloaded/invalid notifiers and Mix task JSON parsing.

## Decisions Made
- Preview API boundaries shouldn't crash on standard error conditions; they now explicitly return tagged tuples to consumers.
- Command-line parsing avoids executable code evaluation to prevent unintended side-effects and follows standard flag extraction strategies.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Notifier maps defined in tests expected atom keys but JSON decoding provides string keys. Test fixtures updated to support either string or atom keys natively to handle JSON testing effectively without compromising original tests.

## Next Phase Readiness
- Rendering preview functionality is safely guarded.
- All urgent rendering durability/preview hardening work from the 21.1 scope is structurally complete and tests pass.

---
*Phase: 21.1-rendering-durability-and-preview-hardening*
*Completed: 2026-04-28*
