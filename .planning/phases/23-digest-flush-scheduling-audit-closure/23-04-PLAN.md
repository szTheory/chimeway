---
phase: 23-digest-flush-scheduling-audit-closure
plan: 04
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/dispatch/oban_worker.ex
  - test/chimeway/integration/delivery_lifecycle_test.exs
autonomous: true
requirements:
  - DIGEST-02
  - DIGEST-03
must_haves:
  truths:
    - "The Phase 23 final gate can run through the deferred-resume lifecycle path without `Chimeway.Dispatch.ObanWorker` crashing on a deferred policy result."
    - "A resumed canonical delivery row either dispatches successfully at perform time or converges through an explicit re-defer path on the same row instead of crashing."
    - "`MIX_ENV=test mix ci.test` returns green before the digest closure artifacts are allowed to claim completion."
  artifacts:
    - path: "lib/chimeway/dispatch/oban_worker.ex"
      provides: "Policy-result handling for resumed deliveries that preserves canonical row convergence instead of crashing on `{:defer, decision}`"
      min_lines: 40
    - path: "test/chimeway/integration/delivery_lifecycle_test.exs"
      provides: "Regression coverage for the resumed-delivery Oban worker path and the restored full-suite gate"
      min_lines: 140
  key_links:
    - from: "lib/chimeway/dispatch/oban_worker.ex"
      to: "lib/chimeway/policy.ex"
      via: "perform-time policy evaluation explicitly handles `{:defer, decision}` instead of assuming only proceed/suppress outcomes"
      pattern: "Policy.evaluate|\\{:defer"
    - from: "lib/chimeway/dispatch/oban_worker.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "the worker reuses canonical delivery-row helpers for any perform-time re-defer state update instead of inventing queue-owned state"
      pattern: "apply_planning_decision|resume"
    - from: "test/chimeway/integration/delivery_lifecycle_test.exs"
      to: "lib/chimeway/dispatch/oban_worker.ex"
      via: "the deferred-resume lifecycle test proves `perform_job(ObanWorker, %{delivery_id: ...})` no longer crashes"
      pattern: "perform_job\\(ObanWorker"
---

<objective>
Clear the red final gate by fixing the deferred-resume worker regression that currently crashes outside the digest code path but blocks truthful Phase 23 closure.

Purpose: Phase 23 cannot be closed while `MIX_ENV=test mix ci.test` still fails in the deferred-resume lifecycle path. This plan must repair the `ObanWorker` policy-result handling so resumed rows converge on the canonical delivery identity instead of raising a `CaseClauseError`.
Output: regression coverage for the resumed worker path, a worker implementation that handles perform-time deferred policy outcomes safely, and a green full-suite gate.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-VERIFICATION.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-REVIEW.md
@lib/chimeway/dispatch/oban_worker.ex
@lib/chimeway/deliveries.ex
@lib/chimeway/policy.ex
@lib/chimeway/policy/settings.ex
@test/chimeway/integration/delivery_lifecycle_test.exs

<interfaces>
From `lib/chimeway/dispatch/oban_worker.ex`:
```elixir
def perform(%Oban.Job{
      args: %{"delivery_id" => delivery_id},
      attempt: attempt,
      max_attempts: max_attempts
    })
```

From `lib/chimeway/policy.ex`:
```elixir
@spec evaluate(Chimeway.Delivery.t(), keyword()) ::
        {:ok, :proceed} | {:suppress, atom()} | {:defer, map()}
```

From `lib/chimeway/deliveries.ex`:
```elixir
def apply_planning_decision(%Delivery{} = delivery, decision) when is_map(decision)
def resume_deferred_delivery(delivery_id, opts) when is_binary(delivery_id) and is_list(opts)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the deferred-resume worker regression with failing integration coverage</name>
  <files>test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <read_first>
    - test/chimeway/integration/delivery_lifecycle_test.exs
    - lib/chimeway/dispatch/oban_worker.ex
    - lib/chimeway/policy.ex
    - lib/chimeway/policy/settings.ex
    - lib/chimeway/deliveries.ex
    - .planning/phases/23-digest-flush-scheduling-audit-closure/23-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: `perform_job(ObanWorker, %{delivery_id: resumed_delivery.id})` on the Phase 23 failing deferred-resume scenario exits cleanly instead of crashing on `{:defer, ...}`.
    - Test 2: the resumed canonical row either succeeds as the existing scenario expects or is explicitly re-deferred on the same delivery id with durable planning facts and zero duplicate rows.
    - Test 3: the regression is observable from the full-suite path, not only a private helper test.
  </behavior>
  <action>Extend `test/chimeway/integration/delivery_lifecycle_test.exs` around the existing `resume_deferred_delivery promotes the existing row to orchestration_state == :ready` scenario so it captures the exact worker failure mode from `23-VERIFICATION.md`. Assert that the Oban worker no longer raises on `{:defer, decision}` and that the canonical row reaches an explicit post-perform state on the same `delivery.id`. Keep the assertions tied to the existing lifecycle story rather than adding a detached unit-only reproduction.</action>
  <acceptance_criteria>
    - `test/chimeway/integration/delivery_lifecycle_test.exs` contains `perform_job(ObanWorker`
    - `test/chimeway/integration/delivery_lifecycle_test.exs` contains `resume_deferred_delivery`
    - `test/chimeway/integration/delivery_lifecycle_test.exs` contains `orchestration_state`
    - `mix test test/chimeway/integration/delivery_lifecycle_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/integration/delivery_lifecycle_test.exs --trace</automated>
  </verify>
  <done>The lifecycle regression is captured in a failing integration test before the worker code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize perform-time `{:defer, decision}` handling in `ObanWorker` and restore the final gate</name>
  <files>lib/chimeway/dispatch/oban_worker.ex, test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <read_first>
    - lib/chimeway/dispatch/oban_worker.ex
    - lib/chimeway/deliveries.ex
    - lib/chimeway/dispatch/oban.ex
    - lib/chimeway/dispatch/deferred_resume_worker.ex
    - lib/chimeway/policy.ex
    - test/chimeway/integration/delivery_lifecycle_test.exs
  </read_first>
  <behavior>
    - Test 1: `ObanWorker.handle_delivery/3` does not crash when policy returns `{:defer, decision}` for a resumed row.
    - Test 2: any perform-time re-defer updates the same delivery row through `Deliveries.apply_planning_decision/2` and reuses the dispatcher’s deferred scheduling seam instead of inventing queue-only state.
    - Test 3: the original resumed happy path remains green and `MIX_ENV=test mix ci.test` succeeds.
  </behavior>
  <action>Update `lib/chimeway/dispatch/oban_worker.ex` so `handle_delivery/3` treats `{:defer, decision}` as an explicit lifecycle branch instead of a missing case. Use canonical delivery-row helpers from `lib/chimeway/deliveries.ex` to persist any re-defer planning facts on the same row and, when the row remains pending/deferred, hand it back through the configured dispatcher so the existing deferred scheduling path owns the next resume attempt. Do not swallow the result with a bare `:ok`; the target state must be either a successful dispatch or a durable deferred row with a new `next_eligible_at`, never a crashed worker or a ready row stranded without follow-up scheduling.</action>
  <acceptance_criteria>
    - `lib/chimeway/dispatch/oban_worker.ex` contains `{:defer, decision}`
    - `lib/chimeway/dispatch/oban_worker.ex` contains `apply_planning_decision`
    - `lib/chimeway/dispatch/oban_worker.ex` contains `dispatch_delivery`
    - `lib/chimeway/dispatch/oban_worker.ex` does not rely on a two-clause `case Policy.evaluate`
    - `mix test test/chimeway/integration/delivery_lifecycle_test.exs --trace` exits 0
    - `MIX_ENV=test mix ci.test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix ci.test</automated>
  </verify>
  <done>The full suite is green again and resumed deliveries no longer crash when policy re-evaluates inside the worker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deferred canonical row -> Oban perform | A resumed delivery re-enters policy evaluation and must not lose lifecycle convergence when the policy result changes |
| worker retry loop -> delivery row state | The worker must persist the next state on the canonical row instead of relying on implicit queue semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-10 | D | `lib/chimeway/dispatch/oban_worker.ex` | mitigate | Add an explicit `{:defer, decision}` branch so a perform-time deferral cannot crash the worker and fail the final gate. |
| T-23-11 | T | deferred-resume lifecycle state | mitigate | Persist any new deferred planning facts through `Deliveries.apply_planning_decision/2` on the existing row rather than inventing queue-owned state or replacement rows. |
| T-23-12 | R | verification gate | mitigate | Keep the regression in the integration suite and require `MIX_ENV=test mix ci.test` to pass before audit artifacts can be realigned. |
</threat_model>

<verification>
Run `mix test test/chimeway/integration/delivery_lifecycle_test.exs --trace`, then run `MIX_ENV=test mix ci.test`.
</verification>

<success_criteria>
The deferred-resume lifecycle path no longer crashes in `ObanWorker`, the canonical row converges explicitly, and the Phase 23 final gate is green again.
</success_criteria>

<output>
After completion, create `.planning/phases/23-digest-flush-scheduling-audit-closure/23-04-SUMMARY.md`
</output>
