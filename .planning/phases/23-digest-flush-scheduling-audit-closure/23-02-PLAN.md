---
phase: 23-digest-flush-scheduling-audit-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260428230000_add_orchestration_snapshot_to_chimeway_notifications.exs
  - lib/chimeway/notifications/notification.ex
  - lib/chimeway/trigger.ex
  - lib/chimeway/notifier.ex
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/deliveries.ex
  - test/chimeway/orchestration/recovery_test.exs
  - test/chimeway/trigger_pipeline_test.exs
autonomous: true
requirements:
  - DIGEST-02
  - DIGEST-03
must_haves:
  truths:
    - "Recovered events that originally belonged in a digest replay the same durable orchestration semantics instead of degrading to immediate send."
    - "The orchestration snapshot needed for recovery is persisted on the notification row at trigger time, not reconstructed from notifier callbacks or queue state."
    - "Recovery explainability stays intact because replayed rows preserve digest hold state and digest-key planning context."
  artifacts:
    - path: "priv/repo/migrations/20260428230000_add_orchestration_snapshot_to_chimeway_notifications.exs"
      provides: "Durable notification-level orchestration snapshot storage for recovery replay"
      min_lines: 12
    - path: "lib/chimeway/trigger.ex"
      provides: "Trigger-time persistence of normalized orchestration snapshots alongside render declarations"
      min_lines: 25
    - path: "test/chimeway/orchestration/recovery_test.exs"
      provides: "Regression coverage proving recovered digest-held events stay digest-held"
      min_lines: 110
  key_links:
    - from: "lib/chimeway/trigger.ex"
      to: "lib/chimeway/notifications/notification.ex"
      via: "notification insert persists a normalized orchestration snapshot per recipient"
      pattern: "orchestration|digest_keys|channels|default"
    - from: "lib/chimeway/deliveries.ex"
      to: "lib/chimeway/delivery_planning.ex"
      via: "recover_event/2 enables persisted orchestration replay when planning deliveries from existing notifications"
      pattern: "use_persisted_orchestration|recover_event"
    - from: "lib/chimeway/delivery_planning.ex"
      to: "lib/chimeway/notifier.ex"
      via: "planner normalizes persisted orchestration snapshots through the existing notifier contract instead of inventing a parallel format"
      pattern: "resolve_orchestration|planner_override"
---

<objective>
Absorb the carried Phase 22 recovery leak by making notifier-defined orchestration replay durable.

Purpose: DIGEST-02 and DIGEST-03 remain leaky if `recover_event/2` can re-drive an originally digest-held notification as an immediate ready delivery. This plan must persist the orchestration snapshot at notification creation time and feed it back into the existing planner during recovery.
Output: notification-level orchestration durability, recovery replay plumbing, and regression tests for digest-held recovery behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-RESEARCH.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-PATTERNS.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-VALIDATION.md
@.planning/phases/22-recovery-outcome-analytics/22-REVIEW.md
@lib/chimeway/trigger.ex
@lib/chimeway/notifier.ex
@lib/chimeway/notifications/notification.ex
@lib/chimeway/delivery_planning.ex
@lib/chimeway/deliveries.ex
@test/chimeway/orchestration/recovery_test.exs

<interfaces>
From `lib/chimeway/notifier.ex`:
```elixir
@type orchestration_resolution :: %{
  default: orchestration_mode(),
  channels: %{String.t() => orchestration_mode()},
  default_digest_key: String.t() | nil,
  digest_keys: %{String.t() => String.t()},
  source: :default | :notifier | :planner_override
}

def resolve_orchestration(notifier, trigger_params, recipient, override \\ :unset)
```

From `lib/chimeway/deliveries.ex`:
```elixir
def recover_event(event_or_id, opts \\ [])
```

From `lib/chimeway/notifications/notification.ex`:
```elixir
schema "chimeway_notifications" do
  field(:render_assigns, :map, default: %{})
  field(:render_channels, :map, default: %{})
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for durable orchestration snapshot persistence and digest-held recovery replay</name>
  <files>test/chimeway/orchestration/recovery_test.exs, test/chimeway/trigger_pipeline_test.exs</files>
  <read_first>
    - test/chimeway/orchestration/recovery_test.exs
    - test/chimeway/trigger_pipeline_test.exs
    - lib/chimeway/trigger.ex
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/deliveries.ex
    - .planning/phases/22-recovery-outcome-analytics/22-REVIEW.md
  </read_first>
  <behavior>
    - Test 1: trigger-time notification persistence stores a normalized orchestration snapshot with `default`, `channels`, `digest_keys`, and `source`.
    - Test 2: `recover_event/2` on a previously digest-held notification replays the persisted orchestration and keeps the recovered delivery in `:digest_held`.
    - Test 3: recovered digest-held rows preserve digest-key planning context instead of becoming `:ready`.
  </behavior>
  <action>Extend `test/chimeway/trigger_pipeline_test.exs` to assert that notifications persist a normalized orchestration snapshot whenever the notifier declares digest behavior. Extend `test/chimeway/orchestration/recovery_test.exs` with a regression that creates a recoverable event whose original orchestration is digest-held, runs `recover_event/2`, and asserts the replayed delivery remains `orchestration_state: :digest_held` with the expected digest planning context and no notifier callback re-entry.</action>
  <acceptance_criteria>
    - `test/chimeway/trigger_pipeline_test.exs` contains `orchestration`
    - `test/chimeway/trigger_pipeline_test.exs` contains `digest_keys`
    - `test/chimeway/orchestration/recovery_test.exs` contains `digest_held`
    - `test/chimeway/orchestration/recovery_test.exs` contains `use_persisted_orchestration`
    - `test/chimeway/orchestration/recovery_test.exs` contains `planning_context`
    - `mix test test/chimeway/trigger_pipeline_test.exs test/chimeway/orchestration/recovery_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/trigger_pipeline_test.exs test/chimeway/orchestration/recovery_test.exs --trace</automated>
  </verify>
  <done>The failing tests define the persistence shape and prove recovered digest-held notifications cannot silently degrade to immediate dispatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist orchestration snapshots on notifications and replay them during recovery</name>
  <files>priv/repo/migrations/20260428230000_add_orchestration_snapshot_to_chimeway_notifications.exs, lib/chimeway/notifications/notification.ex, lib/chimeway/trigger.ex, lib/chimeway/notifier.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/deliveries.ex</files>
  <read_first>
    - lib/chimeway/notifications/notification.ex
    - lib/chimeway/trigger.ex
    - lib/chimeway/notifier.ex
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/deliveries.ex
    - priv/repo/migrations/20260428201500_add_render_channels_to_chimeway_notifications.exs
    - test/chimeway/orchestration/recovery_test.exs
  </read_first>
  <behavior>
    - Test 1: trigger persistence records one normalized orchestration snapshot per notification row.
    - Test 2: recovery reuses the persisted snapshot through the existing planner/notifier normalization seam.
    - Test 3: digest-held recovery preserves digest-key planning context and explainability facts.
  </behavior>
  <action>Create `priv/repo/migrations/20260428230000_add_orchestration_snapshot_to_chimeway_notifications.exs` to add a non-null `:map` column for the persisted orchestration snapshot, then update `lib/chimeway/notifications/notification.ex` to expose it on the schema and changeset. In `lib/chimeway/trigger.ex`, resolve orchestration once per recipient during notification persistence and store the normalized snapshot alongside `render_assigns` and `render_channels`. In `lib/chimeway/delivery_planning.ex`, change orchestration resolution so recovery can opt into `notification`-backed orchestration replay, normalizing the stored map through `Notifier.resolve_orchestration/4` with the override path rather than inventing a second format. In `lib/chimeway/deliveries.ex`, make `recover_event/2` pass `use_persisted_orchestration: true` with the existing persisted-channel replay path. If needed, add small helpers in `lib/chimeway/notifier.ex` to safely serialize and normalize the persisted snapshot shape, but keep the durable identity fields string-keyed and digest-key aware.</action>
  <acceptance_criteria>
    - `priv/repo/migrations/20260428230000_add_orchestration_snapshot_to_chimeway_notifications.exs` contains `add(:orchestration`
    - `lib/chimeway/notifications/notification.ex` contains `field(:orchestration`
    - `lib/chimeway/trigger.ex` contains `resolve_orchestration`
    - `lib/chimeway/trigger.ex` contains `orchestration:`
    - `lib/chimeway/delivery_planning.ex` contains `use_persisted_orchestration`
    - `lib/chimeway/delivery_planning.ex` contains `resolve_orchestration(`
    - `lib/chimeway/deliveries.ex` contains `use_persisted_orchestration: true`
    - `mix test test/chimeway/trigger_pipeline_test.exs test/chimeway/orchestration/recovery_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/trigger_pipeline_test.exs test/chimeway/orchestration/recovery_test.exs --trace</automated>
  </verify>
  <done>Recovery can now replay digest-held orchestration from durable notification data instead of notifier callbacks or defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notifier callback -> persisted notification row | Trigger-time orchestration must be serialized durably without losing digest semantics |
| recovered event -> planner replay | Recovery must not silently change durable user-visible behavior by degrading digest-held deliveries to immediate send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | T | `lib/chimeway/trigger.ex`, `lib/chimeway/notifications/notification.ex` | mitigate | Persist the normalized orchestration snapshot at trigger time so recovery reads canonical notification data instead of recomputing behavior. |
| T-23-05 | R | `lib/chimeway/deliveries.ex`, `lib/chimeway/delivery_planning.ex` | mitigate | Replay persisted orchestration through the existing normalization seam and keep digest-key planning context on the recovered delivery row. |
| T-23-06 | D | recovery replay path | mitigate | Add regression coverage proving recovered digest-held notifications stay held and do not bypass digest accumulation. |
</threat_model>

<verification>
Run `mix test test/chimeway/trigger_pipeline_test.exs test/chimeway/orchestration/recovery_test.exs --trace`.
</verification>

<success_criteria>
Recovered events replay durable digest semantics correctly, and the notification row contains the normalized orchestration snapshot needed to preserve explainability and idempotent digest behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/23-digest-flush-scheduling-audit-closure/23-02-SUMMARY.md`
</output>
