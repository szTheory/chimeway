---
phase: 23-digest-flush-scheduling-audit-closure
plan: 05
type: execute
gap_closure: true
wave: 2
depends_on:
  - 23-01
files_modified:
  - lib/chimeway/digests/accumulation.ex
  - test/chimeway/digests/flush_scheduling_test.exs
  - test/chimeway/digests/accumulation_test.exs
autonomous: true
requirements:
  - DIGEST-02
must_haves:
  truths:
    - "Digest accumulation cannot place a delivery into a bucket for a different recipient, channel, notification key, or notification version via forged `lookup_attrs`."
    - "Caller-supplied lookup attrs remain limited to non-identity helpers that do not override durable host-owned bucket identity."
    - "A mismatched lookup override fails explicitly and leaves no cross-recipient bucket or membership rows behind."
  artifacts:
    - path: "lib/chimeway/digests/accumulation.ex"
      provides: "Validated lookup-attribute boundary that protects durable bucket identity"
      min_lines: 35
    - path: "test/chimeway/digests/accumulation_test.exs"
      provides: "Regression coverage for mismatched identity override rejection"
      min_lines: 50
    - path: "test/chimeway/digests/flush_scheduling_test.exs"
      provides: "Scheduling regression coverage that still passes under the hardened lookup seam"
      min_lines: 80
  key_links:
    - from: "lib/chimeway/digests/accumulation.ex"
      to: "lib/chimeway/notifications/notification.ex"
      via: "recipient identity comes from the persisted notification row and cannot be replaced by caller input"
      pattern: "recipient_identity|lookup_attrs"
    - from: "lib/chimeway/digests/accumulation.ex"
      to: "lib/chimeway/events/event.ex"
      via: "notification key and version stay derived from the persisted event instead of caller-supplied overrides"
      pattern: "notification_key|notification_version"
    - from: "test/chimeway/digests/accumulation_test.exs"
      to: "lib/chimeway/digests/accumulation.ex"
      via: "the mismatch regression proves no forged bucket or membership survives"
      pattern: "invalid_lookup_attrs|recipient_id"
---

<objective>
Close the critical Phase 23 review risk that still makes the digest slice unsafe to ship even after the final gate is green.

Purpose: `23-REVIEW.md` found that Phase 23 accumulation accepts forged `lookup_attrs` capable of overriding recipient/channel/rule identity during bucket selection. That breaks host ownership boundaries and can leak one recipient’s notifications into another recipient’s digest, so it must be fixed in this gap pass rather than deferred.
Output: hardened accumulation lookup validation and explicit regression tests proving cross-recipient and cross-channel overrides are rejected.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-REVIEW.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-RESEARCH.md
@lib/chimeway/digests/accumulation.ex
@test/chimeway/digests/accumulation_test.exs
@test/chimeway/digests/flush_scheduling_test.exs

<interfaces>
From `lib/chimeway/digests/accumulation.ex`:
```elixir
@type lookup_attrs :: %{
  optional(:recipient_id) => String.t() | nil,
  optional(:channel) => String.t() | nil,
  optional(:notification_key) => String.t() | nil,
  optional(:notification_version) => pos_integer() | nil,
  optional(:category) => String.t() | nil,
  optional(:digest_key) => String.t() | nil
}

def accumulate_delivery(%Delivery{} = delivery, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests that reject forged digest lookup identity overrides</name>
  <files>test/chimeway/digests/accumulation_test.exs, test/chimeway/digests/flush_scheduling_test.exs</files>
  <read_first>
    - test/chimeway/digests/accumulation_test.exs
    - test/chimeway/digests/flush_scheduling_test.exs
    - lib/chimeway/digests/accumulation.ex
    - .planning/phases/23-digest-flush-scheduling-audit-closure/23-REVIEW.md
  </read_first>
  <behavior>
    - Test 1: `Accumulation.accumulate_delivery/2` rejects `lookup_attrs` whose `recipient_id` disagrees with the persisted notification recipient.
    - Test 2: the same rejection holds for `channel`, `notification_key`, and `notification_version` mismatches.
    - Test 3: allowed helper attrs such as `category` and `digest_key` continue to work when they do not override durable identity.
    - Test 4: a rejected override leaves no forged bucket, membership, or scheduled flush side effects behind.
  </behavior>
  <action>Extend `test/chimeway/digests/accumulation_test.exs` with explicit mismatch cases for `recipient_id`, `channel`, `notification_key`, and `notification_version`, asserting a tagged failure and zero forged rows. Keep `test/chimeway/digests/flush_scheduling_test.exs` green by proving the normal scheduling path still works when only permitted helper attrs are supplied. The tests should make the ownership boundary visible and grepable rather than relying on indirect count changes alone.</action>
  <acceptance_criteria>
    - `test/chimeway/digests/accumulation_test.exs` contains `invalid_lookup_attrs`
    - `test/chimeway/digests/accumulation_test.exs` contains `recipient_id`
    - `test/chimeway/digests/accumulation_test.exs` contains `notification_version`
    - `test/chimeway/digests/flush_scheduling_test.exs` contains `lookup_attrs`
    - `mix test test/chimeway/digests/accumulation_test.exs test/chimeway/digests/flush_scheduling_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/digests/accumulation_test.exs test/chimeway/digests/flush_scheduling_test.exs --trace</automated>
  </verify>
  <done>The regression suite proves forged lookup identity cannot cross recipient/channel ownership boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden accumulation to derive bucket identity only from durable delivery context</name>
  <files>lib/chimeway/digests/accumulation.ex, test/chimeway/digests/accumulation_test.exs, test/chimeway/digests/flush_scheduling_test.exs</files>
  <read_first>
    - lib/chimeway/digests/accumulation.ex
    - test/chimeway/digests/accumulation_test.exs
    - test/chimeway/digests/flush_scheduling_test.exs
    - .planning/phases/23-digest-flush-scheduling-audit-closure/23-REVIEW.md
  </read_first>
  <behavior>
    - Test 1: `build_lookup_attrs/4` or its replacement keeps `recipient_id`, `channel`, `notification_key`, and `notification_version` anchored to persisted delivery/notification/event facts.
    - Test 2: mismatched identity overrides rollback with a stable tagged error such as `{:invalid_lookup_attrs, ...}`.
    - Test 3: only non-identity helper fields that do not violate the durable boundary remain overridable.
  </behavior>
  <action>Refactor `lib/chimeway/digests/accumulation.ex` so the durable identity for bucket lookup is derived first from the locked delivery, persisted notification, and persisted event, and caller input cannot overwrite those keys. Permit only explicitly non-identity helper attrs to flow through, or require supplied identity keys to match exactly before continuing. Use `Repo.rollback({:invalid_lookup_attrs, mismatch})` or an equivalently stable tagged error so the regression tests can assert the boundary directly. Do not weaken the existing scheduling behavior while hardening this seam.</action>
  <acceptance_criteria>
    - `lib/chimeway/digests/accumulation.ex` contains `invalid_lookup_attrs`
    - `lib/chimeway/digests/accumulation.ex` contains `recipient_id`
    - `lib/chimeway/digests/accumulation.ex` contains `notification_version`
    - `lib/chimeway/digests/accumulation.ex` does not merge caller input blindly over the full derived identity map
    - `mix test test/chimeway/digests/accumulation_test.exs test/chimeway/digests/flush_scheduling_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/digests/accumulation_test.exs test/chimeway/digests/flush_scheduling_test.exs --trace</automated>
  </verify>
  <done>Digest accumulation now preserves host ownership boundaries by construction, and the new regression tests lock the seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-supplied lookup attrs -> bucket selection | Internal or host-side callers can pass attrs, but they must not be able to override durable recipient/channel/rule identity |
| delivery context -> digest membership rows | Membership writes must always correspond to the canonical delivery’s real owner and rule identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-13 | T | `lib/chimeway/digests/accumulation.ex` | mitigate | Reject mismatched identity overrides with a tagged rollback instead of merging caller input over durable bucket identity. |
| T-23-14 | I | digest bucket selection | mitigate | Anchor recipient/channel/notification identity to persisted delivery context so one recipient’s notifications cannot be emitted in another recipient’s digest. |
| T-23-15 | R | review closure | mitigate | Add explicit regression tests for each forged identity key so the ownership boundary is permanently documented in the suite. |
</threat_model>

<verification>
Run `mix test test/chimeway/digests/accumulation_test.exs test/chimeway/digests/flush_scheduling_test.exs --trace`.
</verification>

<success_criteria>
Digest accumulation rejects forged identity overrides, preserves scheduling behavior for valid inputs, and closes the critical Phase 23 review finding.
</success_criteria>

<output>
After completion, create `.planning/phases/23-digest-flush-scheduling-audit-closure/23-05-SUMMARY.md`
</output>
