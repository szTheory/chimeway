---
phase: 23-digest-flush-scheduling-audit-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/digests/accumulation.ex
  - lib/chimeway/digests.ex
  - lib/chimeway/dispatch/oban.ex
  - lib/chimeway/dispatch/digest_flush_worker.ex
  - test/chimeway/digests/flush_scheduling_test.exs
  - test/chimeway/digests/emission_test.exs
autonomous: true
requirements:
  - DIGEST-02
must_haves:
  truths:
    - "Oban-backed installs automatically schedule one future flush job from the durable bucket window instead of relying on manual `emit_bucket/2` calls."
    - "Non-Oban installs keep the durable bucket state and explicit host-managed `emit_bucket/2` seam rather than silently pretending built-in scheduling exists."
    - "Repeated accumulation, repeated scheduling attempts, and repeated worker execution still converge on one emitted digest delivery identity."
    - "The scheduled runtime path stays thin: job args carry only `bucket_id`, and due/idempotency checks remain inside the durable emission service."
  artifacts:
    - path: "lib/chimeway/digests/accumulation.ex"
      provides: "Bucket refresh path that schedules flush work from persisted bucket state"
      min_lines: 40
    - path: "lib/chimeway/digests.ex"
      provides: "Explicit public-facing scope boundary that keeps host-managed flush execution documented when Oban is not installed"
      min_lines: 12
    - path: "lib/chimeway/dispatch/digest_flush_worker.ex"
      provides: "Thin scheduled worker with deterministic scheduled-time uniqueness keyed by bucket identity"
      min_lines: 20
    - path: "test/chimeway/digests/flush_scheduling_test.exs"
      provides: "Automated proof that accumulation schedules exactly one future flush job per bucket window"
      min_lines: 80
  key_links:
    - from: "lib/chimeway/digests/accumulation.ex"
      to: "lib/chimeway/dispatch/digest_flush_worker.ex"
      via: "bucket refresh schedules the worker at `bucket.window_ends_at` using `%{bucket_id: bucket.id}`"
      pattern: "window_ends_at|bucket_id|DigestFlushWorker"
    - from: "lib/chimeway/dispatch/digest_flush_worker.ex"
      to: "lib/chimeway/digests/emission.ex"
      via: "worker delegates execution back to `emit_bucket/2` so the database remains the correctness boundary"
      pattern: "emit_bucket"
    - from: "lib/chimeway/dispatch/oban.ex"
      to: "lib/chimeway/dispatch/digest_flush_worker.ex"
      via: "Oban scheduling reuses the Phase 18 thin-worker pattern rather than creating a second dispatch pipeline"
      pattern: "scheduled_at|replace|unique"
---

<objective>
Close the core runtime gap by scheduling `DigestFlushWorker` automatically from durable bucket state for Oban-backed installs, while documenting the non-Oban boundary explicitly.

Purpose: DIGEST-02 is still incomplete because Phase 20 made bucket emission runnable but not automatically scheduled. This plan must reuse the established Oban thin-worker posture from Phase 18 so bucket windows, not manual calls, drive digest flush execution where Oban is configured, and it must document that non-Oban hosts still use the durable public flush seam instead of an implicit no-op.
Output: automatic Oban-backed bucket scheduling code, explicit non-Oban scope documentation, and tests proving deterministic scheduling and duplicate-safe execution.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-RESEARCH.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-PATTERNS.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-VALIDATION.md
@.planning/phases/18-scheduled-resume-deferred-dispatch/18-02-PLAN.md
@.planning/phases/20-digest-emission-explainability/20-02-SUMMARY.md
@lib/chimeway/digests/accumulation.ex
@lib/chimeway/digests.ex
@lib/chimeway/dispatch/oban.ex
@lib/chimeway/dispatch/digest_flush_worker.ex
@test/chimeway/digests/emission_test.exs

<interfaces>
From `lib/chimeway/digests/accumulation.ex`:
```elixir
def accumulate_delivery(%Delivery{} = delivery, opts \\ [])
```

From `lib/chimeway/digests.ex`:
```elixir
def emit_bucket(bucket_or_id, opts \\ [])
```

From `lib/chimeway/dispatch/digest_flush_worker.ex`:
```elixir
def perform(%Oban.Job{args: %{"bucket_id" => bucket_id}})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED coverage for automatic bucket scheduling and duplicate collapse</name>
  <files>test/chimeway/digests/flush_scheduling_test.exs, test/chimeway/digests/emission_test.exs</files>
  <read_first>
    - test/chimeway/digests/accumulation_test.exs
    - test/chimeway/digests/emission_test.exs
    - lib/chimeway/digests/accumulation.ex
    - lib/chimeway/dispatch/oban.ex
    - lib/chimeway/dispatch/digest_flush_worker.ex
    - lib/chimeway/dispatch/deferred_resume_worker.ex
    - .planning/phases/23-digest-flush-scheduling-audit-closure/23-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: accumulating a digest-held delivery schedules one `DigestFlushWorker` job at `bucket.window_ends_at`.
    - Test 2: accumulating more deliveries into the same bucket does not enqueue duplicate future flush jobs.
    - Test 3: re-running the flush worker or calling `emit_bucket/2` after a scheduled execution reuses the same emitted digest delivery identity.
  </behavior>
  <action>Create `test/chimeway/digests/flush_scheduling_test.exs` using `test/chimeway/digests/accumulation_test.exs` as the closest assertion/style analog, and extend `test/chimeway/digests/emission_test.exs` so Phase 23 locks the automatic scheduling contract. Use `Oban.Testing` assertions to prove that `Accumulation.accumulate_delivery/2` schedules `%{bucket_id: bucket.id}` exactly once at `bucket.window_ends_at`, and add duplicate-execution coverage showing the scheduled worker plus any direct retry still converge on one emitted digest delivery record per bucket window.</action>
  <acceptance_criteria>
    - `test/chimeway/digests/flush_scheduling_test.exs` contains `use Oban.Testing`
    - `test/chimeway/digests/flush_scheduling_test.exs` contains `DigestFlushWorker`
    - `test/chimeway/digests/flush_scheduling_test.exs` contains `window_ends_at`
    - `test/chimeway/digests/flush_scheduling_test.exs` contains `assert_enqueued`
    - `test/chimeway/digests/emission_test.exs` contains `digest_delivery_id`
    - `test/chimeway/digests/emission_test.exs` contains `emit_bucket`
    - `mix test test/chimeway/digests/flush_scheduling_test.exs test/chimeway/digests/emission_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/digests/flush_scheduling_test.exs test/chimeway/digests/emission_test.exs --trace</automated>
  </verify>
  <done>The tests define the exact schedule-at-window-end contract and the duplicate-safe emitted-identity contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schedule `DigestFlushWorker` from bucket refresh for Oban installs and document the non-Oban boundary</name>
  <files>lib/chimeway/digests/accumulation.ex, lib/chimeway/digests.ex, lib/chimeway/dispatch/oban.ex, lib/chimeway/dispatch/digest_flush_worker.ex</files>
  <read_first>
    - lib/chimeway/digests/accumulation.ex
    - lib/chimeway/digests.ex
    - lib/chimeway/dispatch/oban.ex
    - lib/chimeway/dispatch/digest_flush_worker.ex
    - lib/chimeway/dispatch/deferred_resume_worker.ex
    - .planning/phases/18-scheduled-resume-deferred-dispatch/18-02-PLAN.md
    - test/chimeway/digests/accumulation_test.exs
    - test/chimeway/digests/flush_scheduling_test.exs
  </read_first>
  <behavior>
    - Test 1: when the configured dispatcher is `Chimeway.Dispatch.Oban`, bucket refresh schedules a future flush job at the persisted window end.
    - Test 2: scheduling remains keyed by `bucket_id` and deterministic `scheduled_at`, so duplicate accumulation attempts collapse.
    - Test 3: when the configured dispatcher is not Oban, the bucket still accumulates durably and the public flush seam remains explicitly documented instead of implying built-in automatic scheduling.
    - Test 4: the flush worker stays thin and delegates all due/idempotency checks to `Digests.emit_bucket/2`.
  </behavior>
  <action>Update `lib/chimeway/digests/accumulation.ex` so the `:inserted` and `:existing` membership paths both flow through a helper that refreshes the authoritative bucket row and, when the configured dispatcher is `Chimeway.Dispatch.Oban`, inserts a scheduled `DigestFlushWorker` job for `%{bucket_id: bucket.id}` at `bucket.window_ends_at`. Model the queue contract directly on `lib/chimeway/dispatch/deferred_resume_worker.ex`: thin job args, `scheduled_at` sourced from durable state, and uniqueness keyed to bucket identity plus scheduled timestamp. Use `test/chimeway/digests/accumulation_test.exs` as the nearest transaction/update analog for bucket refresh assertions. In `lib/chimeway/digests.ex`, add explicit public documentation that non-Oban hosts retain the durable host-managed `emit_bucket/2` seam and are not part of the built-in automatic scheduling closure for Phase 23. Update `lib/chimeway/dispatch/digest_flush_worker.ex` to use `replace: [scheduled: [:scheduled_at]]` and scheduled-time uniqueness, while keeping `perform/1` as an immediate delegation to `Digests.emit_bucket/2` that treats `{:bucket_not_due, ...}` as a no-op.</action>
  <acceptance_criteria>
    - `lib/chimeway/digests/accumulation.ex` contains `DigestFlushWorker`
    - `lib/chimeway/digests/accumulation.ex` contains `window_ends_at`
    - `lib/chimeway/digests/accumulation.ex` contains `bucket_id`
    - `lib/chimeway/digests.ex` contains `Chimeway.Dispatch.Oban`
    - `lib/chimeway/digests.ex` contains `emit_bucket/2`
    - `lib/chimeway/dispatch/digest_flush_worker.ex` contains `replace: [scheduled: [:scheduled_at]]`
    - `lib/chimeway/dispatch/digest_flush_worker.ex` contains `keys: [:bucket_id]`
    - `lib/chimeway/dispatch/digest_flush_worker.ex` contains `emit_bucket`
    - `mix test test/chimeway/digests/flush_scheduling_test.exs test/chimeway/digests/emission_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/digests/flush_scheduling_test.exs test/chimeway/digests/emission_test.exs --trace</automated>
  </verify>
  <done>Digest accumulation now schedules future flush execution from durable bucket state without introducing a second execution pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bucket state -> scheduled worker job | Durable bucket windows become queued execution and must not drift into queue-owned truth |
| duplicate accumulation -> duplicate worker rows | Multiple membership inserts or retries must not create multiple emitted digest deliveries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | D | `lib/chimeway/digests/accumulation.ex` | mitigate | Schedule from persisted `window_ends_at` on the bucket row and reuse Phase 18 scheduled-job semantics rather than in-memory timers. |
| T-23-02 | T | `lib/chimeway/dispatch/digest_flush_worker.ex` | mitigate | Keep args limited to `bucket_id` and let `emit_bucket/2` re-check due-ness and emitted identity under row lock. |
| T-23-03 | R | scheduled digest execution | mitigate | Assert duplicate accumulation and duplicate worker execution both reuse one canonical `digest_delivery_id`. |
</threat_model>

<verification>
Run `mix test test/chimeway/digests/flush_scheduling_test.exs test/chimeway/digests/emission_test.exs --trace`.
</verification>

<success_criteria>
Due digest buckets schedule one future flush job from durable state, duplicate schedule attempts collapse safely, and the worker continues to delegate correctness to the existing emission service.
</success_criteria>

<output>
After completion, create `.planning/phases/23-digest-flush-scheduling-audit-closure/23-01-SUMMARY.md`
</output>
