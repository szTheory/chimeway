---
phase: 23-digest-flush-scheduling-audit-closure
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01
  - 23-02
files_modified:
  - test/chimeway/integration/digest_delivery_lifecycle_test.exs
autonomous: true
requirements:
  - DIGEST-02
  - DIGEST-03
must_haves:
  truths:
    - "The end-to-end digest path is verified through the scheduled worker path from trigger-time persistence to emitted digest dispatch."
    - "The production-shaped digest proof remains separate from closure-artifact realignment so final DIGEST status cannot advance before the gap-fix plans complete."
  artifacts:
    - path: "test/chimeway/integration/digest_delivery_lifecycle_test.exs"
      provides: "Production-shaped proof that scheduled bucket execution, not manual intervention, drives canonical digest dispatch"
      min_lines: 120
  key_links:
    - from: "test/chimeway/integration/digest_delivery_lifecycle_test.exs"
      to: "lib/chimeway/dispatch/digest_flush_worker.ex"
      via: "integration test drains or performs the scheduled worker instead of calling `emit_bucket/2` as the primary runtime path"
      pattern: "DigestFlushWorker|perform_job|assert_enqueued"
---

<objective>
Finish the proof side of Phase 23 by locking the scheduled production-shaped flow before the closure artifacts are realigned.

Purpose: the code changes are not enough by themselves, but this plan must stop short of updating closure artifacts so final DIGEST status remains owned by the dedicated gap-closure alignment plan. Its job is to supply the production-shaped integration proof that later closure work can cite after 23-04 and 23-05 complete.
Output: end-to-end integration proof for the scheduled worker path and a summary that records the targeted evidence without advancing DIGEST closure documents.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-RESEARCH.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-VALIDATION.md
@.planning/phases/20-digest-emission-explainability/20-VALIDATION.md
@.planning/phases/22-recovery-outcome-analytics/22-VERIFICATION.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-01-PLAN.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-02-PLAN.md
@test/chimeway/integration/digest_delivery_lifecycle_test.exs

<interfaces>
From `test/chimeway/integration/digest_delivery_lifecycle_test.exs`:
```elixir
use Oban.Testing, repo: Chimeway.Repo
assert :ok = perform_job(DigestFlushWorker, %{bucket_id: bucket.id})
assert_enqueued(worker: ObanWorker, args: %{delivery_id: emitted.id})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend end-to-end integration coverage to the scheduled digest worker path</name>
  <files>test/chimeway/integration/digest_delivery_lifecycle_test.exs</files>
  <read_first>
    - test/chimeway/integration/digest_delivery_lifecycle_test.exs
    - lib/chimeway/digests/accumulation.ex
    - lib/chimeway/dispatch/digest_flush_worker.ex
    - lib/chimeway/deliveries.ex
    - .planning/phases/23-digest-flush-scheduling-audit-closure/23-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: the primary runtime path is trigger -> digest-held accumulation -> scheduled `DigestFlushWorker` -> emitted digest dispatch, without a manual `emit_bucket/2` call driving success.
    - Test 2: the integration flow proves emitted digest delivery dispatches by canonical `delivery_id` and source rows converge durably.
    - Test 3: the flow remains stable when recovery-replayed digest-held notifications participate in the same bucket semantics.
  </behavior>
  <action>Extend `test/chimeway/integration/digest_delivery_lifecycle_test.exs` so the production-shaped Phase 23 flow uses accumulation-driven scheduling as the first-class path: assert the flush worker is enqueued at the bucket window boundary, perform or drain that job, then assert the emitted digest dispatch and source-row convergence happened through the canonical delivery lifecycle. Keep any direct `emit_bucket/2` assertions as supporting duplicate-safety checks, not the primary happy path.</action>
  <acceptance_criteria>
    - `test/chimeway/integration/digest_delivery_lifecycle_test.exs` contains `DigestFlushWorker`
    - `test/chimeway/integration/digest_delivery_lifecycle_test.exs` contains `assert_enqueued`
    - `test/chimeway/integration/digest_delivery_lifecycle_test.exs` contains `perform_job`
    - `test/chimeway/integration/digest_delivery_lifecycle_test.exs` contains `delivery_id`
    - `mix test test/chimeway/integration/digest_delivery_lifecycle_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/integration/digest_delivery_lifecycle_test.exs --trace</automated>
  </verify>
  <done>The integration suite now proves the production-shaped scheduled worker path rather than relying on manual flush invocation as the main evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Capture targeted digest evidence without advancing closure artifacts</name>
  <files>test/chimeway/integration/digest_delivery_lifecycle_test.exs</files>
  <read_first>
    - .planning/phases/23-digest-flush-scheduling-audit-closure/23-VALIDATION.md
    - test/chimeway/integration/digest_delivery_lifecycle_test.exs
  </read_first>
  <action>Run the targeted digest/recovery/traces slice after Task 1 and use the resulting command set as proof input for the later closure plan, but do not modify `.planning/phases/20-digest-emission-explainability/20-VERIFICATION.md`, `.planning/REQUIREMENTS.md`, or `.planning/ROADMAP.md` here. The executor should record the exact commands and outcomes in `23-03-SUMMARY.md` so Plan 23-06 can realign the authoritative audit artifacts after 23-04 and 23-05 finish.</action>
  <acceptance_criteria>
    - `mix test test/chimeway/integration/digest_delivery_lifecycle_test.exs test/chimeway/digests/flush_scheduling_test.exs test/chimeway/orchestration/recovery_test.exs test/chimeway/traces_test.exs --trace` exits 0
    - `23-03-SUMMARY.md` records the exact targeted verification commands and outcomes for Plan 23-06 to cite
    - no authoritative DIGEST closure artifact is modified by this task
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/integration/digest_delivery_lifecycle_test.exs test/chimeway/digests/flush_scheduling_test.exs test/chimeway/orchestration/recovery_test.exs test/chimeway/traces_test.exs --trace</automated>
  </verify>
  <done>The targeted digest proof exists as execution evidence, while authoritative DIGEST closure remains reserved for Plan 23-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verified code path -> later closure artifacts | This plan supplies proof input, but the authoritative audit documents must stay unchanged until the dedicated closure-alignment plan runs |
| operator-facing requirement closure -> evidence | DIGEST requirements must not be marked complete from this plan alone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | R | targeted proof handoff | mitigate | Record the scheduled worker path, emitted-identity reuse, and recovery-safe digest replay in the execution summary so Plan 23-06 can cite concrete evidence. |
| T-23-08 | T | closure-artifact ownership | mitigate | Leave `.planning/phases/20-digest-emission-explainability/20-VERIFICATION.md`, `.planning/REQUIREMENTS.md`, and `.planning/ROADMAP.md` untouched in this plan so only the final alignment plan can advance DIGEST closure. |
| T-23-09 | D | integration proof | mitigate | Keep a production-shaped integration test for the scheduled happy path so future regressions cannot fall back to manual flush assumptions. |
</threat_model>

<verification>
Run `mix test test/chimeway/integration/digest_delivery_lifecycle_test.exs test/chimeway/digests/flush_scheduling_test.exs test/chimeway/orchestration/recovery_test.exs test/chimeway/traces_test.exs --trace`, and record the command/output summary in `23-03-SUMMARY.md` for Plan 23-06.
</verification>

<success_criteria>
Phase 23's scheduled end-to-end digest proof exists as targeted execution evidence, and the plan set keeps DIGEST closure documents untouched until the later gap-fix plans resolve the final gate and review blocker.
</success_criteria>

<output>
After completion, create `.planning/phases/23-digest-flush-scheduling-audit-closure/23-03-SUMMARY.md`
</output>
