---
phase: 99-multi-installation-delivery-recovery
plan: "09"
type: execute
wave: 7
depends_on: ["99-07"]
files_modified:
  - lib/chimeway/dispatch/sync.ex
  - test/chimeway/delivery_target_test.exs
autonomous: true
gap_closure: true
requirements: [PUSH-01, PUSH-02, PUSH-03, PUSH-04]
must_haves:
  truths:
    - "D-02/PUSH-01 adjacency: synchronous push dispatch executes every tenant-qualified pending target selected beneath the canonical logical delivery, not only the first target."
    - "PUSH-01 empty assumption: an empty selected target set performs no adapter call and retains the stable no_eligible_targets parent suppression from planning."
    - "PUSH-01 ordering assumption: sync takes a stable ordered target-ID snapshot from actionable_targets/1 and attempts each ID in binding-revision order."
    - "D-03/D-11/PUSH-03 idempotency: repeated or concurrent sync execution can enter the adapter at most once for each durable target because each enumerated ID still passes through begin_target_attempt/2."
    - "D-04/PUSH-04: one target failure cannot prevent later selected targets from being attempted, and the final returned parent is the aggregate derived after the entire snapshot is processed."
  artifacts:
    - path: "lib/chimeway/dispatch/sync.ex"
      provides: "Deterministic all-target synchronous push fan-out through target-ID claims"
    - path: "test/chimeway/delivery_target_test.exs"
      provides: "Two-target sync fan-out, no-short-circuit, empty-set, and repeat-execution regressions"
  key_links:
    - from: "lib/chimeway/dispatch/sync.ex"
      to: "lib/chimeway/delivery_targets.ex"
      via: "actionable_targets/1 supplies the tenant-qualified ordered snapshot"
      pattern: "actionable_targets"
    - from: "lib/chimeway/dispatch/sync.ex"
      to: "lib/chimeway/dispatch/executor.ex"
      via: "each snapshot member executes with its exact target_id through the durable claim/start seam"
      pattern: "run_target.*target_id"
  prohibitions:
    - "Synchronous fan-out must not call the target adapter without an exact durable target claim and attempt-start row."
    - "One target error must not silently strand later selected targets or erase their independent terminal evidence."
    - "Sync results must not claim all-device delivery or expose raw installation/provider material."
---

<objective>
Make synchronous push dispatch deliver the logical decision to every selected opaque installation target.

Purpose: Close the production entry-point gap while preserving deterministic ordering, durable per-target idempotency, and honest parent aggregation.
Output: An all-target sync loop and executable two-target/retry-safe fan-out evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
@.planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
@.planning/phases/99-multi-installation-delivery-recovery/99-01-SUMMARY.md
@.planning/phases/99-multi-installation-delivery-recovery/99-03-SUMMARY.md

<interfaces>
Existing contracts to use directly:
- `Chimeway.DeliveryTargets.actionable_targets/1` returns tenant-qualified pending targets ordered by `binding_revision_ref` then `id`.
- `Chimeway.Dispatch.Executor.run_target/2` accepts `target_id:` and `source:` and obtains provider authority only through `begin_target_attempt/2`.
- `Dispatch.Sync.dispatch_delivery/2` returns the final logical delivery result, while target attempts remain child evidence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute the full deterministic target snapshot through sync dispatch</name>
  <read_first>
    - lib/chimeway/dispatch/sync.ex
    - lib/chimeway/dispatch/executor.ex
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/delivery_target.ex
    - lib/chimeway/delivery_target_attempt.ex
    - test/chimeway/delivery_target_test.exs
    - test/chimeway/dispatch/target_worker_test.exs
    - .planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
  </read_first>
  <files>lib/chimeway/dispatch/sync.ex, test/chimeway/delivery_target_test.exs</files>
  <behavior>
    - Test 1: a resolver-selected two-target push calls the fake adapter once for each exact target ID, creates one ordered attempt per target, and returns a succeeded parent after both accept.
    - Test 2: adapter observation order matches `actionable_targets/1` ordering; repeated and concurrent sync calls add no second attempt or provider call for any target.
    - Test 3: a durable failure for the first target does not prevent the second target from executing; the returned/reloaded parent retains the honest aggregate and both child outcomes.
    - Test 4: no eligible targets produces no adapter calls and returns the stable suppressed parent.
  </behavior>
  <action>Write the multi-target regression matrix first and update the test adapter to inspect the attempt belonging to the current envelope target rather than assuming a single global attempt. D-01: retain one canonical logical Delivery while enumerating child targets. D-03: use exact durable target IDs so repeated execution converges. D-04: preserve no-target suppression and aggregate success/partial-failure truth. D-06: keep claim and attempt-start persistence before every adapter call. D-09: keep all success language limited to provider acceptance. D-11: retain atomic claim authority for every enumerated child. In the push branch of `Sync.do_dispatch/1`, obtain one stable tenant-qualified snapshot with `DeliveryTargets.actionable_targets/1`, then call `Executor.run_target/2` for every exact target ID with `source: "sync"`. Continue across already-claimed noops and durably finalized per-target errors so one installation cannot strand later selected installations; retain the first stable error only for the final caller result when no honest successful parent result is available. After the complete snapshot, reload/recompute the canonical parent through existing tenant-safe delivery/aggregate APIs and return that authoritative row. Preserve non-push `Executor.run_delivery/1`, sync telemetry shape, and the empty-target suppression contract. Do not move adapter I/O into Sync, parse binding references, or add provider-specific result vocabulary.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/dispatch/sync.ex test/chimeway/delivery_target_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs test/chimeway/dispatch/target_worker_test.exs test/chimeway/integration/delivery_lifecycle_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - A two-target sync fixture receives both exact target IDs in stable order and stores exactly one attempt for each target.
    - Repeated and concurrent sync dispatch produce zero additional adapter observations and zero additional target attempts after each target claim wins once.
    - A mixed-result fixture proves the later target executes after the earlier target finalizes an error, and the final parent aggregate retains independent outcomes.
    - An empty-target fixture invokes no adapter and returns `suppressed` with `no_eligible_targets`.
    - Existing non-push lifecycle tests and target-worker claim regressions remain green.
  </acceptance_criteria>
  <done>Synchronous push dispatch processes every selected target exactly through its own durable claim and returns the honest aggregate parent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Durable target snapshot → synchronous executor | Each selected child ID becomes a possible provider request and must retain tenant and claim authority. |
| Per-target outcomes → logical parent result | Independent failures and acceptances are aggregated without erasure or receipt overclaim. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-99-09-01 | Elevation of Privilege | Sync target enumeration | high | mitigate | Enumerate only `actionable_targets/1` for the delivery tenant and pass exact target IDs into the claim seam. | `env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs --warnings-as-errors` |
| T-99-09-02 | Tampering | Concurrent/repeated sync calls | high | mitigate | Keep `begin_target_attempt/2` as the sole provider authority and assert one adapter observation per target. | `env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-09-03 | Repudiation | Mixed fan-out outcomes | high | mitigate | Process the full snapshot and derive the parent only from durable child states. | `env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs --warnings-as-errors` |
| T-99-09-04 | Information Disclosure | Sync telemetry/results | medium | mitigate | Preserve existing safe telemetry and expose only durable IDs and closed aggregate evidence. | `env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs test/chimeway/integration/delivery_lifecycle_test.exs --warnings-as-errors` |
| T-99-09-SC | Tampering | Package supply chain | low | accept | No package-manager operation or dependency change occurs. | `git diff --exit-code -- mix.exs mix.lock` |
</threat_model>

<assumption_delta_decision>
Primary noun: the durable tenant-scoped DeliveryTarget is the unit enumerated and claimed; Delivery remains the single logical result.
Decision: promote — plural resolver output must imply plural sync execution, because the current singular call contradicts PUSH-01/PUSH-02; the promotion is to an all-target loop, not to multiple parent deliveries.
</assumption_delta_decision>

## API Coverage Decision

The existing Phase 99 `COVERAGE.md` declaration remains valid: this plan changes only internal sync orchestration over the established TargetAdapter seam and adds no external provider API or SDK capability.

## Artifacts this phase produces

- Modified existing private symbol: `Chimeway.Dispatch.Sync.do_dispatch/1` gains deterministic all-target execution and authoritative final-parent selection.
- Expanded test adapter behavior and multi-target cases in `test/chimeway/delivery_target_test.exs`.
- New public modules, files, fields, schemas, migrations, configuration flags, or external API surfaces: none.

<verification>
Run focused target, target-worker, and non-push lifecycle tests with warnings as errors; prove exact adapter-call and attempt counts rather than relying on returned status alone.
</verification>

<success_criteria>
Two selected targets both execute through sync, repeated execution adds no provider request, errors do not strand siblings, and empty selection stays stably suppressed.
</success_criteria>

<output>
Create `.planning/phases/99-multi-installation-delivery-recovery/99-09-SUMMARY.md` when done.
</output>
