---
phase: 99-multi-installation-delivery-recovery
plan: "02"
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - lib/chimeway/install/migrations.ex
  - test/chimeway/install/migrations_test.exs
  - test/chimeway/install/golden_diff_test.exs
  - test/chimeway/install/prefix_contract_test.exs
  - test/chimeway/migration_contract_test.exs
  - test/chimeway/generated_prefixed_runtime_proof_test.exs
  - test/chimeway/runtime_prefix_integration_test.exs
  - test/fixtures/installer_golden_public/STDOUT.txt
  - test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_create_chimeway_delivery_targets.exs
  - test/fixtures/installer_golden_prefixed/STDOUT.txt
  - test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_create_chimeway_delivery_targets.exs
autonomous: true
requirements: [PUSH-02, PUSH-03]
must_haves:
  truths:
    - "D-03: repository, copied, public-generated, and prefixed-generated migrations enforce the same target identity and attempt-order uniqueness."
    - "D-05/D-06: both supported static storage modes create target and target-attempt tables before runtime code attempts target planning or dispatch."
    - "Public generation uses unprefixed Chimeway relations and prefixed generation uses the configured static chimeway schema; neither derives storage routing from tenant or binding identity."
    - "Migration generation remains deterministic and idempotent at the exact 35-template count."
  artifacts:
    - path: "test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_create_chimeway_delivery_targets.exs"
      provides: "Exact public/static generated target migration"
    - path: "test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_create_chimeway_delivery_targets.exs"
      provides: "Exact prefixed/static generated target migration"
    - path: "test/chimeway/migration_contract_test.exs"
      provides: "Real PostgreSQL dual-mode schema and uniqueness proof"
  key_links:
    - from: "priv/chimeway_migrations/035_create_chimeway_delivery_targets.exs"
      to: "lib/chimeway/install/migrations.ex"
      via: "numeric template discovery and deterministic prefix rendering"
      pattern: "035_create_chimeway_delivery_targets"
    - from: "lib/chimeway/install/migrations.ex"
      to: "test/fixtures/installer_golden_(public|prefixed)"
      via: "golden refresh produces exact reviewed output for both static modes"
      pattern: "create_chimeway_delivery_targets"
  prohibitions:
    - "FLAGGED-UNVERIFIED — Canonical, repository, public, and prefixed migration copies must not diverge in tables, columns, constraints, or target identity."
    - "FLAGGED-UNVERIFIED — Domain tenant or binding identity must not become a dynamic Ecto, Oban, or database schema prefix."
---

<objective>
Make the target schema adopter-copyable and executable in both supported static storage modes.

Purpose: Ensure the tracer's durable contract is not repository-only and cannot drift across generated public/prefixed installs.
Output: Installer template registration, exact golden fixtures, dual-mode database contracts, and runtime-prefix target smoke evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
@.planning/phases/99-multi-installation-delivery-recovery/99-PATTERNS.md
@.planning/phases/99-multi-installation-delivery-recovery/99-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render and execute migration 035 in public and prefixed installs</name>
  <read_first>
    - priv/chimeway_migrations/035_create_chimeway_delivery_targets.exs
    - lib/chimeway/install/migrations.ex
    - test/chimeway/install/migrations_test.exs
    - test/chimeway/install/golden_diff_test.exs
    - test/chimeway/install/prefix_contract_test.exs
    - test/chimeway/migration_contract_test.exs
    - test/chimeway/generated_prefixed_runtime_proof_test.exs
    - test/chimeway/runtime_prefix_integration_test.exs
    - test/fixtures/installer_golden_public/STDOUT.txt
    - test/fixtures/installer_golden_prefixed/STDOUT.txt
  </read_first>
  <files>lib/chimeway/install/migrations.ex, test/chimeway/install/migrations_test.exs, test/chimeway/install/golden_diff_test.exs, test/chimeway/install/prefix_contract_test.exs, test/chimeway/migration_contract_test.exs, test/chimeway/generated_prefixed_runtime_proof_test.exs, test/chimeway/runtime_prefix_integration_test.exs, test/fixtures/installer_golden_public/STDOUT.txt, test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_create_chimeway_delivery_targets.exs, test/fixtures/installer_golden_prefixed/STDOUT.txt, test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_create_chimeway_delivery_targets.exs</files>
  <behavior>
    - Test 1: installer discovery returns 35 ordered templates and generates migration 035 once on repeated runs.
    - Test 2: public output contains bare target/attempt relations and prefixed output qualifies every table, reference, and index through the established helper contract.
    - Test 3: both generated migrations run against PostgreSQL and enforce target identity plus attempt ordinal uniqueness.
    - Test 4: ordinary runtime target planning writes to the configured static store in public and chimeway modes without accepting a tenant-derived prefix option.
  </behavior>
  <action>Start RED by changing installer count/slug expectations to 35 and adding dual-mode schema assertions, then register `035_create_chimeway_delivery_targets` in the exact expected slug surface. Render and review public/prefixed fixtures through the documented golden acceptance command; update both STDOUT contracts and exact trees. Extend prefix contracts to assert all target tables, references, indexes, and rollback operations use the rendered static prefix helper, with the public branch remaining legacy-unprefixed. Extend the real PostgreSQL migration contract loop to migrate both modes, insert two tenants with the same opaque revision under distinct deliveries, reject a duplicate `{delivery_id, binding_revision_ref}`, accept successive target attempt numbers, reject a duplicate `{delivery_target_id, attempt_number}`, and roll back without touching host-owned schemas. Extend existing generated-prefixed/runtime-prefix tests with one ordinary resolver/planning write and query through normal Repo defaults; no target, recovery, or worker API may accept an operation-level prefix or derive one from tenant/binding data. Keep the migration additive and preserve existing tables/data; do not add a package or provider dependency.</action>
  <reversibility rating="costly">Generated migration output is copied into adopter repositories, so correcting a released schema requires a follow-on migration across four parity surfaces.</reversibility>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/install/migrations.ex test/chimeway/install/migrations_test.exs test/chimeway/install/golden_diff_test.exs test/chimeway/install/prefix_contract_test.exs test/chimeway/migration_contract_test.exs test/chimeway/generated_prefixed_runtime_proof_test.exs test/chimeway/runtime_prefix_integration_test.exs test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_create_chimeway_delivery_targets.exs test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_create_chimeway_delivery_targets.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/install/migrations_test.exs test/chimeway/install/golden_diff_test.exs test/chimeway/install/prefix_contract_test.exs test/chimeway/migration_contract_test.exs --warnings-as-errors &amp;&amp; mix verify.runtime_prefix &amp;&amp; mix verify.install_golden</automated>
  </verify>
  <acceptance_criteria>
    - Installer tests assert exactly 35 deterministic ordered templates and idempotent regeneration.
    - `env MIX_ENV=test mix test test/chimeway/install/migrations_test.exs test/chimeway/install/golden_diff_test.exs test/chimeway/install/prefix_contract_test.exs test/chimeway/migration_contract_test.exs --warnings-as-errors` exits zero.
    - Real PostgreSQL assertions prove both unique indexes reject duplicates in both static storage modes.
    - `mix verify.runtime_prefix` and `mix verify.install_golden` exit zero with the new target migration included.
    - Exact generated migration assertions show no tenant/binding-controlled prefix input and no raw target material column.
  </acceptance_criteria>
  <done>Every supported install path creates the same target identity and attempt-history contract before runtime target work executes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical template → copied host migration | Rendering must preserve constraints and static storage selection exactly. |
| Generated migration → PostgreSQL | Database identity is the final concurrency boundary under races. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-99-02-01 | Tampering | Generated migration parity | high | mitigate | Lock exact public/prefixed output and execute both through real PostgreSQL contract tests. | `mix verify.install_golden &amp;&amp; env MIX_ENV=test mix test test/chimeway/migration_contract_test.exs --warnings-as-errors` |
| T-99-02-02 | Elevation of Privilege | Static storage routing | high | mitigate | Keep prefix selection in generated helpers/Repo defaults and reject domain-derived operation prefixes. | `mix verify.runtime_prefix` |
| T-99-02-03 | Denial of Service | Duplicate target/attempt inserts | medium | mitigate | Enforce unique indexes and prove conflicts deterministically in both modes. | `env MIX_ENV=test mix test test/chimeway/migration_contract_test.exs --warnings-as-errors` |
| T-99-02-SC | Tampering | Package supply chain | low | accept | No package-manager install or dependency change occurs. | `git diff --exit-code -- mix.exs mix.lock` |

ASVS L1 disposition: migration drift and storage-boundary escalation are high severity and receive executable dual-mode proof.
</threat_model>

## Artifacts this phase produces

- Changed installer registry/count: `Chimeway.Install.Migrations` recognizes 35 templates including `035_create_chimeway_delivery_targets`.
- New generated migration fixtures: public and prefixed `TIMESTAMP_create_chimeway_delivery_targets.exs` plus their updated `STDOUT.txt` manifests.
- Changed executable contracts: installer ordering/idempotency, golden diff, prefix helper coverage, real dual-mode PostgreSQL uniqueness, generated-prefixed runtime, and runtime-prefix target planning.
- New database guarantees: unique `{delivery_id, binding_revision_ref}` targets and unique `{delivery_target_id, attempt_number}` attempts in repository, public, and prefixed stores.
- New modules, functions, flags, reason tokens, packages, and environment variables: none.

<verification>
Run the focused installer/migration suite, `mix verify.install_golden`, and `mix verify.runtime_prefix`; all evidence is machine-executable and credential-free.
</verification>

<success_criteria>
Migration 035 is deterministic, prefix-correct, executable, idempotent, and constraint-equivalent across repository, copied, public, and prefixed forms.
</success_criteria>

<output>
Create `.planning/phases/99-multi-installation-delivery-recovery/99-02-SUMMARY.md` when done.
</output>
