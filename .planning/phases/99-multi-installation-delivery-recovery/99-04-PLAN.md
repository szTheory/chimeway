---
phase: 99-multi-installation-delivery-recovery
plan: "04"
type: execute
wave: 3
depends_on: ["99-01", "99-03"]
files_modified:
  - lib/chimeway/delivery_targets.ex
  - lib/chimeway/dispatch/executor.ex
  - lib/chimeway/dispatch/sync.ex
  - lib/chimeway/dispatch/oban.ex
  - lib/chimeway/dispatch/oban_worker.ex
  - test/chimeway/dispatch/target_worker_test.exs
autonomous: true
requirements: [PUSH-02, PUSH-03, RECOV-02]
must_haves:
  truths:
    - "D-05/PUSH-02 concurrency: parallel sync/Oban executions receive adapter authority from one tenant-qualified atomic target claim, so only one request starts for a claim generation."
    - "D-06/PUSH-03 idempotency: duplicate jobs and repeated execution short-circuit terminal/already-claimed/ineligible targets without a new attempt or provider callback."
    - "D-03/D-11/PUSH-03 concurrency: database target identity and claims, not job uniqueness, decide provider-call eligibility under races."
    - "D-06: every actual adapter invocation has an existing ordered attempt_started row and claim evidence before I/O."
    - "D-07/RECOV-02: process interruption after callback entry but before conclusive recording closes the stale started attempt as ambiguous_handoff and does not automatically resend."
    - "D-08: an explicit policy-authorized redrive creates a new numbered attempt linked by prior_attempt_id with duplicate_risk=true; it never rewrites or hides the ambiguous predecessor."
  assumptions:
    - statement: "RECOV-02 unclassified edge probe: no additional edge category was inferable beyond pre-I/O evidence, ambiguous closeout, and policy-gated linked redrive; verifier must flag any uncovered crash boundary."
      status: unresolved
      flagged: true
  artifacts:
    - path: "lib/chimeway/dispatch/executor.ex"
      provides: "Shared target pre-I/O and conclusive-result execution path"
    - path: "lib/chimeway/dispatch/oban_worker.ex"
      provides: "Target-ID/tenant-ID job execution with database claim authority"
    - path: "test/chimeway/dispatch/target_worker_test.exs"
      provides: "Race and fault-injection proof for duplicate and ambiguous handoffs"
  key_links:
    - from: "lib/chimeway/dispatch/oban_worker.ex"
      to: "lib/chimeway/delivery_targets.ex"
      via: "worker carries durable target/tenant IDs and must obtain atomic claim authority"
      pattern: "delivery_target_id|begin_target_attempt"
    - from: "lib/chimeway/dispatch/executor.ex"
      to: "lib/chimeway/target_adapter.ex"
      via: "attempt_started persistence precedes deliver/2; safe result recording follows callback"
      pattern: "run_target"
    - from: "lib/chimeway/delivery_targets.ex"
      to: "lib/chimeway/delivery_target_attempt.ex"
      via: "stale started closeout and policy redrive preserve prior_attempt_id/duplicate_risk"
      pattern: "ambiguous_handoff|duplicate_risk"
  prohibitions:
    - statement: "Oban uniqueness, job presence, or process-local state must not authorize a provider request without a successful durable target claim."
      status: unverified
      flagged: true
    - statement: "A possible-handoff crash must not be labeled known-unsent, silently retried, or presented as exactly-once delivery."
      status: unverified
      flagged: true
    - statement: "Target jobs and telemetry must not carry raw token, endpoint, identity, credential, or provider payload material."
      status: unverified
      flagged: true
---

<objective>
Make target execution race-safe and crash-honest across synchronous and Oban dispatch.

Purpose: Guarantee that every possible provider request has durable pre-I/O evidence and that the unavoidable post-handoff crash window remains explicit instead of triggering a blind resend.
Output: Target-aware jobs, atomic claim/start authorization, fault-injected ambiguous closeout, and linked policy redrive evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
@.planning/phases/99-multi-installation-delivery-recovery/99-RESEARCH.md
@.planning/phases/99-multi-installation-delivery-recovery/99-PATTERNS.md
@.planning/phases/99-multi-installation-delivery-recovery/99-01-SUMMARY.md
@.planning/phases/99-multi-installation-delivery-recovery/99-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate every sync and Oban target request on one durable claim/start</name>
  <read_first>
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/dispatch/executor.ex
    - lib/chimeway/dispatch/sync.ex
    - lib/chimeway/dispatch/oban.ex
    - lib/chimeway/dispatch/oban_worker.ex
    - lib/chimeway/dispatch/deferred_resume_worker.ex
    - test/chimeway/dispatch/oban_worker_test.exs
    - test/chimeway/integration/delivery_lifecycle_test.exs
    - test/chimeway/orchestration/recovery_test.exs
  </read_first>
  <files>lib/chimeway/delivery_targets.ex, lib/chimeway/dispatch/executor.ex, lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban.ex, lib/chimeway/dispatch/oban_worker.ex, test/chimeway/dispatch/target_worker_test.exs</files>
  <behavior>
    - Test 1: two concurrent `run_target` calls yield one successful claim/start and one noop, while the fake adapter call counter equals one.
    - Test 2: duplicate Oban insertions/jobs may coexist or collapse, but concurrent `perform/1` calls still create one started attempt and one adapter invocation.
    - Test 3: terminal, unexpired-claimed, expired, invalidated, retry-exhausted, wrong-tenant, and otherwise ineligible targets return non-disclosing noops before adapter I/O.
    - Test 4: target jobs contain exactly `delivery_target_id` and `tenant_id`; telemetry contains durable IDs and allowlisted classifications only.
  </behavior>
  <action>Extend the tracer target path across Sync and Oban without changing existing non-push worker behavior. Enqueue one target-aware job per actionable child using only `delivery_target_id` and explicit `tenant_id`; use those keys for Oban uniqueness as scheduling defense-in-depth. Make every execution call the same `DeliveryTargets.begin_target_attempt/2` conditional-update/row-lock transaction with target ID, tenant ID, eligible status, and missing/expired lease predicates. Only its claimed result may construct `TargetEnvelope` and call `TargetAdapter.deliver/2`; zero-row/wrong-tenant reloads return a non-disclosing noop and expose no target existence. Persist claim source (`sync`, `oban`, or later `recovery`), lease timestamps, and numbered `attempt_started` before leaving the transaction. Classify generic target adapter results into provider-independent stable target outcomes and closed safe facts; leave APNs-specific reasons and retry mapping to Phase 100. Add barriers/fake counters that race actual claim APIs and worker `perform/1`, assert persisted evidence before callback entry, and verify terminal/ineligible short-circuits. Keep the parent Delivery aggregate derived from target outcomes and preserve current email/other channel worker tests.</action>
  <reversibility rating="costly">Target-aware Oban args and the shared claim contract affect queued work and both dispatch strategies, but use additive durable IDs and can be migrated.</reversibility>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/delivery_targets.ex lib/chimeway/dispatch/executor.ex lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban.ex lib/chimeway/dispatch/oban_worker.ex test/chimeway/dispatch/target_worker_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs test/chimeway/dispatch/oban_worker_test.exs test/chimeway/integration/delivery_lifecycle_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Barrier-based sync and worker races exit zero with exactly one adapter invocation and one started attempt for the claim generation.
    - Persist-before-I/O assertion executes inside the fake adapter and observes claim source, lease, attempt number, and `attempt_started`.
    - Exact job-arg and telemetry assertions contain only durable target/tenant IDs plus allowlisted classifications.
    - Each terminal/ineligible/wrong-tenant case produces zero adapter calls and no new attempt; wrong tenant does not expose row existence.
    - Existing non-push Oban worker and delivery lifecycle tests remain green.
  </acceptance_criteria>
  <done>Duplicate planning/jobs/processes cannot turn into an unexplained additional provider request because durable target claims authorize all I/O.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close stale starts as ambiguous and link policy-authorized redrives</name>
  <read_first>
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/dispatch/executor.ex
    - lib/chimeway/delivery_target_attempt.ex
    - lib/chimeway/safe_evidence.ex
    - test/chimeway/dispatch/target_worker_test.exs
    - .planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
  </read_first>
  <files>lib/chimeway/delivery_targets.ex, lib/chimeway/dispatch/executor.ex, test/chimeway/dispatch/target_worker_test.exs</files>
  <behavior>
    - Test 1: injected process death after fake adapter entry but before result persistence leaves `attempt_started`; stale-close marks that attempt/target `ambiguous_handoff` and does not invoke the adapter again.
    - Test 2: normal retry discovery excludes ambiguous targets and repeated stale-close is idempotent.
    - Test 3: `authorize_target_redrive/3` requires the exact tenant and ambiguous prior attempt, creates a new pending claim generation linked to that attempt, and the next attempt carries `prior_attempt_id` plus `duplicate_risk: true`.
    - Test 4: wrong-tenant, non-ambiguous, already-redriven, expired, or invalidated redrive requests return non-disclosing noops and never call the adapter.
  </behavior>
  <action>Per the resolved research contract and D-07/D-08, add an explicit stale-start closeout transaction that selects only tenant-qualified expired claims with a latest `attempt_started`, locks target/attempt, and closes both attempt and target in the terminal, non-actionable `ambiguous_handoff` outcome. Emit closed reason `possible_provider_handoff`, clear active lease authority, recompute the parent, and never make this state eligible for automatic resend. Make repeated closeout idempotent. Add named `DeliveryTargets.authorize_target_redrive/3` as the only D-08 escape hatch: require host policy evidence expressed as a bounded allowlisted reason, exact tenant, exact ambiguous prior attempt, and a not-yet-redriven state; create a new numbered attempt linked to the terminal ambiguous predecessor with `prior_attempt_id` and `duplicate_risk: true`, without reopening, rewriting, or hiding it. It must never emit exactly-once or known-unsent language. Fault-inject process exit at the callback boundary and prove no automatic callback follows stale closeout. Keep this provider-independent; do not classify APNs responses, invent APNs IDs, or decide retry policy for Phase 100.</action>
  <reversibility rating="costly">Ambiguous-handoff and linked-redrive semantics are durable safety contracts used by recovery and later provider adapters.</reversibility>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/delivery_targets.ex lib/chimeway/dispatch/executor.ex test/chimeway/dispatch/target_worker_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Fault-injection test proves callback entry occurred, conclusive result did not persist, stale closeout wrote `ambiguous_handoff`, and callback count remained one after recovery polling.
    - Repeated stale-close and repeated redrive authorization assertions are idempotent and preserve exact attempt row counts.
    - Authorized redrive produces the next attempt number with exact prior link and duplicate-risk evidence; the earlier ambiguous row remains queryable.
    - Wrong-tenant/ineligible redrive paths expose no target/attempt facts and produce no adapter invocation.
    - Persisted/traced redrive facts contain only durable IDs, stable reason tokens, boolean duplicate risk, and allowlisted timestamps.
  </acceptance_criteria>
  <done>The post-handoff crash window is explicit and non-retrying by default, while any later host-authorized risk is durably linked and honestly labeled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Job/process → target claim | Duplicate workers must not independently authorize provider I/O. |
| Adapter callback → conclusive database result | A crash can leave provider acceptance unknown and must remain ambiguous. |
| Host policy → redrive | Retrying ambiguous work increases duplicate risk and requires explicit tenant-safe authorization/evidence. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-99-04-01 | Tampering | Concurrent target claim | high | mitigate | Use tenant-qualified conditional update/lock as the sole adapter authority and race real workers. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-04-02 | Repudiation | Post-handoff crash window | high | mitigate | Persist start before I/O, fault-inject callback-boundary death, close stale work ambiguous, and forbid automatic resend. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-04-03 | Elevation of Privilege | Redrive authorization | high | mitigate | Require exact tenant/ambiguous predecessor/policy reason and link the next attempt with duplicate-risk evidence. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-04-04 | Information Disclosure | Job args and telemetry | high | mitigate | Restrict to durable IDs and closed classifications; scan exact emitted structures. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-04-SC | Tampering | Package supply chain | low | accept | No package-manager install or dependency change occurs. | `git diff --exit-code -- mix.exs mix.lock` |

ASVS L1 disposition: every high-severity duplicate-handoff, crash-truth, tenant-authority, and leakage threat is exercised through machine-run races or fault injection.
</threat_model>

## Artifacts this phase produces

- Changed execution functions: `Executor.run_target/1`, target branches in `Dispatch.Sync`, `Dispatch.Oban`, and `Dispatch.ObanWorker.perform/1`.
- Changed durable APIs: `DeliveryTargets.begin_target_attempt/2`, `close_stale_started_attempt/2`, and `authorize_target_redrive/3`.
- Changed target job contract: args `delivery_target_id` and `tenant_id`; unique-job keys are defense-in-depth only.
- New/confirmed status and reason tokens: `claimed`, `attempt_started`, `ambiguous_handoff`, `possible_provider_handoff`, and linked redrive `duplicate_risk`.
- New test path: `test/chimeway/dispatch/target_worker_test.exs` with barriers, callback counters, and process-death fault injection.
- New APNs status mappings, IDs, headers, payloads, endpoints, credentials, collapse keys, and device/open behavior: none.

<verification>
Run focused target-worker race/fault tests plus the existing Oban and non-push lifecycle regressions. Require exact call/row counts and zero exits; no human verification applies.
</verification>

<success_criteria>
Every possible target request has durable prior evidence, concurrent execution cannot duplicate claim authority, and a possible-handoff crash remains ambiguous until an explicitly linked risk-aware redrive.
</success_criteria>

<output>
Create `.planning/phases/99-multi-installation-delivery-recovery/99-04-SUMMARY.md` when done.
</output>
