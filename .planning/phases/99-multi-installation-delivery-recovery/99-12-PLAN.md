---
phase: 99-multi-installation-delivery-recovery
plan: "12"
type: execute
wave: 9
depends_on: ["99-11"]
files_modified:
  - lib/chimeway/delivery_targets.ex
  - test/chimeway/delivery_target_test.exs
  - priv/repo/migrations/20260820000000_enforce_delivery_target_tenant_integrity.exs
  - priv/chimeway_migrations/036_enforce_delivery_target_tenant_integrity.exs
  - test/chimeway/migration_contract_test.exs
  - test/chimeway/install/migrations_test.exs
  - test/chimeway/install/golden_diff_test.exs
  - test/chimeway/install/prefix_contract_test.exs
  - test/fixtures/installer_golden_prefixed/STDOUT.txt
  - test/fixtures/installer_golden_public/STDOUT.txt
  - test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_enforce_delivery_target_tenant_integrity.exs
  - test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_enforce_delivery_target_tenant_integrity.exs
autonomous: true
gap_closure: true
requirements: [PUSH-01, PUSH-02, PUSH-03, PUSH-04, RECOV-01, RECOV-02]
must_haves:
  truths:
    - "D-05/D-11/PUSH-03: every public target lifecycle operation locks an exact tenant-owned target and authorizes its requested transition only from that operation's explicit source-state set."
    - "D-03/D-11/PUSH-03: a provider_accepted target remains provider_accepted after ordinary retry, expiry, and invalidation requests, remains absent from actionable snapshots, and causes no additional TargetAdapter invocation."
    - "D-01/D-05/PUSH-02: PostgreSQL accepts a delivery target only when tenant_id and delivery_id identify the same logical delivery, and accepts an attempt only when tenant_id and delivery_target_id identify the same child target."
    - "D-06/D-08/RECOV-02: prior_attempt_id, when present, identifies an attempt with the same tenant_id and delivery_target_id as the new attempt."
    - "D-10/PUSH-02/RECOV-01: repository migrations and copied migration 036 enforce identical structural tenant ownership in legacy public and dedicated chimeway schema modes."
  artifacts:
    - path: "lib/chimeway/delivery_targets.ex"
      provides: "Locked operation-specific source-state predicates for retry, expiry, and invalidation"
    - path: "priv/repo/migrations/20260820000000_enforce_delivery_target_tenant_integrity.exs"
      provides: "Forward repository repair for composite tenant ownership and same-target prior-attempt lineage"
    - path: "priv/chimeway_migrations/036_enforce_delivery_target_tenant_integrity.exs"
      provides: "Prefix-safe adopter repair migration for both supported static storage modes"
    - path: "test/chimeway/migration_contract_test.exs"
      provides: "Executable repository and dual generated-mode rejection evidence for malformed tenant relationships"
    - path: "test/chimeway/delivery_target_test.exs"
      provides: "Accepted-target non-reauthorization and no-adapter-handoff regression evidence"
  key_links:
    - from: "lib/chimeway/delivery_targets.ex"
      to: "lib/chimeway/dispatch/executor.ex"
      via: "only an operation-authorized transition can restore pending status consumed by begin_target_attempt/2"
      pattern: "schedule_retry|allowed_source|pending|begin_target_attempt"
    - from: "chimeway_delivery_targets.(tenant_id,delivery_id)"
      to: "chimeway_deliveries.(tenant_id,id)"
      via: "named composite foreign key backed by a named unique index"
      pattern: "tenant_id.*delivery_id|tenant_id.*id|FOREIGN KEY"
    - from: "chimeway_delivery_target_attempts.(tenant_id,delivery_target_id,prior_attempt_id)"
      to: "chimeway_delivery_target_attempts.(tenant_id,delivery_target_id,id)"
      via: "self-referential composite foreign key enforcing same-target predecessor lineage"
      pattern: "prior_attempt_id|delivery_target_id|FOREIGN KEY"
    - from: "priv/chimeway_migrations/036_enforce_delivery_target_tenant_integrity.exs"
      to: "test/fixtures/installer_golden_{prefixed,public}"
      via: "installer rendering and golden acceptance preserve one repair contract in both static storage modes"
      pattern: "@chimeway_prefix|quoted_relation|enforce_delivery_target_tenant_integrity"
  flagged_assumptions:
    - "PUSH-01 adjacency remains unresolved by the spec-less edge fallback and is unchanged by this closure."
    - "PUSH-01 empty remains unresolved by the spec-less edge fallback and is unchanged by this closure."
    - "PUSH-01 ordering remains unresolved by the spec-less edge fallback and is unchanged by this closure."
    - "PUSH-02 adjacency remains unresolved by the spec-less edge fallback and is unchanged by this closure."
    - "PUSH-02 empty remains unresolved by the spec-less edge fallback and is unchanged by this closure."
    - "PUSH-02 ordering remains unresolved by the spec-less edge fallback and is unchanged by this closure."
    - "PUSH-02 idempotency remains unresolved by the spec-less edge fallback and is unchanged by this closure."
    - "PUSH-02 concurrency remains unresolved by the spec-less edge fallback and is unchanged except for the authoritative structural tenant constraints in this closure."
    - "PUSH-03 idempotency remains unresolved by the spec-less edge fallback and is unchanged except for accepted-target retry authorization in this closure."
    - "PUSH-03 concurrency remains unresolved by the spec-less edge fallback and is unchanged except for locked source-state authorization in this closure."
    - "PUSH-04 adjacency remains unresolved by the spec-less edge fallback and is unchanged by this closure."
    - "PUSH-04 empty remains unresolved by the spec-less edge fallback and is unchanged by this closure."
    - "PUSH-04 ordering remains unresolved by the spec-less edge fallback and is unchanged by this closure."
    - "RECOV-01 was unclassified by the spec-less edge fallback; this closure changes only structural tenant ownership inherited by recovery queries."
    - "RECOV-02 was unclassified by the spec-less edge fallback; this closure changes only same-target prior-attempt lineage enforcement."
  prohibitions:
    - statement: "Chimeway must not persist raw installation tokens, endpoints, credentials, or uncontrolled resolver payloads while repairing target ownership."
      status: unverified
      flagged: true
    - statement: "Provider acceptance must not be represented as device receipt, display, app handling, protected open, inbox state, engagement, or all-installation delivery."
      status: unverified
      flagged: true
---

<objective>
Close the remaining duplicate-handoff and durable tenant-integrity blockers without reopening completed Phase 99 behavior.

Purpose: Make target reauthorization operation-specific under lock and make tenant/target lineage a PostgreSQL-enforced invariant for repository and adopter installations, per D-01 through D-12.
Output: Accepted-target non-reauthorization, forward composite-key repair migrations, same-target predecessor enforcement, and deterministic repository/public/prefixed migration-contract evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
@.planning/phases/99-multi-installation-delivery-recovery/99-RESEARCH.md
@.planning/phases/99-multi-installation-delivery-recovery/99-PATTERNS.md
@.planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
@.planning/phases/99-multi-installation-delivery-recovery/99-11-SUMMARY.md
@.planning/phases/99-multi-installation-delivery-recovery/COVERAGE.md

<interfaces>
Existing contracts to refine:
- `DeliveryTargets.schedule_retry/3`, `expire_target/3`, and `invalidate_target/3` currently delegate to private `transition_target/4`; `begin_target_attempt/2` consumes only pending targets.
- A known pre-handoff adapter failure already closes its attempt as failed and restores its target to pending; policy-authorized ambiguous re-drive is a separate linked-attempt path.
- Migration 035 has already shipped in repository and copied forms, so repair is additive migration 036 rather than rewriting migration history.
- PostgreSQL composite foreign keys require an exact unique referenced column set; Ecto's single-column `references/2` is insufficient for the composite relationships, so named `execute/1` DDL is the supported seam.
- `CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1` is the existing database-contract isolation flag; use it with `--max-cases 1` to avoid unrelated partner pools and make the migration proof deterministic after the prior `too_many_connections` failure.
- COVERAGE.md declares no external API integration for this gap closure; there is no API matrix to create.
</interfaces>
</context>

<assumption_delta_decision>
The generalized primary identity remains the tenant-scoped child target/binding revision beneath one logical delivery; decision: no-change, because both blockers repair authorization and relational ownership without changing D-01/D-03 identity semantics.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock lifecycle authorization to operation-specific source states</name>
  <read_first>
    - .planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
    - .planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/dispatch/executor.ex
    - lib/chimeway/dispatch/sync.ex
    - test/chimeway/delivery_target_test.exs
    - test/chimeway/dispatch/target_worker_test.exs
  </read_first>
  <files>lib/chimeway/delivery_targets.ex, test/chimeway/delivery_target_test.exs</files>
  <behavior>
    - Test 1: `schedule_retry/3` changes an exact same-tenant failed target to pending only while holding its row lock and recomputes the parent.
    - Test 2: `schedule_retry/3` against provider_accepted, retry_exhausted, expired, invalidated, ambiguous_handoff, claimed, or pending returns the closed non-authorizing result, preserves every row and aggregate fact, and creates no attempt.
    - Test 3: after rejected ordinary retry of provider_accepted, both Sync and Oban public execution paths omit the target and the configured TargetAdapter receives no call.
    - Test 4: expiry and invalidation use their own explicit locked source sets and cannot overwrite provider_accepted, retry_exhausted, ambiguous_handoff, or another terminal outcome.
    - Test 5: wrong-tenant and stale parent/target combinations remain non-disclosing and cannot authorize a transition.
  </behavior>
  <action>Write the public-path regressions first. Replace the status-agnostic private `transition_target/4` authority with an operation-specific helper that receives an operation atom, destination state, and literal allowed source-state set, and include the source predicate in the tenant-qualified `FOR UPDATE` target query. For ordinary `schedule_retry/3`, authorize only the documented conclusive `:failed` source; the pre-handoff executor path already restores retryable work directly to pending, and D-08 policy-authorized `:ambiguous_handoff` re-drive remains exclusively in its linked-attempt function. For expiry and invalidation, authorize only pre-handoff actionable `:pending` rows; claimed work must finish or close through the exact attempt finalizers so possible provider handoff cannot be erased. Return the existing closed not-found/non-authorizing result class for every disallowed or foreign state, without revealing current status. Keep terminal attempt insertion and parent recomputation inside the same transaction after authorization. Per D-05 and D-11, terminal or already-claimed targets short-circuit before provider I/O; per D-06 through D-09, do not rewrite attempt evidence, reopen ambiguous work, or change provider_accepted semantics. Do not alter APNs classification, resolver ordering, adapter payloads, or the policy-authorized redrive contract.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/delivery_targets.ex test/chimeway/delivery_target_test.exs &amp;&amp; CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs test/chimeway/dispatch/target_worker_test.exs --max-cases 1 --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source inspection shows each lifecycle operation passes a literal allowed source-state set and the source predicate is evaluated by the locked tenant-qualified query before mutation.
    - Ordinary retry authorizes exactly failed to pending; ambiguous handoff remains reachable only through the policy-authorized linked re-drive path.
    - A provider_accepted target remains byte-for-byte unchanged after retry, expiry, and invalidation requests, remains absent from actionable target snapshots, and produces zero adapter callback messages through Sync and Oban controls.
    - Disallowed and foreign states return one closed non-authorizing shape without exposing the target's current lifecycle state.
    - Existing pre-handoff retry, ambiguous closeout, retry exhaustion, and linked-redrive tests remain green.
  </acceptance_criteria>
  <reversibility rating="reversible">The source-state matrix and regression tests change internal authorization logic behind existing public functions without changing stored data or signatures.</reversibility>
  <done>Public lifecycle functions cannot reopen provider-accepted or otherwise ineligible target truth and thereby authorize an unexplained provider request.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the forward composite tenant-integrity repair</name>
  <read_first>
    - .planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
    - priv/repo/migrations/20260819000001_create_chimeway_delivery_targets.exs
    - priv/chimeway_migrations/035_create_chimeway_delivery_targets.exs
    - priv/repo/migrations/20260813000000_privacy_safe_delivery_evidence.exs
    - priv/chimeway_migrations/034_privacy_safe_delivery_evidence.exs
    - test/chimeway/migration_contract_test.exs
    - lib/chimeway/delivery.ex
    - lib/chimeway/delivery_target.ex
    - lib/chimeway/delivery_target_attempt.ex
  </read_first>
  <files>priv/repo/migrations/20260820000000_enforce_delivery_target_tenant_integrity.exs, priv/chimeway_migrations/036_enforce_delivery_target_tenant_integrity.exs, test/chimeway/migration_contract_test.exs</files>
  <behavior>
    - Test 1: repository migration 036 upgrades an existing valid migration-035 dataset and retains its targets and ordered attempts.
    - Test 2: inserting a target with a delivery ID owned by another tenant fails with the named composite target-to-delivery foreign key.
    - Test 3: inserting an attempt with a target ID owned by another tenant fails with the named composite attempt-to-target foreign key.
    - Test 4: inserting an attempt whose prior_attempt_id belongs to another tenant or another delivery_target fails with the named same-target predecessor foreign key; a predecessor on the same tenant and target succeeds.
    - Test 5: deleting a delivery still cascades through targets and attempts, and down removes only migration-036 constraints/indexes while preserving migration-035 tables.
  </behavior>
  <action>Create additive repository migration `Chimeway.Repo.Migrations.EnforceDeliveryTargetTenantIntegrity` at timestamp 20260820000000 and copied template 036 with the same module and `@chimeway_prefix __CHIMEWAY_PREFIX__`; leave both migration-035 files unchanged because installed databases may already have recorded them. In `up/0`, add named unique indexes on deliveries `(tenant_id,id)`, targets `(tenant_id,id)`, and attempts `(tenant_id,delivery_target_id,id)`, then use prefix-qualified PostgreSQL `ALTER TABLE ... ADD CONSTRAINT` statements for: target `(tenant_id,delivery_id)` to delivery `(tenant_id,id)` with `ON DELETE CASCADE`; attempt `(tenant_id,delivery_target_id)` to target `(tenant_id,id)` with `ON DELETE CASCADE`; and attempt `(tenant_id,delivery_target_id,prior_attempt_id)` to attempts `(tenant_id,delivery_target_id,id)` with PostgreSQL's default non-cascading delete action for same-target lineage. Name every index and constraint explicitly as listed in the artifacts section, quote identifiers through fixed relation/identifier helpers, and do not interpolate runtime tenant or binding input. Order `down/0` as constraint drops before unique-index drops; preserve migration-035 tables and existing ID-only constraints. Let `up/0` fail closed if legacy malformed rows exist rather than deleting, reassigning, or silently normalizing tenant history. Extend the repository-tagged migration contract with helpers that insert tenant-mismatched target/attempt rows and cross-target/cross-tenant predecessors, assert the exact PostgreSQL foreign-key constraint names and delete actions, and include valid controls plus the tested safe down boundary. Keep generated public/prefixed execution out of this task because fixture 036 does not exist until Task 3 refreshes both golden trees. Run only the repository-tagged database contract here with the existing partner-repo skip flag and one ExUnit case so PostgreSQL connection limits cannot make evidence nondeterministic. This implements D-01/D-05 target ownership, D-06/D-08 attempt lineage, and D-10 tenant scope without introducing dynamic prefixes or changing public identity.</action>
  <verify>
    <automated>mix format --check-formatted priv/repo/migrations/20260820000000_enforce_delivery_target_tenant_integrity.exs priv/chimeway_migrations/036_enforce_delivery_target_tenant_integrity.exs test/chimeway/migration_contract_test.exs &amp;&amp; CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 env MIX_ENV=test mix test test/chimeway/migration_contract_test.exs --only migration_copy:repository --max-cases 1 --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Migration 035 remains unchanged, the repository repair upgrades existing repository storage, and copied template 036 is ready for Task 3 to render and execute in both installer modes.
    - PostgreSQL catalog assertions find the three named unique indexes and three named composite foreign keys in repository storage; Task 3 repeats the complete contract against generated legacy public and dedicated chimeway schema fixtures after fixture 036 exists.
    - The repository-tagged contract proves target/delivery tenant mismatch, attempt/target tenant mismatch, cross-tenant predecessor, and same-tenant cross-target predecessor each fail at the intended named constraint while all coherent controls succeed.
    - Catalog or constraint-definition assertions prove the two ownership foreign keys use cascading deletes and the predecessor foreign key retains PostgreSQL's non-cascading delete action.
    - Existing valid target/attempt history survives up, delivery deletion still cascades, and down preserves migration-035 tables and rows after removing only repair objects.
    - The focused command starts no partner TestRepo pools, runs serially, and passes without relying on a conversational or live-CI fallback.
  </acceptance_criteria>
  <reversibility rating="costly">The tested down migration safely removes only migration-036 constraints and unique indexes while preserving migration-035 tables and rows; rollback is operationally costly because it knowingly restores acceptance of malformed tenant relationships.</reversibility>
  <done>PostgreSQL structurally rejects every cross-tenant target/attempt relationship and every cross-target prior-attempt relationship while preserving valid installed data.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve installer and static-storage parity for migration 036</name>
  <read_first>
    - priv/chimeway_migrations/036_enforce_delivery_target_tenant_integrity.exs
    - test/chimeway/migration_contract_test.exs
    - test/chimeway/install/migrations_test.exs
    - test/chimeway/install/golden_diff_test.exs
    - test/chimeway/install/prefix_contract_test.exs
    - test/fixtures/installer_golden_prefixed/STDOUT.txt
    - test/fixtures/installer_golden_public/STDOUT.txt
    - test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_create_chimeway_delivery_targets.exs
    - test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_create_chimeway_delivery_targets.exs
  </read_first>
  <files>test/chimeway/migration_contract_test.exs, test/chimeway/install/migrations_test.exs, test/chimeway/install/golden_diff_test.exs, test/chimeway/install/prefix_contract_test.exs, test/fixtures/installer_golden_prefixed/STDOUT.txt, test/fixtures/installer_golden_public/STDOUT.txt, test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_enforce_delivery_target_tenant_integrity.exs, test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_enforce_delivery_target_tenant_integrity.exs</files>
  <action>Add `enforce_delivery_target_tenant_integrity` as ordered template 036, update exact installer and fixture counts from 35 to 36, and add golden/prefix assertions for all three unique index column sets and all three named composite constraints. Replace the blanket `CASCADE` text rejection in `MigrationContractTest.assert_no_destructive_schema_cleanup!/1` and the prefixed rollback contract with a precise case-insensitive guard that rejects `DROP SCHEMA` outright and rejects `DROP` of schema objects when its statement requests `CASCADE`; it must allow referential `ON DELETE CASCADE`. Add positive migration-text and PostgreSQL-catalog assertions that `chimeway_delivery_targets_tenant_delivery_fkey` and `chimeway_delivery_target_attempts_tenant_target_fkey` use `ON DELETE CASCADE`, while `chimeway_delivery_target_attempts_prior_same_target_fkey` has the default non-cascading delete action. Refresh both committed golden trees only through `MIX_INSTALLER_ACCEPT_GOLDEN=1 mix test test/chimeway/install/golden_diff_test.exs --warnings-as-errors`, then inspect the resulting diff: prefixed output must quote the dedicated chimeway schema, public output must use unprefixed public relations, neither may retain `__CHIMEWAY_PREFIX__`, and neither may contain a runtime tenant-derived prefix, destructive schema/object-drop cleanup, or relation names outside the fixed Chimeway-owned set. Keep migration 035 golden files and assertions intact; 036 is the repair applied after them. Extend the generated-mode migration contracts so both fixtures migrate all 36 versions through normal `Ecto.Migrator`, find all three named unique indexes and all three composite foreign keys, reject the four malformed ownership/lineage cases by exact constraint name, accept coherent controls, preserve valid rows across up, cascade delivery deletion through targets and attempts, and prove the safe down boundary leaves migration-035 tables and rows intact. Per D-10 and the Phase 74 static-storage contract, use the copied template as the sole generator source and preserve local/CI `verify.install_golden` parity. Retain this 12-file cohesive plan as one verification cycle because Task 1 closes provider-I/O authorization while Tasks 2-3 close the corresponding durable ownership invariants; this task is the explicit post-fixture integration gate that proves the repository and both supported generated storage modes together.</action>
  <verify>
    <automated>mix format --check-formatted test/chimeway/migration_contract_test.exs test/chimeway/install/migrations_test.exs test/chimeway/install/golden_diff_test.exs test/chimeway/install/prefix_contract_test.exs test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_enforce_delivery_target_tenant_integrity.exs test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_enforce_delivery_target_tenant_integrity.exs &amp;&amp; CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 env MIX_ENV=test mix test test/chimeway/migration_contract_test.exs test/chimeway/install/migrations_test.exs test/chimeway/install/golden_diff_test.exs test/chimeway/install/prefix_contract_test.exs --max-cases 1 --warnings-as-errors &amp;&amp; CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 env MIX_ENV=test mix verify.install_golden --warnings-as-errors &amp;&amp; CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 env MIX_ENV=test mix verify.runtime_prefix --warnings-as-errors &amp;&amp; CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 env MIX_ENV=test mix ci.test --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `Migrations.list_templates/0` returns exactly 36 ordered slugs with the tenant-integrity repair at position 36.
    - Both golden trees contain the rendered repair migration and retain the unchanged rendered target-creation migration at position 35.
    - Prefixed and public fixtures contain the same index/constraint contract with only static relation qualification differing; both ownership foreign keys use `ON DELETE CASCADE`, while the predecessor foreign key is non-cascading.
    - The destructive-cleanup guards continue to reject every `DROP SCHEMA` and object-drop `CASCADE` statement without rejecting the two intended referential cascades.
    - The final serial integration command executes repository plus both generated modes after fixture generation, asserts all 36 migrated versions, proves all six generated-mode constraints plus malformed-row rejection, valid controls, delete behavior, and safe down behavior, then passes `mix verify.runtime_prefix` and `mix ci.test` under the partner-repo skip flag.
    - Golden, prefix, and migration-contract suites prove copied migration parity; local `mix verify.install_golden` remains the CI entrypoint.
    - No new dependency, package install, external API matrix, field, or CLI flag is introduced.
  </acceptance_criteria>
  <reversibility rating="reversible">Installer catalog assertions and generated fixtures can be regenerated from the canonical copied migration without altering installed data.</reversibility>
  <done>Every new install receives migration 036 in deterministic order and both supported static storage modes enforce the same tenant-integrity contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Lifecycle caller -> target state machine | Tenant-qualified IDs request mutations that can restore provider-I/O authority. |
| Ecto write -> PostgreSQL relations | Application predicates are reinforced by durable composite tenant ownership. |
| Copied migration -> host repository | Static public/prefixed rendering must preserve fixed identifiers and equivalent constraints. |

## STRIDE Threat Register — ASVS Level 1, block_on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-99-12-01 | Elevation of Privilege / Tampering | delivery target/parent and attempt/target relationships | high | mitigate | Named composite tenant/id foreign keys reject cross-tenant durable ownership in repository and both copied storage modes, with negative SQL contract tests. |
| T-99-12-02 | Tampering / Repudiation | schedule_retry/3 -> begin_target_attempt/2 -> TargetAdapter | high | mitigate | Locked operation-specific source predicates exclude provider_accepted and all non-retryable states; public dispatch regressions prove zero additional adapter calls. |
| T-99-12-03 | Tampering / Repudiation | prior_attempt_id lineage | high | mitigate | Composite self-reference includes tenant, target, and predecessor ID so linked re-drive evidence cannot point to another target. |
| T-99-12-04 | Information Disclosure | wrong-tenant lifecycle request and migration errors | medium | mitigate | Closed non-authorizing results and exact constraint-name assertions avoid returning lifecycle, tenant, SQL, or provider material. |
| T-99-12-05 | Denial of Service | migration contract PostgreSQL pools | medium | mitigate | Existing partner-repo skip flag plus serial ExUnit execution bounds connections and provides deterministic local evidence. |
| T-99-12-SC | Tampering | package installation | low | accept | This plan installs no npm, pip, or cargo package and adds no dependency; the package-legitimacy gate is not applicable. |
</threat_model>

## Artifacts this phase produces

- New repository migration file and module: `priv/repo/migrations/20260820000000_enforce_delivery_target_tenant_integrity.exs`, `Chimeway.Repo.Migrations.EnforceDeliveryTargetTenantIntegrity`.
- New copied migration file: `priv/chimeway_migrations/036_enforce_delivery_target_tenant_integrity.exs`.
- New generated fixture files: public and prefixed `TIMESTAMP_enforce_delivery_target_tenant_integrity.exs`.
- New unique indexes: `chimeway_deliveries_tenant_id_id_unique`, `chimeway_delivery_targets_tenant_id_id_unique`, `chimeway_delivery_target_attempts_tenant_target_id_unique`.
- New foreign-key constraints: `chimeway_delivery_targets_tenant_delivery_fkey`, `chimeway_delivery_target_attempts_tenant_target_fkey`, `chimeway_delivery_target_attempts_prior_same_target_fkey`.
- New internal lifecycle symbol(s): literal operation-specific allowed-source sets/helper replacing status-agnostic transition authority; exact private identifier may follow existing module naming conventions.
- New migration-contract helpers/assertions for foreign-key violation names and coherent/cross-tenant/cross-target controls.
- New fields: none. New CLI flags: none. New external API integration: none, per `COVERAGE.md`.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Deliver to eligible opaque installations with independent tenant-safe truth and recovery | 99-12 | COVERED | Closes the two authoritative blockers without duplicating completed fan-out. |
| REQ | PUSH-01 | Opaque tenant-scoped resolver revisions | 99-12 | COVERED | Preserved; privacy prohibition and identity no-change decision are explicit. |
| REQ | PUSH-02 | Independent tenant-safe target lifecycle/history | 99-12 | COVERED | Composite tenant ownership and same-target lineage. |
| REQ | PUSH-03 | No duplicate target or unexplained provider request | 99-12 | COVERED | Locked retry authority and accepted-target no-I/O proof. |
| REQ | PUSH-04 | Stable no-target and honest aggregate outcome | 99-12 | COVERED | Existing outcome behavior preserved and regression suite retained. |
| REQ | RECOV-01 | Bounded tenant-scoped recovery | 99-12 | COVERED | Recovery inherits structural tenant ownership; existing behavior retained. |
| REQ | RECOV-02 | Pre-I/O and ambiguous handoff evidence | 99-12 | COVERED | Same-target predecessor FK and ambiguity/redrive separation retained. |
| RESEARCH | — | Database uniqueness/atomic authorization is the concurrency boundary | 99-12 | COVERED | Source predicates run under row lock; composite uniqueness backs FKs. |
| RESEARCH | — | Migration/template parity in both static storage modes | 99-12 | COVERED | Forward repository/template migration plus dual generated contracts. |
| RESEARCH | — | Tenant non-disclosure and safe evidence | 99-12 | COVERED | Closed mutation results and fixed constraint/error assertions. |
| CONTEXT | D-01–D-04 | Canonical parent, opaque child identity, convergence, honest aggregate | 99-12 | COVERED | Identity no-change; target ownership repaired; aggregate preserved. |
| CONTEXT | D-05–D-09 | Independent handoff state, append-only lineage, ambiguity, redrive, acceptance meaning | 99-12 | COVERED | Operation matrix and same-target predecessor constraint preserve these decisions. |
| CONTEXT | D-10–D-12 | Tenant-bounded recovery, atomic convergence, safe evidence | 99-12 | COVERED | Structural tenant FKs, locked authorization, closed evidence. |
| CONTEXT | Deferred | APNs, CrossWake, adopter twin, physical-device proof | NONE | EXCLUDED | Explicitly owned by Phases 100–103. |

<verification>
- `CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs test/chimeway/dispatch/target_worker_test.exs --max-cases 1 --warnings-as-errors`
- `CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 env MIX_ENV=test mix test test/chimeway/migration_contract_test.exs --max-cases 1 --warnings-as-errors`
- `CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 env MIX_ENV=test mix test test/chimeway/install/migrations_test.exs test/chimeway/install/golden_diff_test.exs test/chimeway/install/prefix_contract_test.exs --max-cases 1 --warnings-as-errors`
- `mix verify.install_golden`
- `mix format --check-formatted` for every plan-owned Elixir file.
</verification>

<success_criteria>
- Provider-accepted and every other non-retryable target state cannot transition to pending through ordinary retry and cannot reach TargetAdapter again.
- Every target belongs structurally to a delivery with the same tenant, every attempt belongs structurally to a target with the same tenant, and every predecessor belongs to the same tenant and target.
- Repository, generated public, and generated prefixed migration contracts all execute serially and reject the four malformed relationship classes with exact named constraints.
- Migration 035 remains historical; forward migration 036 preserves valid data, deterministic copied output, and both static storage modes.
- All six Phase 99 requirements remain represented, all 15 unresolved edge assumptions remain flagged, and no deferred Phase 100–103 work enters the implementation.
</success_criteria>

<output>
Create `.planning/phases/99-multi-installation-delivery-recovery/99-12-SUMMARY.md` when done.
</output>
