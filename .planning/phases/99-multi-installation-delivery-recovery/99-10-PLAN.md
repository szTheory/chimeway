---
phase: 99-multi-installation-delivery-recovery
plan: "10"
type: execute
wave: 7
depends_on: ["99-07"]
files_modified:
  - lib/chimeway/delivery_targets.ex
  - lib/chimeway/dispatch/executor.ex
  - lib/chimeway/dispatch/oban_worker.ex
  - test/chimeway/dispatch/target_worker_test.exs
autonomous: true
gap_closure: true
requirements: [PUSH-02, PUSH-03, PUSH-04, RECOV-01, RECOV-02]
must_haves:
  truths:
    - "D-11/PUSH-04 concurrency: target claim locks and tenant-qualifies both child and parent and authorizes I/O only while the parent is pending/ready."
    - "PUSH-04 adjacency/empty/ordering assumptions: suppressed, cancelled, deferred, terminal, absent, and foreign-parent states all converge to the same non-disclosing no-op before adapter entry, independent of target order."
    - "D-07/RECOV-02: a late provider success can finalize only the exact still-claimed target and still-started attempt; an ambiguous closeout wins permanently when it locks first."
    - "D-06/PUSH-03 idempotency: a final pre-handoff retryable Oban attempt closes the target as retry_exhausted with terminal evidence and completes the job instead of leaving recoverable pending work."
    - "PUSH-03 concurrency assumption: claim, final exhaustion, and late result races are conditional locked transitions, so exactly one terminal history wins and no extra provider request is authorized."
    - "RECOV-01 flagged assumption: ordinary tenant recovery excludes retry_exhausted and ambiguous_handoff targets and therefore cannot resend either terminal outcome."
    - "RECOV-02 flagged assumption: possible-handoff ambiguity remains terminal unless the existing explicit policy_authorized redrive creates linked duplicate-risk evidence."
  artifacts:
    - path: "lib/chimeway/delivery_targets.ex"
      provides: "Parent-gated atomic claims, conditional success finalization, and guarded retry exhaustion"
    - path: "lib/chimeway/dispatch/executor.ex"
      provides: "Non-disclosing handling when a stale provider result loses the durable finalization race"
    - path: "lib/chimeway/dispatch/oban_worker.ex"
      provides: "In-band target retry-budget exhaustion using Oban attempt/max_attempts"
    - path: "test/chimeway/dispatch/target_worker_test.exs"
      provides: "Parent lifecycle, exhaustion/recovery, and stale-closeout/late-success race regressions"
  key_links:
    - from: "lib/chimeway/dispatch/oban_worker.ex"
      to: "lib/chimeway/delivery_targets.ex"
      via: "final pre-handoff target error invokes guarded exhaust_target before returning :ok"
      pattern: "attempt.*max_attempts|exhaust_target"
    - from: "lib/chimeway/dispatch/executor.ex"
      to: "lib/chimeway/delivery_targets.ex"
      via: "adapter success is accepted only by a locked exact target/attempt state transition"
      pattern: "record_target_result"
    - from: "lib/chimeway/delivery_targets.ex"
      to: "lib/chimeway/delivery.ex"
      via: "begin_target_attempt locks parent and child under the same tenant and pending/ready predicate"
      pattern: "FOR UPDATE|orchestration_state"
  prohibitions:
    - "A terminal, suppressed, cancelled, deferred, absent, or foreign parent must not authorize target adapter I/O."
    - "A retry-exhausted target must not return to pending through ordinary execution or recovery."
    - "A stale success result must not overwrite ambiguous_handoff evidence or imply exactly-once delivery."
    - "Worker results and durable evidence must not disclose tenant identity, raw target material, callback values, or provider bodies."
---

<objective>
Close the three durable execution races that can authorize post-terminal sends, unbounded recovery resends, or ambiguity erasure.

Purpose: Make parent lifecycle state, Oban retry budget, and exact locked attempt state jointly authoritative for every target provider request and closeout.
Output: Atomic parent/child claim gating, terminal target exhaustion, fail-closed result finalization, and executable interleaving evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
@.planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
@.planning/phases/99-multi-installation-delivery-recovery/99-04-SUMMARY.md
@.planning/phases/99-multi-installation-delivery-recovery/99-06-SUMMARY.md
@.planning/phases/99-multi-installation-delivery-recovery/99-07-SUMMARY.md

<interfaces>
Existing contracts to refine:
- `DeliveryTargets.begin_target_attempt/2` is the only target-adapter authority and returns `{:noop, :no_eligible_target}` when no claim is available.
- `DeliveryTargets.record_target_failure/4` is the reference locked finalizer: exact tenant, target, and `attempt_started` predicates are required.
- `DeliveryTargets.exhaust_target/3` currently delegates to the generic lifecycle transition and appends `retry_exhausted` evidence.
- Target Oban jobs already carry only `delivery_target_id` and explicit `tenant_id`; Oban supplies `attempt` and `max_attempts` on `%Oban.Job{}`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate target claim and success finalization on current locked lifecycle truth</name>
  <read_first>
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/dispatch/executor.ex
    - lib/chimeway/delivery.ex
    - lib/chimeway/delivery_target.ex
    - lib/chimeway/delivery_target_attempt.ex
    - lib/chimeway/deliveries.ex
    - test/chimeway/dispatch/target_worker_test.exs
    - test/chimeway/orchestration/target_recovery_test.exs
    - .planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
  </read_first>
  <files>lib/chimeway/delivery_targets.ex, lib/chimeway/dispatch/executor.ex, test/chimeway/dispatch/target_worker_test.exs</files>
  <behavior>
    - Test 1: a queued target under each of suppressed, cancelled, failed/succeeded, and pending/deferred parents returns the same noop and never enters the adapter.
    - Test 2: a pending/ready parent and pending same-tenant target still atomically claim, insert attempt_started, and invoke the adapter once.
    - Test 3: after stale closeout changes the exact target and attempt to ambiguous_handoff, a late success finalizer returns a noop/conflict and leaves both rows and parent aggregate unchanged.
    - Test 4: a wrong-tenant or mismatched-parent target is indistinguishable from absent work and leaks no target or parent ID.
  </behavior>
  <action>Start with the parent-state matrix and deterministic stale-closeout/late-success interleaving tests. D-05: keep the target as the independent claim/outcome state machine. D-07: preserve possible-handoff ambiguity when stale closeout wins. D-08: leave only the existing explicit policy-authorized linked redrive as a later-send path. Refactor `begin_target_attempt/2` so one transaction locks the tenant-qualified parent and target in a consistent order and proceeds only when the parent currently has `status: :pending` and `orchestration_state: :ready` and the exact target is `:pending`; apply the same parent predicate to the no-target-ID branch. Return the established non-disclosing noop when any predicate fails. Refactor `record_target_result/4` to ignore caller struct state, lock and reload the exact parent, target, and attempt by IDs plus tenant, require target `:claimed` and attempt `:attempt_started`, then write provider_accepted and recompute the parent inside the guarded transaction. If stale closeout or any competing terminal transition already won, return a non-disclosing noop/conflict without changing target, attempt, or aggregate. Update `Executor.finish_target_attempt` to handle that losing-finalizer result without raising or attempting another write. Preserve `record_target_failure/4` semantics and do not auto-authorize redrive.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/delivery_targets.ex lib/chimeway/dispatch/executor.ex test/chimeway/dispatch/target_worker_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs test/chimeway/orchestration/target_recovery_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The target claim transaction visibly predicates tenant identity, target pending state, parent pending state, and parent ready orchestration state under locks before inserting attempt_started.
    - Suppressed, cancelled, terminal, deferred, foreign, and absent fixtures produce no adapter observation and the same non-disclosing result class.
    - The late-success interleaving leaves target and attempt `ambiguous_handoff`, keeps `possible_provider_handoff`, and does not change the closed parent aggregate.
    - Ordinary pending/ready target execution and explicit linked policy redrive regressions remain green.
  </acceptance_criteria>
  <done>Only a current tenant-matched pending/ready parent can authorize a target request, and stale provider success cannot erase ambiguity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close final target retry exhaustion and prove recovery cannot resend</name>
  <read_first>
    - lib/chimeway/dispatch/oban_worker.ex
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/dispatch/executor.ex
    - lib/chimeway/target_recovery.ex
    - test/chimeway/dispatch/target_worker_test.exs
    - test/chimeway/dispatch/oban_worker_test.exs
    - test/chimeway/orchestration/target_recovery_test.exs
  </read_first>
  <files>lib/chimeway/dispatch/oban_worker.ex, lib/chimeway/delivery_targets.ex, test/chimeway/dispatch/target_worker_test.exs</files>
  <behavior>
    - Test 1: a pre-handoff retryable target job with attempts remaining returns an Oban error, leaves the exact target pending, and records one closed failed attempt.
    - Test 2: the same classification on `attempt == max_attempts` atomically transitions that pending target to retry_exhausted, appends terminal evidence, recomputes the parent, and returns `:ok`.
    - Test 3: a duplicate final job cannot append a second exhaustion record or invoke the adapter again.
    - Test 4: tenant recovery after exhaustion reports/skips terminal work and produces no adapter call or additional attempt.
  </behavior>
  <action>Pattern-match `attempt` and `max_attempts` in the target-job Oban clause. D-10: preserve explicit tenant scope and bounded recovery when proving terminal work is skipped. D-11: make final exhaustion an atomic convergent transition on the existing target. When `Executor.run_target/2` returns `{:error, :pre_handoff_retryable}` with retries remaining, preserve the current retry return. On the final attempt, invoke a tightened `DeliveryTargets.exhaust_target/3` that locks the exact tenant-qualified target and parent, transitions only the expected post-failure `:pending` state to `:retry_exhausted`, appends one terminal lifecycle attempt, recomputes the logical parent, and noops if another terminal transition already won; then return `:ok` so Oban completes rather than discards the job. All ambiguity and unexpected post-callback outcomes remain terminal without this retry path. Add focused proof that `TargetRecovery.recover_tenant/2` does not discover or resend the exhausted target and that duplicate final-job execution adds neither adapter calls nor evidence rows.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/delivery_targets.ex lib/chimeway/dispatch/oban_worker.ex test/chimeway/dispatch/target_worker_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs test/chimeway/dispatch/oban_worker_test.exs test/chimeway/orchestration/target_recovery_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Non-final pre-handoff retry returns an error and final pre-handoff retry returns `:ok` only after durable retry_exhausted target and attempt evidence exists.
    - Exhaustion is conditional on the exact tenant, target, parent, and expected pending post-failure state; a competing terminal result is not overwritten.
    - Duplicate final work leaves one retry_exhausted terminal record and produces no additional adapter observation.
    - A subsequent bounded tenant recovery pass produces no adapter call and no new target attempt for exhausted or ambiguous work.
    - Existing delivery-level Oban exhaustion tests remain green.
  </acceptance_criteria>
  <done>Oban final retry exhaustion closes the target durably and ordinary recovery cannot turn it into another provider request.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Oban target args → parent/target claim | Untrusted/stale job identity must not widen tenant authority or bypass current parent state. |
| Adapter completion → durable finalizer | A stale callback races recovery closeout and must not overwrite newer truth. |
| Retry budget → recovery eligibility | Final retry state determines whether any future provider request is authorized. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-99-10-01 | Elevation of Privilege | Parent/target tenant claim | high | mitigate | Lock and predicate both rows on the explicit tenant plus parent pending/ready lifecycle before attempt insertion. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` |
| T-99-10-02 | Tampering | Late provider success | high | mitigate | Reload and lock exact target/attempt state; accept only claimed plus attempt_started and noop after ambiguity wins. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-10-03 | Repudiation | Final retry exhaustion | high | mitigate | Append terminal retry_exhausted evidence and recompute the parent before completing the final Oban job. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs test/chimeway/dispatch/oban_worker_test.exs --warnings-as-errors` |
| T-99-10-04 | Denial of Service | Recovery resend loop | high | mitigate | Make exhaustion terminal and prove bounded recovery never rediscovers it. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs test/chimeway/orchestration/target_recovery_test.exs --warnings-as-errors` |
| T-99-10-05 | Information Disclosure | Noop/error and attempt evidence | high | mitigate | Use non-disclosing results and closed provider-code facts only; assert raw callback and tenant sentinels absent. | `env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-10-SC | Tampering | Package supply chain | low | accept | No package-manager operation or dependency change occurs. | `git diff --exit-code -- mix.exs mix.lock` |
</threat_model>

<assumption_delta_decision>
Primary noun: the durable tenant-scoped DeliveryTarget is the provider-request and recovery authority beneath one logical Delivery.
Decision: promote — parent lifecycle and retry budget are promoted into the target's claim/finalization predicate because current singular child-state checks permit unsafe plural work; target identity itself remains unchanged.
</assumption_delta_decision>

## API Coverage Decision

The Phase 99 `COVERAGE.md` no-external-API declaration remains accurate. This plan tightens internal Ecto/Oban lifecycle contracts behind the existing TargetAdapter and introduces no provider endpoint, SDK method, or public authentication capability.

## Artifacts this phase produces

- Modified existing symbols: `DeliveryTargets.begin_target_attempt/2`, `record_target_result/4`, and `exhaust_target/3` gain locked parent/child state predicates.
- Modified existing private executor handling for a losing stale-result finalizer.
- Modified target clause of `Chimeway.Dispatch.ObanWorker.perform/1` to consume `attempt` and `max_attempts` and close final retry exhaustion.
- Expanded `test/chimeway/dispatch/target_worker_test.exs` parent-state, race, exhaustion, duplicate-job, and recovery cases.
- New files, schemas, migrations, configuration flags, packages, external APIs, or provider fields: none.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Gap plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | All eligible opaque installations retain independent tenant-safe truth and recovery | 99-08, 99-09, 99-10 | COVERED | Operator truth, all-target sync, and race-safe recovery authority close the five verified gaps. |
| REQ | PUSH-01 | Resolve every eligible opaque installation | 99-09 | COVERED | Existing resolver edge predicates are preserved; sync now consumes all selected targets. |
| REQ | PUSH-02 | Independent target lifecycle and history | 99-08, 99-09, 99-10 | COVERED | All projections, sync fan-out, and exact locked transitions retain independent child truth. |
| REQ | PUSH-03 | Duplicate-safe planning/execution/recovery | 99-09, 99-10 | COVERED | Exact target claims, terminal exhaustion, and race tests prevent additional provider requests. |
| REQ | PUSH-04 | Stable empty result and honest aggregate | 99-09, 99-10 | COVERED | Empty sync is suppressed; full snapshot and parent gates preserve aggregate truth. |
| REQ | RECOV-01 | Bounded tenant recovery | 99-10 | COVERED | Recovery remains bounded and now proves exhausted/ambiguous terminal work is not resent. |
| REQ | RECOV-02 | Pre-I/O evidence and ambiguous possible handoff | 99-10 | COVERED | Conditional late-result finalization preserves ambiguous closeout. |
| RESEARCH | — | Database locks/uniqueness, Oban in-band exhaustion, SafeEvidence projection | 99-08, 99-09, 99-10 | COVERED | Uses established stack; no new package or schema work. |
| CONTEXT | D-01–D-04 | Canonical parent, target identity, no-target and honest aggregate | 99-09, 99-10 | COVERED | No competing parent identity; all selected children execute under parent authority. |
| CONTEXT | D-05–D-09 | Independent state, pre-I/O evidence, ambiguity, linked redrive, handoff-only wording | 99-08, 99-09, 99-10 | COVERED | Trace children, exact claims, and fail-closed late success preserve handoff truth. |
| CONTEXT | D-10–D-12 | Tenant-bounded recovery, atomic convergence, safe evidence | 99-08, 99-10 | COVERED | Recovery no-resend and tenant-safe projections are executable. |
| CONTEXT | Deferred Ideas | APNs mapping, CrossWake registration/open, twin, physical proof | — | EXCLUDED | These remain in Phases 100–103. |

All goal, requirement, research, and locked-decision sources are covered. The specless probe assumptions are surfaced where behavior is affected: PUSH-01/PUSH-02/PUSH-04 adjacency, empty, ordering, idempotency, and concurrency in 99-08/99-09; PUSH-03 idempotency/concurrency and RECOV-01/RECOV-02 manual-review predicates in 99-10. No deferred item is planned.

<verification>
Run the focused target worker, legacy Oban worker, recovery, and tenant-scope suites. Accept only durable row/state/count evidence proving parent gating, single terminal exhaustion, no recovery resend, and ambiguity preservation.
</verification>

<success_criteria>
Queued target work cannot outlive parent authority, final retries become terminal before Oban completion, recovery cannot resend exhausted work, and late success cannot erase ambiguous handoff evidence.
</success_criteria>

<output>
Create `.planning/phases/99-multi-installation-delivery-recovery/99-10-SUMMARY.md` when done.
</output>
