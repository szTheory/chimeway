---
phase: 99-multi-installation-delivery-recovery
plan: "03"
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - lib/chimeway/target_resolver.ex
  - lib/chimeway/delivery_targets.ex
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/safe_evidence.ex
  - lib/chimeway/traces.ex
  - test/chimeway/delivery_target_test.exs
  - test/chimeway/traces_target_test.exs
autonomous: true
requirements: [PUSH-01, PUSH-02, PUSH-03, PUSH-04]
must_haves:
  truths:
    - "D-02/PUSH-02 adjacency: exact-equal revisions converge on one child; distinct revisions, including prefix-adjacent opaque strings, remain separate children."
    - "D-04/PUSH-02 empty: zero selected targets yields an empty ordered target list plus parent suppression; nil resolver output fails planning without partial target rows."
    - "D-05/PUSH-02 ordering: target histories sort by binding revision and attempts sort by attempt_number with stable tie handling."
    - "D-03/PUSH-02 idempotency: repeated and concurrent resolver planning converges on the same target IDs through database uniqueness and tenant-qualified authoritative reload."
    - "D-04/PUSH-04 adjacency: equal terminal outcomes remain independent target facts; aggregate counts them without merging histories."
    - "D-04/PUSH-04 empty: no targets always produces exactly `suppressed/no_eligible_targets`, never generic success or failure."
    - "D-04/D-09/PUSH-04 ordering: aggregation is independent of resolver/result order, and ordered trace output is deterministic."
    - "D-04: after all targets terminate the parent succeeds iff at least one target is provider_accepted; mixed terminal results retain partial_failure and exact per-target failures."
  artifacts:
    - path: "lib/chimeway/delivery_targets.ex"
      provides: "Multi-target identity convergence and derived parent aggregate"
    - path: "lib/chimeway/traces.ex"
      provides: "Tenant-scoped ordered child target histories beneath one logical delivery"
    - path: "test/chimeway/traces_target_test.exs"
      provides: "No-target, multi-target, mixed-result, ordering, and privacy acceptance proof"
  key_links:
    - from: "lib/chimeway/target_resolver.ex"
      to: "lib/chimeway/delivery_planning.ex"
      via: "closed normalization and stable sort feed idempotent target insertion"
      pattern: "normalize|sort"
    - from: "lib/chimeway/delivery_targets.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "parent aggregate and per-target histories use closed reason/outcome vocabulary"
      pattern: "provider_accepted|partial_failure|no_eligible_targets"
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/delivery_target_attempt.ex"
      via: "tenant-qualified child joins/preloads and attempt_number ordering"
      pattern: "attempt_number"
  prohibitions:
    - "FLAGGED-UNVERIFIED — A successful target must not erase, relabel, or hide another target's failure, expiry, invalidation, retry exhaustion, or ambiguous handoff."
    - "FLAGGED-UNVERIFIED — Aggregate status must not claim all-installation or device delivery truth."
    - "FLAGGED-UNVERIFIED — Child joins or preloads must not disclose another tenant's target or attempt evidence, even for deliberately malformed cross-tenant fixtures."
---

<objective>
Expand the tracer into complete deterministic multi-installation planning, aggregation, and explanation.

Purpose: Make empty, duplicate, concurrent, and mixed target sets converge without losing independent installation truth.
Output: Closed resolver edge behavior, race-safe multi-target planning, honest aggregate recomputation, and tenant-safe target traces.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
@.planning/phases/99-multi-installation-delivery-recovery/99-RESEARCH.md
@.planning/phases/99-multi-installation-delivery-recovery/99-PATTERNS.md
@.planning/phases/99-multi-installation-delivery-recovery/99-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Converge empty, duplicate, ordered, and concurrent target planning</name>
  <read_first>
    - lib/chimeway/target_resolver.ex
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/delivery.ex
    - lib/chimeway/deliveries.ex
    - test/chimeway/delivery_target_test.exs
    - test/chimeway/orchestration/delivery_planning_test.exs
    - test/chimeway/orchestration/recovery_test.exs
  </read_first>
  <files>lib/chimeway/target_resolver.ex, lib/chimeway/delivery_targets.ex, lib/chimeway/delivery_planning.ex, test/chimeway/delivery_target_test.exs</files>
  <behavior>
    - Test 1: empty list suppresses once with `no_eligible_targets`; nil list, nil element, tenant mismatch, raw-material fields, uncontrolled nested facts, blank/oversized refs, and malformed callback tuples return `invalid_target_resolution` without partial children.
    - Test 2: unsorted `[rev-b, rev-a, rev-a]` creates exactly rev-a then rev-b in deterministic order; repeated planning preserves their IDs.
    - Test 3: concurrent planners for the same Delivery/revisions return the same authoritative rows and leave one row per exact revision.
    - Test 4: the same opaque revision can exist under another Delivery/tenant without collision, while no direct target lookup crosses tenant scope.
  </behavior>
  <action>Extend `BindingRevision` normalization into a total closed contract and exercise every PUSH-01/PUSH-02 adjacency, empty/null, ordering, idempotency, and concurrency predicate. Treat exact binary equality as identity: deduplicate exact repeated refs after tenant validation, stable-sort refs before insert/reload, and preserve distinct opaque strings without semantic parsing. Keep resolver failure atomic with target persistence and parent transition, so malformed output creates neither a child nor a false no-target suppression. In `DeliveryTargets.plan_targets/3`, use the database unique index and authoritative tenant-qualified reload for every row; make concurrent planning return the same sorted target IDs instead of relying on in-memory dedupe or Oban uniqueness. Preserve D-01's canonical parent and D-02's closed fields; accept no arbitrary selection map and store no resolver-originated value beyond tenant ID, bounded opaque revision ref, and explicitly modeled safe scalar facts already admitted by SafeEvidence.</action>
  <reversibility rating="costly">Resolver normalization and exact opaque identity semantics are public contracts consumed by host resolvers and later provider adapters.</reversibility>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/target_resolver.ex lib/chimeway/delivery_targets.ex lib/chimeway/delivery_planning.ex test/chimeway/delivery_target_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The focused suite exits zero with explicit empty/null, malformed, raw-sentinel, exact-duplicate, distinct-adjacent, stable-order, repeated-plan, concurrent-plan, and cross-tenant cases.
    - Concurrent task assertions compare authoritative DeliveryTarget IDs and exact row counts after all planners complete.
    - Invalid resolver output leaves zero child rows and does not transition the parent to a false terminal state.
    - Resolver normalization returns only the declared BindingRevision fields and never forwards an arbitrary nested map.
  </acceptance_criteria>
  <done>All resolver and target-set edge cases are deterministic, atomic, idempotent, and tenant-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive truthful parent aggregates and ordered per-target traces</name>
  <read_first>
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/traces.ex
    - lib/chimeway/traces/explanation.ex
    - test/chimeway/traces_test.exs
    - test/chimeway/delivery_target_test.exs
    - .planning/phases/98-privacy-safe-delivery-evidence/98-15-SUMMARY.md
  </read_first>
  <files>lib/chimeway/delivery_targets.ex, lib/chimeway/safe_evidence.ex, lib/chimeway/traces.ex, test/chimeway/delivery_target_test.exs, test/chimeway/traces_target_test.exs</files>
  <behavior>
    - Test 1: while any target is pending/claimed/retryable, the parent remains nonterminal and exposes counts without a success claim.
    - Test 2: all terminal with zero accepted targets yields terminal failure/cancellation evidence; one accepted plus one failed/expired/invalidated/retry_exhausted/ambiguous target yields parent success plus `partial_failure: true`.
    - Test 3: two accepted targets remain two independent histories and produce parent success with `partial_failure: false`.
    - Test 4: target trace children sort by binding ref, attempts sort by number, wrong-tenant lookup is indistinguishable from absence, and all device-receipt/identity/provider-body sentinels are absent.
    - Test 5: tenant-qualified `schedule_retry/3`, `expire_target/3`, `invalidate_target/3`, and `exhaust_target/3` mutate only the selected child, retain ordered attempt facts, and leave sibling targets actionable.
  </behavior>
  <action>Implement tenant-qualified `DeliveryTargets.schedule_retry/3`, `expire_target/3`, `invalidate_target/3`, and `exhaust_target/3` so retry eligibility, absolute target expiry, exact opaque revision invalidation, and retry exhaustion change only one selected child, preserve append-only ordered attempt evidence, and immediately recompute the parent; these are provider-independent core transitions, not APNs reason mapping. Implement `DeliveryTargets.recompute_delivery/2` under a parent lock and tenant-qualified target query. Define the terminal set and aggregate facts explicitly: `target_count`, `terminal_target_count`, `provider_accepted_count`, `terminal_failure_count`, `partial_failure`, and `all_targets_terminal`; use existing parent states/reason fields rather than creating a second logical delivery status. Apply D-04 exactly: empty selection is the dedicated suppression path, actionable targets keep the parent nonterminal, all-terminal succeeds iff accepted count is positive, and mixed accepted/non-accepted terminal states retain partial evidence. Extend SafeEvidence with fixed constructors for target aggregate, target row, target attempt, and target timeline facts; reject or omit unknown/provider-controlled material. Extend every tenant-scoped trace family that returns a Delivery to preload/query targets and attempts with explicit tenant predicates, stable ordering, and literal closed maps. Add adversarial fixtures with same refs in two tenants and a deliberately mismatched child row to prove joins remain coherent. Use `provider_accepted` terminology only per D-09; do not introduce `delivered`, receipt, open, seen/read, engagement, APNs reason, request ID, payload, header, expiry-header, or collapse fields.</action>
  <reversibility rating="costly">Aggregate vocabulary and public trace shape become durable operator contracts, though calculation remains derived from target rows.</reversibility>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/delivery_targets.ex lib/chimeway/safe_evidence.ex lib/chimeway/traces.ex test/chimeway/delivery_target_test.exs test/chimeway/traces_target_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs test/chimeway/traces_target_test.exs test/chimeway/traces_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Focused tests cover no-target, all-failed, all-accepted, and each accepted-plus-terminal-failure mix with exact parent status and aggregate map assertions.
    - Every result permutation produces the same aggregate, target order, and attempt order.
    - Exact sibling-isolation assertions prove retry, expiry, invalidation, and exhaustion transitions affect only the tenant-qualified target and remain visible in its ordered trace.
    - Exact trace-key assertions retain independent target outcomes and partial failure without an all-device claim.
    - Wrong-tenant and malformed cross-tenant fixtures expose no target ID, binding revision, status, attempt, or existence signal.
    - Full returned trace terms exclude exact token, endpoint, credential, identity, payload, provider-body, device-receipt, open, seen/read, and engagement sentinels.
  </acceptance_criteria>
  <done>One logical delivery presents deterministic aggregate truth while retaining every installation's independent safe history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Resolver list → target identity set | Duplicate/malformed/unordered host values must converge without partial writes. |
| Target terminal rows → logical aggregate | Parent success must not erase failures or claim device receipt. |
| Tenant-scoped lifecycle → nested trace children | Every target/attempt join repeats tenant ownership and closed projection. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-99-03-01 | Tampering | Concurrent target planning | high | mitigate | Combine database uniqueness, authoritative reload, stable sorting, and atomic planning failure. | `env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs --warnings-as-errors` |
| T-99-03-02 | Information Disclosure | Target trace joins | high | mitigate | Repeat tenant predicates on every child query and scan complete closed terms against hostile cross-tenant/sensitive fixtures. | `env MIX_ENV=test mix test test/chimeway/traces_target_test.exs --warnings-as-errors` |
| T-99-03-03 | Repudiation | Parent aggregate | high | mitigate | Derive exact counts under lock and retain partial/target terminal facts with provider-handoff-only terminology. | `env MIX_ENV=test mix test test/chimeway/delivery_target_test.exs test/chimeway/traces_target_test.exs --warnings-as-errors` |
| T-99-03-SC | Tampering | Package supply chain | low | accept | No package-manager install or dependency change occurs. | `git diff --exit-code -- mix.exs mix.lock` |

ASVS L1 disposition: high-severity convergence, tenant leakage, and false-receipt threats are mitigated through deterministic race and adversarial trace tests.
</threat_model>

## Artifacts this phase produces

- Changed resolver functions: total `BindingRevision.new/2` normalization, exact-ref deduplication, and stable target ordering.
- Changed planning functions: atomic `DeliveryTargets.plan_targets/3` and push planning error/no-target paths.
- Changed lifecycle/aggregate functions: `DeliveryTargets.schedule_retry/3`, `expire_target/3`, `invalidate_target/3`, `exhaust_target/3`, and `recompute_delivery/2`; aggregate tokens are `target_count`, `terminal_target_count`, `provider_accepted_count`, `terminal_failure_count`, `partial_failure`, and `all_targets_terminal`.
- Changed SafeEvidence constructors: closed aggregate, DeliveryTarget, DeliveryTargetAttempt, and target timeline projections.
- Changed trace functions: tenant-qualified ordered target/attempt projection beneath existing logical Delivery maps.
- New test path: `test/chimeway/traces_target_test.exs`; expanded `delivery_target_test.exs` for all PUSH-01/PUSH-02/PUSH-04 edge predicates.
- New APNs, CrossWake, twin, physical-device, credential, endpoint, header, payload, or collapse artifacts: none.

<verification>
Run focused resolver/planning, aggregate, and trace suites plus existing trace regressions. All tests are deterministic, database-backed, and credential-free.
</verification>

<success_criteria>
Empty, malformed, duplicate, concurrent, and multi-target resolution behaves deterministically; parent aggregation remains honest; every target remains independently visible only within its tenant.
</success_criteria>

<output>
Create `.planning/phases/99-multi-installation-delivery-recovery/99-03-SUMMARY.md` when done.
</output>
