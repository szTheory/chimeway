---
phase: 99-multi-installation-delivery-recovery
plan: "07"
type: execute
wave: 6
depends_on: ["99-06"]
files_modified:
  - lib/chimeway/target_recovery.ex
  - lib/chimeway/dispatch/recovery_worker.ex
  - lib/chimeway/safe_evidence.ex
  - test/chimeway/orchestration/target_recovery_test.exs
  - .planning/phases/99-multi-installation-delivery-recovery/99-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [PUSH-01, PUSH-02, PUSH-03, PUSH-04, RECOV-01, RECOV-02]
must_haves:
  truths:
    - "D-02/PUSH-01 adjacency: exact duplicate opaque revisions remain convergent while prefix-adjacent distinct revisions remain distinct during recovered planning."
    - "D-02/PUSH-01 empty: event-only recovery may legitimately resolve zero targets and retains stable no_eligible_targets suppression; nil resolver output remains invalid."
    - "D-02/PUSH-01 ordering: recovered resolver results and target evidence remain deterministic by binding_revision_ref and durable ID."
    - "D-04/PUSH-04 adjacency: equal terminal target outcomes remain independent facts after recovery and are counted rather than merged."
    - "D-04/PUSH-04 empty: a recovered push with zero eligible targets remains exactly suppressed/no_eligible_targets."
    - "D-04/D-09/PUSH-04 ordering: recovery order cannot change the provider-accepted aggregate or erase partial failures, and never upgrades handoff evidence to device delivery."
  assumptions:
    - statement: "RECOV-01 unclassified edge probe: event, target, and stale-attempt streams each require an independent typed keyset continuation and validated per-stream batch cap; verifier must flag any fourth recovery stream without an explicit continuation and bound."
      status: unresolved
      flagged: true
  artifacts:
    - path: "lib/chimeway/target_recovery.ex"
      provides: "Complete event-only and notification-without-delivery discovery plus three independently bounded recovery streams"
    - path: "lib/chimeway/dispatch/recovery_worker.ex"
      provides: "Worker propagation of typed continuations and emitted closed recovery summary"
    - path: "lib/chimeway/safe_evidence.ex"
      provides: "Closed continuation, count, and accurate executor/recovery reason vocabulary"
    - path: "test/chimeway/orchestration/target_recovery_test.exs"
      provides: "Regression evidence for discovery completeness, stream bounds, cursor independence, summary emission, and error reasons"
    - path: ".planning/phases/99-multi-installation-delivery-recovery/99-VALIDATION.md"
      provides: "Truthful Phase 99 validation map for repaired executor and recovery paths"
  key_links:
    - from: "lib/chimeway/dispatch/recovery_worker.ex"
      to: "lib/chimeway/target_recovery.ex"
      via: "worker accepts and forwards event_cursor, target_cursor, and stale_attempt_cursor independently"
      pattern: "event_cursor|target_cursor|stale_attempt_cursor"
    - from: "lib/chimeway/target_recovery.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "tenant-qualified event discovery includes zero-notification and zero-delivery trigger-commit gaps before recover_event/2"
      pattern: "discover_stranded_events|recover_event"
    - from: "lib/chimeway/target_recovery.ex"
      to: "lib/chimeway/dispatch/executor.ex"
      via: "stable executor outcomes map to retryable_pre_handoff, left_ambiguous, claimed, or invalidated recovery reasons without conflation"
      pattern: "pre_handoff_retryable|ambiguous_handoff"
    - from: "lib/chimeway/dispatch/recovery_worker.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "the worker returns and emits only the closed recovery summary"
      pattern: "recovery_summary|recovery.completed"
  prohibitions:
    - statement: "Recovery must not infer tenant ownership from an event ID, notification ID, target ID, binding revision, recipient, or storage prefix."
      status: unverified
      flagged: true
    - statement: "Recovery must not become an unbounded scanner, share a cursor across heterogeneous streams, or blindly resend ambiguous work."
      status: unverified
      flagged: true
    - statement: "Recovery summaries or telemetry must not expose tenant identity, installation identity, raw endpoint/token material, credentials, provider bodies, or callback error text."
      status: unverified
      flagged: true
    - statement: "Refreshed validation evidence must not mark a repaired requirement green unless its exact regression command has executed successfully."
      status: unverified
      flagged: true
---

<objective>
Make tenant recovery complete, independently pageable, bounded, and operationally evidenced.

Purpose: Close RECOV-01 without weakening PUSH-03/RECOV-02 by finding both trigger-commit gap shapes, capping every stream, preserving exact executor reasons, and exposing the closed worker summary.
Output: Three typed keyset continuations, complete event discovery, safe worker telemetry/result evidence, focused tests, and refreshed validation metadata.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
@.planning/phases/99-multi-installation-delivery-recovery/99-RESEARCH.md
@.planning/phases/99-multi-installation-delivery-recovery/99-PATTERNS.md
@.planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
@.planning/phases/99-multi-installation-delivery-recovery/99-05-SUMMARY.md
@.planning/phases/99-multi-installation-delivery-recovery/99-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recover three independently bounded streams and emit accurate closed evidence</name>
  <read_first>
    - lib/chimeway/target_recovery.ex
    - lib/chimeway/dispatch/recovery_worker.ex
    - lib/chimeway/safe_evidence.ex
    - test/chimeway/orchestration/target_recovery_test.exs
    - lib/chimeway/deliveries.ex
    - lib/chimeway/delivery_targets.ex
    - lib/chimeway/dispatch/executor.ex
    - lib/chimeway/events/event.ex
    - lib/chimeway/notifications/notification.ex
    - test/chimeway/orchestration/recovery_test.exs
    - test/chimeway/dispatch/target_worker_test.exs
    - .planning/phases/99-multi-installation-delivery-recovery/99-CONTEXT.md
    - .planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
  </read_first>
  <files>lib/chimeway/target_recovery.ex, lib/chimeway/dispatch/recovery_worker.ex, lib/chimeway/safe_evidence.ex, test/chimeway/orchestration/target_recovery_test.exs</files>
  <behavior>
    - Test 1: within one tenant, an old event with no notification and an old event whose notification has no delivery are both discovered and recovered; equivalent foreign/new/complete rows are excluded without disclosure.
    - Test 2: event, pending-target, and stale-attempt streams each sort by durable target/event ID, accept only their own cursor, return only their own continuation, and process at most the validated `batch_size` (default 50, accepted range 1..100).
    - Test 3: advancing a target cursor cannot skip or stall an event page, and advancing an event cursor cannot alter target or stale-attempt pages; each second page starts strictly after its typed cursor with no repeat.
    - Test 4: explicit pre-handoff executor failure is reported as `:retryable_pre_handoff`, ambiguity as `:left_ambiguous`, claim contention as `:skipped_claimed`, and missing/foreign rows as `:skipped_invalidated`.
    - Test 5: `RecoveryWorker.perform/1` forwards all three continuations, returns `{:ok, closed_summary}`, and emits `[:chimeway, :recovery, :completed]` with bounded counts/reasons/continuations and no tenant or sensitive target material.
  </behavior>
  <action>Write the failing regression matrix first. Per D-10, replace the event inner join with tenant-qualified discovery that selects either an event with no notification or an event having at least one notification without a delivery, while excluding complete and younger-than-cutoff work. Refactor paging into three explicit option keys and response fields: `event_cursor`, `target_cursor`, and `stale_attempt_cursor`; expose them under `continuations: %{event: ..., target: ..., stale_attempt: ...}` in the closed summary. Apply the same validated `batch_size` independently as `order_by durable_id`, `where id &gt; cursor`, and `limit` to event, pending-target, and stale-attempt queries; stale closeout must never load an unbounded ID set. Preserve a nil continuation when a stream returns no rows and never feed one stream's cursor into another query. Per D-11, retain the existing atomic target claim as provider authority and map Plan 99-06 executor results accurately: `{:error, :pre_handoff_retryable}` contributes reason `:retryable_pre_handoff`; `{:error, :ambiguous_handoff}` contributes `:left_ambiguous`; noop claim contention contributes `:skipped_claimed`; only absent/foreign/invalidation cases contribute `:skipped_invalidated`. Per D-12, extend `SafeEvidence.recovery_summary/1` to validate exactly the three continuation keys, add only the new closed reason/count atoms required by these classifications, and reject arbitrary fields/values. Update `RecoveryWorker` to accept optional `event_cursor`, `target_cursor`, and `stale_attempt_cursor`, call `recover_tenant/2`, return `{:ok, summary}`, and emit `[:chimeway, :recovery, :completed]` with counts as measurements and safe reason/continuation metadata; do not emit `tenant_id`, binding revisions, callback terms, or payload data.</action>
  <reversibility rating="reversible">These are internal recovery query/result and worker-argument refinements over existing rows; no schema, durable identity, or irreversible data rewrite is introduced.</reversibility>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/target_recovery.ex lib/chimeway/dispatch/recovery_worker.ex lib/chimeway/safe_evidence.ex test/chimeway/orchestration/target_recovery_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs test/chimeway/dispatch/target_worker_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Focused tests create and recover both tenant-owned trigger-commit gap shapes and prove foreign, complete, and cutoff-adjacent rows are excluded.
    - Each recovery query visibly contains tenant predicate, deterministic durable-ID order, its own strict keyset predicate, and the validated per-stream limit.
    - A mixed three-stream fixture proves independent first/second pages, no duplicates, maximum 100 rows per stream, and no cross-stream cursor influence.
    - Worker tests assert the exact returned summary and telemetry event while sentinel scans reject tenant, binding, token, endpoint, credential, provider-body, and callback-error values.
    - Executor error cases retain `:retryable_pre_handoff` versus `:left_ambiguous`; neither is mislabeled invalidation.
  </acceptance_criteria>
  <done>Tenant recovery finds every required gap shape, pages every stream independently within its bound, and emits accurate privacy-safe evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh Phase 99 validation evidence against the repaired paths</name>
  <read_first>
    - .planning/phases/99-multi-installation-delivery-recovery/99-VALIDATION.md
    - .planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md
    - .planning/phases/99-multi-installation-delivery-recovery/99-06-PLAN.md
    - .planning/phases/99-multi-installation-delivery-recovery/99-07-PLAN.md
    - test/chimeway/dispatch/target_worker_test.exs
    - test/chimeway/orchestration/target_recovery_test.exs
    - test/chimeway/delivery_target_test.exs
    - test/chimeway/traces_target_test.exs
  </read_first>
  <files>.planning/phases/99-multi-installation-delivery-recovery/99-VALIDATION.md</files>
  <action>After Task 1 is green, update `99-VALIDATION.md` rather than merely changing its status label. Add gap-closure rows for PUSH-02 adapter error/unexpected-return finalization, PUSH-03 duplicate-call prevention across retry/ambiguity and recovery races, RECOV-01 event-only plus notification-without-delivery discovery and all three bounded continuations, RECOV-02 possible-handoff no-resend behavior, and RecoveryWorker summary telemetry. Retain the already-green PUSH-01/PUSH-04 rows and cite their existing focused commands. Record plans 99-06/99-07 and waves 5/6. Set `status: validated`, `wave_0_complete: true`, and `nyquist_compliant: true` only after the combined focused command, `mix verify.runtime_prefix`, `mix verify.install_golden`, and `mix ci.test` all exit zero; include the execution date and observed results so the document no longer overclaims the stale pre-repair evidence.</action>
  <reversibility rating="reversible">Validation metadata is executable evidence documentation and can be corrected without runtime or data migration impact.</reversibility>
  <verify>
    <automated>env MIX_ENV=test mix test test/chimeway/dispatch/target_worker_test.exs test/chimeway/orchestration/target_recovery_test.exs test/chimeway/delivery_target_test.exs test/chimeway/traces_target_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors &amp;&amp; mix verify.runtime_prefix &amp;&amp; mix verify.install_golden &amp;&amp; mix ci.test &amp;&amp; rg -n '99-06|99-07|pre-handoff|event-only|event_cursor|target_cursor|stale_attempt_cursor|recovery.*completed' .planning/phases/99-multi-installation-delivery-recovery/99-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `99-VALIDATION.md` names plans 99-06/99-07, waves 5/6, every repaired path, and the exact executable command that proves it.
    - All six requirement IDs remain mapped; PUSH-02 and RECOV-01 cite the new regressions, while PUSH-03 and RECOV-02 cite duplicate/ambiguity evidence.
    - Compliance flags and green statuses are supported by the recorded combined, runtime-prefix, installer-golden, and CI command results.
    - The final `rg` evidence command finds every required repaired-path marker.
  </acceptance_criteria>
  <done>Phase 99 validation metadata truthfully describes and proves the repaired executor and recovery contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Recovery worker args → three tenant queries | Typed cursors and limits must not cross streams, widen tenant authority, or become unbounded. |
| Event/target/stale discovery → lifecycle mutation | Discovery is not dispatch authority; existing tenant claims and ambiguity rules remain decisive. |
| Recovery service → worker result/telemetry | The closed summary must remain useful without exposing tenant or host-owned target material. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-99-07-01 | Elevation of Privilege | Event and target recovery scope | high | mitigate | Repeat explicit tenant predicates at every discovery/join/reload and prove foreign/absent equivalence. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` |
| T-99-07-02 | Denial of Service | Three recovery streams | high | mitigate | Give each stream an independent keyset cursor and validated 1..100 limit, including stale closeout. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs --warnings-as-errors` |
| T-99-07-03 | Tampering | Cursor routing and duplicate execution | high | mitigate | Typed option keys prevent heterogeneous cursor reuse; durable target claims remain I/O authority. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs test/chimeway/dispatch/target_worker_test.exs --warnings-as-errors` |
| T-99-07-04 | Repudiation | Recovery reason projection | high | mitigate | Preserve retryable-pre-handoff versus ambiguous/invalidation reasons in a closed summary and worker telemetry. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs --warnings-as-errors` |
| T-99-07-05 | Information Disclosure | Worker telemetry | high | mitigate | Emit only validated counts, reason atoms, and lifecycle cursors; exclude tenant and host target data with sentinel assertions. | `env MIX_ENV=test mix test test/chimeway/orchestration/target_recovery_test.exs --warnings-as-errors` |
| T-99-07-SC | Tampering | Package supply chain | low | accept | No package-manager operation or dependency change occurs. | `git diff --exit-code -- mix.exs mix.lock` |

ASVS L1 disposition: all high-severity scope, resource, cursor-confusion, explainability, and leakage threats are mitigated with executable evidence.
</threat_model>

<assumption_delta_decision>
Primary noun: each opaque delivery target and its numbered attempts remain the provider-request identity beneath one logical delivery.
Decision: no-change — “additional provider request” names the duplicate side effect that PUSH-03 forbids; it does not introduce a newly plural identity model, and independent target/attempt identity already exists.
</assumption_delta_decision>

## API Coverage Decision

The pre-plan deterministic scan returned `detected: false`; the final scan fired only on the explicit no-external-service declaration in the new plan text. Source inspection confirms this plan repairs internal Ecto queries, an Oban worker, SafeEvidence projection, and ExUnit contracts. `COVERAGE.md` records the required reasoned no-external-API declaration; no capability matrix is applicable.

## Artifacts this phase produces

- New worker option keys: `event_cursor`, `target_cursor`, and `stale_attempt_cursor`.
- New closed summary field: `continuations: %{event: cursor_or_nil, target: cursor_or_nil, stale_attempt: cursor_or_nil}`.
- New closed recovery reason/count vocabulary for `retryable_pre_handoff` while retaining `left_ambiguous`, `skipped_claimed`, and `skipped_invalidated` distinctions.
- New telemetry event: `[:chimeway, :recovery, :completed]` with bounded counts and safe reason/continuation metadata.
- Expanded recovery regression cases and refreshed `99-VALIDATION.md` rows for plans 99-06/99-07.
- New schema, migration, APNs adapter, provider endpoint, token, credential, request, payload, CrossWake, twin, or device artifacts: none.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | All eligible opaque targets retain independent tenant-safe truth and recovery | 99-01 through 99-07 | COVERED | 99-06 closes outcome truth; 99-07 closes complete bounded recovery/evidence. |
| REQ | PUSH-01 | Opaque tenant-scoped resolver | 99-01, 99-03, 99-07 | COVERED | Existing edge behavior reruns in final regression evidence. |
| REQ | PUSH-02 | Independent target lifecycle | 99-06, 99-07 validation | COVERED | Every adapter return finalizes the exact started attempt. |
| REQ | PUSH-03 | Duplicate prevention | 99-06, 99-07 | COVERED | Claim/retry/ambiguity tests plus independent recovery streams. |
| REQ | PUSH-04 | No-target and honest aggregate | 99-03, 99-07 | COVERED | Existing focused aggregate tests remain in final command. |
| REQ | RECOV-01 | Bounded tenant worker and explainable evidence | 99-07 | COVERED | Both event gap shapes, three cursors, three capped streams, emitted summary. |
| REQ | RECOV-02 | Pre-I/O start and ambiguous possible handoff | 99-06, 99-07 | COVERED | Accurate executor/recovery reason and no automatic resend. |
| RESEARCH | — | Ecto keyset queries, durable claims, SafeEvidence, Oban worker | 99-07 | COVERED | Reuses existing stack and static-prefix routing. |
| CONTEXT | D-01, D-02, D-03, D-04 | Canonical parent and honest aggregate | 99-01, 99-03, 99-07 | COVERED | Final regression retains the locked model. |
| CONTEXT | D-05, D-06, D-07, D-08, D-09 | Independent state/handoff truth | 99-06, 99-07 | COVERED | Exact adapter classification and ambiguity evidence. |
| CONTEXT | D-10, D-11, D-12 | Tenant-scoped bounded recovery and safe evidence | 99-07 | COVERED | Every stream and output is scoped, capped, and closed. |
| CONTEXT | Deferred Ideas | APNs mapping/construction, CrossWake, twin, physical proof | — | EXCLUDED | No Phase 100–103 capability is introduced. |

No source item is missing. The plan set preserves section-level equality for the default-ON fallback: 15 edge items surfaced = 13 explicit truths (seven in 99-06 and six here) + 2 flagged RECOV assumptions (one per plan). Prohibition recall surfaced eight bespoke items = four descriptor-less flagged prohibitions in 99-06 + four here; no kept item is dropped or given a fabricated check descriptor.

<verification>
Run the focused repaired-path matrix, then runtime-prefix, installer-golden, and full CI gates. Validate both plan structures before execution.
</verification>

<success_criteria>
Both trigger-commit gap shapes are recoverable; every recovery stream has its own bounded continuation; worker evidence is observable and safe; error reasons are accurate; Phase 99 validation evidence is current.
</success_criteria>

<output>
Create `.planning/phases/99-multi-installation-delivery-recovery/99-07-SUMMARY.md` when done.
</output>
