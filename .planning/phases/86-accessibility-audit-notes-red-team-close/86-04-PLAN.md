---
phase: 86-accessibility-audit-notes-red-team-close
plan: 04
type: execute
wave: 3
depends_on: ["86-01", "86-02", "86-03"]
files_modified:
  - scripts/brandbook-guards.sh
  - notes/red-team.md
autonomous: true
requirements: [NOTES-03]

must_haves:
  truths:
    - "scripts/brandbook-guards.sh --scope allowlist is widened to EXACTLY: brandbook/** + README.md + mix.exs + notes/** + scripts/brandbook-guards.sh + scripts/logo-guards.sh + scripts/render-svg-png.sh + scripts/contrast-audit.sh + .planning/**, keeping the deny-by-default *) fail branch."
    - "scripts/brandbook-guards.sh --scope PASSES on the milestone tree (the two integration edits, notes/**, and the new calc script no longer false-FAIL) and still FAILS on any path outside the allowlist."
    - "notes/red-team.md records the skeptic pass and closes with the captured `git diff --stat` scope audit output AND the repo-size/binary-budget result from scripts/logo-guards.sh --assets."
    - "The binary-budget check confirms exactly 3 committed rasters totalling 38,579B <= 204800B (200KB) ceiling — PASS, with no new binary added by this milestone."
    # Spec-less fallback edge — auto-covered predicate
    - "boundary (NOTES-03): the binary budget PASSES at exactly 3 rasters and <=204800B and FAILS one step either side (4+ rasters, or total >204800B); the current tree (3 rasters / 38,579B) passes."
  prohibitions:
    - "MUST NOT widen the --scope allowlist beyond brandbook/** + README.md + mix.exs + notes/** + guard/render/calc scripts + .planning/** (an over-broad allowlist silently defeats the scope audit)."
    - "MUST NOT remove or weaken the deny-by-default `*) fail` branch of the allowlist case block."
    - "MUST NOT commit any new binary/raster asset (the budget expects exactly the 3 existing rasters)."
    - "MUST NOT edit any value in brandbook/tokens/tokens.css (zero-drift TOKEN-01)."
  artifacts:
    - scripts/brandbook-guards.sh
    - notes/red-team.md
  key_links:
    - "brandbook-guards.sh --scope case block -> machine-enforced scope boundary (not asserted)"
    - "git status --porcelain walk -> FAILs on any stray out-of-scope path"
    - "logo-guards.sh --assets binary budget -> repo-size/binary check captured in red-team.md"
---

<objective>
Run and RECORD the milestone-close red-team: machine-enforce the `brandbook/`-only scope
boundary by widening the existing `--scope` allowlist so it passes on the correct milestone
tree and fails on anything else, then capture the `git diff --stat` scope audit + the
repo-size/binary-budget result in `notes/red-team.md`.

Purpose: NOTES-03 exists to close the vacuous-pass footgun — a pasted diff asserts the
boundary without enforcing it, so a future stray edit outside `brandbook/` + the two
integration files would pass unnoticed. The guard, not the prose, is the enforcement.
Output: widened `scripts/brandbook-guards.sh` + `notes/red-team.md`.

Covers NOTES-03. Runs LAST so the scope audit sees every file this milestone created.
</objective>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)
- Widened `scripts/brandbook-guards.sh` `--scope` allowlist entries: `README.md`, `mix.exs`, `notes/*`, `scripts/logo-guards.sh`, `scripts/render-svg-png.sh`, `scripts/contrast-audit.sh` (added to the existing `case` block; no new flag/mode).
- `notes/red-team.md` — the recorded skeptic pass closing with the captured `--scope` `git diff --stat` output and the `logo-guards.sh --assets` binary-budget result.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-accessibility-audit-notes-red-team-close/86-CONTEXT.md
@.planning/phases/86-accessibility-audit-notes-red-team-close/86-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the --scope allowlist to the exact milestone boundary</name>
  <files>scripts/brandbook-guards.sh</files>
  <read_first>
    - scripts/brandbook-guards.sh (the --scope block, lines ~82-115, and its `case "$path" in` allowlist ~98-105 — the deny-by-default `*) stray=...` branch)
    - scripts/logo-guards.sh (the sibling porcelain walk this one was copied from; binary budget ~265-285)
    - .planning/phases/86-accessibility-audit-notes-red-team-close/86-RESEARCH.md (§ Scope-Boundary Machinery for the exact required allowlist)
    - .planning/phases/86-accessibility-audit-notes-red-team-close/86-CONTEXT.md (D-03 for the exact permitted paths)
  </read_first>
  <action>
    Edit the `case "$path" in` allowlist inside the `--scope` block of scripts/brandbook-guards.sh.
    Keep the existing brandbook/*, scripts/brandbook-guards.sh, and .planning/* entries and the
    deny-by-default fall-through branch that marks any other path stray. ADD exactly these
    entries: README.md (Phase 85 D-01 header lockup), mix.exs (Phase 85 D-02 ExDoc :logo/:favicon),
    notes/* (this phase's record), scripts/logo-guards.sh (binary budget for NOTES-03),
    scripts/render-svg-png.sh (render helper), and scripts/contrast-audit.sh (the Plan 01 calc).
    Widen the default --scope list (one source of truth) rather than adding a new mode. Do NOT
    add any broader glob (no bare scripts/*, no top-level *): an over-broad allowlist silently
    defeats the audit. Update the block's comment to state the milestone boundary.
  </action>
  <reversibility rating="reversible">Allowlist is a case block; the widening is trivially revertible with no downstream state.</reversibility>
  <verify>
    <automated>bash scripts/brandbook-guards.sh --scope; test $? -eq 0 && grep -q 'README.md' scripts/brandbook-guards.sh && grep -q 'mix.exs' scripts/brandbook-guards.sh && grep -q 'notes/\*' scripts/brandbook-guards.sh && grep -q 'contrast-audit.sh' scripts/brandbook-guards.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/brandbook-guards.sh --scope` exits 0 on the current milestone tree (no false-FAIL on README.md, mix.exs, notes/**, or scripts/contrast-audit.sh).
    - The allowlist adds exactly the six D-03 paths and retains the deny-by-default `*)` stray branch.
    - No bare `scripts/*` or top-level `*` glob is introduced (a scratch stray file outside the allowlist still makes --scope FAIL).
  </acceptance_criteria>
  <done>The --scope boundary is machine-enforced at exactly the milestone allowlist and passes on the correct tree.</done>
</task>

<task type="auto">
  <name>Task 2: Record the red-team pass with captured scope + binary-budget output</name>
  <files>notes/red-team.md</files>
  <read_first>
    - scripts/brandbook-guards.sh (the widened --scope; run it and capture stdout)
    - scripts/logo-guards.sh (run `--assets`; capture the binary-budget line)
    - notes/decision-log.md (voice: sourced claim -> disposition -> proof line)
    - .planning/phases/86-accessibility-audit-notes-red-team-close/86-CONTEXT.md (D-04 for what the record must close with)
  </read_first>
  <action>
    Create notes/red-team.md recording the milestone-close skeptic pass in the decision-log
    voice. Enumerate the boundary the milestone claims (changes are brandbook/** + the two
    integration edits README.md + mix.exs + notes/** + the guard/render/calc scripts) and the
    footgun it closes (a pasted diff asserts without enforcing). Close the record with two
    captured, verbatim command outputs: (a) `scripts/brandbook-guards.sh --scope` including its
    `git diff --stat` and the "== scope OK ==" PASS line; (b) `scripts/logo-guards.sh --assets`
    binary-budget result showing exactly 3 committed rasters, 38,579B <= 204800B ceiling, PASS.
    State that the boundary is machine-enforced by the guard (re-runnable), not asserted in
    prose. MUST NOT add any new binary and MUST NOT edit tokens.css.
  </action>
  <verify>
    <automated>git diff --quiet -- brandbook/tokens/tokens.css && test -f notes/red-team.md && grep -Eq 'git diff --stat' notes/red-team.md && grep -Eiq 'scope OK|--scope' notes/red-team.md && grep -Eq 'binary-budget|204800|38579|38,579' notes/red-team.md</automated>
  </verify>
  <acceptance_criteria>
    - notes/red-team.md closes with the captured `--scope` output (incl. `git diff --stat` and the PASS line).
    - It records the binary-budget result: exactly 3 rasters, 38,579B <= 204800B ceiling, PASS.
    - It states the boundary is machine-enforced (re-runnable guard), not a pasted assertion.
  </acceptance_criteria>
  <done>notes/red-team.md is the recorded skeptic pass closing with machine-captured scope + binary-budget proof (NOTES-03 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git working tree -> brandbook-guards.sh --scope | the porcelain walk parses `git status` output to enforce the scope allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-86-03 | Tampering | --scope allowlist over-permissiveness | medium | mitigate | Add exactly the six D-03 paths, no broad globs; retain deny-by-default `*)` branch; acceptance test asserts a stray scratch file still FAILs. |
| T-86-04 | Tampering | porcelain-walk path parsing | low | mitigate | Reuse the existing line-based porcelain walk (no NUL/eval); rename destinations already handled; quote expansions. |
| T-86-07 | Tampering | new/oversized binary slipping in | low | mitigate | logo-guards.sh --assets binary budget (exactly 3 rasters <=204800B) run and captured in the record. |
| T-86-SC | Tampering | package installs | low | accept | No installs; no supply-chain surface. |
</threat_model>

<verification>
- `scripts/brandbook-guards.sh --scope` PASSES on the milestone tree and still FAILs on a stray out-of-scope path.
- `scripts/logo-guards.sh --assets` binary budget PASSES (3 rasters, 38,579B <= 204800B).
- notes/red-team.md closes with both captured outputs.
- `git diff --quiet -- brandbook/tokens/tokens.css` (zero-drift held); no new binary committed.
</verification>

<success_criteria>
NOTES-03 satisfied: the `brandbook/`-only scope boundary is machine-enforced by the widened
`--scope` allowlist and recorded in `notes/red-team.md` alongside the captured `git diff --stat`
scope audit and the repo-size/binary-budget result.
</success_criteria>

<output>
Create `.planning/phases/86-accessibility-audit-notes-red-team-close/86-04-SUMMARY.md` when done.
</output>
