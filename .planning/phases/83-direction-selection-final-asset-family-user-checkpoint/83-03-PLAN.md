---
phase: 83-direction-selection-final-asset-family-user-checkpoint
plan: 03
type: execute
wave: 3
depends_on: [83-02]
files_modified:
  - brandbook/assets/favicon/favicon.svg
  - brandbook/assets/favicon/favicon.ico
  - brandbook/assets/favicon/apple-touch-icon.png
  - brandbook/assets/social/chimeway-og.svg
  - brandbook/assets/social/chimeway-og.png
  - notes/decision-log.md
autonomous: false
requirements: [INTEG-03, LOGO-03, LOGO-04]
tags: [brand, favicon, og, social, raster, decision-log, checkpoint]

must_haves:
  truths:
    - "favicon.svg is a deliberately-simplified keystone that reads at 16px (not the lockup resized)."
    - "favicon.ico carries 16/32/48; apple-touch-icon.png is 180x180 on a solid paper background."
    - "chimeway-og.png is 1200x630, derived from the mark, under the byte budget."
    - "notes/decision-log.md records the Keystone ratification, the OFL face + license, and ship/defer rationale for the other directions."
    - "`scripts/logo-guards.sh --assets` is fully green and `--scope` passes."
    - "A human ratified the direction and confirmed 16px/mono/inverse/favicon/OG legibility at the checkpoint."
  artifacts:
    - brandbook/assets/favicon/favicon.svg
    - brandbook/assets/favicon/favicon.ico
    - brandbook/assets/favicon/apple-touch-icon.png
    - brandbook/assets/social/chimeway-og.svg
    - brandbook/assets/social/chimeway-og.png
    - notes/decision-log.md
  key_links:
    - "favicon.svg is a filename contract consumed by Phase 85 mix.exs docs() wiring — must be stable."
    - "The decision-log ratification section is the written record for Success Criterion #1 and the human checkpoint."
    - "INTEG-03 boundary: Plan 03 ships the favicon assets + the ready-to-paste wiring snippet; the actual README/mix.exs edits are Phase 85."
---

<objective>
Derive the small mark-dependent derivatives — a deliberately-simplified favicon set and the social/OG card — from the ratified mark (not the lockup naively resized), record the formal direction ratification, and gate the whole family at an explicit human checkpoint.

LOCKED DECISIONS (this session): (1) RATIFY the Keystone direction — do NOT re-explore or re-run a metaphor tournament; Phase 82 already selected it. Phase 83's checkpoint is a formal ratification recorded in notes/decision-log.md. (2) Record the OFL face + license chosen in Plan 02 as the shipped source of truth, retiring Optima. (3) INTEG-03: ship the favicon assets + a ready-to-paste `<link>`/`docs()` snippet here — the actual README/mix.exs wiring is Phase 85 (do NOT edit README or mix.exs in this phase).

Purpose: closes LOGO-03/LOGO-04/INTEG-03 and Success Criteria #1-#4, with the human taste decision (the milestone's largest quality lever) captured explicitly.

Output: favicon.svg/.ico + apple-touch-icon.png, chimeway-og.svg/.png, and the `## Logo Direction Ratification` section in notes/decision-log.md.
</objective>

<artifacts>
## Artifacts this plan produces
- `brandbook/assets/favicon/favicon.svg` — SIMPLIFIED keystone tuned to read at 16px (not a resized lockup).
- `brandbook/assets/favicon/favicon.ico` — multi-size 16/32/48 (binary, justified).
- `brandbook/assets/favicon/apple-touch-icon.png` — 180x180, solid paper background + safe padding (binary, justified).
- `brandbook/assets/social/chimeway-og.svg` — 1200x630 source (mark + wordmark on paper, no cage).
- `brandbook/assets/social/chimeway-og.png` — 1200x630 raster (the one justified OG PNG).
- `notes/decision-log.md` — MODIFY: append the `## Logo Direction Ratification` section.
</artifacts>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-direction-selection-final-asset-family-user-checkpoint/83-RESEARCH.md
@.planning/phases/83-direction-selection-final-asset-family-user-checkpoint/83-PATTERNS.md
@notes/logo-options.md
@notes/decision-log.md
@scripts/logo-guards.sh
@scripts/render-svg-png.sh
@svgo.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplified favicon.svg + favicon.ico + apple-touch-icon.png</name>
  <files>brandbook/assets/favicon/favicon.svg, brandbook/assets/favicon/favicon.ico, brandbook/assets/favicon/apple-touch-icon.png</files>
  <read_first>
    - brandbook/assets/logo/chimeway-mark.svg (from Plan 02) — the 24x24 two-tone keystone to simplify from.
    - 83-RESEARCH.md Pattern 3 (2026 minimal favicon set; verified Chrome→PNG→ImageMagick .ico assembly) and Anti-Patterns (never resize the lockup into a favicon).
    - 83-PATTERNS.md sections "favicon.svg" and "favicon.ico + apple-touch-icon.png".
    - scripts/render-svg-png.sh + svgo.config.mjs (Plan 01) — the raster + optimize tools.
  </read_first>
  <action>
    Author favicon.svg by simplifying the standalone keystone (chimeway-mark.svg) so it reads at 16px — thicken the facet / simplify the silhouette as needed; this is a deliberately simplified mark, NOT the lockup shrunk. Keep viewBox/role/aria-label; two-tone token hex; transparent background. Optimize with npx -y svgo@4.0.2 --config svgo.config.mjs -p 2 --multipass.

    Derive the rasters via Chrome only (never ImageMagick's SVG renderer): use scripts/render-svg-png.sh to render favicon.svg at 512, then magick to resize to 16/32/48 and assemble the multi-size favicon.ico. For apple-touch-icon.png, render at 180x180 from an HTML wrapper that gives it a solid --cw-paper (#fffdf8) background plus safe padding (iOS ignores SVG here and renders transparency poorly). Strip/optimize each raster; keep all three committed rasters tiny (the --assets binary-budget check enforces the ceiling).
  </action>
  <verify>
    <automated>xmllint --noout brandbook/assets/favicon/favicon.svg && grep -q 'viewBox' brandbook/assets/favicon/favicon.svg && magick identify brandbook/assets/favicon/favicon.ico | grep -q '16x16' && magick identify brandbook/assets/favicon/favicon.ico | grep -q '32x32' && magick identify brandbook/assets/favicon/favicon.ico | grep -q '48x48' && magick identify brandbook/assets/favicon/apple-touch-icon.png | grep -q '180x180' && bash scripts/render-svg-png.sh brandbook/assets/favicon/favicon.svg 16 /tmp/fav16.png && [ "$(magick identify -format '%k' /tmp/fav16.png)" -gt 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - favicon.svg is a simplified keystone (well-formed, viewBox present, token hex only), visibly not the wordmark lockup resized.
    - favicon.ico carries the 16/32/48 sizes; apple-touch-icon.png is 180x180 on a solid paper background.
    - The 16px render is non-blank (more than one color). All three rasters are under the byte budget.
  </acceptance_criteria>
  <done>favicon.svg (simplified keystone) + favicon.ico (16/32/48) + apple-touch-icon.png (180x180 paper bg) exist and pass the raster-dims + 16px non-blank checks.</done>
</task>

<task type="auto">
  <name>Task 2: OG/social card + decision-log ratification + ephemeral review gallery</name>
  <files>brandbook/assets/social/chimeway-og.svg, brandbook/assets/social/chimeway-og.png, notes/decision-log.md</files>
  <read_first>
    - brandbook/assets/logo/chimeway-logotype.svg + chimeway-mark.svg (Plan 02) — compose the OG card from these.
    - 83-RESEARCH.md Pattern 3 (Chrome render at exact 1200x630) + Pitfall 3 (raster budget, OG well under ~60 KB) + Open Question 3 (INTEG-03 boundary) + Pitfall 5 / Q1 (Optima→OFL rationale).
    - notes/decision-log.md — headings at lines 13 (Sources), 22 (Divergence Summary), 87 (Validation Commands), 106 (Scope Guard); the dated-intro block (3-11), Sources bullets (15-19), and the bulleted-decision idiom (38-41) to mirror.
    - 83-PATTERNS.md sections "chimeway-og.svg + chimeway-og.png" and "notes/decision-log.md".
    - 83-02-SUMMARY.md — the OFL face name + license chosen in Plan 02.
  </read_first>
  <action>
    Compose chimeway-og.svg: the recut wordmark + the icon keystone on a solid --cw-paper (#fffdf8) field at exactly 1200x630, no enclosing cage, token hex only. Optimize with the pinned svgo config. Render chimeway-og.png via scripts/render-svg-png.sh (Chrome headless) at exactly 1200x630; strip/optimize to stay well under the ~60 KB benchmark.

    Append a new top-level `## Logo Direction Ratification` section to notes/decision-log.md, matching the log's existing structure (dated intro, a Sources bullet list, the bulleted-decision idiom, and a fenced Validation Commands block like lines 87-104). Content:
      - Keystone direction RATIFIED (not re-opened) — cite the Phase 82 D-14 lineage and 82-01-SUMMARY, plus ROADMAP Phase 83 Success Criterion #1 and REQUIREMENTS LOGO-03/04.
      - The wordmark is RE-CUT in the chosen OFL/libre humanist face (name it + its license identifier from 83-02-SUMMARY), retiring Optima to resolve the redistribution-licensing question (RESEARCH Pitfall 5 / Q1).
      - Ship/defer/reject rationale for the other explored directions (the finalist ships; the rest are documented in notes/logo-options.md).
      - The INTEG-03 boundary + the ready-to-paste wiring snippet for Phase 85: the favicon `<link>` set (favicon.ico sizes=any, favicon.svg type image/svg+xml, apple-touch-icon.png) and the mix.exs docs() :logo/:favicon note — assets shipped here, wiring applied in Phase 85 (do not edit README or mix.exs now).
      - A Validation Commands fenced block with the gate: bash scripts/logo-guards.sh --assets && bash scripts/logo-guards.sh --scope.

    Then build an ephemeral file:// gallery (in scratchpad, uncommitted) that renders every shipped mark at 16px, in mono, and inverse (and the favicon at 16px, and the OG card at social-preview scale) using scripts/render-svg-png.sh, so the human checkpoint (Task 3) has something to eyeball. Do NOT commit the gallery (scope discipline — the --scope allowlist does not include it).
  </action>
  <verify>
    <automated>xmllint --noout brandbook/assets/social/chimeway-og.svg && magick identify brandbook/assets/social/chimeway-og.png | grep -q '1200x630' && grep -q 'Logo Direction Ratification' notes/decision-log.md && bash scripts/logo-guards.sh --assets && bash scripts/logo-guards.sh --scope</automated>
  </verify>
  <acceptance_criteria>
    - chimeway-og.svg is well-formed (1200x630 field, no cage, token hex only); chimeway-og.png is exactly 1200x630 and under the byte budget.
    - notes/decision-log.md has the `## Logo Direction Ratification` section: Keystone ratified, OFL face + license recorded, other-direction rationale, INTEG-03 wiring snippet, and a Validation Commands block.
    - `bash scripts/logo-guards.sh --assets` is now FULLY green (all six logo SVGs + favicon.svg + the three rasters + og present and passing) and `--scope` passes.
    - An ephemeral 16px/mono/inverse review gallery exists in scratchpad (uncommitted) for the checkpoint.
  </acceptance_criteria>
  <done>chimeway-og.svg/.png (1200x630, mark-derived, under budget) exist; notes/decision-log.md carries the ratification section; --assets and --scope are fully green; the review gallery is staged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human ratification + perceptual gate (16px / mono / inverse / favicon / OG)</name>
  <action>Present the ephemeral file:// review gallery (all marks at 16px, mono, inverse; favicon at 16px; OG at social scale) and the drafted `## Logo Direction Ratification` section, then block for the human to ratify the Keystone/OFL direction or request changes. Do not proceed until an approval or change list is received; amend the decision-log record per any feedback.</action>
  <what-built>
    The full brandbook/assets/ family: six logo SVGs (Plan 02), the simplified favicon set (favicon.svg/.ico + apple-touch-icon.png), and the OG card (chimeway-og.svg/.png), all passing the automated `--assets` and `--scope` gates. A `## Logo Direction Ratification` section drafted in notes/decision-log.md records the Keystone ratification, the OFL face + license, ship/defer rationale, and the Phase 85 wiring snippet. An ephemeral file:// gallery renders every mark at 16px, mono, and inverse.
  </what-built>
  <how-to-verify>
    1. Open the ephemeral gallery from scratchpad in a browser (path printed by Task 2). Confirm at a glance:
       - Every mark reads at 16px (the favicon reads as a keystone, not a shrunk wordmark).
       - The mono variants still read as the mark without the teal facet.
       - The inverse mark reads cleanly on a dark surface with NO enclosing background cage.
       - The OG card reads at typical social-preview scale and is derived from the mark (not the lockup naively resized).
    2. Read the `## Logo Direction Ratification` section in notes/decision-log.md. Confirm it correctly ratifies Keystone, names the OFL face + license, and carries the ship/defer rationale + Phase 85 wiring snippet.
    3. Ratify the direction: confirm Keystone/OFL-recut is the shipped identity, or describe changes.
  </how-to-verify>
  <resume-signal>Type "approved" to ratify, or describe the perceptual/ratification issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored SVG → committed file | favicon.svg + og.svg enter the repo and are rendered by browsers / social scrapers. |
| generated raster → committed binary | favicon.ico, apple-touch-icon.png, og.png are committed binaries — repo-bloat surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-01 | Tampering / Info-disclosure | favicon.svg, chimeway-og.svg | medium | mitigate | `scripts/logo-guards.sh --assets` hygiene greps enforce presentation-only content on both SVGs. |
| T-83-03 | DoS-ish / hygiene | committed raster binaries | low | mitigate | Exactly three rasters; per-file strip/optimize; the `--assets` binary-budget check enforces the total-byte ceiling (feeds the Phase 86 NOTES-03 red-team). |
| T-83-02 | Tampering | raster/optimize pipeline | low | mitigate | Rasterize via Chrome only; optimize SVGs with the pinned `svgo@4.0.2`; xmllint + token-hex gate over SVG output. |
</threat_model>

<verification>
- `bash scripts/logo-guards.sh --assets` fully green (all seven SVGs + three rasters present and passing dims/hygiene/token/viewBox/budget).
- `bash scripts/logo-guards.sh --scope` passes (allowlist covers brandbook/assets + notes/decision-log.md).
- Human checkpoint approved: 16px/mono/inverse/favicon/OG legibility + direction ratified, recorded in notes/decision-log.md.
</verification>

<success_criteria>
The favicon set and OG card are shipped as mark-derived (not resized) assets, the full `--assets`/`--scope` gates are green, and a human has ratified the Keystone/OFL direction with the decision recorded in notes/decision-log.md — closing LOGO-03, LOGO-04, INTEG-03 and Success Criteria #1-#4.
</success_criteria>

<output>
Create `.planning/phases/83-direction-selection-final-asset-family-user-checkpoint/83-03-SUMMARY.md` when done.
</output>
