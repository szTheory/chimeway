---
phase: 83-direction-selection-final-asset-family-user-checkpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/logo-guards.sh
  - svgo.config.mjs
  - scripts/render-svg-png.sh
autonomous: true
requirements: [LOGO-04]
tags: [brand, logo, svg, tooling, svgo, favicon]

must_haves:
  truths:
    - "`scripts/logo-guards.sh --assets` dispatches a file-level gate over brandbook/assets/** (presence, xmllint, token-hex, hygiene, viewBox, raster dims, byte budget)."
    - "`scripts/logo-guards.sh --scope` passes with brandbook/assets/**, notes/decision-log.md, svgo.config.mjs and the render helper in the allowlist."
    - "The Phase 82 doc gate `scripts/logo-guards.sh notes/logo-options.md` still passes (no regression)."
    - "svgo.config.mjs is a valid ESM module and, when run, preserves viewBox, token hex and a11y attributes on a sample SVG."
    - "The render helper turns an SVG into a non-blank PNG at a requested pixel size via Chrome headless."
  artifacts:
    - scripts/logo-guards.sh
    - svgo.config.mjs
    - scripts/render-svg-png.sh
  key_links:
    - "The --scope allowlist must include every path Waves 2-3 commit, or their scope checks fail (RESEARCH Pitfall 1)."
    - "svgo.config.mjs keeps viewBox — dropping it breaks scaling of every committed mark (RESEARCH Pitfall 2)."
---

<objective>
Build the Wave-0 asset tooling that gates and produces the Phase 83 brand-asset family: extend `scripts/logo-guards.sh` with an `--assets` file-level mode and a widened `--scope` allowlist, add the pinned SVGO optimizer config, and add a deterministic Chrome-headless SVG-to-PNG render helper.

Purpose: Waves 2-3 cannot be verified or produced without this tooling. The `--assets` gate is the automated half of LOGO-04 (legibility/hygiene proven on the shipped files, not just the doc); the render helper is the raster pipeline for the favicon set and OG card; the widened `--scope` prevents every new asset from being flagged stray.

Output: `scripts/logo-guards.sh` (extended, in place), `svgo.config.mjs` (repo root), `scripts/render-svg-png.sh`.
</objective>

<artifacts>
## Artifacts this plan produces
- `scripts/logo-guards.sh` — MODIFY: widened `--scope` allowlist + new `--assets` mode.
- `svgo.config.mjs` — CREATE: safe-plugin SVGO config (repo root, NOT inside brandbook/).
- `scripts/render-svg-png.sh` — CREATE: Chrome-headless SVG to PNG render helper.

## Full phase artifact map (produced across plans 01-03)
- Plan 01: the three tooling artifacts above.
- Plan 02: `brandbook/assets/logo/{chimeway-logotype,chimeway-logotype-mono,chimeway-logotype-inverse,chimeway-logotype-stacked,chimeway-mark,chimeway-mark-mono}.svg`.
- Plan 03: `brandbook/assets/favicon/{favicon.svg,favicon.ico,apple-touch-icon.png}`, `brandbook/assets/social/{chimeway-og.svg,chimeway-og.png}`, and the `## Logo Direction Ratification` section appended to `notes/decision-log.md`.
</artifacts>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-direction-selection-final-asset-family-user-checkpoint/83-RESEARCH.md
@.planning/phases/83-direction-selection-final-asset-family-user-checkpoint/83-PATTERNS.md
@.planning/phases/83-direction-selection-final-asset-family-user-checkpoint/83-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend logo-guards.sh — widen --scope allowlist + add --assets mode</name>
  <files>scripts/logo-guards.sh</files>
  <read_first>
    - scripts/logo-guards.sh (full) — extend in place; reuse existing helpers by line: pass/fail/skip (32-34), ALLOWED_HEX (25), the --scope early-dispatch block (42-74) and its allowlist case (58-61), the token-hex loop (146-164), the no-custom-property check (170-175), the hygiene_check helper + its 7 calls (189-207), the xmllint block (213-240), the trailing summary (242-244).
    - 83-PATTERNS.md section "scripts/logo-guards.sh" (Edit A + Edit B) — the exact reuse map.
    - 83-RESEARCH.md Pattern 4 + Pitfall 1 — the --assets check list and the scope-allowlist fix.
  </read_first>
  <action>
    Two edits, both additive — do not change any Phase 82 behavior.

    Edit A (widen --scope allowlist, case at lines 58-61): add allowed arms for the paths Waves 2-3 will commit so they are not classified stray — a glob arm for brandbook/assets/ paths, plus explicit arms for notes/decision-log.md, svgo.config.mjs, and scripts/render-svg-png.sh. Keep the existing ALLOWED_DOC / ALLOWED_GUARD / .planning arms intact.

    Edit B (new --assets mode): add a sibling early-dispatch block modeled on the --scope block (lines 42-74) — guard on the first argument being the assets flag, run the checks below over the real files under brandbook/assets/, then exit with the accumulated status. Run each check per real file (assets are already one-SVG-per-file, so drop the awk block-splitter used for the doc; call xmllint --noout directly on each .svg). Checks:
      - presence: assert each expected family file exists; a missing file is fail (mirrors the doc-presence gate at 81-85 that proves the guard is wired). The expected set is the six logo SVGs under brandbook/assets/logo/ (chimeway-logotype, chimeway-logotype-mono, chimeway-logotype-inverse, chimeway-logotype-stacked, chimeway-mark, chimeway-mark-mono), favicon.svg, favicon.ico, apple-touch-icon.png under brandbook/assets/favicon/, and chimeway-og.svg + chimeway-og.png under brandbook/assets/social/. (These are produced in Waves 2-3, so this mode reports fail until then — that is intended.)
      - token-hex subset: reuse the ALLOWED_HEX loop (146-164) over each .svg (currentColor is not a hex, so it passes).
      - no CSS custom-property references inside any committed SVG: reuse check 6 (170-175) per file.
      - SVG hygiene (security control T-83-01): reuse the hygiene_check helper and its seven calls (189-207) unchanged, iterating over each asset .svg.
      - xmllint well-formedness: reuse the check-8 logic (213-240) per file, minus the awk splitter.
      - viewBox present on every logo SVG: grep for a viewBox attribute per logo .svg, fail if absent (Pitfall 2).
      - inverse has no baked backdrop: assert the inverse file contains no rect painted with the night primitive hex (the value copied verbatim from tokens.css --cw-night). Fail if present (Pitfall 4). Reference that hex only inside this single grep in the guard.
      - raster dims via magick identify (skip gracefully if magick absent, mirroring the xmllint SKIP idiom): favicon.ico carries the 16, 32 and 48 sizes; apple-touch-icon.png is 180x180; chimeway-og.png is 1200x630.
      - binary budget: exactly three committed raster files under brandbook/assets/, total bytes under a documented ceiling (set it generously, e.g. 200 KB, and note the RESEARCH benchmarks: favicon PNG ~5 KB at 512, OG card well under ~60 KB). Feeds NOTES-03.
    Use the existing pass/fail/skip idiom and print a trailing summary line matching the doc gate's shape.
  </action>
  <verify>
    <automated>bash scripts/logo-guards.sh --scope && bash scripts/logo-guards.sh notes/logo-options.md && bash scripts/logo-guards.sh --assets 2>&1 | grep -q 'chimeway-logotype'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/logo-guards.sh --scope` exits 0 with the widened allowlist (no stray-path failures for the future asset paths).
    - `scripts/logo-guards.sh notes/logo-options.md` still exits 0 (Phase 82 gate unbroken — no regression).
    - `scripts/logo-guards.sh --assets` dispatches the new mode and prints presence PASS/FAIL lines for each expected family file — the verify greps for a known family filename (`chimeway-logotype`) so a broken/non-dispatching mode fails loudly rather than being swallowed. The mode's overall exit is non-zero now because the files do not exist yet; it turns green at the end of Plan 03.
    - The inverse-backdrop, viewBox, raster-dims and binary-budget checks are present in the mode and use the existing pass/fail/skip idiom.
  </acceptance_criteria>
  <done>--scope and the Phase 82 doc gate exit 0; --assets mode dispatches and prints per-file presence lines (red until Plan 03).</done>
</task>

<task type="auto">
  <name>Task 2: Add svgo.config.mjs + scripts/render-svg-png.sh</name>
  <files>svgo.config.mjs, scripts/render-svg-png.sh</files>
  <read_first>
    - 83-RESEARCH.md Pattern 1 (safe SVGO config, verified -61.7% with viewBox/hex/a11y preserved) and Pattern 3 / Code Examples (Chrome-headless render wrapper).
    - 83-PATTERNS.md sections "svgo.config.mjs" and "scripts/<render helper>" (both NO-ANALOG — use RESEARCH verbatim).
    - 83-RESEARCH.md section Environment Availability — Chrome at "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome", fallback chromium at /opt/homebrew/bin/chromium.
  </read_first>
  <action>
    Create svgo.config.mjs at repo root (NOT inside brandbook/ — zero-build scope). ESM default export with multipass true and a single preset-default plugin whose overrides keep viewBox (removeViewBox false — critical), keep human-readable ids (cleanupIds false), and keep aria + role attributes. Add a header comment documenting the pinned invocation: run with npx -y svgo@4.0.2 --config svgo.config.mjs -p 2 --multipass -i in.svg -o out.svg. Pin the exact version 4.0.2 in that documented command (supply-chain control T-83-02 — see RESEARCH Package Legitimacy Audit).

    Create scripts/render-svg-png.sh: a small, dependency-light bash helper that takes an input SVG path, a pixel size (and/or explicit width and height), and an output PNG path. It writes an HTML wrapper that pins the exact pixel box, then screenshots it via Chrome headless (--headless --disable-gpu --hide-scrollbars --force-device-scale-factor=1 --window-size). Resolve the Chrome binary from the macOS app path with a fallback to the chromium homebrew path. Rasterize via Chrome only — never ImageMagick's internal SVG renderer (low fidelity on outlined paths). The helper is the raster stage for the favicon set / OG card and the 16px non-blank perceptual check in later plans.
  </action>
  <verify>
    <automated>node -e "import('./svgo.config.mjs').then(m => { if (!m.default || m.default.multipass !== true) process.exit(1); }).catch(() => process.exit(1))" && printf '%s' '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="48" height="48" role="img" aria-label="chimeway mark"><path d="M6 5 L18 5 L15.5 19 L8.5 19 Z" fill="#102027"/><path d="M12 5 L18 5 L15.5 19 L12 19 Z" fill="#0e7c86"/></svg>' > /tmp/cw_icon.svg && bash scripts/render-svg-png.sh /tmp/cw_icon.svg 16 /tmp/cw_icon16.png && magick identify /tmp/cw_icon16.png | grep -q '16x16'</automated>
  </verify>
  <acceptance_criteria>
    - svgo.config.mjs is a valid ESM module exporting a default with multipass true and preset-default overrides that keep viewBox, ids, and aria/role.
    - The documented invocation pins svgo@4.0.2.
    - scripts/render-svg-png.sh renders the sample keystone SVG to a 16x16 non-blank PNG (magick identify reports 16x16); Chrome path resolves with the chromium fallback.
  </acceptance_criteria>
  <done>svgo.config.mjs imports as valid ESM (multipass true, preset-default overrides); the render helper produces a 16x16 non-blank PNG.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authoring tool → committed SVG file | SVGO output and hand-authored SVG cross into the repo; a browser later renders committed SVGs, so active content is a risk. |
| npm registry → build host | `npx` fetches the SVGO optimizer at authoring time (nothing from node_modules is committed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-83-01 | Tampering / Info-disclosure | committed SVG files | medium | mitigate | The new `--assets` mode reuses the existing hygiene_check greps (presentation elements only; no active or remote content) over every asset SVG — this is the phase's primary control. |
| T-83-02 | Tampering | SVGO optimizer supply chain | medium | mitigate | Pin `svgo@4.0.2` in the documented invocation; nothing from node_modules is committed; xmllint + human review of output. |
| T-83-SC | Tampering | `npx` install of svgo | low | mitigate | svgo@4.0.2 is Approved in RESEARCH §Package Legitimacy Audit (official github.com/svg/svgo, mature, very high downloads) — no `[ASSUMED]`/`[SUS]` checkpoint required; ephemeral `npx -y`, zero committed dependency. |
</threat_model>

<verification>
- `bash scripts/logo-guards.sh --scope` exits 0 (widened allowlist).
- `bash scripts/logo-guards.sh notes/logo-options.md` exits 0 (Phase 82 doc gate unbroken).
- `bash scripts/logo-guards.sh --assets` dispatches and prints presence lines (non-zero until Plan 03 — expected).
- svgo.config.mjs imports cleanly; the render helper produces a non-blank 16px PNG.
</verification>

<success_criteria>
The `--assets` gate, the widened `--scope` allowlist, the pinned SVGO config, and the Chrome render helper all exist and function; no Phase 82 behavior regresses.
</success_criteria>

<output>
Create `.planning/phases/83-direction-selection-final-asset-family-user-checkpoint/83-01-SUMMARY.md` when done.
</output>
