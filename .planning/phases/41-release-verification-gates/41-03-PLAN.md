---
phase: 41
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - mix.exs
  - .github/workflows/ci.yml
  - .planning/phases/41-release-verification-gates/41-VALIDATION.md
autonomous: true
requirements:
  - GATE-01
must_haves:
  truths:
    - verify.example runs demo host tests AND chimeway_admin test suite additively (D-10, D-11).
    - GitHub Actions verify_example job runs mix verify.example on every push/PR to main (D-08).
    - verify.example is NOT added to default mix ci alias (D-09).
    - CI job provisions Postgres and runs root ecto.create + ecto.migrate before verify.example.
  artifacts:
    - path: mix.exs
      provides: "Expanded verify.example subprocess chain"
    - path: .github/workflows/ci.yml
      provides: "verify_example merge-blocking CI job"
  key_links:
    - from: mix.exs
      to: examples/chimeway_demo_host/
      via: "verify.example first subprocess — webhook + feedback E2E preserved (D-11)"
    - from: mix.exs
      to: chimeway_admin/test/
      via: "verify.example second subprocess — admin smoke (D-10)"
    - from: .github/workflows/ci.yml
      to: mix verify.example
      via: "Dedicated always-on CI job (D-08)"
---

<objective>
Expand `mix verify.example` to include chimeway_admin operator smoke alongside existing demo-host E2E, and wire a dedicated always-on GitHub Actions job. Satisfies GATE-01 reference-flow CI proof (D-08 through D-11).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-release-verification-gates/41-CONTEXT.md
@.planning/phases/41-release-verification-gates/41-RESEARCH.md
@.planning/phases/41-release-verification-gates/41-VALIDATION.md
@mix.exs
@.github/workflows/ci.yml
@examples/chimeway_demo_host/mix.exs
@examples/chimeway_demo_host/test/
@chimeway_admin/test/
@.planning/phases/40-operator-trace-mvp/40-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 41-03-01: Expand mix verify.example with chimeway_admin subprocess</name>
  <files>mix.exs</files>
  <read_first>
    - .planning/phases/41-release-verification-gates/41-CONTEXT.md D-10, D-11, D-09
    - mix.exs current verify.example alias
    - examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs
    - examples/chimeway_demo_host/test/demo_host_web/controllers/webhooks_controller_test.exs
    - chimeway_admin/test/chimeway_admin/routes_test.exs
    - chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs
  </read_first>
  <action>
    In `mix.exs` `aliases/0`, replace the `verify.example` alias with additive subprocess chain (D-10, D-11):

    ```elixir
    # Pre-ship GATE-01: canonical host-mount E2E + operator admin smoke (D-10, D-11).
    # Run separately from ci.test to preserve fast feedback on core lib tests (Phase 33 D-10).
    "verify.example": [
      "cmd cd examples/chimeway_demo_host && mix deps.get && mix test",
      "cmd cd chimeway_admin && mix deps.get && mix test"
    ],
    ```

    **Preserve (D-11):** First subprocess unchanged — demo host webhook + feedback pipeline E2E remain the first step.

    **Add (D-10):** Second subprocess runs full `chimeway_admin` test suite (routes, LiveAuth, redaction, TraceSearchLive — ~11 tests).

    **Do NOT (D-09):** Add `verify.example` to the default `ci` alias.

    Update the comment above `verify.example` to mention admin smoke alongside host-mount E2E.
  </action>
  <acceptance_criteria>
    - `grep -c 'chimeway_demo_host' mix.exs` ≥ 1 inside verify.example block
    - `grep -c 'chimeway_admin' mix.exs` ≥ 1 inside verify.example block
    - `grep -A5 '"verify.example"' mix.exs | grep -c 'mix test'` ≥ 2
    - `mix verify.example` exits 0 (requires local Postgres with chimeway_test database)
    - Demo host tests still run: `grep -c 'feedback_pipeline_e2e' examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs` ≥ 1 (file unchanged)
  </acceptance_criteria>
  <verify>
    <automated>mix ecto.create --quiet 2>/dev/null; mix ecto.migrate --quiet 2>/dev/null; mix verify.example</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 41-03-02: Add verify_example CI job</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/41-release-verification-gates/41-CONTEXT.md D-08, D-09
    - .github/workflows/ci.yml test job (Postgres service template)
    - .github/workflows/ci.yml install_golden_contract job (dedicated proof-lane pattern — but NO path gating per D-08)
    - mix.exs verify.example alias (post task 41-03-01)
  </read_first>
  <action>
    Add new job `verify_example` to `.github/workflows/ci.yml` after the `test` job block and before `install_golden_contract`.

    **Job specification (D-08):**

    ```yaml
    verify_example:
      name: Example host + admin smoke
      runs-on: ubuntu-latest
      services:
        postgres:
          image: postgres:15
          env:
            POSTGRES_PASSWORD: postgres
          options: >-
            --health-cmd pg_isready
            --health-interval 10s
            --health-timeout 5s
            --health-retries 5
          ports:
            - 5432:5432
      env:
        MIX_ENV: test
        DATABASE_URL: postgres://postgres:postgres@localhost/chimeway_test
      steps:
        - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        - uses: erlef/setup-beam@8251c48667b97e88a0a24ec512f5b72a039fcea7
          with:
            elixir-version: "1.17"
            otp-version: "27"
        - uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
          with:
            path: |
              deps
              _build
            key: ${{ runner.os }}-mix-verify-example-${{ hashFiles('**/mix.lock') }}
            restore-keys: |
              ${{ runner.os }}-mix-verify-example-
        - run: |
            mix local.rebar --force
            mix local.hex --force
            mix deps.get
        - run: mix ecto.create --quiet
        - run: mix ecto.migrate --quiet
        - run: mix verify.example
    ```

    **Critical constraints:**
    - **Always runs** on push/PR to main — NO path-gating detect step (D-08; unlike `install_golden_contract`)
    - **Single OTP matrix** Elixir 1.17 / OTP 27 (research OQ-4)
    - **Postgres required** — root `ecto.create` + `ecto.migrate` before `mix verify.example`
    - **No fetch-depth: 0** — not needed without git diff gating
    - Reuse existing SHA-pinned action versions from sibling jobs
    - Do NOT add `verify.example` to any existing job's `mix ci` step (D-09)
  </action>
  <acceptance_criteria>
    - `grep -c 'verify_example:' .github/workflows/ci.yml` ≥ 1
    - `grep -c 'mix verify.example' .github/workflows/ci.yml` ≥ 1
    - `grep -c 'Example host + admin smoke' .github/workflows/ci.yml` ≥ 1
    - `grep -c 'detect' .github/workflows/ci.yml` inside verify_example job block returns 0 matches
    - `grep -c 'ecto.create' .github/workflows/ci.yml` ≥ 2 (test job + verify_example job)
    - YAML validates: `ruby -ryaml -e "YAML.load_file('.github/workflows/ci.yml')" ` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'verify_example:' .github/workflows/ci.yml && grep -q 'mix verify.example' .github/workflows/ci.yml && ruby -ryaml -e "YAML.load_file('.github/workflows/ci.yml')"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 41-03-03: Finalize phase validation sign-off</name>
  <files>.planning/phases/41-release-verification-gates/41-VALIDATION.md</files>
  <read_first>
    - .planning/phases/41-release-verification-gates/41-VALIDATION.md
    - MAINTAINING.md step 3 (post plan 41-02)
  </read_first>
  <action>
    Update `.planning/phases/41-release-verification-gates/41-VALIDATION.md`:

    1. Mark task 41-03-01 status ✅ green.
    2. Check all Wave 0 Requirements checkboxes complete.
    3. Set frontmatter `nyquist_compliant: true` and `status: complete`.
    4. Set Validation Sign-Off approval checklist items checked.

    Run full GATE-01 pre-ship quartet locally:
    ```bash
    mix ci
    mix ci.docs
    mix ci.verify_gates
    mix verify.example
    ```

    Confirm `mix help ci` does not reference verify.example (D-09 audit).
  </action>
  <acceptance_criteria>
    - `mix ci` exits 0
    - `mix ci.docs` exits 0
    - `mix ci.verify_gates` exits 0
    - `mix verify.example` exits 0
    - 41-VALIDATION.md frontmatter contains `nyquist_compliant: true`
    - 41-VALIDATION.md Wave 0 Requirements section has all items checked
  </acceptance_criteria>
  <verify>
    <automated>mix ci && mix ci.docs && mix ci.verify_gates && mix verify.example</automated>
  </verify>
</task>

</tasks>

<verification>
1. `mix verify.example` runs demo_host then chimeway_admin subprocesses in order
2. `verify_example` CI job has no path-gating — runs on every PR
3. `mix ci` alias unchanged — no verify.example bundled (D-09)
4. Drift test: break chimeway_admin route test → verify.example fails
</verification>

<success_criteria>
- verify.example expanded with admin smoke; demo-host E2E preserved (D-10, D-11)
- verify_example CI job wired and always-on (D-08)
- Default mix ci unchanged (D-09)
- Full GATE-01 pre-ship quartet passes locally
- Phase validation nyquist_compliant sign-off complete
</success_criteria>

<output>
After completion, create `.planning/phases/41-release-verification-gates/41-03-SUMMARY.md` via orchestration.
</output>
