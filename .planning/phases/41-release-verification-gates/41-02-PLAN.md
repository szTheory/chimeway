---
phase: 41
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - mix.exs
  - MAINTAINING.md
  - .planning/phases/41-release-verification-gates/41-VALIDATION.md
autonomous: true
requirements:
  - GATE-01
must_haves:
  truths:
    - mix ci.verify_gates alias runs doc-contract tests only — not full mix test (D-12, D-13).
    - Default mix ci alias does NOT include verify.example (D-09 preserved).
    - MAINTAINING.md step 3 mandates mix ci, mix ci.docs, mix ci.verify_gates, and mix verify.example before tagging (D-14).
    - MAINTAINING.md documents mix ci.install_golden as required when installer surfaces change (D-15).
    - Post-publish verify trio in step 7 remains unchanged — GATE-01 gates are pre-ship only (D-16).
  artifacts:
    - path: mix.exs
      provides: "ci.verify_gates named GATE-01 entrypoint"
    - path: MAINTAINING.md
      provides: "Pre-ship release runbook with GATE-01 quartet + installer conditional"
    - path: .planning/phases/41-release-verification-gates/41-VALIDATION.md
      provides: "Phase validation sign-off after alias + runbook land"
  key_links:
    - from: mix.exs
      to: test/chimeway/doc_contract_test.exs
      via: "ci.verify_gates scoped test path"
    - from: MAINTAINING.md
      to: mix ci.verify_gates
      via: "Step 3 pre-ship mandate (D-14)"
    - from: MAINTAINING.md
      to: mix ci.install_golden
      via: "Installer-change conditional (D-15)"
---

<objective>
Add the named `mix ci.verify_gates` maintainer entrypoint and update `MAINTAINING.md` so GATE-01 pre-ship commands are documented as mandatory. Does not modify post-publish verify trio (D-12 through D-16).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-release-verification-gates/41-CONTEXT.md
@.planning/phases/41-release-verification-gates/41-RESEARCH.md
@.planning/phases/41-release-verification-gates/41-VALIDATION.md
@mix.exs
@MAINTAINING.md
@test/chimeway/doc_contract_test.exs
@AGENTS.md
@.planning/phases/35-installer-task/35-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 41-02-01: Add mix ci.verify_gates alias</name>
  <files>mix.exs</files>
  <read_first>
    - .planning/phases/41-release-verification-gates/41-CONTEXT.md D-12, D-13, D-09
    - mix.exs aliases section (ci.install_golden pattern)
    - test/chimeway/doc_contract_test.exs (target test path from plan 41-01)
    - AGENTS.md Quality Gates section
  </read_first>
  <action>
    In `mix.exs` `aliases/0`, add `ci.verify_gates` immediately after `ci.install_golden` entry, mirroring the scoped-test pattern (D-12, D-13):

    ```elixir
    # GATE-01 doc-contract + version alignment gates (pre-ship; no Postgres required)
    "ci.verify_gates": [
      "cmd env MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --warnings-as-errors"
    ],
    ```

    **Do NOT:**
    - Add `verify.example` to the default `ci` alias (D-09)
    - Bundle `mix ci.docs` inside `ci.verify_gates` — MAINTAINING.md mandates docs separately (D-14 discretion resolved toward separation)
    - Run full `mix test` — scoped path only (D-13)

    Verify `ci` alias remains `["ci.lint", "ci.test"]` unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c 'ci.verify_gates' mix.exs` ≥ 1
    - `grep -c 'doc_contract_test.exs' mix.exs` ≥ 1
    - `mix help ci.verify_gates` exits 0
    - `mix ci.verify_gates` exits 0
    - `mix help ci` output does not contain `verify.example`
    - `grep 'verify.example' mix.exs` inside `ci:` alias block returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>mix ci.verify_gates && ! mix help ci 2>&1 | grep -q 'verify.example'</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 41-02-02: Update MAINTAINING.md release runbook</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - .planning/phases/41-release-verification-gates/41-CONTEXT.md D-14, D-15, D-16
    - MAINTAINING.md current steps 3–9
    - .github/workflows/ci.yml install_golden_contract job (D-15 reference — do not modify CI gating)
    - mix.exs ci.install_golden alias
  </read_first>
  <action>
    Update `MAINTAINING.md` release runbook:

    **Replace step 3 ("Run the full local gate")** with expanded pre-ship GATE-01 quartet (D-14):

    ```markdown
    ### 3. Run the full local gate

    Run all pre-ship verification commands before tagging or publishing:

    ```bash
    mix ci
    mix ci.docs
    mix ci.verify_gates
    mix verify.example
    ```

    - `mix ci` — lint + full test suite
    - `mix ci.docs` — HexDocs build with warnings-as-errors
    - `mix ci.verify_gates` — adoption-surface doc-contract and version-alignment gates (GATE-01)
    - `mix verify.example` — demo host webhook E2E + chimeway_admin operator smoke

    All four must pass before publishing.
    ```

    **Remove old step 4 ("Run the docs gate")** — docs are now part of step 3. Renumber subsequent steps (current runbook is steps 1–9; result is steps 1–8):
    - Former step 5 (Commit the release) → step 4
    - Former step 6 (Tag) → step 5
    - Former step 7 (Publish to Hex) → step 6
    - Former step 8 (Verify the release) → step 7 — **keep verify.clean / verify.parity / verify.published trio unchanged** (D-16)
    - Former step 9 (Create GitHub Release) → step 8

    **Add new subsection after step 3** titled `### Installer template changes` (D-15):

    ```markdown
    ### Installer template changes

    When modifying any of these paths, also run `mix ci.install_golden` locally before merging:

    - `priv/chimeway_migrations/`
    - `lib/mix/tasks/chimeway.gen.migrations.ex`
    - `lib/chimeway/install/`
    - `test/chimeway/install/`
    - `test/fixtures/installer_golden/`

    CI runs `install_golden_contract` on every push to `main` and on PRs that touch installer surfaces (path-gated). Do not change that gating behavior.
    ```

  </action>
  <acceptance_criteria>
    - `grep -c 'mix ci.verify_gates' MAINTAINING.md` ≥ 1
    - `grep -c 'mix verify.example' MAINTAINING.md` ≥ 1
    - `grep -c 'mix ci.docs' MAINTAINING.md` ≥ 1
    - `grep -c 'mix ci.install_golden' MAINTAINING.md` ≥ 1
    - `grep -c 'verify.clean' MAINTAINING.md` ≥ 1
    - `grep -c 'verify.parity' MAINTAINING.md` ≥ 1
    - `grep -c 'verify.published' MAINTAINING.md` ≥ 1
    - `grep -c 'GATE-01' MAINTAINING.md` ≥ 1
    - Step numbering is sequential 1–8 with no duplicate "### 4." headers
    - `grep -c '### 7. Publish to Hex' MAINTAINING.md` = 0
    - `grep -c '### 6. Publish to Hex' MAINTAINING.md` ≥ 1
    - `grep -c '### 7. Verify the release' MAINTAINING.md` ≥ 1
    - `grep -c '### 8. Create GitHub Release' MAINTAINING.md` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'mix ci.verify_gates' MAINTAINING.md && grep -q 'mix verify.example' MAINTAINING.md && grep -q 'mix ci.install_golden' MAINTAINING.md && grep -q 'verify.published' MAINTAINING.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 41-02-03: Update phase validation checklist</name>
  <files>.planning/phases/41-release-verification-gates/41-VALIDATION.md</files>
  <read_first>
    - .planning/phases/41-release-verification-gates/41-VALIDATION.md
    - .planning/phases/37-doc-truth-journey-guides/37-VALIDATION.md (sign-off pattern)
  </read_first>
  <action>
    Update `.planning/phases/41-release-verification-gates/41-VALIDATION.md`:

    1. Mark tasks 41-01-01, 41-01-02, 41-02-01, 41-02-02 status rows ✅ green after verification passes.
    2. Check Wave 0 items for doc-contract describe blocks, version alignment, and `ci.verify_gates` alias.
    3. Set frontmatter `wave_0_complete: true` only after `mix ci.verify_gates` passes (partial — verify.example items remain for plan 41-03).

    Run and record:
    - `mix ci.verify_gates`
    - `mix ci.docs`
    - `mix ci`
  </action>
  <acceptance_criteria>
    - `mix ci.verify_gates` exits 0
    - `mix ci.docs` exits 0
    - `mix ci` exits 0
    - 41-VALIDATION.md contains checked items for doc-contract blocks and ci.verify_gates alias
  </acceptance_criteria>
  <verify>
    <automated>mix ci.verify_gates && mix ci.docs && mix ci</automated>
  </verify>
</task>

</tasks>

<verification>
1. `mix ci.verify_gates` exits 0 in &lt;10s (no Postgres)
2. `mix help ci` does not list verify.example
3. MAINTAINING.md step 3 lists all four pre-ship commands
4. MAINTAINING.md step 7 still documents post-publish verify trio unchanged
</verification>

<success_criteria>
- Named GATE-01 entrypoint `mix ci.verify_gates` ships and passes (D-12, D-13)
- MAINTAINING.md documents pre-ship quartet + installer conditional (D-14, D-15)
- Post-publish verify trio untouched (D-16)
- Default `mix ci` unchanged — verify.example not bundled (D-09)
</success_criteria>

<output>
After completion, create `.planning/phases/41-release-verification-gates/41-02-SUMMARY.md` via orchestration.
</output>
