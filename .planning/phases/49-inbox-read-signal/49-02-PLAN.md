---
phase: 49-inbox-read-signal
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - test/chimeway/orchestration/workflow_progression_test.exs
autonomous: true
requirements: [READ-02, READ-03]
must_haves:
  truths:
    - "`Chimeway.mark_read/3` on a notification whose workflow run is `:waiting` with matching `pending_signals` resumes to `:active` without host `Signal.track` glue (READ-02)"
    - "`signal_received` transition records `context == %{\"event_name\" => \"chimeway.notification.read\"}` with no payload or notification_id keys (READ-03)"
    - "Phase 48 manual-injection test remains as routing regression — not removed or repurposed"
    - "Test does not assert email step cancellation or `:stopped` state — JOUR-06 is Phase 51 scope (Pitfall 2)"
  artifacts:
    - path: "test/chimeway/orchestration/workflow_progression_test.exs"
      provides: "E2E proof: mark_read → SignalRouterWorker → resume → trace"
      contains: "mark_read resumes waiting run (READ-02/03)"
  key_links:
    - from: "test/chimeway/orchestration/workflow_progression_test.exs"
      to: "Chimeway.mark_read/3"
      via: "public API instead of manual Signal.track in new test"
      pattern: "Chimeway\\.mark_read"
    - from: "test/chimeway/orchestration/workflow_progression_test.exs"
      to: "Chimeway.Dispatch.SignalRouterWorker"
      via: "perform_job after mark_read enqueues worker"
      pattern: "perform_job\\(SignalRouterWorker"
    - from: "Chimeway.Workflows.route_signal/1"
      to: "WorkflowTransition.context"
      via: "signal_received reason with event_name only"
      pattern: "signal_received"
---

<objective>
Prove READ-02 and READ-03 end-to-end: inbox `mark_read` emits a signal that `SignalRouterWorker` routes to resume a `:waiting` workflow run, with an explainable `signal_received` transition showing event name only.

Purpose: Unit tests in Plan 01 prove emission; this plan proves the full path through existing Phase 48 routing without host glue and without modifying `route_signal/1` (D-08).

Output: Extended `test/chimeway/orchestration/workflow_progression_test.exs` with a sibling integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-inbox-read-signal/49-CONTEXT.md
@.planning/phases/49-inbox-read-signal/49-RESEARCH.md
@.planning/phases/49-inbox-read-signal/49-PATTERNS.md
@.planning/phases/49-inbox-read-signal/49-VALIDATION.md
@.planning/phases/49-inbox-read-signal/49-01-SUMMARY.md

@lib/chimeway/inbox.ex
@lib/chimeway/workflows.ex
@lib/chimeway/dispatch/signal_router_worker.ex
@test/chimeway/orchestration/workflow_progression_test.exs
@test/chimeway/workflows_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: mark_read → SignalRouterWorker → resume integration test (READ-02, READ-03)</name>
  <files>test/chimeway/orchestration/workflow_progression_test.exs</files>
  <read_first>
    - `test/chimeway/orchestration/workflow_progression_test.exs` — `ChimewayTest.Notifiers.WorkflowProgressionWithSignals` fixture, `"injected signal resumes waiting run via SignalRouterWorker without host update_run glue"` test (~lines 357–398), `trigger_workflow_with_signals!/1` helper, `use Oban.Testing` at module level
    - `lib/chimeway/inbox.ex` — Plan 01 emission implementation (must be complete)
    - `lib/chimeway/workflows.ex` — `route_signal/1` transition context shape (`%{"event_name" => event_name}` only)
    - `test/chimeway/workflows_test.exs` — trace payload safety assertions (~lines 280–288)
    - `.planning/phases/49-inbox-read-signal/49-RESEARCH.md` — Pitfall 2 (do not assert email cancellation), Pitfall 4 (sibling test pattern)
  </read_first>
  <action>
    In `test/chimeway/orchestration/workflow_progression_test.exs`, add a new describe block `"mark_read resumes waiting run (READ-02/03)"` with test `"mark_read emits signal that resumes waiting run via SignalRouterWorker"`:

    1. **Setup** — call `trigger_workflow_with_signals!/1` to get `notification`, `workflow_run`, and in_app delivery. Use the same convergence path as the Phase 48 manual-injection test: transition in_app delivery to `:succeeded` via `Deliveries.transition_status/2` + `Deliveries.record_attempt/2`, then reload `workflow_run`.

    2. **Assert waiting state** — `waiting_run.state == :waiting`, `waiting_run.pending_signals == ["chimeway.notification.read"]`, `waiting_run.status_reason` is the waiting reason (not yet `signal_received`).

    3. **Inbox emission path (READ-02)** — call `Chimeway.mark_read(notification.id, notification.recipient_identity)` (public API, not `Inbox` directly, not `Signal.track`). Assert return `:ok`.

    4. **Drain Oban job** — use `assert_enqueued(worker: SignalRouterWorker, ...)` to capture `signal_id`, then `perform_job(SignalRouterWorker, %{"signal_id" => signal_id})`. Assert `:ok`.

    5. **Assert resume** — reload run: `state == :active`, `pending_signals == []`, `status_reason == "signal_received"`.

    6. **Assert trace (READ-03)** — filter transitions for `reason == "signal_received"`. Assert exactly one transition. Assert `transition.context == %{"event_name" => "chimeway.notification.read"}`. Assert `refute Map.has_key?(transition.context, "payload")`. Assert `refute Map.has_key?(transition.context, "notification_id")`. Assert `transition.from_state == :waiting` and `transition.to_state == :active`.

    7. **Scope fence** — do **not** assert email delivery count, `:stopped` state, or that `due_at` escalation was cancelled. Resume to `:active` + trace is sufficient for Phase 49.

    8. **Regression** — leave the existing `"injected signal resumes waiting run via SignalRouterWorker without host update_run glue"` test unchanged. The new test is a sibling proving inbox emission replaces manual injection for READ-02.

    Verify new test body contains **no** `Signal.track(` call and **no** `Workflows.update_run(..., %{pending_signals: ...})` call.
  </action>
  <verify>
    <automated>mix test test/chimeway/orchestration/workflow_progression_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains describe `"mark_read resumes waiting run (READ-02/03)"`.
    - New test calls `Chimeway.mark_read(notification.id, notification.recipient_identity)` — not `Signal.track`.
    - After `perform_job(SignalRouterWorker, ...)`, run `state == :active` and `pending_signals == []`.
    - `signal_received` transition `context == %{"event_name" => "chimeway.notification.read"}` with no payload/notification_id keys.
    - Phase 48 manual-injection test still present and passing.
    - `mix test test/chimeway/orchestration/workflow_progression_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Integration test proves READ-02 inbox emission path and READ-03 trace safety end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Regression gate — routing stack unchanged (D-08)</name>
  <files>test/chimeway/orchestration/workflow_progression_test.exs</files>
  <read_first>
    - `test/chimeway/workflows_test.exs` — `route_signal/1` trace assertions
    - `test/chimeway/dispatch/signal_router_worker_test.exs` — worker regression patterns
    - `.planning/phases/49-inbox-read-signal/49-CONTEXT.md` — D-08: no changes to route_signal/1
  </read_first>
  <action>
    Run regression suite to confirm Plan 02 added no engine changes:

    1. `mix test test/chimeway/workflows_test.exs test/chimeway/dispatch/signal_router_worker_test.exs --warnings-as-errors`
    2. Confirm `lib/chimeway/workflows.ex` and `lib/chimeway/dispatch/signal_router_worker.ex` have zero diff from Wave 1 baseline (this plan modifies only the progression test file).

    If any regression fails, fix only test file issues — do not modify `route_signal/1` or `SignalRouterWorker` (D-08).
  </action>
  <verify>
    <automated>mix test test/chimeway/workflows_test.exs test/chimeway/dispatch/signal_router_worker_test.exs test/chimeway/orchestration/workflow_progression_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/chimeway/workflows_test.exs test/chimeway/dispatch/signal_router_worker_test.exs test/chimeway/orchestration/workflow_progression_test.exs --warnings-as-errors` exits 0.
    - `git diff --name-only` for this plan includes only `test/chimeway/orchestration/workflow_progression_test.exs`.
    - Phase 48 describe `"wait_until auto-populates pending_signals (READ-01)"` tests still pass.
  </acceptance_criteria>
  <done>Routing stack regression green; READ-02/03 proof isolated to inbox emission path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inbox → SignalRouterWorker | Signal row carries tenant + actor from resolved notification context; worker delegates to `route_signal/1`. |
| Signal routing → workflow resume | `find_runs_waiting_for_signal/3` enforces tenant + recipient_identity + event_name ∈ pending_signals — unchanged (D-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-06 | Spoofing | cross-tenant resume via inbox signal | accept | Inherited from `route_signal/1` tenant filter (T-27-03); Plan 02 adds no routing changes. |
| T-49-07 | Spoofing | cross-recipient resume | accept | `recipient_identity` join unchanged; inbox API scopes update by recipient. |
| T-49-08 | Information Disclosure | signal payload in operator trace | mitigate | Test asserts `context` has `event_name` only — READ-03 contract; `route_signal/1` unchanged. |
| T-49-09 | Elevation of Privilege | mark_read resumes unrelated waiting runs | accept | Match requires `event_name in pending_signals` + same tenant + same actor — existing isolation. |

**No `high`-severity threats.** Test-only plan with no runtime changes; routing isolation inherited from Phase 27/48.
</threat_model>

<verification>
- [ ] `mix test test/chimeway/orchestration/workflow_progression_test.exs --warnings-as-errors` green
- [ ] `mix test test/chimeway/workflows_test.exs test/chimeway/dispatch/signal_router_worker_test.exs --warnings-as-errors` green
- [ ] `mix ci.test` green after Wave 2 merge
- [ ] New test uses `Chimeway.mark_read` — grep confirms no `Signal.track` in new describe block
</verification>

<success_criteria>
- READ-02: `mark_read` → worker → `:waiting` run resumes to `:active` without host glue
- READ-03: `signal_received` transition shows event name only in context
- Phase 48 routing regression tests still pass
- `route_signal/1` and `SignalRouterWorker` unchanged (D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/49-inbox-read-signal/49-02-SUMMARY.md`
</output>
