---
phase: 54-mailglass-adapter-core
plan: 03
type: execute
wave: 3
depends_on: [54-02]
files_modified:
  - test/chimeway/adapters/mailglass_adapter_test.exs
  - test/chimeway/dispatch/executor_mailglass_adapter_test.exs
  - guides/recipes/custom-adapter.md
requirements: [ECOS-02]
must_haves:
  truths:
    - "`Chimeway.Adapters.Mailglass` passes `Chimeway.Adapter.ContractTest` including `simulate_error?/0` (D-16, ECOS-02)"
    - "Success path records Fake delivery via Mailglass.Outbound (proves Mailglass-rendered handoff)"
    - "Error classification tests cover :temporary, :permanent, and :bounced"
    - "Executor resolves `Chimeway.Adapters.Mailglass` for email channel when configured"
  artifacts:
    - path: "test/chimeway/adapters/mailglass_adapter_test.exs"
      provides: "Contract + classification tests"
      contains: "Chimeway.Adapter.ContractTest"
    - path: "test/chimeway/dispatch/executor_mailglass_adapter_test.exs"
      provides: "Per-channel adapter routing proof"
      contains: "Chimeway.Adapters.Mailglass"
  key_links:
    - from: "test/chimeway/adapters/mailglass_adapter_test.exs"
      to: "test/support/chimeway/adapter/contract_test.ex"
      via: "use Chimeway.Adapter.ContractTest"
      pattern: "simulate_error?"
    - from: "test/chimeway/dispatch/executor_mailglass_adapter_test.exs"
      to: "lib/chimeway/dispatch/executor.ex"
      via: "run_delivery/1"
      pattern: "channel_adapters"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-54-05 | Contract test passes without real error shape | medium | `simulate_error?/0` true + explicit assertion on `{:error, class, map}` |
| T-54-06 | Sensitive adapter config leaked in provider_response | high | ContractTest `__contract_check_no_sensitive_keys!/1` on success meta |
</threat_model>

<objective>
Prove ECOS-02: shared adapter contract tests (success meta redaction, classified errors) and executor per-channel routing for the Mailglass adapter.

Purpose: Lock adapter quality before Phase 55 webhooks and Phase 56 demo wiring.

Output: Full contract test module, error-classification tests, executor integration test, minimal doc pointer in custom-adapter recipe.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-mailglass-adapter-core/54-CONTEXT.md
@.planning/phases/54-mailglass-adapter-core/54-RESEARCH.md
@.planning/phases/54-mailglass-adapter-core/54-VALIDATION.md
@lib/chimeway/adapters/mailglass.ex
@test/support/chimeway/adapter/contract_test.ex
@test/support/chimeway/mailglass_fixtures.ex
@test/chimeway/adapters/logger_adapter_test.exs
@test/chimeway/dispatch/executor_adapter_resolution_test.exs
@guides/recipes/custom-adapter.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContractTest suite + simulate_error (D-16, ECOS-02)</name>
  <files>test/chimeway/adapters/mailglass_adapter_test.exs</files>
  <read_first>
    - `test/chimeway/adapters/logger_adapter_test.exs` — ContractTest usage pattern
    - `test/support/chimeway/adapter/contract_test.ex` — macro tests
    - `test/support/chimeway/mailglass_fixtures.ex` — sample_delivery, mailables
    - `lib/chimeway/adapters/mailglass.ex` — `:simulate_error` config branch
    - `.planning/phases/54-mailglass-adapter-core/54-CONTEXT.md` — D-16
  </read_first>
  <action>
    Expand `test/chimeway/adapters/mailglass_adapter_test.exs`:

    - `use Mailglass.DataCase, async: false`
    - `use Chimeway.Adapter.ContractTest`
    - `def adapter_module, do: Chimeway.Adapters.Mailglass`
    - `def sample_delivery, do: Chimeway.TestSupport.MailglassFixtures.sample_delivery()`
    - `def simulate_error?, do: true`

    Setup block:
    - `Mailglass.Adapters.Fake.checkout()` + `Fake.set_shared(self())`
    - `Application.put_env(:chimeway, :channel_adapter_configs, %{"email" => [mailables: MailglassFixtures.mailables()]})`
    - on_exit restore env

    For contract error test with `simulate_error?` true, pass `[simulate_error: true]` as second arg to `deliver/2` in adapter — update adapter if macro calls `deliver(sample_delivery(), [])` only: support Application.put_env in setup `:simulate_mailglass_error, true` read in adapter `config` merge.

    Add test "happy path uses Mailglass.Fake":
    - `deliver(sample_delivery(), mailables: ..., outbound_opts: [])`
    - assert `Mailglass.Adapters.Fake.deliveries()` length increases
    - assert returned meta contains `adapter: "mailglass"`

    Add test "redaction: meta with secret key stripped" — deliver with config that would include secret if not redacted (adapter-internal test via force bad meta prevented; instead assert contract macro passes).

    Module tag `@moduletag :mailglass` for selective CI.
  </action>
  <verify>
    <automated>mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `simulate_error?/0` returns `true` in test module.
    - `mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors` exits 0.
    - Test output includes passing "Chimeway.Adapter contract" describe tests (success shape + error shape).
    - Fake deliveries list non-empty after happy path test.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Error classification matrix (D-14, D-15)</name>
  <files>test/chimeway/adapters/mailglass_adapter_test.exs</files>
  <read_first>
    - `lib/chimeway/adapters/mailglass.ex` — `classify_mailglass_error/1`
    - `lib/mailglass/error.ex` — struct modules
    - `.planning/phases/54-mailglass-adapter-core/54-CONTEXT.md` — D-14, D-15
  </read_first>
  <action>
    Add focused tests (no Mox — use adapter's classify via public test helper or deliver with injected errors):

    **Option A (preferred):** Export `def classify_error_for_test(err), do: classify_mailglass_error(err)` with `@doc false` only in test env — OR test through `deliver/2` branches:

    1. `:temporary` — `simulate_error: true` config → `{:error, :temporary, _}`
    2. `:bounced` — call internal classify with `%Mailglass.SuppressedError{type: :address, message: "x", context: %{}}` constructed via `Mailglass.SuppressedError` if struct allows, else `Mailglass.SuppressedError.new/2` API from mailglass source
    3. `:permanent` — classify `%Mailglass.TemplateError{}` sample

    Each test asserts `reason_class in [:temporary, :permanent, :bounced]` and `is_map(detail)`.

    Document mapping table in test module moduledoc referencing D-15.
  </action>
  <verify>
    <automated>mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - At least three tests name-explicit for temporary, permanent, bounced classification.
    - All tests in mailglass_adapter_test.exs pass.
    - No sensitive keys in error detail maps (`token`, `api_key`, etc.).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Executor routing + recipe doc (D-08, ECOS-01/02)</name>
  <files>test/chimeway/dispatch/executor_mailglass_adapter_test.exs, guides/recipes/custom-adapter.md</files>
  <read_first>
    - `test/chimeway/dispatch/executor_adapter_resolution_test.exs` — channel_adapters pattern
    - `lib/chimeway/dispatch/executor.ex` — `run_delivery/1`
    - `guides/recipes/custom-adapter.md` — contract test section
    - `.planning/phases/54-mailglass-adapter-core/54-CONTEXT.md` — D-08, D-09
  </read_first>
  <action>
    Create `test/chimeway/dispatch/executor_mailglass_adapter_test.exs` (skip when Mailglass not loaded):

    - Use Chimeway.DataCase (existing) + Mailglass.DataCase setup for Fake
    - Insert minimal notification + delivery row with `render_key` + `render_data` matching fixtures
    - `Application.put_env(:chimeway, :channel_adapters, %{"email" => Chimeway.Adapters.Mailglass})`
    - `Application.put_env(:chimeway, :channel_adapter_configs, %{"email" => [mailables: MailglassFixtures.mailables()]})`
    - Call `Chimeway.Dispatch.Executor.run_delivery(delivery)`
    - Assert `{:ok, %{attempt: attempt}}` with `attempt.outcome == :succeeded` and `attempt.adapter_module == "Chimeway.Adapters.Mailglass"`
    - Assert attempt.provider_response contains `"mailglass"` key

    Update `guides/recipes/custom-adapter.md` with short section "Built-in Mailglass adapter (optional)":
    - Register `config :chimeway, channel_adapters: %{"email" => Chimeway.Adapters.Mailglass}`
    - `:channel_adapter_configs` `%{"email" => [mailables: %{"render.key" => {MyMailer, :fn}}]}`
    - Point to `test/chimeway/adapters/mailglass_adapter_test.exs` for contract example
    - Note REQUIREMENTS name `Chimeway.Adapter.Mailglass` vs module `Chimeway.Adapters.Mailglass` (D-07)
  </action>
  <verify>
    <automated>mix test test/chimeway/dispatch/executor_mailglass_adapter_test.exs test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/chimeway/dispatch/executor_mailglass_adapter_test.exs` exists and passes.
    - `guides/recipes/custom-adapter.md` contains `Chimeway.Adapters.Mailglass`.
    - Executor test proves succeeded attempt with mailglass adapter_module string.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/chimeway/adapters/mailglass_adapter_test.exs test/chimeway/dispatch/executor_mailglass_adapter_test.exs --warnings-as-errors`
- `mix ci.lint` (format + credo) if time permits in execute phase
</verification>
