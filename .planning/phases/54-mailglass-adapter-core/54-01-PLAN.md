---
phase: 54-mailglass-adapter-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - mix.lock
  - config/test.exs
  - test/test_helper.exs
  - test/support/chimeway/mailglass_fixtures.ex
  - lib/chimeway/adapters/mailglass.ex
requirements: [ECOS-01]
must_haves:
  truths:
    - "Chimeway compiles when Mailglass is not installed (optional dep + conditional module)"
    - "Mailglass is listed as optional dependency in mix.exs"
    - "Test harness configures Mailglass.TestRepo + Fake adapter for adapter tests"
  artifacts:
    - path: "mix.exs"
      provides: "{:mailglass, optional: true} dependency"
      contains: "mailglass"
    - path: "lib/chimeway/adapters/mailglass.ex"
      provides: "Conditional compile shell for Chimeway.Adapters.Mailglass"
      contains: "Code.ensure_loaded?(Mailglass)"
    - path: "test/support/chimeway/mailglass_fixtures.ex"
      provides: "Test mailable + render_key map for contract tests"
      contains: "Mailglass.Mailable"
  key_links:
    - from: "lib/chimeway/adapters/mailglass.ex"
      to: "lib/chimeway/dispatch/oban.ex"
      via: "same optional-dep compile guard pattern"
      pattern: "Code.ensure_loaded?"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-54-01 | Optional dep accidentally becomes required transitively | medium | `optional: true` in mix.exs; adapter module behind `Code.ensure_loaded?/1` |
| T-54-02 | Test config points at real SMTP adapter | high | `config :mailglass, adapter: {Mailglass.Adapters.Fake, []}` in test env only |
</threat_model>

<objective>
Add optional Mailglass dependency packaging and test infrastructure so later plans can implement `deliver/2` against a real Mailglass stack (Fake adapter + TestRepo).

Purpose: ECOS-01 foundation — hosts opt in; Chimeway core stays compilable without Mailglass (D-05, D-06).

Output: mix.exs dep, conditional adapter module stub, Mailglass test fixtures, test config.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-mailglass-adapter-core/54-CONTEXT.md
@.planning/phases/54-mailglass-adapter-core/54-RESEARCH.md
@.planning/phases/54-mailglass-adapter-core/54-VALIDATION.md
@mix.exs
@lib/chimeway/dispatch/oban.ex
@lib/chimeway/adapters/logger.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optional mailglass dependency (D-05, D-06)</name>
  <files>mix.exs, mix.lock</files>
  <read_first>
    - `mix.exs` — existing `{:oban, optional: true}` entry
    - `lib/chimeway/dispatch/oban.ex` — conditional compilation pattern
    - `.planning/phases/54-mailglass-adapter-core/54-CONTEXT.md` — D-05, D-06, D-56 (version pin discretion)
    - `.planning/phases/54-mailglass-adapter-core/54-RESEARCH.md` — Pitfall 1 (Elixir 1.18)
  </read_first>
  <action>
    In `mix.exs` `deps/0`, add:

    `{:mailglass, "~> 1.3", optional: true}`

    Run `mix deps.get` and commit updated `mix.lock`.

    Do **not** add mailglass to `applications` list — optional dep stays off host apps unless they declare it.

    If compile fails on Elixir version in local environment, document in adapter moduledoc (next task) that Mailglass requires Elixir ~> 1.18; Chimeway CI should use 1.18+ for mailglass tests (per RESEARCH open question 3).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` contains `{:mailglass, "~> 1.3", optional: true}`.
    - `mix compile --warnings-as-errors` exits 0.
    - `mix.lock` includes `:mailglass` entry after deps.get.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Conditional adapter module stub (D-04, D-07)</name>
  <files>lib/chimeway/adapters/mailglass.ex</files>
  <read_first>
    - `lib/chimeway/dispatch/oban.ex` — full `if Code.ensure_loaded?` file pattern
    - `lib/chimeway/adapter.ex` — behaviour callbacks
    - `lib/chimeway/adapters/logger.ex` — module layout reference
    - `.planning/phases/54-mailglass-adapter-core/54-CONTEXT.md` — D-04, D-07, D-18 (no webhooks)
  </read_first>
  <action>
    Create `lib/chimeway/adapters/mailglass.ex` wrapped entirely in:

    `if Code.ensure_loaded?(Mailglass) do ... end`

    Inside, define `Chimeway.Adapters.Mailglass` with:
    - `@moduledoc` explaining product name `Chimeway.Adapter.Mailglass` (REQUIREMENTS) vs module `Chimeway.Adapters.Mailglass` (D-07)
    - `@behaviour Chimeway.Adapter`
    - `@compile {:no_warn_undefined, [Mailglass.Outbound, Mailglass.Message, Mailglass.Tenancy, Mailglass.Error]}`
    - `@impl deliver/2` that returns `{:error, :permanent, %{reason: :not_implemented}}` temporarily OR delegates to a private `not_implemented/0` — **54-02 replaces body**; stub must compile and export `deliver/2`

    Do **not** implement webhook callbacks.

    Module must not exist in BEAM when Mailglass is not loaded (file guard only).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/chimeway/adapters/mailglass.ex` exists and starts with `if Code.ensure_loaded?(Mailglass)`.
    - Module defines `@behaviour Chimeway.Adapter` and `def deliver/2`.
    - `mix compile --warnings-as-errors` exits 0.
    - `rg "verify_webhook" lib/chimeway/adapters/mailglass.ex` returns no matches.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Mailglass test fixtures and config (D-13)</name>
  <files>test/support/chimeway/mailglass_fixtures.ex, config/test.exs, test/test_helper.exs, test/chimeway/adapters/mailglass_adapter_test.exs</files>
  <read_first>
    - Mailglass package `config/test.exs` (in deps) — Fake adapter + TestRepo config reference
    - Mailglass package `test/support/data_case.ex` — DataCase usage
    - `test/support/chimeway/adapter/contract_test.ex` — contract macro
    - `.planning/phases/54-mailglass-adapter-core/54-CONTEXT.md` — D-13, D-16
  </read_first>
  <action>
    Only when `Code.ensure_loaded?(Mailglass)` in test, add Mailglass config to `config/test.exs`:

    - `config :mailglass, adapter: {Mailglass.Adapters.Fake, []}`
    - `config :mailglass, repo: Mailglass.TestRepo`
    - `config :mailglass, Mailglass.TestRepo` — copy connection keys from mailglass `config/test.exs` but database name `chimeway_mailglass_test#{System.get_env("MIX_TEST_PARTITION")}` to avoid colliding with mailglass's own DB

    In `test/test_helper.exs`, when Mailglass loaded: start Mailglass app if needed (`Application.ensure_all_started(:mailglass)`), ensure TestRepo is started.

    Create `test/support/chimeway/mailglass_fixtures.ex` (guard with `Code.ensure_loaded?(Mailglass)`):
    - `Chimeway.TestSupport.MailglassFixtures.TestMailer` using `Mailglass.Mailable` with one function `:test_email/1` building minimal message with `to` from assigns
    - `mailables/0` returning `%{"chimeway.test.email" => {TestMailer, :test_email}}`
    - `sample_delivery/0` building `%Chimeway.Delivery{}` with `render_key: "chimeway.test.email"`, `render_data: %{"to" => "test@example.com"}`, `tenant_id: "test-tenant"`, required ids

    Create minimal `test/chimeway/adapters/mailglass_adapter_test.exs`:
    - Skip entire module when Mailglass not loaded (`@moduletag :skip` or compile guard)
    - `use Mailglass.DataCase, async: false` when present
    - `use Chimeway.Adapter.ContractTest` with `adapter_module` returning `Chimeway.Adapters.Mailglass` only when compiled
    - Placeholder test `test "stub compiles"` — expanded in 54-03

    Run `mix test test/chimeway/adapters/mailglass_adapter_test.exs` — may skip contract assertions until 54-02/03; must not fail compile.
  </action>
  <verify>
    <automated>mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/support/chimeway/mailglass_fixtures.ex` defines `TestMailer` with `use Mailglass.Mailable`.
    - `config/test.exs` configures `Mailglass.Adapters.Fake` when mailglass is available.
    - `mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors` exits 0 (or skips gracefully when Mailglass absent).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix compile --warnings-as-errors` with mailglass in lockfile
- `rg "optional: true" mix.exs` includes mailglass
</verification>
