---
phase: 54-mailglass-adapter-core
plan: 02
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - lib/chimeway/adapters/mailglass.ex
requirements: [ECOS-01]
must_haves:
  truths:
    - "`Chimeway.Adapters.Mailglass.deliver/2` calls `Mailglass.Outbound.deliver/2` with a built `%Mailglass.Message{}`"
    - "Tenant context is stamped via `Mailglass.Tenancy.with_tenant/2` before outbound call (D-11)"
    - "Host `render_key` resolves to mailable module + function from config `:mailables` (D-02)"
    - "Success meta is redacted and includes mailglass delivery id / provider ids without secrets (D-17)"
    - "Mailglass errors map to `:temporary`, `:permanent`, or `:bounced` (D-14, D-15)"
  artifacts:
    - path: "lib/chimeway/adapters/mailglass.ex"
      provides: "Full deliver/2 + classify_error/1"
      contains: "Mailglass.Outbound.deliver"
      min_lines: 80
  key_links:
    - from: "lib/chimeway/adapters/mailglass.ex"
      to: "lib/chimeway/dispatch/executor.ex"
      via: "standard {:ok, meta} | {:error, class, detail} return contract"
      pattern: "classify"
    - from: "lib/chimeway/adapters/mailglass.ex"
      to: "lib/chimeway/dispatch/channel_adapter_config.ex"
      via: "Keyword.get(config, :mailables)"
      pattern: ":mailables"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-54-03 | API keys in adapter meta returned to DB | high | `redact_meta/1` strips `:password`, `:token`, `:secret`, `:api_key`, `:auth` before `{:ok, meta}` |
| T-54-04 | Cross-tenant send via wrong tenant_id | high | `Mailglass.Tenancy.with_tenant(delivery.tenant_id, ...)` — never nil; return `{:error, :permanent, %{reason: :missing_tenant_id}}` when blank |
</threat_model>

<objective>
Implement `Chimeway.Adapters.Mailglass.deliver/2`: build Mailglass message from durable delivery row, stamp tenancy, invoke outbound pipeline, map results to Chimeway adapter contract (D-01–D-03, D-08–D-12, D-14–D-15).

Purpose: ECOS-01 core — Mailglass-rendered outbound without bypassing Chimeway lifecycle.

Output: Complete adapter module (outbound only; no webhooks per D-18).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-mailglass-adapter-core/54-CONTEXT.md
@.planning/phases/54-mailglass-adapter-core/54-RESEARCH.md
@lib/chimeway/adapters/mailglass.ex
@lib/chimeway/delivery.ex
@lib/chimeway/dispatch/channel_adapter_config.ex
@lib/chimeway/dispatch/executor.ex
@deps/mailglass/lib/mailglass/outbound.ex
@deps/mailglass/lib/mailglass/error.ex
@deps/mailglass/lib/mailglass/message.ex
@deps/mailglass/lib/mailglass/tenancy.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Message build + mailable resolution (D-01, D-02, D-03)</name>
  <files>lib/chimeway/adapters/mailglass.ex</files>
  <read_first>
    - `lib/chimeway/adapters/mailglass.ex` — stub from 54-01
    - `lib/chimeway/delivery.ex` — `render_key`, `render_data`, `tenant_id`, `actor_id`
    - `lib/chimeway/dispatch/channel_adapter_config.ex` — config resolution
    - Mailglass dep `lib/mailglass/mailable.ex` — `new/1`, `put_function/2`, builder pattern
    - `.planning/phases/54-mailglass-adapter-core/54-CONTEXT.md` — D-01, D-02, D-03
    - `test/support/chimeway/mailglass_fixtures.ex` — test mailable shape
  </read_first>
  <action>
    Replace stub `deliver/2` implementation per D-01 (invoke `Mailglass.Outbound.deliver/2`, not pre-rendered HTML transport).

    **Config (call time only, D-10):** Read `mailables = Keyword.get(config, :mailables) || Application.get_env(:chimeway, :channel_adapter_configs, %{}) |> get_in(["email", :mailables])` — prefer keyword from `ChannelAdapterConfig.resolve/2` passed as `config` arg; do not use module attributes.

    **Resolution:** `delivery.render_key` → `Map.fetch!(mailables, render_key)` → `{module, function}`; on miss return `{:error, :permanent, %{reason: :unknown_render_key, render_key: delivery.render_key}}`.

    **Recipient email (discretion):** precedence `render_data["to"]` || `render_data["email"]` || parse `actor_id` / `delivery.actor_id` when matching `"user:email@host"` regex `~r/^user:(.+)$/` — on failure `{:error, :permanent, %{reason: :missing_recipient}}`.

    **Build message:**
    1. `assigns = Map.merge(%{"to" => recipient}, delivery.render_data || %{})`
    2. `msg = apply(module, function, [assigns])` when function arity 1, OR `module.new(assigns) |> Mailglass.Message.put_function(function)` if builders return Message — match `TestMailer` fixture convention
    3. Ensure `msg.tenant_id` set to `delivery.tenant_id`
    4. Set swoosh `to` if builder did not

    **Simulate error (D-16 prep):** When `Keyword.get(config, :simulate_error)` is truthy, return `{:error, :temporary, %{reason: :simulated}}` without calling Mailglass.

    Do not call notifier modules (D-03).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/chimeway/adapters/mailglass.ex` contains `Mailglass.Outbound.deliver`.
    - `lib/chimeway/adapters/mailglass.ex` contains `:mailables` config lookup.
    - `lib/chimeway/adapters/mailglass.ex` contains recipient parsing for `"user:"` prefix or `render_data["to"]`.
    - No `Notifier` module references in adapter file.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tenancy + outbound handoff (D-11, D-12)</name>
  <files>lib/chimeway/adapters/mailglass.ex</files>
  <read_first>
    - Mailglass dep `lib/mailglass/tenancy.ex` — `with_tenant/2`
    - Mailglass dep `lib/mailglass/outbound.ex` — `deliver/2` return `{:ok, %Delivery{}} | {:error, _}`
    - `.planning/phases/54-mailglass-adapter-core/54-CONTEXT.md` — D-11, D-12
  </read_first>
  <action>
    Before `Mailglass.Outbound.deliver(msg, outbound_opts)`:

    - Require non-empty `delivery.tenant_id` else `{:error, :permanent, %{reason: :missing_tenant_id}}`
    - Wrap call: `Mailglass.Tenancy.with_tenant(delivery.tenant_id, fn -> Mailglass.Outbound.deliver(msg, outbound_opts) end)`

    `outbound_opts` from `Keyword.get(config, :outbound_opts, [])` only — no compile-time secrets.

    On `{:ok, %Mailglass.Outbound.Delivery{} = mg_delivery}` success per D-12 dual lifecycle:
    - Build meta map: `%{adapter: "mailglass", mailglass_delivery_id: mg_delivery.id, provider_message_id: mg_delivery.provider_message_id, status: mg_delivery.status}`
    - Pipe through `redact_meta/1` (next task) → `{:ok, meta}`

    Do **not** write to Chimeway attempt row here — Executor handles that.
    Do **not** persist `provider_message_id` on Chimeway attempt unless zero-cost (D-19 defer).
  </action>
  <verify>
    <automated>mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/chimeway/adapters/mailglass.ex` contains `Mailglass.Tenancy.with_tenant`.
    - Happy-path test (add in same file if missing) asserts `{:ok, meta}` when Fake configured and mailables map includes test render_key.
    - `meta` includes `"adapter" => "mailglass"`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Error classification + meta redaction (D-14, D-15, D-17)</name>
  <files>lib/chimeway/adapters/mailglass.ex</files>
  <read_first>
    - Mailglass dep `lib/mailglass/error.ex` — `retryable?/1`, struct list
    - `test/support/chimeway/adapter/contract_test.ex` — sensitive keys list
    - `.planning/phases/54-mailglass-adapter-core/54-CONTEXT.md` — D-14, D-15, D-17
    - `.planning/phases/54-mailglass-adapter-core/54-RESEARCH.md` — classification table
  </read_first>
  <action>
    Add private `classify_mailglass_error/1`:

    - `%Mailglass.SuppressedError{}` → `{:error, :bounced, detail}`
    - `%Mailglass.RateLimitError{}` → `{:error, :temporary, detail}`
    - `%Mailglass.SendError{}` → `:temporary` if `Mailglass.Error.retryable?(err)`, else `:permanent`
    - `%Mailglass.TemplateError{}` → `{:error, :permanent, detail}`
    - `%Mailglass.ConfigError{}` → `{:error, :permanent, detail}`
    - `%Mailglass.TenancyError{}` → `{:error, :permanent, detail}`
    - catch-all Mailglass.Error → `{:error, :permanent, detail}`

    `detail` map: `%{type: Mailglass.Error.kind(err), module: Atom.to_string(err.__struct__)}` — no raw provider payloads.

    Add `redact_meta/1` removing keys `:password`, `:token`, `:secret`, `:api_key`, `:auth` (atoms and strings) recursively one level.

    Wire `deliver/2` to pipe Mailglass `{:error, err}` through classifier.
  </action>
  <verify>
    <automated>mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `lib/chimeway/adapters/mailglass.ex` defines `classify_mailglass_error` or `defp classify_mailglass_error`.
    - `lib/chimeway/adapters/mailglass.ex` defines `redact_meta`.
    - Unit test: `simulate_error` config returns `{:error, :temporary, _}`.
    - Unit test: suppressed error path returns `:bounced` when injectable via Mailglass test helper or simulated config branch.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors`
- `mix compile --warnings-as-errors`
</verification>
