---
phase: 02-first-outbound-delivery-slice
plan: "02-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/create_chimeway_deliveries.exs
  - priv/repo/migrations/create_chimeway_delivery_attempts.exs
  - lib/chimeway/delivery.ex
  - lib/chimeway/delivery_attempt.ex
  - lib/chimeway/deliveries.ex
  - lib/chimeway/dispatch.ex
  - lib/chimeway/dispatch/sync.ex
  - lib/chimeway/trigger.ex
  - test/chimeway/deliveries_test.exs
autonomous: true
requirements:
  - DLVR-01
  - DLVR-02
  - DLVR-03
user_setup: []
must_haves:
  truths:
    - "chimeway_deliveries table exists with all required columns including suppression_reason and delay_fallback."
    - "chimeway_delivery_attempts table exists with append-only semantics (no updated_at)."
    - "Chimeway.Deliveries.plan_delivery/2 is idempotent: duplicate calls on same (notification_id, channel) create exactly one row."
    - "Chimeway.Dispatch behaviour defines dispatch/2 callback; Chimeway.Dispatch.Sync satisfies it."
    - "Trigger pipeline calls the dispatcher after notification creation via config seam."
  artifacts:
    - path: lib/chimeway/delivery.ex
      provides: "Ecto schema for chimeway_deliveries with Ecto.Enum status and FK to notifications"
      exports:
        - "Chimeway.Delivery struct"
        - "changeset/2"
    - path: lib/chimeway/delivery_attempt.ex
      provides: "Ecto schema for chimeway_delivery_attempts with Ecto.Enum outcome (append-only)"
      exports:
        - "Chimeway.DeliveryAttempt struct"
        - "changeset/2"
    - path: lib/chimeway/deliveries.ex
      provides: "Context module for delivery planning, attempt recording, and status transitions"
      exports:
        - "plan_delivery/2"
        - "record_attempt/2"
        - "transition_status/2"
        - "get_delivery!/1"
    - path: lib/chimeway/dispatch.ex
      provides: "Behaviour contract for dispatcher seam (Phase 3 Oban swap point)"
      exports:
        - "@callback dispatch/2"
    - path: lib/chimeway/dispatch/sync.ex
      provides: "Sync dispatcher stub — plans delivery rows, returns {:ok, delivery} without adapter call"
      exports:
        - "dispatch/2"
    - path: test/chimeway/deliveries_test.exs
      provides: "Unit tests for idempotent planning, status transitions, and attempt recording"
      min_lines: 40
  key_links:
    - from: lib/chimeway/trigger.ex
      to: lib/chimeway/dispatch.ex
      via: "dispatcher resolved via Application.get_env(:chimeway, :dispatcher, Chimeway.Dispatch.Sync)"
      pattern: "Application\\.get_env.*dispatcher"
    - from: lib/chimeway/deliveries.ex
      to: lib/chimeway/delivery.ex
      via: "Deliveries context uses Delivery schema for planning and transitions"
      pattern: "Chimeway\\.Delivery"
---

<objective>
Add the delivery and attempt persistence layer that Phase 1 deliberately deferred: chimeway_deliveries and chimeway_delivery_attempts tables, the Chimeway.Deliveries context with idempotent planning and atomic attempt recording, and the Chimeway.Dispatch behaviour stub wired into the trigger pipeline.

Purpose: Establish the persistence foundation DLVR-01/02/03 depend on before any adapter is called. Plans 02-02 and 02-03 (and Phase 3 Oban) build on this without altering table shapes.
Output: Two migrations, three schema/context modules, the dispatch behaviour + stub, and unit tests confirming idempotency and atomic attempt recording.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-first-outbound-delivery-slice/02-CONTEXT.md
@.planning/phases/02-first-outbound-delivery-slice/02-RESEARCH.md
@.planning/phases/01-durable-core-spine/01-CONTEXT.md
@lib/chimeway/trigger.ex
@lib/chimeway/notifier.ex
@lib/chimeway/notifications/notification.ex
@priv/repo/migrations/20260424023200_create_chimeway_events.exs
@priv/repo/migrations/20260424023201_create_chimeway_notifications.exs
</context>

<tasks>

<task type="auto">
  <name>Task 02-01-01: Migrations for chimeway_deliveries and chimeway_delivery_attempts</name>
  <files>priv/repo/migrations/</files>
  <read_first>priv/repo/migrations/20260424023200_create_chimeway_events.exs, priv/repo/migrations/20260424023201_create_chimeway_notifications.exs, .planning/phases/02-first-outbound-delivery-slice/02-RESEARCH.md</read_first>
  <action>Create two migration files. Mirror the Phase 1 naming conventions exactly: `primary_key: false`, explicit `add :id, :uuid, primary_key: true, default: fragment("gen_random_uuid()")`, `timestamps(type: :utc_datetime_usec)`, named unique indexes.

Migration 1 — `create_chimeway_deliveries`:
- `id`: UUID PK
- `notification_id`: UUID FK → `chimeway_notifications(id)`, not null, `on_delete: :delete_all`
- `channel`: string, not null
- `status`: string, not null, default `"pending"` (Ecto.Enum maps to string; do NOT use Postgres ENUM type)
- `suppression_reason`: string, nullable (schema-ready; semantic activation is Phase 3)
- `delay_fallback`: boolean, not null, default false (schema-ready; semantic activation is Phase 3)
- `metadata`: map (jsonb), nullable
- `timestamps(type: :utc_datetime_usec)`
- `create unique_index(:chimeway_deliveries, [:notification_id, :channel], name: :chimeway_deliveries_notification_channel_index)`
- `create index(:chimeway_deliveries, [:notification_id])`

Migration 2 — `create_chimeway_delivery_attempts`:
- `id`: UUID PK
- `delivery_id`: UUID FK → `chimeway_deliveries(id)`, not null, `on_delete: :delete_all`
- `outcome`: string, not null (Ecto.Enum; do NOT use Postgres ENUM type)
- `provider_response`: map (jsonb), nullable
- `add :inserted_at, :utc_datetime_usec, null: false` — NO `updated_at` (attempts are immutable append-only records)
- `create index(:chimeway_delivery_attempts, [:delivery_id])`

Run `mix ecto.migrate` after creating both migrations. Run `mix ecto.rollback --step 2` to confirm both rollback cleanly, then re-run `mix ecto.migrate`.</action>
  <verify>mix ecto.migrate && mix ecto.rollback --step 2 && mix ecto.migrate && rg "chimeway_deliveries_notification_channel_index" priv/repo/migrations/ && rg "suppression_reason|delay_fallback" priv/repo/migrations/ && rg "inserted_at" priv/repo/migrations/ | rg "delivery_attempts"</verify>
  <acceptance_criteria>
    - Both migrations apply and roll back cleanly via mix ecto.migrate / mix ecto.rollback
    - Unique index named chimeway_deliveries_notification_channel_index exists on (notification_id, channel)
    - suppression_reason (nullable) and delay_fallback (boolean default false) are present in chimeway_deliveries
    - chimeway_delivery_attempts has inserted_at but NO updated_at (append-only)
    - FK constraint: inserting a delivery_attempts row with a non-existent delivery_id fails at the DB level
  </acceptance_criteria>
  <done>Both tables exist in the DB with correct columns, constraints, and indexes. Both migrations roll back cleanly.</done>
</task>

<task type="auto">
  <name>Task 02-01-02: Chimeway.Delivery and Chimeway.DeliveryAttempt schemas plus Chimeway.Deliveries context</name>
  <files>lib/chimeway/delivery.ex, lib/chimeway/delivery_attempt.ex, lib/chimeway/deliveries.ex, test/chimeway/deliveries_test.exs</files>
  <read_first>lib/chimeway/notifications/notification.ex, lib/chimeway/events/event.ex, .planning/phases/02-first-outbound-delivery-slice/02-RESEARCH.md, .planning/phases/02-first-outbound-delivery-slice/02-CONTEXT.md</read_first>
  <action>Create three modules.

`lib/chimeway/delivery.ex` — `Chimeway.Delivery`:
- `use Ecto.Schema` with `@primary_key false`
- `field :id, :binary_id, primary_key: true`
- `field :channel, :string`
- `field :status, Ecto.Enum, values: [:pending, :dispatched, :succeeded, :failed, :suppressed, :cancelled], default: :pending`
- `field :suppression_reason, :string`
- `field :delay_fallback, :boolean, default: false`
- `field :metadata, :map`
- `belongs_to :notification, Chimeway.Notifications.Notification, type: :binary_id`
- `has_many :attempts, Chimeway.DeliveryAttempt`
- `timestamps(type: :utc_datetime_usec)`
- `changeset/2` validating required: `[:notification_id, :channel, :status]`, validates inclusion via Ecto.Enum

`lib/chimeway/delivery_attempt.ex` — `Chimeway.DeliveryAttempt`:
- `use Ecto.Schema` with `@primary_key false`
- `field :id, :binary_id, primary_key: true`
- `field :outcome, Ecto.Enum, values: [:succeeded, :failed, :bounced, :rejected]`
- `field :provider_response, :map`
- `field :inserted_at, :utc_datetime_usec, autogenerate: {DateTime, :utc_now, []}`
- `belongs_to :delivery, Chimeway.Delivery, type: :binary_id`
- `changeset/2` validating required: `[:delivery_id, :outcome]`
- NOTE: No `updated_at` field — attempts are immutable.

`lib/chimeway/deliveries.ex` — `Chimeway.Deliveries`:
- `plan_delivery(notification_id, channel)` — builds Delivery changeset with status :pending and calls `Repo.insert(changeset, on_conflict: :nothing, conflict_target: [:notification_id, :channel])`. Returns `{:ok, %Delivery{}}`.
- `get_delivery!(id)` — `Repo.get!(Chimeway.Delivery, id)`
- `transition_status(%Delivery{} = delivery, new_status)` — guard table: `@allowed_transitions %{pending: [:dispatched, :suppressed, :cancelled], dispatched: [:succeeded, :failed, :suppressed], failed: [:dispatched]}`. If `new_status in allowed`, update via changeset and `Repo.update`. Else return `{:error, {:invalid_transition, from: delivery.status, to: new_status}}`.
- `record_attempt(delivery, attrs)` — uses `Ecto.Multi` to atomically: (1) insert a `Chimeway.DeliveryAttempt` row, (2) call `transition_status/2` to set the final delivery status. Returns `{:ok, %{delivery: updated_delivery, attempt: attempt}}` or `{:error, step, reason, changes}`.
- `@terminal_states [:succeeded, :suppressed, :cancelled]` module attribute (used by dispatcher in 02-02).

Write `test/chimeway/deliveries_test.exs` covering:
- `plan_delivery/2` creates a delivery row with status :pending
- `plan_delivery/2` called twice with same args creates exactly one row (idempotency test)
- `transition_status/2` accepts valid transitions and rejects invalid ones
- `record_attempt/2` atomically creates attempt row and updates delivery status
- Rollback: if the status update step fails, the attempt insert is also rolled back (use a bad status to force failure)</action>
  <verify>rg "Ecto.Enum" lib/chimeway/delivery.ex && rg "values:.*:pending.*:dispatched.*:succeeded.*:failed.*:suppressed.*:cancelled" lib/chimeway/delivery.ex && rg "values:.*:succeeded.*:failed.*:bounced.*:rejected" lib/chimeway/delivery_attempt.ex && rg "def plan_delivery|def record_attempt|def transition_status|def get_delivery!" lib/chimeway/deliveries.ex && rg "@allowed_transitions" lib/chimeway/deliveries.ex && mix test test/chimeway/deliveries_test.exs --seed 0</verify>
  <acceptance_criteria>
    - Chimeway.Delivery has all six status enum values (:pending, :dispatched, :succeeded, :failed, :suppressed, :cancelled)
    - Chimeway.DeliveryAttempt has all four outcome enum values (:succeeded, :failed, :bounced, :rejected) and no updated_at field
    - Chimeway.Deliveries.plan_delivery/2 called twice produces exactly one row in the DB
    - Chimeway.Deliveries.transition_status/2 rejects invalid transitions with {:error, {:invalid_transition, ...}}
    - Chimeway.Deliveries.record_attempt/2 is atomic: if status update fails, attempt row is also absent
    - All deliveries_test.exs tests pass
  </acceptance_criteria>
  <done>Delivery and attempt schemas are operational; idempotent planning and atomic attempt recording are confirmed by test.</done>
</task>

<task type="auto">
  <name>Task 02-01-03: Chimeway.Dispatch behaviour stub and trigger pipeline wiring</name>
  <files>lib/chimeway/dispatch.ex, lib/chimeway/dispatch/sync.ex, lib/chimeway/trigger.ex</files>
  <read_first>lib/chimeway/trigger.ex, lib/chimeway/deliveries.ex, lib/chimeway/notifier.ex, .planning/phases/02-first-outbound-delivery-slice/02-CONTEXT.md</read_first>
  <action>Create the dispatch behaviour and stub.

`lib/chimeway/dispatch.ex` — `Chimeway.Dispatch`:
- Define as an Elixir behaviour with one required callback:
  `@callback dispatch(notifications :: [Chimeway.Notifications.Notification.t()], opts :: keyword()) :: {:ok, [Chimeway.Delivery.t()]} | {:error, term()}`
- `@moduledoc` documents: the `:chimeway, :dispatcher` config key with default `Chimeway.Dispatch.Sync`; a note that `Chimeway.Dispatch.Oban` will be the Phase 3 alternative; and that dispatch is called after notification creation (outside the notification transaction).

`lib/chimeway/dispatch/sync.ex` — `Chimeway.Dispatch.Sync`:
- `@behaviour Chimeway.Dispatch`
- `dispatch(notifications, opts)` stub: for each notification in the list, call `Chimeway.Deliveries.plan_delivery(notification.id, :in_app)` to create delivery rows. Returns `{:ok, deliveries}` without calling any adapter. (Adapter call lands in 02-02.)
- Note in `@moduledoc` that this is a stub — the full dispatch loop (adapter call, attempt recording) is wired in Plan 02-02.

Wire trigger pipeline in `lib/chimeway/trigger.ex`:
- After the `Repo.transaction(multi)` resolves successfully and notifications are created, call the dispatcher:
  ```elixir
  dispatcher = Application.get_env(:chimeway, :dispatcher, Chimeway.Dispatch.Sync)
  dispatcher.dispatch(notifications, opts)
  ```
- The dispatch call is OUTSIDE the notification Ecto.Multi (per RESEARCH.md pitfall 7 — never hold a DB transaction open during external calls).
- The trigger return value must still reflect the original `{:ok, result}` / `{:duplicate, event}` / `{:error, reason}` shapes from Phase 1 — dispatch failure should be logged but not mask the trigger result in Phase 2 (Phase 3 will harden this).

Add `config :chimeway, dispatcher: Chimeway.Dispatch.Sync` to `config/config.exs` as the documented default.

Confirm existing Phase 1 tests still pass unchanged.</action>
  <verify>rg "@callback dispatch" lib/chimeway/dispatch.ex && rg "@behaviour Chimeway.Dispatch" lib/chimeway/dispatch/sync.ex && rg "Application.get_env.*dispatcher" lib/chimeway/trigger.ex && mix test --seed 0</verify>
  <acceptance_criteria>
    - Chimeway.Dispatch defines @callback dispatch/2 with documented typespec
    - Chimeway.Dispatch.Sync satisfies the behaviour (no compiler warnings for missing callbacks)
    - Trigger pipeline calls Application.get_env(:chimeway, :dispatcher, Chimeway.Dispatch.Sync).dispatch after notification creation
    - Overriding config :chimeway, :dispatcher in test config swaps the implementation without recompilation
    - All existing Phase 1 tests pass unchanged
    - mix test passes for the full suite
  </acceptance_criteria>
  <done>The dispatch seam is wired into the trigger pipeline; delivery rows are created on trigger; Phase 1 tests still pass.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-02-01-SENSITIVE-MIGRATION
    severity: high
    threat: "suppression_reason or provider_response fields persist sensitive data from notifier payload without sanitization."
    mitigation: "metadata and provider_response maps must be run through sanitize_metadata/1 (drop password/token/secret keys) before any Repo.insert. Inherit the sanitization pattern from Phase 1 trigger.ex."
    mitigation_check: "rg 'sanitize_metadata\\|sanitize_payload' lib/chimeway/deliveries.ex lib/chimeway/trigger.ex"
  - id: TM-02-01-POSTGRES-ENUM
    severity: medium
    threat: "Using a custom Postgres ENUM type for status/outcome instead of Ecto.Enum on varchar causes ALTER TYPE DDL pain for OSS consumers."
    mitigation: "Use Ecto.Enum with plain string (varchar) column in migration, not Postgres ENUM type."
    mitigation_check: "rg 'Ecto.Enum' lib/chimeway/delivery.ex lib/chimeway/delivery_attempt.ex && (rg 'create type\\|ENUM' priv/repo/migrations/ && exit 1 || exit 0)"
  - id: TM-02-01-IDEMPOTENCY-GAP
    severity: high
    threat: "Delivery planner creates duplicate rows without the unique constraint, breaking the idempotency chain from Phase 1."
    mitigation: "Enforce unique index on (notification_id, channel) at DB level; use on_conflict: :nothing in plan_delivery/2."
    mitigation_check: "rg 'on_conflict: :nothing' lib/chimeway/deliveries.ex && rg 'chimeway_deliveries_notification_channel_index' priv/repo/migrations/"
  - id: TM-02-01-TRANSACTION-LEAK
    severity: medium
    threat: "Dispatch called inside the notification Ecto.Multi holds a DB connection open during potential external calls in future plans."
    mitigation: "Dispatch call must be outside the notification transaction — after Repo.transaction/1 returns."
    mitigation_check: "rg -A5 'Repo.transaction' lib/chimeway/trigger.ex | rg 'dispatcher\\|dispatch'"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix ecto.migrate` — both tables exist
- [ ] `mix ecto.rollback --step 2 && mix ecto.migrate` — rollback/re-apply clean
- [ ] `mix compile --warnings-as-errors`
- [ ] `mix test test/chimeway/deliveries_test.exs --seed 0`
- [ ] `mix test --seed 0` — full suite including Phase 1 tests
- [ ] `rg "on_conflict: :nothing" lib/chimeway/deliveries.ex` — idempotent planning present
- [ ] `rg "Application.get_env.*dispatcher" lib/chimeway/trigger.ex` — dispatch seam wired
- [ ] Security gate: all high-severity mitigation checks pass
</verification>

<success_criteria>
- chimeway_deliveries and chimeway_delivery_attempts tables exist with correct schema, indexes, and FKs.
- suppression_reason and delay_fallback are present in chimeway_deliveries — no Phase 3 alter migration needed.
- Chimeway.Deliveries.plan_delivery/2 is idempotent under duplicate calls.
- Chimeway.Deliveries.record_attempt/2 is atomic (attempt row + status transition in one Ecto.Multi).
- Chimeway.Dispatch behaviour is defined and satisfied by Chimeway.Dispatch.Sync stub.
- Trigger pipeline creates delivery rows for each triggered notification.
- All Phase 1 tests pass unchanged. mix test is green.
- Security gate remains enabled (ASVS L1, block on high); all high-severity mitigation checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-first-outbound-delivery-slice/02-01-SUMMARY.md`.
</output>
