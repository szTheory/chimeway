---
phase: 60.1
slug: hex-release-pipeline
status: passed
score: 11/11
requirements:
  GATE-06: passed
verified_at: 2026-05-30
---

# Phase 60.1 Verification: Hex Release Pipeline (GATE-06)

**Goal:** Automated Hex release pipeline with ci-gate enforcement — Release Please owns version/changelog SSOT; Hex publish blocked until ci-gate green on release SHA.

**Status:** `passed` — all 11 structural must-haves verified against codebase; `mix ci.verify_gates` green (236 tests). Live GitHub Actions E2E (bootstrap Release PR → tag → Hex publish) deferred to human verification.

## Requirements Traceability

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| **GATE-06** | Automated Hex publish on release tag, gated on ci-gate green; Release Please owns version/changelog SSOT | **passed** (structural) | Release Please config/manifest at 1.0.0; `release.yml` `gate-ci-green` → `publish-hex`; `release-pr-automerge.yml` + recovery `publish-hex.yml`; 11 GATE-06 contract tests in `release_gate_contract_test.exs` |

## Must-Have Verification Table

| # | Must-have | Status | Evidence |
|---|-----------|--------|----------|
| 1 | 9-lane ci-gate in `ci.yml` | **passed** | `ci-gate` job `needs: [lint, test, verify_gates, verify_docs, verify_example, verify_journeys, verify_mailglass, verify_accrue, verify_inbox]`; `if: always()` + 9 env lane checks |
| 2 | `verify_gates` + `verify_docs` jobs | **passed** | Jobs run `mix ci.verify_gates` and `mix ci.docs` respectively (lines 40–84) |
| 3 | `docs.yml` retired | **passed** | `.github/workflows/docs.yml` absent; contract test `docs.yml retired` asserts same |
| 4 | Release Please config/manifest at 1.0.0 | **passed** | `release-please-config.json` (`release-type: elixir`); `.release-please-manifest.json` `{ ".": "1.0.0" }`; `mix.exs` `@version "1.0.0"` |
| 5 | `release.yml` gate-ci-green + pre-publish replay | **passed** | `gate-ci-green` polls `job.name === 'ci-gate'`; `publish-hex` `needs: [release-please, gate-ci-green]`; replay: `mix ci.verify_gates`, `mix ci.docs`, `mix hex.build` |
| 6 | `release-pr-automerge.yml` with ci-gate check | **passed** | `checks.listForRef` filter `run.name === 'ci-gate'`; title `^chore(main): release`; label `autorelease: pending`; post-merge `gh workflow run ci.yml` + `release.yml` |
| 7 | `publish-hex.yml` recovery ci-gate only (no `requiredPrefixes`) | **passed** | `gate-ci-green` poll copied from `release.yml`; no `requiredPrefixes`, `pre-existing Credo debt`, or `Lint job concluded` |
| 8 | `MAINTAINING.md` Release Please default path | **passed** | Release Please SSOT language; recovery via `publish-hex.yml`; `HEX_API_KEY` + optional `RELEASE_PLEASE_TOKEN`; septet "All seven must pass"; no `docs.yml` reference |
| 9 | `release_gate_contract_test.exs` GATE-06 tests | **passed** | `describe "release pipeline contract (GATE-06)"` — 11 tests covering lanes, manifest parity, workflows, MAINTAINING |
| 10 | `install_golden_contract` outside ci-gate | **passed** | Not in ci-gate `needs`; contract test asserts exclusion |
| 11 | `workflow_dispatch` on CI for release bootstrap | **passed** | `ci.yml` `on.workflow_dispatch` present; `bootstrap-release-pr-ci` dispatches CI on release branch |

**Score: 11/11**

## Plan 60.1-01 Must-Haves (Wave 1)

| Truth / Artifact | Status | Evidence |
|------------------|--------|----------|
| ci-gate fails if any of 9 lanes not success | **passed** | Env-var loop over LINT…VERIFY_INBOX |
| Release Please manifest SSOT at 1.0.0 | **passed** | Manifest + mix.exs parity |
| Hex publish waits for gate-ci-green | **passed** | `publish-hex` needs chain |
| install_golden_contract outside ci-gate | **passed** | Path-gated job unchanged |
| docs.yml retired; verify_docs in CI | **passed** | Folded into ci.yml |
| Artifact: `release-please-config.json` | **passed** | Elixir release-type, changelog sections |
| Artifact: `.release-please-manifest.json` | **passed** | `"." : "1.0.0"` |
| Artifact: hardened `release.yml` | **passed** | SHA-pinned actions; no bare `@vN` tags |
| Key link: gate-ci-green → ci-gate poll | **passed** | `job.name === 'ci-gate'` in github-script |

## Plan 60.1-02 Must-Haves (Wave 2)

| Truth / Artifact | Status | Evidence |
|------------------|--------|----------|
| Automerge only when ci-gate succeeds on release SHA | **passed** | `release-pr-automerge.yml` check-run filter |
| Recovery publish gates on ci-gate only | **passed** | No lattice lint bypass |
| MAINTAINING Release Please default + recovery exception | **passed** | Runbook rewritten |
| GATE-06 contract tests lock invariants | **passed** | 11 tests in release_gate_contract_test.exs |
| Artifact: `release-pr-automerge.yml` | **passed** | workflow_run on `[CI]` + dispatch |
| Artifact: `publish-hex.yml` | **passed** | workflow_dispatch inputs tag + release_version |
| Key link: automerge → ci-gate | **passed** | `run.name === 'ci-gate'` |
| Key link: recovery → release.yml gate script | **passed** | Same ci-gate poll pattern |

## Automated Gates

| Gate | Result |
|------|--------|
| `mix ci.verify_gates` | **PASS** (236 tests, 0 failures, seed 139997) |
| Structural: ci-gate 9-lane needs | **PASS** |
| Structural: docs.yml absent | **PASS** |
| Structural: publish-hex.yml lacks `requiredPrefixes` | **PASS** |
| Structural: release.yml unpinned `@vN` absent | **PASS** |
| Structural: manifest 1.0.0 == mix.exs @version | **PASS** |

## Human Verification

| Item | Required? | Notes |
|------|-----------|-------|
| Push Wave 1+2 commits to `origin/main` | **Yes** | Branch ahead of origin; Release Please requires pushed commits |
| Bootstrap Release PR `chore(main): release 1.1.0` ci-gate green | **Yes** | Confirm CI workflow on `release-please--branches--main` head SHA |
| First Release PR merge (manual or automerge) | **Yes** | Validates `release.yml` tag creation + `gate-ci-green` + `publish-hex` |
| Hex 1.1.0 visible on hex.pm after publish | **Yes** | Proves end-to-end GATE-06 operational closure |
| `release-pr-automerge.yml` live merge on subsequent releases | Recommended | Structural guards verified in YAML; runtime merge not exercised here |
| `publish-hex.yml` recovery dispatch | Optional | Exception path; only needed if primary automation fails |
| `.planning/REQUIREMENTS.md` GATE-06 checkbox + traceability table | Planning follow-up | Functional closure verified here; REQUIREMENTS.md still shows GATE-06 unchecked / Pending |

## Gaps Found

None in structural/code must-haves. Operational E2E (first automated Hex publish) pending human verification after push to origin.

## Notes

- Phase 60.1 Wave 1 (`60.1-01`) and Wave 2 (`60.1-02`) summaries report zero plan deviations (Wave 2: one auto-fixed `extract_pre_ship_block` helper for multi-fence MAINTAINING layout).
- Pre-publish replay uses `mix ci.verify_gates` + `mix ci.docs` (not full test suite) per D-60.1-12 — ci-gate already ran full lanes on release SHA.
- First automated release after Hex 1.0.0 targets **1.1.0** per manifest/bootstrap design.
