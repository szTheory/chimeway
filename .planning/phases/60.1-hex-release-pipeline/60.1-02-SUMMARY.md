---
phase: 60.1-hex-release-pipeline
plan: 02
subsystem: infra
tags: [release-please, ci-gate, hex, github-actions, automerge, contract-tests]

requires:
  - phase: 60.1-hex-release-pipeline
    plan: 01
    provides: ci-gate, release.yml gate-ci-green, verify_gates/docs jobs, Release Please SSOT
provides:
  - release-pr-automerge.yml — automerge after ci-gate green on release branch
  - publish-hex.yml — recovery publish with ci-gate-only gate (no lint bypass)
  - MAINTAINING.md automated Release Please runbook + recovery exception path
  - GATE-06 contract tests in release_gate_contract_test.exs (11 new assertions)
affects: [GATE-06, Phase 61, Phase 62]

tech-stack:
  added: [release-pr-automerge workflow, publish-hex recovery workflow]
  patterns:
    - "Automerge requires title ^chore(main): release + autorelease: pending label"
    - "Recovery publish-hex gates on ci-gate poll only — rejects lattice requiredPrefixes/lint bypass"
    - "extract_pre_ship_block selects septet bash block when multiple fenced blocks exist"

key-files:
  created:
    - .github/workflows/release-pr-automerge.yml
    - .github/workflows/publish-hex.yml
  modified:
    - MAINTAINING.md
    - test/chimeway/release_gate_contract_test.exs

key-decisions:
  - "Automerge SHA-pins github-script@ed59741… matching release.yml Wave 1"
  - "Recovery gate maxAttempts=10 (maintainer assumes CI already ran on target SHA)"
  - "GATE-05 extract_pre_ship_block updated for multi-block MAINTAINING layout"

patterns-established:
  - "Post-merge automerge dispatches ci.yml on main + release.yml (GITHUB_TOKEN merge has no push event)"
  - "Contract tests lock full GATE-06 invariant surface: 8-lane ci-gate, manifest parity, workflow absence checks"

requirements-completed: [GATE-06]

duration: 20min
completed: 2026-05-30
---

# Phase 60.1 Plan 02: Hex Release Pipeline Wave 2 Summary

**Release loop closed — automerge after ci-gate, ci-gate-only recovery publish, Release Please runbook, and GATE-06 contract lock**

## Performance

- **Duration:** 20 min
- **Started:** 2026-05-30T12:20:00Z
- **Completed:** 2026-05-30T12:40:00Z
- **Tasks:** 4
- **Files modified:** 4

## Accomplishments

- Created `release-pr-automerge.yml` — merges Release PR when latest ci-gate check-run succeeds; title/label/SHA guards; post-merge CI + release dispatch
- Created `publish-hex.yml` recovery workflow — ci-gate poll copied from `release.yml` only; no `requiredPrefixes` or lint-failure allowance per D-60.1-10
- Rewrote `MAINTAINING.md` — Release Please as default path; manual `mix hex.publish` only via recovery dispatch; secrets and septet preserved
- Extended `release_gate_contract_test.exs` with 11 GATE-06 tests (236 total verify_gates tests green)

## Task Commits

Each task was committed atomically:

1. **Task 1: release-pr-automerge.yml** - `2cb69ec` (feat)
2. **Task 2: publish-hex.yml recovery** - `cd1d50c` (feat)
3. **Task 3: MAINTAINING.md runbook** - `bfc03fc` (feat)
4. **Task 4: GATE-06 contract tests** - `8746641` (feat)

**Plan metadata:** pending (docs commit follows)

## Files Created/Modified

- `.github/workflows/release-pr-automerge.yml` - Automerge after ci-gate green on release branch head SHA
- `.github/workflows/publish-hex.yml` - Recovery Hex publish with ci-gate-only gate and pre-publish replay
- `MAINTAINING.md` - Automated Release Please default + recovery exception + secrets docs
- `test/chimeway/release_gate_contract_test.exs` - GATE-06 describe with 11 invariant tests

## Decisions Made

- Followed D-60.1-09..12: automerge guards, recovery ci-gate only, MAINTAINING PAT docs, recovery pre-publish replay
- Fixed `extract_pre_ship_block/1` to select septet block when MAINTAINING has multiple bash fences (post-publish trio + pre-ship septet)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Correctness] extract_pre_ship_block multi-fence selection**
- **Found during:** Task 4 (GATE-06 contract tests)
- **Issue:** MAINTAINING rewrite added post-publish verify trio bash block before pre-ship septet; GATE-05 tests matched first fence only
- **Fix:** Updated helper to scan all bash blocks and select the one starting with `mix ci`
- **Files modified:** `test/chimeway/release_gate_contract_test.exs`
- **Verification:** `mix ci.verify_gates` — 236 tests, 0 failures
- **Committed in:** `8746641` (Task 4 commit)

---

**Total deviations:** 1 auto-fixed (correctness for GATE-05 regression)
**Impact on plan:** Minimal — helper change required by MAINTAINING structure; no scope creep.

## Issues Encountered

None beyond the multi-fence MAINTAINING layout caught by contract tests during Task 4.

## User Setup Required

None - `HEX_API_KEY` already required from Wave 1. Operator: push to origin → confirm bootstrap 1.1.0 Release PR ci-gate green → manual merge first release OR let automerge handle subsequent releases.

## Verification Results

| Check | Result |
|-------|--------|
| `mix ci.verify_gates` | PASS (236 tests) |
| `release-pr-automerge.yml` exists with ci-gate + release dispatch | PASS |
| `publish-hex.yml` lacks `requiredPrefixes` | PASS |
| MAINTAINING Release Please + publish-hex recovery | PASS |
| MAINTAINING no `docs.yml` reference | PASS |
| GATE-06 describe ≥ 10 tests | PASS (11 tests) |

## Self-Check: PASSED

## Next Phase Readiness

- Phase 60.1 complete — GATE-06 satisfied; hands-off releases enabled after bootstrap 1.1.0
- Operator: push Wave 2 to origin → merge or automerge bootstrap Release PR → confirm Hex 1.1.0 publish
- Phase 61 (Inbox Headless + Package) unblocked

---
*Phase: 60.1-hex-release-pipeline*
*Completed: 2026-05-30*
