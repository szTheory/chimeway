---
phase: 60.1-hex-release-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .github/workflows/release-pr-automerge.yml
  - .github/workflows/publish-hex.yml
  - MAINTAINING.md
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
requirements: [GATE-06]
must_haves:
  truths:
    - "Release PR auto-merges only when latest ci-gate check-run succeeds on release branch head SHA"
    - "publish-hex recovery gates on ci-gate poll only — no per-job prefix checks, no lint-failure allowance"
    - "MAINTAINING documents Release Please as default path; manual mix hex.publish only via publish-hex.yml dispatch"
    - "release_gate_contract_test.exs locks GATE-06 invariants: 8-lane ci-gate, verify_gates/docs jobs, manifest parity, release.yml gate + pre-publish replay, docs.yml absent"
  artifacts:
    - path: ".github/workflows/release-pr-automerge.yml"
      provides: "Automerge after ci-gate green"
    - path: ".github/workflows/publish-hex.yml"
      provides: "Recovery publish with ci-gate-only gate"
    - path: "MAINTAINING.md"
      provides: "Automated release runbook"
    - path: "test/chimeway/release_gate_contract_test.exs"
      provides: "GATE-06 contract tests"
  key_links:
    - from: "release-pr-automerge.yml"
      to: "ci-gate"
      via: "checks.listForRef filter name === ci-gate"
      pattern: "ci-gate"
    - from: "publish-hex.yml"
      to: "release.yml"
      via: "gate-ci-green poll script copied from release.yml (ci-gate only)"
      pattern: "ci-gate"
---

<objective>
Wave 2: Complete the release loop — automerge after ci-gate proven, recovery workflow without lattice lint bypass, MAINTAINING runbook rewrite, and GATE-06 contract test extensions.

Purpose: Close GATE-06 operational + test coverage; enable hands-off releases after bootstrap v1.1.0 manual merge.

Output: release-pr-automerge.yml, publish-hex.yml, updated MAINTAINING.md, extended release_gate_contract_test.exs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60.1-hex-release-pipeline/60.1-CONTEXT.md
@.planning/phases/60.1-hex-release-pipeline/60.1-RESEARCH.md
@~/projects/lattice_stripe/.github/workflows/release-pr-automerge.yml
@~/projects/lattice_stripe/.github/workflows/publish-hex.yml
@.github/workflows/release.yml
@.github/workflows/ci.yml
@MAINTAINING.md
@test/chimeway/release_gate_contract_test.exs
@mix.exs
</context>

<threat_model>
| ID | STRIDE | Threat | Severity | Mitigation |
|----|--------|--------|----------|------------|
| T-60.1-02 | Tampering | Recovery publish-hex.yml allows Hex upload when lint/credo failed | critical | Copy `gate-ci-green` ci-gate poll from `release.yml` only; **reject** lattice `requiredPrefixes`, `runPassesRequiredJobs`, and lint-failure warning block (D-60.1-10) |
| T-60.1-06 | Spoofing | Automerge merges non-release PR | high | Require title `^chore\(main\):\ release\ ` and label `autorelease: pending`; verify head SHA match |
| T-60.1-07 | Repudiation | MAINTAINING still instructs manual version bump + mix hex.publish as default | medium | Rewrite runbook: Release Please default; recovery dispatch documented as exception |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: release-pr-automerge.yml (D-60.1-09 automerge)</name>
  <files>.github/workflows/release-pr-automerge.yml</files>
  <read_first>
    - `~/projects/lattice_stripe/.github/workflows/release-pr-automerge.yml` — canonical automerge
    - `.github/workflows/ci.yml` — workflow name `CI`, ci-gate job name
    - `.github/workflows/release.yml` — post-merge dispatch target
    - `.planning/phases/60.1-hex-release-pipeline/60.1-CONTEXT.md` — D-60.1-09
  </read_first>
  <action>
    Create `.github/workflows/release-pr-automerge.yml` — port lattice_stripe file with Chimeway-specific references only:

    **Triggers:**
    - `workflow_run`: workflows `[CI]`, types `[completed]`
    - `workflow_dispatch` with optional input `head_sha`

    **Permissions:** `contents: write`, `pull-requests: write`, `actions: write`

    **Job `automerge`** — `if:` lattice pattern (dispatch OR successful CI run on `release-please--*` branch)

    **Steps (port verbatim logic):**
    1. Resolve target SHA + branch (`release-please--branches--main`)
    2. `actions/github-script@v8` (SHA-pinned) — `checks.listForRef`, filter `run.name === 'ci-gate'`, require latest `conclusion === 'success'`
    3. Find open PR by head branch; verify title matches `^chore\(main\):\ release\ `; require label `autorelease: pending`; head SHA must match target
    4. Merge loop with `gh pr merge --squash` (+ `--admin` fallback); re-check ci-gate before each attempt
    5. Post-merge bootstrap: `gh workflow run ci.yml --ref main`
    6. Trigger release: `gh workflow run release.yml`

    SHA-pin `actions/github-script` (same SHA as release.yml Wave 1).

    Do **not** change ci-gate job name — automerge depends on exact string `ci-gate`.
  </action>
  <acceptance_criteria>
    - `test -f .github/workflows/release-pr-automerge.yml`
    - File contains `workflows: [CI]`
    - File contains `run.name === 'ci-gate'` (or equivalent filter on name `ci-gate`)
    - File contains `release-please--branches--main`
    - File contains `gh workflow run ci.yml --ref main`
    - File contains `gh workflow run release.yml`
    - File contains `autorelease: pending`
    - No `@v[0-9]` unpinned action tags
  </acceptance_criteria>
  <automated>test -f .github/workflows/release-pr-automerge.yml && grep -q "ci-gate" .github/workflows/release-pr-automerge.yml && grep -q 'release-please--branches--main' .github/workflows/release-pr-automerge.yml && grep -q 'gh workflow run release.yml' .github/workflows/release-pr-automerge.yml</automated>
</task>

<task type="auto">
  <name>Task 2: publish-hex.yml recovery — ci-gate only (D-60.1-10, D-60.1-12)</name>
  <files>.github/workflows/publish-hex.yml</files>
  <read_first>
    - `~/projects/lattice_stripe/.github/workflows/publish-hex.yml` — shell to adapt (DO NOT copy gate script)
    - `.github/workflows/release.yml` — `gate-ci-green` job poll script (copy this for gate)
    - `.github/workflows/ci.yml` — action SHA pins
    - `.planning/phases/60.1-hex-release-pipeline/60.1-CONTEXT.md` — D-60.1-10, D-60.1-12
  </read_first>
  <action>
    Create `.github/workflows/publish-hex.yml` — `name: Publish Hex Recovery`

    **Trigger:** `workflow_dispatch` inputs:
    - `tag` (required) — git tag or 40-char SHA
    - `release_version` (required) — expected `@version` string
    - `dry_run` (boolean, default false)

    **Permissions:** `contents: read`
    **Concurrency:** `publish-hex-${{ github.event.inputs.tag }}`, `cancel-in-progress: false`

    **Job `gate-ci-green`** (NOT lattice per-job gate):
    - Checkout with `fetch-depth: 0`
    - Validate ref resolves to SHA (port lattice validate step)
    - **Gate script:** inline copy of `gate-ci-green` poll from `.github/workflows/release.yml` — poll `workflow_id: 'ci.yml'` for job name `ci-gate` success on SHA
    - Shorter timeout OK: `maxAttempts: 10` (maintainer assumes CI already ran)
    - **Explicitly omit:** `requiredPrefixes`, `runPassesRequiredJobs`, lint-failure warning (lattice lines 75–115)

    **Job `publish`** — `needs: gate-ci-green`
    - Checkout ref from gate outputs
    - setup-beam elixir `1.17`, otp `27`; cache deps
    - Pre-publish replay (match release.yml D-60.1-12):
      - `grep -n "@version \"${RELEASE_VERSION}\"" mix.exs`
      - `mix deps.get`
      - `mix compile --warnings-as-errors`
      - `mix ci.verify_gates`
      - `mix ci.docs`
      - `mix hex.build`
    - Idempotency: `mix hex.info chimeway "${RELEASE_VERSION}"`
    - Dry-run / publish / Hex API poll for package `chimeway`

    SHA-pin actions (checkout/setup-beam/cache/github-script from ci.yml + release.yml).

    Add file header comment: recovery only — default path is Release Please on merge.
  </action>
  <acceptance_criteria>
    - `test -f .github/workflows/publish-hex.yml`
    - File contains job id `gate-ci-green`
    - File contains `job.name === 'ci-gate'` poll logic
    - File does **not** contain `requiredPrefixes`
    - File does **not** contain `pre-existing Credo debt`
    - File does **not** contain `Lint job concluded`
    - File contains `mix ci.verify_gates` and `mix ci.docs`
    - File contains `mix hex.info chimeway`
    - File contains `workflow_dispatch` inputs `tag` and `release_version`
  </acceptance_criteria>
  <automated>test -f .github/workflows/publish-hex.yml && grep -q "ci-gate" .github/workflows/publish-hex.yml && ! grep -q requiredPrefixes .github/workflows/publish-hex.yml && grep -q 'mix ci.verify_gates' .github/workflows/publish-hex.yml && grep -q 'mix ci.docs' .github/workflows/publish-hex.yml</automated>
</task>

<task type="auto">
  <name>Task 3: MAINTAINING.md automated release runbook (D-60.1-11)</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - `MAINTAINING.md` — current manual 8-step runbook
    - `.planning/phases/60.1-hex-release-pipeline/60.1-CONTEXT.md` — D-60.1-09, D-60.1-11
    - `.github/workflows/release.yml` — happy path
    - `.github/workflows/publish-hex.yml` — recovery path (Task 2)
  </read_first>
  <action>
    Rewrite `MAINTAINING.md` **Release Runbook** section:

    **Default path (automated):**
    1. Merge conventional commits to `main` — Release Please opens/updates Release PR (`chore(main): release X.Y.Z`)
    2. Confirm ci-gate green on Release PR head SHA (Actions → CI workflow)
    3. After Wave 2: automerge handles merge; before first automerge test, manual merge OK
    4. On merge: `release.yml` creates tag + GitHub Release → `gate-ci-green` → `publish-hex`
    5. Run post-publish verify trio locally: `mix verify.clean`, `mix verify.parity`, `mix verify.published X.Y.Z`

    **Remove as default:** manual `@version` edit, manual CHANGELOG section move, manual `git tag`, manual `mix hex.publish` on maintainer laptop.

    **Recovery path (exception):** GitHub Actions → `Publish Hex Recovery` → dispatch with `tag` + `release_version`; only when automation failed after ci-gate was green on that SHA.

    **Secrets:** Document `HEX_API_KEY` required; optional fine-grained `RELEASE_PLEASE_TOKEN` if Release PR CI flaky (fallback `GITHUB_TOKEN` documented in release.yml).

    **Pre-ship local commands (unchanged seven):** Keep existing bash block:
    ```bash
    mix ci
    mix ci.docs
    mix ci.verify_gates
    mix verify.example
    mix verify.journeys
    mix verify.mailglass
    mix verify.accrue
    ```
    Clarify: seven local commands map to ci-gate lanes + publish replay — not seven identical CI job names.

    **Installer golden:** Keep path-gated `install_golden_contract` subsection unchanged.

    **SHA refresh:** Update "Refreshing GitHub Actions SHAs" to reference `ci.yml` and `release.yml` (remove `docs.yml` mention).

    **Bootstrap note:** First automated release after Hex 1.0.0 targets **1.1.0**; push all unpushed main commits before first Release Please run.
  </action>
  <acceptance_criteria>
    - `MAINTAINING.md` contains `Release Please` (automated default language)
    - `MAINTAINING.md` contains `Publish Hex Recovery` or `publish-hex.yml`
    - `MAINTAINING.md` does **not** instruct `mix hex.publish` as primary/default publish step (recovery-only context OK)
    - `MAINTAINING.md` still lists all seven pre-ship mix commands
    - `MAINTAINING.md` contains `HEX_API_KEY`
    - `MAINTAINING.md` mentions optional `RELEASE_PLEASE_TOKEN`
    - `MAINTAINING.md` SHA refresh section does not reference `docs.yml`
    - `grep -qi "All seven must pass" MAINTAINING.md`
  </acceptance_criteria>
  <automated>grep -q 'Release Please' MAINTAINING.md && grep -q 'publish-hex' MAINTAINING.md && grep -q 'HEX_API_KEY' MAINTAINING.md && ! grep -q 'docs.yml' MAINTAINING.md</automated>
</task>

<task type="auto">
  <name>Task 4: GATE-06 contract tests in release_gate_contract_test.exs</name>
  <files>test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    - `test/chimeway/release_gate_contract_test.exs` — existing GATE-05 describe + helpers
    - `.planning/phases/60.1-hex-release-pipeline/60.1-RESEARCH.md` — Contract Test Strategy table
    - `.github/workflows/ci.yml` — ci-gate needs list
    - `.github/workflows/release.yml` — gate-ci-green + pre-publish steps
    - `.release-please-manifest.json`
  </read_first>
  <action>
    Add module attributes:
    ```elixir
    @release_yml ".github/workflows/release.yml"
    @manifest ".release-please-manifest.json"
    @ci_gate_lanes ~w(lint test verify_gates verify_docs verify_example verify_journeys verify_mailglass verify_accrue)
    ```

    Add helper `extract_ci_gate_needs/1` — parse `ci-gate:` block `needs:` array from ci.yml (regex or line scan).

    Add describe `"release pipeline contract (GATE-06)"` with tests:

    1. `verify_gates job runs mix ci.verify_gates` — extract_ci_job_block("verify_gates")
    2. `verify_docs job runs mix ci.docs` — extract_ci_job_block("verify_docs")
    3. `ci-gate aggregates 8 required lanes` — assert `@ci_gate_lanes` subset of parsed needs
    4. `install_golden_contract outside ci-gate needs` — ci-gate needs must not include `install_golden_contract`
    5. `docs.yml retired` — `refute File.exists?(".github/workflows/docs.yml")`
    6. `manifest version matches mix.exs @version` — read manifest JSON `"."` vs regex `@version "X.Y.Z"` from mix.exs
    7. `release.yml contains gate-ci-green and ci-gate poll` — assert `@release_yml` contains `gate-ci-green` and `ci-gate`
    8. `release.yml pre-publish replay includes verify_gates and docs` — assert contains `mix ci.verify_gates` and `mix ci.docs`
    9. `publish-hex needs gate-ci-green` — assert release.yml `publish-hex` block has `needs:` including `gate-ci-green`
    10. `MAINTAINING documents Release Please automated path` — assert maintaining contains `Release Please`
    11. `publish-hex recovery exists and gates on ci-gate only` — read `.github/workflows/publish-hex.yml`; assert contains `ci-gate`; refute contains `requiredPrefixes`

    Reuse existing `extract_ci_job_block/2` pattern. Do not break existing GATE-05 tests.
  </action>
  <acceptance_criteria>
    - `mix ci.verify_gates` exits 0
    - New describe contains at least 10 tests
    - Test failure if `docs.yml` reappears
    - Test failure if ci-gate needs drops any of 8 lane IDs
    - Test failure if release.yml omits `mix ci.docs` in publish path
    - Existing GATE-05 tests still pass (no regression)
  </acceptance_criteria>
  <automated>mix ci.verify_gates</automated>
</task>

</tasks>

<verification>
- [ ] `mix ci.verify_gates` green (includes new GATE-06 tests)
- [ ] `grep -L requiredPrefixes .github/workflows/publish-hex.yml` — file exists and lacks lattice bypass
- [ ] `test -f .github/workflows/release-pr-automerge.yml`
- [ ] MAINTAINING automated + recovery paths documented
- [ ] Wave 2 enables automerge for releases after 1.1.0 bootstrap
</verification>

<success_criteria>
1. Release PR auto-merges when ci-gate green (after CI workflow_run on release branch)
2. Recovery publish blocked unless ci-gate succeeded on target SHA — no lint bypass
3. MAINTAINING reflects Release Please default; seven local pre-ship commands preserved
4. Contract tests lock GATE-06 invariants; `mix ci.verify_gates` is release gate SSOT
</success_criteria>

<decisions_covered>
D-60.1-09 (automerge + post-merge dispatch), D-60.1-10 (recovery ci-gate only), D-60.1-11 (MAINTAINING PAT docs), D-60.1-12 (recovery pre-publish replay)
</decisions_covered>
