---
phase: 60.1-hex-release-pipeline
plan: 01
subsystem: infra
tags: [release-please, ci-gate, hex, github-actions, elixir]

requires:
  - phase: 60-accrue-docs-release-gate
    provides: mix ci.verify_gates, release_gate_contract_test.exs, pre-ship septet
provides:
  - Release Please config/manifest SSOT at 1.0.0
  - 8-lane ci-gate aggregation in ci.yml
  - verify_gates and verify_docs CI jobs (docs.yml retired)
  - Hardened release.yml with gate-ci-green, bootstrap-release-pr-ci, pre-publish replay
affects: [60.1-02, GATE-06, MAINTAINING.md]

tech-stack:
  added: [release-please-action@v5 SHA-pinned, github-script@v8 SHA-pinned]
  patterns:
    - "ci-gate stable job name for publish/automerge polling"
    - "Publish blocked until gate-ci-green succeeds on release SHA"
    - "Pre-publish replay: mix ci.verify_gates + mix ci.docs + mix hex.build"

key-files:
  created:
    - release-please-config.json
    - .release-please-manifest.json
  modified:
    - .github/workflows/ci.yml
    - .github/workflows/release.yml

key-decisions:
  - "Manifest seeds at 1.0.0 matching mix.exs; first automated bump targets 1.1.0"
  - "install_golden_contract stays outside ci-gate needs (path-gated lane)"
  - "Wave 1 manual merge of bootstrap Release PR; automerge deferred to 60.1-02"

patterns-established:
  - "ci-gate aggregates 8 lanes with if: always() + env var success check"
  - "gate-ci-green polls job name ci-gate on release SHA with tag-dispatch fallback at attempt 3"

requirements-completed: [GATE-06]

duration: 15min
completed: 2026-05-30
---

# Phase 60.1 Plan 01: Hex Release Pipeline Wave 1 Summary

**8-lane ci-gate, Release Please SSOT at 1.0.0, and hardened release.yml blocking Hex publish until gate-ci-green succeeds — ported from lattice_stripe**

## Performance

- **Duration:** 15 min
- **Started:** 2026-05-30T12:00:00Z
- **Completed:** 2026-05-30T12:15:00Z
- **Tasks:** 4
- **Files modified:** 5

## Accomplishments

- Created `release-please-config.json` and `.release-please-manifest.json` seeding version 1.0.0 SSOT
- Added `verify_gates` and `verify_docs` CI jobs; retired standalone `docs.yml`
- Added `ci-gate` aggregation over 8 required lanes plus `workflow_dispatch` for release branch bootstrap
- Replaced skeleton `release.yml` with lattice_stripe hardened pipeline: release-preflight, bootstrap-release-pr-ci, gate-ci-green poll, pre-publish replay, idempotent Hex publish

## Task Commits

Each task was committed atomically:

1. **Task 1: Release Please SSOT files** - `11f015d` (feat)
2. **Task 2: verify_gates + verify_docs; retire docs.yml** - `28a6f47` (feat)
3. **Task 3: ci-gate aggregation + workflow_dispatch** - `3a60662` (feat)
4. **Task 4: Hardened release.yml** - `76f346e` (feat)

**Plan metadata:** pending (docs commit follows)

## Files Created/Modified

- `release-please-config.json` - Elixir release-type, changelog sections, package config
- `.release-please-manifest.json` - Version SSOT `{ ".": "1.0.0" }`
- `.github/workflows/ci.yml` - verify_gates, verify_docs, ci-gate, workflow_dispatch
- `.github/workflows/release.yml` - Full Release Please + gate-ci-green + publish-hex pipeline
- `.github/workflows/docs.yml` - Deleted (folded into verify_docs)

## Decisions Made

- Followed D-60.1-01..12 from CONTEXT: 8-lane ci-gate, SHA-pinned actions, chimeway package name
- Pre-publish replay uses `mix ci.verify_gates` + `mix ci.docs` (not full `mix test`) per D-60.1-12
- Action SHAs resolved via `gh api`: release-please-action v5 → `0dfd853…`, github-script v8 → `ed59741…`

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required for Wave 1 infra. Operator must push to origin and manually merge bootstrap 1.1.0 Release PR before first automated Hex publish.

## Verification Results

| Check | Result |
|-------|--------|
| `mix ci.verify_gates` | PASS (225 tests) |
| `mix ci.docs` | PASS |
| `test ! -f .github/workflows/docs.yml` | PASS |
| ci-gate needs 8 lanes, no install_golden_contract | PASS |
| release.yml publish-hex needs gate-ci-green | PASS |
| No unpinned `@vN` action tags in release.yml | PASS |
| release-pr-automerge.yml untouched (Wave 2) | PASS |

## Self-Check: PASSED

## Next Phase Readiness

- Wave 1 complete; ready for 60.1-02 (automerge, publish-hex recovery, MAINTAINING runbook, GATE-06 contract test extensions)
- Operator: push Wave 1 to origin → wait for Release Please PR `chore(main): release 1.1.0` → confirm ci-gate green on release branch → manual merge

---
*Phase: 60.1-hex-release-pipeline*
*Completed: 2026-05-30*
