---
phase: 107-operator-timeline-guidance-gate-parity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/traces.ex
  - lib/chimeway/safe_evidence.ex
  - test/chimeway/traces_test.exs
  - test/chimeway/safe_evidence_test.exs
  - chimeway_admin/lib/chimeway_admin/components/timeline_event.ex
  - chimeway_admin/test/chimeway_admin/components/timeline_event_test.exs
  - examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs
autonomous: true
requirements:
  - INT-02
  - GATE-03
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every tenant-authorized delivery explanation independently exposes notification seen and read only when the parent notification has the corresponding persisted timestamp."
    - "Seen/read entries use exact authoritative timestamps, exact empty detail, timestamp-first ordering, and seen-before-read ordering only when timestamps are equal."
    - "Sibling delivery explanations agree on notification lifecycle facts, while recipient identity, caller metadata, tenant, notification content, signal payload, and publisher data remain absent."
    - "The optional admin timeline renders exact accessible labels and stable event hooks without weakening timestamps, detail markup, outcome/suppression semantics, or either redaction layer."
    - "The mounted demo journey proves durable arrival, visible first-seen, one workflow progression, explicit read, repeat idempotency, and the authorized Trace Detail presentation end to end."
    - "The optional-package status and durable identity anchors remain unchanged: core stays Phoenix-free, chimeway_admin stays host-mounted, and no schema or lifecycle authority moves."
  artifacts:
    - path: lib/chimeway/traces.ex
      provides: "Parent-notification lifecycle projection and timestamp-first timeline ordering"
      contains: "notification_seen"
    - path: lib/chimeway/safe_evidence.ex
      provides: "Closed admission of notification_seen and notification_read with existing detail projection"
      contains: "notification_read"
    - path: test/chimeway/traces_test.exs
      provides: "Absent/seen-only/read-only/both, sibling, chronology, tie, scope, and privacy contracts"
    - path: test/chimeway/safe_evidence_test.exs
      provides: "Closed-vocabulary admission and unknown-event rejection contracts"
    - path: chimeway_admin/lib/chimeway_admin/components/timeline_event.ex
      provides: "Exact labels and stable per-event structural hook"
      contains: "data-cw-timeline-event"
    - path: chimeway_admin/test/chimeway_admin/components/timeline_event_test.exs
      provides: "Focused component timestamp, label, hook, detail-list, and re-redaction proof"
    - path: examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs
      provides: "Mounted arrival-to-seen-to-read-to-operator-timeline journey"
  key_links:
    - from: lib/chimeway/traces.ex
      to: lib/chimeway/notifications/notification.ex
      via: "existing tenant-scoped parent preload reads persisted seen_at and read_at"
      pattern: 'notification\.(seen_at|read_at)'
    - from: lib/chimeway/traces.ex
      to: lib/chimeway/safe_evidence.ex
      via: "timeline entries pass through the existing closed SafeEvidence projection"
      pattern: 'SafeEvidence\.(timeline_detail|trace)'
    - from: chimeway_admin/lib/chimeway_admin/components/timeline_event.ex
      to: lib/chimeway/safe_evidence.ex
      via: "the host-mounted component renders the already-safe timeline and re-redacts detail"
      pattern: 'Redaction\.safe_timeline_detail'
    - from: examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs
      to: chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex
      via: "authorized LiveView mount of the real delivery Trace Detail route after inbox transitions"
      pattern: '/admin/chimeway/deliveries/'
---

<objective>
Deliver the production-quality safe operator timeline tracer for INT-02 and the journey evidence consumed by GATE-03.

Purpose: Let an operator distinguish the already-durable notification seen/read facts without creating a second authority, leaking identity/content, or implying engagement.
Output: Safe core timeline entries, explicit optional-admin rendering, and a mounted demo proof from arrival through seen/read to Trace Detail.

The phrase "optional admin timeline" creates no assumption delta: `chimeway_admin` remains an optional host-mounted package, core remains Phoenix-free, and stable notification/tenant/recipient identity anchors do not move.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/107-operator-timeline-guidance-gate-parity/107-CONTEXT.md
@.planning/phases/107-operator-timeline-guidance-gate-parity/107-RESEARCH.md
@.planning/phases/107-operator-timeline-guidance-gate-parity/107-VALIDATION.md
@.planning/phases/107-operator-timeline-guidance-gate-parity/107-PATTERNS.md
@.planning/phases/105-tenant-safe-inbox-change-stream/105-02-SUMMARY.md
@.planning/phases/106-idempotent-seen-lifecycle-workflow-proof/106-01-SUMMARY.md
@.planning/phases/106-idempotent-seen-lifecycle-workflow-proof/106-02-SUMMARY.md

<interfaces>
- `Chimeway.Traces.explain_delivery/2` is the tenant-scoped public read boundary; retain its `{:ok, Explanation.t()} | {:error, :not_found}` contract.
- `Chimeway.SafeEvidence.timeline_detail/1` is the existing closed detail projection; the new entries must use it and remain `%{}`.
- `ChimewayAdmin.Components.TimelineEvent.timeline/1` accepts `timeline: [entry]` and renders `<time>`, the event label, and a redacted `<dl>`.
- `Chimeway.mark_seen/3` and `Chimeway.mark_read/3` are independent durable lifecycle APIs; neither fact may be synthesized from the other.
</interfaces>

<edge_probe_disposition>
- Applicable and required: absent, seen-only, read-only, and both persisted states; exact-equal timestamp tie; cross-rank timestamp ordering; closed event atoms with exact empty details; lifecycle-boundary independence.
- Generic encoding probe is non-applicable because the phase introduces no wire, text, or archive encoding boundary; the new atoms and DateTime values use the existing in-memory/Ecto explanation projection.
- Generic numeric-precision probe is non-applicable because no numeric domain or rounding is introduced; ordering uses the exact persisted DateTime microsecond coordinate only as a comparison key.
</edge_probe_disposition>
</context>

## Artifacts this phase produces

- New closed timeline values `:notification_seen` and `:notification_read`; no new public function, schema field, migration, protocol, or dependency.
- New private `Chimeway.Traces.notification_lifecycle_entries/1` and `notification_lifecycle_entry/2` helpers plus closed `timeline_rank/1` clauses for the two values.
- New explicit `humanize_event/1` clauses for “Notification seen” and “Notification read,” and the `data-cw-timeline-event` structural hook on existing timeline items.
- New file `chimeway_admin/test/chimeway_admin/components/timeline_event_test.exs`.
- Expanded contracts in the three existing test files named in frontmatter; no UI-SPEC or schema artifact is produced.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one parent notification's seen/read facts through the safe delivery explanation</name>
  <read_first>
    - `lib/chimeway/traces.ex` — retain the tenant-scoped delivery/notification preload, `build_timeline/6`, and closed rank helper.
    - `lib/chimeway/safe_evidence.ex` — retain `@timeline_events`, `trace_timeline_entry/1`, and `timeline_detail/1` as the admission boundary.
    - `lib/chimeway/notifications/notification.ex` — confirm `seen_at` and `read_at` are the authoritative parent fields.
    - `lib/chimeway/inbox.ex` — preserve independent seen/read lifecycle semantics.
    - `test/chimeway/traces_test.exs` — extend the existing tenant/privacy/timeline fixture style.
    - `test/chimeway/safe_evidence_test.exs` — extend the closed-vocabulary positive and rejection cases.
  </read_first>
  <reversibility rating="costly">Removing the emitted event values after adopters consume explanations would require coordinated API, admin, docs, and contract changes, but no data migration.</reversibility>
  <files>lib/chimeway/traces.ex, lib/chimeway/safe_evidence.ex, test/chimeway/traces_test.exs, test/chimeway/safe_evidence_test.exs</files>
  <behavior>
    - Absent input: both parent timestamps nil -> neither lifecycle event is emitted.
    - Seen-only input: only persisted `seen_at` -> exactly one `:notification_seen` entry with that timestamp and `%{}`.
    - Read-only input: only persisted `read_at` -> exactly one `:notification_read` entry; seen is not synthesized.
    - Both input: each event appears exactly once and sibling deliveries return identical lifecycle entries.
    - Ordering: cross-ranked events sort by timestamp; equal-microsecond seen/read sort seen before read.
    - Privacy: new entries contain only `:at`, `:event`, and `:detail`, with hostile recipient/caller/content/publisher sentinels absent from the serialized explanation.
    - Admission: both new atoms survive SafeEvidence; an unknown event atom is dropped.
  </behavior>
  <action>Start with failing core contracts covering every behavior above. In `SafeEvidence`, add exactly the two locked event atoms to `@timeline_events` and add no detail fields. In `Traces`, derive independent conditional entries only from the already-preloaded parent notification's `seen_at` and `read_at`; use the named private helpers, preserve the existing tenant-qualified query and explanation shape, and pass exact empty maps through `SafeEvidence.timeline_detail/1`. Change the global timeline key so the persisted timestamp's Unix microseconds are first and the closed event rank is second; add seen/read rank clauses with seen lower than read while preserving all existing relative tie ranks. Do not query or infer from signals, PubSub, delivery/attempt state, protected activation, provider feedback, recipient/caller fields, or engagement. This is the production tracer: one public `explain_delivery/2` path must work from durable row through closed projection before admin expansion begins.</action>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/traces_test.exs test/chimeway/safe_evidence_test.exs --warnings-as-errors</automated>
    <fails_when>The command exits non-zero, emits a compiler warning, either new event is absent/duplicated, state independence or sibling equality fails, ordering is rank-first, detail is non-empty, or any hostile sentinel survives.</fails_when>
  </verify>
  <acceptance_criteria>
    - All four persisted state combinations return the exact independent event set.
    - Cross-rank chronology and the equal-timestamp seen-before-read tie are both non-vacuously proven.
    - Both sibling delivery explanations carry identical lifecycle facts from the parent notification.
    - SafeEvidence admits only the two new known values and projects exact empty details.
    - Tenant scoping and hostile-sentinel privacy negatives remain green.
  </acceptance_criteria>
  <done>`explain_delivery/2` returns safe, genuinely chronological notification seen/read facts from the authoritative parent row with all focused root contracts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render distinct optional-admin lifecycle events without weakening redaction</name>
  <read_first>
    - `chimeway_admin/lib/chimeway_admin/components/timeline_event.ex` — preserve the existing `<time>`, `<dl>`, generic event rendering, and detail redaction.
    - `chimeway_admin/lib/chimeway_admin/redaction.ex` — retain the defense-in-depth allowed-detail boundary unchanged.
    - `chimeway_admin/test/chimeway_admin/redaction_test.exs` — reuse the async component/redaction test conventions.
    - `.planning/phases/107-operator-timeline-guidance-gate-parity/107-PATTERNS.md` — follow the direct `render_component/3` analog.
  </read_first>
  <reversibility rating="costly">The stable data hook can become host CSS and test surface, so later renaming requires coordinated consumer updates.</reversibility>
  <files>chimeway_admin/lib/chimeway_admin/components/timeline_event.ex, chimeway_admin/test/chimeway_admin/components/timeline_event_test.exs</files>
  <behavior>
    - Both new events render the exact labels “Notification seen” and “Notification read.”
    - Each timeline item exposes `data-cw-timeline-event` with its atom converted to the stable string value.
    - Existing ISO `<time datetime>`, empty `<dl>`, generic events, and hostile-detail redaction remain intact.
  </behavior>
  <action>Create the focused async component test first with `Phoenix.LiveViewTest.render_component/3`. Add `data-cw-timeline-event={Atom.to_string(entry.event)}` to the existing list item and explicit `humanize_event/1` clauses for the two locked labels, leaving the generic clause for all other atoms. Assert both hook values, exact labels, the ISO datetime attribute, retained detail-list markup, exact empty new-event details, and absence of hostile detail. Do not change status badge or suppression-reason behavior elsewhere, add allowed detail keys, create a browser harness, promote the package, or add a core Phoenix dependency. Record the assumption-delta result in the task summary: optional-package status and all identity anchors are unchanged.</action>
  <verify>
    <automated>cd chimeway_admin && mix deps.get && mix test test/chimeway_admin/components/timeline_event_test.exs test/chimeway_admin/redaction_test.exs --warnings-as-errors</automated>
    <fails_when>Dependency preparation or tests exit non-zero, warnings are emitted, either exact label/hook is missing, timestamp/detail markup regresses, hostile detail renders, or an existing generic event changes.</fails_when>
  </verify>
  <acceptance_criteria>
    - The new focused test file exists and proves labels, hooks, timestamps, detail markup, and defense-in-depth redaction.
    - Existing generic event humanization, outcome/suppression semantics, and redaction tests remain green.
    - No dependency, schema, identity, package-status, or visual-design change accompanies the timeline rendering.
  </acceptance_criteria>
  <done>The optional admin component distinctly and accessibly renders both safe lifecycle events while retaining every existing presentation and redaction boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove arrival through seen/read to mounted Trace Detail in the demo host</name>
  <read_first>
    - `examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs` — extend the existing `@moduletag :inbox` arrival/read and seen-workflow journeys.
    - `examples/chimeway_demo_host/lib/demo_host_web/router.ex` — use the existing host-mounted inbox and Trace Detail routes.
    - `examples/chimeway_demo_host/lib/demo_host/seeds.ex` — reuse deterministic opaque-recipient notifications.
    - `examples/chimeway_demo_host/lib/demo_host/inbox_auth.ex` — retain current recipient/tenant authorization.
    - `chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex` — assert through the real mounted explanation consumer, not a test-only renderer.
    - `.planning/phases/106-idempotent-seen-lifecycle-workflow-proof/106-02-SUMMARY.md` — preserve the existing once-only workflow and denial evidence.
  </read_first>
  <files>examples/chimeway_demo_host/test/demo_host_web/inbox_bell_proof_test.exs</files>
  <behavior>
    - Seeded durable arrival is visible in the authorized mounted inbox.
    - Opening the bell marks the visible notification seen and progresses the eligible workflow exactly once.
    - Explicit read persists `read_at`; repeat opens/read/reload processing do not duplicate lifecycle or workflow progression.
    - The same authorized host session mounts the delivery Trace Detail and renders both labels, both stable hooks, and no recipient/caller hostile sentinel.
    - Existing wrong-tenant and wrong-recipient denial paths remain unchanged.
  </behavior>
  <action>Extend the existing tagged demo proof rather than creating a parallel harness. Drive the real sequence from `DemoHost.Seeds.seed_inbox/0`: mount `/inbox` with the authorized session, open the bell, drain the signal queue, assert the visible notification's durable `seen_at` and one workflow transition, click its explicit read control, assert durable `read_at`, repeat the interactions and drain to prove once-only behavior, then mount `/admin/chimeway/deliveries/:delivery_id` through the existing authorized Trace Detail route. Assert both exact labels and hook values plus privacy negatives in rendered HTML. Keep the existing cross-tenant/cross-recipient workflow denial test and do not treat reload hints, provider handoff, protected activation, or the rendered page as lifecycle or engagement authority.</action>
  <verify>
    <automated>cd examples/chimeway_demo_host && mix test test/demo_host_web/inbox_bell_proof_test.exs --include inbox --warnings-as-errors</automated>
    <fails_when>The focused journey exits non-zero, warns, fails to persist either timestamp, progresses the workflow more than once, exposes a hostile sentinel, cannot mount Trace Detail, or weakens an existing scope denial.</fails_when>
  </verify>
  <acceptance_criteria>
    - One deterministic test path composes durable arrival, mounted visible seen, explicit read, and mounted operator explanation.
    - Repeated interactions leave first-transition timestamps and the signal-driven workflow count stable.
    - Trace Detail contains both exact labels and both `data-cw-timeline-event` values.
    - Wrong-tenant/wrong-recipient and privacy-negative assertions remain green.
  </acceptance_criteria>
  <done>The real optional packages and host routes prove the complete safe lifecycle-to-operator-timeline path, ready for Wave 2 gate composition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host tenant scope → core trace query | A host-selected tenant authorizes which correlated delivery and parent notification may be loaded. |
| durable notification row → public explanation | Persisted lifecycle facts cross into a privacy-safe operator DTO. |
| safe explanation → optional admin HTML | Closed core evidence is rendered in a host-mounted operator surface with a second redaction layer. |
| authorized inbox session → mounted admin session | The demo proof crosses end-user lifecycle actions into an independently authorized operator read path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-01 | Information Disclosure | `Traces` / `SafeEvidence` / timeline HTML | high | mitigate | Emit exact empty detail, admit only the two closed atoms, retain both redaction layers, and serialize hostile recipient/caller/content/publisher sentinels in root, component, and mounted tests. |
| T-107-02 | Spoofing / Information Disclosure | `explain_delivery/2` and Trace Detail mount | high | mitigate | Preserve `TenantScope.resolve/1`, tenant-qualified parent joins, fail-closed mounted admin auth, and existing wrong-scope demo assertions. |
| T-107-03 | Tampering / Repudiation | seen/read fact source | high | mitigate | Read only persisted parent `seen_at`/`read_at`; never infer from PubSub, signals, delivery status, provider handoff, protected activation, or presentation. |
| T-107-04 | Repudiation | `timeline_sort_key/1` | medium | mitigate | Sort by exact timestamp coordinate before closed rank and require both cross-rank inversion and equal-time tie contracts. |
</threat_model>

<verification>
  <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/traces_test.exs test/chimeway/safe_evidence_test.exs --warnings-as-errors</automated>
  <fails_when>Core timeline, SafeEvidence, tenant-scope, privacy, or warning contracts fail.</fails_when>
  <automated>cd chimeway_admin && mix deps.get && mix test test/chimeway_admin/components/timeline_event_test.exs test/chimeway_admin/redaction_test.exs --warnings-as-errors</automated>
  <fails_when>Optional-admin labels, hooks, markup, redaction, dependency preparation, or warning contracts fail.</fails_when>
  <automated>cd chimeway_inbox && mix test test/chimeway_inbox/live/bell_dropdown_live_test.exs --warnings-as-errors</automated>
  <fails_when>The previously proven mounted bell behavior regresses or emits warnings.</fails_when>
  <automated>cd examples/chimeway_demo_host && mix test test/demo_host_web/inbox_bell_proof_test.exs --include inbox --warnings-as-errors</automated>
  <fails_when>The previously proven focused demo journey or its new Trace Detail assertions fail.</fails_when>
  <automated>mix verify.inbox</automated>
  <fails_when>The current package-plus-demo inbox aggregate fails before Wave 2 expands its composition.</fails_when>
</verification>

<success_criteria>
- INT-02 is implemented at the durable parent-notification, SafeEvidence, optional-admin, and mounted-host seams with no new authority.
- GATE-03 has a real arrival-to-seen-to-read-to-operator-timeline journey ready to be required by the existing named lane.
- All objectively testable acceptance is executable; no human UAT, schema work, UI-SPEC, external integration, or unplanned package change exists.
</success_criteria>

<output>
Create `.planning/phases/107-operator-timeline-guidance-gate-parity/107-01-SUMMARY.md` when done.
</output>
