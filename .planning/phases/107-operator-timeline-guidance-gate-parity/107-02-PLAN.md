---
phase: 107-operator-timeline-guidance-gate-parity
plan: "02"
type: execute
wave: 2
depends_on:
  - "107-01"
files_modified:
  - guides/introduction/inbox-integration.md
  - test/chimeway/doc_contract_test.exs
  - test/chimeway/release_gate_contract_test.exs
  - mix.exs
  - MAINTAINING.md
autonomous: true
requirements:
  - DOCS-03
  - GATE-03
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The canonical inbox guide gives a host source-valid publisher, PubSub, secret, recipient-auth, and tenant-auth configuration and assigns every ownership boundary explicitly."
    - "The guide distinguishes durable arrival/seen/read/archive from provider handoff, visible presentation, protected activation, and engagement, and treats reconnect/messages only as reasons to reload authoritative state."
    - "Every recursive cleanup in the release-gate contract can delete only a validated test-owned immediate child of the system temp root, and refuses the temp root, nested/arbitrary paths, and unowned siblings."
    - "The existing mix verify.inbox alias runs focused core lifecycle/timeline/privacy/Phoenix-optional evidence, the full inbox package, focused admin evidence, focused doc/release contracts, and the demo inbox journey in cheapest-to-broadest order."
    - "The existing verify_inbox CI job remains the single lane owner, and pr-gate and ci-gate require exactly the same lane result while nightly admin/browser work stays outside release topology."
    - "MAINTAINING.md names every inbox evidence class and states that both aggregate gates consume the same verify_inbox lane."
  artifacts:
    - path: guides/introduction/inbox-integration.md
      provides: "Canonical complete host ownership, configuration, isolation, reconnect, and lifecycle-semantics guidance"
      contains: "topic_secret"
    - path: test/chimeway/doc_contract_test.exs
      provides: "Focused positive, ordering, and stale/unsafe-negative DOCS-03 contracts"
      contains: "inbox_gate_parity"
    - path: test/chimeway/release_gate_contract_test.exs
      provides: "Owned-temp refusal guard plus mutation-negative alias/job/aggregate parity contracts"
      contains: "remove_owned_temp_dir!"
    - path: mix.exs
      provides: "Single ordered verify.inbox composition for every required GATE-03 evidence class"
      contains: "verify.inbox"
    - path: MAINTAINING.md
      provides: "Pre-ship inbox evidence and aggregate-parity instructions"
      contains: "verify_inbox"
  key_links:
    - from: guides/introduction/inbox-integration.md
      to: examples/chimeway_demo_host/config/config.exs
      via: "guide copies the tracked working publisher/PubSub/topic-secret configuration shape"
      pattern: 'inbox_change_publisher|pubsub_server|topic_secret'
    - from: guides/introduction/inbox-integration.md
      to: examples/chimeway_demo_host/lib/demo_host/inbox_auth.ex
      via: "guide copies both host authorization callbacks and opaque recipient mapping"
      pattern: 'current_(recipient|tenant)'
    - from: mix.exs
      to: test/chimeway/release_gate_contract_test.exs
      via: "mutation contracts require every exact verify.inbox evidence command and ordering edge"
      pattern: 'verify\.inbox'
    - from: .github/workflows/ci.yml
      to: mix.exs
      via: "unchanged verify_inbox job invokes only the single local alias"
      pattern: 'mix verify\.inbox'
    - from: .github/workflows/ci.yml
      to: scripts/ci/aggregate-gate.sh
      via: "both aggregates carry needs.verify_inbox.result into their closed required-lane sets"
      pattern: 'needs\.verify_inbox\.result'
---

<objective>
Publish the canonical arrival-to-seen-to-read ownership guidance and make the existing inbox lane and both aggregate gates continuously prove it, after first eliminating the release-contract cleanup hazard.

Purpose: Close DOCS-03 and GATE-03 with one canonical guide and one local/CI verification owner, without changing CI topology or turning lossy reload hints into lifecycle truth.
Output: Executable guide contracts, ownership-safe release-test cleanup, the expanded `mix verify.inbox` alias, mutation-locked aggregate parity, and aligned maintainer instructions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/107-operator-timeline-guidance-gate-parity/107-CONTEXT.md
@.planning/phases/107-operator-timeline-guidance-gate-parity/107-RESEARCH.md
@.planning/phases/107-operator-timeline-guidance-gate-parity/107-VALIDATION.md
@.planning/phases/107-operator-timeline-guidance-gate-parity/107-PATTERNS.md
@.planning/phases/107-operator-timeline-guidance-gate-parity/107-01-SUMMARY.md

<interfaces>
- `mix verify.inbox` is the sole local composition owner; `.github/workflows/ci.yml` already has a `verify_inbox` job that invokes it.
- Existing `extract_ci_job_block/2`, `extract_pr_gate_needs/1`, and `extract_ci_gate_needs/1` helpers are the structural CI contract seam; reuse them for exact membership comparisons and mutations.
- `DemoHost.InboxAuth.current_recipient/2` and `current_tenant/2` are the tracked source-valid auth shape for the guide.
- `ChimewayInbox.PubSubPublisher`, `DemoHost.PubSub`, and the host-custodied `:topic_secret` are the tracked source-valid configuration shape.
</interfaces>

<edge_probe_disposition>
- Applicable and required: lifecycle semantics boundaries are locked in guide prose and executable contracts; alias command membership, CI job invocation, and both aggregate memberships/results are compared for exact equality and mutation failure.
- Generic encoding is non-applicable because no wire/file format or decoder changes; Markdown and Mix/CI source remain on existing parsers and exact-string structural contracts.
- Generic numeric precision is non-applicable because documentation and gate composition introduce no numeric calculation, tolerance, or rounding behavior.
</edge_probe_disposition>
</context>

## Artifacts this phase produces

- New tagged contract group `:inbox_gate_parity` in both root documentation and release-gate tests.
- New release-test-only `@owned_temp_prefixes`, `owned_temp_directory!/1`, and `remove_owned_temp_dir!/1` symbols; the former `temporary_path!/1` direct-child file pattern is removed/replaced.
- New focused tag `:release_cleanup_safety` for the owned-directory acceptance/refusal mutation contracts.
- New canonical guide sections for configuration, ownership, authorization/isolation, authoritative reload/reconnect, and the lifecycle semantics table.
- Expanded existing `verify.inbox` alias and pre-ship prose; no new CI job, Mix alias, workflow file, schema file, public runtime symbol, external API contract, dependency, or UI-SPEC.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contract and write the canonical inbox ownership and lifecycle guide</name>
  <read_first>
    - `guides/introduction/inbox-integration.md` — retain this as the single canonical guide and preserve its ordered golden path.
    - `examples/chimeway_demo_host/config/config.exs` — copy the working publisher, PubSub, and topic-secret configuration shape.
    - `examples/chimeway_demo_host/lib/demo_host/inbox_auth.ex` — copy both working authorization callback shapes and production fail-closed warning.
    - `guides/introduction/mobile-adoption-operations.md` — link the established authority for provider-handoff, visible-presentation, protected-activation, inbox-state, and engagement distinctions.
    - `test/chimeway/doc_contract_test.exs` — extend the existing inbox guide contract with a focused tag and ordered positive/negative assertions.
    - `.planning/phases/107-operator-timeline-guidance-gate-parity/107-CONTEXT.md` — implement every Adopter Guidance and Ownership decision exactly.
  </read_first>
  <files>guides/introduction/inbox-integration.md, test/chimeway/doc_contract_test.exs</files>
  <behavior>
    - Contract requires `:inbox_change_publisher`, `:pubsub_server`, high-entropy `:topic_secret`, auth module configuration, and both auth callbacks.
    - Contract requires explicit core/package/host ownership and exact tenant/recipient isolation responsibilities.
    - Contract requires opaque `cw_*` recipient examples, authoritative reload/reconnect semantics, and an arrival/seen/read/archive comparison table.
    - Contract separates inbox facts from provider handoff, visible presentation, protected activation, and engagement; none implies another.
    - Contract rejects stale deferred-seen claims and raw email-shaped recipient examples while preserving current public `Chimeway.*` delegate truth and section ordering.
  </behavior>
  <action>Add tagged `:inbox_gate_parity` documentation contracts first, using required-string and section-order assertions plus negative assertions for stale semantics and unsafe identity examples. Rewrite only `guides/introduction/inbox-integration.md` from the tracked demo configuration/auth shapes: show the publisher, PubSub server, high-entropy secret, auth module, both callbacks, opaque recipient mapping, and production authorization responsibility. State that Chimeway owns durable state and contained post-commit publication; `chimeway_inbox` owns opaque topics, closed reload messages, subscription, and durable reload; the host owns auth, tenant membership, recipient mapping, PubSub supervision, secret custody, and production authorization. Replace stale lifecycle copy with durable arrival, visible-page first-seen, explicit read, independent archive, and reconnect/reload-as-hint semantics. Add the concise comparison table and link the existing mobile operations guide for provider/presentation/protected-activation boundaries. Do not create a second guide, expose raw identity/content, claim engagement, change public APIs, or alter package/runtime behavior.</action>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --only inbox_gate_parity --warnings-as-errors</automated>
    <fails_when>The focused contract exits non-zero, warns, any required config/auth/ownership/semantics item is absent or misordered, a stale/raw example remains, or existing public-delegate/path-package truth regresses.</fails_when>
  </verify>
  <acceptance_criteria>
    - The guide contains all four working configuration/auth inputs and stable opaque recipient examples.
    - Core, package, and host responsibilities plus tenant/recipient isolation and secret custody are explicit.
    - Arrival, visible seen, explicit read, archive, reload/reconnect, provider handoff, presentation, protected activation, and engagement have distinct non-implication semantics.
    - The tagged executable contract proves both required content/order and stale/unsafe exclusions.
  </acceptance_criteria>
  <done>DOCS-03 has one complete, source-valid, executable canonical guide whose ownership and lifecycle claims match the shipped system.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make release-contract recursive cleanup ownership-safe before any full gate run</name>
  <read_first>
    - `test/chimeway/release_gate_contract_test.exs` — inventory every `File.rm_rf*`, `Path.dirname(...)`, `temporary_path!/1`, archive builder, unpack builder, and adoption-run cleanup site before editing.
    - `.planning/phases/107-operator-timeline-guidance-gate-parity/107-RESEARCH.md` — follow the Safe Temp Ownership Guard and Pitfall 8 constraints.
    - `.planning/phases/107-operator-timeline-guidance-gate-parity/107-PATTERNS.md` — apply the closed-prefix, expanded-path, immediate-temp-child guard pattern.
  </read_first>
  <reversibility rating="costly">This centralizes cleanup across a large release-contract module; undo is code-local but would re-expose multiple destructive call sites.</reversibility>
  <files>test/chimeway/release_gate_contract_test.exs</files>
  <behavior>
    - The guard accepts an existing immediate child of the expanded system temp root only when its basename starts with one of the four closed Chimeway-owned prefixes.
    - The guard refuses the temp root itself, nested paths, arbitrary outside paths, and unowned sibling names without invoking recursive deletion.
    - The symbolic-link escape fixture (`outside-created.txt`), required-file link fixture (`outside-marker.ex`/`outside-marker.txt`), and special-record fixture (`outside-special.txt`) each live under a per-test owned scratch directory; cleanup receives that directory, never a parent derived from an arbitrary file path.
    - Every recursive cleanup in this test module routes through the guard; a structural mutation contract rejects any direct recursive call outside the guard, and fresh unique unpack output has no unguarded pre-clean.
  </behavior>
  <action>Before running any full release-gate command, add `:release_cleanup_safety` tests that exercise acceptance for all closed prefixes and refusal for `System.tmp_dir!()`, an owned-looking nested directory, an arbitrary directory outside the temp root, and an unowned immediate sibling. Include a source-structure mutation contract that fails if a recursive removal call exists anywhere outside the one guarded helper. Then add the exact closed `@owned_temp_prefixes` list for `chimeway_release_gate_`, `chimeway_release_archive_`, `chimeway_adoption_security_`, and `chimeway_adoption_run_`; implement `owned_temp_directory!/1` to create a unique test-owned immediate child and `remove_owned_temp_dir!/1` to compare expanded paths, reject the temp root, require the exact immediate parent, require a closed prefix, and only then call recursive removal. Replace the direct-child file behavior of `temporary_path!/1`: specifically move the `outside-created.txt`, `outside-marker.ex`/`outside-marker.txt`, and `outside-special.txt` fixtures beneath separate owned per-test scratch directories and register teardown against those directories. Route all archive, unpack, malicious-archive, and adoption-run recursive teardown paths through the guard, passing the owned directory itself; eliminate direct recursive deletion of derived/caller-controlled parents and remove the unnecessary pre-clean of fresh unique unpack output. Keep non-recursive `File.rm/1` only for explicitly owned files when useful, but the directory guard remains the final teardown. Do not execute the whole release contract, `mix verify.inbox`, or `mix ci.verify_gates` until the focused safety tag is green and committed.</action>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --only release_cleanup_safety --warnings-as-errors</automated>
    <fails_when>The focused command exits non-zero, warns, any forbidden path reaches recursive deletion, any closed owned prefix is rejected, direct recursive cleanup remains outside the guarded helper, or a temp fixture still owns only a file directly beneath the system temp root.</fails_when>
  </verify>
  <acceptance_criteria>
    - Refusal mutation tests cover the temp root, nested, outside, and unowned-sibling cases before deletion.
    - Acceptance tests prove all four closed prefixes can remove only their exact empty test-owned directories.
    - The three unsafe direct-temp cleanup classes and every other recursive teardown in the file use an ownership-validating directory boundary.
    - No full release-gate command has run before this focused safety command passes and the task is committed.
  </acceptance_criteria>
  <done>Recursive cleanup in the release contract is fail-closed at one validated ownership boundary, and focused mutation evidence is green before aggregate execution is permitted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand the single inbox alias and mutation-lock both aggregate gate edges</name>
  <read_first>
    - `mix.exs` — expand only the existing `verify.inbox` alias and preserve neighboring aliases.
    - `test/chimeway/release_gate_contract_test.exs` — reuse existing Mix/CI extraction helpers and mutation-negative topology patterns after Task 2's safety commit.
    - `.github/workflows/ci.yml` — treat as read-only evidence for the existing `verify_inbox`, `pr-gate`, `ci-gate`, and nightly-only admin/browser topology.
    - `MAINTAINING.md` — update the existing pre-ship inventory and inbox/aggregate descriptions in place.
    - `.planning/phases/107-operator-timeline-guidance-gate-parity/107-VALIDATION.md` — include every mapped evidence class and final aggregate command.
    - `.planning/phases/107-operator-timeline-guidance-gate-parity/107-01-SUMMARY.md` — require the exact core/admin/demo artifacts proven in Wave 1.
  </read_first>
  <reversibility rating="costly">The alias is the shared local/CI release contract, so later composition changes require coordinated Mix, maintainer, and mutation-contract updates.</reversibility>
  <files>mix.exs, MAINTAINING.md, test/chimeway/release_gate_contract_test.exs</files>
  <behavior>
    - Alias order is focused root lifecycle/timeline/privacy/Phoenix-optional tests, full inbox package, focused admin timeline/redaction, focused tagged doc/release contracts, then demo `:inbox` journey.
    - Each command is warning-strict; nested optional projects fetch their already-locked dependencies; no command is optional or error-suppressing.
    - Release contracts reject removal/reordering of each evidence command, any CI job command other than the single alias invocation, either missing aggregate need/result token, and inclusion of nightly admin/browser work.
    - MAINTAINING and exact alias/job/pr-gate/ci-gate memberships agree.
  </behavior>
  <action>Add `:inbox_gate_parity` release contracts first for the exact five evidence classes and their cheapest-to-broadest ordering. Mutation-test removal of each alias command, the unchanged `verify_inbox` job's sole `mix verify.inbox` invocation, exact `verify_inbox` membership in both gate `needs` lists, exact `needs.verify_inbox.result` environment wiring and aggregate argument tokens, and exclusion of nightly `verify_admin`/browser jobs. Expand only the existing `verify.inbox` alias with: the five named root lifecycle/timeline/privacy/Phoenix-optional files under `scripts/test-db`; the full `chimeway_inbox` package; focused `TimelineEvent` plus admin redaction tests; tagged inbox doc and release contracts under `scripts/test-db`; and the demo-host `--only inbox` suite last. Preserve warnings-as-errors and nested `mix deps.get`; add no new alias, CI job, workflow edit, Playwright/admin aggregate, sibling checkout, filter escape, or error-suppressing fallback. Update `MAINTAINING.md` to enumerate every evidence class and say both `pr-gate` and `ci-gate` consume the same `verify_inbox` result. Only after Task 2's safety tag is green and committed, run the focused inbox-parity contract here; reserve the expanded alias and full docs/release aggregate for the plan-level verification that follows.</action>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --only inbox_gate_parity --warnings-as-errors</automated>
    <fails_when>The focused contract exits non-zero or warns, any required evidence command is absent/reordered/skipped, alias/job/aggregate membership differs, nightly-only work enters the release topology, or maintainer copy drifts.</fails_when>
  </verify>
  <acceptance_criteria>
    - `mix verify.inbox` executes all five required evidence classes in the locked order with warning-strict tests.
    - Mutation-negative contracts prove exact alias command and job/aggregate edge equality, not marker-only presence.
    - `.github/workflows/ci.yml` remains unmodified and its existing single lane feeds both aggregates.
    - MAINTAINING accurately describes the complete alias and both consumers.
    - Both final aggregate commands pass after cleanup hardening.
  </acceptance_criteria>
  <done>GATE-03 is continuously enforced by one complete local alias, one unchanged CI job, and equal fail-closed dependencies in both aggregate gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked demo config/auth → adopter guide | Working development shapes cross into copy-paste production guidance and must retain host-owned authorization/secret responsibilities. |
| Mix alias → CI verify_inbox job | Local executable evidence becomes the single hosted lane contract. |
| verify_inbox result → pr-gate/ci-gate | One job conclusion is consumed by both fail-closed aggregate scripts. |
| release-test path → filesystem recursive deletion | Test-generated paths cross into a destructive operation and require exact ownership validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-107-05 | Information Disclosure | inbox guide and doc contract | high | mitigate | Require opaque `cw_*` examples, host custody/auth/isolation responsibilities, and negative contracts for raw recipient/caller/content forms. |
| T-107-06 | Spoofing / Information Disclosure | host tenant/recipient guidance | high | mitigate | Require both auth callbacks, explicit tenant membership/recipient mapping ownership, closed topic inputs, and current authorization on reload. |
| T-107-07 | Tampering / Repudiation | lifecycle/reconnect guidance and gate | high | mitigate | State and contract that messages are lossy reload hints, durable rows are authoritative, and inbox/provider/presentation/protected-open/engagement facts do not imply each other. |
| T-107-08 | Repudiation | alias/job/aggregate parity | medium | mitigate | Mutation-test exact alias commands, sole CI invocation, both needs memberships, both result tokens, and aggregate argument membership. |
| T-107-09 | Tampering / Denial of Service | release-contract temp cleanup | critical | mitigate | Before any full release command, centralize deletion behind expanded-path, immediate-parent, closed-prefix ownership checks and refusal mutation tests. |
</threat_model>

<verification>
  <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --only inbox_gate_parity --warnings-as-errors</automated>
  <fails_when>The canonical guide contract fails or warns.</fails_when>
  <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --only release_cleanup_safety --warnings-as-errors</automated>
  <fails_when>The ownership guard accepts a forbidden path, rejects a closed owned path, leaves an unsafe recursive call, fails, or warns.</fails_when>
  <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --only inbox_gate_parity --warnings-as-errors</automated>
  <fails_when>Alias/job/aggregate mutation contracts fail, membership differs, topology broadens, or warnings are emitted.</fails_when>
  <automated>mix verify.inbox</automated>
  <fails_when>Any packaged LiveView, core lifecycle/timeline/privacy/Phoenix-optional, admin, guide/release, workflow, or demo journey evidence fails.</fails_when>
  <automated>mix ci.verify_gates</automated>
  <fails_when>The full documentation/release contract fails after cleanup hardening, or local/release parity is not green.</fails_when>
</verification>

<success_criteria>
- DOCS-03 is source-valid, privacy-safe, and executable in the single canonical inbox guide.
- GATE-03 is enforced by `mix verify.inbox`, the unchanged `verify_inbox` job, and equal `pr-gate`/`ci-gate` dependencies.
- The destructive test-cleanup boundary is hardened and mutation-proven before any full release command executes.
- Generic encoding and numeric-precision probes are explicitly non-applicable; all genuinely applicable edge probes have executable coverage.
- No external API integration, schema change, UI-SPEC, new CI lane, nightly topology change, package promotion, or deferred idea enters the phase.
</success_criteria>

<output>
Create `.planning/phases/107-operator-timeline-guidance-gate-parity/107-02-SUMMARY.md` when done.
</output>
