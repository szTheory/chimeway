---
phase: 100-optional-apns-adapter
plan: 11
type: execute
wave: 10
depends_on: [100-09]
files_modified:
  - scripts/verify-apns.sh
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
gap_closure: true
requirements: [APNS-01]
must_haves:
  truths:
    - "D-01/APNS-01: the enabled packaged consumer compiles Chimeway-owned Elixir source under warnings-as-errors and the complete `bash scripts/verify-apns.sh` gate exits zero."
    - "The gate runs `mix deps.compile` normally, then runs exactly `mix cmd --cd deps/chimeway mix compile --force-elixir --no-deps-check --warnings-as-errors`, so only Chimeway's Elixir compiler is forced and an unrelated Oban source warning is not misreported as a Chimeway warning."
    - "A warning introduced in unpacked Chimeway source still makes the gate nonzero, with compiler diagnostics visible and dependency source left unmodified."
    - "The disabled packaged consumer retains its baseline graph without APNs-only edges, and the enabled consumer retains exact lock, Hex audit, compile, boot, test, result, and privacy evidence."
    - "Local `mix verify.apns` and hosted `verify_apns` continue to consume the same packaged verifier with release-gate parity."
  assumptions:
    - statement: "The Oban unreachable-clause diagnostic belongs to a separately resolved dependency under this enabled graph; Phase 100 owns strict compilation of Chimeway and consumer source, not rewriting or suppressing arbitrary dependency diagnostics."
      flagged: false
    - statement: "No schema, payload, target identity, provider classification, pagination, concurrency, credential, or live-network behavior changes in this verification-only plan."
      flagged: false
  prohibitions:
    - statement: "The packaged proof cannot hide Chimeway-owned warnings by weakening or removing warning-strict compilation."
      status: unverified
      flagged: true
  artifacts:
    - path: "scripts/verify-apns.sh"
      provides: "Passing enabled-consumer proof with dependency preparation separated from Chimeway-owned warning-strict compilation"
    - path: "test/chimeway/release_gate_contract_test.exs"
      provides: "Executable command-order and mutation contract preserving the warning gate"
  key_links:
    - from: "scripts/verify-apns.sh"
      to: "unpacked deps/chimeway"
      via: "mix deps.compile followed by the exact Chimeway-project command `mix compile --force-elixir --no-deps-check --warnings-as-errors`"
      pattern: "mix compile --force-elixir --no-deps-check --warnings-as-errors"
    - from: "mix verify.apns"
      to: "scripts/verify-apns.sh"
      via: "existing Mix alias and verify_apns CI lane"
      pattern: "verify-apns"
---

<objective>
Restore the passing enabled packaged-consumer gate while preserving a strict signal for Chimeway-owned compile warnings, implementing D-01 without hiding or patching arbitrary dependency warnings.

Purpose: The optional adapter is not releasable while its declared package proof is red, but the proof must measure the code Chimeway owns rather than force an unrelated dependency into the same warning domain.
Output: A corrected package compile sequence and release-contract mutations proving warning strictness, ordering, and local/CI parity.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-optional-apns-adapter/100-CONTEXT.md
@.planning/phases/100-optional-apns-adapter/100-RESEARCH.md
@.planning/phases/100-optional-apns-adapter/100-VERIFICATION.md
@.planning/phases/100-optional-apns-adapter/COVERAGE.md
@.planning/phases/100-optional-apns-adapter/100-08-SUMMARY.md
@.planning/phases/100-optional-apns-adapter/100-09-SUMMARY.md
@scripts/verify-apns.sh
@test/chimeway/release_gate_contract_test.exs
@test/fixtures/apns_consumer/mix.exs
@mix.exs
</context>

<assumption_delta_decision>
Primary noun: the exact tenant/environment/topic/binding-revision target identity. Decision: `no-change`. Optional package compilation changes only executable evidence; it neither adds another target identity nor alters cross-installation isolation.
</assumption_delta_decision>

<edge_case_assumptions>
Empty/missing script paths and command failure remain fail-fast under `set -euo pipefail`; the existing focus modes, temp cleanup, dependency lock, disabled/enabled representation, and output sentinel cases remain regression coverage. Input cardinality, pagination, target concurrency, exact binding, schema, and provider-network edge cases are irrelevant to this command-sequencing fix and remain owned by the existing APNs suites.
</edge_case_assumptions>

<prohibition_recall>
Keep one descriptor-less Phase 100 prohibition: the gate cannot conceal Chimeway-owned warnings. Dependency ownership is expressed as positive command-order acceptance and threat mitigation, not as a new product prohibition.
</prohibition_recall>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore a Chimeway-owned warning-strict enabled-consumer proof</name>
  <reversibility rating="reversible">Only the hermetic verifier command sequence and its executable text contract change; no runtime, dependency, lock, or public API changes.</reversibility>
  <files>scripts/verify-apns.sh, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>AGENTS.md, .planning/phases/100-optional-apns-adapter/100-CONTEXT.md, .planning/phases/100-optional-apns-adapter/100-VERIFICATION.md, .planning/phases/100-optional-apns-adapter/100-09-SUMMARY.md, scripts/verify-apns.sh, test/chimeway/release_gate_contract_test.exs, test/fixtures/apns_consumer/mix.exs, mix.exs. Run `mix help compile` and `mix help compile.elixir` before editing; use their documented distinction between project warnings and dependency checks as the command contract.</read_first>
  <behavior>
    - "Enabled setup resolves the committed lock, runs `mix deps.compile`, then runs exactly `mix cmd --cd deps/chimeway mix compile --force-elixir --no-deps-check --warnings-as-errors` before compiling the consumer."
    - "The release contract asserts `mix deps.compile` precedes the exact Chimeway strict command and that the exact Chimeway strict command precedes consumer strict compile/tests."
    - "Mutation removal of `--warnings-as-errors`, `--force-elixir`, `--no-deps-check`, `mix cmd --cd deps/chimeway`, `mix deps.compile`, or required command ordering makes the release contract fail."
    - "No stderr redirection, blanket warning ignore, compiler diagnostic filtering, `|| true`, dependency source patch, replacement with `--force`, or removal of `--force-elixir` can make the contract pass."
    - "The real gate exits zero without the Oban `expected_error?/1` diagnostic while still emitting/failing on a deliberately introduced Chimeway source warning in a temporary mutation probe."
    - "Disabled and enabled package modes retain dependency tree/lock checks, exact Pigeon 2.0.1 graph, unsuppressed Hex audit, boot/tests, output leak sentinel, cleanup, and fixed sandbox-only proof line."
  </behavior>
  <action>Start RED in `release_gate_contract_test.exs` by replacing the old whole-project dependency compile string with a three-stage contract: after `mix deps.get --check-locked`, run `mix deps.compile`; then run exactly `mix cmd --cd deps/chimeway mix compile --force-elixir --no-deps-check --warnings-as-errors`; finally run the existing consumer compile under warnings-as-errors. Assert exact ordering and mutation failures for `mix deps.compile`, the `mix cmd --cd deps/chimeway` project boundary, `mix compile`, `--force-elixir`, `--no-deps-check`, `--warnings-as-errors`, and the consumer stage. Update `scripts/verify-apns.sh` to match this exact sequence. The Chimeway stage must recompile every Chimeway Elixir source file while leaving the already prepared dependency graph unforced, so optional-path duplicate/unreachable/undefined warnings fail without force-recompiling Oban or another resolved dependency in Chimeway's warning domain. Add a hermetic mutation probe in the release contract or verifier test fixture that copies or temporarily modifies only unpacked Chimeway source with a compile-warning fixture, runs that exact strict command, and asserts nonzero without altering the repository checkout; keep the probe bounded and clean it through the script's validated temporary root. Preserve exact enabled lock/audit and all consumer tests. Do not substitute `mix compile.elixir`, use whole-project `--force`, add warning filters or ignore lists, redirect diagnostics, add shell fallbacks, patch dependencies, or change root/fixture dependencies.</action>
  <acceptance_criteria>
    - "`bash scripts/verify-apns.sh` exits 0 and completes both disabled and enabled consumers."
    - "After `mix deps.compile`, the Chimeway-only stage is exactly `mix cmd --cd deps/chimeway mix compile --force-elixir --no-deps-check --warnings-as-errors`, followed by consumer compile/tests."
    - "The existing Oban diagnostic is absent because Oban is not force-recompiled as Chimeway source; output remains visible and no diagnostic suppression/filter is present."
    - "A temporary Chimeway-source warning mutation makes the exact strict stage and packaged verifier nonzero, proving the gate is not vacuous."
    - "Release-gate mutations fail if `mix deps.compile`, `mix cmd --cd deps/chimeway`, `mix compile`, `--force-elixir`, `--no-deps-check`, `--warnings-as-errors`, or their required ordering is removed or replaced."
    - "Pigeon-disabled isolation, exact enabled graph/lock, Hex audit, APNs success/410/retry/fail-closed tests, and token/body leak checks remain green."
    - "`mix verify.apns` passes and the existing release contract confirms the hosted verify_apns lane still calls it."
  </acceptance_criteria>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors &amp;&amp; bash scripts/verify-apns.sh &amp;&amp; mix verify.apns</automated>
  </verify>
  <done>The enabled package gate is green, still fails on Chimeway-owned warnings, and no unrelated dependency warning is hidden or converted into a false Chimeway failure.</done>
</task>

</tasks>

## Artifacts this phase produces

- Modified executable `scripts/verify-apns.sh` with separate normal dependency preparation, Chimeway-only warning-strict compilation, and consumer warning-strict compilation.
- Expanded `test/chimeway/release_gate_contract_test.exs` command-order and non-vacuity mutation evidence.
- No new public symbols, runtime modules, dependency versions, lockfiles, schemas, migrations, credentials, API coverage decisions, target identities, or provider outcome classes.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Resolved enabled graph → unpacked Chimeway project | Dependency preparation must complete without redefining dependency warnings as Chimeway warnings. |
| Unpacked Chimeway source → consumer compile | Chimeway-owned optional code must be force-compiled warning-strictly before the consumer can pass. |
| Local verifier → hosted `verify_apns` lane | The same executable script remains the evidence source for local and CI gates. |

## STRIDE Threat Register

**Security gate:** OWASP ASVS Level 1 with `block_on: high`. Every high-severity mitigation is bound to Task 1's automated command and must pass before the outcome is `passed`; otherwise verification reports `gaps_found`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-100-11-01 | Tampering | package warning gate | high | mitigate | Exact compiler/flag/order contracts plus a temporary Chimeway warning mutation prove the strict stage is load-bearing. |
| T-100-11-02 | Repudiation | enabled compile claim | high | mitigate | Chimeway source is force-compiled before consumer compile/tests and the full script is required by `mix verify.apns`. |
| T-100-11-03 | Denial of Service | unrelated dependency warning | medium | mitigate | Resolve/compile dependencies normally, then use project-only compiler execution with dependency checks disabled for Chimeway's warning domain. |
| T-100-11-04 | Information Disclosure | verifier output | high | mitigate | Existing fixed proof line, token/body sentinel checks, temporary-root validation, and cleanup remain unchanged and rerun. |
| T-100-SC | Tampering | enabled Hex graph | high | mitigate | Existing committed exact lock, check-locked resolution, version assertions, and unsuppressed Hex audit remain unchanged. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Optional APNs use without dependencies for other hosts | 100-11 | COVERED | Restores the only failing packaged enabled-consumer proof and reruns disabled isolation. |
| REQ | APNS-01 | Explicit Pigeon opt-in and clean non-push consumption | 100-11 | COVERED | Both package modes and strict compile evidence pass. |
| REQ | APNS-02, APNS-05 | Failed validation requirements | 100-10 | COVERED | Downstream Plan 100-10 owns reference/collapse code and reruns the repaired full gate after this plan lands. |
| REQ | APNS-03, APNS-04, APNS-06 | Already-satisfied outcome, expiry, and evidence contracts | 100-11 regression | COVERED | Existing enabled consumer and `mix verify.apns` suites rerun without new scope. |
| RESEARCH | — | Static-free optional runtime and clean-consumer compile proof | 100-11 | COVERED | Chimeway and consumer source remain warning-strict; dependency preparation is explicit. |
| RESEARCH | — | Pinned approved Pigeon 2.0.1 graph | 100-11 | COVERED | Lock, version assertions, and Hex audit are unchanged. |
| CONTEXT | D-01, D-02 | Optional adapter and host ownership | 100-11 | COVERED | Package proof restored without root dependency/config changes. |
| CONTEXT | D-03..D-16 | Existing custody, request, and outcome contracts | 100-01..100-10; 100-11 regression | COVERED | Full APNs gate reruns them; this verification-only plan changes none. |

Deferred CrossWake, twin, physical-device, and FCM work remain excluded. COVERAGE.md remains authoritative and unchanged; no research/context/goal item is missing, and no schema-push surface applies.
</source_audit>

<verification>
Run the release-contract suite first, then the complete package verifier and `mix verify.apns`. Machine-readable outcomes are only `passed` or `gaps_found`; no UAT, human-check, or checkpoint applies.
</verification>

<success_criteria>
- The package verifier exits zero in both modes and keeps disabled consumers Pigeon-free.
- Chimeway-owned optional source and enabled consumer source remain warning-strict.
- Unrelated resolved dependency warnings are neither suppressed nor forced into Chimeway's warning domain.
- Mutation evidence proves every compile scope/strictness/order guard is load-bearing.
- Local and hosted APNs verification remain in parity.
</success_criteria>

<output>
Create `.planning/phases/100-optional-apns-adapter/100-11-SUMMARY.md` when done.
</output>
