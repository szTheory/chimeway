---
phase: 100-optional-apns-adapter
plan: 02
type: execute
wave: 2
depends_on: [100-01]
files_modified:
  - priv/chimeway_migrations/037_add_apns_request_intent.exs
  - test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_apns_request_intent.exs
  - test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_apns_request_intent.exs
  - test/chimeway/install/migrations_test.exs
  - test/chimeway/install/golden_diff_test.exs
  - test/chimeway/install/prefix_contract_test.exs
  - test/chimeway/migration_contract_test.exs
  - test/chimeway/generated_prefixed_runtime_proof_test.exs
autonomous: true
requirements: [APNS-02, APNS-04]
must_haves:
  truths:
    - "D-05: every supported installation path creates the same nullable safe APNs intent column without creating token, credential, payload, or response-body columns."
    - "D-06: public and prefixed generated installations preserve stable request intent through migration up/down/up and runtime retry reads."
    - "The canonical template, repository migration, generated public fixture, and generated prefixed fixture remain structurally equivalent."
    - "The additive migration rolls back only the APNs variant column and leaves Phase 99 target identity, rows, attempts, and tenant constraints intact."
  prohibitions:
    - statement: "Installing APNs support must not transfer device-token or Apple-credential custody into Chimeway-owned tables."
      status: unverified
      flagged: true
  artifacts:
    - path: "priv/chimeway_migrations/037_add_apns_request_intent.exs"
      provides: "Prefix-aware copied host migration for safe APNs target intent"
    - path: "test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_apns_request_intent.exs"
      provides: "Public-schema golden migration 037"
    - path: "test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_apns_request_intent.exs"
      provides: "Prefixed-schema golden migration 037"
  key_links:
    - from: "priv/chimeway_migrations/037_add_apns_request_intent.exs"
      to: "lib/chimeway/delivery_target.ex"
      via: "same nullable `apns_request_intent` storage name/type"
      pattern: "apns_request_intent"
    - from: "test/chimeway/install/golden_diff_test.exs"
      to: "both golden fixture trees"
      via: "generated tree equality and explicit migration 037 contract"
      pattern: "assert_migration_037"
---

<objective>
Ship the tracer's additive APNs intent storage through every supported repository and copied-migration installation path.

Purpose: Hosts own their storage mode, so D-05/D-06 are incomplete unless repository, generated public, and generated prefixed databases expose the same reversible safe-intent contract.
Output: Canonical migration 037, both golden copies, installer counts/contracts, database migration/rollback proof, and generated-prefixed runtime evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/100-optional-apns-adapter/100-CONTEXT.md
@.planning/phases/100-optional-apns-adapter/100-RESEARCH.md
@.planning/phases/100-optional-apns-adapter/100-01-SUMMARY.md
@priv/chimeway_migrations/035_create_chimeway_delivery_targets.exs
@priv/chimeway_migrations/036_enforce_delivery_target_tenant_integrity.exs
@priv/repo/migrations/20260820000000_enforce_delivery_target_tenant_integrity.exs
@test/chimeway/install/migrations_test.exs
@test/chimeway/install/golden_diff_test.exs
@test/chimeway/install/prefix_contract_test.exs
@test/chimeway/migration_contract_test.exs
@test/chimeway/generated_prefixed_runtime_proof_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish migration 037 across canonical and golden storage modes</name>
  <reversibility rating="costly">Published copied migrations are durable host artifacts, but the nullable additive column has an exact down operation and does not rewrite existing rows.</reversibility>
  <files>priv/chimeway_migrations/037_add_apns_request_intent.exs, test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_apns_request_intent.exs, test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_apns_request_intent.exs, test/chimeway/install/migrations_test.exs, test/chimeway/install/golden_diff_test.exs</files>
  <read_first>AGENTS.md, .planning/phases/100-optional-apns-adapter/100-CONTEXT.md, .planning/phases/100-optional-apns-adapter/100-01-SUMMARY.md, priv/chimeway_migrations/035_create_chimeway_delivery_targets.exs, priv/chimeway_migrations/036_enforce_delivery_target_tenant_integrity.exs, priv/repo/migrations/20260820000001_add_apns_request_intent.exs, test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_enforce_delivery_target_tenant_integrity.exs, test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_enforce_delivery_target_tenant_integrity.exs, test/chimeway/install/migrations_test.exs, test/chimeway/install/golden_diff_test.exs</read_first>
  <behavior>
    - "Installer lists 37 ordered migrations and migration 037 renders exactly once in either public or chimeway-prefixed mode."
    - "Both golden trees add the nullable map/jsonb intent column to the existing target table and retain the prefix sentinel contract."
    - "The migration contains no raw-token, credential, rendered-payload, or provider-body storage field."
  </behavior>
  <action>Create prefix-aware canonical migration 037 by following the local helper/sentinel pattern from migrations 035/036; alter only `:chimeway_delivery_targets` and add nullable `:apns_request_intent, :map`. Its down path removes only that column. Add the generated public and prefixed golden files with host namespace and exact rendered prefix mode. Extend `@expected_slugs`, migration count/order, golden tree size, and `assert_migration_037_apns_intent_contract!/2`. Preserve the named tenant/attempt constraints from 036 and do not add a separate APNs target table, implementing the assumption-delta no-change decision.</action>
  <acceptance_criteria>
    - "`Migrations.list_templates/0` returns orders 1..37 with `add_apns_request_intent` last."
    - "Golden public and prefixed trees each contain exactly one migration 037 and no unresolved prefix sentinel."
    - "Golden contract assertions require the existing target table, nullable map field, and exactly one rendered storage-prefix attribute."
    - "No migration 037 content adds a token, credential, payload, response body, or tenant-derived schema identifier."
  </acceptance_criteria>
  <verify><automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/install/migrations_test.exs test/chimeway/install/golden_diff_test.exs --warnings-as-errors</automated></verify>
  <done>All copied installation modes render migration 037 with the same safe, nullable, prefix-correct target variant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove migration up/down safety and generated runtime persistence</name>
  <files>test/chimeway/install/prefix_contract_test.exs, test/chimeway/migration_contract_test.exs, test/chimeway/generated_prefixed_runtime_proof_test.exs</files>
  <read_first>AGENTS.md, .planning/phases/100-optional-apns-adapter/100-CONTEXT.md, .planning/phases/100-optional-apns-adapter/100-01-SUMMARY.md, priv/chimeway_migrations/037_add_apns_request_intent.exs, priv/repo/migrations/20260820000001_add_apns_request_intent.exs, test/chimeway/install/prefix_contract_test.exs, test/chimeway/migration_contract_test.exs, test/chimeway/generated_prefixed_runtime_proof_test.exs, test/support/generated_prefixed_runtime_case.ex</read_first>
  <behavior>
    - "Repository and copied migrations add/read a valid safe intent in public and prefixed schemas."
    - "Rollback after inserting Phase 99 target/attempt fixtures removes only the new column; re-up restores the schema while pre-existing target identity and tenant constraints remain valid."
    - "Generated-prefixed runtime proof expects 37 migrations and plans/loads a target with safe APNs intent through Chimeway.Repo."
  </behavior>
  <action>Extend prefix contract assertions for migration 037. Add isolated database tests that migrate through 036, seed tenant-qualified targets/attempts, apply 037, store and read a representative closed intent, roll 037 down, prove the target/attempt rows and 036 constraints survive, and migrate up again. Run this for the repository path and both generated fixture modes using the existing fixed schemas, never tenant-derived prefixes. Update generated-prefixed runtime count and add an APNs-intent target roundtrip without a token or network call.</action>
  <acceptance_criteria>
    - "Migration contract executes 036→037→036→037 in repository, generated public, and generated prefixed modes without data loss in Phase 99 columns."
    - "After rollback, the APNs intent column is absent while target and attempt relations plus named tenant foreign keys remain present."
    - "Generated prefixed runtime count is 37 and the intent is readable only from the configured `chimeway` schema."
  </acceptance_criteria>
  <verify><automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/install/prefix_contract_test.exs test/chimeway/migration_contract_test.exs test/chimeway/generated_prefixed_runtime_proof_test.exs --warnings-as-errors</automated></verify>
  <done>Migration 037 has executable up/down/up and public/prefixed runtime evidence with prior target data and constraints preserved.</done>
</task>

</tasks>

## Artifacts this phase produces

- Canonical copied migration file/module `priv/chimeway_migrations/037_add_apns_request_intent.exs` / `Chimeway.Repo.Migrations.AddApnsRequestIntent`.
- Golden public and prefixed fixture files `TIMESTAMP_add_apns_request_intent.exs` with rewritten host migration modules.
- Installer slug `add_apns_request_intent`, migration order/count 37, `assert_migration_037_apns_intent_contract!/2`, and repository/generated migration version constants.
- Migration rollback fixtures for safe intent, Phase 99 target/attempt preservation, and generated-prefixed runtime target intent.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Installer template → host database | Published migration is copied and executed under host-selected static storage mode. |
| Persisted intent → operator/runtime reads | The new map is durable and must remain a closed non-secret variant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-100-07 | Information Disclosure | migration 037 | high | mitigate | D-03/D-05 allow one safe intent map only; migration/source tests reject secret/payload/provider-body columns. |
| T-100-08 | Tampering | generated public/prefixed migration parity | high | mitigate | Golden equality, explicit prefix assertions, and three-mode DB up/down/up tests prevent storage-mode drift. |
| T-100-09 | Elevation of Privilege | storage prefix | medium | mitigate | Fixed `@chimeway_prefix` helper pattern; no tenant/environment input participates in relation qualification. |
| T-100-10 | Denial of Service | rollback | medium | mitigate | Down removes only the nullable variant column; tests seed and retain Phase 99 target/attempt rows and constraints. |
</threat_model>

<verification>
Run both task commands and `mix verify.install_golden`. Migration and rollback behavior is objectively machine-testable; no human verification applies.
</verification>

<success_criteria>
- All four migration representations agree on one nullable safe-intent column.
- Installer/golden counts advance to 37 with prefix and namespace parity.
- Up/down/up checks preserve existing target identity, attempt lineage, and tenant constraints.
</success_criteria>

<output>
Create `.planning/phases/100-optional-apns-adapter/100-02-SUMMARY.md` when done.
</output>
