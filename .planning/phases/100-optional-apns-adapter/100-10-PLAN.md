---
phase: 100-optional-apns-adapter
plan: 10
type: execute
wave: 11
depends_on: [100-09, 100-11]
files_modified:
  - lib/chimeway/apns/opaque_reference.ex
  - lib/chimeway/apns/request_intent.ex
  - lib/chimeway/apns/payload.ex
  - test/chimeway/apns/request_test.exs
  - test/chimeway/adapters/apns_test.exs
autonomous: true
gap_closure: true
requirements: [APNS-02, APNS-05]
must_haves:
  truths:
    - "D-05/D-07/APNS-02: durable RequestIntent construction, storage reload, and direct Payload construction accept the same closed opaque open-reference grammar."
    - "D-03/D-07/APNS-02: personal identifiers, URLs, control-bearing strings, empty values, and non-binaries are rejected before persistence or provider payload construction."
    - "D-08/APNS-05: caller-supplied collapse IDs contain only the closed APNs-header-safe character set and are at most 64 bytes before Transport.Request construction."
    - "D-08/APNS-05: collapse remains absent by default, the existing exact-scope derived value remains deterministic, and valid explicit values retain their value."
    - "Boundary validation is consistent for atom-key construction, string-key storage reload, and direct payload calls at the accepted maximum and one byte above it."
    - "Existing concurrent target execution and exact tenant/environment/topic/binding-revision invalidation regressions remain green after validation is tightened."
  assumptions:
    - statement: "The closed open-reference grammar is an internal Phase 100 transport contract with an `open-`/`open_` namespace (optionally `cw_`-prefixed) and an ASCII alphanumeric/underscore/hyphen body; this preserves existing opaque fixtures while rejecting unnamespaced identifiers and URL syntax."
      flagged: false
    - statement: "Nil collapse means header omission; nil open reference, empty strings, charlists, maps, and other representations are invalid at both construction boundaries."
      flagged: false
    - statement: "No collection ordering, pagination, schema migration, UI state, Apple-network, credential, or protected-open behavior is introduced by these pure validation gaps; those deterministic edge categories are irrelevant to this plan and existing phase/later-phase contracts remain authoritative."
      flagged: false
  prohibitions:
    - statement: "Personal identifiers, URLs, or control-bearing open references cannot cross the durable intent or direct provider-payload boundary."
      status: unverified
      flagged: true
    - statement: "A caller-controlled collapse value cannot inject control bytes into an APNs header."
      status: unverified
      flagged: true
  artifacts:
    - path: "lib/chimeway/apns/opaque_reference.ex"
      provides: "Single closed open-reference grammar shared by durable intent and direct payload construction"
      exports: ["Chimeway.APNS.OpaqueReference.valid?/1"]
    - path: "lib/chimeway/apns/request_intent.ex"
      provides: "Closed open-reference and APNs-header-safe collapse validation at durable construction/reload"
    - path: "lib/chimeway/apns/payload.ex"
      provides: "The same closed open-reference validation at the direct provider payload boundary"
    - path: "test/chimeway/apns/request_test.exs"
      provides: "Table-driven reference and collapse grammar boundary regressions"
    - path: "test/chimeway/adapters/apns_test.exs"
      provides: "Public adapter no-transport regression for invalid persisted open/collapse input"
  key_links:
    - from: "lib/chimeway/apns/request_intent.ex"
      to: "lib/chimeway/apns/opaque_reference.ex"
      via: "new/2 and from_storage/1 share one validator"
      pattern: "OpaqueReference.valid?"
    - from: "lib/chimeway/apns/payload.ex"
      to: "lib/chimeway/apns/opaque_reference.ex"
      via: "Payload.build/2 revalidates direct callers independently"
      pattern: "OpaqueReference.valid?"
    - from: "lib/chimeway/apns/request_intent.ex"
      to: "lib/chimeway/apns/transport.ex"
      via: "only validated collapse_id reaches Transport.Request.collapse_id"
      pattern: "collapse_id"
---

<objective>
Close the provider-bound input gaps with one consistent opaque-reference grammar and one closed collapse-header grammar, implementing D-03, D-05, D-07, and D-08 without changing target identity or the external APNs capability surface.

Purpose: Prevent host identifiers, URLs, and header-control bytes from becoming durable or crossing to APNs while preserving exact-target collapse derivation and direct-call defense in depth.
Output: A shared internal opaque-reference validator, hardened RequestIntent/Payload boundaries, and focused plus adapter-level executable regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/100-optional-apns-adapter/100-CONTEXT.md
@.planning/phases/100-optional-apns-adapter/100-RESEARCH.md
@.planning/phases/100-optional-apns-adapter/100-PATTERNS.md
@.planning/phases/100-optional-apns-adapter/100-VERIFICATION.md
@.planning/phases/100-optional-apns-adapter/COVERAGE.md
@.planning/phases/100-optional-apns-adapter/100-03-SUMMARY.md
@.planning/phases/100-optional-apns-adapter/100-09-SUMMARY.md
@lib/chimeway/apns/request_intent.ex
@lib/chimeway/apns/payload.ex
@lib/chimeway/adapters/apns.ex
@lib/chimeway/apns/transport.ex
@test/chimeway/apns/request_test.exs
@test/chimeway/adapters/apns_test.exs
</context>

<assumption_delta_decision>
Primary noun: the exact tenant/environment/topic/binding-revision target identity. Decision: `no-change`. The words "another" and "optional" describe cross-installation isolation and an opt-in transport, not a new durable identity; validation remains an attribute boundary on the existing target.
</assumption_delta_decision>

<edge_case_assumptions>
The 11 spec-less probe categories are resolved as follows: (1) nil open refs reject; (2) empty open refs reject; (3) non-binary representations reject; (4) exact accepted open-ref byte bound passes; (5) one-byte-over open ref rejects; (6) identifier forms reject; (7) URL forms reject; (8) C0/DEL controls reject; (9) atom-key construction, string-key reload, and direct payload validation agree; (10) nil collapse omits while empty/control/over-64 collapse rejects; (11) existing concurrency and exact-binding suites are regression gates. Schema, pagination, UI, credentials, and live-provider concerns introduce no new edge in this code-only closure.
</edge_case_assumptions>

<prohibition_recall>
Keep only the two descriptor-less Phase 100 safety prohibitions in `must_haves.prohibitions`: open references cannot carry identifying/routable data, and caller collapse input cannot carry provider-header control bytes. Do not introduce general privacy descriptors or later-phase protected-open rules.
</prohibition_recall>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tracer — reject unsafe references and collapse bytes before the APNs request seam</name>
  <reversibility rating="costly">Tightening accepted durable/reference input can reject previously accepted unsafe host values, but the change is localized and requires no schema or stored-data rewrite.</reversibility>
  <files>lib/chimeway/apns/opaque_reference.ex, lib/chimeway/apns/request_intent.ex, lib/chimeway/apns/payload.ex, test/chimeway/apns/request_test.exs, test/chimeway/adapters/apns_test.exs</files>
  <read_first>AGENTS.md, .planning/phases/100-optional-apns-adapter/100-CONTEXT.md, .planning/phases/100-optional-apns-adapter/100-VERIFICATION.md, .planning/phases/100-optional-apns-adapter/COVERAGE.md, .planning/phases/100-optional-apns-adapter/100-03-SUMMARY.md, .planning/phases/100-optional-apns-adapter/100-09-SUMMARY.md, lib/chimeway/apns/request_intent.ex, lib/chimeway/apns/payload.ex, lib/chimeway/adapters/apns.ex, lib/chimeway/apns/transport.ex, test/chimeway/apns/request_test.exs, test/chimeway/adapters/apns_test.exs, test/chimeway/apns/tracer_test.exs, test/chimeway/apns/result_test.exs</read_first>
  <behavior>
    - "Closed reference acceptance: `open-ref`, `open_opaque_ref`, and `cw_open_opaque_001` pass at RequestIntent.new/2, RequestIntent.from_storage/1, and Payload.build/2; the exact maximum byte representation passes and one byte above fails."
    - "Closed reference rejection: nil, empty, plain `user_123`, email-shaped identifiers, absolute URLs, scheme-relative URLs, whitespace, C0 bytes, CR, LF, DEL, non-ASCII, charlists, and maps fail consistently at every applicable construction boundary."
    - "Stored intent defense: from_storage/1 revalidates the closed format instead of trusting a map that was once persisted."
    - "Collapse header acceptance: nil remains omitted; 1-byte and 64-byte values made only from ASCII letters, digits, underscore, and hyphen pass unchanged; the existing derived Base64URL value remains deterministic and exact-scope sensitive."
    - "Collapse header rejection: empty, 65-byte, whitespace, punctuation outside the allowlist, CR, LF, NUL, other C0 bytes, DEL, non-ASCII, charlist, and map inputs return invalid_apns_request_intent."
    - "End-to-end no-I/O: a target carrying an invalid stored open reference or collapse value returns the existing bounded invalid-intent outcome before binding lookup/Transport.push/2; no fake-transport message is observed and no unsafe value appears in result or captured evidence."
    - "Regression: valid requests still carry the exact validated open reference and optional collapse ID into Transport.Request, while concurrent single-target execution and exact four-field invalidation remain unchanged."
  </behavior>
  <action>Start RED by expanding `request_test.exs` into table-driven accepted/rejected cases that invoke both `RequestIntent.new/2` and `Payload.build/2`, then exercise `RequestIntent.from_storage/1` with string keys. Add `Chimeway.APNS.OpaqueReference` as an internal data-only module with `@moduledoc false` and `valid?/1`; define a fully anchored ASCII grammar accepting the established `open-...`, `open_...`, and `cw_open_...` namespaces with an alphanumeric/underscore/hyphen body and the existing 256-byte ceiling. Reject any other representation rather than normalizing it. Replace both independent blacklist checks with this shared validator per D-05/D-07, while retaining title/body privacy validation separately. In RequestIntent, validate explicit and reloaded collapse IDs with a fully anchored ASCII `[A-Za-z0-9_-]` allowlist and the existing 1..64-byte APNs limit; keep nil omission and the existing exact-scope SHA-256/Base64URL derivation per D-08. Add adapter-level tests that feed malformed stored intent through public `APNS.deliver/2` and assert the existing bounded pre-provider result, zero lookup/transport messages, and no returned/captured unsafe sentinel. Preserve UUID, topic, environment, expiry, payload-size, exact lookup, outcome, evidence, and optional-Pigeon behavior. COVERAGE.md already classifies the relevant open-reference payload and collapse header surfaces as INTEGRATE; do not add, remove, or reclassify rows.</action>
  <acceptance_criteria>
    - "One `OpaqueReference.valid?/1` implementation is called by both RequestIntent and Payload; no duplicate open-reference blacklist remains."
    - "Table tests reject plain identifiers, email identifiers, URLs, CR/LF/NUL/DEL/control-bearing values, empty/nil, non-binaries, and over-bound values at both public construction paths; storage reload repeats the same checks."
    - "Explicit collapse IDs accept only the closed ASCII header grammar through 64 bytes; CR, LF, other controls, whitespace, disallowed punctuation, non-ASCII, empty, and 65-byte values fail before request construction."
    - "Derived collapse remains stable for identical occurrence/binding/environment/topic input and differs when any one scope field changes; default collapse remains nil."
    - "Public APNS delivery with malformed stored values invokes neither lookup nor transport and returns/persists no unsafe value."
    - "Focused request/adapter suites plus tracer/result/worker regressions pass under warnings-as-errors, including concurrency and exact-binding safety."
    - "The API coverage test passes with COVERAGE.md unchanged, confirming no unrelated external surface change."
  </acceptance_criteria>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/apns/request_test.exs test/chimeway/adapters/apns_test.exs test/chimeway/apns/tracer_test.exs test/chimeway/apns/result_test.exs test/chimeway/dispatch/target_worker_test.exs test/chimeway/apns/api_coverage_test.exs --warnings-as-errors &amp;&amp; bash scripts/verify-apns.sh</automated>
  </verify>
  <done>Opaque references are closed and consistent at durable/direct boundaries, unsafe collapse input cannot reach an APNs request header, and valid exact-target behavior remains green.</done>
</task>

</tasks>

## Artifacts this phase produces

- New internal file/module `lib/chimeway/apns/opaque_reference.ex` / `Chimeway.APNS.OpaqueReference` with `valid?/1`; no new public host API, configuration key, schema, migration, dependency, or external capability.
- Modified `Chimeway.APNS.RequestIntent.new/2` and `from_storage/1` closed reference/collapse validation.
- Modified `Chimeway.APNS.Payload.build/2` shared closed reference validation.
- Expanded ExUnit contracts in `test/chimeway/apns/request_test.exs` and `test/chimeway/adapters/apns_test.exs`.
- `.planning/phases/100-optional-apns-adapter/COVERAGE.md` is intentionally unchanged because the existing INTEGRATE rows already own these provider fields.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host intent → durable target | Untrusted open/collapse values must satisfy closed formats before persistence. |
| Direct payload caller → APNs JSON | Direct callers bypassing durable construction must meet the same opaque-reference contract. |
| Durable intent → Transport.Request headers | Caller-controlled collapse input becomes provider-bound only after header-safe validation. |

## STRIDE Threat Register

**Security gate:** OWASP ASVS Level 1 with `block_on: high`. Every high-severity mitigation is bound to Task 1's automated command and must pass before the outcome is `passed`; otherwise verification reports `gaps_found`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-100-10-01 | Information Disclosure | open reference in `RequestIntent`/`Payload` | high | mitigate | D-03/D-05/D-07 shared closed namespace/ASCII grammar plus identifier, URL, control, representation, and byte-bound tests at both boundaries. |
| T-100-10-02 | Tampering | `Transport.Request.collapse_id` | high | mitigate | D-08 anchored ASCII header allowlist and 1..64-byte checks reject CR/LF/C0/DEL before transport construction. |
| T-100-10-03 | Information Disclosure | direct `Payload.build/2` | high | mitigate | Direct payload construction independently calls the shared validator and has URL/identifier/control regression cases. |
| T-100-10-04 | Spoofing | exact target identity | medium | mitigate | Existing concurrent execution and exact tenant/environment/topic/binding-revision result suites rerun unchanged. |
| T-100-10-05 | Denial of Service | input representation/bounds | medium | mitigate | Nil, empty, non-binary, exact-bound, and over-bound cases fail deterministically without provider I/O. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe bounded target-specific APNs requests | 100-10 | COVERED | Closes unsafe provider-bound reference/header input while preserving target outcomes. |
| REQ | APNS-02 | Bounded payload with opaque one-time open reference | 100-10 | COVERED | Shared closed validator at durable, reload, and direct payload boundaries. |
| REQ | APNS-05 | Opt-in installation-safe collapse | 100-10 | COVERED | Explicit input header grammar plus derived/default regressions. |
| REQ | APNS-03, APNS-04, APNS-06 | Already-satisfied result, expiry, and operator-state contracts | 100-10 regression | COVERED | Focused tracer/result/worker commands prevent regression without fabricating new scope. |
| RESEARCH | — | Closed payload and provider-bound validation before I/O | 100-10 | COVERED | Shared reference grammar and collapse allowlist run before lookup/transport. |
| CONTEXT | D-03, D-05, D-07 | Non-disclosing durable and payload reference | 100-10 | COVERED | Identifier/URL/control values reject consistently. |
| CONTEXT | D-08 | Default-absent, bounded exact-target collapse | 100-10 | COVERED | Derived scope preserved; explicit input becomes header-safe. |
| CONTEXT | D-01, D-02, D-04, D-06, D-09..D-16 | Existing optionality, custody, expiry, and outcome decisions | 100-01..100-09; 100-10 regression | COVERED | No decision is reopened; focused regression commands preserve applicable seams. |

Deferred CrossWake protected-open work, hermetic twin work, physical-device proof, and FCM remain excluded exactly as recorded in CONTEXT.md. No research or context item is missing, and no schema-push surface applies.
</source_audit>

<verification>
Run the focused automated command, which includes `bash scripts/verify-apns.sh` after dependency Plan 100-11 has repaired that gate, then run `mix format --check-formatted` for the changed Elixir files. Record the verifier outcome only as `passed` or `gaps_found`; no UAT or human checkpoint applies.
</verification>

<success_criteria>
- Open references have one closed ASCII grammar at durable, reload, and direct payload boundaries.
- Identifiers, URLs, controls, invalid representations, and out-of-bound references never persist or enter APNs JSON.
- Explicit collapse input cannot carry CR/LF/control bytes or exceed the APNs bound before Transport.Request construction.
- Default and exact-scope derived collapse behavior, concurrency, expiry, and exact-binding invalidation remain green.
</success_criteria>

<output>
Create `.planning/phases/100-optional-apns-adapter/100-10-SUMMARY.md` when done.
</output>
