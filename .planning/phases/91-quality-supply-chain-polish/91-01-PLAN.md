---
phase: 91-quality-supply-chain-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .tool-versions
  - .github/workflows/ci.yml
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - ".tool-versions at repo root is the single toolchain source; the 14 single-pinned setup-beam jobs read it via `version-file: .tool-versions` + `version-type: strict` (QUAL-01, D-01/D-02)"
    - "The OTP {26,27} matrix leg (`test`) and the 1.17 floor leg (`test_floor_1_17`) retain explicit `elixir-version`/`otp-version` pins — they deliberately exercise non-canonical versions (D-03)"
    - "Cache keys are unchanged; they still derive from `steps.beam.outputs.elixir-version`/`otp-version` and auto-follow `.tool-versions` with no edit (D-04)"
  backstops:
    - statement: "Every converted job's Setup BEAM step resolves Elixir 1.19.5 and Erlang/OTP 27.3.4 identically in a real CI run (only observable in a live runner)"
      verification: backstop
  artifacts:
    - ".tool-versions"
    - ".github/workflows/ci.yml (14 converted setup-beam blocks)"
  key_links:
    - "erlef/setup-beam `version-file: .tool-versions` + `version-type: strict` ← `.tool-versions` full-patch pins"
  prohibitions:
    - ".tool-versions must not resolve to any Elixir/OTP other than today's exact 1.19.5 / 27.3.4 — no silent toolchain shift (QUAL-01, D-01)"
    - "The matrix `test` leg and the 1.17 `test_floor_1_17` leg must not be converted to version-file — they must keep their explicit pins (D-03)"
---

<objective>
Establish `.tool-versions` as the single canonical toolchain source of truth and convert every
single-pinned `setup-beam` job in `ci.yml` to read it via `version-file:` (QUAL-01, D-01/D-02).
This is the phase tracer: the highest-risk foundational change, proven end-to-end in a real CI run
before any expansion.

Purpose: eliminate the ~14-way duplication of hard-coded `elixir-version:"1.19"`/`otp-version:"27"`
across `ci.yml` so the toolchain is declared once and cannot drift job-to-job.

Output: new `.tool-versions` file (full-patch strict pins) + 14 converted setup-beam blocks in
`ci.yml`; the matrix and 1.17-floor legs left pinned (D-03); cache keys untouched (D-04).
</objective>

<artifacts_produced>
This plan creates / modifies the following symbols and files:
- **NEW file** `.tool-versions` (repo root) — `erlang 27.3.4` + `elixir 1.19.5-otp-27` (full-patch strict pins).
- **MODIFIED** `.github/workflows/ci.yml` — 14 `setup-beam` blocks gain `version-file: .tool-versions` + `version-type: strict` inputs (replacing inline `elixir-version:"1.19"`/`otp-version:"27"` pairs).
- **UNCHANGED (asserted)** — `test` matrix leg (`elixir-version: ${{ matrix.elixir }}`, ci.yml:234), `test_floor_1_17` (`elixir-version: "1.17"`, ci.yml:1141), and all cache-key lines (already `steps.beam.outputs.*`).
</artifacts_produced>

<flagged_assumptions>
The deterministic edge-probe classifier left QUAL-01 `unresolved` (category: concurrency; probe:
"If interrupted or run in parallel, what is guaranteed?") because QUAL-01 is a config/CI requirement,
not a product behavior. Surfaced here (no silent drop):

- **[ASSUMPTION — QUAL-01 concurrency/parallel]** `.tool-versions` is read read-only by each job's
  `setup-beam` step at job start; there is no shared mutable state and no ordering coupling, so all
  parallel jobs resolve the identical toolchain regardless of interleaving or interruption. A job
  that starts mid-run reads the same committed file. **Verification is a real CI run** (backstop):
  every converted job's Setup BEAM log must show `Elixir 1.19.5` / `Erlang/OTP 27.3.4`. If a newer
  1.19.x / 27.x has published since research, re-capture the exact strings from the latest green
  `main` run before pinning (RESEARCH Open Question 1 / Assumption A1).
</flagged_assumptions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/91-quality-supply-chain-polish/91-CONTEXT.md
@.planning/phases/91-quality-supply-chain-polish/91-RESEARCH.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Create .tool-versions and convert the `lint` job (end-to-end tracer)</name>
  <files>.tool-versions, .github/workflows/ci.yml</files>
  <read_first>
    - `.planning/phases/91-quality-supply-chain-polish/91-RESEARCH.md` — Pitfall 1 (★ TOP RISK: `version-file` forces `version-type: strict`, which is an exact-key map lookup, NOT "latest matching patch") and Code Example 1 (copy-exact `.tool-versions` + converted block syntax).
    - `.github/workflows/ci.yml` lines 62-109 — the `lint` job: setup-beam block at 69-73 (`id: beam`, `elixir-version: "1.19"`, `otp-version: "27"`) and cache key at 80 (already reads `steps.beam.outputs.*`).
    - `.github/workflows/ci.yml` lines 231-235 — the `test` matrix block, to see the pinned leg that must NOT be converted (D-03).
  </read_first>
  <action>
    Create `.tool-versions` at the repo root with exactly two lines — `erlang 27.3.4` and
    `elixir 1.19.5-otp-27` — the full-patch strict pins that reproduce today's loose `1.19`/`27`
    resolution (D-01; values from green main run 30512806893, 2026-07-30). Before writing, re-capture
    the current resolved strings from the latest green `main` run's Setup BEAM log (`gh run list
    --branch main --workflow ci.yml --status success` → read a converted-analog job's `Using Elixir
    X.Y.Z` / `Installed Erlang/OTP N.N.N`); use those exact strings if a newer patch has landed.
    Do NOT write loose partial lines (`erlang 27`, `elixir 1.19-otp-27`) — under forced-strict they
    hard-fail with `Requested strict … version not found` (Pitfall 1).

    Then convert ONLY the `lint` job's setup-beam block (ci.yml:69-73): keep `uses: erlef/setup-beam@8251c48667b97e88a0a24ec512f5b72a039fcea7` and `id: beam`, replace the `elixir-version`/`otp-version` input pair with two inputs — `version-file: .tool-versions` and `version-type: strict` (strict is mandatory with version-file; the action errors without it). Leave the cache key at line 80 untouched (D-04). This is the thinnest end-to-end slice: new file → one converted job → proven to resolve identical versions.
  </action>
  <acceptance_criteria>
    - Source: `.tool-versions` contains a line `erlang 27.3.4` and a line `elixir 1.19.5-otp-27` (or the exact strings re-captured from the latest green main run).
    - Source: the `lint` job block (ci.yml, between `  lint:` and `  verify_gates:`) contains `version-file: .tool-versions` and `version-type: strict`, and no longer contains an inline `elixir-version` input.
    - Command: `sed -n '/^  lint:/,/^  verify_gates:/p' .github/workflows/ci.yml | grep -q 'version-file: .tool-versions'` exits 0.
    - Behavior (backstop, CI): the `lint` job's Setup BEAM step logs `Elixir 1.19.5` and `Erlang/OTP 27.3.4` on a real runner.
  </acceptance_criteria>
  <reversibility rating="reversible">Pure config; reverting the two-line file and one block restores inline pins with zero data impact.</reversibility>
  <verify>
    <automated>grep -qx 'erlang 27.3.4' .tool-versions && grep -qx 'elixir 1.19.5-otp-27' .tool-versions && sed -n '/^  lint:/,/^  verify_gates:/p' .github/workflows/ci.yml | grep -q 'version-file: .tool-versions' && sed -n '/^  lint:/,/^  verify_gates:/p' .github/workflows/ci.yml | grep -q 'version-type: strict'</automated>
    <human-check>Terminal proof (phase gate): push a branch and confirm the `lint` job's "Setup BEAM" step resolves Elixir 1.19.5 / Erlang/OTP 27.3.4 — this is the tracer's end-to-end validation and MUST be green before the expansion in Task 2 is trusted. If `actionlint` is installed, `actionlint .github/workflows/ci.yml` should also exit 0.</human-check>
  </verify>
  <done>`.tool-versions` exists with the two strict full-patch pins; the `lint` job reads it via `version-file:` + `version-type: strict`; a real CI run proves it resolves the identical toolchain the inline pins did.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fan out — convert the remaining 13 single-pinned jobs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.planning/phases/91-quality-supply-chain-polish/91-RESEARCH.md` Code Example 1 — the identical converted-block shape to apply to every job.
    - `.github/workflows/ci.yml` — the 13 setup-beam blocks still carrying inline pins at lines 130-134 (`verify_gates`), 172-176 (`verify_docs`), 302-306 (`verify_example`), 379-383 (`verify_runtime_prefix`), 435-439 (`verify_journeys`), 513-517 (`verify_mailglass`), 589-593 (`verify_accrue`), 662-666 (`verify_inbox`), 748-752 (`verify_threadline`), 829-833 (`verify_sigra`), 909-913 (`verify_admin`), 1019-1024 (`install_golden_contract`), 1089-1093 (`nightly_cold_build`).
    - `.github/workflows/ci.yml` lines 1138-1142 — `test_floor_1_17`'s pinned 1.17 block that must NOT be converted (D-03).
  </read_first>
  <action>
    Apply the exact same conversion proven in Task 1 to each of the 13 remaining single-pinned jobs
    (D-02): in each block replace the inline `elixir-version`/`otp-version` input pair with
    `version-file: .tool-versions` + `version-type: strict`, keeping the `uses:` SHA and `id: beam`
    unchanged and every cache-key line untouched (D-04). Do NOT touch the `test` matrix leg
    (`elixir-version: ${{ matrix.elixir }}`, line 234) or the `test_floor_1_17` floor leg
    (`elixir-version: "1.17"`, line 1141) — those deliberately exercise non-canonical versions and are
    outside the QUAL-01 de-duplication (D-03). After conversion the workflow must carry exactly 14
    `version-file: .tool-versions` inputs (the tracer's `lint` + these 13).
  </action>
  <acceptance_criteria>
    - Command: `grep -vE '^\s*#' .github/workflows/ci.yml | grep -c 'version-file: .tool-versions'` prints `14`.
    - Command: `grep -vE '^\s*#' .github/workflows/ci.yml | grep -c 'version-type: strict'` prints `14`.
    - Source (D-03 intact): `grep -q 'elixir-version: ${{ matrix.elixir }}' .github/workflows/ci.yml` exits 0 AND `grep -q 'elixir-version: "1.17"' .github/workflows/ci.yml` exits 0.
    - Source (D-04 intact): cache-key lines still contain `steps.beam.outputs.elixir-version` (unchanged).
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -vE '^\s*#' .github/workflows/ci.yml | grep -c 'version-file: .tool-versions')" -eq 14 && test "$(grep -vE '^\s*#' .github/workflows/ci.yml | grep -c 'version-type: strict')" -eq 14 && grep -q 'elixir-version: ${{ matrix.elixir }}' .github/workflows/ci.yml && grep -q 'elixir-version: "1.17"' .github/workflows/ci.yml</automated>
    <human-check>Phase gate: a green push-tier CI run where all 14 converted jobs' Setup BEAM logs show identical Elixir 1.19.5 / OTP 27.3.4, and the matrix + floor legs still run their non-canonical versions.</human-check>
  </verify>
  <done>All 14 single-pinned jobs read `.tool-versions` via `version-file:` + `version-type: strict`; the matrix and 1.17-floor legs keep their explicit pins; cache keys are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → CI runner | `.tool-versions` (repo-controlled config) is read by `setup-beam` at job start to select the installed toolchain |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-91-01 | Repudiation / silent failure | `.tool-versions` ↔ setup-beam version resolution | high | mitigate | Pin exact full-patch versions captured from a green `main` run; `version-type: strict` fails LOUDLY (`not found in version list`) rather than silently downgrading, and the phase-gate CI run confirms every job logs 1.19.5 / 27.3.4 (QUAL-01) |
| T-91-02 | Tampering | `.tool-versions` committed to repo | low | accept | Same review + branch-protection surface as any repo file; no additional exposure introduced |
</threat_model>

<verification>
- `.tool-versions` holds the two strict full-patch pins reproducing today's resolved toolchain.
- Exactly 14 setup-beam jobs carry `version-file: .tool-versions` + `version-type: strict`.
- The `test` matrix and `test_floor_1_17` floor legs retain explicit pins (D-03); cache keys unchanged (D-04).
- Terminal proof: a green push-tier CI run showing identical resolved versions across all converted jobs.
</verification>

<success_criteria>
QUAL-01 satisfied: `.tool-versions` is the single toolchain source feeding `setup-beam`'s
`version-file:`; cache keys derive from its resolved versions rather than duplicated hard-coded
strings; the matrix and 1.17-floor legs remain deliberately pinned.
</success_criteria>

<output>
Create `.planning/phases/91-quality-supply-chain-polish/91-01-SUMMARY.md` when done.
</output>
