---
phase: 35
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - test/support/installer_fixture.ex
  - test/chimeway/install/golden_diff_test.exs
  - test/chimeway/install/idempotency_test.exs
  - test/fixtures/installer_golden/STDOUT.txt
  - test/fixtures/installer_golden/tree/priv/repo/migrations/
  - mix.exs
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
autonomous: true
requirements:
  - INST-02
must_haves:
  truths:
    - Committed golden fixture at test/fixtures/installer_golden/ with STDOUT.txt and 31-file tree (D-11).
    - Idempotency contract proves second run zero diff and unchanged stdout (D-12).
    - mix ci.install_golden alias runs both contract tests in single invocation (D-13).
    - GitHub Actions install_golden_contract job path-gated on installer surfaces; always runs on push to main (D-13).
    - MIX_INSTALLER_ACCEPT_GOLDEN=1 refresh gate documented and functional.
  artifacts:
    - path: test/support/installer_fixture.ex
      provides: "Tmp host scaffold, subprocess runner, timestamp/path normalizers, golden refresh"
    - path: test/fixtures/installer_golden/
      provides: "Committed expected first-run tree + stdout (D-11)"
    - path: test/chimeway/install/golden_diff_test.exs
      provides: "Golden-diff contract — INST-02"
    - path: test/chimeway/install/idempotency_test.exs
      provides: "Second-run idempotency contract — INST-02"
    - path: mix.exs
      provides: "ci.install_golden alias"
    - path: .github/workflows/ci.yml
      provides: "Path-gated install_golden_contract job"
  key_links:
    - from: test/chimeway/install/golden_diff_test.exs
      to: test/fixtures/installer_golden/
      via: "Normalized tree + STDOUT diff after subprocess mix chimeway.gen.migrations"
    - from: .github/workflows/ci.yml
      to: mix ci.install_golden
      via: "Conditional job step on installer path changes"
    - from: MIX_INSTALLER_ACCEPT_GOLDEN=1
      to: test/fixtures/installer_golden/
      via: "Maintainer refresh gate for intentional template changes"
---

<objective>
Prove INST-02 with golden-diff and idempotency contract tests, wire `mix ci.install_golden`, and add path-gated CI so installer regressions cannot merge silently (D-11, D-12, D-13).

Golden fixture uses sigra-style committed tree under `test/fixtures/installer_golden/` (not README embed — 31 full migration bodies). Timestamp prefixes normalized to `TIMESTAMP` before diff. Maintainer refresh via `MIX_INSTALLER_ACCEPT_GOLDEN=1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-installer-task/35-CONTEXT.md
@.planning/phases/35-installer-task/35-RESEARCH.md
@.planning/phases/35-installer-task/35-PATTERNS.md
@.planning/phases/35-installer-task/35-VALIDATION.md
@mix.exs
@.github/workflows/ci.yml
@/Users/jon/projects/sigra/test/support/install_fixture.ex
@/Users/jon/projects/mailglass/test/support/installer_fixture_helpers.ex
</context>

<tasks>

<task type="auto">
  <name>Task 35-03-01: Create installer test harness (Chimeway.Test.InstallerFixture)</name>
  <files>test/support/installer_fixture.ex</files>
  <read_first>
    - /Users/jon/projects/sigra/test/support/install_fixture.ex
    - /Users/jon/projects/mailglass/test/support/installer_fixture_helpers.ex
    - .planning/phases/35-installer-task/35-PATTERNS.md §2.4
    - mix.exs elixirc_paths(:test) includes test/support
  </read_first>
  <action>
    Create `test/support/installer_fixture.ex` module `Chimeway.Test.InstallerFixture`:

    **Constants:**
    - `@golden_dir Path.expand("../../fixtures/installer_golden", __DIR__)` 
    - `@accept_golden_env "MIX_INSTALLER_ACCEPT_GOLDEN"`

    **Functions:**
    - `new_fixture_root!/1` — unique dir under `System.tmp_dir!()/chimeway_installer_*`
    - `scaffold_host!/1` — write `mix.exs` (`app: :installer_host`, path dep to chimeway repo root), `config/config.exs` (`config :chimeway, repo: InstallerHost.Repo`), empty `priv/repo/migrations/`
    - `run_install!/2` — `System.cmd("mix", ["chimeway.gen.migrations"], cd: root, env: [{"MIX_ENV","dev"}], stderr_to_stdout: true)`; return `{output, status}`
    - `snapshot_migrations_tree!/1` — list `priv/repo/migrations/*.exs` with contents
    - `normalize_content/1` — replace `\r\n` → `\n`; regex `~r/\b\d{14}(?=_[a-z0-9_]+\.exs)/` → `TIMESTAMP`; normalize absolute tmp paths → `<TMP_PATH>`
    - `normalize_stdout/1` — same timestamp + path normalization on stdout
    - `normalize_tree/1` — map of relative path → normalized content; rename files to `TIMESTAMP_{slug}.exs` in keys
    - `accept_golden_refresh?/0` — `System.get_env(@accept_golden_env) == "1"`
    - `write_golden!/2` — when refresh enabled, write `STDOUT.txt` and `tree/priv/repo/migrations/*` under `@golden_dir`
    - `load_golden_tree/0`, `load_golden_stdout/0`
    - `assert_tree_equal/2` — actionable diff on mismatch (missing/extra paths + content diff)

    Tag guidance: consumers use `@moduletag :installer`, `async: false`.
  </action>
  <acceptance_criteria>
    - File `test/support/installer_fixture.ex` exists
    - `grep -c 'MIX_INSTALLER_ACCEPT_GOLDEN' test/support/installer_fixture.ex` ≥ 1
    - `grep -c 'normalize' test/support/installer_fixture.ex` ≥ 2
    - `grep -c 'chimeway.gen.migrations' test/support/installer_fixture.ex` ≥ 1
    - `mix compile --warnings-as-errors` succeeds
  </acceptance_criteria>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 35-03-02: Capture golden fixture and implement golden_diff_test.exs</name>
  <files>test/chimeway/install/golden_diff_test.exs, test/fixtures/installer_golden/STDOUT.txt, test/fixtures/installer_golden/tree/priv/repo/migrations/</files>
  <read_first>
    - test/support/installer_fixture.ex
    - /Users/jon/projects/sigra/test/sigra/install/golden_diff_test.exs
    - .planning/phases/35-installer-task/35-RESEARCH.md §7
    - .planning/phases/35-installer-task/35-CONTEXT.md D-06, D-09, D-10, D-11
  </read_first>
  <action>
    Create `test/chimeway/install/golden_diff_test.exs`:
    - `use ExUnit.Case, async: false`
    - `@moduletag :installer`
    - `@moduledoc` documenting refresh: `MIX_INSTALLER_ACCEPT_GOLDEN=1 mix test test/chimeway/install/golden_diff_test.exs --warnings-as-errors`

    **Test "first run matches golden fixture":**
    1. `root = InstallerFixture.new_fixture_root!("golden") |> InstallerFixture.scaffold_host!()`
    2. `{stdout, 0} = InstallerFixture.run_install!(root, [])`
    3. `tree = InstallerFixture.snapshot_migrations_tree!(root) |> InstallerFixture.normalize_tree()`
    4. `stdout = InstallerFixture.normalize_stdout(stdout)`
    5. If `accept_golden_refresh?()` → `InstallerFixture.write_golden!(tree, stdout)` and pass
    6. Else assert tree == `load_golden_tree()` and stdout == `load_golden_stdout()`

    **Additional assertions (even during refresh capture):**
    - Exactly 31 migration files
    - No file contains `Chimeway.Repo.Migrations` (D-06)
    - No slug `create_oban_jobs_tables` (D-10)
    - Each file matches `# chimeway_migration: {slug}` for its slug (D-09)

    **Capture committed fixture:**
    Run once with `MIX_INSTALLER_ACCEPT_GOLDEN=1 mix test test/chimeway/install/golden_diff_test.exs --warnings-as-errors` and commit:
    - `test/fixtures/installer_golden/STDOUT.txt`
    - `test/fixtures/installer_golden/tree/priv/repo/migrations/TIMESTAMP_*.exs` (31 files)

    Re-run test **without** env var — must pass against committed fixture.
  </action>
  <acceptance_criteria>
    - `test/fixtures/installer_golden/STDOUT.txt` exists
    - `ls test/fixtures/installer_golden/tree/priv/repo/migrations/*.exs | wc -l` outputs **31**
    - `grep -r 'Chimeway.Repo.Migrations' test/fixtures/installer_golden/tree/` returns no matches
    - `grep -r 'create_oban_jobs_tables' test/fixtures/installer_golden/` returns no matches
    - `grep -r '^# chimeway_migration:' test/fixtures/installer_golden/tree/ | wc -l` outputs **31**
    - `mix test test/chimeway/install/golden_diff_test.exs --warnings-as-errors` exits 0 without MIX_INSTALLER_ACCEPT_GOLDEN
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/install/golden_diff_test.exs --warnings-as-errors</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 35-03-03: Implement idempotency_test.exs (D-12)</name>
  <files>test/chimeway/install/idempotency_test.exs</files>
  <read_first>
    - test/support/installer_fixture.ex
    - /Users/jon/projects/mailglass/test/mailglass/install/install_idempotency_test.exs
    - .planning/phases/35-installer-task/35-CONTEXT.md D-07, D-08, D-12
  </read_first>
  <action>
    Create `test/chimeway/install/idempotency_test.exs`:
    - `use ExUnit.Case, async: false`
    - `@moduletag :installer`

    **Test "second run produces no fixture diff":**
    1. Scaffold tmp host via InstallerFixture
    2. First `run_install!` — assert status 0
    3. `before = snapshot |> normalize_tree`
    4. Second `run_install!` — capture stdout, assert status 0
    5. `after = snapshot |> normalize_tree`
    6. `assert before == after`
    7. Assert file count == 31 before and after
    8. Assert second stdout (normalized) contains 31 lines matching `unchanged priv/repo/migrations/`
    9. Assert second stdout contains **no** `created priv/repo/migrations/`

    Do **not** port mailglass managed-block drift tests — copy-only installer (D-03).
  </action>
  <acceptance_criteria>
    - File `test/chimeway/install/idempotency_test.exs` exists
    - `mix test test/chimeway/install/idempotency_test.exs --warnings-as-errors` exits 0
    - Test asserts 31 unchanged lines on second run
    - Test asserts zero `created` lines on second run
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/install/idempotency_test.exs --warnings-as-errors</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 35-03-04: Add mix ci.install_golden alias (D-13)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs aliases section
    - .planning/phases/35-installer-task/35-PATTERNS.md §2.8
    - /Users/jon/projects/mailglass/mix.exs (single mix test invocation note)
  </read_first>
  <action>
    Add to `mix.exs` `aliases/0`:

    ```elixir
    "ci.install_golden": [
      "test test/chimeway/install/golden_diff_test.exs test/chimeway/install/idempotency_test.exs --warnings-as-errors"
    ]
    ```

    **Do not** add `ci.install_golden` to default `ci:` list (keep fast core gate — D-13, RESEARCH §8).

    Verify alias runs both files in one invocation (Mix test deduplication footgun).
  </action>
  <acceptance_criteria>
    - `grep -c 'ci.install_golden' mix.exs` ≥ 1
    - `grep 'ci:' mix.exs | grep -c install_golden` outputs **0** (not in default ci)
    - `mix ci.install_golden` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix ci.install_golden</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 35-03-05: Add path-gated install_golden_contract CI job (D-13)</name>
  <files>.github/workflows/ci.yml, CONTRIBUTING.md</files>
  <read_first>
    - .github/workflows/ci.yml (existing lint/test jobs)
    - /Users/jon/projects/sigra/.github/workflows/ci.yml (install_golden_contract pattern)
    - .planning/phases/35-installer-task/35-RESEARCH.md §8
    - CONTRIBUTING.md (CI table if present)
  </read_first>
  <action>
    Add job `install_golden_contract` to `.github/workflows/ci.yml`:

    ```yaml
    install_golden_contract:
      name: Installer golden + idempotency contract
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
          with:
            fetch-depth: 0
        - name: Detect installer-related changes (PRs only)
          id: detect
          shell: bash
          run: |
            set -euo pipefail
            if [ "${{ github.event_name }}" != "pull_request" ]; then
              echo "run=true" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            git fetch origin "${{ github.base_ref }}" --depth=1
            if git diff --name-only "origin/${{ github.base_ref }}...HEAD" | grep -qE '^priv/chimeway_migrations/|^lib/mix/tasks/chimeway\.gen\.migrations\.ex|^lib/chimeway/install/|^test/chimeway/install/|^test/fixtures/installer_golden/|^test/support/installer_fixture\.ex|^mix\.exs$'; then
              echo "run=true" >> "$GITHUB_OUTPUT"
            else
              echo "run=false" >> "$GITHUB_OUTPUT"
            fi
        - uses: erlef/setup-beam@8251c48667b97e88a0a24ec512f5b72a039fcea7
          if: steps.detect.outputs.run == 'true'
          with:
            elixir-version: "1.17"
            otp-version: "27"
        - uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
          if: steps.detect.outputs.run == 'true'
          with:
            path: |
              deps
              _build
            key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
            restore-keys: |
              ${{ runner.os }}-mix-
        - run: |
            mix local.rebar --force
            mix local.hex --force
            mix deps.get
          if: steps.detect.outputs.run == 'true'
        - run: mix ci.install_golden
          if: steps.detect.outputs.run == 'true'
          env:
            MIX_ENV: test
    ```

    **No Postgres service** — file-generation tests only.

    Update `CONTRIBUTING.md` CI table with row: `mix ci.install_golden` — installer golden-diff + idempotency contract (path-gated in CI).
  </action>
  <acceptance_criteria>
    - `grep -c 'install_golden_contract' .github/workflows/ci.yml` ≥ 1
    - `grep -c 'mix ci.install_golden' .github/workflows/ci.yml` ≥ 1
    - `grep -c 'priv/chimeway_migrations' .github/workflows/ci.yml` ≥ 1
    - `grep -c 'fetch-depth: 0' .github/workflows/ci.yml` ≥ 1
    - `grep -c 'ci.install_golden' CONTRIBUTING.md` ≥ 1 (if file exists; else add minimal CI section)
    - `mix ci.install_golden` exits 0 locally
    - `mix ci` exits 0 (default gate unchanged)
  </acceptance_criteria>
  <verify>
    <automated>mix ci.install_golden && mix ci</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repository | Runs contract tests; no production secrets |
| Golden fixture → regression gate | Committed expected output — tampering breaks CI |
| MIX_INSTALLER_ACCEPT_GOLDEN=1 | Maintainer-only refresh; must never be set in CI job |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-03-01 | Tampering | Golden fixture bypass | High | mitigate | CI never sets MIX_INSTALLER_ACCEPT_GOLDEN; test fails on drift unless maintainer locally refreshes and commits |
| T-35-03-02 | Repudiation | Silent installer regression | Medium | mitigate | Path-gated job includes priv/chimeway_migrations/, test/fixtures/installer_golden/, mix.exs alias changes |
| T-35-03-03 | Denial of Service | Golden fixture size in PRs | Low | accept | 31 files expected; document refresh procedure in test @moduledoc |
| T-35-03-04 | Information Disclosure | Tmp host paths in STDOUT.txt | Low | mitigate | Normalize to `<TMP_PATH>` before commit; strip absolute paths |
| T-35-03-05 | Spoofing | CI job skipped on template change | High | mitigate | Path gate regex includes priv/chimeway_migrations/, lib/chimeway/install/, test/chimeway/install/, installer_fixture.ex, mix.exs |
| T-35-03-06 | Elevation of Privilege | ACCEPT_GOLDEN in CI env | High | mitigate | Do not pass MIX_INSTALLER_ACCEPT_GOLDEN in workflow env block |

**Block gate:** T-35-03-01, T-35-03-05, T-35-03-06 are HIGH — CI must run golden tests without refresh env; path gate must cover all installer surfaces.
</threat_model>

<verification>
1. `mix ci.install_golden`
2. `mix ci` (full default gate)
3. `MIX_INSTALLER_ACCEPT_GOLDEN=1 mix test test/chimeway/install/golden_diff_test.exs --warnings-as-errors` then re-run without env — both green after commit
4. Manual: `mix hex.build --unpack --output /tmp/chimeway_verify && ls /tmp/chimeway_verify/priv/chimeway_migrations | wc -l` expect 31
</verification>

<success_criteria>
- Golden fixture committed with 31 normalized migration files + STDOUT.txt (D-11)
- Idempotency test proves second run zero diff + 31× unchanged stdout (D-12)
- `mix ci.install_golden` alias runs both contracts (D-13)
- Path-gated `install_golden_contract` CI job wired; push to main always runs (D-13)
- INST-02 satisfied; Phase 35 complete
- guides/introduction/installation.md and README still untouched (D-14)
</success_criteria>

<output>
After completion, create `.planning/phases/35-installer-task/35-03-SUMMARY.md` via orchestration.
</output>
