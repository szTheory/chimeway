---
phase: 21-template-versioning-rendering-contracts
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - lib/chimeway/trigger.ex
  - lib/chimeway/deliveries.ex
  - lib/chimeway/delivery_planning.ex
  - test/chimeway/rendering/render_identity_integration_test.exs
autonomous: true
requirements: [TMPL-01, TMPL-02]
must_haves:
  truths:
    - "Structured render inputs persist once per notification."
    - "New canonical delivery rows are created with render identity already present."
    - "Repeated planning preserves the same render identity for the same notification and channel."
  artifacts:
    - path: "lib/chimeway/trigger.ex"
      provides: "Trigger-time render assigns persistence"
    - path: "lib/chimeway/delivery_planning.ex"
      provides: "Planning-time render identity insertion and sync"
    - path: "lib/chimeway/deliveries.ex"
      provides: "Canonical delivery helpers for render identity persistence"
  key_links:
    - from: "lib/chimeway/trigger.ex"
      to: "lib/chimeway/rendering.ex"
      via: "one-time render declaration resolution per recipient"
      pattern: "resolve_declaration"
    - from: "lib/chimeway/delivery_planning.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "insert path carries render_key and render_version"
      pattern: "plan_delivery|apply_render_identity"
---

<objective>
Persist render inputs once and stamp canonical delivery rows with render identity during planning.

Purpose: complete the durable wiring for D-01, D-02, D-09, D-10, and D-12 after the storage and declaration contracts exist.
Output: trigger-time `render_assigns` capture, render-aware delivery insert helpers, and planning regression coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-template-versioning-rendering-contracts/21-CONTEXT.md
@.planning/phases/21-template-versioning-rendering-contracts/21-VALIDATION.md
@.planning/phases/21-template-versioning-rendering-contracts/21-01-PLAN.md
@lib/chimeway/trigger.ex
@lib/chimeway/deliveries.ex
@lib/chimeway/delivery_planning.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist render assigns once and write render identity during planning</name>
  <files>lib/chimeway/trigger.ex, lib/chimeway/deliveries.ex, lib/chimeway/delivery_planning.ex, test/chimeway/rendering/render_identity_integration_test.exs</files>
  <behavior>
    - Test 1: `notifications_attrs/4` stores `render_assigns` once per notification and keeps `metadata` as a derived compatibility projection.
    - Test 2: planning resolves per-channel render identity before `plan_delivery/3` inserts new rows.
    - Test 3: repeated planning reuses the same row while preserving the same render identity for the notification/channel pair.
  </behavior>
  <action>Implement D-09, D-10, and D-12 by calling `Chimeway.Rendering.resolve_declaration/4` once per recipient in `Chimeway.Trigger.notifications_attrs/4`, persisting `render_assigns` on the notification row, and keeping `metadata` as the sanitized compatibility projection. Extend `Chimeway.Deliveries.plan_delivery/3` to accept `render_key` and `render_version` on initial insert, keep `apply_render_identity/2` only as the idempotent sync helper for reused rows, and wire `Chimeway.DeliveryPlanning.plan_one_channel/5` so canonical rows get durable render identity before policy evaluation, deferral, or digest accumulation.</action>
  <verify>
    <automated>mix test test/chimeway/rendering/render_identity_integration_test.exs test/chimeway/orchestration/delivery_planning_test.exs --trace</automated>
  </verify>
  <done>Trigger persistence and delivery planning carry stable render identity without late adapter-time inference or mutable host-data lookups.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trigger input -> notification row | Host inputs become durable render assigns |
| planning -> canonical delivery row | Per-channel render identity crosses into executable delivery storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | T | `lib/chimeway/trigger.ex` | mitigate | Persist `render_assigns` once at trigger time per D-09 so later workers and adapters do not re-read mutable host state. |
| T-21-06 | E | `lib/chimeway/delivery_planning.ex` | mitigate | Resolve render identity before canonical row creation and restrict `apply_render_identity/2` to reused-row synchronization only. |
| T-21-07 | R | `test/chimeway/rendering/render_identity_integration_test.exs` | mitigate | Prove repeated planning preserves stable per-channel identity instead of drifting across retries. |
</threat_model>

<verification>
- `mix test test/chimeway/rendering/render_identity_integration_test.exs test/chimeway/orchestration/delivery_planning_test.exs --trace`
</verification>

<success_criteria>
- `render_assigns` persist once on notifications.
- New delivery rows are created with `render_key` and `render_version` already populated.
- Reused delivery rows stay synchronized to the same durable render identity.
</success_criteria>

<output>
After completion, create `.planning/phases/21-template-versioning-rendering-contracts/21-02-SUMMARY.md`
</output>
