---
phase: 21-template-versioning-rendering-contracts
plan: 04
type: execute
wave: 3
depends_on: [21-02, 21-03]
files_modified:
  - lib/chimeway/deliveries.ex
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/traces.ex
  - lib/chimeway/traces/explanation.ex
  - test/chimeway/orchestration/delivery_planning_test.exs
  - test/chimeway/integration/delivery_lifecycle_test.exs
autonomous: true
requirements: [TMPL-01, TMPL-02]
must_haves:
  truths:
    - "Canonical delivery rows hold validated channel render output before dispatch."
    - "Adapters consume pre-rendered delivery content without late notifier or renderer callbacks."
    - "Explainability surfaces show render identity without leaking rendered body content."
  artifacts:
    - path: "lib/chimeway/deliveries.ex"
      provides: "Render-data persistence helper"
    - path: "lib/chimeway/delivery_planning.ex"
      provides: "Planning-time render-data materialization"
    - path: "lib/chimeway/traces/explanation.ex"
      provides: "Payload-safe render identity projection"
  key_links:
    - from: "lib/chimeway/delivery_planning.ex"
      to: "lib/chimeway/rendering.ex"
      via: "planning calls shared render pipeline before dispatch"
      pattern: "render_delivery"
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/traces/explanation.ex"
      via: "render identity projection without render bodies"
      pattern: "render_key|render_version"
---

<objective>
Materialize validated render output on canonical delivery rows and keep operator traces payload-safe.

Purpose: implement D-08, D-09, D-12, and D-19 using the contracts established in Plans 21-02 and 21-03.
Output: delivery render-data persistence, dispatch-path assertions, and trace-safe render identity projection.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-template-versioning-rendering-contracts/21-CONTEXT.md
@.planning/phases/21-template-versioning-rendering-contracts/21-PATTERNS.md
@.planning/phases/21-template-versioning-rendering-contracts/21-02-PLAN.md
@.planning/phases/21-template-versioning-rendering-contracts/21-03-PLAN.md
@lib/chimeway/deliveries.ex
@lib/chimeway/delivery_planning.ex
@lib/chimeway/traces.ex
@lib/chimeway/adapter.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist validated render data before dispatch and expose safe trace identity</name>
  <files>lib/chimeway/deliveries.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/traces.ex, lib/chimeway/traces/explanation.ex, test/chimeway/orchestration/delivery_planning_test.exs, test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <behavior>
    - Test 1: planning materializes validated `render_data` on the delivery row before any dispatchable state is reached.
    - Test 2: the dispatch lifecycle consumes pre-rendered delivery rows without asking notifiers or adapters to recompute content.
    - Test 3: explanations expose `render_key` and `render_version` but omit `html_body`, `text_body`, and raw `render_data`.
  </behavior>
  <action>Implement D-08, D-09, D-12, and D-19 by following the mapped analogs in `21-PATTERNS.md`: add `Chimeway.Deliveries.apply_render_result/2` using the centralized `normalize_*` then `change |> Repo.update` flow from `lib/chimeway/deliveries.ex`, and call `Chimeway.Rendering.render_delivery/4` from `Chimeway.DeliveryPlanning.plan_one_channel/5` at the planner choke point mapped from `lib/chimeway/delivery_planning.ex` before the row can proceed to deferral, digest accumulation, or dispatch. Extend integration coverage in the style mapped from `test/chimeway/integration/delivery_lifecycle_test.exs` so adapters receive populated `render_data` without late notifier callbacks, and update `Chimeway.Traces.Explanation` plus `Chimeway.Traces.explain_delivery/2` using the explanation projection pattern mapped from `lib/chimeway/traces.ex` so they expose `render_key` and `render_version` only while explicitly excluding rendered body fields and transport blobs.</action>
  <verify>
    <automated>mix test test/chimeway/orchestration/delivery_planning_test.exs test/chimeway/integration/delivery_lifecycle_test.exs --trace</automated>
  </verify>
  <done>Canonical delivery rows hold validated per-channel render output before adapter execution, and trace surfaces stay explainable without leaking bodies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| delivery planning -> adapter execution | Pre-rendered content crosses from Chimeway planning into transport code |
| delivery row -> operator traces | Stored render identity becomes explanation output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-11 | E | `lib/chimeway/delivery_planning.ex` | mitigate | Persist validated `render_data` during planning so adapters and workers never render from mutable host state. |
| T-21-12 | I | `lib/chimeway/traces.ex` | mitigate | Project only `render_key` and `render_version`; exclude `render_data`, `html_body`, and `text_body` from explanation structs. |
| T-21-13 | R | `test/chimeway/integration/delivery_lifecycle_test.exs` | mitigate | Assert the adapter sees pre-rendered delivery rows, proving transport seams remain render-agnostic. |
</threat_model>

<verification>
- `mix test test/chimeway/orchestration/delivery_planning_test.exs test/chimeway/integration/delivery_lifecycle_test.exs --trace`
</verification>

<success_criteria>
- Delivery rows persist validated `render_data` before adapter execution.
- Adapters consume pre-rendered content only.
- Trace explanations expose render identity safely.
</success_criteria>

<output>
After completion, create `.planning/phases/21-template-versioning-rendering-contracts/21-04-SUMMARY.md`
</output>
