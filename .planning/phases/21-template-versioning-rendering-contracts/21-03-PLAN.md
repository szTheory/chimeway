---
phase: 21-template-versioning-rendering-contracts
plan: 03
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - lib/chimeway/rendering.ex
  - lib/chimeway/rendering/channels/in_app.ex
  - lib/chimeway/rendering/channels/email.ex
  - test/chimeway/rendering/channel_contract_test.exs
autonomous: true
requirements: [TMPL-02]
must_haves:
  truths:
    - "In-app rendering uses an explicit validated payload contract."
    - "Email rendering uses an explicit validated payload contract."
    - "Malformed channel payloads fail through runtime validation instead of silently passing."
  artifacts:
    - path: "lib/chimeway/rendering/channels/in_app.ex"
      provides: "Validated in-app output contract"
    - path: "lib/chimeway/rendering/channels/email.ex"
      provides: "Validated email output contract"
    - path: "test/chimeway/rendering/channel_contract_test.exs"
      provides: "Channel render contract tests"
  key_links:
    - from: "lib/chimeway/rendering.ex"
      to: "lib/chimeway/rendering/channels/in_app.ex"
      via: "channel-specific render dispatch"
      pattern: "render_delivery"
    - from: "lib/chimeway/rendering.ex"
      to: "lib/chimeway/rendering/channels/email.ex"
      via: "channel-specific render dispatch"
      pattern: "render_delivery"
---

<objective>
Define the explicit channel renderer contracts that convert durable assigns into validated per-channel payloads.

Purpose: implement D-06, D-07, D-17, and D-18 without coupling the core library to Phoenix or Swoosh.
Output: channel renderer modules, shared render dispatch, and contract tests for `:in_app` and `:email`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-template-versioning-rendering-contracts/21-CONTEXT.md
@.planning/phases/21-template-versioning-rendering-contracts/21-RESEARCH.md
@.planning/phases/21-template-versioning-rendering-contracts/21-PATTERNS.md
@.planning/phases/21-template-versioning-rendering-contracts/21-01-PLAN.md
@lib/chimeway/rendering.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build explicit in-app and email renderer contracts</name>
  <files>lib/chimeway/rendering.ex, lib/chimeway/rendering/channels/in_app.ex, lib/chimeway/rendering/channels/email.ex, test/chimeway/rendering/channel_contract_test.exs</files>
  <behavior>
    - Test 1: `:in_app` rendering returns a validated output map with semantic keys suitable for inbox use.
    - Test 2: `:email` rendering returns a validated output map with `subject`, `html_body`, and `text_body`.
    - Test 3: invalid output shapes fail through runtime validation rather than passing generic maps.
  </behavior>
  <action>Implement D-06, D-07, D-17, and D-18 by following the planner/renderer seam mapped in `21-PATTERNS.md`: extend `Chimeway.Rendering` with `render_delivery/4` using the same normalization/error-wrapping posture mapped from `lib/chimeway/delivery_planning.ex`, then add `Chimeway.Rendering.Channels.InApp` and `Chimeway.Rendering.Channels.Email` using the explicit-field contract posture mapped from `lib/chimeway/delivery.ex` rather than a free-form blob-only payload. Use changeset-backed runtime validation for both channels, normalize outputs into explicit `render_data` maps, avoid hard Phoenix/Swoosh dependencies, and lock the contract with the dedicated channel test in the same tagged-failure style expected by Phase 21 patterns.</action>
  <verify>
    <automated>mix test test/chimeway/rendering/channel_contract_test.exs --trace</automated>
  </verify>
  <done>Both Phase 21 channels render through explicit runtime-validated contracts produced by a shared dispatch function.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable render assigns -> channel renderers | Stored inputs become channel-specific payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | T | `lib/chimeway/rendering/channels/in_app.ex` | mitigate | Validate required semantic in-app fields with runtime checks rather than accepting opaque maps. |
| T-21-09 | T | `lib/chimeway/rendering/channels/email.ex` | mitigate | Validate `subject`, `html_body`, and `text_body` explicitly before any delivery row stores email render data. |
| T-21-10 | R | `test/chimeway/rendering/channel_contract_test.exs` | mitigate | Lock runtime validation failures in CI so channel contract drift is detectable immediately. |
</threat_model>

<verification>
- `mix test test/chimeway/rendering/channel_contract_test.exs --trace`
</verification>

<success_criteria>
- `:in_app` and `:email` have explicit validated render contracts.
- `Chimeway.Rendering.render_delivery/4` routes to those validators.
- Invalid channel payloads fail deterministically in tests.
</success_criteria>

<output>
After completion, create `.planning/phases/21-template-versioning-rendering-contracts/21-03-SUMMARY.md`
</output>
