---
phase: 21-template-versioning-rendering-contracts
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - lib/chimeway/rendering/preview.ex
  - lib/chimeway.ex
  - lib/mix/tasks/preview_rendering.ex
  - test/chimeway/rendering/preview_pipeline_test.exs
autonomous: true
requirements: [TMPL-03, TMPL-02]
must_haves:
  truths:
    - "Developers can preview notification rendering without sending a provider delivery."
    - "Preview output reuses the exact same render pipeline and render identity used for production planning."
    - "The Mix task is only a convenience wrapper over the library preview API."
  artifacts:
    - path: "lib/chimeway/rendering/preview.ex"
      provides: "Pure preview API over the production rendering path"
    - path: "lib/mix/tasks/preview_rendering.ex"
      provides: "CLI convenience wrapper for local preview verification"
    - path: "test/chimeway/rendering/preview_pipeline_test.exs"
      provides: "Parity tests between preview and production render artifacts"
  key_links:
    - from: "lib/chimeway/rendering/preview.ex"
      to: "lib/chimeway/rendering.ex"
      via: "shared production render pipeline"
      pattern: "render_delivery"
    - from: "lib/mix/tasks/preview_rendering.ex"
      to: "lib/chimeway/rendering/preview.ex"
      via: "thin wrapper delegation"
      pattern: "Chimeway\\.Rendering\\.Preview"
---

<objective>
Expose a pure preview surface that proves dispatch and local verification use the same rendering path.

Purpose: implement D-13 through D-16 and finish TMPL-03 without adding browser-only tooling or parallel preview semantics.
Output: library preview API, public entrypoint, Mix task wrapper, and parity tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-template-versioning-rendering-contracts/21-CONTEXT.md
@.planning/phases/21-template-versioning-rendering-contracts/21-VALIDATION.md
@.planning/phases/21-template-versioning-rendering-contracts/21-02-PLAN.md
@lib/chimeway.ex
@lib/chimeway/rendering.ex
@lib/mix/tasks/verify_published.ex

<interfaces>
From plan 21-02:
```elixir
Chimeway.Rendering.render_delivery(notifier, params, recipient, channel)
```

From lib/chimeway.ex:
```elixir
def trigger(notifier, params, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the pure preview API and public entrypoint</name>
  <files>lib/chimeway/rendering/preview.ex, lib/chimeway.ex, test/chimeway/rendering/preview_pipeline_test.exs</files>
  <read_first>.planning/phases/21-template-versioning-rendering-contracts/21-CONTEXT.md, .planning/phases/21-template-versioning-rendering-contracts/21-RESEARCH.md, lib/chimeway.ex, lib/chimeway/rendering.ex</read_first>
  <behavior>
    - Test 1: `Chimeway.preview_rendering/3` returns a stable preview struct containing `channel`, `render_key`, `render_version`, and validated `render_data`.
    - Test 2: preview uses the same normalized declaration and channel renderer path as production delivery planning.
    - Test 3: preview rejects invalid channel names or malformed assigns with the same tagged errors the production renderer returns.
  </behavior>
  <action>Create `Chimeway.Rendering.Preview` with a pure function `preview(notifier, params, opts)` that requires `:recipient` and `:channel`, runs the same declaration normalization and `render_delivery/4` pipeline from Plan 21-02, and returns a stable preview struct containing `notification_key`, `channel`, `render_key`, `render_version`, and validated `render_data`. Add `Chimeway.preview_rendering/3` in `lib/chimeway.ex` as the public entrypoint. Do not persist rows, enqueue jobs, or create a second rendering code path per D-13 and D-14.</action>
  <acceptance_criteria>
    - `rg -n 'def preview_rendering\\(' lib/chimeway.ex` returns 1 match.
    - `rg -n 'def preview\\(' lib/chimeway/rendering/preview.ex` returns 1 match.
    - `rg -n 'render_key|render_version|render_data' test/chimeway/rendering/preview_pipeline_test.exs` returns at least 4 matches.
    - `rg -n 'Repo\\.|Oban|insert' lib/chimeway/rendering/preview.ex` returns 0 matches.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/rendering/preview_pipeline_test.exs --trace</automated>
  </verify>
  <done>The library exposes one pure preview API that returns the same render identity and payload shape production planning would generate, without sending or persisting anything.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the thin Mix preview wrapper without changing semantics</name>
  <files>lib/mix/tasks/preview_rendering.ex, test/chimeway/rendering/preview_pipeline_test.exs</files>
  <read_first>.planning/phases/21-template-versioning-rendering-contracts/21-CONTEXT.md, lib/mix/tasks/verify_published.ex, lib/chimeway/rendering/preview.ex, test/chimeway/rendering/preview_pipeline_test.exs</read_first>
  <behavior>
    - Test 1: the Mix task delegates to the preview API instead of reimplementing rendering logic.
    - Test 2: the task prints stable identity fields and semantic output fields suitable for local verification.
    - Test 3: the task exits non-zero with usage guidance when required flags are missing.
  </behavior>
  <action>Implement `Mix.Tasks.Preview.Rendering` as a convenience wrapper over `Chimeway.preview_rendering/3`. Use concrete flags `--notifier`, `--params`, `--recipient`, and `--channel`; parse file inputs with `Code.eval_file/1` only for `.exs` fixture files or use `File.read!` plus `Code.string_to_quoted!` for Elixir map literals, and keep the task dependency-free. Print a stable human-readable summary with `render_key`, `render_version`, `channel`, and inspected `render_data`. Do not introduce JSON-only output, browser UI, or alternate preview semantics per D-14 through D-16.</action>
  <acceptance_criteria>
    - `rg -n 'defmodule Mix\\.Tasks\\.Preview\\.Rendering' lib/mix/tasks/preview_rendering.ex` returns 1 match.
    - `rg -n 'preview_rendering' lib/mix/tasks/preview_rendering.ex` returns at least 1 match.
    - `rg -n 'Usage: mix preview.rendering' lib/mix/tasks/preview_rendering.ex` returns 1 match.
    - `rg -n 'render_key|render_version|channel' lib/mix/tasks/preview_rendering.ex` returns at least 3 matches.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/rendering/preview_pipeline_test.exs --trace</automated>
  </verify>
  <done>Developers can invoke one local Mix command that simply calls the library preview API and reports the same render artifact shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI input -> preview API | Local file/argument inputs drive preview rendering |
| preview API -> developer-visible output | Render artifacts become inspectable CLI output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-09 | T | `lib/chimeway/rendering/preview.ex` | mitigate | Reuse the production `render_delivery/4` pipeline directly so preview and dispatch cannot drift into separate contracts. |
| T-21-10 | I | `lib/mix/tasks/preview_rendering.ex` | mitigate | Print only stable identity fields plus semantic render data already validated for delivery rows; avoid telemetry/logging hooks and raw provider payload concepts. |
| T-21-11 | D | Mix task CLI | accept | Local preview is developer-invoked and non-persistent; malformed arguments should fail fast with usage guidance instead of retrying or hanging. |
</threat_model>

<verification>
- `mix test test/chimeway/rendering/preview_pipeline_test.exs --trace`
- `mix preview.rendering --help`
</verification>

<success_criteria>
- `Chimeway.preview_rendering/3` exists and reuses the production rendering path.
- `mix preview.rendering` delegates to the library API instead of implementing a separate renderer.
- Preview output includes render identity and validated channel payload fields before provider delivery.
</success_criteria>

<output>
After completion, create `.planning/phases/21-template-versioning-rendering-contracts/21-03-SUMMARY.md`
</output>
