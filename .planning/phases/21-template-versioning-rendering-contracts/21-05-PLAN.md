---
phase: 21-template-versioning-rendering-contracts
plan: 05
type: execute
wave: 4
depends_on: [21-04]
files_modified:
  - lib/chimeway/rendering/preview.ex
  - lib/chimeway.ex
  - lib/mix/tasks/preview_rendering.ex
  - test/chimeway/rendering/preview_pipeline_test.exs
autonomous: true
requirements: [TMPL-03, TMPL-02]
must_haves:
  truths:
    - "Developers can preview notification rendering without sending provider traffic."
    - "Preview output reuses the exact production render pipeline and render identity."
    - "The Mix task is only a thin wrapper over the library preview API."
  artifacts:
    - path: "lib/chimeway/rendering/preview.ex"
      provides: "Pure preview API over the production rendering path"
    - path: "lib/mix/tasks/preview_rendering.ex"
      provides: "CLI wrapper for local preview verification"
    - path: "test/chimeway/rendering/preview_pipeline_test.exs"
      provides: "Parity tests between preview and production render artifacts"
  key_links:
    - from: "lib/chimeway/rendering/preview.ex"
      to: "lib/chimeway/rendering.ex"
      via: "shared production render pipeline"
      pattern: "render_delivery"
    - from: "lib/mix/tasks/preview_rendering.ex"
      to: "lib/chimeway/rendering/preview.ex"
      via: "thin wrapper delegation"
      pattern: "preview_rendering|Preview"
---

<objective>
Expose the pure preview surface and thin Mix wrapper that prove local verification and production dispatch use the same render path.

Purpose: implement D-13 through D-16 and finish TMPL-03 without adding browser-only tooling or alternate preview semantics.
Output: library preview API, public entrypoint, Mix wrapper, and parity tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-template-versioning-rendering-contracts/21-CONTEXT.md
@.planning/phases/21-template-versioning-rendering-contracts/21-PATTERNS.md
@.planning/phases/21-template-versioning-rendering-contracts/21-VALIDATION.md
@.planning/phases/21-template-versioning-rendering-contracts/21-04-PLAN.md
@lib/chimeway.ex
@lib/chimeway/rendering.ex
@lib/mix/tasks/verify_published.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the pure preview API and public entrypoint</name>
  <files>lib/chimeway/rendering/preview.ex, lib/chimeway.ex, test/chimeway/rendering/preview_pipeline_test.exs</files>
  <behavior>
    - Test 1: `Chimeway.preview_rendering/3` returns a stable preview struct containing `channel`, `render_key`, `render_version`, and validated `render_data`.
    - Test 2: preview uses the same normalized declaration and channel renderer path as production delivery planning.
    - Test 3: preview rejects invalid channel names or malformed assigns with the same tagged errors the production renderer returns.
  </behavior>
  <action>Implement D-13 and D-14 by following the thin-public-API analogs in `21-PATTERNS.md`: create `Chimeway.Rendering.Preview.preview/3`, require `:recipient` and `:channel`, and route through the same declaration normalization and `render_delivery/4` pipeline used in production so preview stays aligned with the shared render path. Add `Chimeway.preview_rendering/3` using the delegation style mapped from `lib/chimeway.ex`, keep preview pure with no row persistence or job enqueueing, and preserve parity with the production artifact shape as called out by the mapped `test/chimeway/integration/delivery_lifecycle_test.exs` preview-equivalence guidance.</action>
  <verify>
    <automated>mix test test/chimeway/rendering/preview_pipeline_test.exs --trace</automated>
  </verify>
  <done>The library exposes one pure preview API that returns the same render identity and payload shape production planning would generate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the thin Mix preview wrapper without changing semantics</name>
  <files>lib/mix/tasks/preview_rendering.ex, test/chimeway/rendering/preview_pipeline_test.exs</files>
  <behavior>
    - Test 1: the Mix task delegates to the preview API instead of reimplementing rendering logic.
    - Test 2: the task prints stable identity fields and semantic output fields suitable for local verification.
    - Test 3: the task exits non-zero with usage guidance when required flags are missing.
  </behavior>
  <action>Implement D-14 through D-16 by adding `Mix.Tasks.Preview.Rendering` as a convenience wrapper over `Chimeway.preview_rendering/3`, following the thin Mix task and CLI output/error patterns mapped from `lib/mix/tasks/verify_published.ex`. Support `--notifier`, `--params`, `--recipient`, and `--channel`; validate required args first, keep parsing limited to Elixir literals or `.exs` fixtures, then delegate and print a stable human-readable summary containing `render_key`, `render_version`, `channel`, and inspected `render_data`. Avoid browser UI, JSON-only output, or alternate preview semantics.</action>
  <verify>
    <automated>mix test test/chimeway/rendering/preview_pipeline_test.exs --trace</automated>
  </verify>
  <done>Developers can invoke one local Mix command that simply calls the library preview API and reports the same render artifact shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI input -> preview API | Local file and argument inputs drive preview rendering |
| preview API -> developer-visible output | Render artifacts become inspectable CLI output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-14 | T | `lib/chimeway/rendering/preview.ex` | mitigate | Reuse the production `render_delivery/4` pipeline directly so preview and dispatch cannot drift. |
| T-21-15 | I | `lib/mix/tasks/preview_rendering.ex` | mitigate | Print only stable identity fields and validated semantic payload data; avoid telemetry or raw provider payload concepts. |
| T-21-16 | D | Mix task CLI | accept | Local preview is developer-invoked and non-persistent; malformed arguments should fail fast with usage guidance. |
</threat_model>

<verification>
- `mix test test/chimeway/rendering/preview_pipeline_test.exs --trace`
- `mix preview.rendering --help`
</verification>

<success_criteria>
- `Chimeway.preview_rendering/3` exists and reuses the production render path.
- `mix preview.rendering` delegates to the library API instead of implementing separate semantics.
- Preview output includes render identity and validated channel payload fields before provider delivery.
</success_criteria>

<output>
After completion, create `.planning/phases/21-template-versioning-rendering-contracts/21-05-SUMMARY.md`
</output>
