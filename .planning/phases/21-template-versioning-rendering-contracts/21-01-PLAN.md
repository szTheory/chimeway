---
phase: 21-template-versioning-rendering-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260428123000_add_rendering_contract_fields.exs
  - lib/chimeway/notifications/notification.ex
  - lib/chimeway/delivery.ex
  - lib/chimeway/notifier.ex
  - lib/chimeway/rendering.ex
  - test/chimeway/notifier_contract_test.exs
  - test/chimeway/rendering/render_identity_integration_test.exs
autonomous: true
requirements: [TMPL-01, TMPL-02]
must_haves:
  truths:
    - "Durable notification and delivery rows have explicit rendering fields independent from notifier module names."
    - "Notifier content declarations normalize into one explicit rendering contract before planning-time channel work begins."
    - "The first render-identity regression test exists before later planning tasks rely on it."
  artifacts:
    - path: "priv/repo/migrations/20260428123000_add_rendering_contract_fields.exs"
      provides: "Notification and delivery rendering columns"
    - path: "lib/chimeway/rendering.ex"
      provides: "Normalized notifier rendering declaration seam"
    - path: "test/chimeway/rendering/render_identity_integration_test.exs"
      provides: "Runnable regression coverage for durable render identity"
  key_links:
    - from: "lib/chimeway/notifier.ex"
      to: "lib/chimeway/rendering.ex"
      via: "optional rendering callback normalized through one resolver"
      pattern: "rendering|resolve_declaration"
    - from: "test/chimeway/rendering/render_identity_integration_test.exs"
      to: "lib/chimeway/delivery.ex"
      via: "schema-level render identity assertions"
      pattern: "render_key|render_version"
---

<objective>
Establish the durable rendering storage and notifier declaration contracts that every later Phase 21 slice depends on.

Purpose: implement D-01 through D-06 and D-09 through D-11 before planning or preview work starts.
Output: migration/schema fields, normalized notifier rendering declarations, and a runnable render-identity regression test.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-template-versioning-rendering-contracts/21-CONTEXT.md
@.planning/phases/21-template-versioning-rendering-contracts/21-RESEARCH.md
@.planning/phases/21-template-versioning-rendering-contracts/21-PATTERNS.md
@.planning/phases/21-template-versioning-rendering-contracts/21-VALIDATION.md
@lib/chimeway/notifier.ex
@lib/chimeway/notifications/notification.ex
@lib/chimeway/delivery.ex

<interfaces>
From lib/chimeway/notifier.ex:
```elixir
@callback notification_key() :: String.t()
@callback version() :: pos_integer()
@callback recipients(map()) :: {:ok, [map()]} | {:error, term()}
@callback build(map(), map()) :: {:ok, map()} | {:error, term()}
@callback channels(map(), map()) :: {:ok, [atom() | String.t()]} | {:error, term()}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable rendering fields and seed the render-identity regression</name>
  <files>priv/repo/migrations/20260428123000_add_rendering_contract_fields.exs, lib/chimeway/notifications/notification.ex, lib/chimeway/delivery.ex, test/chimeway/rendering/render_identity_integration_test.exs</files>
  <behavior>
    - Test 1: notification changesets accept `render_assigns` while preserving existing recipient requirements.
    - Test 2: delivery changesets accept `render_key`, `render_version`, and `render_data` without repurposing `planning_context`.
    - Test 3: the new regression test proves render identity lives on delivery rows independently from notifier module names.
  </behavior>
  <action>Implement D-01, D-02, D-09, D-10, and D-11 by adding `render_assigns` to `chimeway_notifications` and `render_key`, `render_version`, plus `render_data` to `chimeway_deliveries`. Keep `render_assigns` and `render_data` as maps, do not add registry tables per D-03, and leave `planning_context` orchestration-only. Extend the new integration test in the same task so the verification path exists immediately rather than waiting on later planning work.</action>
  <verify>
    <automated>mix test test/chimeway/rendering/render_identity_integration_test.exs --trace</automated>
  </verify>
  <done>Storage for durable render inputs and per-delivery render identity exists, and the render-identity regression test is runnable at the end of the task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the normalized notifier rendering declaration seam</name>
  <files>lib/chimeway/notifier.ex, lib/chimeway/rendering.ex, test/chimeway/notifier_contract_test.exs</files>
  <behavior>
    - Test 1: notifiers can declare `assigns` plus per-channel `render_key` and `render_version`.
    - Test 2: `build/2`-only notifiers still normalize through a compatibility fallback.
    - Test 3: blank render keys, invalid channel keys, and non-positive versions return tagged normalization errors.
  </behavior>
  <action>Implement D-04, D-05, and D-06 by adding an optional `rendering/2` notifier callback and a canonical `Chimeway.Rendering.resolve_declaration/4` resolver. Validate channel keys, non-empty `render_key`, and positive `render_version`; preserve `build/2` as a compatibility seam by deriving fallback `assigns` plus `#{notification_key}.#{channel}` identity when `rendering/2` is absent, per D-04.</action>
  <verify>
    <automated>mix test test/chimeway/notifier_contract_test.exs --trace</automated>
  </verify>
  <done>The codebase has one explicit notifier rendering declaration contract with validated legacy fallback behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notifier module -> Chimeway normalization | Host-supplied rendering declarations become durable render facts |
| schema layer -> persisted rows | Render identity fields move into durable storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | T | `lib/chimeway/rendering.ex` | mitigate | Normalize channel declarations and reject blank `render_key` or invalid `render_version` values before any persistence. |
| T-21-02 | T | `lib/chimeway/delivery.ex` | mitigate | Add explicit delivery fields for render identity and keep `planning_context` separate so orchestration metadata cannot masquerade as rendering state. |
| T-21-03 | R | `test/chimeway/rendering/render_identity_integration_test.exs` | mitigate | Create the regression test in the same slice as the schema changes so later plans cannot rely on an absent verification path. |
| T-21-04 | I | notification/delivery render fields | accept | This slice stores identity and structured inputs only; rendered body output is not surfaced yet. |
</threat_model>

<verification>
- `mix test test/chimeway/rendering/render_identity_integration_test.exs --trace`
- `mix test test/chimeway/notifier_contract_test.exs --trace`
</verification>

<success_criteria>
- Notification rows accept explicit `render_assigns`.
- Delivery rows have durable `render_key`, `render_version`, and `render_data` fields.
- Notifier rendering declarations normalize through one validated seam with legacy fallback behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/21-template-versioning-rendering-contracts/21-01-SUMMARY.md`
</output>
