---
phase: 57-docs-release-gates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .github/workflows/ci.yml
  - MAINTAINING.md
requirements: [GATE-04]
must_haves:
  truths:
    - "mix verify.mailglass runs root and demo host mailglass-tagged tests"
    - "Default mix ci.test excludes mailglass for fast feedback"
    - "MAINTAINING pre-ship checklist documents six gates including verify.mailglass"
    - "CI workflow has verify_mailglass job mirroring verify_journeys"
  artifacts:
    - path: "mix.exs"
      provides: "GATE-04 named entrypoint"
      contains: "verify.mailglass"
    - path: ".github/workflows/ci.yml"
      provides: "Shift-left CI"
      contains: "verify_mailglass"
    - path: "MAINTAINING.md"
      provides: "Maintainer runbook"
      contains: "mix verify.mailglass"
  key_links:
    - from: "mix.exs verify.mailglass"
      to: "test/chimeway/adapters/mailglass_adapter_test.exs"
      via: "--only mailglass"
      pattern: "@moduletag :mailglass"
    - from: "mix.exs verify.mailglass"
      to: "examples/chimeway_demo_host/test/demo_host_web/mailglass_delivery_proof_test.exs"
      via: "demo host subprocess"
      pattern: "mailglass_delivery_proof"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-57-05 | verify.mailglass added to mix ci breaking fast path | medium | D-14: do NOT modify ci alias; only add verify.mailglass + ci.test exclude |
| T-57-06 | ci.test exclude hides mailglass regressions until verify run | low | Dedicated CI job + MAINTAINING sextet makes gate mandatory pre-ship |
| T-57-07 | Journey suite picks up mailglass tests | medium | Demo subprocess uses --only mailglass; journey stays --only journey (D-17) |
</threat_model>

<objective>
Implement GATE-04 named Mailglass verify entrypoint (D-12, D-13: root adapter/webhook/executor + demo DEMO-06 proof), fast default CI exclusion (D-15), GitHub Actions job (D-16), journey isolation preserved (D-17), MAINTAINING sextet (D-18), quintet unchanged (D-22), verify.example separate from verify.mailglass (D-23) — closing Phase 54 WR-03 deferral.

Purpose: Mailglass integration proof is explicit in CI and release checklist without slowing default test lane.

Output: `mix verify.mailglass` green locally; `verify_mailglass` CI job; MAINTAINING lists 6 commands.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-docs-release-gates/57-CONTEXT.md
@.planning/phases/57-docs-release-gates/57-RESEARCH.md
@mix.exs
@.github/workflows/ci.yml
@MAINTAINING.md
@test/chimeway/adapters/mailglass_adapter_test.exs
@examples/chimeway_demo_host/test/demo_host_web/mailglass_delivery_proof_test.exs
</context>

<tasks>

<task id="57-03-01" type="execute">
<title>Add verify.mailglass alias and ci.test mailglass exclude</title>

<read_first>
- mix.exs (aliases section)
- .planning/phases/57-docs-release-gates/57-CONTEXT.md (D-12, D-14, D-15)
</read_first>

<action>
In root `mix.exs` `aliases/0`:

1. Change `"ci.test"` from `["cmd env MIX_ENV=test mix test"]` to `["cmd env MIX_ENV=test mix test --exclude mailglass"]` (D-15).

2. Add after `verify.journeys` block (D-12):
```elixir
"verify.mailglass": [
  "cmd env MIX_ENV=test mix test --only mailglass --warnings-as-errors",
  "cmd --shell cd examples/chimeway_demo_host && mix deps.get && mix test --only mailglass --warnings-as-errors"
],
```

3. Confirm `ci` alias remains `["ci.lint", "ci.test"]` only — do NOT append verify.mailglass (D-14).
</action>

<verify>
mix verify.mailglass
</verify>

<acceptance_criteria>
- `mix verify.mailglass` exits 0
- mix.exs contains `"verify.mailglass":` with both subprocess lines
- mix.exs `ci.test` contains `--exclude mailglass`
- mix.exs `ci` alias does NOT include verify.mailglass
</acceptance_criteria>
</task>

<task id="57-03-02" type="execute">
<title>Add verify_mailglass CI job</title>

<read_first>
- .github/workflows/ci.yml (verify_journeys job ~line 123)
- mix.exs verify.mailglass alias
</read_first>

<action>
In `.github/workflows/ci.yml`, add job `verify_mailglass` after `verify_journeys` job. Clone `verify_journeys` structure:
- name: `Mailglass integration gate`
- Same postgres:15 service, MIX_ENV=test, DATABASE_URL
- Distinct cache key: `${{ runner.os }}-mix-verify-mailglass-${{ hashFiles('**/mix.lock') }}`
- Same setup steps: checkout, setup-beam 1.17/OTP 27, deps.get, ecto create, ecto migrate
- Final run step: `mix verify.mailglass` (D-16)
</action>

<verify>
grep -q "verify_mailglass:" .github/workflows/ci.yml && grep -q "mix verify.mailglass" .github/workflows/ci.yml
</verify>

<acceptance_criteria>
- Job id `verify_mailglass` exists in ci.yml
- Job runs `mix verify.mailglass`
- Postgres service and ecto migrate steps present (parity with verify_journeys)
</acceptance_criteria>
</task>

<task id="57-03-03" type="execute">
<title>Extend MAINTAINING.md to pre-ship sextet</title>

<read_first>
- MAINTAINING.md (section 3)
- .planning/phases/57-docs-release-gates/57-CONTEXT.md (D-18, D-22)
</read_first>

<action>
In `MAINTAINING.md` section "### 3. Run the full local gate":
1. Add `mix verify.mailglass` as 6th command in the bash block after `mix verify.journeys`
2. Add bullet describing gate: root adapter + webhook pipeline + demo host DEMO-06 proof (D-18)
3. Change "All five must pass" to "All six must pass"
4. Do NOT remove or reorder existing five commands (D-22)
</action>

<verify>
grep -c "mix verify" MAINTAINING.md | test "$(grep -c 'mix verify' MAINTAINING.md)" -ge 3
grep -q "mix verify.mailglass" MAINTAINING.md
grep -q "six" MAINTAINING.md
</verify>

<acceptance_criteria>
- MAINTAINING.md pre-ship block lists `mix verify.mailglass`
- Prose references six commands (not five only)
- Existing quintet commands unchanged in order before mailglass entry
</acceptance_criteria>
</task>

</tasks>

<verification>
mix verify.mailglass
mix verify.journeys
mix ci.test
</verification>

<success_criteria>
- GATE-04: named verify entrypoint documented and runnable
- Fast CI: ci.test excludes mailglass; journeys gate still green
- MAINTAINING sextet complete
</success_criteria>
