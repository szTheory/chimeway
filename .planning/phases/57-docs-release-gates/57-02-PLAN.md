---
phase: 57-docs-release-gates
plan: 02
type: execute
wave: 2
depends_on: [57-01]
files_modified:
  - test/chimeway/doc_contract_test.exs
requirements: [DOCS-07]
must_haves:
  truths:
    - "Doc-contract describe locks mailglass integration guide required phrases"
    - "Forbidden strings prevent pre-Mailglass-only and fictional module regressions"
    - "mix ci.verify_gates runs new describe and passes"
  artifacts:
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "DOCS-07 CI truth lock"
      contains: "mailglass integration guide doc contract"
  key_links:
    - from: "test/chimeway/doc_contract_test.exs"
      to: "guides/introduction/mailglass-integration.md"
      via: "@mailglass_integration_guide Path.expand"
      pattern: "mailglass-integration.md"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-57-03 | Doc-contract passes while guide omits inbound/Webhooks.process | medium | Required list includes `Chimeway.Webhooks.process` (D-09) |
| T-57-04 | Guide path typo breaks CI | low | Use Path.expand pattern identical to golden-path describe |
</threat_model>

<objective>
Add DOCS-07 doc-contract tests (D-08) for the Mailglass integration guide with D-09 required phrases, D-10 forbidden pre-Mailglass regressions, via existing `mix ci.verify_gates` (D-11).

Purpose: Lock adoption-surface truth alongside ECOS-05 blueprint contract.

Output: New describe block in doc_contract_test.exs; `mix ci.verify_gates` green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/57-docs-release-gates/57-CONTEXT.md
@.planning/phases/57-docs-release-gates/57-RESEARCH.md
@test/chimeway/doc_contract_test.exs
@guides/introduction/mailglass-integration.md
</context>

<tasks>

<task id="57-02-01" type="execute">
<title>Add mailglass integration guide doc-contract describe</title>

<read_first>
- test/chimeway/doc_contract_test.exs (mailglass blueprint describe ~line 245)
- test/chimeway/doc_contract_test.exs (golden path describe ~line 305)
- guides/introduction/mailglass-integration.md
- .planning/phases/57-docs-release-gates/57-CONTEXT.md
</read_first>

<action>
In `test/chimeway/doc_contract_test.exs`, after the mailglass blueprint describe block, add:

1. Module attribute:
   `@mailglass_integration_guide Path.expand("../../guides/introduction/mailglass-integration.md", __DIR__)`

2. Describe: `"mailglass integration guide doc contract (DOCS-06 / DOCS-07)"` with setup reading the guide file.

3. Forbidden loop: reuse `@recipe_forbidden_strings` (same as blueprint describe).

4. Test: forbids `Chimeway.Workflow` (not `Workflows`) via same regex as blueprint describe.

5. Guide-specific forbidden: assert guide does NOT present Logger adapter as the sole documented email delivery path — add test that refutes a phrase like sole Logger-only email path if guide uses Logger for comparison, OR forbid missing `Chimeway.Adapters.Mailglass` when describing email channel (minimum: required list enforces Mailglass adapter).

6. `@required` list minimum (D-09): `Chimeway.Adapters.Mailglass`, `Chimeway.Adapter.Mailglass`, `channel_adapters`, `channel_adapter_configs`, `render_key`, `Chimeway.Webhooks.process`, `Mailglass.Mailable`, `Chimeway.trigger`, `tenant_id`, `idempotency_key`, plus orchestration/templating words (`orchestrates` or `orchestration` AND `templating`).

7. for-loop required phrase tests matching existing describe style.
</action>

<verify>
mix ci.verify_gates
</verify>

<acceptance_criteria>
- `mix ci.verify_gates` exits 0
- doc_contract_test.exs contains describe string `mailglass integration guide doc contract (DOCS-06 / DOCS-07)`
- All D-09 minimum required strings appear in @required list
- `@recipe_forbidden_strings` loop present for guide describe
</acceptance_criteria>
</task>

</tasks>

<verification>
mix ci.verify_gates
</verification>

<success_criteria>
- DOCS-07 satisfied: guide truth locked in CI
- No regression to golden-path or blueprint describes
</success_criteria>
