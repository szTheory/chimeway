---
phase: 14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/chimeway/reliability/duplicate_protection_test.exs
  - test/chimeway/reliability/attempt_history_test.exs
  - test/chimeway/reliability/retry_exhaustion_test.exs
  - test/chimeway/reliability/terminal_convergence_test.exs
autonomous: true
requirements: [REL-01, REL-02, REL-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Four Wave-0 test files compile and run via mix test, all describes are skipped (placeholder), exit 0."
    - "Each Wave-0 test file imports DispatchHelpers and aliases Chimeway.Deliveries (the canonical terminal_states/0 source)."
    - "No production code changes — Wave 0 is test scaffolding only."
  artifacts:
    - path: "test/chimeway/reliability/duplicate_protection_test.exs"
      provides: "REL-01 D-02/D-14 placeholder describes (skipped) for duplicate-trigger, plan re-entry, terminal re-entry"
      contains: "@moduletag :skip"
    - path: "test/chimeway/reliability/attempt_history_test.exs"
      provides: "REL-02 placeholder describes for attempt_number ordinality, error_class taxonomy, concurrent attempt_number"
      contains: "@moduletag :skip"
    - path: "test/chimeway/reliability/retry_exhaustion_test.exs"
      provides: "REL-02 placeholder describes for transient retry, terminal exhaustion, drain_queue end-to-end"
      contains: "@moduletag :skip"
    - path: "test/chimeway/reliability/terminal_convergence_test.exs"
      provides: "REL-03 D-12 placeholder describes for every terminal path"
      contains: "@moduletag :skip"
  key_links:
    - from: "test/chimeway/reliability/*"
      to: "Chimeway.Deliveries.terminal_states/0"
      via: "alias and `Deliveries.terminal_states()` reference inside placeholder asserts"
      pattern: "Deliveries\\.terminal_states\\("
---

<objective>
Wave 0: Scaffold the four new test files Phase 14 needs before implementation tasks can fill them in.
Per RESEARCH.md "Wave 0 Gaps" and VALIDATION.md "Wave 0 Requirements," all four files must exist with skipped placeholder describes so subsequent waves can populate them without recreating module setup boilerplate.

Purpose: establish the test scaffolding pattern (DataCase + Oban.Testing + DispatchHelpers + skip moduletag) so 14-06/14-07/14-08 can drop tests in. This is intentionally pure scaffolding — no production code touched.
Output: four new test files, all describing themselves with `@moduletag :skip` so `mix test` exits 0 with no failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-delivery-reliability-hardening/14-CONTEXT.md
@.planning/phases/14-delivery-reliability-hardening/14-RESEARCH.md
@.planning/phases/14-delivery-reliability-hardening/14-PATTERNS.md
@.planning/phases/14-delivery-reliability-hardening/14-VALIDATION.md
@test/support/chimeway/dispatch_helpers.ex
@test/chimeway/idempotency_constraint_test.exs
@test/chimeway/dispatch/oban_worker_test.exs

<interfaces>
<!-- Patterns the executor must mimic verbatim. -->

DataCase + Oban.Testing setup (from test/chimeway/dispatch/oban_worker_test.exs:23-42):
```elixir
use Chimeway.DataCase, async: false
use Oban.Testing, repo: Chimeway.Repo

@moduletag :oban   # add for files that use Oban.Testing perform_job/3 — retry_exhaustion + attempt_history

import Chimeway.Test.DispatchHelpers

alias Chimeway.{Deliveries, DeliveryAttempt, Dispatch.ObanWorker, Repo}
```

Concurrency-only files (duplicate_protection_test, terminal_convergence_test) DataCase shape (from test/chimeway/idempotency_constraint_test.exs:1-7):
```elixir
use Chimeway.DataCase, async: false

alias Chimeway.{Deliveries, Repo}
```

Skip directive (must compile but execute zero asserts):
```elixir
@moduletag :skip   # remove in Plans 14-06/14-07 when each test is implemented
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold duplicate_protection_test.exs and terminal_convergence_test.exs</name>
  <files>test/chimeway/reliability/duplicate_protection_test.exs, test/chimeway/reliability/terminal_convergence_test.exs</files>
  <read_first>
    - test/chimeway/idempotency_constraint_test.exs (canonical DataCase + Task.async_stream + Sandbox.allow shape)
    - test/support/chimeway/dispatch_helpers.ex (create_pending_delivery, create_notification, mark_notification_read)
    - .planning/phases/14-delivery-reliability-hardening/14-RESEARCH.md (lines 720-787 — Phase Requirements -> Test Map; lines 779-787 — Wave 0 Gaps)
    - .planning/phases/14-delivery-reliability-hardening/14-PATTERNS.md (lines 505-575 — duplicate_protection_test analog; lines 675-700 — terminal_convergence_test analog)
    - .planning/phases/14-delivery-reliability-hardening/14-VALIDATION.md (lines 81-86 — Wave 0 file list)
  </read_first>
  <action>
Create two new test files. Both use `Chimeway.DataCase, async: false` and tag the module `@moduletag :skip` so the placeholder describes do not run.

File 1: `test/chimeway/reliability/duplicate_protection_test.exs`
```elixir
defmodule Chimeway.Reliability.DuplicateProtectionTest do
  @moduledoc """
  REL-01 D-02 / D-14 — duplicate protection contract tests.

  Phase 14 Wave 0 scaffold. All describes are skipped until Plan 14-06 fills them in.
  """

  use Chimeway.DataCase, async: false

  alias Chimeway.{Deliveries, Repo}

  @moduletag :skip

  describe "Trigger.fire/{:duplicate, event} contract (D-02a)" do
    test "serial re-fire returns {:duplicate, event}" do
      # Filled in by Plan 14-06.
      assert true
    end
  end

  describe "plan_notifications/2 re-entry (D-02b)" do
    test "double-call for same event creates exactly one delivery per channel" do
      # Filled in by Plan 14-06.
      assert true
    end
  end

  describe "dispatch short-circuit on terminal delivery (D-02c)" do
    test "sync dispatch on already-terminal delivery records no new attempts" do
      # Filled in by Plan 14-06.
      assert true
    end

    test "Oban dispatch on already-terminal delivery records no new attempts" do
      # Filled in by Plan 14-06.
      assert true
    end
  end

  describe "Phase 12 atomicity preserved (D-02d)" do
    test "partial enqueue failure rolls back planning rows" do
      # Filled in by Plan 14-06.
      assert true
    end
  end

  describe "concurrent re-fires of same trigger (D-14a)" do
    test "10 concurrent triggers with same idempotency_key produce one canonical event" do
      # Filled in by Plan 14-06 (mirrors idempotency_constraint_test.exs:49-74 pattern).
      assert true
    end
  end

  describe "concurrent plan_notifications/2 (D-14b)" do
    test "concurrent planning for same event produces no duplicate deliveries" do
      # Filled in by Plan 14-06.
      assert true
    end
  end

  describe "concurrent dispatch re-entry against terminal delivery (D-14c)" do
    test "concurrent perform_job calls record no extra attempts" do
      # Filled in by Plan 14-06.
      assert true
    end
  end

  # Marker reference so the alias is not unused-warned away.
  defp _terminal_states_anchor, do: Deliveries.terminal_states()
end
```

File 2: `test/chimeway/reliability/terminal_convergence_test.exs`
```elixir
defmodule Chimeway.Reliability.TerminalConvergenceTest do
  @moduledoc """
  REL-03 D-12 — every delivery converges to Deliveries.terminal_states/0.

  Phase 14 Wave 0 scaffold. All describes are skipped until Plan 14-07 fills them in.
  """

  use Chimeway.DataCase, async: false

  alias Chimeway.{Deliveries, Repo}

  @moduletag :skip

  describe "succeeded path (D-12)" do
    test "delivery converges to :succeeded after adapter ok" do
      # Filled in by Plan 14-07. Must assert delivery.status in Deliveries.terminal_states().
      assert true
    end
  end

  describe "retries_exhausted path (D-12)" do
    test "delivery converges to :cancelled / retries_exhausted after Oban gives up" do
      # Filled in by Plan 14-07.
      assert true
    end
  end

  describe "permanent_failure path (D-12)" do
    test "delivery converges to :cancelled / permanent_failure on permanent adapter error" do
      # Filled in by Plan 14-07.
      assert true
    end
  end

  describe "bounced path (D-12)" do
    test "delivery converges to :cancelled / bounced on bounced adapter error" do
      # Filled in by Plan 14-07.
      assert true
    end
  end

  describe "suppressed path (D-12)" do
    test "policy suppression at planning checkpoint converges to :suppressed" do
      # Filled in by Plan 14-07.
      assert true
    end
  end

  describe "manual cancelled path (D-12)" do
    test "manual transition to :cancelled converges as terminal" do
      # Filled in by Plan 14-07.
      assert true
    end
  end

  defp _terminal_states_anchor, do: Deliveries.terminal_states()
  defp _repo_anchor, do: Repo
end
```

Notes:
- Both files MUST compile cleanly (no `mix compile --warnings-as-errors` errors).
- The `_terminal_states_anchor/0` private function exists solely to keep the `Deliveries` alias used; remove the `_repo_anchor/0` line if `Repo` would already be used elsewhere — but for the placeholder version both should exist.
- Do NOT remove `@moduletag :skip` — Plans 14-06 and 14-07 remove it when filling in real test bodies.
- Create the directory `test/chimeway/reliability/` if it does not exist (`mkdir -p test/chimeway/reliability`).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors --force 2>&1 | tail -20 &amp;&amp; mix test test/chimeway/reliability/duplicate_protection_test.exs test/chimeway/reliability/terminal_convergence_test.exs --include skip:false 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `test/chimeway/reliability/duplicate_protection_test.exs` exists and contains `defmodule Chimeway.Reliability.DuplicateProtectionTest do`.
    - `grep -c "@moduletag :skip" test/chimeway/reliability/duplicate_protection_test.exs` returns at least 1.
    - `grep -c "describe" test/chimeway/reliability/duplicate_protection_test.exs` returns at least 7 (D-02a, D-02b, D-02c, D-02d, D-14a, D-14b, D-14c).
    - File `test/chimeway/reliability/terminal_convergence_test.exs` exists and contains `defmodule Chimeway.Reliability.TerminalConvergenceTest do`.
    - `grep -c "@moduletag :skip" test/chimeway/reliability/terminal_convergence_test.exs` returns at least 1.
    - `grep -c "describe" test/chimeway/reliability/terminal_convergence_test.exs` returns 6 (succeeded, retries_exhausted, permanent_failure, bounced, suppressed, manual cancelled).
    - `mix compile --warnings-as-errors --force` exits 0.
    - `mix test test/chimeway/reliability/duplicate_protection_test.exs test/chimeway/reliability/terminal_convergence_test.exs` exits 0 with `0 failures` (skipped tests don't fail).
  </acceptance_criteria>
  <done>Both files exist, are skipped, compile clean, and `mix test` reports 0 failures with skipped tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold attempt_history_test.exs and retry_exhaustion_test.exs</name>
  <files>test/chimeway/reliability/attempt_history_test.exs, test/chimeway/reliability/retry_exhaustion_test.exs</files>
  <read_first>
    - test/chimeway/dispatch/oban_worker_test.exs (lines 23-42 setup pattern; lines 1-6 Test.ObanWorkerFailingAdapter pattern)
    - test/support/chimeway/dispatch_helpers.ex (create_pending_delivery)
    - .planning/phases/14-delivery-reliability-hardening/14-RESEARCH.md (lines 720-787 — test map; lines 433-486 — Pattern 7 Oban.Testing perform_job/3 attempt: option)
    - .planning/phases/14-delivery-reliability-hardening/14-PATTERNS.md (lines 578-672 — attempt_history_test and retry_exhaustion_test analogs)
    - .planning/phases/14-delivery-reliability-hardening/14-VALIDATION.md (lines 55-60, 82-83)
  </read_first>
  <action>
Create two new test files. Both use `Chimeway.DataCase, async: false` and `Oban.Testing` because they exercise `perform_job/3` and the Oban worker. Both tagged `@moduletag :skip` so placeholder describes do not run.

File 1: `test/chimeway/reliability/attempt_history_test.exs`
```elixir
defmodule Chimeway.Reliability.AttemptHistoryTest do
  @moduledoc """
  REL-02 — attempt history schema additions (D-07): attempt_number ordinality,
  error_class taxonomy, concurrent attempt_number race.

  Phase 14 Wave 0 scaffold. All describes are skipped until Plan 14-07 fills them in.
  """

  use Chimeway.DataCase, async: false
  use Oban.Testing, repo: Chimeway.Repo

  @moduletag :oban
  @moduletag :skip

  import Chimeway.Test.DispatchHelpers

  alias Chimeway.{Deliveries, DeliveryAttempt, Dispatch.ObanWorker, Repo}

  setup do
    Application.put_env(:chimeway, :adapter, Chimeway.Adapters.Test)
    Chimeway.Adapters.Test.clear()

    on_exit(fn ->
      Application.put_env(:chimeway, :adapter, Chimeway.Adapters.Logger)
    end)

    :ok
  end

  describe "attempt_number ordinality (REL-02)" do
    test "first attempt for a delivery has attempt_number == 1" do
      # Filled in by Plan 14-07.
      assert true
    end

    test "subsequent attempts increment 1, 2, 3... contiguously" do
      # Filled in by Plan 14-07.
      assert true
    end
  end

  describe "error_class taxonomy (REL-02)" do
    test "succeeded outcome -> error_class is nil" do
      # Filled in by Plan 14-07.
      assert true
    end

    test "temporary outcome -> error_class == \"temporary\"" do
      # Filled in by Plan 14-07.
      assert true
    end

    test "permanent outcome -> error_class == \"permanent\"" do
      # Filled in by Plan 14-07.
      assert true
    end

    test "bounced outcome -> error_class == \"bounced\"" do
      # Filled in by Plan 14-07.
      assert true
    end

    test "changeset rejects error_class outside the whitelist" do
      # Filled in by Plan 14-07.
      assert true
    end
  end

  describe "concurrent attempt_number race (D-14, RESEARCH Pitfall 3)" do
    test "concurrent record_attempt calls do not duplicate attempt_number" do
      # Filled in by Plan 14-07. Pattern: Task.async_stream + Sandbox.allow.
      assert true
    end
  end

  defp _anchor, do: {Deliveries.terminal_states(), DeliveryAttempt, ObanWorker, Repo, create_pending_delivery: true}
end
```

File 2: `test/chimeway/reliability/retry_exhaustion_test.exs`
```elixir
defmodule Chimeway.Test.RetryFailingAdapter do
  @moduledoc """
  Test adapter that always returns {:error, :temporary, _}. Used by retry_exhaustion_test.exs.
  Defined at file top-level so it compiles before the test module references it.
  """
  @behaviour Chimeway.Adapter

  @impl Chimeway.Adapter
  def deliver(_delivery, _config), do: {:error, :temporary, %{reason: "test_failure"}}
end

defmodule Chimeway.Reliability.RetryExhaustionTest do
  @moduledoc """
  REL-02 — Oban-driven retry contract: transient -> {:error, _} for retry,
  permanent/bounced -> :ok no retry, max_attempts -> :cancelled retries_exhausted.

  Phase 14 Wave 0 scaffold. All describes are skipped until Plan 14-07 fills them in.
  """

  use Chimeway.DataCase, async: false
  use Oban.Testing, repo: Chimeway.Repo

  @moduletag :oban
  @moduletag :skip

  import Chimeway.Test.DispatchHelpers

  alias Chimeway.{Deliveries, DeliveryAttempt, Dispatch.ObanWorker, Repo}

  setup do
    Application.put_env(:chimeway, :adapter, Chimeway.Adapters.Test)
    Chimeway.Adapters.Test.clear()

    on_exit(fn ->
      Application.put_env(:chimeway, :adapter, Chimeway.Adapters.Logger)
    end)

    :ok
  end

  describe "transient failure on attempt 1..n-1 returns {:error, _} (REL-02)" do
    test "perform_job/3 with attempt: 1 returns {:error, _} when adapter is :temporary" do
      # Filled in by Plan 14-07.
      assert true
    end

    test "delivery row stays :failed (not yet :cancelled) on intermediate attempts" do
      # Filled in by Plan 14-07.
      assert true
    end
  end

  describe "exhaustion on final attempt (REL-02 / D-10 / D-11)" do
    test "perform_job/3 with attempt: max_attempts writes :cancelled retries_exhausted and returns :ok" do
      # Filled in by Plan 14-07.
      assert true
    end

    test "exhaustion sets suppression_reason to \"retries_exhausted\"" do
      # Filled in by Plan 14-07.
      assert true
    end
  end

  describe "drain_queue end-to-end exhaustion (REL-02 integration)" do
    @tag :integration
    test "always-failing adapter drains 5 attempts then converges to terminal :cancelled" do
      # Filled in by Plan 14-07.
      assert true
    end
  end

  defp _anchor, do: {Deliveries.terminal_states(), DeliveryAttempt, ObanWorker, Repo, create_pending_delivery: true}
end
```

Notes:
- Both files include `@moduletag :oban` (because they use `Oban.Testing`) and `@moduletag :skip` (placeholder).
- The `Chimeway.Test.RetryFailingAdapter` module is defined at top-of-file (matches the existing pattern in `oban_worker_test.exs:1-6` for `Chimeway.Test.ObanWorkerFailingAdapter`). This adapter is used by Plan 14-07.
- The `_anchor/0` private function references all aliases so they don't generate unused-alias warnings while skipped.
- Plans 14-07 will remove `@moduletag :skip` and replace placeholder asserts.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors --force 2>&1 | tail -20 &amp;&amp; mix test test/chimeway/reliability/attempt_history_test.exs test/chimeway/reliability/retry_exhaustion_test.exs --include skip:false 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `test/chimeway/reliability/attempt_history_test.exs` exists and contains `defmodule Chimeway.Reliability.AttemptHistoryTest do`.
    - File contains `use Oban.Testing, repo: Chimeway.Repo`.
    - `grep -c "describe" test/chimeway/reliability/attempt_history_test.exs` returns 3 (ordinality, taxonomy, concurrent).
    - File `test/chimeway/reliability/retry_exhaustion_test.exs` exists and contains BOTH `defmodule Chimeway.Test.RetryFailingAdapter do` and `defmodule Chimeway.Reliability.RetryExhaustionTest do`.
    - File contains `def deliver(_delivery, _config), do: {:error, :temporary, %{reason: "test_failure"}}`.
    - `grep -c "describe" test/chimeway/reliability/retry_exhaustion_test.exs` returns 3 (transient, exhaustion, drain_queue).
    - `mix compile --warnings-as-errors --force` exits 0.
    - `mix test test/chimeway/reliability/attempt_history_test.exs test/chimeway/reliability/retry_exhaustion_test.exs` exits 0 with `0 failures`.
  </acceptance_criteria>
  <done>Both files exist with the test adapter module, are skipped, compile clean, and `mix test` reports 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → Repo | sandboxed Postgres connection — already gated by `Chimeway.DataCase` and `Ecto.Adapters.SQL.Sandbox` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | T (Tampering) | Skipped placeholder describes | mitigate | `@moduletag :skip` is the documented mechanism; Plans 14-06/14-07 explicitly remove it. Without removal, no false confidence: `mix test` reports skipped, not passing. |
| T-14-02 | I (Information disclosure) | Test adapter modules in test/chimeway/reliability/ | accept | `Chimeway.Test.RetryFailingAdapter` only returns synthetic `:temporary` — no real provider data. Same risk surface as existing `ObanWorkerFailingAdapter`. |
| T-14-03 | D (Denial of service) | `mix compile` warnings-as-errors with unused aliases in placeholder files | mitigate | Each placeholder uses an `_anchor/0` private function referencing all aliases so warnings don't appear. Verified via `mix compile --warnings-as-errors`. |

No `high` severity threats. Wave 0 is test scaffolding with no production code touched.
</threat_model>

<verification>
- `mix compile --warnings-as-errors --force` exits 0 (no unused-alias warnings).
- `mix test test/chimeway/reliability/` exits 0 with `0 failures`.
- All four files exist, all four are tagged `@moduletag :skip`.
- `Chimeway.Test.RetryFailingAdapter` is defined exactly once across the codebase (`grep -rl "defmodule Chimeway.Test.RetryFailingAdapter"`).
- `mix ci` passes (format, compile, credo, test).
</verification>

<success_criteria>
- All four Wave 0 test files exist in `test/chimeway/reliability/`.
- All four files have `@moduletag :skip` so they do not affect existing test runs.
- `Chimeway.Test.RetryFailingAdapter` (always-`:temporary` adapter) is defined for use by Plan 14-07.
- `mix ci` green.
- No production code changed.
</success_criteria>

<output>
After completion, create `.planning/phases/14-delivery-reliability-hardening/14-01-SUMMARY.md` documenting:
- Four files created with their describe headers (placeholder content list)
- The `Chimeway.Test.RetryFailingAdapter` module location
- Confirmation that all describes are skipped
- `mix ci` output (or summary thereof)
</output>
