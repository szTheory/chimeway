---
phase: 88-cache-correctness-compile-once
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - test/chimeway/ci_observability_contract_test.exs
autonomous: true
requirements: [CACHE-01, CACHE-03, CACHE-04]
must_haves:
  truths:
    - "A `build` producer job exists in ci.yml (D-09): it runs `setup-beam` (with `id: beam`), then `mix deps.compile` + `mix compile` in `MIX_ENV=test`, and saves two full `actions/cache` entries — a source `deps` cache (path `deps`) and a compiled `build-test` cache (path `_build`)."
    - "Both producer cache keys follow the schema `<role>-${{ runner.os }}-${{ steps.beam.outputs.elixir-version }}-${{ steps.beam.outputs.otp-version }}-${{ hashFiles('mix.lock') }}` (root `mix.lock`, not the double-star glob), with `<role>` ∈ {`deps`, `build-test`} (D-01/D-02/D-03/D-04)."
    - "The `lint` lane runs in `MIX_ENV=test` (D-06), declares `needs: [build]`, and restores BOTH the shared `deps` and `build-test` caches via `actions/cache/restore` (restore-only) with `fail-on-cache-miss: true` — its former full `actions/cache` step and its broad `${{ runner.os }}-mix-` restore-key are gone (D-10)."
    - "`install_golden_contract` declares `needs: [build]`, restores the same two shared caches restore-only with `fail-on-cache-miss: true`, and runs an explicit `mix compile --warnings-as-errors` immediately before `mix ecto.create` (D-13/CACHE-03)."
    - "`build` is absent from the `needs:` arrays of both `pr-gate` and `ci-gate`; `release_gate_contract_test.exs` stays green (ci-gate needs exactly 14 lanes, pr-gate exactly its 4-lane subset) (D-11)."
    - "`ci_observability_contract_test.exs` still refutes `--warnings-as-errors` for the other 13 build lanes but exempts `install_golden_contract` from that refute (D-14)."
    - statement: "On a live run, a missing producer cache makes each consumer fail loudly via `fail-on-cache-miss: true` rather than silently cold-compiling and passing green."
      verification: backstop
  artifacts:
    - ".github/workflows/ci.yml (new `build` producer job; `lint` and `install_golden_contract` converted to restore-only consumers; explicit warnings-as-errors compile on install_golden)"
    - "test/chimeway/ci_observability_contract_test.exs (D-14: the `--warnings-as-errors` refute scoped away from install_golden_contract)"
  key_links:
    - "Consumers reuse the producer's warm caches ONLY because their `build-test`/`deps` key strings are byte-identical to the producer's; any drift in the `steps.beam.outputs.*` interpolation breaks the collision and forces a cold recompile."
    - "`aggregate-gate.sh` treats `skipped` as failure, so a failed `build` skips its consumers and the aggregate gate still fails — this is what makes `build` safe to keep OUT of the `pr-gate`/`ci-gate` `needs:` arrays."
---

<objective>
Prove the compile-once architecture end-to-end on the colliding critical path before any fan-out: stand up the single `build` producer job (compile deps + app once in `MIX_ENV=test`, save a split `deps` + `build-test` cache under the resolved-toolchain key schema), convert `lint` to the first restore-only consumer, then convert `install_golden_contract` (the 373s critical-path lane) into a consumer that runs an explicit `mix compile --warnings-as-errors` before `ecto.create`. This is the tracer slice: producer → one consumer → a second consumer + CACHE-03 + the one forced contract edit, all green under both contract tests.

Purpose: De-risk the entire phase on the agent's best early-context tokens. If the producer/consumer wiring, the key schema, or the gate-safety story is wrong, we learn it after TWO lanes, not after all fourteen.
Output: `ci.yml` carrying a `build` producer job plus `lint` and `install_golden_contract` converted to `needs: [build]` restore-only consumers, and `ci_observability_contract_test.exs` with the D-14 refute relaxed to install_golden scope.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/88-cache-correctness-compile-once/88-CONTEXT.md
@.github/workflows/ci.yml
@test/chimeway/ci_observability_contract_test.exs
@test/chimeway/release_gate_contract_test.exs
@scripts/ci/aggregate-gate.sh
</context>

## Artifacts this phase produces

Structural CI changes introduced by this plan (the tracer slice; Plan 02 fans the identical pattern out to the remaining default-graph lanes):
- A new `build:` producer job in `ci.yml` — checkout → `setup-beam` (`id: beam`) → two full `actions/cache` steps (`id: cache_deps` for `deps`, `id: cache_build_test` for `_build`, both placed before `deps.get` so they restore-then-save) → `mix local.rebar/hex --force` + `mix deps.get` → `mix deps.compile` + `mix compile` in `MIX_ENV=test`. No postgres service, no DATABASE_URL (compile only).
- The shared key schema string, used verbatim by every default-graph consumer: `build-test-${{ runner.os }}-${{ steps.beam.outputs.elixir-version }}-${{ steps.beam.outputs.otp-version }}-${{ hashFiles('mix.lock') }}` and the `deps-…` sibling. The literal `"1.19"` / `"27"` at ci.yml:33-34 is the documented fallback if a future pinned setup-beam SHA drops the `elixir-version`/`otp-version` outputs (the outputs are research-confirmed present for the current SHA; `erlang-version` is a README typo and must never be used).
- `lint` and `install_golden_contract` converted from a single full `actions/cache` to two `actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830` steps (restore-only, `fail-on-cache-miss: true`) plus `needs: [build]`. Each keeps `id: cache_main` on its `build-test` restore step (so the Phase-87 obs-summary keeps reading `steps.cache_main.outputs.*`) and adds `id: cache_deps` on its `deps` restore step.

<tasks>

<task type="tracer">
  <name>Task 1: Stand up the `build` producer job and wire `lint` as the first restore-only consumer (end-to-end tracer)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml lines 26-69 (current `lint` lane: `setup-beam` 31-34 needs `id: beam`; the full `actions/cache` step 35-43 with its broad `${{ runner.os }}-mix-` restore-key is what gets replaced; the trailing obs-summary step 60-69 whose `CACHE_MAIN_*` env must keep resolving to a `cache_main` step)
    - .github/workflows/ci.yml lines 164-221 (the `test` matrix lane — proves version-in-key interpolation at 199 and shows the `id: cache_main` + trailing obs-summary pattern that the observability contract requires on every build lane)
    - scripts/ci/aggregate-gate.sh (the `!= success` → fail loop at 15-21: `skipped` counts as failure, which is why a transitive `build` producer is vacuous-pass-safe without being in the gate `needs:`)
    - test/chimeway/release_gate_contract_test.exs lines 19-20 and 236-269 (`@ci_gate_lanes` = 14, `@pr_gate_lanes` = 4, and the count assertions `build` must NOT perturb)
    - .planning/phases/88-cache-correctness-compile-once/88-CONTEXT.md (decisions D-01, D-02, D-03, D-04, D-06, D-09, D-10, D-11)
  </read_first>
  <action>
    Add a new `build:` producer job to `ci.yml` (place it as the first job under `jobs:`, immediately before `lint:`). Give it `name: Build (compile-once producer)`, `runs-on: ubuntu-latest`, and a job-level `env:` block setting `MIX_ENV: test` (no postgres service, no DATABASE_URL — it only compiles). Steps in order: (1) `actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5`; (2) `erlef/setup-beam@8251c48667b97e88a0a24ec512f5b72a039fcea7` with `id: beam` and `elixir-version: "1.19"` / `otp-version: "27"`; (3) a full `actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830` step `id: cache_deps` with `path: deps` and `key: deps-${{ runner.os }}-${{ steps.beam.outputs.elixir-version }}-${{ steps.beam.outputs.otp-version }}-${{ hashFiles('mix.lock') }}` and a narrow env-scoped `restore-keys:` of `deps-${{ runner.os }}-${{ steps.beam.outputs.elixir-version }}-${{ steps.beam.outputs.otp-version }}-`; (4) a full `actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830` step `id: cache_build_test` with `path: _build` and `key: build-test-${{ runner.os }}-${{ steps.beam.outputs.elixir-version }}-${{ steps.beam.outputs.otp-version }}-${{ hashFiles('mix.lock') }}` and a matching narrow `restore-keys:` of `build-test-${{ runner.os }}-${{ steps.beam.outputs.elixir-version }}-${{ steps.beam.outputs.otp-version }}-`; (5) a `run:` block of `mix local.rebar --force` + `mix local.hex --force` + `mix deps.get`; (6) `- run: mix deps.compile`; (7) `- run: mix compile`. Use the root `mix.lock` glob everywhere (NOT the double-star form) so the key does not churn on nested/partner lockfile bumps (D-04). Do NOT give `build` a `needs:` and do NOT add `build` to any gate (D-11). Do NOT add a postgres service.

    Now convert the `lint` lane into the first consumer. Add `id: beam` to its `setup-beam` step (lines 31-34). Add a job-level `env:` block with `MIX_ENV: test` (D-06 — credo is `only: [:dev, :test]`, so format/compile/credo run in `:test`). Add `needs: [build]` to the lane. Replace the single full `actions/cache` step (35-43) with TWO `actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830` steps: first `id: cache_deps`, `path: deps`, `key:` the exact `deps-…` string above, `fail-on-cache-miss: true`, and NO `restore-keys:`; second `id: cache_main`, `path: _build`, `key:` the exact `build-test-…` string above, `fail-on-cache-miss: true`, and NO `restore-keys:`. Keeping `id: cache_main` on the `build-test` restore preserves the trailing obs-summary step's `CACHE_MAIN_*` wiring (60-69) and the observability contract's `id: cache_main` assertion. Optionally extend that obs-summary step's `env:` with a `CACHE_DEPS_HIT`/`CACHE_DEPS_MATCHED`/`CACHE_DEPS_PRIMARY` triple sourced from `steps.cache_deps.outputs.*` so the deps cache also shows in the summary. Leave `install_golden_contract` and every other lane untouched — Task 2 owns install_golden, Plan 02 owns the rest.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/release_gate_contract_test.exs test/chimeway/ci_observability_contract_test.exs && grep -qE '^  build:' .github/workflows/ci.yml && grep -q 'id: beam' .github/workflows/ci.yml && grep -q 'actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830' .github/workflows/ci.yml && grep -q 'fail-on-cache-miss: true' .github/workflows/ci.yml && grep -q "build-test-\${{ runner.os }}-\${{ steps.beam.outputs.elixir-version }}-\${{ steps.beam.outputs.otp-version }}-\${{ hashFiles('mix.lock') }}" .github/workflows/ci.yml && echo TRACER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE '^  build:'` matches: a `build` producer job exists at job indentation.
    - The `build` job's two `actions/cache` steps use the `deps-…` and `build-test-…` resolved-toolchain keys against root `mix.lock`, and compile via `mix deps.compile` then `mix compile` in `MIX_ENV=test`.
    - The `lint` lane has `MIX_ENV: test`, `needs: [build]`, two `actions/cache/restore` steps (`cache_deps` + `cache_main`) each with `fail-on-cache-miss: true` and no `restore-keys:`.
    - `mix test test/chimeway/release_gate_contract_test.exs` is green — ci-gate still needs exactly 14 lanes and pr-gate exactly 4; `build` is in neither.
    - `mix test test/chimeway/ci_observability_contract_test.exs` is green — `lint` still carries `id: cache_main` and its trailing `if: always()` obs-summary step.
  </acceptance_criteria>
  <done>The `build` producer job compiles the plain-hex graph once in `:test` and saves a split `deps` + `build-test` cache under the resolved-toolchain schema; `lint` consumes both caches restore-only via `needs: [build]` with `fail-on-cache-miss: true`; and both contract tests stay green with `build` outside every gate.</done>
</task>

<task type="auto">
  <name>Task 2: Convert `install_golden_contract` to a consumer, add the explicit warnings-as-errors compile (CACHE-03), and relax the D-14 contract refute</name>
  <files>.github/workflows/ci.yml, test/chimeway/ci_observability_contract_test.exs</files>
  <read_first>
    - .github/workflows/ci.yml lines 930-1005 (the `install_golden_contract` lane: `setup-beam` 966-970 needs `id: beam`; the full `actions/cache` step 971-980 with its broad `${{ runner.os }}-mix-` restore-key is replaced; `mix ecto.create --quiet` at 990 is the insertion anchor; every body step is gated `if: steps.detect.outputs.run == 'true'`)
    - test/chimeway/ci_observability_contract_test.exs line 18 (`@build_lanes`) and lines 248-256 (the `refute String.contains?(lane_block, "--warnings-as-errors")` loop over all 14 lanes that D-14 must scope away from install_golden)
    - .planning/phases/88-cache-correctness-compile-once/88-CONTEXT.md (decisions D-13, D-14)
  </read_first>
  <action>
    Convert `install_golden_contract` into a consumer exactly like `lint`. Add `id: beam` to its `setup-beam` step (966-970). Add `needs: [build]` to the lane. Replace the single full `actions/cache` step (971-980) with TWO `actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830` steps — `id: cache_deps` (`path: deps`, the exact `deps-…` key, `fail-on-cache-miss: true`, no `restore-keys:`) and `id: cache_main` (`path: _build`, the exact `build-test-…` key, `fail-on-cache-miss: true`, no `restore-keys:`). Preserve the lane's existing `if: steps.detect.outputs.run == 'true'` guard on the cache/restore steps (keep it on both restore steps). The lane already carries `MIX_ENV: test` at 947 — leave it.

    Add the CACHE-03 critical-path step (D-13): insert a new `- run: mix compile --warnings-as-errors` step (with the same `if: steps.detect.outputs.run == 'true'` guard) immediately BEFORE the `mix ecto.create --quiet` step at line 990, and AFTER the existing `scripts/ci/obs-recompile.sh` probe step (986-989). This surfaces the compile-warnings gate as its own fast step on already-warm artifacts, so the Phase-87 timing summary shows `ecto.create` as a fast DB-only step instead of a disguised ~135s compile. Add this literal flag ONLY here — do NOT put it in `scripts/ci/obs-recompile.sh` (that would wrongly apply it to all 14 build lanes and perturb committed fixtures).

    Relax the D-14 assertion in `ci_observability_contract_test.exs` (248-256): change the loop so it iterates `@build_lanes -- ["install_golden_contract"]` (the other 13 lanes still must not introduce the compile-warnings flag), and add a paired positive assertion that the `install_golden_contract` lane block DOES contain both `mix compile --warnings-as-errors` and `mix ecto.create`, with the compile step appearing before `ecto.create`. Keep the assertion message pointing at CACHE-03 as the sanctioned exception. Scope the relaxation to `install_golden_contract` only — do not open the door to Phase 89's separate `ci.test --warnings-as-errors` (CONC-03).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/ci_observability_contract_test.exs test/chimeway/release_gate_contract_test.exs && awk '/^  install_golden_contract:/{f=1} f&&/mix compile --warnings-as-errors/{c=NR} f&&/mix ecto.create/{if(c&&NR>c){print "CACHE03_ORDER_OK"; exit}}' .github/workflows/ci.yml | grep -q CACHE03_ORDER_OK && echo INSTALL_GOLDEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `install_golden_contract` declares `needs: [build]` and restores the shared `deps` + `build-test` caches restore-only with `fail-on-cache-miss: true`, keeping its `steps.detect.outputs.run == 'true'` guard.
    - The lane runs `mix compile --warnings-as-errors` after the `obs-recompile.sh` probe and before `mix ecto.create --quiet` (proven by the awk ordering check emitting `CACHE03_ORDER_OK`).
    - `mix test test/chimeway/ci_observability_contract_test.exs` is green: the compile-warnings refute now exempts `install_golden_contract` and still fires for the other 13 lanes, plus the new positive install_golden assertion passes.
    - `mix test test/chimeway/release_gate_contract_test.exs` is green: ci-gate 14 / pr-gate 4 counts unchanged, `install_golden_contract` still runs `mix verify.install_golden` and remains folded into ci-gate.
  </acceptance_criteria>
  <done>`install_golden_contract` consumes the producer's warm caches via `needs: [build]`, runs an explicit warnings-as-errors compile before `ecto.create` (CACHE-03), and the observability contract's compile-warnings refute is scoped away from install_golden while still guarding the other 13 lanes — both contract suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `build` producer job → default-graph consumer lanes | Compiled `_build/test` + `deps` source cross from one job to another via cache; a wrong-key or stale artifact would be silently reused |
| ci.yml required-check set → ruleset 18486746 on `main` | The `pr-gate`/`ci-gate` lane names/counts are the contract; renaming or recounting them can strand the required check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-01 | Tampering | new `build` job wired into the gate set | high | mitigate | `build` is kept OUT of both `pr-gate` and `ci-gate` `needs:`; `release_gate_contract_test.exs` asserts ci-gate=14 / pr-gate=4 and runs in every task's `<verify>`. |
| T-88-02 | Spoofing / DoS (vacuous pass) | consumer cache restore | high | mitigate | Consumers use `actions/cache/restore` with `fail-on-cache-miss: true` — a missing producer cache fails the lane loudly instead of cold-compiling and passing; `aggregate-gate.sh` treats the resulting `skipped` consumers as failure. |
| T-88-03 | Tampering | cache key collision across `MIX_ENV`/toolchain | medium | mitigate | Keys embed the `build-test`/`deps` role + `runner.os` + resolved Elixir + resolved OTP + root `mix.lock`; the broad `${{ runner.os }}-mix-` restore-key that could fall back onto a wrong-`MIX_ENV` tree is removed from both converted lanes. |
| T-88-SC | Tampering | GitHub Actions supply chain | low | accept | No new actions and no `npm`/`pip`/`cargo` installs this phase; `actions/cache/restore` reuses the already-pinned `actions/cache` commit SHA `0057852bfaa89a56745cba8c7296529d2fc39830`, and `setup-beam` stays on its pinned SHA. |
</threat_model>

<verification>
- `mix test test/chimeway/release_gate_contract_test.exs` green — `build` is absent from `pr-gate`/`ci-gate` needs; ci-gate=14, pr-gate=4.
- `mix test test/chimeway/ci_observability_contract_test.exs` green — every build lane keeps `id: cache_main` + trailing `if: always()` obs-summary; the compile-warnings refute exempts only `install_golden_contract`.
- `grep -qE '^  build:'` matches; the producer's two caches use the `deps-…`/`build-test-…` resolved-toolchain keys on root `mix.lock`.
- `grep -q 'actions/cache/restore@…'` and `grep -q 'fail-on-cache-miss: true'` match on the converted consumers.
- The awk ordering check confirms `mix compile --warnings-as-errors` precedes `mix ecto.create` inside `install_golden_contract`.
</verification>

<success_criteria>
The compile-once architecture is proven end-to-end on the two hardest lanes: a single `build` producer job compiles the plain-hex graph once in `:test` and publishes a split `deps` + `build-test` cache under a `MIX_ENV`-and-toolchain-scoped key; `lint` and the 373s `install_golden_contract` both consume it restore-only via `needs: [build]` with `fail-on-cache-miss: true`; CACHE-03's explicit warnings-as-errors compile fronts `ecto.create`; and both contract suites stay green with `build` deliberately outside every gate. Plan 02 can now fan the identical pattern out to the remaining default-graph lanes with confidence.
</success_criteria>

<output>
Create `.planning/phases/88-cache-correctness-compile-once/88-01-SUMMARY.md` when done.
</output>
