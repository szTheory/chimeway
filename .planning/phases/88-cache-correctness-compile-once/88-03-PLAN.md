---
phase: 88-cache-correctness-compile-once
plan: 03
type: execute
wave: 3
depends_on: ["88-02"]
files_modified:
  - .planning/CI-PERF-BASELINE.md
autonomous: false
requirements: [CACHE-05]
must_haves:
  truths:
    - "The delta-ledger table in `.planning/CI-PERF-BASELINE.md` has its `Phase 88 after` and `Δ` cells filled for all four rows, sourced from a warm `main` run whose permalink is cited in this plan's SUMMARY."
    - "The `**Recorded:**` / `**Commit:**` / `**Run:**` header and the entire `Baseline` column of `.planning/CI-PERF-BASELINE.md` are unchanged — they remain the frozen pre-optimization reference point for the whole milestone."
    - statement: "Two consecutive warm `main` runs on an identical `mix.lock` show near-zero dependency recompilation — `obs-recompile.sh` renders a deps count of `0` and `obs-summary.sh` classifies the `build-test` cache as HIT — and warm `ci-gate` wall-clock is under ~3 minutes (the cold path is out of scope, Phase 90)."
      verification: backstop
  artifacts:
    - ".planning/CI-PERF-BASELINE.md (Phase 88 after-row values + Δ appended; header and Baseline column frozen)"
  key_links:
    - "The CACHE-05 win is observable only on a live warm run via the Phase-87 OBS probe (`obs-recompile.sh` → `0` deps + `obs-summary.sh` → cache HIT); the baseline doc records that live evidence as a run-link delta so the win is provable, not a feeling."
    - "The delta ledger is the append-only anchor every later v1.16 phase adds its own after-row to; overwriting the header or Baseline column would break that shared reference point."
---

<objective>
Prove the compile-once win as a durable run-link delta: fill the `Phase 88 after` + `Δ` cells of the frozen delta ledger from a real warm `main` run, then human-verify — via the Phase-87 OBS probe on two consecutive identical-lock runs — that dependency recompilation is near-zero (cache HIT, deps `0`) and warm `ci-gate` wall-clock is under ~3 minutes. This closes CACHE-05: the milestone's central claim ("`_build` actually warms across runs") is now backed by committed numbers and a persistent run page, not an assertion.

Purpose: Make the CACHE-01..04 structural change measurably true. Without a warm-run delta the whole phase is unproven; with it, every downstream phase inherits a trustworthy before/after anchor.
Output: `.planning/CI-PERF-BASELINE.md` with the four after-values + deltas filled (header + Baseline column untouched), and a human-verified warm-run permalink cited in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/88-cache-correctness-compile-once/88-CONTEXT.md
@.planning/CI-PERF-BASELINE.md
@scripts/ci/obs-recompile.sh
@scripts/ci/obs-summary.sh
</context>

## Artifacts this phase produces

- `.planning/CI-PERF-BASELINE.md` — the four `Phase 88 after` cells and their `Δ` filled in from a warm `main` run (the doc already carries the empty `Phase 88 after` / `Δ` columns and the frozen `Baseline` column + `Recorded:`/`Commit:`/`Run:` header from Phase 87). No new file; a scoped edit of the existing table only. The measured values are sourced from the OBS-01/02/03 job-summary instrumentation (cache HIT/MISS table, deps/app recompile counts, per-step timing) that Phase 87 added to every build lane.

<tasks>

<task type="auto">
  <name>Task 1: Fill the Phase 88 after-row + Δ in the frozen delta ledger</name>
  <files>.planning/CI-PERF-BASELINE.md</files>
  <read_first>
    - .planning/CI-PERF-BASELINE.md (the delta-ledger table with columns `Metric | Baseline | Phase 88 after | Δ` and the four rows; the `**Recorded:**`/`**Commit:**`/`**Run:**` header and the "How later phases use this delta ledger" note that mandate the header + Baseline column stay frozen)
    - .planning/phases/88-cache-correctness-compile-once/88-CONTEXT.md (decision D-15 — prove by run-link delta; "under ~3 min" is the WARM target, cold path is Phase 90)
    - scripts/ci/obs-summary.sh and scripts/ci/obs-recompile.sh (the OBS probe whose job-summary tables supply the after-numbers: cache HIT/MISS, deps/app recompile counts, per-step timing)
  </read_first>
  <action>
    Once this phase has merged to `main` and two consecutive warm `main` CI runs on an identical `mix.lock` exist (see Task 2 — the runs are triggered/observed there), fill the `Phase 88 after` and `Δ` cells for all four existing rows of the delta ledger in `.planning/CI-PERF-BASELINE.md`, sourcing each number from the second (fully warm) run's OBS job summaries: (1) `ci-gate wall-clock` — the warm run's ci-gate wall-clock (target under ~3 min); (2) `install_golden job` — the warm `install_golden_contract` job duration; (3) `hidden compile in ecto.create` — now near-zero, since CACHE-03 moved the compile to its own explicit `mix compile --warnings-as-errors` step and the OBS timing table shows `ecto.create` as a fast DB-only step; (4) `dep recompile, 3 identical-lock runs` — the deps recompile count from `obs-recompile.sh` (expect `0`) with the `build-test` cache classified HIT. Express each `Δ` as the concrete improvement (e.g. `-Ns` or `373s → Ms`). Add the warm run's permalink in the exact form `https://github.com/szTheory/chimeway/actions/runs/<digits>` to this plan's SUMMARY (not the frozen header).

    Do NOT touch the `**Recorded:**` / `**Commit:**` / `**Run:**` header line or any cell in the `Baseline` column — those are the fixed pre-optimization reference for the whole v1.16 milestone. Do NOT invent numbers: if the warm runs do not yet exist or the numeric run id cannot be resolved in this environment, do NOT write placeholder values — escalate (this plan is `autonomous: false`) so the maintainer supplies the warm-run permalink and the OBS-sourced after-values, then complete the edit. Keep "under ~3 min" framed as the WARM target; do not record or gate a cold-path number (that is Phase 90).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && grep -q 'Phase 88 after' .planning/CI-PERF-BASELINE.md && grep -q '30410779443' .planning/CI-PERF-BASELINE.md && grep -Eq '373[–-]395s' .planning/CI-PERF-BASELINE.md && awk -F'|' 'NF>=5 && $2 ~ /[A-Za-z]/ && $2 !~ /Metric/ && $3 !~ /^-+$/ {a=$4;d=$5;gsub(/ /,"",a);gsub(/ /,"",d); tot++; if(length(a)>0 && length(d)>0) ok++} END{exit !(tot==4 && ok==4)}' .planning/CI-PERF-BASELINE.md && echo BASELINE_AFTER_OK
  </verify>
  <acceptance_criteria>
    - All four `Phase 88 after` cells are non-empty and each `Δ` expresses the concrete improvement, sourced from the warm run's OBS summaries.
    - The `**Recorded:**` / `**Commit:**` / `**Run:**` header (still citing run `30410779443` and commit `bc8ad4d…`) and every `Baseline` column cell are byte-for-byte unchanged.
    - No placeholder or invented values are committed; the warm-run permalink appears in this plan's SUMMARY.
    - The `hidden compile in ecto.create` after-value reflects the CACHE-03 move (near-zero disguised compile), and the `dep recompile` after-value is `0` with `build-test` HIT.
  </acceptance_criteria>
  <done>The delta ledger's four `Phase 88 after` + `Δ` cells are filled from a real warm `main` run, the frozen header and Baseline column are untouched, and the warm-run permalink is cited in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the warm-run compile-once win (near-zero dep recompile + sub-3-min ci-gate)</name>
  <what-built>Phase 88 wired a single `build` producer job that compiles the plain-hex graph once per run and publishes a split `deps` + `build-test` cache under a `MIX_ENV`-and-toolchain-scoped key; all default-graph `:test` lanes now consume it restore-only via `needs: [build]` with `fail-on-cache-miss: true`, and `install_golden_contract` runs an explicit warnings-as-errors compile before `ecto.create`. The delta ledger in `.planning/CI-PERF-BASELINE.md` records the resulting warm-run numbers against the frozen pre-optimization baseline.</what-built>
  <how-to-verify>
    1. After this phase merges to `main`, trigger two consecutive `main` CI runs on an identical `mix.lock` (a no-op push or `workflow_dispatch` twice, with no dependency change between them): `https://github.com/szTheory/chimeway/actions` → the `CI` workflow.
    2. On the SECOND (fully warm) run, open the `build` producer job summary and a sample of consumer lanes (`lint`, `verify_gates`, `verify_example`, `install_golden_contract`) and confirm each shows: the **Cache** table classifying the `build-test` (and `deps`) cache as EXACT HIT, and a **Recompile** row with a deps count of `0` (near-zero app recompile is acceptable).
    3. Confirm `install_golden_contract`'s **Step timing** table shows `ecto.create` as a fast DB-only step (the ~135s disguised compile is gone — it now appears as the explicit `mix compile --warnings-as-errors` step, warm).
    4. Confirm the warm run's `ci-gate` wall-clock is under ~3 minutes (the WARM target; a fresh-lock cold run is out of scope — Phase 90).
    5. Confirm no consumer lane failed with a cache-miss error (i.e. `fail-on-cache-miss` did not fire — the producer populated the shared caches as intended), and that the `test` OTP-26 leg, `verify_docs` (`:dev`), and the partner lanes each still restored their OWN keys with no clobbering.
    6. Confirm the four `Phase 88 after` values now in `.planning/CI-PERF-BASELINE.md` match what the warm run's summaries actually show, and hand back the warm-run permalink for the SUMMARY citation.
  </how-to-verify>
  <resume-signal>Type "approved" with the warm-run permalink and the four after-values, or describe which lane cold-compiled / missed cache / exceeded the ~3-min warm target.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/CI-PERF-BASELINE.md` (committed, world-readable) | Records only run permalinks, commit shas, and aggregate durations — no secrets |
| Warm-run measurement → committed after-numbers | The after-values are asserted as the milestone's CACHE-05 proof; wrong or invented numbers would misrepresent the win |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-88-08 | Tampering | overwriting the frozen baseline reference | medium | mitigate | Only the `Phase 88 after` + `Δ` cells are edited; the `Recorded:`/`Commit:`/`Run:` header and the entire `Baseline` column are left byte-for-byte unchanged (Task 1 verify greps the frozen run id + baseline figure intact). |
| T-88-09 | Repudiation | provenance of the after-numbers | low | mitigate | Numbers are sourced from the run's OBS job summaries and anchored to a persistent `actions/runs/<digits>` permalink cited in the SUMMARY; a blocking human checkpoint confirms the recorded values match the live run. |
| T-88-10 | Spoofing (green-washing the win) | claiming warm success on an unrepresentative run | medium | mitigate | The checkpoint requires TWO consecutive identical-lock runs and inspects the OBS Cache/Recompile/timing tables directly (HIT + deps `0`), not just a green check; a cache-miss or cold-compile is an explicit reject signal. |
</threat_model>

<verification>
- `.planning/CI-PERF-BASELINE.md` — all four `Phase 88 after` + `Δ` cells filled; `Recorded:`/`Commit:`/`Run:` header and `Baseline` column unchanged (Task 1 automated grep + the awk after-cell check emit `BASELINE_AFTER_OK`).
- Human checkpoint confirms, on the second warm `main` run: `build-test` cache HIT + deps recompile `0` on the sampled lanes, `ecto.create` fast in the timing table, warm `ci-gate` under ~3 min, no `fail-on-cache-miss` firing, and exception lanes (`test` OTP-26, `verify_docs`, partners) restoring their own keys without clobbering.
- Warm-run permalink cited in `88-03-SUMMARY.md`.
</verification>

<success_criteria>
CACHE-05 is satisfied: two consecutive warm `main` runs on an identical `mix.lock` demonstrably recompile near-zero dependencies (OBS probe: `build-test` HIT, deps `0`) and the warm `ci-gate` wall-clock lands under ~3 minutes, and that win is recorded as a durable run-link delta in `.planning/CI-PERF-BASELINE.md` against the frozen pre-optimization baseline — closing Phase 88's central claim that `_build` actually warms across runs.
</success_criteria>

<output>
Create `.planning/phases/88-cache-correctness-compile-once/88-03-SUMMARY.md` when done.
</output>
