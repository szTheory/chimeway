---
phase: "13-policy-preference-maturity"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/chimeway/policy/settings.ex"
  - "lib/chimeway/policy/settings/setting.ex"
  - "priv/repo/migrations/20260425000200_create_chimeway_policy_settings.exs"
  - "test/chimeway/policy_settings_test.exs"
autonomous: true
requirements: ["POL-02"]
must_haves:
  truths:
    - "Users can configure quiet hours."
    - "Users can configure delivery caps."
    - "Policy settings can be loaded for suppression evaluation."
  artifacts:
    - path: "lib/chimeway/policy/settings/setting.ex"
      provides: "Policy settings schema"
    - path: "lib/chimeway/policy/settings.ex"
      provides: "Policy settings context APIs"
    - path: "priv/repo/migrations/20260425000200_create_chimeway_policy_settings.exs"
      provides: "Policy settings table and unique index"
    - path: "test/chimeway/policy_settings_test.exs"
      provides: "Quiet-hours and cap regression coverage"
  key_links:
    - from: "lib/chimeway/policy/settings.ex"
      to: "lib/chimeway/policy/settings/setting.ex"
      via: "settings lookup helpers"
      pattern: "evaluate|quiet_hours|delivery_cap"
    - from: "lib/chimeway/policy/settings/setting.ex"
      to: "priv/repo/migrations/20260425000200_create_chimeway_policy_settings.exs"
      via: "schema/table contract"
      pattern: "schema \"chimeway_policy_settings\""
---

<objective>
Add first-class quiet-hours and delivery-cap settings so policy checks can suppress delivery for explicit, durable user rules.

Purpose: Make time-window and volume policy data explicit before evaluation wiring.
Output: Policy-settings schema, context APIs, and regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/chimeway/policy.ex

<interfaces>
From lib/chimeway/policy.ex:
```elixir
def evaluate(%Chimeway.Delivery{} = delivery, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add policy settings storage contract</name>
  <files>priv/repo/migrations/20260425000200_create_chimeway_policy_settings.exs, lib/chimeway/policy/settings/setting.ex</files>
  <behavior>
    - Persist one policy-settings row per recipient.
    - Store quiet-hours window data and delivery-cap data on the same durable record.
    - Reject invalid minute bounds and non-positive cap/window values.
  </behavior>
  <action>Create the `chimeway_policy_settings` table with UUID primary key, `recipient_id`, quiet-hours window fields, delivery-cap fields, timestamps, and a unique index on `recipient_id`. Add the Ecto schema and changeset that keep settings values string/number based and do not derive atoms from input.</action>
  <verify><automated>mix compile --warnings-as-errors</automated></verify>
  <done>The policy-settings table, schema, and validation contract compile cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add policy settings context APIs</name>
  <files>lib/chimeway/policy/settings.ex</files>
  <behavior>
    - `upsert_settings/1` stores or updates one recipient’s settings row.
    - `get_settings/1` returns the stored row or nil.
    - `evaluate/1` returns `{:ok, :proceed}` or `{:suppress, reason_atom}` based on the stored quiet-hours and cap rules.
  </behavior>
  <action>Add the public `Chimeway.Policy.Settings` context around the new schema module. Keep the API single-row and deterministic, reuse the stored quiet-hours/cap record for evaluation, and return fixed suppress reasons only.</action>
  <verify><automated>mix compile --warnings-as-errors && rg "upsert_settings|get_settings|evaluate" lib/chimeway/policy/settings.ex</automated></verify>
  <done>The settings context exposes explicit storage and evaluation helpers for quiet-hours and cap policy rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand policy-settings regression coverage</name>
  <files>test/chimeway/policy_settings_test.exs</files>
  <action>Write tests for settings persistence, default-enabled behavior, invalid bounds, and the composite quiet-hours/cap evaluation path. Use stable recipient fixtures so the settings row is the sole source of truth.</action>
  <verify><automated>mix test test/chimeway/policy_settings_test.exs</automated></verify>
  <done>Quiet-hours and delivery-cap policy settings are covered by executable regression tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app input → policy-settings write | Untrusted window/cap values are stored in durable policy rows. |
| policy-settings store → suppression evaluator | Evaluation trusts persisted window/cap settings to suppress delivery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | `lib/chimeway/policy/settings/setting.ex` | mitigate | Validate minute bounds, cap counts, and cap windows in the changeset before persistence. |
| T-13-02-02 | Denial of service | `lib/chimeway/policy/settings.ex` | mitigate | Keep evaluation single-row and bounded so quiet-hours/cap checks never fan out across multiple queries. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors`
- `mix test test/chimeway/policy_settings_test.exs`
- `rg "upsert_settings|get_settings|evaluate" lib/chimeway/policy/settings.ex`
</verification>

<success_criteria>
- Quiet-hours settings can be saved and loaded per recipient.
- Delivery-cap settings can be saved and loaded per recipient.
- Invalid quiet-hours or cap values fail fast in the changeset.
</success_criteria>

<output>
After completion, create `.planning/phases/13-policy-preference-maturity/13-02-SUMMARY.md`
</output>
