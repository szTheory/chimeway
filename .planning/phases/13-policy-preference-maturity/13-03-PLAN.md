---
phase: "13-policy-preference-maturity"
plan: "03"
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - "lib/chimeway/policy.ex"
  - "test/chimeway/policy_test.exs"
  - "test/chimeway/dispatch/sync_test.exs"
  - "test/chimeway/dispatch/oban_test.exs"
autonomous: true
requirements: ["POL-03"]
must_haves:
  truths:
    - "Policy suppresses blocked deliveries before enqueue."
    - "Policy suppresses blocked deliveries before adapter execution."
    - "Suppression reasons are stable, persisted, and explainable."
  artifacts:
    - path: "lib/chimeway/policy.ex"
      provides: "Combined preference and policy evaluation entry point"
    - path: "test/chimeway/policy_test.exs"
      provides: "Policy-level suppression regression coverage"
    - path: "test/chimeway/dispatch/sync_test.exs"
      provides: "Planning-time and perform-time sync suppression coverage"
    - path: "test/chimeway/dispatch/oban_test.exs"
      provides: "Planning-time and perform-time Oban suppression coverage"
  key_links:
    - from: "lib/chimeway/policy.ex"
      to: "lib/chimeway/preferences.ex"
      via: "category and channel preference checks"
      pattern: "channel_enabled\?|category_enabled\?"
    - from: "lib/chimeway/policy.ex"
      to: "lib/chimeway/policy/settings.ex"
      via: "quiet-hours and delivery-cap checks"
      pattern: "Policy\.Settings|quiet_hours|delivery_cap"
    - from: "lib/chimeway/policy.ex"
      to: "lib/chimeway/dispatch/sync.ex"
      via: "existing planning/perform call sites"
      pattern: "Policy.evaluate\(delivery,"
---

<objective>
Connect built-in preferences and policy settings to `Policy.evaluate/2` so both planning and perform dispatch paths suppress early with fixed reason atoms and durable explainability.

Purpose: Make suppression behavior explicit at every decision point.
Output: Wired policy engine plus dispatch regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/chimeway/policy.ex
@lib/chimeway/preferences.ex
@lib/chimeway/policy/settings.ex
@test/chimeway/policy_test.exs
@test/chimeway/dispatch/sync_test.exs
@test/chimeway/dispatch/oban_test.exs

<interfaces>
From lib/chimeway/policy.ex:
```elixir
def evaluate(%Delivery{} = delivery, opts \\ []) :: {:ok, :proceed} | {:suppress, atom()}
```

From lib/chimeway/preferences.ex:
```elixir
def channel_enabled?(recipient_id, notification_key, channel)
def category_enabled?(recipient_id, notification_category)
```

From lib/chimeway/policy/settings.ex:
```elixir
def evaluate(delivery)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire policy evaluation to built-in preferences and settings</name>
  <files>lib/chimeway/policy.ex</files>
  <behavior>
    - Channel-disabled deliveries still suppress as `:channel_disabled`.
    - Category-disabled deliveries suppress as `:category_disabled` using persisted category metadata.
    - Quiet-hours and delivery-cap rules suppress as fixed reason atoms from the new settings context.
  </behavior>
  <action>Update `Policy.evaluate/2` to consult channel preferences, category preferences, and `Policy.Settings.evaluate/1` in that order before returning proceed. Derive the category from `event.payload["category"]` only (never from caller-supplied dispatch options), and keep telemetry metadata string-safe.</action>
  <verify><automated>mix compile --warnings-as-errors && rg "category_enabled\?|Policy\.Settings|quiet_hours|delivery_cap" lib/chimeway/policy.ex</automated></verify>
  <done>Policy evaluation returns only fixed suppress reasons and can read the new preference/settings sources.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand policy and dispatch regression coverage</name>
  <files>test/chimeway/policy_test.exs, test/chimeway/dispatch/sync_test.exs, test/chimeway/dispatch/oban_test.exs</files>
  <action>Extend the policy fixtures so category metadata, quiet-hours settings, and delivery caps are exercised through both planning-time and perform-time paths. Assert that suppressed deliveries never reach the adapter and still persist stable suppression reasons.</action>
  <verify><automated>mix test test/chimeway/policy_test.exs test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs</automated></verify>
  <done>Planning and perform suppression are proven end-to-end in both sync and Oban dispatch suites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted notification/event data → policy decision | Policy reads category metadata from stored records, not from caller input. |
| policy decision → delivery suppression | Suppression reasons become durable product state and must remain fixed and explainable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Elevation of privilege | `lib/chimeway/policy.ex` | mitigate | Resolve category and quiet-hours/cap inputs from persisted records only, never from dispatch opts or runtime strings. |
| T-13-03-02 | Repudiation | `lib/chimeway/policy.ex` / dispatch tests | mitigate | Return fixed suppression atoms and persist them through existing delivery suppression paths so traces stay explainable. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors`
- `mix test test/chimeway/policy_test.exs test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs`
- `rg "category_disabled|quiet_hours|delivery_cap|channel_disabled" test/chimeway/policy_test.exs test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs`
</verification>

<success_criteria>
- Blocked deliveries suppress before enqueue and before adapter execution.
- Category, quiet-hours, and delivery-cap failures persist fixed reason atoms.
- Sync and Oban behavior stays aligned on suppression paths.
</success_criteria>

<output>
After completion, create `.planning/phases/13-policy-preference-maturity/13-03-SUMMARY.md`
</output>
