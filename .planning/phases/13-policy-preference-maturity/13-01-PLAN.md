---
phase: "13-policy-preference-maturity"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/chimeway/preferences.ex"
  - "lib/chimeway/preferences/category_preference.ex"
  - "priv/repo/migrations/20260425000100_create_chimeway_category_preferences.exs"
  - "test/chimeway/preferences_test.exs"
autonomous: true
requirements: ["POL-01"]
must_haves:
  truths:
    - "Users can define notification preferences by channel."
    - "Users can define notification preferences by category."
    - "Missing preferences default to enabled."
  artifacts:
    - path: "lib/chimeway/preferences/category_preference.ex"
      provides: "Category preference schema"
    - path: "lib/chimeway/preferences.ex"
      provides: "Channel and category preference lookup APIs"
    - path: "priv/repo/migrations/20260425000100_create_chimeway_category_preferences.exs"
      provides: "Category preference table and unique index"
    - path: "test/chimeway/preferences_test.exs"
      provides: "Preference contract regression coverage"
  key_links:
    - from: "lib/chimeway/preferences.ex"
      to: "lib/chimeway/preferences/category_preference.ex"
      via: "category lookup helpers"
      pattern: "category_enabled\\?|get_category_preference|upsert_category_preference"
    - from: "lib/chimeway/preferences/category_preference.ex"
      to: "priv/repo/migrations/20260425000100_create_chimeway_category_preferences.exs"
      via: "schema/table contract"
      pattern: "schema \"chimeway_category_preferences\""
---

<objective>
Add category-level preferences alongside the existing channel preference store so host apps can enable or disable delivery by notification category without changing the current opt-in default.

Purpose: Make preference scope explicit before policy wiring.
Output: Category preference schema, context APIs, and regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/chimeway/preferences.ex
@lib/chimeway/preferences/notification_preference.ex
@test/chimeway/preferences_test.exs

<interfaces>
From lib/chimeway/preferences.ex:
```elixir
def upsert_preference(attrs)
def get_preference(recipient_id, notification_key, channel)
def channel_enabled?(recipient_id, notification_key, channel)
```

From lib/chimeway/preferences/notification_preference.ex:
```elixir
schema "chimeway_notification_preferences" do
  field(:recipient_id, :string)
  field(:notification_key, :string)
  field(:channel, :string)
  field(:enabled, :boolean, default: true)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add category preference storage contract</name>
  <files>priv/repo/migrations/20260425000100_create_chimeway_category_preferences.exs, lib/chimeway/preferences/category_preference.ex</files>
  <behavior>
    - Persist one row per recipient/category pair.
    - Default missing category preferences to enabled.
    - Reject duplicate recipient/category inserts via a unique index.
  </behavior>
  <action>Create the `chimeway_category_preferences` table with UUID primary key, `recipient_id`, `notification_category`, `enabled`, timestamps, and a unique index on `recipient_id` + `notification_category`. Add an Ecto schema and changeset that mirror the existing notification preference style and do not convert runtime category strings into atoms.</action>
  <verify><automated>mix compile --warnings-as-errors</automated></verify>
  <done>The category preference table, schema, and uniqueness contract compile cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Preferences context with category APIs</name>
  <files>lib/chimeway/preferences.ex</files>
  <behavior>
    - `upsert_category_preference/1` stores or updates one category preference row.
    - `get_category_preference/2` returns a row or nil.
    - `category_enabled?/2` defaults to true when no row exists.
  </behavior>
  <action>Add category-specific upsert, lookup, and boolean helper functions to `Chimeway.Preferences`. Keep the existing channel preference API unchanged, reuse the new schema module, and normalize category keys as strings only.</action>
  <verify><automated>mix compile --warnings-as-errors && rg "upsert_category_preference|get_category_preference|category_enabled\?" lib/chimeway/preferences.ex</automated></verify>
  <done>The preference context exposes explicit category lookups without regressing channel preference behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand preference regression coverage</name>
  <files>test/chimeway/preferences_test.exs</files>
  <action>Extend the preference tests with category create/update/default cases, plus a regression that proves the existing channel preference upsert path still dedupes rows and preserves the enabled flag.</action>
  <verify><automated>mix test test/chimeway/preferences_test.exs</automated></verify>
  <done>Category preference behavior is covered and channel preference regression coverage still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app input → preference write | Untrusted recipient/category strings enter preference rows. |
| preference store → policy readers | Policy code trusts persisted booleans to suppress delivery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | `lib/chimeway/preferences/category_preference.ex` | mitigate | Validate category strings in the changeset and enforce a unique index on `recipient_id` + `notification_category`. |
| T-13-01-02 | Repudiation | `lib/chimeway/preferences.ex` | accept | Preference APIs return explicit boolean state only; no caller-supplied payload data is persisted or echoed back. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors`
- `mix test test/chimeway/preferences_test.exs`
- `rg "category_enabled\?|upsert_category_preference|get_category_preference" lib/chimeway/preferences.ex`
</verification>

<success_criteria>
- Category preferences can be created, updated, and queried by recipient/category.
- Missing category preference rows still mean enabled delivery.
- Existing channel preference behavior remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/13-policy-preference-maturity/13-01-SUMMARY.md`
</output>
