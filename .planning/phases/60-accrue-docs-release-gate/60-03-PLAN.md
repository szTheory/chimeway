---
phase: 60-accrue-docs-release-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - MAINTAINING.md
autonomous: true
requirements: [GATE-05]
must_haves:
  truths:
    - "verify_accrue CI job runs mix verify.accrue with Postgres after sibling Accrue checkout (D-13, D-14)"
    - "MAINTAINING pre-ship checklist documents mix verify.accrue as seventh gate (D-16)"
    - "Existing journey/mailglass verify jobs unchanged — additive CI only (ROADMAP SC #3)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "verify_accrue job"
      contains: "verify_accrue"
    - path: "MAINTAINING.md"
      provides: "Pre-ship septet including accrue gate"
      contains: "mix verify.accrue"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "mix.exs"
      via: "mix verify.accrue command"
      pattern: "mix verify.accrue"
    - from: "MAINTAINING.md"
      to: ".github/workflows/ci.yml"
      via: "Maintainer runs same command locally"
      pattern: "verify.accrue"
---

<objective>
Deliver GATE-05 (Accrue half): formal `verify_accrue` CI job with sibling Accrue repo checkout and MAINTAINING.md pre-ship septet update — without redesigning the existing `mix verify.accrue` alias (D-15).

Purpose: Accrue dunning integration gate runs in CI reproducibly; maintainers have documented pre-ship command parity with Mailglass/journeys gates. **D-01:** Wave 1 parallel with 60-01. **D-02:** Formalizes existing verify alias — no new proof tests.

Output: New CI job, updated MAINTAINING.md checklist (six → seven gates).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-accrue-docs-release-gate/60-CONTEXT.md
@.planning/phases/60-accrue-docs-release-gate/60-RESEARCH.md
@.github/workflows/ci.yml
@MAINTAINING.md
@mix.exs
</context>

<threat_model>
| ID | STRIDE | Threat | Severity | Mitigation |
|----|--------|--------|----------|------------|
| T-60-06 | Tampering | CI runs hex-only accrue without `Accrue.Integrations.Chimeway` | high | Mandatory `actions/checkout` of szTheory/accrue + `ACCRUE_PATH` env |
| T-60-07 | Denial of Service | Broken CI job blocks all PRs | medium | Mirror proven verify_mailglass job structure; pin Accrue SHA |
| T-60-08 | Repudiation | MAINTAINING checklist out of sync with CI jobs | low | Add verify.accrue with GATE-05 label matching job name |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add verify_accrue CI job (D-13, D-14)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` — `verify_mailglass` job (full block)
    - `mix.exs` — `verify.accrue` alias and `accrue_dep/0`
    - `.planning/phases/60-accrue-docs-release-gate/60-RESEARCH.md` — CI checkout pattern
  </read_first>
  <action>
    Add new job `verify_accrue` immediately after `verify_mailglass` job in `.github/workflows/ci.yml`.

    Mirror `verify_mailglass` structure:
    - `name: Accrue dunning integration gate`
    - `runs-on: ubuntu-latest`
    - Same `postgres:15` service block
    - Same `MIX_ENV: test` and `DATABASE_URL`
    - Same checkout/beam/cache/deps.get/ecto.create/ecto.migrate steps (use same action SHA pins as verify_mailglass)

    **Additional steps before deps.get:**
    1. Checkout Chimeway repo (existing first checkout step)
    2. Checkout Accrue sibling:
       ```yaml
       - uses: actions/checkout@v4
         with:
           repository: szTheory/accrue
           ref: de7a3fef53247619d96a26eea60197d74fd14634
           path: accrue/accrue
       ```

    **Env for verify step:**
    ```yaml
    ACCRUE_PATH: ${{ github.workspace }}/accrue/accrue
    ```

    Final step: `mix verify.accrue` (same as mailglass job uses `mix verify.mailglass`).

    Cache key: `${{ runner.os }}-mix-verify-accrue-${{ hashFiles('**/mix.lock') }}`

    Do **not** modify `verify_mailglass`, `verify_journeys`, or default `ci` job.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains job id `verify_accrue`
    - Job checks out `szTheory/accrue` to `accrue/accrue` with pinned ref `de7a3fef53247619d96a26eea60197d74fd14634`
    - Job sets `ACCRUE_PATH` env and runs `mix verify.accrue`
    - `verify_mailglass` job unchanged
    - Local: `ACCRUE_PATH="${ACCRUE_PATH:-../accrue/accrue}" mix verify.accrue --warnings-as-errors` exits 0 when sibling Accrue available
  </acceptance_criteria>
  <automated>grep -q verify_accrue .github/workflows/ci.yml && grep -q szTheory/accrue .github/workflows/ci.yml && grep -q mix\ verify.accrue .github/workflows/ci.yml</automated>
</task>

<task type="auto">
  <name>Task 2: Update MAINTAINING.md pre-ship septet (D-16)</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - `MAINTAINING.md` — pre-ship verification block (mix verify.mailglass entry)
    - `.planning/phases/60-accrue-docs-release-gate/60-CONTEXT.md` — D-16
  </read_first>
  <action>
    In `MAINTAINING.md` pre-ship command block, add after `mix verify.mailglass`:

    ```bash
    mix verify.accrue
    ```

    Add bullet description:
    `- mix verify.accrue — Accrue dunning integration gate (GATE-05 Accrue): ECOS-06 lifecycle tests and DEMO-07 demo host proof; requires sibling Accrue checkout — set ACCRUE_PATH=../accrue/accrue locally or let CI job check out szTheory/accrue`

    Update closing line from "All six must pass" to "All seven must pass before publishing."

    Add short **Accrue sibling checkout** subsection under pre-ship (or inline in bullet): maintainers clone `https://github.com/szTheory/accrue` adjacent to chimeway; CI pins SHA `de7a3fef53247619d96a26eea60197d74fd14634` (update when bumping integration).
  </action>
  <acceptance_criteria>
    - `MAINTAINING.md` contains `mix verify.accrue` in pre-ship command list
    - `MAINTAINING.md` contains "seven" gates language (not "six" as final count)
    - `MAINTAINING.md` documents `ACCRUE_PATH` or sibling checkout expectation
    - `mix verify.inbox` not added (deferred Phase 62)
  </acceptance_criteria>
  <automated>grep -q "mix verify.accrue" MAINTAINING.md && grep -qi seven MAINTAINING.md</automated>
</task>

</tasks>

<verification>
- [ ] `grep verify_accrue .github/workflows/ci.yml` shows single new job
- [ ] `ACCRUE_PATH=../accrue/accrue mix verify.accrue --warnings-as-errors` green locally
- [ ] No changes to `mix.exs` verify.accrue alias unless CI path discovery requires env-only fix (D-15: prefer unchanged)
</verification>

<success_criteria>
- GATE-05 Accrue half: CI job + MAINTAINING documentation complete
- Existing six gates still documented and unchanged in behavior
- mix verify.accrue alias unchanged unless CI proves otherwise
</success_criteria>
