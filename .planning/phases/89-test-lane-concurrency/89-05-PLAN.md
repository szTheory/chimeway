---
phase: 89-test-lane-concurrency
plan: 05
type: execute
wave: 3
depends_on: ["89-02", "89-03", "89-04"]
files_modified:
  - mix.exs
autonomous: true
requirements: [CONC-03]
must_haves:
  truths:
    - "The `ci.test` alias (`mix.exs`, currently line 76) runs with `--warnings-as-errors`, at parity with every `verify.*` alias in the same file (CONC-03/D-05)."
    - "`mix ci.test` exits 0 cleanly after this plan — any latent test-file compiler warning the new flag surfaced has been fixed in the affected test file(s), not suppressed."
    - "A one-time deliberate-warning negative proof was executed and documented: injecting an unused-variable-style warning into a test file made `mix ci.test` exit non-zero; reverting it restored a clean, zero-exit pass — proving the gate actually fires, not just that the flag is present in source."
      verification: backstop
    - "`release_gate_contract_test.exs` (ci-gate=14/pr-gate=4 lane counts) and `ci_observability_contract_test.exs` (which explicitly already anticipates and exempts this phase's separate `ci.test --warnings-as-errors` addition) both stay green — this phase's `mix.exs`-only edit does not touch `.github/workflows/ci.yml` lane structure."
  artifacts:
    - "mix.exs (`ci.test` alias carries `--warnings-as-errors`)"
    - "Any test file(s) whose latent compiler warning was fixed as part of this plan (recorded in the SUMMARY, exact set not knowable ahead of execution)"
  key_links:
    - "`ci.lint`'s `compile --warnings-as-errors` only covers `lib/` compilation — Mix does not compile `test/*.exs` during a normal `mix compile`, so `ci.test`'s own `--warnings-as-errors` is the only gate that can catch a warning inside a test file or `test/support/*.ex`."
---

<objective>
Bring the `ci.test` lane to warnings-as-errors parity with every `verify.*` alias (CONC-03/D-05): add `--warnings-as-errors` to the `ci.test` alias in `mix.exs`, fix whatever latent test-file warnings the flag surfaces (test-file-only hygiene, no `lib/` behavior change), and produce a one-time documented proof that the gate actually fires on a real warning rather than merely existing in source. Close by re-confirming the release-gate and CI-observability contract suites are unaffected, since this is a `mix.exs`-only change with zero `.github/workflows/ci.yml` lane-structure impact.

Purpose: Close the one test-running lane that was missing warnings-as-errors, so a warning inside `test/*.exs` or `test/support/*.ex` can no longer accumulate silently.
Output: `ci.test` runs `--warnings-as-errors`; any surfaced warnings are fixed; the gate's bite is proven once and documented; both contract suites confirmed green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-test-lane-concurrency/89-CONTEXT.md
@.planning/phases/89-test-lane-concurrency/89-RESEARCH.md
@mix.exs
</context>

## Artifacts this plan produces

A single-line `mix.exs` alias edit, whatever test-file warning fixes it surfaces, and a documented one-time negative proof that the new gate fires. No `lib/` files are touched. No `.github/workflows/ci.yml` changes.

<tasks>

<task type="auto">
  <name>Task 1: Add --warnings-as-errors to ci.test and fix any surfaced warnings</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs:74-77 (the `ci.test` alias comment and body — currently `"cmd env MIX_ENV=test mix test --exclude mailglass --exclude accrue --exclude threadline --exclude sigra"`, no `--warnings-as-errors`)
    - mix.exs:97-167 (every `verify.*` alias already carries `--warnings-as-errors` — the parity target)
    - .planning/phases/89-test-lane-concurrency/89-CONTEXT.md (D-05) and 89-RESEARCH.md, "`--warnings-as-errors` on `mix test`" section
  </read_first>
  <action>
    In `mix.exs`, change the `ci.test` alias body from `"cmd env MIX_ENV=test mix test --exclude mailglass --exclude accrue --exclude threadline --exclude sigra"` to the same string with `--warnings-as-errors` appended (CONC-03/D-05) — a single, additive flag with no documented interaction with the existing `--exclude` flags. Run the full `ci.test` alias. If it now fails because a warning surfaced during test compilation (expected, per D-05 — this lane never had compiler-warning visibility into `test/*.exs` before), fix each warning in its actual test file (unused variables/aliases, deprecated calls in test helpers, etc.) — these are test-file edits only, never a `lib/` change. Re-run after each fix until `ci.test` exits 0 cleanly. If a surfaced "warning" turns out to trace back to a real `lib/` bug rather than a test-file issue, STOP and flag it — do not fix `lib/` code under this phase's test/config-only scope.

    Commit the alias change and any warning fixes as `chore(89-05): add --warnings-as-errors to ci.test alias` (plus a follow-up `test(89-05): fix warning surfaced by ci.test --warnings-as-errors` commit per file if fixes are needed) — config/test-hygiene work, not `feat`/`perf`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix ci.test && echo CI_TEST_WARNINGS_AS_ERRORS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs`'s `ci.test` alias body contains `--warnings-as-errors` alongside the existing `--exclude` flags.
    - `mix ci.test` exits 0 with no test-file compiler warnings remaining.
    - Any fix made in response to a surfaced warning touched only a test file, never `lib/`.
  </acceptance_criteria>
  <done>`ci.test` runs at warnings-as-errors parity with every `verify.*` alias, and the full suite passes cleanly under the new flag.</done>
</task>

<task type="auto">
  <name>Task 2: One-time deliberate-warning negative proof (documented evidence, then reverted)</name>
  <files>test/chimeway/signal_test.exs (temporary edit, reverted before task completion — no permanent change)</files>
  <read_first>
    - test/chimeway/signal_test.exs (any one existing `test "..." do` body, as the injection point)
    - .planning/phases/89-test-lane-concurrency/89-VALIDATION.md, "Manual-Only Verifications" row for CONC-03 (this proof's exact intent: show the gate fires, not just that the flag exists)
  </read_first>
  <action>
    Temporarily add one line directly inside any single existing test body in `test/chimeway/signal_test.exs`: bind a local value to a name that is never read afterward (a plain local binding with no leading underscore, so the compiler emits its standard unused-binding warning). Run `mix ci.test` and confirm it now exits non-zero, with the warnings-as-errors failure attributable to the line you just added. Then discard the change (`git checkout` the file, restoring the committed state from Plan 89-01/89-02) and run `mix ci.test` again, confirming a clean, zero-exit pass. Record both exit codes as this task's evidence in the SUMMARY — this is a one-time proof, not a permanent test, per 89-VALIDATION.md.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && git diff --quiet -- test/ mix.exs && mix ci.test && echo NEGATIVE_PROOF_REVERTED_AND_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - The injected line caused `mix ci.test` to exit non-zero when present (recorded in the SUMMARY).
    - After `git checkout`, the working tree carries no residual diff in `test/` or `mix.exs` (confirmed by `git diff --quiet`), and `mix ci.test` exits 0.
  </acceptance_criteria>
  <done>The deliberate-warning negative proof is executed and documented: the gate demonstrably fails the lane on a real warning and is fully reverted with the suite green again.</done>
</task>

<task type="auto">
  <name>Task 3: Guard — re-run the release-gate and CI-observability contract suites</name>
  <files>none (verification-only; no file changes)</files>
  <read_first>
    - test/chimeway/ci_observability_contract_test.exs:272-285 (the test explicitly anticipating and exempting "Phase 89's separate `ci.test --warnings-as-errors` (CONC-03)" from its build-lane refute — confirms this phase's change is expected to be orthogonal to `.github/workflows/ci.yml`'s build-lane structure)
    - test/chimeway/release_gate_contract_test.exs:236-267 (`ci-gate` needs exactly 14 lanes, `pr-gate` needs exactly its 4-lane subset — this phase does not touch `ci.yml`, so these counts must be unchanged)
  </read_first>
  <action>
    Re-run both contract suites as a guard that this `mix.exs`-only change had zero incidental effect on CI lane structure — neither test file is expected to need any edit; this task is a pure confirmation step, not a code change.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/release_gate_contract_test.exs test/chimeway/ci_observability_contract_test.exs --seed 0 && echo CONTRACT_GUARD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `release_gate_contract_test.exs` is green: ci-gate=14 / pr-gate=4 lane counts and names unchanged.
    - `ci_observability_contract_test.exs` is green: the build-lane warnings-as-errors refute still holds for the 13 non-`install_golden_contract` lanes, unaffected by this phase's separate `ci.test` alias change.
  </acceptance_criteria>
  <done>Both contract suites confirm this phase introduced zero lane-structure drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ci.test` alias → test-file compilation | Warnings inside `test/*.exs`/`test/support/*.ex` were previously invisible to any gate; this plan closes that blind spot |
| This plan's `mix.exs` edit → `ci.yml` lane structure (contract tests) | No `ci.yml` change is made, but the contract suites are re-run to prove it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-03 | Tampering | CI test-lane compiler-warning visibility (`mix.exs` `ci.test` alias) | medium | mitigate | `--warnings-as-errors` added at parity with `verify.*` (D-05); Task 2's one-time deliberate-warning proof demonstrates the gate actually fires rather than merely existing in source. |
| T-89-05 | Tampering | ci-gate/pr-gate lane structure (contract tests) | low | mitigate | Task 3 re-runs both contract suites; `ci_observability_contract_test.exs` already explicitly anticipates and exempts this phase's change from its build-lane refute. |
| T-89-SC | Tampering | Package-manager supply chain | n/a | accept | No new packages installed this plan — a single `mix.exs` alias-string edit plus test-file warning fixes. |
</threat_model>

<verification>
- `mix ci.test` — passes cleanly with `--warnings-as-errors` active.
- Negative proof: inject → non-zero exit → revert → zero-exit pass, documented in SUMMARY.
- `mix test test/chimeway/release_gate_contract_test.exs test/chimeway/ci_observability_contract_test.exs` — both green, confirming no lane-structure drift.
</verification>

<success_criteria>
`ci.test` runs at `--warnings-as-errors` parity with every `verify.*` alias, any latent test-file warning is fixed (not suppressed), the gate's bite is proven once via a documented inject/revert cycle, and both release-gate and CI-observability contract suites confirm zero incidental structural drift.
</success_criteria>

<output>
Create `.planning/phases/89-test-lane-concurrency/89-05-SUMMARY.md` when done.
</output>
