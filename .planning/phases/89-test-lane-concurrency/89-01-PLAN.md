---
phase: 89-test-lane-concurrency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/test.exs
  - test/chimeway/signal_test.exs
autonomous: true
requirements: [CONC-01, CONC-02]
must_haves:
  truths:
    - "`config/test.exs` sets an explicit `Chimeway.Repo` `pool_size` (formula `System.schedulers_online() * 2 + 10`) in both `repo_config` branches — the implicit default of 10 is gone (CONC-02/D-04)."
    - "`test/chimeway/signal_test.exs` runs `async: true`, passes standalone under `--seed 0`, and passes inside the full suite — proving the sandbox-allowance + pool-size mechanism end-to-end on one D-02-vetted candidate before the remaining 19 are flipped (CONC-01/D-01/D-02)."
    - "The full-suite run after this first flip shows no missing-`allow/3` or pool/ownership-timeout failure class in its output."
    - "`Chimeway.DataCase`'s own default and every other test module's `async:` tag are untouched — this plan changes exactly one test file's tag plus the pool_size config lines."
  artifacts:
    - "config/test.exs (pool_size formula variable + key in both DATABASE_URL branches)"
    - "test/chimeway/signal_test.exs (async: true)"
  key_links:
    - "`test/support/data_case.ex`'s `setup tags do ... shared: not tags[:async] end` is what makes the flip on `signal_test.exs` meaningful — the sandbox drops to allowance mode for that module only, and stays shared for every other still-serial module."
    - "ExUnit's `max_cases` default (`System.schedulers_online() * 2`) is the concurrency ceiling `pool_size` must exceed with headroom; tying both to `System.schedulers_online()` keeps them self-consistent on any machine, not just the 4-vCPU CI runner."
---

<objective>
Prove the whole test-lane-concurrency mechanism end-to-end on the thinnest possible slice before fanning out to the remaining 19 candidates: give `Chimeway.Repo` an explicit, formula-derived `pool_size` (CONC-02/D-04) and flip exactly one vetted pure-DB `DataCase` module — `signal_test.exs` — to `async: true` (CONC-01/D-01, confirmed D-02-safe by RESEARCH.md's completed per-candidate audit: no app-env mutation, no prefix/dynamic-repo touch, and its `Oban.Testing` usage executes synchronously in the caller process with no process boundary crossed). If the pool-sizing formula or sandbox-allowance mechanism has a flaw, it surfaces after one file, not after twenty.

Purpose: De-risk CONC-01 and CONC-02 together on the agent's best early-context tokens, so Plans 89-02/89-03/89-04 can fan the identical, now-proven pattern out to the remaining 19 D-01 candidates with confidence instead of discovering a config problem mid-fan-out.
Output: `config/test.exs` carrying an explicit `pool_size`; `signal_test.exs` running `async: true` and passing both standalone and inside the full suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-test-lane-concurrency/89-CONTEXT.md
@.planning/phases/89-test-lane-concurrency/89-RESEARCH.md
@test/support/data_case.ex
@config/test.exs
</context>

## Artifacts this plan produces

The tracer slice for the whole phase: an explicit, self-scaling `pool_size` on `Chimeway.Repo` in `config/test.exs`, and one representative pure-DB `DataCase` module (`signal_test.exs`) proven to run correctly under `async: true` against that pool. Plans 89-02/03/04 reuse this exact pattern (no config changes, only tag flips) for the remaining 19 candidates; Plan 89-05 adds `--warnings-as-errors`; Plan 89-06 closes with the seed-parity proof.

<tasks>

<task type="tracer">
  <name>Task 1: Size the pool + flip one representative module end-to-end (signal_test.exs)</name>
  <files>config/test.exs, test/chimeway/signal_test.exs</files>
  <read_first>
    - test/support/data_case.ex:19-23 (the `setup tags do ... Sandbox.start_owner!(Chimeway.Repo, shared: not tags[:async]) end` — the mechanism this whole phase depends on)
    - config/test.exs:1-20 (current `repo_config` case block — no `pool_size` key in either the `nil ->` or `database_url ->` branch; this is the implicit-10 gap CONC-02 closes)
    - test/chimeway/signal_test.exs:1-3 (module header: `use Chimeway.DataCase, async: false` at line 2, `use Oban.Testing, repo: Chimeway.Repo` at line 3 — `Oban.Testing.perform_job/2` runs synchronously in the calling test process per RESEARCH.md's audit, so no process boundary is crossed and no `Sandbox.allow/3` is needed)
    - .planning/phases/89-test-lane-concurrency/89-CONTEXT.md (D-01, D-02, D-04)
    - .planning/phases/89-test-lane-concurrency/89-RESEARCH.md, "Pool Sizing for Async ExUnit" and the per-candidate audit table in section 3
  </read_first>
  <action>
    In `config/test.exs`, add `pool_size = System.schedulers_online() * 2 + 10` as a new line immediately after `import Config` (before `repo_config =`), per D-04/CONC-02. This ties the pool ceiling to ExUnit's own `max_cases` concurrency knob plus fixed headroom, so it self-scales for both the 4-vCPU CI runner (8+10=18) and larger local dev machines (measured 18 cores here → 36+10=46), comfortably under Postgres 15's default connection ceiling. Add `pool_size: pool_size` as a new key to BOTH branches of the `repo_config` case: the `nil ->` branch's keyword list (alongside `username`/`password`/`hostname`/`database`/`pool`) and the `database_url ->` branch's keyword list (alongside `url`/`pool`). Do not touch the `database:` partition-name interpolation, the Oban block, or any other section of the file.

    In `test/chimeway/signal_test.exs`, change line 2 from `use Chimeway.DataCase, async: false` to `use Chimeway.DataCase, async: true` (CONC-01/D-01) — this file is D-02-vetted flip-safe: it never mutates global app-env, never touches tenant prefix or a dynamic repo, and its `Oban.Testing` worker execution is synchronous in-process (confirmed in RESEARCH.md's per-candidate audit), so nothing needs an allowance call.

    Commit as `chore(89-01): size Chimeway.Repo test pool and flip signal_test to async` — this is config/test-tag work, never `feat`/`perf` (would incorrectly trigger release-please for what is purely CI/test infrastructure).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/signal_test.exs --seed 0 && env MIX_ENV=test mix test --exclude mailglass --exclude accrue --exclude threadline --exclude sigra 2>&1 | tee /tmp/89-01-full-run.log | tail -8 && ! grep -qE "OwnershipError|owned the connection for longer than|DBConnection\.ConnectionError" /tmp/89-01-full-run.log && echo TRACER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `config/test.exs` sets `pool_size: pool_size` (formula `System.schedulers_online() * 2 + 10`) in both `repo_config` branches; `mix format --check-formatted config/test.exs` stays clean.
    - `test/chimeway/signal_test.exs` line 2 reads `use Chimeway.DataCase, async: true`.
    - `mix test test/chimeway/signal_test.exs --seed 0` passes standalone.
    - The full `mix test --exclude mailglass --exclude accrue --exclude threadline --exclude sigra` run passes with none of the three DBConnection/Sandbox failure-signature strings present in its captured output.
  </acceptance_criteria>
  <done>`Chimeway.Repo`'s test `pool_size` is explicit and formula-derived; `signal_test.exs` runs `async: true` and passes both standalone and inside the full suite with no pool or ownership errors — the mechanism is proven before fan-out.</done>
</task>

<task type="auto">
  <name>Task 2: Lint + repeat-run guard before fan-out</name>
  <files>config/test.exs</files>
  <read_first>
    - config/test.exs (post-Task-1 state, to confirm formatting)
  </read_first>
  <action>
    Run `mix ci.lint` (format-check + `compile --warnings-as-errors` + credo) to confirm the `config/test.exs` edit introduced no formatting or compile regression before Plans 89-02/89-03/89-04 build on top of it. Then run the full suite a second time, independently, on a fresh random seed, to catch any run-to-run pool-related flakiness this early rather than only discovering it at the CONC-04 closing proof in Plan 89-06. No further file changes are expected from this task — it is a verification-only guard. If `mix ci.lint` or the repeat run fails on something attributable to the Task 1 edit, fix `config/test.exs` inline (blocking-issue auto-fix) and re-run both before proceeding.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix ci.lint && env MIX_ENV=test mix test --exclude mailglass --exclude accrue --exclude threadline --exclude sigra 2>&1 | tee /tmp/89-01-repeat-run.log | tail -8 && ! grep -qE "OwnershipError|owned the connection for longer than|DBConnection\.ConnectionError" /tmp/89-01-repeat-run.log && echo REPEAT_RUN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `mix ci.lint` passes (format, `compile --warnings-as-errors`, credo) with the new `pool_size` lines present.
    - A second, independent full-suite run passes with none of the three failure-signature strings present.
  </acceptance_criteria>
  <done>Two consecutive full-suite runs (Task 1's and this one) both pass cleanly on the new pool_size with signal_test.exs under async — an early two-run flake check ahead of the 19-file fan-out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `test/support/data_case.ex` sandbox mode boundary | Per-module `async:` tag flips a test module from shared-mode (any process can use the connection) to allowance-mode (only the owner process, unless explicitly granted) — the single mechanism this whole phase depends on |
| `Chimeway.Repo` connection pool → concurrent ExUnit test modules | `pool_size` must exceed ExUnit's own `max_cases` concurrency ceiling or async modules starve each other for a connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-01 | Tampering | Ecto Sandbox isolation (`test/support/data_case.ex`) | high | mitigate | Only the single D-02-vetted candidate (`signal_test.exs`) is flipped this plan — no app-env mutation, no prefix/dynamic-repo touch, no un-allowed spawned process. The remaining 19 candidates get the identical vetting treatment in Plans 89-02/03/04; `telemetry_correlation_test.exs` stays `async: false` for a documented fourth-axis reason (RESEARCH.md §4). |
| T-89-02 | Denial of Service | `Chimeway.Repo` connection pool (`config/test.exs`) | medium | mitigate | Explicit `pool_size: System.schedulers_online() * 2 + 10` (D-04) self-scales to `max_cases` plus headroom on both the CI runner and larger dev machines; Task 1 and Task 2 each run a full-suite pass and check for the DBConnection/Sandbox failure signatures. |
| T-89-03 | Repudiation | Ordering-coupling regression introduced by the async flip | low (this plan touches one file) | mitigate | `signal_test.exs` is run twice under `--seed 0` and default random seed across Task 1/Task 2 with identical pass/fail; the full CONC-04 multi-run proof lands in Plan 89-06 once all 20 flips are in. |
| T-89-SC | Tampering | Package-manager supply chain | n/a | accept | This phase installs no new packages — only edits `config/test.exs`, `mix.exs`, and `async:` tags in existing test files (RESEARCH.md's Package Legitimacy Audit is explicitly N/A). |
</threat_model>

<verification>
- `mix test test/chimeway/signal_test.exs --seed 0` — passes standalone under async.
- Full-suite run (Task 1) — passes with `pool_size` explicit, no DBConnection/Sandbox failure signatures.
- `mix ci.lint` (Task 2) — passes, confirming `config/test.exs` formatting/compile is clean.
- Full-suite run repeated (Task 2) — passes a second time, independent seed, no failure signatures.
</verification>

<success_criteria>
`Chimeway.Repo`'s test pool is explicitly sized by a formula that self-scales to the runtime's own core count, and one representative pure-DB `DataCase` module runs correctly under `async: true` against it — twice, on two separate full-suite runs, with zero ownership or pool-exhaustion errors. This proves the mechanism CONC-01 and CONC-02 both depend on before the remaining 19 candidates are flipped.
</success_criteria>

<output>
Create `.planning/phases/89-test-lane-concurrency/89-01-SUMMARY.md` when done.
</output>
