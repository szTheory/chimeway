---
phase: 89-test-lane-concurrency
plan: 03
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - test/chimeway/orchestration/digest_explainability_test.exs
  - test/chimeway/orchestration/delivery_planning_test.exs
  - test/chimeway/integration/trigger_explain_test.exs
  - test/chimeway/integration/readme_snippet_test.exs
  - test/chimeway/idempotency_constraint_test.exs
  - test/chimeway/trigger_sanitization_test.exs
  - test/chimeway/persistence_transaction_test.exs
autonomous: true
requirements: [CONC-01]
must_haves:
  truths:
    - "The 2 orchestration modules, 2 integration modules, and 3 remaining top-level trigger/persistence modules all run `async: true` and pass standalone under `--seed 0` (CONC-01/D-01)."
    - "Every file in this plan that calls `Trigger.trigger/3` (idempotency_constraint_test, trigger_explain_test, persistence_transaction_test, trigger_sanitization_test) fires the same production telemetry spans `telemetry_correlation_test.exs` listens for, but none of them themselves `assert_receive` on a telemetry event — only `telemetry_correlation_test.exs` does, which is why it alone stays `async: false` (RESEARCH.md §4) while these four are safe to flip."
    - "`idempotency_constraint_test.exs`'s `Task.async_stream` (10 concurrent tasks) already calls `Sandbox.allow/3` inside every spawned task before touching `Repo`, consuming exactly 1 pool connection regardless of task count."
  artifacts:
    - "7 test files with `use Chimeway.DataCase, async: true` (was `async: false`)"
  key_links:
    - "The telemetry cross-talk hazard is one-directional: firing `Trigger.trigger` (these 4 files) is safe under concurrency; asserting on the resulting telemetry message by shape alone without a correlation-id match (only `telemetry_correlation_test.exs`) is not — that asymmetry is why this group flips while that one file does not."
---

<objective>
Fan the tracer-proven pattern out to 7 more of the 19 remaining D-01 candidates: the 2 orchestration modules, the 2 integration modules (`trigger_explain_test.exs` and `readme_snippet_test.exs`), and the 3 remaining top-level trigger/persistence modules. This group contains every remaining `Trigger.trigger`-calling candidate except `telemetry_correlation_test.exs` itself (which stays `async: false` per D-01/D-02 and RESEARCH.md's fourth-axis finding) and the one `Task.async_stream`-based candidate not already covered in Plan 89-02.

Purpose: Close out the trigger-firing candidates as a single reviewable group, making the telemetry-safety reasoning (fire-without-assert is safe, assert-by-shape-alone is not) explicit and auditable in one commit set.
Output: 7 test files running `async: true`, each independently verified to still pass under `--seed 0`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-test-lane-concurrency/89-CONTEXT.md
@.planning/phases/89-test-lane-concurrency/89-RESEARCH.md
</context>

## Artifacts this plan produces

Three atomic, directory-grouped async flips. No config files are touched — only the `async:` tag on 7 existing test files, all built on Plan 89-01's proven pool_size + sandbox-allowance mechanism.

<tasks>

<task type="auto">
  <name>Task 1: Flip the orchestration modules to async: true</name>
  <files>test/chimeway/orchestration/digest_explainability_test.exs, test/chimeway/orchestration/delivery_planning_test.exs</files>
  <read_first>
    - test/chimeway/orchestration/digest_explainability_test.exs:1-2, test/chimeway/orchestration/delivery_planning_test.exs:1-2 (each `use Chimeway.DataCase, async: false` at line 2, no app-env/prefix mutation, no spawn primitives per RESEARCH.md's audit)
    - .planning/phases/89-test-lane-concurrency/89-RESEARCH.md, section 3 per-candidate audit table (D-02)
  </read_first>
  <action>
    In each of the 2 files, change line 2 from `use Chimeway.DataCase, async: false` to `use Chimeway.DataCase, async: true` (CONC-01/D-01). Both are in RESEARCH.md's "no spawn primitives found — safe" bucket: no app-env mutation, no prefix/dynamic-repo touch, no cross-process `Repo` access. Make no other change to these files.

    Commit as `test(89-03): flip orchestration DataCase modules to async` — test-tag-only work, not `feat`/`perf`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/orchestration/digest_explainability_test.exs test/chimeway/orchestration/delivery_planning_test.exs --seed 0 && echo ORCHESTRATION_GROUP_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both files read `use Chimeway.DataCase, async: true` at line 2.
    - `mix test` on the 2 files together with `--seed 0` passes with zero failures.
  </acceptance_criteria>
  <done>Both orchestration `DataCase` modules run `async: true` and pass together under a fixed seed.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the integration modules to async: true</name>
  <files>test/chimeway/integration/trigger_explain_test.exs, test/chimeway/integration/readme_snippet_test.exs</files>
  <read_first>
    - test/chimeway/integration/trigger_explain_test.exs:1-2 (`use Chimeway.DataCase, async: false` at line 2; calls `Trigger.trigger/3` but never `assert_receive`s on a telemetry event — safe per the fire-without-assert reasoning below)
    - test/chimeway/integration/readme_snippet_test.exs:14-17 (module header is pushed down by a 13-line documentation comment block; `use Chimeway.DataCase, async: false` is at line 15, NOT line 2 — this file does not call `Trigger.trigger/3` directly, no app-env/prefix mutation, no spawn primitives)
    - .planning/phases/89-test-lane-concurrency/89-RESEARCH.md §4 (the telemetry cross-talk finding) and section 3 audit table (D-02)
  </read_first>
  <action>
    In `trigger_explain_test.exs`, change line 2 from `use Chimeway.DataCase, async: false` to `use Chimeway.DataCase, async: true`. This file calls `Trigger.trigger/3`, which fires the same production telemetry spans `telemetry_correlation_test.exs` listens for — but it never itself `assert_receive`s on a telemetry message, so a concurrently-running sibling's telemetry traffic cannot cause it to observe the wrong event; only the file that both fires AND asserts-by-shape is at risk, and that file (`telemetry_correlation_test.exs`) stays serial.

    In `readme_snippet_test.exs`, change line 15 (NOT line 2 — the file opens with a long documentation comment) from `use Chimeway.DataCase, async: false` to `use Chimeway.DataCase, async: true`. This file has no `Trigger.trigger/3` call and no spawn primitives.

    Commit as `test(89-03): flip integration DataCase modules to async` — test-tag-only work, not `feat`/`perf`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/integration/trigger_explain_test.exs test/chimeway/integration/readme_snippet_test.exs --seed 0 && echo INTEGRATION_GROUP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `trigger_explain_test.exs` line 2 and `readme_snippet_test.exs` line 15 both read `use Chimeway.DataCase, async: true`.
    - `mix test` on the 2 files together with `--seed 0` passes with zero failures.
  </acceptance_criteria>
  <done>Both integration `DataCase` modules run `async: true` and pass together under a fixed seed.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the remaining top-level trigger/persistence modules to async: true</name>
  <files>test/chimeway/idempotency_constraint_test.exs, test/chimeway/trigger_sanitization_test.exs, test/chimeway/persistence_transaction_test.exs</files>
  <read_first>
    - test/chimeway/idempotency_constraint_test.exs:1-2 and its `Task.async_stream` block (10 concurrent tasks, each calling `Sandbox.allow(Repo, parent, self())` before `Trigger.trigger/3` — verified pattern, no `assert_receive` on telemetry)
    - test/chimeway/trigger_sanitization_test.exs:1-2, test/chimeway/persistence_transaction_test.exs:1-2 (each `use Chimeway.DataCase, async: false` at line 2; both call `Trigger.trigger/3` but never `assert_receive` on a telemetry event)
    - .planning/phases/89-test-lane-concurrency/89-RESEARCH.md §4 and section 3 audit table, plus "Pitfall 2" (async_stream pool-connection accounting)
  </read_first>
  <action>
    In all 3 files, change line 2 from `use Chimeway.DataCase, async: false` to `use Chimeway.DataCase, async: true` (CONC-01/D-01). `idempotency_constraint_test.exs` already grants sandbox access inside every one of its 10 concurrent tasks before touching `Repo`, consuming exactly 1 pool connection per RESEARCH.md's Pitfall 2 — it needs no code change to flip safely. All three call `Trigger.trigger/3` but none `assert_receive` on a telemetry event, so the fire-without-assert reasoning from Task 2 applies here too. Make no other change to these files.

    Commit as `test(89-03): flip remaining trigger/persistence DataCase modules to async` — test-tag-only work, not `feat`/`perf`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/idempotency_constraint_test.exs test/chimeway/trigger_sanitization_test.exs test/chimeway/persistence_transaction_test.exs --seed 0 && echo TRIGGER_GROUP_OK</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files read `use Chimeway.DataCase, async: true` at line 2.
    - `mix test` on the 3 files together with `--seed 0` passes with zero failures, including the 10-way concurrent `idempotency_constraint_test.exs` case.
  </acceptance_criteria>
  <done>All 3 remaining top-level trigger/persistence `DataCase` modules run `async: true` and pass together under a fixed seed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `test/support/data_case.ex` sandbox mode boundary | Each of these 7 modules moves from shared-mode to allowance-mode; `idempotency_constraint_test.exs`'s 10 spawned tasks are the highest-concurrency spawn case in the whole phase |
| VM-wide `:telemetry` dispatch (production spans in `lib/chimeway/telemetry.ex`) | 4 of these 7 files fire the exact spans `telemetry_correlation_test.exs` listens for; the sandbox has no equivalent isolation for telemetry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-89-01 | Tampering | Ecto Sandbox isolation | high | mitigate | All 7 files individually read/grepped in RESEARCH.md's D-02 audit; `idempotency_constraint_test.exs`'s spawned tasks already call the sandbox-allowance function before touching `Repo`. |
| T-89-04 | Repudiation | VM-wide telemetry cross-talk between concurrently-running `Trigger.trigger` callers | medium | mitigate | Only `telemetry_correlation_test.exs` asserts on a telemetry message by shape; the 4 fire-only callers in this plan (and elsewhere) cannot corrupt that assertion by being flipped, and `telemetry_correlation_test.exs` itself is deliberately excluded from this phase's flip set (D-01/D-02, RESEARCH.md §4). |
</threat_model>

<verification>
- `mix test <orchestration files> --seed 0` — 2 files pass together.
- `mix test <integration files> --seed 0` — 2 files pass together.
- `mix test <trigger/persistence files> --seed 0` — 3 files pass together, including the 10-way concurrent test.
</verification>

<success_criteria>
7 more of the 19 remaining D-01 candidates run `async: true`, including every remaining `Trigger.trigger`-firing candidate and the second `Task.async_stream`-based candidate, each independently verified to pass under a fixed seed, with the fire-vs-assert telemetry safety asymmetry made explicit.
</success_criteria>

<output>
Create `.planning/phases/89-test-lane-concurrency/89-03-SUMMARY.md` when done.
</output>
