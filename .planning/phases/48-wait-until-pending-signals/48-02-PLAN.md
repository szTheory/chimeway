---
phase: 48-wait-until-pending-signals
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - lib/chimeway/workflows/progression.ex
  - test/chimeway/orchestration/workflow_progression_test.exs
autonomous: true
requirements: [READ-01]
must_haves:
  truths:
    - "Workflow runs entering `:waiting` via `wait_until` persist `pending_signals` from rule `cancel_signals` in the same transaction as `state: :waiting` (D-01)"
    - "Time-only `wait_until` waits (no `cancel_signals` key) persist `pending_signals == []` — behavior unchanged from pre-Phase-48 (D-03, D-06)"
    - "Injected signal matches auto-populated `pending_signals` via `SignalRouterWorker` without host `update_run` glue (D-08)"
    - "`route_signal/1` matching and post-match behavior unchanged (D-02, D-07)"
  artifacts:
    - path: "lib/chimeway/workflows/progression.ex"
      provides: "enter_waiting/6 sets pending_signals in update_run map"
      contains: "pending_signals"
    - path: "test/chimeway/orchestration/workflow_progression_test.exs"
      provides: "READ-01 auto-population + SignalRouterWorker integration proof"
      contains: "wait_until auto-populates pending_signals"
  key_links:
    - from: "lib/chimeway/workflows/progression.ex"
      to: "WorkflowRun.pending_signals"
      via: "Workflows.update_run/3 in enter_waiting/6"
      pattern: "pending_signals:"
    - from: "test/chimeway/orchestration/workflow_progression_test.exs"
      to: "Chimeway.Dispatch.SignalRouterWorker"
      via: "Signal.track + perform_job after progression-driven :waiting entry"
      pattern: "SignalRouterWorker"
---

<objective>
Populate `WorkflowRun.pending_signals` inside `enter_waiting/6` from the persisted `wait_until` rule's `cancel_signals` list, and prove READ-01 with progression integration tests plus a SignalRouterWorker end-to-end path that requires no host `update_run` glue.

Purpose: Close the engine gap where `route_signal/1` already matches on `pending_signals` but waiting entry never set the list. This is the durable write seam per D-01.

Output: Updated `lib/chimeway/workflows/progression.ex` and extended `test/chimeway/orchestration/workflow_progression_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-wait-until-pending-signals/48-CONTEXT.md
@.planning/phases/48-wait-until-pending-signals/48-RESEARCH.md
@.planning/phases/48-wait-until-pending-signals/48-VALIDATION.md
@.planning/phases/48-wait-until-pending-signals/48-01-SUMMARY.md

@lib/chimeway/workflows/progression.ex
@lib/chimeway/workflows.ex
@lib/chimeway/dispatch/signal_router_worker.ex
@test/chimeway/orchestration/workflow_progression_test.exs
@test/chimeway/dispatch/signal_router_worker_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate pending_signals in enter_waiting/6 (D-01, D-03)</name>
  <files>lib/chimeway/workflows/progression.ex</files>
  <read_first>
    - `lib/chimeway/workflows/progression.ex` — `enter_waiting/6` (lines ~251–287), `@waiting_reason`, existing `Workflows.update_run/3` map
    - `lib/chimeway/workflows/workflow_run.ex` — `pending_signals` changeset cast (verify `{:array, :string}` default `[]`)
    - `.planning/phases/48-wait-until-pending-signals/48-CONTEXT.md` — D-01, D-03; do not mirror into `status_context` (RESEARCH discretion: no mirror)
    - `lib/chimeway/notifier.ex` — normalized rule shape: `cancel_signals` key present only when non-empty
  </read_first>
  <action>
    In `enter_waiting/6` inside `lib/chimeway/workflows/progression.ex`, before the `Workflows.update_run/3` call:

    1. Bind `pending_signals = Map.get(rule, "cancel_signals", [])` — handles both omitted key and explicit empty list from persisted JSON.
    2. Add `pending_signals: pending_signals` to the existing `Workflows.update_run/3` attrs map alongside `state: :waiting`, `status_reason: @waiting_reason`, `status_context: status_context`, and `last_transition_at: now`.

    Do **not** add `cancel_signals` to `status_context` (avoid dual-source drift per RESEARCH). Do **not** modify `advance_after_wait/5`, `route_signal/1`, or `find_runs_waiting_for_signal/3`. Do **not** change transition `context` for the waiting entry — it remains anchor/due metadata only.

    Update `@moduledoc` bullet for `wait_until` rules (lines ~10–14) to mention `pending_signals` is set from optional rule `cancel_signals` at wait entry.
  </action>
  <verify>
    <automated>mix test test/chimeway/orchestration/workflow_progression_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `enter_waiting/6` source contains `pending_signals:` inside the `Workflows.update_run/3` map.
    - `enter_waiting/6` source contains `Map.get(rule, "cancel_signals", [])`.
    - No edits to `lib/chimeway/workflows.ex` `route_signal/1` function body.
    - `mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>`enter_waiting/6` persists `pending_signals` atomically with `:waiting` state.</done>
</task>

<task type="auto">
  <name>Task 2: Progression tests for auto-populated pending_signals (READ-01)</name>
  <files>test/chimeway/orchestration/workflow_progression_test.exs</files>
  <read_first>
    - `test/chimeway/orchestration/workflow_progression_test.exs` — `ChimewayTest.Notifiers.WorkflowProgression` fixture (lines ~5–75), `"wait_until rule anchored to prior_delivery_terminal_at (D-01)"` describe (~line 179), `trigger_workflow!/1` helper, PlanOnly dispatcher setup
    - `lib/chimeway/workflows/progression.ex` — Task 1 `pending_signals` assignment
    - `.planning/phases/48-wait-until-pending-signals/48-VALIDATION.md` — Wave 0 gap for describe block name
  </read_first>
  <action>
    Add a new top-level notifier module `ChimewayTest.Notifiers.WorkflowProgressionWithSignals` in `test/chimeway/orchestration/workflow_progression_test.exs` (same structure as `WorkflowProgression` but with `notification_key: "test.workflow_progression_with_signals"` and the `wait_until` rule extended with `"cancel_signals" => ["chimeway.notification.read"]`). Keep the existing `WorkflowProgression` fixture unchanged for regression tests.

    Add describe block `"wait_until auto-populates pending_signals (READ-01)"` with at least two tests:

    1. **Time-only fixture regression** — trigger using existing `ChimewayTest.Notifiers.WorkflowProgression`, converge in_app delivery to `:succeeded` via `Deliveries.transition_status/2` + `Deliveries.record_attempt/2`, reload run, assert `updated_run.state == :waiting` and `updated_run.pending_signals == []`.

    2. **cancel_signals populated** — add helper `trigger_workflow_with_signals!/1` mirroring `trigger_workflow!/1` but using `WorkflowProgressionWithSignals` notifier module. After same convergence path, assert `updated_run.pending_signals == ["chimeway.notification.read"]`.

    Extend the existing `"wait_until rule anchored to prior_delivery_terminal_at (D-01)"` success test with one assertion line: `assert updated_run.pending_signals == []` after the run enters `:waiting` (proves time-only path explicitly).

    Do **not** call `Workflows.update_run/3` manually to set `pending_signals` in any new test — population must come from progression only.
  </action>
  <verify>
    <automated>mix test test/chimeway/orchestration/workflow_progression_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains `defmodule ChimewayTest.Notifiers.WorkflowProgressionWithSignals`.
    - Test file contains describe `"wait_until auto-populates pending_signals (READ-01)"`.
    - Time-only test asserts `pending_signals == []` on `:waiting` entry.
    - `cancel_signals` test asserts `pending_signals == ["chimeway.notification.read"]` without manual host glue.
    - `mix test test/chimeway/orchestration/workflow_progression_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Progression integration tests prove READ-01 population at wait entry.</done>
</task>

<task type="auto">
  <name>Task 3: SignalRouterWorker end-to-end proof without host glue (D-08)</name>
  <files>test/chimeway/orchestration/workflow_progression_test.exs</files>
  <read_first>
    - `test/chimeway/dispatch/signal_router_worker_test.exs` — `perform_job(SignalRouterWorker, ...)` pattern, signal fixture shape, transition assertions for `signal_received`
    - `test/chimeway/orchestration/workflow_progression_test.exs` — Task 2 `WorkflowProgressionWithSignals` fixture and convergence helpers
    - `lib/chimeway/signal.ex` — `Chimeway.Signal.track/4` API for test signal injection
    - `lib/chimeway/workflows.ex` — `route_signal/1` post-match: `state: :active`, `pending_signals: []`, `reason: "signal_received"` (unchanged per D-07)
  </read_first>
  <action>
    In the same describe `"wait_until auto-populates pending_signals (READ-01)"`, add test `"injected signal resumes waiting run via SignalRouterWorker without host update_run glue"`:

    1. `use Oban.Testing, repo: Chimeway.Repo` at module level if not already present in `Chimeway.Orchestration.WorkflowProgressionTest` (add only if needed for `perform_job/2`).
    2. Trigger workflow via `trigger_workflow_with_signals!/1` — capture `notification`, `workflow_run`, and recipient `recipient_identity` from notification row.
    3. Converge in_app delivery to `:succeeded` (same path as Task 2). Assert run `:waiting` with `pending_signals == ["chimeway.notification.read"]`.
    4. Call `Chimeway.Signal.track(tenant_id, recipient_identity, "chimeway.notification.read", %{})` using the run's `tenant_id` and notification's `recipient_identity`. Capture returned signal struct/id.
    5. `perform_job(Chimeway.Dispatch.SignalRouterWorker, %{"signal_id" => signal.id})` — assert `:ok`.
    6. Reload run: assert `state == :active`, `pending_signals == []`, `status_reason == "signal_received"`.
    7. Assert exactly one `WorkflowTransition` with `reason == "signal_received"` and `context["event_name"] == "chimeway.notification.read"`.

    Explicitly verify the test contains **no** `Workflows.update_run` call setting `pending_signals` — grep the new test body for `pending_signals:` in update calls must find none.

    Do **not** modify `feedback_pipeline_e2e_test.exs` or demo seed files (Phase 50 scope).
  </action>
  <verify>
    <automated>mix test test/chimeway/orchestration/workflow_progression_test.exs test/chimeway/dispatch/signal_router_worker_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - New test name contains `SignalRouterWorker` and passes.
    - After signal injection, run `state == :active` and `pending_signals == []`.
    - One `signal_received` transition with `event_name` only in context (no raw payload keys beyond structural metadata).
    - Test does not call `Workflows.update_run(..., %{pending_signals: ...})` manually.
    - `mix test test/chimeway/orchestration/workflow_progression_test.exs test/chimeway/dispatch/signal_router_worker_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>D-08 success proof: auto-populated pending_signals enable SignalRouterWorker matching without host glue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Progression transaction | `enter_waiting/6` writes durable run state (`state`, `status_context`, `pending_signals`) inside existing `Repo.transaction` — must not widen who can be resumed by signals. |
| Signal routing | `route_signal/1` matches tenant + recipient_identity + event_name ∈ pending_signals — unchanged in Phase 48 (D-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-05 | Tampering | `pending_signals` runtime write | mitigate | Values sourced only from normalized persisted rule JSON via `Map.get(rule, "cancel_signals", [])` — not from signal payloads or host callbacks. |
| T-48-06 | Spoofing | cross-actor signal resume | accept | `find_runs_waiting_for_signal/3` join on `recipient_identity == actor_id` unchanged (D-02). |
| T-48-07 | Spoofing | cross-tenant signal resume | accept | `tenant_id` filter unchanged (T-27-03). |
| T-48-08 | Information Disclosure | signal payload in traces | accept | `route_signal/1` records `event_name` only in transition context — no Phase 48 change (D-07). |

**No `high`-severity threats.** Population is a narrow write to an existing column; routing isolation inherited from Phase 27 is preserved.
</threat_model>

<verification>
- [ ] `mix test test/chimeway/orchestration/workflow_progression_test.exs --warnings-as-errors` green
- [ ] `mix test test/chimeway/dispatch/signal_router_worker_test.exs --warnings-as-errors` green (no regressions)
- [ ] `mix ci.test` green after Wave 2 merge
- [ ] `grep pending_signals lib/chimeway/workflows/progression.ex` shows assignment in `enter_waiting/6`
</verification>

<success_criteria>
- READ-01 engine behavior: `wait_until` wait entry auto-populates `pending_signals` (D-01, D-03)
- Time-only waits keep `pending_signals == []` (D-06)
- SignalRouterWorker matches without host glue (D-08)
- `route_signal/1` unchanged (D-02, D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/48-wait-until-pending-signals/48-02-SUMMARY.md`
</output>
