---
phase: 40
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - chimeway_admin/mix.exs
  - chimeway_admin/lib/chimeway_admin.ex
  - chimeway_admin/lib/chimeway_admin/application.ex
  - chimeway_admin/lib/chimeway_admin/auth.ex
  - chimeway_admin/lib/chimeway_admin/live_auth.ex
  - chimeway_admin/lib/chimeway_admin/router.ex
  - chimeway_admin/test/chimeway_admin/live_auth_test.exs
  - chimeway_admin/.formatter.exs
autonomous: true
requirements:
  - OPER-01
must_haves:
  truths:
    - `chimeway_admin/` compiles as sibling Mix project with path dep on chimeway and Phoenix LiveView deps only in admin package.
    - `ChimewayAdmin.Auth` behaviour exists with fail-closed `authorize/3` contract.
    - Mountable router macro registers exactly two LiveView routes (search + detail).
  artifacts:
    - path: chimeway_admin/lib/chimeway_admin/auth.ex
      provides: "Host auth behaviour seam (D-03, D-04)"
    - path: chimeway_admin/lib/chimeway_admin/router.ex
      provides: "Mountable routes macro (D-06, D-07)"
  key_links:
    - from: chimeway_admin/mix.exs
      to: mix.exs
      via: "path: .. — core stays Phoenix-free (D-01, D-02)"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-40-01 | Unauthorized operator accesses trace data | high | `ChimewayAdmin.LiveAuth` on_mount calls configured `auth_module`; LiveViews halt before data load on `{:error, :unauthorized}` |
| T-40-02 | Admin routes accidentally expose write/mutate endpoints | medium | Router macro only declares GET/live routes; no POST/DELETE in admin package |
</threat_model>

<objective>
Wave 1: Scaffold `chimeway_admin` sibling package with auth behaviour, LiveAuth on_mount hook, and mountable router macro — foundation for OPER-01. Uses Phoenix LiveView (D-05) in admin package only; LiveView business logic ships in Plan 40-02 (stubs OK here for compile).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-operator-trace-mvp/40-CONTEXT.md
@.planning/phases/40-operator-trace-mvp/40-RESEARCH.md
@lib/chimeway/notifier.ex
@mix.exs
@prompts/chimeway-host-app-integration-seam.md
</context>

<tasks>

<task type="auto">
  <name>Task 40-01-01: Create chimeway_admin Mix project</name>
  <files>chimeway_admin/mix.exs, chimeway_admin/lib/chimeway_admin.ex, chimeway_admin/lib/chimeway_admin/application.ex, chimeway_admin/.formatter.exs</files>
  <read_first>
    - .planning/phases/40-operator-trace-mvp/40-CONTEXT.md (D-01, D-02)
    - mix.exs (core deps — no Phoenix)
    - examples/chimeway_demo_host/mix.exs (path dep pattern `{:chimeway, path: "../.."}`)
  </read_first>
  <action>
    Create `chimeway_admin/` at repo root:

    1. `mix.exs` — `app: :chimeway_admin`, `elixir: "~> 1.17"`, deps:
       - `{:chimeway, path: ".."}`
       - `{:phoenix, "~> 1.7"}`
       - `{:phoenix_live_view, "~> 1.0"}`
       - `{:jason, "~> 1.4"}`
       - `{:ecto_sql, "~> 3.11"}` (transitive compile for Chimeway.Repo types if needed)
    2. `lib/chimeway_admin.ex` — module doc stating optional operator UI package.
    3. `lib/chimeway_admin/application.ex` — minimal supervisor `strategy: :one_for_one` with no children (library app).
    4. `.formatter.exs` aligned with repo (line_length 98).
  </action>
  <acceptance_criteria>
    - `cd chimeway_admin && mix compile --warnings-as-errors` exits 0
    - `rg 'phoenix' mix.exs` at repo root (chimeway core) returns no matches
    - `chimeway_admin/mix.exs` contains `{:chimeway, path: ".."}`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 40-01-02: Implement ChimewayAdmin.Auth and LiveAuth</name>
  <files>chimeway_admin/lib/chimeway_admin/auth.ex, chimeway_admin/lib/chimeway_admin/live_auth.ex, chimeway_admin/test/chimeway_admin/live_auth_test.exs</files>
  <read_first>
    - .planning/phases/40-operator-trace-mvp/40-CONTEXT.md (D-03, D-04)
    - lib/chimeway/notifier.ex (@callback pattern)
    - prompts/chimeway-host-app-integration-seam.md
  </read_first>
  <action>
    1. `ChimewayAdmin.Auth` behaviour:
       - `@callback authorize(actor :: term(), action :: atom(), context :: map()) :: :ok | {:error, :unauthorized}`
       - Document actions `:search_traces` and `:view_trace`.
       - `defmodule ChimewayAdmin.Auth do` with `def auth_module, do: Application.fetch_env!(:chimeway_admin, :auth_module)` helper.

    2. `ChimewayAdmin.LiveAuth` — `on_mount/4`:
       - Read `auth_module` from config (or mount option override).
       - Build `context` map with `%{live_view: socket.view, session: session}`.
       - Call `auth_module.authorize(actor, action, context)` where `actor` comes from assign `:current_actor` if present else `session["current_actor"]` (document host must set one).
       - On `{:error, :unauthorized}`: `{:halt, redirect(socket, to: "/")}` or assign `:admin_forbidden` true and halt — pick one pattern and use consistently.

    3. Test `live_auth_test.exs` with a stub module returning `{:error, :unauthorized}` — assert halted socket does not proceed.
  </action>
  <acceptance_criteria>
    - `chimeway_admin/lib/chimeway_admin/auth.ex` contains `@callback authorize`
    - `cd chimeway_admin && mix test test/chimeway_admin/live_auth_test.exs` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 40-01-03: Mountable router macro with route stubs</name>
  <files>chimeway_admin/lib/chimeway_admin/router.ex, chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex, chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex</files>
  <read_first>
    - .planning/phases/40-operator-trace-mvp/40-CONTEXT.md (D-06, D-07)
    - examples/chimeway_demo_host/lib/demo_host_web/router.ex
    - .planning/phases/40-operator-trace-mvp/40-RESEARCH.md (Pattern 1)
  </read_first>
  <action>
    1. `ChimewayAdmin.Router` — `defmacro chimeway_admin_routes(opts \\ [])` expanding to:
       - `live "/", TraceSearchLive, :index`
       - `live "/deliveries/:delivery_id", TraceDetailLive, :show`
       - Inside `live_session :chimeway_admin, on_mount: [{ChimewayAdmin.LiveAuth, :default}]` (pass `auth_action` via `on_mount` metadata per route: `:search_traces` vs `:view_trace`).

    2. Stub LiveViews with minimal `render/1` ("Trace search" / "Trace detail") so macro compiles — full logic in Plan 40-02.

    3. Module doc on `ChimewayAdmin.Router` shows host integration example with `pipe_through [:browser]` and `config :chimeway_admin, auth_module: MyApp.AdminAuth`.
  </action>
  <acceptance_criteria>
    - `rg 'live "/"' chimeway_admin/lib/chimeway_admin/router.ex` matches
    - `rg 'TraceSearchLive|TraceDetailLive' chimeway_admin/lib/chimeway_admin/router.ex` shows both modules
    - `cd chimeway_admin && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd chimeway_admin && mix test` green
- [ ] Root `mix.exs` unchanged regarding Phoenix deps
</verification>
