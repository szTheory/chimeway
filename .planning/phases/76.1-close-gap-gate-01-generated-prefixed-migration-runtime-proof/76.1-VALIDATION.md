---
phase: 76.1
slug: close-gap-gate-01-generated-prefixed-migration-runtime-proof
status: ready
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-02
---

# Phase 76.1 - Validation Strategy

Per-phase validation contract for closing GATE-01: generated prefixed migration runtime proof.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | ExUnit via Mix, with Ecto SQL Sandbox and PostgreSQL-backed integration tests |
| **Config file** | `mix.exs`, `config/config.exs`, `config/test.exs`, `.github/workflows/ci.yml` |
| **Quick run command** | `MIX_ENV=test mix test test/chimeway/generated_prefixed_runtime_proof_test.exs --warnings-as-errors` |
| **Full suite command** | `mix verify.runtime_prefix && mix verify.install_golden && mix ci.verify_gates` |
| **Estimated runtime** | ~180 seconds locally, subject to PostgreSQL startup and migration cost |

---

## Sampling Rate

- **After every task commit:** Run the focused command for the file or helper changed.
- **After every plan wave:** Run `mix verify.runtime_prefix` and `mix verify.install_golden`.
- **Before `/gsd:verify-work`:** Run `mix verify.runtime_prefix && mix verify.install_golden && mix ci.verify_gates`.
- **Max feedback latency:** One task may add a red proof before the implementation exists; no later task may proceed without a focused green proof.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 76.1-01-01 | 01 | 1 | GATE-01-GAP | T-76.1-01 | No false release evidence from clone-only schema setup | integration | `MIX_ENV=test mix test test/chimeway/generated_prefixed_runtime_proof_test.exs --warnings-as-errors` | No - Wave 0 creates it | pending |
| 76.1-01-02 | 01 | 1 | GATE-01-GAP | T-76.1-02 | Generated prefixed fixture migrations run through a database used by `Chimeway.Repo` | integration | `MIX_ENV=test mix test test/chimeway/generated_prefixed_runtime_proof_test.exs --warnings-as-errors` | No - Wave 0 creates it | pending |
| 76.1-01-03 | 01 | 1 | GATE-01-GAP | T-76.1-03 | Runtime API rows land only under `chimeway.*` after generated migrations | integration | `mix verify.runtime_prefix` | Existing alias, missing bridge file | pending |
| 76.1-01-04 | 01 | 1 | GATE-01-GAP | T-76.1-04 | Existing generated installer proofs and path-gated release contracts remain intact | contract | `mix verify.install_golden && mix ci.verify_gates` | Yes | pending |

---

## Wave 0 Requirements

- [ ] `test/chimeway/generated_prefixed_runtime_proof_test.exs` - focused red/green bridge proof for generated-prefixed migrations plus runtime API behavior through `Chimeway.Repo`.
- [ ] `test/support/generated_prefixed_runtime_case.ex` or extracted helper functions - materialize the committed prefixed installer fixture, isolate migration execution from SQL Sandbox ownership failures, and clean up temporary database resources.
- [ ] `mix.exs` - include the bridge proof in `verify.runtime_prefix` so the existing `verify_runtime_prefix` CI job covers it.

---

## Manual-Only Verifications

All phase behaviors must have automated verification. CI remains the authoritative PostgreSQL 15 proof when local PostgreSQL differs from the project target.

---

## Validation Sign-Off

- [x] All planned behaviors have automated verification commands or Wave 0 dependencies.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify.
- [x] Wave 0 covers all missing references from research.
- [x] No watch-mode flags.
- [x] Feedback latency target defined for red/green proof iteration.
- [x] `nyquist_compliant: true` set in frontmatter.

**Approval:** approved 2026-07-02
