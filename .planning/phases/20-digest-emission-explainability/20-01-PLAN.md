---
phase: 20-digest-emission-explainability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/delivery.ex
  - lib/chimeway/deliveries.ex
  - lib/chimeway/digests/digest_bucket.ex
  - lib/chimeway/digests/digest_membership.ex
  - lib/chimeway/digests/emission.ex
  - priv/repo/migrations/*_alter_chimeway_digest_buckets_for_emission.exs
  - priv/repo/migrations/*_alter_chimeway_digest_memberships_for_resolution.exs
  - priv/repo/migrations/*_alter_chimeway_deliveries_for_digest_outcome.exs
  - test/chimeway/digests/emission_test.exs
autonomous: true
requirements:
  - DIGEST-02
must_haves:
  truths:
    - "A due digest bucket can be claimed and emitted at most once at the database layer, even when the flush operation is retried."
    - "Each digest membership persists one durable resolution outcome plus the emitted digest delivery linkage when included."
    - "Every source delivery touched by flush converges explicitly on the canonical row as `digested`, `skipped_by_policy`, `emitted_immediately`, or still unresolved for a future flush."
  artifacts:
    - path: "lib/chimeway/digests/emission.ex"
      provides: "Transactional digest bucket claim and emitted digest creation service"
      min_lines: 80
    - path: "lib/chimeway/digests/digest_membership.ex"
      provides: "Membership resolution fields and emitted digest linkage"
      min_lines: 45
    - path: "lib/chimeway/digests/digest_bucket.ex"
      provides: "Flush claim / emitted digest identity fields"
      min_lines: 45
    - path: "lib/chimeway/delivery.ex"
      provides: "Explicit source-row digest outcome and flush-resolution contract"
      min_lines: 40
    - path: "lib/chimeway/deliveries.ex"
      provides: "Named helpers that persist included, skipped, and immediate-send flush outcomes on canonical source rows"
      min_lines: 45
    - path: "priv/repo/migrations/*_alter_chimeway_digest_memberships_for_resolution.exs"
      provides: "Membership resolution columns and foreign-key linkage to digest deliveries"
      contains: "alter table(:chimeway_digest_memberships"
    - path: "test/chimeway/digests/emission_test.exs"
      provides: "Automated proof of single-emission, resolution persistence, and source-row convergence"
      min_lines: 90
  key_links:
    - from: "lib/chimeway/digests/emission.ex"
      to: "lib/chimeway/digests/digest_membership.ex"
      via: "flush resolves memberships with one durable resolution write per source delivery"
      pattern: "resolution|resolution_reason|resolved_at|digest_delivery_id"
    - from: "lib/chimeway/digests/emission.ex"
      to: "lib/chimeway/digests/digest_bucket.ex"
      via: "bucket claim and emitted digest identity prevent duplicate flushes"
      pattern: "flush_state|claimed_at|emitted_at|digest_delivery_id"
    - from: "lib/chimeway/digests/emission.ex"
      to: "lib/chimeway/delivery.ex"
      via: "source rows persist explicit included, skipped, and immediate-send outcomes instead of relying on inference"
      pattern: "digested|digest_flush_outcome|digest_flush_reason|status|orchestration_state"
---

<objective>
Define the durable data contract for digest emission before runtime dispatch or operator explainability work builds on it.

Purpose: DIGEST-02 is not satisfied by Phase 19 accumulation alone. Phase 20 first needs a correctness boundary for bucket claiming, membership resolution persistence, emitted digest identity, and source-row convergence so later dispatch and trace work can rely on one stable truth.
Output: schema/migration changes plus a transactional `Chimeway.Digests.Emission` service and RED/GREEN tests that lock single-emission behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-digest-emission-explainability/20-CONTEXT.md
@.planning/phases/20-digest-emission-explainability/20-RESEARCH.md
@.planning/phases/20-digest-emission-explainability/20-PATTERNS.md
@.planning/phases/20-digest-emission-explainability/20-VALIDATION.md
@.planning/phases/19-digest-data-model-accumulation/19-CONTEXT.md
@lib/chimeway/delivery.ex
@lib/chimeway/deliveries.ex
@lib/chimeway/digests/accumulation.ex
@lib/chimeway/digests/digest_bucket.ex
@lib/chimeway/digests/digest_membership.ex

<interfaces>
From `lib/chimeway/delivery.ex`:
```elixir
field(:status, Ecto.Enum, values: [:pending, :dispatched, :succeeded, :failed, :suppressed, :cancelled], default: :pending)
field(:orchestration_state, Ecto.Enum, values: [:ready, :deferred, :digest_held], default: :ready)
field(:planning_context, :map)
field(:metadata, :map)
```

From `lib/chimeway/deliveries.ex`:
```elixir
def transition_status(%Delivery{} = delivery, new_status)
def suppress_delivery(%Delivery{} = delivery, reason, opts \\ [])
def cancel_deferred_delivery(binary() | Delivery.t(), String.t(), keyword())
def exhaust_delivery(%Delivery{status: :failed} = delivery)
```

From `lib/chimeway/digests/accumulation.ex`:
```elixir
def accumulate_delivery(%Delivery{} = delivery, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock digest emission durability with RED tests</name>
  <files>test/chimeway/digests/emission_test.exs</files>
  <read_first>
    - test/chimeway/digests/accumulation_test.exs
    - test/chimeway/orchestration/delivery_planning_test.exs
    - lib/chimeway/delivery.ex
    - lib/chimeway/digests/digest_bucket.ex
    - lib/chimeway/digests/digest_membership.ex
    - .planning/phases/20-digest-emission-explainability/20-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: emitting the same due bucket twice creates at most one emitted digest delivery identity.
    - Test 2: included memberships persist `resolution`, `resolution_reason`, `resolved_at`, and `digest_delivery_id`.
    - Test 3: included source rows no longer remain `status: :pending` and link back to the emitted digest delivery.
    - Test 4: skipped-at-flush and immediate-send source rows persist explicit canonical-row outcome fields and reasons instead of relying on absence or membership inference.
    - Test 5: unresolved or already-emitted buckets return a no-op or reuse path without duplicating memberships or emitted digest rows.
  </behavior>
  <action>Create `test/chimeway/digests/emission_test.exs` per D-01 through D-06 and D-10 through D-12. Seed a digest rule, bucket, memberships, and source delivery rows using the Phase 19 accumulation path, then require a new `Chimeway.Digests.Emission.emit_bucket/2` contract that: locks one due bucket, creates one emitted digest delivery identity, resolves all included memberships with durable facts, converges included source rows in place, persists canonical-row fields for skipped-at-flush and immediate-send outcomes, and returns the same emitted digest identity on duplicate or retry execution. The tests must prove that correctness survives repeated calls without depending on worker uniqueness and that operators can distinguish `digested`, `emitted_immediately`, `skipped_by_policy`, and unresolved future work without secondary inference.</action>
  <acceptance_criteria>
    - `test/chimeway/digests/emission_test.exs` contains `emit_bucket`
    - `test/chimeway/digests/emission_test.exs` contains `digest_delivery_id`
    - `test/chimeway/digests/emission_test.exs` contains `resolution_reason`
    - `test/chimeway/digests/emission_test.exs` contains `status == :digested`
    - `test/chimeway/digests/emission_test.exs` contains `digest_flush_outcome`
    - `test/chimeway/digests/emission_test.exs` contains `emitted_immediately`
    - `mix test test/chimeway/digests/emission_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/digests/emission_test.exs --trace</automated>
  </verify>
  <done>The tests define the durable correctness boundary for digest emission, emitted digest identity reuse, and source-row convergence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the durable emission contract and schema changes</name>
  <files>lib/chimeway/delivery.ex, lib/chimeway/deliveries.ex, lib/chimeway/digests/digest_bucket.ex, lib/chimeway/digests/digest_membership.ex, lib/chimeway/digests/emission.ex, priv/repo/migrations/*_alter_chimeway_digest_buckets_for_emission.exs, priv/repo/migrations/*_alter_chimeway_digest_memberships_for_resolution.exs, priv/repo/migrations/*_alter_chimeway_deliveries_for_digest_outcome.exs</files>
  <read_first>
    - lib/chimeway/delivery.ex
    - lib/chimeway/deliveries.ex
    - lib/chimeway/digests/accumulation.ex
    - priv/repo/migrations/20260424082833_create_chimeway_deliveries.exs
    - priv/repo/migrations/20260428102100_create_chimeway_digest_buckets.exs
    - priv/repo/migrations/20260428102200_create_chimeway_digest_memberships.exs
    - .planning/phases/20-digest-emission-explainability/20-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `emit_bucket/2` uses one DB transaction and one bucket claim boundary.
    - Test 2: source deliveries included in a digest land on an explicit digest terminal outcome.
    - Test 3: skipped-at-flush and immediate-send outcomes are persisted directly on canonical source rows with explicit fields and reasons.
    - Test 4: membership and bucket rows persist enough durable identity to explain later flush decisions without re-derivation.
  </behavior>
  <action>Implement `lib/chimeway/digests/emission.ex` and extend the digest schemas and migrations per D-01 through D-06 and D-10 through D-12. Add bucket-level fields that let the flush path claim a bucket and persist the emitted digest delivery identity. Add membership-level fields for durable resolution status, reason, resolved timestamp, emitted digest delivery linkage, and rule/window snapshot facts. Extend `Delivery` with an explicit source-row digest outcome contract: included rows may use a terminal `:digested` status, while all flushed source rows must also persist canonical-row fields such as `digest_flush_outcome`, `digest_flush_reason`, `digest_flush_resolved_at`, and emitted digest linkage so skipped and immediate-send outcomes are first-class and queryable. Add named `Deliveries` helper(s) that write these included, skipped, and immediate-send outcomes in one obvious place. `emit_bucket/2` must lock the bucket, short-circuit if it is already emitted, preload unresolved memberships in bulk, create or reuse one emitted digest delivery identity, resolve included memberships, persist the bucket emitted state, and avoid N+1 row loading.</action>
  <acceptance_criteria>
    - `lib/chimeway/delivery.ex` contains `:digested`
    - `lib/chimeway/delivery.ex` contains `field(:digest_flush_outcome`
    - `lib/chimeway/delivery.ex` contains `field(:digest_flush_reason`
    - `lib/chimeway/digests/digest_membership.ex` contains `field(:resolution`
    - `lib/chimeway/digests/digest_membership.ex` contains `belongs_to(:digest_delivery, Chimeway.Delivery)`
    - `lib/chimeway/digests/digest_bucket.ex` contains `field(:flush_state`
    - `lib/chimeway/deliveries.ex` contains `mark_digest_skipped`
    - `lib/chimeway/deliveries.ex` contains `mark_digest_immediate`
    - `lib/chimeway/digests/emission.ex` contains `Repo.transact`
    - `lib/chimeway/digests/emission.ex` contains `FOR UPDATE`
    - `priv/repo/migrations/*_alter_chimeway_deliveries_for_digest_outcome.exs` contains `:digested`
    - `mix test test/chimeway/digests/emission_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/digests/emission_test.exs --trace</automated>
  </verify>
  <done>Phase 20 now has a durable schema and transaction boundary for exactly-once digest emission and explicit source-row convergence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| due digest bucket -> emission transaction | Duplicate execution must not create multiple emitted digest deliveries |
| source delivery rows -> source-row convergence helpers | Included rows must land on an explicit durable outcome, not remain pending |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | T | `lib/chimeway/digests/emission.ex` | mitigate | Claim and emit the bucket inside one transaction with bucket-level emitted identity reuse. |
| T-20-02 | R | `lib/chimeway/digests/digest_membership.ex` | mitigate | Persist immutable resolution facts and emitted digest delivery linkage on the membership itself. |
| T-20-03 | D | `lib/chimeway/delivery.ex`, `lib/chimeway/deliveries.ex` | mitigate | Introduce an explicit digest terminal outcome so included source rows cannot stay pending indefinitely. |
</threat_model>

<verification>
Run `mix test test/chimeway/digests/emission_test.exs --trace`.
</verification>

<success_criteria>
One due bucket produces one durable emitted digest identity, memberships persist exact flush outcomes, and included source rows converge in place with an explicit digest outcome.
</success_criteria>

<output>
After completion, create `.planning/phases/20-digest-emission-explainability/20-01-SUMMARY.md`
</output>
