---
phase: 20-digest-emission-explainability
plan: 03
type: execute
wave: 3
depends_on:
  - 20-01
  - 20-02
files_modified:
  - lib/chimeway/traces.ex
  - lib/chimeway/traces/explanation.ex
  - test/chimeway/orchestration/digest_explainability_test.exs
  - test/chimeway/traces_test.exs
  - test/chimeway/integration/digest_delivery_lifecycle_test.exs
autonomous: true
requirements:
  - DIGEST-03
  - DIGEST-02
must_haves:
  truths:
    - "Operators can explain why a source notification joined a digest, skipped a digest, or emitted immediately instead."
    - "Operators can inspect an emitted digest delivery and see which source deliveries were included, excluded, or deferred under which rule and window."
    - "Digest explanation surfaces remain sanitized and row-centric rather than relying on raw payload blobs or queue logs."
  artifacts:
    - path: "lib/chimeway/traces.ex"
      provides: "Digest-aware source and emitted delivery explanation logic"
      min_lines: 70
    - path: "lib/chimeway/traces/explanation.ex"
      provides: "Explanation contract additions for digest reasoning fields"
      min_lines: 25
    - path: "test/chimeway/orchestration/digest_explainability_test.exs"
      provides: "Automated proof of included/skipped/immediate explanation behavior"
      min_lines: 70
  key_links:
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/digests/digest_membership.ex"
      via: "source explanations read durable membership resolution facts instead of inferring outcomes"
      pattern: "resolution|resolution_reason|digest_delivery_id"
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/traces/explanation.ex"
      via: "digest fields become part of the stable operator explanation contract"
      pattern: "timeline|digest|included|excluded|deferred|skipped|immediate"
---

<objective>
Finish Phase 20 by turning the new durable digest facts into an operator-visible reasoning surface.

Purpose: DIGEST-03 is the product value of this phase. The runtime work is incomplete until operators can answer why a specific source row was included, skipped, or sent immediately, and can inspect an emitted digest without reverse-engineering bucket tables or queue behavior.
Output: digest-aware trace/explanation contracts plus integration tests proving exact, sanitized reasoning on both source and emitted digest rows.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-digest-emission-explainability/20-CONTEXT.md
@.planning/phases/20-digest-emission-explainability/20-RESEARCH.md
@.planning/phases/20-digest-emission-explainability/20-PATTERNS.md
@.planning/phases/20-digest-emission-explainability/20-VALIDATION.md
@.planning/phases/20-digest-emission-explainability/20-01-PLAN.md
@.planning/phases/20-digest-emission-explainability/20-02-PLAN.md
@lib/chimeway/traces.ex
@lib/chimeway/traces/explanation.ex
@lib/chimeway/digests/digest_membership.ex
@test/chimeway/orchestration/traces_deferral_test.exs
@test/chimeway/traces_test.exs

<interfaces>
From `lib/chimeway/traces.ex`:
```elixir
def explain_delivery(String.t(), keyword()) :: {:ok, Explanation.t()} | {:error, :not_found}
```

From `lib/chimeway/traces/explanation.ex`:
```elixir
defstruct [
  :delivery_id,
  :event_id,
  :correlation_id,
  :notification_key,
  :recipient_id,
  :channel,
  :status,
  :planning_reason,
  :planning_context,
  :next_eligible_at,
  :resume_source,
  :resume_scheduled_at,
  :resumed_at,
  :suppression_reason,
  :last_attempt,
  :timeline
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for source and emitted digest explanations</name>
  <files>test/chimeway/orchestration/digest_explainability_test.exs, test/chimeway/traces_test.exs, test/chimeway/integration/digest_delivery_lifecycle_test.exs</files>
  <read_first>
    - test/chimeway/orchestration/traces_deferral_test.exs
    - test/chimeway/traces_test.exs
    - test/chimeway/integration/delivery_lifecycle_test.exs
    - lib/chimeway/traces.ex
    - lib/chimeway/traces/explanation.ex
  </read_first>
  <behavior>
    - Test 1: a source row included in a digest explains the emitted digest linkage, rule/window identity, and inclusion reason.
    - Test 2: a source row skipped at flush explains why it did not join the emitted digest.
    - Test 3: a source row emitted immediately instead explains why it left digest hold.
    - Test 4: the emitted digest delivery explanation lists included, excluded, and deferred source deliveries with durable reasons and without leaking raw payloads or provider response data.
  </behavior>
  <action>Add `test/chimeway/orchestration/digest_explainability_test.exs` and extend `test/chimeway/traces_test.exs` plus `test/chimeway/integration/digest_delivery_lifecycle_test.exs` per D-07 through D-15. The tests must assert exact, durable reasoning fields for included, skipped, deferred, excluded, and immediate-send outcomes, and must prove the emitted digest explanation can enumerate included, excluded, and still-deferred source deliveries plus the rule/window identity used at flush. Keep the assertions sanitized: no raw event payload maps, rendered content, or provider responses should appear in the explanation surface.</action>
  <acceptance_criteria>
    - `test/chimeway/orchestration/digest_explainability_test.exs` contains `included`
    - `test/chimeway/orchestration/digest_explainability_test.exs` contains `emitted_immediately`
    - `test/chimeway/orchestration/digest_explainability_test.exs` contains `digest_delivery_id`
    - `test/chimeway/orchestration/digest_explainability_test.exs` contains `excluded`
    - `test/chimeway/orchestration/digest_explainability_test.exs` contains `deferred`
    - `test/chimeway/traces_test.exs` contains `rule_identity`
    - `mix test test/chimeway/orchestration/digest_explainability_test.exs test/chimeway/traces_test.exs test/chimeway/integration/digest_delivery_lifecycle_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/orchestration/digest_explainability_test.exs test/chimeway/traces_test.exs test/chimeway/integration/digest_delivery_lifecycle_test.exs --trace</automated>
  </verify>
  <done>The failing tests define the operator-visible digest explanation contract for both source and emitted delivery rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend `Chimeway.Traces` and explanation contracts for digest reasoning</name>
  <files>lib/chimeway/traces.ex, lib/chimeway/traces/explanation.ex</files>
  <read_first>
    - lib/chimeway/traces.ex
    - lib/chimeway/traces/explanation.ex
    - lib/chimeway/digests/digest_membership.ex
    - .planning/phases/20-digest-emission-explainability/20-RESEARCH.md
    - .planning/phases/20-digest-emission-explainability/20-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: source explanations expose digest resolution fields from durable membership state.
    - Test 2: emitted digest explanations can surface included, excluded, and deferred-member summaries under `Chimeway.Traces`.
    - Test 3: all digest explanation output remains sanitized and timeline-based rather than payload-dump-based.
  </behavior>
  <action>Update `lib/chimeway/traces.ex` and `lib/chimeway/traces/explanation.ex` so digest-specific reasoning remains under the existing public trace API. Add explanation fields or nested maps as needed to represent source-row digest outcome, emitted digest linkage, membership resolution reason, and emitted-digest member summaries for included, excluded, and still-deferred memberships. Build these from durable delivery and membership fields, not from queue state or raw payloads. Add timeline events for digest convergence where helpful, but keep the primary operator question answerable in one `explain_delivery/2` call for both source and emitted digest rows.</action>
  <acceptance_criteria>
    - `lib/chimeway/traces/explanation.ex` contains `digest`
    - `lib/chimeway/traces.ex` contains `digest_delivery_id`
    - `lib/chimeway/traces.ex` contains `resolution_reason`
    - `lib/chimeway/traces.ex` contains `rule_identity`
    - `lib/chimeway/traces.ex` contains `excluded`
    - `lib/chimeway/traces.ex` contains `deferred`
    - `mix test test/chimeway/orchestration/digest_explainability_test.exs test/chimeway/traces_test.exs test/chimeway/integration/digest_delivery_lifecycle_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/orchestration/digest_explainability_test.exs test/chimeway/traces_test.exs test/chimeway/integration/digest_delivery_lifecycle_test.exs --trace</automated>
  </verify>
  <done>Operators can now answer the Phase 20 reasoning questions from `Chimeway.Traces` without secondary inference or unsafe payload exposure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable digest facts -> operator surfaces | Explanations must expose exact reasoning without leaking sensitive source payloads |
| emitted digest delivery -> operator reasoning | Operators should not need bucket-table archaeology or queue inspection to understand the outcome |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | I | `lib/chimeway/traces.ex` | mitigate | Surface only durable resolution facts, rule/window identity, and digest linkage; never raw payload/provider data. |
| T-20-08 | R | `lib/chimeway/traces.ex`, `lib/chimeway/traces/explanation.ex` | mitigate | Make included/skipped/immediate reasons first-class explanation fields backed by persisted membership resolution data. |
| T-20-09 | D | digest explanation tests | mitigate | Lock the contract with explicit tests for source and emitted digest reasoning so future refactors cannot erase or conflate the operator story. |
</threat_model>

<verification>
Run `mix test test/chimeway/orchestration/digest_explainability_test.exs test/chimeway/traces_test.exs test/chimeway/integration/digest_delivery_lifecycle_test.exs --trace`.
</verification>

<success_criteria>
`Chimeway.Traces` can explain source inclusion, exclusion, and immediate-send decisions plus emitted digest contents from durable sanitized data alone.
</success_criteria>

<output>
After completion, create `.planning/phases/20-digest-emission-explainability/20-03-SUMMARY.md`
</output>
