---
phase: 20-digest-emission-explainability
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - lib/chimeway/digests/emission.ex
  - lib/chimeway/digests.ex
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/dispatch/oban.ex
  - lib/chimeway/dispatch/oban_worker.ex
  - lib/chimeway/dispatch/digest_flush_worker.ex
  - test/chimeway/digests/emission_test.exs
  - test/chimeway/integration/digest_delivery_lifecycle_test.exs
autonomous: true
requirements:
  - DIGEST-02
must_haves:
  truths:
    - "Emitted digest deliveries reuse the normal canonical dispatch lifecycle through an explicit pre-planned delivery handoff seam instead of a digest-only send path."
    - "Duplicate flush worker execution or manual retries reuse the same emitted digest delivery identity and do not resend source memberships."
    - "Flush-time resolution can route a source row to included, skipped, or immediate-send behavior using durable canonical-row outcome fields and canonical handoff paths."
    - "Before any handoff to `Dispatch.Executor`, the emitted digest delivery row already carries adapter-ready rendered metadata/content and does not rely on notifier build callbacks at dispatch time."
  artifacts:
    - path: "lib/chimeway/digests/emission.ex"
      provides: "Flush runtime that creates emitted digest lifecycle records, builds digest content, and hands them to dispatch"
      min_lines: 130
    - path: "lib/chimeway/dispatch.ex"
      provides: "Dispatcher seam extended or clarified for pre-planned delivery handoff"
      min_lines: 20
    - path: "lib/chimeway/dispatch/digest_flush_worker.ex"
      provides: "Optional Oban worker for due digest bucket execution with thin job args"
      min_lines: 35
    - path: "test/chimeway/integration/digest_delivery_lifecycle_test.exs"
      provides: "End-to-end proof that emitted digest deliveries dispatch through the normal lifecycle with pre-populated content"
      min_lines: 80
  key_links:
    - from: "lib/chimeway/digests/emission.ex"
      to: "lib/chimeway/dispatch.ex"
      via: "emission uses an explicit pre-planned delivery handoff seam instead of re-entering notification planning"
      pattern: "dispatch_delivery|enqueue_delivery|delivery_id|orchestration_state: :ready"
    - from: "lib/chimeway/digests/emission.ex"
      to: "lib/chimeway/adapter.ex"
      via: "emission populates digest subject/body/summary metadata on the emitted delivery row before any adapter call"
      pattern: "metadata|subject|body|summary|content"
    - from: "lib/chimeway/digests/emission.ex"
      to: "lib/chimeway/dispatch/oban.ex"
      via: "Oban path inserts or enqueues the emitted digest delivery by delivery_id without replanning notifications"
      pattern: "multi|ObanWorker|delivery_id|enqueue"
    - from: "lib/chimeway/dispatch/digest_flush_worker.ex"
      to: "lib/chimeway/digests/emission.ex"
      via: "worker carries only bucket identity and delegates business logic to the durable emission service"
      pattern: "bucket_id|emit_bucket"
---

<objective>
Make digest emission actually runnable by reusing Chimeway's existing delivery execution path.

Purpose: the durable schema from Plan 20-01 is only half the milestone. DIGEST-02 also requires that emitted digests can be dispatched safely under retries and duplicate job execution without creating a digest-specific send pipeline or queue-shaped truth.
Output: due-bucket runtime, optional flush worker, and end-to-end lifecycle tests proving canonical dispatch reuse and duplicate-execution safety.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-digest-emission-explainability/20-CONTEXT.md
@.planning/phases/20-digest-emission-explainability/20-RESEARCH.md
@.planning/phases/20-digest-emission-explainability/20-PATTERNS.md
@.planning/phases/20-digest-emission-explainability/20-VALIDATION.md
@.planning/phases/20-digest-emission-explainability/20-01-PLAN.md
@lib/chimeway/adapter.ex
@lib/chimeway/digests/emission.ex
@lib/chimeway/dispatch.ex
@lib/chimeway/dispatch/oban.ex
@lib/chimeway/dispatch/oban_worker.ex
@lib/chimeway/dispatch/sync.ex
@lib/chimeway/delivery_planning.ex
@test/chimeway/orchestration/dispatch_gating_test.exs
@test/chimeway/integration/delivery_lifecycle_test.exs

<interfaces>
From `lib/chimeway/adapter.ex`:
```elixir
@callback deliver(delivery :: Chimeway.Delivery.t(), config :: keyword()) ::
            {:ok, map()} | {:error, atom(), map()}
```

From `lib/chimeway/dispatch.ex`:
```elixir
@callback dispatch(notifications :: [Notification.t()], opts :: keyword()) ::
            {:ok, [Delivery.t()]} | {:error, term()}
```

From `lib/chimeway/dispatch/oban.ex`:
```elixir
def dispatch(notifications, opts) when is_list(notifications)
```

From `lib/chimeway/dispatch/oban_worker.ex`:
```elixir
def perform(%Oban.Job{args: %{"delivery_id" => delivery_id}})
```

From `lib/chimeway/digests/emission.ex` after Plan 20-01:
```elixir
def emit_bucket(bucket_id_or_bucket, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED runtime and lifecycle tests for digest dispatch</name>
  <files>test/chimeway/digests/emission_test.exs, test/chimeway/integration/digest_delivery_lifecycle_test.exs</files>
  <read_first>
    - test/chimeway/digests/emission_test.exs
    - test/chimeway/integration/delivery_lifecycle_test.exs
    - test/chimeway/orchestration/dispatch_gating_test.exs
    - lib/chimeway/dispatch/oban.ex
    - lib/chimeway/dispatch/oban_worker.ex
  </read_first>
  <behavior>
    - Test 1: an emitted digest delivery is created as a normal `:ready` delivery, with adapter-ready metadata/content already populated, and is handed off through the explicit pre-planned delivery seam without re-entering notification planning.
    - Test 2: duplicate flush calls or duplicate worker jobs reuse the same emitted digest delivery identity and do not duplicate sends.
    - Test 3: the Oban path enqueues by emitted `delivery_id` inside the flush transaction, while the sync/default path performs a post-commit handoff on the already-created delivery row.
    - Test 4: emitted digest adapter calls receive the expected pre-populated digest metadata such as subject/body/summary fields or equivalent content keys.
    - Test 5: flush-time resolution can mark a member included, skipped, or released to immediate send and each path preserves durable explainable facts.
  </behavior>
  <action>Extend `test/chimeway/digests/emission_test.exs` and add `test/chimeway/integration/digest_delivery_lifecycle_test.exs` per D-01 through D-06 and D-10 through D-12. Require the emitted digest path to produce a canonical ready delivery that can be handed off by `delivery_id` through a new or clarified pre-planned delivery seam, rather than by re-entering notification planning. Add duplicate-execution coverage for both direct `emit_bucket/2` retries and optional Oban worker retries. Add one test for the Oban path proving transactional enqueue of the emitted digest delivery, and one test for the sync/default path proving post-commit execution on the already-created delivery row. Add explicit assertions that the adapter receives pre-populated digest content keys on the emitted delivery row, built before handoff. Add one immediate-send-at-flush fixture proving a source delivery can leave digest hold and dispatch individually while persisting explicit canonical-row immediate-send reasoning.</action>
  <acceptance_criteria>
    - `test/chimeway/integration/digest_delivery_lifecycle_test.exs` contains `emit_bucket`
    - `test/chimeway/integration/digest_delivery_lifecycle_test.exs` contains `orchestration_state == :ready`
    - `test/chimeway/integration/digest_delivery_lifecycle_test.exs` contains `delivery_id`
    - `test/chimeway/integration/digest_delivery_lifecycle_test.exs` contains `post_commit`
    - `test/chimeway/integration/digest_delivery_lifecycle_test.exs` contains `pre_planned`
    - `test/chimeway/integration/digest_delivery_lifecycle_test.exs` contains `subject`
    - `test/chimeway/integration/digest_delivery_lifecycle_test.exs` contains `body`
    - `test/chimeway/digests/emission_test.exs` contains `emitted_immediately`
    - `mix test test/chimeway/digests/emission_test.exs test/chimeway/integration/digest_delivery_lifecycle_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/digests/emission_test.exs test/chimeway/integration/digest_delivery_lifecycle_test.exs --trace</automated>
  </verify>
  <done>The runtime tests define how digest emission reuses the normal delivery lifecycle and survives duplicate execution safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement due-bucket runtime and canonical dispatch handoff</name>
  <files>lib/chimeway/digests/emission.ex, lib/chimeway/digests.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/dispatch.ex, lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban.ex, lib/chimeway/dispatch/oban_worker.ex, lib/chimeway/dispatch/digest_flush_worker.ex</files>
  <read_first>
    - lib/chimeway/dispatch.ex
    - lib/chimeway/digests/emission.ex
    - lib/chimeway/digests.ex
    - lib/chimeway/dispatch/sync.ex
    - lib/chimeway/dispatch/oban.ex
    - lib/chimeway/dispatch/oban_worker.ex
    - .planning/phases/20-digest-emission-explainability/20-RESEARCH.md
    - .planning/phases/20-digest-emission-explainability/20-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: the emitted digest row is created once, populated with digest content/metadata, and handed off through a pre-planned delivery seam the configured dispatcher understands.
    - Test 2: the Oban path can enqueue the emitted digest delivery by `delivery_id` inside the same transaction that creates it.
    - Test 3: the sync/default path performs a post-commit handoff on the already-created delivery without violating the "no adapter call in transaction" rule.
    - Test 4: duplicate bucket execution and duplicate worker jobs do not produce duplicate emitted digest deliveries or duplicate sends.
  </behavior>
  <action>Complete `Chimeway.Digests.Emission` so it creates or reuses the canonical emitted digest event/notification/delivery chain, builds the emitted digest content before dispatch, and hands the emitted delivery through an explicit pre-planned delivery seam. The plan must assign ownership for content synthesis directly to this runtime: aggregate the included source notifications into emitted-delivery metadata keys such as subject/body/summary/items (or the repo's equivalent adapter-facing content fields), persist `notification_key`/version, recipient scope, and emitted digest identifiers on the canonical emitted row, and avoid notifier build callbacks during dispatch. Revise the dispatcher contract or add an adjacent helper so a ready delivery can be executed or enqueued by `delivery_id` without re-entering `DeliveryPlanning.plan_notification/2`. For the Oban path, insert or enqueue the emitted digest `ObanWorker` job inside the same transaction that creates the emitted delivery row. For the sync/default path, commit the emitted digest row first, then immediately perform a post-commit handoff on that pre-created delivery so adapter calls never hold the flush transaction open. If the repo needs a phase-local execution seam for due buckets, add `lib/chimeway/dispatch/digest_flush_worker.ex` using thin job args like `%{bucket_id: ...}` and have it delegate immediately to `emit_bucket/2`. Keep Oban optional: the emission service itself must remain correct without worker uniqueness, and duplicate execution must collapse on the emitted digest identity stored in the database. Where flush-time logic chooses immediate-send instead of inclusion, promote the source row through the same pre-planned delivery handoff while preserving explicit canonical-row immediate-send fields.</action>
  <acceptance_criteria>
    - `lib/chimeway/dispatch.ex` contains `dispatch_delivery`
    - `lib/chimeway/dispatch/sync.ex` contains `dispatch_delivery`
    - `lib/chimeway/digests/emission.ex` contains `delivery_id`
    - `lib/chimeway/digests/emission.ex` contains `orchestration_state: :ready`
    - `lib/chimeway/digests/emission.ex` contains `subject`
    - `lib/chimeway/digests/emission.ex` contains `body`
    - `lib/chimeway/digests/emission.ex` contains `summary`
    - `lib/chimeway/dispatch/digest_flush_worker.ex` contains `bucket_id`
    - `lib/chimeway/dispatch/digest_flush_worker.ex` contains `emit_bucket`
    - `lib/chimeway/dispatch/oban.ex` contains `dispatch_delivery`
    - `lib/chimeway/digests/emission.ex` contains `emitted_immediately`
    - `mix test test/chimeway/digests/emission_test.exs test/chimeway/integration/digest_delivery_lifecycle_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/digests/emission_test.exs test/chimeway/integration/digest_delivery_lifecycle_test.exs --trace</automated>
  </verify>
  <done>Digest emission now runs through the same lifecycle machinery as any other delivery and remains safe under retries and duplicate jobs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| emitted digest creation -> dispatcher | Emitted digest rows must be canonical ready deliveries, not ad hoc provider calls |
| duplicate worker jobs -> durable digest state | Worker retries must reuse DB-backed emitted identity and membership resolutions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | T | `lib/chimeway/digests/emission.ex` | mitigate | Persist emitted digest identity on the bucket and reuse it on duplicate execution. |
| T-20-05 | D | `lib/chimeway/dispatch/digest_flush_worker.ex` | mitigate | Keep job args thin and delegate all correctness decisions to durable DB reads. |
| T-20-06 | R | emitted digest runtime | mitigate | Reuse canonical dispatch paths so attempt history, retries, and terminal convergence stay on normal delivery rows. |
</threat_model>

<verification>
Run `mix test test/chimeway/digests/emission_test.exs test/chimeway/integration/digest_delivery_lifecycle_test.exs --trace`.
</verification>

<success_criteria>
Emitted digest deliveries dispatch through the normal lifecycle, retries reuse one emitted digest identity, and flush-time immediate/skip behaviors remain durable and idempotent.
</success_criteria>

<output>
After completion, create `.planning/phases/20-digest-emission-explainability/20-02-SUMMARY.md`
</output>
