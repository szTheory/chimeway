---
phase: 34-feedback-contract-e2e-proof
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/chimeway/traces_test.exs
autonomous: true
requirements: [FLOW-01]
requirements_addressed: [FLOW-01]
tags: [elixir, test-fixture, vocabulary, audit-closure]

must_haves:
  truths:
    - "Synthetic trace fixtures in `test/chimeway/traces_test.exs` use the canonical signal event-name string `chimeway.delivery.succeeded` (D-01 / D-03), not the drifted `chimeway.delivery.delivered`."
    - "The fix is mechanical and surgical — exactly two lines change (416 and 523); production code is not modified, no new helpers are introduced, and no vocabulary translation table is added."
    - "All existing 45+ tests in `test/chimeway/traces_test.exs` still pass after the edit (the change is fixture-string-only and does not affect any projection logic at `lib/chimeway/traces.ex:570-575`)."
  artifacts:
    - path: "test/chimeway/traces_test.exs"
      provides: "Drift-fixed synthetic fixtures aligned with the canonical `chimeway.delivery.{succeeded,bounced,failed}` vocabulary that production code already produces and asserts."
  key_links:
    - from: "test/chimeway/traces_test.exs:416"
      to: "production canonical contract at process_feedback_worker_test.exs:117"
      via: "string equality of `event_name` value"
      pattern: "chimeway.delivery.succeeded"
    - from: "test/chimeway/traces_test.exs:523"
      to: "production canonical contract at process_feedback_worker_test.exs:117"
      via: "string equality of `event_name` value"
      pattern: "chimeway.delivery.succeeded"
---

<objective>
Close the v1.4 milestone audit "Outcome vocabulary drifts across phases" gap by replacing
the two synthetic-fixture occurrences of `chimeway.delivery.delivered` in
`test/chimeway/traces_test.exs` with the canonical `chimeway.delivery.succeeded` string.
This is a mechanical two-line edit; no production code changes, no normalization shims,
no vocabulary translation table (D-02 / D-03).

Purpose: The fixtures at lines 416 and 523 are the only remaining surface in the repo
carrying the drifted event-name string. Production code in `process_feedback_worker.ex:139`
already canonicalizes `"delivered" -> "succeeded"`, the worker tests at
`process_feedback_worker_test.exs:77,117` already assert the canonical string, and the
trace projection at `traces.ex:570-575` dispatches on `transition.reason == "signal_received"`
(NOT on the context's `event_name`). Aligning the fixtures closes the audit-flagged
drift forward without disturbing any contract.

Output: A 2-line edit to `test/chimeway/traces_test.exs`. After the edit:
`grep "chimeway.delivery.delivered" test/` returns 0 matches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-feedback-contract-e2e-proof/34-CONTEXT.md
@.planning/phases/34-feedback-contract-e2e-proof/34-RESEARCH.md
@.planning/phases/34-feedback-contract-e2e-proof/34-PATTERNS.md

<!-- The file being modified -->
@test/chimeway/traces_test.exs

<!-- The canonical-contract source-of-truth (READ-ONLY) -->
@test/chimeway/webhooks/process_feedback_worker_test.exs

<!-- Trace projection that ANCHORS this drift fix as safe (READ-ONLY) -->
@lib/chimeway/traces.ex

<interfaces>
<!-- Why this 2-line edit is safe — verbatim excerpts from the codebase. -->

== The drift sites — `test/chimeway/traces_test.exs:415-416 and 522-523` (BEFORE) ==
```elixir
# line 415-416 (Scenario B fixture, companion to a :succeeded outcome):
insert_workflow_transition!(run, delivery.id, "signal_received",
  %{"event_name" => "chimeway.delivery.delivered"})

# line 522-523 (PII boundary fixture, companion to a :succeeded outcome at line 513):
insert_workflow_transition!(run, delivery.id, "signal_received",
  %{"event_name" => "chimeway.delivery.delivered"})
```

== The canonical contract — `process_feedback_worker_test.exs:117` (READ-ONLY) ==
```elixir
assert hd(signals).event_name == "chimeway.delivery.succeeded"
```

== Trace projection logic that proves the edit is safe — `traces.ex:570-575` ==
```elixir
defp project_workflow_reason("progressed_on_delivery_outcome"), do: :workflow_progressed
defp project_workflow_reason("workflow_waiting"),               do: :workflow_waiting
defp project_workflow_reason("workflow_stopped"),               do: :workflow_stopped
defp project_workflow_reason("workflow_completed"),             do: :workflow_completed
defp project_workflow_reason(_other),                           do: nil
```
The projection dispatches on `transition.reason` (the second `insert_workflow_transition!/3`
arg), NOT on `context["event_name"]` (the third arg's map value). So changing the value
of `context["event_name"]` cannot affect which atoms surface in the timeline.

== `event_name` value is read only by the lookup helper — `traces.ex:608-627` ==
The value is surfaced as `signal_event_name` on `:webhook_received` entries. Existing tests
at `traces_test.exs:347-397` (Scenario A) already use `chimeway.delivery.bounced` (which
is canonical and unchanged). No existing assertion checks
`signal_event_name == "chimeway.delivery.delivered"` anywhere in the test file
(verifiable: `grep -n "delivered" test/chimeway/traces_test.exs` after the edit returns
0 lines).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace `chimeway.delivery.delivered` with `chimeway.delivery.succeeded` at traces_test.exs:416</name>

  <files>test/chimeway/traces_test.exs</files>

  <read_first>
    - test/chimeway/traces_test.exs lines 380-440 (current state of the Scenario B fixture; line 416 is inside an `:succeeded` outcome scenario per the companion attempt at lines 405-411 — the canonical companion event_name is `chimeway.delivery.succeeded`)
    - .planning/phases/34-feedback-contract-e2e-proof/34-PATTERNS.md "Drift fix — line 416 context" lines 129-135 (paste-ready BEFORE/AFTER)
    - .planning/phases/34-feedback-contract-e2e-proof/34-RESEARCH.md "Synthetic fixture drift fix" lines 906-933 (paste-ready BEFORE/AFTER for both lines)
    - test/chimeway/webhooks/process_feedback_worker_test.exs lines 70-120 (read-only — canonical string `chimeway.delivery.succeeded` at line 117 is the contract this fixture aligns to)
  </read_first>

  <action>
    Edit `test/chimeway/traces_test.exs` line 416. The current text is:
    ```elixir
    insert_workflow_transition!(run, delivery.id, "signal_received",
      %{"event_name" => "chimeway.delivery.delivered"})
    ```
    Change ONLY the value of the `"event_name"` key from `"chimeway.delivery.delivered"`
    to `"chimeway.delivery.succeeded"`. Use the Edit tool with `old_string` set to the
    full two-line block to disambiguate from line 523 (which has identical surrounding
    context).

    Result:
    ```elixir
    insert_workflow_transition!(run, delivery.id, "signal_received",
      %{"event_name" => "chimeway.delivery.succeeded"})
    ```

    Do NOT modify any other line, any production file, or any helper function. Do NOT
    add comments, docstrings, or rationale notes inline (the rationale lives in
    `34-VERIFICATION.md` per Plan 34-03).
  </action>

  <acceptance_criteria>
    - The edit landed at line 416: `sed -n '416p' test/chimeway/traces_test.exs` shows `%{"event_name" => "chimeway.delivery.succeeded"})`.
    - Line 523 still contains the OLD string at this checkpoint (Task 2 fixes it; this checkpoint isolates Task 1's blast radius): `sed -n '523p' test/chimeway/traces_test.exs` still shows `%{"event_name" => "chimeway.delivery.delivered"})`.
    - The full file still parses: `mix compile --warnings-as-errors` exits 0 (no module-level syntax errors).
    - Only one line changed by Task 1: `git diff --stat test/chimeway/traces_test.exs` shows one file changed with one insertion and one deletion (or one modification).
    - No other files in the repo were touched: `git diff --name-only | grep -v -E "^(test/chimeway/traces_test\.exs|\.planning/)"` returns no lines.
  </acceptance_criteria>

  <verify>
    <automated>mix test test/chimeway/traces_test.exs</automated>
  </verify>

  <done>
    Line 416 now uses the canonical `chimeway.delivery.succeeded` string. Existing tests
    in `traces_test.exs` still pass (the projection logic at `traces.ex:570-575`
    dispatches on `reason`, not on `event_name`, so the value change does not affect
    any timeline assertion).
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace `chimeway.delivery.delivered` with `chimeway.delivery.succeeded` at traces_test.exs:523</name>

  <files>test/chimeway/traces_test.exs</files>

  <read_first>
    - test/chimeway/traces_test.exs lines 480-560 (current state of the PII boundary fixture; the companion attempt at line 513 has `outcome: :succeeded`, so the canonical companion event_name is `chimeway.delivery.succeeded`)
    - .planning/phases/34-feedback-contract-e2e-proof/34-PATTERNS.md "Drift fix — line 523 context" lines 137-143 (confirms `:succeeded` companion outcome; no `.bounced` substitution needed)
    - .planning/phases/34-feedback-contract-e2e-proof/34-RESEARCH.md "Pitfall 6: Fixture-drift fix breaks the PII boundary test" lines 659-685 (confirms the edit is mechanical and the projection at `traces.ex:570-575` dispatches on reason string, not event_name value)
  </read_first>

  <action>
    Edit `test/chimeway/traces_test.exs` line 523 (the second occurrence of the drifted
    string, located in the PII boundary describe block). The current text is:
    ```elixir
    insert_workflow_transition!(run, delivery.id, "signal_received",
      %{"event_name" => "chimeway.delivery.delivered"})
    ```
    Change ONLY the value of the `"event_name"` key to `"chimeway.delivery.succeeded"`.
    Result:
    ```elixir
    insert_workflow_transition!(run, delivery.id, "signal_received",
      %{"event_name" => "chimeway.delivery.succeeded"})
    ```

    The PII boundary test (the for-comprehension at lines 540-558 per 34-PATTERNS.md)
    asserts that every event atom appears in the timeline AND no entry's `:detail`
    contains forbidden keys. This edit does not change which atoms project (the
    projection is reason-driven at `traces.ex:570-575`), so the PII assertions still
    hold byte-for-byte.

    Do NOT modify any other line, any production file, or any helper function. Do NOT
    add comments, docstrings, or rationale notes inline.
  </action>

  <acceptance_criteria>
    - The edit landed at line 523: `sed -n '523p' test/chimeway/traces_test.exs` shows `%{"event_name" => "chimeway.delivery.succeeded"})`.
    - Drift is fully eliminated from the test directory: `grep -rn "chimeway.delivery.delivered" test/ 2>/dev/null | grep -v '^Binary file' | wc -l | tr -d ' '` returns 0.
    - Drift is also gone from the entire codebase (sanity grep): `grep -rn "chimeway.delivery.delivered" lib/ test/ examples/ 2>/dev/null | grep -v '^Binary file' | grep -v '^#' | wc -l | tr -d ' '` returns 0.
    - Full traces test file passes: `mix test test/chimeway/traces_test.exs` exits 0 with all 45+ tests green, 0 failures.
    - Full chimeway core test suite passes (no incidental regression): `mix test` exits 0 with 548 tests, 0 failures.
    - Only `test/chimeway/traces_test.exs` modified by this plan: `git diff --name-only -- lib/ examples/` returns no lines.
  </acceptance_criteria>

  <verify>
    <automated>mix test test/chimeway/traces_test.exs && grep -rn "chimeway.delivery.delivered" test/ examples/ lib/ 2>/dev/null | grep -v '^Binary file' | grep -v '^#' | wc -l | xargs -I {} test {} -eq 0</automated>
  </verify>

  <done>
    Both synthetic fixtures align with the canonical contract. `grep "chimeway.delivery.delivered" lib/ test/ examples/` returns 0 lines. All 45+ tests in `traces_test.exs` pass. Full root suite (548 tests) green. Only `test/chimeway/traces_test.exs` was modified by this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Test-fixture-only edit; the file does not cross any trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | N/A | Pure documentation-shaped fixture-string substitution; no new attack surface, no new auth path, no new data ingress. The edit changes a value inside a `%{"event_name" => ...}` map used only in synthetic test setup; production code is not modified. |
</threat_model>

<verification>
- Both lines edited: `grep -c "chimeway.delivery.succeeded" test/chimeway/traces_test.exs` increased by 2 (or more, if pre-existing canonical occurrences exist; the delta is at minimum +2).
- No drift remaining anywhere: `grep -rn "chimeway.delivery.delivered" lib/ test/ examples/ 2>/dev/null | grep -v '^Binary file' | grep -v '^#' | wc -l | tr -d ' '` returns 0.
- Full test file passes: `mix test test/chimeway/traces_test.exs` exits 0 with 45+ tests green.
- Full root suite passes: `mix test` exits 0 with 548 tests green.
- Only one file modified by this plan: `git diff --name-only` shows exactly `test/chimeway/traces_test.exs`.
</verification>

<success_criteria>
- Lines 416 and 523 of `test/chimeway/traces_test.exs` both contain `chimeway.delivery.succeeded`.
- The codebase contains zero occurrences of the drifted string `chimeway.delivery.delivered`.
- `mix test test/chimeway/traces_test.exs` exits 0.
- `mix test` (root suite) exits 0.
- This plan's SUMMARY frontmatter declares `requirements-completed: [FLOW-01]` (D-11 / 33-04-SUMMARY:75 pattern; FLOW-01 is the vocabulary alignment requirement).
</success_criteria>

<output>
After completion, create `.planning/phases/34-feedback-contract-e2e-proof/34-02-SUMMARY.md` with:
- Frontmatter `requirements-completed: [FLOW-01]` (D-11 / 33-04-SUMMARY:75 pattern).
- Frontmatter `tags: [elixir, test-fixture, vocabulary, audit-closure]`.
- Brief notes confirming both lines (416, 523) were updated and that the codebase no longer contains the drifted string anywhere.
</output>