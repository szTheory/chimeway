---
phase: 17-delivery-windows-deferral-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - 17-02
files_modified:
  - lib/chimeway/dispatch/sync.ex
  - lib/chimeway/dispatch/oban.ex
  - lib/chimeway/dispatch/oban_worker.ex
  - lib/chimeway/traces.ex
  - lib/chimeway/traces/explanation.ex
  - test/chimeway/orchestration/dispatch_gating_test.exs
  - test/chimeway/orchestration/traces_deferral_test.exs
  - test/chimeway/integration/delivery_lifecycle_test.exs
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
must_haves:
  truths:
    - "Deferred and digest-held rows remain non-dispatchable in both Sync and Oban paths until a later phase adds scheduled resume behavior."
    - "Explainability surfaces show why a delivery was deferred, which rule and timezone produced the decision, and when it next becomes eligible."
    - "Held rows stay visible as planned-but-not-dispatched lifecycle records with zero attempts in Phase 17."
  artifacts:
    - path: "lib/chimeway/dispatch/sync.ex"
      provides: "Immediate dispatch gate on `orchestration_state == :ready`"
      min_lines: 20
    - path: "lib/chimeway/dispatch/oban.ex"
      provides: "Oban enqueue gate that refuses deferred and digest-held rows"
      min_lines: 20
    - path: "lib/chimeway/traces.ex"
      provides: "Deferred timeline shaping sourced from persisted orchestration facts"
      min_lines: 30
    - path: "test/chimeway/integration/delivery_lifecycle_test.exs"
      provides: "End-to-end proof that held rows keep zero attempts and stay pending in Phase 17"
  key_links:
    - from: "lib/chimeway/dispatch/sync.ex"
      to: "lib/chimeway/dispatch/oban.ex"
      via: "shared gating on ready-for-dispatch orchestration state"
      pattern: "orchestration_state == :ready"
    - from: "lib/chimeway/dispatch/oban_worker.ex"
      to: "lib/chimeway/delivery_planning.ex"
      via: "defensive perform-time guard honors planning state even if a held row is manually enqueued"
      pattern: "digest_held|deferred|ready"
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/traces/explanation.ex"
      via: "explanation struct exposes deferred reason, timezone context, normalized rule identity, and next eligible time"
      pattern: "deferred|planning_reason|planning_context|next_eligible_at"
---

<objective>
Implement Phase 17 execution gating and explainability on top of the declaration and planning semantics delivered in Plan 17-02.

Purpose: Chimeway must keep held rows out of immediate execution and give operators a durable explanation for "not now" decisions, without pulling forward Phase 18 resume scheduling.
Output: Sync/Oban gating, defensive worker guards, deferred trace/explanation expansion, and lifecycle tests proving held rows stay attempt-free.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-delivery-windows-deferral-semantics/17-RESEARCH.md
@.planning/phases/17-delivery-windows-deferral-semantics/17-PATTERNS.md
@.planning/phases/17-delivery-windows-deferral-semantics/17-02-PLAN.md
@lib/chimeway/dispatch/sync.ex
@lib/chimeway/dispatch/oban.ex
@lib/chimeway/dispatch/oban_worker.ex
@lib/chimeway/traces.ex
@lib/chimeway/traces/explanation.ex

<interfaces>
From lib/chimeway/dispatch/sync.ex:
```elixir
@behaviour Chimeway.Dispatch
```

From lib/chimeway/dispatch/oban.ex:
```elixir
@behaviour Chimeway.Dispatch
```

From lib/chimeway/traces.ex:
```elixir
@spec explain_delivery(String.t(), keyword()) :: {:ok, Explanation.t()} | {:error, :not_found}
def explain_delivery(delivery_id, opts \\ []) do
  ...
end
```

From lib/chimeway/traces/explanation.ex:
```elixir
defstruct [
  :delivery_id,
  :event_id,
  :correlation_id,
  :notification_key,
  :recipient_id,
  :channel,
  :status,
  :suppression_reason,
  :last_attempt,
  :timeline
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate Sync and Oban execution on orchestration readiness</name>
  <files>lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban.ex, lib/chimeway/dispatch/oban_worker.ex, test/chimeway/orchestration/dispatch_gating_test.exs, test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <behavior>
    - Test 1: Sync dispatches only deliveries whose `orchestration_state` is `:ready`.
    - Test 2: Oban enqueue and worker perform paths short-circuit deferred and digest-held deliveries even if a row is manually queued.
    - Test 3: integration lifecycle coverage proves deferred and digest-held rows remain pending with zero attempts in Phase 17.
  </behavior>
  <action>Gate both dispatchers on orchestration readiness and add a defensive `ObanWorker` guard so a manually-enqueued deferred or digest-held row still short-circuits rather than dispatching. Keep this task limited to execution-path enforcement and lifecycle assertions; do not add resume scheduling, `scheduled_at` mutation, or any other Phase 18 behavior.</action>
  <verify>
    <automated>mix test test/chimeway/orchestration/dispatch_gating_test.exs test/chimeway/integration/delivery_lifecycle_test.exs -x</automated>
  </verify>
  <done>Deferred and digest-held rows cannot leak into immediate Sync or Oban execution paths, and lifecycle tests prove held rows accumulate zero attempts in Phase 17.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand deferred explainability surfaces</name>
  <files>lib/chimeway/traces.ex, lib/chimeway/traces/explanation.ex, test/chimeway/orchestration/traces_deferral_test.exs, test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <behavior>
    - Test 1: `Traces.explain_delivery/2` returns a deferred timeline entry containing planning reason, timezone, normalized rule identity, and next eligible time.
    - Test 2: explainability keeps deferral separate from suppression by leaving `suppression_reason` unset for deferred rows.
    - Test 3: end-to-end lifecycle tests show a deferred or digest-held delivery as planned-but-not-dispatched with no attempt history yet.
  </behavior>
  <action>Extend the explanation contract and timeline builder so operators can answer why a delivery was deferred, which timezone and normalized rule produced that decision, and when the row becomes eligible again. Add an explicit deferred timeline event sourced from persisted delivery fields rather than inferred scheduler state, and preserve current suppression/cancellation semantics unchanged. Keep the explanation payload safe: include rule identity and scheduling facts, but do not copy raw event payload or provider response data into `planning_context` or trace detail.</action>
  <verify>
    <automated>mix test test/chimeway/orchestration/traces_deferral_test.exs test/chimeway/integration/delivery_lifecycle_test.exs -x</automated>
  </verify>
  <done>Operators can inspect deferred deliveries directly from Chimeway trace surfaces and see deferred facts without conflating them with suppressed outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted delivery state -> dispatchers/workers | Dispatch code trusts durable orchestration fields to decide whether to execute or hold a delivery |
| persisted delivery state -> trace explanation | Operator surfaces expose planning facts and must avoid leaking sensitive payload data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | D | `lib/chimeway/dispatch/sync.ex`, `lib/chimeway/dispatch/oban.ex`, `lib/chimeway/dispatch/oban_worker.ex` | mitigate | Require `orchestration_state == :ready` before dispatch, enqueue, or perform so deferred and digest-held rows cannot leak into immediate execution. |
| T-17-08 | I | `lib/chimeway/traces.ex` | mitigate | Emit deferred explanation details from persisted rule/timezone facts only, leaving `suppression_reason` for suppressed or cancelled outcomes and excluding raw payload/provider-response fields from trace output. |
| T-17-09 | R | `test/chimeway/integration/delivery_lifecycle_test.exs` | mitigate | Assert zero attempts and stable pending state for held rows so later regressions cannot silently erase the evidence that a row was intentionally held. |
</threat_model>

<verification>
Run `mix test test/chimeway/orchestration/dispatch_gating_test.exs test/chimeway/orchestration/traces_deferral_test.exs test/chimeway/integration/delivery_lifecycle_test.exs -x`.
</verification>

<success_criteria>
Sync and Oban paths refuse to execute held rows, `Traces.explain_delivery/2` exposes durable deferral facts, and Phase 17 ends with visible planned-but-not-dispatched deliveries rather than scheduler-driven resume behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/17-delivery-windows-deferral-semantics/17-03-SUMMARY.md`
</output>
