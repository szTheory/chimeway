---
phase: 17-delivery-windows-deferral-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - lib/chimeway/notifier.ex
  - lib/chimeway/deliveries.ex
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/policy.ex
  - lib/chimeway/policy/settings.ex
  - test/chimeway/notifier_contract_test.exs
  - test/chimeway/policy_settings_test.exs
  - test/chimeway/policy_test.exs
  - test/chimeway/orchestration/delivery_planning_test.exs
  - test/chimeway/orchestration/planning_declarations_test.exs
autonomous: true
requirements:
  - ORCH-01
  - ORCH-02
must_haves:
  truths:
    - "Product teams can declare immediate or digest-held participation through an explicit notifier/planner seam, and that declaration persists on the canonical delivery row."
    - "Planning can mark each delivery as ready, deferred, or digest-held while keeping one durable row per (notification_id, channel)."
    - "Quiet-hours decisions defer rather than suppress, and deferred rows persist rule, timezone, and next-eligible facts without introducing Phase 18 scheduling behavior."
  artifacts:
    - path: "lib/chimeway/notifier.ex"
      provides: "Optional notifier declaration contract for orchestration participation"
      min_lines: 15
    - path: "lib/chimeway/delivery_planning.ex"
      provides: "Planner seam that resolves declaration-driven ready/deferred/digest-held outcomes on the existing delivery row"
      min_lines: 40
    - path: "lib/chimeway/policy.ex"
      provides: "Planning policy contract that distinguishes suppression from orchestration declarations"
      min_lines: 40
    - path: "test/chimeway/orchestration/planning_declarations_test.exs"
      provides: "Automated proof that a declared digest rule persists `orchestration_state: :digest_held` on the canonical delivery row"
  key_links:
    - from: "lib/chimeway/notifier.ex"
      to: "lib/chimeway/delivery_planning.ex"
      via: "optional notifier callback or planner option resolves declared orchestration participation per ORCH-01"
      pattern: "orchestration|digest"
    - from: "lib/chimeway/delivery_planning.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "idempotent row creation followed by orchestration-state update on the same delivery row"
      pattern: "plan_delivery|orchestration_state"
    - from: "lib/chimeway/policy.ex"
      to: "lib/chimeway/policy/settings.ex"
      via: "planning-time policy preserves suppressive outcomes for true blocks while converting quiet-hours into deferral facts"
      pattern: "defer|quiet_hours|next_eligible_at"
---

<objective>
Implement the Phase 17 declaration seam and planning semantics on top of the durable orchestration fields introduced in Plan 17-01.

Purpose: ORCH-01 is not satisfied until product teams have an explicit path to declare digest participation or immediate send behavior. Phase 17 also needs planning-time quiet-hours deferral facts without pulling Phase 18 scheduling or Phase 19 digest accumulation forward.
Output: Notifier/planner declaration contract updates, planning policy return-shape updates, canonical delivery-row orchestration writes, and test coverage proving declared digest participation persists `:digest_held`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-delivery-windows-deferral-semantics/17-RESEARCH.md
@.planning/phases/17-delivery-windows-deferral-semantics/17-PATTERNS.md
@lib/chimeway/notifier.ex
@lib/chimeway/delivery_planning.ex
@lib/chimeway/deliveries.ex
@lib/chimeway/policy.ex
@lib/chimeway/policy/settings.ex

<interfaces>
From lib/chimeway/notifier.ex:
```elixir
@callback notification_key() :: String.t()
@callback version() :: pos_integer()
@callback recipients(map()) :: {:ok, [map()]} | {:error, term()}
@callback build(map(), map()) :: {:ok, map()} | {:error, term()}
@callback channels(map(), map()) :: {:ok, [atom() | String.t()]} | {:error, term()}
@callback delayed_fallback_channels(map(), map()) ::
            {:ok, [atom() | String.t()]} | {:error, term()}

@optional_callbacks channels: 2
@optional_callbacks delayed_fallback_channels: 2
```

From lib/chimeway/delivery_planning.ex:
```elixir
@spec plan_notification(Notification.t(), keyword()) :: {:ok, [Delivery.t()]} | {:error, term()}
def plan_notification(%Notification{} = notification, opts \\ []) do
  ...
end
```

From lib/chimeway/policy.ex:
```elixir
@spec evaluate(Chimeway.Delivery.t(), keyword()) :: {:ok, :proceed} | {:suppress, atom()}
def evaluate(%Delivery{} = delivery, opts \\ []) do
  ...
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the product-team declaration seam for orchestration participation</name>
  <files>lib/chimeway/notifier.ex, lib/chimeway/deliveries.ex, lib/chimeway/delivery_planning.ex, test/chimeway/notifier_contract_test.exs, test/chimeway/orchestration/planning_declarations_test.exs, test/chimeway/orchestration/delivery_planning_test.exs</files>
  <behavior>
    - Test 1: notifier contract coverage proves product teams can declare orchestration participation through an explicit callback or planner seam without breaking existing notifiers.
    - Test 2: a declared digest rule produces `orchestration_state: :digest_held` on the existing `(notification_id, channel)` delivery row instead of creating a new digest table row.
    - Test 3: repeated planning of the same notification/channel keeps one canonical delivery row while preserving the declared orchestration state.
  </behavior>
  <action>Add an explicit declaration seam for ORCH-01 by extending `Chimeway.Notifier` with an optional orchestration callback or equivalent planner contract that product teams can use to declare immediate-send or digest participation per D-17 scope. Implement the resolver in `DeliveryPlanning` so notifier declaration flows through the planner and persists `orchestration_state: :digest_held` on the canonical delivery row via explicit `Deliveries` helpers, with no Phase 19 accumulation/emission work and no secondary planning table. Keep backward compatibility for existing notifiers that do not implement the new callback by defaulting to immediate planning.</action>
  <verify>
    <automated>mix test test/chimeway/notifier_contract_test.exs test/chimeway/orchestration/planning_declarations_test.exs test/chimeway/orchestration/delivery_planning_test.exs -x</automated>
  </verify>
  <done>Product teams have a concrete declaration path for digest participation, declared digest participation persists `:digest_held` on the existing delivery row, and idempotent planning keeps one row per `(notification_id, channel)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert planning-time quiet-hours semantics from suppression to deferral</name>
  <files>lib/chimeway/deliveries.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/policy.ex, lib/chimeway/policy/settings.ex, test/chimeway/policy_settings_test.exs, test/chimeway/policy_test.exs, test/chimeway/orchestration/delivery_planning_test.exs</files>
  <behavior>
    - Test 1: quiet-hours planning returns a non-suppressive deferred decision with `planning_reason`, `planning_context`, and `next_eligible_at`.
    - Test 2: existing `policy_settings_test.exs` and `policy_test.exs` assertions that previously expected `{:suppress, :quiet_hours}` now expect the deferred contract while delivery-cap behavior remains suppressive.
    - Test 3: planning persists `:ready`, `:deferred`, or `:digest_held` on the existing delivery row without creating duplicates for the same `(notification_id, channel)`.
  </behavior>
  <action>Introduce a planning-decision layer that preserves suppressive outcomes for true blocks (channel/category/read-state/cap) but converts quiet-hours outcomes into durable deferrals per ORCH-01/ORCH-02. Update `Deliveries` and `DeliveryPlanning` so deferred facts are written back onto the canonical row after idempotent insert, and revise `policy_settings_test.exs` plus `policy_test.exs` so the checked-in contract changes from quiet-hours suppression to quiet-hours deferral. Keep Phase 17 limited to recipient-timezone-aware quiet-hours deferral; do not enqueue resume jobs, mutate `scheduled_at`, implement general allowed-window configuration, or create digest accumulation rows here.</action>
  <verify>
    <automated>mix test test/chimeway/policy_settings_test.exs test/chimeway/policy_test.exs test/chimeway/orchestration/delivery_planning_test.exs -x</automated>
  </verify>
  <done>Phase 17 planning distinguishes deferred from suppressed at the policy/planning layer, quiet-hours now persist deferred facts instead of terminal suppression, and canonical delivery rows carry the new orchestration state without scheduler behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notifier/policy inputs -> delivery planning | Host-supplied notifier declarations and recipient settings influence whether work is immediate, deferred, or digest-held |
| persisted delivery state -> future dispatchers | Phase 17 writes canonical orchestration facts that later execution phases will trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | T | `lib/chimeway/notifier.ex` + `lib/chimeway/delivery_planning.ex` | mitigate | Normalize notifier/planner declarations to a small explicit orchestration contract so product-team input cannot inject arbitrary persisted planning state. |
| T-17-05 | T | `lib/chimeway/delivery_planning.ex` + `lib/chimeway/deliveries.ex` | mitigate | Persist planning facts only through explicit helpers on the canonical delivery row so idempotent re-entry cannot fork parallel state or bypass the `(notification_id, channel)` uniqueness boundary. |
| T-17-06 | I | `lib/chimeway/policy/settings.ex` | mitigate | Persist only rule identity, timezone, and next-eligible facts for deferrals, excluding notification payload content and keeping digest participation as state-only with no Phase 19 accumulation data. |
</threat_model>

<verification>
Run `mix test test/chimeway/notifier_contract_test.exs test/chimeway/policy_settings_test.exs test/chimeway/policy_test.exs test/chimeway/orchestration/planning_declarations_test.exs test/chimeway/orchestration/delivery_planning_test.exs -x`.
</verification>

<success_criteria>
Phase 17 exposes a concrete declaration seam for immediate-vs-digest-held participation, persists those declared outcomes on the canonical delivery row, and converts quiet-hours from suppression to durable deferral without introducing Phase 18 scheduling or Phase 19 digest accumulation.
</success_criteria>

<output>
After completion, create `.planning/phases/17-delivery-windows-deferral-semantics/17-02-SUMMARY.md`
</output>
