---
phase: 16-integration-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: ["guides/recipes/custom-adapter.md"]
autonomous: true
requirements: ["INT-01", "INT-02"]

must_haves:
  truths:
    - "Developers have a dedicated guide to author custom adapters securely."
  artifacts:
    - path: "guides/recipes/custom-adapter.md"
      provides: "Custom adapter documentation"
      min_lines: 30
  key_links: []
---

<objective>
Expand the custom adapter recipe to fully document the `Chimeway.Adapter` behaviour, secure runtime configuration, and contract test macro.

Purpose: Provide a production-grade guide for writing adapters that ensures runtime configuration safety and prevents credential leaks (INT-02).
Output: Expanded `custom-adapter.md` recipe.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-integration-hardening/16-CONTEXT.md
@.planning/phases/16-integration-hardening/16-DISCUSSION-LOG.md
@guides/recipes/custom-adapter.md
@lib/chimeway/adapter.ex
@test/support/chimeway/adapter/contract_test.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Custom Adapter Recipe</name>
  <files>guides/recipes/custom-adapter.md</files>
  <action>Replace the stub content in `custom-adapter.md`. Guide the user through implementing the `Chimeway.Adapter` behaviour. Critically explain that configuration MUST be loaded at runtime via `Application.get_env/3` rather than module attributes to support runtime environment switching, explicitly referencing how this ensures multi-environment safety. Explain the required `{:ok, meta}` and `{:error, class, detail}` return shapes. Finally, explicitly document how to use the `Chimeway.Adapter.ContractTest` macro in their test suite to enforce sensitive data redaction on returned metadata (addressing INT-02 runtime safety). Do NOT create any new test macros for dispatchers; rely entirely on documenting the existing adapter macro.</action>
  <verify>
    <automated>grep -q "Chimeway.Adapter.ContractTest" guides/recipes/custom-adapter.md</automated>
  </verify>
  <done>Recipe comprehensively explains behaviour implementation, secure configuration loading, and contract testing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer -> Custom Adapter | Developers implement outbound integrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Information Disclosure | Custom Adapter | mitigate | Document the use of `Chimeway.Adapter.ContractTest` which enforces redaction of credentials in metadata |
| T-16-04 | Tampering | Custom Adapter | mitigate | Explicitly guide developers to use runtime configuration `Application.get_env/3` rather than compile-time attributes |
</threat_model>

<verification>
- Guide thoroughly details `Chimeway.Adapter.ContractTest` usage
- Guide specifically warns against compile-time configuration
</verification>

<success_criteria>
- Custom adapters authored per the guide will pass automated redaction checks and handle runtime config correctly
</success_criteria>

<output>
After completion, create `.planning/phases/16-integration-hardening/16-03-SUMMARY.md`
</output>
