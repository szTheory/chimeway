---
phase: 85-repo-integration-readme-hexdocs-favicon-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - mix.exs
autonomous: false
requirements: [INTEG-01, INTEG-02, INTEG-04]
must_haves:
  truths:
    - "README header renders the primary lockup via a GitHub-native <picture> element: a <source media=\"(prefers-color-scheme: dark)\"> pointing at brandbook/assets/logo/chimeway-logotype-inverse.svg and a default <img src=\"brandbook/assets/logo/chimeway-logotype.svg\" alt=\"Chimeway\">, replacing the plain # Chimeway text heading (INTEG-01, D-01)."
    - "mix.exs docs() sets logo: \"brandbook/assets/logo/chimeway-mark.svg\" and favicon: \"brandbook/assets/favicon/favicon.svg\" (INTEG-02, D-02)."
    - "A local `mix docs` build succeeds and copies the mark + favicon into the ExDoc output assets/ so the mark renders in the sidebar and the favicon in the browser tab (INTEG-02, D-06)."
    - "package() files: list stays ~w(lib priv guides CHANGELOG.md LICENSE.md README.md mix.exs .formatter.exs) — no brandbook/assets added (INTEG-04, D-04)."
    - "The README numbered snippet blocks (1. Define a notifier … 4. Explain the delivery) and the badges/tagline below the header are untouched (INTEG-04, D-05)."
    - "mix test test/chimeway/doc_contract_test.exs test/chimeway/release_gate_contract_test.exs test/chimeway/integration/readme_snippet_test.exs exits 0 (INTEG-04, D-06)."
  artifacts:
    - "README.md (modified header region, lines 1–9; header lockup markup net-added)"
    - "mix.exs (modified docs() function; logo:/favicon: keys added)"
  key_links:
    - "README <picture>/<img> src/srcset → relative brandbook/assets/logo/*.svg paths GitHub resolves natively (no remote URLs)."
    - "mix.exs docs() :logo/:favicon → brandbook/assets/*.svg files ExDoc copies into HexDocs output at build time (no :assets map, D-03)."
  prohibitions:
    - "Do NOT modify the README's numbered snippet blocks (1. Define a notifier … 4. Explain the delivery) — guarded by test/chimeway/integration/readme_snippet_test.exs (D-05)."
    - "Do NOT add brandbook/assets to package() files: (D-04)."
    - "Do NOT touch chimeway_admin (D-05)."
    - "Do NOT bump ex_doc (leave ~> 0.31; 0.40.1 in mix.lock already supports SVG logo/favicon) and do NOT add an :assets map (D-03)."
    - "Do NOT reference remote URLs for any asset — all asset paths are relative in-repo paths under brandbook/assets/ (T-85-01)."
---

<objective>
Wire the already-shipped Chimeway brand assets into the two surfaces adopters see: the GitHub README header and HexDocs. Two cohesive edits — replace the README's plain text heading with a theme-swapping `<picture>` lockup, and add `:logo`/`:favicon` to `mix.exs` `docs()` — followed by a verification gate that keeps every v1.14 contract green.

Purpose: Make the real brand mark visible on the repo front door (README) and the docs sidebar/tab (HexDocs), the last integration step of the v1.15 brand-book milestone.
Output: Modified README.md header region and mix.exs docs(); no new files, no new code symbols.
</objective>

<artifacts_this_phase_produces>
This phase produces NO new files and NO new code symbols. It modifies two existing files:
- `README.md` — header region (lines 1–9): the plain `# Chimeway` text heading is replaced with `<picture>`/`<img>` lockup markup (net-add of markup, no new file).
- `mix.exs` — `docs()` function (~line 225): two new keyword entries `logo:` and `favicon:` are added to the existing keyword list.

No new modules, functions, structs, configs, migrations, or assets are created. Drift verification should expect zero new symbols. The referenced SVG assets already exist on disk (shipped in Phase 83): brandbook/assets/logo/chimeway-logotype.svg, brandbook/assets/logo/chimeway-logotype-inverse.svg, brandbook/assets/logo/chimeway-mark.svg, brandbook/assets/favicon/favicon.svg.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-repo-integration-readme-hexdocs-favicon-wiring/85-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace README text heading with theme-swapping brand lockup (INTEG-01, D-01)</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 1–10 — current state: plain `# Chimeway` text heading on line 1, tagline paragraph on lines 3–6, Hex.pm + CI badges on lines 8–9. This header region (above the badges) is the ONLY in-scope edit target.
    - .planning/phases/85-repo-integration-readme-hexdocs-favicon-wiring/85-CONTEXT.md — decision D-01 (picture theme-swap over single img), D-05 (scope confined to header region; numbered snippet blocks OFF-LIMITS).
    - brandbook/assets/logo/chimeway-logotype.svg — light-mode primary lockup (dark ink #102027 wordmark, teal #0e7c86; native aspect 381.7×90.4), used as the default <img>.
    - brandbook/assets/logo/chimeway-logotype-inverse.svg — dark-mode lockup (paper #fffdf8 wordmark), used as the dark <source>.
  </read_first>
  <action>
    Replace the line-1 plain `# Chimeway` text heading with a GitHub-native `<picture>` element that theme-swaps the brand lockup. The `<picture>` MUST contain: a `<source>` with a `media` attribute selecting the dark colour scheme whose `srcset` is the relative path `brandbook/assets/logo/chimeway-logotype-inverse.svg`, and a default `<img>` whose `src` is the relative path `brandbook/assets/logo/chimeway-logotype.svg`, carrying an alt attribute equal to `Chimeway` and a `width` of `380` (tunable within a sane header range per D-01). All asset references MUST be relative in-repo paths under `brandbook/assets/` — never remote URLs (T-85-01). Per Claude's Discretion in D-01 you MAY wrap the `<picture>` in an `<a href>` linking to the repo and/or an `<h1>` for an accessible heading, provided the alt text remains `Chimeway`. Leave the tagline paragraph and the Hex.pm/CI badges below the header exactly as they are. Do NOT touch anything at or below the `## When to use` section — the numbered snippet blocks are the runtime contract asserted by readme_snippet_test (D-05). You MAY add a short HTML comment marking the header region edit boundary (D-01 discretion).
  </action>
  <acceptance_criteria>
    - README.md no longer contains the literal line `# Chimeway` as a standalone Markdown H1 text heading (the lockup replaces it).
    - README.md contains a `<picture>` element in the header region (before the Hex.pm badge line).
    - README.md contains a `<source>` with `media="(prefers-color-scheme: dark)"` whose srcset references `brandbook/assets/logo/chimeway-logotype-inverse.svg`.
    - README.md contains an `<img>` whose src references `brandbook/assets/logo/chimeway-logotype.svg` and whose alt attribute value is exactly `Chimeway`.
    - grep confirms no `http://` or `https://` scheme appears inside the `src`/`srcset` of the header lockup (all asset paths relative).
    - The badges `[![Hex.pm]...` and `[![CI]...` and the tagline paragraph remain present and unchanged.
    - The `## When to use` heading and all numbered snippet blocks below it are byte-for-byte unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'prefers-color-scheme: dark' README.md && grep -q 'brandbook/assets/logo/chimeway-logotype-inverse.svg' README.md && grep -q 'brandbook/assets/logo/chimeway-logotype.svg' README.md && grep -q 'alt="Chimeway"' README.md && ! grep -nE 'src(set)?="https?://' README.md && echo LOCKUP_OK</automated>
  </verify>
  <reversibility rating="reversible">README header markup edit; trivially revertible via git.</reversibility>
  <done>README header shows the theme-swapping `<picture>` lockup (light default + dark source) via relative brandbook/assets paths with alt="Chimeway"; badges, tagline, and numbered snippet blocks untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ExDoc :logo and :favicon into mix.exs docs() (INTEG-02, D-02, D-03)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs lines 225–259 — the `docs/0` function returning a keyword list (main:, source_ref:, source_url:, extras:, groups_extras:). The list closes at line 258 (`]`) inside `end` on line 259. This is the ONLY in-scope edit target in mix.exs.
    - mix.exs lines 217–223 — `package/0` with `files: ~w(lib priv guides CHANGELOG.md LICENSE.md README.md mix.exs .formatter.exs)`. This MUST stay unchanged (D-04).
    - .planning/phases/85-repo-integration-readme-hexdocs-favicon-wiring/85-CONTEXT.md — D-02 (square mark for :logo; fixed-color, not mono), D-03 (no :assets map, no ex_doc bump), D-04 (package files: unchanged).
    - brandbook/assets/logo/chimeway-mark.svg — square 48×48 fixed-color mark for the ExDoc sidebar :logo slot.
    - brandbook/assets/favicon/favicon.svg — the ExDoc :favicon.
  </read_first>
  <action>
    Add two keyword entries to the keyword list returned by `docs/0`: a `logo:` key whose value is the relative string path `brandbook/assets/logo/chimeway-mark.svg`, and a `favicon:` key whose value is the relative string path `brandbook/assets/favicon/favicon.svg`. Both are relative in-repo paths under `brandbook/assets/` — never remote URLs (T-85-01). Place them as sibling entries alongside `main:`/`source_ref:` (before `extras:` reads cleanest, but any position inside the same keyword list is acceptable). Use the square `chimeway-mark.svg` for `logo:` (NOT the wide logotype, NOT the mono variant) per D-02. Do NOT add an `:assets` map — ExDoc copies the `:logo`/`:favicon` files itself (D-03). Do NOT change the `ex_doc` dependency requirement (leave `~> 0.31`; mix.lock's 0.40.1 already supports SVG logo/favicon) (D-03). Do NOT modify `package/0` `files:` (D-04). Do NOT alter `extras:` or `groups_extras:`.
  </action>
  <acceptance_criteria>
    - mix.exs `docs/0` keyword list contains the exact entry `logo: "brandbook/assets/logo/chimeway-mark.svg"`.
    - mix.exs `docs/0` keyword list contains the exact entry `favicon: "brandbook/assets/favicon/favicon.svg"`.
    - mix.exs `package/0` `files:` line still reads `~w(lib priv guides CHANGELOG.md LICENSE.md README.md mix.exs .formatter.exs)` (unchanged).
    - mix.exs contains no `assets:` key inside `docs/0` and no change to the `ex_doc` version requirement.
    - grep confirms no `https?://` scheme in the logo:/favicon: values (relative paths only).
    - `mix compile` succeeds (mix.exs parses).
  </acceptance_criteria>
  <verify>
    <automated>grep -qF 'logo: "brandbook/assets/logo/chimeway-mark.svg"' mix.exs && grep -qF 'favicon: "brandbook/assets/favicon/favicon.svg"' mix.exs && grep -qF 'files: ~w(lib priv guides CHANGELOG.md LICENSE.md README.md mix.exs .formatter.exs)' mix.exs && ! grep -qE 'logo:\s*"https?://' mix.exs && mix compile 2>&1 | tail -1</automated>
  </verify>
  <reversibility rating="reversible">Additive docs() config edit; trivially revertible via git.</reversibility>
  <done>mix.exs docs() carries logo: "brandbook/assets/logo/chimeway-mark.svg" and favicon: "brandbook/assets/favicon/favicon.svg"; package() files: unchanged; no :assets map; ex_doc requirement unchanged; mix compile succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify v1.14 contracts green + HexDocs renders the brand mark (INTEG-04, D-06)</name>
  <read_first>
    - .planning/phases/85-repo-integration-readme-hexdocs-favicon-wiring/85-CONTEXT.md — D-06 (re-run the three contract tests, verify HexDocs render with a local mix docs build).
    - test/chimeway/doc_contract_test.exs, test/chimeway/release_gate_contract_test.exs, test/chimeway/integration/readme_snippet_test.exs — the v1.14 contracts that must stay green.
  </read_first>
  <files>README.md, mix.exs</files>
  <what-built>
    Task 1 replaced the README plain heading with a theme-swapping `<picture>` brand lockup (relative brandbook/assets paths). Task 2 wired `logo:` (square mark) and `favicon:` into mix.exs `docs()`. This checkpoint confirms nothing regressed: the three v1.14 contract tests pass and a local `mix docs` build renders the mark in the ExDoc sidebar and the favicon in the browser tab.
  </what-built>
  <action>
    Run the v1.14 contract suite and a local docs build, then have a human visually confirm the HexDocs render. Execute `mix test test/chimeway/doc_contract_test.exs test/chimeway/release_gate_contract_test.exs test/chimeway/integration/readme_snippet_test.exs` (must exit 0), then `mix docs` (must build doc/ output and copy the mark + favicon into doc/assets/). Present the render for human confirmation of the sidebar mark and browser-tab favicon. Do not modify source in this task; if any test fails or the render is wrong, return to Task 1/Task 2.
  </action>
  <verify>
    <automated>mix test test/chimeway/doc_contract_test.exs test/chimeway/release_gate_contract_test.exs test/chimeway/integration/readme_snippet_test.exs && mix docs && ls doc/assets/ && grep -rl "chimeway-mark" doc/ | head -1 && grep -rl "favicon" doc/ | head -1</automated>
    <human-check>Open doc/index.html: confirm the teal+ink square mark renders in the left sidebar logo slot and the favicon shows in the browser tab.</human-check>
  </verify>
  <done>The three v1.14 contract tests exit 0, `mix docs` builds cleanly and copies the mark + favicon into doc/, and a human confirms the mark renders in the ExDoc sidebar and the favicon in the browser tab.</done>
  <acceptance_criteria>
    - `mix test test/chimeway/doc_contract_test.exs test/chimeway/release_gate_contract_test.exs test/chimeway/integration/readme_snippet_test.exs` exits 0.
    - `mix docs` completes without error and writes doc/ output.
    - The ExDoc output copies the referenced assets: grep of the generated doc/ tree finds the mark and favicon filenames referenced in the HTML/assets.
    - Human confirms: the mark renders in the HexDocs sidebar and the favicon shows in the browser tab.
  </acceptance_criteria>
  <how-to-verify>
    Run the contract suite (must exit 0):
      mix test test/chimeway/doc_contract_test.exs test/chimeway/release_gate_contract_test.exs test/chimeway/integration/readme_snippet_test.exs
    Build docs locally and confirm assets copied (should print filenames):
      mix docs && ls doc/assets/ && grep -rl "chimeway-mark" doc/ | head && grep -rl "favicon" doc/ | head
    Then open doc/index.html in a browser and visually confirm: (1) the teal+ink square mark renders in the left sidebar logo slot, and (2) the favicon appears in the browser tab. Also glance at the GitHub README preview (or `grip` locally) to confirm the lockup renders in both light and dark mode.
  </how-to-verify>
  <resume-signal>Type "approved" if all three tests pass and the mark + favicon render, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → GitHub/HexDocs render surface | README markup and mix.exs docs() config reference local SVG assets that GitHub and ExDoc fetch/copy when rendering. |

This phase adds no runtime code, no user-input handling, and no new network/auth surface. It edits Markdown/build-config and references local SVG files by relative in-repo path. ASVS level: L1. Block-on threshold: high. No high-severity threats exist.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-85-01 | Information Disclosure / Tampering | README `<picture>`/`<img>` src + mix.exs docs() :logo/:favicon | low | mitigate | All asset references MUST be relative in-repo paths under brandbook/assets/ — never remote URLs — so no external content is fetched into the GitHub/HexDocs render surface. Enforced by grep gates in Task 1 and Task 2 verify blocks (`! grep -nE 'src(set)?="https?://'` and `! grep -qE 'logo:\s*"https?://'`). |
| T-85-02 | Tampering | README numbered snippet blocks (runtime contract) | low | mitigate | Scope discipline (D-05): header edit stays above the badges; readme_snippet_test.exs re-run in Task 3 asserts the documented notifier→trace chain still compiles. |
</threat_model>

<verification>
Phase-level checks (all run in Task 3):
- The three v1.14 contract tests pass: `mix test test/chimeway/doc_contract_test.exs test/chimeway/release_gate_contract_test.exs test/chimeway/integration/readme_snippet_test.exs` exits 0.
- `mix docs` builds cleanly and copies the mark + favicon into doc/ output.
- README lockup markup uses relative brandbook/assets paths with alt="Chimeway"; no remote URLs.
- package() files: list unchanged; no :assets map; ex_doc requirement unchanged; chimeway_admin untouched.
</verification>

<success_criteria>
- INTEG-01: README header renders the primary lockup via a GitHub-native theme-swapping `<picture>` (relative brandbook/assets SVGs, alt="Chimeway"), replacing the plain text heading.
- INTEG-02: mix.exs docs() wires `:logo` (square mark) and `:favicon` (SVG) so HexDocs renders the brand mark in sidebar + tab.
- INTEG-04: Edits stay in scope (README header + docs() only), package() files: unchanged, chimeway_admin untouched, and all three v1.14 doc-contract/release-gate tests still pass.
</success_criteria>

<output>
Create `.planning/phases/85-repo-integration-readme-hexdocs-favicon-wiring/85-01-SUMMARY.md` when done
</output>