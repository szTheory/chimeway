---
phase: 37
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - test/chimeway/doc_contract_test.exs
  - .planning/phases/37-doc-truth-journey-guides/37-VALIDATION.md
autonomous: true
requirements:
  - DOCS-03
must_haves:
  truths:
    - doc_contract_test.exs asserts journey guide forbids aspirational APIs and requires engine-accurate strings (D-15).
    - 37-VALIDATION.md wave_0_complete and nyquist_compliant updated after tests pass (D-16).
    - mix test doc_contract_test.exs and mix ci pass (DOCS-03 #3).
  artifacts:
    - path: test/chimeway/doc_contract_test.exs
      provides: "Automated journey guide doc-contract gate"
    - path: .planning/phases/37-doc-truth-journey-guides/37-VALIDATION.md
      provides: "Phase validation sign-off checklist"
  key_links:
    - from: test/chimeway/doc_contract_test.exs
      to: guides/flows/multi-step-journeys.md
      via: "Static string assertions on guide content"
---

<objective>
Add lightweight journey-guide doc-contract test (D-15) and finalize phase validation checklist (D-16). Closes DOCS-03 criterion #3 — automated flag for journey guide API drift. Full GATE-01 matrix remains Phase 41.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-doc-truth-journey-guides/37-CONTEXT.md
@.planning/phases/37-doc-truth-journey-guides/37-RESEARCH.md
@.planning/phases/37-doc-truth-journey-guides/37-PATTERNS.md
@.planning/phases/37-doc-truth-journey-guides/37-VALIDATION.md
@test/chimeway/doc_contract_test.exs
@guides/flows/multi-step-journeys.md
</context>

<tasks>

<task type="auto">
  <name>Task 37-03-01: Extend doc_contract_test.exs with journey guide assertions</name>
  <files>test/chimeway/doc_contract_test.exs</files>
  <read_first>
    - .planning/phases/37-doc-truth-journey-guides/37-CONTEXT.md D-15
    - .planning/phases/37-doc-truth-journey-guides/37-PATTERNS.md Pattern 5
    - test/chimeway/doc_contract_test.exs (existing moduledoc tests)
    - guides/flows/multi-step-journeys.md (post plan 37-01)
  </read_first>
  <action>
    Extend `test/chimeway/doc_contract_test.exs` with:

    ```elixir
    @journey_guide "guides/flows/multi-step-journeys.md"

    describe "journey guide doc contract (DOCS-03)" do
      setup do
        content = File.read!(@journey_guide)
        %{content: content}
      end

      @forbidden ~w(
        Chimeway.Workflow
        stop_conditions
        Workflows.Workers
        Chimeway.Trigger.trigger
        type: :wait
        PT2H
      )

      for forbidden <- @forbidden do
        test "forbids #{forbidden} in journey guide", %{content: content} do
          refute String.contains?(content, unquote(forbidden)),
                 "guides/flows/multi-step-journeys.md must not reference #{unquote(forbidden)}"
        end
      end

      @required ~w(
        wait_until
        on_outcome
        Chimeway.trigger
        Chimeway.Signal.track
        Chimeway.Dispatch.WorkflowProgressionWorker
        Chimeway.Dispatch.SignalRouterWorker
        pending_signals
      )

      for required <- @required do
        test "requires #{required} in journey guide", %{content: content} do
          assert String.contains?(content, unquote(required)),
                 "guides/flows/multi-step-journeys.md must reference #{unquote(required)}"
        end
      end

      test "includes Deferred or READ milestone callout", %{content: content} do
        assert String.match?(content, ~r/Deferred|READ-0/),
               "journey guide must defer aspirational read-to-cancel behavior"
      end
    end
    ```

    Adjust if plan 37-01 used slightly different Deferred heading — test must pass on rewritten guide.
  </action>
  <acceptance_criteria>
    - `grep -c 'journey guide doc contract' test/chimeway/doc_contract_test.exs` ≥ 1
    - `mix test test/chimeway/doc_contract_test.exs` exits 0
    - Test file contains at least 5 forbidden and 5 required string assertions
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/doc_contract_test.exs</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 37-03-02: Finalize validation checklist and run CI gate</name>
  <files>.planning/phases/37-doc-truth-journey-guides/37-VALIDATION.md</files>
  <read_first>
    - .planning/phases/37-doc-truth-journey-guides/37-VALIDATION.md
    - .planning/phases/36-golden-path-version-alignment/36-VALIDATION.md (sign-off pattern)
  </read_first>
  <action>
    Update `.planning/phases/37-doc-truth-journey-guides/37-VALIDATION.md`:

    1. Set frontmatter `wave_0_complete: true` and `nyquist_compliant: true` after doc-contract test passes.
    2. Mark Wave 0 Requirements checkboxes complete for journey guide rewrite, oban fix, and doc-contract test.
    3. Run full verification suite and record commands in Validation Sign-Off section.

    Execute:
    - `mix test test/chimeway/doc_contract_test.exs`
    - `mix ci.docs`
    - `mix ci`
    - Repo-wide: `rg 'Workflows\.Workers' guides/` expect 0
  </action>
  <acceptance_criteria>
    - `mix test test/chimeway/doc_contract_test.exs` exits 0
    - `mix ci.docs` exits 0
    - `mix ci` exits 0
    - `rg 'Workflows\.Workers' guides/` returns no matches
    - 37-VALIDATION.md frontmatter contains `wave_0_complete: true`
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/doc_contract_test.exs && mix ci.docs && mix ci && ! rg -q 'Workflows\.Workers' guides/</automated>
  </verify>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-03-01 | Tampering | Doc drift reintroduced after rewrite | High | mitigate | Automated forbidden/required string tests in CI |
| T-37-03-02 | Repudiation | DOCS-03 #3 unverified | Medium | mitigate | mix ci includes doc_contract_test.exs |

**Block gate:** T-37-03-01 HIGH — doc-contract test must pass in mix ci.
</threat_model>

<verification>
1. `mix test test/chimeway/doc_contract_test.exs` green
2. `mix ci` green
3. Manual checklist rows in 37-VALIDATION.md reviewed
</verification>

<success_criteria>
- Journey guide doc-contract test ships and passes (DOCS-03 #3, D-15)
- Phase validation checklist complete (D-16)
- Full CI green with no Workflows.Workers references in guides/
</success_criteria>

<output>
After completion, create `.planning/phases/37-doc-truth-journey-guides/37-03-SUMMARY.md` via orchestration.
</output>
