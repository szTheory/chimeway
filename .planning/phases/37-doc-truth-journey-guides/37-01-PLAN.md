---
phase: 37
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/flows/multi-step-journeys.md
autonomous: true
requirements:
  - DOCS-03
must_haves:
  truths:
    - guides/flows/multi-step-journeys.md describes Notifier workflow/2 with wait_until, on_outcome, stop progress rules (D-03, D-04, D-05).
    - Primary story is time-based in_app to email escalation via wait_until — not read-to-cancel (D-05, D-02).
    - Trigger uses Chimeway.trigger/3 with idempotency_key and tenant_id (D-06).
    - Signal examples use Chimeway.Signal.track(tenant_id, actor_id, event_name, payload) order (D-07).
    - wait_until waiting state, on_outcome/stop vocabulary, and explain/list_traces inspection documented (D-08, D-09, D-10).
    - Delivery-feedback signal path documented with demo E2E and golden-path cross-links (D-11).
    - pending_signals gap and READ milestone deferral in explicit Deferred section (D-01, D-02, D-12).
    - Oban worker names Chimeway.Dispatch.WorkflowProgressionWorker and SignalRouterWorker (D-13).
  artifacts:
    - path: guides/flows/multi-step-journeys.md
      provides: "Engine-accurate multi-step journey guide (DOCS-03 #1, #2)"
  key_links:
    - from: guides/flows/multi-step-journeys.md
      to: test/chimeway/orchestration/workflow_progression_test.exs
      via: "Canonical wait_until fixture pattern"
    - from: guides/flows/multi-step-journeys.md
      to: examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs
      via: "Delivery-feedback signal routing proof (D-11)"
    - from: guides/flows/multi-step-journeys.md
      to: guides/introduction/golden-path.md
      via: "Webhook feedback appendix cross-link"
---

<objective>
Full rewrite of `guides/flows/multi-step-journeys.md` so workflow/journey documentation matches engine capabilities (DOCS-03 #1, #2). Resolves INV-002 via doc-truth: remove aspirational Chimeway.Workflow, stop_conditions, and read-to-cancel primary flow; document real Notifier workflow/2 progress rules and honest signal routing limits (D-01 through D-12).

Oban recipe fixes and doc-contract test are plans 37-02 and 37-03.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-doc-truth-journey-guides/37-CONTEXT.md
@.planning/phases/37-doc-truth-journey-guides/37-RESEARCH.md
@.planning/phases/37-doc-truth-journey-guides/37-PATTERNS.md
@.planning/phases/37-doc-truth-journey-guides/37-VALIDATION.md
@test/chimeway/orchestration/workflow_progression_test.exs
@lib/chimeway/notifier.ex
@lib/chimeway/workflows/progression.ex
@lib/chimeway/workflows/progression_outcome.ex
@lib/chimeway/workflows.ex
@lib/chimeway/signal.ex
</context>

<tasks>

<task type="auto">
  <name>Task 37-01-01: Rewrite guide scaffold and workflow definition (sections 1–2)</name>
  <files>guides/flows/multi-step-journeys.md</files>
  <read_first>
    - .planning/phases/37-doc-truth-journey-guides/37-CONTEXT.md D-03, D-04, D-05
    - .planning/phases/37-doc-truth-journey-guides/37-PATTERNS.md Pattern 1
    - test/chimeway/orchestration/workflow_progression_test.exs lines 40-73
    - lib/chimeway/notifier.ex moduledoc and @callback workflow/2
    - guides/flows/multi-step-journeys.md (current file — replace entirely)
  </read_first>
  <action>
    Replace `guides/flows/multi-step-journeys.md` entirely. Title: `# Multi-Step Journeys`.

    Opening paragraph: Chimeway workflows coordinate multi-channel journeys over time via optional `workflow/2` on a Notifier module — not a separate `Chimeway.Workflow` behaviour.

    **## Scenario: Missed Engagement Escalation** — When a user is mentioned, deliver `in_app` first; if they do not engage within 2 hours, escalate to `email`. Primary mechanism is `wait_until` on the in_app step (D-05), not inbox-read cancellation.

    **## 1. Define the Notifier with a Workflow** — Show `use Chimeway.Notifier` module with `@impl true def workflow(_params, _recipient)` returning `{:ok, %{workflow_key:, workflow_version:, steps: [...]}}`.

    Step shape (exact keys):
    - `step_key: "in_app"`, `step_order: 1`, `channel: :in_app`
    - `config: %{"progress" => [%{"kind" => "wait_until", "anchor" => "prior_delivery_terminal_at", "delay_seconds" => 7200, "to_step" => "email"}]}`
    - Second step: `step_key: "email"`, `step_order: 2`, `channel: :email`, `config: %{}`

    **## 2. Progress Rules** — Explain three rule kinds only: `wait_until`, `on_outcome`, `stop`. State they live in `config["progress"]` on a channel step — there are no separate wait steps, `stop_conditions`, ISO 8601 durations, or `type: :wait` actions.

    Brief `on_outcome` / `stop` example using outcomes from ProgressionOutcome: `delivered`, `suppressed`, `temporary_failure`, `retries_exhausted`, `permanent_failure`, `bounced`. Include WR-02 warning: `temporary_failure` fires on first failed attempt — use `retries_exhausted` when that is the intent (quote or paraphrase notifier.ex moduledoc).
  </action>
  <acceptance_criteria>
    - `grep -c 'Chimeway.Workflow' guides/flows/multi-step-journeys.md` equals 0
    - `grep -c 'stop_conditions' guides/flows/multi-step-journeys.md` equals 0
    - `grep -c 'wait_until' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'prior_delivery_terminal_at' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'delay_seconds' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'on_outcome' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'type: :wait' guides/flows/multi-step-journeys.md` equals 0
    - `grep -c 'PT2H' guides/flows/multi-step-journeys.md` equals 0
  </acceptance_criteria>
  <verify>
    <automated>! grep -q 'Chimeway\.Workflow\|stop_conditions\|type: :wait\|PT2H' guides/flows/multi-step-journeys.md && grep -q 'wait_until' guides/flows/multi-step-journeys.md && grep -q 'prior_delivery_terminal_at' guides/flows/multi-step-journeys.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 37-01-02: Trigger, waiting state, and operator inspection (sections 3–5)</name>
  <files>guides/flows/multi-step-journeys.md</files>
  <read_first>
    - .planning/phases/37-doc-truth-journey-guides/37-CONTEXT.md D-06, D-08, D-09, D-10
    - .planning/phases/37-doc-truth-journey-guides/37-PATTERNS.md Pattern 2
    - lib/chimeway/workflows/progression.ex enter_waiting/6 and moduledoc
    - lib/chimeway/workflows.ex explain/2 and list_traces/2
    - guides/introduction/golden-path.md (trigger pattern)
  </read_first>
  <action>
    Add sections to `guides/flows/multi-step-journeys.md`:

    **## 3. Trigger the Journey** — `Chimeway.trigger(MyApp.Notifiers.MentionEscalation, params, idempotency_key: "...", tenant_id: "...")`. Do NOT use `Chimeway.Trigger.trigger/3`.

    **## 4. How wait_until Works** — After in_app delivery converges, run enters `:waiting` with `status_reason: "waiting_for_step_progression"`. Document `status_context` fields: `due_at`, `to_step`, anchor delivery metadata. Past-due advancement via `Chimeway.Workflows.Progression.progress_run/2`; in production with Oban dispatcher, `Chimeway.Dispatch.WorkflowProgressionWorker` is scheduled at `due_at`.

    **## 5. Inspecting Run State** — Show `Chimeway.Workflows.explain/2` and `Chimeway.Workflows.list_traces/2` with tenant_id scoping. Mention cross-tenant IDs return `:not_found`.
  </action>
  <acceptance_criteria>
    - `grep -c 'Chimeway\.trigger' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'Chimeway\.Trigger\.trigger' guides/flows/multi-step-journeys.md` equals 0
    - `grep -c 'idempotency_key' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'tenant_id' guides/flows/multi-step-journeys.md` ≥ 2
    - `grep -c 'waiting_for_step_progression' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'Workflows\.explain' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'Chimeway\.Dispatch\.WorkflowProgressionWorker' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'Workflows\.Workers' guides/flows/multi-step-journeys.md` equals 0
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Chimeway\.trigger' guides/flows/multi-step-journeys.md && ! grep -q 'Chimeway\.Trigger\.trigger\|Workflows\.Workers' guides/flows/multi-step-journeys.md && grep -q 'waiting_for_step_progression' guides/flows/multi-step-journeys.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 37-01-03: Signal routing, delivery feedback, and Deferred callout (sections 6–8)</name>
  <files>guides/flows/multi-step-journeys.md</files>
  <read_first>
    - .planning/phases/37-doc-truth-journey-guides/37-CONTEXT.md D-07, D-11, D-12, D-02
    - lib/chimeway/signal.ex track/4
    - lib/chimeway/workflows.ex route_signal/1
    - examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs
    - guides/introduction/golden-path.md (webhook appendix)
    - lib/chimeway/dispatch/signal_router_worker.ex
  </read_first>
  <action>
    Add final sections:

    **## 6. Delivery-Feedback Signal Routing (Production Path)** — Document webhook → ProcessFeedbackWorker → `Chimeway.Signal.track/4` with `chimeway.delivery.{succeeded,bounced,failed}` → `Chimeway.Dispatch.SignalRouterWorker` → `Workflows.route_signal/1` → `on_outcome`/`stop` progression. Signal signature: `Chimeway.Signal.track(tenant_id, actor_id, event_name, payload \\ %{})`. Cross-link `feedback_pipeline_e2e_test.exs` and golden-path webhook appendix.

    **## 7. Generic Signal Routing** — Honest contract: `route_signal/1` matches `:waiting` runs where `event_name in pending_signals`, scoped by tenant + recipient. State that `enter_waiting/6` does NOT populate `pending_signals` today — hosts must wire expectations explicitly until READ milestone.

    **## Deferred / Future (READ Milestone)** — Explicit callout (D-02): read-to-cancel via `notification_read` signal is NOT supported out of the box. Reference READ-01 (`pending_signals` auto-population) and READ-02 (inbox read → signal → cancel escalation). Remove any primary-flow implication that viewing a notification halts escalation automatically.

    **## Next Steps** — Link to `guides/recipes/oban-integration.md` for Oban config and Phase 38 recipes when available.
  </action>
  <acceptance_criteria>
    - `grep -c 'Chimeway\.Signal\.track' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'chimeway\.delivery\.' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'SignalRouterWorker' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'Chimeway\.Dispatch\.SignalRouterWorker' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'pending_signals' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -cE 'Deferred|READ-0' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'feedback_pipeline_e2e_test' guides/flows/multi-step-journeys.md` ≥ 1
    - `grep -c 'notification_read' guides/flows/multi-step-journeys.md` equals 0 OR only appears inside Deferred/Future section (manual review)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Chimeway\.Signal\.track' guides/flows/multi-step-journeys.md && grep -q 'Chimeway\.Dispatch\.SignalRouterWorker' guides/flows/multi-step-journeys.md && grep -qE 'Deferred|READ-0' guides/flows/multi-step-journeys.md && grep -q 'pending_signals' guides/flows/multi-step-journeys.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer → journey guide | Author-controlled; must match engine source |
| Adopter → copy-paste | Wrong APIs cause failed adoption before trace |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01-01 | Tampering | Fictional Chimeway.Workflow API | High | mitigate | Grep forbids Chimeway.Workflow; show Notifier workflow/2 only |
| T-37-01-02 | Tampering | Read-to-cancel presented as working | High | mitigate | Deferred section + pending_signals gap; primary story is wait_until |
| T-37-01-03 | Tampering | Wrong trigger/signal signatures | High | mitigate | Chimeway.trigger/3 and Signal.track(tenant, actor, ...) with grep gates |
| T-37-01-04 | Tampering | Wrong Oban worker namespaces | Medium | mitigate | Dispatch.WorkflowProgressionWorker and SignalRouterWorker only |
| T-37-01-05 | Repudiation | Escalation story without explain path | Medium | mitigate | Workflows.explain/2 and list_traces/2 section |

**Block gate:** T-37-01-01, T-37-01-02, T-37-01-03 are HIGH — must pass acceptance criteria before merge.
</threat_model>

<verification>
1. Drift grep suite on `guides/flows/multi-step-journeys.md` (see 37-VALIDATION.md 37-01-* rows)
2. Manual read: Deferred section cites enter_waiting pending_signals gap
3. `mix ci.docs` exits 0 (guide is in mix.exs extras)
</verification>

<success_criteria>
- Journey guide fully rewritten with engine-accurate workflow/2, progress rules, trigger, signals, inspection (DOCS-03 #1)
- INV-002 resolved via doc-truth with explicit Deferred callout (DOCS-03 #2)
- No Chimeway.Workflow, stop_conditions, or Workflows.Workers references remain
</success_criteria>

<output>
After completion, create `.planning/phases/37-doc-truth-journey-guides/37-01-SUMMARY.md` via orchestration.
</output>
