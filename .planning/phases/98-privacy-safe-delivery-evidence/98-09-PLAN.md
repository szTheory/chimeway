---
phase: 98-privacy-safe-delivery-evidence
plan: 09
type: execute
wave: 7
depends_on: [98-07]
files_modified:
  - lib/chimeway/safe_evidence.ex
  - test/chimeway/privacy_test.exs
  - test/chimeway/privacy_boundary_test.exs
autonomous: true
gap_closure: true
requirements: [PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-01/D-03/PRIV-03: one logical-field lookup treats atom/string aliases and repeated tuple/keyword entries as a conflict; equal duplicates fail closed just like unequal duplicates."
    - "PRIV-03 adjacency/ordering: adjacent, separated, reversed, and differently represented duplicates produce the same omitted/error result rather than first- or last-wins selection."
    - "PRIV-03 empty edge: empty maps/lists, nil optional facts, and one valid field have stable safe results without admitting siblings or raw fallbacks."
    - "D-04/D-05/D-06/PRIV-04: provider_code uses the existing closed code grammar, so email-, URL-, credential-, token-, recipient-, body-, and content-shaped text cannot become durable provider evidence."
    - "D-02/D-07: provider_facts/1, attempt_attrs/1, and render_channels/1 share the duplicate-aware lookup contract at both pure and real attempt-persistence boundaries."
  artifacts:
    - path: "lib/chimeway/safe_evidence.ex"
      provides: "Duplicate-aware logical lookup and closed provider-code validation"
    - path: "test/chimeway/privacy_test.exs"
      provides: "Representation, adjacency, equality, ordering, empty, and grammar contract matrix"
    - path: "test/chimeway/privacy_boundary_test.exs"
      provides: "Real DeliveryAttempt persistence rejection for ambiguous provider evidence"
  key_links:
    - from: "lib/chimeway/safe_evidence.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "attempt_attrs/1 rejects ambiguous logical fields before record_attempt/2 constructs its Ecto.Multi"
      pattern: "attempt_attrs"
    - from: "lib/chimeway/safe_evidence.ex"
      to: "lib/chimeway/trigger.ex"
      via: "render_channels/1 omits conflicting channel/render identity entries before notification persistence"
      pattern: "render_channels"
  prohibitions:
    - statement: "No atom/string or repeated keyword/proplist duplicate may select a value by enumeration order, even when duplicate values are equal."
      status: unverified
      verification: flagged
    - statement: "An allowlisted provider key must never admit recipient-, credential-, endpoint-, content-, or provider-controlled text through a length-only check."
      status: unverified
      verification: flagged
    - statement: "Fail-closed duplicate handling must not create a redacted blob, manual-review payload, or other raw fallback."
      status: unverified
      verification: flagged
---

<objective>
Make every Phase 98 logical-field lookup representation-independent and fail closed on ambiguity, then validate provider codes with the same closed grammar used by other safe categorical evidence.

Purpose: Prevent input shape or enumeration order from selecting attacker-controlled durable facts.
Output: One duplicate-aware lookup contract shared by provider, attempt, and render-channel constructors plus pure and PostgreSQL-backed regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-REVIEW.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-07-SUMMARY.md

<interfaces>
- `SafeEvidence.provider_facts/1` returns `{:ok, facts}` or `{:error, :unsafe_evidence}` and currently delegates recognized fields to duplicate-blind `fetch_known/3`.
- `SafeEvidence.attempt_attrs/1` returns a field-tagged unsafe-evidence error and must reject ambiguity before provider facts or adapter/outcome fields are selected.
- `SafeEvidence.render_channels/1` returns a map rather than an error tuple, so an ambiguous normalized channel or identity field is omitted entirely.
- `code?/1` is the existing closed code grammar; provider_code must use it instead of byte length alone.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail closed on duplicate evidence fields in every representation</name>
  <reversibility rating="costly">Ambiguity semantics are shared across adapter attempts, notification render declarations, traces, and proof; changing them later requires coordinated producer/test updates but no migration.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
    - .planning/phases/98-privacy-safe-delivery-evidence/98-REVIEW.md
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/privacy.ex
    - lib/chimeway/deliveries.ex
    - test/chimeway/privacy_test.exs
    - test/chimeway/privacy_boundary_test.exs
  </read_first>
  <files>lib/chimeway/safe_evidence.ex, test/chimeway/privacy_test.exs, test/chimeway/privacy_boundary_test.exs</files>
  <behavior>
    - Test 1: atom/string map duplicates for provider_code, retry_after_ms, accepted_at, outcome, error_class, adapter_module, provider_response, provider_message_id, render_key, and render_version reject/omit the logical field whether values are equal or conflicting.
    - Test 2: repeated atom or string tuple-list entries in adjacent, separated, forward, and reverse order have the identical fail-closed result.
    - Test 3: two top-level render channel keys that normalize to the same channel omit that channel; duplicate inner render identity fields omit the entry.
    - Test 4: empty map/list, nil optional fields, and one valid field return the documented safe empty/single result.
    - Test 5: accepted provider codes match the fixed code grammar; email, URL, bearer/token, recipient, body/content, whitespace, control, prefix/suffix near-miss, and oversized values return unsafe evidence.
    - Test 6: `Deliveries.record_attempt/2` persists no attempt for ambiguous provider/attempt fields and records exactly one typed attempt for one valid representation.
  </behavior>
  <action>Start RED with the verifier's exact atom/string provider_code exploit plus equal-value, keyword/proplist, render-channel, and top-level attempt cases. Replace `fetch_known/3` and the ad hoc nested `Map.get` fallbacks with one logical lookup that enumerates recognized atom/string spellings across maps and two-tuple lists without converting strings to atoms. Return a value only when exactly one logical occurrence exists; distinguish missing from ambiguous internally. For constructors with error tuples, convert ambiguity to the existing stable unsafe-evidence result tagged to the logical field. For `render_channels/1`, group top-level channel names after safe string normalization and omit every conflicting group; also omit an entry if render_key or render_version is ambiguous. Do not compare duplicate values to make equal duplicates acceptable: the collision itself is unsafe and must be representation/order independent per D-01/D-03.

Change optional provider_code validation to use the existing closed `code?/1` grammar and byte bound rather than length alone (D-04/D-05/D-06). Keep provider facts literal, preserve UTC accepted_at and retry bounds, and retain no conflicting/raw value for diagnostics. Make `attempt_attrs/1` accept the map/tuple-list shapes required by the lookup contract while preserving its existing successful output and stable error family. In the PostgreSQL boundary test, prove the ambiguous input exists, `record_attempt/2` returns before `Ecto.Multi`, no attempt row is inserted, and a single valid representation still stores its positive typed fact.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/safe_evidence.ex test/chimeway/privacy_test.exs test/chimeway/privacy_boundary_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/privacy_test.exs test/chimeway/privacy_boundary_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Every recognized logical field uses one duplicate-aware lookup; no string-over-atom, atom-over-string, first-wins, or last-wins fallback remains in the three constructors.
    - Equal and conflicting duplicates fail closed identically across map and tuple-list representations and all tested orders.
    - Provider codes use the closed code grammar and reject the verifier's recipient-shaped exploit.
    - A real ambiguous attempt creates no row, while one valid fact persists and remains explainable.
    - Focused pure and PostgreSQL privacy suites exit zero.
  </acceptance_criteria>
  <done>Input representation and order can no longer select durable provider, attempt, or render evidence, and provider codes admit only closed categorical values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Adapter/provider map or tuple list → SafeEvidence | Untrusted representations may contain duplicate logical fields with attacker-selected ordering. |
| SafeEvidence attempt attrs → PostgreSQL | Ambiguous or recipient-shaped provider facts could become durable evidence. |
| Host render declarations → notification channel snapshot | Atom/string channel and identity collisions could select a different render contract. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-98-09-01 | Tampering | Logical field lookup | high | mitigate | Count normalized occurrences and reject/omit unless exactly one exists; test equal/conflicting and reordered duplicates. | `env MIX_ENV=test mix test test/chimeway/privacy_test.exs --warnings-as-errors` |
| T-98-09-02 | Information Disclosure | provider_code persistence | high | mitigate | Validate through the closed code grammar and prove recipient/credential/content forms create no attempt. | `env MIX_ENV=test mix test test/chimeway/privacy_boundary_test.exs --warnings-as-errors` |
| T-98-09-03 | Denial of Service | Caller-controlled keys | medium | mitigate | Compare existing atom/binary forms only and retain the existing atom-count contract; never create atoms. | `env MIX_ENV=test mix test test/chimeway/privacy_test.exs --warnings-as-errors` |
| T-98-09-SC | Tampering | Package supply chain | low | accept | No dependency or package-manager operation occurs. | `mix ci.audit` |

ASVS L1: high-severity tampering and disclosure threats are mitigated and block on pure plus PostgreSQL-backed tests.
</threat_model>

## Artifacts this phase produces

- Changed private symbols: duplicate-aware logical lookup replacing `fetch_known/3`; provider_code closed-grammar validation; render-channel collision grouping.
- Changed executable contracts: atom/string and repeated tuple-list equality/conflict/ordering matrices, empty/null/single behavior, provider-code hostile grammar cases, and real attempt-write rejection.
- No new path, public API, durable field, migration, external API, package, environment variable, CLI flag, UI surface, or human checkpoint is introduced.

<verification>
Run the pure representation matrix and the real attempt persistence boundary together; both must pass with warnings as errors.
</verification>

<success_criteria>
- Duplicate logical evidence is always ambiguous and fail-closed.
- Provider codes cannot carry recipient, credential, endpoint, or content data.
- Valid singleton facts remain usable and explainable.
- Focused privacy suites exit zero.
</success_criteria>

<output>
Create `.planning/phases/98-privacy-safe-delivery-evidence/98-09-SUMMARY.md` when done.
</output>
