---
phase: 98-privacy-safe-delivery-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/privacy.ex
  - lib/chimeway/safe_evidence.ex
  - lib/chimeway/deliveries.ex
  - lib/chimeway/traces.ex
  - test/chimeway/privacy_test.exs
  - test/chimeway/privacy_boundary_test.exs
autonomous: true
requirements: [PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-01/D-03/PRIV-03: forbidden atom and binary keys are compared case-insensitively and their complete values are omitted at every depth of maps, ordinary lists, and keyword lists without creating atoms from input."
    - "D-03/PRIV-03 edge contract: adjacent or duplicate allowed keyword entries remain distinct and ordered, while every adjacent or duplicate forbidden entry is removed independently."
    - "D-01/PRIV-03 edge contract: empty maps/lists remain empty, nil remains nil, and singleton mixed containers recurse without crashing or changing allowed scalar values."
    - "D-01/PRIV-03 ordering contract: ordinary-list and keyword-list order is stable for retained elements; map ordering is deliberately unspecified."
    - "D-04/D-05/D-06/PRIV-04: recording one real delivery attempt stores only its outcome, stable classification, opaque provider reference, and narrowly validated provider facts; no provider body or generic diagnostic fallback survives."
    - "D-07/D-08: the same recorded attempt is explainable through a tenant-scoped trace using lifecycle IDs, outcome/classification, timestamp, and opaque references only."
  assumptions:
    - "FLAGGED ASSUMPTION — The PRIV-04 specless probe returned an unclassified edge. This plan treats every Chimeway-owned storage, telemetry, log, trace, DTO, and proof boundary as in scope and requires the cross-surface sentinel matrix to fail closed; no narrower exception is assumed."
  artifacts:
    - path: "lib/chimeway/privacy.ex"
      provides: "Atom-safe recursive forbidden-key boundary"
      exports: ["redact/1", "forbidden_key?/1"]
    - path: "lib/chimeway/safe_evidence.ex"
      provides: "Closed durable and diagnostic evidence vocabulary"
      exports: ["opaque_ref/2", "provider_facts/1", "attempt_attrs/1", "telemetry_meta/1", "trace_attempt/1", "timeline_detail/1", "admin_fact/2", "proof/1"]
    - path: "test/chimeway/privacy_boundary_test.exs"
      provides: "Real PostgreSQL attempt-to-trace privacy tracer and shared sentinel helpers"
  key_links:
    - from: "lib/chimeway/deliveries.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "record_attempt/2 validates and projects attrs before DeliveryAttempt.changeset/2"
      pattern: "SafeEvidence.attempt_attrs"
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "last-attempt and timeline maps are built from the same closed fact constructors"
      pattern: "SafeEvidence.(trace_attempt|timeline_detail)"
  prohibitions:
    - "FLAGGED-UNVERIFIED — Chimeway must not retain or emit raw device tokens, endpoints, credentials, recipient or adopter identity, trusted deep links, rendered content, or provider-controlled bodies."
    - "FLAGGED-UNVERIFIED — Chimeway must not preserve a redacted provider body or generic diagnostic map as an explainability fallback."
    - "FLAGGED-UNVERIFIED — Core privacy must not depend on chimeway_admin masking or another optional view layer."
---

<objective>
Prove the privacy architecture through one production delivery-attempt path: hostile nested provider detail crosses the shared boundary, becomes bounded durable evidence, and is explained through a tenant-scoped trace without leaking a sentinel.

Purpose: Validate the highest-risk write-to-read seam before expanding the same vocabulary across trigger, telemetry, logs, DTOs, proof, and historical data.
Output: `Chimeway.Privacy`, `Chimeway.SafeEvidence`, one safe attempt persistence/trace path, and executable recursive plus end-to-end contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-RESEARCH.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-PATTERNS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-VALIDATION.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-01-SUMMARY.md

<interfaces>
- `Chimeway.Deliveries.record_attempt(%Chimeway.Delivery{}, map())` owns atomic attempt numbering, insert, delivery convergence, and post-commit workflow progression.
- `Chimeway.DeliveryAttempt` persists `outcome`, `error_class`, `provider_response`, `provider_message_id`, `adapter_module`, and `inserted_at`; Phase 98 constrains values at the context boundary without renaming the physical columns.
- `Chimeway.Traces.explain_delivery(delivery_id, tenant_id: tenant)` returns `{:ok, %Chimeway.Traces.Explanation{}} | {:error, :not_found}` and must retain Phase 97 tenant predicates.
- New `Chimeway.Privacy.redact/1` is the only recursive forbidden-key policy; `Chimeway.SafeEvidence` consumes it and emits literal-key, validated maps.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Record and explain one hostile adapter attempt without a leak</name>
  <reversibility rating="costly">D-05/D-07 make the shared evidence vocabulary a cross-module contract; renaming facts later requires coordinated persistence and projection changes, but no raw data migration is required for this tracer.</reversibility>
  <read_first>
    - .planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
    - lib/chimeway/deliveries.ex
    - lib/chimeway/delivery_attempt.ex
    - lib/chimeway/traces.ex
    - lib/chimeway/traces/explanation.ex
    - lib/chimeway/telemetry.ex
    - test/chimeway/deliveries_test.exs
    - test/chimeway/traces_test.exs
    - test/support/data_case.ex
    - lib/chimeway/privacy.ex (new file; closest shallow analog is `lib/chimeway/deliveries.ex` `sanitize_metadata/1`)
    - lib/chimeway/safe_evidence.ex (new file; closest closed-output analog is `priv/adoption_proof/artifact_consumer_fixture.ex` `build_safe_evidence!/2`)
    - test/chimeway/privacy_boundary_test.exs (new file; closest integration analog is `test/chimeway/admin_test.exs`)
  </read_first>
  <files>lib/chimeway/privacy.ex, lib/chimeway/safe_evidence.ex, lib/chimeway/deliveries.ex, lib/chimeway/traces.ex, test/chimeway/privacy_boundary_test.exs</files>
  <behavior>
    - Test 1: `record_attempt/2` receives nested token, authorization, recipient, trusted-link, content, and provider-body sentinels plus a safe outcome/classification/reference; the reloaded attempt contains none of the sentinels.
    - Test 2: the matching tenant's `explain_delivery/2` contains the attempt outcome, error classification, ordinal, timestamp, and validated opaque/provider facts, and contains none of the sentinels.
    - Test 3: an unexpected or malformed diagnostic value cannot become a fallback `provider_response`; the call returns a stable unsafe-evidence error or stores an empty typed fact map while preserving the existing transaction error shape.
    - Test 4: wrong-tenant explanation remains `{:error, :not_found}` and never changes privacy behavior.
  </behavior>
  <action>Start RED in the new PostgreSQL-backed boundary suite, then implement the production slice per D-01 through D-08. Create `Chimeway.Privacy.redact/1` as a shape-aware walker: map pairs retain their original key and recurse only when `forbidden_key?/1` is false; keyword lists drop forbidden pairs and recurse retained values; ordinary lists map in order; scalars pass through. Compare atom keys through `Atom.to_string/1` and binary keys through `String.downcase/1` against one compile-time taxonomy; never convert caller strings to atoms, and never traverse the value of a forbidden pair. Create `Chimeway.SafeEvidence` with literal output keys and bounded scalar validators. `opaque_ref/2` validates a host/provider-supplied, domain-tagged bounded `cw_...` reference and never derives one from raw identity, endpoint, token, credential, or provider data; `provider_facts/1` accepts only `provider_code` as a bounded classification string, nonnegative bounded `retry_after_ms`, and UTC `accepted_at`; `attempt_attrs/1` accepts only outcome, whitelisted error class, adapter classification, validated opaque provider reference, and those provider facts. Wire `Deliveries.do_record_attempt/2` to call `SafeEvidence.attempt_attrs/1` before `Ecto.Multi`, remove the private shallow provider-response sanitizer, keep row locking/attempt numbering/error tuples/workflow progression unchanged, and store the typed provider-fact map in the legacy physical `provider_response` column without retaining the source body. Make `Traces.build_last_attempt_map/1` and attempt timeline detail call `SafeEvidence.trace_attempt/1`/`timeline_detail/1`; retain Phase 97 tenant joins and omit raw recipient, correlation, adapter/provider terms from this tracer projection. The new test must serialize persisted rows and the Explanation to binaries and reject every exact sentinel, preventing key-only assertions from passing while a value leaks.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/privacy.ex lib/chimeway/safe_evidence.ex lib/chimeway/deliveries.ex lib/chimeway/traces.ex test/chimeway/privacy_boundary_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/privacy_boundary_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source: `Deliveries.record_attempt/2` reaches `SafeEvidence.attempt_attrs/1` before `DeliveryAttempt.changeset/2`; the old shallow provider-response sanitizer is no longer the write policy.
    - Storage: a real reloaded attempt retains only typed facts and contains none of the five privacy sentinel categories.
    - Projection: tenant-scoped `explain_delivery/2` retains outcome/classification/timing/opaque evidence and contains no raw diagnostic value.
    - Executable evidence: the focused boundary suite exits zero with PostgreSQL persistence enabled.
  </acceptance_criteria>
  <done>One real hostile adapter-result path is safe from input through durable attempt and tenant-scoped explanation, establishing the architecture for later surface expansion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock recursive shape, equality, empty, and ordering semantics</name>
  <read_first>
    - lib/chimeway/privacy.ex
    - lib/chimeway/safe_evidence.ex
    - test/chimeway/trigger_sanitization_test.exs
    - test/chimeway/privacy_test.exs (new file; closest pure sanitizer analog is `test/chimeway/trigger_sanitization_test.exs`)
  </read_first>
  <files>lib/chimeway/privacy.ex, lib/chimeway/safe_evidence.ex, test/chimeway/privacy_test.exs</files>
  <behavior>
    - Test 1: mixed-case atom/string forbidden keys disappear at map, ordinary-list, and keyword-list depths, including adjacent duplicate keyword entries.
    - Test 2: empty map, empty ordinary list, empty keyword list, nil, and one-element containers have explicit stable outputs.
    - Test 3: retained ordinary-list and keyword-list elements preserve input order and allowed duplicate keyword entries; map order is never asserted.
    - Test 4: a forbidden key's value is not traversed or masked, and arbitrary binary keys do not increase `:erlang.system_info(:atom_count)` after warm-up.
    - Test 5: equal safe facts normalize identically, while unknown provider-fact keys and invalid value types are absent or rejected with the documented stable result.
  </behavior>
  <action>Expand the pure contract per D-01/D-03 and the three resolved PRIV-03 specless edges. Use a fixed taxonomy covering credential/auth/token/endpoint, recipient/adopter identity, URL/deep-link, payload/content/rendered-body, and provider-body spellings with case-insensitive atom/binary comparison. Keep `forbidden_key?/1` public for contract tests but do not expose the taxonomy as mutable configuration. Define empty behavior exactly as `redact(%{}) == %{}`, `redact([]) == []`, and `redact(nil) == nil`; retain scalar binaries only when they occur beneath an allowed key because value classification belongs to closed `SafeEvidence` builders. Prove list stability with equality assertions, not map enumeration. Warm the module before capturing atom count, run thousands of unique binary keys through both modules, and assert no count increase. Add focused provider-fact boundary cases so an allowlisted key with an unbounded or structurally invalid value cannot cross into durable evidence.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/privacy.ex lib/chimeway/safe_evidence.ex test/chimeway/privacy_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/privacy_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Unit assertions cover maps, ordinary lists, keyword lists, mixed nesting/casing, duplicates/adjacency, empty/nil/singleton input, and retained-list ordering.
    - Atom-count evidence executes after warm-up and proves unique input keys create no atoms.
    - Provider fact validation checks both key vocabulary and value type/size bounds.
    - The focused unit suite exits zero with warnings treated as errors.
  </acceptance_criteria>
  <done>The single recursive boundary and closed fact validators have explicit, executable behavior for all resolved PRIV-03 edge probes and malicious input shapes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Adapter/provider term → attempt write | Provider-controlled nested values cross into Chimeway-owned PostgreSQL state. |
| PostgreSQL attempt → trace explanation | Durable evidence becomes operator-visible output. |
| Caller-controlled keys → BEAM atom table | Arbitrary binaries could exhaust a VM-global finite resource. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-98-01 | Information Disclosure | `Deliveries.record_attempt/2` | high | mitigate | Replace generic response persistence with `SafeEvidence.attempt_attrs/1`, typed provider facts, and full-value sentinel assertions on reloaded rows. | `env MIX_ENV=test mix test test/chimeway/privacy_boundary_test.exs --warnings-as-errors` |
| T-98-02 | Information Disclosure | `Traces.explain_delivery/2` | high | mitigate | Build attempt/timeline output only from literal safe facts while preserving tenant-scoped joins. | `env MIX_ENV=test mix test test/chimeway/privacy_boundary_test.exs test/chimeway/traces_test.exs --warnings-as-errors` |
| T-98-03 | Denial of Service | Recursive key normalization | high | mitigate | Compare binary forms only; prohibit dynamic atom conversion and prove stable atom count under unique-key load. | `env MIX_ENV=test mix test test/chimeway/privacy_test.exs --warnings-as-errors` |
| T-98-04 | Tampering | Safe provider facts | medium | mitigate | Validate every allowed fact's type, range, and vocabulary before building the literal output map. | `env MIX_ENV=test mix test test/chimeway/privacy_test.exs --warnings-as-errors` |
| T-98-01-SC | Tampering | Package supply chain | low | accept | No package installation or dependency change occurs; use Erlang/Elixir standard crypto and existing lockfile dependencies only. | `mix ci.audit` |

ASVS L1 mapping: V4 retains Phase 97 tenant predicates on the trace read; V5 validates untrusted shapes and safe fact values before persistence. High-severity disclosure/atom-exhaustion threats block on focused executable evidence.
</threat_model>

## Artifacts this phase produces

- New paths: `lib/chimeway/privacy.ex`, `lib/chimeway/safe_evidence.ex`, `test/chimeway/privacy_test.exs`, `test/chimeway/privacy_boundary_test.exs`.
- New public symbols: `Chimeway.Privacy.redact/1`, `Chimeway.Privacy.forbidden_key?/1`, `Chimeway.SafeEvidence.opaque_ref/2`, `provider_facts/1`, `attempt_attrs/1`, `telemetry_meta/1`, `trace_attempt/1`, `timeline_detail/1`, `admin_fact/2`, and `proof/1`.
- Changed functions: `Chimeway.Deliveries.record_attempt/2`/private attempt normalization and `Chimeway.Traces` last-attempt/timeline construction.
- Durable interpretation change: `chimeway_delivery_attempts.provider_response` stores only the fixed `provider_facts` vocabulary; `provider_message_id` is an opaque reference, never a raw provider value.
- No new package, external API, environment variable, CLI flag, or schema path is introduced in this plan.

<verification>
Run the pure recursive suite and the PostgreSQL attempt-to-trace tracer. Both commands must finish under the focused-test latency budget; if combined execution exceeds 60 seconds, retain the two independent commands rather than weakening coverage.
</verification>

<success_criteria>
- The production attempt tracer is green before any later wave executes.
- Recursive redaction covers mixed maps/lists/keywords, casing, duplicates, empty/singleton inputs, ordering, and atom safety.
- Persisted and projected attempt evidence contains only bounded facts and zero raw sentinels.
- Phase 97 tenant isolation and attempt transaction semantics remain intact.
</success_criteria>

<output>
Create `.planning/phases/98-privacy-safe-delivery-evidence/98-01-SUMMARY.md` when done.
</output>
