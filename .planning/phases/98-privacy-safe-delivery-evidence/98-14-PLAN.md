---
phase: 98-privacy-safe-delivery-evidence
plan: 14
type: execute
wave: 12
depends_on: [98-13]
files_modified:
  - lib/chimeway/safe_evidence.ex
  - lib/chimeway/traces.ex
  - test/chimeway/traces_test.exs
  - test/chimeway/privacy_boundary_test.exs
  - test/chimeway/runtime_prefix_integration_test.exs
  - test/chimeway/tenant_identity_test.exs
  - chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex
  - chimeway_admin/test/chimeway_admin/live/privacy_leak_live_test.exs
  - test/chimeway/integrations/sigra_auth_lifecycle_test.exs
  - priv/adoption_proof/artifact_consumer_fixture.ex
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
gap_closure: true
requirements: [PRIV-03, PRIV-04]
must_haves:
  truths:
    - "D-02/D-07/D-09/PRIV-03: get_trace/2, find_traces_for_recipient/2, and find_traces_by_correlation_id/2 return SafeEvidence-built closed maps rather than Event, Notification, Delivery, or DeliveryAttempt schemas."
    - "D-03/D-04/D-06/PRIV-04: lifecycle rows containing legacy raw payload, render, recipient, link, credential, and provider-response sentinels cannot emit those values through any public trace query."
    - "D-05/D-08 and Phase 97 D-01/D-04/D-07: closed trace maps retain the authorized root event tenant_id, tenant-scoped lifecycle IDs, opaque recipient/correlation references, notification/render identity, channel, status/reason/classification, timestamps, attempt ordinal, and established ordering/empty/not-found behavior."
    - "D-04/D-05: generated Core and Mailglass notifiers supply explicit cw_ opaque recipient references, while the Mailglass address exists only in transient host render context and is absent from Chimeway-owned rows, traces, and proof text."
    - "D-07/D-08/PRIV-04: mix verify.adoption_paths --only core and mix verify.adoption_paths both exit zero and emit their closed proof records."
  assumptions:
    - statement: "Spec-less edge probe row PRIV-03 adjacency remains unresolved by this targeted gap closure and is not silently treated as covered."
      status: flagged-unverified
    - statement: "Spec-less edge probe row PRIV-03 empty remains unresolved by this targeted gap closure and is not silently treated as covered."
      status: flagged-unverified
    - statement: "Spec-less edge probe row PRIV-03 ordering remains unresolved by this targeted gap closure and is not silently treated as covered."
      status: flagged-unverified
    - statement: "Spec-less edge probe row PRIV-04 unclassified remains unresolved by this targeted gap closure and is not silently treated as covered."
      status: flagged-unverified
  artifacts:
    - path: "lib/chimeway/safe_evidence.ex"
      provides: "Closed event, notification, delivery, and attempt trace constructors"
    - path: "lib/chimeway/traces.ex"
      provides: "Tenant-scoped public trace APIs that project every loaded row before return"
    - path: "test/chimeway/privacy_boundary_test.exs"
      provides: "Legacy/raw lifecycle sentinel regression across all three public trace queries"
    - path: "priv/adoption_proof/artifact_consumer_fixture.ex"
      provides: "Runnable Core and Mailglass proof notifiers using explicit opaque recipient refs and transient address context"
    - path: "test/chimeway/release_gate_contract_test.exs"
      provides: "Executable focused-Core and complete adoption-path gate assertions"
  key_links:
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "all public trace results are recursively composed through named closed constructors before crossing the API boundary"
      pattern: "SafeEvidence.trace_(event|notification|delivery|attempt)"
    - from: "priv/adoption_proof/artifact_consumer_fixture.ex"
      to: "lib/chimeway/safe_evidence.ex"
      via: "generated notifier recipient_ref values cross recipient_reference/1 and proof output crosses proof/1"
      pattern: "cw_artifact_(core|mailglass)_proof|SafeEvidence.proof"
    - from: "priv/adoption_proof/artifact_consumer_fixture.ex"
      to: "Mailglass mailable render assigns"
      via: "the real test address is supplied only in the in-process rendering map consumed by Swoosh"
      pattern: "Map.fetch!(assigns, \"to\")"
  prohibitions:
    - statement: "A public trace query must not expose a raw lifecycle schema or require optional Admin redaction to become safe."
      status: flagged-unverified
    - statement: "An opaque recipient reference must not be reused as an email address, and a real address must not enter Chimeway persistence, trace, telemetry, log, or proof output."
      status: flagged-unverified
    - statement: "Closing the privacy gap must not weaken tenant predicates, ordering, absence semantics, or the closed proof vocabulary."
      status: flagged-unverified
---

<objective>
Close the remaining Phase 98 privacy gaps by projecting every public raw-schema trace path through SafeEvidence and making the packaged Core/Mailglass adoption proofs obey the strict opaque-recipient boundary.

Purpose: Ensure operators can use every public trace and proof path without Chimeway emitting legacy sensitive lifecycle fields or blocking real adoption evidence.
Output: Closed nested trace DTOs, executable legacy/raw sentinel regressions, compatible Admin/Sigra consumers, corrected proof notifiers, and green focused plus complete adoption gates.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-CONTEXT.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-RESEARCH.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-PATTERNS.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-04-SUMMARY.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-05-SUMMARY.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-12-SUMMARY.md
@.planning/phases/98-privacy-safe-delivery-evidence/98-13-SUMMARY.md

<interfaces>
- `Chimeway.SafeEvidence.trace/1`, `trace_attempt/1`, and `timeline_detail/1` establish the closed, value-validated operator vocabulary; new nested constructors must compose these policies rather than create another allowlist.
- `Chimeway.Traces.get_trace/2` returns `{:ok, trace}` or `{:error, :not_found}`; the two `find_traces*` functions return ordered lists and preserve their current tenant failure/empty behavior.
- The closed nested shapes retain their current root orientation: event for `get_trace/2` and correlation search, notification for recipient search. They use literal maps, never Ecto structs.
- Closed event fields are exactly `id`, `tenant_id`, `notification_key`, projected `correlation_id`, `inserted_at`, and `notifications`; notification fields are exactly `id`, projected `recipient_id`, bounded `recipient_type`, `inserted_at`, and `deliveries`; delivery fields are exactly `id`, `channel`, `status`, `planning_reason`, `suppression_reason`, `render_key`, `render_version`, `inserted_at`, `updated_at`, and `attempts`; attempt fields are exactly `id`, `outcome`, `error_class`, `attempt_number`, projected `provider_message_id`, and `inserted_at`. Retain `tenant_id` only on the event root: Phase 97 D-01 makes it durable ownership evidence, Phase 97 D-04/D-07 require an authorized tenant-scoped result without cross-tenant disclosure, `SafeEvidence.admin_fact/2` already establishes tenant identity as an allowlisted operator fact, and `tenant_identity_test.exs` sanctions the exact root value.
- `ChimewayAdmin.Live.TraceSearchLive` and the Sigra lifecycle test are direct consumers of the current schema-shaped results and must consume the closed map vocabulary without reaching around the public API.
- Repository caller inventory from `rg`: contract-changing callers are `test/chimeway/traces_test.exs`, `test/chimeway/runtime_prefix_integration_test.exs`, `test/chimeway/tenant_identity_test.exs`, `chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex`, and `test/chimeway/integrations/sigra_auth_lifecycle_test.exs`; map-compatible regression callers that still run under `mix ci` or `mix verify.runtime_prefix` are `test/chimeway/integration/delivery_lifecycle_test.exs`, `test/chimeway/generated_prefixed_runtime_proof_test.exs`, and `test/chimeway/tenant_scope_contract_test.exs`. Invocation-only examples in `guides/cheatsheet.cheatmd`, `guides/recipes/tracing-a-notification.md`, `guides/recipes/password-reset-support-trace.md`, `guides/introduction/golden-path.md`, and `examples/chimeway_demo_host/README.md` do not assert Ecto types or excluded fields and therefore require no content change.
- Generated proof notifiers may carry `cw_artifact_core_proof` and `cw_artifact_mailglass_proof` as recipient refs. The real Mailglass address belongs only in transient rendering assigns under `"to"` for the mailable.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tracer — close every public trace query over one hostile lifecycle</name>
  <reversibility rating="costly">The raw Ecto return contract is already public; replacing it with closed maps requires coordinated consumer updates, but D-07/D-09 already mandate that correction.</reversibility>
  <read_first>
    - .planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
    - lib/chimeway/safe_evidence.ex
    - lib/chimeway/traces.ex
    - lib/chimeway/traces/explanation.ex
    - test/chimeway/traces_test.exs
    - test/chimeway/privacy_boundary_test.exs
  </read_first>
  <files>lib/chimeway/safe_evidence.ex, lib/chimeway/traces.ex, test/chimeway/traces_test.exs, test/chimeway/privacy_boundary_test.exs</files>
  <behavior>
    - Test 1: one tenant-scoped lifecycle containing raw values in Event payload, Notification metadata/render assigns, Delivery metadata/planning/render fields, and Attempt provider response is returned by all applicable public trace queries with none of the exact sentinels present.
    - Test 2: each query returns literal nested maps with exactly the declared safe keys; no returned value is an Event, Notification, Delivery, or DeliveryAttempt struct.
    - Test 3: safe IDs, opaque refs, notification/render identity, status/reasons, classifications, timestamps, attempt ordering, list ordering, limit/filter behavior, empty results, and wrong-tenant not-found behavior remain observable.
  </behavior>
  <action>Start RED by adding one real PostgreSQL legacy/raw sentinel lifecycle to `privacy_boundary_test.exs` and focused exact-shape cases to `traces_test.exs`. Follow the `98-PATTERNS.md` SafeEvidence analog at `priv/adoption_proof/artifact_consumer_fixture.ex:310-345`: validate prerequisite values and construct literal-key maps, never forward an input map. Per D-01/D-02/D-03, exercise nested map/list/keyword and mixed-case forbidden values adjacent to positive allowed facts, including empty association lists and multiple ordered attempts. Add named `SafeEvidence.trace_event/1`, `trace_notification/1`, and `trace_delivery/1` literal constructors and extend `trace_attempt/1` with the safe attempt ID; constructors recursively compose only the declared shapes and existing validators/projections. Include the authorized event row's `tenant_id` as the exact root ownership fact per Phase 97 D-01/D-04/D-07 and the existing `SafeEvidence.admin_fact/2` tenant allowlist; do not repeat tenant identity on nested notification or delivery maps. Per D-04/D-05/D-06/D-07/D-08, do not serialize, redact-and-forward, or return a lifecycle struct or generic schema map. Follow the `98-PATTERNS.md` Traces service analog at `lib/chimeway/traces.ex:164-222, 327-390, 749-775` and Shared Pattern `Tenant Scope`: retain every current tenant predicate/preload privately, then project every result immediately before return from `get_trace/2`, `find_traces_for_recipient/2`, and `find_traces_by_correlation_id/2`. Update moduledocs/specs and existing trace assertions to the exact closed map contract while preserving filter, limit, ordering, empty, and not-found semantics. Require the legacy/raw fixture to call all three public query families and scan the complete returned terms for every exact endpoint, credential, recipient, trusted-link, rendered-content, and provider-body sentinel.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/safe_evidence.ex lib/chimeway/traces.ex test/chimeway/traces_test.exs test/chimeway/privacy_boundary_test.exs &amp;&amp; env MIX_ENV=test mix test test/chimeway/traces_test.exs test/chimeway/privacy_boundary_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `env MIX_ENV=test mix test test/chimeway/traces_test.exs test/chimeway/privacy_boundary_test.exs --warnings-as-errors` exits zero.
    - Exact-key assertions prove the three public APIs return only closed literal maps and no lifecycle schema structs.
    - Exact-key assertions include `tenant_id` on each event root, exclude it from nested notification/delivery/attempt maps, and prove the returned value equals the authorized tenant while a wrong-tenant lookup remains `{:error, :not_found}`.
    - The one raw/legacy database fixture reaches all applicable public trace queries and every exact sensitive sentinel is absent from each complete return term.
    - Positive safe facts, tenant isolation, association/attempt order, limits, filters, empty lists, and not-found results remain asserted in the executable tests.
  </acceptance_criteria>
  <done>Every public Traces query crosses a SafeEvidence-backed closed projection on a real hostile lifecycle, with useful operator facts intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move direct consumers onto the closed trace vocabulary</name>
  <read_first>
    - lib/chimeway/traces.ex
    - .planning/phases/98-privacy-safe-delivery-evidence/98-PATTERNS.md
    - chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex
    - chimeway_admin/test/chimeway_admin/live/privacy_leak_live_test.exs
    - test/chimeway/integrations/sigra_auth_lifecycle_test.exs
    - test/chimeway/runtime_prefix_integration_test.exs
    - test/chimeway/tenant_identity_test.exs
  </read_first>
  <files>chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex, chimeway_admin/test/chimeway_admin/live/privacy_leak_live_test.exs, test/chimeway/integrations/sigra_auth_lifecycle_test.exs, test/chimeway/runtime_prefix_integration_test.exs, test/chimeway/tenant_identity_test.exs</files>
  <behavior>
    - Test 1: Admin recipient and correlation searches flatten the closed `recipient_id` and delivery maps into the existing visible rows without matching Ecto schemas.
    - Test 2: Admin rendered output contains notification key, channel, status, and masked opaque recipient evidence while excluding the legacy/raw sentinels.
    - Test 3: Sigra lifecycle proof recognizes attempted closed delivery maps and still rejects token/code/link sentinels in traces and telemetry.
    - Test 4: runtime-prefix trace coverage matches the literal event map instead of `%Event{}`, and tenant-identity coverage observes the exact safe root `tenant_id` while wrong-tenant lookup remains indistinguishable from absence.
  </behavior>
  <action>Adapt only direct public-API consumers required by the Task 1 contract. Follow the `98-PATTERNS.md` Traces/Admin projection analog at `lib/chimeway/traces.ex:164-222, 327-390, 749-775` and `lib/chimeway/admin.ex:36-68, 275-310`, plus the focused integration-test analogs at `test/chimeway/admin_test.exs:20-175`: per D-07/D-08/D-09, update `TraceSearchLive` flattening to read the closed notification/event/delivery maps and projected `recipient_id`, retaining authorization, tenant options, result order, delivery navigation, status, and view-layer masking as defense in depth. Extend its privacy LiveView test with the closed search result path and exact sentinel absence. Update the Sigra lifecycle helper to evaluate the closed delivery map's status and attempts instead of pattern matching the Ecto Delivery schema; preserve its real dispatch, correlation, sensitive trace, and telemetry assertions. Replace the `%Event{notifications: ...}` assertion in `runtime_prefix_integration_test.exs` with an exact root-map/nested-list assertion and retain the existing static-prefix proof. Preserve both `tenant_identity_test.exs` assertions that the returned root `tenant_id` equals `"tenant-a"` and make them exact closed-map assertions while keeping the wrong-tenant not-found checks. The inventory established that `delivery_lifecycle_test.exs`, `generated_prefixed_runtime_proof_test.exs`, and `tenant_scope_contract_test.exs` already consume fields supported by the closed map; do not edit them, but execute them through the CI/runtime-prefix gates below. Do not query lifecycle schemas directly from any consumer to reconstruct fields omitted by SafeEvidence.</action>
  <verify>
    <automated>mix format --check-formatted chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex chimeway_admin/test/chimeway_admin/live/privacy_leak_live_test.exs test/chimeway/integrations/sigra_auth_lifecycle_test.exs test/chimeway/runtime_prefix_integration_test.exs test/chimeway/tenant_identity_test.exs &amp;&amp; mix verify.admin &amp;&amp; env MIX_ENV=test mix test test/chimeway/integrations/sigra_auth_lifecycle_test.exs test/chimeway/tenant_identity_test.exs --warnings-as-errors &amp;&amp; mix verify.runtime_prefix</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.admin` exits zero with recipient and correlation search results rendered from closed maps.
    - `env MIX_ENV=test mix test test/chimeway/integrations/sigra_auth_lifecycle_test.exs --warnings-as-errors` exits zero without schema matching or sensitive-value regressions.
    - `env MIX_ENV=test mix test test/chimeway/tenant_identity_test.exs --warnings-as-errors` exits zero with exact root `tenant_id` and fail-closed wrong-tenant behavior.
    - `mix verify.runtime_prefix` exits zero, covering the revised runtime-prefix assertion plus the unchanged generated-prefixed map consumer.
    - No consumer adds a direct Repo/Ecto query to recover excluded lifecycle fields.
  </acceptance_criteria>
  <done>Existing Admin and Sigra consumers use the safe public contract without bypassing it or losing explainability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restore Core and Mailglass adoption proof with opaque recipients</name>
  <read_first>
    - .planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md
    - lib/chimeway/safe_evidence.ex
    - priv/adoption_proof/artifact_consumer_fixture.ex
    - lib/mix/tasks/verify.adoption_paths.ex
    - test/chimeway/release_gate_contract_test.exs
  </read_first>
  <files>priv/adoption_proof/artifact_consumer_fixture.ex, test/chimeway/release_gate_contract_test.exs</files>
  <behavior>
    - Test 1: generated Core notifier supplies `cw_artifact_core_proof`, reaches Trigger, explanation, and `CHIMEWAY_CORE_PROOF`, and the focused Core command exits zero.
    - Test 2: generated Mailglass notifier supplies `cw_artifact_mailglass_proof`; the real address reaches the fake Mailglass delivery only through transient `"to"` render assigns and appears in no Chimeway row, trace, log, or proof record.
    - Test 3: the complete gate runs Core, Mailglass, and Accrue in order, exits zero, and emits one closed proof record per path.
  </behavior>
  <action>Start RED with release-gate contract assertions that the generated notifier sources use explicit `recipient_ref` values and that the actual focused Core subprocess exits zero. Follow the `98-PATTERNS.md` proof-fixture analogs at `priv/adoption_proof/artifact_consumer_fixture.ex:310-345, 774-806` and focused release-gate analogs at `test/chimeway/release_gate_contract_test.exs:1161-1166, 1266-1357`: construct literal proof records from validated facts and keep unknown/duplicate proof keys string-allowlisted without atom creation. In `notifier_ex/0`, replace the raw Core recipient with `cw_artifact_core_proof`. In `mailglass_notifier_ex/0`, use `cw_artifact_mailglass_proof` and place `proof@example.test` only in the transient rendering assigns under `"to"`, alongside the existing subject/body values consumed by the mailable; do not place the address in notifier recipient identity, trigger params, Oban args, or any durable evidence input. Per D-04/D-05/D-06, strengthen the Mailglass proof to positively assert the fake adapter delivered to the real address while the persisted Event/Notification/Delivery/Attempt, all public trace projections, captured output, and parsed safe proof omit it. Per D-07/D-08, preserve exact closed proof keys, provider-handoff-only semantics, timeline ordering, duplicate/unknown rejection, and non-atomizing parsing. Run the focused Core command first, then the complete adoption-path gate; both results are binding.</action>
  <verify>
    <automated>mix format --check-formatted priv/adoption_proof/artifact_consumer_fixture.ex test/chimeway/release_gate_contract_test.exs &amp;&amp; mix verify.adoption_paths --only core &amp;&amp; mix verify.adoption_paths</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.adoption_paths --only core` exits zero and emits exactly one `CHIMEWAY_CORE_PROOF` record.
    - `mix verify.adoption_paths` exits zero with Core, Mailglass, and Accrue START/proof records in the established order.
    - Executable assertions prove the Mailglass fake received the real address and that no Chimeway-owned row, public trace result, command output outside the adapter assertion, or parsed proof retains it.
    - Generated Core and Mailglass notifier recipients are explicit bounded `cw_` references accepted unchanged by `SafeEvidence.recipient_reference/1`.
  </acceptance_criteria>
  <done>The packaged Core and full adoption proof gates are runnable under the strict recipient contract, and the real Mailglass address remains transient host render context only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Legacy lifecycle rows → public Traces API | Previously persisted or directly inserted raw fields become operator-visible unless every row is closed-projected. |
| Tenant-scoped queries → nested closed DTOs | Projection must not loosen the Phase 97 tenant predicates or association coherence. |
| Generated notifier/render context → Trigger and Mailglass | Opaque correlation evidence enters Chimeway while a real address remains host-owned transient data. |
| Packaged proof process → release-gate parser | Machine-readable output must retain a closed vocabulary and honest provider-handoff semantics. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-98-14-01 | Information Disclosure | Public Traces APIs | high | mitigate | Project every loaded lifecycle row through named SafeEvidence constructors and scan all three query families against raw legacy sentinels. | `env MIX_ENV=test mix test test/chimeway/traces_test.exs test/chimeway/privacy_boundary_test.exs --warnings-as-errors` |
| T-98-14-02 | Tampering | Tenant-scoped trace projection | high | mitigate | Preserve tenant predicates, expose only the authorized root event tenant identity, and assert wrong-tenant, static-prefix, empty, ordering, limit, and filter behavior around the closed projection. | `env MIX_ENV=test mix test test/chimeway/traces_test.exs test/chimeway/tenant_identity_test.exs --warnings-as-errors &amp;&amp; mix verify.runtime_prefix` |
| T-98-14-03 | Information Disclosure | Mailglass proof notifier | high | mitigate | Separate opaque recipient ref from transient `to` assign and assert the address only at the fake adapter boundary, never in Chimeway rows/traces/proof. | `mix verify.adoption_paths` |
| T-98-14-04 | Repudiation | Adoption proof gate | medium | mitigate | Require focused Core and complete ordered proof commands to exit zero with exact proof records. | `mix verify.adoption_paths --only core &amp;&amp; mix verify.adoption_paths` |
| T-98-14-SC | Tampering | Package supply chain | low | accept | No package-manager install or dependency change occurs in this plan. | `git diff -- mix.exs mix.lock` |

ASVS L1 disposition: every high-severity disclosure or tenant-tampering threat is mitigated with executable boundary evidence; the plan introduces no dependency operation.
</threat_model>

## Artifacts this phase produces

- New symbols in `lib/chimeway/safe_evidence.ex`: `Chimeway.SafeEvidence.trace_event/1`, `trace_notification/1`, and `trace_delivery/1`; `trace_attempt/1` gains the closed attempt lifecycle ID.
- Changed public functions in `lib/chimeway/traces.ex`: `get_trace/2`, `find_traces_for_recipient/2`, and `find_traces_by_correlation_id/2` return closed literal nested maps with the interface fields declared above.
- Changed direct consumers: `ChimewayAdmin.Live.TraceSearchLive` closed-map flatteners and the Sigra `delivery_attempted?/1` test helper.
- Changed executable evidence: `test/chimeway/traces_test.exs`, `test/chimeway/privacy_boundary_test.exs`, `test/chimeway/runtime_prefix_integration_test.exs`, `test/chimeway/tenant_identity_test.exs`, `chimeway_admin/test/chimeway_admin/live/privacy_leak_live_test.exs`, and `test/chimeway/integrations/sigra_auth_lifecycle_test.exs` cover closed shapes, raw sentinels, root tenant ownership, static-prefix behavior, rendered output, and integration compatibility.
- Changed generated fixture functions: `notifier_ex/0` and `mailglass_notifier_ex/0` use explicit `cw_` recipient refs; Mailglass rendering carries the real address only in transient `"to"` assigns.
- Changed adoption evidence: `test/chimeway/release_gate_contract_test.exs` binds the focused Core and complete adoption-path commands to zero exit and exact proof records.
- New files, migrations, packages, external APIs, environment variables, and CLI flags: none.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Explain delivery behavior without retaining or emitting sensitive endpoint, credential, identity, or content data | 98-14 Tasks 1–3 | COVERED | Public traces and executable adoption proof both become usable through closed evidence. |
| REQ | PRIV-03 | Recursive case-normalized redaction before diagnostic projection | 98-14 Task 1 | COVERED | Raw nested legacy sentinels exercise all public Traces projections; adjacency/empty/ordering probes remain flagged assumptions. |
| REQ | PRIV-04 | Only opaque references, classifications, and allowlisted facts enter storage/diagnostics | 98-14 Tasks 1 and 3 | COVERED | Raw trace schemas are removed and proof notifiers use explicit opaque refs plus transient host render context. |
| RESEARCH | — | Core projections use DTOs/closed maps instead of raw Ecto schema serialization | 98-14 Tasks 1 and 2 | COVERED | Named SafeEvidence constructors own the nested public shape and direct consumers use it. |
| RESEARCH | — | Host supplies pre-opaque bounded recipient references without Chimeway derivation | 98-14 Task 3 | COVERED | Core and Mailglass generated notifiers supply deterministic `cw_` refs unchanged. |
| RESEARCH | — | No new package; use existing Elixir/Ecto/SafeEvidence stack | 98-14 Tasks 1–3 | COVERED | No dependency operation is planned. |
| CONTEXT | D-01, D-02, D-03 | One recursive, atom-safe boundary before every diagnostic projection | 98-14 Task 1 | COVERED | Closed constructors compose the existing boundary and adversarial nested fixtures prove the public paths. |
| CONTEXT | D-04, D-05, D-06 | Prohibit raw data and generic diagnostic blobs; retain explicit allowlisted evidence | 98-14 Tasks 1 and 3 | COVERED | Raw schemas/provider bodies are not returned, and real Mailglass address custody stays transient. |
| CONTEXT | D-07, D-08, D-09 | Shared safe projection vocabulary preserves useful explanation independently of Admin | 98-14 Tasks 1–3 | COVERED | Core maps are safe first; consumers and proof retain positive facts without view-only repair. |
| CONTEXT | Deferred Ideas | None | — | EXCLUDED | No deferred Phase 98 idea exists. |

No source item is missing. The PRIV-03 adjacency, empty, and ordering rows and PRIV-04 unclassified row remain explicit flagged-unverified assumptions. Descriptor-less product prohibitions retain the public-schema, transient-address, and tenant/explainability safety constraints without fabricating check descriptors; generic OWASP concerns remain in the ASVS L1 threat register.

<verification>
Run the focused closed-trace suites, the Admin and Sigra consumer gates, the tenant and runtime-prefix compatibility gates, the exact Phase 98 eleven-file regression matrix, both required adoption commands, and the full local CI-parity gate. Accept only zero exits; no human verification or conversational UAT substitutes for executable evidence.

Commands:
- `env MIX_ENV=test mix test test/chimeway/traces_test.exs test/chimeway/privacy_boundary_test.exs --warnings-as-errors`
- `mix verify.admin`
- `env MIX_ENV=test mix test test/chimeway/integrations/sigra_auth_lifecycle_test.exs --warnings-as-errors`
- `env MIX_ENV=test mix test test/chimeway/privacy_test.exs test/chimeway/privacy_boundary_test.exs test/chimeway/dispatch/oban_worker_test.exs test/chimeway/trigger_sanitization_test.exs test/chimeway/inbox_query_test.exs test/chimeway/inbox_state_transition_test.exs test/chimeway/workflows_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/dispatch/executor_mailglass_adapter_test.exs test/chimeway/migration_contract_test.exs test/chimeway/release_gate_contract_test.exs --warnings-as-errors`
- `mix verify.adoption_paths --only core`
- `mix verify.adoption_paths`
- `mix verify.runtime_prefix`
- `mix ci`
</verification>

<success_criteria>
- All three public raw-schema trace APIs return SafeEvidence-backed closed maps and preserve useful tenant-scoped lifecycle behavior.
- A real legacy/raw sentinel lifecycle cannot expose its sensitive values through any public trace query or required consumer.
- Generated Core and Mailglass notifiers pass the strict opaque-recipient boundary, and the real address remains transient host render context only.
- Focused trace, Admin, Sigra, tenant identity, runtime-prefix, exact Phase 98 matrix, focused Core adoption, complete adoption, and full local CI-parity (`mix ci`) gates all exit zero.
- PRIV-03 and PRIV-04 have machine-readable gap-closure evidence with no human checkpoint or UAT request.
</success_criteria>

<output>
Create `.planning/phases/98-privacy-safe-delivery-evidence/98-14-SUMMARY.md` when done.
</output>
