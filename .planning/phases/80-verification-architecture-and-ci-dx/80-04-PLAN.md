---
phase: 80-verification-architecture-and-ci-dx
plan: 04
type: execute
wave: 4
depends_on: [80-03]
files_modified:
  - CONTRIBUTING.md
  - MAINTAINING.md
autonomous: false
requirements: [CI-01, CI-02, CI-03, CI-04]
user_setup:
  - service: github-branch-protection
    why: "D-08: PR-required check must be swapped from ci-gate to pr-gate; code cannot enforce branch protection."
    dashboard_config:
      - task: "For branch main, remove ci-gate as a required status check for PRs and add pr-gate as required"
        location: "GitHub → repo Settings → Branches → Branch protection rule for main → Require status checks"
must_haves:
  truths:
    - "CONTRIBUTING.md tells contributors the fast `pr-gate` mirrors local `mix ci`, and points to `scripts/ci/*.sh` for locally reproducing complex gate behavior."
    - "MAINTAINING.md documents the `pr-gate` vs `ci-gate` split and that `ci-gate` remains the release/publish/automerge/recovery source of truth."
    - "MAINTAINING.md documents the D-08 branch-protection required-check swap (`ci-gate`→`pr-gate` for PRs) as an explicit operator step."
    - "The D-08 branch-protection swap is surfaced as an execution-time operator action, not silently assumed."
    - "The MAINTAINING Sigra sibling-checkout ref is corrected to match the ci.yml pin (D-13 opportunistic fix)."
  artifacts:
    - "CONTRIBUTING.md — pr-gate fast-feedback story + scripts/ci local-repro note + canonical clone URL"
    - "MAINTAINING.md — pr-gate/ci-gate split, D-08 operator swap step, scripts/ci note, corrected Sigra ref"
  key_links:
    - "MAINTAINING must NOT drop the existing 'All twelve must pass' copy or the twelve pre-ship `mix verify.*`/`mix ci*` commands (release_gate_contract_test.exs asserts them)."
    - "The D-08 operator swap must accompany the Plan 01 ci-gate PR-exemption, or PRs strand pending forever."
  prohibitions:
    - "MUST NOT remove or reword away the twelve pre-ship commands, the storage-prefix gate copy, or the `ACCRUE_PATH`/`THREADLINE_PATH`/`SIGRA_PATH` sibling-checkout docs (contract-asserted)."
    - "MUST NOT modify release/publish/automerge workflows or any code (docs-only plan)."
    - "MUST NOT weaken any release_gate_contract_test.exs assertion."
---

<objective>
Align contributor/maintainer gate docs with the new topology (D-13): CONTRIBUTING.md gets the fast-`pr-gate`-≈-`mix ci` story and a pointer to `scripts/ci/*.sh`; MAINTAINING.md documents the `pr-gate`/`ci-gate` split, `ci-gate` as the release/publish/automerge/recovery source of truth, the local-reproducibility scripts, and — critically — the D-08 branch-protection required-check swap as an explicit operator step. Surface the D-08 swap as an execution-time operator action.

Purpose: Success criterion 5 (CI-04 doc tail + CI-01/02/03 language agreement) and the anti-pending-trap precondition. Depends on Plan 03 (docs must reference the final scripts and topology). Docs-only; the one non-code action is the operator branch-protection swap.
Output: Updated CONTRIBUTING.md and MAINTAINING.md; operator swap surfaced.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-verification-architecture-and-ci-dx/80-CONTEXT.md
@.planning/phases/80-verification-architecture-and-ci-dx/80-RESEARCH.md

@CONTRIBUTING.md
@MAINTAINING.md
@test/chimeway/release_gate_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CONTRIBUTING.md with the pr-gate fast-feedback story</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - `CONTRIBUTING.md` — "CI Entrypoints" table (44-47); the clone URL line (9, currently `github.com/jonlunsford/chimeway`).
    - CONTEXT D-13 (CONTRIBUTING: local `mix ci` ≈ fast `pr-gate`), D-09 (scripts/ci).
    - RESEARCH "Docs to update (D-13)".
  </read_first>
  <action>
    In CONTRIBUTING.md: add a short subsection (or CI Entrypoints table rows) explaining that on a pull request the required check is the fast `pr-gate` aggregate (lint + test + release-gate contract + docs build) which mirrors what local `mix ci` covers, while the full `ci-gate` (all lanes incl. ecosystem integrations) runs on push-to-main / release dispatch. Add a row/line pointing contributors to `scripts/ci/*.sh` (`detect-installer-changes.sh`, `aggregate-gate.sh`, `sigra-proof.sh`) as the locally-runnable form of the complex CI fragments (CI-04). Opportunistically fix the clone URL on line 9 from the legacy `github.com/jonlunsford/chimeway` to the canonical `github.com/szTheory/chimeway`. Do not touch any workflow or code.
  </action>
  <verify>
    <automated>grep -q 'pr-gate' CONTRIBUTING.md &amp;&amp; grep -q 'scripts/ci/' CONTRIBUTING.md &amp;&amp; grep -q 'github.com/szTheory/chimeway' CONTRIBUTING.md &amp;&amp; ! grep -q 'jonlunsford' CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - CONTRIBUTING.md names `pr-gate` as the fast required PR check and relates it to local `mix ci`.
    - CONTRIBUTING.md points to `scripts/ci/*.sh` for local reproduction.
    - The clone URL uses `github.com/szTheory/chimeway`; no `jonlunsford` reference remains.
  </acceptance_criteria>
  <done>Contributor docs describe the pr-gate fast story and local scripts; canonical clone URL used.</done>
</task>

<task type="auto">
  <name>Task 2: Update MAINTAINING.md with the split, D-08 operator swap, and Sigra ref fix</name>
  <files>MAINTAINING.md</files>
  <read_first>
    - `MAINTAINING.md` — Release Runbook (5-14), Pre-ship local commands block (46-80, note "All twelve must pass" at 78 and the mapping note at 80), Sibling repo checkouts (82-90, Sigra ref `b186f03...` at line 88), installer gating note (106).
    - `test/chimeway/release_gate_contract_test.exs` asserts MAINTAINING contains the twelve pre-ship `mix verify.*`/`mix ci*` commands, "All twelve must pass", storage-prefix gate copy, and `ACCRUE_PATH`/`THREADLINE_PATH`/`SIGRA_PATH` — these MUST remain.
    - CONTEXT D-03/D-08 (ci-gate PR-exempt; operator swaps required check), D-13, D-09; RESEARCH "Pitfall 1" and "Runtime State Inventory".
  </read_first>
  <action>
    In MAINTAINING.md, ADD (do not remove existing contract-asserted copy):
    1. A subsection documenting the `pr-gate` vs `ci-gate` split: `pr-gate` is the fast required check on contributor PRs (fast subset); `ci-gate` remains the source of truth for release, publish, automerge, and recovery and runs on push-to-main + `workflow_dispatch` (release PRs receive it via dispatch).
    2. An explicit, prominent operator step titled for the branch-protection required-check swap: for branch `main`, remove `ci-gate` as a required status check for PRs and add `pr-gate`. State the hazard verbatim in intent: because `ci-gate` is now skipped on `pull_request`, if branch protection still requires `ci-gate` every PR strands in "Waiting for status to be reported"; this swap must accompany the topology change. Note code cannot enforce this (GitHub settings).
    3. A note that complex CI behavior is reproducible locally via `scripts/ci/*.sh`.
    4. Correct the Sigra sibling-checkout ref (line 88) from `b186f03ccc5bbc9416f495df3e5dd0bec2f814a4` to the ci.yml-pinned `62ceb46a38c4e617f6c06d874ecb12e1ab19d97c` (D-13 opportunistic drift fix).
    5. Reconcile the `install_golden_contract` gating description (line ~106, "CI runs `install_golden_contract` on every push to main and on PRs that touch installer surfaces (path-gated). Do not change that gating behavior."). Plan 01 event-guards `install_golden_contract` off `pull_request` per D-04, so it now runs on push-to-main + `workflow_dispatch` only — NOT on PRs. Update this sentence to state the push+dispatch-only behavior and remove the now-false "on PRs that touch installer surfaces" claim and the "Do not change that gating behavior" directive. MUST preserve the contract-asserted substring `path-gated` somewhere in the storage-gate copy (describe the detect step's file-diff logic as path-gated step logic) so the existing "MAINTAINING documents storage-prefix gate responsibilities" assertion (which greps `path-gated`, `mix verify.install_golden`, `mix ci.install_golden`) stays green.
    Keep "All twelve must pass", the twelve pre-ship commands, storage-prefix copy (including the `path-gated`, `mix verify.install_golden`, `mix ci.install_golden` strings), and the three `*_PATH` sibling-checkout entries intact. Do not touch workflows or code.
  </action>
  <verify>
    <automated>grep -q 'pr-gate' MAINTAINING.md &amp;&amp; grep -q 'ci-gate' MAINTAINING.md &amp;&amp; grep -q '62ceb46a38c4e617f6c06d874ecb12e1ab19d97c' MAINTAINING.md &amp;&amp; ! grep -q 'b186f03ccc5bbc9416f495df3e5dd0bec2f814a4' MAINTAINING.md &amp;&amp; grep -q 'All twelve must pass' MAINTAINING.md &amp;&amp; mix ci.verify_gates</automated>
  </verify>
  <acceptance_criteria>
    - MAINTAINING.md documents the `pr-gate`/`ci-gate` split and ci-gate's release/publish/automerge/recovery role.
    - MAINTAINING.md contains an explicit operator step to swap the PR required check `ci-gate`→`pr-gate`, with the pending-trap hazard stated.
    - MAINTAINING.md points to `scripts/ci/*.sh` for local reproduction.
    - Sigra ref is `62ceb46a...`; the stale `b186f03...` is gone.
    - The `install_golden_contract` gating description reflects push+dispatch-only (D-04); the false "on PRs" claim and "Do not change that gating behavior" directive are removed; the contract-asserted `path-gated` / `mix verify.install_golden` / `mix ci.install_golden` strings remain present.
    - "All twelve must pass", the twelve pre-ship commands, and the three `*_PATH` entries remain; `mix ci.verify_gates` passes.
  </acceptance_criteria>
  <done>Maintainer docs document the split, the D-08 operator swap, and local scripts; existing contract-asserted copy preserved; suite green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator swaps the PR-required check ci-gate → pr-gate (D-08)</name>
  <action>Surface the D-08 branch-protection swap to the operator and pause for confirmation. This is a non-code, out-of-repo GitHub settings change (code cannot enforce branch protection); the executor does not edit files here — it presents the operator step below and waits for the resume signal.</action>
  <what-built>Docs now instruct the operator to swap the PR-required check. The actual branch-protection change lives in GitHub repo settings and cannot be made from the repo tree.</what-built>
  <how-to-verify>
    1. Open GitHub → repo Settings → Branches → the branch protection rule for `main`.
    2. Under "Require status checks to pass before merging", REMOVE `ci-gate` from the required checks for PRs and ADD `pr-gate`.
    3. Open (or re-run) a test PR that touches only non-installer files and confirm `pr-gate` reports a conclusion and the PR is no longer stuck in "Expected — Waiting for status to be reported".
    4. Confirm release/publish/automerge flows (which poll `ci-gate` on push/dispatch) are unaffected.
  </how-to-verify>
  <resume-signal>Type "swapped" once pr-gate is the required PR check (or "defer" to keep ci-gate temporarily required — note that ci-gate is PR-exempt so PRs will strand until swapped).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Branch protection (GitHub settings) → merge | Out-of-repo required-check config; the wrong required check name strands or under-gates PRs. |
| Docs → maintainer action | Maintainer relies on MAINTAINING.md for the correct release/recovery + branch-protection procedure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-80-10 | Denial of Service | required-check pending trap (D-08 not performed) | high | mitigate | MAINTAINING documents the swap as an explicit operator step with the hazard stated; a blocking human-action checkpoint surfaces it at execution time so it is not silently skipped. |
| T-80-11 | Repudiation | maintainer docs drift from actual gate topology | medium | mitigate | Docs updated in lockstep with the topology; contract-asserted pre-ship copy preserved so `mix ci.verify_gates` still guards the doc surface. |
| T-80-12 | Tampering | under-gating by requiring only a weakened check | high | accept | The operator sets `pr-gate` (fast subset) as the PR check by design; release confidence stays on `ci-gate` (push/dispatch). Rationale: fast PR feedback with full gate at release is the accepted CI-01/CI-02 trade-off. |
| T-80-SC | Tampering | npm/pip/cargo installs | low | accept | Docs-only plan; no installs, no workflow or code changes. |
</threat_model>

<verification>
- `mix ci.verify_gates` passes (MAINTAINING contract-asserted copy intact).
- Manual read: CONTRIBUTING + MAINTAINING describe pr-gate/ci-gate split, scripts/ci, D-08 swap; Sigra ref corrected.
- Operator checkpoint acknowledged (swap performed or explicitly deferred).
</verification>

<success_criteria>
- CI-01/CI-02/CI-03: contributor and maintainer gate docs agree with the two-aggregate topology and the branch-protection swap.
- CI-04: local reproducibility (scripts/ci) documented.
- `mix ci.verify_gates` green; D-08 operator swap surfaced.
</success_criteria>

## Artifacts this phase produces (this plan)

- CONTRIBUTING.md: `pr-gate` fast-feedback story, `scripts/ci/*.sh` local-repro pointer, canonical clone URL.
- MAINTAINING.md: `pr-gate`/`ci-gate` split, D-08 branch-protection operator swap step, `scripts/ci` note, corrected Sigra ref `62ceb46a...`.
- Execution-time operator action surfaced: branch-protection required-check swap `ci-gate`→`pr-gate` for PRs into `main`.

<output>
Create `.planning/phases/80-verification-architecture-and-ci-dx/80-04-SUMMARY.md` when done.
</output>
