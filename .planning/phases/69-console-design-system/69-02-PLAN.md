---
phase: 69-console-design-system
plan: 02
type: execute
wave: 2
depends_on: [69-01]
files_modified:
  - chimeway_admin/priv/static/chimeway_admin.css
  - chimeway_admin/test/chimeway_admin/design_system_test.exs
  - chimeway_admin/test/chimeway_admin/live/design_system_live_test.exs
  - .planning/phases/69-console-design-system/69-EVIDENCE.md
autonomous: true
requirements: [DES-01, DES-03, DES-04]
user_setup: []
must_haves:
  truths:
    - "DES-03: all seven admin pages render inside the scoped shell with stable shared layout hooks."
    - "DES-03: shared primitives handle mobile and desktop layout without overlapping long IDs, table cells, nav labels, forms, rows, summaries, or metric grids."
    - "DES-04: hover, active, and focus behavior uses non-blocking CSS transitions and reduced-motion-safe overrides."
    - "D-10: screenshot-ready mobile and desktop evidence exists without adding the Phase 72 browser-smoke or mix verify.admin gate."
  artifacts:
    - path: "chimeway_admin/test/chimeway_admin/live/design_system_live_test.exs"
      provides: "rendered LiveView shell and shared hook contracts"
      contains: "Floki.parse_document!"
    - path: "chimeway_admin/priv/static/chimeway_admin.css"
      provides: "responsive shared primitive layout and motion rules"
      contains: "@media (max-width: 900px)"
    - path: ".planning/phases/69-console-design-system/69-EVIDENCE.md"
      provides: "screenshot-ready evidence checklist and verification log for Phase 69"
      contains: "390px"
  key_links:
    - from: "ChimewayAdmin.Components.Layout.admin_shell/1"
      to: "chimeway_admin/priv/static/chimeway_admin.css"
      via: "shared `.chimeway-admin`, `.cw-shell`, `.cw-sidebar`, `.cw-main`, `.cw-page-header`, and `.cw-nav` hooks"
      pattern: "class=\"chimeway-admin\""
    - from: "seven LiveViews"
      to: "responsive shared primitives"
      via: "rendered `.cw-search-form`, `.cw-table-wrap`, `.cw-summary-list`, `.cw-copy-id`, `.cw-metric-grid`, `.cw-row`, and `.cw-grid--two` hooks"
      pattern: "cw-(search-form|table-wrap|summary-list|copy-id|metric-grid|row|grid--two)"
---

<objective>
Harden responsive shared primitives, rendered shell contracts, and screenshot-ready evidence for the seven-page admin console.

Purpose: DES-03 requires usable mobile and desktop layouts across the core operator flows; D-06/D-07 require fixing shared primitives before page-specific patches.
Output: LiveView structure tests, responsive CSS hardening, and Phase 69 evidence notes that do not create the Phase 72 browser-smoke gate.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/68-admin-truth-alignment/68-01-SUMMARY.md
@.planning/phases/68-admin-truth-alignment/68-02-SUMMARY.md
@.planning/phases/69-console-design-system/69-CONTEXT.md
@.planning/phases/69-console-design-system/69-RESEARCH.md
@.planning/phases/69-console-design-system/69-VALIDATION.md
@.planning/phases/69-console-design-system/69-PATTERNS.md
@.planning/phases/69-console-design-system/69-UI-SPEC.md
@chimeway_admin/priv/static/chimeway_admin.css
@chimeway_admin/test/chimeway_admin/design_system_test.exs
@chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs
@chimeway_admin/test/support/live_view_case.ex
@chimeway_admin/lib/chimeway_admin/components/layout.ex
@chimeway_admin/lib/chimeway_admin/components/core.ex
@chimeway_admin/lib/chimeway_admin/components/status.ex
@chimeway_admin/lib/chimeway_admin/components/timeline_event.ex
@chimeway_admin/lib/chimeway_admin/live/dashboard_live.ex
@chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex
@chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex
@chimeway_admin/lib/chimeway_admin/live/feed_live.ex
@chimeway_admin/lib/chimeway_admin/live/definitions_live.ex
@chimeway_admin/lib/chimeway_admin/live/health_live.ex
@chimeway_admin/lib/chimeway_admin/live/recovery_live.ex
</context>

<interfaces>
LiveView mount and auth actions for rendered contracts:
- Dashboard: `ChimewayAdmin.Live.DashboardLive`, on_mount `{ChimewayAdmin.LiveAuth, :search_traces}`, title `Command Center`.
- Trace Lookup: `ChimewayAdmin.Live.TraceSearchLive`, on_mount `{ChimewayAdmin.LiveAuth, :search_traces}`, title `Trace Lookup`, form id `trace-search-form`.
- Trace Detail: `ChimewayAdmin.Live.TraceDetailLive`, on_mount `{ChimewayAdmin.LiveAuth, :view_trace}`, params `%{"delivery_id" => "00000000-0000-0000-0000-000000000000"}`, title `Trace not found` for an unseeded isolated mount.
- Feed Debug: `ChimewayAdmin.Live.FeedLive`, on_mount `{ChimewayAdmin.LiveAuth, :view_feed}`, form id `feed-search-form`.
- Definitions: `ChimewayAdmin.Live.DefinitionsLive`, on_mount `{ChimewayAdmin.LiveAuth, :view_definitions}`.
- Health: `ChimewayAdmin.Live.HealthLive`, on_mount `{ChimewayAdmin.LiveAuth, :view_health}`.
- Recovery: `ChimewayAdmin.Live.RecoveryLive`, on_mount `{ChimewayAdmin.LiveAuth, :list_recovery_candidates}`.

Shared responsive hooks:
- Shell/navigation: `.chimeway-admin[data-cw-theme]`, `.cw-shell`, `.cw-sidebar`, `.cw-nav`, `.cw-nav__item`, `.cw-main`, `.cw-page-header`, `.cw-page-actions`.
- Forms/controls: `.cw-search-form`, `.cw-field`, `.cw-input`, `.cw-select`, `.cw-button`.
- Data and long content: `.cw-row`, `.cw-row-link`, `.cw-table-wrap`, `.cw-table`, `.cw-summary-list`, `.cw-copy-id`, `.cw-detail-hero__ids`, `.cw-timeline__details`.
- Dense summaries: `.cw-metric-grid`, `.cw-grid--two`, `.cw-section-heading`, `.cw-definition-strip`, `.cw-definition-chip`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add rendered LiveView contracts for the shared admin shell</name>
  <files>chimeway_admin/test/chimeway_admin/live/design_system_live_test.exs</files>
  <read_first>
    - chimeway_admin/test/chimeway_admin/live/design_system_live_test.exs
    - chimeway_admin/test/chimeway_admin/live/trace_search_live_test.exs
    - chimeway_admin/test/support/live_view_case.ex
    - chimeway_admin/lib/chimeway_admin/components/layout.ex
    - chimeway_admin/lib/chimeway_admin/components/core.ex
    - chimeway_admin/lib/chimeway_admin/components/status.ex
    - chimeway_admin/lib/chimeway_admin/components/timeline_event.ex
    - chimeway_admin/lib/chimeway_admin/router.ex
    - chimeway_admin/lib/chimeway_admin/live/dashboard_live.ex
    - chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex
    - chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex
    - chimeway_admin/lib/chimeway_admin/live/feed_live.ex
    - chimeway_admin/lib/chimeway_admin/live/definitions_live.ex
    - chimeway_admin/lib/chimeway_admin/live/health_live.ex
    - chimeway_admin/lib/chimeway_admin/live/recovery_live.ex
    - .planning/phases/69-console-design-system/69-PATTERNS.md
    - .planning/phases/69-console-design-system/69-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: every core LiveView mount renders one `.chimeway-admin[data-cw-theme="system"]` root, `.cw-shell`, `.cw-sidebar[aria-label="Chimeway admin"]`, `.cw-nav[aria-label="Admin sections"]`, `.cw-main`, and `.cw-page-header`.
    - Test 2: active pages expose one `.cw-nav__item[aria-current="page"]` and all sidebar labels: `Command Center`, `Trace Lookup`, `Feed Debug`, `Definitions`, `Health`, and `Recovery`.
    - Test 3: shared flow hooks are present where each page owns them: `#trace-search-form.cw-search-form`, `#feed-search-form.cw-search-form`, `.cw-metric-grid`, `.cw-grid--two`, `.cw-table-wrap .cw-table`, `.cw-summary-list`, `.cw-copy-id`, and `.cw-list`.
  </behavior>
  <action>
    Create `chimeway_admin/test/chimeway_admin/live/design_system_live_test.exs` with module `ChimewayAdmin.Live.DesignSystemLiveTest`, `use ChimewayAdmin.LiveViewCase, async: true`, `import Phoenix.LiveViewTest`, and `Floki.parse_document!/1` for selector assertions.

    Mount the seven pages listed in the `<interfaces>` block. For `TraceDetailLive`, mount with params `%{"delivery_id" => "00000000-0000-0000-0000-000000000000"}` and on_mount `{ChimewayAdmin.LiveAuth, :view_trace}`; assert the scoped shell and not-found state, and use a source assertion against `chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex` for `.cw-detail-hero`, `.cw-detail-hero__ids`, `.cw-summary-list`, and `.cw-copy-id` because the unseeded isolated mount intentionally renders the not-found branch.

    Keep the tests structural. Do not seed deliveries, alter authorization modules, call recovery APIs, add browser tooling, or modify LiveView event handlers in this task. This implements D-10/D-11 with ExUnit/LiveViewTest rather than the Phase 72 gate.
  </action>
  <verify>
    <automated>cd chimeway_admin && mix test test/chimeway_admin/live/design_system_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `chimeway_admin/test/chimeway_admin/live/design_system_live_test.exs` exists and uses `ChimewayAdmin.LiveViewCase`.
    - The test file calls `Floki.parse_document!`.
    - The tests mount Dashboard, Trace Search, Trace Detail, Feed, Definitions, Health, and Recovery LiveViews with the auth actions named in `<interfaces>`.
    - The tests assert `.chimeway-admin[data-cw-theme="system"]`, `.cw-shell`, `.cw-sidebar`, `.cw-nav`, `.cw-main`, and `.cw-page-header`.
    - The tests assert `#trace-search-form`, `#feed-search-form`, `.cw-table-wrap .cw-table`, `.cw-metric-grid`, `.cw-summary-list`, and `.cw-copy-id` through rendered HTML or explicit source assertions.
    - `cd chimeway_admin && mix test test/chimeway_admin/live/design_system_live_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>DES-03 rendered shell and shared hook contracts cover the seven-page console without browser-gate infrastructure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden responsive shared CSS primitives and motion safety</name>
  <files>chimeway_admin/priv/static/chimeway_admin.css, chimeway_admin/test/chimeway_admin/design_system_test.exs</files>
  <read_first>
    - chimeway_admin/priv/static/chimeway_admin.css
    - chimeway_admin/test/chimeway_admin/design_system_test.exs
    - chimeway_admin/test/chimeway_admin/live/design_system_live_test.exs
    - chimeway_admin/lib/chimeway_admin/components/layout.ex
    - chimeway_admin/lib/chimeway_admin/components/core.ex
    - chimeway_admin/lib/chimeway_admin/components/status.ex
    - chimeway_admin/lib/chimeway_admin/components/timeline_event.ex
    - chimeway_admin/lib/chimeway_admin/live/dashboard_live.ex
    - chimeway_admin/lib/chimeway_admin/live/trace_search_live.ex
    - chimeway_admin/lib/chimeway_admin/live/trace_detail_live.ex
    - chimeway_admin/lib/chimeway_admin/live/feed_live.ex
    - chimeway_admin/lib/chimeway_admin/live/definitions_live.ex
    - chimeway_admin/lib/chimeway_admin/live/health_live.ex
    - chimeway_admin/lib/chimeway_admin/live/recovery_live.ex
    - .planning/phases/69-console-design-system/69-CONTEXT.md
    - .planning/phases/69-console-design-system/69-VALIDATION.md
    - .planning/phases/69-console-design-system/69-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: CSS contract asserts responsive hooks for `@media (max-width: 900px)`, `.cw-search-form`, `.cw-summary-list`, `.cw-table-wrap`, `.cw-copy-id`, `.cw-row`, `.cw-row-link`, `.cw-metric-grid`, `.cw-page-header`, and `.cw-nav__item`.
    - Test 2: CSS contract asserts long-content protections: `min-width: 0`, `overflow-wrap: anywhere`, intentional table `overflow-x: auto`, and stable `min-height: 40px` or `min-height: 44px` for controls/navigation.
    - Test 3: CSS contract asserts shared components consume tokenized spacing, typography, radius, focus, and motion tokens instead of viewport-scaled font sizes in shared headings.
  </behavior>
  <action>
    Extend `ChimewayAdmin.DesignSystemTest` with responsive source assertions for D-06/D-07/D-08. The tests must require these strings in CSS: `@media (max-width: 900px)`, `grid-template-columns: 1fr`, `overflow-x: auto`, `overflow-wrap: anywhere`, `min-width: 0`, `min-height: 40px`, `min-height: 44px`, `--cw-motion-fast`, and `--cw-motion-pressed`.

    Update `chimeway_admin/priv/static/chimeway_admin.css` so shared primitives use the token scale from Plan 01. Replace shared heading viewport scaling with fixed design tokens: root body uses `font-size: var(--cw-font-size-body)` and `line-height: var(--cw-line-height-body)`; `h1` uses `--cw-font-size-display` and `--cw-line-height-display`; `h2` uses `--cw-font-size-heading` and `--cw-line-height-heading`; label-like text uses `--cw-font-size-label` and `--cw-line-height-label`.

    Implement D-06/D-07 responsive protections on shared selectors, not page-specific one-offs: `.cw-shell`, `.cw-sidebar`, `.cw-main`, `.cw-page-header`, `.cw-page-actions`, `.cw-nav`, `.cw-nav__item`, `.cw-search-form`, `.cw-field`, `.cw-button`, `.cw-input`, `.cw-select`, `.cw-row`, `.cw-row-link`, `.cw-table-wrap`, `.cw-table`, `.cw-summary-list`, `.cw-timeline__details`, `.cw-detail-hero`, `.cw-detail-hero__ids`, `.cw-copy-id`, `.cw-metric-grid`, `.cw-grid--two`, `.cw-section-heading`, `.cw-definition-strip`, and `.cw-definition-chip`.

    Apply these concrete responsive rules: controls `.cw-button`, `.cw-input`, and `.cw-select` have `min-height: 40px`; mobile nav/actions use `min-height: 44px`; direct text containers inside rows, row links, section headings, page headers, definition chips, copyable IDs, tables, summaries, and timeline details have `min-width: 0` and `overflow-wrap: anywhere`; `.cw-table-wrap` keeps intentional `overflow-x: auto`; `.cw-search-form` becomes one column below 900px; `.cw-grid--two` and `.cw-metric-grid` become one column below 900px; `.cw-summary-list` and `.cw-timeline__details` become one column below 640px; `.cw-nav` becomes two columns below 900px and one column below 520px; `.cw-detail-hero__ids` aligns left below 900px.

    Preserve D-08 motion posture: hover/focus states must not change layout dimensions, keyboard focus must use `:focus-visible` outline with `--cw-focus-ring` and `--cw-focus-offset`, and active pressed feedback must use `--cw-motion-pressed`. Do not modify `LiveAuth.ensure_authorized/3`, `handle_event/3` authorization checks, recovery API calls, redaction helpers, DTO read models, or router mount behavior.
  </action>
  <verify>
    <automated>cd chimeway_admin && mix test test/chimeway_admin/design_system_test.exs test/chimeway_admin/live/design_system_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `chimeway_admin/priv/static/chimeway_admin.css` contains `@media (max-width: 900px)` and `@media (max-width: 640px)`.
    - `.cw-button`, `.cw-input`, and `.cw-select` have `min-height: 40px`; mobile nav/actions have `min-height: 44px`.
    - `.cw-search-form` uses one column below 900px.
    - `.cw-summary-list` and `.cw-timeline__details` use one column below 640px.
    - `.cw-table-wrap` retains `overflow-x: auto`.
    - Long-content selectors for rows, tables, copyable IDs, summaries, and timeline details include `min-width: 0` and `overflow-wrap: anywhere`.
    - `cd chimeway_admin && mix test test/chimeway_admin/design_system_test.exs test/chimeway_admin/live/design_system_live_test.exs --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>DES-03 shared responsive primitives and DES-04 non-blocking motion behavior are implemented and contract-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Record screenshot-ready evidence without adding a browser gate</name>
  <files>.planning/phases/69-console-design-system/69-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/69-console-design-system/69-EVIDENCE.md
    - .planning/phases/69-console-design-system/69-CONTEXT.md
    - .planning/phases/69-console-design-system/69-VALIDATION.md
    - .planning/phases/69-console-design-system/69-UI-SPEC.md
    - .planning/phases/68-admin-truth-alignment/68-02-SUMMARY.md
    - examples/chimeway_demo_host/lib/demo_host_web/endpoint.ex
    - examples/chimeway_demo_host/lib/demo_host_web/layouts/root.html.heex
    - chimeway_admin/lib/chimeway_admin/assets.ex
  </read_first>
  <action>
    Create `.planning/phases/69-console-design-system/69-EVIDENCE.md` as a lightweight evidence log for D-10/D-11. Record the automated commands run in this plan and their pass/fail result. Include the exact focused command `cd chimeway_admin && mix test test/chimeway_admin/design_system_test.exs test/chimeway_admin/live/design_system_live_test.exs --warnings-as-errors` and the package command `cd chimeway_admin && mix test --warnings-as-errors`.

    Add a screenshot-ready checklist with these exact viewport rows: `Mobile 390px` and `Desktop 1280px`. For Mobile 390px include Command Center, Trace Lookup form/results, Trace Detail hero IDs/summary list, and Recovery two-panel flow stacked. For Desktop 1280px include sidebar navigation, metric grid, two-column cards, trace table/list, and recovery confirmation.

    State explicitly that Phase 69 does not add `.github/workflows/*`, `mix verify.admin`, Playwright, Wallaby, or a durable browser-smoke gate. Do not commit binary screenshots unless an existing project convention is discovered in a file already read for this task.
  </action>
  <verify>
    <automated>cd chimeway_admin && mix test test/chimeway_admin/design_system_test.exs test/chimeway_admin/live/design_system_live_test.exs --warnings-as-errors && mix test --warnings-as-errors && test -f ../.planning/phases/69-console-design-system/69-EVIDENCE.md && grep -q "Mobile 390px" ../.planning/phases/69-console-design-system/69-EVIDENCE.md && grep -q "Desktop 1280px" ../.planning/phases/69-console-design-system/69-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/69-console-design-system/69-EVIDENCE.md` contains `Mobile 390px` and `Desktop 1280px`.
    - `.planning/phases/69-console-design-system/69-EVIDENCE.md` names Command Center, Trace Lookup, Trace Detail, Feed Debug, Definitions, Health, and Recovery.
    - `.planning/phases/69-console-design-system/69-EVIDENCE.md` contains the command `cd chimeway_admin && mix test test/chimeway_admin/design_system_test.exs test/chimeway_admin/live/design_system_live_test.exs --warnings-as-errors`.
    - `.planning/phases/69-console-design-system/69-EVIDENCE.md` states that Phase 69 does not add `mix verify.admin`, Playwright, Wallaby, or a durable browser-smoke gate.
    - `cd chimeway_admin && mix test --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Phase 69 has screenshot-ready evidence and green package tests without expanding into Phase 72 gate composition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView rendered HTML -> browser CSS | Server-rendered classes and attributes drive all responsive behavior in the packaged stylesheet. |
| Operator browser -> eventful LiveViews | Trace search and recovery include `phx-*` events; visual changes must not weaken authorization checks. |
| Admin read models -> rendered UI | Layout work must preserve existing redaction and DTO boundaries while making long values wrap safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-05 | Tampering | shared LiveView hooks | mitigate | `design_system_live_test.exs` asserts shell, nav, form, table, summary, copy-ID, metric, row, and grid hooks across all seven pages. |
| T-69-06 | Denial of Service | mobile layout and keyboard workflows | mitigate | Responsive CSS contracts require stable control heights, one-column mobile forms/summaries, safe table scroll wrappers, and reduced-motion-safe CSS. |
| T-69-07 | Information Disclosure | trace detail, feed, timeline, and recovery rendering | mitigate | Do not modify `ChimewayAdmin.Redaction`, DTO read models, or rendered data fields; only CSS/test/evidence files are modified in this plan. |
| T-69-08 | Elevation of Privilege | recovery and trace LiveView events | mitigate | Do not modify `LiveAuth.ensure_authorized/3`, recovery `handle_event/3` authorization checks, router live_session actions, or core recovery APIs. |
| T-69-SC | Tampering | package installation | accept | No package installs, dependency edits, Playwright/Wallaby setup, workflow files, or `mix verify.admin` gate changes are allowed in this plan. |
</threat_model>

<verification>
- `cd chimeway_admin && mix test test/chimeway_admin/design_system_test.exs test/chimeway_admin/live/design_system_live_test.exs --warnings-as-errors` exits 0.
- `cd chimeway_admin && mix test --warnings-as-errors` exits 0.
- `.planning/phases/69-console-design-system/69-EVIDENCE.md` contains `Mobile 390px`, `Desktop 1280px`, and the seven admin page labels.
- `git diff --name-only -- .github/workflows mix.exs chimeway_admin/mix.exs | wc -l | tr -d ' ' | grep -qx '0'` exits 0.
</verification>

<success_criteria>
- DES-03 shared layout primitives cover mobile and desktop operator flows without page-specific one-off patches.
- Rendered LiveView contracts prove all seven pages keep the scoped shell and shared design-system hooks.
- DES-04 motion remains CSS-only, tokenized, interruptible, and reduced-motion-safe.
- Phase 69 evidence is screenshot-ready while leaving `mix verify.admin`, CI parity, and durable browser smoke out of this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/69-console-design-system/69-02-SUMMARY.md`.
</output>
