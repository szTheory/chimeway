---
phase: 97-tenant-identity-compatible-upgrade
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/reconciliation.ex
  - test/chimeway/reconciliation_test.exs
  - priv/repo/migrations/20260812000001_make_chimeway_delivery_tenant_nullable.exs
  - priv/chimeway_migrations/033_make_chimeway_delivery_tenant_nullable.exs
  - test/chimeway/migration_contract_test.exs
  - test/chimeway/install/migrations_test.exs
autonomous: true
gap_closure: true
requirements: [TENANT-03]
must_haves:
  truths:
    - "D-08/D-09/TENANT-03: assign_event_tree/3 locks the Event, its Notifications, and all Deliveries beneath those Notifications in one PostgreSQL transaction before validating or changing ownership."
    - "D-09/TENANT-03: a conflicting non-NULL Delivery tenant returns {:error, :ownership_conflict} and rolls back every Event, Notification, and Delivery update."
    - "D-09/TENANT-03: NULL Delivery tenants are assigned the exact trimmed host-supplied tenant atomically with NULL Event and Notification tenants; matching non-NULL Delivery ownership is retained and never inferred."
    - "D-09/TENANT-03: reconciliation report and assignment results expose deterministic delivery counts and IDs without payload, recipient, provider, or other lifecycle details."
  assumptions:
    - "FLAGGED ASSUMPTION — TENANT-03 manual review was unclassified; the current VERIFICATION.md defines delivery locking, conflict rejection, atomic NULL assignment, and delivery counts as this closure's executable policy."
  artifacts:
    - path: "lib/chimeway/reconciliation.ex"
      provides: "Locked, delivery-complete lifecycle ownership reconciliation and versioned delivery evidence"
    - path: "test/chimeway/reconciliation_test.exs"
      provides: "Conflict rollback, NULL delivery assignment, report-count, and concurrency proof"
  key_links:
    - from: "Chimeway.Reconciliation.assign_event_tree/3"
      to: "chimeway_deliveries"
      via: "Delivery rows are selected through Notification.event_id and locked FOR UPDATE inside the existing Repo.transaction/1"
      pattern: "lock(\"FOR UPDATE\")"
    - from: "lib/chimeway/reconciliation.ex"
      to: "Event, Notification, Delivery tenant_id columns"
      via: "conflict validation precedes NULL-only update_all calls in the same transaction"
      pattern: "is_nil.*tenant_id"
  prohibitions:
    - "FLAGGED-UNVERIFIED — Reconciliation must not infer a tenant from an existing Delivery, recipient, workflow, actor, or storage prefix."
    - "FLAGGED-UNVERIFIED — A conflicting Delivery owner must not leave a partially assigned Event or Notification tree."
    - "FLAGGED-UNVERIFIED — Reconciliation output must not expose sensitive lifecycle details beyond durable IDs, NULL ownership, counts, status, schema version, and the explicit assignment instruction."
---

<objective>
Extend explicit legacy ownership reconciliation across the complete event → notification → delivery spine.

Purpose: Prevent the supported upgrade operation from creating or preserving a split-tenant lifecycle tree while retaining non-guessing host ownership.
Output: Delivery-aware locked assignment, deterministic delivery evidence/counts, and transactional adversarial tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-04-SUMMARY.md

<interfaces>
- `Chimeway.Reconciliation.report(opts \\ []) :: map()` returns schema-versioned JSON-safe evidence.
- `Chimeway.Reconciliation.assign_event_tree(event_id, tenant_id, opts \\ [])` returns `{:ok, map()}` or stable atom errors including `:ownership_conflict`.
- `Chimeway.Delivery` belongs to `Chimeway.Notifications.Notification`; Notification belongs to `Chimeway.Events.Event`.
- `Chimeway.Storage.repo_opts/1` remains the only static prefix seam; reconciliation removes tenant options before passing Repo options.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject a conflicting Delivery inside the locked assignment transaction</name>
  <read_first>
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
    - lib/chimeway/reconciliation.ex
    - test/chimeway/reconciliation_test.exs
    - lib/chimeway/delivery.ex
    - lib/chimeway/deliveries.ex
    - lib/chimeway/notifications/notification.ex
  </read_first>
  <files>lib/chimeway/reconciliation.ex, test/chimeway/reconciliation_test.exs</files>
  <behavior>
    - Test 1: a NULL Event/Notification tree with a tenant-b Delivery rejects assignment to tenant-a with `{:error, :ownership_conflict}`.
    - Test 2: after rejection, Event and Notification remain NULL and Delivery remains tenant-b.
    - Test 3: the conflict path locks the Delivery in the same transaction as its Event and Notification parents before performing any update.
  </behavior>
  <action>Start RED by building the exact verifier counterexample with real Event, Notification, and Delivery rows. Alias `Chimeway.Delivery`, and inside `assign_locked_event_tree/3` query every Delivery whose Notification belongs to the named Event, order deterministically by Delivery id, and apply `FOR UPDATE` using the same `repo_opts` and existing `Repo.transaction/1`. Perform all Event, Notification, and Delivery ownership checks after all three row sets are locked and before the first `update_all`. Per D-09, if any Delivery has a non-NULL tenant different from the normalized host-supplied tenant, call `Repo.rollback(:ownership_conflict)`. Preserve existing errors for missing, assigned, or notification-conflict cases and keep tenant identity out of storage-prefix options.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/reconciliation.ex test/chimeway/reconciliation_test.exs &amp;&amp; mix test test/chimeway/reconciliation_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - The focused test creates a NULL Event/Notification with a conflicting non-NULL Delivery and receives exactly `{:error, :ownership_conflict}`.
    - Post-call database assertions prove zero partial parent/child ownership updates.
    - `mix test test/chimeway/reconciliation_test.exs --warnings-as-errors` exits zero.
  </acceptance_criteria>
  <done>The supported assignment path detects a conflicting Delivery under lock and leaves the entire lifecycle tree unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assign NULL Deliveries atomically and report delivery evidence</name>
  <read_first>
    - lib/chimeway/reconciliation.ex
    - test/chimeway/reconciliation_test.exs
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-04-SUMMARY.md
  </read_first>
  <files>lib/chimeway/reconciliation.ex, test/chimeway/reconciliation_test.exs</files>
  <behavior>
    - Test 1: assignment updates NULL Event, Notification, and Delivery tenants to the exact trimmed host value and returns counts for all three levels.
    - Test 2: a Delivery already owned by the requested tenant remains unchanged while NULL siblings are updated; no existing owner is used as an inference source.
    - Test 3: report/1 deterministically lists only durable event, notification, and delivery IDs for ambiguous trees and returns `%{events: E, notifications: N, deliveries: D}` counts.
    - Test 4: concurrent conflicting assignments leave Event, Notifications, and Deliveries with one coherent owner, with one success and one ownership conflict.
    - Test 5: empty reports include a zero delivery count and no lifecycle details.
  </behavior>
  <action>Document the assignment policy in `Chimeway.Reconciliation` moduledoc: host input is the sole ownership authority; NULL Delivery rows under the locked event tree are updated atomically; a non-NULL Delivery that differs from the supplied tenant conflicts; a matching non-NULL Delivery is retained. After validation, update only NULL Event, Notification, and Delivery tenant columns in the existing transaction and add `deliveries` to the returned assignment counts. Extend `report/1` to query Deliveries beneath the reported NULL-owned Event set, expose deterministic `delivery_ids` beside each event's `notification_ids`, and add `deliveries` to report counts while retaining schema version 1 unless the serialized contract tests require an explicit version bump. Keep report content restricted to IDs, NULL tenant evidence, counts, status, schema version, and assignment instruction per D-09; do not serialize schemas or infer ownership. Expand the concurrency assertion to include Delivery ownership and ensure all queries use the existing static `repo_opts`.</action>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/reconciliation.ex test/chimeway/reconciliation_test.exs &amp;&amp; mix test test/chimeway/reconciliation_test.exs test/chimeway/mix/tasks/reconcile_tenants_test.exs --warnings-as-errors &amp;&amp; mix verify.runtime_prefix</automated>
  </verify>
  <acceptance_criteria>
    - Assignment result and report counts include `deliveries`, and report events contain deterministically ordered `delivery_ids` only.
    - NULL Delivery rows receive the supplied tenant in the same commit as parents; conflicting ownership rolls back; matching ownership remains intact.
    - Focused reconciliation/Mix task tests and `mix verify.runtime_prefix` exit zero.
  </acceptance_criteria>
  <done>Reconciliation covers the complete durable lifecycle spine atomically and emits bounded, deterministic delivery evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host-supplied event/tenant → maintenance transaction | Untrusted maintenance identifiers request durable ownership mutation. |
| Event tree → Delivery ownership | Existing child ownership can conflict with the requested parent assignment. |
| Reconciliation report → operator automation | Database ambiguity evidence becomes machine-readable output. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Executable Verification |
|---|---|---|---|---|---|---|
| T-97-13-01 | Tampering | `assign_event_tree/3` | high | mitigate | Lock Event, Notifications, and Deliveries before validation; update NULL rows only; rollback on conflicting non-NULL ownership. | `mix test test/chimeway/reconciliation_test.exs --warnings-as-errors` |
| T-97-13-02 | Elevation of Privilege | Tenant assignment input | high | mitigate | Accept only a validated explicit host tenant; never derive it from child rows or a prefix. | `mix test test/chimeway/reconciliation_test.exs test/chimeway/mix/tasks/reconcile_tenants_test.exs --warnings-as-errors` |
| T-97-13-03 | Information Disclosure | `report/1` | medium | mitigate | Emit only durable IDs, NULL ownership, counts, status, version, and instruction; retain sensitive-value negative assertions. | `mix test test/chimeway/reconciliation_test.exs --warnings-as-errors` |
| T-97-13-04 | Denial of Service | Concurrent assignments | medium | mitigate | Deterministic row ordering plus PostgreSQL row locks serialize assignments and preserve one coherent owner. | `mix test test/chimeway/reconciliation_test.exs --warnings-as-errors` |
| T-97-13-SC | Tampering | Package supply chain | low | accept | No package install or dependency change occurs. | `mix ci.audit` |

ASVS L1 mapping: V4 access control protects ownership mutation and report disclosure; V5 input validation retains UUID/nonblank-tenant checks. High-severity mutation and privilege threats block on focused PostgreSQL-backed tests.
</threat_model>

## Artifacts this phase produces

- New production files: None.
- New production symbols: None.
- Changed public result fields: `report/1` counts gain `:deliveries`; each reported event gains `:delivery_ids`; successful assignment counts gain `:deliveries`.
- Changed internal function: `Chimeway.Reconciliation.assign_locked_event_tree/3` locks, validates, and updates Delivery rows.
- New external API or dependency: None.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe explicit upgrade of lifecycle state | 97-13 | COVERED | Assignment covers the complete durable lifecycle spine. |
| REQ | TENANT-03 | Additive, non-guessing reconciliation | 97-13 | COVERED | Host-supplied assignment is atomic through Deliveries and reporting is bounded. |
| RESEARCH | — | Locked maintenance assignment plus JSON-safe ambiguity report | 97-13 | COVERED | Uses the existing Repo transaction/static-prefix and DTO conventions. |
| CONTEXT | D-08 | Never infer legacy ownership | 97-13 | COVERED | Existing Delivery ownership is validation input only, never the source tenant. |
| CONTEXT | D-09 | Explicit reconciliation evidence and host assignment | 97-13 | COVERED | Delivery IDs/counts and NULL-only atomic updates close the gap. |
| CONTEXT | D-10, D-11 | Static storage routing and compatible upgrade | 97-13 | COVERED | Existing `Storage.repo_opts/1` and runtime-prefix gate remain authoritative. |

<verification>
Run PostgreSQL-backed reconciliation and Mix task tests, then the maintained runtime-prefix gate. Verification must assert database state after success, conflict, repeat, and concurrency paths; no conversational UAT is used.
</verification>

<success_criteria>
- Conflicting Delivery ownership returns `:ownership_conflict` with no partial update.
- NULL Deliveries are assigned atomically with their Event and Notifications under the documented host-input policy.
- Report and assignment results include deterministic Delivery evidence/counts without sensitive lifecycle fields.
- Focused suites and `mix verify.runtime_prefix` exit zero.
</success_criteria>

<output>
Create `.planning/phases/97-tenant-identity-compatible-upgrade/97-13-SUMMARY.md` when done.
</output>

## Execution Expansion

The pre-existing delivery migration makes `chimeway_deliveries.tenant_id` NOT NULL,
which prevents the explicitly required legacy NULL-Delivery reconciliation semantics.
The user approved an additive follow-up migration. Add canonical template `033` and
the repository migration to make only that column nullable, retain the `actor_id`
constraint, and make the down migration irreversible because legacy NULL ownership
cannot safely be discarded. Update both generated installer fixtures and migration
contract proof for prefixed and public schemas.
