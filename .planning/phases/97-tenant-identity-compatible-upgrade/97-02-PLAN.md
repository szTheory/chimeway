---
phase: 97-tenant-identity-compatible-upgrade
plan: 02
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - lib/chimeway.ex
  - lib/chimeway/tenant_scope.ex
  - lib/chimeway/inbox.ex
  - lib/chimeway/admin.ex
  - test/chimeway/tenant_scope_contract_test.exs
  - test/chimeway/inbox_query_test.exs
  - test/chimeway/inbox_state_transition_test.exs
  - test/chimeway/admin_test.exs
autonomous: true
requirements: [TENANT-02]
must_haves:
  truths:
    - "D-04: inbox list/count/transitions and admin reads require one resolved tenant and include it in every read, reload, and mutation predicate."
    - "D-05/D-06: public legacy call shapes work only for the single concrete tenant declared by `:single_tenant_compatibility`; without that declaration they fail closed."
    - "D-07: cross-tenant UUIDs and absent UUIDs retain identical per-surface outcomes: empty/list/count for collection reads and `{:error, :not_found}` for inbox mutations."
    - "D-01/D-04: inbox lifecycle signals use the scoped notification's direct tenant_id and never recover ownership from workflow, delivery, recipient, or UUID state."
    - "D-10: tenant options are consumed as predicates and never reach Ecto prefix, Oban prefix, or per-request storage routing."
  artifacts:
    - path: "lib/chimeway.ex"
      provides: "Tenant-aware public delegate overloads for inbox/admin/recovery"
    - path: "lib/chimeway/inbox.ex"
      provides: "Scoped recipient reads and atomic lifecycle mutations"
    - path: "lib/chimeway/admin.ex"
      provides: "Unconditionally tenant-scoped operator DTO queries"
    - path: "test/chimeway/tenant_scope_contract_test.exs"
      provides: "Core lifecycle tenant isolation matrix"
  key_links:
    - from: "lib/chimeway.ex"
      to: "lib/chimeway/inbox.ex"
      via: "new keyword-option overloads carry tenant_id without removing configured legacy shapes"
      pattern: "tenant_id"
    - from: "lib/chimeway/inbox.ex"
      to: "lib/chimeway/notifications/notification.ex"
      via: "atomic predicates and signal emission use notification.tenant_id"
      pattern: "n.tenant_id"
---

<objective>
Expand the proven tenant contract across core inbox and admin reads or mutations while preserving each established absence outcome and opt-in single-tenant source compatibility.

Purpose: Close UUID/recipient-only authority gaps at the core library boundary.
Output: Tenant-aware public overloads, scoped Inbox/Admin implementations, and a machine-executed cross-tenant surface matrix consumed by recovery Plan 97-07.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-PATTERNS.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-01-SUMMARY.md

<interfaces>
- `Chimeway.TenantScope.resolve(opts)` returns `{:ok, tenant_id}` or a stable fail-closed error.
- Existing public delegates in `lib/chimeway.ex` forward to `Inbox`, `Admin`, and `Deliveries`.
- Inbox mutations currently accept an optional DateTime; preserve those source shapes through guarded overloads and compatibility resolution.
- Recovery zero-row behavior is already represented by existing noop/not-found outcomes and must remain unchanged for wrong scope.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scope inbox reads, transitions, signals, and public overloads</name>
  <reversibility rating="costly">Changing public inbox overloads and stable scope errors later requires coordinated host and optional-package call-site updates.</reversibility>
  <read_first>
    - lib/chimeway.ex
    - lib/chimeway/tenant_scope.ex
    - lib/chimeway/inbox.ex
    - test/chimeway/inbox_query_test.exs
    - test/chimeway/inbox_state_transition_test.exs
    - test/chimeway/tenant_scope_contract_test.exs
    - .planning/milestones/v1.9-phases/61-inbox-headless-package/61-CONTEXT.md
  </read_first>
  <files>lib/chimeway.ex, lib/chimeway/tenant_scope.ex, lib/chimeway/inbox.ex, test/chimeway/tenant_scope_contract_test.exs, test/chimeway/inbox_query_test.exs, test/chimeway/inbox_state_transition_test.exs</files>
  <behavior>
    - Test 1: list/count accept explicit tenant opts and return only matching tenant rows even when recipient identities are equal across tenants.
    - Test 2: mark_seen/read/archive accept tenant-aware overloads; wrong tenant and absent notification both return `{:error, :not_found}` without emitting a signal.
    - Test 3: configured legacy call shapes target only the configured tenant, while absent or malformed compatibility config fails closed.
    - Test 4: first-transition signaling takes tenant from the scoped Notification row and remains idempotent.
  </behavior>
  <action>Start with the inbox rows in the tenant surface matrix, then implement D-04/D-05/D-06/D-07. Resolve tenant once before `base_recipient_query`, counts, pagination, `update_all`, reloads, and signal emission; put `n.tenant_id == ^tenant_id` beside notification ID and recipient predicates. Replace child-based `resolve_tenant_id/1` with the direct tenant field on the notification loaded under the same scope. In `Chimeway` and `Chimeway.Inbox`, add guarded explicit-option forms for `mark_seen`, `mark_read`, and `archive` while retaining DateTime legacy shapes through compatibility resolution; define the exact overloads and docs so a keyword third argument carries `:tenant_id` and optional `:at`, and a legacy DateTime argument remains compatibility-only. Keep list/count DTO, pagination, cursor, timestamp, and first-transition signal semantics unchanged. Strip tenant authority before storage options are passed to `Chimeway.Storage` so it remains a row predicate only.</action>
  <acceptance_criteria>
    - Source: every Inbox query/update includes tenant and no signal path performs a tenant lookup through deliveries/workflows.
    - Behavior: equal recipient identity in two tenants yields isolated list/count results and mutation outcomes reveal no cross-tenant existence.
    - Compatibility: legacy arities fail without a concrete configured tenant and succeed only within the configured tenant.
    - Test command: inbox and tenant-scope contract suites exit zero.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted lib/chimeway.ex lib/chimeway/tenant_scope.ex lib/chimeway/inbox.ex test/chimeway/tenant_scope_contract_test.exs test/chimeway/inbox_query_test.exs test/chimeway/inbox_state_transition_test.exs &amp;&amp; mix test test/chimeway/tenant_scope_contract_test.exs test/chimeway/inbox_query_test.exs test/chimeway/inbox_state_transition_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Inbox reads, transitions, reloads, and signals enforce explicit tenant ownership and legacy shapes are available only under one concrete compatibility tenant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace permissive admin tenant filters with required predicates</name>
  <read_first>
    - lib/chimeway/admin.ex
    - lib/chimeway/tenant_scope.ex
    - lib/chimeway.ex
    - test/chimeway/admin_test.exs
    - test/chimeway/tenant_scope_contract_test.exs
    - .planning/milestones/v1.11-phases/70-recovery-auth-and-tenancy-hardening/70-CONTEXT.md
  </read_first>
  <files>lib/chimeway/admin.ex, test/chimeway/admin_test.exs, test/chimeway/tenant_scope_contract_test.exs</files>
  <behavior>
    - Test 1: command center, problems, definitions, feed, recovery candidates, and outcome totals include only rows owned by the explicit tenant.
    - Test 2: missing scope cannot execute the former `maybe_filter_tenant(query, nil)` path.
    - Test 3: two tenants with equal notification keys, recipients, statuses, or UUID-shaped inputs remain isolated in DTOs and aggregates.
  </behavior>
  <action>Apply D-04/D-05/D-06/D-07 to all `Chimeway.Admin` entrypoints. Resolve tenant before query construction, delete permissive nil/empty tenant filter branches, and add unconditional predicates at the owning Event/Notification/Delivery join appropriate to `command_center/1`, `recent_problem_deliveries/1`, `definitions/1`, `feed/1`, `recovery_candidates/1`, and `outcome_totals/1`. Retain limit, age, status, safe DTO, redaction, and empty aggregate contracts. Extend the shared tenant matrix with paired tenant rows that would collide under every non-tenant filter, proving collection/aggregate emptiness instead of forbidden/existence results.</action>
  <acceptance_criteria>
    - Source: no Admin lifecycle query has a nil/empty no-op tenant branch; each public operation resolves scope first.
    - Behavior: all six operator read models are tenant-isolated and preserve existing empty/DTO shapes.
    - Test command: `mix test test/chimeway/admin_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` exits zero.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/admin.ex test/chimeway/admin_test.exs test/chimeway/tenant_scope_contract_test.exs &amp;&amp; mix test test/chimeway/admin_test.exs test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Every core admin DTO and aggregate is built only from rows owned by the resolved tenant, with fail-closed missing scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public API options → lifecycle contexts | Host-supplied tenant scope authorizes row visibility/mutation. |
| UUID/recipient identifiers → atomic mutations | Attacker-controlled identifiers could target another tenant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Residual Risk | Executable Verification |
|---|---|---|---|---|---|---|---|
| T-97-04 | Information Disclosure | Inbox/Admin reads | high | mitigate | Resolve tenant first and require predicates on root/joined owning rows; preserve empty/not-found outcomes. | Host authorization of the supplied tenant remains host-owned. | `mix test test/chimeway/tenant_scope_contract_test.exs test/chimeway/inbox_query_test.exs test/chimeway/admin_test.exs --warnings-as-errors` |
| T-97-05 | Tampering | Inbox transitions | high | mitigate | Tenant is part of every atomic update predicate; zero-row cross-tenant writes emit no signal. | Database superusers can bypass application predicates. | `mix test test/chimeway/inbox_state_transition_test.exs --warnings-as-errors` |
| T-97-07 | Repudiation | Compatibility fallback | medium | mitigate | Compatibility config explicitly names the one tenant; stable errors distinguish configuration failure without revealing row existence. | Config changes are host deployment events outside Chimeway audit ownership. | `mix test test/chimeway/tenant_scope_contract_test.exs --warnings-as-errors` |

## ASVS Level 1 Execution Contract

Research identifies V2-V5 as the applicable ASVS categories for Phase 97. The owning plan/task named below implements each control; every executor preserves these cross-plan obligations, and phase completion runs the exact evidence commands after all owning plans land.

| ASVS L1 control | Responsible plan/task | Concrete mitigation | Exact automated evidence |
|---|---|---|---|
| V2 Authentication | 97-03 Task 1 and Task 2 | Host-owned Inbox/Admin authentication must authorize a concrete tenant before package state gains lifecycle authority; Chimeway does not authenticate or infer membership. | `mix verify.inbox &amp;&amp; mix verify.admin` |
| V3 Session Management | 97-03 Task 1/Task 2, 97-05 Task 1, and 97-08 Task 1 | Re-run host authorization at LiveView event time and rebuild tenant-bearing Context options for every mutation or refresh; never fall back to a stale or tenantless session value. | `mix cmd --cd chimeway_inbox mix test --warnings-as-errors &amp;&amp; mix cmd --cd chimeway_admin mix test --warnings-as-errors` |
| V4 Access Control | 97-01 Task 1/Task 2, 97-02 Task 1/Task 2, 97-07 Task 1, and 97-08 Task 1 | Resolve one tenant before access, retain its predicate across every root/nested read, atomic update, reload, and replan, and map wrong scope to the established absent outcome. | `mix test test/chimeway/tenant_scope_contract_test.exs test/chimeway/orchestration/recovery_test.exs --warnings-as-errors &amp;&amp; mix verify.inbox &amp;&amp; mix verify.admin` |
| V5 Input Validation | 97-01 Task 1/Task 2 and 97-04 Task 1/Task 2 | Accept only trimmed nonblank binary tenant IDs, validate reconciliation UUIDs, enforce strict CLI flag combinations, and reject invalid scope before any query or mutation. | `mix test test/chimeway/tenant_identity_test.exs test/chimeway/tenant_scope_contract_test.exs test/chimeway/reconciliation_test.exs test/chimeway/mix/tasks/reconcile_tenants_test.exs --warnings-as-errors` |

</threat_model>

## Artifacts this phase produces

- Public overloads/docs in `Chimeway` and `Chimeway.Inbox` for tenant-aware list/count/mark/archive operations while retaining compatibility-gated legacy shapes.
- Tenant-scoped `Chimeway.Admin.command_center/1`, `recent_problem_deliveries/1`, `definitions/1`, `feed/1`, `recovery_candidates/1`, and `outcome_totals/1`.
- Expanded `test/chimeway/tenant_scope_contract_test.exs` surface matrix and focused Inbox/Admin/Recovery regressions.
- Task names: scoped Inbox/public delegates and required Admin predicates.

<verification>
Run focused core contracts after each task, then `mix ci.test` at the wave boundary. All objective tenant checks remain executable; no conversational UAT is required.
</verification>

<success_criteria>
- Core Inbox/Admin reads and Inbox mutations cannot cross the supplied tenant.
- Missing scope fails closed unless one concrete compatibility tenant is configured.
- Wrong-tenant identifiers preserve the same absence outcomes as nonexistent identifiers.
- Tenant data never changes static storage routing.
</success_criteria>

<output>
Create `.planning/phases/97-tenant-identity-compatible-upgrade/97-02-SUMMARY.md` when done.
</output>
