---
phase: 97-tenant-identity-compatible-upgrade
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260812000000_add_tenant_identity_to_events_and_notifications.exs
  - priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs
  - test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs
  - test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs
  - test/chimeway/migration_contract_test.exs
autonomous: true
gap_closure: true
requirements: [TENANT-03]
must_haves:
  truths:
    - "D-08/D-11: migration 032 `down/0` is explicitly irreversible and raises the same clear deterministic error before any index, column, or row mutation in the repository migration, canonical copied template, and both rendered static-prefix fixtures."
    - "D-02/D-08: after two tenants validly use the same idempotency key, attempted downgrade fails by the declared contract while both tenant-owned events, composite uniqueness, tenant columns, and migration version remain intact."
    - "D-10/D-11: public and static `chimeway` generated migrations retain identical irreversibility semantics and tenant identity never becomes a prefix or per-request storage route."
    - "TENANT-03 safety edge: repeated downgrade attempts return the same deterministic failure with no partial DDL or data loss, including after cross-tenant duplicate-key activity."
    - "FLAGGED ASSUMPTION (TENANT-03 unclassified probe, unresolved): explicit irreversibility is the selected safe compatibility contract under the gap instruction's stated preference; adopters needing a loss-aware downgrade require a separately designed host-owned export/transform path."
  artifacts:
    - path: "priv/repo/migrations/20260812000000_add_tenant_identity_to_events_and_notifications.exs"
      provides: "Repository migration with deterministic pre-DDL irreversible down contract"
    - path: "priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs"
      provides: "Canonical copied migration with the same irreversible down contract"
    - path: "test/chimeway/migration_contract_test.exs"
      provides: "Real PostgreSQL public/prefixed duplicate-key downgrade regression proof"
  key_links:
    - from: "priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs"
      to: "test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs"
      via: "deterministic installer rendering changes only the static prefix sentinel"
      pattern: "add_tenant_identity_to_events_and_notifications"
    - from: "priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs"
      to: "test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs"
      via: "the prefixed copy preserves the exact same down failure before DDL"
      pattern: "def down"
    - from: "test/chimeway/migration_contract_test.exs"
      to: "PostgreSQL"
      via: "Ecto.Migrator applies 032, inserts same-key/different-tenant rows, attempts down, and inspects unchanged schema/data/version"
      pattern: "Ecto.Migrator.run"
  prohibitions:
    - "FLAGGED-UNVERIFIED — Downgrade handling MUST NOT delete, merge, reassign, or rewrite valid tenant-scoped lifecycle rows merely to recreate global uniqueness."
    - "FLAGGED-UNVERIFIED — Canonical, public, and prefixed migration copies MUST NOT diverge or convert domain tenant identity into dynamic storage routing."
---

<objective>
Close the migration rollback blocker from `97-VERIFICATION.md` with an explicit, deterministic, lossless irreversibility contract in every migration copy.

Purpose: Prevent a nominal rollback from failing mid-DDL or coercing valid cross-tenant data back into an unsafe global idempotency model.
Output: Synchronized irreversible down paths, refreshed deterministic fixtures, and real PostgreSQL regression evidence for both static storage modes.

Spec-less fallback: Phase 97 has no SPEC. The TENANT-03 unclassified probe is preserved as a flagged assumption; the plan adds explicit repeatability/no-partial-mutation backstops rather than silently dismissing it.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-06-SUMMARY.md

<interfaces>
- Migration 032 `up/0` remains additive: nullable tenant fields, removal of global uniqueness, and creation of `{tenant_id, idempotency_key}` uniqueness.
- Migration 032 `down/0` becomes an explicit unsupported operation with one stable error message raised before any DDL.
- `Chimeway.Install.Migrations` remains the sole deterministic renderer for `prefix: false` and static `prefix: "chimeway"` copies.
- `test/chimeway/migration_contract_test.exs` owns real Ecto.Migrator/PostgreSQL proof for both generated modes.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Refuse unsafe tenant-identity rollback before any schema or data mutation</name>
  <reversibility rating="costly">D-02/D-08 publish cross-tenant idempotency and durable tenant ownership; replacing the refusal with a loss-aware downgrade requires a coordinated host data-policy and copied-migration contract change, while this task itself performs no irreversible mutation.</reversibility>
  <read_first>
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-VERIFICATION.md
    - priv/repo/migrations/20260812000000_add_tenant_identity_to_events_and_notifications.exs
    - priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs
    - test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs
    - test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs
    - test/chimeway/migration_contract_test.exs
    - lib/chimeway/install/migrations.ex
  </read_first>
  <files>priv/repo/migrations/20260812000000_add_tenant_identity_to_events_and_notifications.exs, priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs, test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs, test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs, test/chimeway/migration_contract_test.exs</files>
  <behavior>
    - Test 1: public and prefixed generated databases accept two events with the same idempotency key under distinct nonblank tenants after migration 032.
    - Test 2: rolling back migration 032 raises one exact project-owned error explaining that tenant-scoped idempotency cannot safely return to global uniqueness.
    - Test 3: after the failure, migration version 32, both rows, tenant columns, and the composite unique index still exist; the global unique index does not.
    - Test 4: a repeated rollback attempt raises the identical error and leaves the same schema/data state.
    - Test 5: installer golden verification proves the canonical template and both rendered static-prefix copies remain synchronized.
  </behavior>
  <action>Start RED in `test/chimeway/migration_contract_test.exs` by replacing the obsolete full-rollback expectation for generated migration 032 with a real PostgreSQL contract in both `@generated_modes`: apply all migrations, insert two minimally valid Event rows sharing an idempotency key but using different tenant IDs, attempt to roll back only the latest migration, assert one exact deterministic exception/error message, and verify the migration version, rows, tenant columns, composite index, and absence of the global index are unchanged. Repeat the attempted rollback to prove stable idempotency/no partial mutation. Then implement the preferred safe contract under D-02/D-08/D-10/D-11: make `down/0` in both the repository migration and canonical copied template raise that message as its first and only operation. Do not drop the composite index first, do not recreate global uniqueness, and do not delete, merge, reassign, or export host rows. Regenerate/refresh the committed public and prefixed migration-032 fixtures through the accepted installer golden workflow so only the static prefix sentinel differs. Preserve `up/0`, reconciliation, nullable legacy rows, and static storage-prefix behavior exactly. The costly rating records that replacing this refusal later requires a coordinated host data-policy and copied-migration change; no checkpoint is needed because this implementation mutates no adopter data, the gap instruction reported no assumption delta, and it preferred deterministic irreversibility over a loss-aware downgrade.</action>
  <acceptance_criteria>
    - All four migration copies raise the identical documented error before any downgrade DDL.
    - Real public and prefixed PostgreSQL tests create valid same-key/different-tenant rows and prove one and repeated rollback attempts are lossless and leave migration 032 applied.
    - Migration `up/0`, reconciliation behavior, and both static prefix modes remain unchanged.
    - Installer goldens remain deterministic and synchronized with the canonical template.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted priv/repo/migrations/20260812000000_add_tenant_identity_to_events_and_notifications.exs priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs test/chimeway/migration_contract_test.exs &amp;&amp; mix test test/chimeway/migration_contract_test.exs --warnings-as-errors &amp;&amp; mix verify.install_golden &amp;&amp; mix verify.runtime_prefix</automated>
  </verify>
  <done>Every Phase 97 migration copy refuses the unsafe downgrade deterministically before mutation, and both generated storage modes prove valid cross-tenant duplicate-key data remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical migration template → generated host copies | Irreversibility and prefix rendering must stay deterministic across adopter output. |
| Ecto.Migrator downgrade request → PostgreSQL | A maintenance action must not partially remove tenant safety or corrupt valid lifecycle rows. |
| Tenant-scoped upgraded data → legacy global model | Valid equal keys across tenants cannot be coerced into one global uniqueness domain without host-owned loss/transform decisions. |

## STRIDE Threat Register (ASVS L1; block-on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Residual Risk | Executable Verification |
|---|---|---|---|---|---|---|---|
| T-97-10-01 | Tampering | Migration 032 down path | high | mitigate | Raise the project-owned irreversible error before any DDL or row mutation in every copy. | Adopters can manually edit copied migrations as an explicit host action. | `mix test test/chimeway/migration_contract_test.exs --warnings-as-errors` |
| T-97-10-02 | Denial of Service | Automated downgrade tooling | medium | mitigate | Fail immediately and deterministically with actionable contract text rather than reaching a PostgreSQL uniqueness error after partial commands. | Downgrade remains unavailable until a host designs a data-loss/transform policy. | `mix test test/chimeway/migration_contract_test.exs --warnings-as-errors` |
| T-97-10-03 | Elevation of Privilege | Static prefix rendering | high | mitigate | Regenerate public and `chimeway` fixtures from one canonical template and prove tenant never enters prefix routing. | Host-level database permissions remain outside Chimeway. | `mix verify.install_golden &amp;&amp; mix verify.runtime_prefix` |
| T-97-10-SC | Tampering | Package supply chain | low | accept | No dependency installation occurs; use the existing Ecto/PostgreSQL stack and lockfile. | Existing dependency provenance remains governed by project audit gates. | `mix ci.audit` |

ASVS L1 mapping: V4 access-control integrity is preserved by refusing removal of tenant identity/composite uniqueness; V5 maintenance-input safety is proven by deterministic repeated rollback behavior. No authentication/session surface changes.
</threat_model>

## Artifacts this phase produces

- Changed migration contract: `down/0` in the repository migration, canonical template, and rendered public/prefixed copies raises one deterministic irreversible error before DDL.
- Expanded proof: `test/chimeway/migration_contract_test.exs` covers same-key/different-tenant rows and repeated lossless rollback refusal in both static storage modes.
- New public symbols/files: none.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safely identify, query, and upgrade lifecycle state in an explicit tenant boundary | 97-01..97-10 | COVERED | 97-09 closes canonical trigger/read identity; 97-10 closes safe upgrade rollback. |
| REQ | TENANT-01 | Immutable tenant persistence and composite idempotency | 97-01, 97-09 | COVERED | Padded/canonical equality and concurrency are executable. |
| REQ | TENANT-02 | Explicit tenant-scoped reads/mutations and compatibility | 97-01..03, 97-05, 97-07..09 | COVERED | 97-09 restores writer/reader canonical interoperability; existing scope contracts remain authoritative. |
| REQ | TENANT-03 | Additive, non-guessing, static-prefix-compatible upgrade | 97-01, 97-04, 97-06, 97-10 | COVERED | Explicit irreversible rollback prevents partial failure or guessed/lossy downgrade. |
| RESEARCH | — | One normalized trigger ownership value across persistence and duplicate recovery | 97-09 | COVERED | Extends the established trigger/reader seam without new dependencies. |
| RESEARCH | — | Deterministic copied migration and dual static storage-mode proof | 97-10 | COVERED | Canonical/public/prefixed copies plus PostgreSQL evidence. |
| CONTEXT | D-01, D-03 | Direct immutable ownership and trigger propagation | 97-01, 97-09 | COVERED | Canonical value is propagated through all trigger outputs. |
| CONTEXT | D-02, D-04, D-07 | Composite identity and non-disclosing scoped access | 97-01..02, 97-07, 97-09..10 | COVERED | Equality, wrong-tenant absence, and rollback preservation are tested. |
| CONTEXT | D-05, D-06 | Explicit concrete compatibility only | 97-01..03 | COVERED | Gap plans do not alter compatibility or authorize compatibility writes. |
| CONTEXT | D-08, D-09 | Non-guessing additive upgrade and explicit reconciliation | 97-01, 97-04, 97-06, 97-10 | COVERED | Rollback refusal performs no inference, reassignment, or loss. |
| CONTEXT | D-10, D-11 | Static storage routing and deterministic copied migrations | 97-01, 97-06, 97-09..10 | COVERED | Both gap plans explicitly preserve static prefix behavior. |
| VERIFY WARNING | — | Valid-but-changed Inbox auth context can raise | NONE | EXCLUDED | Hardening warning only; verifier demonstrated no disclosure and did not identify it as a phase-goal blocker. |

No source item is silently missing. The seven supplied spec-less edge rows equal four executable TENANT-01 edge truths plus one TENANT-01 flagged ordering assumption, one TENANT-02 flagged assumption, and one TENANT-03 flagged assumption across 97-09/97-10. Descriptor-less prohibition recall retains only four phase-specific value/safety constraints across the two plans; generic OWASP/database concerns route to the threat models and `$gsd-secure-phase`.

<verification>
Run the focused migration contract, installer golden, and runtime-prefix gates. Evidence must use real PostgreSQL migration execution in both static modes and must inspect unchanged data/schema after the expected refusal.
</verification>

<success_criteria>
- Migration 032 never attempts to reconstruct global uniqueness from valid tenant-scoped data.
- Downgrade refusal is deterministic, repeatable, synchronized across all copies, and occurs before mutation.
- Both static storage modes and the additive/reconciliation up path remain green.
</success_criteria>

<output>
Create `.planning/phases/97-tenant-identity-compatible-upgrade/97-10-SUMMARY.md` when done.
</output>
