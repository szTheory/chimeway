---
phase: 97-tenant-identity-compatible-upgrade
plan: 04
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - lib/chimeway/reconciliation.ex
  - lib/mix/tasks/chimeway.reconcile_tenants.ex
  - test/chimeway/reconciliation_test.exs
  - test/chimeway/mix/tasks/reconcile_tenants_test.exs
  - test/chimeway/install/migrations_test.exs
  - test/chimeway/install/golden_diff_test.exs
  - test/chimeway/migration_contract_test.exs
  - test/chimeway/generated_prefixed_runtime_proof_test.exs
  - test/chimeway/runtime_prefix_integration_test.exs
  - test/fixtures/installer_golden_public/STDOUT.txt
  - test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs
  - test/fixtures/installer_golden_prefixed/STDOUT.txt
  - test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs
autonomous: true
requirements: [TENANT-03]
must_haves:
  truths:
    - "D-08: upgrading adds nullable tenant identity and tenant-scoped indexes without assigning a sentinel or inferring ownership from recipient, delivery, workflow, actor, or prefix data."
    - "D-09: a versioned JSON-safe report deterministically identifies every NULL-owned event tree, and only `assign_event_tree/3` with a host-supplied nonblank tenant makes it tenant-visible."
    - "D-09: report and assignment are idempotent—re-reporting is stable, repeating the same assignment is an explicit already-assigned result, and conflicting reassignment cannot overwrite ownership."
    - "D-10/D-11: copied migration generation/application remains deterministic in both `prefix: false` and static `prefix: \"chimeway\"` modes, with no tenant-driven Ecto or Oban prefix."
    - "Interrupted assignment is atomic across an Event and its NULL Notification children; parallel or conflicting assignments cannot produce mixed ownership."
  artifacts:
    - path: "lib/chimeway/reconciliation.ex"
      provides: "Machine-readable ambiguity report and explicit atomic event-tree assignment"
      exports: ["report/1", "assign_event_tree/3"]
    - path: "lib/mix/tasks/chimeway.reconcile_tenants.ex"
      provides: "JSON report and host-supplied assignment CLI"
      contains: "Mix.Tasks.Chimeway.ReconcileTenants"
    - path: "test/chimeway/reconciliation_test.exs"
      provides: "No-guessing, atomicity, idempotency, and concurrency evidence"
    - path: "test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs"
      provides: "Public-schema copied migration fixture"
    - path: "test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs"
      provides: "Static chimeway-schema copied migration fixture"
  key_links:
    - from: "lib/mix/tasks/chimeway.reconcile_tenants.ex"
      to: "lib/chimeway/reconciliation.ex"
      via: "CLI delegates report/assignment and JSON-encodes returned maps"
      pattern: "Reconciliation.(report|assign_event_tree)"
    - from: "lib/chimeway/reconciliation.ex"
      to: "lib/chimeway/events/event.ex"
      via: "NULL-only Event update and same-event Notification update in one transaction"
      pattern: "is_nil.*tenant_id"
    - from: "priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs"
      to: "test/fixtures/installer_golden_*"
      via: "existing deterministic template renderer with static prefix sentinel"
      pattern: "__CHIMEWAY_PREFIX__"
---

<objective>
Give existing adopters a deterministic, non-guessing upgrade path: inspect ambiguous legacy ownership as JSON, assign one event tree only from a host-supplied tenant, and prove copied migrations in both static storage modes.

Purpose: Make the durable tenant schema safely adoptable without fabricating ownership or changing storage routing.
Output: `Chimeway.Reconciliation`, `mix chimeway.reconcile_tenants`, reconciliation contracts, refreshed copied-migration fixtures, and PostgreSQL/static-prefix proofs.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-CONTEXT.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-RESEARCH.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-PATTERNS.md
@.planning/phases/97-tenant-identity-compatible-upgrade/97-01-SUMMARY.md

<interfaces>
- Canonical template `032_add_tenant_identity_to_events_and_notifications.exs` already exists from Plan 01 and uses the static prefix sentinel.
- `Chimeway.Storage.repo_opts/1` routes one configured host schema and must not receive tenant as prefix.
- Existing `Chimeway.Install.Migrations.run/1` discovers ordered canonical templates and generates idempotent host files.
- Jason 1.4 is already a locked direct dependency and is the repository JSON encoder.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement versioned ambiguity reporting and atomic host assignment</name>
  <reversibility rating="one-way">Assigning a legacy event tree establishes durable ownership and changing it requires a controlled data migration; D-09 already locks explicit host assignment, so the plan records the door without re-asking it.</reversibility>
  <read_first>
    - lib/chimeway/admin.ex
    - lib/chimeway/events/event.ex
    - lib/chimeway/notifications/notification.ex
    - lib/chimeway/storage.ex
    - test/chimeway/admin_test.exs
    - test/support/data_case.ex
    - .planning/phases/97-tenant-identity-compatible-upgrade/97-RESEARCH.md
  </read_first>
  <files>lib/chimeway/reconciliation.ex, test/chimeway/reconciliation_test.exs</files>
  <behavior>
    - Test 1: `report/1` returns `schema_version: 1`, status, stable counts, and sorted event records containing IDs plus NULL ownership and notification IDs—never an inferred tenant.
    - Test 2: `assign_event_tree(event_id, tenant_id, opts)` atomically updates a NULL Event and all its NULL Notifications to the explicit trimmed tenant.
    - Test 3: absent/blank/nonbinary tenant and absent event fail without mutation; a conflicting existing tenant cannot be overwritten.
    - Test 4: repeated report is stable, repeated same assignment has an explicit already-assigned result, and concurrent differing assignments leave one coherent owner rather than mixed rows.
  </behavior>
  <action>Create `Chimeway.Reconciliation` under D-08/D-09/D-10 with `report(opts \\ [])` and `assign_event_tree(event_id, tenant_id, opts \\ [])`. `report/1` must query Event rows where tenant is NULL, load only Notification IDs/tenant values for those event IDs, sort by durable IDs, and return a JSON-safe map with `schema_version: 1`, `status: "ambiguous_tenant_ownership"`, `counts`, `events`, and an explicit host-assignment instruction; include no recipient, payload, delivery, workflow, actor, or inferred ownership data. `assign_event_tree/3` validates a nonblank binary tenant and UUID, then uses one Repo transaction with row locking or guarded NULL-only updates to set the Event and every NULL child Notification; detect preexisting conflicting child/event owners before commit and return stable `:not_found`, `:already_assigned`, `:ownership_conflict`, or validation errors. Consume only static `Storage.repo_opts(opts)` after removing tenant authority from opts; tenant is always a column value/predicate. Add paired, empty-report, single-row, repeated, interrupted/rollback, and parallel-conflict PostgreSQL tests.</action>
  <acceptance_criteria>
    - Report JSON-safe data is deterministic and contains no ownership guess or sensitive lifecycle payload fields.
    - Assignment changes only the explicitly named event tree, commits Event/Notifications atomically, and refuses overwrite/conflict.
    - Empty/single/multiple/repeated/concurrent cases have explicit executable outcomes.
    - Test command: `mix test test/chimeway/reconciliation_test.exs --warnings-as-errors` exits zero.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted lib/chimeway/reconciliation.ex test/chimeway/reconciliation_test.exs &amp;&amp; mix test test/chimeway/reconciliation_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Legacy ambiguity is fully reportable without inference and one host-supplied event-tree assignment is atomic, idempotent, and conflict-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose reconciliation as a strict JSON Mix task</name>
  <reversibility rating="costly">The Mix task flags and JSON schema become an adopter automation contract and renaming them requires coordinated upgrade tooling changes.</reversibility>
  <read_first>
    - lib/mix/tasks/chimeway.gen.migrations.ex
    - lib/mix/tasks/preview_rendering.ex
    - lib/chimeway/reconciliation.ex
    - test/chimeway/install/migrations_test.exs
  </read_first>
  <files>lib/mix/tasks/chimeway.reconcile_tenants.ex, test/chimeway/mix/tasks/reconcile_tenants_test.exs</files>
  <behavior>
    - Test 1: `mix chimeway.reconcile_tenants --report` emits exactly one decodable JSON object matching `Reconciliation.report/1`.
    - Test 2: `--event-id UUID --tenant-id TENANT` delegates explicit assignment and emits a JSON result with updated counts/tenant.
    - Test 3: ambiguous flag combinations, missing tenant, invalid UUID, and conflict exit nonzero with a stable Mix error and no mutation.
  </behavior>
  <action>Create `Mix.Tasks.Chimeway.ReconcileTenants` with strict `OptionParser` flags `--report`, `--event-id`, and `--tenant-id`. Accept exactly report mode or assignment mode; call the core module rather than duplicate Ecto queries; encode the returned versioned map/result through `Jason.encode!/1` and write one JSON object to stdout. Raise `Mix.Error` on invalid combinations, validation errors, missing rows, or ownership conflicts while keeping database details and lifecycle payloads out of output. The task uses the application's existing Repo/static storage config and has no tenant-prefix flag. Test with captured IO/task re-enable conventions used by installer task tests.</action>
  <acceptance_criteria>
    - CLI report/assignment stdout is machine-decodable JSON and delegates all state behavior to `Chimeway.Reconciliation`.
    - Invalid/ambiguous invocations fail nonzero before database mutation.
    - No dynamic prefix or inferred tenant option exists.
    - Test command: Mix task contract tests exit zero.
  </acceptance_criteria>
  <verify>
    <automated>mix format --check-formatted lib/mix/tasks/chimeway.reconcile_tenants.ex test/chimeway/mix/tasks/reconcile_tenants_test.exs &amp;&amp; mix test test/chimeway/mix/tasks/reconcile_tenants_test.exs test/chimeway/reconciliation_test.exs --warnings-as-errors</automated>
  </verify>
  <done>Adopters can automate ambiguity reporting and explicit assignment through a strict JSON-only maintenance task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock copied-migration generation and both static PostgreSQL modes</name>
  <read_first>
    - priv/chimeway_migrations/032_add_tenant_identity_to_events_and_notifications.exs
    - lib/chimeway/install/migrations.ex
    - test/chimeway/install/migrations_test.exs
    - test/chimeway/install/golden_diff_test.exs
    - test/chimeway/migration_contract_test.exs
    - test/chimeway/generated_prefixed_runtime_proof_test.exs
    - test/chimeway/runtime_prefix_integration_test.exs
    - test/fixtures/installer_golden_public/STDOUT.txt
    - test/fixtures/installer_golden_prefixed/STDOUT.txt
  </read_first>
  <files>test/chimeway/install/migrations_test.exs, test/chimeway/install/golden_diff_test.exs, test/chimeway/migration_contract_test.exs, test/chimeway/generated_prefixed_runtime_proof_test.exs, test/chimeway/runtime_prefix_integration_test.exs, test/fixtures/installer_golden_public/STDOUT.txt, test/fixtures/installer_golden_public/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs, test/fixtures/installer_golden_prefixed/STDOUT.txt, test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/TIMESTAMP_add_tenant_identity_to_events_and_notifications.exs</files>
  <behavior>
    - Test 1: generator emits template 032 once, in stable order, and a second run reports it unchanged in public and prefixed modes.
    - Test 2: public and `chimeway` generated migrations apply on PostgreSQL, retain legacy NULL rows, and enforce cross-tenant/same-tenant idempotency semantics.
    - Test 3: report plus explicit assignment work against both static storage modes and tenant input never changes relation prefix.
    - Test 4: down/up contract removes/recreates only the new columns/indexes and the copied outputs remain deterministic.
  </behavior>
  <action>Refresh the two committed golden trees through the repository's acceptance mechanism after template 032 is present, including deterministic stdout. Extend installer ordering/idempotency tests, migration contract tests, generated prefixed runtime proof, and public/prefixed runtime integration tests for D-08/D-09/D-10/D-11. Seed legacy rows before applying 032 and assert their tenants remain NULL; inspect PostgreSQL indexes to prove global uniqueness is gone and composite uniqueness exists; run reconciliation report/assignment in both modes; assert static schema selection still comes only from existing Chimeway storage config. Keep one canonical template and generated copies only—do not add a second generator or hand-edit fixture output outside the accepted golden refresh workflow. Run the existing named gates, then `mix ci.verify_gates` to prove local/CI contract parity without inventing an external API coverage matrix.</action>
  <acceptance_criteria>
    - Golden public/prefixed files and stdout exactly match deterministic generation and the second generation is idempotent.
    - PostgreSQL proof covers nullable legacy rows, composite index enforcement, JSON report, explicit assignment, and static prefix invariants in both modes.
    - Existing `verify.install_golden`, `verify.runtime_prefix`, and `ci.verify_gates` entrypoints all exit zero.
  </acceptance_criteria>
  <verify>
    <automated>mix verify.install_golden &amp;&amp; mix verify.runtime_prefix &amp;&amp; mix ci.verify_gates</automated>
  </verify>
  <done>Copied migrations and reconciliation are deterministic, applicable, and verified under both supported static storage modes with local/CI gate parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Legacy database state → reconciliation report | Ambiguous rows must be exposed safely without manufacturing authority or leaking payload data. |
| Host CLI input → durable ownership update | A supplied event ID and tenant can permanently assign lifecycle ownership. |
| Canonical migration template → adopter database | Generated DDL executes in either supported static schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | Residual Risk | Executable Verification |
|---|---|---|---|---|---|---|---|
| T-97-12 | Tampering | `assign_event_tree/3` | high | mitigate | Validate explicit tenant/UUID, lock or NULL-guard Event tree, reject conflicts, transact all updates. | Host-selected tenant correctness cannot be inferred or validated by Chimeway. | `mix test test/chimeway/reconciliation_test.exs --warnings-as-errors` |
| T-97-13 | Information Disclosure | JSON report/task | medium | mitigate | Emit only stable IDs, NULL ownership, counts, status, schema version, and instructions; omit payload/recipient/provider data. | Lifecycle UUIDs are intentionally operator-visible maintenance identifiers. | `mix test test/chimeway/mix/tasks/reconcile_tenants_test.exs --warnings-as-errors` |
| T-97-14 | Elevation of Privilege | Storage routing | high | mitigate | Use existing static storage opts and forbid tenant-derived prefix/task flags; prove public and `chimeway` schemas. | A host can misconfigure its one static installation prefix outside this task. | `mix verify.runtime_prefix` |
| T-97-15 | Denial of Service | Concurrent reconciliation | medium | mitigate | Bounded event-tree transaction, deterministic row ordering, and conflict-safe concurrent tests. | Very large event fanout can hold row locks for the transaction duration. | `mix test test/chimeway/reconciliation_test.exs --warnings-as-errors` |
| T-97-SC | Tampering | Package installs | low | accept | No new npm/pip/cargo or Hex package install is planned; Jason/Ecto are already locked direct dependencies. | Existing dependency risk remains covered by project audit jobs. | `mix ci.audit` |
</threat_model>

## Artifacts this phase produces

- New module/functions: `Chimeway.Reconciliation.report/1`, `Chimeway.Reconciliation.assign_event_tree/3`.
- New Mix task/flags: `Mix.Tasks.Chimeway.ReconcileTenants`, `mix chimeway.reconcile_tenants --report`, `--event-id`, `--tenant-id`.
- JSON keys/status: `schema_version`, `status`, `counts`, `events`, `notification_ids`, `assignment`; status value `ambiguous_tenant_ownership`.
- New paths: `lib/chimeway/reconciliation.ex`, `lib/mix/tasks/chimeway.reconcile_tenants.ex`, the two new reconciliation test files, and one generated template-032 fixture in each installer golden tree.
- Expanded paths: installer, migration-contract, generated-prefix, runtime-prefix test suites and both `STDOUT.txt` fixtures.
- Task names: versioned ambiguity/assignment service, strict JSON Mix task, dual static-mode copied-migration gate.

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safely identify, query, and upgrade lifecycle state within explicit tenant boundary | 01-04 | COVERED | Tracer proves identity/query; expansions cover all surfaces and upgrade. |
| REQ | TENANT-01 | Immutable event/notification tenant plus composite idempotency | 01 | COVERED | Schema, Trigger, DB index, concurrency proof. |
| REQ | TENANT-02 | Explicit lifecycle scope plus concrete compatibility | 01-03 | COVERED | Core and optional packages. |
| REQ | TENANT-03 | Additive non-guessing upgrade and report/reconciliation | 01, 04 | COVERED | Nullable migration, JSON evidence, explicit assignment, dual modes. |
| CONTEXT | D-01 | Direct immutable event/notification tenant | 01 | COVERED | Durable schema/write contract. |
| CONTEXT | D-02 | Composite tenant/idempotency identity | 01 | COVERED | DB and duplicate recovery agree. |
| CONTEXT | D-03 | Trigger transaction propagation; stable notification identity retained | 01 | COVERED | Event/notification writes use one explicit tenant. |
| CONTEXT | D-04 | Explicit scope on all lifecycle reads/mutations | 01-03 | COVERED | Trace, Inbox, Admin, Recovery, Phoenix callers. |
| CONTEXT | D-05 | Former signatures fail closed by default | 01-03 | COVERED | Resolver/config and caller tests. |
| CONTEXT | D-06 | Compatibility declares concrete tenant | 01-03 | COVERED | Exact nested config key and validation. |
| CONTEXT | D-07 | Cross-tenant absence non-disclosure | 01-03 | COVERED | Per-surface outcome matrix. |
| CONTEXT | D-08 | Additive nullable migration; no sentinel/inference | 01, 04 | COVERED | Migration and legacy-row DB proof. |
| CONTEXT | D-09 | Machine-readable ambiguity evidence and explicit assignment | 04 | COVERED | Core service plus JSON Mix task. |
| CONTEXT | D-10 | Static installation storage routing only | 01-04 | COVERED | Predicate-only tenant contract and prefix gates. |
| CONTEXT | D-11 | Deterministic copied migration in both static modes | 01, 04 | COVERED | Canonical template, goldens, Postgres proof. |
| RESEARCH | — | TenantScope resolver and stable fail-closed errors | 01-03 | COVERED | Explicit option takes precedence over concrete config. |
| RESEARCH | — | Every root/nested/reload/update path retains tenant | 01-03 | COVERED | Surface matrix and focused regressions. |
| RESEARCH | — | Versioned JSON report plus callable module and thin Mix task | 04 | COVERED | Planner adopts the researched assumption under discretion. |
| RESEARCH | — | No new dependencies; retain Ecto/PostgreSQL/copied-generator stack | 01, 04 | COVERED | Existing locked libraries only. |
| RESEARCH | — | Named verify gates and local/CI parity | 03, 04 | COVERED | Existing inbox/admin/install/runtime/release gates. |

Specless edge resolution: Plan 01 covers empty/single/equal ordering, repeat/concurrent idempotency, and trace outcomes; Plans 02-03 cover all TENANT-02 collection/detail/mutation outcome shapes; Plan 04 covers empty/single/repeated/concurrent reconciliation. No probe item is silently dismissed.

Prohibition recall precision: bespoke non-guessing ownership and explicit compatibility transparency are kept descriptor-less as flagged-unverified prohibitions in Plan 01. Generic access control, injection, and compliance candidates are canon and are breadcrumbed to the STRIDE registers plus `$gsd-secure-phase`, not minted as duplicate prohibitions.

<verification>
Run reconciliation tests and both named migration gates during tasks; at wave completion run `mix ci.test`, `mix verify.install_golden`, `mix verify.runtime_prefix`, `mix verify.inbox`, `mix verify.admin`, and `mix ci.verify_gates` as executable phase evidence.
</verification>

<success_criteria>
- Ambiguous legacy ownership stays NULL and machine-visible until explicit assignment.
- Assignment is atomic, conflict-safe, idempotent, and never inferred.
- Both copied-migration modes generate/apply deterministically on PostgreSQL.
- Static storage routing and existing local/CI gate parity remain intact.
</success_criteria>

<output>
Create `.planning/phases/97-tenant-identity-compatible-upgrade/97-04-SUMMARY.md` when done.
</output>
