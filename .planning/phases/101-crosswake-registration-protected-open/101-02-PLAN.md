---
phase: 101-crosswake-registration-protected-open
plan: "02"
type: execute
wave: 2
depends_on: ["101-01"]
files_modified:
  - ../crosswake/lib/crosswake/policy/schema.ex
  - ../crosswake/lib/crosswake/policy/route.ex
  - ../crosswake/lib/crosswake/manifest/builder.ex
  - ../crosswake/lib/crosswake/manifest/types.ex
  - ../crosswake/test/crosswake/policy/schema_test.exs
autonomous: true
requirements: [OPEN-02]
must_haves:
  truths:
    - "D-05/OPEN-02: authoring, route structs, builder, types, and serialization share one closed notification-open representation."
    - "D-06/OPEN-02: legacy `true` normalizes to exactly action `tap`; explicit actions normalize to bounded strings and empty/unknown forms fail closed."
  assumptions:
    - statement: "OPEN-02 resolved probe: `tap` is the canonical default action and `%{actions: [binary()]}` is the canonical compiled representation."
      status: resolved
      flagged: false
  artifacts:
    - path: "../crosswake/lib/crosswake/policy/schema.ex"
      provides: "NotificationOpenPolicy normalization and validation"
    - path: "../crosswake/lib/crosswake/manifest/types.ex"
      provides: "Canonical compiled and serialized action allowlist"
  key_links:
    - from: "Crosswake.Policy.Schema.validate_notification_open/1"
      to: "Crosswake.Manifest.Types.new_route_entry/1"
      via: "normalized map passed unchanged by Route and Builder"
      pattern: "notification_open"
  prohibitions:
    - statement: "Legacy true must not grant arbitrary current or later action identifiers."
      status: unverified
      flagged: true
    - statement: "Compiled manifests must not preserve multiple divergent notification-open encodings."
      status: unverified
      flagged: true
---

<objective>Normalize the notification-open authoring and compiled contract once (D-05, D-06). Purpose: remove policy drift before expanding resolver denials. Output: one `tap`-based closed action policy from schema through serialization.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-RESEARCH.md
@.planning/phases/101-crosswake-registration-protected-open/101-PATTERNS.md
@.planning/phases/101-crosswake-registration-protected-open/101-01-SUMMARY.md
<interfaces>`Schema.validate_notification_open/1` is the only raw-authoring normalizer. `Route.notification_open` and `Types.RouteEntry.notification_open` carry `%{actions: [String.t()]}` or `nil`; builder passes that value without reinterpretation.</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Normalize true and explicit action declarations into one compiled policy</name>
  <reversibility rating="costly">The compiled manifest shape is consumed across Elixir and Swift surfaces; changing it later requires coordinated contract updates, while the locked semantics remain stable.</reversibility>
  <read_first>
    - ../crosswake/lib/crosswake/policy/schema.ex
    - ../crosswake/lib/crosswake/policy/route.ex
    - ../crosswake/lib/crosswake/manifest/builder.ex
    - ../crosswake/lib/crosswake/manifest/types.ex
    - ../crosswake/test/crosswake/policy/schema_test.exs
    - ../crosswake/test/crosswake/manifest/builder_test.exs
  </read_first>
  <files>../crosswake/lib/crosswake/policy/schema.ex, ../crosswake/lib/crosswake/policy/route.ex, ../crosswake/lib/crosswake/manifest/builder.ex, ../crosswake/lib/crosswake/manifest/types.ex, ../crosswake/test/crosswake/policy/schema_test.exs</files>
  <behavior>
    - `true` produces exactly `%{actions: ["tap"]}` in route and compiled serialized output.
    - `[actions: [:tap, :reply]]` produces ordered unique string actions without widening.
    - nil/false remain absent and empty, malformed, non-atom, duplicate, or unrecognized authoring fails validation.
  </behavior>
  <action>Write schema round-trip cases first. Choose `tap` per agent discretion and existing host/action fixtures. Make `validate_notification_open/1` return only nil or the canonical action map, enforcing a non-empty bounded unique allowlist and atom-to-string conversion at this boundary. Update `Route.t`, builder transfer, `Types.RouteEntry`, constructor, and serializer to carry that exact representation unchanged. Keep `true` only as input shorthand and ensure inspection/JSON output never emits raw true or keyword-list variants. Cite D-05 and D-06 in tests and preserve all unrelated route fields.</action>
  <verify><automated>cd ../crosswake &amp;&amp; mix test test/crosswake/policy/schema_test.exs test/crosswake/policy/route_test.exs test/crosswake/manifest/builder_test.exs</automated></verify>
  <acceptance_criteria>
    - Every accepted declaration serializes to one `%{actions: [binary()]}` contract.
    - `true` authorizes only `tap`; malformed/empty declarations fail before manifest construction.
  </acceptance_criteria>
  <done>Policy authoring and compiled output have one exact default-deny action representation.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| host policy authoring -> compiled manifest | Potentially malformed declarations become runtime authorization data. |
## STRIDE Threat Register (ASVS L1; high blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-04 | Tampering/Elevation | Schema normalization | high | mitigate | Single closed normalization with exact default and negative tests. |
| T-101-05 | Repudiation | Serialization | medium | mitigate | Deterministic normalized serialized output under tests. |
</threat_model>
## Artifacts this phase produces
- Symbol: canonical notification-open policy value with `actions` string list.
- Contract surface: `true -> tap` normalization and deterministic route-entry serialization. No new CLI.
<verification>Focused policy, route, and builder tests pass.</verification>
<success_criteria>There is no accepted or serialized policy form whose action meaning differs across layers.</success_criteria>
<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-02-SUMMARY.md` when done.</output>
