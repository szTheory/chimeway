---
phase: 101-crosswake-registration-protected-open
plan: "17"
type: execute
wave: 10
depends_on: ["101-16"]
files_modified:
  - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/metadata_sanitizer.ex
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
autonomous: true
gap_closure: true
requirements: [OPEN-01]
must_haves:
  truths:
    - "D-04/OPEN-01: authenticated registration persists caller metadata as exactly an empty map in both the current token-binding row and every append-only binding audit event."
    - "D-04/OPEN-01: camelCase token names, credentials, authorization values, PII, provider bodies, nested unknown keys, lists, keywords, and oversized values cannot survive the registry-to-Ecto persistence path."
    - "D-02/D-03/OPEN-01: closing caller metadata custody does not change idempotent observation, rotation, exact-revision invalidation, or the explicit binding/audit schema facts."
  assumptions:
    - statement: "FLAGGED ASSUMPTION — OPEN-01 is unclassified and unresolved in the spec-less edge probe; 101-VERIFICATION.md supplies the exact generic binding/audit metadata persistence acceptance repaired here."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-02 is unclassified and unresolved in the spec-less edge probe; this OPEN-01 closure does not alter or claim new manifest-policy behavior."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-03 is unclassified and unresolved in the spec-less edge probe; this OPEN-01 closure does not alter notification-open intent or queue behavior."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-04 is unclassified and unresolved in the spec-less edge probe; existing terminal no-fallback behavior remains unchanged."
      status: unresolved
      flagged: true
  artifacts:
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/metadata_sanitizer.ex"
      provides: "Exact-empty generic caller-metadata projection used by TokenBinding, TokenBindingEvent, and registry binding/audit construction"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs"
      provides: "Adversarial production-path persistence regression over both binding and audit rows"
  key_links:
    - from: "Registry.normalize_evidence/1 and Registry.build_binding_attrs/5"
      to: "TokenBinding.changeset/2"
      via: "MetadataSanitizer.sanitize/1 before and at the binding changeset boundary"
      pattern: "MetadataSanitizer.sanitize|TokenBinding.changeset"
    - from: "Registry.build_audit_attrs/2"
      to: "TokenBindingEvent.changeset/2"
      via: "the same exact-empty projection before append-only event insertion"
      pattern: "build_audit_attrs|TokenBindingEvent.changeset|MetadataSanitizer.sanitize"
  prohibitions:
    - statement: "Registration binding and audit metadata must not retain any caller-controlled token, credential, authorization, PII, provider-body, or nested unknown value."
      status: unresolved
---

<objective>
Close the remaining D-04/OPEN-01 durable metadata escape hatch across authenticated registration and append-only audit evidence.

Purpose: The host's explicit binding and audit columns already carry the closed safe facts needed for lifecycle explanation; generic caller metadata must not create a second uncontrolled persistence channel.
Output: One exact-empty generic metadata projection and an adversarial registry persistence regression proving both durable row types remain empty.
</objective>

<assumption_delta_decision>
Primary noun: explicit host-owned binding and audit schema fields. Decision: no-change — the detector fired only on the roadmap phrase “no fallback route”; that phrase is a denial invariant, not a new identity representation, and generic metadata is removed rather than promoted alongside the explicit schema.
</assumption_delta_decision>

<api_coverage_decision>
The deterministic detector returned `detected=false`. This plan changes only local Phoenix example-host sanitization and ExUnit evidence; the existing reasoned `COVERAGE.md` declaration remains valid and needs no edit.
</api_coverage_decision>

<schema_push_decision>
No schema-push task applies. The contribution's supported detector table does not include Ecto, and this plan modifies no migration or supported ORM schema path.
</schema_push_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
@.planning/phases/101-crosswake-registration-protected-open/101-16-SUMMARY.md

<interfaces>
`MetadataSanitizer.sanitize/1` is called by `Registry.normalize_evidence/1`, `Registry.build_binding_attrs/5`, `Registry.build_audit_attrs/2`, `TokenBinding.changeset/2`, and `TokenBindingEvent.changeset/2`. `TokenBinding.metadata` and `TokenBindingEvent.metadata` are Ecto map fields, while lifecycle-safe provider, platform, environment, installation, scope, state, reason, correlation, and fingerprint facts already have dedicated typed columns.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drop caller metadata across the registration-to-audit persistence path</name>
  <reversibility rating="reversible">The projection is an internal example-host privacy boundary; restoring a deliberately approved safe field later would be a local schema-contract decision behind the existing function.</reversibility>
  <read_first>
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/metadata_sanitizer.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding_event.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-16-SUMMARY.md
  </read_first>
  <files>/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/metadata_sanitizer.ex, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs</files>
  <behavior>
    - Binding observation with metadata containing raw-token-shaped values, camelCase names, credentials, authorization material, email/identity data, provider response bodies, arbitrary safe-looking keys, nested maps/lists/keywords, non-scalar values, and oversized strings succeeds only with binding metadata `%{}`.
    - Every append-only event created by that observation also persists metadata `%{}`; querying durable binding and event rows reveals none of the unique adversarial sentinels.
    - Rotation or repeat observation continues to produce the established current binding and audit lifecycle results while metadata remains empty in refreshed, replacement, displaced, and event rows.
  </behavior>
  <action>Start RED in `registry_test.exs` with a table-driven adversarial metadata corpus that includes arbitrary safe-looking and camelCase keys as well as nested token, credential, authorization/PII, provider-body, unknown, list, keyword, non-scalar, and oversized values. Drive the corpus through the public `Registry.bind_or_rotate/3` registration path, reload `TokenBinding` and all associated `TokenBindingEvent` rows from `Repo`, and assert each `metadata` field is exactly `%{}` plus every unique sentinel is absent from durable inspection. Include a repeat or rotation assertion so the refresh/merge and rotation audit builders cannot retain earlier caller data. Then replace the finite-key recursive `MetadataSanitizer.sanitize/1` contract with a constant empty projection, update its module documentation and obsolete forbidden-key helpers accordingly, and leave `sanitize_notification_open/1` behavior compatible. Do not add a replacement free-form metadata allowlist: D-04 safe facts remain in the existing typed binding/event columns, and no raw or caller-chosen value may be copied into another column or diagnostic surface.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/registry_test.exs test/crosswake_example/chimeway/notification_registration_adapter_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `MetadataSanitizer.sanitize/1` returns `%{}` for every term and contains no finite forbidden-key filter or recursive preserve-unknown branch.
    - Binding assertion: the adversarial public registration path persists and returns binding metadata exactly `%{}` for initial, repeat/refresh, and replacement rows exercised by the test.
    - Audit assertion: all append-only events produced by those operations persist metadata exactly `%{}` and contain none of the token, credential, PII, provider-body, arbitrary, nested, or oversized sentinels.
    - Regression assertion: focused registration and adapter suites exit zero with lifecycle scope and exact-revision behavior unchanged.
  </acceptance_criteria>
  <done>Authenticated registration and its durable audit history expose only explicit typed safe facts, with no generic caller-controlled metadata retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated registration caller -> host registry | Caller-controlled metadata accompanies otherwise authenticated binding evidence. |
| registry -> binding and append-only audit tables | Durable Ecto rows become operator and proof evidence. |

## STRIDE Threat Register (ASVS L1; high blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-43 | Information Disclosure | `MetadataSanitizer.sanitize/1` | high | mitigate | Exact-empty projection plus adversarial persistence assertions for binding and event rows. |
| T-101-44 | Tampering | refresh/rotation metadata merge | high | mitigate | Exercise repeat/rotation through the public registry and require empty current, displaced, and audit metadata. |
| T-101-45 | Repudiation | operator lifecycle evidence | medium | mitigate | Preserve explicit typed state/reason/correlation/fingerprint columns while removing uncontrolled caller facts. |
</threat_model>

## Artifacts this phase produces

- Modified privacy contract: `CrosswakeExample.Chimeway.MetadataSanitizer.sanitize/1` becomes an exact-empty projection for token-binding and audit caller metadata.
- New ExUnit regression cases in `/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs` covering adversarial binding and append-only audit persistence.
- No new production file, module, function, public API, Ecto field, migration, package, dependency, external service, credential, or CLI surface is created.

<verification>
Run the focused public registration and adapter suites; both must exit zero, and the new assertions must prove exact-empty binding/event persistence rather than merely scanning a returned projection.
</verification>

<success_criteria>
No caller-selected registration metadata value survives into a token-binding row or binding audit event, while existing current-binding lifecycle behavior stays green.
</success_criteria>

<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-17-SUMMARY.md` when done.</output>
