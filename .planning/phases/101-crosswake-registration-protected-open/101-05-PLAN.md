---
phase: 101-crosswake-registration-protected-open
plan: "05"
type: execute
wave: 3
depends_on: ["101-03", "101-04"]
files_modified:
  - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
  - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex
  - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent_event.ex
  - ../crosswake/examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs
  - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs
autonomous: true
requirements: [OPEN-03, OPEN-04]
must_haves:
  truths:
    - "D-07/D-10/OPEN-03: one predicate-CAS rechecks tenant, exact binding revision, expiry, session/version, and server-bound route/action and yields one consume winner."
    - "D-11/OPEN-04: replay, expiry, revocation, mismatch, logout, and tenant/session changes return closed denial states and no activation authority."
  assumptions:
    - statement: "OPEN-03 unclassified probe: a PostgreSQL conditional update inside the host transaction is the selected one-winner primitive."
      status: unresolved
      flagged: true
  artifacts:
    - path: "../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex"
      provides: "Tenant/session/exact-binding scoped one-time intent record"
    - path: "../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs"
      provides: "Concurrent consume and current-authority denial matrix"
  key_links:
    - from: "Registry.consume_intent/1"
      to: "NotificationOpenIntent and TokenBinding"
      via: "single transaction predicate and server-bound resolution"
      pattern: "update_all|consumed_at|binding_ref"
  prohibitions:
    - statement: "Intent consumption must not use a read-then-write sequence that permits two winners."
      status: unverified
      flagged: true
    - statement: "Queued evidence must not become a bearer grant after logout or tenant switch."
      status: unverified
      flagged: true
---
<objective>Make host notification intents one-time and authority-current under races (D-07, D-10, D-11). Purpose: turn reconnect evidence into exactly one reauthorized server decision. Output: scoped intent fields, atomic consume, and denial matrix.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-PATTERNS.md
@.planning/phases/101-crosswake-registration-protected-open/101-03-SUMMARY.md
@.planning/phases/101-crosswake-registration-protected-open/101-04-SUMMARY.md
<interfaces>`consume_intent/1` must return closed `OpenResolution` states and host-bound route/action. The durable intent owns tenant/session/version/exact binding/expiry; client evidence owns none of them.</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Atomically consume only a currently authorized host intent</name>
  <reversibility rating="costly">Adding durable authority predicates changes example-host intent storage and coordinated contracts, but remains host-owned and migratable.</reversibility>
  <read_first>
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
    - ../crosswake/examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs
    - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs
  </read_first>
  <files>../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex, ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex, ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent_event.ex, ../crosswake/examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs, ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs</files>
  <behavior>
    - Two concurrent consumers produce one valid resolution and one replay resolution, one consumed timestamp, and one consumed event.
    - Expired, wrong-tenant, wrong-revision, inactive-binding, wrong-session/version, and logged-out cases cannot update the intent.
    - Valid resolution returns only open_ref, route_id, action_ref, closed state, and timestamp.
  </behavior>
  <action>Write race and authority-matrix tests first. Extend the example intent schema/migration with the minimum host-owned tenant/session-version and exact-binding scope required by D-10, keeping route/action server-bound. Replace the current `repo.one -> validate -> changeset update` sequence with a single conditional update/transaction whose predicate includes issued/unconsumed, non-expired, current tenant/session/version, exact active binding revision, and server-bound intent identity. Classify zero-row results through bounded authoritative queries into the closed public states without exposing raw row data. Insert a consumed audit event only for the winning update and return the server-bound route/action in the valid resolution.</action>
  <verify><automated>cd ../crosswake/examples/phoenix_host &amp;&amp; mix test test/crosswake_example/chimeway/notification_open_intent_test.exs test/crosswake_example/chimeway/registry_notification_open_test.exs --seed 0</automated></verify>
  <acceptance_criteria>
    - Concurrent consume has exactly one valid winner and every loser is replay/closed denial.
    - Every current-authority predicate is checked by executable positive and negative cases.
    - Public result and audit details contain closed opaque facts only.
  </acceptance_criteria>
  <done>Reconnect intent consumption is atomic, replay-proof, and current-authority scoped.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| reconnect evidence -> intent registry | Offline evidence attempts to exercise current host authority. |
## STRIDE Threat Register (ASVS L1; high blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-11 | Elevation | Consume race | high | mitigate | Predicate-CAS with one-winner concurrency test. |
| T-101-12 | Spoofing | Tenant/session/binding | high | mitigate | Current scope and exact revision in atomic predicate. |
| T-101-13 | Information Disclosure | Zero-row classification | medium | mitigate | Closed state mapping and safe result assertions. |
</threat_model>
## Artifacts this phase produces
- Durable intent fields: tenant scope, session/version, exact binding revision, server-bound route/action, consumption state.
- Contract: atomic `IntentConsumer.consume_intent/1` one-winner behavior. No CLI.
<verification>Focused intent and registry suites pass with race assertions.</verification>
<success_criteria>Only one currently authorized consumer can obtain a valid resolution; all stale states activate nothing.</success_criteria>
<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-05-SUMMARY.md` when done.</output>
