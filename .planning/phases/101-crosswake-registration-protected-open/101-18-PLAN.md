---
phase: 101-crosswake-registration-protected-open
plan: "18"
type: execute
wave: 10
depends_on: ["101-15"]
files_modified:
  - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825190000_backfill_chimeway_notification_open_intent_scope.exs
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs
autonomous: true
gap_closure: true
requirements: [OPEN-03]
must_haves:
  truths:
    - "D-10/OPEN-03: a new forward migration derives a historical issued intent's scope only from its exact active bound row and preserves matched tenant, subject, installation, and session authority."
    - "D-10/D-11/OPEN-03: an unexpired matched legacy intent upgraded from the released migration boundary is consumed exactly once under matching current authenticated authority."
    - "D-04/D-10/OPEN-03: every issued row that cannot be made complete and authoritative is terminally revoked rather than guessed, left eligible, or emitted with sensitive reconciliation detail."
  assumptions:
    - statement: "FLAGGED ASSUMPTION — OPEN-01 is unclassified and unresolved in the spec-less edge probe; Plan 101-17 independently closes its remaining metadata boundary without sharing migration files."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-02 is unclassified and unresolved in the spec-less edge probe; this OPEN-03 migration does not alter compiled policy semantics."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-03 is unclassified and unresolved in the spec-less edge probe; 101-VERIFICATION.md supplies the exact matched-legacy-intent upgrade and consume acceptance repaired here."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-04 is unclassified and unresolved in the spec-less edge probe; terminal revocation uses the existing closed state and does not create a fallback route."
      status: unresolved
      flagged: true
  artifacts:
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825190000_backfill_chimeway_notification_open_intent_scope.exs"
      provides: "Forward-only exact-binding scope derivation and terminal fail-closed reconciliation"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs"
      provides: "Released-boundary migration plus public consume regression for one matched unexpired legacy intent and terminal unresolved controls"
  key_links:
    - from: "20260825190000_backfill_chimeway_notification_open_intent_scope.exs"
      to: "chimeway_token_bindings.subject_scope"
      via: "correlated exact binding_ref update restricted to complete current authority"
      pattern: "binding_ref|subject_scope|scope"
    - from: "upgraded chimeway_notification_open_intents.scope"
      to: "Registry.consume_current_intent/3"
      via: "existing equality predicate `i.scope == authenticated scope.scope` and exact active-binding EXISTS predicate"
      pattern: "i.scope ==|consume_intent|NotificationOpenEvidence"
  prohibitions:
    - statement: "Authority reconciliation must not invent notification-open scope or leave an incomplete historical issued intent eligible for consumption."
      status: unresolved
---

<objective>
Complete the forward authority upgrade so matched historical one-time intents satisfy current scope authorization and every irreparable issued row fails closed.

Purpose: Existing hosts must be able to consume a still-valid legacy tap after upgrading, without weakening the current scope equality or exact-binding predicates.
Output: One additive forward-only Ecto migration and a throwaway-database regression that migrates and consumes a matched unexpired legacy intent.
</objective>

<assumption_delta_decision>
Primary noun: one host-issued notification-open intent whose authority is derived from its exact binding revision. Decision: no-change — the detector's “fallback” signal describes a denial transition, not a second authority identity; scope remains a binding-derived fact rather than an independently chosen value.
</assumption_delta_decision>

<api_coverage_decision>
The deterministic detector returned `detected=false`. This plan repairs local Ecto persistence and ExUnit proof only; retain the existing reasoned no-external-API declaration in `COVERAGE.md` unchanged.
</api_coverage_decision>

<schema_push_decision>
No schema-push task applies. Although this is an Ecto migration, Ecto is absent from the contribution's supported Payload/Prisma/Drizzle/Supabase/TypeORM detector table; verification uses `Ecto.Migrator` against a throwaway SQLite database instead.
</schema_push_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
@.planning/phases/101-crosswake-registration-protected-open/101-10-SUMMARY.md
@.planning/phases/101-crosswake-registration-protected-open/101-15-SUMMARY.md

<interfaces>
`20260824210000_upgrade_chimeway_registration_authority.exs` adds and backfills `tenant_ref`, `subject_ref`, `session_ref`, and `session_version` but leaves the existing `scope` column unchanged. `Registry.consume_intent/1` builds current authority from `NotificationOpenEvidence.auth_context`; its CAS requires `i.scope == scope.scope`, matching tenant/subject/session or installation facts, an unexpired `issued` state, and an exact active binding through a correlated `EXISTS` query. Valid scope strings are `subject_session` and `subject_installation`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate and consume one matched legacy intent under exact current authority</name>
  <reversibility rating="one-way">The migration terminally revokes historical issued rows whose authority cannot be proven; the exact forward-only repair was already selected by the verification gap, so no decision checkpoint re-asks it.</reversibility>
  <read_first>
    - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260824210000_upgrade_chimeway_registration_authority.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825180000_enforce_chimeway_binding_scope_consistency.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
  </read_first>
  <files>/Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825190000_backfill_chimeway_notification_open_intent_scope.exs, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs</files>
  <behavior>
    - Starting at the released intent schema and pausing after migration `20260824210000`, a matched active session-scoped legacy intent has binding-derived tenant/subject/session facts but missing scope and therefore cannot satisfy current consumption.
    - Running the new migration sets that issued intent's scope to the exact binding's valid subject_scope; the public `Registry.consume_intent/1` call with matching tenant, subject, installation, session ref/version, binding ref, and an unexpired timestamp returns `:valid`, persists `consumed`, and a second call returns `:replayed`.
    - Issued controls with no exact binding, an inactive binding, invalid subject_scope, inconsistent session authority, or mismatched/incomplete backfilled authority end in `revoked` and cannot be consumed.
    - Existing consumed or revoked intents are not resurrected or rewritten into issued authority.
  </behavior>
  <action>Start RED by extending the isolated `registration_authority_migration_upgrade_test.exs` subprocess script: seed a complete active legacy binding and an intent whose expiry is safely later than the process clock, migrate only through `20260824210000`, assert tenant/subject/session were copied while scope is absent, then continue through the new migration and call the real public `Registry.consume_intent/1` with a `NotificationOpenEvidence` carrying the matching authenticated scope. Assert valid then replay outcomes plus durable consumed state/event. Seed terminal controls for absent, inactive, invalid-scope, and authority-inconsistent exact bindings, and assert each issued row becomes revoked while pre-terminal rows are not resurrected. Add `20260825190000_backfill_chimeway_notification_open_intent_scope.exs` as a forward-only migration: derive scope exclusively through the exact `binding_ref` relationship when the bound row is active, uses one of the two closed subject scopes, and its tenant/subject/session facts are complete and consistent with that scope; then terminally set every remaining non-authoritative `issued` intent to `revoked`. Do not modify migration `20260824210000`, weaken `Registry.consume_current_intent/3`, infer a default scope, copy scope from caller metadata, log row values, or add a reverse operation that pretends terminal revocation is losslessly undoable.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs test/crosswake_example/chimeway/registry_notification_open_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Migration assertion: the new migration is ordered after `20260825180000`, obtains scope only from the exact active binding's closed `subject_scope`, and revokes every remaining non-authoritative issued row.
    - Upgrade assertion: the isolated test observes NULL/absent scope after `20260824210000`, the exact derived scope after the new migration, and no edits to the historical migration.
    - Consumption assertion: the unexpired matched legacy intent returns `:valid` once and `:replayed` thereafter through `Registry.consume_intent/1`, with durable consumed state and event evidence.
    - Fail-closed assertion: unmatched, inactive, invalid-scope, and inconsistent-authority controls are terminally revoked and never become consumable; existing terminal rows are not resurrected.
    - Regression assertion: both focused migration and notification-open suites exit zero without printing seeded authority or sensitive values.
  </acceptance_criteria>
  <done>An existing host can upgrade and consume a still-valid matched legacy intent exactly once, while every intent lacking provable exact-binding authority is terminally closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| historical durable rows -> forward migration | Partial legacy authority facts are untrusted until reconciled against the exact binding row. |
| migrated intent -> current consume CAS | Durable scope crosses into live tenant/session/binding authorization. |

## STRIDE Threat Register (ASVS L1; high blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-46 | Elevation of Privilege | scope backfill | high | mitigate | Derive only from exact active binding with closed scope and complete coherent authority facts. |
| T-101-47 | Tampering | irreparable issued rows | high | mitigate | Terminally revoke every issued row that fails the authoritative derivation predicate. |
| T-101-48 | Repudiation | legacy consume result | medium | mitigate | Exercise real public consume, durable consumed state/event, and replay outcome in the isolated upgrade proof. |
| T-101-49 | Information Disclosure | migration/test output | medium | mitigate | Use opaque fixtures and assert state/contracts without logging seeded row values. |
</threat_model>

## Artifacts this phase produces

- New migration file and module: `/Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825190000_backfill_chimeway_notification_open_intent_scope.exs` and `CrosswakeExample.Repo.Migrations.BackfillChimewayNotificationOpenIntentScope`.
- New regression cases inside `/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs` for exact scope derivation, terminal reconciliation, valid legacy consumption, and replay.
- No new public registry function, Ecto field, external API, package, dependency, credential, service, or CLI surface is created.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Host binds APNs registrations and activates routes only under current host authority | 101-01..101-18 | COVERED | 101-17 closes registration evidence custody; 101-18 completes historical current-authority activation. |
| REQ | OPEN-01 | Explicit permission, authenticated binding, rotation/revocation/invalidation | 101-04, 101-06, 101-09..101-12, 101-14..101-15, 101-17 | COVERED | 101-17 closes the remaining D-04 binding/audit metadata blocker. |
| REQ | OPEN-02 | Manifest-consistent default-deny action policy | 101-02, 101-03, 101-09 | COVERED | Previously verified; unresolved spec-less row remains explicitly flagged in both closure plans. |
| REQ | OPEN-03 | Opaque queued tap and exact current one-time reauthorization | 101-01, 101-03, 101-05, 101-07, 101-09, 101-13..101-14, 101-16, 101-18 | COVERED | 101-18 supplies the missing forward scope and legacy consume proof. |
| REQ | OPEN-04 | Stale/denied opens activate no fallback with sanitized evidence | 101-01, 101-05, 101-08, 101-09 | COVERED | Previously verified; terminal migration controls do not add a route or fallback. |
| RESEARCH | — | Host-owned exact binding and intent authority with race-safe conditional writes | 101-04, 101-05, 101-10, 101-12, 101-14..101-15, 101-18 | COVERED | New migration feeds the unchanged current consume predicate. |
| RESEARCH | — | Recursive closed durable privacy boundary | 101-08, 101-14, 101-16..101-17 | COVERED | 101-17 replaces generic finite filtering with an empty projection. |
| RESEARCH | — | Normalized manifest, RouteGate, bounded opaque queue, and no-fallback outcomes | 101-01..101-03, 101-07..101-09, 101-13 | COVERED | Existing verified work is unchanged. |
| CONTEXT | D-01..D-03 | Explicit registration states and exact current host binding authority | 101-04, 101-06, 101-10..101-12, 101-14..101-15 | COVERED | Existing executed lifecycle work remains authoritative. |
| CONTEXT | D-04 | Only opaque refs, fingerprints/classifications, and closed safe durable facts | 101-08, 101-14, 101-16..101-17 | COVERED | 101-17 removes arbitrary binding/audit metadata retention. |
| CONTEXT | D-05..D-09 | Closed manifest policy, host-bound route/action, RouteGate halt, bounded opaque queue | 101-01..101-03, 101-07..101-09, 101-13 | COVERED | Previously verified; no closure-plan widening. |
| CONTEXT | D-10 | Atomic one-time consume rechecks tenant, binding, expiry, and session/version | 101-01, 101-05, 101-13..101-14, 101-18 | COVERED | 101-18 makes matched historical rows reachable by the unchanged exact predicate. |
| CONTEXT | D-11..D-13 | Current manifest/RouteGate authorization, safe outcomes, narrow host/iOS seams | 101-03, 101-06..101-09, 101-13..101-18 | COVERED | Closure remains inside the existing host persistence boundary. |

Excluded without gap: CONTEXT deferred ideas; Phase 102 digital twin, fake APNs transport, named verification commands, and CI; Phase 103 physical-iPhone proof and adoption guidance; Android/FCM; generic offline/background sync; arbitrary notification actions; dashboards, analytics, and engagement tracking. The verifier warning about malformed `issue_notification_open_intent/1` input is not planned: it is not a Phase 101 goal blocker or one of the actionable `gaps:` rows, and gap-closure mode does not expand into independent API ergonomics work.

## Prohibition Recall Audit

| Requirement | Kept descriptor-less prohibition | Plan location |
|---|---|---|
| OPEN-01 | Registration binding/audit metadata cannot retain caller-controlled sensitive or unknown values. | 101-17 `must_haves.prohibitions` |
| OPEN-03 | Authority reconciliation cannot invent scope or leave incomplete historical issued intent authority eligible. | 101-18 `must_haves.prohibitions` |

Both kept values/safety constraints are serialized descriptor-less with `statement` and `status` only, so they remain fail-closed flagged-unverified under the spec-less fallback. OPEN-02 and OPEN-04 produced no new bespoke prohibition after precision filtering: their phase-specific must-NOTs are already locked and covered by executed plans, while generic injection/compliance candidates route to the ASVS threat model. The two recalled items equal the two authored prohibition entries; none is silently dropped.

<verification>
Run the focused migration and notification-open suites; both must exit zero and prove the matched legacy intent traverses the real current consume path rather than stopping at a SQL column assertion.
</verification>

<success_criteria>
Every legacy issued intent is either fully scope-authoritative from its exact active binding and consumable once, or terminally revoked without guessed authority.
</success_criteria>

<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-18-SUMMARY.md` when done.</output>
