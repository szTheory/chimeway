---
phase: 101-crosswake-registration-protected-open
plan: "04"
type: execute
wave: 2
depends_on: ["101-01"]
files_modified:
  - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
  - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
  - ../crosswake/examples/phoenix_host/priv/repo/migrations/20260602100000_create_chimeway_token_bindings.exs
  - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
  - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs
autonomous: true
requirements: [OPEN-01]
must_haves:
  truths:
    - "D-01/D-02/OPEN-01: permission, APNs observation, and authenticated binding remain separate; same observation refreshes one revision and changed token atomically supersedes it."
    - "D-03/OPEN-01: logout, session revocation, permission loss, host revocation, and provider invalidation conditionally disable only the exact current scoped revision."
    - "D-04/OPEN-01: raw APNs token is transient and absent from durable/public diagnostic surfaces."
  assumptions:
    - statement: "OPEN-01 unclassified probe: the example host's Ecto/PostgreSQL registry is the concrete adopter-owned implementation used to prove exact revision races."
      status: unresolved
      flagged: true
  artifacts:
    - path: "../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex"
      provides: "Authenticated bind/rotate and exact-revision invalidation CAS"
    - path: "../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs"
      provides: "Idempotency, rotation, and stale invalidation race proof"
  key_links:
    - from: "Registry.bind_or_rotate/3"
      to: "TokenBinding"
      via: "tenant+installation+provider+environment+topic+subject+session scoped transaction"
      pattern: "bind_or_rotate|superseded"
    - from: "NotificationRegistrationDelegate.revokeNotificationBindingForPermissionLoss"
      to: "CrosswakeExample.Chimeway.NotificationRegistrationAdapter.revoke_for_permission_loss/2"
      via: "versioned callback transcript carrying authenticated tenant/org, subject, installation, provider/environment/topic app identity, session/version, and exact binding_ref scope"
      pattern: "permission_loss|binding_ref|session_version"
    - from: "CrosswakeExample.Chimeway.NotificationRegistrationAdapter.revoke_for_permission_loss/2"
      to: "Registry.revoke_for_permission_loss/2"
      via: "production host adapter validates the authenticated principal against the callback command and forwards the exact fully scoped CAS"
      pattern: "NotificationRegistrationAdapter|Registry.revoke_for_permission_loss"
  prohibitions:
    - statement: "APNs observation must not grant identity, session, tenant, delivery, or route authority."
      status: unverified
      flagged: true
    - statement: "A stale invalidation must not disable a rotated replacement or another scope."
      status: unverified
      flagged: true
---
<objective>Make authenticated APNs binding lifecycle exact, idempotent, and race-safe (D-01 through D-04). Purpose: ensure only the current host-authorized revision can support delivery or open. Output: scoped registry CAS and concurrency evidence.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-RESEARCH.md
@.planning/phases/101-crosswake-registration-protected-open/101-PATTERNS.md
@.planning/phases/101-crosswake-registration-protected-open/101-01-SUMMARY.md
<interfaces>`Registry.bind_or_rotate/3`, `revoke_for_logout/2`, `revoke_for_session_revocation/2`, `revoke_for_permission_loss/2`, and `apply_provider_feedback/2` are host authority commands. `TokenBinding.binding_ref` is exact revision identity.</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Enforce current scoped binding revision under observation and invalidation races</name>
  <reversibility rating="costly">Binding revision and scope constraints are durable host data semantics, but the example-host migration has not been published as a universal storage contract.</reversibility>
  <read_first>
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
    - ../crosswake/examples/phoenix_host/priv/repo/migrations/20260602100000_create_chimeway_token_bindings.exs
    - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
    - ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/redaction.ex
  </read_first>
  <files>../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex, ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex, ../crosswake/examples/phoenix_host/priv/repo/migrations/20260602100000_create_chimeway_token_bindings.exs, ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs, ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs</files>
  <behavior>
    - Repeating the current token observation refreshes `last_seen_at` without a new binding_ref.
    - A changed fingerprint creates one replacement revision and supersedes only the prior matching active revision.
    - Concurrent rotate and stale logout/provider/permission-loss invalidation leave the replacement active; unrelated installation/environment/topic/session/tenant rows remain unchanged.
    - Durable rows, events, errors, and telemetry contain no raw token.
  </behavior>
  <action>Start RED with deterministic clock and `Task.async_stream`/barrier races. Tighten binding uniqueness and registry transactions around the full D-02 scope. Same fingerprint must update the exact current row; changed fingerprint must insert a new opaque binding_ref and supersede the matching prior row atomically. Make every invalidation update predicate include the exact binding revision plus its tenant, installation, provider, environment, topic/app identity, subject, session/version, and active state; a zero-row stale update is a safe no-op. For D-03 permission loss specifically, test `Registry.revoke_for_permission_loss/2` with the exact revision observed by the native caller, rotate before the command arrives, and assert the replacement plus rows for other installations, environments, topics, sessions, and tenants remain active. Accept raw token only at the authenticated boundary long enough to produce the existing keyed fingerprint, and recursively assert it never reaches stored metadata or returned facts.</action>
  <verify><automated>cd ../crosswake/examples/phoenix_host &amp;&amp; mix test test/crosswake_example/chimeway/registry_test.exs test/crosswake_example/chimeway/registry_notification_open_test.exs --seed 0</automated></verify>
  <acceptance_criteria>
    - Same-token observation is idempotent; rotation creates one exact replacement.
    - A stale exact-revision permission-loss command after rotation changes zero replacement or unrelated-scope rows.
    - Rotation/invalidation races always retain the new revision and do not cross scope.
    - Raw token bytes do not appear in persistence, telemetry, errors, or returned maps.
  </acceptance_criteria>
  <done>Only the current fully scoped authenticated binding revision remains usable under all required lifecycle races.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| native token callback -> authenticated registry | Sensitive provider token crosses into host custody. |
| lifecycle command -> PostgreSQL | Concurrent authority changes mutate durable eligibility. |
## STRIDE Threat Register (ASVS L1; high blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-08 | Spoofing | Binding command | high | mitigate | Full authenticated scope required by registry command/tests. |
| T-101-09 | Tampering/DoS | Rotation and permission-loss invalidation | high | mitigate | Exact-revision predicate CAS plus stale permission-loss race evidence that preserves replacement and unrelated scopes. |
| T-101-10 | Information Disclosure | Token custody | high | mitigate | Immediate fingerprinting and recursive output/persistence leak tests. |
</threat_model>
## Artifacts this phase produces
- Registry commands: idempotent `bind_or_rotate/3` and exact lifecycle invalidators.
- Durable fields/constraints: current scoped binding revision and token fingerprint only. No new CLI.
<verification>Focused host registry suites pass with deterministic seed and concurrency cases.</verification>
<success_criteria>Exactly one current scoped binding survives observation, rotation, logout/revocation, permission loss, and provider invalidation races.</success_criteria>
<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-04-SUMMARY.md` when done.</output>
