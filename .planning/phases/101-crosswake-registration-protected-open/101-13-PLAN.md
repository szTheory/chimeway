---
phase: 101-crosswake-registration-protected-open
plan: "13"
type: execute
wave: 7
depends_on: ["101-09"]
files_modified:
  - /Users/jon/projects/crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NotificationOpenQueue.swift
  - /Users/jon/projects/crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NotificationOpenQueueTests.swift
autonomous: true
gap_closure: true
requirements: [OPEN-03]
must_haves:
  truths:
    - "D-09/D-10/OPEN-03: pending notification-open evidence is unique by open_ref across enqueue, persistence reload, and production reconnect drain."
    - "D-10/D-11/OPEN-03: duplicate opaque evidence causes exactly one host consume and exactly one protected activation; no later replay outcome can overwrite the allowed presentation."
    - "D-09/D-12/OPEN-03: deduplication preserves the first bounded pending item and its original age without adding authority fields, retry grants, or sensitive diagnostics."
  assumptions:
    - statement: "FLAGGED ASSUMPTION — OPEN-01 remains unclassified and unresolved in the spec-less edge probe; this OPEN-03 closure does not reclassify or claim new registration behavior."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-02 remains unclassified and unresolved in the spec-less edge probe; this OPEN-03 closure does not reclassify or claim new manifest-policy behavior."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-03 remains unclassified and unresolved in the spec-less edge probe; 101-VERIFICATION.md supplies the duplicate-evidence production-drain acceptance repaired here."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-04 remains unclassified and unresolved in the spec-less edge probe; existing terminal no-fallback coverage remains unchanged and is exercised only as regression evidence."
      status: unresolved
      flagged: true
  artifacts:
    - path: "/Users/jon/projects/crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NotificationOpenQueue.swift"
      provides: "First-wins open_ref uniqueness for new and previously persisted pending evidence"
    - path: "/Users/jon/projects/crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NotificationOpenQueueTests.swift"
      provides: "Production-drain one-consume/one-activation/final-allowed-presentation regression"
  key_links:
    - from: "NotificationOpenQueue.enqueue(_:)"
      to: "NotificationOpenQueue.drain(using:activationCoordinator:)"
      via: "one retained StoredItem per openRef, including legacy persisted duplicates compacted before drain"
      pattern: "openRef|deduplicat|handleProtectedNotificationOutcome"
    - from: "NotificationOpenQueue production drain"
      to: "ActivationCoordinator.handleProtectedNotificationOutcome(_:)"
      via: "one allowed host outcome forwarded exactly once"
      pattern: "delegate.consume|handleProtectedNotificationOutcome"
  prohibitions:
    - requirement_id: OPEN-03
      statement: "Duplicate opaque open evidence must not cause multiple host consumes or replace a successful protected activation with replay denial."
      status: unverified
      flagged: true
    - requirement_id: OPEN-03
      statement: "Queue deduplication must not extend evidence lifetime, resolve authority locally, or introduce a fallback activation path."
      status: unverified
      flagged: true
---

<objective>
Make the offline notification-open queue idempotent by open_ref through the production drain path (D-09 through D-12).

Purpose: Duplicate notification callbacks must not turn one authorized open into a later replay-denial presentation.
Output: First-wins pending-evidence deduplication and executable production-drain regression evidence.
</objective>

<assumption_delta_decision>
Primary noun: one pending opaque notification-open intent per open_ref. Decision: no-change — “no fallback route” is a terminal denial invariant, while duplicate callbacks are repeated evidence for the same one-time intent rather than additional route identities.
</assumption_delta_decision>

<api_coverage_decision>
The deterministic API-coverage detector returned `detected=false`. This plan changes only an existing local Swift queue and its XCTest; it adds no external API, SDK, service, endpoint, credential, or capability surface.
</api_coverage_decision>

<schema_push_decision>
No schema-push contribution applies; this plan changes Swift actor behavior and XCTest evidence only.
</schema_push_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-RESEARCH.md
@.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
@.planning/phases/101-crosswake-registration-protected-open/101-REVIEW.md
@.planning/phases/101-crosswake-registration-protected-open/101-07-SUMMARY.md
@.planning/phases/101-crosswake-registration-protected-open/101-09-SUMMARY.md

<interfaces>
`NotificationOpenQueue.enqueue(_:)` persists opaque `NotificationOpenEvidence`. The production `drain(using:activationCoordinator:)` sends each surviving item to `NotificationOpenDelegate.consume/1` and forwards the closed outcome to `ActivationCoordinator.handleProtectedNotificationOutcome/1`. `openRef` is the one-time intent identity; the queue must retain at most one pending item for it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deduplicate open_ref evidence before production drain</name>
  <reversibility rating="reversible">The first-wins rule is private queue behavior behind established evidence and delegate contracts, with no wire or schema change.</reversibility>
  <read_first>
    - /Users/jon/projects/crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NotificationOpenQueue.swift
    - /Users/jon/projects/crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NotificationOpenDelegate.swift
    - /Users/jon/projects/crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
    - /Users/jon/projects/crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NotificationOpenQueueTests.swift
    - /Users/jon/projects/crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ProtectedNotificationActivationTests.swift
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-07-SUMMARY.md
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-09-SUMMARY.md
  </read_first>
  <files>/Users/jon/projects/crosswake/packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NotificationOpenQueue.swift, /Users/jon/projects/crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NotificationOpenQueueTests.swift</files>
  <behavior>
    - Enqueuing the same openRef twice retains one pending StoredItem and keeps the first item's evidence and enqueuedAt rather than refreshing its lifetime.
    - Previously persisted duplicate openRef items are compacted first-wins before either drain overload calls the host.
    - Production drain with an allowed first host outcome and a replay denial available second performs one delegate consume, one protected activation, removes the pending item, and leaves the coordinator presenting the allowed live route.
    - Different openRef values preserve FIFO order, count/age bounds, terminal removal, and explicit retryable transport retention.
  </behavior>
  <action>Start RED in `NotificationOpenQueueTests` by enqueuing byte-equivalent evidence twice for one openRef, configuring `RecordingOpenDelegate` with `.allowed(...)` followed by `.denied(.replayed)`, and draining through the real `drain(using:activationCoordinator:)` overload with a real test `ActivationCoordinator` and allowed dashboard manifest. Assert one consumed evidence item, one resulting liveView dashboard presentation, one removed queue item, and no replay presentation overwrite. Add a persistence/reload assertion for one retained openRef. In `NotificationOpenQueue`, implement first-wins openRef uniqueness: after age pruning, `enqueue(_:)` returns without appending or refreshing when an item with that openRef is already pending. Also compact loaded/current items by openRef before both drain overloads so queue files written by the prior implementation cannot replay duplicates; retain the earliest stored item and original enqueuedAt, persist compaction before host calls, and keep different refs in original order. Share the compaction path between the callback and production drains so behavior cannot diverge. Do not change `NotificationOpenEvidence`, queue version, allowed/denied outcome contracts, retryable transport semantics, ActivationCoordinator, or add local route resolution/fallback.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/packages/crosswake-shell-core-ios &amp;&amp; swift test --filter NotificationOpenQueueTests &amp;&amp; swift test --filter ProtectedNotificationActivationTests</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: enqueue and both drain overloads use one openRef first-wins uniqueness path, and duplicate enqueue does not replace enqueuedAt.
    - Behavior assertion: the production-drain test records exactly one `NotificationOpenDelegate.consume(_:)` call and the coordinator's final presentation is the allowed dashboard liveView.
    - Persistence assertion: pendingEvidence and reload expose one item for the duplicate openRef, and drain leaves it absent after the allowed terminal outcome.
    - Regression assertion: retryable transport and different-openRef FIFO tests still pass; both focused Swift commands exit zero.
  </acceptance_criteria>
  <done>One opaque open_ref produces one host consume and one stable protected activation even when duplicate callbacks or legacy queue records exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| native notification callback -> durable queue | Repeated untrusted callbacks become persisted opaque evidence. |
| durable queue -> host consumer -> activation coordinator | Each surviving item can consume one-time authority and alter native presentation. |

## STRIDE Threat Register (ASVS L1; high blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-35 | Replay/Elevation | `NotificationOpenQueue` | high | mitigate | First-wins openRef uniqueness on enqueue and legacy-load/drain compaction, proven through production drain. |
| T-101-36 | Tampering | `ActivationCoordinator.presentation` | high | mitigate | Assert duplicate evidence causes one consume and final allowed presentation cannot be overwritten by replay denial. |
| T-101-37 | Denial of Service | Queue count/age bounds | medium | mitigate | Preserve bounded FIFO behavior and prevent duplicate callbacks from consuming capacity or refreshing age. |
| T-101-38 | Information Disclosure | Persisted queue | medium | mitigate | Keep the existing opaque evidence schema and serialized forbidden-field assertions unchanged. |
</threat_model>

## Artifacts this phase produces

- New private queue behavior/helper: first-wins pending-item compaction keyed by `NotificationOpenEvidence.openRef` (helper name may follow local Swift style; no public API is added).
- New XCTest behavior in `/Users/jon/projects/crosswake/packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NotificationOpenQueueTests.swift`: duplicate production drain yields one consume, one activation, and a preserved allowed presentation.
- No new production file, public symbol, persisted field, queue version, delegate method, enum case, package, dependency, external API, credential, telemetry payload, or CLI surface.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Host binds APNs registrations and activates routes only under current authority | 101-01, 101-04..101-13 | COVERED | 101-12 closes exact provider/migration authority; 101-13 closes duplicate reconnect activation. |
| REQ | OPEN-01 | Permission, authenticated binding, rotation/revocation/provider invalidation | 101-04, 101-06, 101-09..101-12 | COVERED | 101-12 addresses both remaining OPEN-01 verifier gaps. |
| REQ | OPEN-02 | Manifest-consistent default-deny notification actions | 101-02, 101-03, 101-09 | COVERED | Existing verified plans remain unchanged; unresolved edge probe is explicitly carried as a flagged assumption. |
| REQ | OPEN-03 | Opaque offline queue and current one-time reauthorization | 101-01, 101-03, 101-05, 101-07, 101-09, 101-13 | COVERED | 101-13 adds production-drain duplicate evidence regression. |
| REQ | OPEN-04 | Stale/denied opens activate no fallback and emit safe evidence | 101-01, 101-05, 101-08, 101-09 | COVERED | Existing verified terminal denial behavior remains unchanged and is rerun as regression evidence. |
| RESEARCH | — | Host-owned exact binding/intent authority with race-safe conditional writes | 101-04, 101-05, 101-10, 101-12 | COVERED | Provider invalidation now follows the same full-scope predicate pattern as permission loss. |
| RESEARCH | — | Deterministic forward compatibility for durable authority schema | 101-10, 101-12 | COVERED | Both new uniqueness domains are reconciled before index creation. |
| RESEARCH | — | Bounded opaque queue with consume-first current authorization | 101-01, 101-05, 101-07, 101-09, 101-13 | COVERED | First-wins openRef deduplication preserves one-time activation. |
| RESEARCH | — | Normalized manifest, RouteGate, closed denial/telemetry | 101-02, 101-03, 101-08, 101-09 | COVERED | No gap-plan changes needed. |
| CONTEXT | D-01 | Permission, token observation, and host binding are separate | 101-06, 101-11 | COVERED | Existing executed work unchanged. |
| CONTEXT | D-02 | Authenticated host registry is sole fully scoped binding authority | 101-04, 101-10, 101-12 | COVERED | Exact provider invalidation closes the remaining cross-scope path. |
| CONTEXT | D-03 | Only the exact current active revision is eligible under every invalidation | 101-04, 101-06, 101-10..101-12 | COVERED | Selection and write predicates plus migration upgrade are covered. |
| CONTEXT | D-04 | Raw tokens remain transient and public/durable facts stay closed | 101-04, 101-06, 101-08, 101-10..101-12 | COVERED | Existing privacy tests and scoped gap evidence remain safe. |
| CONTEXT | D-05..D-08 | One normalized host-bound policy followed by RouteGate, never fallback | 101-01..101-03, 101-08, 101-09 | COVERED | Previously verified; no deferred behavior introduced. |
| CONTEXT | D-09 | Offline tap queue is bounded opaque evidence, not a bearer grant | 101-07, 101-13 | COVERED | Duplicate evidence cannot expand lifetime or capacity. |
| CONTEXT | D-10 | One atomic host consume winner with current tenant/session/binding checks | 101-01, 101-05, 101-13 | COVERED | One openRef reaches the host once from production drain. |
| CONTEXT | D-11 | Current manifest and RouteGate recheck before native activation | 101-03, 101-07, 101-09, 101-13 | COVERED | Allowed outcome remains final after duplicate drain. |
| CONTEXT | D-12 | Stable sanitized queue/consume/denial outcomes | 101-08, 101-09, 101-13 | COVERED | No new payload or diagnostic fields are added. |
| CONTEXT | D-13 | Extend only narrow iOS seams; preserve host ownership | 101-06, 101-07, 101-11, 101-13 | COVERED | Queue-only repair, no new authority seam. |

Excluded without gap: CONTEXT deferred ideas; Phase 102 digital twin/named CI gates; Phase 103 physical-iPhone proof/docs; Android/FCM; generic offline/background sync; arbitrary notification-action framework; campaign, analytics, and engagement surfaces.

## Prohibition Recall Audit

| Requirement | Kept descriptor-less flagged prohibition | Plan location |
|---|---|---|
| OPEN-01 | Provider token evidence cannot become cross-scope revocation authority. | 101-12 `must_haves.prohibitions` |
| OPEN-01 | Reconciliation cannot guess authority or leak sensitive provider/identity data. | 101-12 `must_haves.prohibitions` |
| OPEN-03 | Duplicate evidence cannot cause multiple consumes or overwrite allowed activation. | 101-13 `must_haves.prohibitions` |
| OPEN-03 | Deduplication cannot extend lifetime, resolve locally, or add fallback. | 101-13 `must_haves.prohibitions` |

OPEN-02 and OPEN-04 produced no new bespoke prohibitions after precision filtering: their candidate constraints are already explicit locked decisions and existing executed-plan prohibitions, while generic injection/privacy items route to the ASVS threat model. The four kept rows above equal the four descriptor-less frontmatter entries across this gap plan set; none is silently dropped and no fabricated `check_*` descriptor is present.

<verification>Run the focused queue and protected-activation Swift tests; both must exit zero with one consume and the allowed presentation preserved.</verification>
<success_criteria>Duplicate or legacy repeated open_ref evidence cannot produce a second host consume or replace a successful protected activation.</success_criteria>
<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-13-SUMMARY.md` when done.</output>
