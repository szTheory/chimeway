---
phase: 101-crosswake-registration-protected-open
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/contracts.ex
  - ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/resolver.ex
  - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
  - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs
  - ../crosswake/packages/crosswake_chimeway/test/crosswake/companions/chimeway/resolver_test.exs
autonomous: true
requirements: [OPEN-03, OPEN-04]
must_haves:
  truths:
    - "D-07/D-10/OPEN-03: one opaque tap is consumed once by the authenticated host, returns a server-bound route/action, and activates only after current manifest and RouteGate authorization."
    - "D-08/D-11/OPEN-04: the second use is denied as replay and yields no fallback activation."
  assumptions:
    - statement: "OPEN-03 unclassified probe: the existing Phoenix example host is the executable adopter-owned authority fixture for the production tracer."
      status: unresolved
      flagged: true
    - statement: "OPEN-04 unclassified probe: the tracer's replay case is representative but the complete stale-authority denial cross-product remains required by later plans."
      status: unresolved
      flagged: true
  artifacts:
    - path: "../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/contracts.ex"
      provides: "Server-bound valid OpenResolution route/action contract"
    - path: "../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs"
      provides: "Executable host consume -> manifest -> RouteGate -> replay tracer"
  key_links:
    - from: "CrosswakeExample.Chimeway.Registry.consume_intent/1"
      to: "Crosswake.Companions.Chimeway.Resolver.resolve/3"
      via: "validated OpenResolution containing the host-bound route_id and action_ref"
      pattern: "consume_intent|OpenResolution"
    - from: "Crosswake.Companions.Chimeway.Resolver.resolve/3"
      to: "Crosswake.Compatibility.RouteGate.evaluate/4"
      via: "activation_source: :notification after successful consumption"
      pattern: "activation_source: :notification"
  prohibitions:
    - statement: "Client evidence must not supply route, action, tenant, binding, or session authority."
      status: unverified
      flagged: true
    - statement: "A denied notification open must not invoke fallback navigation."
      status: unverified
      flagged: true
---

<objective>
Prove one production-quality protected-open path through the host authority, companion resolver, current manifest, and RouteGate before expansion (D-07, D-08, D-10, D-11).

Purpose: Retire the hardest architectural risk—atomic host authority followed by current authorization—before broadening registration, queue, and denial cases.
Output: A real default-tap allow followed by deterministic replay denial.
</objective>

<assumption_delta_decision>
Primary noun: host-issued notification-open intent identified by open_ref and exact binding revision. Decision: no-change — the roadmap's “no fallback route” is a denial invariant, not a second route-identity representation.
</assumption_delta_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-RESEARCH.md
@.planning/phases/101-crosswake-registration-protected-open/101-PATTERNS.md

<interfaces>
`IntentConsumer.consume_intent(NotificationOpenEvidence.t())` is the sole host authority seam. `Resolver.resolve/3` returns `{:allow, decision}` or `{:deny, denial}`. Valid resolution must carry the host-bound route/action used for current policy checks; raw client route/action is evidence only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consume and authorize one default notification tap end to end</name>
  <reversibility rating="costly">The public companion resolution shape coordinates the host, resolver, and native caller, but remains versionable behind the existing behaviour seam.</reversibility>
  <read_first>
    - ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/contracts.ex
    - ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/intent_consumer.ex
    - ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/resolver.ex
    - ../crosswake/lib/crosswake/compatibility/route_gate.ex
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs
  </read_first>
  <files>../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/contracts.ex, ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/resolver.ex, ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex, ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs, ../crosswake/packages/crosswake_chimeway/test/crosswake/companions/chimeway/resolver_test.exs</files>
  <behavior>
    - A valid issued intent bound server-side to route `home` and action `tap` is atomically consumed and allowed by the current manifest and RouteGate.
    - The same opaque evidence submitted again returns a stable replay denial and no allow decision.
    - A client-supplied route/action differing from the server binding cannot redirect the authorized result.
  </behavior>
  <action>Start with a failing integration test that issues one host intent, calls the public resolver through `CrosswakeExample.Chimeway.Registry`, asserts the `home`/`tap` allow, repeats the same evidence, and asserts replay denial. Extend `OpenResolution` with validated server-bound `route_id` and `action_ref`; make the host return them only after its transaction accepts the intent. In `Resolver.resolve/3`, validate and consume before selecting the route or action, use only the returned server-bound values for exact current-manifest membership and `RouteGate.evaluate/4`, pass `activation_source: :notification`, and map every unexpected consumer result to a sanitized denial. Preserve the replaceable behaviour seam and D-04 opaque evidence boundary.</action>
  <verify><automated>cd ../crosswake/examples/phoenix_host &amp;&amp; mix test test/crosswake_example/chimeway/registry_notification_open_test.exs &amp;&amp; cd ../../packages/crosswake_chimeway &amp;&amp; mix test test/crosswake/companions/chimeway/resolver_test.exs</automated></verify>
  <acceptance_criteria>
    - One valid intent has exactly one allow outcome and one consumed audit transition.
    - Repeat submission returns `notification.open.replayed` and never returns an allow decision.
    - Resolver route/action selection comes from the valid host resolution, not untrusted evidence.
  </acceptance_criteria>
  <done>The first protected-open path is real, current-authority gated, replay-safe, and executable across host and companion boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| APNs/client evidence -> host | Untrusted opaque correlation crosses into authenticated authority. |
| host resolution -> CrossWake | Closed server-bound route/action crosses into current policy authorization. |
## STRIDE Threat Register (ASVS L1; high blocks)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-01 | Spoofing/Elevation | Resolver input | high | mitigate | Consume first and use only host-bound route/action in resolver and tests. |
| T-101-02 | Elevation | Intent race | high | mitigate | One transactional consume winner plus replay assertion. |
| T-101-03 | Information Disclosure | Denial projection | medium | mitigate | Closed resolution and sanitized denial contracts. |
</threat_model>

## Artifacts this phase produces

- Contract fields: `OpenResolution.route_id`, `OpenResolution.action_ref`.
- Runtime surface: consume-first `Resolver.resolve/3` path using server-bound intent.
- Test contract: host-to-RouteGate allow/replay tracer. No new CLI surface.

<verification>Run the two focused ExUnit files; both must exit zero in under 60 seconds.</verification>
<success_criteria>Default tap allows exactly once through current RouteGate; replay and client redirect attempts activate nothing.</success_criteria>
<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-01-SUMMARY.md` when done.</output>
