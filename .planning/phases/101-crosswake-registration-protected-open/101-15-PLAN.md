---
phase: 101-crosswake-registration-protected-open
plan: "15"
type: execute
wave: 9
depends_on: ["101-14"]
files_modified:
  - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
  - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
  - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825180000_enforce_chimeway_binding_scope_consistency.exs
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
autonomous: true
gap_closure: true
requirements: [OPEN-01]
must_haves:
  truths:
    - "D-02/D-03/OPEN-01: subject_installation bindings cannot acquire session_ref or session_version through the authenticated bind boundary, Ecto changeset, or active database rows."
    - "D-03/OPEN-01: logout and session-revocation selection are restricted to active subject_session bindings, so a valid installation-scoped binding survives session logout."
    - "OPEN-01: focused bind-plus-logout, migration, changeset, and database-bypass regressions fail before the repair and pass afterward."
  assumptions:
    - statement: "FLAGGED ASSUMPTION — OPEN-01 remains unclassified and unresolved in the spec-less edge probe; 101-VERIFICATION.md supplies the concrete installation/session separation and logout-survival acceptance repaired here."
      status: unresolved
      flagged: true
  artifacts:
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex"
      provides: "Changeset-level subject scope/session authority invariant"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825180000_enforce_chimeway_binding_scope_consistency.exs"
      provides: "Fail-closed reconciliation plus active-row insert/update database guards"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex"
      provides: "Scope-consistent bind validation and subject-session-only logout/revocation selection"
  key_links:
    - from: "Registry.bind_or_rotate/3"
      to: "TokenBinding.changeset/2 and chimeway_token_bindings active-row guards"
      via: "one subject_scope/session consistency contract at command, changeset, and database boundaries"
      pattern: "subject_scope|session_ref|session_version|subject_scope_consistency"
    - from: "Registry.revoke_for_logout/2 and Registry.revoke_for_session_revocation/2"
      to: "TokenBinding"
      via: "queries require subject_scope == :subject_session before selecting or updating binding_refs"
      pattern: "subject_scope.*subject_session|logout_revoked|session_revoked"
  prohibitions:
    - statement: "Installation-scoped authority must not be coupled to a login session or become revocable by logout/session lifecycle operations."
      status: unverified
      flagged: true
---

<objective>
Close the remaining installation/session authority failure at its command, changeset, query, and database boundaries (D-02, D-03; OPEN-01).

Purpose: Installation authority must outlive session logout by construction.
Output: Scope-consistent binding command/changeset/database enforcement, subject-session-only logout selection, and executable regression evidence.
</objective>

<assumption_delta_decision>
Primary noun: one exact active binding revision. Decision: no-change — the roadmap phrase “no fallback route” is a denial behavior, not a generalized identity-model or cardinality transition.
</assumption_delta_decision>

<api_coverage_decision>
Preserve and validate `COVERAGE.md`: this plan changes only the existing local Phoenix example-host Ecto modules, migration, and ExUnit evidence. It introduces no third-party API, endpoint, SDK, credential, or unrelated capability surface.
</api_coverage_decision>

<schema_push_decision>
The deterministic ORM scan resolves to Elixir/Ecto only. Use the next ordered Ecto migration and the existing throwaway-database migrator test; do not add a Prisma, Drizzle, Supabase, TypeORM, or schema-push command.
</schema_push_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-RESEARCH.md
@.planning/phases/101-crosswake-registration-protected-open/101-PATTERNS.md
@.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
@.planning/phases/101-crosswake-registration-protected-open/COVERAGE.md
@.planning/phases/101-crosswake-registration-protected-open/101-14-SUMMARY.md

<interfaces>
`TokenBinding.subject_scope` is `:subject_session | :subject_installation`. `TokenBinding.changeset/2` currently requires session authority only for `:subject_session`; `Registry.validate_context/1` and `binding_scope/3` currently allow installation context to carry session fields. `Registry.do_revoke_for_logout_scoped/3` and `do_revoke_for_session_revocation/2` select active rows by session without a subject-scope predicate. The example host uses SQLite and proves ordered migrations through `RegistrationAuthorityMigrationUpgradeTest`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Separate installation authority from session lifecycle at every binding boundary</name>
  <reversibility rating="costly">Reversing the locked D-03 active-row invariant would require a coordinated migration and explicit review of reconciled bindings, although their original scope/session fields remain available for that review.</reversibility>
  <read_first>
    - /Users/jon/projects/crosswake/AGENTS.md
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260824210000_upgrade_chimeway_registration_authority.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-14-SUMMARY.md
  </read_first>
  <files>/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex, /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex, /Users/jon/projects/crosswake/examples/phoenix_host/priv/repo/migrations/20260825180000_enforce_chimeway_binding_scope_consistency.exs, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs</files>
  <behavior>
    - `TokenBinding.changeset/2` rejects `:subject_installation` whenever either session field is present and retains the existing paired, non-negative requirement for `:subject_session`.
    - `Registry.bind_or_rotate/3` rejects an installation context carrying session authority before starting a transaction or creating/refreshing a binding.
    - A valid installation binding and a separate session binding can coexist; logout for the session revokes only the session binding and leaves the installation binding active.
    - Session-revocation selection also excludes installation scope as defense in depth.
    - The forward migration terminally revokes any active row whose scope/session fields disagree, preserves conforming controls, and installs named SQLite INSERT and UPDATE guards that reject any future inconsistent active row even when Ecto changesets are bypassed.
  </behavior>
  <action>Start RED in `RegistryTest` with three layers of regression evidence. First, call `TokenBinding.changeset/2` for `:subject_installation` with a non-nil session_ref/session_version pair and assert the changeset is invalid. Second, call `Registry.bind_or_rotate/3` with the same inconsistent authenticated context and assert a closed validation error with no row inserted. Third, bind a valid installation-scoped row with nil session authority plus a distinct session-scoped row for the same subject/org, call `Registry.revoke_for_logout/2` for the session, and assert only the session binding is revoked; add the analogous `revoke_for_session_revocation/2` control so both session lifecycle paths are protected (D-02/D-03/OPEN-01).

In `TokenBinding.validate_scope_consistency/1`, add an explicit error for installation scope unless both session fields are nil. In `Registry.validate_context/1`, route the constructed context through a private scope-consistency validator: session scope requires a non-empty session_ref and non-negative integer version, installation scope requires both nil, and an unknown scope fails closed. Keep `binding_scope/3` limited to forwarding this validated authority. Add `b.subject_scope == :subject_session` to both the logout and session-revocation selection queries; retain the existing subject/org/session/version and active-state predicates, audit events, closed results, and exact binding_ref update stage.

Create the next ordered Ecto migration `20260825180000_enforce_chimeway_binding_scope_consistency.exs` rather than editing released migrations. In `up/0`, terminally revoke active bindings whose persisted subject scope and session fields disagree, using the existing closed `session_revoked` lifecycle reason and timestamps without printing row values. Then create fixed-name SQLite BEFORE INSERT and BEFORE UPDATE triggers on `chimeway_token_bindings` that abort only when `NEW.state == 'active'` and the row is not either a session scope with non-empty ref/non-negative version or an installation scope with both session fields null. In `down/0`, drop the two triggers and document that reconciled eligibility is not resurrected. Extend `RegistrationAuthorityMigrationUpgradeTest` to seed one malformed installation/session row before the new migration, assert it becomes terminal while valid session and installation controls stay active, inspect `sqlite_master` for both guards, and prove a direct SQL insert/update bypass is rejected after migration. This is an Elixir/Ecto migration; run the real migrator proof and do not add any schema-push task.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/registration_authority_migration_upgrade_test.exs test/crosswake_example/chimeway/registry_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Changeset and public bind assertions reject installation scope carrying either session field and insert no binding.
    - Bind-plus-logout assertion leaves the valid installation binding active while the matching session binding is revoked; the session-revocation assertion has the same scope isolation.
    - Migration assertion terminally reconciles the malformed active row without guessing a replacement authority and preserves conforming active controls.
    - Direct SQL assertions prove both INSERT and UPDATE active-row guard paths reject inconsistent scope/session combinations after all migrations.
    - The focused deterministic ExUnit command exits zero.
  </acceptance_criteria>
  <done>Installation-scoped bindings cannot carry session authority or be selected by logout/session revocation at command, changeset, query, or active-row database boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated bind input -> registry -> Ecto binding row | Host context chooses an authority scope; inconsistent session facts could couple installation authority to logout. |
| session lifecycle command -> binding selection/update | Logout or session revocation must affect session-scoped authority only. |

## STRIDE Threat Register (ASVS L1; high blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-44 | Spoofing / Elevation of Privilege | `Registry.validate_context/1` and `TokenBinding.changeset/2` | high | mitigate | Reject installation/session mixtures at command and changeset boundaries, with direct regressions. |
| T-101-45 | Tampering / Denial of Service | active `chimeway_token_bindings` rows | high | mitigate | Reconcile malformed active rows and enforce fixed INSERT/UPDATE SQLite guards through an executable migration proof. |
| T-101-46 | Tampering / Denial of Service | logout and session-revocation queries | high | mitigate | Require `subject_scope == :subject_session` in selection and prove installation controls remain active. |
| T-101-48 | Repudiation | binding lifecycle evidence | medium | mitigate | Preserve existing closed lifecycle reasons and events while terminally reconciling malformed active rows. |
</threat_model>

## Artifacts this phase produces

- Updated private contract: `TokenBinding.validate_scope_consistency/1` rejects any session authority on `:subject_installation`.
- Updated private registry contract: validated context scope plus subject-session-only `do_revoke_for_logout_scoped/3` and `do_revoke_for_session_revocation/2` queries.
- New Ecto migration/module: `20260825180000_enforce_chimeway_binding_scope_consistency.exs` / `CrosswakeExample.Repo.Migrations.EnforceChimewayBindingScopeConsistency`.
- New database objects: named active-row subject-scope consistency INSERT and UPDATE triggers on `chimeway_token_bindings`.
- New regression behavior in existing ExUnit files: bind rejection, bind-plus-logout/session-revocation isolation, and migration reconciliation/direct-SQL guards.
- No new package, dependency, external API, credential, environment variable, CLI, telemetry field, route, mobile surface, Prisma/Drizzle/etc schema-push operation, or manual verification artifact.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Host binds APNs registrations and activates routes only under current authority | 101-01..101-16 | COVERED | 101-15 and 101-16 close the two authoritative remaining failures independently in Wave 9. |
| REQ | OPEN-01 | Permission, authenticated binding, rotation, logout/revocation, provider invalidation | 101-04, 101-06, 101-09..101-12, 101-14, 101-15 | COVERED | 101-15 enforces installation/session separation and logout survival. |
| REQ | OPEN-02 | Manifest-consistent default-deny actions | 101-02, 101-03, 101-09 | COVERED | Existing completed plans own this unresolved edge probe; verification reports the behavior satisfied. |
| REQ | OPEN-03 | Opaque offline tap, one-time consume, and current reauthorization | 101-01, 101-03, 101-05, 101-07, 101-09, 101-13, 101-14, 101-16 | COVERED | Parallel Plan 101-16 removes the remaining arbitrary durable metadata channel. |
| REQ | OPEN-04 | Stale/denied opens have no fallback and safe evidence | 101-01, 101-05, 101-08, 101-09 | COVERED | Existing completed plans own this unresolved edge probe; verification reports terminal denial behavior satisfied. |
| RESEARCH | — | Host-owned exact binding/intent authority with predicate-CAS and executable races | 101-04, 101-05, 101-10, 101-12, 101-14, 101-15 | COVERED | Command, changeset, query, and database boundaries now agree on authority scope. |
| RESEARCH | — | Closed allowlist rather than per-key privacy blacklists | 101-08, 101-16 | COVERED | Parallel Plan 101-16 uses the stricter intentional drop-all option. |
| RESEARCH | — | Normalized policy, bounded queue, RouteGate, and terminal no-fallback behavior | 101-01..101-03, 101-07..101-09, 101-13 | COVERED | Already executed and verified; no new scope is introduced. |
| CONTEXT | D-01 | Permission, token observation, and authenticated binding remain distinct | 101-06, 101-11 | COVERED | Existing completed work remains unchanged. |
| CONTEXT | D-02 | Authenticated host registry is sole scoped binding authority | 101-04, 101-10, 101-12, 101-14, 101-15 | COVERED | Inconsistent scope is rejected before bind and at durable active-row bypasses. |
| CONTEXT | D-03 | Only the exact current active revision is eligible through every revocation | 101-04, 101-06, 101-10..101-12, 101-14, 101-15 | COVERED | Logout/session lifecycle can select session scope only. |
| CONTEXT | D-04 | Raw tokens and sensitive payloads remain outside durable/public state | 101-04, 101-08, 101-10, 101-12, 101-14, 101-16 | COVERED | Parallel Plan 101-16 makes intent caller metadata empty by contract. |
| CONTEXT | D-05..D-08 | Normalized host-bound policy and RouteGate authorization, never fallback | 101-01..101-03, 101-08, 101-09 | COVERED | Previously verified; no gap-plan modification. |
| CONTEXT | D-09..D-11 | Bounded opaque queue, atomic consume, current recheck, no local grant | 101-01, 101-03, 101-05, 101-07, 101-09, 101-13, 101-14 | COVERED | Previously verified; no authority-plan change. |
| CONTEXT | D-12 | Stable sanitized lifecycle evidence without raw authority/provider data | 101-08, 101-09, 101-14, 101-16 | COVERED | Parallel Plan 101-16 closes arbitrary top-level and nested retention. |
| CONTEXT | D-13 | Narrow iOS seams; host retains persistence and authority | 101-04..101-07, 101-10..101-16 | COVERED | All new work remains inside the existing Phoenix example host. |

Excluded without gap: CONTEXT deferred ideas; Phase 102 digital twin, fake transport, named verification commands, and CI; Phase 103 physical-iPhone proof and adoption guidance; Android/FCM; generic offline/background sync; arbitrary notification actions; dashboards, analytics, and engagement tracking.

## Prohibition Recall Audit

| Requirement | Kept descriptor-less flagged prohibition | Plan location |
|---|---|---|
| OPEN-01 | Installation authority cannot be coupled to or revoked by a login session. | `must_haves.prohibitions` |

The relevant OPEN-01 spec-less assumption remains explicitly flagged. OPEN-03 is repaired by parallel Plan 101-16; OPEN-02 and OPEN-04 remain covered by completed plans and verified behavior.

<verification>
Run the focused task command from `/Users/jon/projects/crosswake/examples/phoenix_host`; it must exit zero. Then run `git -C /Users/jon/projects/crosswake diff --check` and confirm `/Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/COVERAGE.md` still declares only local Ecto/ExUnit scope.
</verification>

<success_criteria>
Installation bindings cannot acquire or be selected by session authority, and session logout/revocation leaves conforming installation authority active.
</success_criteria>

<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-15-SUMMARY.md` when done.</output>
