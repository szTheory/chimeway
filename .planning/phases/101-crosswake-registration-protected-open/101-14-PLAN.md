---
phase: 101-crosswake-registration-protected-open
plan: "14"
type: execute
wave: 8
depends_on: ["101-12"]
files_modified:
  - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
  - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex
  - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/metadata_sanitizer.ex
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs
  - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/notification_open_intent_test.exs
autonomous: true
gap_closure: true
requirements: [OPEN-01, OPEN-03]
must_haves:
  truths:
    - "D-02/D-03/OPEN-01: invalidating provider feedback accepts a valid subject_installation authority context without session fields and conditionally disables only its exact active binding revision."
    - "D-07/D-10/OPEN-03: a subject_installation binding can issue and atomically consume one server-bound notification-open intent using current tenant, subject, installation, scope, expiry, and exact binding authority."
    - "D-10/D-11/OPEN-03: installation-scoped intent consumption has one valid winner, replay is denied, and a wrong tenant, subject, installation, scope, or inactive binding never consumes the intent."
    - "D-04/D-12/OPEN-03: notification-open intent persistence recursively removes raw-token, notification-body, and provider-payload fields from caller metadata before an Ecto row can be written."
  assumptions:
    - statement: "FLAGGED ASSUMPTION — OPEN-01 remains unclassified and unresolved in the spec-less edge probe; 101-VERIFICATION.md supplies the installation-scoped feedback acceptance repaired here."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-02 remains unclassified and unresolved in the spec-less edge probe; this closure does not reclassify or alter the already-verified manifest-policy behavior."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-03 remains unclassified and unresolved in the spec-less edge probe; 101-VERIFICATION.md supplies the installation-scope and metadata-persistence acceptance repaired here."
      status: unresolved
      flagged: true
    - statement: "FLAGGED ASSUMPTION — OPEN-04 remains unclassified and unresolved in the spec-less edge probe; this closure preserves the already-verified terminal no-fallback denial behavior."
      status: unresolved
      flagged: true
  artifacts:
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex"
      provides: "Subject-scope-aware exact provider invalidation and installation-scoped one-time intent issue/consume predicates"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex"
      provides: "Scope-consistent session requirements and sanitized metadata changeset boundary"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/metadata_sanitizer.ex"
      provides: "Recursive atom/string forbidden-key removal across maps, lists, and keyword-shaped metadata"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs"
      provides: "Exact installation-scoped provider invalidation regression"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs"
      provides: "Installation-scoped issue/consume/replay and durable metadata privacy evidence"
    - path: "/Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/notification_open_intent_test.exs"
      provides: "Conditional scope validation and recursive metadata sanitizer unit evidence"
  key_links:
    - from: "Registry.provider_feedback_scope/1"
      to: "Registry.feedback_target_query/2 and do_feedback_invalidate/6"
      via: "one subject_scope-aware authority map reused by both selection and conditional update"
      pattern: "subject_scope|session_ref|session_version|binding_ref|installation_ref"
    - from: "Registry.issue_notification_open_intent/1"
      to: "NotificationOpenIntent.changeset/2"
      via: "server-derived binding scope, tenant, subject, and conditional session authority"
      pattern: "scope|subject_installation|subject_session|session_ref"
    - from: "Registry.consume_intent/1"
      to: "TokenBinding exact active revision"
      via: "scope-specific Ecto predicate inside the atomic issued-to-consumed update"
      pattern: "exists|subject_scope|installation_ref|binding_ref|state"
    - from: "NotificationOpenIntent.changeset/2"
      to: "MetadataSanitizer.sanitize/1"
      via: "sanitization after cast and before Repo.insert"
      pattern: "sanitize_metadata|MetadataSanitizer.sanitize"
  prohibitions:
    - requirement_id: OPEN-01
      category: safety
      statement: "Provider evidence for an installation-scoped binding must not become authority to invalidate another installation, tenant, subject, app identity, binding revision, or session-scoped binding."
      status: unverified
      verification: judgment
      flagged: true
    - requirement_id: OPEN-03
      category: privacy
      statement: "Notification-open intent metadata must not become a durable shadow store for raw device tokens, notification content, or provider payload bodies at any nesting depth."
      status: unverified
      verification: judgment
      flagged: true
---

<objective>
Close the remaining installation-authority and intent-persistence gaps for protected notification opens (D-02 through D-04, D-07, D-10 through D-12; OPEN-01 and OPEN-03).

Purpose: Every binding scope the host schema supports must receive exact lifecycle handling, and no caller-controlled notification payload may cross the durable intent boundary.
Output: Subject-scope-aware provider feedback and intent CAS behavior plus recursive metadata sanitization, all proven by focused ExUnit evidence.
</objective>

<assumption_delta_decision>
Primary noun: one exact active binding revision and one one-time notification-open intent. Decision: no-change — the detector matched “fallback” inside the no-fallback acceptance invariant; the closure neither changes singular/plural cardinality nor adds a fallback route.
</assumption_delta_decision>

<api_coverage_decision>
The deterministic API-coverage detector returned `detected=false`. `COVERAGE.md` records the reasoned no-external-API declaration: this closure changes only existing local Ecto modules and ExUnit evidence.
</api_coverage_decision>

<schema_push_decision>
No schema-push task applies. The scoped files are Elixir/Ecto modules and tests, not Payload, Prisma, Drizzle, Supabase, or TypeORM schema-push surfaces; the existing nullable `scope`, `session_ref`, and `session_version` columns model the two authority variants without a database-column change.
</schema_push_decision>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/101-crosswake-registration-protected-open/101-CONTEXT.md
@.planning/phases/101-crosswake-registration-protected-open/101-RESEARCH.md
@.planning/phases/101-crosswake-registration-protected-open/101-PATTERNS.md
@.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
@.planning/phases/101-crosswake-registration-protected-open/101-05-SUMMARY.md
@.planning/phases/101-crosswake-registration-protected-open/101-08-SUMMARY.md
@.planning/phases/101-crosswake-registration-protected-open/101-12-SUMMARY.md

<interfaces>
`TokenBinding.subject_scope` is `:subject_session` or `:subject_installation`; `validate_scope_consistency/1` requires `session_ref` and non-negative `session_version` only for `:subject_session`. `NotificationOpenIntent` already persists the string `scope` plus nullable `session_ref` and `session_version`. `Registry.apply_provider_feedback/2` builds authority via `provider_feedback_scope/1`, selects with `feedback_target_query/2`, then repeats the same predicates in `do_feedback_invalidate/6`. `Registry.issue_notification_open_intent/1` derives authoritative fields from the exact active binding. `Registry.consume_intent/1` atomically updates an issued intent only when an active `TokenBinding` exists. `MetadataSanitizer.sanitize/1` owns the host persistence vocabulary for raw-token, payload, and notification-content keys.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry exact subject_installation authority through feedback and one-time consume</name>
  <reversibility rating="reversible">The change branches private query construction by an already-persisted authority discriminator and preserves the public registry/consumer signatures.</reversibility>
  <read_first>
    - /Users/jon/projects/crosswake/AGENTS.md
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-05-SUMMARY.md
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-12-SUMMARY.md
  </read_first>
  <files>/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex, /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs</files>
  <behavior>
    - Provider invalidation for a `:subject_installation` binding succeeds without `session_ref` or `session_version`, changes only the exact authenticated active `binding_ref`, and leaves a session-scoped or other-installation binding active.
    - Provider invalidation for `:subject_session` retains the existing exact session-ref/version checks; missing or mismatched session authority returns the same non-disclosing `:no_active_bindings` result.
    - Issuing from a `:subject_installation` binding writes `scope: "subject_installation"` with nil session fields; issuing from `:subject_session` writes `scope: "subject_session"` with both session fields.
    - Installation-scoped evidence with matching tenant, subject, installation, scope, and exact active binding consumes once; a concurrent/subsequent attempt is replayed.
    - Wrong tenant, subject, installation, scope, or inactive exact binding returns a closed denial and leaves the intent issued.
  </behavior>
  <action>Start RED in `RegistryTest` with a `context/1` override using `subject_scope: :subject_installation`, `session_ref: nil`, and `session_version: nil`; bind a target plus session-scoped and other-installation controls, apply `:token_unregistered` feedback using authenticated context, binding_ref, installation_ref, and app_identity_ref but no session options, then assert only the target is revoked. Retain the current session-scope test and add a mismatched-session assertion. In `Registry.provider_feedback_scope/1`, validate common tenant/subject/installation/binding/app-identity authority first, then branch on `ctx.subject_scope`: require and compare session_ref/session_version only for `:subject_session`; require the binding model's nil-session semantics for `:subject_installation`. Build one subject-scope-aware Ecto predicate and use it identically in `feedback_target_query/2` and the `repo.update_all/3` write so selection and mutation cannot drift (D-02/D-03/OPEN-01).

Continue RED in `RegistryNotificationOpenTest` with a helper that inserts an active `:subject_installation` binding with nil session fields. Prove `issue_notification_open_intent/1` succeeds, persists `scope == "subject_installation"`, then `consume_intent/1` accepts auth context carrying `subject_scope`, tenant/org, subject, and installation but no session fields; assert one valid result followed by replay, one consumed event, and no client-selected route/action. Add wrong-installation and revoked-binding cases that leave the row issued. In `Registry.issue_notification_open_intent/1`, overwrite caller authority with the binding-derived `scope`, tenant, subject, and conditional session fields. In `NotificationOpenIntent.changeset/2`, validate scope against `subject_session`/`subject_installation`; require paired non-negative session fields only for session scope and require both nil for installation scope. In `Registry.notification_open_scope/1`, `consume_current_intent/3`, `intent_scope_matches?/2`, and a scope-aware `active_binding_matches?/2`, branch from the durable/validated subject scope: session scope includes exact session predicates, while installation scope includes exact binding_ref, tenant, subject, installation_ref, subject_scope, and active state without manufacturing session values. Keep the issued-to-consumed mutation atomic and preserve the current closed denial vocabulary (D-07/D-10/D-11/OPEN-03).</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/registry_test.exs test/crosswake_example/chimeway/notification_open_intent_test.exs test/crosswake_example/chimeway/registry_notification_open_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `provider_feedback_scope/1` branches on `subject_scope`, and both the feedback select and conditional update include the same common exact-binding predicates plus session predicates only for `:subject_session`.
    - Behavior assertion: installation-scoped provider feedback without session fields revokes exactly one target and preserves the session-scoped and other-installation controls.
    - Persistence assertion: installation-scoped intent issuance succeeds with `scope == "subject_installation"` and nil session fields; session-scoped issuance still requires both session fields.
    - Atomicity assertion: matching installation authority produces one valid consume and replay thereafter; mismatched installation or inactive binding produces no consumed event and leaves state `issued`.
    - Regression assertion: the focused ExUnit command exits zero under deterministic `--seed 0`.
  </acceptance_criteria>
  <done>Every advertised binding scope receives exact provider invalidation and exact one-time protected-open issue/consume semantics without widening authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close notification-open metadata persistence recursively</name>
  <reversibility rating="reversible">The sanitizer hardening affects caller metadata projection before persistence and adds no durable field, migration, or public transport contract.</reversibility>
  <read_first>
    - /Users/jon/projects/crosswake/AGENTS.md
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/metadata_sanitizer.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/token_binding_event.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/notification_open_intent_test.exs
    - /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md
    - /Users/jon/projects/chimeway/.planning/phases/101-crosswake-registration-protected-open/101-08-SUMMARY.md
  </read_first>
  <files>/Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/metadata_sanitizer.ex, /Users/jon/projects/crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/notification_open_intent_test.exs, /Users/jon/projects/crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs</files>
  <behavior>
    - `NotificationOpenIntent.changeset/2` sanitizes metadata after cast, before any insert, using the same host-owned sanitizer as token binding and binding-event rows.
    - Atom- and string-keyed raw token fields, notification title/body fields, provider/raw payload fields, and provider response bodies are absent from the changeset and persisted intent.
    - Forbidden keys are removed recursively inside nested maps, lists, and keyword-shaped values; safe scalar siblings remain available as bounded explainable facts.
    - Non-map metadata is projected to an empty map, matching the existing sanitizer boundary.
  </behavior>
  <action>Start RED in `NotificationOpenIntentTest` with separate top-level and nested adversarial metadata containing atom and string variants of raw token, APNs/device token, notification_body/title, provider_payload/raw_payload, and provider_response_body keys alongside safe scalar siblings. Assert `Ecto.Changeset.get_change(changeset, :metadata)` contains no forbidden key or sentinel at any depth and preserves the safe siblings. In `RegistryNotificationOpenTest`, issue an intent with unique raw-token, notification-body, and nested-payload sentinels, reload the `NotificationOpenIntent` row, and assert neither the row metadata nor `inspect(intent)` contains any sentinel. Update `MetadataSanitizer.sanitize/1` to recurse through maps, lists, and keyword-shaped structures without atomizing string keys; drop each existing forbidden atom/string key at any depth and retain sanitized safe values. Wire `NotificationOpenIntent.changeset/2` through a private `sanitize_metadata/1` step after `cast/3`, matching `TokenBinding` and `TokenBindingEvent`, so direct schema use and registry issuance share one closed persistence boundary (D-04/D-12/OPEN-03). Do not log rejected values, copy them into intent events, add metadata keys, or change the public intent/telemetry/denial shapes.</action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/notification_open_intent_test.exs test/crosswake_example/chimeway/registry_notification_open_test.exs test/crosswake_example/chimeway/registry_test.exs --seed 0</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `NotificationOpenIntent.changeset/2` invokes the shared metadata sanitizer after metadata casting and before validations/insert return.
    - Unit assertion: top-level and nested atom/string forbidden keys are absent while safe scalar siblings survive; non-map metadata becomes `%{}`.
    - Persistence assertion: a reloaded intent row and its inspected representation contain none of the unique raw-token, notification-body, or nested-payload sentinels.
    - Regression assertion: token binding/event sanitizer consumers and all three focused ExUnit files remain green with `--seed 0`.
  </acceptance_criteria>
  <done>Caller-controlled notification-open metadata cannot persist raw token, notification content, or provider payload material at any nesting depth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| APNs/provider evidence -> authenticated host registry | Provider-supplied token facts request a mutation but cannot choose host tenant, subject, installation, session, app identity, or binding authority. |
| Opaque notification evidence -> host intent consumer | Client evidence requests one-time activation while current binding authority remains server-owned. |
| Caller metadata -> Ecto changeset -> durable intent row | Untrusted nested maps/lists could otherwise persist raw tokens, notification content, or provider bodies. |

## STRIDE Threat Register (ASVS L1; high blocks)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-101-39 | Spoofing / Elevation of Privilege | `Registry.provider_feedback_scope/1` | high | mitigate | Branch by durable subject_scope and require exact authenticated binding, tenant, subject, installation, app identity, and session predicates where applicable. |
| T-101-40 | Tampering / Denial of Service | `feedback_target_query/2` and invalidation update | high | mitigate | Reuse one scope-specific predicate for read and write; executable controls prove unrelated bindings remain active. |
| T-101-41 | Elevation of Privilege / Replay | `consume_current_intent/3` | high | mitigate | Atomic issued-to-consumed update requires durable intent scope plus the exact current active binding; prove one winner and closed mismatch/replay results. |
| T-101-42 | Information Disclosure | `NotificationOpenIntent.metadata` | high | mitigate | Recursively remove forbidden token/body/payload keys at the changeset boundary and scan the reloaded row with unique sentinels. |
| T-101-43 | Repudiation | intent and binding events | medium | mitigate | Preserve one issued/consumed event sequence and existing closed outcome vocabulary without copying unsafe metadata into audit details. |
</threat_model>

## Artifacts this phase produces

- Updated `CrosswakeExample.Chimeway.Registry.provider_feedback_scope/1`, `feedback_target_query/2`, and `do_feedback_invalidate/6` behavior: exact predicates branch by `TokenBinding.subject_scope` and share one selection/mutation authority contract.
- Updated `CrosswakeExample.Chimeway.Registry.issue_notification_open_intent/1`, `notification_open_scope/1`, `consume_current_intent/3`, `intent_scope_matches?/2`, and scope-aware `active_binding_matches?/2` behavior: `subject_session` and `subject_installation` follow distinct exact predicates through issue and atomic consume.
- New private `CrosswakeExample.Chimeway.NotificationOpenIntent.validate_scope_consistency/1` and `sanitize_metadata/1` helpers; existing `scope`, `session_ref`, and `session_version` fields carry the closed authority variant without a migration.
- Updated `CrosswakeExample.Chimeway.MetadataSanitizer.sanitize/1` recursive behavior and a private recursive value helper for nested maps, lists, and keyword-shaped values.
- New installation-scope, replay/mismatch, top-level metadata, nested metadata, and persisted-sentinel cases in the three existing ExUnit files listed in `files_modified`.
- New planning artifact `.planning/phases/101-crosswake-registration-protected-open/COVERAGE.md` containing the reasoned no-external-API declaration.
- No new production file, public API, database column, migration, package, dependency, credential, external service, telemetry event, denial code, route, or native/mobile surface.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan coverage | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Host binding and route activation only under current authority | 101-01..101-14 | COVERED | 101-14 closes the two remaining authority-scope and intent-persistence gaps. |
| REQ | OPEN-01 | Permission, registration, binding rotation/revocation/provider invalidation | 101-04, 101-06, 101-09..101-12, 101-14 | COVERED | 101-14 adds exact installation-scoped provider invalidation. |
| REQ | OPEN-02 | Manifest-consistent default-deny actions | 101-02, 101-03, 101-09 | COVERED | Verified behavior is unchanged; the unresolved fallback-probe row remains flagged. |
| REQ | OPEN-03 | Opaque queue, one-time consume, current authority recheck | 101-01, 101-03, 101-05, 101-07, 101-09, 101-13, 101-14 | COVERED | 101-14 carries installation scope through issue/consume and closes metadata persistence. |
| REQ | OPEN-04 | Stale/denied opens have no fallback and safe evidence | 101-01, 101-05, 101-08, 101-09 | COVERED | Verified terminal behavior is unchanged and rerun as regression evidence. |
| RESEARCH | — | Host-owned exact binding/intent authority with predicate-CAS | 101-04, 101-05, 101-10, 101-12, 101-14 | COVERED | Subject-scope-aware read/write predicates preserve the responsibility map. |
| RESEARCH | — | Opaque evidence and recursive privacy boundary | 101-07..101-09, 101-13, 101-14 | COVERED | Intent metadata now uses the existing host sanitizer recursively before persistence. |
| RESEARCH | — | Normalized manifest, RouteGate, terminal no-fallback denial | 101-01..101-03, 101-08, 101-09 | COVERED | No closure modification is required. |
| CONTEXT | D-01 | Explicit permission, observation, then authenticated binding | 101-06, 101-11 | COVERED | Existing executed behavior remains unchanged. |
| CONTEXT | D-02 | Authenticated host registry is sole binding authority | 101-04, 101-10, 101-12, 101-14 | COVERED | Installation scope now reaches provider feedback and intent consumption. |
| CONTEXT | D-03 | Only exact current active revision is eligible | 101-04, 101-06, 101-10..101-12, 101-14 | COVERED | Subject-scope-specific predicates retain exact binding identity. |
| CONTEXT | D-04 | Raw tokens/content/payload remain outside durable/public state | 101-04, 101-08, 101-10, 101-12, 101-14 | COVERED | Recursive intent changeset sanitization closes the remaining durable boundary. |
| CONTEXT | D-05..D-08 | Normalized server-bound policy and RouteGate, never fallback | 101-01..101-03, 101-08, 101-09 | COVERED | Previously verified and not reopened. |
| CONTEXT | D-09 | Bounded opaque offline evidence, no local authority | 101-07, 101-13 | COVERED | Previously verified and not reopened. |
| CONTEXT | D-10 | Atomic one-winner consume rechecks current authority | 101-01, 101-05, 101-13, 101-14 | COVERED | Installation scope now has executable one-winner/mismatch evidence. |
| CONTEXT | D-11 | Current manifest and RouteGate before activation | 101-03, 101-07, 101-09 | COVERED | Host resolution remains upstream of the verified resolver/gate path. |
| CONTEXT | D-12 | Stable sanitized queue/consume/denial outcomes | 101-08, 101-09, 101-14 | COVERED | Intent storage cannot retain raw caller payload material. |
| CONTEXT | D-13 | Narrow iOS seams; host owns auth/persistence/CAS | 101-04..101-07, 101-10..101-14 | COVERED | Closure is confined to the example host; no native authority moves. |

Excluded without gap: CONTEXT deferred ideas; Phase 102 digital twin and named CI gates; Phase 103 physical-iPhone proof and adoption docs; Android/FCM; generic offline/background sync; arbitrary notification-action framework; analytics and engagement surfaces.

## Prohibition Recall Audit

| Requirement | Kept descriptor-less flagged prohibition | Plan location |
|---|---|---|
| OPEN-01 | Installation-scoped provider evidence cannot become cross-scope invalidation authority. | `must_haves.prohibitions` |
| OPEN-03 | Intent metadata cannot become a durable shadow payload/token/body store. | `must_haves.prohibitions` |

OPEN-02 and OPEN-04 yielded no new bespoke values/safety/ethics prohibitions after precision filtering: their candidates duplicate locked product behavior or canonical security/privacy controls already represented by the threat model. The two kept rows equal the two descriptor-less frontmatter entries; neither is silently dropped and neither fabricates a `check_*` descriptor.

<verification>Run the focused registry, intent changeset, and protected-open ExUnit files with `MIX_ENV=test` and `--seed 0`; all must exit zero with exact installation-scoped lifecycle behavior and no persisted privacy sentinel.</verification>
<success_criteria>Subject-installation bindings support exact provider invalidation and exact one-time protected opens, while notification-open intent metadata cannot persist raw token, notification-body, or payload fields.</success_criteria>
<output>Create `.planning/phases/101-crosswake-registration-protected-open/101-14-SUMMARY.md` when done.</output>
