---
phase: 74-prefixed-migration-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/chimeway.gen.migrations.ex
  - lib/chimeway/install/migrations.ex
  - test/chimeway/install/migrations_test.exs
autonomous: true
requirements:
  - MIG-01
  - MIG-03
must_haves:
  truths:
    - "Running mix chimeway.gen.migrations with no prefix argument selects the chimeway generation mode."
    - "Running mix chimeway.gen.migrations --prefix chimeway selects the same chimeway generation mode."
    - "Running mix chimeway.gen.migrations --prefix public selects explicit public legacy generation mode."
    - "Unsupported flags, positional args, and unsupported prefix values fail with actionable Mix errors."
    - "Generation mode comes from CLI arguments and not runtime application prefix config."
  artifacts:
    - path: "lib/mix/tasks/chimeway.gen.migrations.ex"
      provides: "strict OptionParser surface for generation prefix mode"
      exports: ["run/1"]
    - path: "lib/chimeway/install/migrations.ex"
      provides: "mode-aware render pipeline for one canonical migration template tree"
      exports: ["run/1", "render_template/3", "rewrite_namespace/2"]
    - path: "test/chimeway/install/migrations_test.exs"
      provides: "CLI and installer-core generation-mode contracts"
  key_links:
    - from: "lib/mix/tasks/chimeway.gen.migrations.ex"
      to: "lib/chimeway/install/migrations.ex"
      via: "calls Chimeway.Install.Migrations.run(prefix: mode)"
      pattern: "Migrations\\.run\\(prefix:"
    - from: "lib/chimeway/install/migrations.ex"
      to: "priv/chimeway_migrations/*.exs"
      via: "render_template/3 composes namespace rewrite with explicit prefix sentinel rendering"
      pattern: "render_template"
---

<objective>
Create the generation-mode interface and installer-core plumbing for Phase 74. This plan implements D-01, D-02, D-03, D-04, D-05, and D-21 by making `mix chimeway.gen.migrations` default to `:chimeway`, accepting only `--prefix chimeway` and `--prefix public`, rejecting unsupported input, and passing the normalized mode into the installer without reading runtime prefix config.

Purpose: establish a deterministic CLI and core render contract before template conversion starts.
Output: strict Mix task parsing, mode-aware installer rendering hooks, and focused option tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/74-prefixed-migration-generator/74-CONTEXT.md
@.planning/phases/74-prefixed-migration-generator/74-RESEARCH.md
@.planning/phases/74-prefixed-migration-generator/74-PATTERNS.md
@.planning/phases/74-prefixed-migration-generator/74-VALIDATION.md
@.planning/phases/73-storage-prefix-contract/73-01-SUMMARY.md
@.planning/phases/73-storage-prefix-contract/73-02-SUMMARY.md
@.planning/phases/73-storage-prefix-contract/73-03-SUMMARY.md
@lib/mix/tasks/chimeway.gen.migrations.ex
@lib/chimeway/install/migrations.ex
@test/chimeway/install/migrations_test.exs
</context>

## Artifacts This Phase Produces

- CLI flags: default no-flag generation, `--prefix chimeway`, `--prefix public`.
- Mix task symbols: `Mix.Tasks.Chimeway.Gen.Migrations.run/1`, private `parse_prefix_mode!/1`, private `usage/0` or equivalent actionable error text.
- Installer symbols: `Chimeway.Install.Migrations.run/1` extended with `prefix: :chimeway | :public`, `render_template/3`, private `normalize_generation_prefix!/1`, private `prefix_attribute/1`.
- Generated migration helpers: `@chimeway_prefix`, `chimeway_prefix_opts/1`, `chimeway_table/2`, `chimeway_index/3`, `chimeway_unique_index/3`, `chimeway_references/2`, `chimeway_relation/1`, `create_chimeway_schema/0`.
- Test files: `test/chimeway/install/migrations_test.exs`, `test/chimeway/install/golden_diff_test.exs`, `test/chimeway/install/idempotency_test.exs`, `test/chimeway/install/prefix_contract_test.exs`, `test/chimeway/migration_contract_test.exs`.
- Fixture paths: `test/fixtures/installer_golden_prefixed/STDOUT.txt`, `test/fixtures/installer_golden_prefixed/tree/priv/repo/migrations/`, `test/fixtures/installer_golden_public/STDOUT.txt`, `test/fixtures/installer_golden_public/tree/priv/repo/migrations/`.
- Gate symbols: existing Mix alias `ci.install_golden` updated for dual-mode installer proof and migration contract proof; existing CI job `install_golden_contract` updated for path-gated parity.
- Struct fields and helper modules: none created in this phase.

## Source Coverage Audit

| Source | ID | Requirement or Decision | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Make copied host migrations deterministic and schema-respectful, with `chimeway` as default and public as explicit compatibility | 74-01, 74-02, 74-03, 74-04, 74-05 | COVERED | CLI, templates, fixtures, DB proof, and CI gate cover the phase goal. |
| REQ | MIG-01 | Default generator emits dedicated `chimeway` schema migrations | 74-01, 74-02, 74-05 | COVERED | CLI default plus first migration schema creation and golden/DB proof. |
| REQ | MIG-02 | Prefixed migrations create schema and qualify Ecto operations plus raw SQL | 74-02, 74-03, 74-04, 74-05 | COVERED | Template batches plus static and DB contracts. |
| REQ | MIG-03 | Explicit public legacy generation emits unprefixed migrations | 74-01, 74-05 | COVERED | CLI mode and public fixture/static proof. |
| REQ | MIG-04 | Golden, idempotency, and migration contract tests prove both modes | 74-05 | COVERED | Dual fixture roots, idempotency loops, static contracts, DB contract, CI parity. |
| RESEARCH | R-CLI | Strict OptionParser with closed prefix set and actionable errors | 74-01 | COVERED | No custom argv parser or runtime config derivation. |
| RESEARCH | R-TEMPLATE | One canonical `priv/chimeway_migrations/` tree using helpers instead of duplicated trees or broad rewrites | 74-02, 74-03, 74-04 | COVERED | Template batches are disjoint and all use the same helper pattern. |
| RESEARCH | R-SQL | Raw SQL qualification through fixed Chimeway relation helpers | 74-02, 74-03, 74-05 | COVERED | Raw SQL templates 009, 027, and 030 are converted and tested. |
| RESEARCH | R-VERIFY | Dual golden fixtures, idempotency, static contract, DB contract, CI path gate | 74-05 | COVERED | Final proof plan owns all verification artifacts. |
| RESEARCH | R-PACKAGES | No new package installs | 74-01, 74-05 | COVERED | Existing Elixir/Ecto/PostgreSQL stack only. |
| CONTEXT | D-01, D-02, D-03, D-04, D-05 | Generator interface and strict CLI behavior | 74-01 | COVERED | Implemented by task actions in this plan. |
| CONTEXT | D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13 | One template tree, local helpers, schema creation, explicit qualification, safe rollback shape, no runtime-prefix mechanism | 74-02, 74-03, 74-04 | COVERED | Implemented across template batches. |
| CONTEXT | D-14, D-15, D-16, D-17, D-18, D-19, D-20 | Dual-mode fixtures, subprocess tests, idempotency, static and DB contracts, CI scope | 74-05 | COVERED | Implemented by final proof plan. |
| CONTEXT | D-21, D-22, D-23, D-24, D-25 | Generator norms, Ecto/Postgres semantics, copied migration reviewability, Oban separation, golden tests as product contract | 74-01, 74-02, 74-03, 74-04, 74-05 | COVERED | Cited in task actions and verification plan. |

Deferred context items are excluded from this plan set: runtime prefix propagation, demo host proof, Oban guidance, manual data move guidance, dynamic prefixes, and production data move tooling.

## Dependency Graph

| Plan | Creates | Needs | Wave |
|---|---|---|---|
| 74-01 | CLI/core generation-mode contract | Phase 73 prefix decisions | 1 |
| 74-02 | Template helper conversion for migrations 001-015 | 74-01 render sentinel contract | 2 |
| 74-03 | Template helper conversion for migrations 016-030 | 74-01 render sentinel contract | 2 |
| 74-04 | Template helper conversion for migration 031 and whole-template source checks | 74-01 render sentinel contract | 2 |
| 74-05 | Dual fixtures, idempotency, static contract, DB contract, CI parity | 74-01, 74-02, 74-03, 74-04 | 3 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add generation-mode CLI and render tests</name>
  <files>test/chimeway/install/migrations_test.exs</files>
  <read_first>
    @test/chimeway/install/migrations_test.exs
    @lib/mix/tasks/chimeway.gen.migrations.ex
    @lib/chimeway/install/migrations.ex
    @.planning/phases/74-prefixed-migration-generator/74-CONTEXT.md
    @.planning/phases/74-prefixed-migration-generator/74-PATTERNS.md
  </read_first>
  <behavior>
    - Default subprocess invocation accepts no flags and exits 0.
    - `--prefix chimeway` exits 0 and routes through the same generation mode as default.
    - `--prefix public` exits 0 and routes through public legacy generation mode.
    - Unsupported prefix values, unknown switches, and positional args fail with a message that names the accepted flags.
    - `Chimeway.Install.Migrations.render_template/3` replaces a sample prefix sentinel with the selected mode while preserving namespace rewrite behavior.
    - Runtime application prefix config changes do not alter the generation mode selected by command arguments.
  </behavior>
  <action>Add failing ExUnit coverage for D-01 through D-05 in the existing installer test file. Reuse the current tmp-host and subprocess patterns from `migrations_test.exs`; do not call private Mix task functions directly. Add a render-unit test around the new installer render seam so this plan can pass before all 31 canonical templates are converted. Include an assertion that generation code does not inspect runtime prefix config per D-04. Keep the canonical CLI to D-02 only; do not introduce a second public-mode flag.</action>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/install/migrations_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: default, `--prefix chimeway`, and `--prefix public` subprocess cases are covered.
    - CLI assertion: invalid prefix, unknown flag, and positional-arg failures mention the accepted `--prefix chimeway` and `--prefix public` forms.
    - Source assertion: tests prove runtime application prefix config does not drive generated output.
    - Test command: focused installer test command passes.
  </acceptance_criteria>
  <done>Focused tests fail before implementation and pass after Task 2 implements strict parsing and render plumbing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict CLI parsing and installer render plumbing</name>
  <files>lib/mix/tasks/chimeway.gen.migrations.ex, lib/chimeway/install/migrations.ex</files>
  <read_first>
    @lib/mix/tasks/chimeway.gen.migrations.ex
    @lib/chimeway/install/migrations.ex
    @test/chimeway/install/migrations_test.exs
    @.planning/phases/74-prefixed-migration-generator/74-CONTEXT.md
    @.planning/phases/74-prefixed-migration-generator/74-RESEARCH.md
    @.planning/phases/74-prefixed-migration-generator/74-PATTERNS.md
  </read_first>
  <behavior>
    - `Mix.Tasks.Chimeway.Gen.Migrations.run/1` uses strict OptionParser parsing with a closed prefix value set.
    - The Mix task passes a normalized `:chimeway` or `:public` mode into `Chimeway.Install.Migrations.run/1`.
    - `Chimeway.Install.Migrations.run/1` keeps slug idempotency, host repo resolution, timestamp sequencing, and namespace rewrite behavior intact.
    - `render_template/3` composes namespace rewrite with narrow prefix sentinel rendering.
    - No generator code reads runtime storage prefix config.
  </behavior>
  <action>In `lib/mix/tasks/chimeway.gen.migrations.ex`, add `@switches [prefix: :string]`, private `parse_prefix_mode!/1`, and concise usage/error text that implements D-01, D-02, D-03, D-04, D-05, and D-21. Keep `Mix.Task.run("app.config")` for host repo inference, but do not read runtime prefix config to select output. In `lib/chimeway/install/migrations.ex`, extend `run/1` to accept `prefix: :chimeway | :public`, add `render_template/3`, `normalize_generation_prefix!/1`, and `prefix_attribute/1`, and call `render_template/3` where `rewrite_namespace/2` is currently used. The render step must only replace the explicit template prefix sentinel and must not perform relation-name rewrites; Plans 74-02 through 74-04 implement D-06 and D-07 relation qualification through template helpers.</action>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/install/migrations_test.exs --warnings-as-errors</automated>
    <automated>mix format --check-formatted lib/mix/tasks/chimeway.gen.migrations.ex lib/chimeway/install/migrations.ex test/chimeway/install/migrations_test.exs</automated>
    <automated>bash -lc 'if rg -n "Application\\.(fetch|get)_env\\(:chimeway, :prefix|Chimeway\\.Storage\\.validate_prefix|Chimeway\\.Storage\\.repo_opts" lib/mix/tasks/chimeway.gen.migrations.ex lib/chimeway/install/migrations.ex; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the Mix task has a strict `@switches` declaration for `prefix: :string`.
    - Source assertion: the Mix task calls `Chimeway.Install.Migrations.run(prefix: mode)`.
    - Source assertion: installer core has `render_template/3` and keeps `rewrite_namespace/2`.
    - Negative source assertion: generator code does not call runtime prefix validation or application prefix config reads.
    - Test command: focused installer tests and targeted formatting pass.
  </acceptance_criteria>
  <done>Generation mode is deterministic from CLI args, accepted modes route through installer core, invalid inputs fail, and existing installer behavior remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| shell argv -> Mix task | User-controlled CLI arguments select generated output mode. |
| Mix task -> installer core | Normalized generation mode crosses into file-rendering code. |
| installer core -> filesystem | Rendered host migration files are written into `priv/repo/migrations`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-74-01 | Tampering | `Mix.Tasks.Chimeway.Gen.Migrations` | mitigate | Closed-set `OptionParser` parsing accepts only default/chimeway/public generation modes and fails unsupported input with actionable errors. |
| T-74-02 | Tampering | `Chimeway.Install.Migrations.render_template/3` | mitigate | Rendering only replaces explicit sentinels and does not apply broad relation rewrites. |
| T-74-03 | Information Disclosure | generated stdout and errors | mitigate | CLI messages describe structural generation mode only and do not inspect payload or operator data. |
| T-74-SC | Tampering | package manager surface | accept | This plan installs no new packages; Phase 74 uses existing Elixir/Ecto/PostgreSQL dependencies. |
</threat_model>

<verification>
Run the task-level automated commands. This plan is complete only when focused installer tests pass, targeted formatting passes, and the runtime-config negative source assertion is clean.
</verification>

<success_criteria>
`mix chimeway.gen.migrations` has a strict deterministic generation-mode interface, installer core can render the chosen mode from explicit sentinels, and no runtime prefix config is used to determine generated output.
</success_criteria>

<output>
Create `.planning/phases/74-prefixed-migration-generator/74-01-SUMMARY.md` when done.
</output>
