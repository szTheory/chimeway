---
phase: 74-prefixed-migration-generator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/chimeway.gen.migrations.ex
  - lib/chimeway/install/migrations.ex
  - test/chimeway/install/migrations_test.exs
autonomous: true
requirements:
  - MIG-01
  - MIG-03
must_haves:
  truths:
    - "Running mix chimeway.gen.migrations with no prefix argument selects chimeway generation mode."
    - "Running mix chimeway.gen.migrations --prefix chimeway selects the same chimeway generation mode."
    - "Running mix chimeway.gen.migrations --prefix public selects explicit public legacy generation mode."
    - "Unsupported flags, positional args, and unsupported prefix values fail with actionable Mix errors."
    - "Generation mode comes from CLI arguments and not runtime application prefix config."
  artifacts:
    - path: "lib/mix/tasks/chimeway.gen.migrations.ex"
      provides: "strict OptionParser surface for generation prefix mode"
      exports: ["run/1"]
    - path: "lib/chimeway/install/migrations.ex"
      provides: "mode-aware render pipeline for one canonical migration template tree"
      exports: ["run/1", "render_template/3", "rewrite_namespace/2"]
    - path: "test/chimeway/install/migrations_test.exs"
      provides: "CLI and installer-core generation-mode contracts"
  key_links:
    - from: "lib/mix/tasks/chimeway.gen.migrations.ex"
      to: "lib/chimeway/install/migrations.ex"
      via: "calls Chimeway.Install.Migrations.run(prefix: mode)"
      pattern: "Migrations\\.run\\(prefix:"
    - from: "lib/chimeway/install/migrations.ex"
      to: "priv/chimeway_migrations/*.exs"
      via: "render_template/3 composes namespace rewrite with explicit prefix sentinel rendering"
      pattern: "render_template"
---

<objective>
Create the generation-mode interface and installer-core plumbing for Phase 74. This plan implements D-01, D-02, D-03, D-04, D-05, and D-21 by making `mix chimeway.gen.migrations` default to `:chimeway`, accepting only `--prefix chimeway` and `--prefix public`, rejecting unsupported input, and passing the normalized mode into the installer without reading runtime prefix config.

Purpose: establish a deterministic CLI and core render contract before template conversion starts.
Output: strict Mix task parsing, mode-aware installer rendering hooks, and focused option tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/74-prefixed-migration-generator/74-CONTEXT.md
@.planning/phases/74-prefixed-migration-generator/74-RESEARCH.md
@.planning/phases/74-prefixed-migration-generator/74-PATTERNS.md
@.planning/phases/74-prefixed-migration-generator/74-VALIDATION.md
@.planning/phases/73-storage-prefix-contract/73-01-SUMMARY.md
@.planning/phases/73-storage-prefix-contract/73-02-SUMMARY.md
@.planning/phases/73-storage-prefix-contract/73-03-SUMMARY.md
@lib/mix/tasks/chimeway.gen.migrations.ex
@lib/chimeway/install/migrations.ex
@test/chimeway/install/migrations_test.exs
</context>

## Source Coverage Audit

| Source | ID | Requirement or Decision | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Make copied host migrations deterministic and schema-respectful, with `chimeway` as default and public as explicit compatibility | 74-01..74-10 | COVERED | CLI, template batches, fixtures, DB proof, and gates cover the phase goal. |
| REQ | MIG-01 | Default generator emits dedicated `chimeway` schema migrations | 74-01, 74-02, 74-09, 74-10 | COVERED | CLI default, first migration schema creation, golden proof, and DB proof. |
| REQ | MIG-02 | Prefixed migrations create schema and qualify Ecto operations plus raw SQL | 74-02..74-08, 74-10 | COVERED | Template batches plus static and DB contracts. |
| REQ | MIG-03 | Explicit public legacy generation emits unprefixed migrations | 74-01, 74-02..74-09, 74-10 | COVERED | CLI mode, public helpers, public fixtures, and public DB proof. |
| REQ | MIG-04 | Golden, idempotency, and migration contract tests prove both modes | 74-09, 74-10 | COVERED | Dual fixture roots, idempotency loops, static contracts, DB contracts, and CI parity. |
| RESEARCH | R-CLI | Strict OptionParser with closed prefix set and actionable errors | 74-01 | COVERED | No custom argv parser or runtime config derivation. |
| RESEARCH | R-TEMPLATE | One canonical `priv/chimeway_migrations/` tree using helpers instead of duplicated trees or broad rewrites | 74-02..74-08 | COVERED | Template batches are disjoint and all use the same helper pattern. |
| RESEARCH | R-SQL | Raw SQL qualification through fixed Chimeway relation helpers | 74-03, 74-07, 74-10 | COVERED | Raw SQL templates 009, 027, and 030 are converted and tested. |
| RESEARCH | R-ROLLBACK | No destructive schema cleanup; no schema drop unless RESTRICT is explicitly proved | 74-02, 74-10 | COVERED | Generated migrations omit schema-drop SQL and DB proof checks rollback behavior. |
| RESEARCH | R-VERIFY | Dual golden fixtures, idempotency, static contract, DB contract, CI path gate | 74-09, 74-10 | COVERED | Final proof plans own all verification artifacts. |
| RESEARCH | R-PACKAGES | No new package installs | 74-01..74-10 | COVERED | Existing Elixir/Ecto/PostgreSQL stack only. |
| CONTEXT | D-01, D-02, D-03, D-04, D-05 | Generator interface and strict CLI behavior | 74-01 | COVERED | Implemented by this plan. |
| CONTEXT | D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13 | One template tree, local helpers, schema creation, explicit qualification, safe rollback shape, no runtime-prefix mechanism | 74-02..74-08, 74-10 | COVERED | Implemented across template batches and DB proof. |
| CONTEXT | D-14, D-15, D-16, D-17, D-18, D-19, D-20 | Dual-mode fixtures, subprocess tests, idempotency, static and DB contracts, CI scope | 74-09, 74-10 | COVERED | Implemented by fixture and gate plans. |
| CONTEXT | D-21, D-22, D-23, D-24, D-25 | Generator norms, Ecto/Postgres semantics, copied migration reviewability, Oban separation, golden tests as product contract | 74-01..74-10 | COVERED | Cited in task actions and verification. |

Deferred context items are excluded from this plan set: runtime prefix propagation, demo host proof, Oban guidance, manual data move guidance, dynamic prefixes, and production data move tooling.

## Dependency Graph

| Plan | Creates | Needs | Wave |
|---|---|---|---|
| 74-01 | CLI/core generation-mode contract | Phase 73 prefix decisions | 1 |
| 74-02 | Template helper conversion for migrations 001-005 | 74-01 render sentinel contract | 2 |
| 74-03 | Template helper conversion for migrations 006-010 | 74-01 render sentinel contract | 2 |
| 74-04 | Template helper conversion for migrations 011-015 | 74-01 render sentinel contract | 2 |
| 74-05 | Template helper conversion for migrations 016-020 | 74-01 render sentinel contract | 2 |
| 74-06 | Template helper conversion for migrations 021-025 | 74-01 render sentinel contract | 2 |
| 74-07 | Template helper conversion for migrations 026-030 | 74-01 render sentinel contract | 2 |
| 74-08 | Template helper conversion for migration 031 | 74-01 render sentinel contract | 2 |
| 74-09 | Dual fixtures and dual-mode idempotency | 74-02..74-08 | 3 |
| 74-10 | Static, DB, verify alias, and CI parity contracts | 74-09 | 4 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement generation-mode CLI and render plumbing with tests</name>
  <files>lib/mix/tasks/chimeway.gen.migrations.ex, lib/chimeway/install/migrations.ex, test/chimeway/install/migrations_test.exs</files>
  <read_first>
    @test/chimeway/install/migrations_test.exs
    @lib/mix/tasks/chimeway.gen.migrations.ex
    @lib/chimeway/install/migrations.ex
    @.planning/phases/74-prefixed-migration-generator/74-CONTEXT.md
    @.planning/phases/74-prefixed-migration-generator/74-RESEARCH.md
    @.planning/phases/74-prefixed-migration-generator/74-PATTERNS.md
  </read_first>
  <behavior>
    - Default subprocess invocation accepts no flags and exits 0.
    - `--prefix chimeway` exits 0 and routes through the same generation mode as default.
    - `--prefix public` exits 0 and routes through public legacy generation mode.
    - Unsupported prefix values, unknown switches, and positional args fail with a message that names accepted flags.
    - `Chimeway.Install.Migrations.render_template/3` replaces a sample prefix sentinel with the selected mode while preserving namespace rewrite behavior.
    - Runtime application prefix config changes do not alter the generation mode selected by command arguments.
  </behavior>
  <action>Write the failing ExUnit expectations first, then implement the production changes in the same task so the task is independently verifiable. In `lib/mix/tasks/chimeway.gen.migrations.ex`, add strict `OptionParser` parsing with only `prefix: :string`, normalize missing or `chimeway` to `:chimeway`, normalize `public` to `:public`, reject unsupported input with actionable Mix errors, and keep `Mix.Task.run("app.config")` for host repo inference. In `lib/chimeway/install/migrations.ex`, extend `run/1` to accept `prefix: :chimeway | :public`, add `render_template/3`, `normalize_generation_prefix!/1`, and `prefix_attribute/1`, and call `render_template/3` where namespace rewrite currently happens. The render step must only replace the explicit template prefix sentinel and must not rewrite relation names; Plans 74-02 through 74-08 implement D-06 and D-07 through template helpers. Do not read runtime storage prefix config to choose generated output, per D-04.</action>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/install/migrations_test.exs --warnings-as-errors</automated>
    <automated>mix format --check-formatted lib/mix/tasks/chimeway.gen.migrations.ex lib/chimeway/install/migrations.ex test/chimeway/install/migrations_test.exs</automated>
    <automated>bash -lc 'if rg -n "Application\\.(fetch|get)_env\\(:chimeway, :prefix|Chimeway\\.Storage\\.validate_prefix|Chimeway\\.Storage\\.repo_opts" lib/mix/tasks/chimeway.gen.migrations.ex lib/chimeway/install/migrations.ex; then exit 1; fi'</automated>
  </verify>
  <done>Focused tests pass, formatting passes, invalid CLI inputs fail clearly, accepted modes route through installer core, and generator code does not derive output from runtime prefix config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| shell argv -> Mix task | User-controlled CLI arguments select generated output mode. |
| Mix task -> installer core | Normalized generation mode crosses into file-rendering code. |
| installer core -> filesystem | Rendered host migration files are written into `priv/repo/migrations`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-74-01 | Tampering | `Mix.Tasks.Chimeway.Gen.Migrations` | mitigate | Closed-set `OptionParser` parsing accepts only default/chimeway/public generation modes and fails unsupported input with actionable errors. |
| T-74-02 | Tampering | `Chimeway.Install.Migrations.render_template/3` | mitigate | Rendering only replaces explicit sentinels and does not apply broad relation rewrites. |
| T-74-03 | Information Disclosure | generated stdout and errors | mitigate | CLI messages describe structural generation mode only and do not inspect payload or operator data. |
| T-74-SC | Tampering | package manager surface | accept | This plan installs no new packages; Phase 74 uses existing Elixir/Ecto/PostgreSQL dependencies. |
</threat_model>

<verification>
Run the task-level automated commands. This plan is complete only when focused installer tests pass, targeted formatting passes, and the runtime-config negative source assertion is clean.
</verification>

<success_criteria>
`mix chimeway.gen.migrations` has a strict deterministic generation-mode interface, installer core can render the chosen mode from explicit sentinels, and no runtime prefix config is used to determine generated output.
</success_criteria>

<output>
Create `.planning/phases/74-prefixed-migration-generator/74-01-SUMMARY.md` when done.
</output>
