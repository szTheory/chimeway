---
phase: 79-front-door-and-docs-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - mix.exs
  - guides/introduction/golden-path.md
  - test/chimeway/doc_contract_test.exs
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
requirements: [DOCS-14, DOCS-15, DOCS-16, DOCS-17, ADPT-01]

must_haves:
  truths:
    - "README leads with a local-first value prop and preserves the explainability promise (DOCS-14 / D-01)"
    - "README states use cases, non-goals, host-owned boundaries, and optional/preview-surface status (DOCS-15 / D-01)"
    - "README snippets show the notifier notification_key/0 stable key, tenant_id, idempotency_key, storage prefix, and Chimeway.Traces.explain_delivery trace lookup — all on the real public API (DOCS-16 / D-04)"
    - "The three Flows stubs are delinked from README nav and mix.exs docs.extras (files stay on disk); golden-path legacy jonlunsford owner URLs are canonicalized to szTheory (DOCS-17 / D-05 / D-06)"
    - "The packaged (unpacked-Hex) README carries the new DOCS-14/15/16 invariants (ADPT-01 / D-07)"
  artifacts:
    - "README.md rewritten as an additive superset with four decision sections"
    - "mix.exs docs.extras with the three Flows stub entries removed (multi-step-journeys.md kept)"
    - "guides/introduction/golden-path.md with canonical szTheory URLs"
    - "test/chimeway/doc_contract_test.exs README describe extended with new markers, per-trigger invariant, and guards"
    - "test/chimeway/release_gate_contract_test.exs unpacked describe extended with packaged-README invariants"
  key_links:
    - "README decision-marker strings are byte-identical to the doc_contract README assertions AND the release_gate packaged-README assertions (marker-string lockstep, D-07 <-> D-09)"
    - "mix.exs extras L244 multi-step-journeys.md preserved (content-enforced by doc_contract_test.exs:29-90)"
    - "The three stub files remain on disk (cross-linked from getting-started.md:97 and password-reset-support-trace.md:103,122)"
---

<objective>
Rewrite Chimeway's public first-hop docs (README front door + golden-path URL hygiene + Flows-stub delinking) so a new adopter can understand the local-first ownership model, explainability contract, host boundaries, non-goals, optional surfaces, install flow, and the traceable trigger-to-explainability path — then LOCK that story with executable ExUnit contract assertions, including proof it survives Hex packaging.

Purpose: DOCS-14/15/16/17 + ADPT-01. This is a docs/IA rewrite whose truth is enforced by string assertions inside existing `describe` blocks (project convention: docs truth = executable contract). No new runtime code or attack surface.

Output: rewritten `README.md`; delinked stubs in `README.md` + `mix.exs`; canonicalized URLs in `guides/introduction/golden-path.md`; extended `doc_contract_test.exs` README describe; extended `release_gate_contract_test.exs` unpacked describe.

Ordering rationale (doc-first, then lock): each task boundary is a GREEN gate. Task 1 rewrites docs while keeping the existing contracts green (proving no D-02 required string was dropped and no forbidden string introduced). Task 2 then pins the new README story with byte-identical marker strings. Task 3 pins the packaged-README story using the same strings. This avoids intentional-RED task gates (this is a standard, non-TDD plan) while still fully locking the target by end of plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/79-front-door-and-docs-ia/79-CONTEXT.md
@.planning/phases/79-front-door-and-docs-ia/79-RESEARCH.md
@.planning/phases/79-front-door-and-docs-ia/79-PATTERNS.md
@.planning/phases/79-front-door-and-docs-ia/79-VALIDATION.md

@README.md
@guides/introduction/golden-path.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README as additive superset + delink Flows stubs + fix golden-path URLs (DOCS-14/15/16/17, D-01/D-04/D-05/D-06)</name>
  <files>README.md, mix.exs, guides/introduction/golden-path.md</files>
  <read_first>
    - guides/introduction/golden-path.md — the working, contract-green reference for the DOCS-16 snippet chain (notification_key/0 return value, Chimeway.trigger/3 with both opts, prefix config, Chimeway.Traces.explain_delivery with delivery_id from result.trace). Copy API shapes from here, NOT from memory. Also the file whose legacy URLs (L167, L171, L191, L192) are fixed in this task.
    - README.md (current 70-line file) — the additive-superset base; every existing section and every D-02 required string stays.
    - mix.exs docs.extras (the Flows group: trigger-to-delivery.md, policy-and-preferences.md, async-dispatch.md, then multi-step-journeys.md) — delink the three stubs, KEEP multi-step-journeys.md.
    - 79-RESEARCH.md "Contract-Required Strings (D-02)" table (all strings to PRESERVE) and "Forbidden Strings (D-02)" table (all strings to NEVER introduce, each with file:line of its live guard).
    - 79-RESEARCH.md "Common Pitfalls / Landmines" (Pitfalls 1-5) — especially the recipient-identity substring landmine and the multi-step-journeys.md keep-line landmine.
    - 79-PATTERNS.md "README.md — rewrite as additive superset" and "mix.exs — delink 3 Flows stubs" and "golden-path.md — fix stale legacy URLs" sections.
  </read_first>
  <action>
    Rewrite README.md as an ADDITIVE SUPERSET of the current file per D-01: keep the current Installation, storage-prefix, Quick Start, Documentation, and License sections verbatim in substance, and lead with a local-first embedded-notification value proposition plus the preserved explainability promise. Add exactly four decision sections with these headings: `## When to use`, `## Non-goals`, `## Host-owned boundaries`, and `## Optional surfaces`. In Optional surfaces, frame chimeway_admin and chimeway_inbox as in-repo preview/path packages that are not published on Hex yet (D-03) — never as published Hex installs. Use the sibling-status wording already enforced on the packaged guides (the `in-repo preview/path package` and `not published on Hex yet` phrases at release_gate_contract_test.exs:518-522) so the strings stay in lockstep.

    Reproduce the DOCS-16 canonical snippet chain from golden-path (D-04), showing on the REAL public API: (1) the notifier stable key as the notification_key/0 return value plus version/0 — never as a raw string argument to trigger; (2) Chimeway.trigger/3 with BOTH idempotency_key and tenant_id present on every trigger example (tenant_id is required); (3) `config :chimeway, prefix: "chimeway"`; (4) the trace lookup via Chimeway.Traces.explain_delivery/1 with delivery_id sourced from the trigger result trace (there is no Chimeway.explain_delivery delegate).

    PRESERVE every D-02 required string enumerated in 79-RESEARCH.md (mix chimeway.gen.migrations, the Chimeway.trigger Quick Start snippet, idempotency_key, tenant_id, the golden-path link, `{:chimeway, "~> 1.0"}`, the canonical CI badge URL, all six storage-prefix phrases, and all three integration-guide nav links: mailglass/accrue/inbox). Introduce NONE of the forbidden strings enumerated in the 79-RESEARCH.md "Forbidden Strings" table. CRITICAL landmine (Pitfall 1): the README forbid at doc_contract_test.exs:1357-1360 is a PLAIN substring match on the recipient-identity token — do NOT copy golden-path's recipients/1 callback into README; keep recipient-identity wiring out of README entirely.

    DOCS-17 delink (D-05): in README, remove ONLY the trigger-to-delivery Flow nav link (current README.md:65); leave all other nav links. In mix.exs docs.extras, remove ONLY the three Flows stub entries (trigger-to-delivery.md, policy-and-preferences.md, async-dispatch.md); KEEP multi-step-journeys.md (Pitfall 3 — it is content-enforced by doc_contract_test.exs:29-90). Do NOT delete the three stub files from disk (Pitfall 2 — policy-and-preferences.md is cross-linked from getting-started.md:97 and password-reset-support-trace.md:103,122).

    D-06 URL fix: in guides/introduction/golden-path.md, replace the four stale legacy jonlunsford GitHub owner URLs (L167, L171, L191, L192) with the canonical `https://github.com/szTheory/chimeway`. Grep the file for the jonlunsford owner token afterward to confirm zero remain.
  </action>
  <verify>
    <automated>mix test test/chimeway/doc_contract_test.exs test/chimeway/release_gate_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The existing doc_contract and release_gate contract suites stay fully GREEN (all D-02 required strings preserved in README; no forbidden string introduced; the hexdocs-extras ordering contract intact after the mix.exs delink; the unpacked-artifact test still passes).
    - `grep -n "github.com/jonlunsford/chimeway" guides/introduction/golden-path.md` returns zero matches; `grep -c "https://github.com/szTheory/chimeway" guides/introduction/golden-path.md` is >= 1.
    - `grep -n "guides/flows/trigger-to-delivery.md" README.md` returns zero matches; the three stub files still exist on disk (`ls guides/flows/{trigger-to-delivery,policy-and-preferences,async-dispatch}.md` succeeds).
    - `grep -c "guides/flows/multi-step-journeys.md" mix.exs` is >= 1 (kept); the three stub entries are gone from mix.exs docs.extras.
    - README contains the four new decision-section headings and every DOCS-16 snippet is on the public API surface (Chimeway.trigger, Chimeway.Traces.explain_delivery, notification_key, prefix), with the recipient-identity substring absent.
  </acceptance_criteria>
  <done>README.md is an additive-superset decision page on the real public API; stubs are delinked from README + mix.exs (files intact, multi-step-journeys.md kept); golden-path URLs are canonical; the pre-existing contract suites remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the README rewrite in doc_contract_test.exs (DOCS-14/15/16, D-09) + golden-path URL guard (D-06) + README sibling-install forbid (D-03)</name>
  <files>test/chimeway/doc_contract_test.exs</files>
  <read_first>
    - test/chimeway/doc_contract_test.exs, describe "README install doc contract (GATE-01)" (L1330-1391) — the block to extend: setup reads README.md; `@required ~w()` single-token list (L1367-1376); `@storage_prefix_required_strings` applied via a `for` loop (L1385-1390); existing forbid guards including the plain-substring recipient-identity guard (L1357-1360) and the Chimeway.Workflow regex guard (L1362-1365).
    - test/chimeway/doc_contract_test.exs, describe "golden path doc contract" (L1179-1262) — the SIBLING superset block; copy the per-trigger invariant test VERBATIM from L1247-1261, and use it as the home for the new golden-path legacy-URL forbid guard.
    - 79-PATTERNS.md "doc_contract_test.exs — extend README install doc contract" — the three assertion idioms (single-token `~w()` list; explicit string-list attribute for multi-word phrase markers mirroring `@storage_prefix_required_strings`; the per-trigger regex-count invariant; the regex-forbid idiom).
    - 79-RESEARCH.md "Marker strings to assert" and "Wave 0 Gaps" — candidate anchors and the recommended guards.
  </read_first>
  <action>
    Extend ONLY the "README install doc contract (GATE-01)" describe block with new assertions that pin the Task 1 rewrite. Use the marker strings actually written into README in Task 1 (D-09 grants discretion on wording; keep them byte-identical to what Task 3 will assert on the packaged README — marker-string lockstep).

    Append the single-token marker `Chimeway.Traces.explain_delivery` to the existing README `@required ~w()` list (L1367-1376). For the multi-word / heading / phrase markers, add a NEW explicit string-list module attribute (e.g. `@readme_decision_markers`) mirroring the `@storage_prefix_required_strings` list idiom (NOT `~w()`, which splits on whitespace) with its own `for`/`assert String.contains?` loop, containing: the value-prop phrase `local-first`; the four decision-section headings `## When to use`, `## Non-goals`, `## Host-owned boundaries`, `## Optional surfaces`; and the preview-status phrases `in-repo preview/path package` and `not published on Hex yet`.

    Add a README per-trigger invariant test copied verbatim from the golden-path block (doc_contract_test.exs:1247-1261): count Chimeway.trigger( occurrences and assert each is matched one-to-one by an idempotency_key: and a tenant_id: occurrence in the README content.

    Add a README sibling-install forbid (D-03, recommended in 79-RESEARCH.md Open Q2) mirroring the regex/substring forbid idiom already in the block: refute any current-Hex install claim for the sibling packages (the `chimeway_admin`/`chimeway_inbox` `~> 1.0` dep-tuple form forbidden on the packaged guides at release_gate_contract_test.exs:525-528).

    Add a golden-path legacy-URL guard (D-06, recommended in 79-RESEARCH.md Pitfall 4 / Open Q1) so the Task 1 URL fix cannot silently regress: in the golden path describe block, read guides/introduction/golden-path.md and refute it contains the legacy jonlunsford owner GitHub URL, mirroring the release_gate legacy-URL guard loop idiom (release_gate_contract_test.exs:351-357).

    Do NOT weaken or remove any existing guard. Do NOT create a new test file (D-09).
  </action>
  <verify>
    <automated>mix test test/chimeway/doc_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The new README `@required` marker (Chimeway.Traces.explain_delivery), the new decision-marker string list (local-first, the four section headings, the two preview-status phrases), the README per-trigger idempotency_key/tenant_id invariant, the README sibling-install forbid, and the golden-path legacy-URL guard all PASS against the Task 1 files (GREEN).
    - The new phrase/heading markers use an explicit string-list attribute, not `~w()` (verify no multi-word marker was placed in a `~w()` list).
    - All pre-existing doc_contract tests still pass (no guard weakened).
    - Marker strings asserted here are identical to those Task 3 will assert on the packaged README.
  </acceptance_criteria>
  <done>The README rewrite is pinned by executable assertions in the existing describe block; the golden-path URL fix and sibling-install rule are guarded; `mix test test/chimeway/doc_contract_test.exs` is green.</done>
</task>

<task type="auto">
  <name>Task 3: Lock packaged-doc truth in release_gate_contract_test.exs (ADPT-01, D-07)</name>
  <files>test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    - test/chimeway/release_gate_contract_test.exs, describe "unpacked Hex package artifact truth (TRUTH-01/TRUTH-02/TRUTH-03, D-08)" (L466-531) — setup builds+unpacks the real prod package via build_unpacked_package!/0 and exposes `root`; the test "unpacked Hex package carries package truth docs and source links" (L493-530) reads the packaged README (`File.read!(Path.join(root, @readme))`, L495) and asserts install constraint / sibling status / absence of legacy URL. Module attributes @readme (L12), @canonical_repo_url (L14), @legacy_repo_url (L15).
    - 79-PATTERNS.md "release_gate_contract_test.exs — extend unpacked Hex package artifact truth" — the read-from-root-then-assert idiom and the marker-string lockstep note.
    - The final marker strings chosen in Task 2 (must be reused byte-identical here).
  </read_first>
  <action>
    Extend the existing "unpacked Hex package artifact truth" describe block (D-07) so the PACKAGED README is asserted to carry the new DOCS-14/15/16 public-story invariants. After the existing `readme = File.read!(Path.join(root, @readme))` read (L495) in the "carries package truth docs and source links" test (or as a sibling test in the same block that reads the packaged README the same way), add `assert String.contains?(readme, ...)` lines for the marker strings finalized in Task 2: the value-prop phrase (local-first), the `## Non-goals` heading, the host-boundary heading/phrase, the optional-surface status phrase (in-repo preview/path package), and the trace snippet Chimeway.Traces.explain_delivery.

    Reuse the EXACT marker strings from Task 2 (byte-identical) so the source-tree README contract and the packaged README contract stay in lockstep. Do not build a new fresh-host harness (79-RESEARCH.md "Don't Hand-Roll") — extend this block only. Do NOT weaken the existing assertions in the block.
  </action>
  <verify>
    <automated>mix test test/chimeway/release_gate_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The extended unpacked-artifact test asserts the packaged README carries the value-prop phrase, `## Non-goals` heading, host-boundary marker, optional-surface status phrase, and Chimeway.Traces.explain_delivery — and PASSES (the packaged README, rebuilt from source in setup, carries them because Task 1 wrote them into README.md).
    - The asserted strings are byte-identical to the Task 2 doc_contract markers.
    - All pre-existing release_gate tests still pass.
    - Full lane green: `mix ci.verify_gates` passes.
  </acceptance_criteria>
  <done>ADPT-01 is proven: the packaged (unpacked-Hex) README carries the new public-story invariants; `mix test test/chimeway/release_gate_contract_test.exs` and `mix ci.verify_gates` are green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts This Phase Produces

**New README section headings (the DOCS-15 IA symbols):**
- `## When to use`
- `## Non-goals`
- `## Host-owned boundaries`
- `## Optional surfaces`

**New doc_contract_test.exs symbols (test guard symbols):**
- New string-list module attribute (e.g. `@readme_decision_markers`) holding the multi-word / heading markers: `local-first`, `## When to use`, `## Non-goals`, `## Host-owned boundaries`, `## Optional surfaces`, `in-repo preview/path package`, `not published on Hex yet` — asserted via a new `for` loop of `String.contains?` tests against README.
- `Chimeway.Traces.explain_delivery` appended to the existing README `@required ~w()` list.
- New README test: per-trigger `idempotency_key` / `tenant_id` invariant (regex-count, mirrored from golden-path L1247-1261).
- New README forbid test: sibling-package current-Hex install claim (chimeway_admin / chimeway_inbox `~> 1.0` dep-tuple form).
- New golden-path guard test: refutes the legacy jonlunsford owner GitHub URL in guides/introduction/golden-path.md.

**New release_gate_contract_test.exs symbols:**
- New `assert String.contains?(readme, ...)` invariant lines inside the "unpacked Hex package artifact truth" describe, asserting the packaged README carries: `local-first`, `## Non-goals`, the host-boundary marker, `in-repo preview/path package`, and `Chimeway.Traces.explain_delivery` (byte-identical to the doc_contract markers).

**Config edit (no new symbol):**
- `mix.exs` docs.extras: three Flows stub entries removed (`trigger-to-delivery.md`, `policy-and-preferences.md`, `async-dispatch.md`); `multi-step-journeys.md` kept.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs -> adopter | Adopters copy README/golden-path snippets into their own apps; an inaccurate or forbidden API string misleads them into calling a private/non-existent API or an unsafe config. This is the only surface this phase touches. No new runtime input path, auth, crypto, or data access is added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-79-01 | Tampering/Information | README.md public snippets (DOCS-16) | low | mitigate | Executable doc-contract required markers + forbid guards (Chimeway.Trigger.trigger, recipient-identity substring, Chimeway.Workflow, storage-prefix drift phrases) pin snippets to the real public API surface at doc_contract_test.exs:1336-1391; the packaged-README invariants (Task 3) prove the same story survives Hex packaging. |
| T-79-02 | Tampering | guides/introduction/golden-path.md legacy owner URL | low | mitigate | New golden-path legacy-URL guard (Task 2) refutes the jonlunsford owner URL so the D-06 fix cannot silently regress (golden-path was previously outside the release_gate package-facing file list). |
| T-79-03 | Spoofing/Information | Optional-surface copy (chimeway_admin / chimeway_inbox) | low | mitigate | New README sibling-install forbid (Task 2, D-03) refutes a current-Hex `~> 1.0` install claim and the preview-status phrases are required, mirroring the packaged-guide guards, so adopters are not told a preview/path package is a published Hex install. |

No high-severity threats: this phase changes only Markdown, mix.exs docs config, and ExUnit string assertions. No package-manager installs occur (npm/pip/cargo), so the supply-chain checkpoint (T-{phase}-SC) is N/A and no Package Legitimacy audit is required. Security enforcement is ASVS L1, block-on high — nothing in this register blocks.
</threat_model>

<verification>
- Quick lane (after every task): `mix test test/chimeway/doc_contract_test.exs test/chimeway/release_gate_contract_test.exs`
- Full lane (after the plan / before /gsd-verify-work): `mix ci.verify_gates` green.
- Manual (D-08 companion, not the ADPT-01 anchor): `cd examples/chimeway_demo_host && mix demo.up --check`.
- Manual narrative check (Success Criterion 1, human judgment): read the rewritten README top-to-bottom as a first-time adopter and confirm value prop -> when-to-use -> non-goals -> host boundary -> optional surfaces -> install -> trigger->explain reads coherently.
</verification>

<success_criteria>
- DOCS-14: README leads with a local-first value prop + explainability promise (asserted).
- DOCS-15: README states use cases, non-goals, host-owned boundaries, optional/preview-surface status (asserted).
- DOCS-16: public snippets show the notification_key/0 stable key, tenant_id, idempotency_key, storage prefix, and Chimeway.Traces.explain_delivery — all on the real public API (asserted, per-trigger invariant enforced).
- DOCS-17: the three Flows stubs are delinked from README + mix.exs extras (files on disk, multi-step-journeys.md kept); golden-path legacy URLs canonicalized and guarded.
- ADPT-01: the packaged (unpacked-Hex) README carries the new public-story invariants (asserted).
- `mix ci.verify_gates` green.
</success_criteria>

<output>
Create `.planning/phases/79-front-door-and-docs-ia/79-01-SUMMARY.md` when done.
</output>
