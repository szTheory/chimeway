---
phase: 10-telemetry-correlation-enrichment
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified: [lib/chimeway/policy.ex, lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban.ex, lib/chimeway/dispatch/oban_worker.ex, lib/chimeway/deliveries.ex, test/chimeway/telemetry_integration_test.exs]
autonomous: true
requirements: [OPS-02]

must_haves:
  truths:
    - "All delivery-tier and attempt-tier spans emit consistent correlation metadata (notification_key)."
    - "Oban-specific spans emit both notification_key and channel metadata."
    - "Integration tests verify metadata presence and proper redaction."
  artifacts:
    - path: "lib/chimeway/policy.ex"
      provides: "Enriched [:policy, :evaluate] span"
    - path: "lib/chimeway/dispatch/sync.ex"
      provides: "Enriched [:dispatch, :sync] span"
    - path: "lib/chimeway/dispatch/oban.ex"
      provides: "Enriched [:dispatch, :enqueue] span"
    - path: "lib/chimeway/dispatch/oban_worker.ex"
      provides: "Enriched [:dispatch, :perform] span"
    - path: "lib/chimeway/deliveries.ex"
      provides: "Enriched [:attempts, :record] span"
    - path: "test/chimeway/telemetry_integration_test.exs"
      provides: "Correlation verification suite"
  key_links:
    - from: "lib/chimeway/deliveries.ex (Delivery metadata)"
      to: "lib/chimeway/telemetry.ex (safe_meta)"
      via: "Span start metadata"
---

<objective>
Enrich all lifecycle telemetry spans with correlation metadata extracted from delivery records. Verify the enrichment and ensure no sensitive data leakage via updated integration tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-telemetry-correlation-enrichment/10-CONTEXT.md
@.planning/phases/10-telemetry-correlation-enrichment/10-RESEARCH.md
@lib/chimeway/policy.ex
@lib/chimeway/dispatch/sync.ex
@lib/chimeway/dispatch/oban.ex
@lib/chimeway/dispatch/oban_worker.ex
@lib/chimeway/deliveries.ex
@test/chimeway/telemetry_integration_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich Policy and Sync spans</name>
  <files>lib/chimeway/policy.ex, lib/chimeway/dispatch/sync.ex</files>
  <action>
    Extract `notification_key` from `delivery.metadata` and add to the start metadata of the following spans (per D-05, D-06):
    - `[:policy, :evaluate]` in `lib/chimeway/policy.ex`
    - `[:dispatch, :sync]` in `lib/chimeway/dispatch/sync.ex`

    Pattern: `notification_key: Map.get(delivery.metadata || %{}, "notification_key")`.
  </action>
  <verify>
    <automated>grep "notification_key: Map.get(delivery.metadata" lib/chimeway/policy.ex lib/chimeway/dispatch/sync.ex</automated>
  </verify>
  <done>Policy and Sync spans are enriched with notification_key.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich Oban spans</name>
  <files>lib/chimeway/dispatch/oban.ex, lib/chimeway/dispatch/oban_worker.ex</files>
  <action>
    Enrich Oban-related spans with correlation metadata (per D-07, D-08):
    - `[:dispatch, :enqueue]` in `lib/chimeway/dispatch/oban.ex`: Add `notification_key` (from metadata) and `channel` (from struct).
    - `[:dispatch, :perform]` in `lib/chimeway/dispatch/oban_worker.ex`: Add `notification_key` (from metadata).
  </action>
  <verify>
    <automated>grep "notification_key:" lib/chimeway/dispatch/oban.ex lib/chimeway/dispatch/oban_worker.ex</automated>
  </verify>
  <done>Oban spans are enriched with correlation metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Enrich Attempt span and add integration tests</name>
  <files>lib/chimeway/deliveries.ex, test/chimeway/telemetry_integration_test.exs</files>
  <action>
    1. Enrich `[:attempts, :record]` span in `lib/chimeway/deliveries.ex` with `notification_key` and `channel` (per D-09).
    2. Add a new `describe "correlation metadata enrichment"` block in `test/chimeway/telemetry_integration_test.exs` (per D-11, D-12):
       - Assert `[:deliveries, :plan, :stop]` includes `:event_id` and `:notification_key`.
       - Assert `[:dispatch, :sync, :stop]` includes `:notification_key` and `:channel`.
       - Assert `[:attempts, :record, :stop]` includes `:notification_key` and `:channel`.
       - Assert `[:policy, :evaluate, :stop]` includes `:notification_key`.
       - Assert `correlation_id` does NOT appear in spans where it is not explicitly emitted (redaction check).
  </action>
  <verify>
    <automated>mix test test/chimeway/telemetry_integration_test.exs</automated>
  </verify>
  <done>Attempt span is enriched and all metadata improvements are verified by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System -> Telemetry Handlers | Enriched metadata is passed to handlers. safe_meta/1 ensures no PII is leaked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Information Disclosure | lib/chimeway/telemetry.ex | mitigate | Rely on safe_meta/1 to filter all metadata. This plan uses identifiers already present in the @allowed_meta_keys list. |
</threat_model>

<verification>
Run `mix test test/chimeway/telemetry_integration_test.exs` and ensure all tests pass.
</verification>

<success_criteria>
All lifecycle telemetry spans emit the `notification_key`. Integration tests confirm the presence of these keys and the absence of leaked data.
</success_criteria>

<output>
After completion, create `.planning/phases/10-telemetry-correlation-enrichment/10-02-SUMMARY.md`
</output>
