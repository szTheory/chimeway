---
phase: 10-telemetry-correlation-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/chimeway/trigger.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/deliveries.ex]
autonomous: true
requirements: [OPS-02]

must_haves:
  truths:
    - "Correlation metadata (notification_key, event_id, correlation_id) is threaded into dispatch_opts."
    - "Delivery rows persist correlation identifiers in their metadata field."
    - "The [:deliveries, :plan] span emits enriched correlation metadata on success."
  artifacts:
    - path: "lib/chimeway/trigger.ex"
      provides: "Metadata threading and planning span enrichment"
    - path: "lib/chimeway/delivery_planning.ex"
      provides: "Metadata propagation through planning layers"
    - path: "lib/chimeway/deliveries.ex"
      provides: "Metadata persistence in delivery records"
  key_links:
    - from: "lib/chimeway/trigger.ex"
      to: "lib/chimeway/delivery_planning.ex"
      via: "dispatch_opts"
    - from: "lib/chimeway/delivery_planning.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "plan_delivery opts"
---

<objective>
Thread and persist correlation metadata (notification_key, event_id, correlation_id) from the trigger through the planning phase into the delivery records. This ensures all subsequent lifecycle spans can access these identifiers from the delivery struct.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-telemetry-correlation-enrichment/10-CONTEXT.md
@.planning/phases/10-telemetry-correlation-enrichment/10-RESEARCH.md
@lib/chimeway/trigger.ex
@lib/chimeway/delivery_planning.ex
@lib/chimeway/deliveries.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread correlation opts in Trigger</name>
  <files>lib/chimeway/trigger.ex</files>
  <action>
    Update `dispatch_after_trigger/4` to include `notification_key` in `dispatch_opts` (per D-01).
    ```elixir
    # In dispatch_after_trigger/4
    dispatch_opts =
      opts
      |> Keyword.put_new(:notifier, notifier)
      |> Keyword.put_new(:trigger_params, params)
      |> Keyword.put_new(:notification_key, event.notification_key) # Add this
      |> Keyword.put_new(:event_id, event.id)
      |> Keyword.put_new(:correlation_id, event.correlation_id)
    ```
  </action>
  <verify>
    <automated>grep -A 10 "dispatch_opts =" lib/chimeway/trigger.ex | grep "notification_key"</automated>
  </verify>
  <done>notification_key is added to dispatch_opts in Trigger.dispatch_after_trigger/4.</done>
</task>

<task type="auto">
  <name>Task 2: Forward correlation opts in DeliveryPlanning</name>
  <files>lib/chimeway/delivery_planning.ex</files>
  <action>
    Update `plan_notification/2`, `plan_channels/4`, and `plan_one_channel/4` to forward `notification_key`, `correlation_id`, and `event_id` from `opts` down to `Deliveries.plan_delivery/3` (per D-02).
    
    Ensure `plan_one_channel` receives these from the updated calling chain.
  </action>
  <verify>
    <automated>grep -A 10 "Deliveries.plan_delivery" lib/chimeway/delivery_planning.ex | grep "notification_key:"</automated>
  </verify>
  <done>Correlation keys are forwarded to plan_delivery call site in DeliveryPlanning.</done>
</task>

<task type="auto">
  <name>Task 3: Persist correlation metadata in Deliveries</name>
  <files>lib/chimeway/deliveries.ex</files>
  <action>
    Update `plan_delivery/3` to merge `notification_key`, `correlation_id`, and `event_id` from `opts` into the `metadata` map before inserting the `Delivery` record (per D-02).
    Use string keys: `"notification_key"`, `"correlation_id"`, `"event_id"`.
  </action>
  <verify>
    <automated>grep -A 20 "metadata =" lib/chimeway/deliveries.ex | grep "notification_key"</automated>
  </verify>
  <done>Delivery records now persist correlation identifiers in their metadata field.</done>
</task>

<task type="auto">
  <name>Task 4: Enrich [:deliveries, :plan] span</name>
  <files>lib/chimeway/trigger.ex</files>
  <action>
    Update `plan_deliveries_span/4` to pattern-match on the `result` argument.
    On `{:ok, %{event: event}}`, enrich the start metadata with `event_id: event.id` and `correlation_id: event.correlation_id` (per D-04).
    Ensure `notification_key` remains present.
  </action>
  <verify>
    <automated>grep -A 10 "plan_deliveries_span" lib/chimeway/trigger.ex | grep "event_id"</automated>
  </verify>
  <done>Planning span emits enriched metadata on success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trigger -> Dispatch | Identification metadata (IDs, keys) crosses here. These are non-sensitive internal identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | lib/chimeway/deliveries.ex | mitigate | Use string-based identifier keys in metadata. Ensure no sensitive payload fields are included (safe_meta/1 already guards telemetry, this plan only adds IDs to DB). |
</threat_model>

<verification>
Check that `Delivery.metadata` in the database contains the expected keys after a trigger.
</verification>

<success_criteria>
Triggering a notification results in delivery records with `"notification_key"`, `"correlation_id"`, and `"event_id"` in their `metadata` field.
</success_criteria>

<output>
After completion, create `.planning/phases/10-telemetry-correlation-enrichment/10-01-SUMMARY.md`
</output>
