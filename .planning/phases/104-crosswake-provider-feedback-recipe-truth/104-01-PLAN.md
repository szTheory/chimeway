---
phase: 104-crosswake-provider-feedback-recipe-truth
plan: "01"
type: tracer
wave: 1
depends_on: []
files_modified:
  - ../crosswake/examples/phoenix_host/README.md
  - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/provider_feedback_recipe_test.exs
  - priv/adoption/crosswake-provider-feedback-docs-selected-sha
autonomous: true
requirements: [DOCS-02]
must_haves:
  truths:
    - "D-01/D-02/D-04: the copyable worker executes `Redaction.feedback_from_provider_attrs/1`, resolves the complete host-authenticated keyword scope, calls `Registry.apply_provider_feedback/2`, and returns the registry outcome."
    - "D-03/D-06: executable behavior proves advisory acceptance, exact invalidation, session- and installation-scoped authority, stale or mismatched denial, and recursive evidence sanitization."
    - "D-08: every fixture is synthetic and negative assertions prove raw tokens, recipient identity, and provider payload content are absent from durable or returned evidence."
    - "D-09/D-11/D-12: the tested recipe is published only on a new documentation branch rooted in CrossWake's proof-backed Phase 101/103 lineage and its exact SHA is selected separately from physical proof."
  artifacts:
    - path: "../crosswake/examples/phoenix_host/README.md"
      provides: "Copyable durable provider-feedback worker using the real conversion and authenticated registry boundaries"
    - path: "../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/provider_feedback_recipe_test.exs"
      provides: "Executable README recipe plus accepted, invalidating, denial, and recursive-redaction proof"
    - path: "priv/adoption/crosswake-provider-feedback-docs-selected-sha"
      provides: "Exact separately selected remote documentation revision"
  key_links:
    - from: "../crosswake/examples/phoenix_host/README.md"
      to: "../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex"
      via: "Redaction.feedback_from_provider_attrs/1 and Registry.apply_provider_feedback/2 with full keyword authority"
      pattern: "authenticated_context.*binding_ref.*installation_ref.*app_identity_ref"
    - from: "priv/adoption/crosswake-provider-feedback-docs-selected-sha"
      to: "https://github.com/szTheory/crosswake.git refs/heads/phase-104-provider-feedback-recipe-truth"
      via: "non-force publication followed by ls-remote equality"
      pattern: "^[0-9a-f]{40}$"
  flagged_assumptions:
    - requirement_id: DOCS-02
      statement: "The generic spec-less probe raised boundary, adjacency, empty, ordering, and precision questions. For this worker contract, missing or malformed feedback returns an explicit error, exact authority equality is required, order and numeric precision are not domain inputs, and none of these cases may broaden invalidation."
      status: unresolved
      verification: flagged-unverified
  prohibitions:
    - statement: "Provider token references or fingerprints must never become revocation authority or broaden an exact authenticated binding selection."
      status: unverified
      flagged: true
    - statement: "No live account, recipient, device token, or provider payload value may be committed in examples, fixtures, logs, or assertions."
      status: unverified
      flagged: true
---

<objective>
Replace the broken CrossWake worker snippet with a source-valid, behavior-proven recipe and publish its exact revision independently.

Purpose: Give adopters a copyable durable worker whose security and failure semantics match the already proof-backed registry implementation.
Output: Correct README recipe, focused executable example-host proof, a new non-force-published CrossWake documentation branch, and a separate Chimeway SHA authority file.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/104-crosswake-provider-feedback-recipe-truth/104-CONTEXT.md
@../crosswake/AGENTS.md
@../crosswake/examples/phoenix_host/README.md
@../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
@../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs
@../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/redaction.ex
@../crosswake/packages/crosswake_chimeway/test/crosswake/companions/chimeway/redaction_test.exs

<interfaces>
`Redaction.feedback_from_provider_attrs/1` returns `{:ok, ProviderFeedback.t()} | {:error, term()}`.
`Registry.apply_provider_feedback/2` accepts the normalized contract and an exact keyword scope containing authenticated_context, binding_ref, installation_ref, app_identity_ref, plus session_ref/session_version only for session-scoped bindings.
The immutable physical authority remains `priv/mobile_proof/crosswake-selected-sha`; this plan creates `priv/adoption/crosswake-provider-feedback-docs-selected-sha` instead.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the README recipe executable against exact provider-feedback authority</name>
  <reversibility rating="costly">The README becomes a published adoption contract, but the implementation is isolated to a new branch and can be superseded by a later docs revision.</reversibility>
  <files>../crosswake/examples/phoenix_host/README.md, ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/provider_feedback_recipe_test.exs</files>
  <read_first>
    - ../crosswake/AGENTS.md — CrossWake evidence, privacy, and verification rules
    - ../crosswake/examples/phoenix_host/README.md — broken worker recipe and surrounding host-ownership guidance
    - ../crosswake/examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex — exact `provider_feedback_scope/1`, advisory, invalidation, and fail-closed behavior
    - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/registry_test.exs — binding setup and exact-scope test patterns
    - ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/redaction.ex — real provider-attribute conversion and allowlist sanitizer
    - ../crosswake/packages/crosswake_chimeway/test/crosswake/companions/chimeway/redaction_test.exs — recursive unsafe-metadata rejection pattern
    - .planning/phases/104-crosswake-provider-feedback-recipe-truth/104-CONTEXT.md — D-01 through D-08
  </read_first>
  <action>On the clean `../crosswake` checkout rooted at remote `phase-103-chimeway-notification-proof`, create and switch to the new local branch `phase-104-provider-feedback-recipe-truth` without moving any existing branch. First add a focused example-host test that reads the provider-feedback fenced code block from `examples/phoenix_host/README.md`, compiles that exact snippet under test-only Oban and host-authority stubs, and invokes its `perform/1` against real `Redaction.feedback_from_provider_attrs/1` plus `Registry.apply_provider_feedback/2`. Make it fail against the current nonexistent `Contracts.ProviderFeedback.from_attrs/1` snippet. Then update the README snippet to use a `with` chain: normalize through `Redaction.feedback_from_provider_attrs/1`; ask `MyApp.Notifications.authenticated_provider_feedback_opts!/1` for a keyword list; pass the normalized feedback and opts to `Registry.apply_provider_feedback/2`; return `:ok` only for an accepted `{:ok, _result}` and otherwise preserve the error for Oban retry/discard handling. Document the exact required keys `authenticated_context`, `binding_ref`, `installation_ref`, `app_identity_ref`, and conditional current `session_ref`/`session_version`; state that provider token evidence only corroborates authority. In the focused test, create synthetic session- and installation-scoped bindings and assert: delivery acceptance is advisory/audit-only; matching invalidating feedback mutates only the exact binding; stale session version, wrong binding, wrong installation, wrong app identity, and cross-tenant/cross-recipient context each return `{:error, :no_active_bindings}` without mutating controls; nested/list/keyword unsafe values are absent after conversion and from durable/returned evidence. Assert the README lacks `Contracts.ProviderFeedback.from_attrs` and that the extracted module truly calls both public boundaries.</action>
  <verify>
    <automated>bash -lc 'cd ../crosswake/examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/chimeway/provider_feedback_recipe_test.exs --max-failures 1'</automated>
    <fails_when>non-zero exit, zero tests reported, or any assertion exposes a synthetic sensitive sentinel</fails_when>
  </verify>
  <acceptance_criteria>
    - Source: README contains `Redaction.feedback_from_provider_attrs/1`, `authenticated_provider_feedback_opts!/1`, `Registry.apply_provider_feedback/2`, and every required authority key, and does not contain `Contracts.ProviderFeedback.from_attrs`.
    - Behavior: the test compiles the exact fenced recipe and invokes it through real conversion and registry code.
    - Behavior: advisory acceptance, exact invalidation, both subject-scope shapes, and every stale/mismatched denial assertion pass.
    - Security: recursive sensitive sentinels are absent from normalized, returned, and durable inspected evidence.
  </acceptance_criteria>
  <done>The README recipe is copyable, source-valid, and backed by a non-vacuous exact-authority behavior test.</done>
</task>

<task type="auto">
  <name>Task 2: Publish only the new CrossWake documentation branch and select its exact SHA</name>
  <precondition>The focused test from Task 1 passes, `../crosswake` is on `phase-104-provider-feedback-recipe-truth`, the canonical remote still advertises physical branch SHA `3165ab6938fa673f8a289c27699658bb78650ef3`, and the new remote docs branch either does not exist or already equals the local commit; stop on any conflicting remote head.</precondition>
  <reversibility rating="costly">A public branch commit is durable external state, but it does not rewrite or merge any existing reference.</reversibility>
  <files>../crosswake/examples/phoenix_host/README.md, ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/provider_feedback_recipe_test.exs, priv/adoption/crosswake-provider-feedback-docs-selected-sha</files>
  <read_first>
    - ../crosswake/examples/phoenix_host/README.md — final recipe bytes to publish
    - ../crosswake/examples/phoenix_host/test/crosswake_example/chimeway/provider_feedback_recipe_test.exs — final executable contract
    - priv/mobile_proof/crosswake-selected-sha — immutable physical authority that must remain unchanged
    - .planning/phases/104-crosswake-provider-feedback-recipe-truth/104-CONTEXT.md — D-09 through D-12
  </read_first>
  <action>Run the focused example-host test again, commit exactly the README and focused test in CrossWake with an atomic Phase-104 commit, and obtain its lowercase 40-character SHA. Assert that `git ls-remote https://github.com/szTheory/crosswake.git refs/heads/resume/chimeway-notification-physical-proof` still equals `3165ab6938fa673f8a289c27699658bb78650ef3`. Inspect `refs/heads/phase-104-provider-feedback-recipe-truth`; if absent, publish the new branch with a normal non-force push; if it equals the local SHA, continue idempotently; if it differs, stop without updating it. Clone the canonical remote into a fresh temporary directory, fetch the new branch, detach the exact SHA, assert exact HEAD, clean tracked state, correct README markers, absent nonexistent constructor, focused test presence, and run that focused test. Only then create `priv/adoption/crosswake-provider-feedback-docs-selected-sha` containing exactly the proven docs SHA plus newline. Do not edit `priv/mobile_proof/crosswake-selected-sha`, do not push or update the physical branch, and do not retain temporary checkout data.</action>
  <verify>
    <automated>bash -lc 'docs=priv/adoption/crosswake-provider-feedback-docs-selected-sha &amp;&amp; physical=priv/mobile_proof/crosswake-selected-sha &amp;&amp; test -f "$docs" &amp;&amp; docs_sha=$(tr -d "\r\n" &lt; "$docs") &amp;&amp; physical_sha=$(tr -d "\r\n" &lt; "$physical") &amp;&amp; [[ "$docs_sha" =~ ^[0-9a-f]{40}$ ]] &amp;&amp; test "$physical_sha" = 3165ab6938fa673f8a289c27699658bb78650ef3 &amp;&amp; test "$(git ls-remote https://github.com/szTheory/crosswake.git refs/heads/phase-104-provider-feedback-recipe-truth | cut -f1)" = "$docs_sha" &amp;&amp; test "$(git ls-remote https://github.com/szTheory/crosswake.git refs/heads/resume/chimeway-notification-physical-proof | cut -f1)" = "$physical_sha"'</automated>
    <fails_when>non-zero exit, either SHA is malformed, either advertised head differs, or the physical authority file differs from its frozen value</fails_when>
  </verify>
  <acceptance_criteria>
    - Repository: the only new remote reference is `phase-104-provider-feedback-recipe-truth`; no force push or physical-branch mutation occurs.
    - Behavior: a fresh detached checkout of the exact selected docs SHA passes markers and the focused executable recipe test.
    - Source: the new authority file contains that exact SHA and the physical authority file remains byte-identical.
  </acceptance_criteria>
  <done>The source-valid CrossWake recipe is remotely reproducible at an independently selected immutable commit while the physical proof head remains frozen.</done>
</task>

</tasks>

## Artifacts this phase produces

- CrossWake focused test module `CrosswakeExample.Chimeway.ProviderFeedbackRecipeTest`.
- Public CrossWake branch `phase-104-provider-feedback-recipe-truth` at one non-force-published commit.
- Chimeway authority file `priv/adoption/crosswake-provider-feedback-docs-selected-sha`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider payload -> redaction contract | Raw provider data must become a stable, allowlisted `ProviderFeedback` before registry use. |
| Provider evidence -> host authority | Token evidence corroborates but never selects or authorizes invalidation. |
| Local CrossWake branch -> canonical remote | Only the locally tested docs commit may be published on the new branch, never by force. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-104-01 | Spoofing/Elevation of Privilege | high | mitigate | Exact authenticated context, binding, installation, application, and conditional session equality are executed against mismatched controls. |
| T-104-02 | Information Disclosure | high | mitigate | Synthetic recursive sentinels plus negative durable/returned-evidence assertions enforce the redaction boundary. |
| T-104-03 | Tampering | high | mitigate | Atomic commit, non-force new-branch publication, remote-head equality, and fresh detached replay bind the selected SHA. |

## ASVS L1 Applicability and Automated Control Evidence

ASVS V4 applies to exact authority and V5/V8 to validation and privacy. The focused example-host test and fresh-remote equality checks mitigate every high-severity threat before selection.
</threat_model>

<verification>
- The focused CrossWake example-host test passes locally and from a fresh remote detached checkout.
- The new selected docs SHA matches only the new remote docs branch.
- The physical-proof authority file and canonical branch still equal the frozen v1.18 SHA.
</verification>

<success_criteria>
- Adopters can copy a real durable worker recipe with complete authenticated scope and explicit failure behavior.
- Executable proof covers accepted, invalidating, stale/mismatched, and recursively sanitized paths.
- The docs revision is selected independently without moving physical proof.
</success_criteria>

<output>
Create `.planning/phases/104-crosswake-provider-feedback-recipe-truth/104-01-SUMMARY.md` when done.
</output>
