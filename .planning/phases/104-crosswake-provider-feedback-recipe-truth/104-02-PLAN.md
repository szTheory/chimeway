---
phase: 104-crosswake-provider-feedback-recipe-truth
plan: "02"
type: execute
wave: 2
depends_on: ["104-01"]
files_modified:
  - lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex
  - mix.exs
  - test/chimeway/crosswake_provider_feedback_docs_contract_test.exs
  - test/chimeway/doc_contract_test.exs
  - test/chimeway/release_gate_contract_test.exs
  - .github/workflows/ci.yml
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "D-05/D-07: `mix verify.crosswake_provider_feedback_docs` rejects nonexistent conversion APIs, incomplete authority scope, missing executable tests, and vacuous tests that never invoke the real registry path."
    - "D-09/D-10: verification clones the canonical remote, detaches the separately selected docs SHA, runs the focused proof, and independently proves the v1.18 physical branch is still frozen."
    - "The named CI job and local aggregate alias execute the same Mix verifier against the implementation SHA with no conversational UAT or live credentials."
  artifacts:
    - path: "lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex"
      provides: "Credential-free fresh-remote CrossWake recipe verifier and physical-immutability backstop"
      exports: ["Mix.Tasks.Verify.CrosswakeProviderFeedbackDocs.run/1"]
    - path: "test/chimeway/crosswake_provider_feedback_docs_contract_test.exs"
      provides: "Positive and mutation-negative verifier contract"
    - path: ".github/workflows/ci.yml"
      provides: "Named CrossWake documentation revision verification job"
  key_links:
    - from: "lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex"
      to: "priv/adoption/crosswake-provider-feedback-docs-selected-sha"
      via: "strict one-line lowercase SHA authority"
      pattern: "crosswake-provider-feedback-docs-selected-sha"
    - from: ".github/workflows/ci.yml"
      to: "mix.exs"
      via: "the same `mix verify.crosswake_provider_feedback_docs` command used locally"
      pattern: "verify.crosswake_provider_feedback_docs"
  flagged_assumptions:
    - requirement_id: GATE-02
      statement: "The spec-less edge probe classified GATE-02 as unclassified; this plan treats exact remote reachability, source markers, executable invocation, mutation-negative tests, and frozen physical-head equality as its complete machine-verifiable boundary."
      status: unresolved
      verification: flagged-unverified
  prohibitions:
    - statement: "The documentation verifier must not rewrite, infer, or fall back from the selected docs SHA or the frozen physical SHA."
      status: unverified
      flagged: true
    - statement: "Static string presence alone must not satisfy the executable recipe contract."
      status: unverified
      flagged: true
---

<objective>
Make CrossWake provider-feedback documentation truth a deterministic Chimeway release/CI contract.

Purpose: Prevent the recipe from drifting back to a nonexistent API, incomplete authority, or a non-executing example while permanently backstopping v1.18 physical-proof immutability.
Output: Focused Mix verifier, mutation-negative tests, local aggregate alias parity, and named CI job.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/104-crosswake-provider-feedback-recipe-truth/104-CONTEXT.md
@.planning/phases/104-crosswake-provider-feedback-recipe-truth/104-01-SUMMARY.md
@lib/mix/tasks/verify.physical_proof_contract.ex
@mix.exs
@test/chimeway/doc_contract_test.exs
@test/chimeway/release_gate_contract_test.exs
@.github/workflows/ci.yml
@priv/mobile_proof/crosswake-selected-sha
@priv/adoption/crosswake-provider-feedback-docs-selected-sha

<interfaces>
The verifier command is `mix verify.crosswake_provider_feedback_docs` and succeeds only after the fresh selected CrossWake checkout runs the focused recipe test.
The existing `mix verify.physical_proof_contract` and `priv/mobile_proof/crosswake-selected-sha` remain unchanged and authoritative only for v1.18 physical proof.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a mutation-negative fresh-remote documentation verifier</name>
  <reversibility rating="reversible">The new verifier and focused tests are additive and can be reverted without data migration.</reversibility>
  <files>lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex, test/chimeway/crosswake_provider_feedback_docs_contract_test.exs</files>
  <read_first>
    - lib/mix/tasks/verify.physical_proof_contract.ex — strict selected-SHA, remote advertisement, temporary checkout, marker, test, and cleanup pattern
    - priv/adoption/crosswake-provider-feedback-docs-selected-sha — exact docs authority created by Plan 01
    - priv/mobile_proof/crosswake-selected-sha — immutable physical authority
    - test/chimeway/mobile_physical_proof_test.exs — dependency injection and negative-verifier test conventions
    - .planning/phases/104-crosswake-provider-feedback-recipe-truth/104-CONTEXT.md — D-05, D-07, D-09, and D-10
  </read_first>
  <action>Write focused tests first for a new `Mix.Tasks.Verify.CrosswakeProviderFeedbackDocs` task. Inject remote/ref/selected-SHA/checkout command boundaries in tests so mutation fixtures prove rejection when: the README uses `Contracts.ProviderFeedback.from_attrs`; any one of `authenticated_context`, `binding_ref`, `installation_ref`, or `app_identity_ref` is absent; session guidance is absent; the focused test file is absent; or its source lacks actual calls to `Redaction.feedback_from_provider_attrs` and `Registry.apply_provider_feedback`. Add a physical immutability negative where the advertised physical head differs from `3165ab6938fa673f8a289c27699658bb78650ef3`. Implement the task with strict no-argument usage, exact lowercase SHA parsing, canonical remote/ref constants, separate docs and physical refs, temporary checkout cleanup, exact detached HEAD/clean tracked status, positive/negative README markers, registry authority markers, focused-test invocation markers, and execution of the selected checkout's focused example-host test. Reuse installed dependencies only through explicit environment configuration when safe; do not weaken source selection or silently skip execution. Emit one stable success marker and exit 70 on any contract mismatch without echoing checked content.</action>
  <verify>
    <automated>bash -lc 'scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/crosswake_provider_feedback_docs_contract_test.exs --warnings-as-errors'</automated>
    <fails_when>non-zero exit, zero tests reported, or any mutation fixture is accepted</fails_when>
  </verify>
  <acceptance_criteria>
    - API: `Mix.Tasks.Verify.CrosswakeProviderFeedbackDocs.run/1` accepts only `[]`, prints one stable success marker, and exits non-zero for all mismatch classes.
    - Source: the verifier has distinct docs and physical authority paths/refs and hard-binds the physical ref to its frozen SHA.
    - Behavior: positive fixture reaches and executes the focused CrossWake test; string-only and missing-marker mutations fail.
    - Security: verifier errors never echo README, fixture, provider payload, or identity content.
  </acceptance_criteria>
  <done>The Chimeway task independently proves a remotely selected executable docs contract and the frozen physical head.</done>
</task>

<task type="auto">
  <name>Task 2: Wire local/CI parity and protect the release-gate contract</name>
  <reversibility rating="reversible">Alias and workflow wiring are additive and covered by contract tests.</reversibility>
  <files>mix.exs, test/chimeway/doc_contract_test.exs, test/chimeway/release_gate_contract_test.exs, .github/workflows/ci.yml</files>
  <read_first>
    - mix.exs — existing `ci.verify_gates`, `verify.*`, and `ci.alpha_twin` alias patterns
    - test/chimeway/doc_contract_test.exs — executable documentation command contract
    - test/chimeway/release_gate_contract_test.exs — local/CI gate parity assertions
    - .github/workflows/ci.yml — exact-SHA action pins, CrossWake fetch lane, and `pr-gate` dependency aggregation
    - lib/mix/tasks/verify.crosswake_provider_feedback_docs.ex — focused verifier command from Task 1
    - .planning/phases/104-crosswake-provider-feedback-recipe-truth/104-CONTEXT.md — machine-evidence acceptance requirement
  </read_first>
  <action>Add the exact `verify.crosswake_provider_feedback_docs` command to a named `ci.crosswake_provider_feedback_docs` alias and into `ci.verify_gates` so local release-gate verification executes it. Add a dedicated `verify_crosswake_provider_feedback_docs` GitHub job with read-only contents permission, pinned checkout/BEAM setup actions, dependency installation, and the same Mix command; make `pr-gate` require the job result. Extend doc and release-gate contract tests to assert the selected docs authority is packaged where needed, the named command exists exactly once in its alias path, the workflow job invokes the same command, and the final PR gate cannot pass when that job is skipped or fails. Keep the existing physical proof lane, ref, authority file, and command byte-for-byte unchanged. Run the focused contracts, then `mix ci.verify_gates` to prove local parity and the fresh-remote recipe end to end.</action>
  <verify>
    <automated>bash -lc 'scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/crosswake_provider_feedback_docs_contract_test.exs test/chimeway/doc_contract_test.exs test/chimeway/release_gate_contract_test.exs --exclude adoption_paths_e2e --exclude accrue_packaged_cli --warnings-as-errors'</automated>
    <fails_when>non-zero exit, zero tests reported, alias/workflow parity assertions fail, or the physical lane changes</fails_when>
    <automated>mix ci.verify_gates</automated>
    <fails_when>non-zero exit, any test failure, or either packaged/fresh-remote contract lane is absent from output</fails_when>
  </verify>
  <acceptance_criteria>
    - Config: `mix.exs` exposes `verify.crosswake_provider_feedback_docs` and `ci.crosswake_provider_feedback_docs`, with `ci.verify_gates` invoking the new lane.
    - CI: the named job executes the same Mix command and is a required `pr-gate` dependency with fail-closed result assertion.
    - Regression: focused docs/release contracts and the full `mix ci.verify_gates` aggregate pass.
    - Immutability: existing physical-proof selection and workflow fetch ref remain unchanged.
  </acceptance_criteria>
  <done>Provider-feedback recipe truth is a required, locally reproducible CI/release gate with physical-proof immutability protected by contract tests.</done>
</task>

</tasks>

## Artifacts this phase produces

- Mix task module `Mix.Tasks.Verify.CrosswakeProviderFeedbackDocs` with `run/1`.
- Mix aliases `verify.crosswake_provider_feedback_docs` and `ci.crosswake_provider_feedback_docs`.
- Focused test module `Chimeway.CrosswakeProviderFeedbackDocsContractTest`.
- GitHub Actions job `verify_crosswake_provider_feedback_docs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Selected SHA file -> canonical remote | A local selector is accepted only when the canonical named branch advertises the same full SHA. |
| Fresh remote source -> verifier verdict | Static markers and real focused execution must both pass at exact detached HEAD. |
| New docs authority -> old physical authority | Separate files, refs, and equality checks prevent authority substitution or drift. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-104-04 | Spoofing/Tampering | high | mitigate | Strict SHA parsing, canonical ls-remote equality, detached exact HEAD, clean checkout, and no fallback. |
| T-104-05 | Repudiation | high | mitigate | Named local alias and required CI job run the same verifier and are pinned by release-gate tests. |
| T-104-06 | Elevation of Privilege | high | mitigate | Mutation tests reject missing authority keys and the verifier executes exact-scope denial behavior. |
| T-104-07 | Information Disclosure | high | mitigate | Stable non-echoing errors and synthetic mutation fixtures prevent checked content from entering output. |

## ASVS L1 Applicability and Automated Control Evidence

ASVS V4/V5/V8 apply. Focused mutation tests, fresh-remote execution, local/CI parity assertions, and frozen physical-head equality mitigate every high-severity threat.
</threat_model>

<verification>
- Focused verifier mutation tests pass with warnings as errors.
- Documentation and release-gate contract tests prove exact alias/workflow parity and physical-lane immutability.
- `mix ci.verify_gates` runs green including the fresh CrossWake documentation contract.
</verification>

<success_criteria>
- Nonexistent APIs, incomplete authority, and vacuous examples fail deterministically.
- A clean remote checkout at the separately selected docs SHA executes the real recipe proof.
- Local and CI gates agree, and neither can substitute or move v1.18 physical proof.
</success_criteria>

<output>
Create `.planning/phases/104-crosswake-provider-feedback-recipe-truth/104-02-SUMMARY.md` when done.
</output>
