---
phase: 31-feedback-driven-progression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260502000000_add_tenant_and_actor_to_chimeway_deliveries.exs
  - lib/chimeway/delivery.ex
  - lib/chimeway/deliveries.ex
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/workflows/progression.ex
  - test/chimeway/deliveries_test.exs
  - test/chimeway/orchestration/delivery_planning_test.exs
autonomous: true
requirements: [FLOW-01, FLOW-02]

must_haves:
  truths:
    - Delivery records persist the tenant ID from their originating workflow run.
    - Delivery records persist the actor ID representing the recipient identity.
    - Delivery planning correctly populates these fields natively.
  artifacts:
    - path: "priv/repo/migrations/*_add_tenant_and_actor_to_chimeway_deliveries.exs"
      provides: "Migration adding denormalized context to deliveries table"
      min_lines: 15
    - path: "lib/chimeway/delivery.ex"
      provides: "Delivery schema with new fields"
      contains: "field(:tenant_id, :string)"
  key_links:
    - from: "lib/chimeway/delivery_planning.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "tenant_id and actor_id arguments"
      pattern: "tenant_id: .*actor_id:"
---

<objective>
Denormalize `tenant_id` and `actor_id` onto the canonical `Delivery` row to support cheap, inline workflow signal emission.

Purpose: `ProcessFeedbackWorker` needs these IDs to emit progression signals using `Chimeway.Signal.track/4`. Avoiding costly associations keeps asynchronous feedback processing extremely fast and decoupled.
Output: Migration + schema updates and planners to provide these natively.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-feedback-driven-progression/31-PATTERNS.md
@lib/chimeway/delivery.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Migration</name>
  <files>priv/repo/migrations/20260502000000_add_tenant_and_actor_to_chimeway_deliveries.exs</files>
  <action>
    Create a new migration to add `tenant_id` and `actor_id` to `chimeway_deliveries`. Rename the file name using the current timestamp before writing.
    Following 31-PATTERNS.md, use a 3-step pattern:
    1. Add `tenant_id` and `actor_id` as `:string` columns.
    2. Backfill existing deliveries:
       - Update `actor_id` from `chimeway_notifications.recipient_identity` via `notification_id`, using `COALESCE(recipient_identity, 'system')` as a fallback.
       - Update `tenant_id` from `chimeway_workflow_runs.tenant_id` via `workflow_run_id`.
       - For rows lacking a `workflow_run_id`, set `tenant_id = 'default'`.
    3. Modify columns to `null: false`.
  </action>
  <verify>
    <automated>mix ecto.migrate</automated>
  </verify>
  <done>The database table contains non-nullable `tenant_id` and `actor_id` fields, properly backfilled.</done>
</task>

<task type="auto">
  <name>Task 2: Update Delivery Schema and Planners</name>
  <files>
    lib/chimeway/delivery.ex,
    lib/chimeway/deliveries.ex,
    lib/chimeway/delivery_planning.ex,
    lib/chimeway/workflows/progression.ex,
    test/chimeway/deliveries_test.exs,
    test/chimeway/orchestration/delivery_planning_test.exs,
    test/chimeway/orchestration/workflow_progression_test.exs
  </files>
  <action>
    1. Update `Chimeway.Delivery` schema to include `field(:tenant_id, :string)` and `field(:actor_id, :string)`. Add them to `@required_fields` and `validate_required`. Add `validate_length(..., min: 1)` to both. Explicitly reference analog file `lib/chimeway/signals/signal.ex` from 31-PATTERNS.md.
    2. Update `Chimeway.Deliveries.plan_delivery/3` to accept `tenant_id: ...` and `actor_id: ...` in `opts`, extract them, validate them (as strings of len > 0), and add them to the `Delivery.changeset` call. Update tests in `test/chimeway/deliveries_test.exs` if necessary.
    3. Update `Chimeway.DeliveryPlanning.plan_one_channel/5` to pass `tenant_id: Keyword.get(opts, :tenant_id)` and `actor_id: notification.recipient_identity || "system"` when calling `Deliveries.plan_delivery/3`. Update tests in `test/chimeway/orchestration/delivery_planning_test.exs` if affected.
    4. Update `Chimeway.Workflows.Progression.progress_run/2` loop paths (where it plans next steps, e.g., `do_progress_active_run` which delegates to `plan_next_step_delivery`) to pass `tenant_id: run.tenant_id` in `opts` down to `plan_next_step_delivery`.
  </action>
  <verify>
    <automated>mix test test/chimeway/deliveries_test.exs test/chimeway/orchestration/delivery_planning_test.exs test/chimeway/orchestration/workflow_progression_test.exs</automated>
  </verify>
  <done>Deliveries are successfully planned with populated `tenant_id` and `actor_id` values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Planner -> Database | Delivery records receive normalized identity from trustworthy internal system domains. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01-01 | Tampering | Deliveries | mitigate | Require `null: false` at database level and minimum string length in changesets to prevent spoofing anonymous deliveries. |
</threat_model>

<success_criteria>
Migrations apply cleanly. `Delivery` writes require `tenant_id` and `actor_id`.
</success_criteria>iteria>>