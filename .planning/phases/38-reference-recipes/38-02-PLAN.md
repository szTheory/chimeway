---
phase: 38
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/recipes/feedback-escalation-workflow.md
autonomous: true
requirements:
  - RECP-02
must_haves:
  truths:
    - Product Manager can follow send → webhook → workflow progression story with trace proof points.
    - Progress and stop paths are separate subsections aligned with demo host E2E.
    - Recipe uses real worker module names and Chimeway.Signal.track/4 argument order.
  artifacts:
    - path: guides/recipes/feedback-escalation-workflow.md
      provides: "RECP-02 feedback escalation workflow walkthrough"
  key_links:
    - from: guides/recipes/feedback-escalation-workflow.md
      to: examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs
      via: "runnable E2E proof"
    - from: guides/recipes/feedback-escalation-workflow.md
      to: guides/flows/multi-step-journeys.md
      via: "workflow/2 authoring reference"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-38-03 | Recipe documents read-to-cancel as working without pending_signals wiring | medium | Defer READ milestone; primary story is delivery-feedback signals |
| T-38-04 | Wrong Signal.track argument order in examples | medium | Require tenant_id first per signal.ex:20-22 |
</threat_model>

<objective>
Ship RECP-02: persona-driven feedback escalation recipe at `guides/recipes/feedback-escalation-workflow.md` narrating outbound send → webhook feedback → workflow progression visible in `Chimeway.Traces.explain_delivery/1`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-reference-recipes/38-CONTEXT.md
@.planning/phases/38-reference-recipes/38-RESEARCH.md
@examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs
@guides/flows/multi-step-journeys.md
@guides/introduction/golden-path.md
@lib/chimeway/signal.ex
@lib/chimeway/webhooks/process_feedback_worker.ex
</context>

<tasks>

<task type="auto">
  <name>Task 38-02-01: Create feedback-escalation-workflow.md skeleton</name>
  <files>guides/recipes/feedback-escalation-workflow.md</files>
  <read_first>
    - .planning/phases/38-reference-recipes/38-CONTEXT.md (D-08, D-13)
    - guides/introduction/golden-path.md webhook appendix
  </read_first>
  <action>
    Create `guides/recipes/feedback-escalation-workflow.md` with:

    1. Title: `# Feedback Escalation Workflow`

    2. **Who this is for** — Product Manager JTBD: understand progression after delivery feedback; Feature Developer authors notifier + workflow.

    3. **Prerequisites** — link [Multi-step journeys](../flows/multi-step-journeys.md) for full `workflow/2` authoring; link golden-path webhook appendix.

    4. Section headings:
       - `## Feature Developer: notifier with workflow`
       - `## End-to-end feedback loop`
       - `## Progress path: delivered signal resumes the run`
       - `## Stop path: bounced signal stops the workflow`
       - `## Inspecting progression in the trace`
       - `## Runnable proof in the repo`
       - `## Related guides`
  </action>
  <acceptance_criteria>
    - `test -f guides/recipes/feedback-escalation-workflow.md`
    - `grep -c 'Who this is for' guides/recipes/feedback-escalation-workflow.md` ≥ 1
    - `grep -c 'multi-step-journeys' guides/recipes/feedback-escalation-workflow.md` ≥ 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 38-02-02: Feature Developer workflow section (link-first)</name>
  <files>guides/recipes/feedback-escalation-workflow.md</files>
  <read_first>
    - guides/flows/multi-step-journeys.md (progress rule kinds)
    - lib/chimeway/notifier.ex workflow callback
    - .planning/phases/38-reference-recipes/38-CONTEXT.md D-09
  </read_first>
  <action>
    In `## Feature Developer: notifier with workflow`:

    1. State workflows are declared via optional `workflow/2` on a `Chimeway.Notifier` module — **not** `Chimeway.Workflow` behaviour.

    2. Include a **minimal** inline snippet showing envelope shape only:
    ```elixir
    {:ok, %{
      workflow_key: "mention_escalation",
      workflow_version: 1,
      steps: [
        %{step_key: "in_app", step_order: 1, channel: "in_app", config: %{"progress" => [...]}},
        %{step_key: "email", step_order: 2, channel: "email", config: %{"progress" => [...]}}
      ]
    }}
    ```
    with `...` for progress arrays — full example lives in multi-step-journeys.md.

    3. Mention progress rule kinds: `wait_until`, `on_outcome`, `stop` — link journey guide for `on_outcome` outcome vocabulary.

    4. Trigger example with `Chimeway.trigger/3`, `idempotency_key`, `tenant_id`.
  </action>
  <acceptance_criteria>
    - `grep -c 'workflow_key' guides/recipes/feedback-escalation-workflow.md` ≥ 1
    - `grep -c 'wait_until\|on_outcome\|stop' guides/recipes/feedback-escalation-workflow.md` ≥ 2
    - `grep -c 'Chimeway.Workflow' guides/recipes/feedback-escalation-workflow.md` equals 0
    - `grep -c 'multi-step-journeys' guides/recipes/feedback-escalation-workflow.md` ≥ 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 38-02-03: End-to-end feedback loop narrative</name>
  <files>guides/recipes/feedback-escalation-workflow.md</files>
  <read_first>
    - examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs
    - lib/chimeway/webhooks/process_feedback_worker.ex (signal event names)
    - lib/chimeway/dispatch/signal_router_worker.ex
  </read_first>
  <action>
    In `## End-to-end feedback loop`, numbered list (7 steps):

    1. Host triggers notifier → delivery planned  
    2. Adapter sends (or simulates) outbound delivery  
    3. Provider POSTs webhook to host route (reference demo `/webhooks/chimeway/echo` — **no** full controller paste)  
    4. `Chimeway.Webhooks.ProcessFeedbackWorker` normalizes feedback  
    5. `Chimeway.Signal.track(tenant_id, actor_id, "chimeway.delivery.succeeded", %{...})` — document correct argument order  
    6. `Chimeway.Dispatch.SignalRouterWorker` → `Chimeway.Workflows.route_signal/1`  
    7. `Chimeway.Traces.explain_delivery/1` shows `:webhook_received` and/or workflow stop/resume on timeline  

    Name full module paths:
    - `Chimeway.Webhooks.ProcessFeedbackWorker`
    - `Chimeway.Dispatch.SignalRouterWorker`

    Forbid `Chimeway.Workflows.Workers.*`.
  </action>
  <acceptance_criteria>
    - `grep -c 'ProcessFeedbackWorker' guides/recipes/feedback-escalation-workflow.md` ≥ 1
    - `grep -c 'SignalRouterWorker' guides/recipes/feedback-escalation-workflow.md` ≥ 1
    - `grep -c 'Chimeway.Signal.track' guides/recipes/feedback-escalation-workflow.md` ≥ 1
    - `grep -c 'chimeway.delivery.succeeded' guides/recipes/feedback-escalation-workflow.md` ≥ 1
    - `grep -c 'route_signal' guides/recipes/feedback-escalation-workflow.md` ≥ 1
    - `grep -c 'Workflows.Workers' guides/recipes/feedback-escalation-workflow.md` equals 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 38-02-04: Progress and stop paths + trace inspection + E2E links</name>
  <files>guides/recipes/feedback-escalation-workflow.md</files>
  <read_first>
    - feedback_pipeline_e2e_test.exs progress describe (lines 23-115) and stop describe
    - guides/introduction/golden-path.md#next-webhook-feedback-loop
  </read_first>
  <action>
    **Progress path subsection:** succeeded delivery signal → run resumes from `:waiting` → timeline includes `:webhook_received` with `detail.signal_event_name == "chimeway.delivery.succeeded"` (per E2E assertion).

    **Stop path subsection:** bounced signal → `workflow_stopped` on timeline; run state stopped (per stop describe in E2E).

    **Inspecting progression:** IEx snippet:
    ```elixir
    {:ok, %{timeline: timeline}} = Chimeway.Traces.explain_delivery(delivery_id)
    Enum.map(timeline, & &1.event)
    ```

    **Runnable proof:** Markdown links to:
    - `examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs`
    - Golden path [webhook appendix](../introduction/golden-path.md#next-webhook-feedback-loop)

    **Related guides:** multi-step-journeys, golden-path, tracing-a-notification.
  </action>
  <acceptance_criteria>
    - `grep -c 'webhook_received' guides/recipes/feedback-escalation-workflow.md` ≥ 1
    - `grep -c 'workflow_stopped' guides/recipes/feedback-escalation-workflow.md` ≥ 1
    - `grep -c 'feedback_pipeline_e2e_test' guides/recipes/feedback-escalation-workflow.md` ≥ 1
    - `grep -c 'explain_delivery' guides/recipes/feedback-escalation-workflow.md` ≥ 2
    - `grep -c 'golden-path' guides/recipes/feedback-escalation-workflow.md` ≥ 1
  </acceptance_criteria>
  <automated>grep -q 'feedback_pipeline_e2e_test' guides/recipes/feedback-escalation-workflow.md && grep -q 'workflow_stopped' guides/recipes/feedback-escalation-workflow.md</automated>
</task>

</tasks>

<verification>
- RECP-02: outbound → webhook → workflow step visible in trace narrative
- Cross-check E2E describe block names match recipe subsection titles
</verification>
