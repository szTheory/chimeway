---
phase: 01-durable-core-spine
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - lib/chimeway.ex
  - lib/chimeway/inbox.ex
  - test/chimeway/inbox_query_test.exs
  - test/chimeway/inbox_state_transition_test.exs
  - test/chimeway/inbox_integration_test.exs
  - .planning/phases/01-durable-core-spine/01-VALIDATION.md
  - .planning/phases/01-durable-core-spine/VERIFICATION.md
autonomous: true
requirements:
  - INBX-02
  - INBX-03
user_setup: []
must_haves:
  truths:
    - "Inbox API returns recipient notifications newest-first with optional unread-only filtering."
    - "State transitions (`seen`, `read`, `archive`) are explicit function calls and never happen during reads."
    - "Validation and verification docs show executable evidence for every CORE/INBX requirement."
  artifacts:
    - path: lib/chimeway/inbox.ex
      provides: "Inbox list and state transition API"
      exports:
        - "list_for_recipient/2"
        - "mark_seen/3"
        - "mark_read/3"
        - "archive/3"
    - path: test/chimeway/inbox_query_test.exs
      provides: "Unread filtering and ordering tests"
      min_lines: 35
    - path: test/chimeway/inbox_state_transition_test.exs
      provides: "Explicit lifecycle transition tests"
      min_lines: 35
    - path: .planning/phases/01-durable-core-spine/01-VALIDATION.md
      provides: "Updated execution-ready validation matrix"
      contains: "nyquist_compliant: true"
    - path: .planning/phases/01-durable-core-spine/VERIFICATION.md
      provides: "Phase verification status aligned with implemented evidence"
      contains: "## Goal Achievement"
  key_links:
    - from: lib/chimeway.ex
      to: lib/chimeway/inbox.ex
      via: "public API delegates inbox operations"
      pattern: "Inbox\\.(list_for_recipient|mark_seen|mark_read|archive)"
    - from: test/chimeway/inbox_integration_test.exs
      to: lib/chimeway/inbox.ex
      via: "integration assertions on state APIs and query semantics"
      pattern: "mark_seen|mark_read|archive|list_for_recipient"
    - from: .planning/phases/01-durable-core-spine/VERIFICATION.md
      to: .planning/phases/01-durable-core-spine/01-VALIDATION.md
      via: "shared command evidence and requirement status updates"
      pattern: "CORE-01|INBX-03"
---

<objective>
Implement inbox read/query semantics and explicit state transitions, then update phase validation artifacts with executable proof.

Purpose: Close INBX-02 and INBX-03 with behaviorally complete APIs and verifiable evidence so Phase 1 can pass verification gates.
Output: `Chimeway.Inbox` API, state/query tests, and updated `01-VALIDATION.md` + `VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-durable-core-spine/01-RESEARCH.md
@.planning/phases/01-durable-core-spine/01-VALIDATION.md
@.planning/phases/01-durable-core-spine/VERIFICATION.md
@.planning/phases/01-durable-core-spine/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 01-03-01: Implement inbox query and explicit lifecycle APIs</name>
  <files>lib/chimeway/inbox.ex, lib/chimeway.ex</files>
  <read_first>lib/chimeway/inbox.ex, lib/chimeway.ex, lib/chimeway/notifications/notification.ex, .planning/phases/01-durable-core-spine/01-CONTEXT.md</read_first>
  <action>Create `Chimeway.Inbox` functions: `list_for_recipient(recipient_identity, opts \\ [])`, `mark_seen(notification_id, recipient_identity, at \\ DateTime.utc_now())`, `mark_read(notification_id, recipient_identity, at \\ DateTime.utc_now())`, and `archive(notification_id, recipient_identity, at \\ DateTime.utc_now())`. Implement `list_for_recipient/2` query over `Chimeway.Notifications.Notification` with optional `unread_only: true` (`where read_at is nil`) and `order_by: [desc: :inserted_at]`. Ensure read APIs are side-effect free. Scope all mutation updates by both `notification_id` and `recipient_identity` to prevent cross-recipient updates. Add delegates in `lib/chimeway.ex` for each inbox function.</action>
  <verify>rg "def list_for_recipient|def mark_seen|def mark_read|def archive" lib/chimeway/inbox.ex && rg "read_at.*is nil|desc: :inserted_at" lib/chimeway/inbox.ex && rg "recipient_identity == \\^recipient_identity" lib/chimeway/inbox.ex && rg "id == \\^notification_id" lib/chimeway/inbox.ex</verify>
  <acceptance_criteria>
    - lib/chimeway/inbox.ex defines all four functions with exact names and arities listed in action
    - list query contains unread filter on `read_at` and ordering `desc: :inserted_at`
    - mutation queries include both `id == ^notification_id` and `recipient_identity == ^recipient_identity`
  </acceptance_criteria>
  <done>INBX API contract is explicit, deterministic, and scoped for secure recipient-level updates.</done>
</task>

<task type="auto">
  <name>Task 01-03-02: Add inbox behavior tests covering queries and state transitions</name>
  <files>test/chimeway/inbox_query_test.exs, test/chimeway/inbox_state_transition_test.exs, test/chimeway/inbox_integration_test.exs</files>
  <read_first>test/support/data_case.ex, test/chimeway/inbox_query_test.exs, test/chimeway/inbox_state_transition_test.exs, test/chimeway/inbox_integration_test.exs, lib/chimeway/inbox.ex</read_first>
  <action>Create `inbox_query_test.exs` to assert unread filtering and newest-first ordering with mixed records. Create `inbox_state_transition_test.exs` to assert `mark_seen/3` sets only `seen_at`, `mark_read/3` sets `read_at` without auto-archive, and `archive/3` sets `archived_at` independently. Create `inbox_integration_test.exs` that triggers fanout records (from Plan 01-02 pipeline) and validates list + transition behavior end to end for a concrete `recipient_identity` value (e.g., `\"user:42\"`). Include requirement tags/comments (`INBX-02`, `INBX-03`) in each test module.</action>
  <verify>mix test test/chimeway/inbox_query_test.exs test/chimeway/inbox_state_transition_test.exs test/chimeway/inbox_integration_test.exs --seed 0</verify>
  <acceptance_criteria>
    - test/chimeway/inbox_query_test.exs contains assertions for unread-only filter and descending `inserted_at` ordering
    - test/chimeway/inbox_state_transition_test.exs contains separate assertions for `seen_at`, `read_at`, and `archived_at` updates
    - test/chimeway/inbox_integration_test.exs uses recipient `user:42` and includes an assertion that calling `list_for_recipient/2` does not change `read_at` for unread rows
  </acceptance_criteria>
  <done>INBX-02 and INBX-03 are covered by executable, requirement-linked tests.</done>
</task>

<task type="auto">
  <name>Task 01-03-03: Update validation and verification artifacts with command-backed evidence</name>
  <files>.planning/phases/01-durable-core-spine/01-VALIDATION.md, .planning/phases/01-durable-core-spine/VERIFICATION.md</files>
  <read_first>.planning/phases/01-durable-core-spine/01-VALIDATION.md, .planning/phases/01-durable-core-spine/VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>Set `01-VALIDATION.md` frontmatter field `nyquist_compliant: true` after test matrix rows are fully wired. Replace each `❌ W0` placeholder row with concrete file-exists and status values tied to the implemented test files. In `VERIFICATION.md`, replace the sentence `No implementation exists` with command-backed evidence sections, add a requirements table with rows for `CORE-01`, `CORE-02`, `CORE-03`, `CORE-04`, `INBX-01`, `INBX-02`, and `INBX-03`, and set each status to `PASS` only when the matching test command is green. Add explicit command blocks for the exact `mix test` suites run in Plans 01-01, 01-02, and 01-03.</action>
  <verify>rg "nyquist_compliant: true" .planning/phases/01-durable-core-spine/01-VALIDATION.md && ! rg "❌ W0" .planning/phases/01-durable-core-spine/01-VALIDATION.md && rg "CORE-01|CORE-02|CORE-03|CORE-04|INBX-01|INBX-02|INBX-03" .planning/phases/01-durable-core-spine/VERIFICATION.md && ! rg "No implementation exists" .planning/phases/01-durable-core-spine/VERIFICATION.md && rg "mix test test/chimeway/notifier_contract_test\\.exs test/chimeway/trigger_pipeline_test\\.exs --seed 0" .planning/phases/01-durable-core-spine/VERIFICATION.md && rg "mix test test/chimeway/persistence_transaction_test\\.exs test/chimeway/idempotency_constraint_test\\.exs test/chimeway/migration_contract_test\\.exs --seed 0" .planning/phases/01-durable-core-spine/VERIFICATION.md && rg "mix test test/chimeway/inbox_query_test\\.exs test/chimeway/inbox_state_transition_test\\.exs test/chimeway/inbox_integration_test\\.exs --seed 0" .planning/phases/01-durable-core-spine/VERIFICATION.md</verify>
  <acceptance_criteria>
    - 01-VALIDATION.md frontmatter contains `nyquist_compliant: true`
    - VERIFICATION.md contains a requirements coverage table with statuses for all `CORE-01..04` and `INBX-01..03`
    - VERIFICATION.md no longer claims "No implementation exists" after successful execution
  </acceptance_criteria>
  <done>Validation and verification artifacts reflect real executable evidence for Phase 1 closeout.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-03-RECIPIENT-IDOR
    severity: high
    threat: "A user can mutate another recipient's notification state by guessing notification IDs."
    mitigation: "Scope `mark_seen/3`, `mark_read/3`, and `archive/3` updates by both `notification_id` and `recipient_identity`."
    mitigation_check: "rg \"recipient_identity == \\^recipient_identity\" lib/chimeway/inbox.ex && mix test test/chimeway/inbox_state_transition_test.exs --seed 0"
  - id: TM-03-IMPLICIT-READ-SIDE-EFFECT
    severity: high
    threat: "Inbox read operations implicitly set `read_at`, causing hidden state tampering and policy bypass in later phases."
    mitigation: "Keep `list_for_recipient/2` read-only; mutate state only through explicit lifecycle functions."
    mitigation_check: "mix test test/chimeway/inbox_query_test.exs test/chimeway/inbox_integration_test.exs --seed 0"
  - id: TM-03-VERIFICATION-DRIFT
    severity: medium
    threat: "Planning docs report completion without command evidence, masking regressions."
    mitigation: "Update `01-VALIDATION.md` and `VERIFICATION.md` with concrete command evidence and requirement status."
    mitigation_check: "rg \"mix test|CORE-01|INBX-03|PASS\" .planning/phases/01-durable-core-spine/VERIFICATION.md"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix test test/chimeway/inbox_query_test.exs test/chimeway/inbox_state_transition_test.exs test/chimeway/inbox_integration_test.exs --seed 0`
- [ ] `mix test` (full suite)
- [ ] `rg "def list_for_recipient|def mark_seen|def mark_read|def archive" lib/chimeway/inbox.ex`
- [ ] `rg "nyquist_compliant: true" .planning/phases/01-durable-core-spine/01-VALIDATION.md`
- [ ] `rg "CORE-01|CORE-04|INBX-01|INBX-03" .planning/phases/01-durable-core-spine/VERIFICATION.md`
- [ ] Security gate check: all high-severity mitigations pass before plan close
</verification>

<success_criteria>
- Inbox list and state APIs satisfy explicit INBX semantics with no implicit mutations.
- INBX-02 and INBX-03 tests pass in isolation and in full-suite runs.
- Validation and verification docs are evidence-backed and phase-aligned.
- Security gate remains enabled (`ASVS L1`, block on high) with no unresolved high-severity threats.
</success_criteria>

<output>
After completion, create `.planning/phases/01-durable-core-spine/01-03-SUMMARY.md`.
</output>
