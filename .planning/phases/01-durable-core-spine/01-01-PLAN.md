---
phase: 01-durable-core-spine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - config/config.exs
  - config/test.exs
  - lib/chimeway.ex
  - lib/chimeway/notifier.ex
  - lib/chimeway/trigger.ex
  - test/chimeway/notifier_contract_test.exs
  - test/chimeway/trigger_pipeline_test.exs
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-04
user_setup: []
must_haves:
  truths:
    - "Developers define notifier modules with stable notification_key/version callbacks."
    - "trigger/3 requires idempotency_key and rejects missing or blank keys."
    - "Recipient resolution is deterministic (deduped and sorted before persistence handoff)."
  artifacts:
    - path: mix.exs
      provides: "Mix project and dependency aliases for phase execution"
      contains: "def project do"
    - path: lib/chimeway/notifier.ex
      provides: "Notifier behaviour contract with durable identity callbacks"
      exports:
        - "notification_key/0 callback"
        - "version/0 callback"
        - "recipients/1 callback"
    - path: lib/chimeway/trigger.ex
      provides: "Trigger orchestration and deterministic recipient normalization"
      exports:
        - "trigger/3"
        - "normalize_recipients/1"
    - path: test/chimeway/trigger_pipeline_test.exs
      provides: "Contract tests for idempotency validation and deterministic recipient handling"
      min_lines: 40
  key_links:
    - from: lib/chimeway.ex
      to: lib/chimeway/trigger.ex
      via: "public API delegates to Trigger.trigger/3"
      pattern: "Trigger\\.trigger\\("
    - from: lib/chimeway/trigger.ex
      to: lib/chimeway/notifier.ex
      via: "runtime notifier validation uses behaviour callbacks"
      pattern: "notification_key|version|recipients"
---

<objective>
Build the executable contract spine for Phase 1: notifier identity, trigger entrypoint, and deterministic recipient resolution.

Purpose: Lock CORE-01/02/04 behavior before persistence work so later plans can add storage without changing the contract.
Output: Mix scaffold, `Chimeway.Notifier` behaviour, `Chimeway.Trigger.trigger/3` pipeline, and contract tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-durable-core-spine/01-CONTEXT.md
@.planning/phases/01-durable-core-spine/01-RESEARCH.md
@.planning/phases/01-durable-core-spine/01-PATTERNS.md
@.planning/phases/01-durable-core-spine/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 01-01-01: Bootstrap Mix project and core API module layout</name>
  <files>mix.exs, config/config.exs, config/test.exs, lib/chimeway.ex</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-durable-core-spine/01-CONTEXT.md, mix.exs, config/config.exs, config/test.exs, lib/chimeway.ex</read_first>
  <action>Initialize project scaffolding with `mix new . --module Chimeway --sup` if `mix.exs` is absent. In `mix.exs`, define `app: :chimeway`, `elixir: "~> 1.17"`, dependencies `{:ecto_sql, "~> 3.11"}`, `{:postgrex, ">= 0.0.0"}`, and `{:nimble_options, "~> 1.1"}`. Add aliases `verify.phase1` => `["format --check-formatted", "compile --warnings-as-errors", "test"]`. In `lib/chimeway.ex`, define `trigger/3` that delegates to `Chimeway.Trigger.trigger/3`.</action>
  <verify>rg "app: :chimeway|elixir: \"~> 1\\.17\"" mix.exs && rg "ecto_sql|postgrex|nimble_options" mix.exs && rg "def trigger\\(notifier, params, opts \\\\ \\[\\]\\)" lib/chimeway.ex && rg "Chimeway\\.Trigger\\.trigger\\(notifier, params, opts\\)" lib/chimeway.ex</verify>
  <acceptance_criteria>
    - mix.exs contains `def project do` with `app: :chimeway` and `elixir: "~> 1.17"`
    - mix.exs contains dependency entries for `ecto_sql`, `postgrex`, and `nimble_options`
    - lib/chimeway.ex contains `def trigger(notifier, params, opts \\ [])` and calls `Chimeway.Trigger.trigger(notifier, params, opts)`
  </acceptance_criteria>
  <done>Project boots with compile-ready core entrypoint and explicit dependency baseline.</done>
</task>

<task type="auto">
  <name>Task 01-01-02: Implement notifier behaviour with stable key/version callbacks</name>
  <files>lib/chimeway/notifier.ex, test/chimeway/notifier_contract_test.exs</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-durable-core-spine/01-RESEARCH.md, lib/chimeway/notifier.ex, test/chimeway/notifier_contract_test.exs</read_first>
  <action>Create `Chimeway.Notifier` behaviour with callbacks `@callback notification_key() :: String.t()`, `@callback version() :: pos_integer()`, `@callback recipients(map()) :: {:ok, [map()]} | {:error, term()}`, and `@callback build(map(), map()) :: {:ok, map()} | {:error, term()}`. Add helper `validate_module!/1` that enforces callback presence and returns tagged errors for missing `notification_key/0` or `version/0`. Add `test/chimeway/notifier_contract_test.exs` covering valid module acceptance and failure cases for missing callbacks.</action>
  <verify>rg "@callback notification_key\\(" lib/chimeway/notifier.ex && rg "@callback version\\(" lib/chimeway/notifier.ex && rg "@callback recipients\\(" lib/chimeway/notifier.ex && rg "@callback build\\(" lib/chimeway/notifier.ex && rg "def validate_module!" lib/chimeway/notifier.ex && mix test test/chimeway/notifier_contract_test.exs --seed 0</verify>
  <acceptance_criteria>
    - lib/chimeway/notifier.ex contains callback signatures for `notification_key/0`, `version/0`, `recipients/1`, and `build/2`
    - lib/chimeway/notifier.ex contains `def validate_module!` with explicit missing-callback error branches
    - test/chimeway/notifier_contract_test.exs asserts one valid notifier module and at least two invalid callback scenarios
  </acceptance_criteria>
  <done>Notifier contract enforces durable identity primitives required by CORE-01.</done>
</task>

<task type="auto">
  <name>Task 01-01-03: Implement deterministic trigger pipeline and contract tests</name>
  <files>lib/chimeway/trigger.ex, test/chimeway/trigger_pipeline_test.exs</files>
  <read_first>.planning/phases/01-durable-core-spine/01-PATTERNS.md, lib/chimeway/trigger.ex, lib/chimeway/notifier.ex, test/chimeway/trigger_pipeline_test.exs</read_first>
  <action>Implement `Chimeway.Trigger.trigger/3` with explicit steps: `Keyword.fetch(opts, :idempotency_key)`, guard against blank keys, `Chimeway.Notifier.validate_module!/1`, call notifier `recipients/1`, normalize recipients via `normalize_recipients/1` (dedupe by `recipient_identity`, then sort ascending by `recipient_identity`), and return `%{notification_key: key, notification_version: version, idempotency_key: key, recipients: normalized}` for persistence handoff. In tests, add cases that prove missing key returns `{:error, :missing_idempotency_key}`, blank key returns `{:error, :blank_idempotency_key}`, and unsorted duplicate recipients become deterministic sorted unique output.</action>
  <verify>rg "Keyword\\.fetch\\(opts, :idempotency_key\\)" lib/chimeway/trigger.ex && rg "def normalize_recipients\\(recipients\\)" lib/chimeway/trigger.ex && rg "recipient_identity" lib/chimeway/trigger.ex && mix test test/chimeway/trigger_pipeline_test.exs --seed 0</verify>
  <acceptance_criteria>
    - lib/chimeway/trigger.ex contains `Keyword.fetch(opts, :idempotency_key)`
    - lib/chimeway/trigger.ex contains `def normalize_recipients(recipients)` with dedupe key `recipient_identity` and sort by `recipient_identity`
    - test/chimeway/trigger_pipeline_test.exs includes assertions for `:missing_idempotency_key`, `:blank_idempotency_key`, and deterministic normalized recipient order
  </acceptance_criteria>
  <done>Trigger contract is deterministic and ready for transactional persistence wiring in Plan 01-02.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-01-IDENTITY-SPOOF
    severity: high
    threat: "Durable identity spoofing if module names are persisted instead of stable `notification_key` + `version`."
    mitigation: "Use behaviour callbacks and persist callback outputs only; never store notifier module atom/string in event identity fields."
    mitigation_check: "rg -q \"notification_key|version\" lib/chimeway/notifier.ex lib/chimeway/trigger.ex && (rg -q \"notifier_module\" lib/chimeway/trigger.ex && exit 1 || exit 0)"
  - id: TM-01-IDEMPOTENCY-BYPASS
    severity: high
    threat: "Trigger accepts missing/blank idempotency keys, allowing duplicate canonical event creation."
    mitigation: "Require `Keyword.fetch(opts, :idempotency_key)` and explicit blank-key guard with error return."
    mitigation_check: "mix test test/chimeway/trigger_pipeline_test.exs --seed 0"
  - id: TM-01-RECIPIENT-NONDETERMINISM
    severity: medium
    threat: "Recipient ordering randomness causes inconsistent fanout records and flaky duplicate handling."
    mitigation: "Normalize recipient list with deterministic dedupe and stable sort by `recipient_identity`."
    mitigation_check: "rg \"normalize_recipients\" lib/chimeway/trigger.ex && mix test test/chimeway/trigger_pipeline_test.exs --seed 0"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix deps.get`
- [ ] `mix compile --warnings-as-errors`
- [ ] `mix test test/chimeway/notifier_contract_test.exs test/chimeway/trigger_pipeline_test.exs --seed 0`
- [ ] `rg "def trigger\\(|normalize_recipients|notification_key\\(\\)|version\\(\\)" lib/chimeway`
- [ ] Security gate check: no unresolved high-severity threats in `<threat_model>`
</verification>

<success_criteria>
- All tasks completed with concrete contract and deterministic recipient logic in code.
- CORE-01, CORE-02, and CORE-04 are covered by executable tests and passing commands.
- No plan introduces outbound delivery side effects or phase-2 persistence tables.
- Security gate remains enabled (`ASVS L1`, block on high) and all high-severity mitigation checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-durable-core-spine/01-01-SUMMARY.md`.
</output>
