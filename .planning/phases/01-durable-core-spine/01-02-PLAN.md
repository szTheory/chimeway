---
phase: 01-durable-core-spine
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - lib/chimeway/repo.ex
  - lib/chimeway/application.ex
  - lib/chimeway/events/event.ex
  - lib/chimeway/notifications/notification.ex
  - lib/chimeway/trigger.ex
  - priv/repo/migrations/*_create_chimeway_events.exs
  - priv/repo/migrations/*_create_chimeway_notifications.exs
  - test/support/data_case.ex
  - test/chimeway/persistence_transaction_test.exs
  - test/chimeway/idempotency_constraint_test.exs
  - test/chimeway/migration_contract_test.exs
autonomous: true
requirements:
  - CORE-03
  - INBX-01
  - CORE-02
user_setup: []
must_haves:
  truths:
    - "Trigger writes one durable event row before any outbound side effects."
    - "Per-recipient in-app notification rows are persisted atomically with the event."
    - "Repeated trigger calls with the same idempotency key produce one canonical event."
  artifacts:
    - path: lib/chimeway/events/event.ex
      provides: "Event schema with stable identity fields and idempotency constraint"
      contains: "field :idempotency_key, :string"
    - path: lib/chimeway/notifications/notification.ex
      provides: "Per-recipient notification schema with lifecycle fields"
      contains: "field :event_id, Ecto.UUID"
    - path: priv/repo/migrations/*_create_chimeway_events.exs
      provides: "Durable events table and unique idempotency index"
      contains: "unique_index(:chimeway_events, [:idempotency_key], name: :chimeway_events_idempotency_key_index)"
    - path: priv/repo/migrations/*_create_chimeway_notifications.exs
      provides: "Notifications table with unique recipient-per-event constraint and inbox query index"
      contains: "unique_index(:chimeway_notifications, [:event_id, :recipient_identity], name: :chimeway_notifications_event_recipient_index)"
    - path: test/chimeway/idempotency_constraint_test.exs
      provides: "Serial and concurrent duplicate protection tests"
      min_lines: 45
  key_links:
    - from: lib/chimeway/trigger.ex
      to: lib/chimeway/events/event.ex
      via: "Ecto.Multi inserts event changeset before notifications"
      pattern: "Ecto\\.Multi\\.insert\\(:event"
    - from: lib/chimeway/trigger.ex
      to: lib/chimeway/notifications/notification.ex
      via: "notification rows built from normalized recipients"
      pattern: "insert_notifications|notifications_attrs"
    - from: lib/chimeway/events/event.ex
      to: priv/repo/migrations/*_create_chimeway_events.exs
      via: "changeset unique_constraint aligns with DB unique index"
      pattern: "unique_constraint\\(:idempotency_key"
---

<objective>
Add durable persistence for events and in-app notifications with transaction boundaries and database-enforced idempotency.

Purpose: Satisfy CORE-03 and INBX-01 by making the trigger pipeline write canonical rows atomically and safely under concurrency.
Output: Repo wiring, Ecto schemas, migrations with concrete indexes, trigger transaction integration, and persistence/idempotency tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-durable-core-spine/01-CONTEXT.md
@.planning/phases/01-durable-core-spine/01-RESEARCH.md
@.planning/phases/01-durable-core-spine/01-PATTERNS.md
@.planning/phases/01-durable-core-spine/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 01-02-01: Create Repo, Event schema, and Notification schema with required fields</name>
  <files>lib/chimeway/repo.ex, lib/chimeway/application.ex, lib/chimeway/events/event.ex, lib/chimeway/notifications/notification.ex</files>
  <read_first>.planning/phases/01-durable-core-spine/01-RESEARCH.md, lib/chimeway/application.ex, lib/chimeway/repo.ex, lib/chimeway/events/event.ex, lib/chimeway/notifications/notification.ex</read_first>
  <action>Create `Chimeway.Repo` using `Ecto.Repo` OTP app `:chimeway`. Add `Chimeway.Repo` to `Chimeway.Application` supervision tree. Implement `Chimeway.Events.Event` schema `chimeway_events` with fields: `id` (UUID PK), `notification_key` (string), `notification_version` (integer), `idempotency_key` (string), `payload` (map), timestamps. Implement `Chimeway.Notifications.Notification` schema `chimeway_notifications` with UUID PK and fields: `event_id` (UUID FK), `recipient_identity` (string), `recipient_type` (string), `seen_at`, `read_at`, `archived_at`, `metadata` (map), timestamps. Add `changeset/2` validations and `unique_constraint` declarations matching migration index names.</action>
  <verify>rg "defmodule Chimeway\\.Repo" lib/chimeway/repo.ex && rg "Chimeway\\.Repo" lib/chimeway/application.ex && rg "field :notification_key, :string|field :notification_version, :integer|field :idempotency_key, :string" lib/chimeway/events/event.ex && rg "unique_constraint\\(:idempotency_key, name: :chimeway_events_idempotency_key_index\\)" lib/chimeway/events/event.ex && rg "field :recipient_identity, :string|field :seen_at, :utc_datetime_usec|field :read_at, :utc_datetime_usec|field :archived_at, :utc_datetime_usec" lib/chimeway/notifications/notification.ex</verify>
  <acceptance_criteria>
    - lib/chimeway/application.ex starts `Chimeway.Repo` under `children`
    - lib/chimeway/events/event.ex defines required fields and `unique_constraint(:idempotency_key, name: :chimeway_events_idempotency_key_index)`
    - lib/chimeway/notifications/notification.ex defines `recipient_identity`, `seen_at`, `read_at`, and `archived_at`
  </acceptance_criteria>
  <done>Core persistence modules exist and enforce schema-level constraints aligned to durable identity decisions.</done>
</task>

<task type="auto">
  <name>Task 01-02-02: Add migrations with explicit index names for idempotency and inbox access</name>
  <files>priv/repo/migrations/*_create_chimeway_events.exs, priv/repo/migrations/*_create_chimeway_notifications.exs</files>
  <read_first>.planning/phases/01-durable-core-spine/01-PATTERNS.md, priv/repo/migrations/*_create_chimeway_events.exs, priv/repo/migrations/*_create_chimeway_notifications.exs</read_first>
  <action>Create migration `*_create_chimeway_events.exs` with `create table(:chimeway_events, primary_key: false)` and UUID PK plus required columns. Add `create unique_index(:chimeway_events, [:idempotency_key], name: :chimeway_events_idempotency_key_index)`. Create migration `*_create_chimeway_notifications.exs` with UUID PK, FK `event_id` referencing `:chimeway_events`, recipient fields, lifecycle timestamps, metadata map, timestamps. Add `create unique_index(:chimeway_notifications, [:event_id, :recipient_identity], name: :chimeway_notifications_event_recipient_index)` and `create index(:chimeway_notifications, [:recipient_identity, :read_at, :inserted_at], name: :chimeway_notifications_inbox_read_inserted_index)`.</action>
  <verify>mix ecto.migrate && rg "create table\\(:chimeway_events" priv/repo/migrations && rg "chimeway_events_idempotency_key_index" priv/repo/migrations && rg "references\\(:chimeway_events" priv/repo/migrations && rg "chimeway_notifications_event_recipient_index|chimeway_notifications_inbox_read_inserted_index" priv/repo/migrations</verify>
  <acceptance_criteria>
    - events migration contains `create table(:chimeway_events` and the named unique index for `:idempotency_key`
    - notifications migration contains FK `event_id` to `:chimeway_events` and named unique index on `[:event_id, :recipient_identity]`
    - notifications migration contains inbox read/order index `:chimeway_notifications_inbox_read_inserted_index`
  </acceptance_criteria>
  <done>Database guarantees idempotency and per-recipient uniqueness under concurrency.</done>
</task>

<task type="auto">
  <name>Task 01-02-03: Wire Ecto.Multi transactional persistence and idempotency tests</name>
  <files>lib/chimeway/trigger.ex, test/support/data_case.ex, test/chimeway/persistence_transaction_test.exs, test/chimeway/idempotency_constraint_test.exs, test/chimeway/migration_contract_test.exs</files>
  <read_first>lib/chimeway/trigger.ex, lib/chimeway/events/event.ex, lib/chimeway/notifications/notification.ex, test/support/data_case.ex, .planning/phases/01-durable-core-spine/01-VALIDATION.md</read_first>
  <action>Update `Chimeway.Trigger.trigger/3` to persist through one `Ecto.Multi`: `insert(:event, Event.changeset(...))`, then `run(:notifications, fn repo, %{event: event} -> repo.insert_all("chimeway_notifications", rows) end)`. Add `sanitize_payload/1` and `sanitize_metadata/1` helpers in `Chimeway.Trigger` and apply them before event/notification attrs are inserted (drop keys `\"password\"`, `\"token\"`, and `\"secret\"` when present). Translate unique-idempotency conflicts into a single deterministic duplicate return shape `{:duplicate, existing_event}` by matching the idempotency constraint error and querying `chimeway_events` by `idempotency_key`. Add `DataCase` with SQL Sandbox ownership. Add `persistence_transaction_test` to assert event + N notifications rollback together on insertion failure. Add `idempotency_constraint_test` with serial duplicate and concurrent duplicate (`Task.async_stream`) assertions that expect `{:duplicate, existing_event}`. Add `migration_contract_test` to assert both tables and named indexes exist.</action>
  <verify>rg "Ecto\\.Multi\\.new\\(\\)|Ecto\\.Multi\\.insert\\(:event" lib/chimeway/trigger.ex && rg "sanitize_payload\\(|sanitize_metadata\\(" lib/chimeway/trigger.ex && rg "password|token|secret" lib/chimeway/trigger.ex && rg "idempotency_key" lib/chimeway/trigger.ex && mix test test/chimeway/persistence_transaction_test.exs test/chimeway/idempotency_constraint_test.exs test/chimeway/migration_contract_test.exs --seed 0</verify>
  <acceptance_criteria>
    - lib/chimeway/trigger.ex contains `Ecto.Multi.new()` and `Ecto.Multi.insert(:event`
    - lib/chimeway/trigger.ex contains explicit duplicate-branch handling for idempotency collisions plus query by `idempotency_key` returning exactly `{:duplicate, existing_event}`
    - test/chimeway/idempotency_constraint_test.exs includes both serial and concurrent duplicate assertions with expected single canonical event row
  </acceptance_criteria>
  <done>Transactional persistence and idempotency protection satisfy CORE-03 and INBX-01 baseline durability.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-02-RACE-DUPLICATE-EVENTS
    severity: high
    threat: "Concurrent trigger calls with identical idempotency keys create duplicate canonical events."
    mitigation: "Named unique index `:chimeway_events_idempotency_key_index` and duplicate normalization path in Trigger."
    mitigation_check: "mix test test/chimeway/idempotency_constraint_test.exs --seed 0"
  - id: TM-02-PARTIAL-WRITE
    severity: high
    threat: "Event persists while notification fanout fails, producing inconsistent explainability state."
    mitigation: "Single `Ecto.Multi` transaction with rollback on notification insertion failure."
    mitigation_check: "mix test test/chimeway/persistence_transaction_test.exs --seed 0"
  - id: TM-02-PAYLOAD-DATA-LEAK
    severity: medium
    threat: "Sensitive data over-persisted in payload/metadata fields, increasing disclosure risk."
    mitigation: "Persist only minimum required fields (`payload`, `metadata`) and document redaction/default minimization in schema comments/tests."
    mitigation_check: "rg \"field :payload, :map\" lib/chimeway/events/event.ex && rg \"field :metadata, :map\" lib/chimeway/notifications/notification.ex && rg \"sanitize_payload\\(|sanitize_metadata\\(\" lib/chimeway/trigger.ex && mix test test/chimeway/persistence_transaction_test.exs --seed 0"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix deps.get`
- [ ] `mix ecto.create && mix ecto.migrate`
- [ ] `mix test test/chimeway/persistence_transaction_test.exs test/chimeway/idempotency_constraint_test.exs test/chimeway/migration_contract_test.exs --seed 0`
- [ ] `rg "unique_index\\(:chimeway_events, \\[:idempotency_key\\].*chimeway_events_idempotency_key_index" priv/repo/migrations`
- [ ] `rg "unique_index\\(:chimeway_notifications, \\[:event_id, :recipient_identity\\].*chimeway_notifications_event_recipient_index" priv/repo/migrations`
- [ ] Security gate check: all high-severity threat mitigation checks pass
</verification>

<success_criteria>
- Event and notification rows persist atomically with rollback integrity.
- Duplicate idempotency attempts never create more than one canonical event row.
- INBX-01 fanout persistence is validated with tests, not just migration presence.
- Security gate remains enabled (`ASVS L1`, block on high) with no unresolved high-severity threats.
</success_criteria>

<output>
After completion, create `.planning/phases/01-durable-core-spine/01-02-SUMMARY.md`.
</output>
