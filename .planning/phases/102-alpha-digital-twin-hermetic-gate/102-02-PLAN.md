---
phase: 102-alpha-digital-twin-hermetic-gate
plan: "02"
type: execute
wave: 2
depends_on:
  - 102-01
files_modified:
  - priv/alpha_twin/scenario-ledger.json
  - test/fixtures/alpha_twin/lib/alpha_twin/runner.ex
  - test/fixtures/alpha_twin/lib/alpha_twin/proof_summary.ex
  - test/fixtures/alpha_twin/test/alpha_twin_test.exs
  - test/chimeway/alpha_twin_runner_test.exs
autonomous: true
requirements:
  - TWIN-02
must_haves:
  truths:
    - "One closed, versioned, unique, ordered scenario ledger drives host actions, transport outcomes, CrossWake consumption, and proof IDs per D-05."
    - "The merge-blocking ledger covers every D-08 scenario and proves durable lifecycle/explanation convergence rather than returned values alone per D-09."
    - "Repeated and concurrently attempted scenarios converge without duplicate targets, provider observations, recoveries, or route activations."
    - "Proof is emitted only after recursive sentinel scans across storage, traces, telemetry, exceptions, observations, and final bytes pass per D-10."
  artifacts:
    - path: "priv/alpha_twin/scenario-ledger.json"
      provides: "Complete canonical D-08 scenario order"
    - path: "test/fixtures/alpha_twin/lib/alpha_twin/proof_summary.ex"
      provides: "Closed canonical safe proof encoding and recursive leak scan"
    - path: "test/fixtures/alpha_twin/test/alpha_twin_test.exs"
      provides: "Complete process-level scenario and negative matrix"
  key_links:
    - from: "priv/alpha_twin/scenario-ledger.json"
      to: "AlphaTwin.Runner and AlphaTwin.ScriptedAPNSTransport"
      via: "closed string ID lookup with exact order and one-outcome consumption"
    - from: "AlphaTwin.Runner"
      to: "Chimeway.Traces.explain_delivery/2"
      via: "durable tenant-qualified assertions after every ledger step"
    - from: "AlphaTwin.ProofSummary"
      to: "stdout proof bytes"
      via: "closed allowlist, canonical encoding, and final sentinel scan"
  assumptions:
    - id: "EDGE-TWIN-02-IDEMPOTENCY"
      statement: "Flagged unresolved assumption: running the complete ordered ledger twice against equivalent fresh inputs must produce the same canonical proof and no duplicate durable or provider/open effects."
      status: unresolved
      flagged: true
    - id: "EDGE-TWIN-02-CONCURRENCY"
      statement: "Flagged unresolved assumption: interrupted or parallel trigger, dispatch, recovery, invalidation, and open attempts must converge through existing locks/CAS/uniqueness to one explainable terminal effect per authoritative identity."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: TWIN-02
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT collapse local dispatch intent, provider acceptance, exact invalidation, protected activation, inbox seen, and inbox read into one success claim."
    - requirement_id: TWIN-02
      category: safety
      status: unverified
      flagged: true
      statement: "MUST NOT let replay, stale authority, denial, expiry, revocation, or a removed route activate a fallback route or produce a second provider/open side effect."
---

<objective>
Expand the proven tracer into the complete deterministic safety ledger required by TWIN-02 and D-05, D-08, D-09, and D-10.

Purpose: Reject regressions in fan-out, suppression, races, retry/expiry/collapse, crash recovery, privacy, and protected-open authorization with one ordered source of truth.
Output: The complete ledger, durable convergence assertions, negative/open matrix, and closed recursively scanned proof summary.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-CONTEXT.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-RESEARCH.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-PATTERNS.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-VALIDATION.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-01-SUMMARY.md

<interfaces>
- `AlphaTwin.Runner` already executes the accepted path against the extracted package and detached CrossWake checkout.
- `AlphaTwin.Clock.advance/2` is the only test-time advancement seam.
- `AlphaTwin.Registry` owns exact binding revisions and atomic intent consumption.
- `AlphaTwin.ScriptedAPNSTransport` consumes one real request against one ordered ledger outcome.
- `Chimeway.TargetRecovery.recover_tenant/2` accepts explicit `now:` and returns bounded tenant recovery facts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute the complete delivery, race, retry, expiry, collapse, and recovery ledger</name>
  <files>priv/alpha_twin/scenario-ledger.json, test/fixtures/alpha_twin/lib/alpha_twin/runner.ex, test/fixtures/alpha_twin/lib/alpha_twin/proof_summary.ex, test/fixtures/alpha_twin/test/alpha_twin_test.exs</files>
  <read_first>priv/alpha_twin/scenario-ledger.json, test/fixtures/alpha_twin/lib/alpha_twin/runner.ex, test/fixtures/alpha_twin/lib/alpha_twin/registry.ex, test/fixtures/alpha_twin/lib/alpha_twin/scripted_apns_transport.ex, test/fixtures/alpha_twin/lib/alpha_twin/proof_summary.ex, lib/chimeway/delivery_targets.ex, lib/chimeway/target_recovery.ex, lib/chimeway/traces.ex, .planning/phases/99-multi-installation-delivery-recovery/99-VERIFICATION.md, .planning/phases/100-optional-apns-adapter/100-VERIFICATION.md, .planning/phases/102-alpha-digital-twin-hermetic-gate/102-CONTEXT.md</read_first>
  <behavior>
    - The ledger contains exactly one ordered occurrence of two-installation fan-out, zero-target suppression, token rotation, revocation race, retry classification, expiry, opt-in safe collapse, trigger-commit recovery, and post-handoff ambiguity per D-08.
    - Each step uses public lifecycle/recovery APIs and asserts real tenant-qualified rows plus `explain_delivery/2`; staging does not insert Chimeway lifecycle rows per D-02 and D-09.
    - Repeating the ledger and racing dispatch/recovery converges to stable target, attempt, provider-observation, and terminal explanation counts.
    - Provider acceptance never implies protected open, seen, or read; ambiguous handoff never causes silent resend.
  </behavior>
  <action>Start with failing canonical-ledger and process tests. Expand `scenario-ledger.json` to a literal closed ordered list of stable string IDs covering the delivery/recovery portion of D-08; reject missing, duplicate, reordered, unknown, non-string, or extra fields without atomizing input. Teach `AlphaTwin.Runner` to execute each ID through public trigger, planning, dispatch, and `TargetRecovery` calls using explicit clock advances and the ordered transport script. For fan-out assert two independent opaque targets/attempt histories; for no-target assert the stable suppression reason; for rotation and revocation assert exact old-revision CAS; for retry assert reason classification and one later attempt; for expiry assert pre-I/O suppression; for collapse assert opt-in and installation-safe IDs while distinct notifications remain uncoalesced; for trigger-commit recovery assert the committed event is discovered exactly once; for ambiguity assert attempt-start/claim evidence and no automatic resend. After every scenario query the durable lifecycle and `Chimeway.Traces.explain_delivery/2`, assert deterministic stable sorting, and record only closed taxonomy facts. Execute the full ledger twice from equivalent fresh databases plus an orchestrated parallel dispatch/recovery race to make the two flagged assumptions executable.</action>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/fixtures/alpha_twin/test/alpha_twin_test.exs --only alpha_twin_delivery_matrix --seed 0 --warnings-as-errors &amp;&amp; mix verify.alpha_twin &amp;&amp; mix verify.alpha_twin</automated>
  </verify>
  <acceptance_criteria>The canonical ledger accepts only the complete unique order; all nine delivery/recovery scenario groups produce the specified durable and explanation facts; two consecutive commands emit identical canonical proof bytes; forced parallelism preserves one authoritative target/provider/recovery effect; old invalidation cannot affect a replacement; ambiguity never silently resends; and removing any scenario or durable explanation assertion makes the gate fail.</acceptance_criteria>
  <done>The delivery/recovery matrix is deterministic, idempotent, concurrency-safe, durable, and fully explainable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close leak prevention and offline open reauthorization, denial, and replay</name>
  <files>priv/alpha_twin/scenario-ledger.json, test/fixtures/alpha_twin/lib/alpha_twin/runner.ex, test/fixtures/alpha_twin/lib/alpha_twin/proof_summary.ex, test/fixtures/alpha_twin/test/alpha_twin_test.exs, test/chimeway/alpha_twin_runner_test.exs</files>
  <read_first>priv/alpha_twin/scenario-ledger.json, test/fixtures/alpha_twin/lib/alpha_twin/runner.ex, test/fixtures/alpha_twin/lib/alpha_twin/proof_summary.ex, lib/chimeway/safe_evidence.ex, lib/chimeway/traces.ex, ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/resolver.ex, ../crosswake/lib/crosswake/proof_lane/evidence.ex, .planning/phases/98-privacy-safe-delivery-evidence/98-VERIFICATION.md, .planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md, .planning/phases/102-alpha-digital-twin-hermetic-gate/102-CONTEXT.md</read_first>
  <behavior>
    - The same ledger contains recursive-leak, offline queue/reauthorization, denial, and replay IDs exactly once per D-05 and D-08.
    - An opaque offline tap activates only after reconnect, host one-time consumption, current tenant/revision/session/expiry/manifest checks, and current RouteGate authorization.
    - Expired, revoked, mismatched, logged-out, tenant-switched, removed-route, denied, and replayed inputs activate no route and emit only stable closed denial IDs.
    - Persisted safe facts, traces, telemetry captures, exception/error output, redacted transport observations, and final proof bytes are recursively scanned with raw token, identity, URL, payload, credential, and provider-body sentinels before output per D-10.
  </behavior>
  <action>Add the remaining D-08 IDs to the same ledger and drive all open cases through CrossWake's real `IntentConsumer`/`Resolver`/RouteGate contracts, never copied policy logic. Queue only opaque evidence offline; advance fixed time and mutate host authority before reconnect to prove current-policy reauthorization. Cover authorized activation once plus expired, revoked, revision mismatch, session mismatch/logout, tenant switch, removed action/route, explicit denial, and replay with no fallback. Implement `AlphaTwin.ProofSummary` as a closed versioned schema with exact allowed keys, bounded scalar values, complete unique ordered scenario results, separate outcome taxonomy fields, and deterministic canonical encoding. Recursively scan every gathered source and the final encoded bytes using unique nested sentinels for raw token, actor/recipient identity, deep URL/open content, payload text, credentials, and provider body. On failure return only a stable rule ID and safe path; never echo rejected values, generic maps, inspected exceptions, or transport bodies. Add mutation tests for each unknown key, reordered ID, taxonomy collapse, missing scan source, sentinel location, and replay activation.</action>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/fixtures/alpha_twin/test/alpha_twin_test.exs test/chimeway/alpha_twin_runner_test.exs --only alpha_twin_safety_matrix --seed 0 --warnings-as-errors &amp;&amp; mix verify.alpha_twin</automated>
  </verify>
  <acceptance_criteria>All D-08 IDs are complete/unique/ordered; one offline open activates once only after current authorization; every stale/denied/replayed case activates nothing and has a sanitized rule ID; no fallback route exists; all six evidence-source classes plus final bytes are scanned; each injected sentinel is rejected without appearing in error/proof output; unknown maps/keys fail closed; and the final proof keeps provider acceptance, invalidation, protected open, seen, and read distinct.</acceptance_criteria>
  <done>The complete TWIN-02 matrix passes as one hermetic, closed, privacy-safe, durable proof.</done>
</task>

</tasks>

## Artifacts this phase produces

- Ordered ledger scenario IDs for: accepted tracer; two-installation fan-out; zero-target suppression; token rotation; revocation race; classified retry; expiry; opt-in collapse; trigger-commit recovery; post-handoff ambiguity; recursive leak prevention; offline reconnect authorization; stale/denied open; replay rejection.
- `AlphaTwin.Runner` scenario executors and convergence assertions for the complete matrix.
- `AlphaTwin.ProofSummary` fields `schema_version`, `proof_class`, `chimeway_artifact_sha256`, `crosswake_sha`, `scenario_results`, and separated `claim_taxonomy`; exact keys remain closed.
- Recursive sentinel corpus embedded in fixture tests for token, identity, URL/content, payload, credential, and provider-body leakage.
- Process-level tests for idempotent rerun, forced concurrency, durable explanation, negative authorization, and non-echoing failures.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger input -> runner/transport | Untrusted decoded IDs and fields select safety-critical actions/outcomes. |
| Concurrent worker/recovery -> durable lifecycle | Multiple claimants may race for one authoritative target/event. |
| Offline evidence -> host/CrossWake authorization | Client evidence is non-authoritative until current policy reauthorization. |
| Collected diagnostics -> proof bytes | Sensitive values may be nested in storage, traces, captures, errors, or observations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-102-05 | Tampering | scenario ledger | high | mitigate | Exact keys, version, complete unique order, closed string lookup, no atom creation, and mutation tests. |
| T-102-06 | Tampering | rotation/recovery races | high | mitigate | Tenant/revision-qualified CAS, durable locks/uniqueness, repeated/parallel process tests, and explanation counts. |
| T-102-07 | Elevation of Privilege | offline protected open | high | mitigate | Atomic host consumption plus current tenant/session/revision/expiry/manifest/RouteGate checks and no-fallback negative matrix. |
| T-102-08 | Information Disclosure | proof aggregation | high | mitigate | Closed projection, recursive sentinels across every source and final bytes, stable rule/path-only errors. |
| T-102-09 | Repudiation | outcome taxonomy | medium | mitigate | Separate durable facts for intent, provider outcome, invalidation, open, seen, and read in explanations and proof. |
</threat_model>

<verification>
Run the complete fixture matrix with `--seed 0`, run `mix verify.alpha_twin` twice, compare proof bytes, and execute mutation tests that remove/reorder IDs, force races, inject sentinels, and replay opens.
</verification>

<success_criteria>
- Every D-08 scenario has one ordered ledger identity and executable durable assertions.
- Idempotent rerun and concurrency probes converge without duplicate effects.
- Negative opens fail closed without fallback or leaked input.
- Closed proof bytes are deterministic, non-echoing, and preserve the honest claim taxonomy.
</success_criteria>

<output>
Create `.planning/phases/102-alpha-digital-twin-hermetic-gate/102-02-SUMMARY.md` when done.
</output>
