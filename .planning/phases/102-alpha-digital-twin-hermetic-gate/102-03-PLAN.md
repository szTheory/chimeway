---
phase: 102-alpha-digital-twin-hermetic-gate
plan: "03"
type: execute
wave: 3
depends_on:
  - 102-02
files_modified:
  - lib/chimeway/mobile_proof/extension.ex
  - lib/mix/tasks/verify.physical_proof_contract.ex
  - test/fixtures/alpha_twin_physical_proof/valid.json
  - test/fixtures/alpha_twin_physical_proof/negative-corpus.json
  - test/chimeway/mobile_proof_extension_test.exs
  - test/chimeway/alpha_twin_provenance_test.exs
  - mix.exs
  - .github/workflows/ci.yml
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
requirements:
  - GATE-01
  - TWIN-01
  - TWIN-02
must_haves:
  truths:
    - "`mix verify.alpha_twin` and `mix verify.physical_proof_contract` are deterministic, non-interactive, credential-free, independently rerunnable entrypoints per D-11."
    - "The final safe proof bytes cryptographically bind the validated Chimeway artifact digest and exact CrossWake SHA, and reject mutable/mismatched provenance per D-13."
    - "The versioned Chimeway extension references CrossWake's canonical Phase 162 contract, separates executable facts from subjective visible-alert observation, and rejects every D-15 malformed class per D-14 and D-15."
    - "The `verify_alpha_twin` job checks out the canonical CrossWake remote at the locked full SHA, proves a clean detached checkout, runs both named commands without Apple credentials, and is required/result-propagated by both `pr-gate` and `ci-gate` per D-12."
  artifacts:
    - path: "lib/chimeway/mobile_proof/extension.ex"
      provides: "Versioned closed Chimeway physical-proof extension validator"
    - path: "lib/mix/tasks/verify.physical_proof_contract.ex"
      provides: "Credential-free valid/negative physical-proof fixture gate"
    - path: ".github/workflows/ci.yml"
      provides: "Required verify_alpha_twin job and both aggregate-gate links"
  key_links:
    - from: "Chimeway.MobileProof.Extension"
      to: "Crosswake.ProofLane.PhysicalIphoneContract"
      via: "reference/digest and canonical validator delegation, never copied assertion ownership"
    - from: ".github/workflows/ci.yml#verify_alpha_twin"
      to: "mix verify.alpha_twin and mix verify.physical_proof_contract"
      via: "credential-free PostgreSQL lane at the locked detached CrossWake SHA"
    - from: "pr-gate and ci-gate"
      to: "verify_alpha_twin"
      via: "needs membership, result environment, and aggregate-gate.sh invocation"
  assumptions:
    - id: "EDGE-GATE-01-UNCLASSIFIED"
      statement: "Flagged unresolved assumption: GATE-01 is complete only when both named local entrypoints, exact provenance, the hosted verify_alpha_twin job, both aggregate needs/result links, malformed-fixture rejection, and non-vacuous execution are asserted programmatically."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: GATE-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT treat schema-valid committed fixtures or a green hermetic CI job as evidence that a physical iPhone displayed an alert."
    - requirement_id: GATE-01
      category: safety
      status: unverified
      flagged: true
      statement: "MUST NOT accept a floating CrossWake ref, dirty checkout, mismatched Chimeway digest, reordered/unknown assertion, or proof owned by the wrong project/class as promotion evidence."
---

<objective>
Complete GATE-01 with strict provenance, a closed future physical-proof extension, and a required credential-free CI lane implementing D-11 through D-15.

Purpose: Make the full Alpha proof reproducible and merge-blocking while ensuring CI never claims real Apple/device evidence.
Output: The physical-proof contract gate, provenance-bound proof bytes, `verify_alpha_twin` CI job, and programmatically locked `pr-gate`/`ci-gate` aggregation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-CONTEXT.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-RESEARCH.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-PATTERNS.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-VALIDATION.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-02-SUMMARY.md

<interfaces>
- `Crosswake.ProofLane.PhysicalIphoneContract.schema_version/0`, `assertions/0`, and report validation remain canonical for Phase 162 ownership/order.
- `Crosswake.ProofLane.Evidence` supplies the established closed schema, privacy scan, digest barrier, and physical proof-class boundary.
- `scripts/ci/aggregate-gate.sh` is the only pass/fail aggregator for `pr-gate` and `ci-gate`.
- `mix ci.verify_gates` runs `test/chimeway/release_gate_contract_test.exs` and must remain the local topology contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind immutable provenance into a closed CrossWake-referencing mobile proof extension</name>
  <reversibility rating="costly">The versioned extension becomes Phase 103's machine-readable boundary, so incompatible changes require a deliberate version and coordinated fixture/consumer update.</reversibility>
  <files>lib/chimeway/mobile_proof/extension.ex, lib/mix/tasks/verify.physical_proof_contract.ex, test/fixtures/alpha_twin_physical_proof/valid.json, test/fixtures/alpha_twin_physical_proof/negative-corpus.json, test/chimeway/mobile_proof_extension_test.exs, test/chimeway/alpha_twin_provenance_test.exs</files>
  <read_first>priv/alpha_twin/scenario-ledger.json, scripts/prove-alpha-twin.exs, test/fixtures/alpha_twin/lib/alpha_twin/proof_summary.ex, lib/chimeway/safe_evidence.ex, lib/mix/tasks/verify.adoption_paths.ex, ../crosswake/lib/crosswake/proof_lane/physical_iphone_contract.ex, ../crosswake/lib/crosswake/proof_lane/evidence.ex, ../crosswake/script/verify_physical_iphone_report_contract.sh, .planning/phases/102-alpha-digital-twin-hermetic-gate/102-CONTEXT.md, .planning/phases/102-alpha-digital-twin-hermetic-gate/102-RESEARCH.md</read_first>
  <behavior>
    - Final Alpha proof canonical bytes contain and validate the exact 64-hex Chimeway archive SHA-256 and the exact 40-hex CrossWake SHA from the ledger per D-13.
    - `Chimeway.MobileProof.Extension` accepts only its exact version/keys, hermetic executable-fact fields, canonical CrossWake report reference, owner/proof class, ordered scenario/assertion identities, and matching revisions per D-14.
    - Missing, duplicate, reordered, unknown, wrong-owner, wrong-proof-class, sensitive, malformed, or revision-mismatched cases return one stable rule/path and never echo rejected data per D-15.
    - Visible-alert observation is represented as explicitly subjective/not-asserted in Phase 102 and cannot be promoted by this command.
  </behavior>
  <action>Start with failing provenance and contract tests. Update the final Alpha proof validation so it compares the exact archive digest computed from bytes and the checked-out `git rev-parse HEAD` against the closed ledger before canonical encoding; reject branch/tag refs, dirty/mismatched trees, and substituted digests. Implement `Chimeway.MobileProof.Extension` with a numeric/string versioned closed schema, bounded values, explicit owner `chimeway`, proof class `hermetic`, CrossWake canonical contract schema/reference digest, exact full `crosswake_sha`, exact `chimeway_artifact_sha256`, complete ordered scenario IDs, and separate executable-fact versus subjective-observation fields. Delegate canonical physical assertion order/ownership validation to the CrossWake module loaded from the pinned checkout; do not copy its assertion vocabulary. Build one valid fixture and a single closed negative-corpus file whose ordered cases cover every D-15 fault independently with expected stable rule IDs. Implement `Mix.Tasks.Verify.PhysicalProofContract` as an argv-free task that loads only committed fixtures, requires every named negative case to fail for its exact safe rule/path, requires the valid fixture to pass, and exits nonzero on missing/extra/vacuous cases. Do not call `xcodebuild`, Apple tools, APNs, device services, or the CrossWake shell script that owns physical execution.</action>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/mobile_proof_extension_test.exs test/chimeway/alpha_twin_provenance_test.exs --seed 0 --warnings-as-errors &amp;&amp; mix verify.physical_proof_contract &amp;&amp; mix verify.alpha_twin</automated>
  </verify>
  <acceptance_criteria>The valid fixture passes; every D-15 negative case fails with its exact non-echoing rule/path; removing any case creates a count/order failure; mutated archive bytes, SHA, remote, dirty checkout, proof owner/class, schema version, scenario/assertion order, and CrossWake reference all fail; final Alpha proof bytes contain the actual digest and exact `f2c502cdb1ce572a4a57257d9e3c051665704b90`; no output contains injected sensitive values; and no result asserts visible-alert observation.</acceptance_criteria>
  <done>Both named commands independently pass with strict immutable provenance and honest physical-proof boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require the credential-free Alpha lane in both aggregate gates</name>
  <files>mix.exs, .github/workflows/ci.yml, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>mix.exs, .github/workflows/ci.yml, test/chimeway/release_gate_contract_test.exs, scripts/ci/aggregate-gate.sh, lib/mix/tasks/verify.alpha_twin.ex, lib/mix/tasks/verify.physical_proof_contract.ex, priv/alpha_twin/scenario-ledger.json, .planning/phases/102-alpha-digital-twin-hermetic-gate/102-CONTEXT.md, .planning/phases/102-alpha-digital-twin-hermetic-gate/102-VALIDATION.md</read_first>
  <behavior>
    - CI has one job named exactly `verify_alpha_twin` with PostgreSQL 15, pinned action SHAs, explicit timeout, canonical CrossWake checkout at the exact full SHA, detached/clean validation, migrations, and both locked Mix invocations per D-12.
    - The job defines no Apple credential/secrets input, Xcode/macOS runner, APNs network call, or physical-proof execution.
    - Both `pr-gate` and `ci-gate` include `verify_alpha_twin` in `needs`, propagate its result under a unique environment key, and pass that key to `aggregate-gate.sh`.
    - `mix ci.verify_gates` programmatically asserts aliases, exact remote/SHA, checkout cleanliness, both commands, non-vacuous fixture execution, job topology, and both aggregate links.
  </behavior>
  <action>Write failing release-gate contract cases first. Add any root alias needed for local parity without duplicating the two locked command implementations. Add `verify_alpha_twin` to `.github/workflows/ci.yml` using `ubuntu-latest`, PostgreSQL 15, the repository's pinned checkout/setup action SHAs, explicit `MIX_ENV`/`DATABASE_URL`, and a measured explicit timeout. Check out Chimeway normally; check out `https://github.com/szTheory/crosswake.git` into a sibling path at exactly `f2c502cdb1ce572a4a57257d9e3c051665704b90`; assert `rev-parse HEAD`, detached HEAD, canonical origin, and empty porcelain before running database setup, `mix verify.alpha_twin`, and `mix verify.physical_proof_contract`. Add the job to both aggregate `needs` lists, result environment blocks, and `aggregate-gate.sh` inputs. Extend `release_gate_contract_test.exs` with structural extraction plus mutation cases for job rename/removal, SHA shortening/floating, wrong remote/path, missing detached/clean checks, either missing Mix command, Apple secret/Xcode/macOS introduction, absent timeout/Postgres/migration, zero-test/vacuous fixture logic, and every missing/misnamed `pr-gate` or `ci-gate` needs/result/aggregate link. Keep all acceptance machine-testable; do not add UAT or human checkpoints.</action>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --only alpha_twin_gate_contract --seed 0 --warnings-as-errors &amp;&amp; mix verify.alpha_twin &amp;&amp; mix verify.physical_proof_contract &amp;&amp; mix ci.verify_gates</automated>
  </verify>
  <acceptance_criteria>The focused topology suite programmatically finds exactly one `verify_alpha_twin` job, exact remote/full SHA, detached/clean assertions, both commands, explicit timeout/PostgreSQL/migrations, no Apple/Xcode/macOS dependency, and exact needs/result/aggregate membership in both `pr-gate` and `ci-gate`; every listed mutation turns the contract red; both named commands and `mix ci.verify_gates` exit 0.</acceptance_criteria>
  <done>The hermetic Alpha proof is a required, non-vacuous, credential-free lane in both aggregate gates with local/CI parity locked.</done>
</task>

</tasks>

## Artifacts this phase produces

- Module `Chimeway.MobileProof.Extension` with `schema_version/0`, `validate/2`, and `canonical_bytes/1`.
- Mix task `Mix.Tasks.Verify.PhysicalProofContract` and command `mix verify.physical_proof_contract`.
- Fixture files `test/fixtures/alpha_twin_physical_proof/valid.json` and `negative-corpus.json`; corpus case IDs cover missing, duplicate, reordered, unknown, wrong-owner, wrong-proof-class, sensitive, malformed, and revision-mismatch faults.
- Proof fields `schema_version`, `owner`, `proof_class`, `chimeway_artifact_sha256`, `crosswake_sha`, `crosswake_contract_digest`, `scenario_ids`, `executable_facts`, and `subjective_observation`.
- CI job `verify_alpha_twin`, result environment key `VERIFY_ALPHA_TWIN`, and required `pr-gate`/`ci-gate` needs and aggregate inputs.
- Release-gate contract tag `alpha_twin_gate_contract` and mutation coverage for provenance, commands, credentials, job topology, non-vacuity, and both aggregate gates.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Proof JSON -> extension validator | Future physical evidence is untrusted and may spoof owner/class/revision or contain secrets. |
| Repository provenance -> proof bytes | Mutable/substituted source can masquerade as the tested implementation. |
| CI job -> aggregate gates | Missing/skipped/misnamed result wiring can create a false green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-102-10 | Spoofing | mobile proof extension | high | mitigate | Exact owner/proof class/version/revision checks plus canonical CrossWake validator and complete negative corpus. |
| T-102-11 | Tampering | artifact and CrossWake provenance | high | mitigate | Bind actual SHA-256/full SHA into canonical bytes; reject floating refs, dirty trees, mismatches, and unknown fields. |
| T-102-12 | Information Disclosure | malformed evidence errors | high | mitigate | Recursive sensitive scan and stable rule/path-only failures with non-echo mutation tests. |
| T-102-13 | Repudiation | visible-alert/device claim | medium | mitigate | Separate subjective observation field and forbid Phase 102 promotion/device claims in fixture and CI contracts. |
| T-102-14 | Tampering | aggregate CI topology | high | mitigate | Mutation-tested needs/result/aggregate membership in both gates and fail-closed `aggregate-gate.sh`. |
</threat_model>

<verification>
Run `mix verify.alpha_twin`, `mix verify.physical_proof_contract`, and `mix ci.verify_gates`. The release contract must programmatically prove both aggregate gates and exact provenance; no conversational UAT applies.
</verification>

<success_criteria>
- Both named gates are deterministic, credential-free, and independently green.
- Safe proof bytes bind the actual Chimeway archive digest and exact CrossWake SHA.
- Every D-15 malformed physical-evidence class is rejected without echoing input.
- `verify_alpha_twin` is required and result-propagated by both `pr-gate` and `ci-gate`.
- The GATE-01 unclassified assumption and descriptor-less prohibitions remain explicitly flagged.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Full host/Chimeway/CrossWake path reproducible without Apple credentials and regression-blocking | 01-03 | COVERED | Tracer, full ledger, proof contract, and required CI lane. |
| REQ | TWIN-01 | Sanitized real-persistence host, deterministic time, registry, scripted APNs | 01 | COVERED | Public packaged tracer plus closed seams. |
| REQ | TWIN-02 | Complete delivery/recovery/privacy/offline-open matrix | 01-02 | COVERED | One ordered ledger plus idempotency/concurrency/negative proof. |
| REQ | GATE-01 | Named CI proof and physical-proof contract without Apple credentials | 03 | COVERED | Two commands, strict provenance, job, and both aggregates. |
| RESEARCH | — | Immutable package archive and real copied PostgreSQL migrations | 01 | COVERED | `ArtifactArchive` and clean fixture orchestration. |
| RESEARCH | — | Narrow system-default clock across exercised target/APNs/recovery/open boundaries | 01 | COVERED | Clock provider and explicit fixture advancement. |
| RESEARCH | — | Closed ledger, durable explanations, and recursive safe proof | 02 | COVERED | Canonical IDs, convergence, and sentinel scan. |
| RESEARCH | — | CrossWake canonical physical contract reference and negative corpus | 03 | COVERED | Extension delegates ownership/order validation. |
| RESEARCH | — | Required CI lane with local/release-gate parity | 03 | COVERED | Mutation-tested both-gate topology. |
| CONTEXT | D-01 | Dedicated sanitized Alpha fixture | 01 | COVERED | No demo/example retrofit. |
| CONTEXT | D-02 | Immutable package, copied migrations, public lifecycle path | 01-02 | COVERED | Tracer and every scenario use public/durable truth. |
| CONTEXT | D-03 | Real CrossWake contracts at full immutable SHA | 01,03 | COVERED | Canonical remote and exact SHA are executable contracts. |
| CONTEXT | D-04 | Host-owned token/binding/eligibility/intent authority | 01-02 | COVERED | Registry boundary and leak tests. |
| CONTEXT | D-05 | One closed versioned ordered ledger | 01-02 | COVERED | Same IDs drive all participants and proof. |
| CONTEXT | D-06 | Narrow clock, production default, no sleeps | 01-02 | COVERED | Explicit advancement and mutation tests. |
| CONTEXT | D-07 | Scripted transport behind shipped behaviour | 01-02 | COVERED | Real request and production classifier. |
| CONTEXT | D-08 | Exact merge-blocking safety matrix | 02 | COVERED | Every named scenario has one ledger ID. |
| CONTEXT | D-09 | Durable truth and honest lifecycle taxonomy | 01-02 | COVERED | Repository plus `explain_delivery/2` per step. |
| CONTEXT | D-10 | Closed recursively scanned proof summary | 02-03 | COVERED | All source classes and final bytes scanned. |
| CONTEXT | D-11 | Two named deterministic credential-free tasks | 01,03 | COVERED | Both commands independently rerunnable. |
| CONTEXT | D-12 | Required verify_alpha_twin job and both aggregates | 03 | COVERED | Exact job/topology mutation tests. |
| CONTEXT | D-13 | Chimeway digest plus CrossWake SHA in proof bytes | 01,03 | COVERED | Computed/validated provenance binding. |
| CONTEXT | D-14 | Versioned Chimeway extension references CrossWake contract | 03 | COVERED | Separate executable/subjective facts. |
| CONTEXT | D-15 | Reject malformed/mismatched physical evidence | 03 | COVERED | Complete ordered negative corpus. |

Excluded without gap: Phase 103 real APNs/iPhone evidence and docs; FCM/Android, generic offline sync, analytics, rich actions, and broad device management. No new package install, fabricated external API matrix, assumption-delta checkpoint, or schema-push task is planned.

### Probe no-silent-drop accounting

- Edge probe surfaced 4 unresolved items; authored assumptions = 4 (`EDGE-TWIN-01-UNCLASSIFIED`, `EDGE-TWIN-02-IDEMPOTENCY`, `EDGE-TWIN-02-CONCURRENCY`, `EDGE-GATE-01-UNCLASSIFIED`); dropped = 0.
- Descriptor-less prohibition recall kept 6 bespoke product-safety/transparency items; authored `must_haves.prohibitions` = 6; dropped = 0.
- Canon breadcrumbs only: generic injection/path-traversal/privacy-compliance controls remain owned by the threat models and `$gsd-secure-phase`; they were not minted as probe prohibitions.

<output>
Create `.planning/phases/102-alpha-digital-twin-hermetic-gate/102-03-SUMMARY.md` when done.
</output>
