---
phase: 102-alpha-digital-twin-hermetic-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/verify.alpha_twin.ex
  - scripts/prove-alpha-twin.exs
  - priv/alpha_twin/scenario-ledger.json
  - test/fixtures/alpha_twin/mix.exs
  - test/fixtures/alpha_twin/config/config.exs
  - test/fixtures/alpha_twin/lib/alpha_twin/application.ex
  - test/fixtures/alpha_twin/lib/alpha_twin/runner.ex
  - test/fixtures/alpha_twin/lib/alpha_twin/proof_summary.ex
  - test/chimeway/alpha_twin_runner_test.exs
autonomous: true
requirements:
  - TWIN-01
  - TWIN-02
must_haves:
  truths:
    - "The dedicated sanitized Alpha host consumes one validated Chimeway package artifact, applies copied package migrations to PostgreSQL, and executes public trigger through explanation APIs per D-01 and D-02."
    - "The accepted-handoff tracer crosses the host registry, real Chimeway target/APNs path, durable repository truth, and real CrossWake protected-open resolver at the locked full SHA per D-03, D-04, and D-09."
    - "The accepted tracer uses the shipped transport behaviour with a bounded accepted response and no Pigeon, Apple credentials, or network I/O; deterministic multi-outcome transport is completed in Plan 02 per D-07."
  artifacts:
    - path: "scripts/prove-alpha-twin.exs"
      provides: "Immutable-package, CrossWake-provenance, migration, fixture, and proof orchestration"
    - path: "test/fixtures/alpha_twin/lib/alpha_twin/runner.ex"
      provides: "Clean-room host to Chimeway to CrossWake tracer"
    - path: "priv/alpha_twin/scenario-ledger.json"
      provides: "Versioned ordered scenario and provenance source"
  key_links:
    - from: "lib/mix/tasks/verify.alpha_twin.ex"
      to: "scripts/prove-alpha-twin.exs"
      via: "Mix task loads the runner and propagates its closed exit status"
    - from: "scripts/prove-alpha-twin.exs"
      to: "test/fixtures/alpha_twin/mix.exs"
      via: "CHIMEWAY_PACKAGE_PATH and validated CROSSWAKE_PATH point only at immutable inputs"
    - from: "test/fixtures/alpha_twin/lib/alpha_twin/runner.ex"
      to: "Chimeway.Traces.explain_delivery/2"
      via: "tenant-qualified public trigger, dispatch, and explanation path"
  assumptions:
    - id: "EDGE-TWIN-01-UNCLASSIFIED"
      statement: "Flagged unresolved assumption: TWIN-01's unclassified probe is fully represented by a clean-room packaged host, real PostgreSQL migrations, deterministic clock, host registry, scripted transport, and public CrossWake contracts; mix verify.alpha_twin must fail if any of those path segments is absent or vacuous."
      status: unresolved
      flagged: true
  prohibitions:
    - requirement_id: TWIN-01
      category: safety
      status: unverified
      flagged: true
      statement: "MUST NOT transfer raw device-token custody, binding authority, eligibility, or one-time intent authority from the Alpha host into Chimeway or CrossWake."
    - requirement_id: TWIN-01
      category: transparency
      status: unverified
      flagged: true
      statement: "MUST NOT describe a scripted transport result as a live APNs, physical-device, visible-alert, protected-open, seen, or read observation."
---

<objective>
Establish the production-quality immutable-package Alpha tracer for TWIN-01 and the first executable slice of TWIN-02, implementing D-01 through D-05 and D-09.

Purpose: Prove the architecture across immutable package bytes, copied migrations, real persistence, host authority, Chimeway delivery, and CrossWake protected-open policy before expanding the behavior matrix.
Output: `mix verify.alpha_twin`, a committed clean-room host, immutable Chimeway/CrossWake provenance, and one accepted-handoff/protected-open proof path.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-CONTEXT.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-RESEARCH.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-PATTERNS.md
@.planning/phases/102-alpha-digital-twin-hermetic-gate/102-VALIDATION.md
@.planning/phases/100-optional-apns-adapter/100-VERIFICATION.md
@.planning/phases/101-crosswake-registration-protected-open/101-VERIFICATION.md

<interfaces>
- `Chimeway.AdoptionProof.ArtifactArchive.with_validated_archive/4` validates the archive SHA-256 and materialized package tree before invoking a callback.
- `Chimeway.APNS.Transport.push/3` accepts a `%Chimeway.APNS.Transport.Request{}` and returns a typed transport result or a closed error.
- `Chimeway.DeliveryTargets.begin_target_attempt/2` and `Chimeway.TargetRecovery.recover_tenant/2` are the tenant-qualified durable attempt/recovery seams.
- `Chimeway.Traces.explain_delivery/2` is the public durable explanation surface.
- `Crosswake.Companions.Chimeway.Resolver.resolve/3` is the authoritative manifest/current-RouteGate protected-open path.
- Canonical CrossWake origin is `https://github.com/szTheory/crosswake.git`; locked implementation is `f2c502cdb1ce572a4a57257d9e3c051665704b90`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trace one accepted notification from immutable package bytes to one authorized CrossWake activation</name>
  <precondition>The exact CrossWake commit `f2c502cdb1ce572a4a57257d9e3c051665704b90` is fetchable from `https://github.com/szTheory/crosswake.git`; assert with an isolated `git fetch --depth=1` and halt without substituting another revision if canonical upload-pack reports `not our ref`.</precondition>
  <reversibility rating="costly">The versioned ledger and package/provenance contract will be consumed by the remaining phase plans, so changing their shape requires coordinated fixture, proof, and CI updates.</reversibility>
  <files>lib/mix/tasks/verify.alpha_twin.ex, scripts/prove-alpha-twin.exs, priv/alpha_twin/scenario-ledger.json, test/fixtures/alpha_twin/mix.exs, test/fixtures/alpha_twin/config/config.exs, test/fixtures/alpha_twin/lib/alpha_twin/application.ex, test/fixtures/alpha_twin/lib/alpha_twin/runner.ex, test/fixtures/alpha_twin/lib/alpha_twin/proof_summary.ex, test/chimeway/alpha_twin_runner_test.exs</files>
  <read_first>AGENTS.md, .planning/phases/102-alpha-digital-twin-hermetic-gate/102-CONTEXT.md, .planning/phases/102-alpha-digital-twin-hermetic-gate/102-RESEARCH.md, .planning/phases/102-alpha-digital-twin-hermetic-gate/102-PATTERNS.md, scripts/prove-adoption-paths.exs, priv/adoption_proof/artifact_archive.ex, lib/mix/tasks/verify.adoption_paths.ex, test/fixtures/apns_consumer/mix.exs, test/fixtures/apns_consumer/lib/apns_consumer.ex, lib/chimeway/delivery_targets.ex, lib/chimeway/adapters/apns.ex, lib/chimeway/target_recovery.ex, lib/chimeway/traces.ex, ../crosswake/AGENTS.md, ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/contracts.ex, ../crosswake/packages/crosswake_chimeway/lib/crosswake/companions/chimeway/resolver.ex</read_first>
  <behavior>
    - The root task builds Chimeway once, hashes those exact archive bytes, validates/extracts them through `ArtifactArchive`, and the fixture depends on that extracted package rather than the source checkout per D-02.
    - The runner validates the canonical CrossWake origin, a clean detached 40-character SHA equal to the ledger value, and loads real `crosswake` plus `crosswake_chimeway` contracts per D-03.
    - The committed host copies and applies package migration inputs to a fresh PostgreSQL database, invokes public trigger/planning/dispatch APIs, observes accepted provider handoff, and asserts event -> notification -> delivery -> target -> attempt plus `explain_delivery/2` per D-02 and D-09.
    - The host consumes one opaque intent once through CrossWake's real resolver/current RouteGate path; provider acceptance, protected activation, seen, and read remain distinct facts.
  </behavior>
  <action>Start with a failing `alpha_twin_runner_test.exs` proving the entire accepted path. Implement `Mix.Tasks.Verify.AlphaTwin` using the established parse/exit contract and `Chimeway.AlphaTwinProofRunner` in `scripts/prove-alpha-twin.exs`. The runner must execute `mix hex.build`, compute and constant-time validate the SHA-256 through `ArtifactArchive`, validate `origin` exactly as `https://github.com/szTheory/crosswake.git`, fetch the locked commit if absent, create a disposable detached worktree at `f2c502cdb1ce572a4a57257d9e3c051665704b90`, require `git status --porcelain` to be empty, and pass only the extracted Chimeway package root and detached CrossWake root into the committed Alpha host per D-02 and D-03. Copy migrations from the validated package into the fixture runtime scratch tree and run them through the fixture Repo before calling the public lifecycle. Keep the accepted tracer's token and one-time intent authority inside the fixture runner and pass only opaque tenant/binding/open references across the D-04 boundary; use the existing shipped transport behaviour with a bounded accepted response, leaving the reusable clock, registry, and ordered scripted-transport modules to dependent Plan 02. Seed `scenario-ledger.json` with a closed schema revision, canonical remote/full SHA, and the accepted-handoff/protected-open tracer ID per D-05. Emit one bounded canonical proof line only after the accepted durable explanation and CrossWake activation are verified per D-09. Do not invoke Pigeon, Apple tooling, a source-path Chimeway dependency, direct lifecycle-row inserts, the demo host, or the CrossWake example host.</action>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/alpha_twin_runner_test.exs --only alpha_twin_tracer --seed 0 --warnings-as-errors &amp;&amp; mix verify.alpha_twin</automated>
  </verify>
  <acceptance_criteria>The command exits 0 without Apple variables or Xcode; exactly one Chimeway archive digest and the exact CrossWake SHA are present in the closed proof; a fresh migrated database contains the tenant-qualified lifecycle spine and accepted attempt; `Chimeway.Traces.explain_delivery/2` returns safe target history; CrossWake resolves one authorized opaque open; a replay does not activate; and mutation tests fail when the fixture uses a Chimeway source path, skips migrations, changes the remote/SHA, dirties the checkout, inserts lifecycle rows directly, or omits the explanation assertion.</acceptance_criteria>
  <done>The canonical remote fetches the exact locked SHA, and the accepted-handoff tracer is production-quality, sleep-free, independently runnable through `mix verify.alpha_twin`, and exercises every phase layer once.</done>
</task>

</tasks>

## Artifacts this phase produces

- File `priv/alpha_twin/scenario-ledger.json`: fields `schema_version`, `crosswake_remote`, `crosswake_sha`, and ordered `scenario_ids`.
- Mix task `Mix.Tasks.Verify.AlphaTwin` and alias/command `mix verify.alpha_twin`.
- Script module `Chimeway.AlphaTwinProofRunner` in `scripts/prove-alpha-twin.exs`.
- Fixture `test/fixtures/alpha_twin/` with `AlphaTwin.Application`, `AlphaTwin.Runner`, and `AlphaTwin.ProofSummary`; Plan 02 adds the reusable deterministic seams.
- Fixture fields for opaque tenant, installation, binding revision, token fingerprint, intent reference, eligibility, scripted outcome, and fixed time; raw token values remain host-private.
- Tests `test/chimeway/alpha_twin_runner_test.exs` and `test/fixtures/alpha_twin/test/alpha_twin_test.exs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tree -> package fixture | Only validated immutable Chimeway archive bytes may enter the clean host. |
| Git remote -> detached CrossWake worktree | Only the canonical origin and locked full SHA may supply mobile contracts. |
| Host registry -> Chimeway/CrossWake | Raw tokens and authority remain host-private; only opaque closed values cross. |
| Chimeway adapter -> scripted transport | A real bounded request crosses, but observation must be redacted before capture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-102-01 | Tampering | package and CrossWake inputs | high | mitigate | Validate archive SHA-256, canonical origin, exact detached SHA, and clean worktree in `scripts/prove-alpha-twin.exs`; mutation-test every predicate. |
| T-102-02 | Information Disclosure | tracer host authority and transport observation | high | mitigate | Keep raw values inside the fixture runner's host-owned authority state, redact before observation, scan proof bytes, and fail on unknown fields/sentinels; Plan 02 extracts the same boundary into `AlphaTwin.Registry`. |
| T-102-03 | Elevation of Privilege | protected-open resolver | high | mitigate | Consume host intent once and require current CrossWake manifest/RouteGate authorization with replay-denial test. |
| T-102-04 | Denial of Service | scripted processes and DB ownership | medium | mitigate | Bound scenario/request sizes, use synchronous Sandbox ownership, await tasks, and enforce explicit command timeouts in CI later. |

## ASVS L1 Control-to-Verification Trace

| High Threat | ASVS L1 controls | Executable control verification |
|-------------|------------------|---------------------------------|
| T-102-01 | V5.1.1/V5.1.2 allowlisted structured-input validation; V6.2.1 approved digest use and fail-closed verification | `scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/alpha_twin_runner_test.exs --only alpha_twin_tracer --seed 0 --warnings-as-errors` mutates archive bytes, remote, full SHA, detached state, and worktree cleanliness and requires every substitution to fail. |
| T-102-02 | V5.2.1 sanitization of untrusted data; V5.3.1 safe output encoding; V4.1.1 least-privilege host boundary | The same focused suite injects nested token, identity, URL, payload, credential, and provider-body sentinels into fixture authority/transport/proof inputs and asserts rule/path-only failure with no sentinel in output. |
| T-102-03 | V2.2.1 anti-replay authentication controls; V3.7.1 session termination/revalidation; V4.1.2 deny-by-default and V4.2.1 record-level authorization | The tracer consumes one opaque intent through the real CrossWake resolver, then mutates session, tenant, binding revision, and RouteGate state and asserts denial plus zero second activation. |
</threat_model>

<verification>
Run `mix verify.alpha_twin` twice consecutively from clean state, then run the focused clock/registry/transport tests. Both runs must emit byte-stable proof apart from no fields at all: fixed time and immutable provenance make the complete proof deterministic.
</verification>

<success_criteria>
- The tracer proves a real packaged host -> Chimeway persistence/APNs -> CrossWake protected-open path.
- Production time defaults are unchanged; all twin time is explicit and sleep-free.
- Host authority and transport observations preserve the privacy boundary.
- The TWIN-01 unclassified probe and two descriptor-less prohibitions remain explicitly flagged and are not silently dropped.
</success_criteria>

<output>
Create `.planning/phases/102-alpha-digital-twin-hermetic-gate/102-01-SUMMARY.md` when done.
</output>
