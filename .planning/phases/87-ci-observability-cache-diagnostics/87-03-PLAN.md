---
phase: 87-ci-observability-cache-diagnostics
plan: 03
type: execute
wave: 3
depends_on: ["87-02"]
files_modified:
  - .planning/CI-PERF-BASELINE.md
  - test/chimeway/ci_observability_contract_test.exs
autonomous: false
requirements: [OBS-04]
must_haves:
  truths:
    - ".planning/CI-PERF-BASELINE.md exists, contains an `actions/runs/` run permalink, the four baseline facts (~373-395s main wall-clock, 373s install_golden, ~135s hidden compile in ecto.create, dead-flat compile across 3 identical-lock runs), and a delta-ledger table for later phases to append to."
    - "The contract test asserts the baseline doc exists, matches the `actions/runs/` permalink pattern, and contains the four baseline numbers."
    - statement: "On a live push-to-main run with instrumentation active, every build lane's job summary visibly renders the cache, recompile, and timing tables, and the baseline run link resolves to that run."
      verification: backstop
  artifacts:
    - .planning/CI-PERF-BASELINE.md
    - "test/chimeway/ci_observability_contract_test.exs (extended: OBS-04 baseline-doc assertion)"
  key_links:
    - "Baseline durability relies on GitHub Actions run pages persisting beyond log/artifact retention; committing the numbers in git makes them permanent regardless of run retention."
    - "The delta-ledger table is the anchor Phase 88 (CACHE-05) and every later phase append their after-rows to, proving wins by run-link delta."
---

<objective>
Record the pre-optimization baseline as the evidence of record: commit `.planning/CI-PERF-BASELINE.md` with the four already-measured baseline facts, a delta-ledger table, and a durable `actions/runs/` run permalink; assert it by contract test; and confirm via a live run that the instrumentation renders all three tables.

Purpose: OBS-04 makes every later milestone win provable by a before/after delta rather than a feeling. The baseline is ALREADY measured (do not re-derive numbers); this plan is about the recording mechanism + a durable run link + the human render confirmation.
Output: `.planning/CI-PERF-BASELINE.md` (committed, with run link), the OBS-04 contract assertion, and a human-verified live render.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-ci-observability-cache-diagnostics/87-RESEARCH.md
@.planning/phases/87-ci-observability-cache-diagnostics/87-PATTERNS.md
@.planning/phases/87-ci-observability-cache-diagnostics/87-VALIDATION.md
@.planning/CI-HARDENING-BACKLOG.md
</context>

## Artifacts this phase produces

- `.planning/CI-PERF-BASELINE.md` — the committed OBS-04 baseline doc (sibling to `.planning/CI-HARDENING-BACKLOG.md`). Contains a `**Recorded:**` / `**Commit:**` / `**Run:**` header, the four baseline facts, and a delta-ledger markdown table (`Metric | Baseline | Phase 88 after | Δ`).
- `test/chimeway/ci_observability_contract_test.exs` — extended with a `describe "OBS-04 baseline (baseline doc)"` block asserting the file exists, matches the `actions/runs/` permalink regex, and contains the four numbers.

<tasks>

<task type="auto">
  <name>Task 1: Create the baseline doc + OBS-04 contract assertion</name>
  <files>.planning/CI-PERF-BASELINE.md, test/chimeway/ci_observability_contract_test.exs</files>
  <read_first>
    - .planning/CI-HARDENING-BACKLOG.md (the sibling CI-ops doc format/tone to match)
    - .planning/phases/87-ci-observability-cache-diagnostics/87-RESEARCH.md (## Code Examples 4 — the baseline skeleton + delta-ledger table; ## Pattern 4)
    - .planning/ROADMAP.md (Phase 87 Success Criterion 4 + Key context line — the exact four baseline numbers)
    - test/chimeway/ci_observability_contract_test.exs (module to extend)
  </read_first>
  <action>
    Create `.planning/CI-PERF-BASELINE.md` from the Code Example 4 skeleton: a title `# CI Performance Baseline (v1.16, pre-optimization)`; a header line with `**Recorded:** 2026-07-28`, `**Commit:** <sha>` (the current HEAD sha), and `**Run:** <RUN_URL>`; then the delta-ledger table with columns `Metric | Baseline | Phase 88 after | Δ` and rows for the four baseline facts — `ci-gate wall-clock` = `~373-395s`, `install_golden job` = `373s`, `hidden compile in ecto.create` = `~135s`, `dep recompile, 3 identical-lock runs` = `dead-flat (cache never warms)`. Fill `<RUN_URL>` with the pre-optimization baseline `main` CI run permalink in the exact form `https://github.com/szTheory/chimeway/actions/runs/<run_id>` (append `/attempts/<n>` if known). Obtain the run id via `gh run list --workflow=CI --branch=main --event=push --status=success --limit=5` (pick the most recent pre-Phase-87 run). **Never commit a non-numeric placeholder** — a literal `<run_id>` has no digits and would fail this task's own `actions/runs/\d+` contract assertion. If `gh` is unavailable or no numeric run id can be resolved in this environment, do NOT commit a placeholder URL: escalate as a checkpoint (this plan is `autonomous: false`) so the maintainer supplies the real pre-optimization `main` run permalink. Task 1 is complete only once `.planning/CI-PERF-BASELINE.md` contains a real numeric `actions/runs/<digits>` permalink. Add a short "How later phases use this" note pointing at the delta-ledger.

    Extend `test/chimeway/ci_observability_contract_test.exs` with a `describe "OBS-04 baseline"` block: assert `File.exists?(".planning/CI-PERF-BASELINE.md")`; read it and assert it matches `~r{actions/runs/\d+}`; assert it contains each of the four baseline markers (`373`, `135`, `dead-flat`, and a wall-clock figure such as `395`). Keep assertions on the numbers, not on the exact prose.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/ci_observability_contract_test.exs && grep -Eq 'actions/runs/[0-9]+' .planning/CI-PERF-BASELINE.md && echo BASELINE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/CI-PERF-BASELINE.md` exists and `grep -Eq 'actions/runs/[0-9]+'` matches.
    - The file contains all four baseline facts (373s install_golden, ~135s hidden compile, ~373-395s wall-clock, dead-flat compile across 3 identical-lock runs) and a delta-ledger table with a `Phase 88 after` column.
    - `mix test test/chimeway/ci_observability_contract_test.exs` exits 0 with the new `OBS-04 baseline` describe block green.
  </acceptance_criteria>
  <done>The baseline doc is committed with the four facts, a delta ledger, and a durable `actions/runs/` run link, and the contract test asserts all of it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the live render + finalize the baseline run link</name>
  <what-built>All 14 build lanes in `ci.yml` now emit a cache table, a deps/app recompile line, and a per-step timing table to their job summary, and `.planning/CI-PERF-BASELINE.md` records the four baseline facts with a run permalink.</what-built>
  <how-to-verify>
    1. After this phase merges to `main` (or via `workflow_dispatch`), open the resulting CI run: `https://github.com/szTheory/chimeway/actions` -> the latest `CI` run.
    2. For a representative sample of lanes (at minimum `lint`, `test`, `verify_example`, `verify_admin`), open each job's Summary and confirm it shows: a **Cache** table (EXACT HIT / PARTIAL / MISS per cache), a **Recompile** row (deps + app counts; expect near-zero on a warm run), and a **Step timing** table (or a non-fatal "timing unavailable" row).
    3. Confirm the two `test` OTP legs (26, 27) each have their own summary with no clobbering.
    4. Open the `Run:` link in `.planning/CI-PERF-BASELINE.md` and confirm it resolves to a real baseline run. If Task 1 left the link pending, provide the correct baseline run URL now and have it written into the doc.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with the finalized baseline run URL), or describe which lane's summary did not render.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/CI-PERF-BASELINE.md` (committed, world-readable) | Contains only a run URL, commit sha, and aggregate timing numbers — no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-87-01 | Information Disclosure | committed baseline doc | low | mitigate | Doc records only a run permalink, commit sha, and aggregate durations — no env, secrets, or tokens. |
| T-87-03 | Repudiation | baseline provenance | low | accept | The `actions/runs/` permalink + commit sha make the baseline auditable; run-page durability is the citable evidence. Accepted: run pages may eventually age out, but the committed numbers are permanent. |
</threat_model>

<verification>
- `.planning/CI-PERF-BASELINE.md` exists, matches `actions/runs/\d+`, and carries the four facts + delta ledger.
- `mix test test/chimeway/ci_observability_contract_test.exs` green with the OBS-04 baseline assertions.
- Human checkpoint confirms every sampled lane's summary renders the three tables and the baseline link resolves.
</verification>

<success_criteria>
OBS-04 satisfied: the pre-optimization baseline is committed with a durable run link and a delta ledger, and a live run visibly proves the observability tables render — so Phase 88 and every later phase can cite a before/after delta.
</success_criteria>

<output>
Create `.planning/phases/87-ci-observability-cache-diagnostics/87-03-SUMMARY.md` when done.
</output>
