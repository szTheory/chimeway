---
phase: 87-ci-observability-cache-diagnostics
plan: 02
type: execute
wave: 2
depends_on: ["87-01"]
files_modified:
  - .github/workflows/ci.yml
  - test/chimeway/ci_observability_contract_test.exs
autonomous: true
requirements: [OBS-01, OBS-02, OBS-03]
must_haves:
  truths:
    - "Every one of the 14 build lanes gives each of its `actions/cache` steps a stable `id:` and carries a trailing observability summary step (if: always())."
    - "Instrumentation never gates or materially slows real verify work — the recompile probe is behavior-neutral (plain `mix compile`, no `--warnings-as-errors`) and each summary step is trailing `if: always()`, never a `needs:`/gate dependency."
    - "The recompile probe replaces the compile hidden inside `mix ecto.create` with an explicit `mix deps.compile` + `mix compile`, splitting deps vs. app counts, so `ecto.create` becomes a fast DB op."
    - "The install_golden_contract observability steps mirror the lane's `if: steps.detect.outputs.run == 'true'` guard (summary as `if: always() && steps.detect.outputs.run == 'true'`) so they never run when the lane body is skipped."
    - statement: "The `test` lane's two OTP matrix legs (26, 27) each write their own job summary without clobbering each other."
      verification: backstop
  artifacts:
    - ".github/workflows/ci.yml (all 14 build lanes instrumented: cache ids + recompile probe + trailing summary)"
    - "test/chimeway/ci_observability_contract_test.exs (extended: all-build-lanes id: + summary iteration)"
  key_links:
    - "verify_accrue's existing `mix deps.compile` step is replaced by the obs-recompile.sh step so counts reflect the ACCRUE_PATH path-dep graph under the job env."
    - "verify_sigra's probe runs after `mix deps.get` and before the `Prepare root test database` step, under CHIMEWAY_SKIP_ACCRUE_DEP/CHIMEWAY_SKIP_THREADLINE_DEP, so counts reflect the sigra graph."
    - "obs-summary.sh discovers each lane's caches generically from CACHE_<id>_HIT env, so adding a lane only requires wiring its cache `id:`s and env triples — no script change."
---

<objective>
Fan the proven Plan 01 observability pattern out to the remaining 13 build lanes so every build lane's job summary shows the cache, recompile, and timing tables. Extend the contract test to assert the wiring across ALL 14 build lanes.

Purpose: OBS-01/02/03 require coverage on EVERY build lane, not just the tracer lane. This plan does the mechanical (but detail-sensitive) fan-out, handling the special lanes (accrue, inbox, sigra, admin, install_golden_contract) correctly.
Output: `ci.yml` with all 14 build lanes instrumented; the contract test iterating `@build_lanes` to assert per-lane cache `id:`s and a trailing summary step.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-ci-observability-cache-diagnostics/87-RESEARCH.md
@.planning/phases/87-ci-observability-cache-diagnostics/87-PATTERNS.md
@.planning/phases/87-ci-observability-cache-diagnostics/87-01-SUMMARY.md
</context>

## Artifacts this phase produces

New cache-step `id:`s introduced by this plan in `ci.yml` (net-new symbols, excluded from source-drift verification):
- `cache_main` — the root `deps`/`_build` cache on every remaining build lane (verify_gates, verify_docs, test, verify_example, verify_runtime_prefix, verify_journeys, verify_mailglass, verify_accrue, verify_inbox, verify_threadline, verify_sigra, verify_admin, install_golden_contract).
- `cache_demo` — the `examples/chimeway_demo_host` cache on the 8 demo-host lanes (verify_example, verify_journeys, verify_mailglass, verify_accrue, verify_inbox, verify_threadline, verify_sigra, verify_admin).
- `cache_nested_inbox` — the `chimeway_inbox` cache on verify_inbox.
- `cache_nested_admin` — the `chimeway_admin` cache on verify_admin.
- Reuses the existing `playwright-cache` id on verify_admin (already present).
- Test module symbol: `@build_lanes` module attribute (the 14-lane list, mirroring `@ci_gate_lanes` in the release-gate analog) added to `Chimeway.CIObservabilityContractTest`.

<tasks>

<task type="auto">
  <name>Task 1: Instrument the single-cache DB lanes (verify_gates, verify_docs, test, verify_runtime_prefix)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (verify_gates lines 57-97; verify_docs lines 99-120; test matrix lines 122-165; verify_runtime_prefix lines 240-279)
    - scripts/ci/obs-recompile.sh, scripts/ci/obs-summary.sh (the env contract created in Plan 01)
    - .planning/phases/87-ci-observability-cache-diagnostics/87-01-SUMMARY.md (the exact lint-lane wiring shape to replicate)
  </read_first>
  <action>
    For each of `verify_gates`, `verify_docs`, `test`, and `verify_runtime_prefix`: add `id: cache_main` under the root `actions/cache` step's `uses:` line; insert a `- name: Compile (instrumented — deps then app)` step running `scripts/ci/obs-recompile.sh` (shell: bash) after the `mix deps.get` block — for the DB lanes place it immediately BEFORE `mix ecto.create --quiet` (so ecto.create becomes a fast DB op), and for `verify_docs` (no DB) place it after `mix deps.get` and before `mix ci.docs`; append a trailing `- name: CI observability summary` step (`if: always()`, shell: bash) with `GH_TOKEN`, `RUN_URL`, and the `CACHE_MAIN_HIT`/`MATCHED`/`PRIMARY` triple from `steps.cache_main.outputs.*`, running `scripts/ci/obs-summary.sh`. Do NOT add `--warnings-as-errors`. The `test` lane is a 2-leg OTP matrix (26, 27) — instrument the single lane definition once; each leg is a separate job that writes its own summary, so no extra work is needed to keep them from clobbering.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && python3 -c "import re,sys; y=open('.github/workflows/ci.yml').read()
import re
def block(j):
  m=re.search(j+r':(.*?)(?:\n  [a-z_]+:|\Z)', y, re.S); return m.group(1) if m else ''
lanes=['verify_gates','verify_docs','test','verify_runtime_prefix']
bad=[l for l in lanes if 'id: cache_main' not in block(l) or 'scripts/ci/obs-summary.sh' not in block(l) or 'scripts/ci/obs-recompile.sh' not in block(l)]
print('MISSING:',bad); sys.exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Each of verify_gates, verify_docs, test, verify_runtime_prefix contains `id: cache_main`, an `scripts/ci/obs-recompile.sh` step, and a trailing `scripts/ci/obs-summary.sh` step with `if: always()`.
    - In the DB lanes the obs-recompile step precedes `mix ecto.create --quiet`; in verify_docs it precedes `mix ci.docs`.
    - No `--warnings-as-errors` token is introduced.
    - `python3` block-scan above exits 0 (all four lanes wired).
  </acceptance_criteria>
  <done>The four single-cache lanes render all three observability tables; recompile probe sits ahead of ecto.create (or ci.docs) as a behavior-neutral explicit compile.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument the two-cache demo-host lanes (verify_example, verify_journeys, verify_mailglass, verify_threadline)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (verify_example lines 182-238 incl. the two cache steps + the "Warm :dev build" step; verify_journeys lines 281-338; verify_mailglass lines 340-387; verify_threadline lines 509-562)
    - scripts/ci/obs-summary.sh (generic CACHE_<id>_* discovery — each lane passes two triples)
  </read_first>
  <action>
    For each of `verify_example`, `verify_journeys`, `verify_mailglass`, `verify_threadline`: add `id: cache_main` to the root `deps`/`_build` cache step and `id: cache_demo` to the `examples/chimeway_demo_host` cache step; insert the `scripts/ci/obs-recompile.sh` step after `mix deps.get` and before `mix ecto.create --quiet`; append the trailing `if: always()` `scripts/ci/obs-summary.sh` step passing TWO env triples — `CACHE_MAIN_*` from `steps.cache_main.outputs.*` and `CACHE_DEMO_*` from `steps.cache_demo.outputs.*`. Leave the existing "Warm :dev build for JOUR-05" steps (verify_example, verify_journeys) untouched — they are unrelated real work. Do NOT add `--warnings-as-errors`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && python3 -c "import re,sys; y=open('.github/workflows/ci.yml').read()
def block(j):
  m=re.search(j+r':(.*?)(?:\n  [a-z_]+:|\Z)', y, re.S); return m.group(1) if m else ''
lanes=['verify_example','verify_journeys','verify_mailglass','verify_threadline']
bad=[l for l in lanes if not all(t in block(l) for t in ['id: cache_main','id: cache_demo','scripts/ci/obs-recompile.sh','scripts/ci/obs-summary.sh','CACHE_DEMO_HIT'])]
print('MISSING:',bad); sys.exit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four demo-host lanes contains `id: cache_main`, `id: cache_demo`, an obs-recompile step before `mix ecto.create --quiet`, and a trailing `if: always()` obs-summary step passing both `CACHE_MAIN_*` and `CACHE_DEMO_*` triples.
    - The existing "Warm :dev build for JOUR-05" steps remain unchanged in verify_example and verify_journeys.
    - `python3` block-scan above exits 0.
  </acceptance_criteria>
  <done>The four two-cache demo lanes each render a Cache table with two rows (main + demo), a recompile row, and a timing table.</done>
</task>

<task type="auto">
  <name>Task 3: Instrument the special lanes (accrue, inbox, sigra, admin, install_golden_contract) and extend the contract test to all 14 build lanes</name>
  <files>.github/workflows/ci.yml, test/chimeway/ci_observability_contract_test.exs</files>
  <read_first>
    - .github/workflows/ci.yml (verify_accrue lines 389-450 incl. its existing `mix deps.compile`; verify_inbox lines 452-507 with 3 caches; verify_sigra lines 564-635 with named DB step + sigra-proof.sh; verify_admin lines 637-704 with 4 caches incl. existing `id: playwright-cache`; install_golden_contract lines 706-766 with `if: steps.detect.outputs.run == 'true'` guards)
    - test/chimeway/ci_observability_contract_test.exs (the module + lint-lane assertions from Plan 01; `extract_ci_job_block/2`)
    - test/chimeway/release_gate_contract_test.exs (`@ci_gate_lanes` list at line 19; the `for {job_id, slug} <- @lanes do test` parametrization idiom at lines 402-418)
  </read_first>
  <action>
    Instrument the five special lanes:
    - `verify_accrue`: add `id: cache_main` + `id: cache_demo`; REPLACE the existing `- run: mix deps.compile` step (before ecto.create) with the `scripts/ci/obs-recompile.sh` step (it runs deps.compile + compile, capturing counts under ACCRUE_PATH); append the trailing summary step with both triples.
    - `verify_inbox`: add `id: cache_main`, `id: cache_nested_inbox` (the `chimeway_inbox` cache), and `id: cache_demo`; insert obs-recompile before `mix ecto.create --quiet`; append the trailing summary step passing THREE triples (`CACHE_MAIN_*`, `CACHE_NESTED_INBOX_*`, `CACHE_DEMO_*`).
    - `verify_sigra`: add `id: cache_main` + `id: cache_demo`; insert the obs-recompile step after `mix deps.get` and BEFORE the `Prepare root test database` step (this lane has no bare `mix ecto.create --quiet`); append the trailing summary step with both triples. The lane's `CHIMEWAY_SKIP_ACCRUE_DEP`/`CHIMEWAY_SKIP_THREADLINE_DEP` job env is inherited by obs-recompile so counts reflect the sigra graph. Do not touch the `scripts/ci/sigra-proof.sh` invocations.
    - `verify_admin`: add `id: cache_main`, `id: cache_nested_admin` (the `chimeway_admin` cache), keep the existing `id: playwright-cache`, and add `id: cache_demo`; insert obs-recompile before `mix ecto.create --quiet`; append the trailing summary step passing `CACHE_MAIN_*`, `CACHE_NESTED_ADMIN_*`, `CACHE_PLAYWRIGHT_*`, and `CACHE_DEMO_*` triples.
    - `install_golden_contract`: add `id: cache_main` to the conditional cache step (which already carries `if: steps.detect.outputs.run == 'true'`); insert the obs-recompile step before `mix ecto.create --quiet` guarded by the SAME `if: steps.detect.outputs.run == 'true'`; append the trailing summary step as `if: always() && steps.detect.outputs.run == 'true'` so it never runs when the lane body is skipped.
    Do NOT add `--warnings-as-errors` on any lane.

    Extend `test/chimeway/ci_observability_contract_test.exs`: add a `@build_lanes` module attribute listing all 14 lanes (mirror `@ci_gate_lanes`). Add a parametrized `for lane <- @build_lanes do test "#{lane} carries a cache id and a trailing obs-summary step"` that asserts `extract_ci_job_block(ci_yml, lane)` contains `id: cache_main`, `scripts/ci/obs-summary.sh`, and `if: always()`. Add a focused assertion that the `install_golden_contract` obs-summary step's `if:` also contains `steps.detect.outputs.run == 'true'`. Add an assertion that no build-lane obs step introduces `--warnings-as-errors`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/chimeway && mix test test/chimeway/ci_observability_contract_test.exs && echo ALL_LANES_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/chimeway/ci_observability_contract_test.exs` exits 0 with the `@build_lanes` parametrized tests present (14 lane assertions).
    - verify_accrue's former `mix deps.compile` step is now the obs-recompile step (counts captured under ACCRUE_PATH).
    - verify_inbox passes three cache triples; verify_admin passes four (incl. the pre-existing `playwright-cache`); verify_sigra's probe precedes `Prepare root test database`.
    - install_golden_contract's obs steps carry `steps.detect.outputs.run == 'true'` in their `if:` (summary as `if: always() && steps.detect.outputs.run == 'true'`).
    - `grep -c 'scripts/ci/obs-summary.sh' .github/workflows/ci.yml` returns at least 14 (one per build lane).
    - No new `--warnings-as-errors` token in `ci.yml`.
  </acceptance_criteria>
  <done>All 14 build lanes are instrumented and the contract test asserts every lane's cache `id:` and trailing summary step; the special-lane guards (sigra ordering, accrue deps.compile replacement, install_golden conditional) are correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| obs-summary.sh -> `$GITHUB_STEP_SUMMARY` (world-readable) | Same public-summary boundary as Plan 01, now across all 14 lanes incl. partner lanes carrying `*_PATH` env |
| partner path-dep checkouts (accrue/threadline/sigra) -> obs-recompile.sh | obs-recompile compiles the partner graph under the job env; must not surface partner secrets or paths beyond counts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-87-01 | Information Disclosure | obs-summary.sh across partner lanes | high | mitigate | Reuse Plan 01's whitelist-only emission; partner lanes add no new emitted field — still only cache keys/counts/durations/run-URL. Never echo `*_PATH`, `DATABASE_URL`, or `GH_TOKEN`. |
| T-87-02 | Tampering | first-party mix/cache/REST values in the summary | low | mitigate | Rendered as markdown table cells, never `eval`'d; values are first-party. |
</threat_model>

<verification>
- `mix test test/chimeway/ci_observability_contract_test.exs` green with all 14 `@build_lanes` assertions.
- `grep -c 'scripts/ci/obs-summary.sh' .github/workflows/ci.yml` >= 14.
- `grep -c 'id: cache_main' .github/workflows/ci.yml` >= 14 (lint from Plan 01 + 13 here).
- No `--warnings-as-errors` introduced anywhere in `ci.yml` by this phase.
- install_golden_contract obs steps carry the `steps.detect.outputs.run == 'true'` guard.
</verification>

<success_criteria>
Every build lane's job summary will render the cache, recompile, and timing tables (OBS-01/02/03 satisfied fleet-wide), with partner/conditional lane specifics handled correctly and asserted by contract test.
</success_criteria>

<output>
Create `.planning/phases/87-ci-observability-cache-diagnostics/87-02-SUMMARY.md` when done.
</output>
