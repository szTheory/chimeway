---
phase: 11-channel-adapter-safety-and-explainability-hardening
plan: "11-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/dispatch/channel_adapter_config.ex
  - lib/chimeway/dispatch/executor.ex
  - lib/chimeway/traces.ex
  - lib/chimeway/traces/explanation.ex
autonomous: true
requirements:
  - INTG-02
  - OPS-01
user_setup: []
must_haves:
  truths:
    - "`Chimeway.Dispatch.Executor.run_delivery/1` resolves adapter config without `String.to_atom/1` or interpolated atom creation from `delivery.channel`."
    - "Adapter config resolution uses `:channel_adapter_configs` string-keyed map first, then legacy `adapter_<channel>` lookup via `Application.get_all_env/1`, then `[]` default."
    - "`Chimeway.Traces.explain_delivery/1` returns explainability for custom string channels without `String.to_existing_atom/1` conversion failures."
    - "`Chimeway.Traces.Explanation` contract documents `channel` as string and remains aligned with persisted `chimeway_deliveries.channel` values."
    - "Phase 11 explicitly leaves Oban enqueue step-name atom creation in `lib/chimeway/dispatch/oban.ex` unchanged (deferred to Phase 12)."
  artifacts:
    - path: lib/chimeway/dispatch/channel_adapter_config.ex
      provides: "Atom-safe channel adapter config resolver with explicit compatibility ordering"
      exports:
        - "resolve/2"
    - path: lib/chimeway/dispatch/executor.ex
      provides: "Shared sync/Oban worker execution path using safe adapter config lookup"
      exports:
        - "run_delivery/1"
    - path: lib/chimeway/traces.ex
      provides: "Non-raising explainability response for persisted custom channels"
      exports:
        - "explain_delivery/1"
    - path: lib/chimeway/traces/explanation.ex
      provides: "String-based explainability channel contract for operator surfaces"
      exports:
        - "t/0"
  key_links:
    - from: lib/chimeway/dispatch/executor.ex
      to: lib/chimeway/dispatch/channel_adapter_config.ex
      via: "Executor delegates channel config lookup to atom-safe resolver before adapter call."
      pattern: "ChannelAdapterConfig\\.resolve\\("
    - from: lib/chimeway/dispatch/channel_adapter_config.ex
      to: config/runtime.exs
      via: "Resolver reads `:channel_adapter_configs` (preferred) and legacy `adapter_<channel>` env keys."
      pattern: "channel_adapter_configs|adapter_"
    - from: lib/chimeway/traces.ex
      to: lib/chimeway/traces/explanation.ex
      via: "Explainability assigns channel string directly and aligns with explanation type contract."
      pattern: "channel: delivery\\.channel"
---

<objective>
Harden the shared delivery executor and explainability surfaces so custom channel strings remain safe and traceable without runtime atom conversion.

Purpose: close the Phase 11 safety gap while preserving host-app adapter configurability and operator-facing reliability.
Output: atom-safe channel adapter config resolver, executor integration, and string-safe explainability contract.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-channel-adapter-safety-and-explainability-hardening/11-CONTEXT.md
@.planning/phases/11-channel-adapter-safety-and-explainability-hardening/11-RESEARCH.md
@.planning/phases/11-channel-adapter-safety-and-explainability-hardening/11-PATTERNS.md
@lib/chimeway/dispatch/executor.ex
@lib/chimeway/dispatch/sync.ex
@lib/chimeway/dispatch/oban_worker.ex
@lib/chimeway/traces.ex
@lib/chimeway/traces/explanation.ex
</context>

<tasks>

<task type="auto">
  <name>Task 11-01-01: Introduce atom-safe adapter config resolver and wire executor to it</name>
  <files>lib/chimeway/dispatch/channel_adapter_config.ex, lib/chimeway/dispatch/executor.ex</files>
  <read_first>lib/chimeway/dispatch/executor.ex, lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban_worker.ex, .planning/phases/11-channel-adapter-safety-and-explainability-hardening/11-CONTEXT.md, .planning/phases/11-channel-adapter-safety-and-explainability-hardening/11-RESEARCH.md</read_first>
  <action>Create a resolver module and replace runtime atom creation in executor:

1. Add `Chimeway.Dispatch.ChannelAdapterConfig` with:
```elixir
@spec resolve(String.t(), keyword()) :: keyword()
```
2. Implement deterministic lookup order with concrete config keys:
   - Preferred: `Application.get_env(:chimeway, :channel_adapter_configs, %{})` and lookup by exact channel string key (for example `"in_app"`, `"email"`, `"sms_custom"`).
   - Compatibility fallback: scan `Application.get_all_env(:chimeway)` for atom keys whose `Atom.to_string(key) == "adapter_#{channel}"`; return that value when present.
   - Final fallback: return the passed default (`[]` in executor callsite).
3. Update `Chimeway.Dispatch.Executor.run_delivery/1` to replace:
   - `String.to_atom("adapter_#{delivery.channel}")`
   - `Application.get_env(:chimeway, channel_key, [])`
   with:
```elixir
adapter_config = Chimeway.Dispatch.ChannelAdapterConfig.resolve(delivery.channel, [])
```
4. Preserve existing adapter module lookup (`:adapter`) and attempt classification behavior exactly.
5. Do not edit `lib/chimeway/dispatch/oban.ex` in this task (deferred D-05 remains untouched).</action>
  <verify>rg "defmodule Chimeway\\.Dispatch\\.ChannelAdapterConfig" lib/chimeway/dispatch/channel_adapter_config.ex && rg "@spec resolve\\(String\\.t\\(\\), keyword\\(\\)\\) :: keyword\\(\\)" lib/chimeway/dispatch/channel_adapter_config.ex && rg "channel_adapter_configs" lib/chimeway/dispatch/channel_adapter_config.ex && rg "Application\\.get_all_env\\(:chimeway\\)" lib/chimeway/dispatch/channel_adapter_config.ex && rg "ChannelAdapterConfig\\.resolve\\(delivery\\.channel, \\[\\]\\)" lib/chimeway/dispatch/executor.ex && ! rg "String\\.to_atom\\(\"adapter_" lib/chimeway/dispatch/executor.ex && mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_worker_test.exs</verify>
  <acceptance_criteria>
    - `executor.ex` contains no dynamic atom creation from `delivery.channel`
    - resolver reads `:channel_adapter_configs` first and returns a keyword list for valid string channel keys
    - legacy `adapter_<channel>` env keys are supported without creating new atoms
    - executor still uses existing adapter module selection and attempt recording flow
    - sync and Oban worker dispatch tests pass with resolver in shared execution seam
  </acceptance_criteria>
  <done>Shared dispatch execution no longer depends on runtime channel-to-atom conversion.</done>
</task>

<task type="auto">
  <name>Task 11-01-02: Make explainability channel contract string-safe</name>
  <files>lib/chimeway/traces.ex, lib/chimeway/traces/explanation.ex</files>
  <read_first>lib/chimeway/traces.ex, lib/chimeway/traces/explanation.ex, lib/chimeway/delivery.ex, .planning/phases/11-channel-adapter-safety-and-explainability-hardening/11-CONTEXT.md, .planning/phases/11-channel-adapter-safety-and-explainability-hardening/11-PATTERNS.md</read_first>
  <action>Remove atom casting in explainability and align contract types/docs:

1. In `Chimeway.Traces.explain_delivery/1`, assign explanation channel directly from persisted delivery:
```elixir
channel: delivery.channel
```
2. Remove any use of `String.to_existing_atom(delivery.channel)` from explainability path.
3. Update `Chimeway.Traces.Explanation` moduledoc field description to state `channel` is a string (examples: `"in_app"`, `"email"`, `"webhook_partner"`).
4. Update `@type t` in `traces/explanation.ex`:
```elixir
channel: String.t()
```
5. Keep timeline ordering and existing suppression metadata fields (`policy_checkpoint`, `delayed_fallback_source`) unchanged.</action>
  <verify>rg "channel: delivery\\.channel" lib/chimeway/traces.ex && ! rg "String\\.to_existing_atom\\(delivery\\.channel\\)" lib/chimeway/traces.ex && rg "channel: String\\.t\\(\\)" lib/chimeway/traces/explanation.ex && rg "delivery channel string" lib/chimeway/traces/explanation.ex && mix test test/chimeway/traces_test.exs</verify>
  <acceptance_criteria>
    - explainability no longer raises from custom channels missing pre-existing atoms
    - explanation struct contract documents `channel` as string
    - suppression and timeline explainability fields remain present and unchanged
    - traces test suite passes with updated contract
  </acceptance_criteria>
  <done>Operator explainability remains stable for valid custom channel strings.</done>
</task>

<task type="auto">
  <name>Task 11-01-03: Add focused custom-channel regression tests for safety and explainability</name>
  <files>test/chimeway/dispatch/sync_test.exs, test/chimeway/dispatch/oban_worker_test.exs, test/chimeway/traces_test.exs</files>
  <read_first>test/chimeway/dispatch/sync_test.exs, test/chimeway/dispatch/oban_worker_test.exs, test/chimeway/traces_test.exs, test/support/chimeway/dispatch_helpers.ex, lib/chimeway/dispatch/channel_adapter_config.ex, lib/chimeway/traces.ex</read_first>
  <action>Add deterministic regressions that prove Phase 11 behavior:

1. In sync and Oban worker tests, add custom-channel scenario using a concrete channel string:
   - channel value: `"sms_custom"`
   - configure preferred map:
```elixir
Application.put_env(:chimeway, :channel_adapter_configs, %{"sms_custom" => [provider: "acme_sms", timeout_ms: 1500]})
```
2. Define a test-local adapter module in each test file that sends `{:adapter_config, config}` to calling process and returns `{:ok, %{adapter: "capture"}}`; assert received config equals `[provider: "acme_sms", timeout_ms: 1500]`.
3. Add legacy compatibility test with explicit key:
```elixir
Application.put_env(:chimeway, :adapter_sms_custom, [provider: "legacy_sms"])
Application.delete_env(:chimeway, :channel_adapter_configs)
```
and assert adapter receives `[provider: "legacy_sms"]`.
4. In `traces_test.exs`, add `explain_delivery/1` case for planned delivery with channel `"webhook_partner"` and assert:
   - `{:ok, %Explanation{channel: "webhook_partner"}}`
   - no exception is raised during explainability call.
5. Add `INTG-02` and `OPS-01` tags/comments in new tests for grep-auditable requirement traceability.</action>
  <verify>rg "sms_custom" test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_worker_test.exs && rg "channel_adapter_configs" test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_worker_test.exs && rg "adapter_sms_custom" test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_worker_test.exs && rg "webhook_partner" test/chimeway/traces_test.exs && rg "INTG-02|OPS-01" test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_worker_test.exs test/chimeway/traces_test.exs && mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_worker_test.exs test/chimeway/traces_test.exs</verify>
  <acceptance_criteria>
    - custom-channel config lookup is covered for preferred map and legacy fallback paths
    - sync and Oban worker tests both assert adapter receives expected concrete config values
    - explainability test verifies custom string channel returns successfully
    - new tests include grep-visible requirement tags for INTG-02 and OPS-01
  </acceptance_criteria>
  <done>Phase 11 safety and explainability regressions are locked with custom-channel test evidence.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-11-01-RUNTIME-ATOM-EXHAUSTION
    severity: high
    threat: "Runtime channel strings can trigger unbounded atom creation in executor config lookup, allowing VM exhaustion and dispatch denial of service."
    mitigation: "Use `ChannelAdapterConfig.resolve/2` with string-map lookup + legacy key scan over existing env atoms; remove `String.to_atom(\"adapter_\")` path."
    mitigation_check: "rg 'ChannelAdapterConfig\\.resolve\\(|channel_adapter_configs' lib/chimeway/dispatch/executor.ex lib/chimeway/dispatch/channel_adapter_config.ex && ! rg 'String\\.to_atom\\(\"adapter_' lib/chimeway/dispatch/executor.ex"
  - id: TM-11-01-EXPLAINABILITY-RAISE
    severity: high
    threat: "Explainability endpoint raises on valid custom channel strings due to `String.to_existing_atom/1`, causing operator visibility loss during incident response."
    mitigation: "Treat `delivery.channel` as string in `Traces.explain_delivery/1` and in `Traces.Explanation` contract."
    mitigation_check: "rg 'channel: delivery\\.channel' lib/chimeway/traces.ex && ! rg 'String\\.to_existing_atom\\(delivery\\.channel\\)' lib/chimeway/traces.ex"
  - id: TM-11-01-CONFIG-COMPATIBILITY-REGRESSION
    severity: medium
    threat: "Changing adapter config lookup breaks host apps that still use legacy `:adapter_<channel>` env keys."
    mitigation: "Resolver includes explicit legacy fallback using `Application.get_all_env(:chimeway)` key-name scan before defaulting to `[]`."
    mitigation_check: "rg 'Application\\.get_all_env\\(:chimeway\\)|adapter_' lib/chimeway/dispatch/channel_adapter_config.ex"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix compile --warnings-as-errors`
- [ ] `mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_worker_test.exs test/chimeway/traces_test.exs`
- [ ] `mix test test/chimeway/dispatch/oban_test.exs`
- [ ] `rg "defmodule Chimeway\\.Dispatch\\.ChannelAdapterConfig" lib/chimeway/dispatch/channel_adapter_config.ex`
- [ ] `rg "channel_adapter_configs" lib/chimeway/dispatch/channel_adapter_config.ex`
- [ ] `rg "ChannelAdapterConfig\\.resolve\\(delivery\\.channel, \\[\\]\\)" lib/chimeway/dispatch/executor.ex`
- [ ] `! rg "String\\.to_atom\\(\"adapter_" lib/chimeway/dispatch/executor.ex`
- [ ] `! rg "String\\.to_existing_atom\\(delivery\\.channel\\)" lib/chimeway/traces.ex`
- [ ] `rg "channel: String\\.t\\(\\)" lib/chimeway/traces/explanation.ex`
- [ ] `rg "INTG-02|OPS-01" test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_worker_test.exs test/chimeway/traces_test.exs`
</verification>

<success_criteria>
- Executor adapter lookup no longer creates atoms from runtime channel strings.
- Explainability returns structured output for valid custom channels without conversion exceptions.
- Shared sync and Oban worker execution seam retains deterministic adapter behavior across custom channels.
- Regression tests provide explicit, grep-verifiable INTG-02 and OPS-01 evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/11-channel-adapter-safety-and-explainability-hardening/11-01-SUMMARY.md`.
</output>
