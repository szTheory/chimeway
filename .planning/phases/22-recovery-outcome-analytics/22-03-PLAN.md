---
phase: 22-recovery-outcome-analytics
plan: 03
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
files_modified:
  - lib/chimeway/traces.ex
  - test/chimeway/traces_test.exs
autonomous: true
requirements:
  - OPS-02
  - OPS-01
must_haves:
  truths:
    - "Operators can inspect recovery facts through `Chimeway.Traces`, including who re-drove a row and when."
    - "Operators can query aggregate outcomes by notification key, channel, and lifecycle result from durable delivery state."
    - "Aggregate lifecycle buckets distinguish `exhausted` from other cancelled outcomes and do not collapse delayed or digested rows into generic counts."
    - "Analytics surfaces remain payload-safe and stay within `Chimeway.Traces`."
  artifacts:
    - path: "lib/chimeway/traces.ex"
      provides: "Outcome aggregate query API over events, notifications, and deliveries"
      min_lines: 70
    - path: "test/chimeway/traces_test.exs"
      provides: "Automated proof of grouped counts across sent, suppressed, delayed, digested, failed, and exhausted buckets"
      min_lines: 90
  key_links:
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/delivery.ex"
      via: "aggregate outcome mapping derives from delivery status, orchestration state, and suppression reason"
      pattern: "succeeded|suppressed|deferred|digested|failed|retries_exhausted"
    - from: "lib/chimeway/traces.ex"
      to: "test/chimeway/traces_test.exs"
      via: "grouped Ecto query results are locked by exact bucket count assertions"
      pattern: "aggregate|outcome|notification_key|channel|exhausted"
---

<objective>
Finish Phase 22 by turning the durable lifecycle facts into recovery-aware operator traces and aggregate analytics.

Purpose: OPS-02 is the user-visible payoff for the recovery work, and Phase 22 success criterion 2 also requires that recovery remains explainable after the fact. The library needs grouped outcome queries that stay row-centric, plus trace surfaces that expose durable recovery facts without leaking payload or provider data.
Output: `Chimeway.Traces` aggregate query functions and recovery explainability updates plus tests proving accurate counts and payload-safe output.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recovery-outcome-analytics/22-CONTEXT.md
@.planning/phases/22-recovery-outcome-analytics/22-RESEARCH.md
@.planning/phases/22-recovery-outcome-analytics/22-VALIDATION.md
@.planning/phases/22-recovery-outcome-analytics/22-01-PLAN.md
@.planning/phases/22-recovery-outcome-analytics/22-02-PLAN.md
@lib/chimeway/traces.ex
@lib/chimeway/delivery.ex
@test/chimeway/traces_test.exs

<interfaces>
From `lib/chimeway/traces.ex`:
```elixir
def get_trace(String.t(), keyword()) :: {:ok, Event.t()} | {:error, :not_found}
def find_traces_for_recipient(String.t(), keyword()) :: [Notification.t()]
def find_traces_by_correlation_id(String.t(), keyword()) :: [Event.t()]
def explain_delivery(String.t(), keyword()) :: {:ok, Explanation.t()} | {:error, :not_found}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for grouped outcome analytics in `Chimeway.Traces`</name>
  <files>test/chimeway/traces_test.exs</files>
  <read_first>
    - test/chimeway/traces_test.exs
    - lib/chimeway/traces.ex
    - lib/chimeway/delivery.ex
    - .planning/phases/22-recovery-outcome-analytics/22-CONTEXT.md
    - .planning/phases/22-recovery-outcome-analytics/22-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `explain_delivery/2` exposes `recovery_source`, `recovery_reason`, and `recovered_at` after a delivery is re-driven.
    - Test 1: aggregate queries group counts by `notification_key`, `channel`, and lifecycle bucket.
    - Test 2: `sent`, `suppressed`, `delayed`, `digested`, `failed`, and `exhausted` are counted as distinct buckets.
    - Test 3: `exhausted` counts rows with `status == :cancelled and suppression_reason == "retries_exhausted"` while other cancelled reasons do not leak into that bucket.
    - Test 4: deferred rows count as `delayed` only when still pending and deferred, not after they resume or finalize.
    - Test 5: aggregate return values contain identifiers and counts only, without payload/provider data.
  </behavior>
  <action>Extend `test/chimeway/traces_test.exs` with Phase 22 recovery-and-analytics coverage. Seed events, notifications, and deliveries spanning succeeded, suppressed, pending+deferred, digested, failed, cancelled+retries_exhausted, cancelled+other reasons, and a recovered delivery whose metadata contains exact `recovery_source`, `recovery_reason`, and `recovered_at` facts. Require `Chimeway.Traces.explain_delivery/2` to surface those recovery facts and to add a clear recovery timeline event or equivalent structured fields. Require one or more new aggregate functions that can filter by `notification_key`, `channel`, and optional time window, and that return grouped counts with explicit lifecycle bucket names. Add assertions that generic cancelled rows do not count as `exhausted`, that delayed rows stop counting once they are resumed or completed, and that aggregate result maps remain limited to safe identifiers and integer counts.</action>
  <acceptance_criteria>
    - `test/chimeway/traces_test.exs` contains `recovery_source`
    - `test/chimeway/traces_test.exs` contains `recovery_reason`
    - `test/chimeway/traces_test.exs` contains `recovered_at`
    - `test/chimeway/traces_test.exs` contains `sent`
    - `test/chimeway/traces_test.exs` contains `suppressed`
    - `test/chimeway/traces_test.exs` contains `delayed`
    - `test/chimeway/traces_test.exs` contains `digested`
    - `test/chimeway/traces_test.exs` contains `failed`
    - `test/chimeway/traces_test.exs` contains `exhausted`
    - `test/chimeway/traces_test.exs` contains `notification_key`
    - `test/chimeway/traces_test.exs` contains `channel`
    - `mix test test/chimeway/traces_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/traces_test.exs --trace</automated>
  </verify>
  <done>The tests define the exact aggregate outcome contract required for Phase 22 operator analytics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement payload-safe aggregate outcome queries in `Chimeway.Traces`</name>
  <files>lib/chimeway/traces.ex</files>
  <read_first>
    - lib/chimeway/traces.ex
    - lib/chimeway/delivery.ex
    - test/chimeway/traces_test.exs
    - .planning/phases/22-recovery-outcome-analytics/22-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: recovery metadata is projected through `explain_delivery/2` as primary operator-facing trace data.
    - Test 1: aggregation uses grouped Ecto queries across events, notifications, and deliveries rather than per-row preload loops.
    - Test 2: lifecycle bucket mapping derives from durable delivery state and suppression reason only.
    - Test 3: the API supports filtering by notification key and channel without widening the payload surface.
  </behavior>
  <action>Implement Phase 22 recovery-aware analytics inside `lib/chimeway/traces.ex` by adding two capabilities. First, project durable recovery metadata through `explain_delivery/2`: read `recovery_source`, `recovery_reason`, and `recovered_at` from delivery metadata, expose them in the explanation struct or nested map, and add one recovery timeline event if that best matches the current trace posture. Second, add one or more public aggregate query functions, such as `aggregate_outcomes/1` and `aggregate_outcomes_for_notification/2`, that join `events`, `notifications`, and `deliveries`, group by `notification_key` and `channel`, and project counts into explicit lifecycle buckets. Use durable mappings from research: `sent -> :succeeded`, `suppressed -> :suppressed`, `delayed -> :pending + :deferred`, `digested -> :digested`, `failed -> :failed`, and `exhausted -> :cancelled + suppression_reason == \"retries_exhausted\"`. Keep both trace and aggregate result shapes limited to safe identifiers, timestamps, reasons, and counts, and do not preload payloads, rendered content, or provider responses.</action>
  <acceptance_criteria>
    - `lib/chimeway/traces.ex` contains `recovery_source`
    - `lib/chimeway/traces.ex` contains `recovery_reason`
    - `lib/chimeway/traces.ex` contains `recovered_at`
    - `lib/chimeway/traces.ex` contains `def aggregate_`
    - `lib/chimeway/traces.ex` contains `group_by`
    - `lib/chimeway/traces.ex` contains `notification_key`
    - `lib/chimeway/traces.ex` contains `channel`
    - `lib/chimeway/traces.ex` contains `retries_exhausted`
    - `lib/chimeway/traces.ex` contains `count(`
    - `mix test test/chimeway/traces_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/traces_test.exs --trace</automated>
  </verify>
  <done>Operators can now query grouped lifecycle outcomes from `Chimeway.Traces` without reading queue state or unsafe payload data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable delivery facts -> aggregate analytics | Counts must reflect canonical lifecycle state and not double-count through attempts |
| aggregate operator API -> support tooling | Summary surfaces must stay payload-safe and easy to explain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | I | `lib/chimeway/traces.ex` | mitigate | Return grouped identifiers and counts only; exclude payload, render data, and provider responses. |
| T-22-08 | T | lifecycle bucket mapping | mitigate | Derive buckets from explicit delivery status/orchestration/suppression facts and distinguish `retries_exhausted` from other cancelled reasons. |
| T-22-09 | D | aggregate query performance | mitigate | Use grouped joins rather than attempt-history loops or per-row preloads. |
</threat_model>

<verification>
Run `mix test test/chimeway/traces_test.exs --trace`.
</verification>

<success_criteria>
`Chimeway.Traces` can explain recovered deliveries and can report sent, suppressed, delayed, digested, failed, and exhausted counts by notification key and channel from durable delivery state alone.
</success_criteria>

<output>
After completion, create `.planning/phases/22-recovery-outcome-analytics/22-03-SUMMARY.md`
</output>
