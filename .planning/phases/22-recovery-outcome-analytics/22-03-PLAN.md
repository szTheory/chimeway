---
phase: 22-recovery-outcome-analytics
plan: 03
type: execute
wave: 3
depends_on:
  - 22-02
files_modified:
  - lib/chimeway/traces.ex
  - lib/chimeway/traces/outcome_summary.ex
  - test/chimeway/traces_test.exs
autonomous: true
requirements: [OPS-02]
must_haves:
  truths:
    - "Operators can query grouped outcome counts by notification key, channel, and outcome from `Chimeway.Traces`."
    - "Outcome buckets are derived from canonical delivery state and suppression reasons, not attempt history or Oban jobs."
    - "Deferred and exhausted rows appear distinctly as delayed and exhausted outcomes, while aggregate responses stay payload-safe."
  artifacts:
    - path: "lib/chimeway/traces.ex"
      provides: "Grouped outcome summary queries under the existing operator API"
    - path: "lib/chimeway/traces/outcome_summary.ex"
      provides: "Structured aggregate result type for key/channel/outcome counts"
    - path: "test/chimeway/traces_test.exs"
      provides: "Outcome-bucket regression coverage for sent, suppressed, delayed, digested, failed, exhausted, and other cancelled rows"
  key_links:
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/delivery.ex"
      via: "query maps canonical status, orchestration_state, and suppression_reason to operator outcome buckets"
      pattern: "suppression_reason|orchestration_state|group_by|count"
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/traces/outcome_summary.ex"
      via: "aggregate rows are returned as typed summary structs under the Traces namespace"
      pattern: "OutcomeSummary"
    - from: "test/chimeway/traces_test.exs"
      to: "lib/chimeway/traces.ex"
      via: "tests assert grouped counts and payload-safe response shape"
      pattern: "list_outcome_summaries|outcome: :exhausted|outcome: :delayed"
---

<objective>
Expose aggregate outcome analytics from the canonical lifecycle spine under `Chimeway.Traces`.

Purpose: implement D-03, D-04, and OPS-02 by adding grouped outcome queries to the existing operator surface, using durable delivery-row state instead of attempt history or queue state.
Output: `Chimeway.Traces` outcome summary APIs, a small nested result struct, and tests proving sent/suppressed/delayed/digested/failed/exhausted aggregation without payload leakage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-recovery-outcome-analytics/22-CONTEXT.md
@.planning/phases/22-recovery-outcome-analytics/22-RESEARCH.md
@.planning/phases/22-recovery-outcome-analytics/22-VALIDATION.md
@.planning/phases/20-digest-emission-explainability/20-CONTEXT.md
@.planning/phases/22-recovery-outcome-analytics/22-02-PLAN.md
@lib/chimeway/delivery.ex
@lib/chimeway/traces.ex
@lib/chimeway/traces/explanation.ex
@test/chimeway/traces_test.exs

<interfaces>
From lib/chimeway/traces.ex:
```elixir
@spec find_traces_for_recipient(String.t(), keyword()) :: [Notification.t()]
def find_traces_for_recipient(recipient_id, opts \\ []) do
  Repo.all(query, repo_opts)
end

@spec explain_delivery(String.t(), keyword()) :: {:ok, Explanation.t()} | {:error, :not_found}
def explain_delivery(delivery_id, opts \\ []) do
```

From lib/chimeway/traces/explanation.ex:
```elixir
defstruct [
  :delivery_id,
  :event_id,
  :correlation_id,
  :notification_key,
  :recipient_id,
  :channel,
  :status,
  :suppression_reason,
  :timeline
]
```

From lib/chimeway/delivery.ex:
```elixir
field(:status, Ecto.Enum,
  values: [:pending, :dispatched, :succeeded, :failed, :suppressed, :cancelled, :digested]
)
field(:orchestration_state, Ecto.Enum, values: [:ready, :deferred, :digest_held])
field(:suppression_reason, :string)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add grouped outcome summaries to `Chimeway.Traces`</name>
  <files>lib/chimeway/traces.ex, lib/chimeway/traces/outcome_summary.ex, test/chimeway/traces_test.exs</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/22-recovery-outcome-analytics/22-CONTEXT.md, .planning/phases/22-recovery-outcome-analytics/22-RESEARCH.md, .planning/phases/20-digest-emission-explainability/20-CONTEXT.md, lib/chimeway/delivery.ex, lib/chimeway/traces.ex, lib/chimeway/traces/explanation.ex, test/chimeway/traces_test.exs</read_first>
  <behavior>
    - Test 1: `list_outcome_summaries/1` returns `%Chimeway.Traces.OutcomeSummary{notification_key, channel, outcome, count}` rows grouped by notification key, channel, and derived outcome bucket.
    - Test 2: bucket mapping uses delivery-row truth only: `:succeeded -> :sent`, `:suppressed -> :suppressed`, `:pending + :deferred -> :delayed`, `:digested -> :digested`, `:failed -> :failed`, `:cancelled + suppression_reason == "retries_exhausted" -> :exhausted`, and other `:cancelled` rows stay distinct as `:cancelled`.
    - Test 3: responses do not expose payloads, render bodies, provider responses, or attempt rows; only grouped keys and counts are returned.
  </behavior>
  <action>Implement D-03 and D-04 in `Chimeway.Traces` with the exact public API `list_outcome_summaries(opts \\\\ [])`. Add `lib/chimeway/traces/outcome_summary.ex` defining `%Chimeway.Traces.OutcomeSummary{notification_key, channel, outcome, count}` and use it as the return type. In `lib/chimeway/traces.ex`, build one grouped Ecto query joining `Delivery`, `Notification`, and `Event`, with optional filters `notification_key:`, `channel:`, `since:`, and `until:` applied to delivery `inserted_at`, then derive the outcome bucket from canonical fields only using a SQL `CASE` or equivalent Ecto fragment over `status`, `orchestration_state`, and `suppression_reason`. Do not join attempts or consult Oban. Extend `test/chimeway/traces_test.exs` with exact cases named `test "list_outcome_summaries groups counts by key channel and outcome"` and `test "list_outcome_summaries splits exhausted from other cancelled rows"` that build rows for sent, suppressed, delayed, digested, failed, exhausted, and generic cancelled outcomes and assert the exact counts returned.</action>
  <acceptance_criteria>
    - `rg -n 'def list_outcome_summaries\\(opts \\\\ \\[\\]\\)' lib/chimeway/traces.ex` returns one match.
    - `rg -n 'defmodule Chimeway\\.Traces\\.OutcomeSummary do|defstruct \\[:notification_key, :channel, :outcome, :count\\]' lib/chimeway/traces/outcome_summary.ex` returns matches.
    - `rg -n 'retries_exhausted|orchestration_state|group_by|count\\(' lib/chimeway/traces.ex` returns the grouped bucket-mapping query.
    - `rg -n 'DeliveryAttempt|oban_jobs|provider_response|render_data' lib/chimeway/traces.ex lib/chimeway/traces/outcome_summary.ex` returns no matches in the new aggregate path.
    - `rg -n 'list_outcome_summaries groups counts by key channel and outcome|list_outcome_summaries splits exhausted from other cancelled rows' test/chimeway/traces_test.exs` returns both test names.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/traces_test.exs -x</automated>
  </verify>
  <done>`Chimeway.Traces` can return payload-safe grouped outcome counts by notification key, channel, and canonical lifecycle outcome, including explicit delayed and exhausted buckets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator analytics query -> trace context | Aggregate operator requests enter the shared `Chimeway.Traces` surface |
| trace query -> delivery/notification/event tables | Analytics joins business-truth tables to derive counts |
| aggregate response -> operator tooling | Summary data leaves the library for dashboards, IEx, or admin tooling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | I | `lib/chimeway/traces.ex` | mitigate | Return only grouped `notification_key`, `channel`, `outcome`, and `count`; do not join attempts or project payload/render/provider fields. |
| T-22-08 | T | `lib/chimeway/traces.ex` | mitigate | Derive buckets from canonical `status`, `orchestration_state`, and `suppression_reason` only, preserving the queue-is-not-truth rule from Phase 20. |
| T-22-09 | R | `test/chimeway/traces_test.exs` | mitigate | Add explicit regression tests for delayed and exhausted bucket mapping so future lifecycle changes cannot silently rewrite operator analytics semantics. |
</threat_model>

<verification>
- `mix test test/chimeway/traces_test.exs -x`
</verification>

<success_criteria>
- Operators can query grouped outcome counts from `Chimeway.Traces`.
- Analytics are derived from canonical delivery-row state, not attempt history or Oban.
- Delayed and exhausted outcomes are distinguishable in aggregate responses.
</success_criteria>

<output>
After completion, create `.planning/phases/22-recovery-outcome-analytics/22-03-SUMMARY.md`
</output>
