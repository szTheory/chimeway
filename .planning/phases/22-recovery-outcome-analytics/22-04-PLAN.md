---
phase: 22-recovery-outcome-analytics
plan: 04
type: execute
wave: 4
depends_on:
  - 22-02
  - 22-03
files_modified:
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/deliveries.ex
  - test/chimeway/orchestration/delivery_planning_test.exs
  - test/chimeway/orchestration/recovery_test.exs
autonomous: true
gap_closure: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "Ordinary notifier-less planning preserves the pre-recovery contract instead of fanning out across persisted render channels."
    - "Persisted `render_channels` fanout remains available only for explicit event recovery re-drive paths per D-01 and D-02."
    - "A dispatcher error during `recover_delivery/2` does not strand a `:pending` + `:ready` row; the row remains recoverable for retry."
    - "Phase 22 clears the validation gate with the targeted regressions fixed and the full `mix test` suite green."
  artifacts:
    - path: "lib/chimeway/delivery_planning.ex"
      provides: "Recovery-only persisted-channel fallback that does not widen ordinary planning semantics"
      min_lines: 20
    - path: "lib/chimeway/deliveries.ex"
      provides: "Recovery error compensation that preserves retryability after dispatcher handoff failure"
      min_lines: 20
    - path: "test/chimeway/orchestration/delivery_planning_test.exs"
      provides: "Regression coverage for ordinary notifier-less planning versus explicit recovery fanout"
      min_lines: 25
    - path: "test/chimeway/orchestration/recovery_test.exs"
      provides: "Regression coverage for recovery-only persisted-channel usage and retryable dispatcher failures"
      min_lines: 35
  key_links:
    - from: "lib/chimeway/delivery_planning.ex"
      to: "test/chimeway/orchestration/delivery_planning_test.exs"
      via: "an explicit recovery-only opt controls persisted channel fanout while ordinary notifier-less planning stays single-path"
      pattern: "use_persisted_channels|render_channels|in_app"
    - from: "lib/chimeway/deliveries.ex"
      to: "test/chimeway/orchestration/recovery_test.exs"
      via: "failed dispatcher handoff compensates the recovery claim so `list_recoverable_deliveries/1` can return the row again"
      pattern: "recover_delivery|recovered_at|list_recoverable_deliveries|{:error,"
---

<objective>
Close the Phase 22 verification gaps by restoring the intended planning contract, fixing the stranded-row recovery bug, and making the full-suite gate explicit.

Purpose: verification and review agree the missing work is no longer feature breadth. The remaining gap is correctness around recovery-only persisted-channel behavior plus retryability after dispatcher failure, followed by proving the whole suite is green.
Output: one recovery-only channel-resolution fix, one dispatcher-error compensation fix, regression tests that lock both behaviors separately, and a full-suite verification command that passes.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-recovery-outcome-analytics/22-CONTEXT.md
@.planning/phases/22-recovery-outcome-analytics/22-RESEARCH.md
@.planning/phases/22-recovery-outcome-analytics/22-PATTERNS.md
@.planning/phases/22-recovery-outcome-analytics/22-VERIFICATION.md
@.planning/phases/22-recovery-outcome-analytics/22-REVIEW.md
@.planning/phases/22-recovery-outcome-analytics/22-VALIDATION.md
@.planning/phases/22-recovery-outcome-analytics/22-02-PLAN.md
@lib/chimeway/delivery_planning.ex
@lib/chimeway/deliveries.ex
@test/chimeway/dispatch/oban_test.exs
@test/chimeway/dispatch/sync_test.exs
@test/chimeway/orchestration/delivery_planning_test.exs
@test/chimeway/orchestration/recovery_test.exs

<interfaces>
From `lib/chimeway/delivery_planning.ex`:
```elixir
@spec plan_notifications([Notification.t()], keyword()) ::
        {:ok, [Delivery.t()]} | {:error, term()}
def plan_notifications(notifications, opts \\ [])

@spec plan_notification(Notification.t(), keyword()) :: {:ok, [Delivery.t()]} | {:error, term()}
def plan_notification(%Notification{} = notification, opts \\ [])
```

From `lib/chimeway/deliveries.ex`:
```elixir
@spec list_recoverable_deliveries(keyword()) :: [Delivery.t()]
def list_recoverable_deliveries(opts \\ [])

@spec begin_recovery(binary() | Delivery.t(), keyword()) ::
        {:ok, Delivery.t()} | {:noop, Delivery.t()}
def begin_recovery(delivery_or_id, opts \\ [])

@spec recover_delivery(binary() | Delivery.t(), keyword()) ::
        {:ok, map()} | {:noop, map()} | {:error, term()}
def recover_delivery(delivery_or_id, opts \\ [])

@spec recover_event(binary() | Event.t(), keyword()) ::
        {:ok, map()} | {:noop, map()} | {:error, term()}
def recover_event(event_or_id, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for recovery-only persisted-channel fanout and retryable recovery failures</name>
  <files>test/chimeway/orchestration/delivery_planning_test.exs, test/chimeway/orchestration/recovery_test.exs</files>
  <read_first>
    - test/chimeway/orchestration/delivery_planning_test.exs
    - test/chimeway/orchestration/recovery_test.exs
    - test/chimeway/dispatch/oban_test.exs
    - test/chimeway/dispatch/sync_test.exs
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/deliveries.ex
    - .planning/phases/22-recovery-outcome-analytics/22-VERIFICATION.md
    - .planning/phases/22-recovery-outcome-analytics/22-REVIEW.md
    - .planning/phases/22-recovery-outcome-analytics/22-PATTERNS.md
    - .planning/phases/22-recovery-outcome-analytics/22-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: ordinary notifier-less planning keeps the prior non-recovery contract by planning only the default `in_app` path instead of expanding to every persisted `render_channels` entry.
    - Test 2: explicit event recovery still fans out across persisted `render_channels` when the planner is called with a recovery-only opt, per D-01 and D-02.
    - Test 3: when the recovery dispatcher returns `{:error, :boom}`, `recover_delivery/2` returns `{:error, :boom}`, clears any blocking recovery claim, and `list_recoverable_deliveries/1` still returns the row for retry.
    - Test 4: the existing Oban and sync dispatch expectations remain valid without rewriting their intended single-delivery contract.
  </behavior>
  <action>Extend `test/chimeway/orchestration/delivery_planning_test.exs` with a regression that calls `DeliveryPlanning.plan_notification/2` without `:notifier` and without any recovery opt on a notification whose persisted `render_channels` include `email`, `in_app`, and `sms_custom`; assert the result is a single `in_app` delivery and no `email` or `sms_custom` rows are planned. Extend `test/chimeway/orchestration/recovery_test.exs` in two ways using the existing recovery dispatcher stub and integration setup patterns from `22-PATTERNS.md`: first, make the recovery path assert that `recover_event/2` still reaches the planner with an explicit opt named `use_persisted_channels: true` and still yields the persisted multi-channel set; second, add a dispatcher-failure case returning `{:error, :boom}` and assert the delivery stays `:pending` + `:ready`, `metadata[\"recovered_at\"]` is absent after the failure, and `Deliveries.list_recoverable_deliveries/1` returns the same row on the next query. Keep the existing dispatch regression files green by using them as the contract reference rather than broadening their expectations.</action>
  <acceptance_criteria>
    - `test/chimeway/orchestration/delivery_planning_test.exs` contains `use_persisted_channels`
    - `test/chimeway/orchestration/delivery_planning_test.exs` contains `"sms_custom"`
    - `test/chimeway/orchestration/delivery_planning_test.exs` contains `"in_app"`
    - `test/chimeway/orchestration/recovery_test.exs` contains `{:error, :boom}`
    - `test/chimeway/orchestration/recovery_test.exs` contains `use_persisted_channels: true`
    - `test/chimeway/orchestration/recovery_test.exs` contains `list_recoverable_deliveries`
    - `test/chimeway/orchestration/recovery_test.exs` contains `recovered_at`
    - `mix test test/chimeway/orchestration/delivery_planning_test.exs test/chimeway/orchestration/recovery_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/sync_test.exs -x` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/orchestration/delivery_planning_test.exs test/chimeway/orchestration/recovery_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/sync_test.exs -x</automated>
  </verify>
  <done>The regression tests separately lock ordinary notifier-less planning, explicit recovery fanout, and retryable recovery failure behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restrict persisted-channel fallback to recovery and compensate failed recovery claims</name>
  <files>lib/chimeway/delivery_planning.ex, lib/chimeway/deliveries.ex</files>
  <read_first>
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/deliveries.ex
    - test/chimeway/orchestration/delivery_planning_test.exs
    - test/chimeway/orchestration/recovery_test.exs
    - test/chimeway/dispatch/oban_test.exs
    - test/chimeway/dispatch/sync_test.exs
    - .planning/phases/22-recovery-outcome-analytics/22-REVIEW.md
    - .planning/phases/22-recovery-outcome-analytics/22-PATTERNS.md
    - .planning/phases/22-recovery-outcome-analytics/22-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `resolve_channels/2` uses persisted `notification.render_channels` only when the planner receives the explicit recovery opt, not for general notifier-less planning.
    - Test 2: `recover_event/2` passes that explicit recovery opt through the dispatcher so D-01 recovery fanout still works with notifier-less persisted notifications.
    - Test 3: `recover_delivery/2` compensates `begin_recovery/2` when `dispatch_delivery/2` returns `{:error, reason}` so the row remains recoverable.
    - Test 4: the phase closes with `mix test` green, satisfying the verification gap and validation gate.
  </behavior>
  <action>Update `lib/chimeway/delivery_planning.ex` so `resolve_channels/2` branches in three explicit modes: notifier callback path first, persisted-channel path only when `Keyword.get(opts, :use_persisted_channels, false) == true`, and otherwise the pre-Phase-22 default `in_app` path for general notifier-less planning. Keep persisted channel normalization stable, but do not let ordinary planner calls widen fanout or force non-`in_app` render validation. Update `lib/chimeway/deliveries.ex` so `recover_event/2` passes `use_persisted_channels: true` inside the dispatcher opts used for re-drive planning. In the same file, add a named compensation helper that follows the same guarded `Repo.update_all` row-mutation style highlighted in `22-PATTERNS.md` and mirrors the existing `resume_deferred_delivery/2` / `recovery_delivery_result/4` posture: when `dispatch_delivery/2` returns `{:error, reason}`, remove `recovery_source`, `recovery_reason`, and `recovered_at` from the canonical row, reload it, and return the original error without changing the row to a non-recoverable state or changing the tagged recovery result shape on success/noop paths. After the code changes, run the targeted regression command and then `mix test`; do not close the plan unless the full suite exits 0 per the verification gap.</action>
  <acceptance_criteria>
    - `lib/chimeway/delivery_planning.ex` contains `use_persisted_channels`
    - `lib/chimeway/delivery_planning.ex` contains `[:in_app]`
    - `lib/chimeway/deliveries.ex` contains `use_persisted_channels: true`
    - `lib/chimeway/deliveries.ex` contains `dispatch_delivery`
    - `lib/chimeway/deliveries.ex` contains `{:error, reason_term}`
    - `lib/chimeway/deliveries.ex` contains `recovery_source`
    - `lib/chimeway/deliveries.ex` contains `recovery_reason`
    - `lib/chimeway/deliveries.ex` contains `recovered_at`
    - `mix test test/chimeway/orchestration/delivery_planning_test.exs test/chimeway/orchestration/recovery_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/sync_test.exs -x` exits 0
    - `mix test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>Persisted render-channel fallback is recovery-only, failed recovery handoffs remain retryable, and the full project suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ordinary planner callers -> persisted rendering declarations | Non-recovery callers must not gain broader fanout or stricter render requirements implicitly |
| recovery claim -> dispatcher handoff | A failed enqueue/send handoff must not consume the operator's only recovery attempt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | T | `lib/chimeway/delivery_planning.ex` | mitigate | Gate persisted-channel fanout behind an explicit recovery-only opt so ordinary planner semantics cannot be widened accidentally. |
| T-22-11 | D | `lib/chimeway/deliveries.ex` | mitigate | Compensate failed recovery claims so transient dispatcher errors do not strand ready rows outside future recovery scans. |
| T-22-12 | R | recovery metadata on canonical rows | mitigate | Preserve recovery facts only after successful handoff; remove failed claims so traces do not falsely imply that recovery actually occurred. |
</threat_model>

<verification>
Run `mix test test/chimeway/orchestration/delivery_planning_test.exs test/chimeway/orchestration/recovery_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/dispatch/sync_test.exs -x` and then `mix test`.
</verification>

<success_criteria>
Phase 22 no longer widens ordinary planning semantics, recovery retries remain possible after dispatcher failure, and the project-wide verification gate passes with a green full suite.
</success_criteria>

<output>
After completion, create `.planning/phases/22-recovery-outcome-analytics/22-04-SUMMARY.md`
</output>
