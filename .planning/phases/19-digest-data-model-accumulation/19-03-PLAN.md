---
phase: 19-digest-data-model-accumulation
plan: 03
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - lib/chimeway/notifier.ex
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/policy.ex
  - test/chimeway/orchestration/planning_declarations_test.exs
  - test/chimeway/orchestration/delivery_planning_test.exs
autonomous: true
requirements:
  - DIGEST-01
must_haves:
  truths:
    - "Planner-triggered digest accumulation only occurs after the canonical delivery row remains pending and `:digest_held`."
    - "Teams can declare digest participation with an optional explicit digest key while preserving the normalized persisted mode `:digest_held`."
    - "Repeated planning calls keep one delivery row and one digest membership for the same held source work item."
  artifacts:
    - path: "lib/chimeway/notifier.ex"
      provides: "Normalized orchestration declaration support for optional explicit digest keys"
      min_lines: 30
    - path: "lib/chimeway/delivery_planning.ex"
      provides: "Planning hook that invokes accumulation only after policy-safe held-delivery outcomes"
      min_lines: 60
    - path: "test/chimeway/orchestration/delivery_planning_test.exs"
      provides: "Automated proof that repeated planning is row-idempotent and membership-idempotent"
      min_lines: 80
  key_links:
    - from: "lib/chimeway/notifier.ex"
      to: "lib/chimeway/delivery_planning.ex"
      via: "normalized orchestration data carries `digest_key` while still mapping persisted mode to `:digest_held`"
      pattern: "digest_key|digest_held|source"
    - from: "lib/chimeway/delivery_planning.ex"
      to: "lib/chimeway/digests/accumulation.ex"
      via: "planner calls accumulation only after policy leaves the canonical delivery pending and held"
      pattern: "pending|digest_held|accumulate_delivery"
---

<objective>
Wire digest accumulation into the existing planning choke point without breaking the canonical delivery-row contract or widening scope into digest emission.

Purpose: Phase 19 is complete only when the planner can resolve digest declarations into durable rule matches and accumulate held source deliveries idempotently at the point where orchestration and policy have already settled.
Output: notifier normalization for optional digest keys, planner integration that invokes accumulation after safe held outcomes, and orchestration tests that prove one-row and one-membership idempotency.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-digest-data-model-accumulation/19-CONTEXT.md
@.planning/phases/19-digest-data-model-accumulation/19-RESEARCH.md
@.planning/phases/19-digest-data-model-accumulation/19-PATTERNS.md
@.planning/phases/19-digest-data-model-accumulation/19-VALIDATION.md
@.planning/phases/17-delivery-windows-deferral-semantics/17-02-SUMMARY.md
@.planning/phases/18-scheduled-resume-deferred-dispatch/18-03-SUMMARY.md
@lib/chimeway/notifier.ex
@lib/chimeway/delivery_planning.ex
@lib/chimeway/policy.ex
@lib/chimeway/digests.ex
@lib/chimeway/digests/accumulation.ex
@test/chimeway/orchestration/planning_declarations_test.exs
@test/chimeway/orchestration/delivery_planning_test.exs

<interfaces>
From `lib/chimeway/notifier.ex`:
```elixir
@callback orchestration(map(), map()) ::
            {:ok, :immediate | :digest | :digest_held | keyword(atom()) | map()}
@type orchestration_mode :: :immediate | :digest_held
def resolve_orchestration(notifier, trigger_params, recipient, override \\ :unset)
```

From `lib/chimeway/delivery_planning.ex`:
```elixir
def plan_notification(%Notification{} = notification, opts \\ [])
with {:ok, delivery} <- Deliveries.plan_delivery(notification.id, channel, ...),
     {:ok, orchestration} <- resolve_orchestration(opts, trigger_params, recipient),
     {:ok, delivery} <- apply_declared_orchestration(delivery, channel, orchestration) do
  evaluate_planning_policy(delivery, opts)
end
```

From `lib/chimeway/policy.ex`:
```elixir
def evaluate(%Delivery{} = delivery, opts \\ []) do
  context = %{notification_key: Map.get(delivery.metadata || %{}, "notification_key"), category: delivery_category(delivery)}
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock planner-side digest accumulation behavior with RED orchestration tests</name>
  <files>test/chimeway/orchestration/planning_declarations_test.exs, test/chimeway/orchestration/delivery_planning_test.exs</files>
  <read_first>
    - test/chimeway/orchestration/planning_declarations_test.exs
    - test/chimeway/orchestration/delivery_planning_test.exs
    - lib/chimeway/notifier.ex
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/policy.ex
    - .planning/phases/19-digest-data-model-accumulation/19-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: notifier orchestration declarations can supply `{:digest, digest_key: "thread:123"}` and still persist `orchestration_state: :digest_held`.
    - Test 2: repeated planning for the same notification/channel creates one canonical delivery row and one digest membership.
    - Test 3: category grouping uses the resolved event payload category string at planning time.
    - Test 4: suppressed or immediate deliveries do not create digest memberships.
  </behavior>
  <action>Extend `test/chimeway/orchestration/planning_declarations_test.exs` and `test/chimeway/orchestration/delivery_planning_test.exs` per D-02, D-06, D-07, D-09, D-12, and D-15. Add a notifier fixture that returns `{:ok, [email: {:digest, [digest_key: "thread:123"]}]}` and assert the persisted delivery still uses `orchestration_state: :digest_held` with `planning_context["digest_key"] == "thread:123"`. Add planning tests that seed a matching digest rule, plan the same notification twice, and assert exactly one `chimeway_deliveries` row plus one `chimeway_digest_memberships` row exist. Add negative cases proving `:ready` and suppressed deliveries do not create membership rows. Do not add emission, flush jobs, or inclusion/exclusion explanation assertions from Phase 20 per D-12.</action>
  <acceptance_criteria>
    - `test/chimeway/orchestration/planning_declarations_test.exs` contains `digest_key`
    - `test/chimeway/orchestration/delivery_planning_test.exs` contains `chimeway_digest_memberships`
    - `test/chimeway/orchestration/delivery_planning_test.exs` contains `orchestration_state: :digest_held`
    - `test/chimeway/orchestration/delivery_planning_test.exs` contains `member_count`
    - `mix test test/chimeway/orchestration/planning_declarations_test.exs test/chimeway/orchestration/delivery_planning_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/orchestration/planning_declarations_test.exs test/chimeway/orchestration/delivery_planning_test.exs --trace</automated>
  </verify>
  <done>The orchestration tests define exactly when planner-side accumulation should run, how explicit digest keys are declared, and how repeated planning remains idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize digest-key declarations and invoke accumulation only after held planning outcomes</name>
  <files>lib/chimeway/notifier.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/policy.ex</files>
  <read_first>
    - lib/chimeway/notifier.ex
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/policy.ex
    - lib/chimeway/digests.ex
    - lib/chimeway/digests/accumulation.ex
    - .planning/phases/19-digest-data-model-accumulation/19-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: normalized orchestration preserves the existing persisted mode contract while exposing optional `digest_key` metadata.
    - Test 2: the planner calls accumulation only after policy returns a delivery that is still pending and held.
    - Test 3: category grouping reads the same category resolution path policy uses today, avoiding late re-derivation drift.
  </behavior>
  <action>Update `lib/chimeway/notifier.ex`, `lib/chimeway/delivery_planning.ex`, and `lib/chimeway/policy.ex` per D-02, D-06, D-07, D-09, D-13, D-15, and D-16. In `Chimeway.Notifier`, extend orchestration normalization so channel entries may be `:digest`, `:digest_held`, or `{:digest, [digest_key: "..."]}`, but normalize the persisted mode to `:digest_held` and keep the optional `digest_key` string in the resolved orchestration data. In `Chimeway.Policy`, expose the existing category derivation as a reusable public helper named `delivery_category/1` so accumulation can snapshot the same category value policy checks. In `Chimeway.DeliveryPlanning.plan_notification/2`, after `evaluate_planning_policy/2`, call `Chimeway.Digests.Accumulation.accumulate_delivery/2` only when the returned delivery still has `status == :pending` and `orchestration_state == :digest_held`; pass lookup attrs containing `recipient_id`, `channel`, `notification_key`, `notification_version`, `category`, and `digest_key`. If no matching rule exists, leave the delivery row unchanged and return it without creating buckets or memberships. Keep digest windows separate from deferred `next_eligible_at` and do not add Phase 20 emission work.</action>
  <acceptance_criteria>
    - `lib/chimeway/notifier.ex` contains `digest_key`
    - `lib/chimeway/notifier.ex` contains `:digest_held`
    - `lib/chimeway/policy.ex` contains `def delivery_category`
    - `lib/chimeway/delivery_planning.ex` contains `Chimeway.Digests.Accumulation.accumulate_delivery`
    - `lib/chimeway/delivery_planning.ex` contains `status == :pending`
    - `lib/chimeway/delivery_planning.ex` contains `orchestration_state == :digest_held`
    - `mix test test/chimeway/orchestration/planning_declarations_test.exs test/chimeway/orchestration/delivery_planning_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/orchestration/planning_declarations_test.exs test/chimeway/orchestration/delivery_planning_test.exs --trace</automated>
  </verify>
  <done>The planner now resolves digest declarations into rule-aware accumulation at the correct durable choke point while preserving the canonical delivery-row contract and idempotency boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notifier/planner orchestration input -> normalized digest declarations | Optional digest-key inputs are untrusted and must not bypass the normalized `:digest_held` contract |
| policy/planning results -> accumulation entry point | Accumulation must run only after the canonical delivery row has survived orchestration and policy checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | S | `lib/chimeway/notifier.ex` | mitigate | Normalize digest declarations into the existing `:digest_held` mode and require `digest_key` to be a string, preventing arbitrary planner state injection. |
| T-19-08 | T | `lib/chimeway/delivery_planning.ex` | mitigate | Invoke accumulation only after policy returns a delivery still marked `status: :pending` and `orchestration_state: :digest_held`, so immediate or suppressed rows cannot be inserted into buckets. |
| T-19-09 | R | planner -> digest tables | mitigate | Reuse canonical delivery IDs, persisted planning context, and bucket snapshots so repeated planning remains one row plus one membership with an auditable explanation path. |
</threat_model>

<verification>
Run `mix test test/chimeway/orchestration/planning_declarations_test.exs test/chimeway/orchestration/delivery_planning_test.exs --trace`.
</verification>

<success_criteria>
Planning now turns digest-held delivery outcomes into durable accumulation records at the correct point in the lifecycle, with support for recipient-scoped grouping by notification key, category, or explicit digest key.
</success_criteria>

<output>
After completion, create `.planning/phases/19-digest-data-model-accumulation/19-03-SUMMARY.md`
</output>
