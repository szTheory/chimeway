---
phase: 73-storage-prefix-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 73-01
files_modified:
  - lib/chimeway/application.ex
  - config/config.exs
  - test/chimeway/application_validation_test.exs
autonomous: true
requirements:
  - PFX-01
  - PFX-02
must_haves:
  truths:
    - "PFX-02: invalid prefix config fails before Chimeway.Repo or Oban children start per D-06"
    - "PFX-01: this repository's current public-schema runtime is explicit via prefix: false, not hidden missing config per D-03/D-04"
  artifacts:
    - path: "lib/chimeway/application.ex"
      provides: "early storage-prefix validation before child construction"
      exports: ["Chimeway.Application.start/2"]
    - path: "config/config.exs"
      provides: "explicit current-repo public-schema legacy config"
      contains: "prefix: false"
    - path: "test/chimeway/application_validation_test.exs"
      provides: "boot validation tests for prefix config"
  key_links:
    - from: "lib/chimeway/application.ex"
      to: "lib/chimeway/storage.ex"
      via: "start/2 calls Chimeway.Storage.validate_prefix!/0 before children"
      pattern: "Chimeway\\.Storage\\.validate_prefix!\\(\\)"
    - from: "config/config.exs"
      to: "lib/chimeway/storage.ex"
      via: "explicit app env value consumed by validate_prefix!/0"
      pattern: "prefix:\\s+false"
---

<objective>
Wire the storage-prefix contract into application boot and make the repository's current public-schema behavior explicit.

Purpose: PFX-01/PFX-02 require a strict runtime config contract that fails early for invalid input. This plan implements D-03, D-06, D-09, D-10, D-16, and D-17 after Plan 73-01 creates `Chimeway.Storage`.

Output: early boot validation before Repo/Oban child startup, explicit `prefix: false` in current project config, and application validation tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/73-storage-prefix-contract/73-CONTEXT.md
@.planning/phases/73-storage-prefix-contract/73-RESEARCH.md
@.planning/phases/73-storage-prefix-contract/73-VALIDATION.md
@.planning/phases/73-storage-prefix-contract/73-PATTERNS.md
@lib/chimeway/application.ex
@lib/chimeway/storage.ex
@config/config.exs
@config/test.exs
@test/chimeway/application_validation_test.exs
</context>

<must_haves>
- `Chimeway.Application.start/2` validates storage prefix config before building the supervisor child list.
- The boot validation path raises `Chimeway.ConfigError` for invalid/missing prefix config.
- `config/config.exs` explicitly configures this repository's current public-schema mode with `prefix: false`.
- Boot validation does not start Repo, Oban, or check database schema existence before reporting invalid config.
</must_haves>

## Artifacts This Phase Produces

- Module: `Chimeway.ConfigError` from Plan 73-01
- Exception fields: `:message`, `:type`, `:key`, `:value`
- Module: `Chimeway.Storage` from Plan 73-01
- Function: `Chimeway.Storage.validate_prefix!/0`
- Function: `Chimeway.Storage.repo_opts/1`
- Boot integration: `Chimeway.Application.start/2` calls storage validation before Repo/Oban children
- Config key: `config :chimeway, prefix: false` in this repository's current config
- Test file: `test/chimeway/application_validation_test.exs`
- Docs and legacy contract updates in Plan 73-03

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configure explicit public mode and validate storage prefix before children start</name>
  <read_first>
  - .planning/phases/73-storage-prefix-contract/73-CONTEXT.md
  - .planning/phases/73-storage-prefix-contract/73-PATTERNS.md
  - config/config.exs
  - lib/chimeway/application.ex
  - lib/chimeway/storage.ex
  - lib/chimeway/config_error.ex
  - test/chimeway/application_validation_test.exs
  </read_first>
  <files>config/config.exs, lib/chimeway/application.ex, test/chimeway/application_validation_test.exs</files>
  <behavior>
  - Default repository/test config has an explicit `config :chimeway, prefix: false` value for the current public-schema behavior per D-03/D-04 before boot validation is wired.
  - `Chimeway.Application.start/2` raises `Chimeway.ConfigError` for invalid or missing `:prefix` before child construction per D-06.
  - Missing-config coverage explicitly deletes the `:prefix` application env key inside the test and restores the prior value in `on_exit` per D-02/D-03.
  - Existing `validate_channel_render_modules!/0` behavior remains intact.
  - Boot validation does not query Postgres schema existence per D-09.
  </behavior>
  <action>First add `prefix: false` to the existing top-level `config :chimeway` block in `config/config.exs` so this repository's current public-schema legacy runtime is explicit before any verify command starts the application per D-03/D-04. Then extend `test/chimeway/application_validation_test.exs` with `async: false` tests that snapshot and restore `Application.get_env(:chimeway, :prefix)`, set invalid prefix values, explicitly call `Application.delete_env(:chimeway, :prefix)` for the missing-config case, and assert `Chimeway.Application.start(:normal, [])` raises `Chimeway.ConfigError` with `type: :invalid_prefix`, `key: :prefix`, and the rejected value. Finally update `lib/chimeway/application.ex` so `start/2` calls `Chimeway.Storage.validate_prefix!/0` before the `children =` assignment and before the existing Repo/Oban child list is built. Preserve `validate_channel_render_modules!/0`; do not replace it. Do not add a default argument to application env reads, schema-existence validation, Oban config changes, or compile-time config mechanisms per D-09/D-10/D-16/D-22.</action>
  <acceptance_criteria>
  - `config/config.exs` contains `prefix: false` in the `config :chimeway` application config before boot validation tests run.
  - `start/2` invokes `Chimeway.Storage.validate_prefix!/0` before constructing the child list.
  - Application validation tests prove invalid prefix config fails through the boot path with `Chimeway.ConfigError`.
  - Application validation tests prove missing prefix config by deleting and restoring the `:prefix` key explicitly.
  - Existing channel render module validation tests remain green.
  - No database query or schema existence check is introduced in `lib/chimeway/application.ex`.
  </acceptance_criteria>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/application_validation_test.exs --warnings-as-errors</automated>
    <automated>bash -lc 'if rg -n "Ecto.Adapters.SQL|information_schema|to_regclass|Repo\\." lib/chimeway/application.ex; then exit 1; fi'</automated>
  </verify>
  <done>Invalid storage-prefix config fails at application boot before Repo/Oban children can start per PFX-02 and D-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert explicit public-schema config remains accepted</name>
  <read_first>
  - .planning/phases/73-storage-prefix-contract/73-CONTEXT.md
  - .planning/phases/73-storage-prefix-contract/73-RESEARCH.md
  - config/config.exs
  - config/test.exs
  - lib/chimeway/storage.ex
  - test/chimeway/application_validation_test.exs
  </read_first>
  <files>test/chimeway/application_validation_test.exs</files>
  <behavior>
  - `Chimeway.Storage.validate_prefix!/0` returns `false` under the repository's normal test config.
  - Missing config remains invalid when tests delete the env key and restore it afterward.
  </behavior>
  <action>Extend application validation coverage to assert the normal test config from Task 1 is explicit and accepted by `Chimeway.Storage.validate_prefix!/0`. Keep the missing-config case invalid by requiring that test to delete `:prefix` from the application env and restore the prior value afterward. Do not add any default argument to `Application.get_env/3`, `Application.fetch_env!/2`, or any fallback that hides missing config. Do not change Oban config; D-22 keeps Chimeway's storage prefix and Oban's job-table prefix separate.</action>
  <acceptance_criteria>
  - `config/config.exs` contains `prefix: false` in the `config :chimeway` application config.
  - Focused tests pass with the normal test config.
  - Deleting the `:prefix` key in tests still raises `Chimeway.ConfigError`.
  - No Oban prefix setting or Oban config proof is added in Phase 73.
  </acceptance_criteria>
  <verify>
    <automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/application_validation_test.exs test/chimeway/storage_test.exs --warnings-as-errors</automated>
    <automated>bash -lc 'if rg -n "prefix:" config/config.exs config/test.exs | rg "Oban"; then exit 1; fi'</automated>
  </verify>
  <done>The repository no longer relies on missing prefix config for its current public-schema behavior, and missing config remains invalid per PFX-01/PFX-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host config -> Chimeway.Application | Untrusted host configuration is read during boot before supervised children start. |
| Chimeway.Application -> Repo/Oban children | Invalid config must stop startup before storage or job children can act on it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-05 | Information Disclosure / Tampering | `Chimeway.Application.start/2` | mitigate | Call `Chimeway.Storage.validate_prefix!/0` before building child specs so missing config cannot silently write public data. |
| T-73-06 | Denial of Service | boot validation | mitigate | Validate config shape only and avoid schema existence checks that can fail before migrations run. |
| T-73-07 | Tampering | `config/config.exs` | mitigate | Make current public-schema mode explicit with `prefix: false`; missing config stays invalid. |
| T-73-08 | Tampering | Oban configuration | accept | Phase 73 does not configure Oban prefix; D-22 keeps Oban job-table prefix separate for Phase 76 documentation. |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | Phase 73 Plan 02 installs no packages and modifies no package manifests. |
</threat_model>

<verification>
<automated>CHIMEWAY_SKIP_OBAN=1 MIX_ENV=test mix test test/chimeway/application_validation_test.exs test/chimeway/storage_test.exs --warnings-as-errors</automated>
<automated>mix format --check-formatted</automated>
<automated>bash -lc 'if rg -n "Ecto.Adapters.SQL|information_schema|to_regclass|Repo\\." lib/chimeway/application.ex; then exit 1; fi'</automated>
</verification>

<success_criteria>
- PFX-02: invalid/missing prefix config raises `Chimeway.ConfigError` through the application boot path.
- PFX-01: the repository config explicitly represents current public-schema mode with `prefix: false`.
- Boot validation runs before Repo/Oban child specs are built.
- No boot-time schema existence check, Oban prefix configuration, migration generation change, or runtime Repo propagation is introduced.
</success_criteria>

<output>
Create `.planning/phases/73-storage-prefix-contract/73-02-SUMMARY.md` when done.
</output>
