---
phase: 27-journey-traces-host-signal-api
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - lib/chimeway/workflows.ex
  - test/chimeway/workflows_inspection_test.exs
autonomous: true
requirements:
  - OPS-04
must_haves:
  truths:
    - Operators can call `explain` to get state from the database.
    - Trace listing endpoints strictly omit payload data by default.
    - Trace and execution queries strictly isolate by tenant.
  artifacts:
    - path: "lib/chimeway/workflows.ex"
      provides: "explain/2 and list_traces/3 inspection endpoints"
  key_links:
    - from: "lib/chimeway/workflows.ex"
      to: "chimeway_workflow_runs"
      via: "Ecto queries enforcing tenant_id"
      pattern: "where: \\[tenant_id: \\^tenant_id\\]"
---

<objective>
Expose tenancy-aware inspection API answering workflow state and traces.

Purpose: Operators need to inspect workflow states entirely from persisted data without exposing PII. Trace queries must be strictly isolated per tenant.
Output: Inspection endpoints in `Chimeway.Workflows` for retrieving execution state and structurally-safe traces.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-journey-traces-host-signal-api/27-RESEARCH.md
@.planning/phases/27-journey-traces-host-signal-api/27-PATTERNS.md
@.planning/phases/27-journey-traces-host-signal-api/27-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose Journey Inspection API</name>
  <files>lib/chimeway/workflows.ex, test/chimeway/workflows_inspection_test.exs</files>
  <behavior>
    - `Chimeway.Workflows.explain(tenant_id, execution_id)` returns the workflow run state including `status`, `current_step_name`, `suspended_until`, `pending_signals`, and `terminal_reason`.
    - `Chimeway.Workflows.list_traces(tenant_id, execution_id, opts \\ [])` returns `WorkflowTransition` records associated with the execution.
    - Any payload data MUST be omitted from these results by default to protect PII.
  </behavior>
  <action>Implement `explain/2` and `list_traces/3` in `Chimeway.Workflows`. Crucially, both functions must enforce `tenant_id` as the primary query filter alongside `execution_id` to prevent cross-tenant data leakage. Ensure that the returned trace structures omit any sensitive payload data (structural traces only).</action>
  <verify>
    <automated>mix test test/chimeway/workflows_inspection_test.exs</automated>
  </verify>
  <done>Inspection APIs cleanly return data securely scoped to the requested tenant with no PII exposure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> Chimeway | Operator querying workflow trace states |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Information Disclosure | `Chimeway.Workflows.list_traces` | mitigate | Enforce `tenant_id` at the function signature level to guarantee query isolation. Explicitly drop or omit payload map contents in returned structs. |
| T-27-05 | Elevation of Privilege | `Chimeway.Workflows.explain` | mitigate | Only return data associated with the requested `tenant_id`, explicitly querying `tenant_id` alongside primary keys. |
</threat_model>

<verification>
- `mix test test/chimeway/workflows_inspection_test.exs` comprehensively covers cross-tenant rejection scenarios.
</verification>

<success_criteria>
An operator can retrieve a clear picture of why a workflow is suspended and what steps have occurred, using only persisted structural traces, cleanly isolated by tenant.
</success_criteria>

<output>
After completion, create `.planning/phases/27-journey-traces-host-signal-api/27-03-SUMMARY.md`
</output>
