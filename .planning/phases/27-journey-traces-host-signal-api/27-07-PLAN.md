---
phase: 27
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/workflows.ex
  - test/chimeway/workflows_test.exs
autonomous: true
requirements: [GAP-CLOSURE]
must_haves:
  truths:
    - "Signals are matched using both tenant_id and actor_id against the notification recipient"
    - "Resumed workflow runs clear the suspended_until timestamp"
  artifacts:
    - path: "lib/chimeway/workflows.ex"
      provides: "route_signal/1 implementation"
    - path: "test/chimeway/workflows_test.exs"
      provides: "Isolation and resumption test coverage"
  key_links:
    - from: "lib/chimeway/workflows.ex"
      to: "Chimeway.Notifications.Notification"
      via: "Ecto join query"
---

<objective>
Address gaps in `route_signal/1` by enforcing `actor_id` matching against `notification.recipient_identity` and clearing `suspended_until` on resumption.

Purpose: Ensure strict tenant+actor isolation for signal routing and fix explainability bugs where active runs appeared suspended.
Output: Patched routing logic and new test coverage.
</objective>

<context>
@.planning/phases/27-journey-traces-host-signal-api/27-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test coverage for gap closure</name>
  <files>test/chimeway/workflows_test.exs</files>
  <behavior>
    - Test 1: `route_signal/1` does not resume a waiting run if the signal's `actor_id` does not match the notification's `recipient_identity` (same tenant, different actor).
    - Test 2: `route_signal/1` sets `suspended_until` to `nil` when resuming a waiting run.
  </behavior>
  <action>
    Add a test case in the "route_signal/1 — basic matching" describe block demonstrating that a signal sent by a different `actor_id` than the run's recipient identity is ignored, even if `tenant_id` and `event_name` match.
    
    Update the existing resumption test (or add a new one) to assert that `suspended_until` is `nil` after `route_signal/1` succeeds. You will need to seed `suspended_until: DateTime.utc_now()` in the setup of that test.
  </action>
  <verify>
    <automated>mix test test/chimeway/workflows_test.exs</automated>
  </verify>
  <done>Tests are written, correctly failing before implementation, and asserting the right conditions.</done>
</task>

<task type="auto">
  <name>Task 2: Fix `route_signal/1` gaps</name>
  <files>lib/chimeway/workflows.ex</files>
  <action>
    Modify `find_runs_waiting_for_signal/2` to become `find_runs_waiting_for_signal/3` accepting `(tenant_id, actor_id, event_name)`.
    
    Update the Ecto query to `join: n in Chimeway.Notifications.Notification, on: wr.notification_id == n.id` and add `n.recipient_identity == ^actor_id` to the `where` clause. Ensure the query selects only `wr`.
    
    Modify `route_signal/1` to pass `signal.actor_id` to this helper.
    
    Modify the `update_run` call in `route_signal/1` to include `suspended_until: nil` alongside the other state transitions.
  </action>
  <verify>
    <automated>mix test test/chimeway/workflows_test.exs</automated>
  </verify>
  <done>All tests pass, confirming isolation and state correctness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Signal -> Workflow | Untrusted signal payload triggers internal workflow state changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07-01 | Elevation of Privilege | route_signal/1 | mitigate | Enforce `actor_id` matches `recipient_identity` so users cannot resume other users' workflows. |
</threat_model>

<verification>
mix test test/chimeway/workflows_test.exs
</verification>

<success_criteria>
- `route_signal/1` strictly filters by `actor_id`.
- `suspended_until` is cleared when a run resumes.
- Test coverage exists for both behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/27-journey-traces-host-signal-api/27-07-SUMMARY.md`
</output>
