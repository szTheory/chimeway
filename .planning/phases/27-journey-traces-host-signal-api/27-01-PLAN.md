---
phase: 27-journey-traces-host-signal-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/create_chimeway_signals_and_spine.exs
  - lib/chimeway/signals/signal.ex
  - lib/chimeway/signal.ex
  - lib/chimeway/workflows/workflow_run.ex
  - lib/chimeway/dispatch/signal_router_worker.ex
  - test/chimeway/signal_test.exs
autonomous: true
requirements:
  - API-01
must_haves:
  truths:
    - Host applications can track a progression event via the Signal API.
    - Tracked signals are durably stored in the database.
    - Workflow runs have a columnar representation of their suspension state.
  artifacts:
    - path: "lib/chimeway/signals/signal.ex"
      provides: "Signal schema for durable event storage"
    - path: "lib/chimeway/workflows/workflow_run.ex"
      provides: "State spine columns in schema"
    - path: "lib/chimeway/signal.ex"
      provides: "Chimeway.Signal.track/4 API boundary"
  key_links:
    - from: "lib/chimeway/signal.ex"
      to: "lib/chimeway/signals/signal.ex"
      via: "Ecto.Multi.insert"
      pattern: "Multi.insert.*Signal\\.changeset"
---

<objective>
Introduce the Authoritative State Spine on `WorkflowRun` and provide the durable Signal API boundary for host applications.

Purpose: Allow host apps to submit facts (events) without blocking on internal workflow routing latency. Evolve `WorkflowRun` to fully capture suspension reasons in columnar format.
Output: Signal schema, updated WorkflowRun schema, Ecto migrations, and the `Chimeway.Signal.track` API boundary.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-journey-traces-host-signal-api/27-RESEARCH.md
@.planning/phases/27-journey-traces-host-signal-api/27-PATTERNS.md
</context>

<interfaces>
From `lib/chimeway/workflows/workflow_run.ex`:
```elixir
  schema "chimeway_workflow_runs" do
    belongs_to(:notification, Notification)
    belongs_to(:workflow_definition, WorkflowDefinition)
    belongs_to(:current_step, WorkflowStep)
    has_many(:transitions, WorkflowTransition)

    field(:state, Ecto.Enum, values: @state_values, default: :active)
    field(:started_at, :utc_datetime_usec)
    field(:last_transition_at, :utc_datetime_usec)
    field(:status_reason, :string)
    field(:status_context, :map, default: %{})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Write SignalRouterWorker interface contract</name>
  <files>lib/chimeway/dispatch/signal_router_worker.ex</files>
  <action>Create a barebones Oban worker module `Chimeway.Dispatch.SignalRouterWorker` so that `Chimeway.Signal.track/4` can reference it and enqueue jobs. Do not implement the `perform/1` logic yet, just return `{:ok, :noop}`.</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>The module exists and compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add State Spine to WorkflowRun</name>
  <files>priv/repo/migrations/create_chimeway_signals_and_spine.exs, lib/chimeway/workflows/workflow_run.ex</files>
  <behavior>
    - Ensure schema defines `suspended_until`, `pending_signals`, `tenant_id`, and `terminal_reason`.
  </behavior>
  <action>Create a single Ecto migration that alters `chimeway_workflow_runs` to add `tenant_id` (string), `suspended_until` (utc_datetime_usec), `pending_signals` (array of strings, default: []), and `terminal_reason` (string). Ensure `tenant_id` is required. Update the `WorkflowRun` schema to include these fields in `@required_fields`/`@optional_fields` and `cast`.</action>
  <verify>
    <automated>mix test test/chimeway/workflows/workflow_run_test.exs</automated>
  </verify>
  <done>WorkflowRun schema and database table reflect the new State Spine columns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the Signal domain model and API</name>
  <files>priv/repo/migrations/create_chimeway_signals_and_spine.exs, lib/chimeway/signals/signal.ex, lib/chimeway/signal.ex, test/chimeway/signal_test.exs</files>
  <behavior>
    - `Chimeway.Signal.track/4` accepts `tenant_id`, `actor_id`, `event_name`, and `payload`
    - Inserts a durable record into `chimeway_signals`
    - Enqueues an Oban job to route the signal via `SignalRouterWorker`
  </behavior>
  <action>Update the migration from Task 1 to also create the `chimeway_signals` table (id, tenant_id, actor_id, event_name, payload, timestamps). Create the `Chimeway.Signals.Signal` schema. Implement `Chimeway.Signal.track(tenant_id, actor_id, event_name, payload \\ %{})` using `Ecto.Multi` to durably insert the signal and enqueue `Chimeway.Dispatch.SignalRouterWorker` passing the new signal ID.</action>
  <verify>
    <automated>mix test test/chimeway/signal_test.exs</automated>
  </verify>
  <done>Calling track inserts a Signal and enqueues a routing job in the same transaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host App -> Chimeway | Host application submitting signal events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Information Disclosure | `Chimeway.Signal.track` | mitigate | Ensure `track/4` enforces tenant isolation naturally via the `tenant_id` argument and handles potentially malicious JSON payloads safely (stored as map, not evaluated). |
| T-27-02 | Denial of Service | `Chimeway.Signal.track` | accept | Host application rate limiting is out of scope for the embedded library; bounded queue limits on Oban provide backpressure. |
</threat_model>

<verification>
- `mix compile` succeeds
- `mix test` passes for the new modules
- Database migrations successfully roll up and down
</verification>

<success_criteria>
The state spine schema changes are durably persisted and the Signal API accepts requests and enqueues a placeholder Oban routing job.
</success_criteria>

<output>
After completion, create `.planning/phases/27-journey-traces-host-signal-api/27-01-SUMMARY.md`
</output>
