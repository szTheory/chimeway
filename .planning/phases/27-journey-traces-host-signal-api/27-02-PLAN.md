---
phase: 27-journey-traces-host-signal-api
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - lib/chimeway/dispatch/signal_router_worker.ex
  - lib/chimeway/workflows.ex
  - test/chimeway/dispatch/signal_router_worker_test.exs
autonomous: true
requirements:
  - OPS-03
must_haves:
  truths:
    - The signal router worker finds suspended workflows waiting on the signal.
    - The workflow state is appropriately updated and resumed upon matching.
    - Traces are written durably upon transition.
  artifacts:
    - path: "lib/chimeway/dispatch/signal_router_worker.ex"
      provides: "Oban worker for fanning out signals"
    - path: "lib/chimeway/workflows.ex"
      provides: "Routing logic for matching signals to workflows"
  key_links:
    - from: "lib/chimeway/dispatch/signal_router_worker.ex"
      to: "lib/chimeway/workflows.ex"
      via: "Chimeway.Workflows.route_signal/1"
      pattern: "Workflows\\.route_signal"
---

<objective>
Process incoming signals and wake up suspended workflows.

Purpose: Ensure that once a signal is tracked, active workflows waiting on that specific event are resumed securely and trace records are generated.
Output: Implemented `SignalRouterWorker`, workflow routing logic matching on `tenant_id` and `pending_signals`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-journey-traces-host-signal-api/27-RESEARCH.md
@.planning/phases/27-journey-traces-host-signal-api/27-PATTERNS.md
@.planning/phases/27-journey-traces-host-signal-api/27-01-SUMMARY.md
</context>

<interfaces>
From `lib/chimeway/workflows/workflow_run.ex` (Phase 27 enhancements):
```elixir
    field(:state, Ecto.Enum, values: @state_values, default: :active)
    # ...
    field(:tenant_id, :string)
    field(:suspended_until, :utc_datetime_usec)
    field(:pending_signals, {:array, :string}, default: [])
    field(:terminal_reason, :string)
```

From `lib/chimeway/signals/signal.ex`:
```elixir
  schema "chimeway_signals" do
    field(:tenant_id, :string)
    field(:actor_id, :string)
    field(:event_name, :string)
    field(:payload, :map, default: %{})
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Signal Routing Logic in Workflows</name>
  <files>lib/chimeway/workflows.ex</files>
  <behavior>
    - Match `WorkflowRun` for matching `tenant_id`, waiting state, where `pending_signals` contains the signal's `event_name`.
    - Update `WorkflowRun` state (clear pending signals, set active).
    - Insert a `WorkflowTransition` trace reflecting the signal payload conceptually (but omitting PII payload keys by default).
  </behavior>
  <action>Implement `Chimeway.Workflows.route_signal(signal)`. It should query for all `WorkflowRun` records where `tenant_id` equals the signal's tenant, state is `:waiting` (or evaluating signals), and the signal's `event_name` is present in `pending_signals`. Iterate over matched runs to update their state and append a `WorkflowTransition`. Wrap operations in `Ecto.Multi`.</action>
  <verify>
    <automated>mix test test/chimeway/workflows_test.exs</automated>
  </verify>
  <done>route_signal accurately updates matched workflows and appends transition traces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete SignalRouterWorker implementation</name>
  <files>lib/chimeway/dispatch/signal_router_worker.ex, test/chimeway/dispatch/signal_router_worker_test.exs</files>
  <behavior>
    - Fetches the exact signal from `signal_id`.
    - Delegates processing to `Workflows.route_signal/1`.
  </behavior>
  <action>Flesh out `perform(%Oban.Job{args: %{"signal_id" => signal_id}})` in `Chimeway.Dispatch.SignalRouterWorker`. Retrieve the `Signal` from the database. Call `Chimeway.Workflows.route_signal(signal)`. Ensure that success and standard Oban retry failure modes (`{:ok, _}` / `{:error, _}`) are cleanly handled.</action>
  <verify>
    <automated>mix test test/chimeway/dispatch/signal_router_worker_test.exs</automated>
  </verify>
  <done>The worker successfully routes valid signals using their IDs and retries appropriately if queries fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Oban Worker -> Chimeway State | Internal processing of asynchronous external signals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Elevation of Privilege | `Chimeway.Workflows.route_signal` | mitigate | Always explicitly filter workflow runs by `tenant_id = ^signal.tenant_id` in the matching query. Cross-tenant signal bleeding must be structurally impossible. |
</threat_model>

<verification>
- `mix test test/chimeway/dispatch/signal_router_worker_test.exs` passes
- `mix test test/chimeway/workflows_test.exs` verifies multi-tenant isolation
</verification>

<success_criteria>
Pending workflows correctly resume when targeted signals arrive via Oban workers without interfering with non-matching or cross-tenant workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/27-journey-traces-host-signal-api/27-02-SUMMARY.md`
</output>
