---
phase: 94-mailglass-transactional-email-proof
plan: 03
type: execute
wave: 3
depends_on:
  - 94-01
  - 94-02
files_modified:
  - test/support/artifact_consumer_fixture.ex
  - test/chimeway/release_gate_contract_test.exs
  - guides/introduction/mailglass-integration.md
  - test/chimeway/doc_contract_test.exs
autonomous: true
gap_closure: true
requirements:
  - MAIL-01
  - MAIL-02
must_haves:
  truths:
    - "MAIL-01 / D-02: One host-owned ArtifactConsumer.Repo migrates and persists both Chimeway and Mailglass state through normal Ecto migrations and the public Mailglass migration wrapper."
    - "MAIL-02 / D-11: The canonical guide accurately states that the generated proof configures, migrates, supervises, and routes both libraries through ArtifactConsumer.Repo."
    - "The generated consumer proves at runtime that Chimeway's active Ecto repo, Mailglass's configured repo, the Ecto migration repo, and the supervised process all identify ArtifactConsumer.Repo; Chimeway.Repo is not started as a second persistence process."
    - "Repository-topology proof remains a private contract assertion; adopter-facing lifecycle evidence still comes only from Chimeway.Traces.explain_delivery/1 and retains the exact sanitized CHIMEWAY_MAILGLASS_PROOF allowlist."
  artifacts:
    - path: test/support/artifact_consumer_fixture.ex
      provides: "generated ArtifactConsumer.Repo configuration/supervision, Chimeway dynamic-repo routing, and executable runtime topology assertion"
    - path: test/chimeway/release_gate_contract_test.exs
      provides: "MAIL-01 structural and runtime contract for ArtifactConsumer.Repo across Chimeway, Mailglass, supervision, and migrations"
    - path: guides/introduction/mailglass-integration.md
      provides: "canonical description of the executable clean-consumer repo topology and unchanged Fake/live-provider boundary"
    - path: test/chimeway/doc_contract_test.exs
      provides: "MAIL-02 contract coupled to the generated fixture topology rather than phrase presence alone"
  key_links:
    - from: test/support/artifact_consumer_fixture.ex
      to: test/chimeway/release_gate_contract_test.exs
      via: "prove_mailglass!/2 executes a runtime repo-identity assertion and returns the generated config/application sources for structural checks"
    - from: test/support/artifact_consumer_fixture.ex
      to: guides/introduction/mailglass-integration.md
      via: "the exact ArtifactConsumer.Repo module and host-ownership boundary described by the guide"
    - from: test/chimeway/doc_contract_test.exs
      to: test/support/artifact_consumer_fixture.ex
      via: "shared topology contract/helper or source-derived assertion that fails when executable configuration and prose diverge"
---

<objective>
Close the two Phase 94 verification gaps by making the generated consumer, release-gate proof, canonical guide, and documentation contract agree on the locked single host-owned `ArtifactConsumer.Repo` topology, per D-02 and D-11.

Purpose: A green proof must demonstrate that `ArtifactConsumer.Repo` is the configured, migrated, supervised, and active persistence identity for both libraries, not merely repeat a one-repo sentence while `Chimeway.Repo` also runs.
Output: Runtime and structural `ArtifactConsumer.Repo` assertions in the artifact fixture/release contract, plus guide and doc contracts mechanically coupled to that topology without changing the public evidence boundary.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/94-mailglass-transactional-email-proof/94-CONTEXT.md
@.planning/phases/94-mailglass-transactional-email-proof/94-RESEARCH.md
@.planning/phases/94-mailglass-transactional-email-proof/94-PATTERNS.md
@.planning/phases/94-mailglass-transactional-email-proof/94-VERIFICATION.md
@.planning/phases/94-mailglass-transactional-email-proof/94-VALIDATION.md
@.planning/phases/94-mailglass-transactional-email-proof/94-01-SUMMARY.md
@.planning/phases/94-mailglass-transactional-email-proof/94-02-SUMMARY.md
</context>

<interfaces>
- `Chimeway.Application.start/2` supervises `Chimeway.Repo` when the dependency application starts normally; the generated host must load Chimeway as an included application and own repo startup so that this artifact proof does not create a second repo process.
- Chimeway persistence calls use the `Chimeway.Repo` Ecto facade; the generated synchronous proof must bind that facade's active dynamic repo to `ArtifactConsumer.Repo` before triggering or explaining a delivery, while `config :chimeway, repo: ArtifactConsumer.Repo` records the host-owned identity.
- `Mailglass` reads its repo module from `config :mailglass, repo: ...` and its public `Mailglass.Migration.up/0` / `down/0` wrapper runs through the host's normal Ecto migration path.
- `Chimeway.Test.ArtifactConsumerFixture.prove_mailglass!/2` already executes the generated consumer and returns proof/configuration artifacts suitable for contract assertions.
- `Chimeway.Traces.explain_delivery/1` remains the sole adopter-facing lifecycle evidence source; repo-topology checks are private verification only.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make one configured repo executable and prove its runtime identity</name>
  <files>test/support/artifact_consumer_fixture.ex, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    test/support/artifact_consumer_fixture.ex
    test/chimeway/release_gate_contract_test.exs
    lib/chimeway/application.ex
    lib/chimeway/repo.ex
    lib/chimeway/install/migrations.ex
    .planning/phases/94-mailglass-transactional-email-proof/94-CONTEXT.md
    .planning/phases/94-mailglass-transactional-email-proof/94-VERIFICATION.md
    .planning/phases/94-mailglass-transactional-email-proof/94-01-SUMMARY.md
  </read_first>
  <behavior>
    - "MAIL-01 / D-02: the generated host configures Ecto, Chimeway, and Mailglass to ArtifactConsumer.Repo, supervises that host module once, and makes it Chimeway's active dynamic repo for the synchronous trigger-and-explain path."
    - "MAIL-01 / D-02: Chimeway is loaded as an included dependency application for this proof, so Chimeway.Application does not start Chimeway.Repo beside the host-owned ArtifactConsumer.Repo."
    - "MAIL-01 / D-06: `mix ecto.migrate` applies both generated Chimeway migrations and the public Mailglass migration wrapper through ArtifactConsumer.Repo."
    - "MAIL-01 / D-01: the proof still uses only the unpacked Chimeway artifact, one unique temporary database, serialized execution, and failure-safe cleanup."
    - "MAIL-02 / D-07..D-09: private topology assertions do not add repo identity or database data to the public proof line, whose existing allowlist and Fake label remain unchanged."
  </behavior>
  <action>
    Start with a failing release-gate contract that demonstrates the current split: the generated Ecto migration repo, `Application.fetch_env!(:chimeway, :repo)`, `Application.fetch_env!(:mailglass, :repo)`, `Chimeway.Repo.get_dynamic_repo/0`, and the live supervised repo process must all identify `ArtifactConsumer.Repo`. Exercise this inside the generated consumer process so the test proves loaded runtime configuration and process state rather than matching prose or counting source strings.

    Resolve the split exactly per D-02: retain the generated `ArtifactConsumer.Repo`, configure `:artifact_consumer` Ecto migrations, `:chimeway`'s `:repo`, and `:mailglass`'s `:repo` to that module, and have `ArtifactConsumer.Application` supervise only `ArtifactConsumer.Repo`. Declare Chimeway as an included application in the generated host so its modules are loaded but `Chimeway.Application` does not independently start `Chimeway.Repo`. In the synchronous proof process, call `Chimeway.Repo.put_dynamic_repo(ArtifactConsumer.Repo)` before the trigger and keep that binding through `Chimeway.Traces.explain_delivery/1`, restoring the previous dynamic repo in an `after` block so every Chimeway Ecto call in the proof resolves to the host repo. Do not replace `ArtifactConsumer.Repo` with the published `Chimeway.Repo`, and do not modify `lib/chimeway/**`, root config, root dependencies, or published migration behavior.

    Add an explicit generated runtime assertion before the Mailglass trigger that verifies both library settings and the Ecto migration repo equal `ArtifactConsumer.Repo`, `Chimeway.Repo.get_dynamic_repo/0` equals `ArtifactConsumer.Repo`, the `ArtifactConsumer.Repo` process is alive, and no `Chimeway.Repo` process is registered. Keep the assertion private: do not serialize repo names, database identifiers, query results, or configuration into `CHIMEWAY_MAILGLASS_PROOF`. Preserve `Chimeway.Traces.explain_delivery/1` as the one public lifecycle source and retain the exact existing evidence-key set per D-07/D-08. Preserve Mailglass Fake checkout/shared ownership before the synchronous trigger; dynamic-repo routing must not replace or weaken that boundary.

    Return only non-sensitive generated config/application source or a fixed topology result needed by the release contract. Extend the release-gate test to prove the generated config, supervisor ownership, public Mailglass migration wrapper, successful Mailglass transaction, artifact provenance, and cleanup together. Preserve all existing parser, Fake ownership, stable render-key, and lifecycle assertions from D-01 and D-04 through D-09.
  </action>
  <acceptance_criteria>
    - The generated consumer's Ecto migration repo, Chimeway configured and active dynamic repo, Mailglass configured repo, and live supervised repo all resolve specifically to ArtifactConsumer.Repo during the executed proof.
    - The generated host loads Chimeway without starting Chimeway.Application, supervises ArtifactConsumer.Repo exactly once, and the release contract fails if Chimeway.Repo is registered as a second repo process.
    - The generated Mailglass migration still delegates to `Mailglass.Migration.up/0` and `Mailglass.Migration.down/0` and runs under the same `mix ecto.migrate` invocation as Chimeway migrations.
    - The focused contract executes the generated consumer and asserts topology from loaded configuration/process state, not solely from a required phrase or regex count.
    - `CHIMEWAY_MAILGLASS_PROOF` retains its exact pre-remediation field allowlist, `transport=fake`, one `Chimeway.Traces.explain_delivery/1` source, and no repo/config/database fields.
    - Unpacked-artifact provenance, deterministic Mailglass outcome, parser rejection cases, filesystem cleanup, and database cleanup remain green.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The clean consumer runs both Chimeway and Mailglass persistence through the host-owned ArtifactConsumer.Repo and proves its configuration, migration, supervision, and active runtime identity while keeping public evidence trace-only and sanitized.</done>
  <reversibility rating="reversible">The correction changes generated test-consumer templates and contracts only; no published runtime or storage contract changes.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Couple canonical guidance to the executable repo topology</name>
  <files>guides/introduction/mailglass-integration.md, test/chimeway/doc_contract_test.exs, test/support/artifact_consumer_fixture.ex</files>
  <read_first>
    guides/introduction/mailglass-integration.md
    guides/recipes/mailglass-integration-blueprint.md
    test/chimeway/doc_contract_test.exs
    test/support/artifact_consumer_fixture.ex
    test/chimeway/release_gate_contract_test.exs
    .planning/phases/94-mailglass-transactional-email-proof/94-CONTEXT.md
    .planning/phases/94-mailglass-transactional-email-proof/94-VERIFICATION.md
    .planning/phases/94-mailglass-transactional-email-proof/94-02-SUMMARY.md
  </read_first>
  <behavior>
    - "MAIL-02 / D-11: the guide names ArtifactConsumer.Repo as the single host-configured module exercised by the generated consumer for both libraries, without claiming a separate Mailglass or Chimeway persistence repo."
    - "MAIL-02 / D-10: documentation contracts fail when the guide's repo topology diverges from the executable fixture contract, even if a generic one-repo phrase remains present."
    - "MAIL-02 / D-07..D-10: Fake recording, successful adapter attempt, public trace-only evidence, and all live-provider exclusions remain exactly bounded."
  </behavior>
  <action>
    First replace the phrase-only repo assertion with a failing documentation contract tied to the fixture's canonical topology declaration. Expose one test-only canonical topology value/helper from `Chimeway.Test.ArtifactConsumerFixture` if needed so both release and doc contracts compare against the same source of truth; keep it compile-time/static and free of database credentials. The doc contract must parse or compare the guide's explicit module/config example against that canonical value, and it must fail for the former split configuration even when generic host-owned/one-repo prose is present.

    Update the guide per D-02/D-11 to describe the exact executable clean-consumer topology: the host defines and supervises `ArtifactConsumer.Repo`, configures both Chimeway and Mailglass to it, migrates both libraries through it, loads Chimeway without separately starting `Chimeway.Repo`, and binds the synchronous Chimeway proof path to the host repo. Show or name the concrete repo settings sufficiently for an adopter to reproduce them. Retain the `ArtifactConsumer.Repo` claim and remove any implication that this proof uses the published `Chimeway.Repo` as its persistence identity. Preserve the broader truth that Mailglass accepts a host-configured repo.

    Keep the clean-consumer boundary from D-09/D-10 intact: Fake recorded the host-composed message, Chimeway recorded a successful Mailglass adapter attempt, and the proof excludes provider acceptance, sender/domain verification, inbox placement/display, credentials, callbacks, and live webhook feedback. Preserve the blueprint cross-link and repository-maintainer labeling for `mix verify.mailglass`. Do not add provider/webhook execution, new public output fields, a new API integration, or a CI lane.
  </action>
  <acceptance_criteria>
    - The guide's clean-consumer section names ArtifactConsumer.Repo and accurately describes its configuration, migration, supervision, active Chimeway routing, and host ownership in the executed fixture.
    - The documentation contract derives or compares against the fixture's canonical topology and fails for a two-module configuration even when one-repo prose is still present.
    - The release contract and doc contract share one canonical topology expectation rather than maintaining independent phrase-only assertions.
    - Existing Fake-versus-live-provider required/forbidden contracts, blueprint cross-link, webhook boundaries, and maintainer-suite label remain green.
    - The focused release/doc contract command and `mix ci.verify_gates` pass without changes under `lib/`, root `mix.exs`, `priv/`, or `.github/workflows/`.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs test/chimeway/doc_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The canonical guide and its contract describe and enforce the exact repo topology executed by the clean consumer, while all public evidence and provider-boundary guarantees remain unchanged.</done>
  <reversibility rating="reversible">The guide, test-only canonical topology helper, and contracts can change together without migrating adopter data or changing public APIs.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated config/supervisor -> executable proof | Static claims must agree with the repo module actually configured, migrated, and running. |
| executable fixture -> canonical guide | Documentation must derive its topology from the tested fixture contract rather than an independent phrase. |
| private topology assertion -> public proof output | Repo/config/database details must remain private while public lifecycle evidence stays trace-derived and allowlisted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-94-G1 | Spoofing | generated one-repo claim | high | mitigate | Execute a runtime identity assertion across Ecto, Chimeway, Mailglass, and the supervised repo process; fail on any mismatch. |
| T-94-G2 | Tampering | guide/fixture topology drift | medium | mitigate | Share a canonical test-only topology expectation across release and documentation contracts and exercise both in the focused gate. |
| T-94-G3 | Information Disclosure | proof-line serialization | high | mitigate | Retain the exact trace-derived allowlist and contract-test that repo, config, and database facts never enter public evidence. |
| T-94-G4 | Denial of Service | duplicate repo supervision | medium | mitigate | Load Chimeway as an included application, supervise only ArtifactConsumer.Repo, and assert Chimeway.Repo has no registered process before triggering. |
</threat_model>

## Artifacts this phase produces

- `test/support/artifact_consumer_fixture.ex`: corrected generated ArtifactConsumer.Repo config/supervision and dynamic routing, private runtime topology assertion, and canonical non-sensitive topology contract.
- `test/chimeway/release_gate_contract_test.exs`: executable MAIL-01 proof that migration, Chimeway, Mailglass, and the supervised process all use ArtifactConsumer.Repo.
- `guides/introduction/mailglass-integration.md`: accurate, reproducible description of that tested clean-consumer topology.
- `test/chimeway/doc_contract_test.exs`: MAIL-02 topology contract coupled to the fixture plus preserved Fake/live-provider boundaries.

No production module, root dependency, published migration, provider integration, public proof field, or CI workflow is created or modified.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Evaluate the Mailglass path in a clean consumer with public evidence and an exact boundary. | 94-01, 94-02, 94-03 | COVERED | Plan 03 repairs the executable ownership boundary without changing proven delivery evidence. |
| REQ | MAIL-01 | Configured transactional-email orchestration and trace evidence in the clean consumer. | 94-01, 94-03 | COVERED | Runtime one-repo topology joins the already-green Mailglass path. |
| REQ | MAIL-02 | Fake behavior is distinguished from live-provider delivery and feedback. | 94-01, 94-02, 94-03 | COVERED | Existing boundary is preserved while its ownership claim is corrected. |
| RESEARCH | — | Extend the existing fixture, use one host-configured repo, normal Ecto migrations, and Mailglass's public wrapper. | 94-01, 94-03 | COVERED | Task 1 closes the missed one-repo constraint. |
| RESEARCH | — | Fake ownership, trace-only allowlist, strict parsing, provenance, and cleanup. | 94-01, 94-03 | COVERED | Task 1 retains and re-runs these contracts. |
| RESEARCH | — | Guide and contracts state the exact Fake/live-provider and repo boundary. | 94-02, 94-03 | COVERED | Task 2 couples prose to executable topology. |
| CONTEXT | D-01 | Reuse Phase 93 artifact fixture/provenance/database/cleanup/serialization. | 94-01, 94-03 | COVERED | Task 1 preserves and verifies it. |
| CONTEXT | D-02 | One host-owned ArtifactConsumer.Repo migrates and persists both libraries. | 94-03 | COVERED | Task 1 proves ArtifactConsumer.Repo is the configured, migrated, supervised, and active runtime repo while Chimeway.Repo remains unstarted. |
| CONTEXT | D-03 | Retain direct Oban/Mailglass consumer dependencies and support floor. | 94-01, 94-03 | COVERED | Task 1 forbids root/runtime/support-floor changes. |
| CONTEXT | D-04 | Stable notifier/render identity and fixed tenant/idempotency. | 94-01, 94-03 | COVERED | Existing proof assertions remain green. |
| CONTEXT | D-05 | Exact render-key mapping to host mailable/adapter. | 94-01, 94-03 | COVERED | Existing E2E contract remains green. |
| CONTEXT | D-06 | Fake ownership and public migration wrapper. | 94-01, 94-03 | COVERED | Task 1 verifies the wrapper under the corrected repo. |
| CONTEXT | D-07 | Public trace-only evidence; Fake assertion private. | 94-01, 94-03 | COVERED | Task 1 prevents topology leakage. |
| CONTEXT | D-08 | Strict Fake-labeled allowlist and unsafe-field rejection. | 94-01, 94-03 | COVERED | Exact existing key set remains enforced. |
| CONTEXT | D-09 | Calm literal wording without live-delivery overclaim. | 94-02, 94-03 | COVERED | Task 2 preserves positive/negative contracts. |
| CONTEXT | D-10 | Exact exclusions and blueprint cross-reference. | 94-02, 94-03 | COVERED | Task 2 preserves all boundary clauses. |
| CONTEXT | D-11 | Correct host-configured repo guidance and maintainer-suite label. | 94-03 | COVERED | Task 2 binds guidance to executable topology. |

Deferred live-provider, webhook, browser/admin, Phase 96 CI/front-door, and production-runtime work remains excluded and is not a gap.

<verification>
- `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors`
- `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs test/chimeway/doc_contract_test.exs --warnings-as-errors`
- `mix ci.verify_gates`
- `git diff --exit-code -- lib mix.exs priv .github/workflows`
</verification>

<success_criteria>
- Both failed truths in `94-VERIFICATION.md` are closed by executable ArtifactConsumer.Repo identity proof and topology-coupled guidance.
- MAIL-01 and MAIL-02 are covered in frontmatter and objectively verified by focused release/doc contracts plus `mix ci.verify_gates`.
- D-02 and D-11 are implemented without weakening D-01 or D-03 through D-10.
- Public evidence remains the same sanitized, Fake-labeled, trace-only contract.
- No production code, root dependency, migration template, external API integration, or CI-lane change is introduced.
</success_criteria>

<output>
Create `.planning/phases/94-mailglass-transactional-email-proof/94-03-SUMMARY.md` when done.
</output>
