---
phase: 55-inbound-feedback-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/adapter.ex
  - lib/chimeway/dispatch/executor.ex
  - lib/chimeway/webhooks.ex
  - test/chimeway/dispatch/executor_test.exs
  - test/chimeway/webhooks_test.exs
requirements: [ECOS-03]
must_haves:
  truths:
    - "Outbound Mailglass success persists provider_message_id on the attempt row for webhook correlation"
    - "Webhooks.process/4 threads raw_body and headers through config to verify_webhook/3"
    - "Adapters may optionally implement parse_webhook_body/3 for non-JSON webhook normalization"
  artifacts:
    - path: "lib/chimeway/dispatch/executor.ex"
      provides: "provider_message_id lift from adapter meta"
      contains: "provider_message_id"
    - path: "lib/chimeway/webhooks.ex"
      provides: "Config enrichment + optional parse_webhook_body dispatch"
      contains: "raw_body"
    - path: "lib/chimeway/adapter.ex"
      provides: "Optional parse_webhook_body/3 callback documented"
      contains: "parse_webhook_body"
  key_links:
    - from: "lib/chimeway/dispatch/executor.ex"
      to: "lib/chimeway/webhooks/process_feedback_worker.ex"
      via: "provider_message_id on attempt row"
      pattern: "get_delivery_by_provider_message_id"
    - from: "lib/chimeway/webhooks.ex"
      to: "lib/chimeway/adapters/mailglass.ex"
      via: "parse_webhook_body/3 hook (implemented in plan 02)"
      pattern: "parse_webhook_body"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-55-01 | PII from webhook raw body logged or persisted on ingress | high | Ingress stores normalized status only; raw body never written to DB |
| T-55-02 | Unauthorized webhook creates ingress row | high | verify_webhook runs before parse/insert; unauthorized returns before Multi |
| T-55-03 | Breaking change to existing EchoAdapter/MockAdapter webhook path | medium | parse_webhook_body optional; default Jason.decode unchanged |
</threat_model>

<objective>
Extend the Chimeway feedback spine so Mailglass webhooks can correlate outbound deliveries and parse provider-native payloads without duplicating provider parsers.

Purpose: ECOS-03 foundation — `provider_message_id` correlation (D-05, D-06) and webhook config threading (D-11).

Output: Executor lift, optional `parse_webhook_body/3` callback, Webhooks.process/4 enrichment, regression tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-inbound-feedback-bridge/55-CONTEXT.md
@.planning/phases/55-inbound-feedback-bridge/55-RESEARCH.md
@.planning/phases/55-inbound-feedback-bridge/55-VALIDATION.md
@lib/chimeway/dispatch/executor.ex
@lib/chimeway/webhooks.ex
@lib/chimeway/adapter.ex
@lib/chimeway/delivery_attempt.ex
@test/chimeway/webhooks_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist provider_message_id on outbound attempts (D-05)</name>
  <files>lib/chimeway/dispatch/executor.ex, test/chimeway/dispatch/executor_test.exs</files>
  <read_first>
    - `lib/chimeway/dispatch/executor.ex` — current `record_attempt` attrs at lines 40-46
    - `lib/chimeway/delivery_attempt.ex` — `provider_message_id` field definition
    - `lib/chimeway/adapters/mailglass.ex` — success meta includes `:provider_message_id`
    - `lib/chimeway/deliveries.ex` — `get_delivery_by_provider_message_id/1`
    - `.planning/phases/55-inbound-feedback-bridge/55-CONTEXT.md` — D-05, D-06
  </read_first>
  <action>
    In `Dispatch.Executor.run_delivery/1`, when building attrs for `Deliveries.record_attempt/2`, add `provider_message_id` extracted from success meta:

    - On `{:ok, meta}` path: set `provider_message_id` to `Map.get(meta, :provider_message_id) || Map.get(meta, "provider_message_id")` when binary, else omit/nil.
    - On error paths: leave `provider_message_id` nil (unchanged).

    Add or extend `test/chimeway/dispatch/executor_test.exs` with a test module that uses a stub adapter returning `{:ok, %{provider_message_id: "msg-abc-123"}}` and asserts the recorded attempt row has `provider_message_id == "msg-abc-123"`. Follow existing dispatch test patterns (Mox or inline stub module in test file).
  </action>
  <acceptance_criteria>
    - `lib/chimeway/dispatch/executor.ex` passes `provider_message_id:` into `Deliveries.record_attempt/2` when present in adapter meta
    - Executor test asserts attempt row `.provider_message_id == "msg-abc-123"` after stub adapter success
    - `mix test test/chimeway/dispatch/executor_test.exs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>mix test test/chimeway/dispatch/executor_test.exs --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 2: Optional parse_webhook_body callback + config threading (D-11)</name>
  <files>lib/chimeway/adapter.ex, lib/chimeway/webhooks.ex, test/chimeway/webhooks_test.exs</files>
  <read_first>
    - `lib/chimeway/adapter.ex` — existing optional webhook callbacks
    - `lib/chimeway/webhooks.ex` — `process/4` with-chain and `decode_body/1`
    - `test/chimeway/webhooks_test.exs` — MockAdapter patterns
    - `.planning/phases/55-inbound-feedback-bridge/55-RESEARCH.md` — Pattern 2 (parse seam)
  </read_first>
  <action>
    1. Add optional callback to `Chimeway.Adapter`:

    `@callback parse_webhook_body(raw_body :: binary(), headers :: list(), config :: keyword()) :: {:ok, map()} | {:error, :unparseable_body}`

    Add to `@optional_callbacks` list.

    2. In `Chimeway.Webhooks.process/4`:
    - Before verify: `config = Keyword.merge(config, raw_body: raw_body, headers: headers)`
    - Replace direct `decode_body(raw_body)` with `decode_webhook_body(adapter_module, raw_body, headers, config)`:
      - If `function_exported?(adapter_module, :parse_webhook_body, 3)`: call adapter callback
      - Else: existing `Jason.decode/1` path

    3. Add test module `ParseBodyAdapter` in `webhooks_test.exs` that implements `parse_webhook_body/3` returning `{:ok, %{"custom" => true}}` and assert `process/4` succeeds with that parsed shape through resolve/normalize (reuse MockAdapter resolve/normalize clauses or inline minimal callbacks).

    Existing MockAdapter tests must still pass unchanged (backward compatibility).
  </action>
  <acceptance_criteria>
    - `lib/chimeway/adapter.ex` documents and declares optional `parse_webhook_body/3`
    - `Chimeway.Webhooks.process/4` merges `:raw_body` and `:headers` into config before `verify_webhook/3`
    - Adapters without `parse_webhook_body/3` still use Jason.decode (all existing webhooks_test.exs tests pass)
    - New test proves ParseBodyAdapter path returns `{:ok, ingress}` when parse_webhook_body succeeds
    - `mix test test/chimeway/webhooks_test.exs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>mix test test/chimeway/webhooks_test.exs --warnings-as-errors</verify>
</task>

</tasks>

<verification>
After both tasks:
- `mix test test/chimeway/dispatch/ test/chimeway/webhooks_test.exs --warnings-as-errors`
- `mix compile --warnings-as-errors`
</verification>

## PLAN COMPLETE
