---
phase: 55-inbound-feedback-bridge
plan: 02
type: execute
wave: 2
depends_on: [55-01]
files_modified:
  - lib/chimeway/adapters/mailglass.ex
  - test/chimeway/adapters/mailglass_adapter_test.exs
  - test/support/chimeway/mailglass_fixtures.ex
requirements: [ECOS-03]
must_haves:
  truths:
    - "D-02: Feedback ingress uses Chimeway.Webhooks.process/4 only — Mailglass.Webhook.Plug is NOT wired as Chimeway ingress"
    - "D-04: Uses Mailglass core Mailglass.Webhook.* APIs only — no mailglass_inbound dependency added"
    - "Chimeway.Adapters.Mailglass implements all four webhook callbacks behind compile guard"
    - "verify_webhook delegates to Mailglass.Webhook.Provider for configured provider"
    - "Delivery-relevant Mailglass events map to :delivered, :bounced, or :failed"
    - "Engagement events (:opened, :clicked, etc.) return :error from normalize_feedback"
  artifacts:
    - path: "lib/chimeway/adapters/mailglass.ex"
      provides: "Webhook callbacks verify/resolve/normalize/event_id"
      contains: "verify_webhook"
    - path: "test/support/chimeway/mailglass_fixtures.ex"
      provides: "Postmark Delivery and Bounce webhook fixtures"
      contains: "postmark_delivery"
  key_links:
    - from: "lib/chimeway/adapters/mailglass.ex"
      to: "Mailglass.Webhook.Providers.Postmark"
      via: "Provider.verify!/3 and Provider.normalize/2"
      pattern: "Mailglass.Webhook.Provider"
    - from: "lib/chimeway/adapters/mailglass.ex"
      to: "lib/chimeway/webhooks.ex"
      via: "parse_webhook_body/3 internal event shape"
      pattern: "_mailglass_event"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-55-03 | Webhook signature bypass | high | Delegate to Mailglass Provider verify!; map SignatureError → :unauthorized |
| T-55-04 | Engagement events trigger workflow progression | medium | normalize_feedback returns :error for non-delivery types (D-09) |
| T-55-05 | Webhook secrets in module attributes | high | Read provider config via Application.get_env at call time only |
</threat_model>

<objective>
Implement Mailglass adapter webhook callbacks bridging provider crypto and Anymail event taxonomy into Chimeway's canonical feedback vocabulary.

Purpose: ECOS-03 core — signed payload verification, delivery identity resolution, outcome normalization (D-01..D-10).

Output: Four callbacks on `Chimeway.Adapters.Mailglass`, Postmark test fixtures, unit tests for verify/resolve/normalize.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-inbound-feedback-bridge/55-CONTEXT.md
@.planning/phases/55-inbound-feedback-bridge/55-RESEARCH.md
@lib/chimeway/adapters/mailglass.ex
@deps/mailglass/lib/mailglass/webhook/provider.ex
@deps/mailglass/lib/mailglass/webhook/providers/postmark.ex
@deps/mailglass/lib/mailglass/events/event.ex
@examples/chimeway_demo_host/lib/demo_host/adapters/echo_adapter.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: parse_webhook_body + verify_webhook (D-03, D-11, D-12)</name>
  <files>lib/chimeway/adapters/mailglass.ex, test/support/chimeway/mailglass_fixtures.ex</files>
  <read_first>
    - `lib/chimeway/adapters/mailglass.ex` — outbound deliver/2 patterns, compile guard
    - `deps/mailglass/lib/mailglass/webhook/providers/postmark.ex` — verify!/3 + normalize/2
    - `deps/mailglass/lib/mailglass/events/event.ex` — @anymail_event_types
    - `.planning/phases/55-inbound-feedback-bridge/55-CONTEXT.md` — D-03, D-09, D-12
    - `lib/chimeway/webhooks.ex` — parse_webhook_body dispatch (from plan 55-01)
  </read_first>
  <action>
    In `Chimeway.Adapters.Mailglass`:

    1. **`parse_webhook_body/3`**: Resolve webhook provider module from config key `:webhook_provider` (atom like `:postmark`) or fallback `Application.get_env(:mailglass, :webhook_providers)` map. Call `provider.normalize(raw_body, headers)`, filter events to delivery-relevant types (`:delivered`, `:sent`, `:bounced`, `:failed`, `:rejected`), take first match (D-12), return `{:ok, %{"_mailglass_event" => event}}`. Empty list → `{:error, :unparseable_body}`.

    2. **`verify_webhook/3`**: Resolve same provider module. Call `provider.verify!(raw_body, headers, provider_config_map)` inside try/rescue:
       - Success → `:ok`
       - `%Mailglass.SignatureError{}` → `{:error, :unauthorized}`
       - `%Mailglass.ConfigError{}` → `{:error, :unauthorized}`

    3. Helper `webhook_provider_module/1` and `webhook_provider_config/1` — read at call time from adapter config keyword and/or Application env. Default test provider `:postmark`.

    In `test/support/chimeway/mailglass_fixtures.ex` add:
    - `postmark_webhook_config/0` — `basic_auth: {"user", "pass"}` map for Postmark provider
    - `postmark_delivery_payload/1` — JSON map with `RecordType: "Delivery"`, `MessageID`, etc. (minimal valid Postmark Delivery shape)
    - `postmark_delivery_headers/0` — Authorization Basic header matching config
    - `postmark_open_payload/0` — RecordType "Open" for engagement rejection tests (plan 02 task 2)
  </action>
  <acceptance_criteria>
    - `Chimeway.Adapters.Mailglass` exports `parse_webhook_body/3` and `verify_webhook/3`
    - `rg "verify_webhook" lib/chimeway/adapters/mailglass.ex` returns matches
    - Postmark fixture with valid Basic auth returns `:ok` from verify_webhook
    - Invalid auth returns `{:error, :unauthorized}`
    - parse_webhook_body on Delivery fixture returns map with `"_mailglass_event"` key
  </acceptance_criteria>
  <verify>mix test test/chimeway/adapters/mailglass_adapter_test.exs --only webhook --warnings-as-errors 2>/dev/null || mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 2: resolve_delivery, normalize_feedback, resolve_provider_event_id (D-06..D-09)</name>
  <files>lib/chimeway/adapters/mailglass.ex, test/chimeway/adapters/mailglass_adapter_test.exs</files>
  <read_first>
    - `examples/chimeway_demo_host/lib/demo_host/adapters/echo_adapter.ex` — resolve_delivery/normalize_feedback patterns
    - `deps/mailglass/lib/mailglass/webhook/providers/postmark.ex` — metadata keys (`message_id`, `provider_event_id`)
    - `.planning/phases/55-inbound-feedback-bridge/55-CONTEXT.md` — D-06, D-07, D-08, D-09
    - `lib/chimeway/webhooks/ingress.ex` — normalized_status vocabulary
  </read_first>
  <action>
    Implement remaining callbacks on `Chimeway.Adapters.Mailglass`:

    1. **`resolve_delivery/1`**: Pattern-match `%{"_mailglass_event" => %Mailglass.Events.Event{metadata: md}}`. Extract provider message id from metadata string keys `"message_id"` or `"provider_message_id"` (first non-empty binary). Return `{:ok, %{provider_message_id: id}}` or `:error`.

    2. **`normalize_feedback/1`**: Map event `:type`:
       - `:delivered`, `:sent` → `{:ok, %{status: :delivered}}`
       - `:bounced` → `{:ok, %{status: :bounced}}`
       - `:failed`, `:rejected` → `{:ok, %{status: :failed}}`
       - `:opened`, `:clicked`, `:complained`, `:autoresponded`, and all other types → `:error`

    3. **`resolve_provider_event_id/1`**: Read `metadata["provider_event_id"]` when binary → `{:ok, id}`; else `:none`.

    Add `describe "webhook callbacks"` in `mailglass_adapter_test.exs`:
    - verify success/failure
    - resolve_delivery returns provider_message_id from Delivery fixture
    - normalize delivered/bounced/failed event types
    - Open/Click payload returns `:error` from normalize_feedback
    - resolve_provider_event_id returns id from fixture metadata

    Tag tests `@tag :mailglass` and gate file with existing `Code.ensure_loaded?` guards.
  </action>
  <acceptance_criteria>
    - All four webhook callbacks exported on Mailglass adapter module
    - Unit test: Delivery event → `normalize_feedback` returns `{:ok, %{status: :delivered}}`
    - Unit test: Bounce event → status `:bounced`
    - Unit test: Open event → `:error`
    - Unit test: resolve_delivery extracts provider_message_id string from event metadata
    - `mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors</verify>
</task>

</tasks>

<verification>
- `mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors`
- `mix compile --warnings-as-errors`
- D-02: `rg "Mailglass\.Webhook\.Plug" lib/chimeway/adapters/mailglass.ex` exits 1 (no matches)
- D-04: `rg "mailglass_inbound" mix.exs` exits 1 (no dependency added)
</verification>

## PLAN COMPLETE
