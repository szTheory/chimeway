---
phase: 55-inbound-feedback-bridge
plan: 03
type: execute
wave: 3
depends_on: [55-02]
files_modified:
  - test/support/chimeway/adapter/contract_test.ex
  - test/chimeway/adapters/mailglass_adapter_test.exs
  - test/chimeway/adapters/mailglass_webhook_pipeline_test.exs
  - test/support/chimeway/mailglass_fixtures.ex
requirements: [ECOS-03, ECOS-04]
must_haves:
  truths:
    - "Mailglass webhook contract tests cover verify, resolve, normalize, and dedup"
    - "End-to-end: outbound deliver → webhook → worker → signal → trace without host glue"
    - "Operator trace timeline shows webhook_received with provider_message_id and adapter_module"
  artifacts:
    - path: "test/chimeway/adapters/mailglass_webhook_pipeline_test.exs"
      provides: "ECOS-04 Chimeway-level feedback pipeline proof"
      contains: "chimeway.delivery.succeeded"
    - path: "test/support/chimeway/adapter/contract_test.ex"
      provides: "Optional webhook callback contract macros"
      contains: "verify_webhook"
  key_links:
    - from: "test/chimeway/adapters/mailglass_webhook_pipeline_test.exs"
      to: "lib/chimeway/webhooks/process_feedback_worker.ex"
      via: "Oban.drain_queue :chimeway_delivery"
      pattern: "ProcessFeedbackWorker"
    - from: "test/chimeway/adapters/mailglass_webhook_pipeline_test.exs"
      to: "lib/chimeway/traces.ex"
      via: "Traces timeline projection"
      pattern: "webhook_received"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-55-06 | Duplicate provider retries create duplicate signals | medium | provider_event_id dedup via ingress partial unique index (existing Phase 33) |
| T-55-07 | Integration test couples to demo host routes | low | Call Webhooks.process/4 directly (D-17) |
</threat_model>

<objective>
Prove ECOS-03 and ECOS-04 with contract tests and a Chimeway-level feedback pipeline integration test mirroring demo host E2E patterns.

Purpose: Lock adapter webhook quality before Phase 56 demo wiring (D-15, D-16, D-13, D-14).

Output: Webhook contract test macros, Mailglass dedup tests, full pipeline integration test with trace assertions.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-inbound-feedback-bridge/55-CONTEXT.md
@.planning/phases/55-inbound-feedback-bridge/55-RESEARCH.md
@test/support/chimeway/adapter/contract_test.ex
@test/chimeway/webhooks_test.exs
@examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs
@lib/chimeway/traces.ex
@lib/chimeway/webhooks/process_feedback_worker.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Webhook contract test macros (D-15)</name>
  <files>test/support/chimeway/adapter/contract_test.ex, test/chimeway/adapters/mailglass_adapter_test.exs</files>
  <read_first>
    - `test/support/chimeway/adapter/contract_test.ex` — existing deliver/2 contract macros
    - `test/chimeway/webhooks_test.exs` — dedup and ingress tests
    - `examples/chimeway_demo_host/lib/demo_host/adapters/echo_adapter.ex` — reference callback shapes
    - `.planning/phases/55-inbound-feedback-bridge/55-CONTEXT.md` — D-15
  </read_first>
  <action>
    Extend `Chimeway.Adapter.ContractTest` with optional webhook contract tests activated when adapter module exports `verify_webhook/3`:

    1. **`webhook_contract?/0`** optional callback default `false`; Mailglass test sets `true`.

    2. When `webhook_contract?/0` is true, add describe block "Chimeway.Adapter webhook contract" with tests requiring optional callbacks:
       - `webhook_fixtures/0` callback returning `%{valid_body: ..., valid_headers: ..., config: ..., provider_message_id: ..., provider_event_id: ...}`
       - verify_webhook valid → `:ok`
       - verify_webhook invalid → `{:error, :unauthorized}`
       - resolve_delivery on parsed fixture → `{:ok, %{provider_message_id: _}}`
       - normalize_feedback → `{:ok, %{status: status}}` where status in `[:delivered, :bounced, :failed]`
       - resolve_provider_event_id → `{:ok, id}` when fixture supplies event id

    3. In `mailglass_adapter_test.exs`: implement `webhook_contract?/0` → true and `webhook_fixtures/0` using MailglassFixtures Postmark Delivery payload. Add dedup test: call `Chimeway.Webhooks.process/4` twice with same provider_event_id; assert single ingress row (mirror webhooks_test.exs dedup pattern).

    Keep deliver/2 contract tests unchanged.
  </action>
  <acceptance_criteria>
    - ContractTest macro includes webhook tests gated by `webhook_contract?/0`
    - Mailglass adapter test activates webhook contract block
    - Dedup test: second identical provider_event_id webhook does not duplicate ingress count
    - `mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>mix test test/chimeway/adapters/mailglass_adapter_test.exs --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 2: ECOS-04 feedback pipeline integration test (D-13, D-14)</name>
  <files>test/chimeway/adapters/mailglass_webhook_pipeline_test.exs, test/support/chimeway/mailglass_fixtures.ex</files>
  <read_first>
    - `examples/chimeway_demo_host/test/demo_host_web/controllers/feedback_pipeline_e2e_test.exs` — drain pattern, signal assertions
    - `lib/chimeway/dispatch/executor.ex` — outbound path for provider_message_id
    - `lib/chimeway/traces.ex` — `:webhook_received` projection
    - `lib/chimeway/webhooks.ex` — process/4 entry
    - `.planning/phases/55-inbound-feedback-bridge/55-CONTEXT.md` — D-13, D-14, D-17
  </read_first>
  <action>
    Create `test/chimeway/adapters/mailglass_webhook_pipeline_test.exs` (guarded by `Code.ensure_loaded?(Mailglass)`):

    1. Setup: Mailglass.DataCase + Chimeway.Repo sandbox + Mailglass.Adapters.Fake checkout (mirror mailglass_adapter_test.exs setup).

    2. **Outbound step**: Deliver via `Chimeway.Adapters.Mailglass.deliver/2` with Fake adapter; capture returned `provider_message_id` from meta. Run through `Dispatch.Executor.run_delivery/1` on a pending delivery fixture so attempt row stores the same `provider_message_id`.

    3. **Inbound step**: Build Postmark Delivery webhook body/headers where `MessageID` (or metadata message_id) matches the outbound `provider_message_id`. Call `Chimeway.Webhooks.process(Chimeway.Adapters.Mailglass, raw_body, headers, webhook_config)`.

    4. Assert `{:ok, ingress}` with `normalized_status == "delivered"`.

    5. `Oban.drain_queue(queue: :chimeway_delivery, with_scheduled: true)` — assert attempt outcome `:succeeded`, signal event_name `"chimeway.delivery.succeeded"`.

    6. **Trace step**: Build trace for the delivery journey via `Traces` API (follow existing trace test patterns in codebase). Assert timeline includes entry with type/key indicating webhook received and fields `provider_message_id` + `adapter_module` containing `"Chimeway.Adapters.Mailglass"`.

    Extend `mailglass_fixtures.ex` with helper `postmark_delivery_payload_for_message_id/1` if needed.

    Do NOT add demo host routes or controller tests (D-17).
  </action>
  <acceptance_criteria>
    - `test/chimeway/adapters/mailglass_webhook_pipeline_test.exs` exists and compiles
    - Integration test passes: outbound provider_message_id matches webhook correlation
    - After drain: signal count >= 1 with `event_name == "chimeway.delivery.succeeded"`
    - Trace assertion includes webhook_received semantics with provider_message_id
    - `mix test test/chimeway/adapters/mailglass_webhook_pipeline_test.exs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>mix test test/chimeway/adapters/mailglass_webhook_pipeline_test.exs --warnings-as-errors</verify>
</task>

</tasks>

<verification>
- `mix test test/chimeway/adapters/mailglass_adapter_test.exs test/chimeway/adapters/mailglass_webhook_pipeline_test.exs test/chimeway/webhooks_test.exs --warnings-as-errors`
- `mix test --warnings-as-errors` (full suite before phase verify)
</verification>

## PLAN COMPLETE
