---
phase: 64-sigra-auth-flows-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - mix.lock
  - config/test.exs
  - test/test_helper.exs
  - test/support/sigra/test_repo.ex
  - test/support/sigra/user.ex
  - test/support/sigra/user_token.ex
  - test/support/sigra/migrations/20260530000001_create_sigra_auth_tables.exs
  - test/support/sigra/data_case.ex
  - test/support/sigra/fixtures.ex
  - test/chimeway/integrations/sigra_auth_harness_test.exs
  - lib/chimeway/trigger.ex
  - test/chimeway/trigger_sanitization_test.exs
autonomous: true
requirements: [ECOS-09]
must_haves:
  truths:
    - "Chimeway compiles with and without optional `{:sigra, \"~> 0.3\", optional: true, runtime: false}` dep (D-02, D-10)"
    - "`mix ci.test` excludes `@moduletag :sigra` tests; no `mix verify.sigra` alias yet (D-10, D-12)"
    - "When Sigra dep is present, test harness bootstraps `Sigra.TestRepo`, configures `:sigra` repo/schemas, and proves config round-trip (D-10, D-11)"
    - "`@sensitive_keys` strips `url`, `code`, `raw_token`, `magic_link_url` from payload and render_assigns (D-08)"
    - "Harness test proves Sigra module loaded + TestRepo reachable without integration dispatch (Wave 64-02 deferred)"
  artifacts:
    - path: "mix.exs"
      provides: "optional sigra dep, ci.test exclude sigra, SIGRA_PATH helper"
      contains: "--exclude sigra"
    - path: "test/support/sigra/data_case.ex"
      provides: "Shared SQL sandbox for Sigra.TestRepo + Chimeway.Repo"
      contains: "Sigra.DataCase"
    - path: "test/support/sigra/fixtures.ex"
      provides: "User insert helpers + chimeway logger adapter config"
      contains: "configure_chimeway_logger_adapter"
    - path: "test/chimeway/integrations/sigra_auth_harness_test.exs"
      provides: "Wave 64-01 harness stub + config round-trip"
      contains: "@moduletag :sigra"
    - path: "lib/chimeway/trigger.ex"
      provides: "Extended @sensitive_keys for auth-flow redaction"
      contains: "magic_link_url"
    - path: "test/chimeway/trigger_sanitization_test.exs"
      provides: "Unit proof that url/code/raw_token stripped from sanitize_payload/1"
      contains: "sanitize_payload"
  key_links:
    - from: "mix.exs"
      to: "test/chimeway/integrations/sigra_auth_harness_test.exs"
      via: "mix test --only sigra"
      pattern: "--exclude sigra"
    - from: "test/test_helper.exs"
      to: "Sigra.TestRepo"
      via: "conditional bootstrap when Code.ensure_loaded?(Sigra)"
      pattern: "Sigra\\.TestRepo"
    - from: "lib/chimeway/trigger.ex"
      to: "test/chimeway/trigger_sanitization_test.exs"
      via: "@sensitive_keys extension"
      pattern: "raw_token"
    - from: "test/support/sigra/fixtures.ex"
      to: "Sigra.Auth"
      via: "user_schema + user_token_schema config"
      pattern: "request_magic_link"
---

<objective>
Establish the Sigra selective-CI lane, integration test harness, and trigger redaction baseline for Phase 64: optional `sigra` dependency, `@moduletag :sigra` exclusion from default CI, conditional test bootstrap, extended `@sensitive_keys`, and harness test proving Sigra TestRepo reachability without integration dispatch yet.

Purpose: Wave 64-02 (`Sigra.Integrations.Chimeway` + notifiers + lifecycle proof) depends on harness infrastructure and `@sensitive_keys` gaps closed in research §6 (D-08, D-10). This wave delivers infrastructure + redaction baseline only — no `Sigra.Integrations.Chimeway` module yet.

Output: Updated `mix.exs`, `config/test.exs`, `test/test_helper.exs`, `test/support/sigra/*`, `lib/chimeway/trigger.ex`, `test/chimeway/trigger_sanitization_test.exs`, and `test/chimeway/integrations/sigra_auth_harness_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-sigra-auth-flows-core/64-CONTEXT.md
@.planning/phases/64-sigra-auth-flows-core/64-RESEARCH.md
@.planning/phases/64-sigra-auth-flows-core/64-VALIDATION.md
@AGENTS.md

@mix.exs
@config/test.exs
@test/test_helper.exs
@test/support/accrue/data_case.ex
@test/support/accrue/fixtures.ex
@test/chimeway/integrations/accrue_dunning_harness_test.exs
@test/chimeway/integrations/threadline_telemetry_harness_test.exs
@lib/chimeway/trigger.ex
@lib/chimeway/traces.ex
@lib/chimeway/telemetry.ex
@../sigra/lib/sigra/auth.ex
@../sigra/mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optional sigra dep + selective CI exclude (D-10, D-12)</name>
  <files>mix.exs, mix.lock</files>
  <read_first>
    - `mix.exs` — `threadline_deps/0` helper (lines ~136–150); `ci.test` exclude list (line ~68)
    - `.planning/phases/64-sigra-auth-flows-core/64-CONTEXT.md` — D-10, D-12
    - `.planning/phases/64-sigra-auth-flows-core/64-RESEARCH.md` — §7.1 mix.exs table
  </read_first>
  <action>
    In `mix.exs` `deps/0`, append sigra dep after threadline deps:

    ```elixir
    ] ++ accrue_deps() ++ threadline_deps() ++ sigra_deps()
    ```

    Add helpers mirroring `threadline_deps/0` and `threadline_dep/0`:

    ```elixir
    defp sigra_deps do
      if System.get_env("CHIMEWAY_SKIP_SIGRA_DEP") in ["1", "true"] do
        []
      else
        [sigra_dep()]
      end
    end

    defp sigra_dep do
      # Local dev: SIGRA_PATH=../sigra mix deps.get
      case System.get_env("SIGRA_PATH") do
        nil -> {:sigra, "~> 0.3", optional: true, runtime: false}
        path -> {:sigra, path: path, optional: true, runtime: false}
      end
    end
    ```

    Update `aliases/0`:

    1. Change `"ci.test"` from `["cmd env MIX_ENV=test mix test --exclude mailglass --exclude accrue --exclude threadline"]` to include `--exclude sigra`.
    2. Do **not** add `"verify.sigra"` alias — deferred to Phase 66 GATE-07 (D-12).

    Run `mix deps.get` to update `mix.lock` when Sigra is available (hex or `SIGRA_PATH`). Do **not** modify `.github/workflows/ci.yml` in this wave.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` contains `{:sigra,` with `optional: true` and `runtime: false`.
    - `mix.exs` defines `sigra_dep/0` with `SIGRA_PATH` env override and `CHIMEWAY_SKIP_SIGRA_DEP` skip gate.
    - `mix.exs` `"ci.test"` alias includes `--exclude sigra`.
    - `grep -n "verify.sigra" mix.exs` returns no matches (D-12).
    - `mix compile --warnings-as-errors` exits 0 with Sigra dep absent OR present.
    - No changes to `../sigra/lib/sigra/integrations/chimeway.ex` in this task.
  </acceptance_criteria>
  <done>Optional sigra dep and selective CI exclude mirror Accrue/Threadline pattern without verify alias (D-10, D-12).</done>
</task>

<task type="auto">
  <name>Task 2: Sigra test config + test_helper bootstrap (D-10, D-11)</name>
  <files>config/test.exs, test/test_helper.exs</files>
  <read_first>
    - `config/test.exs` — Accrue unconditional harness block (lines ~52–89)
    - `test/test_helper.exs` — Accrue bootstrap block (lines ~35–98)
    - `../sigra/test/fixtures/install_golden/tree/priv/repo/migrations/*auth*` — users + user_tokens schema fields
    - `.planning/phases/64-sigra-auth-flows-core/64-RESEARCH.md` — §7.2, §7.3
  </read_first>
  <action>
    In `config/test.exs`, append unconditional Sigra harness config (mirror Accrue — config loads before optional dep compile):

    ```elixir
    config :sigra, ecto_repos: [Sigra.TestRepo]
    config :sigra, repo: Sigra.TestRepo
    config :sigra, :user_schema, Chimeway.TestSupport.Sigra.User
    config :sigra, :user_token_schema, Chimeway.TestSupport.Sigra.UserToken

    config :sigra, Sigra.TestRepo,
      username:
        System.get_env("POSTGRES_USER") || System.get_env("PGUSER") ||
          System.get_env("USER") || "postgres",
      password: System.get_env("POSTGRES_PASSWORD") || System.get_env("PGPASSWORD") || "postgres",
      hostname: System.get_env("POSTGRES_HOST") || System.get_env("PGHOST") || "localhost",
      database: "chimeway_sigra_test#{System.get_env("MIX_TEST_PARTITION")}",
      pool: Ecto.Adapters.SQL.Sandbox
    ```

    Do **not** set `:sigra, :chimeway` integration config in `config/test.exs` — fixtures set it at runtime in harness/lifecycle tests (Wave 64-02).

    In `test/test_helper.exs`, after the Threadline block, add conditional Sigra bootstrap gated on `Code.ensure_loaded?(Sigra)`:

    1. If `Code.ensure_loaded?(Chimeway)` and not `Code.ensure_loaded?(Sigra.Integrations.Chimeway)`, optionally `Code.compile_file` `lib/sigra/integrations/chimeway.ex` from `SIGRA_PATH` / deps path (Accrue pattern lines ~36–49).
    2. `Application.ensure_all_started(:sigra)` when available.
    3. Resolve migrations path: prefer `test/support/sigra/migrations/*.exs`; fallback empty until Task 3 lands.
    4. `Ecto.Adapters.Postgres.storage_up/1` for `Sigra.TestRepo` config.
    5. Temporarily swap pool to `DBConnection.ConnectionPool` for migration (Mailglass/Accrue pattern).
    6. `Ecto.Migrator.with_repo/2` run migrations when path non-empty.
    7. Restore sandbox pool config; `Sigra.TestRepo.start_link/0`.
    8. `Ecto.Adapters.SQL.Sandbox.mode(Sigra.TestRepo, :manual)`.

    Do **not** enable Sigra Oban delivery workers in test_helper — integration tests use Chimeway Logger adapter (D-11).
  </action>
  <verify>
    <automated>mix test --exclude sigra --exclude accrue --exclude mailglass --exclude threadline --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `config/test.exs` configures `Sigra.TestRepo` with database `chimeway_sigra_test`.
    - `config/test.exs` sets `:user_schema` and `:user_token_schema` to Chimeway harness modules.
    - `test/test_helper.exs` conditionally starts `Sigra.TestRepo` when Sigra is loaded.
    - `mix test --exclude sigra --exclude accrue --exclude mailglass --exclude threadline --warnings-as-errors` exits 0 (default CI lane unaffected).
    - When Sigra dep is fetched, `mix test test/chimeway/integrations/sigra_auth_harness_test.exs --only sigra` does not crash on missing TestRepo after Task 3 migrations exist.
  </acceptance_criteria>
  <done>Sigra TestRepo bootstrap when optional dep is present (D-10, D-11).</done>
</task>

<task type="auto">
  <name>Task 3: Sigra DataCase, fixtures, migrations, and harness stub test (D-10, D-11)</name>
  <files>test/support/sigra/test_repo.ex, test/support/sigra/user.ex, test/support/sigra/user_token.ex, test/support/sigra/migrations/20260530000001_create_sigra_auth_tables.exs, test/support/sigra/data_case.ex, test/support/sigra/fixtures.ex, test/chimeway/integrations/sigra_auth_harness_test.exs</files>
  <read_first>
    - `test/support/accrue/data_case.ex` — dual-repo sandbox owner pattern
    - `test/support/accrue/fixtures.ex` — `configure_chimeway_logger_adapter!/0`
    - `test/chimeway/integrations/threadline_telemetry_harness_test.exs` — conditional module + `@moduletag` pattern
    - `../sigra/lib/sigra/auth.ex` — `request_magic_link/3` opts (`:user_schema`, `:user_token_schema`, `:url_fun`)
    - `.planning/phases/64-sigra-auth-flows-core/64-RESEARCH.md` — §7.4
    - `.planning/phases/64-sigra-auth-flows-core/64-VALIDATION.md` — task 64-01-03
  </read_first>
  <action>
    Create `test/support/sigra/test_repo.ex`:

    ```elixir
    defmodule Sigra.TestRepo do
      use Ecto.Repo,
        otp_app: :sigra,
        adapter: Ecto.Adapters.Postgres
    end
    ```

    Create minimal `Chimeway.TestSupport.Sigra.User` and `UserToken` schemas (integer `id` primary keys; fields aligned with Sigra install golden `users` + `user_tokens` — `email`, `hashed_password`, `confirmed_at`, `user_id`, `token` hashed binary, `context`, `sent_to`, `inserted_at`). Use `binary_id: false` consistently and document in moduledoc.

    Create migration `test/support/sigra/migrations/20260530000001_create_sigra_auth_tables.exs` creating `users` and `user_tokens` tables with indexes Sigra.Auth expects.

    Create `test/support/sigra/data_case.ex` (conditional compile when `Code.ensure_loaded?(Sigra)`):

    - `defmodule Sigra.DataCase` — mirror `Accrue.DataCase` / `Threadline.DataCase` naming (tests use `use Sigra.DataCase`).
    - `use ExUnit.CaseTemplate` with `using` block importing Ecto helpers and `Chimeway.TestSupport.SigraFixtures`.
    - `setup` starts shared SQL sandbox owners for **both** `Sigra.TestRepo` and `Chimeway.Repo` (`shared: true` when not async).
    - Export `allow_repo_access/1` to share sandbox between processes.

    Create `test/support/sigra/fixtures.ex` as `Chimeway.TestSupport.SigraFixtures` (conditional compile):

    - `configure_chimeway_logger_adapter!/0` — `Application.put_env(:chimeway, :channel_adapter_configs, email: {Chimeway.Adapters.Logger, []})`.
    - `insert_user!/1` — inserts harness user with unique email.
    - `url_fun/1` — returns deterministic test magic-link URL `https://example.test/magic/#{token}` for Sigra.Auth opts (used in 64-02; define here for harness).
    - `secret_key_base/0` — fixed 64+ byte secret for confirmation token generation in 64-02.

    Create `test/chimeway/integrations/sigra_auth_harness_test.exs` wrapped in:

    ```elixir
    if Code.ensure_loaded?(Sigra) do
      defmodule Chimeway.Integrations.SigraAuthHarnessTest do
        use Sigra.DataCase, async: false
        @moduletag :sigra
        ...
      end
    end
    ```

    Minimum describe `"sigra auth harness (ECOS-09 wave 1)"` with tests:

    1. **Sigra module loaded** — assert `function_exported?(Sigra.Auth, :request_magic_link, 3)`.
    2. **TestRepo reachable** — `Sigra.TestRepo` insert/count on `users` table returns expected count after `insert_user!/0`.
    3. **Schema config round-trip** — assert `Application.get_env(:sigra, :user_schema) == Chimeway.TestSupport.Sigra.User`.

    Do **not** call `Sigra.Integrations.Chimeway` or assert Chimeway delivery rows — deferred to 64-02.
  </action>
  <verify>
    <automated>mix test test/chimeway/integrations/sigra_auth_harness_test.exs --only sigra --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test/support/sigra/data_case.ex` exists with shared sandbox for Sigra + Chimeway repos.
    - `test/support/sigra/migrations/*.exs` creates `users` and `user_tokens`.
    - `test/chimeway/integrations/sigra_auth_harness_test.exs` uses `@moduletag :sigra`.
    - `mix test test/chimeway/integrations/sigra_auth_harness_test.exs --only sigra --warnings-as-errors` exits 0 when Sigra dep is present.
    - `mix test --exclude sigra --warnings-as-errors` skips sigra tests cleanly.
  </acceptance_criteria>
  <done>Sigra integration harness proves config + TestRepo reachability without Chimeway integration glue (D-11 stub).</done>
</task>

<task type="auto">
  <name>Task 4: Extend @sensitive_keys for auth-flow redaction (D-08)</name>
  <files>lib/chimeway/trigger.ex, test/chimeway/trigger_sanitization_test.exs</files>
  <read_first>
    - `lib/chimeway/trigger.ex` — `@sensitive_keys` (line ~36); `sanitize_payload/1` and `sanitize_render_assigns/1` (lines ~354–368)
    - `.planning/phases/64-sigra-auth-flows-core/64-CONTEXT.md` — D-08
    - `.planning/phases/64-sigra-auth-flows-core/64-RESEARCH.md` — §6.1
    - `test/chimeway/telemetry_integration_test.exs` — `@pii_keys` pattern for refute assertions
  </read_first>
  <action>
    In `lib/chimeway/trigger.ex`:

    1. Extend `@sensitive_keys` from `~w(password token secret)` to include auth-flow keys:

       ```elixir
       @sensitive_keys ~w(password token secret url code raw_token magic_link_url)
       ```

    2. Update moduledoc on `trigger/3` (if present) or module doc to document that `url`, `code`, `raw_token`, and `magic_link_url` are stripped from persisted event payload and notification metadata/render_assigns (D-08 defense-in-depth — identifier-only trigger params remain the primary contract per D-07).

    Create `test/chimeway/trigger_sanitization_test.exs` with describe `"sanitize_payload/1 auth-flow keys (D-08)"`:

    1. **Strips url and code** — assert `Chimeway.Trigger` private sanitizer behavior via public trigger path OR test through a small test-only helper if needed; preferred: trigger a test notifier with params containing `url`, `code`, `raw_token`, `magic_link_url` and assert persisted event payload lacks those keys (use existing test notifier pattern from `trigger_pipeline_test.exs` if available).

    2. **Unit coverage** — if sanitizer is private, add tests in same file using the same code path `trigger/3` uses:

       ```elixir
       params = %{
         "user_id" => "1",
         "url" => "https://secret.example/login/abc",
         "code" => "123456",
         "raw_token" => "raw-secret",
         "magic_link_url" => "https://secret.example/magic/xyz"
       }
       ```

       After trigger (with test notifier), reload event and `refute` payload contains sensitive values; assert `user_id` retained.

    3. **render_assigns** — assert notification `render_assigns` also omits sensitive keys when passed through trigger sanitization path.

    Do **not** add integration lifecycle tests here — deferred to 64-02.
  </action>
  <verify>
    <automated>mix test test/chimeway/trigger_sanitization_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "magic_link_url" lib/chimeway/trigger.ex` matches `@sensitive_keys` (all five auth-flow keys: `url`, `code`, `raw_token`, `magic_link_url`).
    - `mix test test/chimeway/trigger_sanitization_test.exs --warnings-as-errors` exits 0.
    - Tests prove `url`, `code`, `raw_token`, and `magic_link_url` stripped from persisted `chimeway_events.payload` while identifier keys remain.
    - Tests prove notification `render_assigns` omits auth-flow sensitive keys when passed through trigger sanitization path (D-08).
    - `mix test --exclude sigra --warnings-as-errors` still exits 0 (no regression on default lane).
  </acceptance_criteria>
  <done>`@sensitive_keys` extended for auth-flow defense-in-depth before Sigra integration dispatch (D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chimeway repo → optional Sigra dep | Sigra is optional; core library must compile and test without it (D-02, D-10). |
| Test harness → Sigra Test.Repo | Harness uses isolated test DB — not production auth data. |
| Trigger params → chimeway_events.payload | `sanitize_payload/1` is defense-in-depth; identifier-only contract enforced in 64-02 (D-07, D-08). |

## ASVS L1 Security Register

| Threat ID | ASVS Ref | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-64-01 | V14.4.1 | Denial of Service | optional dep breaks default CI | mitigate | `@moduletag :sigra` + `--exclude sigra` on `ci.test`; conditional compile via `Code.ensure_loaded?/1`. |
| T-64-02 | V14.2.1 | Tampering | test config leaks to runtime | mitigate | Sigra repo config in `config/test.exs` only; integration env set in test fixtures. |
| T-64-03 | V8.3.1 | Information Disclosure | url/code in event payload | mitigate | Extend `@sensitive_keys` (D-08); unit tests assert stripping before 64-02 integration path. |
| T-64-04 | V8.3.1 | Information Disclosure | secrets in test fixtures | accept | Harness uses synthetic emails/URLs only; no production credentials. |

**No `high`-severity threats.** Infrastructure + redaction baseline only; no Sigra integration module yet. Block condition not triggered.
</threat_model>

<verification>
- [ ] `mix compile --warnings-as-errors` green
- [ ] `mix test --exclude sigra --exclude accrue --exclude mailglass --exclude threadline --warnings-as-errors` green (default CI lane)
- [ ] `mix test test/chimeway/integrations/sigra_auth_harness_test.exs --only sigra --warnings-as-errors` green when Sigra dep fetched
- [ ] `mix test test/chimeway/trigger_sanitization_test.exs --warnings-as-errors` green
- [ ] `grep -n "verify.sigra" mix.exs` returns no matches
- [ ] `grep -rn "@moduletag :sigra" test/` returns harness test file
- [ ] No edits to `../sigra/lib/sigra/integrations/chimeway.ex` in this plan
</verification>

<success_criteria>
- Optional `sigra` dep + selective CI exclude operational (D-10, D-12)
- Test harness bootstraps Sigra.TestRepo and schema config helpers (D-10, D-11)
- `@sensitive_keys` extended with unit test proof (D-08)
- Default `mix ci.test` unaffected when Sigra dep absent
- No `Sigra.Integrations.Chimeway`, demo host, blueprint, or `mix verify.sigra` in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/64-sigra-auth-flows-core/64-01-SUMMARY.md`
</output>
