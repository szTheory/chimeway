---
phase: 06-delivery-planning-and-policy-checkpoint-repair
plan: "06-02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - lib/chimeway/deliveries.ex
  - lib/chimeway/dispatch/executor.ex
  - lib/chimeway/dispatch/sync.ex
  - lib/chimeway/dispatch/oban_worker.ex
  - lib/chimeway/traces.ex
autonomous: true
requirements:
  - POLC-02
  - INTG-02
user_setup: []
must_haves:
  truths:
    - "`Chimeway.Deliveries.suppress_delivery` supports checkpoint source metadata (`planning` or `perform`) while preserving `suppression_reason`."
    - "Sync and Oban worker adapter execution paths use one shared execution module (`Chimeway.Dispatch.Executor`) for outcome classification."
    - "Outcome classification mapping is identical in sync and Oban worker paths (`:ok -> :succeeded`, `:temporary -> :failed`, `:permanent -> :rejected`, `:bounced -> :bounced`)."
    - "`Chimeway.Traces.explain_delivery/1` timeline suppression entries include checkpoint source detail when available."
    - "Existing terminal-state guards remain in both sync and worker flows."
  artifacts:
    - path: lib/chimeway/deliveries.ex
      provides: "Checkpoint-aware suppression persistence helpers"
      exports:
        - "suppress_delivery/2"
        - "suppress_delivery/3"
    - path: lib/chimeway/dispatch/executor.ex
      provides: "Shared adapter execution and attempt recording for sync/Oban worker parity"
      exports:
        - "run_delivery/1"
    - path: lib/chimeway/traces.ex
      provides: "Suppression timeline includes policy checkpoint source metadata"
  key_links:
    - from: lib/chimeway/dispatch/sync.ex
      to: lib/chimeway/dispatch/executor.ex
      via: "sync dispatch delegates adapter attempt execution to shared executor"
      pattern: "Dispatch\\.Executor\\.run_delivery"
    - from: lib/chimeway/dispatch/oban_worker.ex
      to: lib/chimeway/dispatch/executor.ex
      via: "worker delegates adapter attempt execution to shared executor"
      pattern: "Dispatch\\.Executor\\.run_delivery"
    - from: lib/chimeway/traces.ex
      to: lib/chimeway/deliveries.ex
      via: "suppression checkpoint source read from delivery metadata"
      pattern: "policy_checkpoint"
---

<objective>
Eliminate execution-path drift by consolidating sync and Oban worker adapter-attempt logic, and make suppression checkpoint provenance queryable in trace output.

Purpose: Preserve POLC-02 dual-checkpoint semantics while ensuring INTG-02 adapter behavior remains consistent regardless of execution mode.
Output: checkpoint-aware suppression persistence, shared dispatch executor, and trace timeline enhancement.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-delivery-planning-and-policy-checkpoint-repair/06-CONTEXT.md
@.planning/phases/06-delivery-planning-and-policy-checkpoint-repair/06-RESEARCH.md
@lib/chimeway/deliveries.ex
@lib/chimeway/dispatch/sync.ex
@lib/chimeway/dispatch/oban_worker.ex
@lib/chimeway/policy.ex
@lib/chimeway/traces.ex
</context>

<tasks>

<task type="auto">
  <name>Task 06-02-01: Add checkpoint-aware suppression persistence</name>
  <files>lib/chimeway/deliveries.ex</files>
  <read_first>lib/chimeway/deliveries.ex, lib/chimeway/delivery.ex, lib/chimeway/policy.ex, .planning/phases/06-delivery-planning-and-policy-checkpoint-repair/06-CONTEXT.md</read_first>
  <action>Enhance suppression persistence to track checkpoint source without breaking existing call sites:

1. Keep existing API:
```elixir
suppress_delivery(delivery, reason)
```
2. Add new arity with options:
```elixir
suppress_delivery(delivery, reason, opts)
```
3. Accept `checkpoint:` option values `:planning` or `:perform`; default to `:perform`.
4. Merge checkpoint into delivery metadata map as:
```elixir
%{"policy_checkpoint" => "planning" | "perform"}
```
5. Preserve existing behavior:
   - `status` becomes `:suppressed`
   - `suppression_reason` persists as string reason
   - no schema migration required (`metadata` field already exists)
6. Ensure metadata merge is non-destructive for existing keys.

Do not change `suppress_delivery/2` callers in this task; compatibility is required for incremental rollout.</action>
  <verify>rg "def suppress_delivery\\(%Delivery\\{} = delivery, reason, opts\\)" lib/chimeway/deliveries.ex && rg "policy_checkpoint" lib/chimeway/deliveries.ex && rg "Atom\\.to_string\\(reason\\)" lib/chimeway/deliveries.ex && mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - `suppress_delivery/3` exists with `checkpoint:` support
    - `suppress_delivery/2` remains valid and backward-compatible
    - suppression metadata stores `policy_checkpoint` as `"planning"` or `"perform"`
    - existing metadata keys are preserved on update
    - project compiles cleanly
  </acceptance_criteria>
  <done>Delivery suppression persistence now captures checkpoint source without API breakage.</done>
</task>

<task type="auto">
  <name>Task 06-02-02: Extract shared dispatch executor and wire sync/worker parity</name>
  <files>lib/chimeway/dispatch/executor.ex, lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban_worker.ex</files>
  <read_first>lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban_worker.ex, lib/chimeway/deliveries.ex, lib/chimeway/adapter.ex, .planning/phases/06-delivery-planning-and-policy-checkpoint-repair/06-PATTERNS.md</read_first>
  <action>Create `lib/chimeway/dispatch/executor.ex` and move shared adapter-attempt logic there:

1. Implement:
```elixir
def run_delivery(%Delivery{} = delivery) do
  with {:ok, dispatched} <- Deliveries.transition_status(delivery, :dispatched) do
    adapter = Application.get_env(:chimeway, :adapter, Chimeway.Adapters.Logger)
    channel_key = String.to_atom("adapter_#{delivery.channel}")
    adapter_config = Application.get_env(:chimeway, channel_key, [])
    {attempt_outcome, provider_response} = classify(adapter.deliver(dispatched, adapter_config))
    Deliveries.record_attempt(dispatched, %{outcome: attempt_outcome, provider_response: provider_response})
  end
end
```
2. Keep classification mapping exactly aligned with existing sync/worker behavior.
3. Update `sync.ex` to delegate adapter execution to `Dispatch.Executor.run_delivery/1`.
4. Update `oban_worker.ex` to delegate adapter execution to the same function.
5. Keep policy-evaluate and terminal-state checks in current modules (do not move them into executor).

Ensure both paths still return their current public return shapes (`{:ok, delivery}` in sync result tuples and `:ok | {:error, ...}` in worker perform flow) by adapting executor return at call sites.</action>
  <verify>test -f lib/chimeway/dispatch/executor.ex && rg "def run_delivery" lib/chimeway/dispatch/executor.ex && rg "Dispatch\\.Executor\\.run_delivery" lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban_worker.ex && rg "temporary|permanent|bounced" lib/chimeway/dispatch/executor.ex && mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs</verify>
  <acceptance_criteria>
    - Shared executor module exists and classifies adapter outcomes once
    - Sync and worker modules both call shared executor
    - Policy and terminal-state checks remain in sync/worker (not removed)
    - Existing sync and Oban worker tests pass after refactor
  </acceptance_criteria>
  <done>Sync and Oban worker execution semantics are unified via one shared executor module.</done>
</task>

<task type="auto">
  <name>Task 06-02-03: Surface suppression checkpoint source in trace timeline</name>
  <files>lib/chimeway/traces.ex</files>
  <read_first>lib/chimeway/traces.ex, lib/chimeway/traces/explanation.ex, lib/chimeway/delivery.ex, .planning/phases/06-delivery-planning-and-policy-checkpoint-repair/06-CONTEXT.md</read_first>
  <action>Update suppression explanation details in `lib/chimeway/traces.ex`:

1. In `build_timeline/4`, when building `:suppressed` event detail, include checkpoint source:
```elixir
detail: %{
  reason: delivery.suppression_reason,
  policy_checkpoint: Map.get(delivery.metadata || %{}, "policy_checkpoint", "unknown")
}
```
2. Keep existing shape and ordering of timeline events unchanged.
3. Do not change `Explanation` struct fields in this task; checkpoint source is timeline-level detail only.

This preserves backwards compatibility while improving operator trace clarity for planning vs perform suppressions.</action>
  <verify>rg "policy_checkpoint" lib/chimeway/traces.ex && rg "event: :suppressed" lib/chimeway/traces.ex && mix test test/chimeway/integration/delivery_lifecycle_test.exs</verify>
  <acceptance_criteria>
    - Suppressed timeline entries include `policy_checkpoint` in detail map
    - Timeline ordering remains deterministic and unchanged
    - Existing trace lifecycle integration test remains green
  </acceptance_criteria>
  <done>Trace timeline explains whether suppression happened at planning or perform checkpoint.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-06-02-EXECUTOR-DRIFT
    severity: high
    threat: "Sync and Oban worker paths classify adapter outcomes differently after partial refactor."
    mitigation: "Both modules must call one shared executor function for adapter outcome classification and attempt persistence."
    mitigation_check: "rg 'Dispatch\\.Executor\\.run_delivery' lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban_worker.ex"
  - id: TM-06-02-CHECKPOINT-LOSS
    severity: high
    threat: "Suppression checkpoint metadata is not persisted, making policy decision provenance non-explainable."
    mitigation: "Persist `policy_checkpoint` in delivery metadata via `suppress_delivery/3` and include it in traces timeline."
    mitigation_check: "rg 'policy_checkpoint' lib/chimeway/deliveries.ex lib/chimeway/traces.ex"
  - id: TM-06-02-METADATA-OVERWRITE
    severity: medium
    threat: "Suppression update clobbers existing delivery metadata keys."
    mitigation: "Merge checkpoint key into existing metadata map; do not replace map wholesale."
    mitigation_check: "rg 'Map\\.merge|Map\\.put' lib/chimeway/deliveries.ex"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix compile --warnings-as-errors`
- [ ] `mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs`
- [ ] `mix test test/chimeway/integration/delivery_lifecycle_test.exs`
- [ ] `test -f lib/chimeway/dispatch/executor.ex`
- [ ] `rg "Dispatch\\.Executor\\.run_delivery" lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban_worker.ex`
- [ ] `rg "policy_checkpoint" lib/chimeway/deliveries.ex lib/chimeway/traces.ex`
- [ ] `rg "Dispatch\\.Executor\\.run_delivery" lib/chimeway/dispatch/sync.ex lib/chimeway/dispatch/oban_worker.ex`
- [ ] `rg "policy_checkpoint" lib/chimeway/deliveries.ex lib/chimeway/traces.ex`
</verification>

<success_criteria>
- Suppression checkpoint source is durably persisted and queryable.
- Sync and Oban worker adapter-attempt execution logic is unified.
- Trace timeline can distinguish planning-time suppression from perform-time suppression.
- Existing dispatch and integration tests remain stable after refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/06-delivery-planning-and-policy-checkpoint-repair/06-02-SUMMARY.md`.
</output>
