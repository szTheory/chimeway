---
phase: 06-delivery-planning-and-policy-checkpoint-repair
plan: "06-03"
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - test/chimeway/trigger_pipeline_test.exs
  - test/chimeway/dispatch/sync_test.exs
  - test/chimeway/dispatch/oban_test.exs
  - test/chimeway/policy_test.exs
  - test/chimeway/integration/delivery_lifecycle_test.exs
  - test/support/chimeway/dispatch_helpers.ex
autonomous: true
requirements:
  - DLVR-01
  - INTG-02
  - POLC-01
  - POLC-02
user_setup: []
must_haves:
  truths:
    - "Trigger fanout tests assert per-recipient, per-channel delivery row planning instead of single hardcoded in-app rows."
    - "Sync and Oban tests both prove planning-time suppression for disabled channels before adapter/worker execution."
    - "Oban tests explicitly verify suppressed deliveries are not enqueued."
    - "Integration tests cover standard outbound success flow with fanout and durable attempt records."
    - "Parity assertions compare sync and Oban outcomes for equivalent inputs (status, suppression reason, attempt presence)."
  artifacts:
    - path: test/chimeway/trigger_pipeline_test.exs
      provides: "Notifier channels callback and fallback fanout contract tests"
    - path: test/chimeway/dispatch/sync_test.exs
      provides: "Sync planning-time policy and fanout behavior tests"
    - path: test/chimeway/dispatch/oban_test.exs
      provides: "Oban enqueue-time policy gating and worker parity tests"
    - path: test/chimeway/integration/delivery_lifecycle_test.exs
      provides: "End-to-end durable lifecycle assertions for standard outbound fanout flow"
    - path: test/support/chimeway/dispatch_helpers.ex
      provides: "Reusable fixture helpers for parity scenarios and reduced duplication"
  key_links:
    - from: test/chimeway/dispatch/sync_test.exs
      to: test/chimeway/dispatch/oban_test.exs
      via: "paired parity scenarios for identical policy and fanout inputs"
      pattern: "channel_disabled|already_read|planning"
    - from: test/chimeway/integration/delivery_lifecycle_test.exs
      to: lib/chimeway/delivery_planning.ex
      via: "integration assertions on per-channel planned deliveries and attempt outcomes"
      pattern: "delivery_count|channel"
---

<objective>
Lock in regression protection for Phase 6 by adding parity-focused tests across trigger, sync, and Oban flows, plus an end-to-end outbound success path that asserts fanout and planning-time policy enforcement.

Purpose: Make DLVR-01 / INTG-02 / POLC-01 / POLC-02 verifiable with stable automated evidence.
Output: Expanded unit + integration test matrix covering channel fanout, planning checkpoint behavior, and sync/Oban parity.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-delivery-planning-and-policy-checkpoint-repair/06-CONTEXT.md
@.planning/phases/06-delivery-planning-and-policy-checkpoint-repair/06-RESEARCH.md
@test/chimeway/trigger_pipeline_test.exs
@test/chimeway/dispatch/sync_test.exs
@test/chimeway/dispatch/oban_test.exs
@test/chimeway/policy_test.exs
@test/chimeway/integration/delivery_lifecycle_test.exs
@test/support/chimeway/dispatch_helpers.ex
</context>

<tasks>

<task type="auto">
  <name>Task 06-03-01: Expand trigger and fanout tests for channels callback contract</name>
  <files>test/chimeway/trigger_pipeline_test.exs, test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <read_first>test/chimeway/trigger_pipeline_test.exs, test/chimeway/integration/delivery_lifecycle_test.exs, lib/chimeway/notifier.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/trigger.ex</read_first>
  <action>Add deterministic fanout tests that prove channel expansion is explicit and durable:

1. In `trigger_pipeline_test.exs`:
   - Add notifier fixture implementing `channels/2` returning `[:in_app, :email]`.
   - Assert trigger still returns deduped recipient identities.
   - Assert resulting delivery rows contain both `"in_app"` and `"email"` per recipient.

2. Add fallback compatibility case:
   - Notifier without `channels/2` still creates exactly one `"in_app"` delivery row.

3. In `delivery_lifecycle_test.exs`:
   - Add scenario with multiple channels for one recipient and assert:
     - one notification row
     - two delivery rows (one per channel)
     - attempt rows for dispatchable channels only.

Use explicit query assertions (`Repo.aggregate`, channel set equality) rather than list-order assumptions.</action>
  <verify>mix test test/chimeway/trigger_pipeline_test.exs test/chimeway/integration/delivery_lifecycle_test.exs && rg "channels/2|channels\\(" test/chimeway/trigger_pipeline_test.exs && rg "in_app|email" test/chimeway/trigger_pipeline_test.exs</verify>
  <acceptance_criteria>
    - Trigger tests include explicit `channels/2` notifier fixture
    - Fanout asserts per-recipient, per-channel delivery row counts
    - Fallback path test proves no-breaking behavior for notifiers without `channels/2`
    - Integration test asserts standard outbound fanout durable chain
  </acceptance_criteria>
  <done>Fanout contract is covered by trigger-level and integration-level automated tests.</done>
</task>

<task type="auto">
  <name>Task 06-03-02: Add sync/Oban planning-time policy parity tests</name>
  <files>test/chimeway/dispatch/sync_test.exs, test/chimeway/dispatch/oban_test.exs, test/chimeway/policy_test.exs</files>
  <read_first>test/chimeway/dispatch/sync_test.exs, test/chimeway/dispatch/oban_test.exs, test/chimeway/policy_test.exs, lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban.ex, lib/chimeway/policy.ex</read_first>
  <action>Add parity assertions for planning-time policy enforcement:

1. Sync tests:
   - Disabled channel preference before dispatch yields suppressed delivery with reason `"channel_disabled"`.
   - Assert no adapter call is recorded for suppressed planning rows.

2. Oban tests:
   - Disabled channel preference before enqueue yields suppressed delivery row during planning.
   - Assert no job is enqueued for that suppressed delivery (`refute_enqueued` with delivery id).
   - Keep perform-time tests for delayed fallback in worker.

3. Policy tests:
   - Add focused test proving planner checkpoint path uses `Policy.evaluate(delivery, [])`.
   - Assert checkpoint metadata source equals `"planning"` when suppression occurs at planning.

Use shared helper(s) in `test/support/chimeway/dispatch_helpers.ex` to avoid copy/paste fixture drift.</action>
  <verify>mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/policy_test.exs && rg "channel_disabled" test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs && rg "refute_enqueued" test/chimeway/dispatch/oban_test.exs && rg "policy_checkpoint|planning" test/chimeway/policy_test.exs</verify>
  <acceptance_criteria>
    - Sync tests assert planning suppression before adapter call
    - Oban tests assert planning suppression before enqueue (no job row for suppressed delivery)
    - Policy tests verify planning checkpoint metadata persistence
    - Shared fixtures reduce duplicated setup logic
  </acceptance_criteria>
  <done>Planning-time policy parity is explicitly tested across sync and Oban dispatch paths.</done>
</task>

<task type="auto">
  <name>Task 06-03-03: Build phase-level regression matrix and run full suite</name>
  <files>test/chimeway/dispatch/sync_test.exs, test/chimeway/dispatch/oban_test.exs, test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <read_first>test/chimeway/dispatch/sync_test.exs, test/chimeway/dispatch/oban_test.exs, test/chimeway/integration/delivery_lifecycle_test.exs, .planning/phases/06-delivery-planning-and-policy-checkpoint-repair/06-VALIDATION.md</read_first>
  <action>Finalize an explicit regression matrix and execute verification commands:

1. Ensure matrix coverage includes these scenarios in tests:
   - fanout success (multi-channel)
   - planning suppression (`channel_disabled`)
   - perform suppression (`already_read`)
   - fallback channels behavior (no channels callback)
   - sync vs Oban parity on status/reason/outcome

2. Add short test comments that map each scenario to requirement IDs (`DLVR-01`, `INTG-02`, `POLC-01`, `POLC-02`).

3. Run verification commands in this exact order:
   - `mix test test/chimeway/trigger_pipeline_test.exs test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/policy_test.exs`
   - `mix test test/chimeway/integration/delivery_lifecycle_test.exs`
   - `mix test`

4. If flaky failure appears in Oban tests, stabilize with deterministic setup/cleanup (`Chimeway.Adapters.Test.clear`, explicit dispatcher env reset in `on_exit`).</action>
  <verify>mix test test/chimeway/trigger_pipeline_test.exs test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs test/chimeway/policy_test.exs && mix test test/chimeway/integration/delivery_lifecycle_test.exs && mix test</verify>
  <acceptance_criteria>
    - Regression matrix scenarios are represented in test suite
    - Requirement IDs are traceable in scenario descriptions/comments
    - Targeted suite passes and full suite passes
    - No flaky ordering dependency remains in sync/Oban tests
  </acceptance_criteria>
  <done>Phase 6 behavior is locked by stable targeted tests and a full green suite.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-06-03-PARITY-GAP
    severity: high
    threat: "Tests validate sync path only, allowing Oban planning-time policy regressions to ship."
    mitigation: "Paired sync and Oban parity scenarios must exist for planning suppression and fanout behavior."
    mitigation_check: "rg 'planning|channel_disabled|refute_enqueued' test/chimeway/dispatch/oban_test.exs"
  - id: TM-06-03-SPINE-REGRESSION
    severity: high
    threat: "Fanout refactor breaks standard outbound success chain without immediate detection."
    mitigation: "Integration lifecycle test must assert event->notification->delivery->attempt chain for fanout case."
    mitigation_check: "rg 'Scenario|delivery_count|attempt' test/chimeway/integration/delivery_lifecycle_test.exs"
  - id: TM-06-03-FLAKE-RISK
    severity: medium
    threat: "Oban/job test state leakage causes nondeterministic CI failures."
    mitigation: "Explicit adapter/env reset and queue assertions per test; avoid shared mutable state."
    mitigation_check: "rg 'on_exit|clear\\(\\)|Application\\.put_env' test/chimeway/dispatch/oban_test.exs"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix test test/chimeway/trigger_pipeline_test.exs`
- [ ] `mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs`
- [ ] `mix test test/chimeway/policy_test.exs`
- [ ] `mix test test/chimeway/integration/delivery_lifecycle_test.exs`
- [ ] `mix test`
- [ ] `rg "DLVR-01|INTG-02|POLC-01|POLC-02" test/chimeway/**/*.exs`
- [ ] `rg "planning|channel_disabled|refute_enqueued" test/chimeway/dispatch/oban_test.exs`
- [ ] `rg "Scenario|delivery_count|attempt" test/chimeway/integration/delivery_lifecycle_test.exs`
</verification>

<success_criteria>
- Test suite proves per-recipient, per-channel fanout behavior.
- Sync and Oban paths both enforce planning-time policy checks before execution.
- Standard outbound success flow has end-to-end fanout coverage with durable attempt evidence.
- Full suite is green with stable, non-flaky dispatch tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-delivery-planning-and-policy-checkpoint-repair/06-03-SUMMARY.md`.
</output>
