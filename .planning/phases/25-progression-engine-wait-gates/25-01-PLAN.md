---
phase: 25-progression-engine-wait-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/notifier.ex
  - lib/chimeway/workflows/progression_outcome.ex
  - test/chimeway/notifier_contract_test.exs
  - test/chimeway/workflows/progression_outcome_test.exs
autonomous: true
requirements:
  - WRK-02
must_haves:
  truths:
    - "Workflow step configs persist explicit outgoing progression rules with one active-step source of truth."
    - "Curated workflow outcomes resolve from persisted delivery facts into a small stable vocabulary or `:not_branchable_yet`."
    - "Invalid progression declarations fail normalization before any workflow definition is persisted."
  artifacts:
    - path: "lib/chimeway/notifier.ex"
      provides: "Workflow progress-rule normalization and validation inside persisted step config"
      min_lines: 60
    - path: "lib/chimeway/workflows/progression_outcome.ex"
      provides: "Pure delivery-facts-to-workflow-outcome mapper"
      min_lines: 50
    - path: "test/chimeway/workflows/progression_outcome_test.exs"
      provides: "Unit proof for curated vocabulary and non-branchable states"
      min_lines: 70
  key_links:
    - from: "lib/chimeway/notifier.ex"
      to: "lib/chimeway/workflows/progression_outcome.ex"
      via: "normalized `progress` config uses the same curated outcome vocabulary that runtime progression resolves"
      pattern: "progress|delivered|temporary_failure|retries_exhausted|permanent_failure|bounced"
    - from: "test/chimeway/notifier_contract_test.exs"
      to: "lib/chimeway/notifier.ex"
      via: "contract tests lock the persisted step-config shape for replay-safe progression rules"
      pattern: "progress|anchor|to_step|outcome"
---

<objective>
Lock the workflow progression contract before runtime wiring.

Purpose: Phase 25 depends on one replay-safe rule shape and one curated workflow-outcome vocabulary. This plan defines both at the declaration boundary so later engine and worker plans can implement progression without inventing semantics on the fly.
Output: validated `progress` step config support, pure outcome mapping module, and unit/contract tests that freeze the vocabulary and invalid-shape failures.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/25-progression-engine-wait-gates/25-CONTEXT.md
@.planning/phases/25-progression-engine-wait-gates/25-RESEARCH.md
@.planning/phases/25-progression-engine-wait-gates/25-PATTERNS.md
@.planning/phases/25-progression-engine-wait-gates/25-VALIDATION.md
@AGENTS.md
@lib/chimeway/notifier.ex
@lib/chimeway/delivery.ex
@lib/chimeway/deliveries.ex
@test/chimeway/notifier_contract_test.exs

<interfaces>
From `lib/chimeway/notifier.ex`:
```elixir
@spec normalize_workflow_declaration(map()) :: {:ok, workflow_resolution()} | {:error, term()}
def normalize_workflow_declaration(%{} = declaration)
```

Persisted workflow step shape today:
```elixir
%{
  step_key: normalized_step_key,
  step_order: normalized_step_order,
  channel: normalized_channel,
  config: normalized_config
}
```

From `lib/chimeway/deliveries.ex`:
```elixir
defp terminal_or_failed_transition(delivery, :succeeded, _error_class), do: transition_status(delivery, :succeeded)
defp terminal_or_failed_transition(delivery, _outcome, "permanent"), do: cancel_with_reason(delivery, "permanent_failure")
defp terminal_or_failed_transition(delivery, _outcome, "bounced"), do: cancel_with_reason(delivery, "bounced")
defp terminal_or_failed_transition(delivery, _outcome, _error_class), do: transition_status(delivery, :failed)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED coverage for progression rule normalization and curated outcome mapping</name>
  <files>test/chimeway/notifier_contract_test.exs, test/chimeway/workflows/progression_outcome_test.exs</files>
  <read_first>
    - test/chimeway/notifier_contract_test.exs
    - lib/chimeway/notifier.ex
    - lib/chimeway/delivery.ex
    - lib/chimeway/deliveries.ex
    - .planning/phases/25-progression-engine-wait-gates/25-CONTEXT.md
    - .planning/phases/25-progression-engine-wait-gates/25-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: per D-06/D-08 a step config with `progress` persists explicit outgoing rules under string-keyed fields and rejects blank or mixed rule shapes.
    - Test 2: per D-03/D-04 the mapper returns only `delivered`, `suppressed`, `temporary_failure`, `retries_exhausted`, `permanent_failure`, or `bounced`.
    - Test 3: per D-05 `:pending`, `:dispatched`, and `:digested` delivery states return `:not_branchable_yet`.
  </behavior>
  <action>Extend `test/chimeway/notifier_contract_test.exs` with workflow declarations whose step `config` contains a string-keyed `progress` list. Lock the concrete rule shape to `%{"kind" => "wait_until", "anchor" => "prior_delivery_terminal_at", "delay_seconds" => 1800, "to_step" => "email"}` and `%{"kind" => "on_outcome", "outcome" => "bounced", "to_step" => "sms"}` per D-01, D-03, D-06, and D-08. Add a new `test/chimeway/workflows/progression_outcome_test.exs` that builds delivery structs and optional latest-attempt evidence, then asserts exact mapper outputs for the six curated outcomes plus `:not_branchable_yet`.</action>
  <acceptance_criteria>
    - `test/chimeway/notifier_contract_test.exs` contains `"kind" => "wait_until"`
    - `test/chimeway/notifier_contract_test.exs` contains `"anchor" => "prior_delivery_terminal_at"`
    - `test/chimeway/notifier_contract_test.exs` contains `"kind" => "on_outcome"`
    - `test/chimeway/workflows/progression_outcome_test.exs` contains `:not_branchable_yet`
    - `test/chimeway/workflows/progression_outcome_test.exs` contains `"retries_exhausted"`
    - `mix test test/chimeway/notifier_contract_test.exs test/chimeway/workflows/progression_outcome_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/notifier_contract_test.exs test/chimeway/workflows/progression_outcome_test.exs --trace</automated>
  </verify>
  <done>The tests freeze the data-first rule DSL and the curated workflow-outcome vocabulary required by WRK-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize step progress rules and implement the pure progression outcome mapper</name>
  <files>lib/chimeway/notifier.ex, lib/chimeway/workflows/progression_outcome.ex</files>
  <read_first>
    - lib/chimeway/notifier.ex
    - test/chimeway/notifier_contract_test.exs
    - test/chimeway/workflows/progression_outcome_test.exs
    - lib/chimeway/delivery.ex
    - lib/chimeway/deliveries.ex
    - .planning/phases/25-progression-engine-wait-gates/25-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `Notifier.normalize_workflow_declaration/1` preserves valid `progress` rules with string-keyed persisted fields and rejects invalid anchors, outcomes, blank `to_step`, and mixed wait/outcome rule bodies.
    - Test 2: `Chimeway.Workflows.ProgressionOutcome.from_delivery/2` derives branchable workflow outcomes from canonical delivery status plus suppression/attempt evidence.
    - Test 3: non-terminal rows stay unbranchable even if attempt evidence exists.
  </behavior>
  <action>Create `lib/chimeway/workflows/progression_outcome.ex` with a pure public function `from_delivery/2` that returns `{:branchable, outcome, evidence}` or `:not_branchable_yet`, using D-04's exact vocabulary and persisting evidence fields named `delivery_status`, `suppression_reason`, `attempt_outcome`, and `attempt_error_class`. Update `lib/chimeway/notifier.ex` so `normalize_workflow_step/1` validates optional `config["progress"]` as a list of rule maps. Accept only `"wait_until"` rules with exact keys `anchor`, `delay_seconds`, and `to_step`, where `anchor` must equal `"prior_delivery_terminal_at"` per D-01, and only `"on_outcome"` rules with exact keys `outcome` and `to_step`, where `outcome` is one of the curated D-04 values. Preserve string-keyed config for replay safety per D-08 and return tagged `{:workflow_resolution_failed, reason}` errors on bad shapes.</action>
  <acceptance_criteria>
    - `lib/chimeway/workflows/progression_outcome.ex` contains `def from_delivery`
    - `lib/chimeway/workflows/progression_outcome.ex` contains `:not_branchable_yet`
    - `lib/chimeway/notifier.ex` contains `"prior_delivery_terminal_at"`
    - `lib/chimeway/notifier.ex` contains `"wait_until"`
    - `lib/chimeway/notifier.ex` contains `"on_outcome"`
    - `lib/chimeway/notifier.ex` contains `workflow_resolution_failed`
    - `mix test test/chimeway/notifier_contract_test.exs test/chimeway/workflows/progression_outcome_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/notifier_contract_test.exs test/chimeway/workflows/progression_outcome_test.exs --trace</automated>
  </verify>
  <done>Workflow declarations now carry replay-safe progression rules and the runtime has one pure, stable mapper for branch outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notifier callback -> persisted workflow definition | Untrusted notifier-supplied rule data crosses into durable workflow state |
| canonical delivery facts -> workflow branch vocabulary | Progression semantics must not be spoofed by queue-only or ad hoc attempt detail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | T | `lib/chimeway/notifier.ex` progress config normalization | mitigate | Validate exact rule keys, anchors, outcomes, and `to_step` fields before persistence; reject mixed or blank rule shapes per D-06/D-08. |
| T-25-02 | R | `lib/chimeway/workflows/progression_outcome.ex` | mitigate | Emit one curated outcome plus raw evidence fields so operators can replay why a branch was chosen from durable facts alone per D-12. |
| T-25-03 | E | notifier workflow declarations | mitigate | Keep rules data-first and string-keyed; do not permit lambdas, callback re-entry, or queue-state predicates per D-08. |
</threat_model>

<verification>
Run `mix test test/chimeway/notifier_contract_test.exs test/chimeway/workflows/progression_outcome_test.exs --trace`.
</verification>

<success_criteria>
The workflow declaration layer persists explicit progression rules, and the curated workflow-outcome vocabulary is centrally implemented and covered by unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/25-progression-engine-wait-gates/25-01-SUMMARY.md`
</output>
