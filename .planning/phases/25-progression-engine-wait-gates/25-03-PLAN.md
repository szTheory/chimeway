---
phase: 25-progression-engine-wait-gates
plan: 03
type: execute
wave: 3
depends_on:
  - 25-02
files_modified:
  - lib/chimeway/dispatch/workflow_progression_worker.ex
  - lib/chimeway/workflows/progression.ex
  - test/chimeway/dispatch/workflow_progression_worker_test.exs
  - test/chimeway/reliability/workflow_progression_race_test.exs
autonomous: true
requirements:
  - ESC-03
  - WRK-02
must_haves:
  truths:
    - "Oban-backed due progression wakes one durable workflow run by stable id and delegates all correctness to the shared progression seam."
    - "Concurrent due-run claims and duplicate worker retries emit at most one next-step delivery."
    - "Non-Oban and Oban hosts share the same internal progression semantics because both call the same due-run engine."
  artifacts:
    - path: "lib/chimeway/dispatch/workflow_progression_worker.ex"
      provides: "Thin scheduled worker that reloads by workflow_run_id and delegates to the progression service"
      min_lines: 60
    - path: "test/chimeway/dispatch/workflow_progression_worker_test.exs"
      provides: "Worker-level proof for thin delegation, noop handling, and single enqueue semantics"
      min_lines: 80
    - path: "test/chimeway/reliability/workflow_progression_race_test.exs"
      provides: "Concurrency regression proof for one-winner due-step progression"
      min_lines: 90
  key_links:
    - from: "lib/chimeway/dispatch/workflow_progression_worker.ex"
      to: "lib/chimeway/workflows/progression.ex"
      via: "worker args carry only `workflow_run_id` and delegate to `progress_run/2`"
      pattern: "workflow_run_id|progress_run"
    - from: "test/chimeway/reliability/workflow_progression_race_test.exs"
      to: "lib/chimeway/workflows/progression.ex"
      via: "concurrent due-run entry proves one-winner row claims and single next-step emission"
      pattern: "Task.async_stream|workflow_run_id|next-step"
---

<objective>
Finish the due-run execution boundary and race-proof the progression engine.

Purpose: Phase 25 is not complete until due waiting steps can be woken automatically and duplicate claims collapse safely under retries or concurrency. This plan adds the thin worker boundary and proves the engine remains idempotent under load.
Output: workflow progression worker, due-run scheduling/delegation support in the progression service, and worker/race regression suites.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/25-progression-engine-wait-gates/25-CONTEXT.md
@.planning/phases/25-progression-engine-wait-gates/25-RESEARCH.md
@.planning/phases/25-progression-engine-wait-gates/25-PATTERNS.md
@.planning/phases/25-progression-engine-wait-gates/25-VALIDATION.md
@AGENTS.md
@lib/chimeway/dispatch/deferred_resume_worker.ex
@lib/chimeway/workflows/progression.ex
@test/chimeway/orchestration/deferred_resume_test.exs
@test/chimeway/reliability/duplicate_protection_test.exs

<interfaces>
From `lib/chimeway/dispatch/deferred_resume_worker.ex`:
```elixir
def perform(%Oban.Job{args: %{"delivery_id" => delivery_id}})
```

Worker transaction pattern:
```elixir
Multi.new()
|> Multi.run(:resume_delivery, resume_delivery_step(delivery_id))
|> Multi.run(:dispatch_job, &dispatch_job_step/2)
|> Repo.transaction()
```

From `lib/chimeway/workflows/progression.ex` after Plan 25-02:
```elixir
def progress_run(workflow_run_or_id, opts \\ [])
def progress_due_runs(opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED worker and concurrency coverage for due-run progression</name>
  <files>test/chimeway/dispatch/workflow_progression_worker_test.exs, test/chimeway/reliability/workflow_progression_race_test.exs</files>
  <read_first>
    - test/chimeway/orchestration/deferred_resume_test.exs
    - test/chimeway/reliability/duplicate_protection_test.exs
    - lib/chimeway/workflows/progression.ex
    - .planning/phases/25-progression-engine-wait-gates/25-CONTEXT.md
    - .planning/phases/25-progression-engine-wait-gates/25-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: the worker takes only `workflow_run_id`, delegates to the internal progression seam, and no-ops safely when the run is already advanced or not due.
    - Test 2: duplicate worker executions enqueue or emit only one next-step delivery.
    - Test 3: concurrent direct calls into the due-run progression seam collapse to one winner and stable workflow state.
  </behavior>
  <action>Create `test/chimeway/dispatch/workflow_progression_worker_test.exs` mirroring the deferred-resume worker style: schedule a waiting workflow run, perform the worker twice with the same `%{workflow_run_id: run.id}`, and assert one next-step delivery plus durable noop on the second run. Create `test/chimeway/reliability/workflow_progression_race_test.exs` using the duplicate-protection concurrency structure to launch repeated `Task.async_stream` calls against the same due workflow run and assert exact counts for workflow transitions, next-step deliveries, and worker-visible noop results.</action>
  <acceptance_criteria>
    - `test/chimeway/dispatch/workflow_progression_worker_test.exs` contains `workflow_run_id`
    - `test/chimeway/dispatch/workflow_progression_worker_test.exs` contains `perform_job`
    - `test/chimeway/reliability/workflow_progression_race_test.exs` contains `Task.async_stream`
    - `test/chimeway/reliability/workflow_progression_race_test.exs` contains `workflow_run_id`
    - `mix test test/chimeway/dispatch/workflow_progression_worker_test.exs test/chimeway/reliability/workflow_progression_race_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/dispatch/workflow_progression_worker_test.exs test/chimeway/reliability/workflow_progression_race_test.exs --trace</automated>
  </verify>
  <done>The tests prove that due-run execution stays duplicate-safe at both the worker boundary and the concurrency boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the thin progression worker and finalize due-run scheduling semantics</name>
  <files>lib/chimeway/dispatch/workflow_progression_worker.ex, lib/chimeway/workflows/progression.ex</files>
  <read_first>
    - lib/chimeway/dispatch/deferred_resume_worker.ex
    - lib/chimeway/workflows/progression.ex
    - test/chimeway/dispatch/workflow_progression_worker_test.exs
    - test/chimeway/reliability/workflow_progression_race_test.exs
    - .planning/phases/25-progression-engine-wait-gates/25-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the worker remains thin and all business decisions stay in the progression service.
    - Test 2: waiting runs schedule or wake by workflow-run id, while non-Oban/manual callers still reuse `progress_due_runs/1`.
    - Test 3: duplicate due-run claims no-op once the run state or step cursor has already changed.
  </behavior>
  <action>Create `lib/chimeway/dispatch/workflow_progression_worker.ex` as an Oban worker patterned after `DeferredResumeWorker`, but with job args limited to `%{"workflow_run_id" => workflow_run_id}` per D-10. Delegate to `Chimeway.Workflows.Progression.progress_run/2` inside a `Multi`, normalize `{:ok, {:advanced, run}}`, `{:ok, {:waiting, run}}`, and `{:ok, {:noop, run}}` to `:ok`, and never encode rule data or delivery facts in job args. Extend `lib/chimeway/workflows/progression.ex` so entering a `wait_until` state optionally schedules `WorkflowProgressionWorker` at the persisted `due_at` when the configured dispatcher is `Chimeway.Dispatch.Oban`, while leaving `progress_due_runs/1` as the shared manual seam for sync-first hosts per D-11. Keep row-level claim predicates or `FOR UPDATE` locking around due-run advancement so the second worker or concurrent caller sees the changed run state/current step and returns noop without emitting another delivery.</action>
  <acceptance_criteria>
    - `lib/chimeway/dispatch/workflow_progression_worker.ex` contains `workflow_run_id`
    - `lib/chimeway/dispatch/workflow_progression_worker.ex` contains `Chimeway.Workflows.Progression`
    - `lib/chimeway/dispatch/workflow_progression_worker.ex` contains `def perform`
    - `lib/chimeway/workflows/progression.ex` contains `WorkflowProgressionWorker`
    - `lib/chimeway/workflows/progression.ex` contains `progress_due_runs`
    - `lib/chimeway/workflows/progression.ex` contains `due_at`
    - `mix test test/chimeway/dispatch/workflow_progression_worker_test.exs test/chimeway/reliability/workflow_progression_race_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/dispatch/workflow_progression_worker_test.exs test/chimeway/reliability/workflow_progression_race_test.exs --trace</automated>
  </verify>
  <done>Due waiting runs can be progressed automatically or manually through one shared engine, and duplicate claims collapse safely under retries and concurrency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Oban job args -> workflow progression service | Scheduler input must remain stable-id only and never become the correctness boundary |
| concurrent callers -> workflow run current state | Multiple retries or manual ticks may target the same due run simultaneously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | S | `lib/chimeway/dispatch/workflow_progression_worker.ex` | mitigate | Accept only `workflow_run_id` job args and reload all workflow/delivery truth from Chimeway-owned rows before acting. |
| T-25-08 | D | `lib/chimeway/workflows/progression.ex` due-run scheduling | mitigate | Keep `progress_due_runs/1` and worker execution on the same locking/claim path so duplicate jobs and repeated host calls converge to noop. |
| T-25-09 | R | workflow wait-gate execution history | mitigate | Persist transition reasons and due/anchor facts before or alongside advancement so retries leave an auditable durable trail instead of queue archaeology. |
</threat_model>

<verification>
Run `mix test test/chimeway/dispatch/workflow_progression_worker_test.exs test/chimeway/reliability/workflow_progression_race_test.exs --trace` and then `mix test test/chimeway/**/*workflow* test/chimeway/**/*delivery*`.
</verification>

<success_criteria>
Due waiting runs progress through a thin worker or manual tick seam with identical semantics, and concurrency tests prove next-step delivery emission remains one-winner and duplicate-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/25-progression-engine-wait-gates/25-03-SUMMARY.md`
</output>
