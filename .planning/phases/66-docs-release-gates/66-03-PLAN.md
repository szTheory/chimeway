---
phase: 66-docs-release-gates
plan: 03
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - test/chimeway/doc_contract_test.exs
autonomous: true
requirements: [DOCS-11]

must_haves:
  truths:
    - "Doc-contract test fails if Threadline guide omits any of the 8 required strings"
    - "Doc-contract test fails if Sigra guide omits any of the 11 required strings"
    - "Doc-contract test fails if either guide contains @recipe_forbidden_strings"
    - "Doc-contract test fails if Sigra guide contains :raw_token or :magic_link_url in atom syntax"
    - "Hexdocs extras doc-contract test also validates threadline and sigra guide presence and ordering"
    - "mix ci.verify_gates exits 0 with all guide contracts passing"
  artifacts:
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "Two new describe blocks for DOCS-11 + updated hexdocs extras block"
      contains: "threadline integration guide doc contract"
  key_links:
    - from: "test/chimeway/doc_contract_test.exs @threadline_integration_guide"
      to: "guides/introduction/threadline-integration.md"
      via: "Path.expand + File.read! in setup"
      pattern: "threadline-integration.md"
    - from: "test/chimeway/doc_contract_test.exs @sigra_integration_guide"
      to: "guides/introduction/sigra-auth-integration.md"
      via: "Path.expand + File.read! in setup"
      pattern: "sigra-auth-integration.md"
---

<objective>
Add two new describe blocks to test/chimeway/doc_contract_test.exs that lock the Threadline and Sigra integration guide content as truth. Also update the hexdocs extras describe block to validate both new guides appear in mix.exs docs extras in the correct order.

Purpose: DOCS-11 — guide regressions must fail CI via the verify_gates lane, not silently drift.
Output: Two new describe blocks in doc_contract_test.exs (DOCS-10 Threadline + Sigra) + updated hexdocs extras block.

Depends on: Plan 01 (guides must exist for File.read! in setup to succeed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/66-docs-release-gates/66-CONTEXT.md
@.planning/phases/66-docs-release-gates/66-PATTERNS.md

@.planning/phases/66-docs-release-gates/66-01-SUMMARY.md
</context>

<interfaces>
<!-- Extracted from test/chimeway/doc_contract_test.exs — executor must read before editing. -->

Module-level attributes (lines 104-108) inherited by all new describe blocks:
  @recipe_forbidden_strings ~w(
    stop_conditions
    Workflows.Workers
    Chimeway.Trigger.trigger
  )

Existing module-level file path attributes pattern (lines 502, 601):
  @accrue_integration_guide Path.expand("../../guides/introduction/accrue-dunning-integration.md", __DIR__)
  @inbox_integration_guide Path.expand("../../guides/introduction/inbox-integration.md", __DIR__)

New attributes to add (as module-level, before the new describe blocks):
  @threadline_integration_guide Path.expand("../../guides/introduction/threadline-integration.md", __DIR__)
  @sigra_integration_guide Path.expand("../../guides/introduction/sigra-auth-integration.md", __DIR__)

Full describe block skeleton (from lines 504-599, accrue pattern):
  describe "..." do
    setup do
      content = File.read!(@attribute_name)
      %{content: content}
    end
    for forbidden <- @recipe_forbidden_strings do
      test "forbids #{forbidden} in [guide name]", %{content: content} do
        refute String.contains?(content, unquote(forbidden)), "..."
      end
    end
    test "forbids Chimeway.Workflow module (not Workflows) in [guide name]", %{content: content} do
      refute Regex.match?(~r/Chimeway\.Workflow(?![s])/, content), "..."
    end
    @required ~w(...)
    for required <- @required do
      test "requires #{required} in [guide name]", %{content: content} do
        assert String.contains?(content, unquote(required)), "..."
      end
    end
    test "sections appear in golden-path order...", %{content: content} do  [headings order test]
  end

Space-escaping pattern in @required ~w() (from line 535-536):
  config\ :chimeway  (backslash-escaped space keeps as one token — matches substring "config :chimeway")
  mix\ verify.threadline  (same)
  mix\ verify.sigra  (same)

Hexdocs extras describe block (lines 908-963):
  @integration_guides ~w(
    guides/introduction/mailglass-integration.md
    guides/introduction/accrue-dunning-integration.md
    guides/introduction/inbox-integration.md
  )
  — add two more entries —
  Ordering tests use :binary.match and assert index_A < index_B pattern.
  New ordering tests needed: inbox_index < threadline_index, threadline_index < sigra_index
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Threadline and Sigra doc-contract describe blocks to doc_contract_test.exs</name>
  <files>test/chimeway/doc_contract_test.exs</files>
  <read_first>
    - test/chimeway/doc_contract_test.exs (read lines 490-965 covering the accrue describe block through hexdocs extras block — executor needs full view of both patterns and the insertion points)
    - .planning/phases/66-docs-release-gates/66-CONTEXT.md (D-04 Threadline @required 8 strings; D-05 Sigra @required 11 strings and @sigra_forbidden atom forms)
    - .planning/phases/66-docs-release-gates/66-PATTERNS.md (Pattern 2: Doc-Contract Describe Block Structure with full code examples)
    - guides/introduction/threadline-integration.md (verify file exists from Plan 01)
    - guides/introduction/sigra-auth-integration.md (verify file exists from Plan 01)
  </read_first>
  <action>
    Edit test/chimeway/doc_contract_test.exs by appending after the last existing describe block (the "hexdocs extras doc contract" block at lines 908-963) and also updating the hexdocs extras block itself.

    Step A — Find insertion point: Locate the end of the inbox integration guide describe block (the block after the accrue block). The two new guide describe blocks go after the inbox describe block, BEFORE the hexdocs extras describe block.

    Step B — Add module-level file path attributes. Place these two attribute declarations immediately before the threadline describe block (after the inbox describe block ends):
      @threadline_integration_guide Path.expand("../../guides/introduction/threadline-integration.md", __DIR__)
      @sigra_integration_guide Path.expand("../../guides/introduction/sigra-auth-integration.md", __DIR__)

    Step C — Add Threadline describe block (per D-04):
    describe "threadline integration guide doc contract (DOCS-10)" do
      setup do: %{content: File.read!(@threadline_integration_guide)}

      [for forbidden <- @recipe_forbidden_strings loop — same pattern as accrue block]
      [test "forbids Chimeway.Workflow module (not Workflows) in threadline integration guide" — same regex pattern]

      @required ~w(
        Chimeway.Telemetry.ThreadlineReporter
        attach/0
        config\ :chimeway
        correlation_id
        notification_suppressed
        DemoHost.Seeds.seed_threadline_notification
        /admin/chimeway
        mix\ verify.threadline
      )
      [for required <- @required loop]

      [test "sections appear in golden-path order..." with headings:
        "## 1. Dependencies", "## 2. Attach reporter", "## 3. What gets recorded", "## 4. Verification"
        — asserts all headings present and their byte indices are ascending]
    end

    Step D — Add Sigra describe block (per D-05):
    describe "sigra auth integration guide doc contract (DOCS-10)" do
      setup do: %{content: File.read!(@sigra_integration_guide)}

      [for forbidden <- @recipe_forbidden_strings loop]
      [test "forbids Chimeway.Workflow module (not Workflows) in sigra auth integration guide"]

      @sigra_forbidden ~w(:raw_token :magic_link_url)
      for forbidden <- @sigra_forbidden do
        test "forbids #{forbidden} in sigra auth integration guide", %{content: content} do
          refute String.contains?(content, unquote(forbidden)),
                 "sigra auth integration guide must not reference #{unquote(forbidden)} in code examples"
        end
      end

      @required ~w(
        Sigra.Integrations.Chimeway
        sigra.auth.magic_link
        sigra.auth.confirmation_code
        Chimeway.trigger
        idempotency_key
        tenant_id
        DemoHost.Seeds.seed_sigra
        /admin/chimeway
        mix\ verify.sigra
        SIGRA_PATH
        orchestrates
      )
      [for required <- @required loop]

      [test "sections appear in golden-path order..." with headings:
        "## 1. Dependencies", "## 2. Integration seam", "## 3. Notifier reference", "## 4. Auth event triggers", "## 5. Verification"
        — asserts headings present and byte indices are ascending]
    end

    Step E — Update hexdocs extras describe block: In the existing @integration_guides ~w() attribute (lines 914-918), append two new entries:
      guides/introduction/threadline-integration.md
      guides/introduction/sigra-auth-integration.md

    Add two new ordering tests after the existing "inbox after accrue" ordering test:
      test "lists threadline integration guide after inbox integration guide in extras"
        — inbox_index < threadline_index using :binary.match on "guides/introduction/inbox-integration.md" and "guides/introduction/threadline-integration.md"
      test "lists sigra integration guide after threadline integration guide in extras"
        — threadline_index < sigra_index using :binary.match on "guides/introduction/threadline-integration.md" and "guides/introduction/sigra-auth-integration.md"

    CRITICAL implementation notes:
    - The @sigra_forbidden and @required attributes INSIDE a describe block shadow the module-level @required from the previous describe block. Elixir module attributes in describe blocks are local to that block's compile context. Verify this follows the exact pattern from the accrue block (lines 528-544) where @required is redeclared inside the describe block.
    - Use `setup do\n  content = File.read!(@threadline_integration_guide)\n  %{content: content}\nend` not a one-liner if the existing blocks use the multi-line form.
    - Do not redefine @recipe_forbidden_strings — it is inherited at module level.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --only describe:"threadline integration guide doc contract" 2>&1 | tail -5; MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --only describe:"sigra auth integration guide doc contract" 2>&1 | tail -5; MIX_ENV=test mix ci.verify_gates 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test/chimeway/doc_contract_test.exs contains describe "threadline integration guide doc contract (DOCS-10)" block
    - test/chimeway/doc_contract_test.exs contains describe "sigra auth integration guide doc contract (DOCS-10)" block
    - Threadline describe block has @required with all 8 strings from D-04 (including config\ :chimeway and mix\ verify.threadline with backslash-escaped spaces)
    - Sigra describe block has @sigra_forbidden ~w(:raw_token :magic_link_url) loop with refute String.contains? assertions
    - Sigra describe block has @required with all 11 strings from D-05
    - Both describe blocks have sections-in-order tests asserting correct heading byte-index ordering
    - hexdocs extras @integration_guides ~w() contains threadline-integration.md and sigra-auth-integration.md
    - Two new ordering tests for inbox < threadline < sigra
    - `MIX_ENV=test mix test test/chimeway/doc_contract_test.exs` exits 0 (all tests pass)
    - `mix ci.verify_gates` exits 0
  </acceptance_criteria>
  <done>Both doc-contract describe blocks active and passing; hexdocs extras block validates all 5 integration guides in order; mix ci.verify_gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc_contract_test → guide files | File.read! reads local guide files; no external I/O |
| @sigra_forbidden atom forms | Test enforces guide does not expose atom-syntax dangerous params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-06 | Tampering | @sigra_forbidden ~w(:raw_token :magic_link_url) | mitigate | Colon-prefixed atom form in ~w() means test only fires on `:raw_token` (with colon) in guide content, not on prose "raw_token" (no colon) — prevents false positives per RESEARCH.md pitfall 4 |
| T-66-07 | Information Disclosure | config\ :chimeway in @required | accept | Verifies guide teaches correct config key; no security risk from test file content |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | No package installs — test file edits only |
</threat_model>

<verification>
After task completes:

1. `MIX_ENV=test mix test test/chimeway/doc_contract_test.exs` exits 0
2. `mix ci.verify_gates` exits 0
3. `grep -c "threadline integration guide doc contract" test/chimeway/doc_contract_test.exs` = 1
4. `grep -c "sigra auth integration guide doc contract" test/chimeway/doc_contract_test.exs` = 1
5. `grep -c ":raw_token" test/chimeway/doc_contract_test.exs` >= 1 (the @sigra_forbidden entry)
6. `grep -c "threadline-integration.md" test/chimeway/doc_contract_test.exs` >= 2 (attribute + @integration_guides)
</verification>

<success_criteria>
All guide doc-contract describe blocks pass. mix ci.verify_gates green. Both new guides are locked by their respective contract tests — any future regression that removes a required string or adds a forbidden string will fail the verify_gates CI lane.
</success_criteria>

<output>
Create .planning/phases/66-docs-release-gates/66-03-SUMMARY.md when done
</output>
