---
phase: 92-reliability-triage-determinism
plan: 03
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - .github/workflows/ci.yml
  - test/chimeway/release_gate_contract_test.exs
  - test/chimeway/ci_observability_contract_test.exs
  - .planning/CI-HARDENING-BACKLOG.md
  - examples/chimeway_demo_host/test/mix/tasks/demo_up_test.exs
autonomous: true
requirements: [REL-02, REL-03]
user_setup: []

must_haves:
  truths:
    - "A test_seed_zero lane exists in ci.yml, gated on needs.resolve_tiers.outputs.run_nightly == 'true', running `mix test --seed 0` with the ci.test excludes (mailglass/accrue/threadline/sigra) and --warnings-as-errors (REL-03)."
    - "test_seed_zero runs ONLY on the nightly tier; PR and push runs keep the random ExUnit seed — no seed-0 lane is added to ci-gate or pr-gate (REL-03 adjacency edge)."
    - "With --seed 0 the default suite runs in a fixed deterministic order and passes identically to random-seed runs, guarding against test-ordering coupling going forward (REL-03 ordering edge)."
    - "nightly-gate needs and its aggregate-gate.sh token list both include test_seed_zero / TEST_SEED_ZERO, and release_gate_contract_test.exs asserts the updated 5-lane nightly composition in the SAME change (REL-03)."
    - "test_seed_zero is exempt from @build_lanes (like test_floor_1_17 / nightly_cold_build), with the exemption documented in ci_observability_contract_test.exs (REL-03)."
    - "CI-HARDENING-BACKLOG.md #2 (demo.up --check) and #3 (Accrue path-dep) are each recorded verified-fixed with their pinned root-cause mechanisms, OR quarantined behind a linked GitHub tracking issue — no undocumented CI-only reproduction gap remains (REL-02)."
    - "The verified-fixed vs quarantine decision is driven by a log-assertable fact: both lanes' conclusions on the phase HEAD push run — all success ⇒ verified-fixed, otherwise ⇒ quarantine + tracking issue linked in the backlog (REL-02 decision fork)."
    - statement: "test_seed_zero runs the full default suite (never an empty selection); `mix test --seed 0` with zero matching tests still exits 0."
      verification: backstop
  prohibitions:
    - "Must not add the seed-0 lane to ci-gate or the PR path, and must not pin a global ExUnit seed (green-washes coupling)."
    - "Must not mark backlog #2/#3 resolved without a green-on-push proof and a documented mechanism (no silent CI-only reproduction gap)."
    - "Must not remove the load-bearing job-level DB-URL env or the :dev pre-warm step from the demo lanes when tightening the interim timeout tag (Pitfall 5)."
    - "Must not change lib/ runtime behavior (milestone invariant)."
    - "Must not interpolate untrusted gh API strings into shell eval."
  artifacts:
    - .github/workflows/ci.yml (test_seed_zero job + nightly-gate wiring)
    - test/chimeway/release_gate_contract_test.exs (nightly-gate 5-lane assertions)
    - test/chimeway/ci_observability_contract_test.exs (test_seed_zero exemption assertion)
    - .planning/CI-HARDENING-BACKLOG.md (#2/#3 resolution)
  key_links:
    - "test_seed_zero → nightly-gate needs → aggregate-gate.sh TEST_SEED_ZERO token"
    - "release_gate_contract_test.exs nightly-gate assertion ↔ ci.yml nightly-gate needs (must change together or CI breaks)"
---

<objective>
Deliver REL-03 (nightly `--seed 0` ordering guard) and REL-02 (verify-or-quarantine the two CI-only
backlog issues), the two CI-facing halves of Phase 92.

Purpose: REL-03 extends the Phase 90 nightly tier with a `test_seed_zero` lane that keeps the Phase 89
CONC-04 ordering shakeout enforced going forward, while the random ExUnit seed stays on PR/push (a pinned
global seed would green-wash coupling). REL-02 closes the two open `CI-HARDENING-BACKLOG.md` entries: both
lanes are already green on every push and both root causes are pinned in RESEARCH.md — the deliverable is an
honest, log-assertable verified-fixed write-up (with a quarantine fallback) so no silent CI-only reproduction
gap remains.
Output: `test_seed_zero` lane + nightly-gate/contract wiring + backlog resolution + a tightened interim timeout tag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-reliability-triage-determinism/92-RESEARCH.md
@.planning/CI-HARDENING-BACKLOG.md
</context>

## Artifacts this phase produces (full phase set — for source-grounding drift exclusion)

New symbols/files created across all of Phase 92 (this plan creates/updates the ones marked **[92-03]**):
- [92-01] `Chimeway.TestSupport.EnvHelper` (`test/support/env_helper.ex`) + `put_env_isolated/3`; `test/chimeway/test_support/env_helper_test.exs`; async-adoption assertion in `ci_observability_contract_test.exs`; `policy_test.exs` adoption
- [92-02] `scripts/ci/reliability-report.sh`, `test/fixtures/ci/run_list_sample.json`, `test/chimeway/ci_reliability_contract_test.exs`, `.planning/CI-RELIABILITY-REPORT.md`
- **[92-03]** `test_seed_zero` job in `.github/workflows/ci.yml`
- **[92-03]** new nightly-gate 5-lane assertions in `test/chimeway/release_gate_contract_test.exs`
- **[92-03]** `test_seed_zero` exemption assertion + note in `test/chimeway/ci_observability_contract_test.exs`
- **[92-03]** `#2`/`#3` resolution in `.planning/CI-HARDENING-BACKLOG.md`; tightened `@tag timeout` in `examples/chimeway_demo_host/test/mix/tasks/demo_up_test.exs`

<tasks>

<task type="auto">
  <name>Task 1: add nightly-only test_seed_zero lane + wire nightly-gate + update contract assertions (same change)</name>
  <files>.github/workflows/ci.yml, test/chimeway/release_gate_contract_test.exs, test/chimeway/ci_observability_contract_test.exs</files>
  <read_first>
    - .github/workflows/ci.yml — the `test_floor_1_17` job block (~lines 1182–1225, its postgres service, `needs: [resolve_tiers]`, and `if: needs.resolve_tiers.outputs.run_floor` gating) to mirror; the `nightly-gate` job (~lines 1258–1271, needs list + `aggregate-gate.sh NIGHTLY_COLD_BUILD TEST TEST_FLOOR_1_17 VERIFY_ADMIN`); and `resolve_tiers` `run_nightly` output (~lines 46–69).
    - test/chimeway/release_gate_contract_test.exs — the nightly-gate contract (~lines 475–498): the EXACT needs-string assertion `[resolve_tiers, nightly_cold_build, test, test_floor_1_17, verify_admin]`, the `~w(...)` lane loop, and the `aggregate-gate.sh NIGHTLY_COLD_BUILD TEST TEST_FLOOR_1_17 VERIFY_ADMIN` token assertion — all must be updated in this change.
    - test/chimeway/ci_observability_contract_test.exs — the `@build_lanes` list (~line 28) and the Phase-90 exemption block + tests (~lines 19–28, 315–345) for `test_floor_1_17` / `nightly_cold_build`; model the `test_seed_zero` exemption on those.
    - scripts/ci/aggregate-gate.sh — the token→env-var gate loop the new TEST_SEED_ZERO token plugs into.
    - mix.exs — the `ci.test` alias (line 76) for the exact excludes: `--exclude mailglass --exclude accrue --exclude threadline --exclude sigra --warnings-as-errors`.
  </read_first>
  <action>
    In `ci.yml`, add a `test_seed_zero` job named "Test ordering guard (--seed 0)" modeled on `test_floor_1_17`
    but gated on `if: needs.resolve_tiers.outputs.run_nightly == 'true'` (nightly-only): same postgres service,
    `needs: [resolve_tiers]`, checkout + setup-beam via `.tool-versions` (`version-type: strict`), deps.get,
    compile, ecto.create/migrate, then `mix test --seed 0 --exclude mailglass --exclude accrue --exclude threadline
    --exclude sigra --warnings-as-errors`. Keep its own `test-seed-zero-` cache namespace (do NOT reuse another
    lane's key). Do NOT add it to `ci-gate` or `pr-gate`, and do NOT touch the random-seed default on any push/PR
    lane. Wire it into `nightly-gate`: add `test_seed_zero` to the needs list (new exact list
    `[resolve_tiers, nightly_cold_build, test, test_floor_1_17, verify_admin, test_seed_zero]`), add a
    `TEST_SEED_ZERO: ${{ needs.test_seed_zero.result }}` env, and append `TEST_SEED_ZERO` to the
    `aggregate-gate.sh` token list. Update `release_gate_contract_test.exs` in the SAME change: the exact needs-string
    assertion, the `~w(...)` lane loop, and the `aggregate-gate.sh ... TEST_SEED_ZERO` token assertion to the new
    5-nightly-lane composition. In `ci_observability_contract_test.exs`, add an assertion that `test_seed_zero` is
    NOT a member of `@build_lanes` (exempt like `test_floor_1_17` / `nightly_cold_build`, being nightly and
    ordering-focused, not an OBS-parity target) and document the exemption in the existing exemption comment.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs test/chimeway/ci_observability_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `release_gate_contract_test.exs` asserts nightly-gate needs == `[resolve_tiers, nightly_cold_build, test, test_floor_1_17, verify_admin, test_seed_zero]` and that the aggregate-gate token list includes `TEST_SEED_ZERO`; the test file is green.
    - `ci_observability_contract_test.exs` asserts `refute "test_seed_zero" in @build_lanes` and the exemption is green.
    - `grep -n "run_nightly == 'true'" .github/workflows/ci.yml` matches the `test_seed_zero` job block; `grep -c "seed 0" .github/workflows/ci.yml` >= 1.
    - `ci-gate` needs is unchanged at 14 lanes (existing `extract_ci_gate_needs` assertion still green); no seed-0 lane on the PR path.
  </acceptance_criteria>
  <reversibility rating="reversible">The ci.yml lane + gate wiring and its coupled contract assertions are a single revertible commit; the risk is forgetting to update release_gate_contract_test.exs in the same change (CI would go red), so both edits land together.</reversibility>
  <done>The nightly-only test_seed_zero lane exists, is wired into nightly-gate needs + aggregate-gate tokens, is exempt from @build_lanes, and both contract tests are green with the updated composition.</done>
</task>

<task type="auto">
  <name>Task 2: prove test_seed_zero green on a real nightly dispatch (live log-assert)</name>
  <files>.planning/phases/92-reliability-triage-determinism/92-03-SUMMARY.md</files>
  <read_first>
    - .github/workflows/ci.yml — the `workflow_dispatch` `run_nightly` input (~lines 12–17) and how `resolve_tiers` maps it to `run_nightly=true`.
    - test/chimeway/release_gate_contract_test.exs — confirm the nightly-gate composition the live run must satisfy.
  </read_first>
  <precondition>`gh auth status` succeeds with workflow-dispatch permission on szTheory/chimeway AND the phase branch is pushed — the nightly tier only runs via `schedule` or a `-f run_nightly=true` dispatch.</precondition>
  <action>
    Trigger the nightly tier on the phase HEAD with `gh workflow run ci.yml --ref <phase-branch> -f run_nightly=true`,
    wait for the run to complete, then assert the `test_seed_zero` lane and the `nightly-gate` both concluded
    `success` by reading `gh run view <id> --json jobs -q` (select by job name). Record the run id + permalink in
    the plan SUMMARY. If the lane is red, capture the failure (it would indicate real ordering coupling the guard
    is designed to catch) and stop for triage rather than papering over it.
  </action>
  <verify>
    <automated>gh run view "$(gh run list --workflow=ci.yml --event workflow_dispatch --limit 1 --json databaseId -q '.[0].databaseId')" --json jobs -q '.jobs[] | select(.name|test("seed 0")) | .conclusion' | grep -qx success && echo SEED0_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - A `-f run_nightly=true` dispatch on the phase HEAD shows `test_seed_zero` (job name "Test ordering guard (--seed 0)") concluding `success`.
    - `nightly-gate` on that dispatch concludes `success` (aggregates the 5 nightly lanes including the new one).
    - The run id + `actions/runs/<id>` permalink is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The seed-0 ordering guard is proven green on a real nightly dispatch, with the run permalink recorded.</done>
</task>

<task type="auto">
  <name>Task 3: REL-02 — verify-fixed both backlog lanes on push, resolve the backlog, tighten the interim timeout tag</name>
  <files>.planning/CI-HARDENING-BACKLOG.md, examples/chimeway_demo_host/test/mix/tasks/demo_up_test.exs</files>
  <read_first>
    - .planning/CI-HARDENING-BACKLOG.md — issue #2 (`demo.up --check` dev-DB hang) and #3 (Accrue path-dep compile), including the "interim/partial — revisit" note.
    - .planning/phases/92-reliability-triage-determinism/92-RESEARCH.md — "State of the Art" pins both root causes: #2 = the `:dev` subprocess inherits the job DB-URL env so it connects to the created test DB + the `:dev` pre-warm step removes the cold-compile timeout; #3 = full-tree `mix deps.compile` produces `accrue.app` in dependency order.
    - examples/chimeway_demo_host/test/mix/tasks/demo_up_test.exs — the interim `@tag timeout: 300_000` on the "JOUR-05 mix demo.up --check exits 0" test to tighten.
  </read_first>
  <precondition>The phase's commits are pushed to `main` and a push-triggered CI run has completed — REL-02's proof reads the `verify_accrue` / `verify_example` / `verify_journeys` lane conclusions on that run.</precondition>
  <action>
    Read the lane conclusions on the phase HEAD push run via `gh run view <push-run> --json jobs -q` for
    `verify_accrue`, `verify_example`, and `verify_journeys`. Decision fork (log-assertable): if all three are
    `success` ⇒ mark #2 and #3 VERIFIED-FIXED in `CI-HARDENING-BACKLOG.md`, documenting the pinned mechanisms from
    RESEARCH.md (#2: inherited job DB-URL env routes the `:dev` subprocess to the created test DB + the `:dev`
    pre-warm step, both load-bearing; #3: full-tree `mix deps.compile` produces `accrue.app` in dependency order,
    plus the nested `ACCRUE_PATH` layout) and cite the run permalink; otherwise ⇒ `gh issue create` a tracking issue
    per lane and link its number back into the backlog (quarantine — no silent gap). Then tighten the interim
    `@tag timeout: 300_000` on the JOUR-05 test to a warm-appropriate `120_000` (compile is now pre-warmed; runs ~2 s
    warm). Do NOT remove the job-level DB-URL env or the `:dev` pre-warm step (both load-bearing per Pitfall 5).
    After tightening, re-assert the demo lanes stay `success` on a fresh push so the tighten cannot silently
    reintroduce a red lane.
  </action>
  <verify>
    <automated>bash -c 'set -e; run=$(gh run list --branch main --event push --workflow=ci.yml --limit 1 --json databaseId -q ".[0].databaseId"); for j in verify_accrue verify_example verify_journeys; do c=$(gh run view "$run" --json jobs -q ".jobs[]|select(.name|test(\"$j\"))|.conclusion"); echo "$j=$c"; [ "$c" = success ]; done; grep -Eiq "verified.?fixed|#[0-9]+" .planning/CI-HARDENING-BACKLOG.md'</automated>
  </verify>
  <acceptance_criteria>
    - On the phase HEAD push run, `verify_accrue`, `verify_example`, and `verify_journeys` all conclude `success` (log-assertable via `gh run view`).
    - `.planning/CI-HARDENING-BACKLOG.md` records #2 and #3 as verified-fixed with the pinned mechanisms + a run permalink, OR links a tracking issue number per quarantined lane — no undocumented CI-only reproduction gap remains.
    - The JOUR-05 `@tag timeout` is tightened from `300_000` to `120_000`, and the demo lanes stay `success` on a fresh push after the tighten (re-asserted).
    - The load-bearing job-level DB-URL env and `:dev` pre-warm step are intact (`grep` confirms they were not removed).
  </acceptance_criteria>
  <reversibility rating="reversible">Backlog is documentation; the timeout tighten is a one-line revertible edit guarded by a re-asserted green push.</reversibility>
  <done>Both backlog lanes are proven green on the phase HEAD push, the backlog records verified-fixed (or quarantine + linked issue) with mechanisms, and the interim timeout tag is tightened without regressing the lanes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions API → gh assertion scripts | Untrusted run/branch/job titles cross into shell selection |
| new CI job (test_seed_zero) → runner secrets | A new nightly job runs with whatever permissions it declares/inherits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-92-06 | Elevation of Privilege | test_seed_zero job permissions | low | mitigate | Job inherits the top-level `permissions: contents: read` (Phase 91/QUAL-03); it needs no escalation (checkout + test only), so it declares none |
| T-92-07 | Tampering | gh dispatch/assert scripts selecting by job name | low | mitigate | Select via `--json jobs -q` (jq); no `eval`; quoted expansions around `$run`/`$j` |
| T-92-08 | Information Disclosure | nightly lane logs / demo lanes | low | accept | No new secret emission; the seed-0 lane mirrors existing test-lane hygiene; the load-bearing DB-URL env is a non-secret local test URL already present on push lanes |

No package installs occur in this plan (zero npm/pip/hex/crates), so no supply-chain install threat applies. No `lib/` runtime behavior changes.
</threat_model>

<verification>
- `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs test/chimeway/ci_observability_contract_test.exs --warnings-as-errors` exits 0 (updated nightly composition + exemption).
- A `-f run_nightly=true` dispatch shows `test_seed_zero` and `nightly-gate` green.
- `verify_accrue` / `verify_example` / `verify_journeys` are `success` on the phase HEAD push; the backlog records the resolution; the timeout tag is tightened without regression.
- `ci-gate` needs unchanged at 14 lanes; no seed-0 lane on the PR path; random seed retained on push/PR.
</verification>

<success_criteria>
REL-03 satisfied: a nightly-only `--seed 0` ordering guard runs, is aggregated by `nightly-gate`, is contract-pinned, and is proven green on a real dispatch — the random seed stays on PR/push. REL-02 satisfied: both CI-only backlog issues are verified-fixed (with documented mechanisms + run permalink) or quarantined behind a linked tracking issue, and the interim timeout band-aid is tightened without regressing the lanes.
</success_criteria>

<output>
Create `.planning/phases/92-reliability-triage-determinism/92-03-SUMMARY.md` when done.
</output>