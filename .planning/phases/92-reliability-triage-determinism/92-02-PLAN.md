---
phase: 92-reliability-triage-determinism
plan: 02
type: execute
wave: 2
depends_on: [92-01]
files_modified:
  - scripts/ci/reliability-report.sh
  - test/fixtures/ci/run_list_sample.json
  - test/chimeway/ci_reliability_contract_test.exs
  - .planning/CI-RELIABILITY-REPORT.md
autonomous: true
requirements: [REL-01]
user_setup: []

must_haves:
  truths:
    - "scripts/ci/reliability-report.sh classifies push-on-main ci-gate JOB conclusions: success = green, failure = real failure, cancelled/skipped = excluded from the denominator (REL-01)."
    - "The report computes failure_rate = real_failures / (completed − excluded) and a consecutive-green streak counted most-recent-first, SKIPPING excluded (cancelled/skipped) runs and STOPPING at the first real failure (REL-01 boundary/adjacency edge)."
    - "The pass bar is strict: failure_rate < 10% AND streak >= 5; exactly 10% FAILS, a streak of exactly 4 FAILS and exactly 5 PASSES; the threshold comparison uses integer arithmetic (real_failures * 10 vs the denominator) so no float rounding shifts the boundary (REL-01 boundary + precision edge)."
    - "The report emits only run ids, ci-gate conclusions, counts, and run permalinks to stdout and $GITHUB_STEP_SUMMARY — never a token, raw env, or the DB-URL secret (secret hygiene)."
    - ".planning/CI-RELIABILITY-REPORT.md records the measured rate + streak + real actions/runs/<id> permalinks so the reliability signal survives GitHub log retention (REL-01 durability)."
  prohibitions:
    - "Must not use the run-level conclusion or count workflow_dispatch / release-please-branch / nightly-dispatch runs — the population is event=push, branch=main, ci-gate JOB conclusion only."
    - "Must not count cancelled/skipped runs as failures (infra supersession is not a reliability defect)."
    - "Must not leak secrets or env (token, DB URL) into world-readable job summaries."
    - "Must not interpolate untrusted run/branch title strings into shell eval — parse gh --json output with jq only."
  artifacts:
    - scripts/ci/reliability-report.sh
    - test/fixtures/ci/run_list_sample.json
    - test/chimeway/ci_reliability_contract_test.exs
    - .planning/CI-RELIABILITY-REPORT.md
  key_links:
    - "reliability-report.sh → gh run list (--branch main --event push) + gh run view (ci-gate job conclusion) [population]"
    - "ci_reliability_contract_test.exs → test/fixtures/ci/run_list_sample.json + inline boundary fixtures [parser proof]"
---

<objective>
Deliver REL-01: a re-runnable, log-assertable measurement script that classifies push-on-`main`
`ci-gate` outcomes, plus a fixture-backed parser test and a committed snapshot — making the already-met
reliability bar (11/11 completed-green in the current window) provable and re-measurable on demand.

Purpose: The trustworthy signal is the `ci-gate` JOB conclusion on `event=push, branch=main` runs; the
run-level conclusion is contaminated by `workflow_dispatch`, `release-please--branches--main`, and
nightly-dispatch failures. This plan encodes that population definition in a script whose classifier is
unit-tested offline against fixtures (obs-summary precedent), and commits the measured numbers + permalinks
so the signal outlives log retention.
Output: `scripts/ci/reliability-report.sh` + `run_list_sample.json` fixture + `ci_reliability_contract_test.exs` + `.planning/CI-RELIABILITY-REPORT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-reliability-triage-determinism/92-RESEARCH.md
@.planning/CI-PERF-BASELINE.md
</context>

## Artifacts this phase produces (full phase set — for source-grounding drift exclusion)

New symbols/files created across all of Phase 92 (this plan creates the ones marked **[92-02]**):
- [92-01] `Chimeway.TestSupport.EnvHelper` (`test/support/env_helper.ex`) + `put_env_isolated/3`; `test/chimeway/test_support/env_helper_test.exs`; async-adoption assertion in `ci_observability_contract_test.exs`; `policy_test.exs` adoption
- **[92-02]** `scripts/ci/reliability-report.sh`
- **[92-02]** `test/fixtures/ci/run_list_sample.json`
- **[92-02]** `test/chimeway/ci_reliability_contract_test.exs`
- **[92-02]** `.planning/CI-RELIABILITY-REPORT.md`
- [92-03] `test_seed_zero` job in `.github/workflows/ci.yml`; `release_gate_contract_test.exs` nightly-gate 5-lane assertions; `ci_observability_contract_test.exs` `test_seed_zero` exemption; updated `.planning/CI-HARDENING-BACKLOG.md`

<tasks>

<task type="auto">
  <name>Task 1: reliability-report.sh + fixture + fixture-backed parser contract test</name>
  <files>scripts/ci/reliability-report.sh, test/fixtures/ci/run_list_sample.json, test/chimeway/ci_reliability_contract_test.exs</files>
  <read_first>
    - scripts/ci/obs-summary.sh — the gh/jq/$GITHUB_STEP_SUMMARY renderer + secret-hygiene shape (whitelist-emit only, never raw env/token); mirror its structure and the `OBS_JOBS_JSON` test-hook idiom.
    - test/chimeway/ci_observability_contract_test.exs — the bash-against-committed-fixture harness (`System.cmd("bash", [script], env: ...)`) and the secret-hygiene test (feeds a fake token, refutes it and DATABASE_URL appear in output); mirror both for the reliability script.
    - scripts/ci/aggregate-gate.sh — `set -euo pipefail` re-runnable-script conventions.
  </read_first>
  <action>
    Write `scripts/ci/reliability-report.sh` (`set -euo pipefail`) that measures push-on-`main` `ci-gate`
    reliability. Population/collection seam: if `RELIABILITY_RUNS_JSON` is set, read a fixture JSON array of
    `{databaseId, gate_conclusion}` objects (the resolved ci-gate JOB conclusions); otherwise collect live via
    `gh run list --workflow=ci.yml --branch main --event push --limit 30 --json databaseId` then, per run,
    `gh run view <id> --json jobs -q '.jobs[]|select(.name=="ci-gate")|.conclusion'`. Classify each: `success`=green,
    `failure`=real failure, `cancelled`/`skipped`=excluded. Compute `denominator = completed − excluded`,
    `failure_rate = real_failures / denominator`, and `streak` = consecutive `success` scanning most-recent-first,
    SKIPPING excluded runs and STOPPING at the first `failure`. Emit a markdown table (columns: run id, ci-gate
    conclusion, permalink `https://github.com/${GITHUB_REPOSITORY}/actions/runs/<id>`) plus a summary line
    (`failures=<n> excluded=<n> rate=<pct> streak=<n>`) to stdout and `$GITHUB_STEP_SUMMARY`. Whitelist those
    fields only — never echo env, the token, or the DB-URL secret. Exit nonzero when the bar is missed: the
    threshold check must use integer arithmetic (`real_failures * 10 >= denominator` fails; strictly-under-10%
    passes) AND `streak >= 5` (streak 4 fails, 5 passes). Parse all API JSON with `jq` — never interpolate run/branch
    titles into `eval` or unquoted expansions. Add `test/fixtures/ci/run_list_sample.json`: a canonical mixed sample
    (e.g. several `success` + one `cancelled`) whose denominator excludes the cancelled run, yields 0 real failures,
    and a streak that meets the bar. Add `test/chimeway/ci_reliability_contract_test.exs` mirroring the obs harness:
    (a) feed `run_list_sample.json` via `RELIABILITY_RUNS_JSON` and assert exit 0 + the printed `streak=`/`rate=`
    values; (b) write inline tmp fixtures asserting the exact boundaries — exactly-10% (5 failures / 50) exits
    nonzero, just-under-10% exits 0, streak of exactly 4 exits nonzero and exactly 5 exits 0, and a cancelled run
    between greens does NOT break the streak; (c) a secret-hygiene test that passes a fake token + DB-URL value in
    the env and refutes both appear in the rendered summary.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/ci_reliability_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `MIX_ENV=test mix test test/chimeway/ci_reliability_contract_test.exs` exits 0.
    - Boundary tests prove: a `real_failures*10 == denominator` case (exactly 10%) exits nonzero; a strictly-under case exits 0; streak==4 exits nonzero; streak==5 exits 0; a `cancelled` run interleaved with greens is skipped, not counted as a break.
    - Secret-hygiene test asserts the rendered summary contains neither the fake token sentinel nor the literal `DATABASE_URL`.
    - `bash -n scripts/ci/reliability-report.sh` reports no syntax error; the script contains no `eval` of gh-derived strings (`grep -v '^#' scripts/ci/reliability-report.sh | grep -c 'eval' == 0`).
  </acceptance_criteria>
  <done>The script classifies success/failure/cancelled, computes rate+streak with the exact strict boundaries, emits secret-clean summaries, and the fixture-backed parser test (including boundary + hygiene cases) is green.</done>
</task>

<task type="auto">
  <name>Task 2: measure live main history and commit the durable snapshot</name>
  <files>.planning/CI-RELIABILITY-REPORT.md</files>
  <read_first>
    - .planning/CI-PERF-BASELINE.md — the committed-doc precedent (durable `actions/runs/<id>` permalink, delta ledger); mirror the permalink discipline the OBS-04 baseline contract already enforces.
    - scripts/ci/reliability-report.sh — the script just written; run it to produce the live numbers.
  </read_first>
  <precondition>`gh auth status` succeeds with a token scoped to read Actions run history (actions:read / repo) on szTheory/chimeway — the live measurement reads run + job conclusions via `gh`.</precondition>
  <action>
    Run `scripts/ci/reliability-report.sh` against live `main` push history and capture its output. Author
    `.planning/CI-RELIABILITY-REPORT.md` recording: the measured `failure_rate` and consecutive-green `streak`,
    the population definition (event=push, branch=main, ci-gate JOB conclusion, cancelled excluded), the run-id +
    `actions/runs/<id>` permalink for each classified run in the window, and an explicit statement that the bar
    (`< 10%` failure AND `>= 5` consecutive green) is met. Include a one-line "re-run: scripts/ci/reliability-report.sh"
    note so the snapshot is regenerable. Do not paste any token or DB secret into the doc.
  </action>
  <verify>
    <automated>scripts/ci/reliability-report.sh; echo "exit=$?"; grep -Eq 'actions/runs/[0-9]+' .planning/CI-RELIABILITY-REPORT.md && grep -Eiq 'streak' .planning/CI-RELIABILITY-REPORT.md && echo DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/ci/reliability-report.sh` exits 0 on live `main` history (bar met).
    - `.planning/CI-RELIABILITY-REPORT.md` exists and contains at least one real `actions/runs/<digits>` permalink and the measured `streak` + `rate` figures.
    - The doc names the population filter (event=push / branch=main / ci-gate job conclusion / cancelled excluded) so the number is not misread.
  </acceptance_criteria>
  <done>The reliability report script exits 0 on live history and a durable snapshot with permalinks + the population definition is committed under .planning/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions API → bash script | Untrusted run/branch titles cross into a shell script |
| CI job summary → public readers | Job summaries are world-readable on a public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-92-03 | Information Disclosure | reliability-report.sh → $GITHUB_STEP_SUMMARY | low | mitigate | Whitelist-emit run ids/conclusions/counts/permalinks only; secret-hygiene contract test refutes token + DB-URL in output (obs-summary precedent) |
| T-92-04 | Tampering | gh --json run/branch strings in shell | low | mitigate | Parse with jq / `--json -q`; no `eval`; quoted expansions; contract asserts no `eval` of gh-derived strings |
| T-92-05 | Elevation of Privilege | token scope (if ever run as a CI job) | low | mitigate | Maintainer-run script by default; any future in-CI job declares `permissions: contents: read` + `actions: read` only (re-declaring both, job block replaces top-level) |

No package installs occur in this plan (zero npm/pip/hex/crates), so no supply-chain install threat applies.
</threat_model>

<verification>
- `MIX_ENV=test mix test test/chimeway/ci_reliability_contract_test.exs --warnings-as-errors` exits 0 (parser + boundary + hygiene).
- `scripts/ci/reliability-report.sh` exits 0 on live `main` history.
- `.planning/CI-RELIABILITY-REPORT.md` carries a real `actions/runs/<id>` permalink and the measured rate/streak.
</verification>

<success_criteria>
REL-01 satisfied: the measured completed-run failure rate on `main` `ci-gate` is under 10% with >= 5 consecutive green, proven by a re-runnable, fixture-tested script and a committed snapshot with permalinks. Population contamination (dispatch/nightly/release-please/cancelled) is excluded by construction.
</success_criteria>

<output>
Create `.planning/phases/92-reliability-triage-determinism/92-02-SUMMARY.md` when done.
</output>
