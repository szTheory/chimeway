---
phase: 95-accrue-billing-escalation-proof
plan: 03
type: execute
wave: 3
depends_on:
  - 95-01
  - 95-02
files_modified:
  - test/support/artifact_consumer_fixture.ex
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
gap_closure: true
requirements:
  - ACCR-01
  - ACCR-02
must_haves:
  truths:
    - "ACCR-01 / D-01 / D-02: The clean consumer creates a real Accrue billing fixture, starts Accrue.Processor.Fake, enters through invoice.payment_failed, reaches waiting, and exits that wait through invoice.paid without a host-side notifier or signal call."
    - "ACCR-01 / D-05 / D-06: Waiting and outcome state, status reason, workflow identity, and ordered reasons are read from Chimeway.Workflows.explain/2 and list_traces/2 before the sole proof line is emitted."
    - "ACCR-02 / D-07 / D-08 / D-09: Provenance is classified from the generated consumer's resolved Accrue dependency and integration module; only exact Hex 1.3.0 is release evidence and only the exact locked Git SHA is compatibility evidence."
    - "Failed lifecycle, dependency, integration-load, evidence, and cleanup checks exit without CHIMEWAY_ACCRUE_PROOF."
  artifacts:
    - path: test/support/artifact_consumer_fixture.ex
      provides: "Real Accrue event-to-workflow proof, Fake lifecycle management, public evidence projection, and fail-closed provenance classifier"
    - path: test/chimeway/release_gate_contract_test.exs
      provides: "Executed clean-consumer lifecycle and both resolved-dependency provenance contracts"
  key_links:
    - from: test/support/artifact_consumer_fixture.ex
      to: Accrue.Processor.Fake
      via: "start_link/1 ownership tracking, reset/0 before events, and after-block cleanup"
      pattern: "Accrue\\.Processor\\.Fake\\.(start_link|reset)"
    - from: "generated invoice.payment_failed and invoice.paid events"
      to: "Chimeway.Workflows.explain/2 and list_traces/2"
      via: "fixture-private opaque run lookup followed by public-only lifecycle fact projection"
      pattern: "Chimeway\\.Workflows\\.(explain|list_traces)"
    - from: "generated Mix dependency resolution"
      to: "CHIMEWAY_ACCRUE_PROOF provenance"
      via: "SCM, exact version/ref, resolved path, integration source, and loaded module validation"
      pattern: "(released_package|compatibility)"
  prohibitions:
    - id: P-95-ACCR-01
      requirement_id: ACCR-01
      category: privacy
      status: resolved
      verification: test
      statement: "MUST NOT emit tenant, customer, subscription, invoice, recipient, payload, metadata, credential, raw-struct, or database-result values as adoption proof evidence."
    - id: P-95-ACCR-02
      requirement_id: ACCR-02
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT classify a source checkout, unresolved dependency, mutable Git reference, or source-file presence alone as released-package evidence."
---

<objective>
Replace the fabricated Accrue lifecycle record with a production-quality clean-consumer tracer whose facts and provenance come from the running generated consumer.

Purpose: Close the core execution and classification gaps that make the current proof incapable of supporting ACCR-01 or ACCR-02.
Output: A real event-to-wait-to-signal proof and release-gate contracts that execute it against a built/unpacked Chimeway package.

Assumptions from unresolved edge probes:
- ACCR-01 is unclassified and unresolved. Treat the required edge as the exact observed chain `invoice.payment_failed -> waiting/waiting_for_step_progression -> invoice.paid -> active/signal_received`; fail before output when any public observation differs.
- ACCR-02 is unclassified and unresolved. Treat dependency provenance as untrusted until the generated consumer proves one complete exact release or exact-SHA schema; unknown forms have no fallback label.

Prohibition recall: the retained privacy and transparency prohibitions in `must_haves.prohibitions` intentionally have no `check_*` descriptor. Verification must fail closed until the plan's executed negative contracts prove them; do not infer a green disposition from prose.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/95-accrue-billing-escalation-proof/95-CONTEXT.md
@.planning/phases/95-accrue-billing-escalation-proof/95-RESEARCH.md
@.planning/phases/95-accrue-billing-escalation-proof/95-VERIFICATION.md
@.planning/phases/95-accrue-billing-escalation-proof/95-REVIEW.md
@.planning/phases/95-accrue-billing-escalation-proof/95-01-SUMMARY.md
@test/support/artifact_consumer_fixture.ex
@test/support/accrue/fixtures.ex
@test/chimeway/integrations/accrue_dunning_lifecycle_test.exs
@test/chimeway/release_gate_contract_test.exs
@lib/chimeway/workflows.ex
@lib/chimeway/workflows/progression.ex
@deps/accrue/lib/accrue/processor/fake.ex
@deps/accrue/lib/accrue/integrations/chimeway.ex

<interfaces>
- `Chimeway.Test.ArtifactConsumerFixture.prove_accrue!/1,2` returns a map containing `output`, `proof_source`, `mix_source`, `config_source`, `evidence`, `identity`, `artifact_root`, and `cleanup`.
- `Accrue.Processor.Fake.start_link([])` returns `{:ok, pid}` or `{:error, {:already_started, pid}}`; `reset/0` returns `:ok`.
- `Chimeway.Workflows.explain(tenant_id, run_id)` returns `{:ok, %{state:, status_reason:, ...}}`; `list_traces/2` returns ascending `WorkflowTransition` values.
- `Chimeway.Workflows.Progression.progress_run/2` is the existing deterministic wait-entry seam after the initial delivery reaches a terminal outcome.
- `Oban.drain_queue(queue: :chimeway_signals, with_scheduled: true, with_safety: false)` must return exactly one successful signal job and zero failed/cancelled/discarded/snoozed jobs.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute one real Accrue event-to-public-workflow evidence path</name>
  <files>test/support/artifact_consumer_fixture.ex, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    test/support/artifact_consumer_fixture.ex
    test/chimeway/release_gate_contract_test.exs
    test/support/accrue/fixtures.ex
    test/chimeway/integrations/accrue_dunning_lifecycle_test.exs
    lib/chimeway/workflows.ex
    lib/chimeway/workflows/progression.ex
    deps/accrue/lib/accrue/test.ex
    deps/accrue/lib/accrue/processor/fake.ex
    .planning/phases/95-accrue-billing-escalation-proof/95-VERIFICATION.md
  </read_first>
  <behavior>
    - "A built/unpacked Chimeway package passed to prove_accrue!/1 creates an isolated consumer and emits one parsed record only after public waiting and signal_received observations agree."
    - "Accrue.Processor.Fake is explicitly started, reset before use, and stopped only when the proof owns the process."
    - "A missing Fake process, unsuccessful event, unexpected Oban drain, absent/ambiguous run, or mismatched public trace raises and cleanup removes the temporary database and tree without emitting a proof record."
    - "The generated proof never directly invokes the bundled DunningNotifier, Chimeway.trigger/3, or Chimeway.Signal.track/4."
  </behavior>
  <action>
    Start with a failing `:accrue_artifact_proof` release-gate test that calls `build_unpacked_package!/0`, resolves `unpacked_package_root!/1`, invokes `ArtifactConsumerFixture.prove_accrue!/1`, and asserts the returned evidence plus filesystem/database cleanup. Replace `accrue_proof_ex/0`'s literal `waiting` and `outcome` maps with the same real billing topology used by `Chimeway.TestSupport.AccrueFixtures`: create Accrue Customer, Subscription, and failed Invoice rows in the generated consumer's isolated repo; configure `Accrue.Integrations.Chimeway` and the deterministic Logger channel; stub the Fake processor's invoice/subscription fetches; trigger `:invoice_payment_failed`; converge the first delivery; and call `Chimeway.Workflows.Progression.progress_run/2` to enter the durable wait.

    Explicitly start `Accrue.Processor.Fake` before `Accrue.Test.setup_fake_processor/0`, accept only the documented already-started result, reset it, remember process ownership, and use an `after` block to reset and stop only an owned live process. Locate exactly one `accrue.dunning` run using fixture-private repository code solely to obtain the opaque run and notification IDs. Derive the waiting state/reason and workflow identity from `Chimeway.Workflows.explain/2` plus ordered reasons from `list_traces/2`; do not copy lifecycle facts from the lookup query.

    Stub the paid invoice/subscription response, trigger `:invoice_paid`, require the exact zero-error/one-success `:chimeway_signals` drain result, then call `explain/2` and `list_traces/2` again. Project the one output record from those public values only after validating waiting precedes signal_received and the outcome transition context names `invoice.paid`. Keep D-01/D-02 natural event entry, D-05 one-line output, and D-06 safe-field allowlist. Remove comments and constants that could let the proof line succeed without a workflow.
  </action>
  <acceptance_criteria>
    - The focused contract executes `prove_accrue!/1` against a freshly built/unpacked package and observes `waiting / waiting_for_step_progression` followed by `active / signal_received` from public APIs.
    - Generated code starts and resets `Accrue.Processor.Fake`, tolerates only its documented already-started result, and cleans up owned processor state on success and failure.
    - The proof record cannot be produced when either Accrue event, delivery convergence, wait progression, signal drain, public explanation, ordered trace, or cleanup check fails.
    - The generated source contains public `Workflows.explain/2` and `list_traces/2` calls and contains no literal lifecycle evidence maps or direct host invocation of the notifier/trigger/signal seams.
    - The temporary consumer root and database are absent after the successful focused contract.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --only accrue_artifact_proof --warnings-as-errors</automated>
  </verify>
  <done>ACCR-01's single happy path is real end to end: Accrue events drive a durable wait and outcome signal, and the emitted facts come from public workflow evidence.</done>
  <reversibility rating="reversible">The generated proof implementation and focused contracts are internal verification tooling behind the unchanged strict record schema.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify exact release and immutable-SHA resolution fail closed</name>
  <files>test/support/artifact_consumer_fixture.ex, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    test/support/artifact_consumer_fixture.ex
    test/chimeway/release_gate_contract_test.exs
    deps/accrue/hex_metadata.config
    deps/accrue/lib/accrue/integrations/chimeway.ex
    mix.lock
    .github/workflows/ci.yml
    .planning/phases/95-accrue-billing-escalation-proof/95-CONTEXT.md
  </read_first>
  <behavior>
    - "D-07/D-09: released_package requires a generated-consumer Hex dependency at exactly 1.3.0, resolved package metadata that declares that version and integration file, and a successfully loaded Accrue.Integrations.Chimeway module from that resolved path."
    - "D-08/D-09: compatibility requires a Git dependency whose resolved lock revision equals 236fa2f1649e771f3b515603495436badeed3c7b and whose integration loads from that checkout."
    - "Path dependencies, branches/tags, abbreviated or different SHAs, missing/malformed metadata, dependency/module path disagreement, mixed fields, and unknown SCMs raise before output."
  </behavior>
  <action>
    Add failing classifier contracts before changing generated dependency/proof code. Introduce a binary/string-key resolved-dependency descriptor produced inside the generated consumer from `Mix.Dep` plus `mix.lock`, `Application.spec(:accrue, :vsn)`, the resolved dependency path, `hex_metadata.config`, and the integration module source location. For the D-07 release branch require Hex SCM, exact application and metadata version `1.3.0`, metadata membership of `lib/accrue/integrations/chimeway.ex`, a regular source file beneath the resolved dependency root, and successful module loading from that source. Read the Chimeway version from the unpacked dependency's package metadata/application version and validate equality before emission.

    For the D-08 compatibility branch, allow only an explicitly selected generated-consumer Git resolution whose full lock revision equals `236fa2f1649e771f3b515603495436badeed3c7b`; require the same integration-path containment and module-load checks, emit `accrue_ref` instead of version fields, and keep this branch out of release/install claims. Exercise both branches through resolved generated consumers; use the exact immutable Git ref in the compatibility contract and tag expensive cases `:accrue_artifact_proof`. Unknown or partially verified resolution must raise rather than selecting a label. Extend negative contracts for path/source checkout, mutable ref, wrong SHA, metadata version/file mismatch, module loaded from another root, mixed schemas, and cleanup after classification failure. Preserve the parser's fixed binary-key behavior and P-95 prohibitions without adding atom creation.
  </action>
  <acceptance_criteria>
    - The release E2E contract derives `released_package`, Accrue `1.3.0`, and the exact Chimeway artifact version from the generated consumer's resolved package data.
    - The compatibility E2E contract derives `compatibility` only from the full locked SHA and emits no release version fields.
    - Every other SCM/version/ref/metadata/module-origin shape exits without a proof record and cleans fixture resources.
    - Source-file presence by itself cannot authorize either provenance label.
    - Unknown/duplicate/mixed/sensitive proof fields remain rejected without converting untrusted keys to atoms.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --only accrue_artifact_proof --warnings-as-errors</automated>
  </verify>
  <done>ACCR-02 classification is generated-consumer evidence: exact Hex release or exact immutable Git SHA, with no optimistic or directory-derived label.</done>
  <reversibility rating="costly">Changing the provenance schema later requires coordinated runner, parser, release-contract, and adopter-guide updates, but no persisted data migration.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Accrue event input -> generated consumer | Deterministic billing fixtures cross into Accrue's webhook reducer and may contain sensitive values. |
| Generated consumer DB -> public workflow API | Private lookup may identify the run, but lifecycle evidence must cross only through tenant-scoped public APIs. |
| Resolved dependency -> provenance label | Package/Git metadata and module origin are untrusted until mutually validated. |
| Generated process -> proof parser | Output is untrusted text and must match one exact binary-key schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-95-13 | Spoofing | Accrue provenance classifier | high | mitigate | Task 2 requires exact SCM, version/ref, lock, metadata, path containment, and loaded-module origin before labeling. |
| T-95-14 | Tampering | lifecycle proof facts | high | mitigate | Task 1 derives states/reasons/order from public APIs and aborts on any mismatch before serialization. |
| T-95-15 | Information Disclosure | CHIMEWAY_ACCRUE_PROOF | high | mitigate | Task 1 retains the fixed D-06 allowlist; Task 2 keeps binary-key adversarial tests for all sensitive categories. |
| T-95-16 | Denial of Service | Fake/Oban/generated resources | medium | mitigate | Task 1 owns Fake startup/reset and requires try/after cleanup; both tasks test temporary DB/tree teardown on failures. |
| T-95-17 | Repudiation | event-to-outcome ordering | medium | mitigate | Task 1 asserts ordered public transitions and the invoice.paid transition reason/context before output. |
| T-95-SC | Tampering | generated `mix deps.get` | high | mitigate | Task 2 permits only audited Accrue 1.3.0 Hex resolution or the locked full compatibility SHA and rejects all other dependency sources. |
</threat_model>

## Artifacts this phase produces

- `test/support/artifact_consumer_fixture.ex`: real Accrue lifecycle proof, Fake management, public evidence projection, and exact resolved-dependency provenance classifier.
- `test/chimeway/release_gate_contract_test.exs`: executed release and compatibility paths plus failure, disclosure, provenance, and cleanup contracts.

<verification>
- `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --only accrue_artifact_proof --warnings-as-errors`
- `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors`
- The focused test output shows real package build/unpack and generated-consumer command execution before one proof line.
</verification>

<success_criteria>
- All three failed execution truths (real workflow, public trace evidence, resolved provenance classification) are demonstrably non-static.
- Fake startup/reset and all success/failure cleanup paths are covered by executed contracts.
- ACCR-01 and ACCR-02 edge assumptions fail closed rather than becoming undocumented inference.
- Both descriptor-less prohibitions have concrete negative-contract evidence, while remaining fail-closed to automated prohibition projection.
</success_criteria>

<output>
Create `.planning/phases/95-accrue-billing-escalation-proof/95-03-SUMMARY.md` when done.
</output>
