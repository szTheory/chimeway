---
phase: 95-accrue-billing-escalation-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/prove-accrue-consumer.exs
  - test/support/artifact_consumer_fixture.ex
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
requirements:
  - ACCR-01
  - ACCR-02
must_haves:
  truths:
    - "ACCR-01 / D-01 / D-02: A clean consumer starts dunning only through Accrue's invoice.payment_failed event, reaches waiting / waiting_for_step_progression, and ends that wait only through invoice.paid without directly invoking the bundled notifier or Chimeway.Signal.track/4."
    - "ACCR-01 / D-05 / D-06: The resulting public workflow evidence is active / signal_received and is described as the outcome signal ending the wait, never as workflow completion."
    - "ACCR-01 / D-03 / D-04: The proof preserves the unpacked-artifact-only :chimeway dependency, isolated temporary host/database, provenance validation, serialized execution, and failure-safe cleanup."
    - "ACCR-02 / D-07 / D-08 / D-09: released_package is accepted only when the generated consumer resolves exact Accrue 1.3.0 and proves its integration source/module; otherwise the only valid provenance is compatibility with SHA 236fa2f1649e771f3b515603495436badeed3c7b."
    - "ACCR-01 / D-03 / D-04 / D-06: The documented runner accepts one explicit unpacked-artifact root and emits the single CHIMEWAY_ACCRUE_PROOF record only after the isolated proof and cleanup succeed; unknown, duplicate, missing, malformed, forged, sensitive, or out-of-order evidence fails closed without atomizing untrusted text."
  artifacts:
    - path: scripts/prove-accrue-consumer.exs
      provides: "committed adopter-facing command that accepts one unpacked Chimeway artifact root and prints the fixture's single sanitized proof record"
    - path: test/support/artifact_consumer_fixture.ex
      provides: "prove_accrue!/2, generated Accrue consumer topology and proof script, conditional provenance classification, and strict Accrue evidence parser"
    - path: test/chimeway/release_gate_contract_test.exs
      provides: "serialized event-to-signal artifact proof plus lifecycle, provenance, isolation, disclosure, cleanup, and parser contracts"
  key_links:
    - from: scripts/prove-accrue-consumer.exs
      to: test/support/artifact_consumer_fixture.ex
      via: "MIX_ENV=test mix run scripts/prove-accrue-consumer.exs -- --artifact-root <absolute-unpacked-root> invokes prove_accrue!/1 and prints only its CHIMEWAY_ACCRUE_PROOF record"
    - from: test/chimeway/release_gate_contract_test.exs
      to: test/support/artifact_consumer_fixture.ex
      via: "Chimeway.Test.ArtifactConsumerFixture.prove_accrue!/2 and parse_accrue_evidence!/1"
    - from: "generated Accrue.Test.trigger_event/2 invoice.payment_failed invocation"
      to: "generated public Chimeway.Workflows.explain/2 and list_traces/2 evidence"
      via: "Accrue.Integrations.Chimeway start_campaign/3, dunning workflow progression, invoice.paid cancel_campaign/3, and deterministic :chimeway_signals queue drain"
    - from: "generated consumer dependency resolution"
      to: "CHIMEWAY_ACCRUE_PROOF provenance fields"
      via: "exact Accrue 1.3.0 source/module validation or immutable-SHA compatibility classification"
  prohibitions:
    - "Direct invocation of Accrue.Integrations.Chimeway.DunningNotifier or Chimeway.Signal.track/4 from the generated proof."
    - "Describing active / signal_received as workflow completion or a terminal workflow state."
    - "Emitting IDs, tenant values, billing data, recipient data, payloads, metadata, credentials, raw structs, or database results in CHIMEWAY_ACCRUE_PROOF."
    - "Using DemoHost, the repository source tree, direct database inspection, or a repository-maintainer verify lane as adopter-proof evidence."
    - "Presenting SHA 236fa2f1649e771f3b515603495436badeed3c7b as released-package proof or installation guidance."
    - "Adding verify.adoption_paths or a dedicated adoption CI lane before Phase 96."
---

<objective>
Deliver the production-quality Accrue tracer and its fail-closed lifecycle/provenance evidence boundary, implementing D-01 through D-09 for ACCR-01 and ACCR-02.

Purpose: A prospective adopter can independently observe Accrue begin a real Chimeway dunning workflow from a billing-failure event and end its wait from the natural payment-outcome signal, while trusting both the sanitized evidence and the release-support label.
Output: An extended artifact-consumer fixture and serialized release-gate contracts for the event-to-wait-to-signal path.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/95-accrue-billing-escalation-proof/95-CONTEXT.md
@.planning/phases/95-accrue-billing-escalation-proof/95-RESEARCH.md
@.planning/phases/95-accrue-billing-escalation-proof/95-PATTERNS.md
@.planning/phases/95-accrue-billing-escalation-proof/95-VALIDATION.md
@.planning/phases/93-hermetic-artifact-harness-core-trace-proof/93-01-SUMMARY.md
@.planning/phases/94-mailglass-transactional-email-proof/94-04-SUMMARY.md
</context>

<interfaces>
- `Chimeway.Test.ArtifactConsumerFixture.prove_mailglass!/2` supplies the exact resource identity, generated-host scaffold, artifact validation, Mix command, result-map, rescue/catch cleanup, and one-line parser pattern to extend.
- `Accrue.Test.trigger_event/2` is the producer boundary for `:invoice_payment_failed` and `:invoice_paid`; `Accrue.Integrations.Chimeway.start_campaign/3` and `cancel_campaign/3` are integration internals the generated proof exercises but does not call directly.
- `Chimeway.Workflows.explain/2` returns tenant-scoped state and status_reason; `Chimeway.Workflows.list_traces/2` returns ordered structural transitions without raw signal payloads. These public APIs are the adopter-facing workflow evidence seam.
- `Oban.drain_queue(queue: :chimeway_signals, with_scheduled: true)` is the established deterministic signal-routing seam that the generated consumer must prove is available before relying on it.
- Accrue 1.3.0 package metadata includes `lib/accrue/integrations/chimeway.ex`, but the module is conditionally compiled. The generated consumer must inspect its own resolved dependency path and loadability before assigning released_package provenance.
</interfaces>

## Resolved implementation seams

- **R-95-01 (ACCR-01 queue execution):** checked-in Oban 2.x exposes public `Oban.drain_queue/1`; the generated host configures the same started Oban instance with `repo: ArtifactConsumer.Repo`, `testing: :manual`, and `queues: false`, then calls `Oban.drain_queue(queue: :chimeway_signals, with_scheduled: true, with_safety: false)`. The proof requires `%{cancelled: 0, discard: 0, failure: 0, snoozed: 0, success: 1}` before reading post-signal public evidence. No private Oban test helper or database polling is needed.
- **R-95-02 (ACCR-02 conditional loading):** Accrue 1.3.0's package metadata contains `lib/accrue/integrations/chimeway.ex`, whose outer `Code.ensure_loaded?(Chimeway)` guard is the sole conditional-load boundary. After generated dependencies and the host project are compiled, the proof resolves that file from `Mix.Project.deps_paths()[:accrue]`, compiles it from that resolved dependency path only when the module is absent, and requires `Code.ensure_loaded?(Accrue.Integrations.Chimeway)` before `released_package`; missing source or failed load raises before output. The exact-SHA compatibility run applies the same source/ref validation and fail-closed rule.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one Accrue billing failure and payment outcome through the unpacked artifact</name>
  <files>scripts/prove-accrue-consumer.exs, test/support/artifact_consumer_fixture.ex, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    test/support/artifact_consumer_fixture.ex
    test/chimeway/release_gate_contract_test.exs
    deps/oban/lib/oban.ex
    deps/oban/lib/oban/queue/drainer.ex
    .planning/phases/95-accrue-billing-escalation-proof/95-CONTEXT.md
    .planning/phases/95-accrue-billing-escalation-proof/95-RESEARCH.md
    .planning/phases/95-accrue-billing-escalation-proof/95-PATTERNS.md
    test/chimeway/integrations/accrue_dunning_lifecycle_test.exs
    deps/accrue/lib/accrue/integrations/chimeway.ex
    deps/accrue/hex_metadata.config
    test/test_helper.exs
    lib/chimeway/workflows.ex
    lib/chimeway/signal.ex
  </read_first>
  <behavior>
    - "ACCR-01 / D-01 / D-02: `prove_accrue!/2` triggers `:invoice_payment_failed`, establishes public waiting / waiting_for_step_progression evidence, triggers `:invoice_paid`, drains the signal route, and establishes public active / signal_received evidence."
    - "ACCR-01 / D-03 / D-04: one serialized generated host uses its unique database and only the unpacked path for :chimeway; it cleans filesystem/database resources on success, rescue, and catch."
    - "ACCR-01 / D-05 / D-06: exactly one fixed-order CHIMEWAY_ACCRUE_PROOF line is projected from public workflow APIs and contains no durable IDs, tenant/billing/recipient content, payloads, metadata, credentials, raw structs, or database results."
    - "ACCR-02 / D-07 / D-08 / D-09: the proof emits released_package with exact Accrue 1.3.0 and the resolved Chimeway artifact version only after release-source/module checks pass; otherwise it emits compatibility with the exact immutable SHA and no release-installation claim."
  </behavior>
  <action>
    Begin with a failing serialized ExUnit contract for `Chimeway.Test.ArtifactConsumerFixture.prove_accrue!/2`, then extend the existing fixture rather than adding another harness, per D-03. Preserve `validate_artifact_dependency!/3`, unique resource identity, isolated PostgreSQL lifecycle, try/rescue/catch cleanup, and the returned proof-map conventions. Keep exactly one unpacked-path `:chimeway` dependency and direct Oban opt-in, add exact `{:accrue, "1.3.0"}` only to the generated consumer, and do not touch root dependencies, DemoHost, source-tree paths, or CI topology per D-04/D-09.

    Generate the fixture-local Accrue repo/config/migration and deterministic billing records using the public/established shapes from the lifecycle test and resolved Accrue package. Configure Accrue's dunning engine as `Accrue.Integrations.Chimeway`, keep Chimeway on the generated `ArtifactConsumer.Repo`, and configure the deterministic Logger email path. Resolve both dependencies first; from `Mix.Project.deps_paths()[:accrue]`, require `lib/accrue/integrations/chimeway.ex`, compile it only from that generated consumer dependency path when conditional compilation omitted the module, and require `Code.ensure_loaded?(Accrue.Integrations.Chimeway)` before classifying the default exact-1.3.0 branch as released_package per D-07/D-09. Support a fixture-internal compatibility mode only when its Accrue dependency is independently resolved at exact ref `236fa2f1649e771f3b515603495436badeed3c7b`; validate the resolved ref/source before running and never expose that dependency form as adopter guidance. If neither the release checks nor exact-ref checks pass, raise before emitting a proof record.

    In the generated `priv/prove_accrue.exs`, construct non-sensitive deterministic customer/subscription/invoice fixtures and the established fake-processor fetch stubs, then invoke only `Accrue.Test.trigger_event(:invoice_payment_failed, ...)` to begin the campaign per D-01. Use fixture-private setup only to locate the new workflow run ID needed by the public API, progress the initial step to the wait using the established worker/progression seams, and call `Chimeway.Workflows.explain/2` plus `list_traces/2` to assert `:waiting`, `waiting_for_step_progression`, workflow key `accrue.dunning`, and version 1. Configure and start Oban in the generated application with `repo: ArtifactConsumer.Repo`, `testing: :manual`, and `queues: false`. Then invoke only `Accrue.Test.trigger_event(:invoice_paid, ...)`, execute the resolved R-95-01 seam with `Oban.drain_queue(queue: :chimeway_signals, with_scheduled: true, with_safety: false)`, require equality with `%{cancelled: 0, discard: 0, failure: 0, snoozed: 0, success: 1}`, and call the same public workflow APIs to assert `:active`, `signal_received`, and ordered transition reasons per D-02/D-05. Do not call the bundled notifier, `Chimeway.trigger/3`, `Chimeway.Signal.track/4`, use an Oban private/test-only helper, inspect the database for outcome evidence, or claim a terminal/completed workflow.

    Serialize exactly one fixed-order `CHIMEWAY_ACCRUE_PROOF` line. Use safe fields for provenance mode, exact released versions or exact compatibility SHA, stable workflow key/version, waiting state/reason, outcome event, outcome state/reason, and ordered reason tokens. Return proof source, mix/config/provenance sources, parsed evidence, identity, artifact root, and cleanup result. The release branch must include `provenance=released_package`, `accrue_version=1.3.0`, and the resolved Chimeway package version; the independently exact-ref-resolved branch must include `provenance=compatibility` and `accrue_ref=236fa2f1649e771f3b515603495436badeed3c7b` without presenting that ref as a dependency recommendation per D-07 through D-09.

    Add the committed adopter-facing runner `scripts/prove-accrue-consumer.exs`. Its exact supported invocation is `MIX_ENV=test mix run scripts/prove-accrue-consumer.exs -- --artifact-root /absolute/path/to/unpacked/chimeway`. Accept exactly one named absolute directory input, reject missing/relative/extra inputs before creating resources, call `Chimeway.Test.ArtifactConsumerFixture.prove_accrue!/1`, print exactly its sanitized `CHIMEWAY_ACCRUE_PROOF` record to stdout, and exit nonzero without a proof record on validation, provenance, lifecycle, or cleanup failure. This command is a committed clean-consumer runner, not ExUnit, `mix verify.accrue`, a root Mix alias, or the deferred Phase 96 CI lane. Contract-test the command, input validation, exact output schema, artifact-only dependency, and fail-closed exit behavior in `release_gate_contract_test.exs`.
  </action>
  <acceptance_criteria>
    - `test/support/artifact_consumer_fixture.ex` exports separately callable `prove_accrue!/1` and `/2` while Core and Mailglass proof APIs remain supported.
    - `MIX_ENV=test mix run scripts/prove-accrue-consumer.exs -- --artifact-root /absolute/path/to/unpacked/chimeway` is the exact supported guide command; it accepts no implicit/source-tree artifact, prints exactly one validated `CHIMEWAY_ACCRUE_PROOF` record on success, and exits nonzero with no proof record on invalid input or proof failure.
    - Every generated `mix.exs` has exactly one unpacked-path `:chimeway` and direct `:oban`; release mode resolves exact `:accrue` 1.3.0, compatibility mode proves the exact immutable Accrue ref, and neither mode accepts a local sibling path or `ACCRUE_PATH`.
    - The generated proof source contains both `Accrue.Test.trigger_event(:invoice_payment_failed` and `Accrue.Test.trigger_event(:invoice_paid`, and contains none of `Chimeway.trigger(`, `Chimeway.Signal.track(`, or a direct DunningNotifier invocation.
    - Public evidence records waiting / waiting_for_step_progression before invoice.paid and active / signal_received after its signal job executes; ordered traces include both reasons.
    - The generated host starts Oban against `ArtifactConsumer.Repo` in manual mode with queues disabled, and its public `Oban.drain_queue/1` result equals `%{cancelled: 0, discard: 0, failure: 0, snoozed: 0, success: 1}` before post-signal evidence is accepted.
    - The proof wording and fields contain no completed/terminal claim for active / signal_received.
    - A released_package result proves exact Accrue 1.3.0 integration source/module resolution and includes the resolved Chimeway version; a compatibility result additionally proves its Accrue dependency resolved at the exact immutable SHA; missing provenance evidence emits no proof line.
    - The release-gate contract proves unpacked-artifact provenance, source/output exclusions, isolated cleanup, and exactly one CHIMEWAY_ACCRUE_PROOF line.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>ACCR-01 and ACCR-02 have one production-quality clean-consumer tracer from Accrue payment failure through public waiting evidence and invoice.paid signal_received evidence, with conditional provenance that cannot overstate release support.</done>
  <reversibility rating="reversible">The work extends test-only generated-host templates and private release-gate contracts without changing published runtime or storage contracts.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed on forged Accrue lifecycle, provenance, or sensitive evidence</name>
  <files>test/support/artifact_consumer_fixture.ex, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    test/support/artifact_consumer_fixture.ex
    test/chimeway/release_gate_contract_test.exs
    .planning/phases/95-accrue-billing-escalation-proof/95-CONTEXT.md
    .planning/phases/95-accrue-billing-escalation-proof/95-PATTERNS.md
    lib/chimeway/workflows.ex
    deps/accrue/hex_metadata.config
    .github/workflows/ci.yml
    MAINTAINING.md
  </read_first>
  <behavior>
    - "ACCR-01 / D-05 / D-06: `parse_accrue_evidence!/1` accepts exactly one complete allowlisted record with exact lifecycle values and ordered reason tokens; unknown, duplicate, missing, malformed, repeated, and forged values fail closed."
    - "ACCR-01 / D-06: randomized record keys and timeline values are never converted to atoms, and every ID, tenant, billing, recipient, payload, metadata, credential, raw-struct, and database-result key is rejected."
    - "ACCR-02 / D-07 / D-08 / D-09: released_package and compatibility fields are mutually exclusive; only exact 1.3.0 plus resolved Chimeway version is valid for release, and only the exact SHA is valid for compatibility."
    - "ACCR-01 / D-03: parser, subprocess, and provenance failures preserve exact temporary filesystem/database cleanup."
  </behavior>
  <action>
    First add failing adversarial contracts, then add `parse_accrue_evidence!/1` using a compile-time binary-key-to-existing-atom map, `Map.has_key?/2` duplicate detection, exact required key sets per provenance branch, and a one-and-only-one `CHIMEWAY_ACCRUE_PROOF` extractor following the Mailglass pattern. Validate `workflow_key=accrue.dunning`, `workflow_version=1`, `waiting_state=waiting`, `waiting_reason=waiting_for_step_progression`, `outcome_event=invoice.paid`, `outcome_state=active`, `outcome_reason=signal_received`, and the exact ordered reason list. Do not atomize any subprocess-derived key, state, or reason text per D-06.

    Enforce mutually exclusive provenance schemas per D-07 through D-09: `released_package` requires `accrue_version=1.3.0`, a syntactically valid resolved Chimeway version, and no `accrue_ref`; `compatibility` requires only the exact SHA in `accrue_ref` and no released Accrue-version claim. Add table-driven mutations for every allowlisted value, unknown/random keys, duplicate/missing/malformed pairs, multiple proof lines, reordered/unknown transition reasons, false `completed`/`terminal` values, forged versions/SHAs, and cross-branch fields.

    Add source/output boundary contracts that reject identifiers and sensitive categories including workflow/delivery/signal/tenant/customer/subscription/invoice/recipient IDs, amounts/currency, email/recipient values, payload/context/metadata, secrets/tokens/credentials, raw structs/inspect output, SQL/Ecto query results, repository-root semantics, and direct notifier/signal calls per D-04/D-06. Prove randomized unknown keys and timeline values remain unavailable to `String.to_existing_atom/1`. Add failure-injection coverage demonstrating cleanup on pre-command, provenance, subprocess, and parser failures. Keep this plan inside the existing release-gate test surface; do not add a Mix alias or workflow lane.
  </action>
  <acceptance_criteria>
    - The parser returns exactly one of two complete schemas: verified released_package or exact-SHA compatibility; mixed or partial provenance cannot pass.
    - Every fixed lifecycle field and the ordered waiting_for_step_progression,signal_received sequence has a negative mutation contract.
    - Unknown, duplicate, missing, malformed, repeated-prefix, cross-branch, sensitive-key, forged-state, forged-version, forged-ref, and reordered-timeline inputs each raise a specific error.
    - Randomized unknown keys and reason tokens are still rejected by `String.to_existing_atom/1` after parsing fails.
    - Generated source and public output contracts exclude direct notifier/signal calls, source-tree semantics, direct database evidence, IDs, billing/recipient data, payloads, metadata, credentials, and raw structs.
    - Failure-path tests confirm temporary filesystem and PostgreSQL resources are cleaned exactly.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The Accrue proof boundary accepts only the exact public lifecycle and truthful provenance schemas, with adversarial evidence that no sensitive, forged, atomizing, source-tree, or database-bypass record can pass.</done>
  <reversibility rating="reversible">The evidence schema and tests are private release-gate surfaces that can evolve together without a public data migration.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated consumer output -> release-gate parser | Subprocess proof text is untrusted and must match one complete fixed lifecycle/provenance schema without atom creation. |
| tenant-scoped workflow API -> public proof line | IDs and tenant/billing/recipient/payload context must be reduced to stable non-sensitive lifecycle facts. |
| repository -> unpacked artifact consumer | The proof must reject source-tree substitution, direct database evidence, and maintainer-lane shortcuts. |
| resolved Accrue dependency -> provenance label | A release claim is permitted only after exact version, source-presence, and module-loadability checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-95-01 | Information Disclosure | `CHIMEWAY_ACCRUE_PROOF` projection | high | mitigate | Task 1 projects only fixed workflow/provenance fields from public APIs; Task 2 contract-rejects IDs, billing/recipient data, payloads, metadata, credentials, and raw structs. |
| T-95-02 | Tampering | `parse_accrue_evidence!/1` untrusted keys and values | high | mitigate | Task 2 uses a fixed binary-key map, exact branch schemas, value/timeline validation, duplicate detection, and atom-safety tests. |
| T-95-03 | Information Disclosure | tenant-scoped workflow reads | high | mitigate | Task 1 calls `Workflows.explain/2` and `list_traces/2` with the matching tenant only, returns no tenant/ID fields, and Task 2 tests cross-tenant/data-shaped exclusions. |
| T-95-04 | Spoofing | released-package versus compatibility provenance | high | mitigate | Tasks 1-2 require exact Accrue 1.3.0 resolved source/module evidence for released_package and exact immutable SHA-only evidence for compatibility. |
| T-95-05 | Tampering | artifact/source/database proof boundary | high | mitigate | Preserve pre-command unpacked-path validation; contract-test absence of repository-root paths, direct notifier/signal calls, SQL/Ecto evidence, DemoHost, and maintainer verify lanes. |
| T-95-06 | Repudiation | active / signal_received outcome semantics | medium | mitigate | Require both ordered public transition reasons and fixed outcome state/reason; reject completion/terminal values and claims. |
</threat_model>

## Artifacts this phase produces

This plan creates one committed proof-command artifact and modifies two test surfaces:

- `scripts/prove-accrue-consumer.exs`: exact adopter-facing unpacked-artifact command with validated input, one-record success output, and fail-closed exit behavior.
- `test/support/artifact_consumer_fixture.ex`: `prove_accrue!/2`, `parse_accrue_evidence!/1`, release/compatibility classifiers, and generated consumer templates for exact Accrue 1.3.0 lifecycle proof.
- `test/chimeway/release_gate_contract_test.exs`: serialized Accrue artifact proof plus lifecycle, provenance, parser, sensitive-output, tenant-isolation, source/database-bypass, and cleanup contracts.

Runtime-generated consumer files include the Accrue config/repo/migration support and `priv/prove_accrue.exs`; they live only in the fixture's temporary root and are removed during cleanup.

<verification>
- `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors`
- `git diff --name-only -- lib mix.exs priv .github/workflows` prints nothing, proving no product runtime, root dependency, committed migration, or CI-lane change.
</verification>

<success_criteria>
- ACCR-01 has a runnable end-to-end Accrue event/outcome tracer with public waiting and signal_received evidence.
- ACCR-02 has executable conditional provenance with no path from a pinned SHA to a release-installation claim.
- D-01 through D-09 are implemented and cited in task behavior/actions.
- Both spec-less assumptions are resolved by executable fail-closed behavior rather than hidden planner defaults.
- Every high-severity threat has a concrete automated mitigation.
</success_criteria>

<output>
Create `.planning/phases/95-accrue-billing-escalation-proof/95-01-SUMMARY.md` when done.
</output>
