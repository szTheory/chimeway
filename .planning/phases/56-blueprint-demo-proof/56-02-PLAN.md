---
phase: 56-blueprint-demo-proof
plan: 02
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - guides/recipes/mailglass-integration-blueprint.md
  - guides/recipes/custom-adapter.md
  - test/chimeway/doc_contract_test.exs
requirements: [ECOS-05]
must_haves:
  truths:
    - "Published blueprint recipe documents Chimeway orchestration vs Mailglass templating split with copy-paste notifier and adapter config"
    - "Recipe aligns stable keys teampulse.invite_sent and teampulse.invite_sent.email with runnable demo host module pointers"
    - "CI doc-contract describe block fails if recipe regresses required phrases or reintroduces forbidden anti-patterns"
  artifacts:
    - path: "guides/recipes/mailglass-integration-blueprint.md"
      provides: "ECOS-05 reference blueprint for adopters"
      contains: "Chimeway.Adapters.Mailglass"
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "mailglass blueprint recipe doc contract (ECOS-05) describe"
      contains: "mailglass blueprint recipe doc contract"
  key_links:
    - from: "guides/recipes/mailglass-integration-blueprint.md"
      to: "guides/recipes/custom-adapter.md"
      via: "Prerequisites cross-link to built-in Mailglass stub section"
      pattern: "custom-adapter"
    - from: "guides/recipes/mailglass-integration-blueprint.md"
      to: "DemoHost.Notifiers.InviteSent"
      via: "Runnable demo pointers (RECP-03 pattern)"
      pattern: "DemoHost.Notifiers.InviteSent"
    - from: "test/chimeway/doc_contract_test.exs"
      to: "guides/recipes/mailglass-integration-blueprint.md"
      via: "@mailglass_blueprint_recipe Path.expand"
      pattern: "mailglass-integration-blueprint"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-56-04 | Recipe documents fictional Chimeway APIs (Workflow vs Workflows, Chimeway.Trigger.trigger) | medium | Reuse `@recipe_forbidden_strings` + fictional `Chimeway.Workflow` regex test in doc-contract (RECP-01 pattern) |
| T-56-05 | Blueprint absorbs Phase 57 scope (golden-path guide, inbound webhook route, mix verify.mailglass) | medium | D-17: recipe stays notifier + adapter + responsibility split + demo pointers; defer inbound webhook and verify gate to Phase 57 prose boundary |
| T-56-06 | Recipe references demo modules that do not exist after 56-01 | medium | `depends_on: [56-01]` — write recipe only after `DemoHost.Mailers.InviteEmail` and proof test land |
</threat_model>

<objective>
Publish ECOS-05: a Mailglass + Chimeway reference blueprint adopters can copy, with CI doc-contract truth lock.

Purpose: Make SEED-003 orchestration vs templating split adoptable via recipe + stable identifiers shared with demo proof (D-01..D-06, D-18).

Output: `guides/recipes/mailglass-integration-blueprint.md`, optional `custom-adapter.md` cross-link, and `describe "mailglass blueprint recipe doc contract (ECOS-05)"` in `doc_contract_test.exs`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-blueprint-demo-proof/56-CONTEXT.md
@.planning/phases/56-blueprint-demo-proof/56-RESEARCH.md
@.planning/phases/56-blueprint-demo-proof/56-VALIDATION.md
@.planning/seeds/SEED-003-ecosystem-integrations.md
@guides/recipes/mention-escalation.md
@guides/recipes/custom-adapter.md
@test/chimeway/doc_contract_test.exs
@examples/chimeway_demo_host/lib/demo_host/notifiers/invite_sent.ex
@examples/chimeway_demo_host/lib/demo_host/mailers/invite_email.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish mailglass-integration-blueprint.md (D-01, D-02, D-03, D-04, D-17, D-18)</name>
  <files>guides/recipes/mailglass-integration-blueprint.md, guides/recipes/custom-adapter.md</files>
  <read_first>
    - `guides/recipes/mention-escalation.md` — persona sections, prerequisites, runnable demo pointers
    - `guides/recipes/custom-adapter.md` — existing Mailglass stub (D-18 additive cross-link target)
    - `.planning/seeds/SEED-003-ecosystem-integrations.md` — orchestration vs templating split prose
    - `.planning/phases/56-blueprint-demo-proof/56-CONTEXT.md` — D-01..D-04, D-17, D-18
    - `examples/chimeway_demo_host/lib/demo_host/notifiers/invite_sent.ex` — copy-paste notifier source
    - `examples/chimeway_demo_host/lib/demo_host/mailers/invite_email.ex` — mailable reference (from 56-01)
  </read_first>
  <action>
    Create `guides/recipes/mailglass-integration-blueprint.md` with sections:

    1. **Who this is for** — Adopter (host app wiring) and Feature Developer (notifier authoring) personas (RECP-03 style)

    2. **Prerequisites** — Link [Golden Path](../introduction/golden-path.md) and [Custom adapter](custom-adapter.md) Mailglass stub section (D-18)

    3. **Responsibility split (SEED-003)** — Explicit prose:
       - Chimeway: when/why, durable lifecycle, suppression, idempotency, operator traces
       - Mailglass: templating, MJML, Swoosh delivery
       - Include **both** product name `Chimeway.Adapter.Mailglass` and module `Chimeway.Adapters.Mailglass` (Phase 54 D-07)

    4. **Notifier authoring** — Copy-paste excerpt showing `notification_key` `"teampulse.invite_sent"`, `render_key` `"teampulse.invite_sent.email"` in `rendering/2`; pointer to `DemoHost.Notifiers.InviteSent` (D-03, D-04)

    5. **Adapter registration** — Copy-paste `config :chimeway` block with `channel_adapters` and `channel_adapter_configs` / `mailables` map using `DemoHost.Mailers.InviteEmail` (D-02)

    6. **Host mailable mapping** — Short section on host-owned `Mailglass.Mailable` module; pointer to `DemoHost.Mailers.InviteEmail`

    7. **Trigger example** — `Chimeway.trigger(DemoHost.Notifiers.InviteSent, %{email: ..., team_name: ...}, idempotency_key:, tenant_id: "teampulse")` referencing `DemoHost.Seeds.seed_invite/0` as runnable demo trigger

    8. **Operator trace inspectability** — Note `/admin/chimeway` search by recipient identity; stable notification key visible on delivery detail (DEMO-06 alignment, no Phase 57 inbound webhook route docs)

    9. **Out of scope** — One short paragraph: golden-path integration guide, `mix verify.mailglass`, Mailglass inbound webhook host route → Phase 57 (D-17)

    In `guides/recipes/custom-adapter.md`, add one sentence cross-link under the Mailglass stub section pointing to `mailglass-integration-blueprint.md` for the full TeamPulse blueprint (D-18; do not replace stub).
  </action>
  <acceptance_criteria>
    - File `guides/recipes/mailglass-integration-blueprint.md` exists with all sections above
    - Recipe contains stable strings `teampulse.invite_sent` and `teampulse.invite_sent.email` (not module names as durable identity)
    - Recipe references `DemoHost.Notifiers.InviteSent`, `DemoHost.Mailers.InviteEmail`, and `DemoHost.Seeds.seed_invite/0`
    - Recipe does **not** document Phase 57 items: `mix verify.mailglass`, demo inbound webhook route wiring, or DOCS-06 golden-path guide as primary deliverable
    - `custom-adapter.md` links to the new blueprint recipe
  </acceptance_criteria>
  <verify>test -f guides/recipes/mailglass-integration-blueprint.md && rg -q 'teampulse\.invite_sent' guides/recipes/mailglass-integration-blueprint.md && rg -q 'Chimeway\.Adapters\.Mailglass' guides/recipes/mailglass-integration-blueprint.md</verify>
</task>

<task type="auto">
  <name>Task 2: ECOS-05 doc-contract describe block (D-05, D-06)</name>
  <files>test/chimeway/doc_contract_test.exs</files>
  <read_first>
    - `test/chimeway/doc_contract_test.exs` — `@recipe_forbidden_strings`, RECP-01/02/03 describe pattern
    - `guides/recipes/mailglass-integration-blueprint.md` — phrase inventory to lock
    - `.planning/phases/56-blueprint-demo-proof/56-CONTEXT.md` — D-05, D-06 minimum required phrases
  </read_first>
  <action>
    In `test/chimeway/doc_contract_test.exs`:

    1. Add module attribute:
       `@mailglass_blueprint_recipe Path.expand("../../guides/recipes/mailglass-integration-blueprint.md", __DIR__)`

    2. Add `describe "mailglass blueprint recipe doc contract (ECOS-05)"` with:
       - `setup` reading recipe file into `content`
       - `for forbidden <- @recipe_forbidden_strings` tests (same as RECP-01)
       - `test` forbidding fictional `Chimeway.Workflow` module (regex `~r/Chimeway\.Workflow(?![s])/`)
       - `@required ~w(...)` list including at minimum:
         - `Chimeway.Adapters.Mailglass`
         - `Chimeway.Adapter.Mailglass` (product name D-07)
         - `channel_adapters`
         - `channel_adapter_configs`
         - `render_key`
         - `teampulse.invite_sent`
         - `teampulse.invite_sent.email`
         - `DemoHost.Notifiers.InviteSent`
         - `DemoHost.Mailers.InviteEmail`
         - orchestration/templating responsibility phrases (e.g. include both `orchestrat` and `templat` substring checks OR explicit multi-word phrases present in recipe)
       - `for required <- @required` assertion tests

    3. Do **not** add Phase 57 guide paths or `mix verify.mailglass` to required phrases.
  </action>
  <acceptance_criteria>
    - `describe "mailglass blueprint recipe doc contract (ECOS-05)"` exists in `doc_contract_test.exs`
    - Describe reuses `@recipe_forbidden_strings` and includes fictional Workflow regex test
    - All `@required` phrases are present in the recipe file today
    - `MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --warnings-as-errors</verify>
</task>

</tasks>

<verification>
After both tasks:
- `MIX_ENV=test mix test test/chimeway/doc_contract_test.exs --warnings-as-errors`
- `mix ci.verify_gates` (if available) or confirm doc-contract alias still targets same test file
</verification>

## PLAN COMPLETE
