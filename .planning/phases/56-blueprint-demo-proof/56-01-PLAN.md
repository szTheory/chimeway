---
phase: 56-blueprint-demo-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/chimeway_demo_host/mix.exs
  - examples/chimeway_demo_host/config/test.exs
  - examples/chimeway_demo_host/test/test_helper.exs
  - examples/chimeway_demo_host/lib/demo_host/mailers/invite_email.ex
  - examples/chimeway_demo_host/test/demo_host_web/mailglass_delivery_proof_test.exs
requirements: [DEMO-06]
must_haves:
  truths:
    - "Demo host delivers invite email through Chimeway.Adapters.Mailglass when :mailglass tests run"
    - "Operator can inspect teampulse.invite_sent delivery at /admin/chimeway with Mailglass adapter visible in trace detail"
    - "JOUR-01..08 journey suite still passes with default Logger adapter (no global Mailglass registration)"
  artifacts:
    - path: "examples/chimeway_demo_host/mix.exs"
      provides: "Non-optional {:mailglass, ~> 1.3} host dependency"
      contains: "mailglass"
    - path: "examples/chimeway_demo_host/lib/demo_host/mailers/invite_email.ex"
      provides: "Host mailable for teampulse.invite_sent.email render_key"
      contains: "Mailglass.Mailable"
    - path: "examples/chimeway_demo_host/test/demo_host_web/mailglass_delivery_proof_test.exs"
      provides: "DEMO-06 proof with @moduletag :mailglass"
      contains: "Chimeway.Adapters.Mailglass"
  key_links:
    - from: "examples/chimeway_demo_host/test/demo_host_web/mailglass_delivery_proof_test.exs"
      to: "DemoHost.Seeds.seed_invite/0"
      via: "Chimeway.trigger with Mailglass channel_adapters in setup"
      pattern: "seed_invite"
    - from: "examples/chimeway_demo_host/test/demo_host_web/mailglass_delivery_proof_test.exs"
      to: "/admin/chimeway"
      via: "LiveViewTest trace search by alex_identity"
      pattern: "teampulse.invite_sent"
    - from: "DemoHost.Notifiers.InviteSent"
      to: "DemoHost.Mailers.InviteEmail"
      via: "mailables map key teampulse.invite_sent.email"
      pattern: "teampulse.invite_sent.email"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-56-01 | Global Mailglass adapter in demo `config/test.exs` breaks JOUR-01..08 (Logger path) | medium | Register `Chimeway.Adapters.Mailglass` only in `:mailglass`-tagged test `setup` via `Application.put_env`; restore on `on_exit` (D-10) |
| T-56-02 | Raw PII (email body, recipient) leaks into attempt meta or admin trace HTML | high | Assert `adapter_module` contains Mailglass module string only; refute raw `@alex_email` or full `html_body` in trace detail render; rely on adapter `redact_meta/1` (Phase 54) |
| T-56-03 | Mailglass TestRepo sandbox not started in demo host → flaky or cross-test pollution | medium | Mirror root `test/test_helper.exs` Mailglass repo bootstrap in demo `test/test_helper.exs`; use `Fake.checkout/0` + `set_shared/1` per mailglass test module |
</threat_model>

<objective>
Prove DEMO-06 on the TeamPulse demo host: outbound invite email delivers through `Chimeway.Adapters.Mailglass` with operator trace inspectability at `/admin/chimeway`, without breaking the existing `:journey` CI gate.

Purpose: Runnable adoption proof that mirrors stable keys `teampulse.invite_sent` / `teampulse.invite_sent.email` (D-03, D-07, D-15, D-16).

Output: Mailglass dep + test harness, `DemoHost.Mailers.InviteEmail`, `mailglass_delivery_proof_test.exs` green under `--only mailglass` and `mix verify.journeys` green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-blueprint-demo-proof/56-CONTEXT.md
@.planning/phases/56-blueprint-demo-proof/56-RESEARCH.md
@.planning/phases/56-blueprint-demo-proof/56-VALIDATION.md
@examples/chimeway_demo_host/lib/demo_host/notifiers/invite_sent.ex
@examples/chimeway_demo_host/lib/demo_host/seeds.ex
@examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs
@config/test.exs
@test/test_helper.exs
@test/support/chimeway/mailglass_fixtures.ex
@test/chimeway/adapters/mailglass_adapter_test.exs
@lib/chimeway/adapters/mailglass.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mailglass dependency and demo host test harness (D-09, D-11)</name>
  <files>examples/chimeway_demo_host/mix.exs, examples/chimeway_demo_host/config/test.exs, examples/chimeway_demo_host/test/test_helper.exs</files>
  <read_first>
    - `examples/chimeway_demo_host/mix.exs` — current deps list (no mailglass yet)
    - `config/test.exs` lines 29–50 — root Mailglass Fake/TestRepo/tenancy block to mirror
    - `test/test_helper.exs` — Mailglass.TestRepo storage_up, migrations, sandbox mode
    - `.planning/phases/56-blueprint-demo-proof/56-CONTEXT.md` — D-09, D-10, D-11
    - `mix.exs` root — `{:mailglass, "~> 1.3"}` version pin
  </read_first>
  <action>
    1. Add `{:mailglass, "~> 1.3"}` to `examples/chimeway_demo_host/mix.exs` `deps/0` (non-optional host dep per D-09).

    2. Append a Mailglass test harness block to `examples/chimeway_demo_host/config/test.exs` mirroring root `config/test.exs` (Fake adapter, `Mailglass.TestRepo`, tenancy, suppression_store, async_adapter, adapter_endpoint, tracking salts, TestRepo DB credentials with `MIX_TEST_PARTITION` suffix). Use a distinct database name such as `chimeway_demo_mailglass_test#{partition}` to avoid clashing with root mailglass DB.

    3. **Do NOT** set `config :chimeway, channel_adapters: %{... Mailglass ...}` in demo `config/test.exs` — journey tests must keep default Logger routing (D-10).

    4. Extend `examples/chimeway_demo_host/test/test_helper.exs` with the same Mailglass.TestRepo bootstrap as root `test/test_helper.exs`: `Application.ensure_all_started(:mailglass)`, `storage_up`, run migrations from `Path.expand("../../../test/support/mailglass/migrations", __DIR__)`, start TestRepo, `Ecto.Adapters.SQL.Sandbox.mode(Mailglass.TestRepo, :manual)`. Wrap in `if Code.ensure_loaded?(Mailglass)` guard.

    5. Run `cd examples/chimeway_demo_host && mix deps.get && mix compile --warnings-as-errors`.
  </action>
  <acceptance_criteria>
    - `examples/chimeway_demo_host/mix.exs` lists `{:mailglass, "~> 1.3"}`
    - Demo `config/test.exs` contains Mailglass Fake + TestRepo config and does **not** register global `channel_adapters` Mailglass mapping
    - Demo `test/test_helper.exs` starts Mailglass.TestRepo with migrations applied
    - `cd examples/chimeway_demo_host && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>cd examples/chimeway_demo_host && mix deps.get && mix compile --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 2: DemoHost.Mailers.InviteEmail mailable (D-13, D-14)</name>
  <files>examples/chimeway_demo_host/lib/demo_host/mailers/invite_email.ex</files>
  <read_first>
    - `examples/chimeway_demo_host/lib/demo_host/notifiers/invite_sent.ex` — `rendering/2` assigns (`subject`, `html_body`, `text_body`, `headline`, etc.)
    - `test/support/chimeway/mailglass_fixtures.ex` — `TestMailer` / `test_email/1` pattern (reference only)
    - `lib/chimeway/adapters/mailglass.ex` — `render_data` passed to Mailglass outbound
    - `.planning/phases/56-blueprint-demo-proof/56-CONTEXT.md` — D-13, D-14, stable render_key
  </read_first>
  <action>
    Create `lib/demo_host/mailers/invite_email.ex`:

    - `defmodule DemoHost.Mailers.InviteEmail`
    - `use Mailglass.Mailable, stream: :transactional`
    - Implement `invite_email/1` (or `:email/1` — pick one, document in moduledoc) accepting assigns map from notifier `rendering/2`
    - Build Swoosh email using `subject`, `html_body`, `text_body` from assigns; set `to` from delivery render_data recipient (follow `MailglassFixtures.TestMailer` pattern: read `"to"` or derive from known demo seed email `alex@teampulse.test` when assign missing)
    - `|> Mailglass.Message.put_function(:invite_email)` (match chosen function name)

    Module is host-owned under `DemoHost.Mailers` namespace — do not add to Chimeway core test support.
  </action>
  <acceptance_criteria>
    - File exists at `examples/chimeway_demo_host/lib/demo_host/mailers/invite_email.ex`
    - Module `use Mailglass.Mailable` and exports the function referenced by future mailable map `{DemoHost.Mailers.InviteEmail, :invite_email}`
    - `cd examples/chimeway_demo_host && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <verify>cd examples/chimeway_demo_host && mix compile --warnings-as-errors</verify>
</task>

<task type="auto">
  <name>Task 3: Mailglass delivery + admin trace proof test (D-15, D-16)</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/mailglass_delivery_proof_test.exs</files>
  <read_first>
    - `examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs` — JOUR-04 LiveView search + detail assertions
    - `test/chimeway/adapters/mailglass_adapter_test.exs` — `Fake.checkout`, `channel_adapter_configs` setup/on_exit
    - `test/chimeway/adapters/mailglass_webhook_pipeline_test.exs` — `adapter_module` string assertion pattern
    - `examples/chimeway_demo_host/lib/demo_host/seeds.ex` — `seed_invite/0`, `alex_identity/0`
    - `.planning/phases/56-blueprint-demo-proof/56-CONTEXT.md` — D-07, D-10, D-15, D-16
  </read_first>
  <action>
    Create `test/demo_host_web/mailglass_delivery_proof_test.exs`:

    1. `use DemoHostWeb.ConnCase, async: false` and `import Phoenix.LiveViewTest`
    2. `@moduletag :mailglass` on the module (D-10); do **not** add `:journey` tag
    3. `setup` block:
       - `Mailglass.Adapters.Fake.checkout()` and `set_shared(self())`
       - Save/restore prior `Application.get_env(:chimeway, :channel_adapters)` and `:channel_adapter_configs`
       - `put_env` `%{"email" => Chimeway.Adapters.Mailglass}` and configs `%{"email" => [mailables: %{"teampulse.invite_sent.email" => {DemoHost.Mailers.InviteEmail, :invite_email}}]}`
       - Checkout Mailglass.TestRepo sandbox allowance for test pid (mirror root mailglass tests)
       - `on_exit` restores env keys

    4. Test `"DEMO-06 invite email delivers via Mailglass adapter"`:
       - `DemoHost.Seeds.seed_invite/0`
       - Find email-channel `Chimeway.Delivery` for notification key `teampulse.invite_sent`
       - Load `Chimeway.DeliveryAttempt` rows; assert at least one succeeded attempt with `String.contains?(attempt.adapter_module, "Chimeway.Adapters.Mailglass")`
       - Assert `Mailglass.Adapters.Fake.deliveries()` count increased (optional sanity)

    5. Test `"DEMO-06 admin trace shows invite Mailglass delivery"` (same setup):
       - After seed, `live(conn, "/admin/chimeway")`, submit trace search with `DemoHost.Seeds.alex_identity()`, open email delivery detail
       - Assert rendered detail contains `teampulse.invite_sent` and `Chimeway.Adapters.Mailglass` (or `Mailglass` adapter label consistent with admin UI)
       - **T-56-02:** refute full raw invite HTML body string from notifier if it would expose PII beyond operator-safe surfaces; do not assert on private assign blobs

    6. Run journey regression from repo root after test file lands.
  </action>
  <acceptance_criteria>
    - New test module tagged `@moduletag :mailglass` only (not `:journey`)
    - Delivery proof asserts `adapter_module` contains `"Chimeway.Adapters.Mailglass"` for invite email channel
    - Admin trace proof asserts `teampulse.invite_sent` on detail page for Alex search
    - `cd examples/chimeway_demo_host && mix test --only mailglass --warnings-as-errors` exits 0
    - `mix verify.journeys` from repo root exits 0 (10 journey tests unchanged)
  </acceptance_criteria>
  <verify>cd examples/chimeway_demo_host && mix test --only mailglass --warnings-as-errors</verify>
</task>

</tasks>

<verification>
After all tasks:
- `cd examples/chimeway_demo_host && mix test --only mailglass --warnings-as-errors`
- `mix verify.journeys`
</verification>

## PLAN COMPLETE
