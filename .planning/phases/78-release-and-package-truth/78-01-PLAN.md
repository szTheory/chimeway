---
phase: 78-release-and-package-truth
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - README.md
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
requirements:
  - TRUTH-01
  - TRUTH-02
must_haves:
  truths:
    - "TRUTH-01/D-01: root @version, .release-please-manifest.json, release-please-config.json, CHANGELOG.md release heading, HexDocs source_ref, README root install constraint, and release/publish workflow version checks agree on package SemVer such as 1.0.0 and tag refs such as v1.0.0."
    - "TRUTH-02/D-03: package-facing repository/source URLs in mix.exs, README badges/links, CHANGELOG.md links, and release truth contracts use https://github.com/szTheory/chimeway."
    - "D-02: Release Please and publish workflows remain root-package only and do not add sibling publish lanes."
    - "D-04: Phase 78 edits only narrow package-facing README/source truth and does not perform the Phase 79 front-door rewrite or Phase 80 contributor CI cleanup."
  artifacts:
    - path: "mix.exs"
      provides: "Root package metadata, package links, HexDocs source_url/source_ref, verify aliases, and root dependency declarations."
      contains: "@version \"1.0.0\""
    - path: "README.md"
      provides: "Root package install constraint and package-facing badge links."
      contains: "{:chimeway, \"~> 1.0\"}"
    - path: "test/chimeway/release_gate_contract_test.exs"
      provides: "TRUTH-01/TRUTH-02 release/package/source contract tests."
      exports: ["Chimeway.ReleaseGateContractTest"]
  key_links:
    - from: "mix.exs"
      to: ".release-please-manifest.json"
      via: "release-gate contract parses both root @version and manifest root package version"
      pattern: "@version|\\.\"\\s*:"
    - from: "mix.exs"
      to: "README.md"
      via: "release-gate contract derives README install constraint from @version MAJOR.MINOR"
      pattern: "\\{:chimeway, \"~> 1\\.0\"\\}"
    - from: "test/chimeway/release_gate_contract_test.exs"
      to: ".github/workflows/release.yml"
      via: "workflow contract asserts release tag checkout, gate replay, and root Hex build"
      pattern: "release_created|tag_name|mix hex.build"
---

<objective>
Make root release identity and canonical package-facing source links executable truth.

Purpose: Phase 78 needs the root package metadata, Release Please inputs, changelog anchor, HexDocs source refs, README install constraint, and release/publish workflow contracts to agree on the real root `chimeway` package state before artifact proof is added.
Output: Canonical root package/source metadata in `mix.exs` and `README.md`, plus release-gate contracts that fail on package version drift, release namespace drift, stale source URLs, or sibling publish-lane drift.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/77-truth-baseline-and-package-model-decision/77-PACKAGE-MODEL-DECISION.md
@.planning/phases/77-truth-baseline-and-package-model-decision/77-02-SUMMARY.md
@.planning/phases/78-release-and-package-truth/78-CONTEXT.md
@.planning/phases/78-release-and-package-truth/78-RESEARCH.md
@.planning/phases/78-release-and-package-truth/78-VALIDATION.md
@.planning/phases/78-release-and-package-truth/78-PATTERNS.md
@mix.exs
@README.md
@CHANGELOG.md
@.release-please-manifest.json
@release-please-config.json
@.github/workflows/release.yml
@.github/workflows/publish-hex.yml
@test/chimeway/release_gate_contract_test.exs
</context>

## Artifacts this phase produces

- Modified file `mix.exs`:
  - Package link value `https://github.com/szTheory/chimeway`.
  - HexDocs `source_url` value `https://github.com/szTheory/chimeway`.
  - Existing HexDocs `source_ref: "v#{@version}"` preserved.
  - Existing Mix alias `ci.verify_gates` preserved as the ExUnit contract entrypoint.
- Modified file `README.md`:
  - Root dependency snippet `{:chimeway, "~> 1.0"}` preserved.
  - CI badge image/link moved to `https://github.com/szTheory/chimeway/actions/workflows/ci.yml`.
- Modified module `Chimeway.ReleaseGateContractTest` in `test/chimeway/release_gate_contract_test.exs`:
  - New module attributes `@canonical_repo_url`, `@legacy_repo_url`, and `@package_facing_source_files`.
  - New contract test `root package release identity uses package SemVer refs`.
  - New contract test `Release Please config stays root-only`.
  - New contract test `package-facing repository and source URLs use the canonical repository`.
  - New contract test `release and publish workflows stay rooted on chimeway`.
  - New helper `root_version!/0` if useful for sharing version parsing.
  - Existing helpers `extract_ci_job_block/2` and `extract_ci_gate_needs/1` preserved.
- Workflow keys read and contract-tested but not created: `release-please.outputs.tag_name`, `release-please.outputs.version`, `HEX_API_KEY`, `RELEASE_PLEASE_TOKEN`, `workflow_dispatch.inputs.tag`, and `workflow_dispatch.inputs.release_version`.

<tasks>

<task type="auto">
  <name>Task 1: Normalize root package source metadata and contract release identity</name>
  <files>mix.exs, README.md, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
  - AGENTS.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/77-truth-baseline-and-package-model-decision/77-PACKAGE-MODEL-DECISION.md
  - .planning/phases/78-release-and-package-truth/78-CONTEXT.md
  - .planning/phases/78-release-and-package-truth/78-RESEARCH.md
  - .planning/phases/78-release-and-package-truth/78-VALIDATION.md
  - .planning/phases/78-release-and-package-truth/78-PATTERNS.md
  - mix.exs
  - README.md
  - CHANGELOG.md
  - .release-please-manifest.json
  - release-please-config.json
  - test/chimeway/release_gate_contract_test.exs
  </read_first>
  <action>Per D-01 and D-03, update `mix.exs` so `package()` has `links: %{"GitHub" => "https://github.com/szTheory/chimeway"}` and `docs()` has `source_url: "https://github.com/szTheory/chimeway"` while preserving `@version "1.0.0"`, `version: @version`, `files: ~w(lib priv guides CHANGELOG.md LICENSE.md README.md mix.exs .formatter.exs)`, and `source_ref: "v#{@version}"`. Update the README CI badge image URL and badge link to `https://github.com/szTheory/chimeway/actions/workflows/ci.yml`, preserving the Hex badge and the root install snippet `{:chimeway, "~> 1.0"}`. Extend `Chimeway.ReleaseGateContractTest` instead of adding a shell checker per D-07. Add a `root package release identity uses package SemVer refs` contract that parses root `@version`, decodes `.release-please-manifest.json` with `Jason.decode!`, confirms the manifest has exactly the root `"."` entry equal to `@version`, confirms `CHANGELOG.md` has a `## 1.0.0` package release heading for the current version, confirms `mix.exs` keeps `source_ref: "v#{@version}"`, and confirms README contains `{:chimeway, "~> 1.0"}` derived from the current MAJOR.MINOR. Add a `package-facing repository and source URLs use the canonical repository` contract that checks `mix.exs`, `README.md`, `CHANGELOG.md`, `.github/workflows/release.yml`, and `.github/workflows/publish-hex.yml` for the canonical repository value and rejects the legacy owner URL in those Phase 78-owned package-facing surfaces. Do not edit `.release-please-manifest.json`, `release-please-config.json`, `CHANGELOG.md`, release workflows, publish workflows, `CONTRIBUTING.md`, `MAINTAINING.md`, or broad README value-prop/IA copy in this task unless a focused contract failure proves direct Phase 78 package truth drift.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors</automated>
    <automated>mix format --check-formatted mix.exs test/chimeway/release_gate_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
  - Source assertion: `mix.exs` contains `links: %{"GitHub" => "https://github.com/szTheory/chimeway"}`.
  - Source assertion: `mix.exs` contains `source_url: "https://github.com/szTheory/chimeway"` and still contains `source_ref: "v#{@version}"`.
  - Source assertion: `README.md` links the CI badge to `https://github.com/szTheory/chimeway/actions/workflows/ci.yml`.
  - Behavior assertion: `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors` passes and includes contracts for manifest/root version parity, changelog SemVer heading, README install constraint, canonical repository URL, and legacy repository URL rejection.
  - Scope assertion: no sibling package publish lanes, package metadata, or release manifest entries are added for `chimeway_admin` or `chimeway_inbox`.
  </acceptance_criteria>
  <done>Root package metadata and README package-facing badge links use the canonical repository, and release-gate tests fail if root package version, manifest, changelog, HexDocs source ref, README install constraint, or package-facing source URL truth drifts.</done>
</task>

<task type="auto">
  <name>Task 2: Contract root-only Release Please and publish workflows</name>
  <files>test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
  - AGENTS.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/77-truth-baseline-and-package-model-decision/77-PACKAGE-MODEL-DECISION.md
  - .planning/phases/78-release-and-package-truth/78-CONTEXT.md
  - .planning/phases/78-release-and-package-truth/78-RESEARCH.md
  - .planning/phases/78-release-and-package-truth/78-PATTERNS.md
  - test/chimeway/release_gate_contract_test.exs
  - release-please-config.json
  - .github/workflows/release.yml
  - .github/workflows/publish-hex.yml
  </read_first>
  <action>Per D-02 and D-07, add release-gate assertions that keep Release Please and both publish paths rooted on the single root package. Decode `release-please-config.json` with `Jason.decode!` and assert `packages` has exactly the `"."` key, `packages["."]["changelog-path"]` is `CHANGELOG.md`, and `packages["."]["include-v-in-tag"]` is `true`. Extend workflow contracts so `.github/workflows/release.yml` is required to use `release-please-config.json`, `.release-please-manifest.json`, checkout `needs.release-please.outputs.tag_name`, verify `@version "${RELEASE_VERSION}"`, replay `mix ci.verify_gates` and `mix ci.docs`, run `mix hex.build`, and keep `HEX_API_KEY` only on Hex publish steps. Extend recovery publish contracts so `.github/workflows/publish-hex.yml` keeps `workflow_dispatch.inputs.tag`, `workflow_dispatch.inputs.release_version`, the existing tag-or-40-character-SHA validation message, `grep -n "@version \"${RELEASE_VERSION}\"" mix.exs`, `mix ci.verify_gates`, `mix ci.docs`, `mix hex.build`, `mix hex.publish --dry-run --yes`, and `mix hex.publish --yes`. Add assertions that release/publish workflow text does not introduce `chimeway_admin` or `chimeway_inbox` publish jobs, package names, or Hex publish commands. Keep broader required-check topology and contributor CI cleanup out of scope for Phase 80 per D-04.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors</automated>
    <automated>mix format --check-formatted test/chimeway/release_gate_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
  - Behavior assertion: a release-gate contract fails if `release-please-config.json` contains any package key other than `"."`.
  - Behavior assertion: a release-gate contract fails if `include-v-in-tag` is not `true` for the root package.
  - Behavior assertion: release workflow contracts require Release Please config/manifest inputs, release-tag checkout, root version grep, `mix ci.verify_gates`, `mix ci.docs`, and `mix hex.build`.
  - Behavior assertion: recovery workflow contracts require tag-or-SHA validation, root version grep, dry-run support, and root `mix hex.publish` commands.
  - Behavior assertion: workflow contracts reject sibling package publish drift for `chimeway_admin` and `chimeway_inbox`.
  </acceptance_criteria>
  <done>Release Please, automated publish, and recovery publish are contract-tested as root-only package workflows with package SemVer refs, gate replay, and no sibling publish lanes.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 78 | Align root package metadata, release manifest, changelog, HexDocs source refs, README install constraints, package files whitelist, canonical repo/source links, sibling package install-status copy, and package truth contracts. | 78-01, 78-02, 78-03 | COVERED | Plan 78-01 covers root metadata/source/release contracts; 78-02 covers sibling copy/contracts; 78-03 covers package artifact proof.
REQ | TRUTH-01 | Root package version, release manifest, changelog, HexDocs source ref, README install guidance, release automation, package files whitelist, and unpacked artifact proof agree. | 78-01, 78-03 | COVERED | 78-01 covers metadata/release identity; 78-03 covers package build and artifact proof.
REQ | TRUTH-02 | Canonical package-facing repository/source links use https://github.com/szTheory/chimeway and reject stale package-facing links. | 78-01, 78-03 | COVERED | 78-01 updates and contracts source URLs; 78-03 verifies the packaged artifact carries them.
REQ | TRUTH-03 | Sibling docs state preview/path status and do not advertise current Hex dependency installs. | 78-02, 78-03 | COVERED | 78-02 updates guide copy/contracts; 78-03 proves package artifact includes those corrected guides.
RESEARCH | Default Hex build blocker | Default `mix hex.build --unpack` fails because `sigra` uses an overridden dependency; fix before artifact proof. | 78-03 | COVERED | 78-03 starts with the blocker remediation task.
RESEARCH | ExUnit truth anchors | Extend `release_gate_contract_test.exs` and `doc_contract_test.exs`; do not add a shell-only checker. | 78-01, 78-02, 78-03 | COVERED | All truth checks are ExUnit contracts plus existing Mix aliases.
RESEARCH | Local-first deterministic package proof | Use local `mix hex.build --unpack --output DIR`; live Hex checks remain manual/release-time evidence. | 78-03 | COVERED | Artifact proof is local temp-dir based.
CONTEXT | D-01 | Preserve root-only `chimeway` release identity and package SemVer tags. | 78-01, 78-03 | COVERED | Release identity contracts and artifact proof.
CONTEXT | D-02 | Keep Release Please and publish workflows rooted on the single root package. | 78-01 | COVERED | Workflow contracts reject sibling publish lanes.
CONTEXT | D-03 | Normalize package-facing repository/source truth to canonical repo URL. | 78-01, 78-03 | COVERED | Metadata/docs update and artifact source proof.
CONTEXT | D-04 | Leave broad front-door docs rewrite and contributor cleanup to Phases 79/80. | 78-01, 78-02 | COVERED | Task actions restrict README and workflow edits to package truth.
CONTEXT | D-05 | Keep sibling packages as in-repo preview/path packages. | 78-02 | COVERED | Guide copy and doc contracts.
CONTEXT | D-06 | Remove current Hex dependency claims for unpublished siblings and contract public copy. | 78-02 | COVERED | Doc contracts forbid sibling current-Hex install snippets.
CONTEXT | D-07 | Extend existing ExUnit anchors rather than a parallel shell checker. | 78-01, 78-02, 78-03 | COVERED | Release/doc contracts are the primary truth checks.
CONTEXT | D-08 | Package artifact proof builds on `mix hex.build --unpack` / `verify.parity`. | 78-03 | COVERED | Artifact plan owns verify.parity and unpack contract.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo package metadata -> Hex/HexDocs consumers | Root package metadata and docs links become public package-facing truth after release. |
| release workflow inputs -> publish action | Release Please outputs and manual recovery inputs control which ref is built and published. |
| local contracts -> release confidence | ExUnit contracts decide whether package/release truth is allowed to ship. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Tampering | `mix.exs` `@version`, package metadata, docs source refs | mitigate | Contract-test root `@version`, manifest version, README install constraint, changelog SemVer heading, and `source_ref: "v#{@version}"` per D-01. |
| T-78-02 | Spoofing | package-facing repository/source links | mitigate | Normalize `mix.exs` and README package-facing links to `https://github.com/szTheory/chimeway` and add release-gate stale URL rejection per D-03. |
| T-78-03 | Tampering | `.github/workflows/release.yml` and `.github/workflows/publish-hex.yml` | mitigate | Contract-test release tag checkout, root `RELEASE_VERSION` grep, gate replay, root `mix hex.build`, and absence of sibling publish lanes per D-02. |
| T-78-04 | Information Disclosure | `HEX_API_KEY` and release tokens | mitigate | Keep secret names only in workflow secret contexts; contract workflow shape without printing secret values. |
| T-78-SC | Tampering | package-manager installs | accept | No new npm/pip/cargo/Hex package install is planned; existing dependencies and Mix aliases are used. |
</threat_model>

<verification>
Plan-level automated checks:
- `MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --warnings-as-errors`
- `mix format --check-formatted mix.exs test/chimeway/release_gate_contract_test.exs`
</verification>

<success_criteria>
- Root package metadata and docs source URL use `https://github.com/szTheory/chimeway`.
- README package-facing CI badge uses `https://github.com/szTheory/chimeway/actions/workflows/ci.yml`.
- Release-gate contracts cover root `@version`, manifest root entry, Release Please root-only config, changelog package heading, HexDocs `source_ref`, README install constraint, canonical repo/source URL, and root-only publish workflows.
- No sibling package release manifest entries, Release Please package entries, or publish workflow lanes are added.
</success_criteria>

<output>
Create `.planning/phases/78-release-and-package-truth/78-01-SUMMARY.md` when done.
</output>
