---
phase: 08-trigger-dispatch-outcome-surfacing
plan: "08-02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - test/chimeway/trigger_pipeline_test.exs
  - test/chimeway/dispatch/sync_test.exs
  - test/chimeway/dispatch/oban_test.exs
autonomous: true
requirements:
  - DLVR-04
  - OPS-01
user_setup: []
must_haves:
  truths:
    - "Trigger tests assert `dispatch_outcome`, `dispatch_mode`, and `trace` fields for success and failure cases."
    - "Trigger dispatch failure remains `{:ok, map}` with explicit `dispatch_outcome: {:error, reason}`."
    - "Duplicate idempotency path remains non-dispatching in tests (dispatcher not invoked)."
    - "Sync and Oban dispatch tests keep error-shape compatibility (`{:error, {:planning_failed, reason}}` where applicable)."
    - "Outcome metadata assertions remain deterministic and independent of adapter implementation details."
  artifacts:
    - path: test/chimeway/trigger_pipeline_test.exs
      provides: "Trigger contract regression matrix for dispatch visibility and duplicate bypass guarantees"
      exports:
        - "Chimeway.Trigger.trigger/3 contract assertions"
    - path: test/chimeway/dispatch/sync_test.exs
      provides: "Sync dispatcher contract compatibility assertions used by trigger outcome normalization"
    - path: test/chimeway/dispatch/oban_test.exs
      provides: "Oban dispatcher enqueue/planning contract compatibility assertions used by trigger outcome normalization"
  key_links:
    - from: test/chimeway/trigger_pipeline_test.exs
      to: lib/chimeway/trigger.ex
      via: "Tests lock enriched response fields added in Phase 8 trigger implementation."
      pattern: "dispatch_outcome|dispatch_mode|trace"
    - from: test/chimeway/trigger_pipeline_test.exs
      to: lib/chimeway/dispatch/sync.ex
      via: "Failure and success tests verify trigger handles dispatcher result contracts as-is."
      pattern: "planning_failed|\\{:error, reason\\}"
    - from: test/chimeway/dispatch/oban_test.exs
      to: lib/chimeway/dispatch/oban.ex
      via: "Oban contract tests preserve enqueue/planning error shape used by trigger."
      pattern: "planning_failed|dispatch\\("
---

<objective>
Add contract-level tests that prove Phase 8 outcome surfacing behavior and guard against regressions in duplicate semantics and dispatcher error-shape compatibility.

Purpose: lock caller-visible behavior before integration verification.
Output: focused test coverage for trigger response enrichment plus sync/Oban contract parity checks.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-trigger-dispatch-outcome-surfacing/08-CONTEXT.md
@.planning/phases/08-trigger-dispatch-outcome-surfacing/08-RESEARCH.md
@lib/chimeway/trigger.ex
@lib/chimeway/dispatch/sync.ex
@lib/chimeway/dispatch/oban.ex
@test/chimeway/trigger_pipeline_test.exs
@test/chimeway/dispatch/sync_test.exs
@test/chimeway/dispatch/oban_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 08-02-01: Add trigger success/failure outcome envelope assertions</name>
  <files>test/chimeway/trigger_pipeline_test.exs</files>
  <read_first>test/chimeway/trigger_pipeline_test.exs, lib/chimeway/trigger.ex, lib/chimeway/dispatch/sync.ex, .planning/phases/08-trigger-dispatch-outcome-surfacing/08-CONTEXT.md</read_first>
  <action>Extend `trigger_pipeline_test.exs` with explicit outcome envelope checks:

1. In a successful trigger scenario, assert all new keys:
   - `result.dispatch_outcome == :ok`
   - `result.dispatch_mode in [:sync, :oban, :unknown]` (expected default path is `:sync`)
   - `is_map(result.trace)`
   - `result.trace.event_id == result.event.id`
   - `Map.has_key?(result.trace, :correlation_id)`
   - `is_list(result.trace.delivery_ids)`
2. Add a dedicated failing-dispatch test using a local test dispatcher module returning `{:error, :forced_dispatch_failure}`.
3. In failing-dispatch test, assert:
   - trigger still returns `{:ok, result}`
   - `result.dispatch_outcome == {:error, :forced_dispatch_failure}`
   - `result.trace.event_id` and `result.trace.correlation_id` are still present.
4. Restore previous `:dispatcher` app env in `on_exit` to avoid cross-test leakage.</action>
  <verify>rg "dispatch_outcome|dispatch_mode|trace" test/chimeway/trigger_pipeline_test.exs && rg "forced_dispatch_failure" test/chimeway/trigger_pipeline_test.exs && mix test test/chimeway/trigger_pipeline_test.exs</verify>
  <acceptance_criteria>
    - trigger success path asserts all enriched outcome keys
    - dispatch failure path asserts caller-visible structured error outcome
    - tests prove failure does not change top-level `{:ok, map}` shape
    - dispatcher env is restored after test execution
  </acceptance_criteria>
  <done>Trigger-level DLVR-04 visibility contract is explicitly locked by unit tests.</done>
</task>

<task type="auto">
  <name>Task 08-02-02: Add duplicate idempotency non-dispatch regression test</name>
  <files>test/chimeway/trigger_pipeline_test.exs</files>
  <read_first>test/chimeway/trigger_pipeline_test.exs, lib/chimeway/trigger.ex, test/chimeway/integration/delivery_lifecycle_test.exs</read_first>
  <action>Add a duplicate-path regression proving dispatcher is not called:

1. Introduce a local spy dispatcher module that sends:
```elixir
send(self(), {:dispatch_called, notifications})
```
before returning `{:ok, []}`.
2. Set app env `:dispatcher` to spy module for this test only.
3. Trigger once with idempotency key (creates durable rows).
4. Trigger second time with the same idempotency key and assert `{:duplicate, event}`.
5. Assert no second dispatch invocation message is received on duplicate path:
```elixir
refute_receive {:dispatch_called, _}
```
6. Keep existing duplicate row-count assertions intact.</action>
  <verify>rg "dispatch_called|refute_receive" test/chimeway/trigger_pipeline_test.exs && rg "\\{:duplicate, _event\\}" test/chimeway/trigger_pipeline_test.exs && mix test test/chimeway/trigger_pipeline_test.exs</verify>
  <acceptance_criteria>
    - duplicate-trigger test confirms non-dispatching behavior
    - dispatcher spy demonstrates duplicate short-circuit path is preserved
    - existing idempotency durability assertions remain true
  </acceptance_criteria>
  <done>Phase 8 preserves idempotency safety while adding outcome surfacing.</done>
</task>

<task type="auto">
  <name>Task 08-02-03: Add sync/Oban contract parity assertions for trigger consumers</name>
  <files>test/chimeway/dispatch/sync_test.exs, test/chimeway/dispatch/oban_test.exs</files>
  <read_first>test/chimeway/dispatch/sync_test.exs, test/chimeway/dispatch/oban_test.exs, lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban.ex, lib/chimeway/dispatch.ex</read_first>
  <action>Add explicit dispatcher return-contract tests that Trigger relies on:

1. In `sync_test.exs`, add one planner-failure scenario by stubbing notifier/planner input to force a planning error and assert shape:
```elixir
{:error, {:planning_failed, reason}}
```
2. In `oban_test.exs`, add matching planning-failure assertion with the same tagged error shape.
3. Keep existing success-path assertions unchanged (`{:ok, deliveries}` list shape).
4. Add comments tagged with `DLVR-04` indicating these tests protect trigger-level outcome normalization assumptions.</action>
  <verify>rg "planning_failed" test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs && rg "DLVR-04" test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs && mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs</verify>
  <acceptance_criteria>
    - sync and oban suites each assert planning failure error tagging
    - success tuple contract remains `{:ok, deliveries}`
    - dispatch contract assumptions used by Trigger are explicit in tests
    - targeted dispatch tests pass after additions
  </acceptance_criteria>
  <done>Dispatcher contract parity assumptions are codified for Phase 8 trigger consumers.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-08-02-CONTRACT-DRIFT
    severity: high
    threat: "Trigger outcome normalization drifts from dispatcher contracts and mislabels failures."
    mitigation: "Sync and Oban tests assert explicit planning-failure shapes and success tuple invariants."
    mitigation_check: "rg 'planning_failed' test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs"
  - id: TM-08-02-DUPLICATE-SIDE-EFFECT
    severity: high
    threat: "Duplicate trigger path accidentally dispatches or enqueues work."
    mitigation: "Duplicate-path spy dispatcher test asserts no dispatch invocation on second trigger."
    mitigation_check: "rg 'refute_receive \\{:dispatch_called, _\\}' test/chimeway/trigger_pipeline_test.exs"
  - id: TM-08-02-HIDDEN-FAILURE-REGRESSION
    severity: medium
    threat: "Future refactors revert to warning-only dispatch failures."
    mitigation: "Failure-path test asserts `dispatch_outcome: {:error, reason}` in caller-visible payload."
    mitigation_check: "rg 'dispatch_outcome == \\{:error, :forced_dispatch_failure\\}' test/chimeway/trigger_pipeline_test.exs"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix test test/chimeway/trigger_pipeline_test.exs`
- [ ] `mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs`
- [ ] `rg "dispatch_outcome|dispatch_mode|trace" test/chimeway/trigger_pipeline_test.exs`
- [ ] `rg "dispatch_called|refute_receive" test/chimeway/trigger_pipeline_test.exs`
- [ ] `rg "planning_failed" test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs`
</verification>

<success_criteria>
- Trigger test suite proves dispatch/enqueue outcomes are visible to callers in both success and failure paths.
- Duplicate idempotency behavior remains non-dispatching and regression-protected.
- Sync and Oban dispatcher contract assumptions are explicit and parity-safe for trigger outcome mapping.
</success_criteria>

<output>
After completion, create `.planning/phases/08-trigger-dispatch-outcome-surfacing/08-02-SUMMARY.md`.
</output>
