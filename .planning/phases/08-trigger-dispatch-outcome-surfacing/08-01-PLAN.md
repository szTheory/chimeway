---
phase: 08-trigger-dispatch-outcome-surfacing
plan: "08-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/trigger.ex
autonomous: true
requirements:
  - DLVR-04
  - OPS-01
user_setup: []
must_haves:
  truths:
    - "`Chimeway.Trigger` keeps top-level tuple contract unchanged: `{:ok, map} | {:duplicate, event} | {:error, reason}`."
    - "Every `{:ok, map}` trigger response includes `dispatch_outcome` and `trace` keys."
    - "`trace` payload contains `event_id`, `correlation_id`, and `delivery_ids` derived from durable rows."
    - "Dispatcher failures are surfaced in `dispatch_outcome` instead of being warning-only side effects."
    - "Duplicate idempotency path remains non-dispatching and bypasses outcome enrichment."
  artifacts:
    - path: lib/chimeway/trigger.ex
      provides: "Caller-visible dispatch/enqueue outcome envelope and durable trace pointer surfacing"
      exports:
        - "trigger/3"
  key_links:
    - from: lib/chimeway/trigger.ex
      to: lib/chimeway/dispatch/sync.ex
      via: "Trigger surfaces sync dispatch results through additive `dispatch_outcome` fields."
      pattern: "dispatcher\\.dispatch\\("
    - from: lib/chimeway/trigger.ex
      to: lib/chimeway/dispatch/oban.ex
      via: "Trigger surfaces Oban enqueue/planning results through the same normalized outcome contract."
      pattern: "dispatch_mode|delivery_ids"
    - from: lib/chimeway/trigger.ex
      to: lib/chimeway/traces.ex
      via: "Trigger returns pointers that map directly to trace lookup APIs."
      pattern: "event_id|correlation_id|delivery_ids"
---

<objective>
Implement Phase 8 core behavior in `Chimeway.Trigger`: expose dispatch/enqueue outcomes and trace pointers to callers without changing public tuple shapes.

Purpose: close the trigger-side visibility gap where dispatcher failures are logged but hidden from callers.
Output: enriched `{:ok, map}` trigger envelope with normalized dispatch outcome and durable trace correlation fields.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-trigger-dispatch-outcome-surfacing/08-CONTEXT.md
@.planning/phases/08-trigger-dispatch-outcome-surfacing/08-RESEARCH.md
@lib/chimeway/trigger.ex
@lib/chimeway/dispatch/sync.ex
@lib/chimeway/dispatch/oban.ex
@lib/chimeway/traces.ex
</context>

<tasks>

<task type="auto">
  <name>Task 08-01-01: Seed additive dispatch fields in normalized trigger success payload</name>
  <files>lib/chimeway/trigger.ex</files>
  <read_first>lib/chimeway/trigger.ex, .planning/phases/08-trigger-dispatch-outcome-surfacing/08-CONTEXT.md, .planning/phases/08-trigger-dispatch-outcome-surfacing/08-RESEARCH.md</read_first>
  <action>Update `normalize_trigger_result/3` for the `{:ok, %{event: event, notifications: notifications_inserted}}` branch to add explicit default dispatch metadata:

1. Keep all existing keys unchanged.
2. Add `dispatch_outcome: :pending` to indicate post-trigger dispatch has not yet been merged.
3. Add `dispatch_mode: :unknown` as default before dispatcher result is known.
4. Add `trace` map with concrete values:
```elixir
trace: %{
  event_id: event.id,
  correlation_id: event.correlation_id,
  delivery_ids: []
}
```
5. Do not alter `{:duplicate, event}` and error branches in this task.</action>
  <verify>rg "dispatch_outcome: :pending" lib/chimeway/trigger.ex && rg "dispatch_mode: :unknown" lib/chimeway/trigger.ex && rg "delivery_ids: \\[\\]" lib/chimeway/trigger.ex && mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - success envelope preserves all pre-existing keys and tuple shape
    - success envelope includes default `dispatch_outcome`, `dispatch_mode`, and `trace`
    - duplicate and error normalization branches remain unchanged in this task
    - project compiles with warnings treated as errors
  </acceptance_criteria>
  <done>Trigger success payload is pre-wired for deterministic dispatch outcome enrichment.</done>
</task>

<task type="auto">
  <name>Task 08-01-02: Replace dispatch swallowing with structured outcome merge</name>
  <files>lib/chimeway/trigger.ex</files>
  <read_first>lib/chimeway/trigger.ex, lib/chimeway/dispatch/sync.ex, lib/chimeway/dispatch/oban.ex, lib/chimeway/dispatch.ex, .planning/phases/08-trigger-dispatch-outcome-surfacing/08-CONTEXT.md</read_first>
  <action>Rework `dispatch_after_trigger/4` so dispatcher return values are merged into the existing `{:ok, result}` map:

1. Keep dispatcher selection and notification query flow as-is.
2. Extend `dispatch_opts` with event pointers for downstream parity:
```elixir
|> Keyword.put_new(:event_id, event.id)
|> Keyword.put_new(:correlation_id, event.correlation_id)
```
3. Replace swallow logic with explicit merge:
   - on `{:ok, deliveries}`:
     - set `dispatch_outcome: :ok`
     - set `dispatch_mode` using dispatcher module (`Chimeway.Dispatch.Sync -> :sync`, `Chimeway.Dispatch.Oban -> :oban`, fallback `:unknown`)
     - set `trace.delivery_ids` to `Enum.map(deliveries, & &1.id)`
   - on `{:error, reason}`:
     - set `dispatch_outcome: {:error, reason}`
     - keep tuple as `{:ok, map}` (event persistence succeeded)
     - preserve `trace.event_id` and `trace.correlation_id`
4. Remove warning-only behavior as the sole failure signal; logging can remain as secondary signal.
5. Keep non-`{:ok, %{event: event}}` clauses unchanged (`dispatch_after_trigger(result, ...) -> result`).</action>
  <verify>rg "dispatch_outcome: :ok|dispatch_outcome: \\{:error, reason\\}" lib/chimeway/trigger.ex && rg "dispatch_mode" lib/chimeway/trigger.ex && rg "delivery_ids" lib/chimeway/trigger.ex && rg "Keyword\\.put_new\\(:event_id, event\\.id\\)" lib/chimeway/trigger.ex && rg "Keyword\\.put_new\\(:correlation_id, event\\.correlation_id\\)" lib/chimeway/trigger.ex && mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - dispatcher success maps to `dispatch_outcome: :ok` and populated `trace.delivery_ids`
    - dispatcher error maps to `dispatch_outcome: {:error, reason}` without changing top-level tuple
    - `dispatch_mode` is explicit and deterministic for sync vs oban dispatchers
    - no path silently drops dispatcher error information from caller-visible response
    - trigger module compiles cleanly
  </acceptance_criteria>
  <done>Trigger returns caller-visible structured dispatch outcomes with durable trace pointers.</done>
</task>

<task type="auto">
  <name>Task 08-01-03: Add private outcome helpers to keep contract deterministic</name>
  <files>lib/chimeway/trigger.ex</files>
  <read_first>lib/chimeway/trigger.ex, .planning/phases/08-trigger-dispatch-outcome-surfacing/08-RESEARCH.md</read_first>
  <action>Add private helpers in `trigger.ex` to avoid inline shape drift:

1. `dispatch_mode_for/1`:
   - `Chimeway.Dispatch.Sync -> :sync`
   - `Chimeway.Dispatch.Oban -> :oban`
   - `_ -> :unknown`
2. `trace_with_delivery_ids/2`:
   - merges existing trace map and sets `delivery_ids` from delivery structs
3. `merge_dispatch_outcome/4`:
   - centralizes map updates for `dispatch_outcome`, `dispatch_mode`, and `trace`
4. Ensure helpers never alter tuple type; they only update `%{}` content inside existing `{:ok, result}` tuples.</action>
  <verify>rg "defp dispatch_mode_for\\(" lib/chimeway/trigger.ex && rg "defp trace_with_delivery_ids\\(" lib/chimeway/trigger.ex && rg "defp merge_dispatch_outcome\\(" lib/chimeway/trigger.ex && mix compile --warnings-as-errors</verify>
  <acceptance_criteria>
    - helper functions define exactly one canonical way to set dispatch outcome fields
    - sync, oban, and unknown dispatchers are mapped deterministically
    - trace delivery ID enrichment uses one reusable helper path
    - tuple compatibility is preserved by helper usage
  </acceptance_criteria>
  <done>Outcome enrichment logic is centralized and less prone to regression drift.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-08-01-SILENT-DISPATCH-FAILURE
    severity: high
    threat: "Trigger hides dispatch/enqueue failures, causing blind retries and support ambiguity."
    mitigation: "Mandatory `dispatch_outcome` field in every `{:ok, map}` response with explicit error payload."
    mitigation_check: "rg 'dispatch_outcome' lib/chimeway/trigger.ex"
  - id: TM-08-01-TRACE-LINK-BREAK
    severity: high
    threat: "Caller outcome cannot map to durable trace records, blocking incident investigation."
    mitigation: "Expose `trace.event_id`, `trace.correlation_id`, and `trace.delivery_ids` from persisted data."
    mitigation_check: "rg 'event_id|correlation_id|delivery_ids' lib/chimeway/trigger.ex"
  - id: TM-08-01-IDEMPOTENCY-REGRESSION
    severity: medium
    threat: "Outcome enrichment accidentally changes duplicate path behavior and reintroduces dispatch side effects."
    mitigation: "Keep duplicate branch untouched and preserve short-circuit dispatch bypass."
    mitigation_check: "rg '\\{:duplicate, event\\}|dispatch_after_trigger\\(result, _notifier, _params, _opts\\), do: result' lib/chimeway/trigger.ex"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix compile --warnings-as-errors`
- [ ] `mix test test/chimeway/trigger_pipeline_test.exs`
- [ ] `rg "dispatch_outcome" lib/chimeway/trigger.ex`
- [ ] `rg "dispatch_mode" lib/chimeway/trigger.ex`
- [ ] `rg "event_id|correlation_id|delivery_ids" lib/chimeway/trigger.ex`
- [ ] `rg "dispatch_after_trigger\\(result, _notifier, _params, _opts\\), do: result" lib/chimeway/trigger.ex`
</verification>

<success_criteria>
- Trigger success responses include structured dispatch/enqueue outcome and trace pointer metadata.
- Dispatcher failures are caller-visible in the response contract rather than log-only side effects.
- Sync and Oban dispatchers are tagged with explicit mode metadata for stage-aware interpretation.
- Duplicate and error tuple contracts remain backward-compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/08-trigger-dispatch-outcome-surfacing/08-01-SUMMARY.md`.
</output>
