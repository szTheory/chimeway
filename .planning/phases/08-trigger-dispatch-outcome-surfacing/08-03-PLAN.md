---
phase: 08-trigger-dispatch-outcome-surfacing
plan: "08-03"
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - test/chimeway/integration/delivery_lifecycle_test.exs
  - test/chimeway/trigger_pipeline_test.exs
  - test/chimeway/traces_test.exs
autonomous: true
requirements:
  - DLVR-04
  - OPS-01
user_setup: []
must_haves:
  truths:
    - "Integration tests prove trigger-returned trace pointers resolve through `Chimeway.Traces` APIs."
    - "Caller-visible `delivery_ids` from trigger result correspond to durable delivery rows for the same event."
    - "Duplicate trigger outcomes remain idempotent while retaining trace lookup continuity."
    - "OPS-01 explainability flows continue working with Phase 8 trigger outcome enrichment."
    - "Phase 8 closes with full targeted and CI verification evidence."
  artifacts:
    - path: test/chimeway/integration/delivery_lifecycle_test.exs
      provides: "End-to-end trigger outcome to trace lookup evidence across durable lifecycle chain"
    - path: test/chimeway/traces_test.exs
      provides: "Trace API-level correlation guarantees aligned with trigger-returned pointers"
  key_links:
    - from: test/chimeway/integration/delivery_lifecycle_test.exs
      to: lib/chimeway/trigger.ex
      via: "Integration tests verify trigger response fields added in Phase 8 are operationally useful."
      pattern: "dispatch_outcome|trace"
    - from: test/chimeway/integration/delivery_lifecycle_test.exs
      to: lib/chimeway/traces.ex
      via: "Event and correlation pointers from trigger map to trace query APIs."
      pattern: "Traces\\.get_trace|find_traces_by_correlation_id"
    - from: test/chimeway/traces_test.exs
      to: lib/chimeway/events/event.ex
      via: "Trace APIs preserve event/correlation identity required for caller support workflows."
      pattern: "correlation_id"
---

<objective>
Deliver final integration and trace evidence that Phase 8 caller-visible outcomes are durable, queryable, and aligned with operator workflows.

Purpose: prove the new trigger envelope is not just present, but actionable for support and retry flows.
Output: integration tests that map trigger result metadata to `Chimeway.Traces` lookup behavior plus full verification evidence.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-trigger-dispatch-outcome-surfacing/08-CONTEXT.md
@.planning/phases/08-trigger-dispatch-outcome-surfacing/08-RESEARCH.md
@lib/chimeway/trigger.ex
@lib/chimeway/traces.ex
@test/chimeway/integration/delivery_lifecycle_test.exs
@test/chimeway/traces_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 08-03-01: Add trigger-to-trace integration scenario for durable pointer usability</name>
  <files>test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <read_first>test/chimeway/integration/delivery_lifecycle_test.exs, lib/chimeway/trigger.ex, lib/chimeway/traces.ex, .planning/phases/08-trigger-dispatch-outcome-surfacing/08-CONTEXT.md</read_first>
  <action>Add a new integration scenario that executes `Chimeway.trigger/3` and validates returned pointers:

1. Capture `{:ok, result}` from trigger call.
2. Assert:
   - `result.dispatch_outcome in [:ok, {:error, _}]`
   - `is_map(result.trace)`
   - `result.trace.event_id == result.event.id`
   - `Map.has_key?(result.trace, :correlation_id)`
   - `is_list(result.trace.delivery_ids)`
3. Resolve `result.trace.event_id` through:
```elixir
{:ok, trace_event} = Chimeway.Traces.get_trace(result.trace.event_id)
```
and assert `trace_event.id == result.event.id`.
4. If `result.trace.correlation_id` is non-nil, call:
```elixir
events = Chimeway.Traces.find_traces_by_correlation_id(result.trace.correlation_id)
```
and assert the trigger event is included in returned events.
5. Assert `result.trace.delivery_ids` match durable rows for that event:
   - load notification IDs for event
   - load delivery IDs for those notifications
   - compare as sets.</action>
  <verify>rg "dispatch_outcome|trace\\.event_id|find_traces_by_correlation_id|Traces\\.get_trace" test/chimeway/integration/delivery_lifecycle_test.exs && mix test test/chimeway/integration/delivery_lifecycle_test.exs</verify>
  <acceptance_criteria>
    - integration test verifies trigger response pointers are directly usable with trace APIs
    - returned `delivery_ids` are validated against durable delivery rows
    - event and correlation linkage is asserted from caller response to trace surfaces
  </acceptance_criteria>
  <done>Caller-visible Phase 8 fields are proven operationally useful in end-to-end flow.</done>
</task>

<task type="auto">
  <name>Task 08-03-02: Add trace-suite assertions aligned to trigger outcome metadata contract</name>
  <files>test/chimeway/traces_test.exs</files>
  <read_first>test/chimeway/traces_test.exs, lib/chimeway/traces.ex, lib/chimeway/trigger.ex, .planning/phases/08-trigger-dispatch-outcome-surfacing/08-RESEARCH.md</read_first>
  <action>Extend `traces_test.exs` with assertions that align to trigger response contract:

1. Add test proving `get_trace/1` and `find_traces_by_correlation_id/1` can both recover the same event identity for a known `correlation_id`.
2. Add test asserting delivery IDs found via trace preload are suitable for equality checks against trigger `trace.delivery_ids` shape (list of UUIDs).
3. Keep assertions payload-safe: do not assert raw event payload/body values in this plan.
4. Tag new tests with inline comments for `OPS-01` traceability.</action>
  <verify>rg "OPS-01|find_traces_by_correlation_id|get_trace|delivery_ids" test/chimeway/traces_test.exs && mix test test/chimeway/traces_test.exs</verify>
  <acceptance_criteria>
    - trace tests explicitly connect event and correlation lookups in one contract assertion
    - delivery ID extraction assertions align with trigger `trace.delivery_ids` usage
    - new tests preserve existing payload redaction expectations
  </acceptance_criteria>
  <done>Trace API regression suite directly supports Phase 8 caller outcome contract.</done>
</task>

<task type="auto">
  <name>Task 08-03-03: Run final Phase 8 verification matrix and requirement traceability checks</name>
  <files>test/chimeway/integration/delivery_lifecycle_test.exs, test/chimeway/trigger_pipeline_test.exs, test/chimeway/traces_test.exs</files>
  <read_first>.planning/phases/08-trigger-dispatch-outcome-surfacing/08-VALIDATION.md, test/chimeway/integration/delivery_lifecycle_test.exs, test/chimeway/trigger_pipeline_test.exs, test/chimeway/traces_test.exs</read_first>
  <action>Execute and document final verification commands in order:

1. `mix test test/chimeway/trigger_pipeline_test.exs`
2. `mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs`
3. `mix test test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/traces_test.exs`
4. `mix ci.test`
5. `mix ci`

Then run traceability greps:

6. `rg "DLVR-04|OPS-01" test/chimeway/trigger_pipeline_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/traces_test.exs`
7. `rg "dispatch_outcome|dispatch_mode|trace" test/chimeway/trigger_pipeline_test.exs test/chimeway/integration/delivery_lifecycle_test.exs`
8. `rg "event_id|correlation_id|delivery_ids" lib/chimeway/trigger.ex test/chimeway/**/*.exs`

If any step fails, fix implementation/tests and re-run the full sequence until all commands exit 0.</action>
  <verify>mix test test/chimeway/trigger_pipeline_test.exs && mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs && mix test test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/traces_test.exs && mix ci.test && mix ci && rg "DLVR-04|OPS-01" test/chimeway/trigger_pipeline_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/traces_test.exs && rg "dispatch_outcome|dispatch_mode|trace" test/chimeway/trigger_pipeline_test.exs test/chimeway/integration/delivery_lifecycle_test.exs && rg "event_id|correlation_id|delivery_ids" lib/chimeway/trigger.ex test/chimeway/**/*.exs</verify>
  <acceptance_criteria>
    - targeted unit/integration suites pass
    - full CI gates pass
    - requirement traceability for DLVR-04 and OPS-01 is grep-discoverable
    - Phase 8 outcome fields are present in implementation and tests
  </acceptance_criteria>
  <done>Phase 8 verification evidence is complete and release-grade.</done>
</task>

</tasks>

<threat_model>
security_gate:
  enabled: true
  standard: ASVS_L1
  block_on: high

threats:
  - id: TM-08-03-UNUSABLE-TRACE-POINTERS
    severity: high
    threat: "Trigger returns pointers that do not resolve through trace APIs, breaking support workflows."
    mitigation: "Integration test resolves trigger event and correlation pointers through `Chimeway.Traces` APIs."
    mitigation_check: "rg 'Traces\\.get_trace|find_traces_by_correlation_id' test/chimeway/integration/delivery_lifecycle_test.exs"
  - id: TM-08-03-DURABILITY-MISMATCH
    severity: high
    threat: "Trigger `delivery_ids` diverge from durable DB state for the same event."
    mitigation: "Integration test compares trigger `trace.delivery_ids` against DB-derived delivery IDs."
    mitigation_check: "rg 'delivery_ids' test/chimeway/integration/delivery_lifecycle_test.exs"
  - id: TM-08-03-TRACEABILITY-GAP
    severity: medium
    threat: "Outcome contract changes ship without requirement-linked evidence."
    mitigation: "Final verification includes requirement-tag grep checks for DLVR-04 and OPS-01."
    mitigation_check: "rg 'DLVR-04|OPS-01' test/chimeway/trigger_pipeline_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/traces_test.exs"
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `mix test test/chimeway/trigger_pipeline_test.exs`
- [ ] `mix test test/chimeway/dispatch/sync_test.exs test/chimeway/dispatch/oban_test.exs`
- [ ] `mix test test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/traces_test.exs`
- [ ] `mix ci.test`
- [ ] `mix ci`
- [ ] `rg "DLVR-04|OPS-01" test/chimeway/trigger_pipeline_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/traces_test.exs`
- [ ] `rg "dispatch_outcome|dispatch_mode|trace" test/chimeway/trigger_pipeline_test.exs test/chimeway/integration/delivery_lifecycle_test.exs`
- [ ] `rg "event_id|correlation_id|delivery_ids" lib/chimeway/trigger.ex test/chimeway/**/*.exs`
</verification>

<success_criteria>
- Trigger-returned trace pointers are proven resolvable and durable through integration tests.
- DLVR-04 caller outcome visibility and OPS-01 traceability guarantees are both evidenced in tests.
- Full project verification gates pass with Phase 8 changes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-trigger-dispatch-outcome-surfacing/08-03-SUMMARY.md`.
</output>
