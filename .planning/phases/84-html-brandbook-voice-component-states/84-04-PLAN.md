---
phase: 84-html-brandbook-voice-component-states
plan: 04
type: execute
wave: 4
depends_on: ["84-03"]
files_modified:
  - brandbook/index.html
autonomous: false
requirements: [VOICE-01, VOICE-02, VOICE-03]
tags: [brandbook, html, voice, microcopy, naming, checkpoint]

must_haves:
  truths:
    - "Voice/tone is documented by context (docs, errors, marketing, CLI) with concrete good/bad examples lifted verbatim from prompts/chimeway-brand-book.md (VOICE-01)"
    - "A named reusable error-message template ('what happened → why it matters → how to fix') is documented with its canonical example (VOICE-02)"
    - "CTA style + naming rules are documented, including lowercase chimeway graphic vs title-case Chimeway prose, preferred + banned vocabulary (VOICE-03)"
    - "The full guard suite (bash scripts/brandbook-guards.sh) is green and the book opens correctly via file:// in Chromium"
  artifacts:
    - brandbook/index.html
  key_links:
    - "all copy lifts verbatim from prompts/chimeway-brand-book.md — no invented examples (UI-SPEC Copywriting Contract)"
    - "the section-presence guard requires the anchors docs/errors/marketing/cli, the phrase 'what happened', and both chimeway + Chimeway to be present"
---

<objective>
Complete `brandbook/index.html` with the brand-voice content and close the phase: author the voice-by-context section (docs/errors/marketing/CLI with good/bad), the named error-message template (VOICE-02), and the naming/CTA rules (VOICE-03) — then verify the full guard suite is green and the book renders correctly under `file://`.

Purpose: Deliver the VOICE requirements and prove the whole book is file://-safe, scoped, and complete.
Output: `#voice`, `#errors`, `#naming` sections + a human-verified, guard-green book.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-html-brandbook-voice-component-states/84-CONTEXT.md
@.planning/phases/84-html-brandbook-voice-component-states/84-UI-SPEC.md
@.planning/phases/84-html-brandbook-voice-component-states/84-RESEARCH.md
@prompts/chimeway-brand-book.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Voice-by-context section (docs / errors / marketing / CLI) with good/bad</name>
  <files>brandbook/index.html</files>
  <read_first>
    - .planning/phases/84-html-brandbook-voice-component-states/84-UI-SPEC.md (§Copywriting Contract — the voice-by-context matrix with exact good/bad copy and cited line refs)
    - prompts/chimeway-brand-book.md (§25 voice examples :1349-1409; §9 voice principles :251-311 — lift copy verbatim)
  </read_first>
  <action>
    Author the `#voice` section (`.cwb-section`) documenting voice/tone across four contexts, each with a good/bad pair lifted VERBATIM from prompts/chimeway-brand-book.md (do not invent examples) — VOICE-01. Use the four context anchors/labels `docs`, `errors`, `marketing`, `cli` (the guard section-presence check requires all four literals). For each: Docs — good "Use an idempotency key when the same domain event may be retried…" / bad "Chimeway magically knows when not to duplicate messages."; Errors — good "Delivery suppressed: recipient disabled email for `invoice.paid`." / bad "This chime did not ring."; Marketing — the app-owned-database good line / bad "Supercharge your user engagement with next-generation omnichannel notification journeys."; CLI — good "Provider accepted the push notification. Device display is not guaranteed by the provider response." / bad "Push delivered." Render the "bad" examples clearly marked as don'ts (reusing `.cwb-dont` styling where useful). Present the banned-vocabulary list (blast/campaign/journey/omnichannel/AI-powered/magical/zero-config/…) and preferred vocabulary (notification/event/recipient/delivery/attempt/trace/policy/…).
  </action>
  <verify>
    <automated>grep -q 'id="voice"' brandbook/index.html && for a in docs errors marketing cli; do grep -qi "$a" brandbook/index.html || { echo "MISSING $a"; exit 1; }; done && grep -q 'idempotency key' brandbook/index.html && grep -q 'omnichannel' brandbook/index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Four voice contexts (docs/errors/marketing/cli) each with a verbatim good/bad pair
    - Banned + preferred vocabulary lists present
    - Copy lifted verbatim from prompts/chimeway-brand-book.md (no invented examples)
  </acceptance_criteria>
  <done>Voice-by-context section documents all four contexts with verbatim good/bad examples and the vocabulary lists.</done>
</task>

<task type="auto">
  <name>Task 2: Named error-message template (VOICE-02) + naming/CTA rules (VOICE-03)</name>
  <files>brandbook/index.html</files>
  <read_first>
    - .planning/phases/84-html-brandbook-voice-component-states/84-UI-SPEC.md (§Copywriting Contract error template name + canonical example; §Naming & CTA rules)
    - .planning/phases/84-html-brandbook-voice-component-states/84-CONTEXT.md (specifics — CTA label `install chimeway`; error pattern name)
    - prompts/chimeway-brand-book.md (§11 naming :382-450; graphic-vs-prose case :509-519; §26 claims/CTA :1411-1445)
  </read_first>
  <action>
    Author the `#errors` section documenting the named reusable error-message template (VOICE-02): "Chimeway error message pattern: what happened → why it matters → how to fix". The literal phrase `what happened` must appear (guard requires it). Show the canonical worked example `Delivery suppressed: recipient disabled email for `invoice.paid`.` broken into the three template slots, and the anti-example to avoid ("This chime did not ring.").

    Author the `#naming` section documenting CTA + naming rules (VOICE-03): the graphic wordmark is lowercase `chimeway` while prose is title-case `Chimeway` (both literals must appear — guard checks both cases); package names lowercase (`chimeway`, `chimeway_ecto`), modules `Chimeway.*`. Demonstrate the developer-to-developer primary CTA label `install chimeway` (never a sales CTA; no "Supercharge/omnichannel/journey" CTAs). Lift all copy verbatim from prompts/chimeway-brand-book.md.
  </action>
  <verify>
    <automated>grep -q 'id="errors"' brandbook/index.html && grep -q 'id="naming"' brandbook/index.html && grep -q 'what happened' brandbook/index.html && grep -q 'install chimeway' brandbook/index.html && grep -q 'chimeway' brandbook/index.html && grep -q 'Chimeway' brandbook/index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Named error template present with the "what happened → why it matters → how to fix" phrasing and canonical example split into slots
    - Naming section shows lowercase `chimeway` graphic vs title-case `Chimeway` prose and the `install chimeway` CTA
    - Copy verbatim from the source prompt
  </acceptance_criteria>
  <done>The named error template and naming/CTA rules are documented with verbatim examples covering VOICE-02 and VOICE-03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify full guard suite green + file:// render in Chromium</name>
  <action>Run the full guard suite and open the book under file:// in Chromium; confirm all four guard families are green, the working-tree scope is clean, and the rendered book (logos, states, do/don't, theme toggle, contrast matrix, voice sections) works with no console errors and is responsive. Await human approval.</action>
  <what-built>The complete standalone brand book (brandbook/index.html + brandbook/brandbook.css) rendering the locked token/logo system, all nine component states, do/don't pairs, a tri-state theme toggle, a live contrast matrix, and full brand-voice content — enforced by scripts/brandbook-guards.sh.</what-built>
  <how-to-verify>
    1. Run `bash scripts/brandbook-guards.sh` — confirm it exits 0 (all four families green: file://-safety, scope-nonleak, section-presence, D-05 parity).
    2. Run `bash scripts/brandbook-guards.sh --scope` — confirm the working tree carries only brandbook/** + the guard.
    3. Open `brandbook/index.html` directly via a `file://` path (double-click / paste the file:// URL) in Chromium — confirm: all logos render (mono marks recolor on theme flip), swatches/type/spacing show, the nine states are visible frozen, do/don't pairs read correctly, the theme toggle flips light → dark → system (system follows OS appearance), the contrast matrix badges flip pass/fail on theme change, voice/error/naming sections read correctly. Confirm the browser console shows NO errors.
    4. Resize the viewport — confirm the book is responsive and reads professionally.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g., a state that doesn't render, a console error, a leaked style).</resume-signal>
</task>

</tasks>

<artifacts_produced>
Completes `brandbook/index.html`:
- Section anchors: `voice`, `errors`, `naming`
- Named error-message template "what happened → why it matters → how to fix"
- Voice-by-context good/bad pairs (docs/errors/marketing/cli); banned + preferred vocabulary lists
- Naming/CTA rules (lowercase `chimeway` graphic vs `Chimeway` prose; `install chimeway` CTA)

Phase-complete artifact set: `brandbook/index.html`, `brandbook/brandbook.css`, `scripts/brandbook-guards.sh`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk→browser (file://) | Final book must remain fully file://-safe with all four guard families green |
| author content→DOM | All voice copy is author-static; no user/network content is interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-84-01 | Tampering | final index.html supply chain | high | mitigate | Guard family 1 green at phase gate; blocking human checkpoint confirms no console errors / no external loads |
| T-84-06 | Injection (XSS) | rendered content | low | accept | All content is author-static; no user or network data is interpolated into the DOM — no injection surface |
| T-84-03 | Tampering | scope leakage | medium | mitigate | Guard family 2 green at phase gate; `--scope` confirms working-tree boundary |
| T-84-SC | Tampering | package installs | high | mitigate | Zero installs across the phase; guard bans any future CDN `<script>` |
</threat_model>

<verification>
- `bash scripts/brandbook-guards.sh` exits 0 (all four families green) — the phase gate.
- `bash scripts/brandbook-guards.sh --scope` passes.
- Blocking human-verify: file:// render in Chromium clean, toggle + matrix live, no console errors, responsive.
</verification>

<success_criteria>
Voice-by-context, the named error template, and naming/CTA rules are documented verbatim; the full guard suite is green; the book opens and works under file:// with no console errors. All eight phase requirements (BOOK-01/02/03, STATE-01/02, VOICE-01/02/03) are satisfied across the four plans.
</success_criteria>

<output>
Create `.planning/phases/84-html-brandbook-voice-component-states/84-04-SUMMARY.md` when done.
</output>
