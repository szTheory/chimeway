---
phase: 84-html-brandbook-voice-component-states
plan: 02
type: execute
wave: 2
depends_on: ["84-01"]
files_modified:
  - brandbook/index.html
autonomous: true
requirements: [BOOK-01, BOOK-02, BOOK-03]
tags: [brandbook, html, logos, tokens, theme-toggle, static-html]

must_haves:
  truths:
    - "brandbook/index.html opens directly via file:// with no server/build; all asset refs are relative (no remote URLs, no root-absolute paths, no network fetches, no cross-file use-sprites) — BOOK-01, D-01"
    - "The document carries <body class=\"cw-brandbook\"> and links tokens/tokens.css + brandbook.css by relative path — BOOK-02, D-02"
    - "The logo family renders: the two currentColor marks inline as <svg> (theme-recoloring), the fixed-color/raster assets via <img src> — BOOK-03, D-04 (corrected to two inlined marks)"
    - "Color/token swatches, the 4-size/2-weight type scale, and the spacing scale render token-driven — BOOK-03"
    - "A tri-state light/dark/system toggle via a single inline classic <script> writes data-theme on document.documentElement — BOOK-03, D-03"
  artifacts:
    - brandbook/index.html
  key_links:
    - "index.html <link>s the Phase-81 tokens.css SSOT by relative path — reads --cw-*, never redefines"
    - "inline mono-mark <path d=…> strings match brandbook/assets/logo/*.svg (bound by the guard's D-05 parity check from plan 01)"
    - "data-cwb-theme buttons drive data-theme on :root; system removes the attribute to defer to prefers-color-scheme"
---

<objective>
Author the book's document shell and its first rendered sections into `brandbook/index.html`: the head (relative `<link>`s to `tokens/tokens.css` and `brandbook.css`, favicon link), the `.cw-brandbook` body root, anchor-nav chrome, the tri-state theme toggle with its single inline classic `<script>`, the logo-family section, and the color/token-swatch + typography + spacing sections.

Purpose: Stand up the file://-safe single-file scaffold (D-01) and prove the locked visual system renders — logos recolor with theme, swatches show name+resolved value, type is 4 sizes / 2 weights, spacing is the 4px scale.
Output: A valid, file://-safe `index.html` covering the header/logo/color/type/spacing/toggle portion of the ten-section order.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-html-brandbook-voice-component-states/84-CONTEXT.md
@.planning/phases/84-html-brandbook-voice-component-states/84-UI-SPEC.md
@.planning/phases/84-html-brandbook-voice-component-states/84-PATTERNS.md
@.planning/phases/84-html-brandbook-voice-component-states/84-RESEARCH.md
@brandbook/tokens/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document shell, head links, anchor nav, tri-state theme toggle + inline JS</name>
  <files>brandbook/index.html</files>
  <read_first>
    - .planning/phases/84-html-brandbook-voice-component-states/84-PATTERNS.md (§"brandbook/index.html" — relative <link> head, <body class="cw-brandbook">, and the copy-ready tri-state theme-toggle inline script)
    - .planning/phases/84-html-brandbook-voice-component-states/84-UI-SPEC.md (§Design System, §Component States "Interaction contract" — theme toggle writes data-theme; system removes it)
    - .planning/phases/84-html-brandbook-voice-component-states/84-RESEARCH.md (§1 file:// safety matrix; recommended section order 1-10)
    - brandbook/tokens/tokens.css (confirm [data-theme="light"]/[data-theme="dark"] + @media dark blocks exist so the toggle has something to flip)
  </read_first>
  <action>
    Create brandbook/index.html as a single standalone document (D-01: one long scroll, no multi-page split). In `<head>`, link the tokens SSOT and book CSS by RELATIVE path — `tokens/tokens.css` then `brandbook.css` (D-02) — and add `<link rel="icon">` to the relative favicon asset. Use only relative asset references: no remote (http/https) URLs, no root-absolute (`/`-leading) paths, no runtime network fetch, no XHR, no `type="module"` script, no cross-file SVG use-sprite (BOOK-01). Give the root `<body class="cw-brandbook">` (BOOK-02 scope root).

    Add anchor-nav chrome (`.cwb-nav`) linking the ten section anchors: `logo`, `color`, `contrast`, `type`, `spacing`, `states`, `dodont`, `voice`, `errors`, `naming`. Add the header with a tri-state theme control: three buttons carrying `data-cwb-theme` values `light`, `dark`, and `system`. Add ONE inline classic `<script>` (the only JS that runs under file://, D-03) that, on click, applies the mode by setting `data-theme` on `document.documentElement` for light/dark and REMOVING the attribute for system (deferring to `@media (prefers-color-scheme: dark)` in tokens.css); initialize in system mode. Do not add a second script tag — the contrast matrix (plan 03) will extend this same inline script.
  </action>
  <verify>
    <automated>grep -q 'class="cw-brandbook"' brandbook/index.html && grep -q 'href="tokens/tokens.css"' brandbook/index.html && grep -q 'href="brandbook.css"' brandbook/index.html && grep -q 'data-cwb-theme' brandbook/index.html && grep -q 'data-theme' brandbook/index.html && ! grep -Eq 'src=["'"'"']https?://|href=["'"'"']/[^>]|type=["'"'"']module|fetch\(|XMLHttpRequest' brandbook/index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `<body class="cw-brandbook">` present; tokens.css and brandbook.css linked by relative path
    - Three `data-cwb-theme` buttons (light/dark/system); one inline classic script toggles/removes `data-theme` on document.documentElement
    - No remote URLs, no root-absolute refs, no module script, no fetch, no XHR (file://-safe)
  </acceptance_criteria>
  <done>Document shell opens file://-safe with relative links, scope root, anchor nav, and a working tri-state theme toggle in one inline classic script.</done>
</task>

<task type="auto">
  <name>Task 2: Logo-family section (inline two currentColor marks; <img> the rest)</name>
  <files>brandbook/index.html</files>
  <read_first>
    - .planning/phases/84-html-brandbook-voice-component-states/84-PATTERNS.md (§CRITICAL FINDING — the corrected D-04 inline/img split; §"Logo embedding split")
    - .planning/phases/84-html-brandbook-voice-component-states/84-CONTEXT.md (D-04 rationale; the critical_correction reconciling D-04 to two inlined marks)
    - brandbook/assets/logo/chimeway-mark-mono.svg and brandbook/assets/logo/chimeway-logotype-mono.svg (copy their exact `<path d="…">` verbatim for inlining — parity-checked by the guard)
  </read_first>
  <action>
    Author the `#logo` section (`.cwb-section`) showing the six-mark family with clearspace + min-size notes. Per D-04 AS CORRECTED by the PATTERNS CRITICAL FINDING (this reconciles a factual error in CONTEXT D-04, which listed three inline marks): inline as `<svg>` ONLY the two `fill="currentColor"` marks — `chimeway-mark-mono.svg` (path `M6 5h12l-2.5 14h-7Z`) and `chimeway-logotype-mono.svg` — copying their exact `<path d="…">` verbatim so they inherit `color: var(--cw-fg)`/`var(--cw-accent)` and recolor with the theme (BOOK-03). Render every other asset via `<img src="assets/logo/…">`: `chimeway-logotype.svg`, `chimeway-logotype-stacked.svg`, `chimeway-mark.svg`, and `chimeway-logotype-inverse.svg` (the fixed paper-white lockup — it does NOT carry currentColor, so `<img>` renders it correctly on a dark field, which is its purpose). Also render favicon and OG previews via `<img>`. Present marks on open field — NO rectangular background cage behind the mark (RESEARCH anti-pattern; the caged version belongs only in the plan-03 do/don't pair). Note in an HTML comment that only two marks are inlined (D-04 corrected).
  </action>
  <verify>
    <automated>grep -q 'M6 5h12l-2.5 14h-7Z' brandbook/index.html && grep -q 'm27.24 39.84-1.45 3.48' brandbook/index.html && grep -q 'img src="assets/logo/chimeway-logotype-inverse.svg"' brandbook/index.html && grep -q 'id="logo"' brandbook/index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Exactly the two currentColor marks are inlined as `<svg>` with verbatim `d=` paths matching the SSOT assets (guard D-05 parity passes)
    - The inverse lockup and the three fixed-color marks render via `<img src>` (not inlined)
    - Logo section has clearspace/min-size notes and no background cage
  </acceptance_criteria>
  <done>Logo family renders with the two themeable marks inline and all fixed-color/raster assets via <img>, matching the corrected D-04 policy and passing D-05 parity.</done>
</task>

<task type="auto">
  <name>Task 3: Color/token swatches + typography + spacing sections</name>
  <files>brandbook/index.html</files>
  <read_first>
    - .planning/phases/84-html-brandbook-voice-component-states/84-UI-SPEC.md (§Color incl. accent-reservation list + status triads; §Typography 4 sizes/2 weights; §Spacing scale)
    - brandbook/tokens/tokens.css (exact --cw-* primitive, semantic, status-triad, spacing, and type token names to render)
    - .planning/phases/84-html-brandbook-voice-component-states/84-PATTERNS.md (token families inventory)
  </read_first>
  <action>
    Author the `#color`, `#type`, and `#spacing` sections using the `.cwb-swatch`/`.cwb-panel`/`.cwb-grid`/`.cwb-label` classes from plan-01 CSS. Color: render primitive, semantic-alias, and status-triad swatches — each swatch shows the token NAME plus its resolved value (drive the swatch background/foreground from `var(--cw-*)` so it reflects the live theme; do not hard-code hex). Include the status triads (success/warning/danger/info/neutral, each -text/-surface/-border) as labelled pills paired with a label + icon (never color-alone, per UI-SPEC). Document the accent-reservation list (accent used only for primary button, `.is-selected` ring, on-theme mark stroke, active-nav). Typography: render exactly the four sizes (label 14 / body 16 / heading 20 / display 28) at exactly two weights (regular 400 / semibold 600) via `var(--cw-font-size-*)`/`var(--cw-font-weight-*)`, with the sans/mono/display font stacks. Spacing: render the seven-step 4px scale (`--cw-space-xs…3xl`) as visual bars. Reference token names only — zero hard-coded hex (a hex literal would signal drift).
  </action>
  <verify>
    <automated>grep -q 'id="color"' brandbook/index.html && grep -q 'id="type"' brandbook/index.html && grep -q 'id="spacing"' brandbook/index.html && grep -q 'cw-space-' brandbook/index.html && grep -q 'cw-font-size-' brandbook/index.html && grep -q 'cw-status-' brandbook/index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Color section renders primitive + semantic + status-triad swatches, each with name + resolved value, driven by `var(--cw-*)`
    - Status conveyed by color + label + icon (never color-alone)
    - Type section shows 4 sizes / 2 weights; spacing section shows the 7-step 4px scale
    - No hard-coded hex color literals in these sections
  </acceptance_criteria>
  <done>Color/token, typography, and spacing sections render token-driven, reflecting the live theme, with zero hard-coded values.</done>
</task>

</tasks>

<artifacts_produced>
New file:
- `brandbook/index.html` (created this plan; extended in waves 3-4).

Named anchors/attributes introduced:
- Section anchor ids: `logo`, `color`, `type`, `spacing` (plus nav references to `contrast`, `states`, `dodont`, `voice`, `errors`, `naming` for later waves)
- `data-cwb-theme` toggle buttons (values `light`/`dark`/`system`); `data-theme` on `document.documentElement`
- Inline classic `<script>` theme toggle (extended by plan 03)
- Inlined `<svg>` marks: chimeway-mark-mono, chimeway-logotype-mono
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk→browser (file://) | index.html loads from disk; cross-file loads are CORS-blocked, so only relative `<link>`/`<img>`/inline assets work |
| shipped asset→inlined mark | The two inlined mark `d=` paths must match the SSOT logo assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-84-01 | Tampering | index.html supply chain | high | mitigate | Only relative refs + one inline classic script authored; guard family 1 (plan 01) bans any remote-`src` script, fetch, XHR, module script, or cross-file use-sprite |
| T-84-02 | Information disclosure | file:// local-file read | medium | mitigate | No network-fetch/XHR authored; platform blocks them under file:// anyway |
| T-84-04 | Tampering | inlined mark drift | low | mitigate | Verbatim `d=` copy from SSOT; guard D-05 parity (plan 01) binds them |
| T-84-SC | Tampering | package installs | high | mitigate | Zero installs; no dependency surface introduced |
</threat_model>

<verification>
- `bash scripts/brandbook-guards.sh` file://-safety family (1) passes over index.html; presence-gate for index.html now satisfied (section-presence family still RED until plans 03-04 add states/voice — expected).
- Manual: open `file://…/brandbook/index.html` in Chromium — logos visible, mono marks recolor on theme flip, swatches/type/spacing render, toggle works, no console errors.
</verification>

<success_criteria>
index.html opens file://-safe with relative links and scope root; the logo family, color/token swatches, typography, spacing, and a working tri-state theme toggle all render token-driven with zero hard-coded values.
</success_criteria>

<output>
Create `.planning/phases/84-html-brandbook-voice-component-states/84-02-SUMMARY.md` when done.
</output>
