---
phase: 76-prefix-docs-demo-and-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/chimeway_demo_host/config/dev.exs
  - examples/chimeway_demo_host/config/test.exs
  - examples/chimeway_demo_host/README.md
  - examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs
autonomous: true
requirements:
  - DEMO-01
requirements_addressed:
  - DEMO-01
must_haves:
  truths:
    - "The demo host runs with config :chimeway, prefix: \"chimeway\" in dev and test. Covers D-15 and DEMO-01."
    - "The demo proof uses DemoHost.Seeds or Chimeway.trigger/3 public APIs rather than primary fixture inserts. Covers D-16."
    - "The demo proof shows trigger-to-trace behavior and verifies Chimeway rows land under chimeway.* while public chimeway_* tables remain empty. Covers D-15."
  artifacts:
    - path: "examples/chimeway_demo_host/config/dev.exs"
      provides: "Demo default isolated Chimeway schema runtime config"
    - path: "examples/chimeway_demo_host/config/test.exs"
      provides: "Demo test isolated Chimeway schema runtime config"
    - path: "examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs"
      provides: "Public seed trigger-to-trace and schema placement proof"
    - path: "examples/chimeway_demo_host/README.md"
      provides: "Demo host copy documenting storage prefix proof"
  key_links:
    - from: "examples/chimeway_demo_host/config/test.exs"
      to: "examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs"
      via: "test runtime config sets Chimeway prefix before seed-driven trace proof"
      pattern: "prefix: \"chimeway\""
    - from: "examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs"
      to: "examples/chimeway_demo_host/lib/demo_host/seeds.ex"
      via: "DemoHost.Seeds.seed_invite/0 public API"
      pattern: "DemoHost.Seeds.seed_invite"
---

<objective>
Make the demo host prove DEMO-01 by running against the default isolated Chimeway schema and exercising a public trigger-to-trace path.

Purpose: Show adopters that the default storage-isolation setup works in the example host without teaching storage internals as the primary workflow.
Output: Demo host config defaults, a public seed trigger-to-trace schema placement test, and demo README proof copy.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/76-prefix-docs-demo-and-gates/76-CONTEXT.md
@.planning/phases/76-prefix-docs-demo-and-gates/76-RESEARCH.md
@.planning/phases/76-prefix-docs-demo-and-gates/76-PATTERNS.md
@.planning/phases/75-runtime-prefix-propagation/75-VERIFICATION.md
@examples/chimeway_demo_host/config/dev.exs
@examples/chimeway_demo_host/config/test.exs
@examples/chimeway_demo_host/lib/demo_host/seeds.ex
@examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs
@test/support/prefixed_runtime_case.ex
@test/chimeway/runtime_prefix_integration_test.exs
</context>

## Artifacts this phase produces

- Modified demo config: `examples/chimeway_demo_host/config/dev.exs`
- Modified demo test config: `examples/chimeway_demo_host/config/test.exs`
- Modified demo proof test module: `DemoHostWeb.AdminTraceLiveTest`
- New or modified helper functions in `examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs` for schema-qualified Chimeway table counts
- Modified demo README: `examples/chimeway_demo_host/README.md`
- Verification command: `cd examples/chimeway_demo_host && MIX_ENV=test mix test test/demo_host_web/admin_trace_live_test.exs --warnings-as-errors`

<tasks>

<task type="auto">
  <name>Task 1: Configure demo host for isolated Chimeway schema</name>
  <files>examples/chimeway_demo_host/config/dev.exs, examples/chimeway_demo_host/config/test.exs, examples/chimeway_demo_host/README.md</files>
  <read_first>
    - examples/chimeway_demo_host/config/dev.exs
    - examples/chimeway_demo_host/config/test.exs
    - examples/chimeway_demo_host/README.md
    - .planning/phases/76-prefix-docs-demo-and-gates/76-CONTEXT.md
    - .planning/phases/76-prefix-docs-demo-and-gates/76-PATTERNS.md
  </read_first>
  <action>
    Add prefix: "chimeway" to the top-level config :chimeway block in examples/chimeway_demo_host/config/dev.exs and examples/chimeway_demo_host/config/test.exs per D-15. Do not add prefix settings to config :chimeway, Chimeway.Repo or config :chimeway, Oban, because D-09 keeps Chimeway storage prefix separate from Oban job-table prefix. Update examples/chimeway_demo_host/README.md with short proof copy that the demo host uses the isolated Chimeway schema by default and that trigger-to-trace proof is exercised through DemoHost.Seeds and public Chimeway APIs.
  </action>
  <verify>
    <automated>cd examples/chimeway_demo_host &amp;&amp; MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    The dev and test demo-host top-level Chimeway config contains prefix: "chimeway"; Oban config remains separate and does not use the Chimeway storage prefix; demo README states the proof path without implying Oban prefix coupling.
  </acceptance_criteria>
  <done>Demo host config defaults to the isolated Chimeway schema and compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove public seed trigger-to-trace writes only under chimeway schema</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs</files>
  <read_first>
    - examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs
    - examples/chimeway_demo_host/lib/demo_host/seeds.ex
    - test/support/prefixed_runtime_case.ex
    - test/chimeway/runtime_prefix_integration_test.exs
    - .planning/phases/75-runtime-prefix-propagation/75-VERIFICATION.md
  </read_first>
  <behavior>
    - The test starts with DemoHost.Seeds.seed_invite/0 or an equivalent public Chimeway.trigger/3 path.
    - The test calls Chimeway.Traces.explain_delivery/1 for a delivery ID returned by the seed result.
    - The test asserts nonzero chimeway schema rows for core Chimeway tables and zero public rows for those same Chimeway-owned tables.
    - The test uses schema-qualified SQL with identifier normalization, not search_path.
  </behavior>
  <action>
    Extend DemoHostWeb.AdminTraceLiveTest with a DEMO-01 proof tagged for focused execution. Reuse DemoHost.Seeds.seed_invite/0 as the primary entrypoint per D-16, obtain a delivery ID from result.trace.delivery_ids, call Chimeway.Traces.explain_delivery/1, and assert the explanation references the invite notification key and the existing seed correlation. Add local schema-count helpers modeled on Chimeway.PrefixedRuntimeCase: normalize schema/table identifiers, check to_regclass, count rows from schema-qualified names, and assert chimeway.chimeway_events, chimeway.chimeway_notifications, chimeway.chimeway_deliveries, and chimeway.chimeway_delivery_attempts have rows while public versions remain zero. Do not create Event, Notification, Delivery, or DeliveryAttempt structs directly as the primary proof.
  </action>
  <verify>
    <automated>cd examples/chimeway_demo_host &amp;&amp; MIX_ENV=test mix test test/demo_host_web/admin_trace_live_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    The demo proof passes from the public seed API through Chimeway.Traces.explain_delivery/1 and validates chimeway-only row placement for the core lifecycle spine.
  </acceptance_criteria>
  <done>DEMO-01 is proven by a demo-host ExUnit test that exercises the public trigger-to-trace path and schema placement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keep demo proof safe for redaction and public-mode regression boundaries</name>
  <files>examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs, examples/chimeway_demo_host/README.md</files>
  <read_first>
    - examples/chimeway_demo_host/test/demo_host_web/admin_trace_live_test.exs
    - examples/chimeway_demo_host/README.md
    - AGENTS.md
    - .planning/phases/76-prefix-docs-demo-and-gates/76-CONTEXT.md
    - .planning/phases/76-prefix-docs-demo-and-gates/76-VALIDATION.md
  </read_first>
  <behavior>
    - Demo proof assertions do not expose raw payload fields, provider bodies, tokens, secrets, auth codes, or full recipient PII in output assertions.
    - Existing admin redaction assertions remain intact.
    - Demo README describes storage-isolation proof as support evidence, not as an operator move procedure.
  </behavior>
  <action>
    Review the added demo test and README copy against AGENTS.md sensitive-payload guidance and D-18/D-19. Keep assertions on notification key, correlation ID, delivery IDs, explanation status, and schema row counts. Preserve assert_redacted_recipient/2 behavior in the existing admin tests. Avoid raw render data, provider body, token, secret, auth code, and full recipient PII literals in new test assertions or README copy. Keep the manual data-move procedure in the storage guide from Plan 76-01, not in the demo README.
  </action>
  <verify>
    <automated>cd examples/chimeway_demo_host &amp;&amp; MIX_ENV=test mix test test/demo_host_web/admin_trace_live_test.exs --warnings-as-errors</automated>
    <automated>mix verify.example</automated>
  </verify>
  <acceptance_criteria>
    Demo-host focused tests and mix verify.example pass, existing redaction checks still pass, and the README does not become the canonical upgrade runbook.
  </acceptance_criteria>
  <done>Demo proof is safe, public-API based, redaction-preserving, and included in the existing example verification path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo public API -> Chimeway storage | DemoHost.Seeds/Chimeway.trigger writes durable rows into configured storage. |
| Demo trace UI/tests -> operator-visible output | Admin trace tests inspect redacted output and delivery explanations. |
| Test SQL -> database schemas | Schema-qualified SQL inspects chimeway and public schemas directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-05 | Tampering | examples/chimeway_demo_host/config/*.exs | mitigate | Set prefix only on top-level config :chimeway and assert demo behavior through public APIs so accidental public fallback is caught. |
| T-76-06 | Repudiation | DemoHostWeb.AdminTraceLiveTest | mitigate | Require Chimeway.Traces.explain_delivery/1 and schema-count assertions so DEMO-01 proves trigger-to-trace plus storage placement. |
| T-76-07 | Information Disclosure | demo tests and README | mitigate | Keep assertions on IDs, status, keys, correlation, and row counts; preserve redaction checks and avoid raw payload/provider/token/secret examples. |
| T-76-08 | Tampering | schema-count helpers | mitigate | Normalize identifiers and use schema-qualified SQL; do not rely on search_path. |
| T-76-SC | Tampering | package installs | accept | No npm, pip, or cargo package installs are planned; the demo proof uses existing Mix dependencies and local tests. |
</threat_model>

<verification>
Overall plan verification:

1. `cd examples/chimeway_demo_host && MIX_ENV=test mix test test/demo_host_web/admin_trace_live_test.exs --warnings-as-errors`
2. `mix verify.example`
</verification>

<success_criteria>
- DEMO-01: Demo host runs under config :chimeway, prefix: "chimeway".
- DEMO-01: Public seed/trigger proof reaches Chimeway.Traces.explain_delivery/1.
- DEMO-01: Chimeway-owned lifecycle rows are present under chimeway.* and absent under public.chimeway_* for the demo proof.
- Demo proof preserves host ownership and redaction boundaries.
</success_criteria>

<output>
Create `.planning/phases/76-prefix-docs-demo-and-gates/76-02-SUMMARY.md` when done.
</output>
