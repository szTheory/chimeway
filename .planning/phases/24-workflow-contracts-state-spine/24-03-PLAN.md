---
phase: 24-workflow-contracts-state-spine
plan: 03
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
files_modified:
  - lib/chimeway/delivery.ex
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/deliveries.ex
  - lib/chimeway/workflows.ex
  - priv/repo/migrations/*_alter_chimeway_deliveries_for_workflow_linkage.exs
  - test/chimeway/orchestration/recovery_test.exs
  - test/chimeway/integration/delivery_lifecycle_test.exs
autonomous: true
requirements:
  - WRK-03
  - API-02
  - WRK-01
must_haves:
  truths:
    - "The active step's canonical delivery row links back to the owning workflow run and workflow step."
    - "Recovery and replay paths can read persisted workflow declarations without re-entering notifier workflow callbacks."
    - "Current workflow state remains readable from durable run and transition rows plus linked deliveries, not queue state."
  artifacts:
    - path: "lib/chimeway/delivery.ex"
      provides: "Delivery-level workflow linkage fields for run and step identity"
      min_lines: 20
    - path: "lib/chimeway/delivery_planning.ex"
      provides: "Planning seam that stamps the active-step delivery with workflow linkage"
      min_lines: 40
    - path: "lib/chimeway/deliveries.ex"
      provides: "Recovery path that reuses persisted workflow declarations via explicit opts"
      min_lines: 40
    - path: "test/chimeway/orchestration/recovery_test.exs"
      provides: "Regression proof that workflow replay does not callback into notifier declarations"
      min_lines: 70
  key_links:
    - from: "lib/chimeway/delivery_planning.ex"
      to: "lib/chimeway/delivery.ex"
      via: "first-step delivery rows carry `workflow_run_id` and `workflow_step_id` as canonical execution linkage"
      pattern: "workflow_run_id|workflow_step_id"
    - from: "lib/chimeway/deliveries.ex"
      to: "lib/chimeway/workflows.ex"
      via: "recovery reads persisted workflow definitions instead of re-entering notifier callbacks"
      pattern: "use_persisted_workflow|persisted_workflow"
---

<objective>
Link canonical deliveries and replay paths to the durable workflow spine.

Purpose: Phase 24 is only explainable if the active step's delivery rows and replay paths attach to the persisted workflow declaration/run data. This plan wires delivery linkage into the planner seam and extends recovery tests so historical truth can be reconstructed from stored data alone.
Output: delivery-level workflow linkage fields, planner/recovery wiring, and replay regression tests proving callback-free persisted workflow reuse.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-workflow-contracts-state-spine/24-CONTEXT.md
@.planning/phases/24-workflow-contracts-state-spine/24-RESEARCH.md
@.planning/phases/24-workflow-contracts-state-spine/24-PATTERNS.md
@AGENTS.md
@lib/chimeway/delivery.ex
@lib/chimeway/delivery_planning.ex
@lib/chimeway/deliveries.ex
@test/chimeway/orchestration/recovery_test.exs
@test/chimeway/integration/delivery_lifecycle_test.exs

<interfaces>
From `lib/chimeway/delivery_planning.ex`:
```elixir
def plan_notification(%Notification{} = notification, opts \\ [])
defp plan_one_channel(notification, channel, delayed_fallback_set, delayed_fallback_source, opts)
```

From `lib/chimeway/deliveries.ex`:
```elixir
def recover_event(event_or_id, opts \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED coverage for delivery workflow linkage and persisted-workflow replay</name>
  <files>test/chimeway/orchestration/recovery_test.exs, test/chimeway/integration/delivery_lifecycle_test.exs</files>
  <read_first>
    - test/chimeway/orchestration/recovery_test.exs
    - test/chimeway/integration/delivery_lifecycle_test.exs
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/deliveries.ex
    - lib/chimeway/delivery.ex
    - .planning/phases/24-workflow-contracts-state-spine/24-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the first active-step delivery row persists `workflow_run_id` and `workflow_step_id`.
    - Test 2: replaying a recoverable event with `use_persisted_workflow: true` reuses the stored workflow declaration and does not call the notifier workflow callback again.
    - Test 3: the run's current step remains derivable from the linked delivery plus persisted run/transition rows.
  </behavior>
  <action>Extend `test/chimeway/integration/delivery_lifecycle_test.exs` with assertions that a workflow-enabled notification's canonical delivery row contains exact linkage fields `workflow_run_id` and `workflow_step_id`. Extend `test/chimeway/orchestration/recovery_test.exs` with a notifier fixture whose workflow callback would raise if called during recovery, then prove `recover_event/2` succeeds when passed `use_persisted_workflow: true` and reuses the persisted workflow definition/run linkage instead of calling the notifier again.</action>
  <acceptance_criteria>
    - `test/chimeway/integration/delivery_lifecycle_test.exs` contains `workflow_run_id`
    - `test/chimeway/integration/delivery_lifecycle_test.exs` contains `workflow_step_id`
    - `test/chimeway/orchestration/recovery_test.exs` contains `use_persisted_workflow`
    - `test/chimeway/orchestration/recovery_test.exs` contains `raise`
    - `mix test test/chimeway/orchestration/recovery_test.exs test/chimeway/integration/delivery_lifecycle_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/orchestration/recovery_test.exs test/chimeway/integration/delivery_lifecycle_test.exs --trace</automated>
  </verify>
  <done>The tests define the canonical delivery-linkage and callback-free replay contract for the workflow spine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stamp workflow linkage on canonical deliveries and replay from persisted workflow declarations</name>
  <files>lib/chimeway/delivery.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/deliveries.ex, lib/chimeway/workflows.ex, priv/repo/migrations/*_alter_chimeway_deliveries_for_workflow_linkage.exs</files>
  <read_first>
    - lib/chimeway/delivery.ex
    - lib/chimeway/delivery_planning.ex
    - lib/chimeway/deliveries.ex
    - lib/chimeway/workflows.ex
    - test/chimeway/orchestration/recovery_test.exs
    - test/chimeway/integration/delivery_lifecycle_test.exs
    - .planning/phases/24-workflow-contracts-state-spine/24-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `Deliveries.plan_delivery/3` or its nearest seam persists workflow linkage on the active-step delivery row.
    - Test 2: recovery can opt into persisted workflow replay the same way it already opts into persisted channels and orchestration.
    - Test 3: the workflow run remains the source of current truth, while delivery linkage preserves the execution artifact join.
  </behavior>
  <action>Add nullable `workflow_run_id` and `workflow_step_id` foreign keys to `lib/chimeway/delivery.ex` and create a migration `*_alter_chimeway_deliveries_for_workflow_linkage.exs` that adds those columns plus indexes. Update `lib/chimeway/delivery_planning.ex` so `plan_one_channel/5` receives persisted workflow run/step context for the active step and stamps the canonical delivery row with those exact fields when creating or reusing the first-step delivery. Add helpers in `lib/chimeway/workflows.ex` that resolve the active step for a run and expose persisted workflow snapshots for replay. Update `lib/chimeway/deliveries.ex` so `recover_event/2` accepts and forwards `use_persisted_workflow: true` alongside `use_persisted_channels` and `use_persisted_orchestration`, then reconstructs workflow planning inputs from durable workflow definition/run data instead of callback re-entry.</action>
  <acceptance_criteria>
    - `lib/chimeway/delivery.ex` contains `belongs_to(:workflow_run`
    - `lib/chimeway/delivery.ex` contains `belongs_to(:workflow_step`
    - `lib/chimeway/delivery_planning.ex` contains `workflow_run_id`
    - `lib/chimeway/delivery_planning.ex` contains `workflow_step_id`
    - `lib/chimeway/deliveries.ex` contains `use_persisted_workflow`
    - `rg -n 'workflow_run_id|workflow_step_id' priv/repo/migrations` returns matches
    - `mix test test/chimeway/orchestration/recovery_test.exs test/chimeway/integration/delivery_lifecycle_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/orchestration/recovery_test.exs test/chimeway/integration/delivery_lifecycle_test.exs --trace</automated>
  </verify>
  <done>Canonical delivery rows and replay paths now connect to the persisted workflow spine without depending on notifier callback re-entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow run -> delivery row | Execution artifacts must point back to the correct durable workflow truth |
| persisted workflow snapshot -> recovery replay | Recovery must reuse stored data rather than executing arbitrary notifier callback code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | T | `lib/chimeway/delivery_planning.ex` | mitigate | Persist exact `workflow_run_id` and `workflow_step_id` on canonical deliveries instead of metadata-only linkage. |
| T-24-08 | E | `lib/chimeway/deliveries.ex` recovery path | mitigate | Add explicit `use_persisted_workflow: true` gating so replay only uses durable rows and does not re-enter notifier callbacks. |
| T-24-09 | R | workflow current-state explainability | mitigate | Keep workflow run as current truth and use append-only transitions plus linked deliveries for auditability. |
</threat_model>

<verification>
Run `mix test test/chimeway/orchestration/recovery_test.exs test/chimeway/integration/delivery_lifecycle_test.exs --trace`.
</verification>

<success_criteria>
The active-step delivery row links to the workflow run and step, and recovery/replay can reconstruct workflow declarations from persisted state alone without notifier callback re-entry.
</success_criteria>

<output>
After completion, create `.planning/phases/24-workflow-contracts-state-spine/24-03-SUMMARY.md`
</output>
