---
phase: 24-workflow-contracts-state-spine
plan: 03
type: execute
wave: 3
depends_on:
  - 24-02
files_modified:
  - lib/chimeway/delivery.ex
  - lib/chimeway/delivery_planning.ex
  - lib/chimeway/deliveries.ex
  - lib/chimeway/workflows.ex
  - priv/repo/migrations/20260429132000_add_workflow_linkage_to_chimeway_deliveries.exs
  - test/chimeway/orchestration/recovery_test.exs
  - test/chimeway/trigger_pipeline_test.exs
autonomous: true
requirements:
  - WRK-03
  - API-02
must_haves:
  truths:
    - "The first planned delivery row links back to the owning workflow run and active workflow step."
    - "A transition row records that the active step produced a canonical delivery."
    - "Recovery/replay can rebuild the delivery linkage from persisted workflow declarations and run state without re-entering notifier workflow callbacks."
  artifacts:
    - path: "lib/chimeway/delivery.ex"
      provides: "Workflow run and step foreign-key linkage on deliveries"
      min_lines: 40
    - path: "lib/chimeway/deliveries.ex"
      provides: "Replay path that reuses persisted workflow state"
      min_lines: 80
    - path: "priv/repo/migrations/20260429132000_add_workflow_linkage_to_chimeway_deliveries.exs"
      provides: "Delivery workflow linkage columns and indexes"
      contains: "alter table(:chimeway_deliveries)"
  key_links:
    - from: "lib/chimeway/delivery_planning.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "workflow_run_id/workflow_step_id planning opts"
      pattern: "workflow_run_id|workflow_step_id"
    - from: "lib/chimeway/deliveries.ex"
      to: "lib/chimeway/workflows.ex"
      via: "persisted workflow replay"
      pattern: "use_persisted_workflow|append_transition!"
---

<objective>
Link workflow runs to canonical delivery rows and prove persisted replay.

Purpose: Finish the Phase 24 spine by attaching delivery execution artifacts back to the run/step truth and ensuring replay reads stored workflow declarations instead of calling notifier workflow callbacks again.
Output: Delivery workflow FKs, planning/recovery integration, delivery-linked transition capture, and replay regression tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-workflow-contracts-state-spine/24-CONTEXT.md
@.planning/phases/24-workflow-contracts-state-spine/24-RESEARCH.md
@.planning/phases/24-workflow-contracts-state-spine/24-PATTERNS.md
@.planning/phases/22-recovery-outcome-analytics/22-CONTEXT.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-RESEARCH.md
@.planning/phases/23-digest-flush-scheduling-audit-closure/23-06-SUMMARY.md
@lib/chimeway/delivery.ex
@lib/chimeway/delivery_planning.ex
@lib/chimeway/deliveries.ex
@test/chimeway/orchestration/recovery_test.exs
@.planning/phases/24-workflow-contracts-state-spine/24-02-PLAN.md

<interfaces>
From lib/chimeway/delivery.ex:
```elixir
schema "chimeway_deliveries" do
  belongs_to(:notification, Notification)
  belongs_to(:digest_delivery, Chimeway.Delivery)
end
```

From lib/chimeway/delivery_planning.ex:
```elixir
Deliveries.plan_delivery(notification.id, channel,
  render_key: render_result[:render_key],
  render_version: render_result[:render_version],
  render_data: render_result[:render_data]
)
```

From lib/chimeway/deliveries.ex:
```elixir
dispatch_opts = [
  event_id: event.id,
  notification_key: event.notification_key,
  correlation_id: event.correlation_id,
  post_commit: true,
  use_persisted_channels: true,
  use_persisted_orchestration: true
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp canonical deliveries with workflow run and step linkage</name>
  <files>lib/chimeway/delivery.ex, lib/chimeway/delivery_planning.ex, lib/chimeway/deliveries.ex, lib/chimeway/workflows.ex, priv/repo/migrations/20260429132000_add_workflow_linkage_to_chimeway_deliveries.exs, test/chimeway/trigger_pipeline_test.exs</files>
  <read_first>
    .planning/phases/24-workflow-contracts-state-spine/24-CONTEXT.md
    lib/chimeway/delivery.ex
    lib/chimeway/delivery_planning.ex
    lib/chimeway/deliveries.ex
    lib/chimeway/workflows.ex
    test/chimeway/trigger_pipeline_test.exs
  </read_first>
  <behavior>
    - Test 1: the first planned delivery for a workflow-enabled notification persists `workflow_run_id` and `workflow_step_id`.
    - Test 2: a transition row with exact reason `delivery_planned` links the workflow run, active step, and canonical delivery id.
    - Test 3: non-workflow delivery planning still succeeds with nil workflow linkage.
  </behavior>
  <action>Alter `chimeway_deliveries` to add nullable `workflow_run_id` and `workflow_step_id` foreign keys referencing `chimeway_workflow_runs` and `chimeway_workflow_steps`, plus indexes on each column and on `[:workflow_run_id, :workflow_step_id]`. Add the matching associations to `Chimeway.Delivery`. Extend `Deliveries.plan_delivery/3` to accept exact opts `workflow_run_id:` and `workflow_step_id:` and persist them on insert. In `Chimeway.DeliveryPlanning`, when a notification has a persisted workflow run, load the run and its `current_step_id`, pass that identity into `plan_delivery/3`, and append a workflow transition with exact reason `delivery_planned` plus context keys `channel`, `notification_id`, and `delivery_id`. Keep the phase bounded to the initial active step only; do not add wait, branch, escalation, or stop semantics. This task implements D-04, D-05, and D-08.</action>
  <acceptance_criteria>
    `rg -n "add\\(:workflow_run_id|add\\(:workflow_step_id" priv/repo/migrations/20260429132000_add_workflow_linkage_to_chimeway_deliveries.exs` returns both columns.
    `rg -n "belongs_to\\(:workflow_run|belongs_to\\(:workflow_step" lib/chimeway/delivery.ex` returns both associations.
    `rg -n "workflow_run_id|workflow_step_id|delivery_planned" lib/chimeway/delivery_planning.ex lib/chimeway/deliveries.ex lib/chimeway/workflows.ex` returns all three workflow linkage markers.
    `rg -n "workflow_run_id|workflow_step_id|delivery_planned" test/chimeway/trigger_pipeline_test.exs` returns integration assertions.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/trigger_pipeline_test.exs</automated>
  </verify>
  <done>Canonical delivery rows now carry workflow linkage, and the run history records which active step produced the delivery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replay workflow-linked deliveries from persisted state without workflow callback re-entry</name>
  <files>lib/chimeway/delivery_planning.ex, lib/chimeway/deliveries.ex, lib/chimeway/workflows.ex, test/chimeway/orchestration/recovery_test.exs</files>
  <read_first>
    .planning/phases/24-workflow-contracts-state-spine/24-CONTEXT.md
    lib/chimeway/delivery_planning.ex
    lib/chimeway/deliveries.ex
    lib/chimeway/workflows.ex
    test/chimeway/orchestration/recovery_test.exs
  </read_first>
  <behavior>
    - Test 1: `Deliveries.recover_event/2` re-plans a workflow-enabled notification from persisted definition/run rows with `use_persisted_workflow: true`.
    - Test 2: recovery does not invoke the notifier `workflow/2` callback after the initial trigger.
    - Test 3: the recovered delivery preserves the same `workflow_run_id`, `workflow_step_id`, and active-step transition reasoning.
  </behavior>
  <action>Extend persisted replay so `Deliveries.recover_event/2` passes `use_persisted_workflow: true` alongside `use_persisted_channels: true` and `use_persisted_orchestration: true`. Add a workflow override path in `Chimeway.DeliveryPlanning` that loads the persisted run, definition, and current step from `Chimeway.Workflows` when the notifier is absent or persisted replay is requested, then uses that data instead of invoking `Notifier.resolve_workflow/3`. Update the recovery probe tests to assert that only the initial trigger emits workflow callback messages, recovery emits none, and the recovered delivery row keeps the same run/step linkage. This task implements D-02, D-06, and D-08 and provides the explicit replay proof required by Phase 24 success criterion 4.</action>
  <acceptance_criteria>
    `rg -n "use_persisted_workflow: true" lib/chimeway/deliveries.ex` returns one match.
    `rg -n "use_persisted_workflow|persisted workflow" lib/chimeway/delivery_planning.ex lib/chimeway/workflows.ex` returns the replay seam.
    `rg -n "workflow_called|workflow_run_id|workflow_step_id" test/chimeway/orchestration/recovery_test.exs` returns callback-avoidance and linkage assertions.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/orchestration/recovery_test.exs test/chimeway/trigger_pipeline_test.exs</automated>
  </verify>
  <done>Workflow replay uses persisted declarations and run state only, without notifier workflow callback re-entry, while preserving canonical delivery linkage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow state -> delivery planning | Persisted workflow truth drives canonical delivery creation. |
| recovery replay -> notifier callback seam | Historical replay must not trust mutable notifier code for workflow identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | T | `Deliveries.plan_delivery/3` workflow linkage | mitigate | Persist workflow foreign keys directly on the canonical delivery row instead of reconstructing linkage from planning_context or metadata. |
| T-24-08 | I | recovery replay path | mitigate | Load persisted workflow definition/run rows and avoid notifier workflow callback re-entry so historical truth cannot drift with code changes. |
| T-24-09 | D | delivery planning during replay | mitigate | Keep replay additive to the existing notification/delivery spine and gate it with explicit `use_persisted_workflow: true` rather than implicit fallback magic. |
</threat_model>

<verification>
Run `mix test test/chimeway/orchestration/recovery_test.exs test/chimeway/trigger_pipeline_test.exs`.
</verification>

<success_criteria>
The workflow state spine now reaches the canonical delivery row and survives recovery/replay entirely from persisted Chimeway-owned data.
</success_criteria>

<output>
After completion, create `.planning/phases/24-workflow-contracts-state-spine/24-03-SUMMARY.md`
</output>
