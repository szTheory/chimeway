---
phase: 24-workflow-contracts-state-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/notifier.ex
  - lib/chimeway/workflows.ex
  - lib/chimeway/workflows/workflow_definition.ex
  - lib/chimeway/workflows/workflow_step.ex
  - priv/repo/migrations/20260429130000_create_chimeway_workflow_definitions_and_steps.exs
  - test/chimeway/notifier_contract_test.exs
autonomous: true
requirements:
  - WRK-01
  - API-02
must_haves:
  truths:
    - "A notifier can declare a workflow with stable workflow_key and workflow_version without persisting module names."
    - "Ordered workflow steps persist as first-class rows that can be queried independently of notification metadata."
    - "Replay-time consumers can look up a workflow definition from durable rows alone."
  artifacts:
    - path: "lib/chimeway/workflows/workflow_definition.ex"
      provides: "Versioned workflow declaration identity"
      min_lines: 40
    - path: "lib/chimeway/workflows/workflow_step.ex"
      provides: "Ordered durable workflow step rows"
      min_lines: 40
    - path: "priv/repo/migrations/20260429130000_create_chimeway_workflow_definitions_and_steps.exs"
      provides: "Workflow definition and step tables with unique constraints"
      contains: "create table(:chimeway_workflow_definitions"
  key_links:
    - from: "lib/chimeway/notifier.ex"
      to: "lib/chimeway/workflows.ex"
      via: "normalized workflow declaration payload"
      pattern: "resolve_workflow|serialize_workflow"
    - from: "lib/chimeway/workflows.ex"
      to: "lib/chimeway/workflows/workflow_definition.ex"
      via: "upsert/query declaration rows"
      pattern: "workflow_key|workflow_version"
---

<objective>
Create the durable workflow declaration contract for Phase 24.

Purpose: Implement D-01, D-02, and D-07 by giving journey-enabled notifiers a replay-safe declaration seam backed by first-class tables instead of metadata blobs.
Output: Normalized workflow declaration APIs, durable definition/step schemas, migration, and contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/24-workflow-contracts-state-spine/24-CONTEXT.md
@.planning/phases/24-workflow-contracts-state-spine/24-RESEARCH.md
@.planning/phases/24-workflow-contracts-state-spine/24-PATTERNS.md
@.planning/phases/21-template-versioning-rendering-contracts/21-CONTEXT.md
@.planning/phases/21-template-versioning-rendering-contracts/21-05-SUMMARY.md
@lib/chimeway/notifier.ex
@lib/chimeway/digests/digest_rule.ex
@test/chimeway/notifier_contract_test.exs

<interfaces>
From lib/chimeway/notifier.ex:
```elixir
@callback orchestration(map(), map()) ::
            {:ok, :immediate | :digest | :digest_held | keyword(atom()) | map()}
            | {:error, term()}

def resolve_orchestration(notifier, trigger_params, recipient, override \\ :unset)
def serialize_orchestration(orchestration) when is_map(orchestration)
```

From lib/chimeway/digests/digest_rule.ex:
```elixir
field(:rule_key, :string)
field(:rule_version, :integer)

|> unique_constraint([:rule_key, :rule_version],
  name: :chimeway_digest_rules_rule_key_rule_version_index
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the notifier workflow declaration contract and normalization seam</name>
  <files>lib/chimeway/notifier.ex, lib/chimeway/workflows.ex, test/chimeway/notifier_contract_test.exs</files>
  <read_first>
    .planning/phases/24-workflow-contracts-state-spine/24-CONTEXT.md
    .planning/phases/24-workflow-contracts-state-spine/24-RESEARCH.md
    lib/chimeway/notifier.ex
    lib/chimeway/rendering.ex
    test/chimeway/notifier_contract_test.exs
  </read_first>
  <behavior>
    - Test 1: a notifier `workflow/2` declaration with `workflow_key`, `workflow_version`, and ordered `steps` normalizes to string-keyed durable data.
    - Test 2: blank workflow keys, non-positive versions, duplicate step orders, blank step keys, and invalid channels return tagged `{:workflow_resolution_failed, ...}` errors.
    - Test 3: notifiers without `workflow/2` continue to resolve as `{:ok, nil}` so existing notification flows remain valid.
  </behavior>
  <action>Modify `Chimeway.Notifier` to add optional callback `workflow/2` and public helpers `resolve_workflow/3` and `serialize_workflow/1`. Normalize declarations into a map with exact keys `workflow_key`, `workflow_version`, `steps`, and `source`; normalize step entries to maps containing exact fields `step_key`, `step_order`, `channel`, and `config`; enforce string storage for channel and key fields; reject module names or callback-only identity by never serializing the notifier module; implement the persistence-facing functions in `Chimeway.Workflows` so later plans can call `upsert_definition/1`, `get_definition!/1`, and `definition_identity/1` against the normalized declaration. This task implements D-01, D-02, and D-07.</action>
  <acceptance_criteria>
    `rg -n "@callback workflow\\(map\\(\\), map\\(\\)\\)" lib/chimeway/notifier.ex` returns one match.
    `rg -n "def resolve_workflow\\(|def serialize_workflow\\(" lib/chimeway/notifier.ex` returns both functions.
    `rg -n "workflow_key|workflow_version|step_key|step_order" lib/chimeway/workflows.ex` shows the normalized declaration keys.
    `rg -n "workflow_resolution_failed" test/chimeway/notifier_contract_test.exs` returns new tagged-error assertions.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/notifier_contract_test.exs</automated>
  </verify>
  <done>The repo has a stable workflow declaration seam parallel to rendering/orchestration, with contract tests locking the exact normalization and error shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create durable workflow definition and step storage</name>
  <files>lib/chimeway/workflows.ex, lib/chimeway/workflows/workflow_definition.ex, lib/chimeway/workflows/workflow_step.ex, priv/repo/migrations/20260429130000_create_chimeway_workflow_definitions_and_steps.exs, test/chimeway/notifier_contract_test.exs</files>
  <read_first>
    .planning/phases/24-workflow-contracts-state-spine/24-CONTEXT.md
    lib/chimeway/workflows.ex
    lib/chimeway/digests/digest_rule.ex
    lib/chimeway/digests/digest_membership.ex
    priv/repo/migrations/20260428102000_create_chimeway_digest_rules.exs
    priv/repo/migrations/20260428102200_create_chimeway_digest_memberships.exs
  </read_first>
  <behavior>
    - Test 1: inserting the same `workflow_key` + `workflow_version` twice converges on one durable definition row.
    - Test 2: step rows persist explicit `step_order` and `channel` values and load in ascending order.
    - Test 3: definition lookup works from durable rows without invoking notifier callbacks.
  </behavior>
  <action>Create `chimeway_workflow_definitions` with columns `id`, `workflow_key`, `workflow_version`, `notification_key`, `inserted_at`, and `updated_at`; add unique index `:chimeway_workflow_definitions_workflow_key_workflow_version_index` and supporting index on `notification_key`. Create `chimeway_workflow_steps` with columns `id`, `workflow_definition_id`, `step_key`, `step_order`, `channel`, `config`, `inserted_at`, and `updated_at`; add unique indexes `:chimeway_workflow_steps_definition_step_key_index` on `[:workflow_definition_id, :step_key]` and `:chimeway_workflow_steps_definition_step_order_index` on `[:workflow_definition_id, :step_order]`. Implement `WorkflowDefinition` and `WorkflowStep` schemas with positive version/order validation, ordered association loading, and `config` kept as a tightly scoped map for deferred Phase 25-26 semantics only. Extend `Chimeway.Workflows` with explicit insert/upsert and preload helpers that later trigger/recovery plans can call directly. This task implements D-01, D-02, and D-07 without leaking workflow truth into `Notification.orchestration` or delivery metadata.</action>
  <acceptance_criteria>
    `rg -n "create table\\(:chimeway_workflow_definitions|create table\\(:chimeway_workflow_steps" priv/repo/migrations/20260429130000_create_chimeway_workflow_definitions_and_steps.exs` returns both tables.
    `rg -n "chimeway_workflow_definitions_workflow_key_workflow_version_index|chimeway_workflow_steps_definition_step_order_index" priv/repo/migrations/20260429130000_create_chimeway_workflow_definitions_and_steps.exs` returns both index names.
    `rg -n "field\\(:workflow_key|field\\(:workflow_version|field\\(:step_key|field\\(:step_order" lib/chimeway/workflows/workflow_definition.ex lib/chimeway/workflows/workflow_step.ex` returns all four fields.
    `rg -n "upsert_definition|get_definition!" lib/chimeway/workflows.ex` returns the durable lookup helpers.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/notifier_contract_test.exs</automated>
  </verify>
  <done>Workflow declarations and ordered steps persist in first-class rows with stable identity/version semantics and deterministic lookup helpers for later plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notifier callback -> workflow normalization | Untrusted notifier output becomes durable workflow declaration truth. |
| app code -> database declaration rows | Trigger-time persistence writes long-lived workflow identity and step order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | T | `Chimeway.Notifier.resolve_workflow/3` | mitigate | Validate `workflow_key`, `workflow_version`, `step_key`, `step_order`, and `channel` before any persistence; reject blank keys and duplicate orders. |
| T-24-02 | R | workflow definition storage | mitigate | Persist explicit versioned rows plus unique indexes so historical declarations are queryable and replay-safe without callback ambiguity. |
| T-24-03 | E | notifier module identity leakage | mitigate | Never serialize module names or MFA references into workflow rows; store only stable string keys and versions per D-02. |
</threat_model>

<verification>
Run `mix test test/chimeway/notifier_contract_test.exs`.
</verification>

<success_criteria>
Workflow declarations are normalized once, stored with stable identity/version semantics, and recoverable from durable definition and step rows without notifier callback re-entry.
</success_criteria>

<output>
After completion, create `.planning/phases/24-workflow-contracts-state-spine/24-01-SUMMARY.md`
</output>
