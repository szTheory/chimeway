---
phase: 24-workflow-contracts-state-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chimeway/notifier.ex
  - lib/chimeway/workflows.ex
  - lib/chimeway/workflows/workflow_definition.ex
  - lib/chimeway/workflows/workflow_step.ex
  - priv/repo/migrations/*_create_chimeway_workflow_definitions.exs
  - priv/repo/migrations/*_create_chimeway_workflow_steps.exs
  - test/chimeway/notifier_contract_test.exs
autonomous: true
requirements:
  - WRK-01
  - API-02
must_haves:
  truths:
    - "Workflow declarations persist a stable `workflow_key` and `workflow_version` rather than module-name identity."
    - "Ordered workflow steps are stored as explicit durable rows, not only inside notification metadata or planning context."
    - "Workflow declaration normalization is replay-safe and does not require callback re-entry after persistence."
  artifacts:
    - path: "lib/chimeway/notifier.ex"
      provides: "Workflow declaration callback, normalization, and durable serialization seam"
      min_lines: 60
    - path: "lib/chimeway/workflows/workflow_definition.ex"
      provides: "Versioned workflow definition schema with `(workflow_key, workflow_version)` uniqueness"
      min_lines: 40
    - path: "lib/chimeway/workflows/workflow_step.ex"
      provides: "Ordered workflow step schema linked to the definition row"
      min_lines: 40
    - path: "test/chimeway/notifier_contract_test.exs"
      provides: "Contract coverage for valid and invalid workflow declarations"
      min_lines: 60
  key_links:
    - from: "lib/chimeway/notifier.ex"
      to: "lib/chimeway/workflows/workflow_definition.ex"
      via: "normalized workflow declarations serialize into durable definition identity plus ordered step facts"
      pattern: "workflow_key|workflow_version|steps"
    - from: "lib/chimeway/workflows/workflow_definition.ex"
      to: "lib/chimeway/workflows/workflow_step.ex"
      via: "one durable definition owns ordered step rows"
      pattern: "has_many|belongs_to|step_order"
---

<objective>
Establish the durable workflow declaration contract and schema layer that Phase 24 depends on.

Purpose: Phase 24 needs a first-class workflow identity model before any run state or delivery linkage can exist. This plan creates the notifier workflow declaration seam, the versioned workflow definition tables, and contract tests that prove Chimeway persists stable workflow identity without relying on notifier module names.
Output: workflow declaration normalization code, definition/step schemas and migrations, and contract tests covering valid and invalid declarations.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/24-workflow-contracts-state-spine/24-CONTEXT.md
@.planning/phases/24-workflow-contracts-state-spine/24-RESEARCH.md
@.planning/phases/24-workflow-contracts-state-spine/24-PATTERNS.md
@AGENTS.md
@lib/chimeway/notifier.ex
@lib/chimeway/digests/digest_rule.ex
@test/chimeway/notifier_contract_test.exs

<interfaces>
From `lib/chimeway/notifier.ex`:
```elixir
@callback notification_key() :: String.t()
@callback version() :: pos_integer()
def resolve_orchestration(module() | nil, map(), map(), term())
def serialize_orchestration(orchestration)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED contract coverage for workflow declaration normalization</name>
  <files>test/chimeway/notifier_contract_test.exs</files>
  <read_first>
    - test/chimeway/notifier_contract_test.exs
    - lib/chimeway/notifier.ex
    - lib/chimeway/rendering.ex
    - .planning/phases/24-workflow-contracts-state-spine/24-CONTEXT.md
    - .planning/phases/24-workflow-contracts-state-spine/24-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: a notifier can declare `workflow_key`, `workflow_version`, and ordered steps with per-step `step_key`, `channel`, and `step_order`.
    - Test 2: blank `workflow_key`, non-positive `workflow_version`, duplicate `step_key`, and non-sequential or invalid `step_order` values are rejected with tagged workflow-declaration errors.
    - Test 3: serialization emits string-keyed durable data that can be replayed without notifier callback re-entry.
  </behavior>
  <action>Extend `test/chimeway/notifier_contract_test.exs` with workflow declaration fixtures and assertions beside the existing orchestration/rendering contract coverage. Add tests that call a new workflow resolution function in `Chimeway.Notifier` and assert the normalized output contains `workflow_key`, `workflow_version`, and a `steps` list sorted by `step_order`. Add negative tests for `workflow_key: \"\"`, `workflow_version: 0`, duplicate `step_key`, duplicate `step_order`, blank `channel`, and invalid declaration shapes so the executor has a red test suite before implementing the contract.</action>
  <acceptance_criteria>
    - `test/chimeway/notifier_contract_test.exs` contains `workflow_key`
    - `test/chimeway/notifier_contract_test.exs` contains `workflow_version`
    - `test/chimeway/notifier_contract_test.exs` contains `step_order`
    - `test/chimeway/notifier_contract_test.exs` contains `invalid_workflow_declaration`
    - `mix test test/chimeway/notifier_contract_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/notifier_contract_test.exs --trace</automated>
  </verify>
  <done>The contract tests define the exact durable workflow declaration surface and invalid-shape error posture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement workflow declaration normalization and versioned schema storage</name>
  <files>lib/chimeway/notifier.ex, lib/chimeway/workflows.ex, lib/chimeway/workflows/workflow_definition.ex, lib/chimeway/workflows/workflow_step.ex, priv/repo/migrations/*_create_chimeway_workflow_definitions.exs, priv/repo/migrations/*_create_chimeway_workflow_steps.exs</files>
  <read_first>
    - lib/chimeway/notifier.ex
    - lib/chimeway/digests/digest_rule.ex
    - lib/chimeway/digests/digest_membership.ex
    - priv/repo/migrations/20260428102000_create_chimeway_digest_rules.exs
    - test/chimeway/notifier_contract_test.exs
    - .planning/phases/24-workflow-contracts-state-spine/24-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: notifiers expose an optional workflow declaration callback that resolves into one normalized durable map.
    - Test 2: durable workflow definitions persist with uniqueness on `workflow_key` plus `workflow_version`.
    - Test 3: ordered step rows persist separately from the definition row and remain queryable by `step_order`.
    - Test 4: serialization remains string-keyed and detached from notifier module names.
  </behavior>
  <action>Add an optional `workflow/2` callback to `lib/chimeway/notifier.ex` and implement a workflow-resolution pipeline parallel to `resolve_orchestration/4`. Normalize declarations into a map with exact keys `workflow_key`, `workflow_version`, and `steps`, where each step has `step_key`, `step_order`, `channel`, and a persisted `config` map for later-phase progression metadata. Add `serialize_workflow/1` and a replay helper that accept only durable data. Create `lib/chimeway/workflows/workflow_definition.ex` with `workflow_key`, `workflow_version`, `notification_key`, and `has_many :steps`; validate `workflow_version > 0` and add a uniqueness constraint named for `(workflow_key, workflow_version)`. Create `lib/chimeway/workflows/workflow_step.ex` with `workflow_definition_id`, `step_key`, `step_order`, `channel`, and `config`; validate required fields and add uniqueness constraints on `(workflow_definition_id, step_key)` and `(workflow_definition_id, step_order)`. Add `lib/chimeway/workflows.ex` with explicit upsert/fetch helpers that store one definition row and ordered step rows from the normalized declaration. Create two migrations that add `chimeway_workflow_definitions` and `chimeway_workflow_steps` tables with the same explicit-column posture used by the digest migrations.</action>
  <acceptance_criteria>
    - `lib/chimeway/notifier.ex` contains `@callback workflow(map(), map())`
    - `lib/chimeway/notifier.ex` contains `def resolve_workflow`
    - `lib/chimeway/notifier.ex` contains `def serialize_workflow`
    - `lib/chimeway/workflows/workflow_definition.ex` contains `field(:workflow_key, :string)`
    - `lib/chimeway/workflows/workflow_definition.ex` contains `field(:workflow_version, :integer)`
    - `lib/chimeway/workflows/workflow_step.ex` contains `field(:step_order, :integer)`
    - `lib/chimeway/workflows/workflow_step.ex` contains `field(:channel, :string)`
    - `rg -n 'create table\\(:chimeway_workflow_definitions\\)|create unique_index\\(:chimeway_workflow_definitions, \\[:workflow_key, :workflow_version\\]\\)' priv/repo/migrations` returns matches
    - `rg -n 'create table\\(:chimeway_workflow_steps\\)|step_order' priv/repo/migrations` returns matches
    - `mix test test/chimeway/notifier_contract_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/notifier_contract_test.exs --trace</automated>
  </verify>
  <done>Chimeway has a durable workflow declaration contract and a versioned schema layer that stores ordered steps explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notifier callback -> durable declaration rows | Untrusted callback output must normalize into explicit stable workflow identity |
| workflow definition -> ordered steps | Durable workflow truth must not drift into opaque metadata blobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | T | `lib/chimeway/notifier.ex` | mitigate | Reject blank keys, invalid versions, duplicate step ids, and invalid channels during normalization. |
| T-24-02 | R | workflow declaration persistence | mitigate | Persist explicit `workflow_key`, `workflow_version`, and ordered step rows with uniqueness constraints so module-name drift cannot rewrite history. |
| T-24-03 | I | workflow schema shape | mitigate | Keep step declarations in first-class rows and limit free-form `config` to bounded future-phase metadata instead of hiding identity in JSON blobs. |
</threat_model>

<verification>
Run `mix test test/chimeway/notifier_contract_test.exs --trace`.
</verification>

<success_criteria>
Workflow declarations normalize into one explicit durable contract and persist as versioned definition plus ordered step rows that are independent from notifier module names.
</success_criteria>

<output>
After completion, create `.planning/phases/24-workflow-contracts-state-spine/24-01-SUMMARY.md`
</output>
