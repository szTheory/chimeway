---
phase: 75-runtime-prefix-propagation
plan: 05
type: execute
wave: 3
depends_on: ["75-04"]
files_modified:
  - lib/chimeway/digests.ex
  - lib/chimeway/digests/accumulation.ex
  - lib/chimeway/digests/emission.ex
  - lib/chimeway/webhooks.ex
  - lib/chimeway/webhooks/process_feedback_worker.ex
autonomous: true
requirements: [RUN-03]

must_haves:
  truths:
    - "Digest buckets, digest memberships, digest emission, webhook ingress, and feedback processing use the configured Chimeway prefix per RUN-03."
    - "Digest string/source insert_all and update_all calls are audited per D-10."
    - "Webhook and digest workers reload durable IDs from Chimeway storage and never carry prefix values in job args per D-12."
    - "Sensitive webhook/provider payload fields do not leak in new diagnostics per project security guidance."
  artifacts:
    - path: "lib/chimeway/digests.ex"
      provides: "Digest rule entrypoints audited under repo defaults"
    - path: "lib/chimeway/digests/accumulation.ex"
      provides: "Digest bucket/member insert_all and update_all audited under repo defaults"
    - path: "lib/chimeway/digests/emission.ex"
      provides: "Digest emission event/notification/delivery writes audited under repo defaults"
    - path: "lib/chimeway/webhooks.ex"
      provides: "Webhook ingress transaction audited under repo defaults"
    - path: "lib/chimeway/webhooks/process_feedback_worker.ex"
      provides: "Ingress worker reload-by-id audited under repo defaults"
  key_links:
    - from: "lib/chimeway/digests/accumulation.ex"
      to: "lib/chimeway/repo.ex"
      via: "Repo.insert_all/update_all operation defaults"
      pattern: "Repo\\.(insert_all|update_all)"
    - from: "lib/chimeway/webhooks/process_feedback_worker.ex"
      to: "lib/chimeway/webhooks.ex"
      via: "durable ingress_id worker reload"
      pattern: "ingress_id"
---

<objective>
Propagate runtime prefix behavior through digest and webhook feedback paths after the core async boundary is safe.

Purpose: digest and webhook flows are high-risk RUN-03 paths because they combine transactions, worker reloads, and bulk operations.
Output: green digest/webhook prefixed tests and unchanged public-mode digest/webhook behavior.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/75-runtime-prefix-propagation/75-CONTEXT.md
@.planning/phases/75-runtime-prefix-propagation/75-RESEARCH.md
@.planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
@lib/chimeway/digests.ex
@lib/chimeway/digests/accumulation.ex
@lib/chimeway/digests/emission.ex
@lib/chimeway/webhooks.ex
@lib/chimeway/webhooks/process_feedback_worker.ex
@test/chimeway/runtime_prefix_integration_test.exs
@test/chimeway/digests/accumulation_test.exs
@test/chimeway/digests/emission_test.exs
@test/chimeway/digests/flush_scheduling_test.exs
@test/chimeway/webhooks/ingress_test.exs
@test/chimeway/webhooks/process_feedback_worker_test.exs
</context>

## Artifacts This Phase Produces

- Prefixed proof for `Chimeway.Digests`, `Chimeway.Digests.Accumulation`, and `Chimeway.Digests.Emission`
- Prefixed proof for `Chimeway.Webhooks.process/4`
- Prefixed proof for `Chimeway.Webhooks.ProcessFeedbackWorker.perform/1`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove digest accumulation and emission use configured storage</name>
  <files>lib/chimeway/digests.ex, lib/chimeway/digests/accumulation.ex, lib/chimeway/digests/emission.ex</files>
  <read_first>
    lib/chimeway/digests.ex
    lib/chimeway/digests/accumulation.ex
    lib/chimeway/digests/emission.ex
    test/chimeway/runtime_prefix_integration_test.exs
    test/chimeway/digests/accumulation_test.exs
    test/chimeway/digests/emission_test.exs
    test/chimeway/digests/flush_scheduling_test.exs
    .planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
  </read_first>
  <behavior>
    - Digest rules, buckets, memberships, emitted events, emitted notifications, and emitted deliveries are read/written in configured storage.
    - Digest `insert_all`, `update_all`, and reload paths receive repo defaults or a justified explicit Storage repo opts exception.
    - Digest flush scheduling remains compatible with the Oban job-table boundary from Plan 75-04.
  </behavior>
  <action>Run and satisfy the `:runtime_prefix_digest` proof. Prefer `Chimeway.Repo.default_options/1` for ordinary digest reads and writes. If a digest `Repo.insert_all`, `Repo.update_all`, or reload operation bypasses repo defaults in this stack, add `Chimeway.Storage.repo_opts/1` to that operation only and cite the exact operation in the summary per D-10. Keep digest notification identity stable with `notification_key` and version per D-27, and do not add prefix values to digest flush job args per D-12.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_digest --warnings-as-errors</automated>
    <automated>MIX_ENV=test mix test test/chimeway/digests/accumulation_test.exs test/chimeway/digests/emission_test.exs test/chimeway/digests/flush_scheduling_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: digest rows for rules, buckets, memberships, emitted notification, emitted delivery, and attempts are proven in configured storage by the runtime prefix test.
    - Source assertion: digest flush job args remain `bucket_id` based and do not include prefix values.
    - Source assertion: no digest code relies on transaction opts or `search_path` for Chimeway table routing.
  </acceptance_criteria>
  <done>Digest accumulation and emission paths use configured storage and existing digest tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove webhook ingress and feedback worker use configured storage</name>
  <files>lib/chimeway/webhooks.ex, lib/chimeway/webhooks/process_feedback_worker.ex</files>
  <read_first>
    lib/chimeway/webhooks.ex
    lib/chimeway/webhooks/process_feedback_worker.ex
    test/chimeway/runtime_prefix_integration_test.exs
    test/chimeway/webhooks/ingress_test.exs
    test/chimeway/webhooks/process_feedback_worker_test.exs
    .planning/phases/75-runtime-prefix-propagation/75-CONTEXT.md
  </read_first>
  <behavior>
    - `Chimeway.Webhooks.process/4` persists ingress rows and enqueues `ProcessFeedbackWorker` by durable `ingress_id`.
    - `ProcessFeedbackWorker.perform/1` reloads ingress and updates delivery, attempt, signal, and trace facts from configured storage.
    - Feedback processing keeps provider payload details redacted in operator-facing surfaces.
  </behavior>
  <action>Run and satisfy the `:runtime_prefix_webhook` proof. Prefer repo defaults for `Chimeway.Webhooks` transaction inserts and `ProcessFeedbackWorker` reloads. If a specific `Repo.get`, transaction callback, or feedback update bypasses repo defaults, add a local explicit `Chimeway.Storage.repo_opts/1` only to that operation and document why. Preserve durable `ingress_id` job args per D-12 and avoid payload-bearing logs or diagnostics per project security guidance and D-23.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_webhook --warnings-as-errors</automated>
    <automated>MIX_ENV=test mix test test/chimeway/webhooks/ingress_test.exs test/chimeway/webhooks/process_feedback_worker_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: webhook ingress rows and feedback-updated delivery/attempt/signal rows are proven in configured storage.
    - Source assertion: `ProcessFeedbackWorker` job args remain `ingress_id` based for current path handling.
    - Behavior assertion: existing webhook public-mode tests remain green.
    - Source assertion: no new diagnostics include raw payload, provider body, rendered data, or secret-bearing metadata.
  </acceptance_criteria>
  <done>Webhook ingress and feedback worker paths use configured storage and preserve redaction boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| digest scheduler -> Oban job table | Digest flush jobs use Oban while digest rows remain Chimeway-owned storage. |
| webhook ingress -> feedback processing | External feedback becomes durable ingress and delivery/signal updates. |
| provider payload -> operator surfaces | Sensitive provider data must remain redacted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-16 | Information Disclosure | Digest accumulation/emission | mitigate | Prove digest rows in configured schema and keep job args durable-ID only. |
| T-75-17 | Information Disclosure | Webhook feedback processing | mitigate | Preserve feedback redaction and avoid payload-bearing logs while proving prefixed ingress rows. |
| T-75-18 | Tampering | Bulk digest operations | mitigate | Audit insert_all/update_all and add operation-level Storage opts only where repo defaults do not apply. |
| T-75-SC | Tampering | Package installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run the digest and webhook runtime prefix tags plus existing digest and webhook tests listed in task verification.
</verification>

<success_criteria>
Digest and webhook portions of RUN-03 are green in configured prefix mode, public-mode tests remain green, and sensitive payloads remain out of diagnostics.
</success_criteria>

<output>
Create `.planning/phases/75-runtime-prefix-propagation/75-05-SUMMARY.md` when done.
</output>
