---
phase: 75-runtime-prefix-propagation
plan: 03
type: execute
wave: 2
depends_on: ["75-02"]
files_modified:
  - lib/chimeway/admin.ex
  - lib/chimeway/traces.ex
  - lib/chimeway/deliveries.ex
  - lib/chimeway/inbox.ex
autonomous: true
requirements: [RUN-02, RUN-04]

must_haves:
  truths:
    - "Admin, trace, inbox, and recovery reads use configured storage by default per RUN-04."
    - "Explicit prefix probes remain available for tests and diagnostics per D-05."
    - "Tenant predicates and redacted operator DTOs remain unchanged while prefix routing is added per D-20, D-23, and D-24."
    - "Ordinary inbox and recovery APIs do not require or expose prefix options per D-03 and D-21."
  artifacts:
    - path: "lib/chimeway/admin.ex"
      provides: "Admin read-model option filtering aligned with Storage repo options"
    - path: "lib/chimeway/traces.ex"
      provides: "Trace/explainability repo option filtering with probe preservation"
    - path: "lib/chimeway/deliveries.ex"
      provides: "Recovery query path verified under repo defaults"
    - path: "lib/chimeway/inbox.ex"
      provides: "Inbox list/read/seen signal path verified under repo defaults"
  key_links:
    - from: "lib/chimeway/admin.ex"
      to: "lib/chimeway/storage.ex"
      via: "local domain-option filtering delegates to Storage.repo_opts/1 when helper is present"
      pattern: "repo_opts"
    - from: "lib/chimeway/traces.ex"
      to: "test/chimeway/traces_test.exs"
      via: "explicit prefix probe tests remain green"
      pattern: "prefix: \"nonexistent_schema\""
---

<objective>
Propagate the configured storage prefix through operator-facing read/write surfaces while preserving tenant and redaction contracts.

Purpose: RUN-04 is not just row placement; support operators must keep the same explainability and privacy behavior with prefixed storage.
Output: aligned option filtering for admin/trace helpers and green prefixed operator-surface tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/75-runtime-prefix-propagation/75-CONTEXT.md
@.planning/phases/75-runtime-prefix-propagation/75-RESEARCH.md
@.planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
@lib/chimeway/admin.ex
@lib/chimeway/traces.ex
@lib/chimeway/deliveries.ex
@lib/chimeway/inbox.ex
@test/chimeway/runtime_prefix_integration_test.exs
@test/chimeway/admin_test.exs
@test/chimeway/traces_test.exs
@test/chimeway/inbox_integration_test.exs
@test/chimeway/orchestration/recovery_test.exs
</context>

## Artifacts This Phase Produces

- Updated admin domain-option filtering for `Chimeway.Admin.recent_problem_deliveries/1`, `definitions/1`, `feed/1`, and `recovery_candidates/1`
- Updated trace option filtering for `Chimeway.Traces.get_trace/2`, `find_traces_for_recipient/2`, `find_traces_by_correlation_id/2`, and `explain_delivery/2` where needed
- Prefixed operator proof for inbox read/seen signal emission, trace reads, admin read models, and recovery APIs

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align admin and trace option filtering with Storage contract</name>
  <files>lib/chimeway/admin.ex, lib/chimeway/traces.ex</files>
  <read_first>
    lib/chimeway/admin.ex
    lib/chimeway/traces.ex
    lib/chimeway/storage.ex
    test/chimeway/admin_test.exs
    test/chimeway/traces_test.exs
    test/chimeway/runtime_prefix_integration_test.exs
    .planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
  </read_first>
  <behavior>
    - Domain/query keys such as `:limit`, `:tenant_id`, `:recipient_id`, `:older_than`, `:now`, and filters do not reach Repo operations.
    - Any explicit `:prefix` probe left after domain keys are dropped remains caller-controlled.
    - Admin read models remain tenant-filtered and do not expose payload, render, provider, or metadata secrets.
  </behavior>
  <action>Update local option-filtering helpers in `Chimeway.Admin` and `Chimeway.Traces` only where they currently prepare Repo opts. The target shape is domain-key dropping followed by `Chimeway.Storage.repo_opts/1` per D-04 and D-05, while still letting caller-supplied `:prefix` win through `Keyword.put_new/3` in Storage per D-02. Preserve existing query predicates, DTO fields, redaction behavior, and explicit prefix-probe tests. Do not introduce a new storage context struct, wrapper repo, or broad facade per D-07.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/admin_test.exs test/chimeway/traces_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `lib/chimeway/admin.ex` repo option helper drops domain keys and delegates to `Chimeway.Storage.repo_opts/1`.
    - Source assertion: trace option filtering preserves caller `:prefix` probes after dropping trace-only keys.
    - Behavior assertion: admin tenant/redaction tests continue to pass.
    - Behavior assertion: trace explicit prefix probe tests continue to raise against the probe schema rather than silently using configured storage.
  </acceptance_criteria>
  <done>Admin and trace Repo option filtering is centralized on Storage while existing privacy and probe behavior stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove inbox and recovery surfaces route through configured storage</name>
  <files>lib/chimeway/deliveries.ex, lib/chimeway/inbox.ex</files>
  <read_first>
    lib/chimeway/deliveries.ex
    lib/chimeway/inbox.ex
    lib/chimeway/signal.ex
    test/chimeway/runtime_prefix_integration_test.exs
    test/chimeway/inbox_integration_test.exs
    test/chimeway/inbox_state_transition_test.exs
    test/chimeway/orchestration/recovery_test.exs
  </read_first>
  <behavior>
    - `Chimeway.list_for_recipient/2`, `unread_count/2`, `mark_read/3`, `mark_seen/3`, and archive/read update paths use configured storage.
    - First-transition inbox signals land in configured storage and still skip unresolved tenants.
    - `Chimeway.recover_event/2`, `Chimeway.recover_delivery/2`, and `Deliveries.list_recoverable_*` preserve tenant scope and recovery metadata redaction.
  </behavior>
  <action>Run the `:runtime_prefix_operator` proof against inbox and recovery paths. Keep `Chimeway.Inbox` and `Chimeway.Deliveries` APIs storage-agnostic per D-03 and D-21. If a path fails because default repo options do not reach a specific `Repo.update_all`, `Repo.get_by`, or recovery query, add explicit `Chimeway.Storage.repo_opts/1` only at that operation and keep tenant predicates such as `recipient_identity`, `tenant_id`, and recovery metadata filters intact per D-10, D-20, D-23, and D-24. Do not add payload-bearing diagnostics or expose Ecto prefix terms in operator-facing data.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --only runtime_prefix_operator --warnings-as-errors</automated>
    <automated>MIX_ENV=test mix test test/chimeway/inbox_integration_test.exs test/chimeway/inbox_state_transition_test.exs test/chimeway/orchestration/recovery_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: `:runtime_prefix_operator` exits 0 and proves inbox, signal, admin, trace, and recovery rows are read/written from configured storage.
    - Behavior assertion: existing inbox transition and recovery tests remain green in public legacy mode.
    - Source assertion: no prefix value is added to ordinary inbox or recovery public API arguments.
    - Source assertion: recovery metadata remains allowlisted and existing raw session/auth values are not persisted.
  </acceptance_criteria>
  <done>Operator-facing runtime paths use configured storage and preserve tenant/redaction behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator query opts -> Repo | Admin/trace domain filters and diagnostic prefix probes cross into Repo opts. |
| recipient API -> inbox rows | Recipient identity must remain a data predicate, not a DB prefix. |
| recovery API -> durable metadata | Operator evidence is persisted on recovery rows with allowlisted fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-07 | Information Disclosure | Admin and traces | mitigate | Preserve tenant filters and redacted DTO tests while adding Storage repo opts. |
| T-75-08 | Elevation of Privilege | Inbox and recovery recipient/tenant predicates | mitigate | Keep `recipient_identity` and `tenant_id` predicates unchanged and run existing tenant tests. |
| T-75-09 | Information Disclosure | Recovery/operator metadata | mitigate | Preserve allowlisted recovery metadata and avoid payload-bearing diagnostics. |
| T-75-SC | Tampering | Package installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run the operator runtime prefix tag plus existing admin, trace, inbox, and recovery tests listed in task verification.
</verification>

<success_criteria>
RUN-04 surfaces are green in configured prefix mode, explicit diagnostic probes still work, and public legacy tests remain green.
</success_criteria>

<output>
Create `.planning/phases/75-runtime-prefix-propagation/75-03-SUMMARY.md` when done.
</output>
