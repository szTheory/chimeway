---
phase: 75-runtime-prefix-propagation
plan: 07
type: execute
wave: 4
depends_on: ["75-03", "75-05", "75-06"]
files_modified:
  - mix.exs
autonomous: true
requirements: [RUN-01, RUN-02, RUN-03, RUN-04]

must_haves:
  truths:
    - "mix verify.runtime_prefix runs the focused Phase 75 prefix proof per D-19."
    - "mix verify.runtime_prefix passes after runtime prefix propagation plans complete."
    - "mix ci.test and mix verify.install_golden remain green, proving public legacy mode and generated migration proof are still intact per D-14 and Phase 74 dependencies."
    - "No Phase 75 gate expands beyond focused runtime-prefix proof."
  artifacts:
    - path: "mix.exs"
      provides: "Focused runtime prefix verification alias"
      exports: ["verify.runtime_prefix"]
  key_links:
    - from: "mix.exs"
      to: "test/chimeway/repo_prefix_test.exs"
      via: "verify.runtime_prefix command"
      pattern: "repo_prefix_test"
    - from: "mix.exs"
      to: "test/chimeway/runtime_prefix_integration_test.exs"
      via: "verify.runtime_prefix command"
      pattern: "runtime_prefix_integration_test"
---

<objective>
Add the focused local runtime-prefix verification gate and run final Phase 75 proof without broadening the alias beyond runtime-prefix tests.

Purpose: make the runtime prefix behavior repeatable for maintainers while preserving CI/local parity patterns.
Output: `mix verify.runtime_prefix` alias and final green verification evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/75-runtime-prefix-propagation/75-CONTEXT.md
@.planning/phases/75-runtime-prefix-propagation/75-RESEARCH.md
@.planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
@.planning/phases/75-runtime-prefix-propagation/75-VALIDATION.md
@mix.exs
@test/chimeway/repo_prefix_test.exs
@test/chimeway/runtime_prefix_integration_test.exs
@test/chimeway/migration_contract_test.exs
</context>

## Artifacts This Phase Produces

- `mix verify.runtime_prefix` alias
- Final phase proof command set: `mix verify.runtime_prefix`, `mix ci.test`, and `mix verify.install_golden`
- Phase 75 summary evidence that RUN-01, RUN-02, RUN-03, and RUN-04 are covered

<tasks>

<task type="auto">
  <name>Task 1: Add focused runtime prefix verify alias</name>
  <files>mix.exs</files>
  <read_first>
    mix.exs
    test/chimeway/repo_prefix_test.exs
    test/chimeway/runtime_prefix_integration_test.exs
    .planning/phases/75-runtime-prefix-propagation/75-CONTEXT.md
    .planning/phases/75-runtime-prefix-propagation/75-VALIDATION.md
  </read_first>
  <action>Add a `verify.runtime_prefix` alias to `mix.exs` near the existing `verify.install_golden` alias. The command must run `MIX_ENV=test mix test test/chimeway/repo_prefix_test.exs test/chimeway/runtime_prefix_integration_test.exs --warnings-as-errors`. Keep the alias focused on Phase 75 runtime proof per D-19; do not add commands outside those focused runtime-prefix test files. Do not remove or rewrite existing `mix ci.test`, `verify.install_golden`, or ecosystem `verify.*` aliases.</action>
  <verify>
    <automated>mix verify.runtime_prefix</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `mix.exs` contains `"verify.runtime_prefix"` in `aliases/0`.
    - Source assertion: the alias command references `test/chimeway/repo_prefix_test.exs` and `test/chimeway/runtime_prefix_integration_test.exs`.
    - Source assertion: existing `verify.install_golden`, `ci.test`, `verify.mailglass`, `verify.accrue`, `verify.inbox`, `verify.threadline`, `verify.sigra`, and `verify.admin` aliases remain present.
    - Behavior assertion: `mix verify.runtime_prefix` exits 0.
  </acceptance_criteria>
  <done>The focused runtime prefix alias exists and passes.</done>
</task>

<task type="auto">
  <name>Task 2: Run final runtime-prefix and legacy public gates</name>
  <files>mix.exs</files>
  <read_first>
    mix.exs
    .planning/phases/75-runtime-prefix-propagation/75-VALIDATION.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </read_first>
  <action>Run the final Phase 75 automated proof: `mix verify.runtime_prefix`, `mix ci.test`, and `mix verify.install_golden`. Treat `mix verify.runtime_prefix` as the RUN-01 through RUN-04 phase gate, `mix ci.test` as public legacy compatibility proof per D-14, and `mix verify.install_golden` as Phase 74 migration-generation regression proof. If any command fails because of Phase 75 work, fix the failing plan-owned source. If a command fails due unrelated dirty-worktree drift, record the exact failure and scope in the summary without reverting user changes.</action>
  <verify>
    <automated>mix verify.runtime_prefix</automated>
    <automated>mix ci.test</automated>
    <automated>mix verify.install_golden</automated>
  </verify>
  <acceptance_criteria>
    - CLI output assertion: `mix verify.runtime_prefix` exits 0.
    - CLI output assertion: `mix ci.test` exits 0 or the summary records a pre-existing unrelated failure with evidence.
    - CLI output assertion: `mix verify.install_golden` exits 0 or the summary records a pre-existing unrelated failure with evidence.
    - Source assertion: no Phase 75 code introduces request-scoped database prefixes, `@schema_prefix`, or new public prefix options on ordinary Chimeway APIs.
  </acceptance_criteria>
  <done>Final focused runtime-prefix proof and legacy public/migration gates have executable evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer CLI -> test database | Verify alias runs DB-backed prefix proof. |
| local gate -> release confidence | Focused runtime proof must stay limited to runtime-prefix behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-13 | Repudiation | mix verify.runtime_prefix | mitigate | Make the phase gate executable and focused on concrete test files. |
| T-75-14 | Information Disclosure | Final test output | mitigate | Do not add payload-bearing diagnostics to verification output. |
| T-75-15 | Tampering | Existing verify aliases | mitigate | Keep existing CI/local parity aliases intact and avoid broad alias changes in this phase. |
| T-75-SC | Tampering | Package installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run `mix verify.runtime_prefix`, `mix ci.test`, and `mix verify.install_golden`.
</verification>

<success_criteria>
The phase has a focused green runtime-prefix gate, public legacy mode remains green, and installer migration proof remains green.
</success_criteria>

<output>
Create `.planning/phases/75-runtime-prefix-propagation/75-07-SUMMARY.md` when done.
</output>
