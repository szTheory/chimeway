---
phase: 75-runtime-prefix-propagation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/support/prefixed_runtime_case.ex
  - test/chimeway/repo_prefix_test.exs
  - test/chimeway/runtime_prefix_integration_test.exs
autonomous: true
requirements: [RUN-01, RUN-02, RUN-03, RUN-04]

must_haves:
  truths:
    - "Prefixed runtime tests use Chimeway.Repo itself against real PostgreSQL rows in the configured chimeway schema per D-15 and D-17."
    - "Repo guardrails assert Chimeway.Repo.default_options/1 delegates to Chimeway.Storage.repo_opts/1 and preserves explicit prefix probes per D-01, D-02, D-05, and D-18."
    - "Runtime prefix integration coverage exists for trigger-to-trace, duplicate idempotency, inbox read/seen, workflow progression, digest, webhook, recovery, admin, trace, and explicit dispatch worker reload paths per D-16."
    - "Default public-schema legacy mode remains explicit and is restored after prefixed tests per D-14."
  artifacts:
    - path: "test/support/prefixed_runtime_case.ex"
      provides: "Serialized prefixed runtime ExUnit case and schema placement helpers"
      exports: ["Chimeway.PrefixedRuntimeCase"]
    - path: "test/chimeway/repo_prefix_test.exs"
      provides: "Repo default-options and Storage.repo_opts guardrails"
      exports: ["Chimeway.RepoPrefixTest"]
    - path: "test/chimeway/runtime_prefix_integration_test.exs"
      provides: "End-to-end prefixed runtime proof tags for RUN-01 through RUN-04"
      exports: ["Chimeway.RuntimePrefixIntegrationTest"]
  key_links:
    - from: "test/support/prefixed_runtime_case.ex"
      to: "test/fixtures/installer_golden_prefixed/"
      via: "runs generated prefixed fixture migrations or equivalent normal migrate path against Chimeway.Repo"
      pattern: "installer_golden_prefixed"
    - from: "test/chimeway/repo_prefix_test.exs"
      to: "lib/chimeway/repo.ex"
      via: "asserts Chimeway.Repo.default_options/1 operation behavior"
      pattern: "default_options"
    - from: "test/chimeway/runtime_prefix_integration_test.exs"
      to: "lib/chimeway/trigger.ex"
      via: "ordinary Chimeway.trigger/3 calls, not caller-supplied prefix options"
      pattern: "Chimeway\\.trigger"
---

<objective>
Create the Phase 75 Wave 0 test harness and guardrails that define the storage-prefix runtime contract before production code changes land.

Purpose: lock the evidence surface for RUN-01 through RUN-04 so later plans can make focused runtime changes without inventing new prefix APIs.
Output: prefixed runtime case support, repo default-option tests, and a tagged runtime integration suite.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/75-runtime-prefix-propagation/75-CONTEXT.md
@.planning/phases/75-runtime-prefix-propagation/75-RESEARCH.md
@.planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
@.planning/phases/75-runtime-prefix-propagation/75-VALIDATION.md
@.planning/phases/73-storage-prefix-contract/73-01-SUMMARY.md
@.planning/phases/74-prefixed-migration-generator/74-09-SUMMARY.md
@.planning/phases/74-prefixed-migration-generator/74-10-SUMMARY.md
@test/support/data_case.ex
@test/chimeway/storage_test.exs
@test/chimeway/migration_contract_test.exs
@test/chimeway/trigger_pipeline_test.exs
@test/chimeway/inbox_integration_test.exs
@test/chimeway/orchestration/workflow_progression_test.exs
@test/chimeway/dispatch/oban_worker_test.exs
@test/chimeway/orchestration/deferred_resume_test.exs
@test/chimeway/digests/emission_test.exs
@test/chimeway/webhooks/process_feedback_worker_test.exs
@test/chimeway/orchestration/recovery_test.exs
</context>

## Source Coverage Audit

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | - | Thread the storage-prefix contract through real runtime behavior | 01-07 | COVERED | Plans define guardrails, repo defaults, runtime surface proof, async proof, policy/preference proof, and final gate.
REQ | RUN-01 | Trigger fanout writes events, notifications, deliveries, attempts into configured prefix | 01, 02, 07 | COVERED | Runtime tests include trigger-to-trace and string-source insert_all placement.
REQ | RUN-02 | Idempotency, lifecycle, traces, explainability read configured prefix | 01, 02, 03, 07 | COVERED | Runtime tests include duplicate idempotency plus trace/admin reads.
REQ | RUN-03 | Workflow, signals, digests, policy/preferences, webhooks, workers, insert_all propagate prefix | 01, 04, 05, 06, 07 | COVERED | Runtime tests include workflow/signal/dispatch-worker/digest/webhook tags.
REQ | RUN-04 | Admin, inbox, trace, recovery surfaces remain tenant/redaction safe | 01, 03, 07 | COVERED | Operator-surface tests include tenant and redaction assertions.
RESEARCH | default-options | Use Chimeway.Repo.default_options/1 as primary hook | 01, 02 | COVERED | D-01 and Ecto merge behavior are tested before implementation.
RESEARCH | oban-exception | Direct Oban.Job queries must not inherit Chimeway table prefix | 01, 04 | COVERED | Async proof includes digest duplicate collapse and Oban job table handling.
RESEARCH | prefixed-suite | Separate non-async prefixed runtime integration proof | 01 | COVERED | PrefixedRuntimeCase owns serialized app-env and schema setup.
RESEARCH | no-packages | No package installs for Phase 75 | 01-05 | COVERED | No install tasks appear in this plan set.
CONTEXT | D-01, D-02, D-03, D-04, D-05 | Repo defaults, single Storage contract, no ordinary API prefix, local option filtering, explicit probes | 01, 02, 03 | COVERED | Guardrails and actions cite each decision.
CONTEXT | D-06, D-07, D-08 | Reject schema_prefix/wrapper/manual primary propagation | 01, 02 | COVERED | Tests and actions forbid those interface shapes.
CONTEXT | D-09, D-10, D-11, D-12, D-13 | Transaction, insert_all, worker args, Oban prefix separation | 01, 02, 04 | COVERED | Runtime tests cover nested flows and Oban separation.
CONTEXT | D-14, D-15, D-16, D-17, D-18, D-19 | Verification strategy and focused alias | 01, 07 | COVERED | Wave 0 tests and final gate implement this strategy.
CONTEXT | D-20, D-21, D-22, D-23, D-24 | Backend-only DX/operator explainability | 01, 03 | COVERED | Tests use ordinary APIs and preserve traces/admin DTO safety.
CONTEXT | D-25, D-26, D-27, D-28, D-29 | Lessons: visible namespace, deterministic migrations, stable keys, typed config, static prefix | 01-07 | COVERED | Plans avoid ambient search_path, request-scoped DB routing, and durable module-name identity.

## Artifacts This Phase Produces

- `Chimeway.PrefixedRuntimeCase` in `test/support/prefixed_runtime_case.ex`
- `Chimeway.RepoPrefixTest` in `test/chimeway/repo_prefix_test.exs`
- `Chimeway.RuntimePrefixIntegrationTest` in `test/chimeway/runtime_prefix_integration_test.exs`
- Runtime test tags: `:runtime_prefix_trigger`, `:runtime_prefix_operator`, `:runtime_prefix_oban_boundary`, `:runtime_prefix_workflow_signal`, `:runtime_prefix_dispatch_worker`, `:runtime_prefix_digest`, `:runtime_prefix_webhook`, `:runtime_prefix_preferences`, `:runtime_prefix_policy_eval`, and `:runtime_prefix_public`
- Test helper functions for prefixed/public row counts, prefix env restore, and generated prefixed migration setup

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create serialized prefixed runtime case</name>
  <files>test/support/prefixed_runtime_case.ex</files>
  <read_first>
    .planning/phases/75-runtime-prefix-propagation/75-CONTEXT.md
    .planning/phases/75-runtime-prefix-propagation/75-RESEARCH.md
    .planning/phases/75-runtime-prefix-propagation/75-PATTERNS.md
    .planning/phases/75-runtime-prefix-propagation/75-VALIDATION.md
    test/support/data_case.ex
    test/chimeway/migration_contract_test.exs
    test/fixtures/installer_golden_prefixed/STDOUT.txt
  </read_first>
  <behavior>
    - Test support switches `Application.get_env(:chimeway, :prefix)` to `"chimeway"` for each prefixed runtime test and restores the original value on exit.
    - Test support uses `Chimeway.Repo` as the repo under test; it must not route runtime proof through `GeneratedRepo`.
    - Test support creates or migrates the configured `chimeway` schema from the Phase 74 generated prefixed fixture root or an equivalent normal migrate path.
    - Test support exposes helpers that can assert row counts in `chimeway.chimeway_*` and `public.chimeway_*` tables without leaking payload contents.
  </behavior>
  <action>Create `Chimeway.PrefixedRuntimeCase` as an `ExUnit.CaseTemplate` for non-async runtime prefix tests per D-14, D-15, D-16, D-17, and D-18. Reuse the `Chimeway.DataCase` sandbox pattern, the migration materialization pattern from `Chimeway.MigrationContractTest`, and the committed `test/fixtures/installer_golden_prefixed/` fixture root. The case must set `prefix: "chimeway"` only around prefixed tests, restore the original application env, clean or recreate Chimeway-owned rows in the prefixed schema between tests, and provide source-level helpers such as `prefixed_count/1`, `public_count/1`, and `assert_prefixed_only/2`. Do not use schema-level prefixes per D-06, process state/search_path per D-09, a wrapper repo per D-07, or hand-written partial table DDL when the generated prefixed fixture path is usable.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/storage_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test/support/prefixed_runtime_case.ex` defines `defmodule Chimeway.PrefixedRuntimeCase`.
    - Source assertion: the support file references `Application.put_env(:chimeway, :prefix, "chimeway")` and restores the previous `:prefix` value in `on_exit`.
    - Source assertion: the support file references `test/fixtures/installer_golden_prefixed` or the committed prefixed fixture migration path.
    - Behavior assertion: running the existing storage test after this task compiles the new support module through the test elixirc path; expected failures, if any, are not missing-module or syntax/setup errors from `Chimeway.PrefixedRuntimeCase`.
  </acceptance_criteria>
  <done>The prefixed runtime case exists, compiles, controls global prefix env safely, uses generated prefixed migration evidence, and exposes placement helpers for later tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add repo default-options guardrails</name>
  <files>test/chimeway/repo_prefix_test.exs</files>
  <read_first>
    test/chimeway/repo_prefix_test.exs
    test/chimeway/storage_test.exs
    lib/chimeway/repo.ex
    lib/chimeway/storage.ex
    deps/ecto/lib/ecto/repo.ex
  </read_first>
  <behavior>
    - `Chimeway.Repo.default_options(:transaction)` is expected to be `[]`.
    - Ordinary operation defaults such as `:all`, `:insert`, `:update`, `:delete`, and `:insert_all` are expected to include `[prefix: "chimeway"]` when app config is `"chimeway"`.
    - Public legacy mode is expected to return unprefixed repo options when app config is `false`.
    - Explicit caller probes are expected to win through `Chimeway.Storage.repo_opts(prefix: "probe")` and through an actual repo operation that raises against a probe schema.
  </behavior>
  <action>Create `Chimeway.RepoPrefixTest` using the env-restore pattern from `Chimeway.StorageTest`. The tests must describe the desired `Chimeway.Repo.default_options/1` behavior before Plan 75-02 implements it, and they must prove that `Chimeway.Storage.repo_opts/1` remains the single mapping contract per D-01, D-02, D-05, and D-18. Include a negative source assertion or behavior assertion that no `@schema_prefix`, `Chimeway.Storage.Repo`, or request-scoped prefix input is introduced per D-06, D-07, D-08, D-21, and D-29.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/repo_prefix_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `test/chimeway/repo_prefix_test.exs` defines `defmodule Chimeway.RepoPrefixTest`.
    - Source assertion: tests mention `Repo.default_options(:transaction)` and at least one non-transaction operation atom.
    - Source assertion: tests mutate and restore `Application.fetch_env(:chimeway, :prefix)`.
    - Behavior assertion: this command is allowed to RED before Plan 75-02; failures must be the expected missing default-options behavior, not compile errors.
  </acceptance_criteria>
  <done>Repo prefix guardrail tests exist and fail only for the production behavior that later plans are expected to implement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add tagged runtime prefix integration proof</name>
  <files>test/chimeway/runtime_prefix_integration_test.exs</files>
  <read_first>
    test/chimeway/runtime_prefix_integration_test.exs
    test/support/prefixed_runtime_case.ex
    test/chimeway/trigger_pipeline_test.exs
    test/chimeway/inbox_integration_test.exs
    test/chimeway/orchestration/workflow_progression_test.exs
    test/chimeway/dispatch/oban_worker_test.exs
    test/chimeway/orchestration/deferred_resume_test.exs
    test/chimeway/digests/emission_test.exs
    test/chimeway/webhooks/process_feedback_worker_test.exs
    test/chimeway/orchestration/recovery_test.exs
    test/chimeway/admin_test.exs
    test/chimeway/traces_test.exs
  </read_first>
  <behavior>
    - `:runtime_prefix_trigger` proves trigger fanout writes event, notification, delivery, attempt, duplicate idempotency, trace, and string-source insert_all rows in the configured schema per RUN-01, RUN-02, D-10, and D-11.
    - `:runtime_prefix_operator` proves inbox list/unread/mark_read/mark_seen, admin reads, trace reads, and recovery paths use configured storage while preserving tenant filters and redaction per RUN-04, D-20, D-23, and D-24.
    - `:runtime_prefix_oban_boundary` proves direct `Oban.Job` reads/deletes use Oban's table-prefix domain while Chimeway-owned rows use configured storage per D-13.
    - `:runtime_prefix_workflow_signal` proves workflow progression, signal routing, SignalRouterWorker signal reloads, and WorkflowProgressionWorker workflow-run reloads per RUN-03, D-12, and D-13.
    - `:runtime_prefix_dispatch_worker` proves `Chimeway.Dispatch.ObanWorker.perform/1` reloads a delivery by durable `delivery_id` from configured storage and `Chimeway.Dispatch.DeferredResumeWorker.perform/1` reloads/resumes a deferred delivery by durable `delivery_id` from configured storage per RUN-03, D-12, D-13, and D-16.
    - `:runtime_prefix_digest` proves digest accumulation, digest emission, digest worker reloads, and digest string-source/bulk operations per RUN-03, D-10, and D-12.
    - `:runtime_prefix_webhook` proves webhook ingress, `ProcessFeedbackWorker`, feedback updates, and durable ingress reloads per RUN-03, D-12, and D-23.
    - `:runtime_prefix_preferences` proves preference and policy settings rows use configured storage per RUN-03.
    - `:runtime_prefix_policy_eval` proves policy evaluation reloads configured storage and preserves suppression explainability per RUN-03.
    - `:runtime_prefix_public` proves legacy public mode remains green per D-14.
  </behavior>
  <action>Create `Chimeway.RuntimePrefixIntegrationTest` on `Chimeway.PrefixedRuntimeCase`. Port compact scenarios from the listed analog tests instead of copying broad suites. The tests must call ordinary public APIs such as `Chimeway.trigger/3`, `Chimeway.list_for_recipient/2`, `Chimeway.mark_read/3`, `Chimeway.mark_seen/3`, `Chimeway.recover_event/2`, and `Chimeway.recover_delivery/2` without passing prefix options per D-03 and D-21. Assert positive placement in `chimeway` and practical absence of accidental public-row reads/writes for Chimeway-owned tables. Keep Oban job args durable-ID based and never store prefix values in job args per D-12. Use stable `notification_key` plus version assertions where notifications are created per D-27.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/chimeway/runtime_prefix_integration_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the integration file defines tags `:runtime_prefix_trigger`, `:runtime_prefix_operator`, `:runtime_prefix_oban_boundary`, `:runtime_prefix_workflow_signal`, `:runtime_prefix_dispatch_worker`, `:runtime_prefix_digest`, `:runtime_prefix_webhook`, `:runtime_prefix_preferences`, `:runtime_prefix_policy_eval`, and `:runtime_prefix_public`.
    - Source assertion: the `:runtime_prefix_dispatch_worker` coverage names `Chimeway.Dispatch.ObanWorker.perform/1`, `Chimeway.Dispatch.DeferredResumeWorker.perform/1`, and durable `delivery_id` reload assertions.
    - Source assertion: the integration file includes assertions for `chimeway_events`, `chimeway_notifications`, `chimeway_deliveries`, and `chimeway_delivery_attempts`.
    - Source assertion: the integration file includes ordinary Chimeway API calls and does not thread prefix options through those calls.
    - Behavior assertion: this command is allowed to RED before Plans 75-02 through 75-06; failures must identify missing runtime prefix behavior, not broken harness setup.
  </acceptance_criteria>
  <done>Runtime integration proof exists with tagged scenarios for each later implementation plan and for final phase acceptance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app config -> Chimeway.Repo | Static storage prefix config becomes repo operation options. |
| runtime tests -> PostgreSQL schemas | Test helpers inspect public and chimeway schemas and must not leak payloads in assertions. |
| public Chimeway API -> storage | Ordinary API calls must not accept request-scoped storage prefixes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Information Disclosure | PrefixedRuntimeCase and runtime integration tests | mitigate | Assert Chimeway-owned rows are read from the configured schema and not accidental public fallback. |
| T-75-02 | Tampering | Prefix test setup | mitigate | Restore application env on exit and reject request-scoped prefix inputs in tests. |
| T-75-03 | Information Disclosure | Operator-surface test assertions | mitigate | Assert redaction/tenant behavior through existing admin, trace, inbox, and recovery analogs without printing payload secrets. |
| T-75-SC | Tampering | Package installs | accept | Phase 75 installs no packages; research package audit reports none added. |
</threat_model>

<verification>
Wave 0 creates RED guardrails. Run `MIX_ENV=test mix test test/chimeway/repo_prefix_test.exs test/chimeway/runtime_prefix_integration_test.exs --warnings-as-errors` and confirm failures are expected missing implementation behavior, not harness compile/setup errors.
</verification>

<success_criteria>
The prefixed runtime harness exists, tagged integration scenarios exist for RUN-01 through RUN-04, and the test failures point directly at production prefix propagation work planned in later files.
</success_criteria>

<output>
Create `.planning/phases/75-runtime-prefix-propagation/75-01-SUMMARY.md` when done.
</output>
