---
phase: 96-adoption-front-door-proof-gate
plan: "02"
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - guides/introduction/adoption-paths.md
  - README.md
  - mix.exs
  - .github/workflows/ci.yml
  - test/chimeway/doc_contract_test.exs
  - test/chimeway/release_gate_contract_test.exs
autonomous: true
requirements: [ADPT-01, ADPT-02, GATE-02, DOCS-01]

must_haves:
  truths:
    - "Per D-01/D-02, adopters see one first-listed static ExDoc Introduction selector with exactly Core, Mailglass, and Accrue outcome-first rows and complete responsibility/proof/boundary fields."
    - "Per D-03/D-04/D-09, README routes concisely to the selector, while the three canonical guides retain detailed setup and exact calm proof-boundary language."
    - "Per D-10/D-11/D-12, exactly one serial PostgreSQL 15 `verify_adoption_paths` job runs the aggregate command outside pull requests, joins `ci-gate`, and never joins `pr-gate`."
    - "Per D-13/D-14/D-15, existing ExUnit doc/release contracts fail on missing or renamed selector/task/job/service/command/aggregation links and keep structural proof distinct from the behavioral lane."
  artifacts:
    - path: "guides/introduction/adoption-paths.md"
      provides: "Canonical three-path adoption selector"
      contains: "mix verify.adoption_paths --only"
    - path: "README.md"
      provides: "Concise prominent route to the canonical selector"
      contains: "guides/introduction/adoption-paths.md"
    - path: "mix.exs"
      provides: "First ExDoc Introduction extra and explicit packaged runner membership"
      contains: "guides/introduction/adoption-paths.md"
    - path: ".github/workflows/ci.yml"
      provides: "Dedicated PostgreSQL 15 aggregate adoption proof job and ci-gate wiring"
      contains: "verify_adoption_paths:"
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "Selector, README, ExDoc, guide, command, evidence, and limitation drift contracts"
      contains: "adoption selector"
    - path: "test/chimeway/release_gate_contract_test.exs"
      provides: "Task/runner/package/CI/service/gate topology drift contracts"
      contains: "verify_adoption_paths"
  key_links:
    - from: "README.md"
      to: "guides/introduction/adoption-paths.md"
      via: "prominent Quick Start or Documentation link without duplicated selector content"
      pattern: "guides/introduction/adoption-paths\.md"
    - from: "guides/introduction/adoption-paths.md"
      to: "guides/introduction/(golden-path|mailglass-integration|accrue-dunning-integration).md"
      via: "one canonical detailed-guide link per exact selector row"
      pattern: "(golden-path|mailglass-integration|accrue-dunning-integration)\.md"
    - from: ".github/workflows/ci.yml"
      to: "lib/mix/tasks/verify.adoption_paths.ex"
      via: "dedicated job invokes `mix verify.adoption_paths` with no selector"
      pattern: "mix verify\.adoption_paths"
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/aggregate-gate.sh"
      via: "aligned `needs`, `VERIFY_ADOPTION_PATHS` env result, and aggregate argument membership"
      pattern: "VERIFY_ADOPTION_PATHS"
  assumptions:
    - id: "EDGE-ADPT-01-BOUNDARY"
      status: "flagged-unverified"
      statement: "No SPEC resolved the boundary probe; assume each row must explicitly delimit Host, Chimeway, and partner responsibility rather than use a combined ownership statement."
    - id: "EDGE-ADPT-01-ADJACENCY"
      status: "flagged-unverified"
      statement: "No SPEC resolved the adjacency probe; assume Core, Mailglass, and Accrue must remain directly comparable in one selector with no fourth supported row between them."
    - id: "EDGE-ADPT-01-EMPTY"
      status: "flagged-unverified"
      statement: "No SPEC resolved the empty probe; assume a missing ownership cell or missing path row is a contract failure, not an acceptable empty state."
    - id: "EDGE-ADPT-01-ORDERING"
      status: "flagged-unverified"
      statement: "No SPEC resolved the ordering probe; assume the stable selector order is Core, Mailglass, then Accrue, matching aggregate dispatch and progressive complexity."
    - id: "EDGE-ADPT-01-PRECISION"
      status: "flagged-unverified"
      statement: "No SPEC resolved the precision probe; assume responsibility language must name the owning party and concrete responsibility for every path."
    - id: "EDGE-ADPT-02-BOUNDARY"
      status: "flagged-unverified"
      statement: "No SPEC resolved the boundary probe; assume every observable outcome must sit beside an explicit does-not-cover statement drawn from its canonical guide."
    - id: "EDGE-ADPT-02-ADJACENCY"
      status: "flagged-unverified"
      statement: "No SPEC resolved the adjacency probe; assume command, record shape, limitation, and detailed-guide link must remain within the same path section so evidence cannot be misattributed."
    - id: "EDGE-ADPT-02-EMPTY"
      status: "flagged-unverified"
      statement: "No SPEC resolved the empty probe; assume every path requires non-empty command, outcome, evidence, limitation, and guide link fields."
    - id: "EDGE-ADPT-02-ORDERING"
      status: "flagged-unverified"
      statement: "No SPEC resolved the ordering probe; assume each path explains choose/ownership before command/evidence, then limitation and next-step guide."
    - id: "EDGE-ADPT-02-PRECISION"
      status: "flagged-unverified"
      statement: "No SPEC resolved the precision probe; assume commands and `CHIMEWAY_*_PROOF` prefixes must be literal and exact, while volatile values remain representative safe tokens."
    - id: "EDGE-GATE-02-UNCLASSIFIED"
      status: "flagged-unverified"
      statement: "No SPEC classified GATE-02; assume job ID, PostgreSQL version/service, event guard, aggregate command, serial topology, ci-gate membership, and pr-gate exclusion are all load-bearing."
  prohibitions:
    - id: "PROHIB-ADPT-01-TRANSPARENCY"
      requirement: "ADPT-01"
      category: "transparency"
      status: "unresolved"
      verification: null
      flagged_unverified: true
      statement: "MUST NOT blur host, Chimeway, and partner responsibility in a way that makes an adopter believe Chimeway owns provider acceptance or host policy."
    - id: "PROHIB-ADPT-02-TRANSPARENCY"
      requirement: "ADPT-02"
      category: "transparency"
      status: "unresolved"
      verification: null
      flagged_unverified: true
      statement: "MUST NOT describe deterministic local proof evidence as external delivery, provider acceptance, workflow completion, or released-package support beyond the verified provenance."
    - id: "PROHIB-GATE-02-TRANSPARENCY"
      requirement: "GATE-02"
      category: "transparency"
      status: "unresolved"
      verification: null
      flagged_unverified: true
      statement: "MUST NOT imply that a structural documentation contract executed behavior, or that a behavioral lane alone protects copyable documentation from drift."
---

<objective>
Publish the canonical three-path adoption selector and bind it to the proven command through one dedicated non-PR PostgreSQL CI lane and fail-closed structural contracts.

Purpose: Let prospective adopters choose and trust the right proof while keeping documentation, package contents, task naming, CI topology, and behavioral evidence aligned over time.
Output: Static selector, concise README route, first ExDoc extra, packaged runner membership, one aggregate CI job, and expanded doc/release contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/96-adoption-front-door-proof-gate/96-CONTEXT.md
@.planning/phases/96-adoption-front-door-proof-gate/96-RESEARCH.md
@.planning/phases/96-adoption-front-door-proof-gate/96-PATTERNS.md
@.planning/phases/96-adoption-front-door-proof-gate/96-VALIDATION.md
@.planning/phases/96-adoption-front-door-proof-gate/96-01-SUMMARY.md

<interfaces>
- Plan 01 provides `mix verify.adoption_paths` and the exact focused forms `--only core`, `--only mailglass`, and `--only accrue`.
- The selector must use the existing fixture prefixes `CHIMEWAY_CORE_PROOF`, `CHIMEWAY_MAILGLASS_PROOF`, and `CHIMEWAY_ACCRUE_PROOF`; examples are representative sanitized shapes, not reconstructed runtime records.
- `.github/workflows/ci.yml` aggregates jobs through three coupled locations: `ci-gate.needs`, an uppercase `${{ needs.<job>.result }}` environment entry, and the `scripts/ci/aggregate-gate.sh` argument list.
- `test/chimeway/release_gate_contract_test.exs` currently declares `@ci_gate_lanes` and `@pr_gate_lanes`; update the former only.
</interfaces>
</context>

## Artifacts this phase produces

- `guides/introduction/adoption-paths.md`, titled as the canonical adoption-path selector and containing exact Core/Mailglass/Accrue sections.
- A README link to `guides/introduction/adoption-paths.md` and a first-position ExDoc extra for the same path.
- Explicit `scripts/prove-adoption-paths.exs` membership in the `mix.exs` package file allowlist.
- The `.github/workflows/ci.yml` job symbol `verify_adoption_paths` and `VERIFY_ADOPTION_PATHS` ci-gate result binding.
- Selector and CI drift contract sections in the existing doc and release-gate ExUnit modules.

<tasks>

<task type="auto">
  <name>Task 1: Publish and contract-check the exact three-path adoption selector</name>
  <reversibility rating="costly">D-01/D-02 establish a canonical URL, first ExDoc position, and stable three-row information architecture consumed by README and contracts; changing it later is coordinated but requires no migration.</reversibility>
  <files>guides/introduction/adoption-paths.md, README.md, mix.exs, test/chimeway/doc_contract_test.exs, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    - `guides/introduction/golden-path.md` — authoritative Core durable-lifecycle/public-trace boundary and next-step destination.
    - `guides/introduction/mailglass-integration.md` — copy the exact Fake local-composition, successful adapter-attempt, and live-provider exclusions from its What happened/Why it matters/Next step section.
    - `guides/introduction/accrue-dunning-integration.md` — copy the exact event-to-signal, `active / signal_received`, released-package, and SHA-qualified compatibility wording.
    - `README.md` — add one concise prominent route in the existing Quick Start/Documentation surface, preserving README's routing role.
    - `mix.exs` — add the selector before every current `extras` item and add only `scripts/prove-adoption-paths.exs` to the explicit package whitelist.
    - `test/chimeway/doc_contract_test.exs` — extend existing README, ExDoc-order, and guide-marker contract styles.
    - `test/chimeway/release_gate_contract_test.exs` — bind package membership and exact task/runner/fixture symbols; retain the existing suite rather than adding a shell checker.
    - `guides/introduction/adoption-paths.md` — target does not exist; create the canonical selector here.
  </read_first>
  <action>Create one static semantic Markdown selector per D-01, ordered Core, Mailglass, Accrue and containing all-and-only those path keys. For each path, include the same ordered fields: intended outcome/“Choose this when”, separate Host/Chimeway/partner responsibilities, exact `mix verify.adoption_paths --only <path>` command, one representative sanitized matching proof-record shape, literal “Does not cover” boundary, and canonical detailed-guide link per D-02. Preserve the exact Core external-delivery, Mailglass Fake/provider, and Accrue event-to-signal/non-terminal/provenance limits from the existing guides per D-09; use calm what happened → why it matters → next step language and required vocabulary per D-04. Keep detailed setup in those guides and add only one concise README link per D-03. Put the selector first in ExDoc `extras` and explicitly package the new runner without broadening the whitelist per D-01/D-13. Extend existing ExUnit contracts to assert exact path set/order, non-empty row fields, ownership anchors, commands, safe prefixes, limitations, guide links, README link, first-extra ordering, runner package membership, and task/runner/fixture symbols. Add mutation-oriented negative cases for missing links, missing fields, unsafe or duplicate example records, wrong command forms, renamed task, and maintainer-suite commands per D-08/D-13/D-14. Keep the ten flagged ADPT edge assumptions visible: tests should resolve them through explicit assertions, not silently treat them as already specified.</action>
  <acceptance_criteria>
    - The selector is the first ExDoc extra and README prominently links it without copying its contents.
    - Exactly Core, Mailglass, and Accrue appear as comparable selector paths, each with all required ownership/proof/boundary/link fields in stable order.
    - Proof claims preserve canonical guide truth, including Mailglass Fake and Accrue non-terminal/provenance wording.
    - Existing ExUnit contracts fail for missing selector links/fields, unsafe or duplicate example records, maintainer commands, renamed task/runner, or missing package membership.
  </acceptance_criteria>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/doc_contract_test.exs test/chimeway/release_gate_contract_test.exs --only adoption_paths_docs_contract --warnings-as-errors</automated>
  </verify>
  <done>Adopters have one concise canonical selector whose exact commands, evidence shapes, ownership splits, limitations, links, package surface, and navigation position are contract checked.</done>
</task>

<task type="auto">
  <name>Task 2: Add the single PostgreSQL adoption lane and couple every gate edge</name>
  <reversibility rating="costly">D-10/D-11 add a release-confidence dependency to shared CI aggregation; undo requires coordinated workflow and contract changes but no persisted-data migration.</reversibility>
  <files>.github/workflows/ci.yml, test/chimeway/release_gate_contract_test.exs</files>
  <read_first>
    - `.github/workflows/ci.yml` — copy the root checkout/setup-beam/cache/deps/compile and PostgreSQL 15 service pattern from `install_golden_contract`; update all three `ci-gate` coupling points and leave `pr-gate` untouched.
    - `scripts/ci/aggregate-gate.sh` — understand the required uppercase result-variable argument contract; do not modify the script.
    - `test/chimeway/release_gate_contract_test.exs` — update `@ci_gate_lanes`, preserve `@pr_gate_lanes`, and reuse `extract_ci_job_block/2`, `extract_ci_gate_needs/1`, and `extract_pr_gate_needs/1` assertions.
    - `lib/mix/tasks/verify.adoption_paths.ex` and `scripts/prove-adoption-paths.exs` — the CI lane must invoke only the aggregate task established by Plan 01.
  </read_first>
  <action>Add exactly one job with literal ID `verify_adoption_paths`, PostgreSQL 15 service/health check and mapped localhost port, established root checkout/setup-beam/cache/dependency/compile steps, literal `timeout-minutes: 30`, and final `mix verify.adoption_paths` execution per D-10. Guard the whole job with `github.event_name != 'pull_request'`; do not add a path filter, matrix, partner checkout, partner suite, registry, Compose layer, source fallback, generated-host/database cache, or parallel path process per D-11/D-12. Add `verify_adoption_paths` to `ci-gate.needs`, add `VERIFY_ADOPTION_PATHS: ${{ needs.verify_adoption_paths.result }}`, and pass `VERIFY_ADOPTION_PATHS` to `scripts/ci/aggregate-gate.sh`; leave all three `pr-gate` locations unchanged. Extend the existing release contract to assert the literal job/task names, PostgreSQL image/service/health/port, exact 30-minute timeout, aggregate no-selector command, serial/no-matrix/no-partner topology, non-PR guard, all three ci-gate coupling points, and pr-gate absence. Include negative drift tests or mutation helpers demonstrating that a renamed job/task, missing service, changed or removed timeout, missing command, missing aggregate member, or accidental PR membership fails per D-13/D-14. Keep these structural assertions under `mix ci.verify_gates`, distinct from the job's clean-room behavioral execution per D-15.</action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` has exactly one `verify_adoption_paths` job using PostgreSQL 15, `timeout-minutes: 30`, and the unscoped aggregate command.
    - The job is non-PR, non-matrix, serial, and contains no partner checkout/suite or alternate orchestration mechanism.
    - `ci-gate` includes the job consistently in needs, environment result, and aggregate arguments; `pr-gate` excludes it.
    - Release contracts fail on missing/renamed job or task, missing PostgreSQL service/command, removed aggregation, or PR-gate inclusion.
  </acceptance_criteria>
  <verify>
    <automated>scripts/test-db env CHIMEWAY_SKIP_PARTNER_TEST_REPOS=1 MIX_ENV=test mix test test/chimeway/release_gate_contract_test.exs --only adoption_paths_ci_contract --warnings-as-errors &amp;&amp; mix ci.verify_gates</automated>
  </verify>
  <done>One bounded PostgreSQL-backed behavioral lane runs the aggregate proof on push/dispatch and is mechanically coupled to ci-gate while remaining outside pr-gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → adopter command | Copyable Markdown determines which local command adopters trust and what behavior they believe it proves. |
| GitHub event → CI topology | Pull request, push, and dispatch events determine whether the expensive behavioral job and aggregate gates execute. |
| CI job result → ci-gate | A job can exist yet be omitted, skipped, renamed, or miswired across needs/env/aggregate arguments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-96-06 | Spoofing | `guides/introduction/adoption-paths.md` | high | mitigate | Existing doc contracts require exact command/prefix/guide/ownership/limitation markers and reject maintainer commands and unsafe/duplicate evidence examples. |
| T-96-07 | Information Disclosure | selector evidence examples | high | mitigate | Use only representative allowlisted `CHIMEWAY_*_PROOF` shapes; contracts reject unsafe diagnostic fields and copied fixture/CI internals. |
| T-96-08 | Tampering | `verify_adoption_paths` workflow job | high | mitigate | Release contracts bind literal job ID, PostgreSQL 15 service, aggregate command, non-PR guard, serial/no-matrix topology, and absence of partner mechanisms. |
| T-96-09 | Repudiation | `ci-gate` aggregation | high | mitigate | Assert aligned job membership in needs, uppercase result env, and aggregate arguments; mutation cases fail if any edge is removed or renamed. |
| T-96-10 | Denial of Service | dedicated serial lane | medium | mitigate | Apply a bounded job timeout, run one lane serially, reuse root caches, and keep the lane off pull requests per D-10/D-11. |
</threat_model>

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | — | Choose canonical path, understand ownership, trust CI-backed truthful docs | 96-01, 96-02 | COVERED | Runner proves behavior; selector and CI/contracts expose and preserve it. |
| REQ | ADPT-01 | Choose Core/Mailglass/Accrue and understand responsibility | 96-02 | COVERED | Task 1, D-01/D-02/D-09. |
| REQ | ADPT-02 | Command, outcome, and boundary for every path | 96-02 | COVERED | Task 1, D-02/D-04/D-09. |
| REQ | GATE-01 | Aggregate clean-room proof without partner suites | 96-01 | COVERED | Tracer plus exact serial expansion, D-05–D-08/D-12. |
| REQ | GATE-02 | Dedicated PostgreSQL proof lane | 96-02 | COVERED | Task 2, D-10–D-12. |
| REQ | DOCS-01 | Contract-check front door, fixture command, and CI | 96-01, 96-02 | COVERED | Existing ExUnit suites extended per D-13–D-15. |
| RESEARCH | — | Strict OptionParser facade and local build-once runner | 96-01 | COVERED | Both Plan 01 tasks. |
| RESEARCH | — | Preserve archive SHA/metadata/containment validation | 96-01 | COVERED | Shared extraction from the existing packaged Accrue CLI. |
| RESEARCH | — | Reuse fixture proof functions and exact evidence parsers | 96-01 | COVERED | Static three-function dispatch; no proof recreation. |
| RESEARCH | — | First ExDoc selector and concise README route | 96-02 | COVERED | Task 1. |
| RESEARCH | — | One non-PR PostgreSQL 15 ci-gate lane | 96-02 | COVERED | Task 2. |
| CONTEXT | D-01 | One static first-listed selector, README link | 96-02 | COVERED | Task 1. |
| CONTEXT | D-02 | Exact three complete outcome-first rows | 96-02 | COVERED | Task 1. |
| CONTEXT | D-03 | Progressive disclosure | 96-02 | COVERED | Task 1. |
| CONTEXT | D-04 | Literal calm proof vocabulary | 96-02 | COVERED | Task 1. |
| CONTEXT | D-05 | Aggregate and bounded focused Mix task | 96-01 | COVERED | Both tasks. |
| CONTEXT | D-06 | One local build-once serial runner | 96-01 | COVERED | Both tasks. |
| CONTEXT | D-07 | Exact evidence plus redacted framing | 96-01 | COVERED | Both tasks. |
| CONTEXT | D-08 | No maintainer commands or unsafe output | 96-01, 96-02 | COVERED | Runner and selector negative contracts. |
| CONTEXT | D-09 | Preserve path truth boundaries | 96-02 | COVERED | Task 1. |
| CONTEXT | D-10 | Exactly one PostgreSQL 15 lane | 96-02 | COVERED | Task 2. |
| CONTEXT | D-11 | ci-gate, never pr-gate | 96-02 | COVERED | Task 2. |
| CONTEXT | D-12 | No suites/checkouts/fallback/registry/Compose/matrix | 96-01, 96-02 | COVERED | Negative source/topology contracts. |
| CONTEXT | D-13 | Extend existing ExUnit truth contracts | 96-01, 96-02 | COVERED | Both plans modify the two existing modules only. |
| CONTEXT | D-14 | All named negative drift cases | 96-01, 96-02 | COVERED | Runner, docs, evidence, and CI mutation contracts. |
| CONTEXT | D-15 | Structural and behavioral proof remain separate | 96-01, 96-02 | COVERED | Tagged contracts plus actual Mix command/lane. |

Deferred ideas and future requirements remain excluded: browser/cards/admin UI, three jobs/matrix or PR lane, general Hex-consumer CLI/JSON/arbitrary selectors, live provider/payment behavior, and Sigra/Threadline/Inbox paths.

<verification>
- `mix format --check-formatted mix.exs test/chimeway/doc_contract_test.exs test/chimeway/release_gate_contract_test.exs`
- `mix ci.verify_gates`
- `mix docs --warnings-as-errors`
- Contract extraction proves `verify_adoption_paths` has PostgreSQL 15, executes `mix verify.adoption_paths`, appears in all three ci-gate coupling points, and is absent from pr-gate.
- The phase is not considered behaviorally complete until the dedicated GitHub Actions `verify_adoption_paths` job succeeds from its service-backed environment.
</verification>

<success_criteria>
- The selector is canonical, first in ExDoc, concise from README, exact to three complete path choices, and truthful to each existing guide.
- All focused commands and representative records align with the Plan 01 task/runner/fixture contract.
- Exactly one non-PR PostgreSQL 15 lane runs the aggregate command serially and gates release confidence through ci-gate.
- `mix ci.verify_gates`, docs generation, and the live behavioral lane are green; negative drift cases demonstrate renamed/missing/unsafe topology cannot pass silently.
</success_criteria>

<output>
Create `.planning/phases/96-adoption-front-door-proof-gate/96-02-SUMMARY.md` when done.
</output>
