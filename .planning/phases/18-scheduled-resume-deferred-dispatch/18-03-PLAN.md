---
phase: 18-scheduled-resume-deferred-dispatch
plan: 03
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - lib/chimeway/traces.ex
  - lib/chimeway/traces/explanation.ex
  - test/chimeway/orchestration/traces_deferral_test.exs
  - test/chimeway/integration/delivery_lifecycle_test.exs
  - test/chimeway/orchestration/deferred_resume_test.exs
autonomous: true
requirements:
  - ORCH-03
must_haves:
  truths:
    - "Trace and explanation surfaces can show both the original deferral facts and the later resume or cancellation outcome for the same delivery row."
    - "Scheduled resume preserves correlation, notification identity, and operator-facing continuity because all evidence remains attached to the existing delivery row."
    - "Resumed, cancelled, or superseded deliveries converge to one durable final explanation path with no ambiguity about why the row did or did not send."
  artifacts:
    - path: "lib/chimeway/traces.ex"
      provides: "Timeline shaping for deferred, resumed, cancelled, and superseded delivery history"
      min_lines: 40
    - path: "lib/chimeway/traces/explanation.ex"
      provides: "Explanation contract fields for durable resume audit facts"
      min_lines: 20
    - path: "test/chimeway/orchestration/traces_deferral_test.exs"
      provides: "Automated proof that resumed and cancelled deferred rows stay explainable"
      min_lines: 50
  key_links:
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/deliveries.ex"
      via: "resume audit metadata is surfaced into delivery explanations"
      pattern: "resume_source|resume_scheduled_at|resumed_at"
    - from: "lib/chimeway/traces.ex"
      to: "lib/chimeway/traces/explanation.ex"
      via: "explanation struct gains durable resume fields without exposing payload data"
      pattern: "timeline|planning_context|last_attempt"
---

<objective>
Finish Phase 18 by extending explainability and end-to-end lifecycle coverage around resumed deferred deliveries.

Purpose: deferred scheduling is not complete until operators can answer why the row was held, when it resumed, and whether it eventually sent, was cancelled, or was superseded, all from the same durable lifecycle spine.
Output: trace/explanation updates for resume audit fields plus end-to-end tests proving identity and final-state continuity after resume.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-scheduled-resume-deferred-dispatch/18-CONTEXT.md
@.planning/phases/18-scheduled-resume-deferred-dispatch/18-RESEARCH.md
@.planning/phases/18-scheduled-resume-deferred-dispatch/18-VALIDATION.md
@.planning/phases/18-scheduled-resume-deferred-dispatch/18-01-PLAN.md
@.planning/phases/18-scheduled-resume-deferred-dispatch/18-02-PLAN.md
@lib/chimeway/traces.ex
@lib/chimeway/traces/explanation.ex
@test/chimeway/orchestration/traces_deferral_test.exs
@test/chimeway/integration/delivery_lifecycle_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing trace and lifecycle continuity tests for resumed deliveries</name>
  <files>test/chimeway/orchestration/traces_deferral_test.exs, test/chimeway/integration/delivery_lifecycle_test.exs, test/chimeway/orchestration/deferred_resume_test.exs</files>
  <read_first>
    - test/chimeway/orchestration/traces_deferral_test.exs
    - test/chimeway/integration/delivery_lifecycle_test.exs
    - test/chimeway/orchestration/deferred_resume_test.exs
    - lib/chimeway/traces.ex
    - lib/chimeway/traces/explanation.ex
  </read_first>
  <behavior>
    - Test 1: `Traces.explain_delivery/2` exposes the original deferral facts plus durable resume fields `resume_source`, `resume_scheduled_at`, and `resumed_at`.
    - Test 2: a resumed delivery shows a `:resumed` timeline event before later attempt or terminal events.
    - Test 3: cancelled or superseded deferred rows remain explainable and do not show duplicate attempt history.
  </behavior>
  <action>Extend `test/chimeway/orchestration/traces_deferral_test.exs`, `test/chimeway/integration/delivery_lifecycle_test.exs`, and `test/chimeway/orchestration/deferred_resume_test.exs` with RED coverage that requires `Traces.explain_delivery/2` to preserve `planning_reason`, `planning_context`, and `next_eligible_at` while also surfacing `resume_source`, `resume_scheduled_at`, and `resumed_at`. The tests must assert that the timeline includes a `:resumed` event for successful promotion and that rows cancelled with `suppression_reason: \"superseded\"` remain visible as one converged delivery history with zero duplicate attempts.</action>
  <acceptance_criteria>
    - `test/chimeway/orchestration/traces_deferral_test.exs` contains `:resumed`
    - `test/chimeway/orchestration/traces_deferral_test.exs` contains `resume_scheduled_at`
    - `test/chimeway/integration/delivery_lifecycle_test.exs` contains `resume_source`
    - `mix test test/chimeway/orchestration/traces_deferral_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/orchestration/deferred_resume_test.exs --trace` exits 0 after implementation
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/orchestration/traces_deferral_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/orchestration/deferred_resume_test.exs --trace</automated>
  </verify>
  <done>The failing tests define the operator-visible explanation contract for resumed, cancelled, and superseded deferred deliveries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend explanation surfaces for resumed and converged deferred rows</name>
  <files>lib/chimeway/traces.ex, lib/chimeway/traces/explanation.ex</files>
  <read_first>
    - lib/chimeway/traces.ex
    - lib/chimeway/traces/explanation.ex
    - lib/chimeway/deliveries.ex
    - .planning/phases/18-scheduled-resume-deferred-dispatch/18-CONTEXT.md
    - .planning/phases/18-scheduled-resume-deferred-dispatch/18-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: explanation structs expose durable resume audit fields without moving identity into Oban payloads.
    - Test 2: timeline generation shows `:deferred`, then `:resumed`, then later attempt or terminal events in timestamp order.
    - Test 3: cancelled/superseded deferred rows keep one explanation path with clear reason strings and no duplicate lifecycle branches.
  </behavior>
  <action>Update `lib/chimeway/traces/explanation.ex` to add explicit fields for `resume_source`, `resume_scheduled_at`, and `resumed_at`. Update `lib/chimeway/traces.ex` so `explain_delivery/2` reads those values from the canonical delivery metadata, preserves `planning_reason`, `planning_context`, and `next_eligible_at` for original deferral evidence, and adds a `:resumed` timeline entry when `resumed_at` is present. Keep trace output sanitized: expose only scheduling source labels, timestamps, and the existing normalized planning facts, and continue to represent supersession through `status: :cancelled` and `suppression_reason: \"superseded\"`.</action>
  <acceptance_criteria>
    - `lib/chimeway/traces/explanation.ex` contains `:resume_source`
    - `lib/chimeway/traces/explanation.ex` contains `:resume_scheduled_at`
    - `lib/chimeway/traces.ex` contains `event: :resumed`
    - `lib/chimeway/traces.ex` contains `"resume_source"`
    - `mix test test/chimeway/orchestration/traces_deferral_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/orchestration/deferred_resume_test.exs --trace` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/chimeway/orchestration/traces_deferral_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/orchestration/deferred_resume_test.exs --trace</automated>
  </verify>
  <done>Operators can inspect a resumed deferred delivery and see one durable story from original hold through resume to final outcome or supersession.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| delivery metadata -> operator surfaces | Resume audit facts become user-visible and must remain sanitized and durable |
| lifecycle tests -> regression protection | The verification suite is the only guard against future regressions that erase resume history or duplicate attempts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | I | `lib/chimeway/traces.ex` | mitigate | Surface only resume timestamps/source labels and existing planning facts; do not copy raw payload or provider response fields into explanations. |
| T-18-08 | R | `lib/chimeway/traces.ex`, `lib/chimeway/traces/explanation.ex` | mitigate | Emit an explicit `:resumed` timeline event from durable row metadata so operators can audit when the row left deferred state. |
| T-18-09 | D | lifecycle convergence tests | mitigate | Assert one delivery history with zero duplicate attempts for cancelled or superseded deferred rows so future resume changes cannot fork operator-visible state. |
</threat_model>

<verification>
Run `mix test test/chimeway/orchestration/traces_deferral_test.exs test/chimeway/integration/delivery_lifecycle_test.exs test/chimeway/orchestration/deferred_resume_test.exs --trace`.
</verification>

<success_criteria>
Trace surfaces preserve deferral and resume evidence on the canonical delivery row, resumed work keeps correlation and delivery identity continuity, and final outcomes for resumed or superseded rows remain durably explainable.
</success_criteria>

<output>
After completion, create `.planning/phases/18-scheduled-resume-deferred-dispatch/18-03-SUMMARY.md`
</output>
