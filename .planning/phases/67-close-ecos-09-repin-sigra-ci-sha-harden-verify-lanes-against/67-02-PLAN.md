---
phase: 67-close-ecos-09-repin-sigra-ci-sha-harden-verify-lanes-against
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/introduction/sigra-auth-integration.md
  - guides/introduction/threadline-integration.md
  - test/chimeway/doc_contract_test.exs
autonomous: true
requirements: [DOCS-10, DOCS-11]
must_haves:
  truths:
    - "D-03: the sigra guide trigger example uses the real Chimeway.trigger/3 shape — notifier MODULE first arg (a *Notifier), params map second, idempotency_key/tenant_id/correlation_id opts third — NOT a string first arg and NOT a params: option"
    - "D-03 WR-2: the copy-pasteable Mod.fun/0 arity-notation lines inside ```elixir fences in BOTH guides are fixed (inline code or a real call), so the fenced snippet does not raise on paste"
    - "D-04: the sigra doc-contract forbids `Chimeway.trigger(\"` (string-first-arg) and `params:`, and positively asserts the trigger example references a *Notifier module"
    - "D-04 WR-3: the doc-contract pins the full seed name DemoHost.Seeds.seed_sigra_auth (not the rename-vulnerable prefix seed_sigra)"
  artifacts:
    - path: "guides/introduction/sigra-auth-integration.md"
      provides: "corrected golden-path trigger example + fence-safe seed line"
      contains: "Sigra.Integrations.Chimeway.MagicLinkNotifier"
    - path: "guides/introduction/threadline-integration.md"
      provides: "fence-safe seed line (WR-2)"
    - path: "test/chimeway/doc_contract_test.exs"
      provides: "forbidden-pattern + *Notifier assertion + full seed-name pin for the sigra describe block"
  key_links:
    - from: "guides/introduction/sigra-auth-integration.md trigger example"
      to: "lib/chimeway/trigger.ex trigger/3 (module(), map(), keyword())"
      via: "notifier-module-first call shape matching seeds.ex:280"
      pattern: "Chimeway.trigger\\(Sigra.Integrations.Chimeway"
    - from: "test/chimeway/doc_contract_test.exs sigra describe"
      to: "the corrected guide"
      via: "forbidden Chimeway.trigger(\" + params: ; required *Notifier"
      pattern: "Chimeway.trigger\\("
---

<objective>
Fix the Sigra golden-path guide so it matches the real `Chimeway.trigger/3` API, and strengthen the doc-contract so the invalid shape would have been caught.

This plan lands D-03 (rewrite the invalid `Chimeway.trigger("sigra.auth.magic_link", recipient, params: …)` example to the verified `seeds.ex:280` shape; fix the WR-2 fenced-arity copy-paste hazard in both guides) and D-04 (add forbidden-pattern + `*Notifier` assertions and the full seed-name pin to the doc-contract).

Purpose: DOCS-10 (golden-path Sigra guide is correct) and DOCS-11 (doc-contract would catch the invalid shape).
Output: Edited sigra + threadline guides and doc_contract_test.exs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-close-ecos-09-repin-sigra-ci-sha-harden-verify-lanes-against/67-CONTEXT.md
@.planning/phases/67-close-ecos-09-repin-sigra-ci-sha-harden-verify-lanes-against/67-RESEARCH.md

<interfaces>
<!-- Verified during planning. Use directly. -->

Chimeway.trigger/3 signature (lib/chimeway/trigger.ex:46-48): trigger(notifier :: module(), params :: map(), opts :: keyword()). validate_module!/1 enforces notifier is a module (trigger.ex:59). There is NO `params:` option — params is the 2nd positional arg.

CORRECT call shape (verified from examples/chimeway_demo_host/lib/demo_host/seeds.ex:280):
  Chimeway.trigger(Sigra.Integrations.Chimeway.MagicLinkNotifier, trigger_params, [
    idempotency_key: idempotency_key,
    tenant_id: @tenant_id,
    correlation_id: correlation_id
  ])
  i.e. notifier MODULE first, identifier-only params map second, opts keyword third.

INVALID example to replace (sigra guide lines 65-70):
  Chimeway.trigger("sigra.auth.magic_link", recipient,
    idempotency_key: "...", tenant_id: user_id, params: %{user_id: user_id, email: email})
  Two defects: (1) string first arg "sigra.auth.magic_link" (must be a notifier module); (2) `params:` option (no such option).

WR-2 fenced-arity hazards (Mod.fun/0 inside ```elixir fences — raises on paste):
  - guides/introduction/sigra-auth-integration.md line 84: `DemoHost.Seeds.seed_sigra_auth/0` inside an elixir fence (fence 83-85).
  - guides/introduction/threadline-integration.md line 87: `DemoHost.Seeds.seed_threadline_notification/0` inside an elixir fence (fence 86-88).
  Fix: write the real call `DemoHost.Seeds.seed_sigra_auth()` / `seed_threadline_notification()`, or move to inline `code` / a non-elixir fence. (Arity refs inside PROSE backticks elsewhere are fine — only the ones inside elixir fences are the bug.)

doc_contract_test.exs sigra describe @752-823 (substring/order only today):
  - @sigra_forbidden ~w(:raw_token :magic_link_url) @771
  - @required list @780-799 includes Chimeway.trigger, Sigra.Integrations.Chimeway, idempotency_key, tenant_id, and the PREFIX-only `DemoHost.Seeds.seed_sigra` @787 (the WR-3 defect — actual fn is seed_sigra_auth/0).
  - @recipe_forbidden_strings (shared, @104) = stop_conditions / Workflows.Workers / Chimeway.Trigger.trigger — do NOT collide new sigra-specific forbids with this shared list; add a sigra-scoped attribute instead.
  - Existing precedent regex style (mailglass guide describe @471-476): Regex.match?(~r/Chimeway\.Webhooks\.process\(\s*(?:adapter_module|Chimeway\.Adapters\.Mailglass)/, content) — mirror this style for the trigger-first-arg-is-a-module check.

Guide `params:` occurrences today = 1 (only the invalid example). After D-03 removes it, a blanket `params:` forbid is safe (verify 0 after edit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the Sigra guide trigger example and the WR-2 fenced arity in both guides (D-03)</name>
  <files>guides/introduction/sigra-auth-integration.md, guides/introduction/threadline-integration.md</files>
  <read_first>
    - guides/introduction/sigra-auth-integration.md (invalid example lines 65-70; fenced seed line 84)
    - guides/introduction/threadline-integration.md (fenced seed line 87)
    - examples/chimeway_demo_host/lib/demo_host/seeds.ex around line 280 (the correct shape)
    - lib/chimeway/trigger.ex lines 44-60 (signature, validate_module!, @sensitive_keys — do NOT touch @sensitive_keys)
    - 67-RESEARCH.md Coordinate 5 + Coordinate 6
  </read_first>
  <action>
    In guides/introduction/sigra-auth-integration.md, replace the invalid trigger example at lines 65-70. The new example must call `Chimeway.trigger(Sigra.Integrations.Chimeway.MagicLinkNotifier, params_map, opts)` where: first arg is the notifier MODULE `Sigra.Integrations.Chimeway.MagicLinkNotifier` (not the string "sigra.auth.magic_link"); second arg is an identifier-only params map (e.g. `%{user_id: user_id, email: email}`); third arg is an opts keyword list with `idempotency_key:`, `tenant_id:`, and `correlation_id:` (matching seeds.ex:280 and the Phase 65 blueprint). Remove the `params:` option entirely — params is positional. Keep the surrounding "Do not pass raw_token/magic_link_url/confirmation_code" warning block intact.

    Fix WR-2 in BOTH guides — the bare `Mod.fun/0` arity notation inside ```elixir fences raises on copy-paste:
    - sigra guide line 84 (fence 83-85): change `DemoHost.Seeds.seed_sigra_auth/0` to the real call `DemoHost.Seeds.seed_sigra_auth()`.
    - threadline guide line 87 (fence 86-88): change `DemoHost.Seeds.seed_threadline_notification/0` to `DemoHost.Seeds.seed_threadline_notification()`.
    (Arity references inside ordinary prose backticks elsewhere in the guides are valid documentation — leave them.)

    Do NOT modify lib/chimeway/trigger.ex or @sensitive_keys (out of scope).
  </action>
  <verify>
    <automated>grep -q "Chimeway.trigger(Sigra.Integrations.Chimeway" guides/introduction/sigra-auth-integration.md && ! grep -q 'Chimeway.trigger("' guides/introduction/sigra-auth-integration.md && ! grep -q "params:" guides/introduction/sigra-auth-integration.md && ! grep -q "seed_sigra_auth/0" guides/introduction/sigra-auth-integration.md && ! grep -q "seed_threadline_notification/0" guides/introduction/threadline-integration.md</automated>
  </verify>
  <acceptance_criteria>
    - sigra guide contains `Chimeway.trigger(Sigra.Integrations.Chimeway` (notifier-module-first) and references a `*Notifier` module name.
    - sigra guide contains zero occurrences of the substring `Chimeway.trigger("` and zero occurrences of `params:`.
    - sigra guide no longer contains `seed_sigra_auth/0`; threadline guide no longer contains `seed_threadline_notification/0` (the arity forms inside elixir fences are replaced with real `()` calls).
    - The trigger example still passes identifier-only params and retains the no-secrets warning.
  </acceptance_criteria>
  <done>The sigra guide shows the valid notifier-module-first trigger/3 shape with no `params:` option, and both guides' fenced seed snippets are copy-paste safe.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen the sigra doc-contract to catch the invalid shape (D-04 + WR-3)</name>
  <files>test/chimeway/doc_contract_test.exs</files>
  <read_first>
    - test/chimeway/doc_contract_test.exs sigra describe block @752-823 (@sigra_forbidden @771, @required @787, the shared @recipe_forbidden_strings @104, and the mailglass regex precedent @471-476)
    - the corrected guides/introduction/sigra-auth-integration.md (from Task 1) to confirm the new positive/negative patterns match
    - 67-RESEARCH.md Coordinate 7 + Pitfall 4
  </read_first>
  <action>
    Extend the sigra describe block in test/chimeway/doc_contract_test.exs (DOCS-10/DOCS-11). Do NOT add these to the shared @recipe_forbidden_strings (it is reused by other describes and would false-flag them). Add a sigra-scoped attribute, e.g. `@sigra_invalid_patterns`, containing the unambiguous forbidden patterns:
    - `Chimeway.trigger("` (string-first-arg — unambiguous; safe substring forbid).
    - `params:` (no such trigger option). Per Pitfall 4, scope safely: confirm the corrected guide contains zero `params:` occurrences (Task 1 removed the only one) so the blanket forbid is sound; if any legitimate `params:` remains in prose, anchor the pattern to the trigger call instead.
    Add a `for forbidden <- @sigra_invalid_patterns` test loop asserting `refute String.contains?(content, forbidden)`.

    Add a positive assertion that the trigger example references a `*Notifier` module — mirror the mailglass regex precedent (@471-476). Use a regex such as `~r/Chimeway\.trigger\(\s*Sigra\.Integrations\.Chimeway\.\w*Notifier/` (or at minimum assert the guide references a `Notifier` module within the trigger context), and `assert Regex.match?(...)`.

    Fix WR-3: in the @required list, change the prefix-only `DemoHost.Seeds.seed_sigra` (@787) to the full name `DemoHost.Seeds.seed_sigra_auth` so a rename to a different seed_sigra_* would fail the contract. (Note the new guide uses `seed_sigra_auth()` — the substring `DemoHost.Seeds.seed_sigra_auth` is still present.)
  </action>
  <verify>
    <automated>mix test test/chimeway/doc_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - doc_contract_test.exs sigra describe forbids both `Chimeway.trigger("` and `params:` via a sigra-scoped attribute (NOT appended to @recipe_forbidden_strings).
    - doc_contract_test.exs sigra describe contains a `Regex.match?` positive assertion that the trigger example references a `*Notifier` module.
    - The @required list pins the full name `DemoHost.Seeds.seed_sigra_auth` (the bare prefix `DemoHost.Seeds.seed_sigra` is replaced).
    - `mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` exits 0 against the corrected guide from Task 1.
  </acceptance_criteria>
  <done>The sigra doc-contract forbids the string-first-arg and params: shapes, positively requires a *Notifier reference, and pins the full seed name — so the D-03 defect would now fail CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> adopter (copy-paste) | adopters copy guide snippets into their own code; an invalid example is a correctness hazard, not a security one |
| guide content -> doc-contract test | the doc-contract is the truth-lock that prevents guide drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-05 | Tampering / correctness-drift | sigra golden-path guide trigger example | mitigate | D-03 corrects the example to the verified seeds.ex:280 shape; D-04 adds forbidden-pattern + *Notifier assertions so the invalid shape cannot silently return. The doc-contract is the existing truth-lock mechanism (do not hand-roll review). |
| T-67-06 | Spoofing / false-green | doc-contract substring-only check passing the invalid example | mitigate | D-04 replaces the blunt substring check for trigger with a forbidden-pattern (Chimeway.trigger(") + regex *Notifier assertion; WR-3 swaps the rename-vulnerable prefix pin for the full seed name. |
| T-67-07 | Information disclosure | guide encouraging passing secrets to trigger | accept | The guide's no-secrets warning + the @sigra_forbidden :raw_token/:magic_link_url doc-contract forbids are verified-good and retained; not modified here. The corrected example still passes identifier-only params. |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan; docs + a test file only. |
</threat_model>

<verification>
- `mix test test/chimeway/doc_contract_test.exs --warnings-as-errors` exits 0.
- `grep 'Chimeway.trigger("' guides/introduction/sigra-auth-integration.md` matches nothing.
- `grep 'Chimeway.trigger(Sigra.Integrations.Chimeway' guides/introduction/sigra-auth-integration.md` matches.
</verification>

<success_criteria>
- Sigra guide uses the valid notifier-module-first trigger/3 shape; no string-first-arg, no params: option.
- Both guides' fenced seed snippets are copy-paste safe (real () calls, not /0 arity).
- Doc-contract forbids the invalid shape, requires a *Notifier, and pins the full seed name.
</success_criteria>

<output>
Create `.planning/phases/67-close-ecos-09-repin-sigra-ci-sha-harden-verify-lanes-against/67-02-SUMMARY.md` when done
</output>
